# 17.4 ECS Infrastructure

Hello and welcome to Cloud Folks Hub.My name is Bhavesh Katara.In the previous video I explain you about ECS cluster.In this video I am going to explain you about cluster infrastructure.Let me explain you that from where we are getting this particular option right now, I am inside my,uh, ECS console and I'm going to click on Get Started.If you want to set up ECS, the first step is to create cluster.Now I'm going to click on Create Cluster.And here I have to give cluster name.And then I have to option to choose one AWS Fargate and Amazon EC2 instance.Now which option will be best suitable for my environment?Which option would be the better choice?You will find out all these details in this video.So let's start.As you know, that is a part of cluster infrastructure.We have two options one Amazon EC2.We also call it self-managed infrastructure and AWS Fargate.Here I have 12 points comparison between them.So let's start with EC2.Here are infrastructure provisioning.You launch and manage EC2 instances.Let me explain you from zero.If I want to set up high availability, I have to set up multiple Docker hosts into the same cluster.Right now this Docker host are actually EC2 instances.If you choose this particular option Amazon EC2 instance.So it is simple like We are going to create EC2 instance with Docker service installed, and then weare going to run container inside this Docker host.We are going to create at least two docker host so they can work like a team.They can be added into the single cluster environment right.So here when I'm going to choose Amazon EC2 instances, simply it is EC2 instance with Docker installedand part of the cluster of ECS.If I'm going to choose Fargate AWS provisioned infrastructure automatically, it is a kind of serverlessservice.We are not going to setup any Docker host.AWS will provide us infrastructure whenever we want to add some Docker container inside this.So it is actually not going to provision any server or docker host the service or Infrastructure providedon the on demand, right?When we are going to talk about EC2, we have full control level means we have full control over OSinstance type and storage and Ami.Look at this.Here I am going to choose Amazon EC2 instance.I can choose provision model.I want to set up On-Demand instance or instance to save money.I can choose Ami for my EC2 instance.I can choose instance type like T2, T2, micro, or C1 whatever the instance size you want to defineokay.You can also set up your EBS volume size for your EC2 instance as well.So here we have full flexibility when we are going to use EC2 as a part of the cluster docker host.But if I were to choose Fargate.We have no access to the underlying OS or compute layer right now.You can consider it as a plus point and minus point as well.If I'm going to talk about control level over here.We have full control over OS instance type storage and Ami.But we have to manage all of this right.So we have control but we have to manage them.If I'm going to talk about over here we have no control and we have one.Another point is as well because it is a serverless.We don't need to manage them.Means it is simple to set up right now.Launch time.If you set up a cluster with EC2, it is going to take time because literally it is going to createone EC2 instances.Sorry, multiple EC2 instances.It is going to set up Docker host.Then it is going to connect these EC2 Two instances with your X, so it will take some time here inthe Fargate.Because it is a serverless, it is going to provide infrastructure on demand.Creating this EKS cluster with Fargate option is actually quick.Then billing model.You pay for the EC2 uptime supports on demand sports and saving plans here.If I want to create EC2, I can choose my instance type and as you know that you have to pay for thetype of instance you choose and all the configuration is based on the instance type, right?So you are paying for the instances that you have created here, pay per vcpu and memory usage per secondfor each running task.So you know, once you create ECS cluster and once you will create container and whenever the containerwill utilize this resources, you are paying that actual resources in case of the fire.Gate.Right.So it is not going to create infrastructure in advance.Whenever you will create any task or containers inside this Fargate environment, it will provide resourcesto that particular container.And you are paying for the resources consumption by that particular container, right?VPC selection timing.Let's say that if I am going to choose this Amazon EC2 instance, I am actually creating EC2.Literally it is going to create EC2.So you have to select all the options that you are choosing while you are creating your EC2.For example, here I have to choose my VPC.I have to choose my subnet.I have to choose my security group.Whether you need to assign automatic public IP or not.So you have to choose all the options that you are actually choosing normally when you are creatingEC2 instances, right?But in the Fargate you do not need to choose anything.Look at this here.I am going to select Fargate, right?It is not asking me anything.So you may have one question as well.How my containers are running without VPC.How I can access services running inside the container without VPC.What about the IP and all this right.Here we have VPC selection required as well.But VPC selection is actually available while you are creating task or services right.So once we have our our cluster ready and then we are going to create container inside this particularcontainer a particular cluster we need to choose VPC right.So VPC selection is there.But when we will create task or container inside that particular cluster we need to select this in theFargate.Okay, now auto scaling configuration is auto scaling group is created during cluster creation.But you manage instance and scaling policy.Now look at this here.If I want to choose Amazon EC2 instance it is also going to create Auto scaling group.We can choose desired capacity.We can choose minimum and maximum instance as well.We can choose size for instance as well.So we have full control on scalability.And we like AWS will automatically create auto scaling group for us as well.But we can manage auto scaling policy in case of Fargate.ECS handles the scaling for Fargate task automatically, so AWS will manage scalability.We don't need to worry about this, right?So this is kind of plus point we have because AWS is managing scaling over here.Scaling flexibility based on the EC2 limits.Scale EC2 first and then task.You know if you have container running inside your cluster and you have EC2 as an infrastructure.Now if you want to scale your container, you have to scale your EC2 first.If my EC2 is a T2 micro and if I have only one CPU, how my containers can use more than one CPU, right?So in this case, if I want to scale my containers running inside my cluster, I have to scale my EC2instances first and then I can scale my container.In case of Fargate, we have this flexibility over here.Directly scale the task right?Because whenever your container will consume any resources, AWS Fargate will provide required infrastructureto them so we can scale our container or task.What is container?What is task?What is service that I will explain you in upcoming days, right?Maintenance responsibility.You manage patching, scaling and instance like we have our normal EC2 instance which is having OS andall this.So we have to maintain this here AWS manage all the underlying infrastructure because we don't haveany control on that.Fargate infrastructure task means ECS plays the task on EC2 instances based on the available resources.Let's say that if I have three Docker hosts as a EC2, which container is going to run inside whichparticular Docker host, ECS will manage this?In case of Fargate ECS directly on the task in Fargate compute then container isolation.Multiple containers are same EC2 instances.Resources.Let's say that here in this particular Docker host, there are two containers running C1 and C2, whichis normal, uh, in the in the Docker environment.But here we have one container isolation facility as well.Each task runs in isolated environment with its own elastic network interface, so whenever you willlaunch any task or a container, AWS Fargate will provide isolated Fargate environment for this, whichis good for the application to run properly.Use case fit best for as needed OS level access or GPU support.If you have application which required OS level access, or if you want GPU support, it would be agood idea to go with EC2.For any normal microservices based application, you can go with the with Fargate.So best for serverless microservices, batch jobs and quick deployment.Suitable for long running apps OS customization required.You want to select GPU and SSD needs and Fargate is suitable for microservices fast scaling API andshort lived workload.So here we have 12 differences.And this will help you to choose one of the best option for your ECS cluster environment.Definitely we are going with the practical as well.We are going to setup EC2 cluster and Fargate cluster together, and we can compare them directly withthe practical as well.But before I'm going to perform this lab, I want you to understand ECS anywhere as well.Because if you look at over here we have this particular line.External instance using ECS anywhere.Now what is this ECS anywhere?I'm going to explain you in the next video.So we are going to understand all this options first.And then we are going to perform labs for both of these infrastructure options.Thank you very much.See you in the next video if you have any question I'm always available for you.Only one message away.Goodbye.