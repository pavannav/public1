# 17.30 ECS Project Step 5 Task Role Conditional Vs Task Execution Role Part 6

Hello and welcome to Cloud Fox.My name is Bhavesh Shitara.You are learning X step by step through the project.Right now we are on step number five.And here we are actually understanding task definition.In the task definition we have so many options and we covered many of them as well.But in this video we are going to talk about task roles.If you go to the task definition over here we are getting this option task execution role.And in this we have two options as well task role and task execution role.Why they are actually providing two options over here.What is the use case and what are the differences between these two options.We are going to discuss about all of this in this video.So let's start.As always we are going to start with the introduction.ECS task role and task execution role are special IAM roles attached to the task definition that definethe permission required for running an ECS task.Now both options are actually used to attach IAM role with the task, and this role is actually givingpermission so your task can access other AWS services, right?But the question is why they provided two options over here.Now to understand this we have to compare both of them right.So I have this comparison.And the first is task role and task execution role.I have ten points.So let's start with the point number one when you will use task role.Who used it.See the first difference is who used it.Application container running inside the ECS task task role will be used by application container runninginside your ECS task.And if I'm going to talk about task execution role ECS agent will use this.Let's understand this using whiteboard.See we have our task right.And inside task containers are actually we are running container inside this.And then we are running app.So app code.Now for example, if my application wants to access S3 bucket or any other AWS service, I'm just givingyou S3 as example and and want to store data into S3.In this case, your application must have permission to access S3 bucket, right?Without permission, your app cannot run S3 bucket.Now how you can give permission to your S3 bucket.You can give permission by attaching task role.I want to give you a practical example of this.So let's go back to the flashback.You know we are going to host one application inside ECS environment, right?Do you remember this application?Let me open this application.See right now I have this application.This application is currently hosted in ECS.Sorry EC2 right.For a temporary demo I just hosted this and as you know that we are going to use this particular applicationwhich is actually going to upload file into the S3 bucket.For example, right now I am going to upload this app ABC dot png file.right upload now file has been uploaded, you can go to S3.Here I have this bucket my X app bucket.And here you will get this ABC png file.So right now we have to this role because our application is going to store data into S3.Our application itself is using another AWS service.So we have to use this task role right.And we will do this when we are going to create final task definition for our task.But you understand the use case right.This is a specific use case for task role.Now what is the use case of task execution role.It is used by ECS agent.Now let me explain you this.When you create your cluster in the cluster you.You have two types of.In two types of infrastructure one Fargate and another one is EC2 instance.So EC2 instance will be container host.Now to manage this container host by ECS, EKS is actually installing ECS agent inside your EC2 instanceor container host.Now when you are going to create task definition and when you attach any role with the task executionrole, you are giving permission to your agent, right?ECS agent which is running inside your container host.So both are having different use case.If I'm going to talk about task role, it is a permission.We are assigning permission to application running inside the container.And here we are giving permission to ECS agent.Right now Purpose gives application code permission to access AWS service at runtime.I give you live demo.Okay, my application cannot store data directly into the S3 bucket without attaching permission withthe task role.Now let's understand the task execution role as well.Why we need to use this.So the purpose is gives ECS agent permission to pull image push logs fetch secret during the task startup.See in upcoming video, we are going to setup container and as you know that our container image isstored in ECR.So we have to provide ECR URI.And then it is going to you know, face the image from the ECR.So have your EC2 instance or a container host is going to fetch image.Again you are using another service, you are fetching your container image from another service.So in this case we need to use this here.We need to provide permission to agent.So we have to use.Task execution role to give permission okay.Now it is to ECS task definition.Same ECS task definition.There is no difference in this particular point when used during the runtime.While the containerized app runs and makes AWS API call.Let's say that right now I am accessing my application and now when I will click on upload, it is goingto the S3 and it is requesting S3 to store this file.So at that time it is going to use this task role.If I am going to talk about when we will use task execution role during the task launch.You know, after creating task definition, we are going to launch this task at that time your.Task definition needs to pull the image at that time it is going to use this task.Execution role permission.Common permission.It is almost related to the application access.So S3 get object.Put object DynamoDB.Read and write data from the DynamoDB SQS.Send messages, secret manager, get password and all this.If I'm going to talk about common permission for the execution role, it is ECR get authorization token.Get downloaded image CloudWatch logs.Again, secret is also possible over here.Almost all the permission related to run your task.Write credential type temporary credentials injected inside the container.Temporary credential used by the ECS agent outside the container.Example use case app needs to read and write the file in your S3 bucket.Example use case.X needs to pull the email from ECR and push the container logs to CloudWatch when it is not needed.If the app doesn't access any AWS service, let's say that right now we have this particular application.As a part of this application process, we are actually storing data into S3, but I have very simpleapplication.I just want like I have static web page.I just want to display this web page.I'm not using any AWS services.Then no need to worry about this right?You can create task definition without doing this.Like say none.It works same way.Task execution role is also not needed like it is conditional if using public Docker image and no CloudWatchlogs or secret.Let's say that instead of ECR image, ECR image is always stored in the private Repository.So authentic authentication and all these things are needed.But if I have my image stored in in Docker Hub public repo, anybody can download image or pull theimage and create a container from this image.So in this case you don't need task execution role.So you can go to this and you can choose none as well.It is depend.But for our project we have to because our image is already stored in ECR security scope scope to theapplication inside this task.Scope to the ECS infrastructure task image pooling and logging.Right.So this is like these are the differences between these two.And I hope you got complete understanding about these two.If you are going to get question related to these two options, you will you are able to answer thisfor sure in exam or in interview.Thank you very much.See you in the next video.Our journey will continue from the next video.Goodbye.