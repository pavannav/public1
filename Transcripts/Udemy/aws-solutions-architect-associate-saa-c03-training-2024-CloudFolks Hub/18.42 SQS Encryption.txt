# 18.42 SQS Encryption

Hello and welcome to CloudFoxHub. My name is Bhave Shatara. You are learning AmazonSQS. Currently, we are talking about all the options related to SQS queue. We already discussedabout queue types. We also understood queue configuration options. Now, in this video,we are going to talk about queue encryption. Let's start.Introduction SQS encryption protects messages while storedin the queue so that the data is unreadable if someone accesses the storage directly.This is called encryption at rest. Server-side encryption SSE in SQS. You can enable or disableserver-side encryption for SQS queue. Default is enable. Look at this. Here I am creatingnew queue and here I am getting this option server-side encryption. If I want to disableit, I can. But by default, it is enable and it is always good idea to enable this forthe security. Now when you are going to enable this, you have two options. Server-side encryptionwith SQS and server-side encryption with KMS. Now let's understand the differences betweenthese two options. SSE-SQS. AWS manages the encryption key. When you are going to encryptany data, you must have a key. Now here AWS is going to manage the entire encryption process.This process is completely transparent to the user. So we don't need to do anything.No key management required. Simple and recommended for most use cases. Means your queue is actuallysecure and you are not going to manage anything related to the encryption. So it is a verysimple process. Now we have another option. As you know that we have one service. Keymanagement service. We call it KMS. So here we are using AWS KMS key. You have to setup key into the KMS. KMS is a separate service. We will discuss about it in upcoming videos.So we have to create KMS key and then we have to use this KMS key along with this SQS queue.Now if you have your own key, you can control key permission and rotation. You can alsoaudit this key as well. So this is really very secure option we have. Because here youhave total control on your key. It is used for compliance and strict security requirement.So this is very simple option we have. Now keep this in mind. Encryption in transit.When data is flowing between producer to SQS queue to consumer, it is always protectedusing HTTPS TLS. No configuration is needed. Here we are talking about encryption at rest.What is the meaning of this? Look. As you know that we have this message retention period.The message can be there in the queue between 1 minute to 14 days. Means we are storingthese messages into the queue. Now when the message is stored in the queue, we call itencryption at rest. And you can encrypt these messages using this encryption option. Right.So that's all about encryption. Very simple process. Now in the next video, we are goingto talk about another interesting point related to the SQS queue, which is known as accesspolicy. Thank you very much. See you in the next video. Have a nice day. Goodbye.Transcribed by https://otter.ai