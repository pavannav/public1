# 18.78 EventBridge Target

Hello and welcome to CloudFox.My name is Bhavesh Chatara.In this video, we are going to talk about the last component of the EventBridge workflow.We call it Target.Let's understand what is Target and how the entire workflow works.First of all, I am going to start with introduction, where the actual action happens.Target is a service that gets triggered after the rule match is an event.Rule decides, target executes.Now I want you to understand this entire workflow with practical because once we will completemeans after this video we are going to perform our first lab from the next video and we will use same steps.EC2 scenario, when EC2 instance stop automatically send email using EventBridge.Now the first step will be event generated by EC2.When instance stop, EC2 automatically generate event.This is actually sample event in a simplified format.You do not create this, AWS generates it automatically and AWS services generates itfor any event, right?So it can be stop, start, terminate any event.Now event send to EventBus.This event goes to default EventBus, automatically no configuration needed, EC2 will send thisto EventBridge using default EventBus.Now we need to configure rule, we have to configure this rule that when we are goingto get stop instance event from EC2, right?So we need to configure this.Event comes rule check, isSource AWS.EC2, yes, isState equal to stop, if yes, rule trigger.If no, rule will be, there is no rule, no trigger, right?So rule will not trigger there.Okay, so we need to create this rule and we will do this into the next video.Now rule send event to the target SNS, new rule forward the event to SNS, pull eventor transform event is sent.What SNS will receive?SNS will receive this detail.Now SNS will send this mail because SNS is actually going to trigger email.SNS topic will send email to subscriber, SNS converts the message and send email to user.Example of email, subject EC2 alert, now here it is a information message and SNS will convertthis message, right?Okay, so this are like entire steps of EventBridge workflow.Now common target, it can be AWS Lambda, SNS, Notification, SQS, Step Functions, Kinesis and ECS task.Now most important target permission, EventBridge must have permission to trigger target exampleinvoke Lambda function or publish to SNS and this permission can be controlled by IAM role.Now don't worry about this permission, why?Because from the next video we will perform lab and we will perform same lab.So how to give permission that I will tell you during this particular lab, we are notgoing to set up only one lab, we will set up 2 to 3 labs according to the real worlduse case for the EventBridge.So don't worry about it.Once you will complete the lab, your entire concept will be crystal clear.So please watch next video right now. Thank you. Goodbye.