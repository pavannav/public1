# 17.34 ECS Project Step 5 Environment Variable Part 9

Hello and welcome to Cloud Fox.My name is Bhavesh Katara.You are learning each elastic container service.Right now we are actually talking about all the options that we are getting while we are creating taskdefinition.In this video I'm going to talk about environment variables of task definition.So let's start.Before I'm going to explain you about environment variables in task definition, I want to run my demoapplication from the EC2 instance.So I have this EC2 instance right now with Docker installed.I have my Docker image as well.We already uploaded this image into the ECR.So now using this particular image I am going to run our application locally on this particular EC2instance.This is excess of my EC2 instance.Now if I want to run my application locally, I already pulled image okay.So if I'm going to give command docker images I have this image locally.So I have to create container from this image.Now here we have this command to create container from this particular image.If you look at this command carefully you find out that in this particular command I provided my bucketname.So you know we are going to store images into this particular bucket.I also provided region of the bucket and this is access key and secret key.We will talk about access key and secret key later.But right now I'm going to talk about these two options.Now here you may have one question.Why you are specifying all these options means S3 bucket, uh, name and AWS region over here whileyou are creating container.Now here we have two options.We can specify this bucket name and AWS region name into our PHP code.Our application is PHP.So we can specify into the PHP code or we can specify over here.So out of these two options which option is better.So I'm going to open this code okay.My application code.Let me open this.See this is my application code.Instead of creating this kind of uh variables here I'm creating two two variables S3 bucket and AWSregion Instead of specifying or declaring variable, I can specify a bucket name here.Okay.Bucket name here and bucket region here.But if I am going to do this in my code, I will be in trouble.Because whenever I am going to change my bucket or region, I have to change code.Now, whenever I am going to change the code, I have to recreate the image.So every time you are going to change your bucket name, you are going to change your region from thePHP code.Now you are creating image.So this will be long process.Instead of this.Here I define variable.Now I'm just passing variable values over here.While running this particular container.So in this case if I'm going to change bucket name or region.I just need to specify over here when I am going to run this container.So I do not need to change code every time.So this is actually good option which is giving us flexibility.Now the question is you are running this particular application from the local EC2 instance by creatingcontainer.But I want to run this application from the ECS task.Now when I'm creating this particular task, how I can pass these two values.And the answer is environment variables.So when you create your task definition over here and you know that you have to pass some values, inour case we have to pass these two values S3 bucket and AWS region.So.While you are creating your task definition, you can click on Add Environment Variables add key.For example here I'm going to set that S3 bucket.Now here you have to provide value.So it can be any value of your bucket name.Same way add variable.Again we need to define same variable as I have here it is AWS region and whatever the region that youhave.So same way right now I am creating this particular container locally and passing this particular environmentvariable values here.Right.Same way, if you want to pass this values for the ECS task you have to define environment variable,right.This is how the things are actually working.Now I already created this container, so let's verify how the task is actually working so you willget better understanding here.This is IP address of my EC2 instance.My application is actually running on port number 8080.Look at this.I'm getting this particular, uh, application web page.And now if I'm going to choose any file and if I'm going to upload this particular file, okay, imageuploaded successfully, it is going to store this into the My App bucket.Let me refresh.And here we got this file.Now if you will change this bucket name no need to worry about this.You just need to change this bucket name over here.And it is going to work without modifying code.And this is This or the advantage of using variables.So you I hope you got complete understanding about this.Now I'm going to the mind map to explain you some overall detail about environment variables.What are the environment variables?Environment variables are small key value pair that you pass to your container at runtime.Key value.Key S3 bucket value.My x bucket.Now this value can be changed according to your situation.Your bucket name.They work like a configuration value.Your app needs without changing the code.Now it is actually optional if you don't want to pass value.And if you want to hardcode this value into the code, it is okay.During your task definition, do not specify any value right?So it is a completely optional.Now why they are used to avoid hard coding values inside the code to use different values for dev test,and without changing the app, and to keep secret and configuration outside of the code.So these are the use cases.Now when you are going to provide environment variable detail over here you have two options.You can provide this detail by adding key value pairs over here.Or if you have so many environment you can create file then you need to store this file into the S3.And then you can specify the detail about location of the file.Right.So both options are available.Now when we will perform our lab when we will create our task definition.After understanding all of these options at that time we will use environment variables.Right now I am just going to remove this because I am just giving you overview about all these options.I hope you got complete understanding about environment variables.Still, if you have any question, I am always available.Thank you.Goodbye.