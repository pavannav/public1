# 18.12 Part-1 Lab Prerequisites

Hello and welcome to Cloud Fox Hub.My name is Bhavesh Edara.We are doing our first API gateway lab and as we have decided that we are going to perform this labinto the four parts, introduction is already over in previous video.Now we are going to start part one of our lab.Let's start.Part one is all about prerequisites.And here we will explore three steps.Step one we will create DynamoDB table because this will be our back end.Then we will understand API.And then we are going to set up IAM role for Lambda.We will go step by step.Let's start with DynamoDB table because this will be our back end.So we have to create it first.To create DynamoDB table we have to go to the DynamoDB console.Here I am inside my AWS console.Now I am going to search DynamoDB, and if I go to the tables, I don't have any table.So I'm going to click on Create Table.Here I have to provide table name.I request you to use same table name as I'm using in this particular Mindmap, because we will use thisname into the code as well.So yes.User table.Now we need to create partition key.Here we are giving name.It is user ID.So partition key.Keep this in mind that I is capital over here.Other characters are actually small and then the type will be string.We do not need to touch any other options.We need to click on create table.And now it is creating this table.So it is very simple.Now we just created our backend.But what is the next.So we need to set up API.Now why we need to set up API.Let me explain you first.Then we will go for this.Here it is.Introduction.Let me close this.Introduction of API.Since we are accessing this application from mobile app built using React Native or Flutter, we cannotdirectly talk to the DynamoDB table.We have our backend ready, but our front end cannot directly communicate with our backend.So we need to create API that will allow us to store new record fetch records and delete records.Right?So we need to create API to store, read or delete data from this DynamoDB table that we have alreadycreated.Now what is API logic?When we say API logic, we are talking about actual code that actually runs behind the scene when youuse your application like saving new user, fetching user data, or deleting a record.When do we write API logic?You can write this logic using Python, Node.js, Java, PHP, go.In any language.You can write this logic and specifically software engineers going to write this right.So can we host this logic in AWS using AWS Lambda EC2 x or x.So once you have this logic which is going to talk with the DynamoDB table, you can run this logicfrom Lambda EC2 x.You can use any platform.But in our lab we are going to use Lambda right.Okay.What we are doing now.So in our lab, what is our current situation?We will create three separate lambda functions to handle three different API logic tasks.One.Create user function to create new user.Get user function to fetch user information and delete user function to delete user record.Note technically, you can write all three tasks in a single lambda function, but it becomes messyand harder to manage best practice.It's better to keep each function focus on one task.This follows the separation of concerns principle and it is easier to test and maintain, right?So now we understood this entire process.We have this DynamoDB table.Okay.Now in this particular table we want to add record.We want to read record or we want to delete this record from the front end, but we cannot directlycommunicate from front end to back end, right?So here what we are doing, we are actually creating three APIs and we are hosting this into the Lambda.Now this API will perform this operation.So post will add new record get will read record and delete will delete the record from the table.So we need to write three different lambda functions right.Now this lambda functions are actually going to communicate with DynamoDB table.And you know that in AWS one service cannot perform operations on another service.We need to provide permission.So we need to create IAM role which is actually giving permission to all these functions to performoperations related to DynamoDB table.And that's why you need to create IAM role.Let me explain you step by step.Introduction.Before we create lambda functions, we need to give permission to do their job.Just like you can't enter building without ID card, Lambda also needs IAM role a digital permissioncard to access DynamoDB.If you are not going to set up this role, Lambda cannot perform any operations on our backend meansDynamoDB table.Now what is IAM role and IAM role is in?AWS is like a access path.It tells AWS hey, this Lambda function is trusted and it can perform specific action.In our case, our Lambda function will need a permission to read and write DynamoDB table.So we will create IAM role with Amazon DynamoDB full access.Look.As per best practice, you should give permission on specific DynamoDB table.But right now our concentration is to learn API gateway, not Im, so we are just making it very easy.We will give Amazon DynamoDB full access permission.Okay, now we need to create role.So here we have this tab.I am going to perform this step.So I am going to the IAM.Now inside Im we have role option.So we need to create roles.When we are giving permission service to service we are always creating role.So here I am going to click on Create Role.Now we want to give permission to Lambda right.So here in the AWS service we need to select Lambda.Then next.Now what Lambda is going to perform.It is going to add recording to the DynamoDB table.So I'm going to give full access DynamoDB full access write DynamoDB.Here we have two DynamoDB full access DynamoDB version two.Choose both.Okay, next.Now row name.You can give any row name, but here I am using this name Lambda DynamoDB execution role.So please use same name because in upcoming steps when we will use this name, it would be easy to rememberright?So create role.And it has been created.So what I can say that my prerequisites has been completed.I have DynamoDB table.I have one role with the permission to store record into DynamoDB table.So our prerequisites has been over.Part one completed in the part two will create this API.Who is going to perform, post, get and delete operations on our DynamoDB table?Part two is really very interesting, so please watch this.Goodbye.Thank you.