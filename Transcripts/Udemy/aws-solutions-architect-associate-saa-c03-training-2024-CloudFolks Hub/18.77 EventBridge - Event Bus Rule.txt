# 18.77 EventBridge - Event Bus Rule

Hello and welcome to CloudForks Hub.My name is Bhavesh Adara.As you know that EC2, Lambda, ECS, RDS, S3 all AWS services are actually event sourcefor event bridge.They are sending multiple events to the event bridge using default event bus.But by default, all these events not going to trigger any action. Why?Because there is no rule configured inside event bridge.If there is no rule, there is no trigger action.So if you want to trigger action on a specific event, you have to set up rule inside event bridge.Now how we can set up this rule?I am going to explain you in this particular video. Let's start.Event Bus Rule IntroductionRule is a filter plus decision maker that decides which event should trigger which action.Event Bus receives all the events, rules checks which event is important and what should happen next.If there is no rule, there is no action.Keep this in your mind.And as a cloud engineer, we have to configure this rule.Now here I am going to give you one simple example.Scenario EC2 monitoring.You have an EC2 instance running in AWS.Now imagine instance is running normally, suddenly someone stopped the instance.Now what happened internally, EC2 generates event, which is automatic.Event goes to event bus, that's also automatic and normal.But now you have to set up rule.So if you have this rule, event bridge is going to check this rule and then it is goingto trigger action based on the rule.Means your rule, if EC2 instance equal to stopped, send alert.Now question is how we can configure these rules?Now to set up rules, first of all, you need to understand types of rules.So here we have two options, event pattern rule, most used and schedule rule.First of all, we are going to talk about event pattern rule.Event pattern rule is used to filter specific event based on the defined condition in JSON format.As you know that events are actually generated by event source and they are sending it intothe JSON format.So we are going to use the same JSON format over here.The rule can be defined in two ways.Event pattern rule can be defined in two ways, simple filtering and detailed filtering.Now let's talk about simple filtering.Look, here the filtering is actually very simple.Source means AWS EC2, right?This is simple filtering.Now what will happen?Here our event bus is actually going to receive events from all the source, right?It is going to receive events from EC2, Lambda, ECS, RDS, S3.But now what I want, I want that whenever I am going to get any, keep this in mind,any event from EC2, I want to run this Lambda function.In this case, I can use simple filtering, right?Now in this case, if I am going to receive events from EC2, it is going to trigger Lambdafunction because I already specified this into the rule.Now if I am going to receive events from ECS, it is not going to take any action becausewe don't have a rule for ECS event, right?But this event is actually a simple event.This rule is actually a simple rule, it means whenever my event bridge is going to receiveevents related to EC2 start, stop, terminate, any EC2 related event, no condition on state,everything is allowed and it is going to trigger Lambda function for all the events, right?So this is simple filtering event.Now let us talk about detail filtering event, meaning trigger action only when EC2 instance stopped.Look here, we are triggering Lambda function from any event of EC2.Here we are triggering Lambda function specifically if we are going to get stop instance, right?So here we have detail filtering, we want EC2 instance state change notification onlyrelated to EC2, but when the instance stop, I want to run this particular Lambda function.Now you may have another question as well, let us say that I have 10 EC2 instances.Now out of 10, I just want to trigger Lambda function when specific database events stop. Is it possible?Yes, it is possible and we will do this practically as well.Right now, we do not have this provision over here that instance ID is not specified.It means whenever any instance is going to stop, it is going to trigger Lambda function,but if you provide specific instance ID, event will be triggered when this specific EC2 instance will stop.So no need to worry about this, we will talk about this specific event into the upcomingvideo when we will perform lab, okay?So that is event pattern rule.It is totally based on JSON.Now schedule rule works like a cron, rule runs at fixed time, no event required, examplerun Lambda event 5 minutes, run backup every night.This is not event based, it is a time based.Now look, you do not need to worry about this schedule rule, why?Because right now we are just going to concentrate on event pattern rule because we want to perform lab of this.Once we will complete lab, then we will talk about schedule rule.So there is a separate video for this and we will also perform lab of the schedule rule as well.So right now I just added this because it is a part of types of rule, that is why.So do not worry about it, just concentrate on event pattern rule for this video.Now what happened inside rule?Match event, check event pattern, here we have, right?We are getting event from EC2 instance and yes instance stop event, we are receivingit, if matches, trigger the target.Now trigger can be Lambda, SNS, SQS, time function, any.Now keep this in mind, one rule, multiple target, it means one rule can trigger multiple target.Here EC2 instance stop rule and rule is actually going to trigger Lambda, SNS and SQS, right?So one event, one rule and multiple action is also possible, same way.Schedule rule, right? For same event.So here we have one event, EC2 instance stop and here we created three rules, Lambda, email and login.So you can create separate rule or one rule can trigger multiple target as well.It is a flexible architecture.So both condition is possible and we will set up this in upcoming videos.So don't worry about it.Now I hope you got complete understanding about event bus rule and when we will configurethis, your concept will be crystal clear.Thank you very much.See you in the next video.Have a nice day. Goodbye.