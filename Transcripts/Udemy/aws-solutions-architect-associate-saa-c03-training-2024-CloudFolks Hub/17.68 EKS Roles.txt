# 17.68 EKS Roles

Hello and welcome to Cloud Fox Hub.My name is Bhavesh Katara.Now you are very much aware about ECS cluster creation.We have three modes to create ECS cluster.We have quick configuration with ECS auto mode.We have custom configuration with ECS auto mode or we have custom configuration with ECS auto mode disabled.Now, no matter which option you are going to choose, you have to set up cluster IAM role and nodeIAM role in advance before you are going to create ECS cluster.In this video I am going to explain you what is the use case of this IAM roles.So let's start.Here I am going to start with introduction.Amazon ECS uses IAM roles to allow AWS services like X, X ECS and Fargate to perform actions securely.You know ECS is going to manage your cluster environment on your behalf.It is going to create worker nodes automatically as well.So in this case, we have to provide some permissions to the ECS.We can do this using ECS roles.Every ECS setup it can be in auto mode or in the classic needs at least one cluster role and one computerole.Now compute role or node role will be same.These roles define who controls the control plane and who manages worker nodes.Without correct IAM roles, ECS cluster creation will fail or your workload won't launch.So it is very important.Now roles required for each cluster configuration mode.If you are going with the quick configuration with auto mode, you have to create cluster role and nodeno role.Same we have for custom configuration.Auto mode on.But if you are using classic mode means custom configuration.Auto mode of custom role will be same.But here you have two options.Here we can add two types of worker nodes EC2 or Fargate.So based on this we can select node role or Fargate role.Now I'm going to explain you.What is the use case of these roles.And I'm going to provide you one of the easiest way to create these roles okay.So here we have IAM role detail and required policy.First we have custom IAM role in any of any of this mode.You have to create this cluster IAM role.This role gives permission to ECS service means control plan to create and manage required AWS resourceson your behalf like EC2 networking and load balancer.If you have auto mode, it is going to create EC2 automatically.So using this role it is getting this permission to create EC2.It is.It can also manage networking or load balancer.So everything uh will be managed through the ECS.Now ECS having this permission to manage all these things because of cluster IAM role.Now you are thinking about where is permission?Where is policy.Because when we are creating role, the biggest confusion is which policy to attach.You don't need to worry about this.Watch entire video.I am going to provide you easiest way to understand which policy we can attach.Then we have node IAM role and we have Fargate port execution role.It is same because if we are using classic mode, we have EC2.If we are using EC2, we can use node IAM role.If we are using Fargate we can go with Fargate port.Right.So use case is almost similar.The role gives permission to the EC2 instance worker nodes that are part of the EKS cluster.This allows them to register themselves with the EKS control plane.Pull the container images from the ECR.Communicate with other AWS services like CloudWatch.Log SSM for the remote access S3 to store or access data from the application.Let's say that I have one application.An application's requirement is to store the data from S3 bucket or retrieve the data from S3 bucket.Now how I can assign permission to this application?Simply, I can attach this role with the node.So node having permission to store the data or access the data from S3.So application will get it automatically.So which policy you are going to attach with the node IAM role.It is totally depend.There are some required policy, minimum policy.And then it is totally depend on the application that you are hosting inside this node.Run your application properly inside the cluster.Now as I explained you earlier, Fargate, it is like a same, uh, but it is only available if youare using classic mode.This role gives permission to AWS Fargate service to run your Kubernetes pod without using EC2 instances.This allow Fargate to pull the container image, create an attach Enis, send logs to CloudWatch logs,access other AWS services like S3 or DynamoDB.So these are like similar kind of use case.Now we are going to talk about the main point, how I can find out required policy that we have to attachwith the role.Because if I am creating role I have to attach policy.Right.So I am going to provide you easiest way.You do not need to remember anything, right?AWS is giving this flexibility to us.How?Let me show you.When you go to the cluster configuration here, we have to create a cluster IAM role.How I can do this?Look here they are providing us this create recommended role when you click over here.AWS service.EKS auto cluster everything selected automatically.And when you click on next here the attach minimum required policy.They attach Amazon ECS block storage policy Amazon EKS cluster policy, Amazon EKS compute policy,Amazon EKS load balancing policy Amazon EKS networking policy.So minimum required policy are already preconfigured and you don't need to remember this.So when you click on next next it is going to create this role.And this is same applicable for the node IAM role as well.Let me click over here.Now it is going to create IAM role for the node.So here this time I am getting EKS auto node right instead of EKS auto cluster.Next also added minimum policy Amazon EC2 container registry.Pull only an Amazon EKS worker node.Minimal policy.Now if my requirement is to, uh, attach another policy like, uh, store the data to S3 and retrievethe data from S3 that I can attach as well.But here they are giving us ready to use recommendation role that we can directly use, and we do notneed to remember which policy to attach with specific IAM role.This is actually giving us great flexibility.So I hope you understand everything related to the EKS roles.Now there is no confusion and still if you have any question, I am always available for you.Thank you very much.See you in the next video.Have a nice day.Goodbye.