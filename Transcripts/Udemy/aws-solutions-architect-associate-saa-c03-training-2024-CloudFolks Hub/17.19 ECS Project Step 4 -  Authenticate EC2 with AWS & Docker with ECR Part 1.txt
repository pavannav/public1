# 17.19 ECS Project Step 4 -  Authenticate EC2 with AWS & Docker with ECR Part 1

Hello and welcome to Cloud Folks Hub.My name is Bhavesh Edara.Right now we are doing our ECS project and now we are on step number four.In this particular step, we are going to understand how we can push Docker image to ECR.But before we are going to understand this particular step, let me explain you current situation.We have our build machine.Our Docker image is there inside this build machine.Now we want to deploy application inside ECS using this particular image.But you cannot send this image directly from build machine to ECS.So it is not possible.In this case we have to use ECR.We need to create repository inside ECR and then from build machine we have to push this image to theECR repository as per current situation.We have this ECR repository also available.Now once you will push this image from build machine to ECR, whenever ECS wants this particular image,it will pull this from the ECR, right.But the first step is to push this image from build machine to ECR repository.So we are going to perform this particular step in this video.Okay.Now this process is actually two step process.And we need to understand how we can follow AWS best practices as well.So this is really very interesting.Let's understand this practically.To push images from your build to ECR repository, you need to follow these two steps.One.Set up authentication and another tag and push image.So our main aim is to tag and push the image.But we cannot do this directly.We have to set up authentication.And that's why we are going to start with the step number 4.1 setup authentication.In the setup authentication we have two step process as well.The first is authenticate.The EC2 means our build machine with AWS.Let's understand this first.Why this step?To push Docker images to ECR, the EC2 instance must be authorized to use ECR command via AWS CLI.See if you want to push this image from your built machine to ECR, we have to use aws CLI.So it means in our build machine we must have AWS command line installed.Right now it is always there when you are using Amazon Linux.But if you want to use aws cli, you have to login with aws cli first.So we have to give command like AWS configure.Then it is going to ask for access key secret key default region output format.So in this case we have to enter access key and secret key right.Once you add this access key secret key you will be like you are authenticated with aws CLI.And then you can run AWS CLI commands.But according to AWS best practices you should not enter access key and secret key inside any EC2 instance.It is like hardcoding authentication.It is not best practice because if your EC2 will be compromised, hackers can easily get access of youraccess key and secret key.And then using this, they can compromise your AWS account as well.So this is not best practice, right?It means I want to run authentication command for ECR without adding access key and secret key.How I can do this?I can use option like we have two option one use access key and secret key and another one is use IAMrole.I am not going to use this access key and secret key.I don't want to generate access key and secret key and I don't want to enter this inside my EC2 instancemeans built machine because I want to follow security best practices, right?So we are using second option use IAM role.So here we have this.We need to create one IAM role.And then we need to attach this role with our EC2 instance.So let's do this right now to create IAM role I'm going to the IAM.IAM console is open here I'm going to the roles.And then I have this option like create role.Yes I want to create this role to attach with AWS service which service we call it like EC2.Then I am going to click on next.Now we have to attach policy.Now this policy is AWS managed policy and ready to use.So the policy is Amazon EC2 Container Registry Full Access.Let me search this.Yes I got this AWS managed policy going to select this next now role name I'm going to give name ECRaccess.Role means this role is giving access like giving permission or authorizing my EC2 instance to accessECR.Now create role so role successfully created.Now I have to attach this role with my EC2.So going to the build machine.Imagine going to the action, going to the security modify IAM role.And here we have this ECR access role.So it is done.Now if I want to run any ECR command I do not need to use AWS configure.I do not need to enter access key and secret key.Now if this process is successful okay, the next step will be successful as well.So we are not going to verify anything right now.We will verify after executing the second step.If the second step will be successful means our authentication process is also successful right.So step number one of setup authentication is done.Now let's talk about authenticate Docker with Amazon ECR.Now why this step.Your Docker client on EC2 needs permission to pull and push image from the private ECR registry, right?We have this particular image here.And we want to push this image.But in order to push this we have to run some commands.To do this we generate temporary login token using aws cli cli and pass it to the docker.Now how we can do this?It is very simple process.Okay, see you have to go to your ECR.You already created this repository right.Now here you will get this view push command.We have Mac and Linux system.So we are going to use this.Now here we have four steps.But our aim is to complete authentication process right.So for us 234 will be in the next video.We just want to complete login process.So I'm going to copy this command going to my build machine.And here I'm going to paste this.Now once you press enter.And because our role and everything is perfect.See, I got this login.Succeeded means everything is good.These tabs also completed properly.And another step means 4.2 is okay.Sorry 4.1.Step one and two is completed.Right now we can push image.Now pushing image is easy process but we need to understand tagging first.And then we are going to push this image.And I want that you understand the entire process perfectly.So right now our login process is succeed.We can now easily push this image from build machine to ECR.But before this we need to understand tagging and we are going to understand tagging in the next video.I hope you got complete understanding about this.Still, if you have any question, I am always available.Goodbye.Thank you.