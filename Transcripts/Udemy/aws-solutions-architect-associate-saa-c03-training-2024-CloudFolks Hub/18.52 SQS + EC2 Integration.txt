# 18.52 SQS + EC2 Integration

Hello and welcome to CloudFoxHub. My name is Pawar Shatara. You are learning AmazonSQS and in this video, we are going to talk about SQS EC2 integration.If you are thinking that why I am repeating this, you are wrong here. Because this timeI am going to explain you this integration according to the exam scenario.As I said you earlier, in AWS exam, you will get questions related to SQS for sure. Andin AWS exam, we have all these questions related to the scenarios. So, I want you to understandthis particular scenario which will be helpful for you in the exam. Let's start.Here I am going to start with scenario or we can say that introduction.A company has an application that generates background tasks such as report generation,video or file processing. This application is producer. Look at this picture, I haveone producer. Producer app is running inside EC2 instance.Whenever task is created, this application sends task as a message to the SQS queue.Here I have this SQS queue. Producer pushing message inside this particular SQS queue.And then we have consumers and here I have multiple consumers. So, let's talk about them as well.On other side means consumer side, we have multiple Amazon EC2 instances, read messagesfrom SQS queue. Each EC2 instances process one task at a time.Now, since these EC2 instances means our consumer EC2 instances, we also call it worker nodes.Why I am specifying this because in the exam, instead of saying consumer nodes, they areusing word worker nodes. Now, I am explaining you everything inside SQS by specifying consumernodes. And in the exam, if they are going to use worker nodes, it may confuse you. Right?So that's why I'm going to specifying this particular terminology. So, let's go backto the mind map. Since these EC2 instances means consumer EC2 instances, right? Do nothandle user requests. Only pick tasks from the queue. They are not going to talk withthe users. They're just going to pick up the task from the queue. Perform background processing.They are commonly called worker nodes. Worker node is a kind of consumer EC2 instances doingbackground work. So, we can say that consumer node or worker node is the same thing. Right?Okay. So, you understood the scenario. Producer and then we have queue and we have multipleworker nodes or consumers who is going to pull the task and process it. Okay. This iskind of high-level architecture. Producer, SQS and EC2 worker nodes.Now, let's talk about how each component works. I know that you are aware about it, but let'sdo it for 2-3 minutes. Producer application creates background tasks, sends each taskas a message to SQS, does not wait for task completion. Asynchronous. Amazon SQS storesthe task safely. Act as a buffer between producer and EC2 worker nodes. Deliver thetask to EC2 worker nodes. EC2 worker nodes continuously pull SQS for messages. Pick upone task at a time. Process the task. Delete the messages after successful processing.Now here, we have something new. Right? And because of this, I explain you everything.And the new is, these EC2 instances are part of auto-scaling group. Okay. So, we have multipleworker nodes and these worker nodes are actually part of auto-scaling. You guys are aware aboutauto-scaling. Increasing and decreasing number of EC2 instances according to requirement.Right? We already done lab of auto-scaling. So, now this is something new we have thatwe have worker nodes and these worker nodes are actually part of auto-scaling. Okay. Nowyou are wondering like what is new over here. Let's talk about it. How EC2 auto-scalingwork using SQS queue depth. In this architecture, EC2 instances are managed using auto-scalinggroup. Means our worker nodes are in the auto-scaling group. But the key point is EC2 scaling isdriven by SQS queue, not by CPU usage and not by schedule. We are not going to schedulescaling for EC2 instances. We are not going to schedule our EC2 instances based on theCPU. If you remember the lab of EC2 auto-scaling that we have done, it is totally based onCPU usage. We have an application running inside EC2 instances. From this application,we increase CPU utilization and it has created new EC2 instances. We also decrease this andall the EC2 instances terminated. So, what is actually happening, in normal conditionwe have this fixed formula in our mind that when we are going to talk about auto-scaling,it is based on CPU utilization. But this is not the case. Right. And that's why I am explainingyou this particular situation. Now, look, why CPU-based scaling is not enough. Herein this scenario, if I am going with CPU-based auto-scaling, I am doing wrong. Why? Let'sunderstand this. In the background job processing, EC2 CPU may be low, but many tasks may stillwaiting in SQS. Look at these instances. What is the main use case of this instance? Pullthe message, process is faster and delete this from the queue. Right. This is the maintask of this worker nodes. So, in this task, if I am just going with the CPU, it is notproper process. Right. Okay. If we scale only on CPU, backlog increasing, jobs wait toolong. So, CPU alone does not represent real workload. Now, what metric is used instead?So, question is, okay, if we are not going with the CPU, then what? Okay. So, here wehave auto-scaling using SQS queue metric, mainly approximate number of messages visible.So, SQS having one metric, auto-scaling using this metric to scale EC2 instances. Okay.And this metric is approximate number of messages visible. This metric tells us how many tasksare waiting to be processed. This directly reflects actual demand. Simple scaling logic.If queue has a very few messages, now auto-scaling can find it out because of this metric, runminimum EC2 instances. If queue size increases, launch more EC2 worker nodes. If queue becomesalmost empty, terminate extra EC2 instances. Scaling always follows queue size, not thatCPU. Okay. So, this is the one thing that you need to remember.Whenever you use SQS plus EC2 variable and unpredictable workload, auto-scaling shouldbe based on SQS queue DAP. Right. So, this is kind of what I call it most important pointthat you need to understand related to AWS solution architect exam. And specificallywhen you are going to get questions related to SQS EC2 integration. Right. Same way, thereis a question related to SQS plus Lambda integration. Okay. We will discuss about it into the upcomingvideo. Thank you. Goodbye.