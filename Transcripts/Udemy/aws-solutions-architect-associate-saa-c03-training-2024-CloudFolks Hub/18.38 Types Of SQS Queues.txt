# 18.38 Types Of SQS Queues

Hello and welcome to cloud, folks.My name is Bhavesh Katara.Right now I am inside SQL console and I'm going to create New Cube.Now when you are going to create new queue you have to choose queue type.We have two options standard and Fifo.Which option should I select right now?What are the differences between standard and Fifo?I'm going to explain you differences between standard and Fifo in this video.So let's start.Introduction.SQL provides two types of queue standard queue which is default.And in most of the cases we are using standard.Other option I have is having specific use case.We call it first in.First out means Fifo queue.The main deciding factor is Ordering and duplication.Tolerance.How?Let's understand both of them.Standard Q default and most used.When you click on create Q, the standard is automatically selected.Why?Because in most of the cases we are always creating standard Q what it means.Messages are delivered at least once.Look here when we are saying that messages are delivered at least once means guarantee delivery is there,but messages can be delivered multiple times as well.Okay, so duplication can happen if you are going to use standard Q order is not guaranteed.Now look at this here.Producer is sending messages into the sequence.The sequence would be a b C, d.But look at over here in the Q we are not actually going to maintain this order.It means.Consumer can pull this message without following sequence.Producer is sending message a, B, C, d in the sequence, but consumer pulling this a first, thenc, then b, then D means here.In the standard queue we are not actually maintaining order.Okay, we can say that these two are actually drawbacks.But there is one advantage of the standard queue as well.Very high throughput and scalable.It can handle millions of messages.Now when to use order of message does not matter.Duplicate messages are acceptable.Speed and scale are more important.E-commerce example sending email notification.Sending WhatsApp SMS updates.Logging activities.If two email swap order no problem.You are not going to follow sequences over here right now.In this case, you can use standard Q standard Q throughput.Now this is actually a plus point of the standard Q.Virtually unlimited throughput can handle very high number of messages per second.AWS automatically scale it in simple words standard Q equal to no practical sending limit, use casefit, high traffic spikes, background jobs, and notification.Right.So these are like a deciding factors for the standard.Q now let's talk about Fifo.Again.It has a specific use case right okay.Let's start with what it means.Messages are delivered exactly once.Look at this.Here it is a drawback of standard Q.Right.Because multiple messages can be delivered Here it is a plus point of five.Okay.Same way.Strict order is guaranteed.Look at this.One.Two.Three.Four.So all the messages will maintain strict order sequence, which is not going to follow in the standard.But again we have one drawback as well.Lower throughput than the standard.Okay.So what we can say that these two are plus .54 -0.4 standard.But this one is plus point for standard and -0.45 for okay lower throughput than the standard.Now when to use order must be preserved.Duplicate processing is not allowed.E-commerce example inventory update.Wallet balance deduction.Right.When we are going to deduct any money from the from the wallet, we must get these messages into thesequence.No duplication allowed over there.Okay.Financial matter.So in this case, Fifo would be the better choice.Order.State.Change.Order placed.This is the first message that you are going to get.Then packed, then shipped.Right here the sequence must be maintained.Processing must be happening.Sequence.Now Fifo queue.Throughput limit.Now this is a drawback.Fifo queue has strict limit because they guaranteed order up to 300 messages per second.Now if you are going to send this as a batch process, it is going to accept 3000.But how?Let me explain.Look batching send multiple messages in one request to increase speed.You have practical limit of 300 messages per second.That's for sure.But what if you are going to send ten messages into the one message, like you are going to combinemessage ten messages as one message.In this case you have practical limit of 3000 messages.Okay, we'll talk about it.But you just need to remember these numbers ordering.And exactly once deliveries are maintained in simple word Fifo queue means control speed for correctness.And this is a simple decision rule.Standard queue fast scalable order not guaranteed Fifo queue order exactly once and control processing.So now we have clear idea when we can select standard.When we can select Fifo, what we will do?We will understand all the standard options.First we'll create standard and then we will go for the practical of Fifo.This is our plan.I hope you are getting complete understanding about SQL and you don't have any confusion.But still, if you have, I'm just one message away from you.Thank you very much.See you in the next video.Have a nice day.Goodbye.