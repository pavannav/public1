# 7.1 Introduction Of Load Balancer

Hello and welcome.From today we are going to start another AWS topic, AWS Load Balancer.Yes, we will understand A to Z about load balancer using 30% theory and 70% practical.So if you want to learn load balancer from zero, complete this particular series.Now before I'm going to explain you about technical stuff of load balancer, I want you to understandhow load balancer is actually working and why we need to use this load balancer.Although we have four types of load balancer first application load balancer, network load balancer,gateway load balancer and the old one classic load balancer.Here I am starting all this concept with the application load balancer.So here I am using a icon of application Load balancer.Yes.Throughout this session we will discuss about each and every type of load balancer.Okay, first of all we are going to understand the use case.Let's say that I have a this kind of VPC virtual private cloud right.We already completed VPC series.And this is prerequisites okay.So if you don't know about VPC if you don't know about private and public subnet and and anything relatedto the internet gateway or Net gateway, I recommended you to complete previous VPC series and thencome back.Okay.Right now throughout this series, I am assuming that you guys already having knowledge of private,private and public subnet.Okay.If you look at the picture I have, I have one public subnet in each of availability zone AP South oneand AP South one B and one private subnet.In each of these availability zone I have multiple public subnet.Multiple private subnet.Why?Because of high availability.According to the AWS.If you want redundancy, if you want uninterrupted service, it is always a good idea to create yourresources into the Multiple availability zone.Now, if I want to use multiple availability zone, I have to have subnets over there.And that's why I have this kind of a VPC design.Now let's say that you are responsible to set up web server over here okay.And you don't know about the load balancer.So what you will do you will create you will create EC2 instance into the public subnet right here forhigh availability.You are going to create two EC2 instances one in each of this availability zone okay.Of course you have to create this into the public subnet.Why?Because you are creating web server if you are not creating this into the public subnet, then nobodycan access this over the internet.So that's why that's why we have two EC2 instance into the public subnet.So anybody can access this EC2 instance over the internet using public IP.Of course you have to provide public IP to this EC2 instance as well.So let's say then I am giving public IP like 5.5.5.5 to this web server and 6.6.6.6 to another EC2 instance.Right now I want to use these two EC2 instance.I don't want that anybody will access this particular EC2 instance, and if this will be down, we willredirect user to the another EC2 instance.Why?Because I am paying for both, right?If I am paying for both of these EC2 instances.Why I want to make one EC2 instance as a passive instance.You can load balance the traffic between these two EC2 instances of course using a service called Route53.Yes, we already completed route 53 series guys.Why?Because this is also prerequisites over here.So if you don't know about this you can watch our route 53 series as well.Okay so there is a one way to load balance the traffic between this EC2 instance using AWS DNS serviceknown as route 53.But we have one very important service over here, which is very easy to set up.It is also giving us.Very easy way to set up the things.It is also give us a flexibility to to.Secure our EC2 instance and the service we call it load balancer.Now suppose instead of depending on route 53 for load balancing between these two EC2 instances, youcan use load balancer if you are going to use load balancer.The biggest advantage is you do not need to set up this EC2 instance into the public subnet.Even you don't need.Public IP address for this EC2 instance as well.So instead of setting up your EC2 instance in the public subnet, you can set up them into the.You can set up them into the private subnet.Now you may have this question that okay, now my EC2 instance in a private subnet and we don't have.Public IP address for this EC2 instance as well.Now how users can access this particular.EC2 instance.So here user can access own resources only in the public subnet.Right.So here what we will do.We will set up load balancer okay.So our EC2.Instance will be in the private subnet.And here we are going to set up.Load balancer.Okay.Load balancer.Now when you set up your load balancer load balancer will have URL.Using this URL.Anybody on the internet let's say that there is one person over here.He is going to access this load balancer using URL.Now this load balancer will send.This request to the EC2 instance in private subnet.Okay.And it will load balance the traffic among any number of EC2 instances.So using load balancer you can divide the traffic between your EC2 instance.First advantage.Second advantage is you do not need to place your EC2 instance in the public subnet, because load balancerwill be in the public subnet, and it will redirect this request to the EC2 instance in the privatesubnet.In the load balancer, we have health checkup facility as well.Let's say that this scenario is working very well, but because of some problem this EC2 instance isnow not available.In our load balancer we have health check facility.So what happens if this EC2 instance having any issue.The load balancer will verify this using Health Check Setup, and it will automatically remove thisEC2 instance from the rotation.So if I am going to get new request, this request is going to forward it to the this EC2 instance becauseit is healthy.Now whenever you will repair this EC2 instance or when the EC2 instance will come back, right now itis working very well.Okay so.So load balancer will again verify this and it will say that okay.Now this EC2 instance is working very well.Now whenever you will say new request again it will forward it and it will add them into the rotation.Right.So this is what the load balancer is.Yes.You can load balance the traffic between route 53.But if you want a proper load balancing system with application layer and networking layer facilityload balancer would be the better choice.Now there are lots of stuff to understand because how we are going to set up this load balancer.There are four types of load balancer.What are these types and why.There is four types of load balancer.How we can set up load balancer.So it will also uh utilize high availability.How we can set up load balancer into the more than two availability zones.If you look at this carefully instead of setting up load balancer into the particular one availabilityzone, here I use two availability zone.Why?Because we have to take care about high availability of load balancer as well.And how we can utilize load balancer to redirect the traffic among multiple group of EC2 instances.We are going to discuss about all this, but not in this video.It is just the introduction of load balancer.I hope you understand this.See you in the next video.Have a nice day.Goodbye.