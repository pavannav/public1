# 18.20 REST API Integration Types

Hello and welcome to Cloud Fox Hub.My name is Bhavesh Edara.Currently we are discussing about all the options that we are getting while creating method inside restAPI.In previous video I explained you about method type.In this video I am going to talk about integration type.So let's start.First of all we are going to understand what is integration.So introduction of integration integration equal to where this method will send the request after theclient calls it.For example client calls gets less users integration decides which backend gets this request.As you know that we have invoke URL for our rest API or http api.Now client will use this invoke URL to send requests to your API gateway from the browser or from thepostman.Now, when your API gateway will receive this request, it is going to forward this request to the backend.Now which backend you want to use will be decided by the integration.Here rest API supports this integration type.We are going to discuss this step by step.The first one is Lambda function.Look at this here I'm getting this.Now Lambda function is actually most popular backend API gateway lambda and returns lambda responseused for serverless backend.Same way if you don't want to use any AWS service and you want to forward request to outside world,you can use http api gateway for the request to public HTTP endpoint.For example your public URL.It is simple pass through process.Then if you want to test your environment, you can use mock.Because for mock we have no backend API.Gateway returns fixed response.Great for testing and demo, and the most important one is VPC link.Now what is VPC link?A private bridge between API gateway and resources inside your VPC.Here when I'm saying inside your VPC means you have back end without public access means you have backend running inside private subnet.Now you want to send request to them.Now in this case you are using VPC link but VPC link having some limitation.So let's talk about it.Why VPC link is needed.API gateway is public while backend services are in the private VPC.Now look at this picture.Here I have.API gateway.It is a public rest API.So external client can connect it.But now this API gateway cannot forward request directly to this resources or this bacon which is runninginto the private subnet.Right.So we are using VPC link.Now VPC link also can not able to communicate directly with these resources running inside this privatesubnet.Because VPC link links requirement is to have Alb or NLB right.So VPC link is going to communicate with Alb or NLB and then Alb and NLB is going to communicate withthis back end running into the private subnet.Previously it was VPC link version one.Version one supports only NLB.Recently AWS announced that we have VPC link version two as well, which supports Alb.So now we can say that VPC link supports both Alb and NLB.Now this enables secure access without exposing backend resources to the internet.So this option we have over here and the final option is AWS services.Now look at the list here.We have all these services available.You can send requests directly to it from your API gateway without using Lambda API.Gateway can directly call AWS services like S3, DynamoDB, SQS, SNS Kinesis without using Lambda requiresyou have to set up IAM role permission to call target services and request and response mapping velocitytemplate or mapping template.It is like so many things that you have to set up and then you can use these services.Example rest API is going to use Post request integration.Kinesis API gateway pushes records directly into Kinesis Data Stream.Right.So this is integration type supported by rest API.Now we already discuss about the integration in http API as well.So we have to understand what are the differences between Rest API integration and HTTP integration.Both Rest API and http API supports HTTP.VPC link and AWS service integration.However, rest API supports broader and more flexible set of AWS service integration.With advanced mapping capabilities, HTTP API supports only limited set of service integrations anddesigned for simpler, low cost use cases.Look here I am getting list of services.Look at this.This list is so long because I am setting up integration for rest API.Now if you have http api, let's say that I have this http API.And if you go over here to attach integration, create new integration and look at this.These are AWS services supported by HTTP integration.So list is small.And for Rest API we have so many services.So we can say like this HTTP supports only limited while for rest API we have broader.Now first time I'm giving you exam tip as well because it is actually confusing in the exam that okay,based from the question, you have to find out that okay so like Rest API or http API okay.So here it is.Exam tip if question says direct integration with many AWS services complex mapping, then the answerwould be rest API.If question says simple plus low cost API with limited AWS service integration, answer will be httpAPI, right?So this is all about rest API integration.I hope you got complete understanding about this step by step.We need to talk about so many options available, so don't worry about it.In the next video we'll talk about proxy integration.Thank you very much.See you in the next video.Have a nice day.Goodbye.