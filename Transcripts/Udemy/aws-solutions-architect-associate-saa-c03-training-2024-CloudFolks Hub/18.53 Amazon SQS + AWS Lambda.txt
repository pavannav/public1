# 18.53 Amazon SQS + AWS Lambda

Hello and welcome to CloudFoxHub, my name is Bhavesh Attara, you are learning Amazon SQS.As a student while you are learning SQS you may have some simple questions in your mindbut there are some advanced questions as well and you will face these type of questionsin the exam or in the interview as well.So I want you to understand these questions first and the solution of this question.In this video, I am going to provide you detail about one of the questions which is relatedto Amazon SQS and Lambda integration.I will tell you what is the issue and what is the solution. So let's start.Here I am going to start with introduction.In the previous video, we understood how Amazon EC2 instances act as a consumer worker nodes.How EC2 instances poll SQS for messages.How EC2 auto scaling is done based on SQS queue depth.You already covered this.Now we want to understand that how Lambda works as a consumer on worker node.Now you may have this question in your mind like ok both are services related to the computeso it may works like a same. No it is not.First of all we need to understand that how EC2 works and how Lambda works and what arethe differences between them.Our normal pattern is same.It is a producer SQS queue and consumer.Now consumer can be EC2, consumer can be Lambda.But both are working in a different way.So let's understand these differences.When we are talking about EC2 instances, EC2 instances are always running.So EC2 application continuously poll SQS.In real world environment, you are using EC2 instance.Why because you want to run your application 24 by 7.And your EC2 instance having your application code.Now in this code, you have this mechanism, you have this code so it is going to pollyour queue continuously.Means polling is continuous.And here we are writing all the polling logic inside our application code.So whenever your EC2 means your worker node will be free, it is going to start polling.Then it is going to pull the message and it is going to process this right.And this is a normal process of EC2.Autoscaling is based on SQS queue depth that I already explained you in the previous video.So EC2 uses always on execution plus metric based autoscaling.This is actually how EC2 is working right.But if I am going to talk about Lambda, different use case.Let's understand this.Lambda is event driven, does not run continuously.If you want event driven execution of code, you are hosting this on the Lambda.It means Lambda is not always on.It means Lambda cannot continuously poll SQS queue because if Lambda wants to continuouslypoll SQS queue, it has to run 24 by 7.And it is not possible with Lambda because it is event driven.So question is how polling works when we have Lambda because it is not like EC2 instance right.Second, in the Lambda, we don't have a kind of tool for the scaling.In EC2 instance, we are configuring autoscaling, but here we don't have any configuration facility.Scaling is 100% automatically.So how Lambda is going to scale based on messages in the queue, right.So Lambda uses event driven execution, which is not running 24 by 7 and it has automatic scaling.So like our normal EC2 instance, we are using metric here, how we are going to scale basedon the messages.That's the question we have.And in the most of the cases, when you are in the interview, they are asking this questionlike how Lambda is going to poll SQS queue and how Lambda scale based on the messages in the queue.So this is a question, okay.Now you will get answer of this question as well.So I'm going to explain you about how SQS trigger Lambda and here it is a event source mapping.Now what is this event source mapping?I'm going to explain it.You already know SQS is pull based service.It means SQS will keep your messages into the queue.Now if you want to process these messages, you have to go to the queue.You have to pull the messages from the queue, right.SQS queue will never pull messages. Okay.Lambda is event driven service.So Lambda is not running 24 by 7.So how Lambda is going to pull these messages, right?So the natural question is how does Lambda receive messages from SQS? Key concept.Lambda does not pull SQS by itself, right?Lambda don't have this facility.AWS pulls SQS on behalf of Lambda.So AWS added a facility, which is going to pull messages and then trigger Lambda function.This connection is created using event source mapping, right?So here, look at this.Here we have this event source mapping, which is actually part of Lambda.So it is kind of SQS connector, which is going to pull SQS queue, right?So what is event source mapping?Event source mapping is AWS managed configuration that continuously pull SQS queues, read messagesin batches, invoke Lambda function automatically.So your Lambda function is not going to pull SQS.There is a configuration called AWS event source mapping, which continuously pullingyour SQS queue on behalf of Lambda.And when there will be message to process, it is going to trigger Lambda function andLambda is going to process this, right?Lambda code never contain pulling logic.When we are using EC2 instances, here we have our application.Inside this application, we have code for the pulling.But when we are using Lambda, in the Lambda function, we don't need to add any pullingcode because here pulling will be done through the event source mapping, right?Now where event source mapping lies?Even though it looks like you can configure it from SQS or Lambda, event source mappingalways belongs to Lambda.So SQS only provides messages, Lambda owns the pulling and invocation behavior.Now look, if you want to set up Lambda as a consumer, as a worker node, you can setup it using both ways.You can go to SQS queue and you can choose Lambda function and you can say that, look,trigger this Lambda function, right?Another way is you can go to Lambda and then you can set up SQS queue, okay?So whenever there is a message in the SQS queue, trigger this Lambda function.So we can set up integration from Lambda, we can set up integration from SQS, right?But this event source mapping is a facility of Lambda, not SQS, right?So even though if you are going to the SQS and going to set up this integration, thisevent source mapping will be configured inside Lambda, not SQS, okay?So you got the complete understanding?Now how AWS Lambda scale with SQS because the first question was about how it is goingto trigger and the second question is how it is going to scale.Lambda scaling is not the same as EC2 scaling.There are no servers, no auto-scaling groups, no scaling policies to configure because Lambda is serverless.So question is how it is going to scale my Lambda function based on the messages in the queue.AWS continuously polls SQS queue using event source mapping.As the number of messages increases, AWS increases the number of Lambda invocations.As the queue becomes empty, Lambda invocation automatically reduces, scaling happens automaticallyand instantly and it is actually handled by event source mapping, okay?So let's talk about the final process, SQS plus EC2, execution always running, executionevent river in Lambda, polling by EC2 code, here polling by AWS manage and specificallywe call it event source mapping, right?Scaling auto-scaling group, scaling automatic, scaling trigger, queue depth metric, scalingtrigger, messages, message volumes, right?So these are the differences and now you got complete understanding about how our EC2 instanceis using polling and how we are using polling with Lambda.So now there will be no need to worry about it.You can answer any questions related to Lambda and SQS integration.Thank you very much, see you in the next video, have a nice day, goodbye.