# 17.25 ECS Project Step 5 Bridge Network Mode Part 4.2

Hello and welcome to Cloud Folks Hub.My name is Bhavesh and you are learning AWS through the project.Now when you want to launch your ECS task, you have to create a task definition.Now inside task definition, we are getting lots of options.And one of the option that we need to select is network mode.Now here we have five network modes.We already discussed about AWS VPC network mode in previous video.In this video we are going to talk about Bridge network mode.And we are also going to compare this with AWS VPC.So let's start.So here I have my map.You already discuss about AWS VPC.Let's go with the bridge in AWS VPC mode.Each task gets on Eni and private IP from your VPC, PC providing full network isolation just like EC2instance.This is a picture that I used in the previous video.If you look at this particular picture, we have one container host, a EC2 instance and two tasks arerunning inside this.Now in this particular mode, AWS VPC mode, each task will get specific.Eni means Elastic Network interface.It is literally directly connected with your EC2 instance.Now this is full Eni.So each Eni is going to get IP address and it is connected with VPC.So your task is literally connected with a specific VPC.Here in the AWS VPC mode, each task will have this dedicated Eni.You are not going to share this Eni with any other task, but in the case of bridge mode, it is.It is little different here.You are actually going to share any.So here I have same EC2 instance and each EC2 instance having this host Eni.But we are not going to create multiple Eni.This Eni will be shared with host on service and also with the task.So all the tasks are going to use same host Eni.No sharing of Eni in this mode, but here it is 100% sharing of Eni in the bridge mode.How I'm going to explain you this step by step.In bridge mode, the task shares containers, host, Eni and IP address and use port mapping to connectno separate Eni or private IP addresses assigned.sign.So what?We can say that aws VPC mode is totally different than the bridge mode, right?But keep this in mind that this mode is only supported by EC2 instance.If you have Fargate infrastructure, right?Serverless infrastructure, you don't have any option.You have to use AWS VPC.If you are using Amazon EC2 instance, then you can choose any of the mode.And in general we are always using AWS VPC, but bridge mode is also one of the option.So we are going to talk about it right now.How this sharing of Eni is actually works.I'm going to explain you this entire process.First of all, we have clear understanding that in this particular bridge mode we are going to sharethis Eni.Right.So this is pix.In the aws VPC mode.We are not sharing any.We have any for each and every task.So these two points are very crystal clear.So let's start with the point number three.Now to make sharing possible, Docker creates virtual bridge on your EC2 host which acts as a gatewayand assign internal IPS to the container.So what?We can say that when you are going to use this bridge mode, okay, it is actually creating kind ofbridge inside your container host or Docker host.And this bridge also having IP.Okay, let me show you IP 172 17 0.1.Now this can be any IP okay.But let's use this one 7217 0.1 right.So it is going to create kind of virtual bridge inside your Docker host.And this is a gateway.So it is already having IP right.The bridge is connected to the EC2 instance ini, so all incoming and outgoing traffic from the taskflow through this single Eni.With Docker using Nat to handle IP translation between the bridge and Eni.Now this bridge is actually connected with the host Eni.Okay, so host Eni connected with the bridge.Now you know there are many people who is actually coming from the network background may have the questionthat how it is, how it is possible, how it is actually able to connect.Because here we have one 7216 10.10 IP.And here we have one 7217 0.1 IP.So how this connection is possible right.This is true.But here it is actually using Nat Nat in the as a internal part.So network address translation will happen automatically here.And because of this, they can communicate with each other even though they are from the different range.Now we are not going for the deep inside of the net and how it works, because it is not part of oursyllabus.It is a part of the Docker actually.Right.So you just understand that these two can communicate even though they are in the different networkbecause of the net is actually happening behind the scene.Right.Now.Let's go to the point number five.When task starts, Docker creates virtual Ethernet pair.One end connects to the task and other to the bridge.So let's say that I am going to start this task.So it is actually task is going to connect with the bridge.Right.Any task that you will create and you can select the bridge for your task as well.So multiple bridge creation is also possible.But right now we are just going with the one.So all the tasks that you will start, you are going to connect with this particular bridge.It is a virtual connectivity.There is no cable, right?The task gets internal IP from the bridge, not from the VPC.Now task will get IP address as well.So this bridge is going to provide IP.It is working like a DHCP.So one 7217 0.2 this task may get like one 7217 0.3.So bridge will assign IP address to the task.But these IP address are actually internal IP.It is not IP address of the host DNA right.Now the final step sees the task has no VPC ip x.Use port mapping.Accessing 172 1610 .18 80.Forward the traffic to the task internal one 7217 0.2.Let me explain you this with using very easy way.We see here, let's say that somebody sitting over here on the on your network, like he or she wantsto access a web application running inside this particular task on port number 80.So how it is going to access we have to set up port mapping how that I will explain you.But it will go through like this one 7216 1010 colon 8080.If somebody is going to send request on port number 8080 of my host, Eni means 172 1610 dot one 8080forward the traffic on.One 7217 0.2 colon 80.So you are going to get web application from this particular task.Now you may have one another question as well that okay but how to access this?Let's say that one another application running on the same port 80 over here as well.Right.So how I can access this now we need to change this port.So what we will do over here if somebody is going to access 172 1610 dot ten, there is no IP address.Ten because we have only one host Eni, one IP that we are sharing.And here we will say that if somebody is going to access 172 1610 1080 81, it was 8080 for this mappingwas 8080 means task 180, 80, 82 means task 280.So whenever the host Eni will receive one 7216 10.10 8081, it is going to send traffic on a task.280 means one 7217 0.3 colon 80.It is going to forward this right.So this is how like you are able to access this.Now you may have one last question here.That okay.But how we can define this mapping.I'm going to show you this as well.We are going to understand this practically.But let's understand this for a while right now.So see here I'm going to choose AWS VPC mode.Here we need to configure container.Here it is only asking me about the container port.Why?Because you have your dedicated Eni.You just need to specify that the port on which your web application running inside your container.So you just need to specify container port in the AWS VPC mode.But if I am going to choose bridge mode, look at this.It is asking me about host port.Host port means.Host IP plus 8080.This is host port means container host port.Now, if I'm going to get request on a host port 8080, send it to the 80 on that particular container.I can add multiple port as well.So 8081 will send traffic on to another task.But right now we are creating this particular task definition.So I can say that task one will have 8080.On a container port 80.For another task I can add 8081 on a container port 80 as well.Right.So this is how the things are actually working when you are going to use.Bridge mode.Now bridge mode is not so popular because in most of the cases when we are using ECS, we are usingAWS VPC.But yes, we must have knowledge.So I explain you the entire process.In the next video we are going to talk about default mode.Thank you very much.See you in the next video.Have a nice day.Goodbye.