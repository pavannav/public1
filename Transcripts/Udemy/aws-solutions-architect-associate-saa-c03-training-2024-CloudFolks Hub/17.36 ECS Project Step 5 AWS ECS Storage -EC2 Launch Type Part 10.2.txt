# 17.36 ECS Project Step 5 AWS ECS Storage -EC2 Launch Type Part 10.2

Hello and welcome to Cloud Folks Hub.My name is Bhavesh Katara.Right now you are learning storage options inside the ECS task definition.In task Definition, we have different storage options for Fargate and different storage options forEC2 launch type.We already discussed about all the storage options related to Fargate in the previous video.In this video, we are going to discuss about all the storage options related to the EC2 launch type.So let's start.Here I am inside my task definition.I just selected Amazon EC2 instance so I can get all the options related, all the storage options relatedto the EC2 launch type.I am going to explain you all this step by step through this particular mind map.The first thing that we need to understand about EC2 instance storage.Let me explain you.What is it?Local disk physically attached to your EC2 instance.You know when we are using EC2 launch type, EC2 is our container host and it is a part of our ECS cluster.Now in any container host because it is EC2 instance, we have EBS volume attached by default, right?So let's say that this EBS volume of my container host and the size of this volume is actually 20 GB.Now container host will use this EBS volume to store its own operating system as well as when you createany task.Ultimately task is actually creating container.Let's say that here I have created one container.So this container can also store data into this volume right.It is actually creating Writeable layer.And whenever you are going to store data into the container.Container is actually ultimately storing data into this EBS volume.Let's say that if I have 20 GB left, this container can store 20 GB data into this particular storage.But keep this in mind if you have any files or folders in your EBS volume for your container host operatingsystem.Let's say that here I have my Linux operating system and I have one app data folder.Right.My operating system of container host is created.This.It is actually in the file system of my Docker host.Right.Now your container cannot access this app data folder directly okay.So container actually creating isolated writable layer from the EBS volume.But it is isolated.So.containers cannot access file system of your container host by default.Okay, now you got complete idea about this.Let's talk about bind mount.You are going to get this particular option over here see bind mount.What is it.Mount a specific directory or file system from the EC2 instance storage into the container.So container can access or share that host level data persistent as long as the instance run.Because you are storing this like you have this data stored into the instance file system, right?Shared storage no can share the data between container in the same task.Yes.Mount required.Yes.Let me explain you.Let's say that I have this container C1.Now I want that this container can access this app data.So in this case I have to mount this particular directory from the C1 container.It is called bin mount.Now whenever I will mount this from C1, this app data directory will be accessible from my C1 containerright.So main use case of the bin mount is if you want to access data from the file system of the containerhost, you can use bin mount.Another use case is let's say that if I have C1 another container C2 as well, like I have multiplecontainer running inside the task.Now if I want to mount same folder from C2 as well, if data is mounted with C1 already now from C2,I am also going to mount.So this app data would be like a shared folder for both C1 and C2 container as well.So specifically two use cases we have for bin mount one to access the data from the container's hostfile system, and second is shared folder.Right.But you have to mount this.There is no automatic process how you can mount this.Here we have option bind mount.And then we have to provide source.Path means path of our app data path of our app data here.Then we have another option known as Docker volumes.See in Docker natively they supports both bin mount and now they supports volume as well right.Volume is specifically used to share data across the multiple container.Bin Mount is a traditional method or Docker volume is a new method.Bin mount.We have two use cases.First, I want to access data from the operating system's file system one, and it is also used to sharethe data between container Docker volume specifically used to share Volume between two containers.So I want one volume which can be shared by the multiple container.I will use this.Right.So what is it?Name docker manage volume share under where lib docker volumes specifically.This is a location where volume has been created inside the EC2 instance storage.Persistent persistent as long as instance run.Because here we are using file system of our container host.So if you delete this container this data will be there because this data is actually inside your containerhost right shared across the task.No because it is running inside your container.Host can share data between container in the same task.Yes.If you mount this volume with the multiple container it can be shared storage mount required.Yes you have to mount.And how you can mount this.Here we have mount options right now.You know these two is actually useful if you want to access data, or if you want to use EC2 instancelocal storage, right?If you want external, we have mainly EFS and FSX, but let's talk about the EBS as well.See I have a 20 GB volume in EBS.It is attached right.If I want more space I can increase capacity of this storage.Because in AWS you have this flexibility to increase size of your EBS volume.Or you can attach another volume as well.Let's say that I am going to attach another volume.Now if I want this another volume can be accessible from this containers as well.I can mount this volume from the container right.So if you have additional volume EBS volume which is attached with your container host, again you canaccess this From your container by using the same concept of build mode or Docker volume.Right.But these three options are basically for the internals.Storage system means the storage system which is connected with your Docker host.What about the external?Now if I am going to talk about external we have two options.Let me close these two.We have EFS Elastic File System which is one of the AWS service.If you want Linux file system storage you can create EFS volume.And then you can mount this with your container.So manage network file system accessible from multiple EC2 instance and task.And same way a fully managed file system FSX for Windows File server and FSX for persistent.Yes, because it is totally external.If you, uh are, stop your tasks.If you terminate your instance, the data which is stored inside these two storage will be always availableshared across the task.Yes, because it is external.If I'm going to talk about these four, it is actually used for the internal system means storage whichis directly connected with your container host.This is external.So yes it can be used to share across the task as well.Can share data between the container.Yes.If you mount this mount required definitely yes.Now when you will use EFS, if you have Linux based system, you can go with EFS.If your windows system then you can go with the FSX.So which kind of Docker host or container host you have based on this you can select one of them right.But both are kind of external and accessible from the multiple task and also persistent.Now there is a final thought about uh.EC2 launch type storage option, ephemeral storage configuration, which can be the size of 20 to 200like in Fargate, is not available.We already discuss about Fargate storage options in the previous video, and I explain you about theephemeral storage.Right now, ephemeral storage is only available with Fargate, right?So it is not available with EC2 launch type.This is like important point that you must know.And mounting is required for any external storage.If you want to mount EFS, EBS, you have to mount this so that container can access this.Okay, so I hope you got complete understanding about storage options available.Uh, during the task definition creation.Now we discuss about everything which is the most important part of task definition.In the next video we are going to create task definition for our project.So you will get better understanding about all these options that we already understood.Thank you very much.See you in the next video.Have a nice day.Goodbye.