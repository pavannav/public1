# 17.27 ECS Project Step 5 Host & None Network Mode Part 4.4

Hello and welcome to Cloud Folks Hub.My name is Bhavesh Edara.You are learning AWS Elastic Container Service.You already covered all these points for the ECS.And right now we are on step number five understanding ECS Task definition.If you go to the ECS console, you have to create task definition.And here we have lots of options available.So we started with the understanding of ECS task definition, task definition, family and launch type,operating system and architecture.We already covered all this.Right now we are understanding network mode.Here we have five network modes.Out of this we already completed AWS, VPC bridge and default mode.In this video we are going to cover two modes host and none.So let's start.Here I'm going to explain you host mode using mind map.And we are going to compare host mode with bridge mode to get better understanding about host mode.So first point about host mode.In host mode container does not use separate docker network.Here I have picture of bridge mode.We use this particular picture to understand bridge mode.And if you look at over here in the bridge mode, we have this virtual bridge created by Docker workslike a gateway.Now in the host mode we don't have this.So here I have this picture.And if I'm going to talk about bridge mode we have this gateway.But in the case of host mode we don't have this gateway.Now this gateway having one IP address, and this gateway also actually responsible to provide IP toall the containers connected with this particular bridge.So if I am saying that in the host mode we don't have this virtual bridge means.We have to delete this.And we also need to delete this IP, because this bridge is responsible to provide IP address to allthe containers.Now the question is okay if we don't have this bridge, how we are going to access any application runninginside this container.I'm going to explain you this step by step.It use same network as a system where it is running.So both use same IP address.So if I'm using host mode IP address of my host Eni will be IP address of my task if I am running twotasks means I will have same IP address for the both of this task, right?So here we can say like one 7216 10.10 one 7216 10.10 right.My containers are actually going to use same IP address as a host Eni.Now is it possible to provide same IP.Then how you are going to access this particular application.So here we need to provide port number and we just need to provide port number to the container.You know when we are using bridge mode we are actually going to map these things in the bridge mode.We have to provide host Eni port number.Here we have host Eni port number 8080.And it is going to connect container on one 7216 1010, 218.Right.So this is how we are actually mapping here.We don't need to provide host port number.Let me show you.So here I'm just going to choose bridge mode.Now in the bridge mode if I move the container setting it is asking me for host port.And container port means I'm saying that if anybody is going to send requests on port 8080, forwardthis on a container port number 80.But if I'm going to choose host mode over here, I do not need to provide IP address of the host.I just need to provide container port.So whatever the container you are going to create you need to provide IP port number.So let me say here I have this container and I am going to provide port number 80.Port number 80.Now if I want to access this particular application running inside this container.How I can access this?I'm going to explain you step by step.Let's say that if I'm going to use any external host and I'm going to use like this 172 1610 dot tencolon 20 2322.Now in this case, request will go over here.The host and I will understand that it is actually going to talk about SSH access.So host itself is providing SSH access.So you will get SSH access of your host itself.Now if you are going to write like this one 7216 10.10 colon 80.Then host and I will receive this request.And it is going to forward this request to this one because this container is actually using port number80.Now here we have one question.Let's say that I'm going to create another task.Can I run this particular task on port number 80?No, you can't do this.Why?Because one container is already using this port.Number 80.So you have to run this container on different port, right?You have to run this on port number 81.Now if you want to access this the process is simple one 7216 10.1 81.So here we don't have port mapping.Whatever the container port number is you're going to access this using IP address of host DNA.Here nothing is actually happening inside the Docker host.There is no routing is happening.Because if I'm going to use this particular bridge mode here, routing is actually happening.And here it is possible to run container on the same port because IP address is actually defining uniqueness.But we don't have bridge over here so we cannot provide unique IP addresses.So we are actually providing unique port number over here in the in the host mode.Because routing is actually not happening.It is directly accessing your host Eni compared to bridge mode, host mode is faster as well.Right now you may have one question over here.What will happen if I'm going to provide same port number to both of this container?Let me explain you this as well.Because of this, if one container is using port, in our case this container T1 is actually using portnumber 80.Another container cannot use the same port.If both try to use same port, error will come right?So you have to use different port.Over here in the bridge mode we have this flexibility.But here we don't have.Now if I'm going to talk about host mode it supports EC2 because Fargate only supports AWS VPC mode.So definitely other four modes are actually supported by only EC2, but specifically for Linux if youhave windows Container host.Again, it is not supported, right?So this is all about host mode.We compare this with the breed mode.So you got like a complete understanding about how the things are working.Now the last mode we are not going to create separate video.It is just a simple process.No external networking, mostly used for testing and special use case.So if you don't want your container to be accessible from outside world, you just select the mode.Again, it's supported by EC2.I hope you got complete understanding about network modes.Now.How network modes affect task definition.We are going to discuss about this in the next video.Thank you very much.See you in the next video.Have a nice day.Goodbye.