# 18.16 REST API Endpoint Type

Hello and welcome to cloud folks.My name is Bhavesh Adara.You are learning API gateway.If you are watching this video, it means you have successfully completed our first API gateway lab.In this particular lab we use http API.Now what we are doing now we are going to replace http api with rest API.It means we are going to use same components of previous lab like DynamoDB table API logics hosted onLambda.Everything will be same.We are just going to replace http api with rest API.Now rest API is actually complex than http API.So before we are going to create rest API, I want you to explore all the options that we are gettingwhile creating rest API.So step by step let's explore all the options first and then we will set up rest API.In this particular video I am going to talk about rest API endpoint types.Let's start.Right now.I am inside my AWS API gateway console.I'm going to click on create API.Now this time we want to go with the rest API.So I am going to choose rest API build option.Here I am getting very simple options like new API.If you want to create completely new API, you can choose this option if you have existing API, andif you want to copy and paste same logic here you can select clone.Clone API.You can import API from the file as well.And AWS is giving some example API as well that you can explore.You want to create new API.So I'm going to choose this one.Now we can give API name.The first important option that we are getting is API endpoint type.Now if you look over here we have three options regional, age optimized and private.Now what are the use cases of these three, and which option we can choose for our API.Let's understand this step by step using this mind map.First of all, it is an introduction as you know that it is actually supporting three endpoint types.Each endpoint types decide how client reach to your API and how AWS routes the requests inside its network.The first option we have is actually regional.So how regional endpoint works your API deployed in one AWS region.Right now I am inside AP South.One means Mumbai.So it is going to deploy my API into this particular region.User connect to your API directly over the internet and request goes straight to that particular region.Means if you want to access this API, you have to come to Mumbai flow client.Client can be anywhere.Now client is going to access my API gateway using internet, and then API gateway is going to triggerback end logic.It can be lambda or anything.Now this flow is very important because this flow is actually different for each types.Okay.You will get idea about it step by step.Now when to use it.Your users are near the AWS region.Low latency inside same region loop.I know that most of users coming from India, my application is actually specifically for Indian users.So in this case, if I am going to create regional API then no need to worry about it.But the problem or we can say that the limitation of regional endpoint is higher latency for users farfrom region.If users are actually coming from US or Europe, they need to connect.My India region means they have to come to India.So in this case there will be higher latency.User far away from the region may experience more latency right.So this is drawback of regional endpoint.Now to overcome this drawback we have age optimized endpoint.Now this age optimized endpoint is actually using global CDN network from Amazon.We call it CloudFront.So it is for international users.How it works.Your Rest API fronted by CloudFront automatically user hit the nearest CloudFront edge location.CloudFront routes the API gateway in your region.Yes, my API will be hosted in India, that's for sure.But we are going to use CloudFront over here.So user needs to connect to their local CloudFront pop location.And it is going to route this request to India.This will provide low latency access to the international users.Flow client.Now we have global client client going to connect nearest CloudFront and CloudFront.Pop is going to connect with API gateway, which is hosted on your specific region.When to use it public API with global users.Mobile apps with the user across the country's API needing low latency worldwide.Now if I'm going to talk about limitations, it is actually slightly expensive because here we are actuallyusing CloudFront.So we have to pay some more money related to CloudFront as well.So it is little more slightly expensive than your normal regional edge API setup might may be higherdue to CloudFront propagation.So it is going to take more time because CloudFront always taking time to propagate your process.Right.So yeah these two are actually limitation of aged optimized endpoint.Now we have third option.We call it private endpoint.Now this private endpoint will be accessible from VPC only.So these two are actually accessible over the internet, but this will be accessible from your VPC only.So how it works.Your API is not accessible on the public internet.It can be accessible using VPC endpoint.So if you want to access this particular private endpoint, you have to set up VPC endpoint.And using VPC endpoint you can access this API directly from the VPC.Okay.It is not going to add this particular gateway directly inside your VPC.Okay.You have to set up endpoint.So endpoint will provide connectivity between your API gateway and your VPC.If you look at over here the flow is you have your EC2 instance Lambda or ECS inside VPC.Now it is going to use VPC interface endpoint to access your API gateway.And your API gateway is private, so it will be accessible through this particular endpoint only.When to use it.Internal company API secure backend communication when internet access must be blocked.If I'm going to talk about limitations, we cannot say that it is a limitation.But just for your data, I am just talking about it only accessible inside VPC and must create VPC endpoint.Now this is a limitation you have to create VPC endpoint.But this is not limitation only accessible inside the VPC.Because it is specifically built to access from the VPC only.It is providing private connectivity, right.Okay.So these are the differences between all of these three options.Now one of the most important point that I want to tell you if you are creating one API right now I'mcreating rest API.You will have only one API endpoint, for example, for this particular API If I am going to createregional endpoint, then I cannot create age, optimize or private.Each API will have only one endpoint.Now if you have requirement that API can accessible from the internet and VPC only, in this case youcan create multiple API, one API for regional access and another API for your VPC access.Right?So this is possible, but one API cannot have multiple endpoint type.It can have only one endpoint type.Right.So this is all about API endpoint type.Now here we have one option security policy.Now what is the security policy.How it is actually working.We are going to discuss about this into the next video.As I explained you earlier, we'll talk about each and every options that we are getting while creatingour rest API first.And then we will create Rest API to test the functionality.Thank you very much.See you in the next video.Have a nice day.Goodbye.