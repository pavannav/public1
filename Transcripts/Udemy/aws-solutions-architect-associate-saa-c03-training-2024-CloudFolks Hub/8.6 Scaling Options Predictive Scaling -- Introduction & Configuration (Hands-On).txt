# 8.6 Scaling Options Predictive Scaling -- Introduction & Configuration (Hands-On)

Hello and welcome!We are learning AWS Auto Scaling.So far we have discussed about all these five points.In this video we are going to discuss about predictive scaling.If you want to learn AWS Auto Scaling from zero, please watch our previous videos.You will find out all the details about auto scaling.Now this video is specifically for predictive scaling.Right now I am going to my AWS console.Now when you go to the AWS console here we have auto scaling.I have configured one auto scaling group already.So we are going to use this.Now when you go to the ASG here you will find out automatic scaling.In the automatic scaling we have three types of scaling.Schedule scaling dynamic scaling policy and predictive scaling.We already discussed schedule scaling practically.Also dynamic scaling policy is also completed.Here we are going to discuss about what is predictive scaling policy and how it works.So I have this particular mind map.Let's understand this.First is introduction.Predictive scaling is kind of advanced feature that enables your cloud infrastructure, specificallyEC2 instance, to automatically scale in and scale out based on predicted traffic.Scale out means increase the number of EC2 instances, and scaling in means decrease the number of EC2instances.But here the main point is it is going to scale in and scale out based on predicted traffic.Now how it is going to predict the traffic, right.So here this is a main point by using ML machine learning model to analyze historical traffic data.So first of all it is going to analyze historical data.It requires at least three weeks to analyze this data.And based on this predictive scaling.Accurately forecasts the future demand and proactively manage the compute capacity of your auto scalinggroup.You can use predictive scaling for only forecasting or if you want to forecast, and plus you want autoscaling.To add resources means scale out and scale in.It is also able to perform this task.Proactive scaling can be used in conjunction with dynamic scaling and schedule scaling.If we talk about dynamic scaling.Dynamic scaling is kind of reactive, right?Let's say that if you got sudden spike of traffic instantly.Dynamic scaling can increase the capacity.But if we talk about predictive scaling it is kind of proactive.Okay.So it will analyze the pattern and it will find out that okay tomorrow at 10 p.m. or 10 a.m., you aregoing to get this kind of traffic so you can proactively manage this particular environment, and itis always a good idea to manage your environment using both proactive and reactive.So if you combine both of them, it works very well.So proactive scaling prepare for expected changes, offering more proactive approach.Now if you want to set up this okay, it is very difficult to provide you everything into the singleten minutes demo because here, you know, we have to provide them historical data.And based on this it is going to take a decision.So practically implementation of predictive scaling is a long term process okay.So I'm not going to provide you practical demo about it.But when you click on Create Predictive Scaling policy okay I'm going to explain you all these optionsproperly okay.I'm going to give you example for all of these options as well.So you will get better understanding and when you will work in the field you can implement this as well.So when you create policy or predictive scaling policy, first of all you have to provide name.Let's say that.Policy one.Now here we have turn on scaling.So let's discuss about this four options step by step.The first option is scale based on the forecast.Now if you look at this you can on it and off it right now if you own if you if you own it then whathappens?Predictive scaling will automatically adjust the number of instances in your auto scaling group basedon the prediction of the future traffic.Okay, now if you off this, it means we are using Predictive scaling on a forecasting mode.It means it will forecast the about the traffic, but it is not going to add or remove our EC2 instance.So scaling is not enabled.Just we are using this forecasting for our our information.Right.So predictive scaling will make a prediction about the future traffic.But it will not take any automatic action to scale your resources.So it is now up to you that do you want to scale or not.You can go with the manual or you can go with the dynamic.But here we are using this forecast data okay.So that's why we have two options.We can we can forecast only and scale as well if you want to uh, if you want that AWS auto scalingcan increase and decrease instances for you.Now here we have another options.Now if you look at this we have two options metric Metrics and target utilization.So let's talk about both of these.First of all metrics.The chosen metric is what the system will analyze historically and predict the future needs.This could be CPU utilization network I o request count per target, or any relevant metric that reflectsthe load or performance of your instance.See if you have application server.In this case, main components is CPU utilization.Let's say that if you have a database server, then the memory utilization is a main component for thedatabase server, right?So based on your application server or the database server or a web server, which kind of server youhave, you can select the metrics and auto scaling will use this particular metric to analyze the data.Let's say that if I am selecting CPU utilization over here, it means auto scaling will concentrateon predictive scaling, will concentrate on the CPU utilization, it will only verify, or it will onlyconsider a CPU utilization for the data analysis to analyze the data.Okay.Now here you have networking byte.You have network out byte.You can also uh analyze application load balancer request.You can also set up the custom.There are many options over here okay.But most of the cases we are always going for the CPU utilization.So we want to collect historical data of my EC2 instance.And we want to collect CPU utilization okay.That is what we can say based on this.Now target utilization.Now this is the most important part that you need to understand guys okay.But let's finish the metric first then.Now here it is.Example.If you choose CPU utilization, the algorithm will look at the past CPU usage trends to forecast thefuture CPU requirement.Now it will consider only CPU.Okay, now let's talk about the target utilization.Target utilization refers to the desired level of resources.Resource usage that you want to maintain.It's a threshold that guide the scaling process.Now let's take an example.If the metric is CPU utilization right now we have this right.The metric is CPU utilization.Setting a target utilization of 50%.Let's say that here we have the 50%, right.We are going with the default option.Means you are indicating that idle operational state is when each server is around 50% of its availableCPU power.It means you are informing your auto scaling group that if 50% of.Per instance is going on, we don't have any problem.If it is utilizing more.Than 50% like 6,070%, then we want that to add new instances.If it is going below 50% then scaling remove the instances.So what you want right.That you are deciding over here.And you can go up to 50%, 60% 70%.Now percentage is totally based on your application requirements okay.So this is what we call it a target utilization.Now when you go below this target utilization here you will get additional scaling setting and pre-launchinstances okay.So let's discuss about this as well.As we know that this is proactive.Right.So this setting configure the amount of time by which the instance launch time can be advanced.So this setting configures the amount of time by which the instance launch type can be advanced.Okay.So let me explain you this because it is a proactive approach.So let's say that see the forecast says to add capacity at 10 a.m. okay.My forecast is saying that tomorrow 10 a.m. you will get more traffic okay.And you need to add more EC2 instance.And you choose to preload the instance by five minutes.So let's say that here I said five minutes, which is default, right.In the case the instance will be launched at 9:55 a.m..Okay.So first of all, we are predicting that there is a spike in the traffic based on our historical data.Tomorrow at 10 a.m. and we are launching our instance at 950.We have one hour for advance.Okay.Because the maximum time is 60s.So this is like real proactive approach.Now the last option we have known as buffer maximum capacity above the forecast capacity.Okay.All these are very interesting.And we need to understand.And that's why I'm giving you perfect example for this as well.Now buffer maximum capacity above the forecast capacity.Let us discuss about this.This is extra percentage or number of instances that you choose to add on top of the forecast capacity.For example, suppose predictive scaling algorithm forecasts that you need ten instances to meet thedemand at certain time.If you set the buffer maximum capacity of 20%.Let us say that if I am going over here and I am going to say I want 20%.Okay.It will actually provision 12 instance.Why?Because ten is requirement again proactive.We want more than 10% ten which is like 20%.We want more.So ten plus 20% of ten will be two extra.So now it is going to create ten plus 212 EC2 instance.So these are like all options okay.We are very sorry that because it is using historical data and all this okay.If I am going to click on create there is no effect for this.So that's why we are not going to create this particular scaling policy right now.But I hope you got better understanding about this.If you have any query, if you have any question please feel free to contact us anytime.Thank you very much.See you in the next video.Have a nice day.Goodbye.