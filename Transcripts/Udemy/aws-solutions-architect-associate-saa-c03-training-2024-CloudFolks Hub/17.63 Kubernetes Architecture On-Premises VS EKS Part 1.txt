# 17.63 Kubernetes Architecture On-Premises VS EKS Part 1

Hello and welcome to Cloud Fox Hub.My name is Bhavesh Katara.Now we are going to discuss about Kubernetes architecture.Here we are going to discuss about most important components of Kubernetes.And we are also going to discuss about how it is going to run on premises versus EKS.This point is most important point.I want you to understand this point step by step.That's why we are going to divide this particular topic into two parts.This video is a part one of Kubernetes architecture on premises versus EKS.So let's start.In this particular part one we are going to discuss about control plane, worker node and pods and containers.I am going to start with control plane.What is control plane.The control plane is a brain of Kubernetes.It makes all the decisions where to run ports, how many replicas, when to restart, and how to scale.I know that you guys are not aware about ports.Now let me give you a brief introduction about ports.Ports are basically smallest units in Kubernetes, which is actually going to run your container inone port.You may have multiple containers running.Right now you can say that ports is like containers.Okay.So if I'm going to talk about control plane.Control plane makes all the decisions about ports where to run ports.You may have multiple servers.So control plane is going to decide in which particular container host you want to run.This particular ports.How many replicas of ports you want.When to restart your ports.Everything will be decided by control plane.Now here I have this particular picture.Let's say that you want to set up your Kubernetes infrastructure on premises.The first thing is you have to set up control plane.Now control plane contain all these components.So how you are going to start this entire process.First of all you have to set up one machine.It can be virtual machine.It can be physical machine.Then you have to install required operating system.Let's say that I am going to install Linux.Then you need to install all these components of Kubernetes control plane.Once you install all these components this particular server will be works like a control plane.So you have to install all these components step by step.So let's discuss about all the components You install and manage all control plane components like youare responsible to manage this because you want to set up your, uh, Kubernetes on premises.The first component we have is API server.It is main entry point for the commands.You know, if you want to manage your Kubernetes environment you have to use command line utility.Uh, Kubernetes command line utility is known as kube control.Or some people also call it kube cutter.So you can manage entire control plane using this command line utility known as kube control.And kube control is actually going to communicate with the control plane through the API server.There is one another component of control plane known as scheduler.Decide which node runs which pod.As you know that we are running our containers inside port, and port is going to run inside workernode.So cellular is actually responsible to decide which ports is going to run on which particular workernode.Then we have control manager.If the cluster in desired state we have all the components of the cluster is in running state will bedecided by the controller manager.Then we have one etcd.It is a kind of database which is actually going to store cluster state.So all these components we are going to install into one particular physical machine or virtual machinewe call it control plane.Now if you have your on premises infrastructure you handle high availability of control plane.You are responsible to upgrade your control plane patching and backups.All these are actually responsibility of Kubernetes administrator.But here we have one good advantage of using AWS EKS when we are going to use X, we are going to getsame control plane.We are going to get all these components as well.But AWS runs entire control plane for you.It is multi-az secure and auto scale.So we are not responsible for scaling.We are not responsible for high availability.AWS is actually going to provide us ready to use or manage control plan to us when we are using EKS,you don't worry about server or etcd reliability because AWS is going to manage all of this.Now here what we can say that advantages of EKS no operational headache AWS guarantees high and securityof your control plane.Right.So you know the main complexity of Kubernetes is to set up control plane.Now when we are using EKS this portion is totally removed.Now let's talk about another component of Kubernetes.We call it worker nodes.Definition.Worker nodes are the machine where you, your application container actually runs.You know, control plane is actually managing worker nodes and ports running inside worker nodes.We are not going to run any ports or container inside control panel.So we are running our ports inside worker nodes.Control plane is just going to manage all these worker nodes and ports running inside this worker nodes.So actually our containers are running inside the worker nodes.Now in the on premises infrastructure.These are my worker nodes.And you provide and manage servers.So you have to set up servers.Let's say that if I want to set up my on premise infrastructure, I have to set up one server, whichis actually my control plane.Then I have to set up multiple other servers, which is actually my worker nodes.Here I have to install a Linux operating system.Then I have to install some components of worker nodes.Then I have to connect all these worker nodes with my control plane.Right.So this is how we are going to set up entire Kubernetes cluster.You must install Cubelet cube proxy container runtime inside the worker node you manage patching scalinghealth check C we are going to install like we are responsible to install all these components.We are also going to uh, manage all these worker nodes.We have to maintain a routine process of patching, scaling, health, check everything.If we have this entire infrastructure on premises.But what if I'm going to use X here?We are going to get two kinds of worker nodes.It is like same we have in the X as well.So we are going to get EC2 and even we are going to get serverless worker nodes.We call it Fargate.So worker nodes can be easy to node group.You manage infrastructure and Fargate fully.Serverless worker node scaling is integrated with AWS Auto scaling.Look if I'm going to talk about Fargate, it is a fully serverless.So we are not responsible to manage any kind of scaling.But if I am going to use EC2, I have to scale my infrastructure as well because I'm using EC2 instancesover here.But as you know that if you are using EC2, you can manage scaling using EC2 auto scaling.So they are actually giving us facility to use AWS EC2 Auto Scaling in order to scale your worker nodesin EKS.Advantages of EKS flexible.Choose EC2 for control and Fargate for serverless.Simplicity.You know if you want to go with serverless, there is like one of the best option we have for Fargate.But there are so many companies who want to control their worker nodes.They wants to set a boundary for their worker nodes or resources.Now, in this case, they can use EC2 as a worker node in the EKS.Now let's talk about ports.Ports and container.Definition.A port in this is the smallest unit in the Kubernetes a pod is in.EKS is like a task in EKS.We already completed entire series of ECS and now we are aware about task in ECS.So in the ECS we are creating tasks which is actually containing multiple containers.Here in the ECS we are creating Pod.Both are the smallest unit that actually runs one or more containers.Pods run inside the worker node.Here.As you know, that control plane is used to manage worker nodes and ports, but we are not going torun any ports inside worker inside control plane.So all our ports we are going to run inside our worker nodes.But control plane having command on all the ports that we have running inside worker node.On premises you create ports and must ensure infra is ready.If I'm going to talk about ECS, same port concept, we have to create ports using kube control.We are going to use same Kubernetes command line to create port.Yeah, that is the one advantage of using ECS.AWS manages infra scaling behind it.Let's say that if I have two worker nodes and I am running so many ports inside this worker node, now,I want to scale my worker node so it can provide better infrastructure to ports.Now in this case, if you have on premises, you are responsible to scale your worker nodes.Here in the ECS, you can easily scale your worker nodes because AWS is providing autoscaling for EC2and for Fargate.It is automatic scaling, so worker node scaling would be very easy if you are using EKS and we canrun any number of ports inside our worker nodes.Advantage of EKS.You focus only on deploying ports.We are going to use same Kubernetes command line utility as we have in on premises infrastructure.So there is no differences.But yes AWS handles scaling infrastructure in case of ECS.So this is like most important part of Kubernetes.We understood this how we are going to use this, all the components on premises and how we are goingto use this into the EKS.Now we have to discuss about another four components as well.But we are going to discuss about all these components into the part two of this particular video.Thank you very much.Goodbye.