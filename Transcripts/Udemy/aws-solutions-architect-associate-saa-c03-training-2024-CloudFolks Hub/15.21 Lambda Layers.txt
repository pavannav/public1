# 15.21 Lambda Layers

Hello and welcome to Cloud Fork Sub.My name is Bhavesh Edara and as you know that our Lambda series is going on.Today in this video we are going to talk about lambdas most important feature known as layers.How layers works and how we can utilize this functionality.We are going to discuss everything related to AWS Lambda layers in this video.So let's start.Now before I'm going to start this, I want you to just understand this terminology module and libraryas a prerequisite.You know if you have coding background you are aware about programming languages.Definitely.You have clear understanding about modules and libraries.But if you don't have this background or if you don't have knowledge of modules and library, understandthis first and then we will go for the, uh, lambda layers.Because if you don't know about this one, it would be problematic for you to understand layers okay.So let's understand modules and library.Let's start with the definition.A module or library is a collection of code that we can import and reuse in our programs or lambda functions.So we don't have to write the code again and again.You know, it is kind of code reuse reusability.If you want to use same code again and again, you can convert this code into the module and then youcan use it.There are many modules available, like there are thousands of modules available that you can add directlyin your program.And you can use this now here I use this two terminology.So let's understand the differences between them.A module is usually single file with reusable code and libraries is a collection of modules.Now, for a language like Python, there are millions of libraries available that you can directly importin your program, and you can use it using libraries.And module is actually smart work because you do not need to write code which is readily available.Here I want to give you an example of the of the very well known famous library known as Boto3.Now Boto3 is actually a Python library developed by AWS itself that that you can use in your program.And from your Python program or Python function, you can directly interact with AWS environment.Now, whenever you are going to use this kind of readily available code, we call it like library orum or module.You have to import this, right?So if you look at this here, I have one code which is going to copy file from our S3 bucket.Now here I'm using this import Boto3.Why we need to write this.Because this is how you can inform Lambda or a Python program that we need to use this Boto3 libraryhere.So you have to write this import Boto3.If you are not going to write this Python or or lambda function, you can not able to understand that.What is this code you have right now?Important note in AWS Lambda you must import the library in your function to use it.Some libraries like Boto3 are built in and available by default.But if library is not built in like like request, Panda, etc., you must either package it with yourfunction and provide it with the lambda.Now see, we have to import this library because the code must be in your system in order to run themright.So you have to write import over here.And this code must be installed in your in your local computer or maybe in the lambda as well.Some are predefined which is readily available, but some codes are not predefined.Now now let's understand the whole process.But I hope after this you got complete understanding about modules and library right.Modules and library actually saving over time.We have standard code written.We are just going to use this in our lambda function or program.Right?Now let's understand the definition of lambda layer.Lambda layer is a feature in AWS Lambda that allow you to store and manage common code, modules, orlibraries separately from your main function code.If you are going to use any libraries, you have to add this library.Or we can say that install this library.Right.But using lambda layers you can make them separate.And making them separate is a good choice.Because once you add this library you can use with multiple function, you can attach a layers to oneor more lambda functions to reuse the common logic without repeating in every function.This helps keep your function lightweight, cleaner, and easier to maintain.Now see these three lines.Not easy to understand for non programming background.If you are programming background you can easily understand this.I.I completely agree with this.So what I'm doing I'm going to give you very very easy example.So even if you don't have programming background you can able to understand.So let's start with the built in library means libraries which is readily available in Lambda right.So here.Step one.You write the code.You write the small Python function that generates the random number between 1 to 10 using a libraryor a module called random.So here see the first line import random.Then I'm just going to use this function.Now this function is going to provide us a random number between 1 to 10.Now if you want to use this particular function or a library, you have to find out that okay, thislibrary is actually available as a standard library in Lambda or not.If it is, you don't need to worry.If it is not, you have to go with some process.Let's understand.This is your complete Lambda function.Now you deploy the function in AWS Lambda.You paste this code in the AWS Lambda console and click deploy.No need to install or upload anything extra.Why?Because import random is readily available.You click test and execute your function.Since random is a part of Python's built in standard library, it is already available in the Lambda'senvironment.This random library is already part of standard Python library, so you don't need to import this.You don't need to worry about anything related to this particular library.Lambda will manage this automatically.Lambda executed.Lambda import the random module from the built in library.It called random 1 to 10, which generates the random number.No errors, no layers, no packaging is needed.Everything is like very normal process.But, But you know, there are like millions of modules and libraries.Now, we cannot add everything as a standard library because everybody needs to use different libraries.Let's say that if I'm actually building application related to AI and ML.Now in this case I have to import all the libraries, which is supported by AI and ML.If I am going to develop some application which is used to manage my AWS environment now AIML libraryis not my choice.My choice will be Boto3 library, right?So in this case, if they are going to add each and every library as a standard, it would be problematic.So in the standard we have some libraries, but some libraries are not available as a standard library.Now in this case how we can manage them.That is what I'm actually explaining you.So I already explained you about built in means your library is a part of Standard The library.No need to worry.But what if I have library which is not part of standard library?So library using layers.Now here you write the Lambda function.Again you are writing the basic lambda function that sends a confirmation message.Now here the library that I'm using is actually emoji.Look at this.In my code I have import emoji written.Now because of this what will happen?You will get like you will you can send message.Your bill is ready with this kind of emoji.If you look at the output, you will notice that we have added an emoji at the end of the message.To do this, we are using very famous python library called emoji that helps us to display emoji usingsimple text code like check mark button.See here.I just wrote this check mark button and I got the emoji of this check mark button because of this librarycalled emoji.But here the important part.Even though this library is well known, it is not built into AWS Lambda by default like random library.It is already there.It is a built in library, so we use this directly.But if you want to use this emoji library, it is not a part of built in.Right.So you try to run this code in Lambda without adding emoji library.It will fail.Now if you copy and paste this code directly into the lambda, you will get this kind of error becausethis library is not available in the lambda.So you have to install this library into the lambda first and then you can use this.Otherwise you are going to get this module not found error right.Now how to solve this problem.Now if you want to solve this problem we have two ways.We are not going to use layers.We are not using smart way The first one package the library with your function code.And the second way is go with the smart way.Understand the technology and use them.That is what we call it.Lambda layer.Now we are going to discuss about both of these.So your lambda layer concept will be very clear.So let's understand solution solution number one which is not recommended.Pick the library with your function code.Now here what we will do.Install the library locally.See whatever the library you want.First of all you have to install locally.Locally means in your own computer.You may ask question to me that okay, but I want to run this into the AWS Lambda environment.Why do I need to install this locally?I'm going to explain you.First of all you need to install locally.Now you may have one another question like how to install this.That is what the practical is that we are going to perform in the next video.Right now understand the theory first, so don't worry about how to install it and how to manage it.First of all, you need to install library locally on your coder's laptop or computer.Create a one zip file that include both.Now you have your code and library, so you are going to include both of them into the zip file, right?So you have zip file in which you have your function code and library code or a library.Upload this combine zip to lambda and then it is done.So what I am doing here, let's say that I have function one.So I have a code plus my library.Both combine as a zip file and I added here into the function one.How to import zip file that we will go for the practical.Don't worry.And now I can run my function because my functions requirement to have a emoji library that I alreadyadded here and now it is working.Right now the problem here is let's say that I want to use this emoji here as well.How I can use this?Because the problem is you cannot directly use this here.So here you have to write the code.Plus you have to add emoji library and then use with the function two.Now I want to use with function three.Again you have to create zip file code plus library and use this.So for each and every functions that you are using you have to go with the same process.So let's understand the drawback.Every time you update the code or create new function, you will need to repackage and re-upload theentire library again.This become repetitive task and hard to manage.And then if you want to upgrade library again, you will be in a problem because you have to go to eachand every function and use library Any, uh, update library separately because you are using separatelibrary for each and every functions that you are running in the lambda, right?Not the smart way.We should not go with this particular option.Right?If you are going with this, then the operation overall will be very high and you have to perform likerepetitive task.So what is the best idea?Use lambda layers.Now let me explain.You install the library locally again.We need to use this library.So we have to install this locally on our laptop or desktop.Create a zip file that include only library.Now look at this.Last time we added both function code and library.This time I'm just going to add library.Now upload the zip file as a lambda layer.So we have to go to the layers.And here we will upload our zip file which contain only emoji library Okay emoji library.Now how to do this?That is like a practical part.In the next video you are going to perform this lab, so don't worry.Now then edit the layer to your lambda function separately.Now the process is simple.Layers is already ready.Now you just need to verify that okay I have this function here I have the function code.Now do I need emoji library here?If yes, attach this layer with function one.Okay.With this do we need emoji library?Yes.Attach this function that's done.In order to update this library or anything.You just need to go to this here.You can update this and it is going to update with all the functions as well.So this will be the smart choice now.Advantages.Reusability.You can reuse the same library again and again with any function clear code.Your code and library are totally separate, so your code structure will be very clear, easy to maintainbecause you are maintaining code separately and library separately.So if you want to update the code, it is not going to affect your library.Or if you're going to update library, it is not going to affect your code.And deployment time will be faster as well, because your code will be very small and it is very easyand quicker to upload because you know, there are some library which is really very heavy, right?Like pandas and OpenCV.Uh, is, is is like a big library.So it is going to create zip file and upload them along with the code will take time here.It is not going to disturb this.So this is all about.Lambda layers.Now you will get 100% understanding once you will perform the lab right in the next video we are goingto perform this lab.Thank you very much.See you in the next video.Have a nice day.Goodbye.