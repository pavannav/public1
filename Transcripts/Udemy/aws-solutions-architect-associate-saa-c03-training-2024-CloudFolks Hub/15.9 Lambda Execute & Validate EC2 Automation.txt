# 15.9 Lambda Execute & Validate EC2 Automation

Hello and welcome to Cloud Folks Hub.As you know that our Lambda series is going on, and in this video we are going to create one lambdafunction.This lambda function is really very interesting because using this lambda function we are going to manageour AWS environment.So let's start.As always we are going to start with introduction.In AWS Lambda we can write functions to automate real world tasks such as sending SMS alert for OTPverification, resizing image when uploaded on a website, processing orders in e-commerce, generatinginvoice for online purchase, and sending reminders for upcoming appointments.Look at this.All are event based.We will definitely talk about this kind of functions as well.We will create this, but if you want to create this type of function, the process is really very complexand I don't want to give you high dose within starting of our Lambda series, we are going to understandall this.But step by step here we are going to make some process easy.We are writing some basic function to start EC2 instance using Lambda Python function to understandhow Lambda works and how we can use it for automation.Yes, we are not going for all this complex tasks here.We are going to create one basic Python function, which is going to start my EC2 instance automatically.Now you may have one question here as well that why you are using Python.Yes, I am using Python for this particular function because I want you to be aware about Boto3 library.See, Python is really very old language, right?AWS started in 2007, but using Python, we are able to manage whole AWS environment.Why?Because AWS developed a specific library as a part of this AWS SDK known as Boto3.Using Boto3 library, you can manage your entire AWS environment using Python.Okay.And yes, as we are going to start our EC2 instance automatically using Python function, we need touse Boto3 library.Boto3 allows us to automate and manage any AWS services easily.It provides simple function to interact with AWS resources like EC2, S3, and DynamoDB.We are using Python with Boto3 to write basic function to start EC2 instance and understanding how Lambdafunction works with the automation.So here we are going to talk about Boto3.We are also going to talk about system administration.And definitely we are going to make the whole process automatic as well.Right?So this is what we are going to set up here in this particular video.Now let's start with the lab execution.Before I am going to start lab execution we have to understand prerequisites.Now this is very important part.Before writing the lambda function we need to create an EC2 instance an IAM role.Now definitely you guys are okay with this that we need to have an EC2 instance that we are going tostop with this particular Lambda function.So yeah, we must have this here.I provided you all the details.You can create your EC2 instance using your own way.I have my region.It is AP South one A.And here I have this state v test VM.I already created this because it is the same process to create a virtual machine.Here my instance is stop set.Why?Because we want to start this using lambda function.So I created this instance and stopped it.Okay.Now the other process we have is to create IAM role.Now you are aware about like why we need to create EC2 instance.That is completely okay.But you guys may have one question here.That why we are going to create IAM role right.So I'm going to explain you this and then we are going to create this.So in this step we will create IAM role that allows Lambda functions to interact with AWS service.Why we need to create execution role.AWS Lambda runs code without server but needs permission to access AWS services.By default, Lambda has no permission to allow it to manage EC2.We need to create IAM role with the right permission.This role act as an identity for Lambda, giving the ability to start the EC2 instance.Now let me explain you this using whiteboard.So it will be very easy for you to see what is happening over here?Right.So we have one EC2 instance, right?And we have one lambda function.Now this lambda function is going to this EC2 instance.And it is saying that hey EC2 instance start.Now according to AWS all these services are isolated from each other.So if Lambda is going to the EC2 instance and if it is giving instructions to the EC2 instance, start,this EC2 instance will definitely ask him why.Who gave you this particular permission?Right?So in this case, one AWS service is actually accessing another AWS service, right?So in this case we need to create IAM role.So here we will create one IAM role which is having full EC2 access.And this role will be attached with Lambda.This role will give identity to the lambda that okay you can manage our EC2 environment.Okay.So this is what we required right now.Here I provided you all these steps that how you can create this particular role.So here I'm just going to start this process.See in my map I am giving you everything.Why.Because you guys just follow this mind map after watching this video.So my idea is watch video entirely first time.And then here we have step by step that you can execute.So you do not need to watch video forward or pause or anything.You can directly perform this lab using mind map.Okay.So here I am going to create this.And this will be the role name.So how I can do this.First of all I have to go to the IAM.Here I have to click on roles.I have to create new role.So create role.Yes I am giving this creating this role to give permission to the AWS service.AWS service.Now which service?I'm going to give permission to Lambda.So here I am going to choose Lambda.Next here we need to give permission.The first permission we have is to give EC2 full access.Right now we can give specific permission by creating our own policy.But we are not going to do this because right now our aim is to understand lambda, not IAM.If you want to understand IAM.You can watch our all the videos related to IAM.So we have this playlist as well.I hope you have completed this.So this is like I give full access.Now another one we have to give is as well which is known as basic Lambda basic execution role.Now you may have question as well here that okay we are giving EC2 full access.That is completely okay.But why you are giving this particular Lambda execution role Permission.See, once you execute your lambda function, the old logs will be stored in the CloudWatch.So you know, if you are not giving this particular permission of Lambda basic execution role, whatwill happen?You are not able to get all the logs related to your function into the CloudWatch.Because by giving this particular permission, we are giving our Lambda function to write the log intothe CloudWatch.So that's why it is required.Okay.Next.Here I am giving name of the role.It is Lambda EC2 role and let me click on Create Role.So now my role has been created and now prerequisites is also done.Everything is clear.Now let's start with create Lambda function.Here we have three step process.The first step is to set up everything.Here I already provided you this detail.I'm just going to execute this directly.So going to the lambda.Now here I don't have any functions.So create new function author from scratch.Here I just provide a name.So I'm just going to use this as well right.My function is in Python 3.1.So I'm going to choose this.Here we have to choose create existing role.You already created this role Lambda EC2 role.So I'm just going to choose this right.So in prerequisites you already find it out.So create function.So I can say that all this is done.Now we need to write function.So here I provided you this function ready to use.If you don't know Python don't worry.You just need to remember 2 to 3 things.First here I'm using Boto3 library.Why we are using Boto3 library because it is a library developed by AWS to manage your AWS environmentusing Lambda.So I'm using this Boto3 library here.It is a functions.You have to create functions that already I explain you in earlier days, then if you have your EC2instance in AP South one, you have to write AP South one here.Like if you are having resources in other region, you can add region name.You have to add this and then you have to add your instance ID.I'm telling you that don't worry about this lambda function or Python function.Why?Because I'm going to tell you that how we can write this function using I.Right?So in upcoming days we are going to write this function using I.We are not going to write this.So don't worry even if you don't know using I agent we can write this easily.Okay so here I'm going to copy this.Now here in the code area I have to paste it.So first of all I have to remove this sample code okay.So I'm going to select this removed.And here I'm going to paste this.Now I have my EC2 instance already in AP South one, so no need to worry here.I'm going to remove this.And here I'm going to copy this instance id.So this is instance id.Here I am going to copy this then.Now I have to deploy this function.Updating the function successfully updated the function EC2 function.Now see after writing your function you have to make some necessary changes according to your functionrequirement.For example, let's say that here.If you look at this, this function is going to be timeout within three seconds.Now within three seconds your lambda function cannot able to start your EC2 instance.So it can be timeout.So in this case what you can do here you can increase the timeout.So you can increase the timeout to 10s.So within 10s Definitely it is going to start my EC2 instance.So this is what I have changed here.You don't need to worry about this.I already provided you this detail over here as well, right?So now it is done.Our lambda function is ready.Now we have to create test event.So in this case how we can create test event.So you can go to the test.Here you can give test event name.Let's say that I have this test event name start EC2 test.Okay.And you can save this.Here we are not going to use any JSON or any value over here in upcoming days.Definitely we will work with on it as well.But what I can say that it is now saved.Okay.Run the test.Now we are going to run this test.But before this we just need to confirm that here we have our EC2 instance and the instance is in stockstate.Now here I'm going to test this event.Okay this is event name.And let me click on test execution function started.It is going to get maximum 10s.And look at this function is succeed.Here it is providing all the details requested.Starting EC2 instance EC2 instance started.Everything is good.And if you go to this EC2 console here you will find out that your EC2 instance is running state.So yes, our function is run successfully.Now you can verify this using two ways.Of course.The first is very easy.You can directly go to your EC2 console and you can verify that instance is running.Another one is you can go to the monitor.Here you can click on view CloudWatch logs.And here because our Lambda execution role we are going to get this detail.Here you will get one log.And if you look at this you are going to get the same log.Say EC2 instance started instance started successfully.So step one and two is actually printed that we have added instructions here as well.So if you look at this code code here we are going to print two things which is print n and we are gettingit into the CloudWatch log.Now we are going to add some automation into this particular process as well.Here we are just testing function by manually creating event.No, we are not going to setup this in upcoming days.We want that this trigger should be automatic.We want that at every a every day.9 a.m. my EC2 instance start automatically at every day at 6 p.m. it is going to stop automatically.So we are going to provide some automation.We are going to add event trigger into this particular process as well.But for this you have to watch next video.Thank you very much.See you in the next video.Have a nice day.Goodbye.