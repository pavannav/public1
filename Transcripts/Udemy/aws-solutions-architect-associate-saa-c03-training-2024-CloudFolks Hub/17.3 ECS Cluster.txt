# 17.3 ECS Cluster

Hello and welcome to Cloud Fox.My name is Bhavesh Katara.Now we already started our ECS training.In this video we are going to understand ECS cluster.We need to understand this because this is first starting point of setting up ECS environment.Here I am inside my AWS console.I'm going to search ECS service and going to the ECS console.Now once you go to the ECS console, you are going to get Get Started tab.Once you click over here, the first tab is to set up cluster.Now what is this cluster?I'm going to explain you in this particular video.So let's start.First of all let's start with definition.An ECS cluster represents the group of Docker host acting as a unified environment for container orchestration,let's use the same example that I explained you during the prerequisites video.Here, let's assume that I have only docker host one inside this docker host one I have multiple containerhosted like C1, C2, and C3.Now if I have only one docker host, it is a problematic because this can be single point of failure.If any reason if my docker host one goes down my all the three containers will be down as well.Now I want high availability.I have a requirement of 100% uptime.In this case, if my docker host one goes down I will be in trouble.To provide redundancy you can set up multiple docker host.For example, I have docker host one, docker host two, and docker host three.In case of failure of docker host one, I can run my containers from docker host 2 or 3.But if you just create Docker host separately, and if there is no coordination between them, how Dockerhost two and three will find out.There is a problem with docker host one.So if you have multiple docker host, we must have to set up coordination between them.We want that this Docker host can communicate with each other and they work as a single team members.In this case we must have container orchestration service like ECS.Now once you set up container orchestration service like ECS, the first step is to set up cluster.Now once you set up cluster inside this cluster you are going to setup Docker host.It can be Docker host 2 or 3, but at minimum two are required.Once you add docker host multiple docker host inside the cluster, they will act like a team.Now all the coordination will be defined by container orchestration service.Now container orchestration service will schedule all the containers inside this Docker host.So it will find out that okay Docker host one you are going to run C1 container Docker host two youare going to run C2 container docker host three you are going to run C3 container.So all this schedules and how container is going to run inside this cluster environment will be definedby container orchestration service.So it is very important as a first step of setting up the ECS to set up cluster and add multiple Dockerhosts inside the cluster.Once you add multiple Docker host inside the cluster, because all these hosts are member of singlecluster, they will act like a team.It provides resource pool where ECS can schedule and run containerized application.Once you will add all these Docker hosts inside the cluster, the resource will be combined together,right?So let's say that if I have four vcpu for the docker host, one another for vcpu for docker host two,and then four for the docker host three, it is going to combine all the resources.We can say that I have cluster with 12 Vcpu right?So it is going to create resource pool.Once you create or add docker host inside your cluster, creating cluster is required because ECS needa centralized control point and manage task resource isolation and service scaling.Now what is this task placement?What is this service scaling?We are going to discuss about all of this.Right now.You can say that cluster is required because once you create cluster all the Docker hosts can communicatelike a team and they can run containers inside this particular cluster, you can run multiple servicesand tasks inside an ECS cluster.Now, ECS supports two types of cluster infrastructure for running containers one Amazon EC2 and AWSFargate.Look it over here.I'm going to click on Create Cluster here.The first step is to provide cluster name.I'm going to provide name like my cluster.The second option I have is to select infrastructure.I can choose AWS Fargate which is providing me serverless option.And another one is Amazon instances as well.Now what is the difference between AWS Fargate and Amazon EC2 instances?Can I combine both of them or what we are going to discuss about all of this in the next video?If you look at the title over here, this is about ECS cluster and infrastructure.I already explained you about ECS cluster.Now we are going to discuss about infrastructure in next video.Thank you very much.See you in the next video.Have a nice day.Goodbye.