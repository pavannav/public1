# 15.10 Lambda Trigger

Hello and welcome to Cloud Folks Hub.Right now, our Lambda with AI agent series is going on.In the previous video, I explained you how we can create our Lambda function in Python.To start our EC2 instance.We created this particular function and tested it as well.But we want to add more functionality.We want to trigger this Lambda function automatically.Here I'm going to give you one simple example like how we can trigger our lambda function.So here I have actually two functions.One function is actually starting my instance.One function is actually stopping my instance.Right.And I have this particular instance as well.Now if you look at this the instance is in stop state.Right now I want to trigger this function means I want to start this instance.So I want to trigger this function.How I can do this?For this, I have one web page.Now if I'm going to click over here, start EC2 in the background, it is actually going to run thisparticular function, start EC2, and then it is actually going to start my instance.Look at this instance that has been changed from stop to running.It means here we have added lambda function trigger.How I can add this.How many types of triggers we have.I'm going to discuss about all of this in this video.Let's start.First of all I'm going to the lambda function.Now when you are going to the lambda function.Lambda function don't have any trigger.You have to add trigger manually.How you can add this.Here we have this particular option to add a trigger.We can also add trigger from configuration tab as well.Here I have added one trigger known as API gateway.So what I'm doing I'm going to delete this see.Let's click on delete.Now trigger has been deleted.Now if I'm going to this particular web page and if I'm going to click on start EC2 instance, nothinggoing to happen because I just deleted this figure from my Lambda function.Now let's add this trigger okay.But before this let's test out that this is working or not.So right now the instance is running.Let me stop.This stop instance will work over here because we just remove trigger from the start right.So let's refresh this.It is stopping.Now if I'm going to click over here start.Nothing going to happen because I remove this trigger from the lambda function.So this button is like right now is useless, right?So it is actually going to stop my instance now.I cannot do anything from this particular web page.Now let's add this trigger.So here we have this option add trigger.And we have so many trigger options.Look at this.We can also trigger from Alexa API gateway application load balancer code commit.We will discuss about many of the trigger.But right now I want to make things simple.So I'm going to click through the API gateway.Now I know that you don't know anything about API gateway because we are going to learn API gatewayin upcoming days.I am giving you promise that even if you have any confusion, I will solve all your confusion when wewill learn API gateway.But right now we just want this API gateway.So we are making the things simplified and we are just going to create new API.Here.They are giving us three options.I just want to go with the http API.And I don't want any security.So I'm just going to choose open means the trigger will run from anywhere.Okay, we will talk about API in upcoming days.Right now I'm just creating simple API.Now once you click on add, it will provide one URL.Now this URL is going to trigger your lambda function right.So let's say that if I am going to copy this and if I am going to the browser whenever I will pressenter, it is going to trigger my lambda function and it is going to start my instance.Let's test this.See.This added.Now if you refresh this here pending and after some time you will get running state as well.So this particular API is triggering my lambda function.Now the process is very simple.I can go to this I can copy this URL.Okay.And here in this particular page I will add this.So I have this web page.I will provide you this web page.Uh in the video description you guys can download this.So I just need to change this URL.Okay.Start api url added.Let me click on save.I hosted this web page into the S3.Okay so let's update the page as well.Let's go to the S3.Yeah I'm in S3.Here I have index dot HTML.I'm just going to upload this file.So it is going to overwrite.I don't have versioning enabled.So now upload and let me refresh.Right now my EC2 instance status is running.Okay so let's verify both functions.Let's check this out.Running.Let's stop.It is stopping now.We also added API for start.So wait it is going to start my EC2 instance as well.Right now it is in stopping state.Instead.Let's click on start.So now it will start this.So now you got the idea that how we can add trigger right.Let me click on start.Yes.Pending.And it is going to start my instance as well.Now which type of trigger you are going to add.It is totally depend on the use case.For example here I have this website and I want this click event to run this Lambda function.But sometime let's say that I have instances and we decided that okay, we want to start all our EC2instance at 9 a.m. in the morning, and we want to stop all our EC2 instances at 6 p.m..In this case you can use trigger like EventBridge.Okay, so first of all let's check out this event.So this is in running right.So what I'm doing I'm going to add this trigger into the into the stop EC2 instance.Right.So going to the store EC2 instance here I have this API gateway trigger.Already there I'm going to add new trigger.So add trigger here I'm going to choose EventBridge.Definitely we will talk about EventBridge in upcoming days.I will provide you a complete explanation about this.But right now what I'm doing I'm going to click on Create New Rule.Let's add that rule.There's new.There's one.Here we have event pattern.Like we can add many event patterns health RDS.Other options are available but we will discuss about it in upcoming days.Right now I want to make things easy.So I'm going to choose this schedule expression.Here we have to add cron job.Now how we can add this cron job.So in this particular web page AWS provided us detailed example.These are the options available for the cron job.Now here we have this run at 10 a.m. every day.So many options they provided right.What I'm doing I'm going to choose this one right?Because for video, the most convenient way is to go with this run every 15 minutes.So this.Lambda function will trigger at every 15 minute interval.Right.So I'm going to add this here how we should add this.They also provided one example.So I'm just going to paste this now here instead of 15 minute I'm just going to use two minutes okay.And we have to remove the space as well.So the idea is at every two minute interval event will trigger because we have this configuring theEventBridge right.Now if you look at this we are actually working on stop EC2 instance.My instance status is running.Now I'm going to add this trigger.Look at this add.And it is for stop EC2 instance.It means after two minutes this event will run automatically.Sorry, this EventBridge is going to trigger my lambda function and it is going to stop this instance,right?So now we have to wait in order to verify the status.So in this particular video I explain you that we have two options to run our lambda function or triggerour lambda function.We can trigger our lambda function using API or even we can use EventBridge.In upcoming days we are going to add more trigger.But for this video these are enough.Let's wait for the status to be changed within two minutes.I'm just going to refresh this.Look at this.Stopping it is automatically stopping my instance because we have this EventBridge added over there.Right.See I'm I just want to make the whole process simplified.Okay.So I'm not giving you very hard example right now, which is not easy to understand.Definitely we will talk about some complex situation, how we can create complex lambda functions aswell, but slowly.Okay.Thank you very much.See you in the next video.Have a nice day.Goodbye.