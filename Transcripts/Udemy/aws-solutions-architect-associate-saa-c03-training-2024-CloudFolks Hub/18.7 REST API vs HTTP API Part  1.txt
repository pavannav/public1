# 18.7 REST API vs HTTP API Part  1

Hello and welcome to Cloud Fox Hub.Right now I am inside my API gateway console and I just opened it first time.Now how to start.You need to click on create an API.Now here you need to choose API type.We have mainly four types http api, WebSocket API, rest api, rest api private.Now we can put rest API and rest API private into the same bucket.The only difference is rest API is public.Anybody can access over the internet.Rest API private is only accessible from the VPC.So all other features are almost same.Now the main confusion we have is actually in between http api and rest API.WebSocket API is altogether different game.So we'll discuss about this in another upcoming videos.But the most important task is to choose between http API and rest API.Now you can only take right decision if you have idea of their features.I want you to understand these features properly so you can take decision that okay, in this particularcase I can choose http API or rest API.In the AWS Solution Architect exam.They are asking questions related to this.In the interview you may get questions related to these as well.So this is really very important point.So in this video we are going to talk about rest API versus http API.Because we have so many features to compare, we decided that we are going to divide this video intotwo parts.This is part one of rest API versus http API.So let's start.As always, we are going to start with introduction.AWS API gateway supports three types of API rest API rest API private.We are just putting them into the same bucket http API and WebSocket API.Rest API and http API are almost same because both works on normal request response model.Your client sends one request back and sends response and connection closed.They use same HTTP protocol, same style design and same integration pattern.Only difference is is in the features.Rest API gives full advanced features and HTTP gives faster and cheaper performance with limited features.Here cheaper means less expensive.If you compare rest API and http api, http API is 70% cheaper in term of price than rest API.Right.But yes, faster is actually http API.Now WebSocket API is completely different because it is used for real time communication where connectionstays open.Look at this here.Request response.Connection close in between.Request response.We have API gateway.But here connection stays open like WhatsApp chat or live dashboard where both client and server cancontinuously send messages.So in this video we will compare only rest API and http api because WebSocket API works in a completelydifferent way and cannot be compared with Rest or HTTP API's.So we have specific video for it.Now I want you to understand all these features and then also understand the differences right.So I'm going to explain a new feature then supported by HTTP or rest API.We are going to start with the first authentication and authorization.API keys.Now first thing is like you have to understand what is API keys.API keys are used when you want to allow only specific customers to call your API.Example paid APIs like weather API only user with API key can access.I want you to access my API.I'm going to give you key.If you have key then API gateway allows you right now supported by rest API.Yes http api.No.So API keys are actually not supported by http api.It doesn't mean that authentication and authorization is not supported by http API.Right.It is actually supporting different types of authentication and authorization.Right?Okay.Now I am Authentication.Introduction.AWS IAM users roles can call API using sign V signing only.AWS resources with proper IAM permission can call your API.Example EC2 instance.Calling your API using its IAM role now supported by rest API.Yes http API not supporting this third Cognito authentication.Cognito is authentication app from AWS.Definitely.We will talk about this right now.You just need to understand that for authentication we can use this particular service.So user login via Cognito to get token to call your API app.User signing using Cognito allows them example mobile app login Cognito call protected API.Now Cognito authentication is supported by rest API and http API both.Then the fourth one JWT, JSON web Token or OAuth authentication.Modal login using OAuth, Okta, Google sign in.You know, whenever you are going to open any website, we have sign in using Google, sign in usingFacebook, we call it like OAuth authentication.Example user sign in using Google call your API using JWT token.Now Rest API does not support native JWT OAuth 2.0 authentication.Keep this in your mind because rest API was older than JWT token, right?So it is not supporting this natively.Now if you want to go with some custom Lambda function and all this, you can you can support it, butnatively it is not supported by rest API.Now http API is modern, it is new.So it is also supports modern technology like JWT token.So http API.Yes.Now look at over here API keys not supported by http api JWT supported by http API.Right?So there is features, some supported by some supported by HTTP.Now let's talk about security and traffic control throttling and rate limiting.What is the meaning of this limit?How many requests a client can send?Example.Protect backend from overloading during the traffic spike supported by rest API.Fully advanced support http API supporting it but with a very basic level.AWS Web Application Firewall integration.Same like Cognito.WAF is another service of AWS, so protect API from attacks like SQL injection and bots.Example block suspicious requests coming from hackers rest API.Yes, it supports directly http API, directly not supported, but it supports via CloudFront then resourcepolicy introduction control.Who can access your API based on IP, VPC.AWS account API only from certain network or AWS account.Now resource policy allow API only from company's office IP and supported by both right?So Rest API and API both supports resource policy.So keep this in mind now.Request and response transformation.Now this is the most important part here you can get final detail that okay now I am going to chooserest API or http API.So this is the kind of one of the decision maker.Now I want you to understand this entire process properly.So we are going to end this part one.In the next video we will talk about all these other features.Thank you very much.See you in the next video.Have a nice day.Goodbye.