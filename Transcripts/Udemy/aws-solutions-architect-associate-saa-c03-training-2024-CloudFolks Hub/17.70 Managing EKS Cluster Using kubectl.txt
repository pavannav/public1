# 17.70 Managing EKS Cluster Using kubectl

Hello and welcome to Cloud Fox.My name is Bhavesh Katara.I have my ECS cluster ready and I want to deploy an application inside ECS cluster.How I can do this?As I explain you in the previous video, you cannot deploy an application using AWS console.You have to use kube control Kubernetes native command line tool.In this video we are going to discuss about kube control.So let's start.Here I am going to start with introduction.We can manage an Amazon EKS cluster using two ways.One it is AWS Management Console.AWS console does not control Kubernetes directly.It only interacts with the EKS cluster setting.Not inside.Kubernetes console is not Kubernetes client, blind, so it cannot manage all these things like createports, create deployments, apply YAML, execute commands in ports, check log scale.Application means day to day.Kubernetes administration task cannot be done through the AWS console.Why?Reason is Kubernetes resources are created via Kubernetes API server, which is a part of control panel.Kubernetes control panel and console is not designed to send those API requests.Means your AWS console cannot send any requests to the Kubernetes API server.And because of this, we cannot manage our Kubernetes environment using AWS Management Console.Right now, if I want to do this, then I have to use kube control.Kube control is actually an official command line tool of Kubernetes.And kube control is actually able to talk directly with the Kubernetes API server and perform all operations.Simply, you can say that kube control equals to Kubernetes control tool.With Kubernetes.With kubectl you can manage everything right.So all this is the same here.So I'm not going to read again means kube control equals to real remote control of Kubernetes in normal.If I have on premises Kubernetes cluster or even inside AWS, if I want to manage my Kubernetes environment,I'm always using kube control, right?Okay.Now how I can use this I want to use kube control.But how I'm going to explain you both ways.You have two options depending on your lab setup to use kube control one.Install kube control on your local PC if you are going to install this into your local PC means youhave remote access.You have kube control installed inside local system, right?And our cluster is in AWS so it is remote access.And option two install kube control on Amazon EC2 instance.Now we are going to talk about both of these options.I am going to start with option one.Install kube control on your local PC.Introduction.You can install kube control on your local computer whether you are using windows, Mac or Linux rightnow.For example, I am using windows Kube control plus aws cli.You have to install both kube control and aws cli.Now you may have a question like how I can install it.Don't worry, we'll go for the practical as well.So you must have kube control plus aws cli allow you to access and manage your EKS cluster remotelyfrom anywhere.You do not need to be inside AWS ORS or inside same VPC.This is the most common method used by cloud and DevOps and Kubernetes engineer.Now I decided that okay, I am going with this particular option.So what I must have inside my local system.First I have to install kube control.Second, I have to install aws cli and I must have AWS access key and secret key for the authentication.Now when you are using this particular option you have to verify endpoint as well.So we have to test or verify EKS endpoint.Let me explain you your EKS cluster endpoint must be public or public plus private because it is a remoteaccess.You are accessing this from your home means you are accessing it through the internet.In this case, your Ex endpoint must be public or public plus private.Now how you can verify this?Here I have my EKS ECS cluster.Then you can go to the networking here.You can check this endpoint access if it is public it works.If it is a public and private, it also works.But if it is a private only then you cannot use this option now.Why?Why it must be public or public?Private because your local PC cannot access private ECS endpoint directly.Now in some case, let's say that I have this private and I want to access this anyhow from the remotesystem.Then you must connect to the VPN, AWS Direct Connect or Secure Tunnel inside the VPC.Only then your local PC can reach this private endpoint without VPN access will fail because privateendpoint is not exposed on the internet, right?So this is option number one.Now option two is actually very simple.Install kube control on the EC2 instance inside AWS.So if you install kube control on the EC2 instance inside same VPC as your EKS cluster, it can accesspublic endpoint, private endpoint, and private public endpoint.So here I know that my EKS cluster having private endpoint.And I want to access this.I can simply create one EC2 instance into the same VPC as I have my EKS cluster.And then I can access easily using this private endpoint as well.The only thing is you have to install kube control and aws cli inside your EC2 instance.Now why it is actually allowing you to access private because EC2 instance inside the same VPC, itautomatically has a network visibility to EKS private endpoint.What you need on the remote EC2 instance EC2 instance inside the same VPC as ECS.Your VPC must be same your ECS cluster and your EC2 instance.You have to install kube control.Then you have to install aws cli.As we are using EC2 instance we have security group.So we have to make sure that outbound rule of our EC2 instance security group must have Https meanstcp port 443 allowed.It is outbound, right.And then we have to provide IAM permission to access ECS.So this is the process.Now we can choose any of this option.I want to go for the practical I want you to understand this practically.We are going to choose option number two.In the next video we will create EC2 instance.We will install AWS cli and then kube control.And then we can access our EKS cluster.So this is the process.Next video is actually practical of option number two.Thank you very much.See you in the next video.Have a nice day.Goodbye.