# 17.33 ECS Project Step 5 Resource Allocation Limits Part 8.2

Hello and welcome to Cloud Fox.My name is Bhavesh Katara.You are learning X step by step.Right now we are discussing about container configuration options of task definition.We have divided this whole configuration options into the two part.Previous video was about part one and we discuss about all these options of container configurationin this video.Means part two we are going to discuss about resource allocation limits.If you go to the task definition configuration options here we have this container configuration.And this is option that we are going to discuss.So let's start.First of all let's understand the use case in ECS.You can control how much CPU and memory your application can use.This resource is resource allocation limits can be set at two levels.You can define this at task level and also at container level.As you know that we are creating task, but task may contain multiple containers.Let's say that right now I'm configuring my container one.If I want to add multiple container in the task I can do this.Okay.So you have two options.One you can define task size.Task size is actually defining.We are defining a task level.And here we have this option as well.Look at this task size we already discussed about task size in a step number 5.5.If you want more detail you can go to that particular video.Watch this video and come back.But here I'm going to provide you brief details about this.This is total CPU and memory available for whole task right.So whatever the resources you are going to provide to the task will be shared with the multiple container.Let's say that here I'm going to provide four vcpu to my task.I can also provide Ram.So let's say that I'm going to provide 512 MB of Ram to this particular task.Now this resources will be shared between multiple containers.Right.Think of it like a big box where all containers inside must fit.Now for Fargate task size is mandatory.If you have launch type Fargate, you have to set up task size.Why?Because Fargate is a serverless, so somehow you need to set up limits for your task and that you candefine using task size for EC2.It is optional because in EC2 we have like EC2 instance and EC2 instance already having size, so thissize will be the size of the task as well.But yeah, if you are going to set up this EC2, EC2 will reserve that specific resources for specifictasks.This will be like added advantage.So we have like a clear idea about task level.Now we are going to talk about.Container allocation right.So see here if I have like two containers running inside.Same task I have four virtual CPU.Now if you are not going to set up container level resource allocation what will happen.This container is actually using too much resources and this container don't have resource left.Let's say that out of four virtual CPU, this container is using three Vcpu.Now this container two can only use one.So we don't want in balance between the containers.We want to allocate resources in between container properly.So in this case you can use container level allocation.However we define CPU memory for each container inside the task.It helps control how much the task resources a single container can use.You can controlling resource utilization of specific container so it cannot use over resources and itwill be harmful for another container.Now you can use for settings over here.If you look at here we have these four settings.First of all we are going to talk about CPU CPU units for the container example 0.25 CPU or 0.5 CPUor one CPU.This is what I said over here as well.Let's say that I have four vcpu, so I am going to assign two vcpu to this particular container.So out of four this is a limit for container one to use maximum of two vcpu.Now we have one question over here.That okay.But what if I want to provide half of CPU.So in this case you can use like this 0.25 quarter of your CPU, half of your CPU.Or if you have full decimal then no need to worry about it.Now this is for CPU but memory having two options.Memory hard limit.Memory soft limit.So let's understand this as well.Memory hard limit container is killed if it crosses this limit.Let's say that here I am saying that this container is going to use maximum of two GB Ram.Now if this container is trying to use more than two GB, it will be killed by X.Here we have same like if you want to assign 0.25 you can assign like 0.25.Do not write 256.Because when we are configuring this through the console, we are giving in GB.So if I want to assign 256 I will use 0.25 GB.Okay.We will set up this when we are going to set up task definition for our project.Memory soft limit.Now understand the hard limit means dangerous.If your container will try to use more resources it will be killed.Soft limit is kind of good because we are actually providing reservation over here.Reservation X tries to keep the at least this much free for the container.Again, setting up the things are same.So let's say that here I am going to set up like one.It means X will always try to reserve one GB of Ram for this particular task.And this is good because you have clear idea about the memory requirement of your container.So x will keep this much amount of Ram free for this container, which is giving kind of security foryour container.Security means you have your resources available, right?So this is good.Soft limit is really very good.Now here we have GPU option as well.Look at this right now GPU.We are using this for a specific use case.It defines how many physical GPU your container can use.GPU are used for AI workload or image video processing and rendering.So it is based on which kind of applications that you are using.GPU is always set per container, not at the task level.We cannot setup GPU at task level.Now.The GPU field in ECS console is grayed out by default and become.It becomes available only when these conditions are met.Look at this.This is grayed out.Why it is grayed out.First of all, launch type task must be EC2 GPU not supported on Fargate.See, right now it is greyed out.One of the reason is I have both of these options selected.If you select only EC2 then it will be available.Right?If you select mics or if you select Fargate only, it is not going to work.Now.Whenever you are going to use GPU, you have to add EC2 and EC2 instance must support GPU.So these are the EC2 instances that are supporting GPU.Let's say that if you have T2 micro instance, there is no GPU inside T2 micro.So when you will add T2 micro instance, this option will be grayed out.So whatever the instance you have in your cluster means container instance, this must have GPU.And these instances are actually supporting this A for your container host, you have to use X optimizedGPU RMI, so your EC2 instance RMI will be different, right?So we have to add this.Right now I don't have any container host.So that's why system is assuming that I will add this kind of host.So so that's why it is highlighted.Otherwise if you have existing host which is not supporting all of this, it will be grayed out right.Task definition.You cannot mix Fargate and EC2 because if you will mix them this will be grayed out again because actuallyFargate is not supporting GPU, right.So yeah.So this is all about part two resource allocation limit.And now in the next video we are going to discuss about environment variable.Now this is the most important part as well.But for this you have to watch next video.If you have any question I'm always available.Thank you very much.Goodbye.