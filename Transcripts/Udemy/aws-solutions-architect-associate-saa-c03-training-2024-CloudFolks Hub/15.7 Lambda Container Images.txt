# 15.7 Lambda Container Images

Hello and welcome to Cloud Folks Hub.You know you are updating your skill every day by learning from Cloud Folks Hub.Right now our Lambda series is going on and today we are going to discuss about Lambda container images.This is really very interesting topic.So please watch this video till the end.So let's start.First of all here I am going to create new lambda function.And as you know that when we are going to create lambda function we have to choose runtime.For example, if I have my code in Node.js, Lambda is actually providing us ready to use runtime forNode.js.I just need to select my runtime, and then I can load the code, and I can run this code directly usingLambda means serverless execution of code.Here they are actually providing many options.But what if my runtime is not listed here in this runtime list?I am going to explain you this particular process using this mind map.Let's start with introduction.AWS Lambda provides preconfigured runtime for popular programming languages like Python, Node.js,Java dotnet, Ruby.Right.So these are most popular languages and they are providing runtime for this.But what if your framework or language is not officially supported by AWS Lambda?For example a go language from Google is not supported over here means they are not providing any predefinedruntime.They don't have any predefined runtime for rust.They don't have any predefined runtime for Cplusplus.Now in this case what you can do in this.In such cases, AWS allow you to run your Lambda function inside the container using two options here.One option is container image.See here we have this option container image.Another option that we have is actually here known as OS only runtime.In both cases, actually AWS is creating container, but here we are giving entire container image.But if you are going to choose this one, they are giving us container image with a base OS.It can be Amazon Linux 2023 or Amazon Linux two.Here they will provide us only container with base Amazon Linux OS.Then we have to install runtime and set up the entire function.Okay, so both options are actually useful when you are going to run any code which is which has noruntime listed over here in the list of runtimes provided by Lambda.But how I am going to use this two option.I am going to explain you this by five differences.Okay.So one option we have is container image.Another option we have is OS runtime.Let's understand these two.First base OS.When you are going to choose this option you have to create your container image.When we are going to create container image we are actually setting up everything for example base OSruntime.Like we are providing everything to them okay.You can create this container image inside Docker using docker file.Now if you don't have any knowledge of Docker, you don't need to worry.You can check the description of the video.We have docker course as well.You can enroll and watch and learn docker as well.But right now if you don't want to learn this, no need to worry.You just need to understand the concept that if you don't have runtime listed and if you want to runthis code, you can create container image using Docker, and then you can use this Docker image directlywith your Lambda function.Here we are also going to create a kind of container but language sorry.Operating system of the container speaks here.As we are creating our image, we can choose any operating system like Alpine Linux, ubuntu, AmazonLinux, any.Here the image based OS image is fixed.Amazon Linux now runtime.You must include runtime inside the container image.So if you're going to choose this okay you just need to provide image image have everything runtimeand whatever the prerequisites we have.So runtime is already included into the into your base image.If I'm going to talk about OS only, you must install your own runtime inside Amazon Linux because theyare just going to provide you container with only base OS.So you have to install runtime how you are going to install this After completing this process.Okay.Here you are going to select Amazon Linux.Then you need to create function.And then you need then you have to install runtime according to your requirement.Control full control over OS runtime and dependency.Here we have full control on runtime and dependency.But OS is fixed because our container based image is Amazon Linux based for full custom environment,large application, unsupported language here.Lightweight setup but custom runtime on Amazon Linux so we don't have any control on operating system.But yes, custom runtime is also supported by both.If I'm going to talk about use case, deploying ML means machine learning model with TensorFlow in acustom container here.Running Node.js version on Amazon Linux.Let's say that here they provided many NodeJS version, right?But you have code which is like a previous version of NodeJS which is not listed over here.In this case, no need to worry.You can choose Amazon Linux as a base image for your container.Then you can use any Node.js runtime.Yes, definitely you need to install this.But yeah, you can run any Node.js version for your custom application.Here we have to install this after creating function.But here we have ready to use image.We have to create this image.And then we have to upload this image into the Elastic Container Registry.And then we can face this image and run this as a lambda function.So these are the differences I hope you got complete understanding about this.Still if you have confusion no need to worry about this because as you will learn all the process stepby step, you will get better understanding.I know that if you don't have knowledge of Docker, it would be very difficult for you to understandthis.But don't worry, just understand the overall concept.I will give you proper detail about this in upcoming videos as well.Thank you.Goodbye.