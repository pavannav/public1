# 15.12 NAT Design Options

Network address, translation or Nat nothing.Design options in AWS is an important session video that I created because there are different wayswhere where you can do nothing.But before we start with these options, let's clearly agree on something.Nothing is not a simple design.That is something you should consider it as a foundational or you add it in every design.Nothing typically helps to overcome some overlapping issues.Sometimes it's considered as a security layer.It is to some extent, yes, provide a security in terms of, for example, if the session initiatedfrom inside does not allow the session initiation from outside.That being said, it does not mean that inside host is not hackable because there are some exploit andhacking at the application layer in which it might compromise that host and start initiating the connectionfrom inside.So don't consider it by itself.It's a security mechanism.It can be used in conjunction with different security layers.However, there are many scenarios and different use cases where we need to use nothing.Regardless of the requirement.We will discuss key common design architectures and options that you can deploy and use Natting in AWS,starting with the very simple and common one where, for example, you would like to have a host ina private subnet to access the internet or public services using public IPS where you can provisiona Nat gateway or maybe you put a virtual appliance from AWS marketplace, for example, and then youperform netting at that device, as you can see.So we discussed multiple design options with these across this course previously.The other option is that when you have a central architecture like what we discussed previously in thissection in terms of Ingress, Egress, VPC or VPC, and you want to perform Natting in a central VPCwhere the Internet gateway is provisioned and the Internet is connected.So with this architecture, we can have a centralized netting.But keep in mind here that with regard to the number of instances that you have in these vpcs trafficvolume, because if you have many vpcs and you have those instances, those Nat gateways, for example,or maybe manage firewalls, they might be overutilized.So it's very important to understand how much traffic that you will be sending across.And this same architecture can be also used in a multi-account setup.This is very useful if you have an architecture where we have Multi-account setup and you don't wantto, for example, provision multiple nat gateways and you want to reduce the cost where you can doand commonly it's done.You can have a networking account where for example, you put your net transit gateway, the edge,VPC, etcetera and with the nat ING capabilities and then through the transit gateway, you can connectthose vpcs.They are in different account, as you can see, highlighted in different colors, and then they willhave the ability to connect to these nat gateways or centralized virtual appliances, performing nattransit gateway route tables designed in a similar manner we discussed previously.So there is no change here.It's just a matter about the sending traffic to reach that edge.VPC Let's look into a little bit more detailed scenarios.So as we can see here, we have on prem side and we have a VPC in AWS connected to Transit Gateway.So and it's obvious that we have overlapping IP addresses, which is ten 100 slash 16.So how can we enable communication between these two networks?Who has overlapping private IP addresses?So this is one design option.There can be multiple option ways that you can do it.But let's discuss this option.So typically this private network residing behind a firewall in which we can do a network address translationat that firewall and translate the whole subnet.So now we announce this subnet as 20 100 slash 16 and what you can do at the edge router of the on premisesside, you set up a static route point to 22 100 slash 16 and the next hop or the target will be theinternal firewall.And then this static route maybe can be advertised over BGP to Transit Gateway from the VPC side.What you can do, you can add.Tonight, Gateway and on the transit gateway, what you do, you set up a static route that point tothe subnet of the Nat gateways, which is 192616800 slash 16.And now through VPN, we receive the route 20 100 16.And based on that for the VPC network, when when it wants to reach the on premises network, it willbe published or announced or targeted as 20.100 slash 16.So the VPC should have no idea about the overlapping.So the local route tables of this subnet will look like something like this.So as you can see, the 20 100 16.2, the Nat gateway from the private subnet and from the and fromthe Nat Gateway subnet interfacing the transit gateway where the transit gateway attached it has theroute points to the any of the transit gateway.So in this case, if we have a packet coming from that private subnet source from ten one 110 goingto 21 120, it will hit the Nat gateway.So the source will be nat ID and now the transit gateway will see the source as 1921681. ten and goingto 21 120 and it will send it to the VPN peer where the VPN peer that is the customer device gatewayor edge router at the customer site will see the source as 192168.So here shown as six eight.This is a typo .1. ten going to 21 120 and based on the static route set up in this router, typicallyit will forward that packet to the firewall and typically the firewall will perform the netting in whichit will keep the source as it is.1921681. ten and then the destination will be netted because the whole subnet is netted.It will be translated to ten .1.1. 20 and then this.Typically the destination will see the source as a native address and it will lead that destinationand the way back it will work in a similar manner.Another option like if you have VPN setup is that, for example, you can provision a virtual appliancefrom your preferred vendor from the AWS marketplace and you set a VPN to that virtual appliance andthen you can do source and destination destination netting at this virtual appliance in which you canhave a static route in the local subnet of the VPC, pointing to the native IP address of the on premand you point it to the any of the virtual firewall.And once the packet reaches the firewall, the firewall has the policy set up, it will change the destinationas well as it will not the source.So that can be done before even sending the packet over the VPN tunnel.And here is just a high level example.So typically you should have a setup with multiple virtual appliances across different availabilityzones for availability.ET cetera.The other design option or architecture for netting with multiple vpcs is where, for example, we havethis design in terms of we have consumers or customers.Vpcs depends on the scenarios and the shared services or application Vpcs that are represented on theright hand side.As you can see from both Vpcs there is overlapping subnet which is the 10.1 Cidr range By using NatGateway to perform private netting.Here we can do we can hide the subnet from the source or instances, for example, and also by havingan elastic load balancer here.Or it could be third party load balancer.It depends on the solution and design.We can also hide the instances and residing in the shared services or application vpcs by using theload balancing subnet to interface those applications.So for example, here in this design, let's assume this network load balancer and has the IP 17261.ten.Then if there is an instance and from the subnet that is 10.11 ten let's say here, this is the hostwants to reach that application in front end and with the network load balancer that has this IP 17216 110 So typically based on the route table configuration, it should be directed to the Nat Gateway,and that IP should be not in terms of the source IP and the destination will stay intact the same andthen it will be forwarded to the transit gateway based on the other subnet of the Nat Gateway routetable to be forwarded to the ENI of the Transit Gateway and Transit Gateway typically has the routingfor the one 7216 zero zero that points to the shared services VPC here named like Services VPC.And then it will be sent to that network load balancer and typically it will be forwarded to the backend instances.So the back end instances will see the source as 19216281 ten, and then it can reply back to the samerange of the IPS.With this same architecture, it can be expanded on a bigger scale in which, like the 10.1, rangescan be reused.So this maybe we can call it the reusable IP range or Cidr range, for example.Maybe you have larger scale setup.You may have like hundreds or thousands of containers and you would like to have maybe reusable IPSand then you have something that is going to.Be shared among those different vpcs, which is highlighted here in yellow color.Among all the different vpcs.And in such design approach, as you can see in the transit gateway route table, the only subnets ornetworks are presented in the route table are the one in yellow.So these maybe we can call it like the reachability subnet or shared and shareable subnet among thedifferent vpcs.While the 10.1 this is like the reusable private ranges that can be used by any other vpcs without beingpublished or even advertised in terms of reachability.So in such case we can achieve higher scale and only what we need to do is have the smaller cidrs forthe nat nat gateways.Or it could be a third party night device or nat instance configuration.ET cetera.As long as it's capable of doing such address translation and maybe from the shared services or application.ET cetera.You front ended with load balancing like application load, balancer, network load balancer.If you are using third party load balancer, make sure that it does not, for example, expose thoseprivate IPS in terms of the back end instances.