# 6.13 ELB - Migration Design Scenario

Time for a design scenario example.So in this example there is an on premises legacy load balancing solution that take requests from outsideclient and send it to multiple back end clients.The technical requirement or specifications of this solution.First of all, it requires static IP for client.For whitelisting purposes.So clients, they connect, for example, from certain partners.And those partners, they need to first whitelist that IP in order to allow their applications or userto connect to that IP for whatever specific use case or scenario.This is a common requirement in many industries.Also, there are requirements around SSL, TLS offloading, Http header inspection with path based routing,web ACLs and client white black listing capabilities.Also the requirement for multiple domains SSL cert to be installed.So these are from, let's say.Functional requirement, we can say these are the functional requirement that is expected from the solution.There are also architectural requirement in which the solution should provide application modernization.For example, by exposing lambda function for backend serverless solution.For example, when you put slash new for a new application or version that you want to introduce with,let's say, canary deployment or a new version.Also there is requirement around the solution in terms of providing elasticity to the architecture.No need to upfront provisioning stuff and for sure this trigger now in your mind the Autoscaling capability.In addition to that, there is a preference to utilize wherever possible managed services or serverserverless capabilities in the architecture.And also there is requirement around providing seamless migration.So there must be a ability to have migration without interrupting user traffic.We will look into how this might can be achieved.So typically you are taking notes as a designer or architect.So in general, based on this meeting outcomes, you know, there is requirement for elastic solutionand there need to be a managed solution provided as part of it.Also, there are some security functions to be taken into consideration and easy migration.So it is like high level notes with the specific capabilities.So typically, based on our discussions with load balancing, the proposed solution could be based onapplication load balancer.Okay.With the back end target or maybe multiple back end target based on the specific requirement of workload,etcetera.Using Autoscaling for elasticity and lambda function to front end any serverless requirement based onthe high level architecture specification mentioned an addition to that, you will need to integratewith AWS WAF for the web filtering and client blacklisting whitelisting requirement as well as to providethe ease of deployment of SSL certificate.You can take advantage of AWS certificate manager ACM and typically when you make connections, thisis trusted outside.So if for example, when you connect to a website and you get this like the green lock in your browser,so this tells you a secure connection, untrusted by your browser.So this way you can get out of the ACM with Alb and it's very simplified and you don't need to do keyrotation etcetera by yourself.Let's dive deeper into the specific requirement and how it with the solution capabilities that we arediscussing now.We'll go to we will go through them one by one.So when we say static IP for client for whitelisting requirement, So typically you can achieve thisby using application load balancer plus global accelerator.We discussed this or mentioned this in a high level previously.This is a capability that help you to get a static IP address.Typically it's anycast IP address.You get two IP addresses for redundancy and they will be advertised across AWS edge locations, poppops worldwide.So this provide even better performance not only for the requirement of a static IP.The other requirement, which is SSL offloading.So as we just discussed with ACM, you can easily achieve this.When it's come to Http header inspection.So and path based routing.This is natively supported by the Alb, so you don't need to integrate with any other service web,ACL and client white black listing capabilities or rules.This is where you can get advantage of the Alb integration with AWS WAF web Application firewall.Also multiple domain SSL cert installed.So Alb with SNI capability, it's natively supported.So this has some implication around browser version.So I would recommend you if you want to go into details around this, just go and search applicationload balancer and see.There are a lot of documentations around it to to understand it in more details.But in general, this capability of functionality helps you to have multiple SSL cert to be installedfor different domains on the Alb.Up modernization, exposing a lambda function, etcetera.So as we discussed, this is easy to be achieved by using LP plus lambda target.That being said, if you go to a previous session where we discuss LP with or versus API Gateway, thereare some design patterns we discussed there in which maybe you slightly can, for example, based onthe requirement, change the architecture in terms of where you place maybe API gateway, if you wouldlike to introduce it to the architecture.So there are multiple options, but this should not necessarily change the overall load balancing solution.Maybe you just need to integrate here and there differently based on the specific requirement.Introducing elasticity to the back end.Typically this is something that you can get by using application load balancer targeted group as partof autoscaling groups.So auto scaling groups here will give you the elasticity in the back end to to make it something responsiveto the and increase our reduction in the load.And we discussed previously there are multiple options and design for the auto scaling that you canconsider also as part of your solution.Utilizing, manage or serverless.So here if you look at it, Alb, ACM, WAF and even Lambda, so all of these manage slash serverlesscapabilities which will give you the ease of deployment and reduce the operational overhead.Last but not least, providing seamless migration.This is where you can get advantage of, for example, weighted routing in which that you can provideweighted routing to different targeted groups.Maybe one targeted group you with IP address, IP target to on prem instances or servers, and thenyou have another targeted group or targeted groups and the cloud where you start moving your workloadand then you start gradually changing the weight of the request routing.So this can provide a seamless, non-disruptive type of traffic routing and migration until you completethe migration.Let's expand on this scenario and add some routing to the mix.Let's assume this company application needs to connect to a third party partner.To validate some information.So the request comes from the end user lands at the Alb.The Alb establish a connection with an instance or instances in the targeted group back end.That instance then will initiate another connection to the third party partner.So this will be separate connection outbound initiated from the instance to the partner over the internet.This partner need 1 or 2 static IP to whitelist them and accept traffic from.So now we need any instance that can be recently added to the fleet of instances as part of Autoscalingto make outbound connections securely without being exposed directly to the internet as well as thesource IP need to be from predefined IPS.So by adding two nat gateways each in an AC, we could achieve this requirement.Two Nat Gateways.If you remember from the VPC fundamental networking session, we discussed that we can have the Natgateway at the AC or subnet level for Multi-az setup.We need to provision at least two Nat gateways.And by assigning elastic IP to each Nat gateway, you can ensure the source IP and instances when connectingto the outside will be from these elastic IPS.So now elastic IP means a static public IP.Once you assign them to the Nat gateway, it will be the source IP of any connection going through orvia that Nat gateway.Moreover, with this design, the instance is still in a private subnet and do not need to be exposedto the outside.So now you might be wondering, should I put the Nat gateway in different subnet than the ELB?Sorry.Then the Alb as it's shown here, for example.Or I put them in same subnet.Technically both works.So if you put them in terms of public subnet, the Nat gateway and they'll be in the same or you separatethem as it presented in front of you.As you know, technically they work, but better to have your design structured in which your subnetare structured by function.This is even more simplify any specific filtering that you need.You may need in the future or you don't know what type of integration that you will add to the mix inthe future in terms of hybrid connectivity or multi vpcs.So as long as your design is structured, anything comes in the future, it should be simpler than everythingin a single subnet.Then you will go into the headache of how I separate stuff.