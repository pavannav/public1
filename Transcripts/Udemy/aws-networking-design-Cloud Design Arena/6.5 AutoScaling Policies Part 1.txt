# 6.5 AutoScaling Policies Part 1

All right.So the question here is, can we combine multiple scaling policies?So we discussed like how auto scaling works and you can set up a policy based on predefined metrics.ET cetera.But can we create like multiple type of metrics and we make different layers of policies?Before we answer this question, let's first of all dive a little bit deeper with auto scaling to understandmore about it and how it works and the type of policies.So as you remember, we mentioned that in your auto scaling you can have like minimum and maximum.So these are like the.The boundaries you set for the scaling in terms of, okay, I don't want to have less than this numberand I don't want to have more than this number and my scaling policies.And then between these two numbers, you can have the flexibility to add on the auto scaling group doesthe calculations based on the preset metrics.So let's say you have an auto scaling group with two instances where one instance is at 60% CPU andthe other is at 40% CPU on average they are at at 50% CPU from the aggregate auto scaling group.Then let's say that you set a target value of 60% for CPU utilization.And your autoscaling group then exceed this target.Once it's exceeded.Autoscaling calculation might determine that, adding maybe 1.5 instances will decrease the CPU utilizationto close to 60%.Because it's not possible to add 1.5 instances auto scaling group round up and add two instances.This might even decrease the CPU utilization to a value below 50%, but it ensures that your applicationhas enough resources to support it.Similarly, if the auto scaling group determines that removing 1.5 instances increase your CPU utilizationto above 60% the target that you put auto scaling group, then remove just one instance.And this is in the middle where we have something, call it.You can call it like desired capacity.This is the plus minus.This is where the add remove when the policy is in effect.Amazon EC2 auto scaling adjust the group desired capacity up and down when the alarm is triggered.From Cloudwatch side, there is another type of scaling policies call it.A step policy.So now maybe you are asking, okay, what about the one we just discussed?The one we just discussed, as you may notice, we mentioning about Target.Target.So it is a target tracking scaling policy.While the step policy is more complicated.But what you can do, it can help you to set up more advanced type of policies.So let's look at this example here just in a high level hypothetical, but just give you the idea ofwhat maybe you can set up.But we will discuss how this also has some implication on you when you do the setup.So, for example, your policy may specify like, okay, increase the instance, count by 10% when mymetric value is higher than or equal to 70, but less than 80%.Okay.Then you have another threshold in your policy.Say, okay, increase the instance, count by 20% when metric value is more than or equal to 80, butless than 90.And then you set up even another threshold in which that you increased by 30% when the metric valueis more or equal to 90%.So this is just an example how you can set up different thresholds and different actions.However, with the step scaling policy, it requires that you create the cloudwatch alarm for the scalingpolicy, specify high and low threshold for the alarm, define whether to add remove instances and howmany, or maybe set a group to an exact size.So which means it is not straightforward.So that's why if you can achieve it with, for example, the target tracking policy, just always focuson the simplest and easiest way to achieve it.If you have something complex, maybe you can do it with the step policy scaling policy.