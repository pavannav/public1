# 5.5 Connect to AWS Services Part 1

In this sessions, we will be focusing on the design options to connect your VPC workload to AWS services.So where these services reside.If you remember from the AWS Global Infrastructure Overview Session has many regions across the globe.AWS services reside outside of your VPC in a region and globally across the AWS regions and these servicesreachable via public IP ranges.Therefore, to reach these services you will need an internet gateway route to the Internet gatewayand your host placed in a public subnet.And for sure, as we discussed before, the subnet, ACL and security groups need to be configured toallow traffic.Also, as discussed in the previous session, we have different options to connect to the internet inwhich you can utilize these options in a similar manner to connect to AWS services.In addition to the options we just discussed, where your instances in a public subnet with an internetgateway, you can, for example, utilize a Nat gateway that's placed in a public subnet while yourhost or workload is placed in a private subnet.Similarly, you may do natting using third party virtual firewall appliance and then through the InternetGateway you can communicate with the AWS services that reside outside of your VPC.Or you may want to use AWS, Gateway Load Balancer or AWS Network Firewall, as we discussed in theprevious session, to be in the path.For the details about how the routing and route table design is, refer to the previous session as itwas discussed in further details.With all of that, you still may have compliance requirements, for example, in which maybe you needto remove the Internet gateway from the VPC.While the workload in the VPC should still be able to communicate with AWS services.So the question here, how to connect privately to these services?What exactly mean by privately here?We are referring to the communication with these services without using Internet gateway and or theneed to use public IP ranges.Yes.With Nat, Nat King, Nat Gateway or using any firewall innocence, for example, doing Nat in capabilitieswe could communicate privately, but we still need to have Internet gateway.But here the question is maybe we need to completely not utilize the Internet gateway.This is the question and this is where we can take advantage of VPC endpoints that enable private communicationbetween your VPC and the supported AWS services.Gateway load balancer endpoint was discussed in the previous session and the associated use cases.Therefore, this session focuses on the gateway endpoint and the interface endpoint with regard to accessingAWS services.A VPC gateway endpoint enables a private connections between your VPC and Amazon S3 and DynamoDB.Technically you specify a gateway endpoint as a route target in your route table for traffic that isdistance for the supported services.And typically the target will be prefixed list that is going to be automatically defined when you createit for that service.And that prefixes include the actual public IP ranges for that service.With gateway endpoint, you specify one or more route tables.So it's not only a single route table.You can associate the same endpoint with multiple route tables, okay?And each subnet that's associated with this route table or tables has access to the endpoint and trafficfrom instances in these subnets to the service will be routed through the endpoint.As you can see here in this design, there is no internet gateway.In order for your application to reach these services using domain name, you must enable DNS resolution.In your VPC.Typically.Or you can use your own DNS setup in a VPC, but you need to make sure request are pointing to the rightIP addressing, which is a bit of a more complex to maintain.Better to turn on DNS in the VPC unless you cannot for specific reasons.So there is an option.All right.So since here we are mentioned about routing or route tables, let's zoom in into these topics.In a scenario like this where you have a gateway endpoint for an AWS service, typically here, eitherAmazon S3 or DynamoDB and Internet Gateway.Most specific route that match the traffic, determined how the the traffic will be routed.The longest match.The longest prefix match principles typically.So in this case, here we have an existing default route and the route table that points to an Internetgateway.The gateway endpoint route here takes precedence for all traffic distance for the service like S3,because the IP address range for the service is more specific than the default route.All other internet traffic goes to your internet gateway, including traffic that is not distance forthe service in another region.Okay, so let's discuss this point about the service and other regions in a bit more details.With gateway endpoint, you will have access to S3 and DynamoDB in your region and you cannot use itto access these services in another region.If you need to access these services in another region, you can use the Internet gateway and one ofthe design options highlighted earlier without using an ad gateway or using a firewall instance.And then this will help you to connect to S3 and DynamoDB.And technically you will be having access to any other service because now you're just going throughthe Internet gateway.It's about IP and DNS configurations.Let's expand on this use case and introduce another VPC that is connected over VPC peering to your VPCwhere you have the endpoint With gateway endpoint, you cannot extend the connection through the peeringor any other means to another VPC.Okay, the same is true.For extending it to an on premises site over VPN or direct connect.Still you may want you can do like certain workarounds using a proxy server or some type of connectionlike that to to achieve this.But it's still a work around not a natively supported.That being said, for S3, later on when we discuss internet interface endpoint, we have a solutionfor that.So just giving you a heads up here.