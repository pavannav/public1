# 11.5 DX with VGW

After we discussed the physical connectivity options of direct connect and the different options ofthe virtual interfaces of a direct connect connections.Now it's time to look at the design of direct Connect using different network services endpoints.Technically, a direct connect connection can be used with virtual Private Gateway, Direct Connect,Gateway and Transit Gateway in addition to the public peering using public Vif.Public Vif, which is public virtual interface routing considerations, was already discussed as partof this section.Therefore, the focus now on the design and design considerations of direct connect with Virtual privateGateway.Direct Connect.Gateway and Transit Gateway.Let's start with the direct Connect.Connection with Virtual Private Gateway, where the virtual private Gateway VW link, the direct connect,or specifically the private virtual interface directly to a VPC.From data plane point of view, each cross-connect subinterface is mapped to a private virtual interfaceas discussed before, and then associated with the VPC using virtual private gateway.Control plane to exchange routing information is done using BGP.As discussed before as well.Each physical, direct connect supports up to 50 private virtual interface as private virtual interfaces.As you know, this means that you can configure up to 50 sub interfaces to connect to 50 vpcs and eachconnection require typically a separate BGP peering session, which means 50 BGP peering sessions toconnect to 50 vpcs.This design model can be extended to vpcs in different accounts as well.As we discussed before this in this section, using a hosted private virtual interface which is importantfor solutions with Multi-account setup.And as you can see, by adding another virtual interface, you will add a Subinterface and BGP peeringsession at the customer router to establish the end to end connectivity.If you have a design that requires end to end separation over a single physical, direct connect connection,you may use separation of virtual routing and forwarding concept at the customer router in which youextend the separation from on premises all the way to the different vpcs.Or you might be using EMB BGP with Mpls from on prem to the customer Gateway like a managed layer threeVPN.If you are not experienced with these advanced networking terms, it's fine.Here we just highlighting what might be used as a doable scenario or advanced scenario with separationand this might be more common for SP offering hosted connections or SP like setup, which is not likea common use case scenario, but it's a duel.Doable option if that type of separation required end to end.As we discussed before, if you have multiple site to site VPN connections, you can provide securecommunication between sites using the VPN cloud hub that terminates to virtual private gateway.This enables your remote site to communicate with each other and not just with the VPC.The VPN Cloud Hub operate on simple hub and spoke model.This design is suitable if you have multiple branch offices and existing internet, primary or backupconnectivity.That can be a cost effective simple to set up.And you can also.At the site or introduce or integrate a site with a direct connect that has a private.Terminate directly to the VPC.Remember that you cannot do this direct connect.By using direct connect Gateway.But this option is about attaching the private.Directly to VW, which enables VPN and direct connect cloud hub communication.Keep in mind here, this design can be complex if you have multiple vpcs that typically need to communicatewith the on premises sites in which you will need to consider Transit Gateway.That's going to be discussed as part of this section.All right.So let's analyze the key design consideration of this connectivity option.First of all, from the virtual interfaces, scales point of view, it's a scalable design.You can have like up to 50 private virtual interfaces per connection.When we look at VPC scale, it's limited because you can have up to 50 vpcs per typically 50 vpcs perdirect connect and one VPC per virtual interface.In terms of control plane complexity.It's complex at scale because you will need BGP peering per virtual private interface.So if you have 50 VIPs, that means you need to have 50 BGP sessions to connect to 50 vpcs only.Manageability at scale.As a result of this point, we highlighted its complex many virtual interfaces, many BGP sessions,etcetera to manage and maintain.And also this indicated its limited in terms of difficult to achieve different communication models,including centralized, direct connect as well as its in a single region.So the direct connect need to be in the same region of the vpcs.