# 5.4 Service Chaining

Service chaining what is meant by service, chaining in simple words, it's about inserting multipleservices or functions in the traffic flow path, like what is shown here.In this simple example, there is a source instance, could be a client application that has to go throughweb filtering.Then it need to get netted by using Nat network address translation.And then there must be, for example, some firewalling inspection to be applied.And these can vary depending on the requirement.There might be a load balance on the path.There could be intrusion detection prevention.ET cetera.So the idea is to have multiple services or functions along the traffic flow path based on differentrequirement from networking or security.To achieve this, from networking point of view, there must be some route tweaking or route engineeringneed to be in place in order to achieve the desired requirement to keep it simple.Let's go through a sample use case or design scenario.As always, we discussed design through use cases and to keep it consistent to where we ended in thelast video session.Let's use the same example that we ended the session with, which was a question about how we achievesuch requirement by doing firewall inspection.And then we do the natting to the packet or the traffic flow.So the answer was yes.However, let's see how this can be done within a VPC.So typically this will be achieved by using different route tables and then we have more specific routesdefined.But the main.Element here that need to keep in consideration from design point of view.You need to have these components in different subnets because typically they will be used in the differentroute tables as targets.So to achieve that in a simple way, and if you remember from the design principle, this even makeit more modular.Let's start from the bottom and we take it all the way to the top.If we look at the subnet where the source instance and a private subnet residing we see in that routetable, the default route point to the firewall elastic network interface and that could be a firewallappliance from the marketplace or something you build or later on we'll look into how this might beEven a gateway load balancer or network firewall of AWS.And in the firewall subnet, the default route here has the target as the Nat gateway.So now the packet will get inspected, then goes out of that interface and we'll look at the subnetroute table and it will see, okay, if I want to go out to the internet, I need to go to the Nat Gateway.Once it reached there, the Nat Gateway has the default route point to the Internet gateway.But the important point here to highlight, if we look at the route that highlighted in red, whichis more specific route associated with the Nat Gateway subnet, this is required because now when thetraffic comes back, if we don't have that more specific route point to the firewall elastic networkinterface, now the Nat gateway will send the packet or the traffic directly to the instance, whichis not the ultimate goal that you need.We bypass the firewall inspection and the return and this could cause issues in the communication aswell.So in order to make it symmetrical all the way, you need to have the most specific point to the instancesubnet and then the target will be the same firewall.NE And as mentioned, this can even exactly use in the same structure for to, to achieve it with gatewayload balancer or even it could be network network firewall.AWS network firewall and a specific.If you see here, the target replies from the firewall to the gateway load balancer and the endpointthat typically represented in the VPC and connect back to the Gateway Load balancer or if it's an AWSnetwork firewall over the AWS Privatelink.So this is a flexible design, and typically you can have different services in the path and you canhave the sequence defined differently.It all depends on the requirement, but the logic will stay the same as long as you know how to havethe most specific, where you put the target and define them or place them in different subnets to makesure that they can be used as a target in the different route tables.