# 6.9 Network Load Balancer - NLB

All right.Till now, the concept, capabilities and design cover should be applicable to both application andnetwork load balancer unless stated otherwise.Let's start with more specific capabilities and design considerations for each type, starting withthe AWS network load balancer.First of all, from very high level and design in general, when there is performance and scale.As you may know, if you are architect or designer, it's sometimes common to find some trade offs inwhich if you want high performance or very high performance, you may not achieve very high scale.Not always, but in many scenarios, many systems with network load balancer, you can achieve bothat a very low latency.This.What makes the NLB unique?However, like any technology, it is not the best fit for every design.So let's zoom in more into the NLB and find out more about its capabilities and operations.The network load balancer.The NLB Network load Balancer operates at the transport layer, Layer four of the Open Systems interconnectionmodel or OSI model.If you are from networking background, you are pretty sure you are familiar with those I model.If you are not, just try to search it and look into these the layers of the OSI model and it can handlemillions of requests per second with ultra low latency.This is what we just mentioned about earlier about performance and scale and I'll be mainly handle TCP,UDP and TLS type of traffic given it's at the layer four and I'll be primarily designed for applicationsarchitectures in a single zone.Still, if something in that availability zone fails, requests to NLB can automatically failover tothe other healthy availability zone or zones where you have provisioned back end target groups or groups.Amazon, Route 53, DNS and health checks helps with such failover scenarios, so we will go into thedetails of it.So at this stage we will just look into the high level.Then we will dive deeper.Also, NLB offers SSL, TLS termination.Let's zoom in in one of these nlbs and discuss more about the NLB and how it handles encryption andtransit.With SSL and TLS with the NLB, you can set the listener port as port four, four, three and then passthrough the encrypted traffic.To the target instances for sure where it should have the SSL TLS certificate installed.To decrypt the connection.Or you can offload the SSL, TLS certificate at the NLB and send the traffic to the backend instances.If you are thinking now which design is recommended?The answer It depends on the requirement.For example, offloading the SSL, TLS at the load balancer.It's a common use case and it's still secure, especially if you have your instances in a private subnetand you configured, for example, the security group properly and the OS level hardening stuff.You've done it properly at the instance level, it should be fine.That being said, if the requirement mandate end to end encryption in transit, that might be drivenby compliance requirement, for example, then you have no choice but to select the other option.Therefore, make sure to understand the application and security compliance requirement before makingthe design choices.Let's zoom in more into the NLB and learn more about its capabilities and operations.When you create a network load balancer, it also creates a network interface.Elastic Network Interface E.n.i.For each availability zone that you enable, each load balancer node and the availability zone usesthis network interface to get a static IP address.So remember this a static IP address.You can optionally associate one elastic IP address with each network interface when you create theload balancer in the back end.There can be several nodes to handle the increased traffic volume, but all of that is abstracted toyou with a single ENI, an IP that you deal with.So you don't need to worry about how the scaling happens in the back end, that that's why it can handlehigh volume of traffic without intervene, without the need of someone to intervene.So this is the benefit of a managed service here by AWS because you don't need to worry about the infrastructurepart in terms of scaling out and in.It's handled for you or what you need to to do is just provision the service and ideally just put theright design for it.With NLB, targeted groups can register EC2 instances and IP target that can be an AWS or even on premto route traffic to.So you have the flexibility to choose where you write your traffic in this case.If you remember I mentioned about static IP.The static IP of each NLB in an AC is very useful in scenarios where you need to integrate with, forexample, firewalling or IPS solutions and an IP of the ELB load balancer need to be whitelisted toallow the traffic to flow.All right, let's expand now the discussion and look at the operation where there are instances distributedacross different Azs.As discussed earlier.AWS network Load balancer by default provides zonal load balancing solution.If your solution is not zonal solution, this may cause an issue.Let's analyze it with this example.With the zonal design network load balancer route request only to target located in the same availabilityzone.Because it's zonal, this means in this scenario, the target groups distributed across Asia and ASBAmazon Route 53 Response to client request.With the IP address of the load balancer node, and the client typically will pick one of them to sendrequests to.Let's assume that traffic distribution from the DNS point of view that each load balancer node receives50% of the traffic from the client and ILB nodes in each AZ distribute its share of the traffic acrossthe registered target.And the scenario we have eight instances and the target group of Asa and two instances in the targetgroup in AZ B As a result, each instance in Asa receive about 6.2% of the traffic, while each instancein ACB receive about 25% of traffic.And if the client caches the DNS information, this can be even unpredictable and for sure imbalancedin terms of traffic distribution in meaning that you would have to keep closely monitoring and adjustingyour target capacity, which is not an efficient approach.Also, in case of failure of one of the Azs, the other healthy AZ will handle the request with thelocal AZ target group in which you need to plan the capacity for this type of design or design yourautoscaling to take care of such situation.This is where cross zone load balancing helps where each load balancer node distribute traffic acrossregistered target in all enabled availability zones.With this approach, wherever the client request lands, it will be served from a Multi-az backend targetgroup or groups with NLB.As mentioned earlier, this capability disabled by default and you can enable it.And as discussed in the LP section, Amazon, Route 53, based on its health checks status, it sendsthe IPS of the healthy nodes only to client DNS requests.In this design, since the cross load balancing is enabled with NLB, it should be fine if the NLB inone AZ has an issue because the other NLB in another AZ will still be able to distribute the requestacross the target instances in different Azs rather than constrained to the instances in one AZ.That being said, if the design is intended to be zonal design in which you do not want to distributethe request across Azs, then you can disable cross zone load balancing, which should be disabled bydefault with NLB.It means traffic will be shifted to the healthy NLB node and target instances in another AZ.In case of one AZ failure.Therefore, for such design, the sizing of the target instances should cater for such failure scenario.With auto scaling, it can be more cost effective as you do not need to provision fault capacity upfront.As you may remember, we covered Keppel in a couple of previous sessions about AWS Privatelink and howto provide secure private connectivity between Amazon Vpcs and AWS services and on premises applicationson the AWS network.AWS Privatelink also allows customers to create an application in their Amazon VPC, referred to asa service provider, VPC as represented here and offers the application as an AWS private link enabledservice or VPC Endpoint Service.A VPC endpoint service lets customer.Or maybe it could be internal users that you have in your organization that you provision it to themwith some separations.It's enable you or customers to host a service and have it accessed by other consumers using AWS Privatelink.And as you can see in this design, the network load balancer is a key component here.The front end, the application instances and proxy the connection through the AWS privatelink.And this can be extended even to on premises user over a private hybrid connection like VPN or directconnect.We will discuss this architecture in more details as part of multi VPC architecture section.At this stage we just need to understand that NLB or the AWS Network load Balancer is a key componentin such connectivity model.