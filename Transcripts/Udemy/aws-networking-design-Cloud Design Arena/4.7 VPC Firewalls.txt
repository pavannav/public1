# 4.7 VPC Firewalls

So once I designed my routing everything, communicate with everything.The answer The answer is no.Let's find out why and how.So typically, you have your VPC, you put your Cidr and you picked your AZ.We'll focus into one AZ here for simplicity.And then you have your subnet and you have instance.At the subnet level, there is something called network ACL.Access control list.So typically this is where you define what is allowed and what is not allowed to communicate at a subnetlevel.So for example, if you allow TCP port, for example, for four three coming from anywhere, Okay.To your subnet.And so in terms of inbound rule, then the traffic will go back and then we'll be denied.Why is that?So typically the network ACL or NaCl is a stateless packet filtering of firewall in which that you needexplicitly to define the inbound and outbound return traffic in the same manner or outbound and thereturn traffic.So you need to take in consideration this and this is very important and it's at the subnet level.So later on we'll take into consideration how to design it.It's not an instance level, it's a subnet wide and you have to allow any communication that needs tohappen at the subnet level.Otherwise it will be blocked.The other layer or aspect that we can have and we must have is the security group.And the security group is at the instance level.In fact, it's at the interface level because if we have instance with two Enis, we can have two differentsecurity groups associated with these different interfaces.And so it's a very powerful tool to define fine grained rules.I would say it's the equivalent concept or it's even the very early application of the concept thatappeared recently called micro segmentation.And this is available since years in AWS and Amazon VPC.So it's a true micro segmentation because now I'm doing filtering at the interface of an instance orany service that can be attached through an ENI to the VPC.And the good news is if you allow the traffic inbound, the return traffic will be allowed as well becauseit is stateful.So that's important aspect.And similarly, if you define a rule outbound, it will be allowed inbound automatically due to thestateful capability of it.And even more than this, it has the ability to reference other security groups.Let's describe it with a sample design.So we have here in the right side of the slide a typical maybe 2 or 3 tier architecture.We have a load balancer, we have front end instances and we have back end.And each of these layers, a component has its own security group.So instead of I'm going and define the IP address of those I can reference, I say any traffic comingfrom security group, for example, of the load balancer or front end instances allowed on this port,for example.And the return will be permitted automatically.So this gives us also the simplicity in the situation where, for example, let's assume the front endinstances has autoscaling and those instances are scaled.So in such scenario, if you do it based on IP, it will be difficult because those IPS, they willbe allocated automatically and when these instances goes, these IPS will disappear.So we cannot control.So now we don't deal need to deal with these IPS.We just know those instances part of a security group X or Y reference that security group and the history.So this is very, very powerful to do referencing between security groups.All right.So now it's time to look into consideration of both network, ACL and security groups from design pointof view.And it is very, very important when you put your design.So first of all, it's important to remember what we mentioned.Network ACL, Knackles, stateless security group stateful.So this important to tell you which one you can go deeper and which one you need to define more andallow, etcetera.And the other aspect is an ACL.It's at the subnet level while security group at the instance or specifically at the level.So that also important in terms of how many and where you assign it and how fine grained it can be.And the most important one, I believe, is the scale.Because if we look at the maximum rules we see like knackles are limited, while security group canprovide high scale in terms of number of security groups.So as any other service capabilities when it comes to limit, always refer to AWS documentations andquotas because these always they change and typically increase.But in general, you can tell the difference and.Because of this, I would say a limited number of rules you can define as the network ACL.You need to make sure it is not something that you go and define very specific rules, what is allowed,what is not.Maybe just decide whether you would like to use the approach of blacklisting or whitelisting.For example, if it's a blacklisting, you blacklist certain sources that or maybe ranges that you don'twant it and then allow anything else, or if it's whitelisting, deny everything, just allow certainranges to come in into that subnet and that's it.Then at the security group you can go maybe very specific from where on which port, etcetera.So you need to find a strategy to cater for that.And last but not least, is the ability to be referenced as you remember.So with the network ACL, there is no reference ability while with the security group you can referenceit even across Vpcs with VPC peering.We'll discuss it later.So this is very important because once you can reference, your design can be simplified and provideyou abstraction of worrying about what's the IP from the source and destination you just look into thesecurity group allocated to each source and destination.