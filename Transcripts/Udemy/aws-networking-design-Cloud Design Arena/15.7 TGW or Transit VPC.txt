# 15.7 TGW or Transit VPC

So previously we discussed about the trans VPC architecture and we highlighted a few limitations, especiallyif it's used with VPC peering when it comes at scale as well as some performance implications.There is a heavy reliance on VPN, IPsec VPN tunnels in which the capacity will be limited to the capacityof these VPN tunnels.And then we covered the benefits of Transit gateway that add scale to the solution, as well as simplicityand more flexibility in terms of routing control.ET cetera.So you might be thinking, okay, now Transit Gateway is a replacement to the transit VPC architecture,or I don't need the transit VPC architecture anymore.And by using this architecture, I will be able to replace it.But the reality is it's no so transit gateway, even making the use case of a transit VPC architecturemore flexible.Let's see how so?By integrating a transit VPC here, for example, called Transit Edge VPC.The transit VPC could be serving multiple purposes.You might have a shared services.Edge Transit VPC and you have my maybe Ingress, Egress, VPC to the internet and you might have transitVPC for the connectivity to your on premises.Connectivity for the hybrid connections.ET cetera.And then Transit Gateway comes in the middle.So rather than relying on these a lot of mesh of point to point IPsec VPNs and limitation in terms ofperformance or ability to do route update ET cetera.We can use transit gateway in the middle, which will give us flexibility and best of both to a highdegree with such architecture.As you can see here, we have an edge VPC that transit VPC considered ingress egress, VPC to the internet.In this sample or example scenario and this architecture using VPC attachment directly to that transitedge VPC.So one consideration to keep in mind that, for example, if you have appliances and you would liketo consider some failover here, you need to think about monitoring those appliances and the route anddo some type of automations to achieve failover to other appliances if required and to make it moreautomated and maybe failover aware if you want to maintain flow consistency to the same appliances forinspection, specifically for stateful inspection, you could consider source Nat Also we will discusslater in the subsequent sessions about transit, gateway, appliance mode, etcetera.You will see how this can add advantage to the design and the utilized availability zones.ET cetera.Where these appliance are sitting.Also in this design, keep in mind that you are going to have one transit gateway attachment per availabilityzone.The other design option here is to connect via VPN to those appliances in which you take advantage ofthe ability to add more appliances easily, and you can even add more than one virtual appliance peravailability zones for horizontal scaling if that's something required.With such a scale and across multiple availability zones as well.And by using dynamic routing BGP over these tunnels and taking advantage maybe for equal cost multipathingor not depends on your design and the type of traffic forwarding dynamic routing will give you the abilityto have dynamic failover as well in case of failure or root disappearance.And even such architecture may integrate with on premises as we discussed.So maybe you have this as an edge transit VPC where you terminate the VPN and you do some inspectionand then you send it back.So the use cases are different and the idea of a transit VPC, it's providing certain functions so youcan have multiple transit vpcs as long as the requirement defined and using transit gateway will addadvantages.And it's not about replacing the purpose or the use of transit VPC.So it's better to think about using them together as much as you can and where possible.And the subsequent sessions we'll discuss more around centralized and localized combination of securitycontrols, and you'll see different scenarios of using such edge slash transit VPC with Transit Gateway.