# 9.4 AWS Transit Gateway TGW Part 1

Time for AWS Transit Gateway.Either.This transit gateway is a scalable regional service that enables you to connect multiple vpcs with eachother as well as with the on premises networks over CSI, VPN or direct connect using a single centralizedgateway.It is important to note Tgw, which is the abbreviation of Transit Gateway, is available within a regionnot like a direct connect gateway that acts as a global service.It's still with Transit Gateway.We can achieve global reachability by by using transit gateway peering.We will discuss more when we start discussing more around multiple vpcs architectures.Technically, the transit gateway acts like a virtual router in concept, but technically it is morescalable than an actual virtual router because Transit Gateway uses AWS hyperplane system and the backend, which makes it very scalable service.If you are not familiar or aware of what is Hyperplane with regard to AWS Hyperplane system, thereis a nice intro description in one of AWS Reinvent, so maybe you can go to YouTube and search it orGoogle just put Hyperplane system.Therefore, with AWS Transit Gateway, you only need to create a manage connectivity rules and routecontrols from one central gateway and typically per region.And when you expand, you may have multiple ones and we'll discuss different options and you can havetransit gateway manager cetera, but in general you can connect thousands of vpcs at time of recording.I think you can connect up to 5000 vpcs to a single transit gateway, which is huge.From connectivity model point of view.Transit Gateway creates a hub and spoke connectivity model where it acts as the hub that control howtraffic is routed among all other connected network which acting like spokes.So this includes the connectivity to on prem.That's what makes it very flexible and interesting.This approach typically simplifies management and reduces operational overhead because each VPC andon prem network only has to connect to the Tgwu or Transit gateway and not to every other network.So here we are eliminating the need for having mesh connectivity, which is a key element factor thatcreate complexity at a scale.This ease of connectivity makes it easy to scale your network architecture as you grow.If you are not familiar with Transit Gateway, it is important to understand its components and howit operates.So let's start with IT component.So there are at least four components that we need to touch with regard to AWS Transit Gateway, whichis the attachment route table association and route propagation.All right.So let's start with the attachment which refers to the connecting element to the transit gateway, whichcan be a VPC and a direct AWS direct connect appearing connection with another transit gateway or aVPN connection.A transit gateway attachment is both a source and destination of packets.That's interesting because this is how it act when it comes to routing operations.It can be a target or it can be a source of a packet.And typically you can attach multiple different elements to the transit gateway as attachment, likeone or more vpcs one or more VPN or direct connect connections, one or more direct connect gatewaysor one or more transit gateway Peering connections.And I mentioned VPN and direct connect connection.So typically the direct connect connection comes through direct connect Gateway.So let's not confuse that.So the direct Connect gateway represent the connectivity to direct connect.Route table, on the other hand, performs typical routing table function like any other router androute table includes dynamic and or static route that decide the next hop based on destination IP addressof the packet.The target of this route could be VPC direct connect, which is direct connect, gateway or VPN connectionand ultimately its attachment.So the next hop will be attachment, but the attachment represents one of these element Transit Gatewayoffers the ability to have multiple route tables and because simply we can have multiple route table,we route tables, we can do more advanced route isolation and traffic engineering designs and use cases.We will elaborate on this more, so let's park it for now.Association here refers to the linkage between an attachment and route table.Typically, each attachment is associated with exactly one route table.So here we have 1 to 1, while each route table can be associated with zero to many attachments.So here one to many or zero to many.So what that means, that means, for example, in this picture, we have one route table and that routetable associated with two attachments.While each of these attachments can be associated only with one route table.On top of that, we have root propagation.This referring to the way that routing IP reachability information being shared with transit Gateway,a VPC and different on premises connections like Direct, connect and VPN.So let's analyze this a little bit further and understand how actually route propagation work with regardto Transit Gateway.Simply with the VPC, you can create static route to send traffic to the transit gateway by settingthe next hop or target in the VPC route table as the ENI presented in the VPC using static routing.So this is important.So from the VPC perspective, you need to use static route and you point to the ENI that is representingan attachment of transit gateway from VPC to Transit Gateway.Route propagation.The Cidr blocks of the VPC are propagated into the AWS Transit Gateway route table using internal APIs,not BGP.Here, you can enable or disable this route propagation capability for each transit gateway attachmentin which here you will need to add static route per attachment where propagation is not enabled.This might be useful in scenarios where, for example, you want to provide partial reachability andnot full reachability to the subnet range or subnet subnet within a VPC.For example, with the VPN connection or gateway connection.So here presented via the gateway.So this represents the direct connect connection typically.Route are propagated from the GW Transit gateway to the on premises using BGP.So in both cases we will be using the transit BGP.However, we can use static route with VPN.So let's remember this.We will look into these different use cases later on when we will have a separate design scenarios withregard to these two hybrid connectivity options.When you want to have peering with other transit gateway.Typically in another region you need to create a static route and the transit gateway route table topoint to the peering attachment based on this one key consideration to keep in mind that VPC staticroute to Transit Gateway needs to be updated to reflect any changes in the routing via the transit gatewayor maybe using some routing that covers a wider subnet range.You can reduce, for example, the changes here.However, remember that using summer routing, sometimes you create a blackholing issues.So make sure that you really know what you're doing exactly in terms of what subnets and ranges youare, for example, steering to the certain direction, a target that could be attachment or going toon prem or the other way to a VPC with the black hole.Summary This might be a use case that you can deploy.For example, you want to block certain subnets rather than using ACLs.Maybe you create a similar route and you black hole that traffic and you send it and there is no forwardingfurther.So this is a possible use case as well.Also with AWS Transit Gateway, you can have the ability and flexibility to create multiple route domainsor tables.We mentioned this earlier, so let's elaborate more on this.So if you are from networking background, this is something similar to VRF concept virtual routingand forwarding where you can selectively leak routing between these different routing domains so youpropagate route.This is useful, typically and flexible in some scenarios where you need to do separation.We will discuss these different use cases, especially in the multi VPC architecture section where thereare designs that take advantage of this capability, such as route separation, to integrate with differentvpcs and security controls.A simple example where this can be useful is the one presented in front of you where we have VPC, Aand B both can communicate with the on premises network over VPN or could be the connection while theircommunication between these vpcs is not allowed in this architecture.And this is typically done using raw tables.So as you can see, there is no firewall or any excess list.Simply here is just raw propagation between route table associated with the VPC.So there is no direct propagation between these two route tables of the vpcs.Communication.In such case, as you can see it direct between the Vpcs and the on premises, but not between thosevpcs directly.