# 6.11 Application Load Balancer - ALB

All right, time for application Load Balancer.An application load balancer functions at the application layer.The seventh layer of the Open Systems Interconnection OSI model.After the DNS, resolve the name to an IP or IPS of the Alb.The load balancer receives a request.Then it's evaluate the listener rules in a priority order to determine which rule to apply, and thenselect a target from the target group or groups for the rule action.Technically, you can configure rules for your listener that forward request based on the URL in therequest.This enables you to structure your application as small as services and route requests to the correctservices based on the content of the URL.For example, when the request has the forward slash videos, it will be sent to specific target groupwhile requests with forward slash images.For example, the request goes to a different target group based on your application.Also, you can configure rules for your listener to forward requests based on the host field in theHttp header.This enables you to route requests to multiple domains using a single load balancer.Let's zoom in into the Alb and discuss it in more details.Like network load balancer.Alb supports both instances registered with a target group with an instance ID or IP target that canbe in your VPC or even on premises.In addition, you can register your lambda function or functions as target with the Alb and configurea listener rule to forward requests to the target group for your lambda function.When the application load balancer forwards the request to a target group with a lambda function asa target, it invokes your lambda function and passes the content of the request to the lambda functionin Json format.You can also choose to send the traffic to different lambda functions based on host or your path andthe request that comes to the Alb.This provides typically a very good front end design with event driven and serverless architectures.This is especially useful for hybrid application where requests from specific domains or URL are handledwith instances, for example, EC2 instances or even legacy on premise host, for example.And other requests are handled with serverless infrastructure provided by AWS or fronted by AWS lambdafunction.And this is one way to front end Serverless Solutions with a lambda function.We discussed in a previous video session part of this section more details of other design patternsalong with API gateway so you can refer to it if you did not watch it.Continuing with the Alb.And now let's focus into the connection, specifically the secure connection.The application load balancer allow you to create an Https listener which uses an encrypted connections.This is a common use case where the traffic is encrypted between your load balancer and the client thatinitiate SSL or TLS connection.Then the terminate the secure Http connection and establish an encrypted connection with the back endtarget group.Also, this is referred to commonly as SSL offload.When you when you create an Https listener, you deploy SSL or TLS server certificate on your load balancer.Typically, the load balancer uses the server certificate to terminate the front end connection andthen decrypt requests from a client before sending them to the target back end target.If you are wondering whether this secure design or not.Same to the and I'll be discussion.We had the simple answer yes for many scenarios because the Elbe is the front end point to the backend instances and the outside users cannot reach the back end instances directly unless you configureit via other route, like a firewall management host or maybe another ENI in the instances.The other question here is why the Alb needs to decrypt the connection.The main reason why the Alb needs to decrypt the request or connection is because it operates at theapplication layer of the open systems interconnection OSI model and it needs to inspect the requestto perform request routing.You can use ELB to route, for example based on Http headers, methods, query parameter, etcetera.That's why when you use Alb to load balance your applications, SSL, TLS termination is done at theAlb and typically the connection between Alb and the back end application is left unencrypted.However.If you need end to end encryption, you still have to terminate SSL at the Alb and also have an SSLcertificate installed on the target installed on the target instances.The target instances certificate could be something like a self-signed cert since only the Alb is goingto see that certificate, not the client.And in such case you will have technically two separate connection.However, both of them are encrypted, so this provide end to end encryption in transit if that's somethingthat you require.Continuing with the security capabilities.There are a couple of also important capabilities worth mentioning here.An Alb can integrate with AWS Web application Firewall WAF, which helps to protect your web applicationfrom common application layer exploits that can affect availability or consume excessive resources.So this is an additional layer of security that can help you to add to your solution an applicationif needed.Also provide the ability to authenticate users of your applications through the corporate or socialidentities before routing requests.This is one of the use cases of an API gateway and the different use cases of using API Gateway versusAlb or Alb in general was discussed in a previous session.Next, we will look into how to assess and build a load balancing migration solution from an on premiseslegacy load balancing solution to AWS Cloud.