# 7.3 Route53 Policies Part 2

Now moving to another type of routing policy of Route 53 DNS routing, which is the failover practicallyin many scenarios, including the ones that we discussed as part of, for example, the failover strategiesor options in the cloud you will need to have DNS to achieve, for example, active standby type ofarchitecture, also known as failover with failover.After routing you route traffic to a resource when the resource is healthy or to a different resourcewhen the first resource is unhealthy.So a typical primary, secondary or active standby type of scenario, the primary and secondary recordcatastrophic to anything from an Amazon S3 bucket that is configured as a website to a complex treeof record.And we will look into a couple of the design scenarios.As I mentioned as part of this sections, one of them utilizing failover as part of it.So as you can see here in the diagram we have a primary record that points to the, for example, primaryor active part of the solution.Okay?And when there is not failure, but let's say there is a problem with the health check related to theRoute 53 health checks.Okay.That that reported to the Route 53, then the request will start responding with the secondary record.All right.That point maybe to a different VPC that reside in a different region.Okay.And as you can see, for example, this might be a warm standby type of architecture where you have,for example, full-scale solution in your primary region in which that you, for example, invoke ascenario or failover, then you scale it up or out.Now we are moving into a little bit more interesting and advanced type of routing policy, which isthe weighted routing policy.From the name you can tell it's obvious it's about sending traffic to resources based on certain ratioin which it lets you associate multiple resources with a single domain name, example.com or subdomain,for example, production dot example.com, and then you can specify the ratio or weight of how muchtraffic to be routed to each resource.This can be useful for variety of designs and purposes, including load balancing and for example,testing a new version of a software you're doing like blue green deployment, canary deployment, etcetera.So or migration scenarios as well.It's very useful.We discussed similar approach in the section.To configure the weighted routing, you typically create records that have the same name and type foreach of your resources.Then you assign each record a relative weighted or weight that corresponds with how much ratio in termsof traffic you want to be sending to these resources, which translate into percentage.So you can see in this diagram there are three resources or elbs with different weights.This weight translates into traffic percentage, which is typically about the percentage of each resource.IP is returned to the client request, and it's not based on other metrics of a traffic like bandwidth.The formula is easy to calculate the estimated percentage.Okay, so let's remember this formula.It's about the way divided by the sum of all of the weights.And then you can and you can see the the total value based on the formula we just discussed.The estimated weight will be in terms of percentage of routing requests is 20%, 40%, 40%.If this record associated with health checks for for example, here in this scenario associated withthe different elbs that we have a in region A and region B and then a region B, for example, let'sassume the be facing an issue that is reported by the health check to the Route 53.Then Route 53 will exclude this record from the health record.And as a result, the ratio of the weight will change based on the coming new request.As you can see now, this is removed, so the calculation will change.And typically, based on the formula we discussed, the distribution will be about 33% will be A inregion A and about 66% to be in region A This is important to be aware of from design point of view,because you always need to design for failure.And if, for example, the workload is not designed to scale a region A to handle 100% of traffic ormaybe one of the Vpcs or Region A is not designed to handle more than the initial ratio.Then here it will be an issue from performance and scale point of view.What if we have a double failure?This is maybe something you also take into account in your design, but it has less possibility.Depends on the solution criticality.You can decide on the level of resiliency that you need to cater for in your design.And today cloud enabled solutions with a global reach.It is very common that you see there are requirement to do client request routing based on a geographicallocation.Specifically for global architecture, where you have two or more AWS regions and the architecture inwhich you can route traffic based on user location.And this could be driven based on latency, language, regularity, requirement, etcetera.And this design, for example, let's assume you want to route user's request to the nearest AWS region.So traffic from the UK, Geo or geography and two needs to be routed to an AWS region and the US whiletraffic from, for example, Egypt users goes to a region in the Middle East which is AWS Bahrain region.This architecture or this route distribution or routing is enabled by the Route 53 routing policies.That's called geolocation.The question that you might be thinking of now is how Amazon routing determines the location of theuser.Route 53 Support the end user client subnet extension of E DNS zero.The Recursor or local name server append Edns client subnet to the DNS query to make a DNS lookup onthe client source IP subnet.So now from the resolver you will get the IP address of the actual client where it's located.Without the DNS client subnet extension, these services would use a DNS recursive resolvers IP addressto determine the location.Commonly, Internet users will not change the DNS server setting where they will get DNS server fromtheir broadband provider, for example, which is typically near the end user geographically.However, for those users who use open DNS.Google DNS, for example, intentionally changing the DNS or DNS server, the DNS server may not bephysically near the end user when there is significant geographic separation between the recursive resolverand the client.This going to break the geolocation operational concept that we are discussing because there are 53will look the source itself from certain geography, but the actual user is in a different geography.So therefore the client subnet extension DNS is used by the recursive resolver to give more informationabout the location of the end user client.And it's typically based on IETF draft that you can go and read more about it.And this is another example.So here, for example, maybe we change the logic.We say traffic coming from a user in Europe goes to a region in Europe, maybe, for example, for dataregulations and for example, Europe and then for users.For example, in Asia Pacific, requests should go to a region that is deployed in Australia, Sydneyregion.So there are multiple use cases that you can apply with this logic, language based, location based,regularity based.ET cetera.