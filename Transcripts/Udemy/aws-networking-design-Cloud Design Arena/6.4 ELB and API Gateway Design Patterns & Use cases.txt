# 6.4 ELB and API Gateway Design Patterns & Use cases

From design point of view, a common question people ask is about using API Gateway versus NLB, specificallyusing Amazon API Gateway versus using AWS ELB, which is a valid question specifically from design pointof view.So let's start from the beginning and on the foundations of API Gateway, let's define it.Then we will go through some design use cases in which we can compare and contrast.So first of all, an API gateway refers to the API management.So API application programming interface API management software that is deployed in front of a collectionof backend services request to an application are routed through the API Gateway and the API Gatewayprovide common features such as authentication, authorization, request throttling, sometimes cachingand more.API Gateway can be implemented as a custom solution or a managed service.Like with the Amazon API Gateway, it's a managed service.Using an API Gateway introduces the ability to add features to your application without requiring the,for example, the code to be presented in your application.Okay.To support these features.For example, with an API gateway you can validate request schemas before passing the request back tothe service, thereby removing the need for the backend service to validate the incoming request schema.API, Gateway and Load Balancer can both be used to proxy requests to downstream targets.And this is where sometimes the confusion comes and this is the goal of this session is to clarify it.However, these solutions differs in terms of their supported features and intended use cases.API Gateway solutions typically offer different feature sets than load balancing solutions to a certainextent.For Amazon API gateway.The ability to introduce request validation rate limiting API keys, SDK generation or Proxying requestsdirectly to AWS service like Amazon Kinesis are all features that are not supported by any ELB.However, connecting to AWS services, there are some capabilities we will discuss it in which thatcan provide such connectivity, but it's not natively directly supported.Amazon API Gateway, on the other hand, does not support the ability to balance requests between anumber of downstream targets, which means it does not support traditional load balancing capabilitiesbetween or to different application nodes belonging to the same service.So this like in high level also offloading API authentication and authorization is another common usecase for API gateway.However, application load Balancer also support this, so we'll discuss it in a bit.And in front of you there are a list of common capabilities that is provided and used by API gatewaysand many applications, including serverless type of applications.Based on that from the from an architect or designer point of view, the question now needs to be thoughtdifferently.So you as an architect or designer now you need to think about the initial question we ask why?Why we don't use API gateway instead of load balancer.Now you need to think about it differently.Now you need to think about when should I use an API gateway instead of a load balancer?That was the initial question.And when should I use an API gateway in front of a load balancer as well as when should I use NLB infront of an API gateway?So now we have different type of use cases and structures and scenarios.To simplify it, we will go through a few common design patterns and let's see how we can apply differentdesign selections in which it will answer these questions.So let's take them one by one, for example.The first one from the left side.The requirement is Proxying requests directly to AWS services.So typically by using Amazon API gateway, you can easily.Request a proxy the request to AWS services.So that's an easy one.And then maybe the requirement is validate request schema and rate limiting with lambda back end.So that is most probably going to be an API gateway because it will provide these functions and canintegrate directly with lambda in the back end.Then you might have another requirement.Well you need a static IP with lambda back end.So what can you do here?Using Elbe in a specific application load balancer.Okay, You can get a static IP by using Global Accelerator, which is a capability provided by CloudFrontCDN and then associated with the application load balancer that a static IP that is globally accessiblefrom the AWS edge locations.And then application load balancer can have a target backend as a lambda function.All right.So that scenario is doable with application load balancer, then you might have requirement that say,okay, I want to validate request the schema or rate limiting with lambda backend.And then maybe from the lambda you do some processing and then the request need to be load balancedto backend instances.Now we have two different layers.So the bottom one is easy selection because it require load balancing and then the request.So we just introduce an ELB while the front end we could do it through an Amazon API gateway.So now we have a couple of layers.Using an API gateway in a place of load balancer can be a good fit for applications using Lambda asthe main compute provider or application that need proxy requests directly to AWS services or otherexisting Http endpoint.Using using API gateway in front of a load balancer can be a good fit for application that want to leverageAPI management features or offered by API gateway that are not supported by an Alb, for example, butstill need to distribute requests across a number of application nodes.Still, we have, if you remember one more question which is about when to use an ELB in front of APIgateway and this will be the.Coming use case scenario.So here, for example, let's say the requirement is that you use a static IP for API gateway.How can we achieve that?And then, for example, you need to access certain services.This can be achieved by having an ELB specifically application load balancer, and then the applicationload balancer can communicate back with the Amazon API gateway in which it will take that request andcommunicate back to the required other services.But here there are some technical routing networking design components.So let's go a little bit deeper and understand it more with the scenario.So if we look at the architecture here, we have from the outside to the inside, we will look at it.We have AWS, Global infrastructure backbone.And on the left top corner there is the pop or edge locations.Okay.Here where you're going to have the IP address of a global accelerator advertised.So this is a globally distributed by AWS edge locations.Okay.And that IP can be tied with the application load balancer here in our scenario.And if you remember from a previous session within a VPC, we can provision VPC endpoint type of interfaceendpoint that can provide private communication with AWS services.One of them is Amazon API Gateway.So now what are we going to do?We take advantage of the private IP provisioned for this VPC interface endpoint and use them as a targetIP in the application load balancer to communicate back with Amazon API gateway in which the requestwill be.Something like this.So the request comes from the client goes to the static IP that's advertised through the closest islocated and then back to the Alb and the Alb were routed to the IP address of the VPC interface endpointin which it will send it through a AWS privatelink, which is the capability that powers the communicationto AWS services from VPC endpoint that goes to the Amazon API Gateway and API Gateway for example,might be associated with a lambda and it does its function based on the solution or use case intendedfor.So now we have the use case of having application load balancer in front of Amazon API Gateway.It's quite interesting.Now there are a couple of different options and this is where the interesting part of being a designeror architect because you need to collect the requirement and what's the intended solution, Then youcan make the decision which one is more suitable.Let's take another scenario and some scenarios such as containerized applications.When you need to expose an application or a microservice to outside of a VPC, you could do it withAPI gateway.But in this model shown here, it's not secure because as you can see, the workload is in public subnetand may not be scalable way.So we need an abstraction layer here to be placed in between to simplify this communication as wellas maybe introduce type of security or a layer of security.This is where NLB can be placed.In between.The Alb is used from the containerized infrastructure to expose a service or services in which it canbe reached by the API gateway sitting outside the VPC without the need to put the workload in a publicsubnet.Still, this may not be optimal private way of communication that some companies may need, for example,to meet certain compliance requirements.VPC link is powered by AWS.Privatelink provides the ability to maintain the communication between Amazon API Gateway and AWS ELBprivate.As you can see in the design, ELB is used to front end the containerized workload.If you need to use rest API, then you need to use network load balancer and I'll be instead of Alb,which is again at the time of the recording.Therefore it's better always to verify with AWS documentation before building any design.This is this is also another reminder that always check with AWS documentations when it comes to anyfeatures that mentioned in these videos and sessions, because these the specific features they canchange at any time.However, as long as you understand the logic and the capabilities and how the architecture tie together,it should be fine because these features can change and may have minimal implications.If there is a major implications, these videos can be updated at that time.So in this design, by using the VPC link that provide the intermediate communication between AmazonAPI Gateway and a VPC, that is, for example, using load balancing NLB or Alb, you achieve the privatecommunications end to end and typically from outside of the VPC to the VPC link endpoint, it's poweredby AWS Privatelink.So this is another use case scenario where for example, you can have Amazon API Gateway works withelastic load balancing as well as you can, for example, optimize it to meet certain compliance requirementin terms of private communication.ET cetera.So that means you need to look at different layers in terms of functionalities.What functional cities these services are providing, as well as how the communication model going tohappen between these services and how you can optimize it if required.