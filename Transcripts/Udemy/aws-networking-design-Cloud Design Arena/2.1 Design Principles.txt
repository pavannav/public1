# 2.1 Design Principles

Let's start with some design principles.So typically as a designer architect, there are key design principles that you need to be aware ofthat you will typically need to consider it in your design.There are many of them, so we will cover some of the common ones that are important to be aware of.There are many different design principles we will be covering the most common ones used in common designsand architectures.First one is hierarchy.So adding hierarchy to the overall design and within each domain or block of a specific architecturecan significantly simplify the design and enhance its flexibility to introduce more efficient placesto isolate fault domains as well as provide seamless integrations.Modularity, on the other hand, is commonly used in software development, where an application canbe built from multiple blocks of code typically that collectively integrate to form the desired application.These building blocks of modular structure simplify the design and enhance its flexibility when it comes,for example, in terms of integration.So if an issue exists in one block or module of that software, it can easily be isolated from the othermodules and fixed separately without impacting other parts.Furthermore, from perspective of ongoing enhancements, it's easier to add additional modules or blocksto this structure if a new feature is required.For example, this makes it as an overall more structured and flexible as well as manageable.Application or software architecture.Similarly, modularity is one of the fundamental principles of a structured cloud infrastructure andnetworking in a structured cloud design.The architecture can be, for example, divided into maybe multiple functional modules that can be representedby different virtual private cloud, also referred to as Vpcs.Or maybe you structured the architecture based on different subnets, availability, zones and functionswithin a VPC itself as well.High availability.It's more around the service uptime.We will discuss as part of this section more around high availability and resiliency, where resiliencyrefers more to the ability to, for example, failover to another redundant component and how each,for example, impact the other one will be discussed in more details.But it's very important design principle.When you're building a design, you need to look into what was the service uptime or solution uptimeand where are the single point of failures, etcetera.Scalability.It's a common design principle.I'm pretty sure many of you know it and aware of it.However, you need to think about scalability in terms of today or current requirement as well as youneed to have future thinking to look into.Okay, what's the required scale in the near future, Longer term future, because this is maybe somethingyou don't take it into consideration for the current requirement and when the business decide to grow.The design is not built to scale and this is where it's facing a challenge in terms of may require completere-architecture.Simplicity.So this is, as a rule of thumb, always keep it simple.So this is around avoid overengineering.As long as there is a simple way to achieve the requirement from the business and applications, etcetera,just use that simple way.Don't try to overengineer it to make it more exciting and advanced because typically you may end upintroducing complexity around managing the solution or scaling the solution, etcetera.And as you can see, security is around everywhere.So when you design scalable solution or modular solution, etcetera, it's all need to take into considerationthat support security natively.So security needs to be considered as part of all the stages.And along with all of the design principle, whether you are adding security features as part of the,for example, initial solution or later on the solution need to have the ability to support securityfeatures and capabilities, integrations and ultimately by by going through these different design principle.And there are many different ones, you will have something like more optimal compared to somethingthat you just build it without taking these considerations into your account.All right.So now the question is, should we consider each of these design principles separately in the designor how they, for example, may influence or promote each other?Let's look into.Some examples, and this ultimately will help us to start thinking big, looking at the big picturerather than designing in isolation.So typically, by introducing hierarchy to the design, you will promote modularity.Because now you you introducing layers and the structure and these layers can be.Represented as modules.And ultimately it will create more flexible design.So flexible means as long as it's modular and structure, we have flexibility of doing changes and integrationinto specific parts rather than impacting the entire solution.And a common hierarchical design is when you have multi tier solution, you have maybe load balancer,front end, back end, etcetera.So does this means that we need hierarchy only to make the design modular of a flexible.The answer is not always because you may introduce hierarchy, but the layers are not flexible enoughto be considered as fully isolated or independent in which they can operate or integrate with othersystems unless they use one of the layers of that system.So here it's not really a modular design, but it operate in tiers.So this is something that you need to look deeper into it to identify whether it's a promote modularityor not, because this might relate to the actual technological solution.Similarly, by using hierarchical design that promote modularity, it can also help to reduce singlepoint of failures as shown in this sample design.On the left side.And ultimately it will offer enhanced high availability because when we have it structured this way.So let's assume the left side of the application is in availability zone A and the right hand side isan availability zone B as a cloud solution, or if it's physical in data centers or different racks,etcetera.So here we're talking in general.This means that such a structure help to promote the ability to introduce more available design, moreredundant design.So let's look into an example that is more AWS VPC focused design with regard to that.So this VPC has a almost lack.To the modularity and hierarchy in the design.And obviously there are multiple single point of failures here.So here we see like a layer with the Nat Gateway load balancer.And then then there is a subnet that has a private that has instances managed database, relationaldatabase, and as we can see, it's in a single availability zone.So there are multiple and I would say almost everywhere, single point of failures.And if we need to expand this design, we don't know what limitation we're going to face.By introducing a structure to the design where each service or function in its own subnet, and thenwe replicate it across different availability zones which make it more modular.As a result, it is more optimized from a high availability point of view.So in a brief, always look at the design principle from the lens of integrated designs or design ofintegrated systems and how each design principle can promote or support another one and not in isolation.Sometimes you need to look at the possible trade offs of using one principle versus other as well.A common example here is a scaling performance.It is common in systems design, but not always with high scale.You may lose some performance.This is where the business priority comes to the picture in which you can make a business driven designdecision in this case.