# 4.3 VPC Route Table

Okay, so let's look into the key differences between VPC route table and classical router Route table.There are a few differences which I want to highlight to understand the logic behind each of them.Let's start with the classical route table or the traditional way that route table works.As you can see here, we have a couple of routers and let's focus on the router at the top.So there are subnet.A provision to the connected interfaces, as you can see.And here also we have the network which is ten .1.60 slash 24 advertise from another router over BGPas a routing protocol.So here with this route table, it's a destination based route.Table.Why?Because the router will look into the routing table and then we'll decide where to send the packet basedon the destination that received it, based on the destination of the packet that wants to go to.So if the packet going to a destination.And the ten .1.60 slash 24.So typically the next hop is ten 142.And from forwarding plane point of view, it is through interface two two.As you can see, this can this behavior can be changed in a way or another.You can do source based routing, doing tunneling mechanisms, doing policy based routing, etcetera.But that's not the, I would say the default and traditional or classical behavior of a router or routetable.All right.So this is the router route table, how it work typically.Now let's look into the routing within a VPC.So we've discussed the different couple of routing options in the previous video session here.We will just focus more into some operational aspect of such routing behavior.So in this example, as you can see or design, there are three subnets each associated with a routetable.And let's start with the one on the left.As you can see here, it has a public subnet, which is the in this case, the instance can access theInternet directly.And typically with this design, when it has a public subnet, as we discussed previously, the networkaddress translation netting from private to public IP is performed at the Internet gateway of the VPCby default without any intervention.And as you can see as well, from the route table point of view, the default route point to the Internetgateway of the VPC.On the other hand, let's look on the right hand side, the ASB, where we have two subnets.So let's start from the bottom where we have the instance that reside in a private subnet associatedwith route table B, and this route table has the default route point to the Nat gateway that is ina different subnet.And from the Nat gateway we can see the subnet associated with it.It has the default route point to the internet gateway.So now by doing such association, we steer the traffic to take different path.Let's elaborate more around that.So up until now at the route table, it is still destination based.Yes, that's exactly same behavior we discussed with the classical route table.But let's look at it from a different point of view.So it's still the same logic.We have a couple of instances X and Y, we have couple of route tables and we associated those routetables with route table A that has the default route point to the Internet gateway.So now easy if the destination going to anything other than the Cidr local Cidr block of the VPC, itwill be forwarded based on the destination to the Internet gateway.However, we may change that and we associate an instance Y with the second route table.This could be the reason based on for example, a requirement that this instance needs to go throughdifferent paths.I want to send this maybe through Nat Gateway or maybe through certain security inspection, appliances,etcetera, as we're going to discuss later in this course.So with this change, what we achieved now we are controlling the source.Now I want to route traffic from those sources over different path.So this is the beauty of the VPC routing and route table.It is source based based on the association.So now I have subnet and these subnets as host and depend on where I associate them in terms of routetables, I'm controlling the next step, which is where they will be routed once they are associated.Now their route decision will be based on the destination.It will be destination based in this case.So I hope this clarified the slight differences.But it is just important to know about this because in fact this is a flexibility provided to you.You don't need to do like policy based routing or any type of tweaking and tunneling.It's abstracted to you and you just need to utilize such flexibility to achieve both destination andsource based routing based on your desired requirement.