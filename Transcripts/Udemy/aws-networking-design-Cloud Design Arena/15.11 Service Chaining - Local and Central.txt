# 15.11 Service Chaining - Local and Central

All right.So in a previous video sessions, we discussed different use cases and a few scenarios around usingcentralized inspection and security control.The question might arise that what if I want to do localized security inspection on specific vpcs thatare that that is specific to that VPC requirement?And then I have a centralized inspection that might be at the edge, which is something global likeall the vpcs that need to go to the internet has to go through that inspection.So now we have two layers of inspections VPC level that is more customized to the workload and thenedge inspection.That is something unified across all the different vpcs or different type of workloads.So how can we do that?So we will look into a scenario where in a simplified way, so we're looking to a single VPC, thenwe'll zoom in into a single availability zone and a few subnets and one edge VPC with a couple of virtualinstances, and there is not enough capability as well.So at the VPC level, on the left hand side, we have AWS network Firewall.It's a managed firewall with firewall elastic network interfaces or Enis that is gateway load balancertype of any provisions into two availability zones where each associated with different subnets.So always when we need to do customized routing and control with services, it's better like we separatethose components in different subnets as they will be used as target in different route tables.So let's look into the the routing of such architecture from the left hand side all the way to the edge.First we'll look into the instances or the source workload route table where the source instances areresiding.This route table is simple.So there is the local Cidr ten 100 slash 16 and there is a default route point to the firewall and provisionedof the network firewall.And typically this would be the ENI within the same availability zone here in this design.So we have different Azs, which with different Enis, so we'll have a specific forwarding to each AZ.And when the packet goes to the firewall, it gets inspected and then we'll return back to that.And then the ENI will use its local subnet route table.Here we have another route table associated with that subnet.This route table also simple.It has.Local cider as well as it has default.Right now it's pointing to the transit gateway.Now we'll be sending the traffic to the transit gateway.And as you can see, the transit gateway has a couple of routes.So here we are not doing customized separations and multiple route tables for simplicity because it'snot the focus.So there is a 10.100 16.2, the source VPC and there is default route point to the VPC and the trafficwill go to that edge VPC from the transit Gateway.And as described in previous sessions, there will be multiple route tables at the Edge VPC, whereit forward the packet to the virtual appliances get inspected, then will be forwarded to the Nat gatewayhere.Or you might do nothing at that virtual appliance.So it depends if you combine the function or separate them and then ultimately it will be forwardedto that VPC Internet gateway when the packet gets returned and sent to the transit Gateway Transit Gatewaywill send it to the VPC where it's attached.And typically it will use the route table associated with that subnet.And in this route table we have the local Cidr as well as we have the most specific route of the sourcesubnet.So here we're focusing on one subnet, one easy.If we have multiples, we will define different one for the different destination subnets.In such case, and as you can see, ten 11.0 slash 24 which is more specific, This will be steeredto the firewall again and then we'll get inspected back.And when it's sent to that knee, the knee will see, okay, it's local route then will be forwardedaccordingly.And based on that, the forwarding will take the path as expected.So this is a sample scenario where you can do service chaining as localized and centralized, whichgives the flexibility, for example, to to meet a lot of different security requirements that you mayhave in your design or based on the desire requirement.