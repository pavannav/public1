# 8.8 CDN - Security part 4

All right.Encryption specifically encryption in transit is very important when it comes to cloudfront, becausehere where we need to handle Https type of traffic end to end.So let's look at how this is handled.End to end.Just to have an idea what's happening for the connection legs when you connect, terminate and reconnectto the origin.So typically a viewer submit an Https request to cloudfront.There is some typically SSL, TLS negotiation taking place between the viewer and CloudFront and theend.The viewer submit the request in unencrypted format.If the object is in cloudfront edge cache.Typically CloudFront encrypts the response and returned to the viewer and the viewer decrypted and canlook at the content.If the object is not in cloudfront cache cloudfront perform SSL, TLS negotiation with the origin andwhen the negotiation is complete, it forward the request to the origin in an encrypted format, thenyour origin typically decrypt the request and then encrypt the requested object and return it to cloudfrontin encrypted format.Again, cloudfront decrypt the response re encrypted and forward the object to the viewer.CloudFront also save the object in the edge cache.So here we are.Assuming this is a cached object and if it was not a cache hit or this is a cache miss in CloudFronttermination, the object will be fetched from the origin and then it will be cached, which means thatonce you are caching it now we need to think about what happens with that cached object.Is it encrypted or not?And this is we're talking now about encryption at rest.So typically an encryption at rest happens at the Amazon, CloudFront edge locations and regional edgelocations.So an encrypted SSD for the edge location and encrypted EBS volume for the regional edge locations.