# 11.3 Virtual Interfaces

After we discussed the physical connectivity options of AWS Direct Connect, let's zoom in and uncoverthe direct connect connection to understand what is going on under the hood and how it operates, orwhat are the different options to design it.This is where the virtual interfaces comes to the picture.We will discuss each type from architecture point of view and the possible use cases briefly and laterin this section and the following sections as well.You will be expanding on these concepts and see how it can be used and applied in different design scenarios.If you remember from the previous session, we discussed the physical connectivity options, and thiswas one of them, the most straightforward one.So how the routing and link association with AWS actually happens.First.Physically, we have the cross-connect between the customer router and the direct connect router.And this links need to be configured with subinterfaces each using 802.1 Vlan tagging from the router.Each subinterface with the Vlan tag will be mapped to something called virtual private interface inthis design or type of connectivity.Each private virtual interface then can be associated with a VPC using virtual private gateway or mightbe associated with direct connect Gateway as it's shown in this design.We will discuss later the considerations of using each of these options in terms of virtual privateGateway and Direct Connect Gateway as we will move into connectivity discussion with service endpoint.In this design, the forwarding plane or the data plane?Is it provided by the Subinterfaces with Vlan tag and the private virtual interfaces.While the control plane is provided by BGP peering to exchange routing information.Last but not least, from scale point of view, you can have up to 50 private vifs per direct connectlink.This reflects how many vpcs can be connected to a direct connect connection link or might be a mix ofVpcs and direct connect gateway using VW and Direct Connect Gateway.We will discuss the different design options and driver for such type of connectivity options as partof the multi VPC architecture sections.Here we have again the same logic in terms of the physical connectivity.We have the cross connect, the Subinterfaces, etcetera, and this link needs to be configured with82.1 Q in a similar manner.Also, we can have the virtual interfaces.In addition to that, we can have another type of virtual interfaces, call it a transit Vif transitvirtual interface.This typically require when you need to use AWS transit gateway with a direct connect that is goingto be through a direct connect gateway as it's shown in this diagram.All right.So the transit is mainly for attaching transit gateway from data plane perspective.Cross-connect and the VIPs are used for the forwarding, while BGP between the customer router and thedirect connect gateway is used to exchange routing information.From scale point of view.Keep in mind at the time of recording you can have one transit Vif transit virtual interface per directconnect connection.However, you can have up to three Transit Gateway per direct Connect Gateway.So this is something that you need to keep in mind as well as if you have a design you need to verifywith the direct connect and transit gateway limit or quotas, an AWS documentation typically.There is another type of virtual interfaces called.Hosted Virtual interface.So to use your direct connect connection with another account, you can create a hosted virtual interfacefor that account.Hosted Viv can connect to public resources or Amazon Virtual Private Cloud VPC and the same way as astandard virtual interface.That could be private or public public virtual interface.We'll discuss it next.Or even similar to the transit Vif.However, the account that owns the Vive is different from the connection owner.So remember here it can be public, private or transit Vive.All of them can be called hosted when we have two owners or two different accounts.And this is typically the architecture is common when you have larger scale of vpcs and different accounts.And maybe you want to create an account for the networking side that where you place, for example,the direct connect connection, the direct Connect, Gateway Transit, Gateway, etcetera.And then you start sharing this with different vpcs that belong to different accounts.So again, the account that owns the Vive is different from the connection owner.However, bandwidth will be shared across all all the Vive virtual interfaces because typically theyare using the same physical connection that belongs to the parent connection account.It's important here.Just remember that you do not mix this with the hosted direct connect connection.So this is hosted Virtual Interface connection, not direct connect connection.The hosted connection we discussed.It is a direct connect connection hosted and provided by an AWS direct connect partner.As you remember.So these two different terms do not mix between them because they serve different purposes.Now we are moving to the fourth virtual interface type, which is the public virtual interface.A public virtual interface can access all public services using public IPS.Which is a form of public peering where customers need public IP for the peering and as well as theyneed the public BGP number.However, it's still possible to peer with the private BGP number.Typically will verify your ownership of the public IPS and BGP number as part of the provisioning processbefore it's being approved.With public peering over public virtual interface advertises, the appropriate Amazon prefixes to youso that you can reach either your Vpcs or other AWS services.Using public ranges.You can access all prefixes through this connection for example, Amazon, EC2, Amazon, S3 and Amazon.com.Also, you have the ability to control routing policies with public Vif.So we will discuss it in the next video session around how you control the routing advertisement andscope of the advertisement.And as you can see, we have up to 50 public VIPs.Remember, this is a combination with the private virtual interfaces.So it is 50 in total.And you can use it for private and public.And it's not handled in total.In addition to one transit Vif that we described earlier per direct connect connection link.So let's zoom into the peering part and discuss it a little bit further.So here we have a cross connect and we're using virtual public virtual interface to access public resources.And typically you need public BGP number or private number from the range shown here.And always, as always, refer to AWS documentation to the most up to date numbers and specifics.Also you need public IP range for the peering slash 30 or smaller.You can also advertise it over BGP for reachability for sure.And you can do nothing or pat port address Translation If your private network which is typically willbe the case, needs to access public resources and you do not have enough public IPS.So for example, if you have only the the slash 30 IPS and then you don't have public IPS to assignto your resources that's in your private network or on prem that require access to public resources,then what you can do at your device, you perform nothing or port address translation in which the thispublic IP that you used it for peering, it can be used as well to access these resources as a sourcewith Nat.Also because you can access VPC resources through its public IP.For example, you can set up a VPN that terminates on a self-managed VPN virtual devices that has publicelastic IP over the public, and then over the VPN.You can access VPC private resources or you may terminate the VPN connection over the direct connectwith public Vif to a transit gateway.We will discuss this design option in the following section when we cover encryption in transit withDirect Connect.Just keep in mind this is a possible scenario facilitated by public virtual interface.