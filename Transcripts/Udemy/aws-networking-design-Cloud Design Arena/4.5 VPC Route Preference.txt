# 4.5 VPC Route Preference

But before we discuss this important topic, I just want to highlight this discussion is focused onthe VPC route table only.In a future session when we discuss hybrid network design, other routing priority or preferences willbe discussed that relate to other AWS network services such as Virtual Private Gateway Transit Gateway,in which you can complete the picture of end to end routing preference.All right.So the list presented in front of you.Has certain routing decisions and the order they are presented in, it's a sequential.So typically when you have route table and you have routing within a VPC, first of all, the localroute, the Cidr range is always preferred.So even if there is something longer, much in terms of IP prefix that overlap with the Cidr, the Cidrwill be preferred.So that means anything within a Cidr has to be local.Period.Then the next decisions for preference or route priority is the longest prefix match.So if you have a route that has slash 16 and a route has slash 24 and they have overlapping for sure,the 24 will take precedence because it's the longest match and then after that static route.So if I have.The same prefix one coming through static root and one coming through propagated root.As we can see, for example, the static root will be preferred.However, there is one point to highlight.There is a static root that references a prefix list, so you have the ability to define perfect listand then you reference it into your static root.So static root without a perfect list take precedence if it's overlapped with a static root that hasa prefix list referenced.Let's take a couple of examples to to make it clear.Okay.So in this root table, we can see like we have a default root, which is anything point to Internetgateway.And we have I just picked a random something assuming as a public IP that point to and maybe let's assumethis is a public range that you have it on prem or you want to access it through the on prem network.So in this case, the longest match will be preferred.Here we have another example.We have two slash 24 subnet 19216800 slash 24 one coming through transit gateway as a static and onecoming through the as propagated.So as we highlighted the static will take precedence here and will be preferred and the same conceptapply in terms of static route to different AWS services that listed in front of you.