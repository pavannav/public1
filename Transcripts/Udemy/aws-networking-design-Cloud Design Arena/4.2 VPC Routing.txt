# 4.2 VPC Routing

So in routing or traditional routing or typical routing, if we have, let's say, a router with multipleinterfaces and a LAN network connected to it.If an instance needs to communicate to another one, typically it will send a packet to that routerand the router should have a routing table.And then based on that, it will make the routing decision whether it's a connected network or it'sa remote network that I know about it through static route or any certain routing protocol.All right.So now the question that you might be thinking of is where do I need to configure a router in a VPC?So I start route between these subnets.The good news is you don't need to configure it because once you provision your subnet, there is alreadya virtual router that you can already use and route between traffic.And typically this router in order for it to to make that decision, it will need to use a route table.And this route table, there are two routes.The first one is always presented to cover the local Cidr.While the other one it's a default route which points to Internet gateway for Internet access.So in this diagram we see IG Icon, which stands for the Internet Gateway.So an Internet gateway and Amazon VPC serves two purposes to provide a target in your VPC route tablefor internet routable traffic so it gives you access to the internet.Okay.As well as perform network translation for instances that have been assigned public IPv4 addresses.So if you remember, we mentioned that you can allocate static or elastic IP to an instance.Also, you can make subnet as public in which every private IP will get a dynamic public IP and theaddress translation happen at the Internet Gateway.Then since we are dealing with design, we always question frequently, right?So can we have more than one right table?Then if we have a one route table, can we have multiple ones?The answer is yes.So in this design, the VPC has two subnets across two availability zones and each subnet is associatedwith a route table.And the fact is there are two types of route tables, the main route table and custom route table.So you need to know about this because it's important.So the main route table is the route table that come automatically with your VPC and it's control therouting for all subnet that are not explicitly associated with any other route table.This is very important because you might have a main route table and custom route table.So the custom route table is a route table that you create in addition to the one that comes by default.Then maybe you under the impression that, okay, I have these two route table, you do the configuration,but it doesn't work the way you want because the reality is if that subnet is not associated with thecustomer table, it will be associated by default with the main route table and might have differentconfiguration and routing.And as you can see on the left side on Asa, we have public IP mentioned.So this subnet also configured as public subnet in which instances will get dynamic IP, public IP addressesso they can communicate with the internet.Okay.So now if we look at the raw tables, you see like one with Internet and one without.So we'll get into this in a bit.There are some other considerations.Let's go through it.In terms of route tables, it's very important to be aware of.First one is a route table can be associated with multiple subnets.So if I create a route table, typically I can have multiple routes subnet.However, a subnet can only be associated with one route table at a time.Any subnet not explicitly shows associated with a route table implicitly associated with the main routetable by default.So this is we just covered it.It's important to remember this is, I would say, more of a deployment, but in terms of design, youjust need to know there are different types of route tables and you can do multiple route tables.Again, you are a designer and you still doing probing questions and now you asking why we may needmore than a route table.Okay, I can do it.But why?I would need it.So here we start going into more designs and use cases.So we'll cover a couple of use cases.So for example, you may want to have a private subnet that's completely isolated from the Internet,or you may want to have a subnet that instances can communicate with the Internet, but they cannotbe reached from the Internet.Or maybe for security requirement in the same manner you want to steer the traffic through securitydevices like virtual Firewall, IPS, etcetera.So you do some traffic engineering for certain subnet and not every other subnet.Or maybe you have some connectivity through on perimeter or two on prem, and that also need to be controlledin terms of reachability.And the fact is there are many use cases that you can think of.So these are just sample common examples.Don't think about it like, okay, these are the use cases.So these are just sample and we'll dive into a couple of them just to demonstrate how these can be designedfrom a VPC point of view, starting with the simple common one which is a private subnet.So one of the main driver to have a completely private subnet is security.So in this design, an instance that could be an EC2 or maybe honored as database instance or even unattached,any elastic interface of a service such as Lambda.So in traditional networking you typically need to do some tuning to VLANs routing configuration toachieve this design.And specifically with Amazon VPC, it can be done as simple as having different route tables.A route table here, for example, and the diagram shown in front of you, there are a route table,a route, table B, the route table B, which is associated with the subnet that reside in ASB has nodefault route, which means it cannot forward traffic to the internet and typically it's not configuredas public.So if you see the other one on the left side, we say public IP, but we know from from networking,even if it has a public IP, as long as there is no default route that can be routed, it will be isolatedfrom the internet.So it will have no internet reachability.So that's one way that you can achieve it and you can tell from design perspective.Now we are controlling the communication through the route table.Let's take another example.Let's say now I want the instance to connect to the Internet, but I wanted to connect outbound onlyand which, for example, I have some software update on that instance that need to run over the internet.But I don't want this instance to be reachable from the internet.So this instance can initiate traffic to the internet.However, it cannot be reached directly.So I use Nat Gateway.In this case in AWS there is a service called Nat Gateway.If you if you don't know about it.And now I can point my default route in the route table be to that Nat gateway again route table beassociated with the subnet and ASB and route table associated with subnet and Asa.So that's why we have different routing directions.In fact, when when you look at this from a networking background, you can't tell VPC routing withmultiple route table provide both destination based routing as well as source based routing.Because as you can see here, we have two instances and we engineer how each can have Internet accessusing different path or next, hop over.Now I want my instance.To go and communicate with a via a virtual firewall.So typically I might have a virtual firewall appliance and I want my traffic to go to that virtual firewall,then go out to the Internet.So simply you make the any of that virtual firewall as a next hop.For your default route and that route table that you associated with the desired subnet.I just want to highlight one thing.Technically, you will need additional configuration to make the outbound.The inbound properly working.There is something called ingress route table in which you point the traffic back to the firewall.However, let's not discuss this.Other sessions we will discuss this topic.So I don't want to touch into many topics too, and to confuse you.So let's focus on the route tables concept.The goal is not how to make the synchronous inbound and outbound.That will be a different session.So the goal is we have such use case scenario having multiple route tables, we can achieve it.Let's expand on the same topic, you may ask.Okay, what if the virtual firewall in the same subnet as the instance?So as we can see now, those instances in different subnets?What if I put the virtual firewall and the EC2 instance in the same subnet?So technically that default route will not work the static entry.So we need to do some tweaking here.You need to define a static route entry at the instance level.If you have a such edge use case scenario, I would call it.It's not very common for whatever reason.Maybe it's a workaround, it's something temporary, or you're trying to fix something or it's a requirement.There are options where you can define route at the instance level.However, from networking best practice design in general, it's better like to avoid having some routingdefinitions at instance level.It's better to keep it separated.So you have the separation of concerns and also responsibilities between, for example, DevOps or systemadmins and networking team.All right.So let's expand and take a deeper look with an example where we have multiple route tables we have onpremises side and how this can be achieved.So this is just like putting the concept or topics that we discussed about route tables together.So simply here we have our two top subnets in A and B associated with route table A in which they havedirect routing to the Internet.And also we have an ad gateway.The subnet residing in C has has has a route table B associated with it in which the default route pointto the Nat gateway.Also it has a route that point to VG.So VW stands for the Virtual Private Gateway.It's one of the networking endpoint and that you can use it to, to terminate your VPN connection.So just think about it.It's one of the way that you can establish VPN to your on prem side.We will discuss the different types of VPN termination point or connective or or service endpoint forVPN in more details and the private and the hybrid connectivity.So don't worry about it.Now, just I want you to think about it.This is where you establish your VPN, all right?And then you can have either static route entry or propagated.So propagated.This is for let's, let's, let's think.Here we are using BGP over the VPN and you would like to have propagation in which the route can alsobe dynamically injected in the route table.So that's a possible option as well.So the route can be either static or propagated.We will discuss the implication of those in terms of route preference in a bit.So it's a straightforward example.There is nothing complex, it's just showing the different route direction that we can have in a VPCwhen we have multiple route tables and there might be other use case scenarios for sure.We will discuss it in a future.Topics.