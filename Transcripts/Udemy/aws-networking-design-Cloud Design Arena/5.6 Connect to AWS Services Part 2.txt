# 5.6 Connect to AWS Services Part 2

Now we need to think about other services.How can we access it from a VPC?Private.Privately.This is where we can take advantage of Amazon VPC interface endpoint.An interface endpoint or interface.VPC endpoint allows you to connect to services powered by AWS Privatelink.These services include some AWS services services hosted by other AWS customer and partner and couldbe as well as services as part of the marketplace.In the previous session, we discussed about private links that powers the Gateway load balancer endpointand with the VPC interface endpoint, it is also powered by private Privatelink, which helps you toprovide private connectivity over AWS infrastructure without going through the internet and also withoutthe need to use public IP addressing.As you can see in this design, there is an interface endpoint provision per availability zone per AZ,which is a recommended approach for better resiliency.Also, it is important that you understand that by using interface endpoint, the communication withan AWS service.Cannot be initiated from the service to the resources in your VPC through the endpoint and endpointonly returns response to traffic that is initiated from resources in your VPC.And as a designer or architect, typically before you integrate a service using an endpoint review,the service specific VPC endpoint documentation for any service, specific configurations and limitationsto make sure that is everything.Supported in the way that you want to design it and configure it.As mentioned earlier, the interface endpoint is a parazzi in which you can have it in one subnet perAZ.As you can see on the left hand side of Asa, however, you can route traffic to it from any other subnetas long as the traffic is permitted by NaCl and security group and DNS configure correctly.So let's discuss more about DNS.When you create an interface endpoint.Generate endpoint DNS specific hostname for that endpoint that you can use to communicate with thatservice for services.The private DNS option turned on by default.Associate a private hosted zone with your VPC.The hosted zone contains a record set for the default DNS name for the service that resolves to theprivate IP address of the endpoint network interface in your VPC.So what this means, this means it will allow you to make requests to the service using its defaultDNS host name instead of the endpoint specific DNS host name.Let's assume in the scenario shown here, the private DNS for the interface endpoint is turned on.The route table for the public subnet has default route pointing to the internet gateway.In this scenario, the instance and the private subnet can use the interface endpoint specific DNS namefor that interface to access the targeted service.While the instance and the public subnet just uses the service DNS name and will be routed via the Internetgateway using IP to communicate the public public IP to communicate with that service.With the assumption that you already permitted the traffic communication through the Knakal and thesecurity groups as required for that service.And the traffic flow will look like this.If we turn on and the interface endpoint, private DNS, then instances in either subnet now can sendrequests to AWS service of that endpoint through the interface endpoint using either the default DNShost name or the endpoint specific DNS host name.Even the instances and public subnet has default route pointing to the internet gateway.The routing is controlled here through DNS, so this is important to know from design perspective.DNS is controlling your traffic flow for that type of communication with regard to AWS services.Unlike Gateway endpoint with interface endpoint, you can extend the service reachability over VPC peeringto remote peer VPC.However, you need to make sure that you take that you have taken into consideration some DNS setupfor reachability.So for example.So the main thing is you need to create a private hosted zone for DNS.And maybe here for example, as you can see in the text, the idea to keep exactly same name of theserver.So you might disable the private DNS and the VPC and then you create exactly same name, then throughthe host zone, then you share it with the other VPC.Or maybe you can create a separate private host zone with a custom name.Then you share that private host zone that points to the interface endpoint and share it with the otherVPC.All right.Also, it can be reached through VPN or direct Connect in which you can extend the communication toon premises.Okay.However, you still need to take into consideration DNS setup.And here also you may need to look into Route 53 resolver for hybrid DNS and the forwarder configurationon your on premises DNS server.The VPC peering to access AWS services, sometimes used in Japan spoke architecture where they haveVPC can be thought of a of a central VPC to access AWS services privately.Typically there must be a requirement driving you to design it like this, maybe less interfaces tomanage and pay and for example, less interfaces to pay for or could be easier in terms of securitycontrol.So it depends on the requirement, not necessarily this is the best way.However, it is a design that you may consider if you have such requirement needed and the peering canbe extended to a VPC in a different region.This is an interesting one because it means over peering you can access an AWS service in a differentregion privately.While this is not supported with the gateway endpoint, again you must have a specific requirement todo so.Otherwise it's better that each VPC should better each VPC to use the service in its own local region.This is commonly used when you want to expose like a service through interface endpoint as a serviceprovider.So you're providing maybe SaaS services, etcetera.This topic will be discussed in further details in the future.Just wanted to highlight this as a common use case that use in such way where you have peering to connectto another service, for example, that is available in different region through interface endpoint.Traffic using Inter-region VPC.Peering stays on the AWS global backbone infrastructure and never traverse the public internet.However, you need to make sure again of the configuration of your DNS and IP routing as well as thesecurity filtering In terms of netting.Sorry, Nat and Security group as well as if you have any other security function or capability in thepath.The same principles applicable when using transit gateway because the idea is about IP reachabilityalong with the required DNS setup.Please note using VPC Peering versus Transit gateway for your design is outside the scope of this session.There will be detailed discussion about this topic in a future session.The main point here at this stage, just to make you aware of the possibility of the different designoptions.And in the same manner the connection can be extended to on premises with our direct connect or VPNthat connect to the transit gateway.Like with the gateway endpoint, you can only access services in the region over an interface endpoint,so it cannot extend the connectivity to a different region.In which you may use one of the options discussed previously that utilize Internet Gateway.Or you could use VPC peering, if you remember, or Transit gateway that has peering with another transitgateway in that region to provide the connection privately.If you have a valid reason to use this option, it's something doable and you can look into it and itsdetails.So during the discussion you might notice that S3 was part of both types of VPC endpoints.Now, you might be wondering from design perspective, which one is the best option or is the recommended.So the typical answer it depends on the need and use case.To simplify it, let's have a look at this simplified comparison table from AWS documentation.As you can see, there are a few attributes different when it comes to using interface endpoint versusgateway endpoint for S3.How much the difference is significant.This depends on the use case you have.So for example, if we look at gateway endpoint, it does not allow access from an on premises, whileinterface endpoint gives you the ability to access it from on premises.So if you have a use case where your workload on prem and require access to S3 privately, that's avalid reason, for example, to consider interface endpoint.There is another aspect in terms of build and not build.So in order to to make it easier to understand in terms of design and architecture, let's have a lookat the scenario where we have a hybrid architecture and both the instances in Amazon, VPC and on premisessite need to access S3 privately based on the attributes of each.We can take advantage of both here.So instances.And the VPC can use gateway endpoint since it is not built.And technically those instances they can just use the default S3 DNS naming.While the instances and instances, for example, where the instances on prem, they can take advantageof the interface endpoint, which will help in this design to extend the private connectivity.From on premises site all the way to the S3 service over VPN or direct connect.However, instances on premises they need to use DNS endpoint specific naming and combine it with Route53 resolver for hybrid DNS setup.So as you can see, always the decision must be drive by the requirement and the scenario.We selected both options.So it's not always like one single option and it's the best.Always analyze the requirement and then look into the priorities, whether it's caused the priority,whether it's the optimal path, whether it's a throughput.ET cetera.Then you can make your best design decision.