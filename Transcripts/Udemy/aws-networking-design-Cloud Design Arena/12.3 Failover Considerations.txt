# 12.3 Failover Considerations

So now we are going to discuss the second consideration or aspect for reliable hybrid connectivity andresilient, which is the failover.Before we discuss failover, let's look at it in the context of hybrid solution resiliency with resiliency.We need the infrastructure to recover from a failure event automatically.In which first we need secondary component.This is what we discussed as part of the redundancy considerations.However, only having a redundant component won't make the solution resilient.You need to have the ability to fail over to the secondary component and this design we have two directconnect connections.If one of these links or edge routers fails and we can fail over to the other connection or maybe thefailover takes too long, we can describe the solution as resilient.Therefore failover or specifically the ability to failover is another key element of having a resilientand reliable solution.I use the figure shown in this slide from my Cisco press book and I always like to refer to it whenit comes to discussing network resiliency and failover times simply, as you can see, there are differenttimers and there are specified as T, and these are like the generic or main timers that influence overallconvergence time when it comes to routing failover and resiliency.The main one that I want to focus on here is the failure detection.This is very important aspect.You may have a routing design or protocol tuned to fail over very quickly.However, this failover operation won't be triggered fast if the failure itself is not reported fastenough.There are many techniques to speed up.The failure detection depends on the connectivity type, physical medium, etc. With hybrid connectivity,if you are using VPN, you may need to look into VPN peer detection.If you are working with direct connect connection, you need to look into bi directional forwardingdetection which allow for faster routing reconvergence time.So let's discuss it with some more details.BI directional forwarding is a detection protocol that provide fast forwarding path failure detectiontimers.These fast failure detection times facilitate faster routing reconvergence timers.Therefore, it is best to practice to enable BFD for fast fail failure, detection and failover whenconnecting to AWS services over direct connect connection.Asynchronous BFD is automatically enabled for direct connect virtual interfaces on the side.However, you must configure your router to enable this asynchronous BFD for your connection.Here there are some samples of the BGP tunable timers that can be deployed for failover at the sideand the router or customer router.So typically you can have for example with the default settings three Keepalives and giving that keepalivemessages every 32nd in the default setup that translate to 90s.So these are tunable.However, as you can see, these are in seconds with BFD, you can have in milliseconds okay with minimummultiplier of three.So in 900 millisecond you can report the failure.So that provide much faster than routing protocol.With VPN, you can use dead peer detection or BGP timers.You may have faster convergence with BGP timers.So it depends on the how fast you want to report and converge.With regard to routing protocol, just double check into the timers.So the main idea here is just highlighting the detections mechanisms and the ability to adjust the routingprotocol.And here in the discussion is BGP.To achieve that convergence required the convergence time based on the required failover.