# 11.7 DX with TGW

All right, now time for AWS Transit Gateway with Direct Connect Connection.If you remember from the previous session of Direct, Connect with the Direct Connect Gateway, we hadthis design architecture.So.Here by adding AWS Transit gateway to the design with Direct Connect Gateway, there is slight differencesfrom architecture point of view.We can see the virtual interface, the Vif now it's using different type, which is the transit as youknow from the virtual interfaces sessions discussion and the connection from the direct Connect Gatewaygoes via transit gateway and not direct to the Vpcs.So let's look at.What this simple change can add to the architecture.First of all, now we can connect up to 5000 vpcs to a single transit gateway.This means from the customer router point of view over a single BGP session and physical link, youcan access 5000 vpcs.That being said, this is not the recommendation here to connect 5000 vpcs to a single physical linkas this will be single point of failure as well as in terms of performance.This link needs to be sized carefully at scale.However, the point here is that you can do such a scale with Transit Gateway and Direct Connect Gatewaywith a direct connect connection.If you would like to connect to 5000 vpcs, consider resiliency for sure and consider performance asas it required.ET cetera.And we'll discuss this as part of the upcoming sessions when we discuss reliable, direct, connect,connection or hybrid connectivity.And as you remember, so over transit Vif here we can have 100 route propagated from on prem to thedirect Connect Gateway and 20 route from a transit gateway to the direct Connect Gateway.So direct connect Gateway support up to three transit Gateway.So which means we can receive 60 routes.So this is something to keep in mind.It's if it's a limitation, there are a couple of workarounds that we will discuss later on if thisreally something serious.Otherwise the recommended way is to use route summarization rather than you send specific route.If you can do summarization with a single prefix, you aggregate multiple prefixes that will help tooptimize the number of advertised route.Traffic using Inter-region Transit gateway.Peering always stays on network and never travels the public internet.So here in this architecture, we have transit gateway peering.If you look at the red line, this is where we can expand the number of connected regions, connectedtransit, gateway, etcetera, to a higher number or to multiple regions by using this peering.And as mentioned, it's over global infrastructure.And because it's over global infrastructure, it reduces the threat vector such as common exploits andDDoS attack.In addition to that, Inter-region transit gateway peering and encrypts inter-region traffic with nosingle point of failure.So this is an important consideration, especially if you have some regulated type of environment thatyou are designing for.Remember that transit gateway peering is a inter-region peering.So if you have such a requirement where you have a couple of transit gateway, we will look as partof the multi VPC design some considerations.So in high level, maybe you could, for example, have a transit VPC here and you have a managed VPNsolution.And through this managed customer managed VPN, you can do VPN termination to these two different transitgateway in which that you can exchange routing information between them as as an option.However, each transit gateway can scale up to 5000 vpcs.So I would say the the requirement of having more more than 5000 vpcs in a single region might be veryrare.But as a designer, you should always knows about where are the limitations and what are the alternativesfor sure.As a key consideration.Also to remember if the connection is a hosted connection, you know, flow through like a service,a service provider that is AWS direct connect connection partner at the time of recording, you cannotcreate a transit virtual interface on a hosted connection with capacity less than one gigabit.So keep this in mind if you want to provision less than one gigabit and you have planned to use transitgateway with transit VP, this is something that you need to keep in mind and maybe you consider havinghigher bandwidth or capacity for the actual link.All right.Time to look into the design considerations.Virtual private interfaces.Scales.Limited?Yes.Because we can have one transit Vif per connection.However, when it comes to scales, it's extremely high because we can have 5000 per transit gatewayand we can connect three of them to a single direct connect gateway that goes over a single transitvive, which means 15,000 vpcs, which is a huge number.Control plane is slow because single BGP peering session can connect to thousands of vpcs.And here we mentioned 5000 per transit gateway.So let's keep it generic in terms of thousands because it's not about recommending to have the maximumcapacity, but you can achieve it.This is the flexibility of transit gateway when it comes to manageability and scale.It's simple, provides centralized and even simpler with Transit Gateway manager, which is not discussed.But you can look at the transit gateway manager and how you can get a more simplified management toyour transit gateway solution.When it's come to design flexibility, it's very flexible for multi domain routing requirement, aswe discussed, and as part of the Transit Gateway Service endpoint when it was discussed, you can havemultiple routing tables, you can have very flexible routing control among Vpcs and we will discussmore advanced type of design with Transit Gateway and multiple Vpcs as part of the multi VPC architecture.So review these sessions for advanced designs.As you remember, in terms of number of routes, we mentioned small numbers.However, these numbers can change, can improve, but it's still something that you need to keep inmind.And if it's something that is really limiting for the design, there are a couple of options we willdiscuss later, like provisioning, some overlay tunneling, VPN or sd-wan to overcome that number.That limit in terms of number of route can be advertised.Also, there is cost consideration.So by using transit gateway, it incurs additional transit gateway processing costs for data transferbetween and on premises.So this is something also you need to keep in mind.