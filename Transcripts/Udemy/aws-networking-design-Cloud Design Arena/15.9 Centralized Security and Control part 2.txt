# 15.9 Centralized Security and Control part 2

Okay, so let's analyze the issue that were highlighted in terms of traffic hairpinning and the virtualappliances in the central or egress ingress VPC here.Call it the Edge VPC.So here this is more of a zoom in design.As you can see, there is an additional subnet compared to the previous design that is associated withthe transit gateway Attachment in the Edge VPC, as you can see it on the right hand side.And in this subnet or typically two subnets here, because we have two availability zones, there aretwo routes, one for the local subnet Cidr, and there is another word route.That is the default route, which is anything that points to the elastic network interface of the instancethat is acting as a virtual appliance or virtual firewall.So typically what happens when traffic comes in, anything hit that subnet coming from Transit Gateway.It will be forwarded to those appliances for inspection.And typically this subnet has its own routing table of the firewall, which is the inside, let's callit, where there are more specific routes for the VPC.Cidr blocks.The internal VPC is pointing to the transit gateway enis, and the external subnet has its own forwarding.So now let's not worry about that outside subnet because now we're not going to focus on the trafficgoing out.So here the point is that traffic will come in from one of the subnets that is has a destination likefor example, here from VPC, A the source going to be from ten 100 something going to 172 16 .0. somethingfor example.So once it hit that firewall, now there is possibility if that allowed, the traffic will be sent backand forwarded to that VPC because what will happen, this subnet will send it to the local based onits routing table configuration on the instance will send it to the private network interface.And that subnet has a route for these local subnet of these vpcs pointing to the transit gateway.And as you can see here, the orange light orange route table has the specific routes associated withthis VPC where it forwarded the traffic to the respective VPC.So now, in fact we have some sort of route bridging.We configure transit gateway to not allow the communication between these two Vpcs However, that routinghappening on the central VPC where we have virtual appliances and this could mess up maybe your routingdesign concept and maybe breach your security requirements.So how can this be avoided?One way to avoid this is by routing configuration, by introducing something called the black hole routeand the Transit Gateway.This is where you define specific routes and that to be black hole.So that route typically will be dropped based on the specific route being matched.So in this case, when there is a packet coming and distance to that private subnet, it will be droppedfrom the beginning as close to the source VPC as possible.So this is easier design and easier configurations.Or maybe, for example, you might you, you would like to control it centrally in which that you can,for example, block the forwarding based on source and destination IPS at your security appliances.So this can be done on different ways.Some firewalls vendor, for example, have the concept of defining zones and allowing and blocking trafficbased on source and destination zone.So the idea here is to control the forwarding at the firewall level.If you don't do the black calling of route concept at the transit gateway level.So hope this was helpful and it's an important consideration if you have such architecture of multiplevpcs and there is some route control and blocking between the vpcs in terms of communication.