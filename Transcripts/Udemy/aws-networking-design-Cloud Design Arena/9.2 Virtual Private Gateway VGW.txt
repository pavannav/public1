# 9.2 Virtual Private Gateway VGW

All right.So starting from where we finished in the introduction of the hybrid architectures and hybrid model,now we will start expanding these topics.And as we mentioned about the AWS services endpoint.But before we jump into these services, if we take a step back and we look at the bigger picture whenit comes to the building blocks, you can see like the three of the options expand.So this is what typically we are going to cover as part of this section.So when you look at it deeper, there are multiple service, multiple options of each component thatwe have presented in the previous slide.So with that, let's start jumping with the AWS network services endpoint, starting with the VirtualPrivate Gateway, Virtual Private Gateway or VW.So remember VW, we are going to refer to it a lot.It's a regional logical object or a service which is part of AWS software defined networking, and it'shighly available service redundant component within a VPC across multiple availability zones.So you don't need to worry about the redundancy and resiliency of it.It already has built in redundancy across multiple availability zones and offer distributed IP routingand forwarding at at a VPC level.In other words, it act as a gateway for the VPC to terminate IP connection or connections to communicatewith other networks such as other vpcs or it might be on premises site.VW is capable of terminating VPN connections as well as direct connect connection.However, with the direct connect connection, the physical connection and the VPC must be in the sameregion.We will discuss more about it in details as we go into the direct Connect connection and the specificdesign related to it.If you watch the VPC Networking Fundamentals session, you may remember this basic design.So here we are just doing a quick review specifically for the route table or tables of a VPC.As you can see here, for example, we have a static route or dynamic route in VPC terms called propagatedroute, where the VW propagates the associated route table with the route that it receives or learnsthrough the remote end like over BGP routing session with your on premises router or firewall, maybeover a VPN connection.So this is how you inject the route back to the VPC.Or you can statically point to the VW by using the VW as a target.And there are table.Also from routing section of the VPC networking fundamental.The route priorities was covered from the VPC level point of view.Now we will be expanding this view to the other networking services as each it has its own route, priorityor selection order.As you can see from view, this is at the VW level.Always with IP routing.Longest match is a preferred.That is something given.So we will see.This is always the first one.Then if you have two IP prefixes with the same subnet length, there is another order to decide whichroute is preferred.First BGP route coming from a direct connect connection is preferred, then static route defined fora VPN connection.And last the route that is propagated via BGP over a VPN connection.So let's look into a couple of examples to simplify the concept and make it more design style.Rather than we just look into orders.So in this simple scenario, there is a VPN connection and a direct connect connection with VW.Same IP prefix received over both VPN and direct connect.All right, so now we see there is prefix or based on static route and the other one BGP.However, both of them, they are the same length based on the VW route priority.We just discussed.The route over the direct connect connection will be preferred to reach the subnet.Typically.Let's expand on this.Let's look into a little bit of a variation around this and let's assume VW receive different prefixlength over BGP, over both connections.In this case, all traffic going to the most specific, which is the 192168.1.0 slash 24 subnet willbe going over the VPN connection because here it is the longest prefix match.While the traffic going to something beyond that subnet.Typically we use the BGP session that is going over the direct connect.These are simple examples, but it's important to be aware of it and understand the writing preferences.VW Which will help you to understand more complex traffic engineering design scenarios that we are goingto discuss.So these are simple, however very important, to build up the knowledge and logic of designing andtraffic engineering.So this was an intro to VW.There are still a few aspects related to VPN and direct connect that will be covered when we start discussingVPN and direct connect and separate sessions and we dive deeper with each connection type and its design.