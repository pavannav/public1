# 6.12 Security Filtering Considerations

All right.So this is going to be a quick video session.However, it has an important aspect that worth highlighting.Let's start with a quick reminder of the security group functionality.So as you remember or maybe you know from your experience, a security group provide firewalling capabilitiesat the instance level or specifically at the elastic network interface of an instance, the ENI.So if the traffic, for example, coming in and that traffic is allowed by a rule as part of the securitygroup, it will be automatically allowed outbound, outbound.And this is typically the attribute or characteristic of any stateful firewall in capability.And this is what the security group can provide you.All right.Let's look at security groups in the context of load balancing and ELB, and we'll look at it from thelenses of Alb and NLB application load balancer and network load balancer because they work or behavedifferently.Or the or the capabilities available in one of them and not available in the other.So with application load balancer, for example, let's say we have here multi-tiered application andwe take advantage of using security groups and referencing these security groups.Okay.In terms of allowing traffic from.Instances or be in one security group coming to the other and vice versa.So this simplifies the configuration.So now when we have auto scaling, etcetera, we know you don't need to worry about to track the IPSof instances.It's a matter of we reference the security groups.Also, you need to allow, for example here the health checks traffic from the load balancer in thesame manner, not only the traffic in terms of the type of listener port that you are allowing for yourapplication.With the NLB, it's a bit different because with NLB you cannot associate a security group with it.And because of that, the security groups for your target.Must use IP addresses to allow traffic from the load balancer as well as from client in this case.So you need to allow the VPC, Cidr typically or for example, if you have instances behind the NLBin a private subnet and they need to accept traffic from the internet, you may need to allow the 000/0,which means any source IP.However, this should not be a concern in terms of these instances going to be reached directly fromthe outside for these instances has no public IP.Typically in a private subnet your instances.Also the connection cannot reach them unless it goes via the NLB listener port that you configured.So from the connectivity to the instances directly, this is not something to worry about.It's a bit different than classical networking logic, but it's important to understand how it worksto avoid any confusion.Also, you need to allow health check traffic from the load balancer by either allowing the VPC Cidrrange or by checking, for example, the allocated IP or IPS to the NLB node and then allow them directly.Last but not least, the network ACL need also to allow traffic inbound and outbound in the same mannersince and since it's a stateless as you remember from the networking fundamentals of the VPC.Okay.And it's at the subnet level, you will end up doing the allow and deny in different direction, typicallyin the inbound outbound because it's not like the security group, it's the stateless.So if you would like to see the specific recommended configurations refer to AWS documentations, thereis a good level of details how to be configured.However, from design perspective, it's important that you know, such for example, differences whenit comes to Alb and NLB with regard to security groups and Knackles.So this is important, although it's a quick session, but it's important because if you want to puta design, it's a good note maybe that you can put it to the people who are going to deploy the solutionto focus on that aspect and make sure they deploy it properly.