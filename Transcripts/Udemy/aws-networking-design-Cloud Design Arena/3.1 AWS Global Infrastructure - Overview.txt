# 3.1 AWS Global Infrastructure - Overview

Global infrastructure has the largest global infrastructure footprint of any cloud provider, and thisfootprint is expanding continuously to help customers to deliver better end user experiences and rapidlyexpand operations to virtually any region or country.Has the concept of a region which is a physical location around the world where it has a cluster ofdata centers, calls each group of logical data centers an availability zone?In other words, each AWS region consists of multiple, isolated and physically separated availabilityzones, or commonly known as Azs within a geographic area.With this, for example, if an organization wants to serve its customers, let's say in Europe, thisorganization should be able to choose from a selection, broad selection of regions in Europe.At the time of this recording has 25 launched regions and 80 availability zones.And as mentioned, this footprint is expanding continuously.Therefore, better to always check website about the latest global infrastructure updates.Moreover, every data center, Asia and region is interconnected via a purpose built, highly availableand low latency private global network infrastructure.The network is built on a globally fully redundant Pearl 100 gig metro fiber network.It's important to be aware of the high performance, low latency of AWS global infrastructure, becausein subsequent sections and sessions we will discuss some designs where you can take advantage of thisglobal network throughout your traffic over it.All right.So now let's zoom into an AWS region and discuss the concept of Azs with further details.To simplify the concept and structure, let's look at Asia-Pacific Geographic region and then zoom ininto Sydney region in Australia.So an availability zone, as you can see here in the structure, is one or more physical data centerswith redundant power network connectivity in an AWS region.Aziz which means the availability zones are physically separated by kilometers from any other AZ.With this approach, Aziz makes partitioning applications for highly for high availability easy.So if an application is partitioned across availability zones, companies are better isolated and protectedfrom issues such as power outage, earthquakes, or any other failure that impact a single locationin a region.All ISIS and AWS region are interconnected with high bandwidth low latency network over four redundantdedicated metro fiber, providing high throughput low latency network between Azs.All traffic between Azs is encrypted.The network performance is sufficient to accomplish synchronous replication between Azs.And this is this is important to be aware of, because if you are architecting such a solution thatrequires synchronous replications for databases.ET cetera.Having this enabled across multiple Azs is doable and easy to be done.That being said, you may do this across regions.However, this has a different type of approach and implications because as you are connecting to anotherregion, it's a little bit further in terms of distance.Therefore, the design and considerations are different with that type of architecture.So now with this.Design and structure of the Azs.If a user in a very oversimplified way now we're discussing it, try to access an application by usingDNS.So typically the DNS can be configured.Maybe it's Amazon Route 53 to send the user traffic or serve the IPS of the workload that is availableor deployed in the multiple Azs or a single AZ, or it might point to another region, or it could sendthe user to AWS edge locations, CloudFront and the AWS Cloud and or, and the AWS content Deliverynetwork.So let's discuss around what is the content delivery network and edge locations.Just want to highlight here, we are going to cover it in a very oversimplified way because there isa dedicated section in this course that discusses CloudFront and CDN focusing from network design pointof view.So there is further details around it in this course.However, here let's define the concept and what's its value with regard to AWS global infrastructure,since we are discussing AWS global infrastructure.So in simple words from networking point of view.If a user in Region B wants to connect to a VPC in region A So and typically based on internet routing,the traffic might go over multiple Internet service providers, ISPs and then get injected into AWSglobal network infrastructure where the peering happening at that region.However, by using AWS edge locations or with point of presence pops, the user traffic will be injectedas close to the user as possible, where AWS is doing peering.So it will be it could be even through the same service provider, internet service provider where theuser is connected.Depending on the peering between AWS and the different Internet service providers.Amazon CloudFront Peer with many Tier one, two, three telecom carriers globally and is well connectedwith all major access networks for optimal performance and has hundreds of terabytes of deployed capacity.CloudFront Edge locations are connected to the AWS region through the network backbone fully redundantmultiple 100 gig pearl fiber that circles the globe.So with Amazon CloudFront, as mentioned, to speed up the distribution of your content by routing endusers to the edge location that can best serve each end user's request in a worldwide network of edgelocations.So this is just an oversimplified way how the content delivery network of AWS fit within the AWS globalinfrastructure.All right.So there are other AWS infrastructure extensions.So we will highlight it here.And briefly define what each of these can offer our does offer.Starting with the AWS local zone.So local, it's the places compute storage database and other select services close to large population,population industry and it centers.So with AWS local zones, you can easily run applications that need single digit millisecond latencycloser to the end users in a specific geography.Local zones are ideal for use cases such as media and entertainment, content creation, real time gaming,live video streaming and machine learning inferences.The other type of infrastructure extension is the wavelength zones.Which is infrastructure deployment that embed compute storage service within telecommunication serviceproviders data center at the edge of 5G networks.So application traffic from 5G devices can reach application servers running in wavelength zones withoutleaving the telecommunications network.And the third one is the outpost.So typically with Outpost, you have a fully managed and configurable compute and storage rack builtwith AWS designed hardware and is designed for workloads that need to remain on prem for different reasons.Could be for data residency, it could be, for example, low latency to have data processing betweensome workloads.You have it on prem and then you have you want something to be connected to AWS, but you do some processingcloser to it where for example, Outpost can serve that purpose as well.And you can check AWS website for the supported services at the time of deployment to make sure thatwhat you want is available on Outpost.For more information about these infrastructure extensions, please refer to AWS website.Also, you can find out more about the availability of these infrastructure extensions in terms of regionsand countries.All right.So now the question is we talked about infrastructure, the huge infrastructure with a lot of redundanciesand capacity.You might be thinking, okay, where does a VPC fit in this huge global infrastructure?So we talked about the region and we have in the region multiple data centers that are clustered indifferent locations that called availability zones within a region.And a VPC is logically distributed across the different availability zones.So typically, if there are three availability zones, you can configure your VPC across 2 or 3 of them.And as you can see here, you can have subnets, network subnets configured in each availability zone.So you can have one or multiple ones in each AZ or availability zone.Also, there are services that are already provided to you with redundancy and resiliency across theavailability zones such as Internet Gateway here.So it's already redundant and distributed across Asia.You don't need to deploy it across Azs, you just need to consume it.And there are many other services like this.We're going to discuss it as part of this course.And this simple VPC architecture can be expanded globally across different regions and connect it todifferent on premises, data centers.And with such architecture, networking design become more important and interesting as well.And this is what we are going to discuss in this course, how we design such architectures, differentarchitectures, how we identify the requirement then which design option is best fit for certain requirement,and what are the different between difference between these different, for example, designs and whichdesign can be more most optimal for certain requirement versus the other one?