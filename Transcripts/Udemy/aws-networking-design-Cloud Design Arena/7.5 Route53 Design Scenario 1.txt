# 7.5 Route53 Design Scenario 1

Hello.After discussing the Amazon Route 53 request routing policy, it's time now to look into a bit moreadvanced routing design policies for DNS requests and the scenario.We will discuss how geolocation and failover DNS request routing can be used together.So let's assume that you have an architecture.And you have several AWS regions deployed globally serving same content or might be slightly differentbased on the geographical location of the user.Let's focus on one region, for example, a user in Europe.When try to resolve the for for example, here we will say example.com should be routed to Frankfurtregion in Germany.If the region or ELB has any issue reported via the health check status, the request from Europe usersokay should be routed or redirected to a region in the US.So now we have two layers in the policy.First geolocation and second is failover.Let's dive deeper and find out how this can be deployed first and how it will operate.Even though we don't touch on configurations in this course.But sometimes when the logic of the configuration is important to know and reflect how things works,we discuss it.So with DNS or A53 routing policies from configuration point of view, you build it bottom up, startingwith the setup of the primary and failover record and then enabling health checks for the ELB to havethe failover working as desired.So here the primary will be, for example, pointing to primary example.com.Okay.And then that typically as an alias record and point to an Alb in Frankfurt and then you build the secondaryand then you point it to the desired region.For example, LZ record LP in that region.So now we have these two records set up as a primary and enabled secondary with it.Then after that, you can set up the geolocation record for the Europe continent by setting the actualdomain name.In this example, it is the main dot example.com that's point to another DNS record that was createdas a failover record in this example, which is the primary dot example.com.So after the record deployed, the operation will be top down.So let's see how technically they will operate as a flow.So with this example, as the user requests arrive at Amazon Route 53 name server for the main dot example.com,the user location is identified, for example here coming from Europe.Then it will be sent to the primary dot example.com based on the configuration we just discussed andthe primary dot example.com typically has a health check associated with it.If the health check is healthy, then the IP address of the Alb in Frankfurt will be returned to theclient and the request will be redirected there.While if the health check is not healthy, the status is reported as not healthy.Then the failover secondary record will be used in which the IP address of the Alb in the US regionwill be returned to the client with the same logic.You can deploy for the other regions in this architecture if you have in your design multiple regionsthat need to follow the same logic and you can set up a default geolocation record as well, where ifthere is no matching to the client, geolocation or the location can be identified.This default record will be used.