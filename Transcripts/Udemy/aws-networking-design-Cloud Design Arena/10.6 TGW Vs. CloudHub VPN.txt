# 10.6 TGW Vs. CloudHub VPN

Previously we discussed cloud hub option for VPN as well as Transit Gateway.And sometimes these solutions, they may look similar or a bit bit, a little bit confusing in termsof which one I select.So now in this quick video session, we will demystify it and we will simplify the concept and architectureof each in which that you can identify the suitable use case or use cases for each.If you have multiple AWS site to site VPN connection, you can provide secure communication betweensite using AWS VPN Cloud Hub.This enables your remote site to communicate with each other and not just with the VPC.The VPN Cloud Hub operate on a simple hub and spoke topology or model that you can use with or withouta VPC.This design is suitable if you have multiple branch offices and existing internet connection and wouldlike to implement a convenient potentially low cost hub and spoke model.Or maybe that can be a primary or backup connectivity between these remote sites site that use AWS directconnect connection to the virtual private Gateway can also be part of this cloud hub connectivity.Remember, you cannot do this with a direct connect gateway, but this option is about to attach thedirect to attach the direct connect with virtual private interface.I will discuss in the direct connect section directly to a GW and that option is available to enableVPN to direct connect over cloud hub use case, as you can see here in this design.In contrast, if you have bigger solutions such as higher number of vpcs in a single or region as wellas you need more advanced capabilities of routing and you may need to take advantage of higher throughputwith equal cost Multipathing ICMP.Then using AWS transit gateway is the best option here.Cloud Hub cannot extend to such a scale with multiple vpcs and offer flexible routing control like transitgateway doing not to mention the ability to provide equal cost multipathing accelerated VPN as wellas Multi-region integration.Or you may even want to use it for integration with your existing sd-wan solution.So the design options are more flexible and scalable with Transit Gateway compared to Cloud Hub.Cloud Hub has certain use cases, as we discussed, and I would say it, it shouldn't be intended forlarge scale and global architecture, even with medium size of solution here.Let's be specific how you define small medium.As long as you have several vpcs and you want to provide flexible connectivity, go with Transit Gateway.Unless you have a specific reason and you're okay with any limitation you may face without using transitgateway in your design.Because sometimes when you look at the limitations or risk of a given design, it will drive you tothink again about the design differently.So look at the design, look at the limitation of each option, not only the advantages, and then makeyour design decision.And as always, don't think about the current requirement.Only think about what the future requirement can be near future and longer term future requirement.Because this also important to be taken into consideration as part of the design.Because you don't want to build a design, for example, that cannot scale to a number of connectionsor vpcs or whatever other requirement that the business may require in a year, time or two years time.And this require major change to the design.So this is how you start thinking of a business driven design.