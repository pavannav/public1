# 15.1 Architectures part 1

All right, time for multi VPC architectures.There are different types of design options and architectures that each serve different need and purpose.So let's start with a basic multi VPC design.Let's assume we have these three Vpcs, VPC, A, B and C connected over VPC peering.Let's start with the hub and spoke connectivity in such a manner where VPC A connected to VPC, C andVPC, B connected to VPC, C, and let's assume as part of the communication model, needed VPC A needsto communicate with VPC.B.Technically this will not work because VPC peering does not provide or facilitate transitive routing.Therefore, in this case we will need to do a direct peering between VPC, A and B, which is fine.It's not an issue with this type of architecture.It's not that big.The question here, what if if we have ten vpcs, how many peering you will need to build, maintainand use in the routing table?ET cetera.So based on the mesh formula we need around or specifically 4 or 5 peerings for ten Vpcs to communicatedirectly, which is a high number.What if we have 5100 or thousand vpcs?This is where we will start to look for alternatives.But before we look into alternatives, let's look how such architecture can integrate with on premisesnetwork.So now we will be adding hybrid design to the mix.As you may remember from the customer managed VPN design sessions, this architecture is based on thetransit VPC model where IPsec VPN tunnels are used to connect the vpcs and here even the connectivityto the on premises over VPN from VPC connectivity point of view, we can add VPC peering among themand it has to be full mesh if we want to maintain any to any type of communication model.Also, we could connect the transit VPC to other vpcs using VPC, peering and establish VPN on top ofit if that's something required by in all cases.Here we are talking about high number of IPsec VPN tunnels and peering connections to establish andmanage.And the larger the architecture become, the more complex it will be to maintain.Update ET cetera.And as you can see, if we add direct connect, there is also limitation around performance and bandwidthcapacity here for the spoke vpcs since they will be relying on the max capacity of the VPN tunnel.In other words, this design is proven and used practically.However, if you consider to, for example, to use it on a larger scale, architectures that can scaleimmediately or it can scale in the near future.Think, think about considering this architecture and better to compare it with other alternatives thatwe discussed previously, like with Transit Gateway, and we will discuss it further in this section.However, as we are discussing designs, let's analyze this design and its consideration in more details.In terms of scale, as you may notice, there is limitation around number of connections because ifwe need to scale the number of vpcs, we need to scale the number of peering.And according to documentations, you can have up to 125 peering connection with that scalable for example,or something that you can request to scale it, but there is limitation around it.So this is something you need to look into it and cater for.When it comes to flexibility, it's limited when there is a hybrid or high number of vpcs, we are goingto hit a lot of complexities as we grow, so the flexibility will reduce as a result of that.And when it comes to complexity, as we mentioned, the more vpcs to be peered, the more complex tomanage and this introduce manageability complexity in terms of manageability and also from routing pointof view, it's going to be complex because you will need to update the routing tables if there is anychange that need to be reflected for these period.Vpcs as in terms of performance as it's limited as well, because such architecture, especially withtransit VPC, there is limitation around the number of vpcs connected to, for example, a direct connectand how much for example, the direct connect is ten gig.Can a VPC get advantage of that ten gig?If it's over a VPN connection, it's not going to be possible.So that's something you need to consider if it's just a mesh over AWS.So it's quite flexible to the capacity of Peered Vpcs or ten gig.So just look into the overall architecture.But in terms of performance, if it's mesh over, there might not be that much of performance implications,especially it's over AWS, but with aforementioned limitations.You may think about considering other architectures, especially at a.If it's a small scale peering, it's a good option.