# 6.6 AutoScaling Policies Part 2

So now we will be answering the original question.Which is about multiple dynamic policies.So the answer is simply yes, we can have multiple policies.However, let's analyze it a bit more.This level of analysis that we are doing in these sessions helps you to build an in-depth design mindsetin which you analyze how things work and identify where it may impact the design.So you can set the right expectations with the different stakeholders.When you're building a design as well as helps you to make the right design choices based on differentrequirements, constraints, etcetera, that could be from business or technological side.All right, so let's assume that you have an application where users upload images from mobile and youprovide storing and processing to these images for whatever use case when images uploaded get firstto S3 and then the app plays the bitmap data of the images and SK SK for processing and then it processimages and then publish it to the place where people can utilize these images for the any use case scenariothat the application is intended to.And the processing happens at the EC2 instances with auto scaling.With this architecture you want to do scaling in response to activity in an Amazon SQS queue.All right.But maybe you want to have another level of metric to help ensure that the application performs at optimumlevel.There are two policies that control when the auto scaling group threshold scale out.One is a target scaling policy that uses a custom metric to add or remove capacity based on the numberof SQS, queue messages and the queue.The other is a step scaling policy that uses the Amazon Cloudwatch CPU utilization metric to add capacitywhen the instance, for example, exceeds maybe 90% utilization for a specified length of time.So now we have two layers of policies.When there are multiple policies in effect at the same time, there is a chance that each policy couldinstruct the auto scaling group to scale out or in at the same time.When these situations occur, Amazon EC2 auto scaling chooses the policy that provides the largest capacitywhen these situations occur.Amazon EC2 auto scaling chooses the policy that provide the largest capacity.For both scale out and scale in it is the largest capacity.Okay.For scale out.This means for in this example, if both policies triggered, then two instances will be added.As you can see now in these two different policies, we have one to add two instances.Okay?And we have another one that adds one instance.So it will consider the one with the largest capacity.The approach of giving percentage to the policy that provide the largest capacity applies even whenthe policies use different criteria for scale.N So this is also when it gets reduced in which in this example, for example, there is a scale inthe result and the auto scaling group terminate one instance.The intention here is to prevent Autoscaling from removing too many instances.And at the end of the day, to maintain like application performance at an acceptable level.That being said, such approach of multiple policies from different type is not straightforward whenusing target tracking, scaling policies with steps, scaling policies.For example, here, because conflicts between these policies can cause undesirable behavior.Therefore, you need to test, test, test and look at the timers of each policies to ensure you reacha good level of stability before moving it to production.But again, it might have some implication because maybe one policy, for example, get triggered andafter a few seconds in which it will lock the resources, for example, for certain scaling activityin or out, and then the other one after a few seconds take effect.So here it might introduce undesirable behavior.That's why it's better you test it and you look at the behavior and how it act.Even this recommendations available in AWS documentation at the time of recording.