# 10.2 VPN  with VGW Part 1

To simplify it, let's discuss the AWS managed VPN solution component using a virtual private gateway.So first, typically we have IPsec connection that include IPsec Tunnels.All right.So we will elaborate more around that.So IPsec Protocol Suite is used for securing Internet protocol, IP communications between two separatenetwork by authenticating and encrypting each IP packet of data stream in which a secure VPN connectioncan be established between the customer network and AWS.The other component is the Customer Gateway, an AWS logical resource, the customer gateway that helpsto define attributes on AWS, about the on premises customer gateway device.All right, so let's discuss what is a customer gateway device.Typically it's the physical device or it could be a software virtual appliance on the on premises sideof the site to site VPN connection, also commonly called CPE or edge device router firewall.So it depends on the terminology that you use, but in general it represent the on premises side ofthe connection.In this diagram, a VPC has attached virtual private gateway that is connected over IPsec tunnels tothe on premises remote network and these two IPsec tunnels, both called VPN Connection.Typically, the customer gateway must be configured to enable IPsec site to site VPN connection as wellas the required routing static or BGP Et-cetera to maintain valid routing reachability information.And as you can see, the two tunnels that we mentioned about are created, each with each VPN connectionat the virtual private Gateway by default and at the site.These tunnels terminate on different availability zones for enhanced availability.From capacity perspective, each tunnel provide about 1.2 gigabit of bandwidth.So let's zoom in more into these tunnels and routing aspect and understand more about it.So this is just a high level architecture.Just to summarize it, a VPN connection here consists of two IPsec tunnels, and these IPsec tunnelstypically use as part of a single connection to provide resiliency or redundancy as well as redundancy,because technically they terminate into different availability zones within the VPC side.As we know, we need a mechanism to route traffic over the IPsec Tunnels with AWS site to site VPN.We can use either static routing or dynamic routing with BGP as a routing protocol.Let's examine how BGP works in this scenario by looking under the hood.How BGP session is established over an IPsec tunnel or both tunnels.So typically in your VPN setup over the public internet here we are assuming over the public internetyou will need public IP addresses to establish the tunnel.So this is just to establish the IPsec Tunnel itself.And then there will be tunnel IPS.So these inside the tunnels, typically these use for the communication over the tunnel and it's froma slash 30 Cidr block that is from 169.24.2254.0. slash 60 range.You can refer to AWS documentation for the latest ranges and the specific configuration aspect and technicallywhat you need to know from design perspective, this range should not must be unique within the sameVW.So if you have multiple connections, they need to be unique and as you know from routing perspective,they need to be unique because otherwise you will have overlapping IPS and it will be a problem whenyou need to do a routing.All right.With this setup, so the BGP session or packet in order to establish a session.So let's, let's focus on the upper pair of IPS, the .37 and dot 38 from source from 37 and distanceto the 38 to establish a BGP session.And then what will happen?This bucket will get encapsulated with IPsec and then it will be forwarded over the internet using publicIP, addressing all right distance to the customer gateway.Once it's reached to the customer gateway, it gets the capsulated and then after the Decapsulation,the packet will be forwarded to the destination IP, which is the BGP peer.So now here we establish full reachability in terms of BGP.So someone might ask, okay, what if I don't have static public IP?Because the discussion that we had with the assumption that you have static IPS and you configure itfrom the VW side and you define it.So the good news is that AWS site to site VPN support digital certificate for Internet key exchangeIP authentication.So for site VPN connection, you can now use private certificate for this scenario from AWS certificatemanager instead of Pre-shared Keys.This enables you to take advantage of the added security and flexibility that digital certificates offer,including the ability that you do not specify an IP address for your customer gateway so you can updatethe IP address of your device, which is the on premises device without having to reconfigure the VPNconnection from routing point of view of the on premises side.First you will need to propagate the routing information in a way to maintain full reachability.There are different ways to do that.Let's say you will redistribute BGP into your interior routing protocol.That could be OSPF for example, so you can have reachability from the internal network to the VPN,Cidr range from the customer gateway routing and forwarding point of view.The tunnels, both tunnels that we have here as part of a single connection can be used to forward trafficto AWS.From AWS site.Typically, the VPC routing information will be based on either propagated route from the VW that wediscussed previously as part of the VPC.Networking fundamentals could be defined as static route.So when you use a static route, typically you manually define the route and set the target or nexthopfor this route as the VW.Once the packet reached the VW here, you need to be aware that VW will pick only one tunnel like customergateway that can use both tunnels.So this can introduce asymmetrical routing issue where traffic goes over one tunnel and come back overanother tunnel.And this may introduce some implications related to latency or maybe filtering if there is, for example,a stateful firewall in between where the traffic goes from one session and go back and through anothersession or interface.So this is something that you need to be aware of if you want to, for example, design a VPN with VWand better like to have it in a more stable and deterministic manner.So how can we optimize this routing behavior?Before we answer the question, let's expand the design into something similar, but it's a little bitdifferent.And now, as you can see, there are two VPN connections and each connection should have its own pairof IPsec Tunnel.And as you can see, one connection here using static routing and the other one is using BGP.If we look at routing priorities at the site, typically VPC route table has no other target but justthe destination to VW that is pointing to the 192.168.10 slash 24.On premises then the VG at the VW level based on routing priority.Static route will be preferred for the same prefix learned via BGP.All is good, right?But from design point of view, if we rethink about it, this may not be the very dynamic way that youwould like to design it, because the more you use a static routing, the more that you need to introducesome mechanism to update the routing information if there are changes, failure, etcetera.Also here we are dealing with two mechanisms of propagating routing information, which is not the simplestor optimal way if we have the option.And the other question that might arise, what if both tunnels are using, for example, a static route?So typically in this situation, if they are both in a upper status, which means the VPN connectionsare active active.This technically means that you cannot specifically decide or determine which tunnel will be picked.And this again fall under the asymmetrical routing situation that we discussed.So one tunnel will be picked over another one, and then your VPN gateway at the on premises site needto be deployed or configured in terms of features or capability to to accept such asymmetrical routingbehavior.And since it's not deterministic, it's not the preferred way that you should aim for.So that's why we need to look into alternatives.Another limitation here to be aware of with the static routing is when you use policy based VPN connectionto connect to an AWS VPN endpoint limit the number of security associations to a single pair, the singlepair and exclude one inbound and one outbound security association.Some on prem VPN gateways try to establish a new security association for every new network they wantto route over the VPN tunnel and such scenario.And this can break the connection.So it might be better.For example, you advertise or route toward a summary route that covers all the subnet.In such case you will have like a single wider range to cover all the IPS.However, this is a workaround.That's why if we look into dynamic routing, we might have better options there in order to optimizethis design as well as the previous one we discussed with Single connection and two tunnels.Same concept applies better to use BGP over both VPN connections or tunnels as well.In such case you will have more control in terms of routing preference using BGP attributes such asas Path or advertise more specific routing information or using the multi exit discriminator.At this stage, we are focusing on the behavior and the limitations or concern that we need to be awareof in terms of how we apply the the recommendation that we just mentioned about about using BGP andsome attribute or maybe more specific route.Advertisement.We will have a couple of use case scenarios that will discuss such designs in details covering the endto end routing design from on prem to AWS side as part of this section, which make it simpler to understand.And apply it in a use case.That is something that maybe you can use or replicate conceptually.As long as the behavior now is clear, you should be fine at this stage.That being said, if you are not from networking background and maybe you are a little bit confusedabout the terms I mentioned as path and mid.So let's clarify the path amid a bit further to help you understand the logic.