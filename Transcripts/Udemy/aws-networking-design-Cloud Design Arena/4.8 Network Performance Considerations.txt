# 4.8 Network Performance Considerations

All right.Time to discuss some throughput and performance considerations within a VPC and for networking in generaland AWS Cloud.From networking point of view, this is very important topic because there are multiple factors thatwe are going to discuss, although the discussion is not going to be very detailed.However, it's going to be to the level that is required for you to analyze any design and assess whetherthere is performance or throughput implications or not when you are architecting and building certaindesigns on AWS.When it comes to network performance in general, performance is defined by the lowest component thatwe have.So keep in mind that if, for example, an IO system is limited because of the processors are limited,then the network is limited.That's really going to become the bottleneck for the performance of the system in such case.Therefore, it's important to understand the influencing factors to have a better network performance.We will cover the key and primary ones that you need to be aware of and take into considerations.Starting with physical proximity of instances in terms of communications over network infrastructurecould be hybrid.So are these instances or two endpoints that need to communicate in the same availability zones?They are in the same placement group.They are in the same region or across region.Are they, for example, across hybrid connection or not?Is that connection over direct connect or it's over Internet, etcetera?So this is very important because now we need to define where they are located.So the physical proximity is important.Also from physics, we know that the longer the distance will be, the higher the latency will be regardless.For example, is this over fiber or just over a tunnel link?Just in general terms, the distance has implications in terms of performance and latency.A key influencing factor here, maximum transmission unit, MTU.So the largest possible packet size and bytes that your connection can pass.This is where the MTA, this is what the MTA you typically refer to in a specific.And here we can think of the jumbo frames from networking.If you are from networking background, I'm pretty sure you're aware of the jumbo frame.So jumbo frames allow more than the typical value or the common used value, which is the 1500 bytesof data by increasing the payload size per packet with jumbo frames and thus increasing the percentageof the packet that is not packet overhead.Therefore, fewer packets are needed to send same amount of usable data.However, traffic is limited to a maximum of 1500 when, for example, traffic is outside of a singleVPC over inter-region vpc peering connection.Or if it's going, for example, over VPN connection or traffic going over internet gateway.So in such cases jump off frame will not work, at least at the time of recording.Size and type of EC2 instances.This has a very, very important impact because the specifications of the instance change the capabilitiesof processor of I o capabilities as well as the supported network interfaces.So typically in general term, the larger instance size for an instance typically provide better networkperformance than smaller instances.This is like a general rule of thumb.Enhanced network interface or enhanced or instance enhanced networking.So typically this refers to enhanced networking that uses single route IO virtualization that providehigh performance networking capabilities on the supported instance type.So the single route IO virtualization is a method of device virtualization that provide higher IO performanceand lower CPU utilization when compared to traditional virtual network interfaces, enhanced networkingprovide higher bandwidth, higher packet per second in terms of performance and consistent lower interinstance latencies.And the other good thing here is that there is no additional charge for using these enhanced networkinginterfaces.In instance, network IO created.This is a little bit special, I would say use case or special type of instances.It's it's not very common, but it's good to be aware of.So there are instances designated with the symbol shown here and the networking performance column.When you look at the description of the instance types and its IO capabilities and these type of instances,they can have a network credit that that based on a certain utilization baseline and which can increasethe performance on certain period of time.So what you can do, you can open a support case and with AWS and ask about the baseline bandwidth forspecific instance type that you are interested in.If you would like to consider such type of instances.However, this indicate network performance here varies for these instances, so this is something totake into consideration.All right, now let's take this into a more visualized view.And the question here, what is the network bandwidth on EC2?And I can provide the answer here is that the aggregate bandwidth throughput for instances between availabilityzones within a VPC or between instances in peer VPC scenarios between 1025 and 10.So depending on the instance type as we mentioned earlier.Similarly, aggregate bandwidth to VPC endpoints such as Amazon, S3, etcetera is limited to the instancesvalue.We'll look into that in a bit.But here we are referring into the communication between those instances across and availability zonesand within availability zone.But there is an important consideration here is that single TCP flow is limited to five gig.So the definition of a flow here, TCP flow is defined as traffic going through a single TCP port betweentwo instances.All right.However, what if you're using placement group?Okay.In which place?A placement group can be defined in simple words as a logical grouping of or cluster of instances withina single availability zone that allows application to use low latency communication where the flow canbe increased up to ten gig.So if you require such high bandwidth up to ten gig of flow flow bandwidth, this is where you needto look into placement group.And for the communication with S3, as we mentioned earlier, we get between 25 to 100 gig.This is from the instance point of view, but this is something also subject to bandwidth considerationof the server endpoint itself.So this is as mentioned here by the VPC service endpoint capacity limit.So it depends.For example, are you using interface endpoint, are you going through Internet gateway, etcetera.So it's better to test it and look into the documentation of the interface endpoint that you are usingit to access that service in a specific.All right.So the key takeaway from this slide here is the communication within a VPC or to outside VPC dependsto a high degree on the instance type and size where it can range between from 1025 all the way to 100gig.And as we mentioned here, the flow in terms of single flow communication over single port between twoendpoint two instances is limited to five gig unless it is used over a placement group.That being said, at the time of recording.EC2 p4 d instances are the first in the cloud to support 400 gig instance networking.It does this typically by using multiple hundred gig network interfaces.So this is something just to remember and keep in mind.So this design for certain use cases and this also gives you an indication how much the performanceis being optimized over the cloud, specifically over AWS cloud and the networking performance capacityis a huge.So as a designer, the important learning from this slide is that you need to look into the trafficsource and destination, how much bandwidth is required by by that application in terms of aggregatebandwidth as well as in terms of flow level bandwidth.This will give you the ability to understand how you design it, what capabilities you need to introduce.ET Cetera.So this is the important aspect of it.Taking into consideration the other influencing factor we just mentioned in the previous slide.So because those they work collectively together.