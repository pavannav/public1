# 8.4 CDN - Performance part 3

Okay.So now you might be wondering, okay, we've been talking about the static content that's typicallycached at the edge location or the regional edge location.What about the dynamic content that we previously mentioned about?What's the benefit when we are using AWS CDN in such use case?The simple answer is first, let's go to the foundational concept that we started with with regard tocontent delivery network in a specific Amazon CloudFront From performance point of view, your end userconnection are terminated at cloudfront location closer to them, which helps in reducing the overallround trip time required to establish a connection.These CloudFront locations are connected to the highly resilient Amazon Backbone network that providessuperior performance and availability for connection to AWS.Origins.In addition, various other optimizations such as persistent TCP connections to the origin SSL enhancementssuch as session stickiness and Ocsp stapling, which all helps in improving the performance even forNon-cacheable content that is typically dynamic content in a specific Https adds a further overheadbecause setting up a secure communication channel through either SSL or TLS protocols involves exchangingsignaling messages in order to negotiate cipher algorithms and agree on common set of encryption keys.This can add additional couple of round trips and the initial stage of retrieving the media content.For example, or any other type of content that is not cache by going directly somewhere or even whenit's cached.So in general, Https, TLS and SSL has such consideration or concern.Once a viewer has set up a TLS session with CloudFront using the full negotiation process, it can usethe session ID to re-establish the TLS session with the same attribute negotiated during the initialsession creation.So there is a significant optimization when using CloudFront for such connections, specifically withTCP.So let's look at a real world example, specifically where there was an optimization around SSL, TLS.So this example is about Slack.When they used CloudFront, they enabled it specifically for their API integration to achieve API acceleration.So typically it was mainly dynamic content and it's not something that cached.And the description here is based on a session that is available on YouTube.You can go and look at it described by Slack team.As you can see the source available in the bottom.It's called Slack talks about secure API acceleration with Amazon.CloudFront So in that session the description of the architecture was like they introduced CloudFrontand they used Amazon Route 53 weighted routing policy.If you remember from the Amazon Route 53 discussion we had about routing policies.So they distributed like 99 and 1% and they wanted to see the optimization performance.ET cetera.And through that design, they have highlighted significant improvement in terms of connection, interms of milliseconds.So if you look at it, there is a significant reduction in terms of the latency or delay of the connectionestablishment.And that was specifically around optimization.In terms of SSL, if you look at the left hand side that was going direct to ELB, it's showing about488 milliseconds.While on the right hand side, after introducing CloudFront and the in the middle, it reduced to 199milliseconds.So this is interesting because someone might think, okay, I'm going to add to my connection additionalhop.So how is this going to reduce my latency?Interestingly, it does.And apparently based on the graph, you can see there is a significant reduction in terms of the SSLconnection time.And we just describe in a high level about the logic behind it, even though, for example, you seethe wait time is larger on the right hand side.But remember, this is a fraction of 200 milliseconds, while on the right, left hand side is a fractionof almost 500 milliseconds.So in any way, the right hand side, it's about half or maybe less.So this is an example where the content is not cache, it's dynamic.And you can look at many examples from AWS documentation and public references again.So it depends on the use case and what you're trying to achieve with you need to, as a designer orarchitect at the beginning, you need to understand the targeted solution architecture and type of contentas well.You need to go to that level and understand what they are serving.Is it website and in the website what they are serving images, dynamic content, media streaming,etcetera.And based on that you can provide the best suitable design.Another consideration that maybe you might think of as we discussed Elbe and Route 53.ET cetera.And you might now be wondering, okay, if I introduce now CloudFront to the mix, I was able, forexample, to with Amazon S3 and Elbs to route traffic based on location.So now what can I do with CDN?If I put it as my front end, how can I achieve the same or is this something doable?So the answer is yes.So CloudFront natively corralled requests to different origins based on path patterns.Which can be configured in cache behaviors.So typically in CloudFront there is something you configure called distribution.And under the distribution you can configure different behaviors.Then you can base, for example, do route forwarding based on certain values in the header that couldbe part of the client request or identify a location or using CloudFront geolocation headers.And this design you can see, for example, all locations use S3 for static content.If presented, for example in your design, then based on location you can send them to different origin.In addition to that, you have the flexibility to add lambda to your edge.Location distribution.This code called lambda at the edge.And here you can do a lot of customizations and a lot of advanced capabilities that you can add to handlingand securing the requests.ET cetera.So the answer is yes, it's doable.And they all work together.In fact, they are better together.