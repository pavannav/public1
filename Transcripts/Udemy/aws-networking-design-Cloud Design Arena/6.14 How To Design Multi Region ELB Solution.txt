# 6.14 How To Design Multi Region ELB Solution

So after we discussed different designs, the question now is if the design requires a global levelresiliency across different regions where in each region there is, for example, at least a VPC withan ELB that could be application load balancer, not network load balancer or even classic load balancer.How can we architect the solution to cater for such requirement and scale?This is where you can add DNS or specifically Amazon Route 53 to the mix.Or you may consider using content delivery network CDN like AWS, CloudFront or a mix of both.These two topics are big and going to be discussed each in its own section, starting with the AmazonRoute 53 and the subsequent section.