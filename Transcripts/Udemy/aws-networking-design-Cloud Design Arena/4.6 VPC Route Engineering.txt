# 4.6 VPC Route Engineering

So in this session, we will focus on the value and the use case or maybe use cases of the more specificroute within a route table.So we covered the multiple route tables and the association with the different subnets and how we cansteer traffic.ET.Let's see how we can take this into to the next level to do traffic engineering at the VPC level byusing the more specific route.To keep it simple, let's go through a design scenario.So in this scenario, we have some requirement.The first requirement is that the communication between the EC2 instances in these two subnets thatare located in two different availability zones has to go through the firewall instance.So this could be a third party firewall to do some packet filtering and inspection, etcetera.And the other requirement is that the instance in the private subnet, which is ten .1.20 slash 24 ifit wants to communicate to the internet, it has to go through the Nat gateway, while the other instancethat is in a public subnet ten .1.10 slash 24 It can go directly to the internet gateway.So let's see how we can achieve this by using different route tables along with the more specific routedefined in each route table.First, let's start with the simple one.The instance in a public subnet, as we can see in this route table.First, we have the default route as 000. to the target or the target for that route is the Internetgateway.So the traffic can go direct to the Internet as it required.The other component or the other route in this route table, which is the more specific route.So the Cidr block of this VPC is ten 100 slash 16.However, the more specific here which points to the subnet of where the other instance residing, whichis the subnet that is 10.120 slash 24 and the target to reach this subnet here specified as the firewalland elastic interface.So now we are steering the traffic to go to the firewall and then let's see how the other subnet willdo the routing.So as you remember, we mentioned that we need the internet traffic to go through the night gatewayand here the default route is pointing to the night Gateway as a target for the default route.While the traffic that is going to the ten 111 where the public subnet instance is residing has to goagain through the firewall ENI.So we are sending it back to that firewall.So now we are achieving symmetrical routing in both direction.We send it to the firewall for the encryption to keep symmetrical traffic or traffic flow communication.If we look at the firewall subnet, it's very simple.The firewall subnet doesn't need to have detailed or more specific route because the thing is, everytime a packet reached that firewall getting inspected, then it will be sent to the default routing,which is the local route of the subnet and will be routed to the relevant subnet in that VPC.While the Nat Gateway route table has the default route pointing to the Internet gateway.Because now if the instance in the private subnet that use route table A, as you can see here, sendthe traffic to the Nat Gateway, the Nat Gateway need to send it back to the Internet in which it willuse the Nat Gateway subnet route table and forward it to the Internet gateway.With this, we can achieve the required traffic routing or traffic engineering.So the instances they communicate through the firewall while this instance goes to the internet viathe Nat gateway and the other instance in the public subnet goes directly to the Internet gateway.Let's look into another example.Here we're adding to the mix hybrid connectivity, which is a VPN.Even though this is not a the even though this section is not discussing the hybrid connectivity.So don't worry about that part.And what is Virtual Private Gateway and all of these details, because these are discussed as part ofthe hybrid connectivity and hybrid model section.However, now what we need to focus on is the definition of the more specific route and how we can steerthe traffic even when we have, for example, VPN connection.So here we have IPsec site to site VPN from on premises site terminated on Virtual Private Gateway,which is one of the network service endpoints that you can use to terminate or establish a VPN connectionwith your on premises.And the requirement here that the traffic between on premises side and the subnet ten .1.20 slash 24has to go through a firewall.The firewall understands that we see it here while the other subnet ten 1110 slash 24 can go directlyto the on premises side network.So how can we achieve this?First here we have the ability to use something called Gateway Route Table or Edge Gateway Associationand sometimes called as ingress route table.We will discuss the ingress route table with the Internet gateway in the next section.However, now we highlighting this with the virtual private Gateway.We can associate a route table in which that when traffic comes in to that virtual private gateway andthen need to be forwarded within the VPC.Okay, So that route table will be used to look into the local route and if there is any specific route.So once we associated that now with the ingress, traffic can be steered and as you can see, to reachthe ten 120 slash 24, you need to go through the firewall.And if we look at the firewall route table definition in that firewall subnet, here we have two routes.First, we have the local route to forward it to the instance here or any other instance.If you add it in different subnet that is within the Cidr.And also in this route table, the on premises subnet is defined with the target as the virtual privategateway because now we want to send it back to the virtual private gateway when we have a return traffic.If we look at the instance subnet route table here, what we can see, we see the same on premise subnetthat we defined as well in the firewall subnet.However, here the next hop or the target is the firewall.ENI Well, if we compare it to the other subnet that can direct to the virtual private gateway, wesee that the target for this subnet to be reached is the virtual private gateway ID.So by using the more specific route, we can do different type of route steering and traffic engineeringwithin the VPC to send traffic to different directions and do maybe service chaining.We'll discuss it in the next section, etcetera.