# 5.1 Connect to the Internet Part 1

We will focus on the connectivity to outside from an Amazon VPC in detail.With this.The first logical question from design point of view is what are the common use cases or design optionsto connect a VPC to the outside?So before we dive into the design options, first let's define what's meant by outside here.So typically the first thing that comes in mind when we talk about outside is the internet, which isvalid and which is the most commonly used destination as outside.However, there are multiple other categories.For example, it can be another VPC or VPC, such as a partner or service provider, VPC or even yourown vpcs or it might be an on premises site or site.And this is commonly known as a hybrid model.And it also can be an AWS service.Typically an AWS public service that's reside outside of your VPC.So all of these they have different designs and capabilities in your VPC to use it to connect to eachof these categories or types of destinations.Let's just get started.With the connectivity to the Internet and in a specific as a use case connecting a workload in a publicsubnet.First of all, and the design discussions, you might notice there's always an EC2 representing theworkload in a VPC.However, it can be any type of workload that reside in a subnet and can can get an IP address and usesthe route table associated with the subnet.Like containerized workload instances, etcetera.So this just for simplicity.You might see always an EC2 instance, but it could be anything that support IP connectivity, a routetable and a VPC.This design option is very simple and straightforward design.As you can see that the route table has default route with a target as the VPC Internet gateway.Also, the subnet where the host is located is a public subnet in which it will have a public IP address.If you remember from the previous session, the actual public IP is translated or nated at the Internetgateway.So that's very straightforward way that you connect to the Internet public subnet.You have Internet gateway default gateway in your route table to the Internet gateway and all set.So now what use case might require such a setup?For example, it could be a management bastion host, for example, and the access control managed orcontrolled through nacls and security groups.And simply, for example, if you want to add redundancy, you may provision another instance instanceand another subnet in different AZ.So from design perspective, you might ask, okay, what if I want to introduce an inspection in thepath I put like a security service or capability in the path?And you can achieve this simply by adding a virtual firewall appliance.Typically from the AWS marketplace, you can pick your preferred vendor from connectivity point of view.As you remember from the previous session, we should replace the firewall in a different subnet.Otherwise, if it's in the same subnet of the host, you will need to put the default route at the instancelevel, which is something you should avoid.You need to avoid any dependencies when it comes to design of the cloud networking of at the instanceas much as you can as it's shown in the in the table associated with the instance subnet.The default route target is the ENI elastic network interface of the virtual firewall, while the targetof the default route for of the virtual firewall instance is the Internet gateway.It is important to note that this firewall is not doing any nothing.The instance is in a public subnet and the private public IP.Nothing happens at the internet gateway.Therefore, to ensure the internet gateway, send the return traffic back to the firewall and not directlyto the instance, you will need to use internet Gateway Ingress Route.Table.So we I mentioned about this in the previous session, but I did not discuss it.So this route table, as you can see, has a more specific route.Of the instance subnet and uses the virtual firewall ENI as the target in this route table.So with this, we will make sure that we don't have asymmetrical routing where, for example, the outboundgoing through the firewall instance and the traffic coming back will go direct to the instance, whichis not the desired behavior that you would like to achieve.And it typically leads to disconnectivity.ET cetera and security breach.Let's have a deeper look into this design.So we looked at it as a high level just instance.But typically these virtual firewalls, normally they have multiple interfaces.And let's keep it simple.They have two interfaces.So think about it like your traditional firewall.There is inbound and outbound interface.So here this virtual firewall has a couple of nodes.The inbound and outbound ENI, for example.Anybody think about it like the inside interface and the Nia is the outside interface with this designsimply as you can see, VPC route table, which is associated with the subnet of the instance, hasthe default route pointing to NIB.And the virtual firewall has now placed in the subnet that is associated with.Route table.See?On the left side, which has the default route, pointing to the Internet gateway route, Table B,as you can see where the arrow is pointing, the blue arrow has a default route, but we can removethis default route because I think about technically this subnet is only forwarding the traffic internallyto the internal Cidr subnet.So if we if we remove the default route, it shouldn't impact the traffic crowding.And at the and most importantly, we have the ingress route table that has the most specific route forthe instance subnet pointing to the any of the virtual firewall.All right.So now the question here, what if this firewall or AC, where this firewall is located, has an outage?So we need to think about how can we introduce AC so simply we can, for example, have two virtualfirewalls and two different Azs and we just replicate the exact structure we just discussed.So as you can see, there are instances of our tables and there is the outside route tables associatedwith the.And see.And if you look at the ingress route table now, it has the most specific.However, it's sending it to the relevant virtual firewall.An important note here.I just want to highlight this Design is the most simple design that maybe you can achieve with virtualfirewall.However, the issue with this approach depends on the firewall vendor solution.So if you are considering any vendor third party firewall solution on AWS, it's better that you goand check the documentation and how they do the edge.It can be active standby or active active.So this is very important because different vendors, they do it differently and you need to consultto that.All right.So now we have couple of options.Let's look at some design considerations of what we have just discussed.So, first of all.In terms of availability if we have the firewall placed in a single.We know that's a single point of failure.So this is not something that we should target or something that we aim for.This is where we start looking into multi AZ.However, that's not straightforward always.It depends on the vendor and how they're doing it, and sometimes there is tunneling and some configurationetcetera.So it's not a straightforward and it's a vendor dependent, whether it's meet your requirement or no,for sure.That's something that you need to check and validate and test.And the other aspect is related to performance with elasticity.So what do I mean by this?Imagine I have my virtual firewall, but I want to take advantage of the AWS cloud, the capabilityof elasticity.So when I have higher, for example, traffic volume or request, I need to scale my firewalls in termsof instances.And then when I have less, I want to scale down.So how can I achieve this?And what about if we add, for example, redundancy to this across multiple Azs?It's not straightforward.So what's the alternative here?This is where innovation comes to the picture.Specifically the AWS Gateway load balancer.From the service name.You can tell it performs two functions.Gateway and load balancing.Let's discuss it in more details in the context of connecting a VPC to the Internet.