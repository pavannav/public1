# 15.10 Centralized Security and Control part 3

Now let's look at the same design in a different way.Let's assume that you want the communication between VPC, A and VPC B to happen.However, traffic between those vpcs you want it to be inspected by those virtual appliance appliancesand the Central Security VPC, VPC, whatever you call it.So technically, based on the routing discussion and design, we explained in previous videos, as youcan see, the routing of the VPC designed to let this happen because there is a default route of anythingpoint to the firewall and at the VPC and when the traffic hit that firewall, it will send it back toits private subnet where it has the specific route of those vpcs pointing to the transit gateway enis.And when it reached the transit gateway, it will reach the route table and light orange color herewhere it has the specific route of each VPC, which means if an instance, a VPC here in availabilityzone a send a traffic that distance to an instance located in VPC.B, it will go this way and the return traffic will go back and possibly get dropped.So why such behavior happens?Technically, a transit gateway attempts to keep traffic crowded between VPC attachments and the originatingavailability zone until it reaches its destination.Traffic crosses availability zones between attachments only if there is an availability zone failureor if there is no subnet, for example, associated with a VPC attachment in that availability zone.So based on that, now the response traffic that originated from availability Zone B and VPC B here,as you can see, is routed by the transit gateway to the same availability zone and the VPC, whichis availability zone B, the traffic is therefore dropped because the firewall or virtual applianceshere it's performing a tasteful inspection and it's an availability zone B that let's assume here it'snot aware about the state information of the other firewall that typically knows about the originatingrequest source, etcetera.Therefore, if it doesn't have the state information, it will be dropped.So how can we fix this?Technically, you can use a capability in transit gateway, call it appliance mode.So when transit gateway appliance mode is enabled, a transit gateway, select a single network interface,and the appliance VPC, which is the VPC in this example, using a flow hash algorithm to send trafficfor the life of the flow.The Transit gateway uses the same network interface for the return traffic.This ensures that bidirectional traffic is routed symmetrically.It's routed through the same availability zone and the VPC attachment for the life of the flow.If you have multiple transit gateways in your architecture, then each transit gateway maintain itsown session affinity and each transit gateway can select a different network interface so they are notsharing such selection or maintaining a state if they are.There are multiple transit gateway, so typically this is a rare situation where you send it over differenttransit gateway and then you do an inspection and send it back.The ideally or typically it will be in a region and within a single transit gateway.But it's good to highlight based on documentation.Therefore, as a general design documentation here, if your VPC attachment spans multiple multipleavailability zones and you require traffic between source and destination host to be routed throughthe same security virtual appliance, for example for stateful inspection, then enable appliance modesupport for the VPC attachment in which the appliance is located.So this is very important to be aware of such capability because it may impact the traffic inter zoneforwarding as well as with Bitcoin has implication with the traffic dropping or not.So by understanding how the traffic in details goes between these different availability zones and howit may impact where the return traffic may be landed, it's very important.This security inspection can be performed also by using AWS, Gateway load balancer that was discussedpreviously in this course to provide centralized security inspection.So.Typically here, the forwarding will be toward the gateway load balancer and then back to the virtualfirewalls instances.And then through the forwarding through the route tables, it will handle such forwarding.And by using appliance mode also we can ensure the selection of a single availability zone for the lifeof a flow.So let's analyze a little bit more around the Edge VPC and how the packet actually get forwarded tothat gateway load balancer.And just to make sure that we are on the same page.So as the packet enters the edge, VPC from the Transit Gateway based on the subnet attached to theAWS Transit Gateway Elastic interface, any subnet route table packet will be forwarded.And here, as you can see in the blue route table, it has default route point to the gateway load balancerENI.So the packet will be forwarded to the ENI and even for the specific traffic as well here it's specifiedfor the other VPC is pointing to the ENI.So with other traffic now is routed to between these subnets or to anything to the internet will besent to that ENI, the gateway load balancer, ENI.And once it reached that elastic interface, it will, as as you remember from the discussions aroundGateway Load Balancer, it will be forwarded over the AWS, Privatelink over AWS infrastructure andthen back to the Gateway Load balancer.And then it will be sent to one of the backend virtual appliances that could be Firewall, Next GenerationFirewall or IPS.ET cetera, And it will be forwarded back to the same knee.Once the inspection is complete and now the forwarding will be based on the destination subnet and theroute table associated with that knee.So now let's assume its distance to the Internet.So what will happen?The default route of this subnet, as you can see it in the top route table, it's point to the Natgateway.So now it will be sent to the Nat Gateway.So the IP will be translated, netted, and then based on the Nat gateway subnet, that's where theinterface residing for the outside interface and a public subnet, it has the default route pointingto the subnet to the sorry to the VPC Internet gateway where the traffic will be sent out and the returntraffic should follow the same logic because the return traffic will point to the Nat gateway firstand the nat gateway to reach private VPC subnets or VPC.A and B has to go through the Gateway load balancer.And when the packet sent back from the inspection to the back, it has the forwarding point back tothe transit gateway.Elastic interfaces and back to the specific vpcs.So I hope this clarifies the packet forwarding in details and in terms of what's happening in such architectureand the previous architecture works in a little bit different way because there was no ENI and backto the Gateway load balancer that there was only virtual appliances installed in that subnet.So these are two different options.And keep in mind here, if you are using AWS network Firewall, it will be exactly same logic as thegateway load balancer in terms of forwarding because it use the gateway load balancer type and the forwardingrouting design works exactly the same.Only the AWS network firewall will provide you with a managed firewall capabilities in which you don'tneed to provision firewalls, etcetera.