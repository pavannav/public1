# 6.1 Drivers for ELB

All right.Let's start with the most basic design to expose an application to the outside or Internet in the scenario,which is based on using a public subnet that is that has a route table with default route using VPCInternet Gateway as a target.Also, we could use DNS in this design, for example, using Amazon Route 53 to provide access to theapplication or service using a name rather than an IP address.It's obvious this is a basic, easy and quick to get your application up and running.Although this design works and straight forward, it has several concerns that you need to be awareof.To simplify it, let's analyze it from three primary design attributes point of view, starting withresiliency.This design requires self-created and managed resiliency to monitor the health of an instance or applicationand perform failover, which is not easy to build and manage from scalability point of view.This solution requires manual scaling or response to increase capacity or maybe provisioning highercapacity from the beginning to cater for the peak period.Like you expecting, for example, certain amount of requests at certain point of time within the dayor maybe within a month.You need to provision capacity to cater for that, for that increased capacity, although you coulduse auto scaling, but without a unified entry to point to these instances or this instance will bevery difficult to keep track of.New instances registered as a result of a scale out and how to route traffic to these new instances.You need to come up with a custom script.Maybe lambda.ET cetera too.And trying doing so is something.It's going to make it even more complex to operate, which means it lacks simplified elasticity whereyou can scale out and based on certain metrics.As a result, it is not scalable or cost effective design.From security point of view, this design simply expose the instances to the public internet in whichit requires a lot of OS and application hardening.Still, it has no security layers apart from network, ACL and security group.Also, when using Amazon Route 53, Layer three and layer four Dos provided from AWS shield standardcapability.But internet facing instances can be scanned and saturated easily, which makes the attack surface wider.Therefore, it's prone to application layer attacks more than something that we will be discussing inthis session where we can have more constrained and reduced attack surface.So the question now, okay, what we need to do to enhance the solutions, resiliency, scalabilityand security simply by introducing load balancing layer using AWS, elastic load balancers, ELB, whichwe will be using across the this session.So remember this terminology ELB, which refers to the AWS elastic load balancing.The term load balancing refers to the practice of distributing requests across multiple applicationnodes with the objective of maximizing resource usage and increasing overall application performance.A load balancer is a piece of software, usually running on a dedicated node that receive incoming requestsand distribute them to application nodes.Load balancer are often presented as the entry point to an application, and they typically offer morefunctionality than simple request routing, such as SSL, TLS offloading, DDoS protection, headerinspection or redirection performance and health monitoring, etcetera.For cloud native architecture, the need of such architecture with load balancers is even more criticalto provide elasticity and performance without compromising security needs.Therefore, load balancers should be carefully architected in order to achieve the desired performance,security and availability of your application.Talking about Cloud Native architectures, AWS has several load balancing options that meet differentworkloads and solutions requirements.Classic Load Balancer is a legacy load balancer recommended mainly to be used with classic EC2.If you go to AWS website of comparing the different type of load balancer, gateway load balancer,mainly related to security and firewalling related use cases that were covered in a previous sectionsor sessions that you can refer to.Therefore.This session or set of sessions as part of this section, focuses on the two primary elastic load balancingoptions of AWS, the network load balancer and the applications load balancer, which are fully managedload balancing services that provide powerful feature rich load balancing solutions with simple configurations.This means applications owner do need do not need to have access to the underlying computer resourcesor operating systems powering the load balancing nodes.In turn, they do not need to worry about scaling out and maintaining the ELB node.