# 5.2 Connect to the Internet Part 2

We've discussed this architecture or design where we have a virtual firewall, provisioned a coupleof interfaces, and then we need to use route table to steer the traffic and then we can introduce ashape based on the firewall vendor capabilities and supported.When it comes to the gateway load balancer, the architecture is a slightly different.In fact, it is simpler from a VPC point of view.On the left side you will have a VPC endpoint type of gateway load balancer that is typically you usesa knee to forward the traffic.Elastic network interface and the endpoint can be used in the route table of the VPC as a target tosend the traffic to.So if you look at here in the route table, we're using this from both.The ingress route table associated with the Internet gateway and from the subnet route table where Sorry.Instance subnet route table where the instances or hosts are residing.So now we are steering the traffic toward that gateway load balancer endpoint or any.The endpoint connected to another VPC where you have only virtual firewall appliances from your preferredand supported vendor.It's important supported vendor because there is a list of supported vendor, so you need to go andcheck if the preferred vendor that you want to deploy is supported with Gateway Load Balancer or not.The connection from the endpoint gateway to the load balancer is powered by.AWS Privatelink.So AWS Privatelink provides private connectivity between Vpcs or AWS services and on premises networkwithout exposing your traffic to the public Internet.This means that traffic is sent or received over the private link over AWS infrastructure without beingsent over the internet, which means no concern about internet.Dido's et cetera.As well as.Whole instances in your VPC.They don't require public IP address to communicate.So that's very important because if you use completely private communication, that's doable.You don't need to have that.And then the traffic goes back to the registered firewall or IPS instances with the Gateway load balancer.So let's look at it from the other side where the firewalls are sitting.So typically we have the firewall IPS that's doing the inspections.And then there is the firewall and registration between the firewall or IPS instances with the GatewayLoad Balancer and Geneve encapsulation.So this encapsulation is used in a high level without going into the details, to help the Gateway Loadbalancer maintain like a state and symmetry of traffic to make sure that traffic always goes to thesame virtual firewall or IPS instance, when there are multiple ones they spin up.And then we have the communication between the gateway load balancer and and any securely over the privatelink here without any cap.And then you have native communication between the gateway load balancer, native IP communication ofof the VPC, between the Gateway Load Balancer, ENI and the IP destination within your VPC.All right.So let's look at it from packet forwarding point of view.So typically we have the ingress route table at the top where we enforce the most specific for thatsubnet back to the NE.Okay.And from the subnet, also the outbound, we enforce it back to the NE of the gateway load balancer.So this to ensure that outbound and inbound always being enforced to the NE for inspection in whichwhen we have a traffic going from this instance, it will go this way all the way to the security applianceand then it will go back, exit the NE and go back to the internet.And if we have multiple Azs, which we should for better reliability in this design, we will have anendpoint of the gateway load balancer in each AZ, at least each, at least a subnet in that AZ.And typically those enis they should be of the gateway load balancer, they should be in their own subnet,okay, because they will be used in route tables as target for that specific AZ.All right.And the rest will be same concept we just discussed in terms of inbound and outbound.It's just will be here and specific forwarding.Okay, so let's look at the inspection from a different perspective.So this architecture and routing design, it's nice.It's more scalable and reliable.However you might thinking, okay, what if I want to manage firewall capability using the same routingconcept that we've just discussed?So this is possible by using the AWS network firewall.Let's have a look about AWS network firewall from an architecture point of view.AWS Managed Network Firewall uses an ENI firewall, ENI or firewall endpoint.In fact, the firewall endpoint shown here is powered by the Gateway Load balancer and therefore Elasticnetwork interface of the endpoint is gateway load balancer endpoint type and should work in the samemanner as the gateway load balancer, which we've just discussed.However, here we don't need to provision any firewall appliance because it is a managed service.So in terms of routing, it is exactly the same of what we have just discussed.However, the key difference here, it's not something that you need to provision firewalls from certainvendors that you would like to use.All right.And in terms of traffic forwarding, it should be as well in a similar manner.When you have the traffic, it's pointed to the NE of the firewall and goes out and in the way back,use the ingress route table to enforce it back to the same NE.And if we expand it to multiple Azs, it's exactly using same concept we've just discussed.Okay?You just need to cater for the specificity of the route table per AZ, especially with regard to theingress route table as well.Make sure you forward it to the right NE of the return traffic for that availability zone.Please note this is not to recommend using AWS network firewall over next generation firewall here,so you need to check the required capabilities.Okay.And then validate it.Check the documentation of AWS network firewall, what's going to provide you what you need comparedto next generation firewall, whether it's meet your requirement, this is where you as a designer,you make the choice and this is outside of the scope of this session.So I'm just highlighting this to be clear.Okay.And you may decide to use, for example, NG firewall of your preferred vendor.And if it's supported by a gateway load balancer, your life will be easier because of the discusseddesigns.Or you might decide to use multi-layer security in which you can use combination of both.So all that depends on your security requirement in terms of design, capabilities, compliance, etcetera.