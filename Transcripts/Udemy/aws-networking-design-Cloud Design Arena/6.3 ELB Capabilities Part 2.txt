# 6.3 ELB Capabilities Part 2

Up until now, a few terms were discussed.Let's structure them in the context of each component briefly.First, we need to have a listener for the target port.The client will send traffic too.Second, we need to define the target group or groups for the backend where instances will reside.Then we should define the required health checks and also enable auto scaling if auto scaling is required.Most of the cases you should use it.Unless you don't, you don't really need it and you have just a specific number of instances required.Last but not least, traffic to the ELB port are returned.Traffic to the client need to be allowed.Similarly, traffic from the ELB along with health check traffic to the backend instances need to beallowed with return traffic to the ELB as well.This is very important using Knackles security group or any other type of filtering you have in thepast.Otherwise traffic will be blocked or maybe if you allow the traffic for the application port but youdon't allow it for the health checks, your ELB will not be able to send the health checks.These instances will be reported as unhealthy and then they will not be also registered as part of thetarget group.So this is very, very important from the network security perspective.Other aspects like routing policy, DNS setup and policies, also CloudFront and CDN that all can beadded to these components that collectively structure could be regional or global type of architecturewith load balancing.So we will look into how utilize DNS in details with Elbs as well as CloudFront in terms of CDN andsubsequent sections and sessions.Okay.Now someone might ask, okay, can we take advantage of AWS, Gateway Load Balancer or AWS Network Firewallto protect the public subnet for the.The answer is 100% yes.Even though such design was covered in a previous session, let's analyze the routing table and trafficflow of such design.As you may remember, using both Gateway Load Balancer and AWS Network Firewall, we use Gateway loadBalancer endpoint to communicate with the firewall and function facilitated by AWS Privatelink.To do so from outbound traffic point of view, the route table of the ELB public subnet has the targetfor the default route as the gateway load balancer endpoint.So typically the default route is where you send traffic out to the internet while the endpoint subnethas the target for the default route as the VPC Internet gateway.So now we are sending the traffic out when it is source from that endpoint from inbound traffic pointof view.We just need to put the target as the gateway load balancer endpoint for the traffic distance to theELB subnet.This is very important.The destination is the ELB public subnet.This is where the IPS are created for the nodes of the Alb and then the target next hop will be thegateway load balancer endpoint in which we will have traffic flow as it presented.Now from the instance to the ELB, back to the firewalling capabilities and back out to the internetand the return traffic will follow the same exact path.If you are deploying AWS network firewall, the design and architecture will look exactly the same.Okay.Now, maybe someone might have a hybrid architecture.And the question here.Okay, what if I'm connecting to LP from an on premises side?The answer is use internal LP.So what is internal LP?All right, so nodes of an internet facing load balancer have a public IP addresses.The DNS name of an Internet facing load balancer is publicly resolvable to the public IP addresses ofthe nodes of that LP.Therefore, Internet facing load balancer can route requests from client over the internet, while thenodes of an internal load balancer have only private IP addresses.The DNS name of an internal load balancer is publicly resolvable to private IP addresses of the nodes.Therefore, internal load balancers can only route requests from client with reachability had accessto the VPC private subnets of the load balancer.Also, if your application has multiple tiers, you can design an architecture that use both internetand internal load balancers, for example, internet facing and internal load balancer.For example, if your application uses web servers that must be connected to the internet and applicationservers that are only connected to the web servers, what you need to do is create an Internet facingload balancer and register the web server with it.Then create an internal load balancer and register the application servers with it.With this design, the web servers receive requests from the Internet facing load balancer that is receivedfrom client request and send requests for the application servers.To the internal load balancer.The application servers receive requests from the internal load balancer.So it is tiered architecture.Load balancer servers.Then the servers send back to another load balancer and that load balancer.Send it to the registered back end servers.Okay.And typically you can have these servers or services within a VPC or if you have a hybrid setup likeyou can have a target as an IP address and you send it back maybe to on premises.Okay.We will discuss this further when we look at into network and application load balancer.This architecture is doable.All right, so what if someone told you, okay, why don't we just use Amazon API gateway to exposean application instead of an AWS ELB?That's an interesting and valid question, and this is what we will be discussing in the next sessionsection of this session.