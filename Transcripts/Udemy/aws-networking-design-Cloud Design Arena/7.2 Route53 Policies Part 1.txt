# 7.2 Route53 Policies Part 1

Time to dive into Amazon Route 53 DNS request policies.There are several types of DNS or Route 53 routing policies.We will analyze each one of them and the common use case of each.In this section, we'll cover also design scenarios that use advanced type of routing policies of Route53.So we'll see how, for example, we can use maybe combined options of these policies as an architector designer.It is very important to understand these policies and the associated or possible use cases as when youstart designing more advanced solutions in the cloud and cloud that need to be resilient across differentvpcs as well as it might span multiple AWS regions, considering DNS by using Route 53 routing policiesto route DNS requests based on certain policy requirements will be key for such type of designs.Before we start with the policies, if you have basic knowledge or experience with DNS record, youshould know there are different types of DNS records.I just want to specifically define in a high level, there are 53 Alias record, Amazon, Route 53,Alias, record or record provide a Route 53 specific extension.Two DNS functionality.RLS records allow you to route traffic to certain select AWS resources like CloudFront distributionor maybe elastic load balancing or even S3 bucket.Such record also allows you to route traffic from one record to a hosted zone to another record.Ali's records typically have a type of a or a a, a, a if it's IPV six, but they work similar conceptor logic in the in the back end like CNAME record.When you use an alias record to route traffic to an AWS resource.Route 53 automatically can identify any changes in the resource.For example, let's say an Alias record, for example.MLB.com points to a load balancer.If the IP address of the load balancer changes, Route 53 automatically starts to respond to DNS querieswith the new IP address.So this is significant because this abstract a lot of headache and operational overhead that you needto do, for example, to do such automated response.If you do it yourself, which is something possible, but with with the Alias record and using AWS certainresources that's already abstracted to you and it's something that you can just consume.I mean, we'll look into how, for example, we can associate health checks and we make it even moreresilient.With that, let's start with the routing policy, starting with the simple routing policy.So a simple routing policy from the name.It's simple where it lets you configure standard DNS record with no special Route 53 routing policysuch as weighted or latency, etcetera, that we will look into in a bit.With simple routing, you typically route traffic to a single resource, for example, to a web serverthat is hosted on a certain instance, for example, for your website.In addition, if you choose the simple routing policy for an Alias record, you can you can specifyonly one resource or one record and the current hosted zone.Although you have the option to set multiple values to a single record like multiple IP addresses fromdesign point of view, you need to be careful with such design because Route 53 returns all the valuesto the recursive resolver in random order and the resolver return the values to the client, such asour browser that submitted the DNS query.The client then chooses a value and submit the query so it is not not a bad approach.It's doable.But as long as you know the behavior and it's fine with your design, then you should be fine.The other routing policy or request routing policy is called Multivalue Answer, which lets you configureAmazon Route 53 to return multiple values such as IP addresses for your web servers, a response toDNS queries.You can specify multiple values for almost any record, but multi value answer routing also lets youcheck the health of each resource.So this is different from the simple policy.Now we're checking the health.So Route 53in such case returns only values for healthy resources.It's not a substitute of a load balancer.So now we are not changing load balancer function with this policy or routing policy, but the abilityto return multiple health checkable IP addresses is a method or a capability to use DNS to improve availabilityand load balancing, because now we can have our solution spanning multiple vpcs or region or maybeour web servers directly to route traffic approximately randomly to multiple resources such as web servers.You need one multi value answer record for each resource and optionally associate a 53 health checkwith each record and typically Route 53 request response to DNS request with up to eight healthy recordsand give different answer to different DNS resolver.If a web server becomes unavailable after resolver caches a response, client software can try anotherIP address and the response.So as you can see in the slide presented, the order is different based on the client has requestedthe the DNS resolution with Route 53 routing policy.There are some considerations to be aware of which is highlighted in the AWS Route 53 documentation.First, if you associate a health check with a multi value answer record or 53 response to DNS querieswith the corresponding IP address only when the health check is healthy, which is perfect if you don'tassociate health, check with a multi value answer record.Route 53.Always consider the record to be healthy.So that is something.Maybe it's not very resilient if you do so because there might be values of of a record that point toa certain resource that is, for example, experiencing an issue.So maybe considering using health checks as a better option.If you have eight or fewer health records after your response to all DNS queries with all health recordwhen all records are unhealthy.This is interesting.Route 53 Response to DNS queries with up to eight unhealthy records.This is highlighted in the documentation you can refer and double check it.It's worth mentioning here unhighlight, because if you're doing such design, you need to be awareof the behavior.So this is not only operational side, this is also impacting the design.So that's why highlighting this here and important to be aware of.