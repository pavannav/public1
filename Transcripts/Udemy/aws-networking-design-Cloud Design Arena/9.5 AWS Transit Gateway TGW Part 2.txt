# 9.5 AWS Transit Gateway TGW Part 2

This is another view of of the design that we've just discussed.So we may use such a view more across subsequent sections.So just presenting this to be more familiar with it, not get confused with different types of diagramsso that one think about it of a more zoom in.And this is like of more of a full architecture.Let's expand on this design and let's, for example, introduce shared services and the same concept.So seal here, there are route propagation, controlling the communications.There is no direct route propagation between, for example, the route tables that belongs to the vpcson the on the top, while the bottom VPC, which is shared services here in this design, just hypotheticallyplaced in the same route table where the on premises connection or attachment are associated in whichroute propagation to those vpcs are available for the shared services VPC and vice versa.So now the communication can happen between any VPC and the shared services vpcs.But between those top vpcs there are communication is not allowed and for sure communication to on prem.And this design is allowed.So you don't need to to dig deeper at this stage into how that route specific information being exchanged.Just let's focus on the conceptual architecture that is provided to us by Transit Gateway.We will have more scenarios and details around this type of design.Another important consideration and capabilities which we discussed before with the direct Connect Gatewayis the ability to use Multi-account design.So with Transit Gateway, you can have, for example, a common architecture, a networking account,and you can have the different accounts that your vpcs belongs to.This sometimes a common architecture where you can have separation of responsibilities.So you keep like stuff related to networking in terms of networking services, the Gateway Transit,Gateway Connections, et cetera in one account, and then you start sharing it with the other account.So this applicable in large and medium type of designs where you have, for example, multiple accountsand you want to start providing separation of concerns or responsibilities.Now let's zoom into the transit gateway more and look at some aspects related to the integration withthe VPC and how the routing happens.So typically and as discussed, you will be associating the Transit gateway with VPC subnet, not theentire VPC.And then you can use that attachment.Any elastic network interface as a target in the VPC route table based on this design.As you may remember from the IP routing discussion as part of VPC Networking Fundamentals section,you can have multiple subnets or route tables to control the routing and and the desired way to connectto outside of a VPC.However, this association of a transit gateway with VPC subnet or subnet has an important considerationto be aware of.So let's discuss it.First, let's start with the basic connectivity and packet forwarding.So here we have two route tables.One route table belongs to a VPC and another route table belongs to Transit Gateway.And based on this definition of the routing information, let's assume there is a packet distance tothe subnet range that reside on prem, which is 192.168 1010 the house.So typically the packet will be forwarded based on the VPC route table to the transit gateway.And then based on the transit gateway route table, the decision will be made.So here the propagation of route coming through through two tunnels and here there is a possibilityof equal cost multipathing and typically based on per flow.For example, forwarding for ICMP one tunnel will be picked and the forwarding will happen.So it's important to know like ICMP, typically it does per flow.So it could be based on IP and port and each flow will be sent over a single tunnel.However, in aggregate, the bandwidth will be the total of the number of tunnels bandwidth.So aggregation.However, the actual flows each will be dedicated to a tunnel so that the hashing of the equal costmultipathing.So far so good.Now let's assume in this scenario the transit gateway is associated with one subnet, let's say thesubnet in Az1 as it's shown in this diagram.And let's assume there is a packet from host and subnet 10 or 1 down two, for example, is trying toreach that host on premises.10.10.Based on W Operation Transit Gateway.Operation.This forwarding will not take place.That's interesting.So why?The reason why in order for a subnet to be able to follow a packet to a transit gateway, it must haveat least one subnet in that availability zone associated with transit gateway.So in our example there were ten .1.2 is in EC2, which has no subnet associated with transit, gatewayand or attachment.This is something important that you you are aware of in terms of routing design with resiliency becauseyou need to avoid having transit gateway not attached to all of the required availability zones in yourdesign.Assuming that the VPC route table can take care of the forwarding via the other AZ in case of a failure.So in this scenario, if AZ one fails or the attachment to AZ one fails, for example, then Tgw W willbe isolated from that VPC.That means all the communication will be not happening while your architecture already multi-az.So simply by adding another attachment from the tgw to that to a subnet that belongs to the other isyou will be fine and the forwarding will take place.When it comes to raw priority with the transit gateway.So there is a sequence of route evaluation.Transit Gateway will look at before making a decision.So, for example, as you can see here, as always and as everywhere, longest prefix, always preferred,that's given.Then if we have the same prefix length, then there will be static route have get priority VPC cidrsget priority.So be aware of having overlapping with VPC Cidrs because you will get into an issue.That's why always we need to pick our subnets wisely.Then route propagated through the gateway and then propagation from VPN and if there is.Two route coming with the same length and they are coming from the same connection type.So it's either a VPN or Gateway Direct Connect.Then Transit Gateway will perform equal cost Multipathing and as we highlighted earlier.So it will the traffic will be per flow distribution.So in terms of bandwidth aggregate, it will be the aggregate of all of the connections.However, the single flow will be limited to a single either direct connect connection or a tunnel ifit's VPN tunnels.There is an interesting blog that from AWS that I recommend you to look at it describing the scale ofthroughput using transit gateway, multiple VPNs, and it has step by step configurations if you wouldlike to try it yourself.So just search for this title and you will find it.And it's highly recommended to go through the configuration details if you are interested into suchlevel of details.