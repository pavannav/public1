# 8.3 CDN - Performance part 2

So what is a regional edge cache that we just mentioned?Let's review the AWS edge location map quickly again.As you can see in this map, there are edge locations distributed globally as well as there is somethingcalled regional edge caches.What do these regional edge caches do?In addition to CloudFront global infrastructure of edge location or locations, regional edge cachesserve as a mid-tier caching layer to provide cache hits and consolidate origin requests or origin requests.Here we mean that requests going to the actual source of the content for the viewers and the closestgeographical region.Viewer requests are routed first to the CloudFront Edge location where the cache is supposed to be exist.And if the object is not cached in that location, the request is sent on to the regional edge cache.So now the request goes to the regional cache.When viewers are in different geographical region, requests can be routed through different regionaledge caches, each of which can send a request to your origin for the same content.Also one of the advantages of such architecture of caching to improve overall performance of viewersis that regional edge caches have a larger cache than individual pop.So now objects remain in cache longer at the nearest regional edge cache location.This technically helps to keep more of your content closer to your viewer, reducing the need for CloudFrontto go back to your origin server.In other words, technically the request will only be served from the origin in case the data is notpresented either in the edge location or in the regional edge cache.For for the files are not exist, for example in the edge location and the regional edge cache.Typically the regional edge cache will fetch that file or content from the origin and then it will bepopulated in the regional edge cache as well as when it's returned to the user.It will be populated in the edge location, so the subsequent request will get it from the edge locationin terms of cache content.As highlighted earlier, when viewers are in different geographical regions, requests can be routedthrough different regional edge caches, each of which can send a request to your origin for the samecontent.Therefore, your origin might receive duplicate requests for the same content as shown in this scenario.At this scale, this may add load to the origin.So our goal with caching is to reduce the load on the origin.But if we have it at scale globally and we go through this scenario, we may again indirectly addingload to the origin.So how can we improve this architecture further from performance at scale point of view?The solution in such scenario is by using CloudFront Origin Shield, which is an additional layer inCloudFront caching infrastructure that helps to minimize your origins, load, improve its availabilityand reduce its operating cost.As is shown in this design, the Origin Shield acts like an aggregator to the request from differentregional edge caches, and only one request is sent to the origin.As a result, it increases the likelihood of a cache hit.Okay, so cache hit is mean, like when there is a request and it will find the intended content forthat request and typically handling fewer requests at your origin which can preserve your origin availabilityduring peak load or unexpected traffic spikes, for example.Another use case is when you may use multiple CD strategy that might be driven, for example, basedon certain requirement.Let's say you need to provide caching for users in some city, in some cities where no cloudfront edgelocation presented there.So without Origin shield, your origin might receive many duplicate requests for the same content,each coming from different CDN like like as it's shown here.As you can see in this diagram, by using origin shield with the CloudFront, it can reduce the loadon your origin.Last but not least, from high availability point of view, you can set up CloudFront with origin failoverfor scenarios that require high availability.So if you are making an architecture for a solution, you must consider edge in your design.Also, you should dive a bit deeper into the use case to make the suitable design.For example, some use cases like streaming video content you might want CloudFront to failover to thesecondary origin quickly so the design need to take into consideration fine tuning cloudfront cloudfrontfailover over the secondary origin by adjusting the origin connection, timeout and origin connectionattempt etcetera.Such details.Typically you can find it in the AWS services documentation, which means the main point here is toidentify what is needed to be considered at the design stage.Then you can find about the how when you start doing the actual deployment.