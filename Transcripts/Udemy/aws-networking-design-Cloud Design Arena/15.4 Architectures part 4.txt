# 15.4 Architectures part 4

In this video session, we will discuss an architecture that facilitates creating centralized servicesthat can even be offered as software, as a service SaaS service to customers on AWS.So this architecture we have presented on screen has two types of vpcs as you can see, the servicesconsumer VPC, where the client or source of traffic comes from, and the service provider VPC, wherethe service or services to be accessed are residing.As you can see, this architecture uses AWS Privatelink along with AWS network load Balancer to connectinterface endpoint to services.As discussed in the elastic load balancing section previously in this course, a network load balanceroperates at the network transport layer layer four and from the OSI model and can handle millions ofrequests per second.And this type of architecture, the network load balancer is a front end or represented inside the consumerVPC by an endpoint network interface.This endpoint network interfaces or VPC endpoint interfaces ideally should be provisioned in differentsubnets that map to at least two availability zones for better solution availability and resiliencyas it's shown in this sample architecture.From IP communication point of view, typically you will need to have service DNS mapped to one of thedifferent VPC interface endpoint and traffic flow will go over the Privatelink, over AWS global infrastructureand never traverse the internet as part of this communication model.When consumer service send traffic to a service through an interface endpoint, the source IP addressprovided to the application are the private IP address of the network load balancer nodes and not theIP address of the service consumers.Therefore, as you can see in the service consumer and service provider, the IP subnets are overlapping.This is not a mistake or a typo, but to show that you do not need to worry about IP overlapping insuch design, which is a great flexibility and this architecture can offer service access to multiplevpcs in the same manner we discussed these Vpcs could belong to the same organization or it might evenbelong to different companies or organizations such as customers who can consume a service offered usingthis architecture all over AWS infrastructure.Also, such service access can be extended to an on premises site by extending the reachability of theVPC interface endpoint and the consumer VPC.Or it might be the other way around.A service that may reside in an on premises site can be made available for other vpcs using this architecturein which the network load balancer needs to have the back end target IP as IP target as they are locatedin an on premises site.This could be also an option to provide an interim solution to access applications on an on premisesside that to be migrated to AWS.And as discussed this also helpful and useful as well.If an on premises site has overlapping IP range with the consumer vpcs IP ranges in which here you'renot going to face any issue, or maybe you don't even need to introduce any network address translationnetting in between.