# 8.2 CDN - Performance part 1

All right, time to dive into the performance aspect with regard to Content delivery Network.CloudFront.Before we start, I just want to know that performance here.We are referring to a combination of latency jitter bandwidth.So it is not just bandwidth.That's something that you think about as a performance.With this discussion, let's start from a high level, the basic high level architecture to explainthe concept of CDN, to make sure that we are all on the same page and for sure, in the context ofperformance optimization.In this scenario, we have a user or region be accessing an application located in AWS region.A for example, could be different countries, continent and etcetera without CDN, for example, inthis design, the connection typically will go to the intended VPC, which is in another region as highlightedby going through multiple Internet service providers.ISPs here presented like three ISPs could be two.It could be more this all dependent on the Internet routing and the peering between the region wherethe user located and the ISP that they are connecting to and where the destination VPC where the applicationhosted and that region.And typically there will be peering between pop and a local ISP in that country or region.In contrast, when a user access the same region, for example, or the content that you are providingand CDN is used as part of the solution.In contrast, when CDN is used as part of the solution and a user requests content that are served withAmazon CloudFront, the user will be routed to to the edge location that provide the lowest latency.So in other words, the content will be delivered with better performance because it has lower latencyand it's going to be closer to the user.So technically the connection or traffic from the user will enter the reliable AWS global infrastructurefrom a closest edge peering location that most probably will be over the same ISP might be your ISPhas a direct peering with AWS in which the connection will go through your internet and back to yourjust direct ISP and then back to AWS.However, this is not always the case, but maybe it's going to be a couple of ISPs, I would say maximumdue to the reason that we discussed previously, that CloudFront peers with too many Tier one, twoand three ISPs.So the possibility that you will have spanning couple of ISPs will be minimal from performance pointof view.The shortest path to enter AWS, backbone or global infrastructure will always offer better performancecompared to using public internet.Assuming that there is a close or direct edge location, peering with the user ISP or in close proximitywithin the same city or country, one of the capabilities that you can take advantage of these edgelocation for network connectivity is the AWS Global Accelerator, which is a service that you can useto let AWS advertise the public IPS from these edge locations that are distributed, as we discussedpreviously, across many cities across the globe.But what services that you can use with this global accelerator at the time of recording, you can usethe you can use it to advertise an EC2 static or elastic IP.You can associate it with an application load balancer, which is a great way to give an Alb a staticIP address as well as announce the Alb static IP in such case through AWS edge locations.So typically this will provide better performance as well as sometimes for regularity requirement.You might need to provide static IP for whitelisting.So it gives you both advantages in this case or you can use with with VPN, also known as acceleratedVPN in which the connectivity for the VPN will enter AWS backbone through the closest edge locationor pub for peering with the local ISP.And this is something that we're going to discuss in further details as part of hybrid connectivity.However, in high level this typically available when you are using VPN with AWS Transit Gateway.So this is from a network connectivity point of view.Let's go a little bit higher and look at it from the application point of view as well and analyze itfurther.We still in the performance aspect, but let's go a bit higher from the network layer to the applicationlayer and this snapshot from Amazon.com page we.You can see there are some multimedia content like the one highlighted with the Red Circle.It could be also a main page banner or some videos like Live or on Demand depends on the use case.So static content that persists for a certain period of time are requires loading like videos, imagesor even the main HTML page can be cached at the edge location close to the users users who are accessingit while dynamic content like some type of API request with a related to shopping cart product searchcontent frequently changing like weather or news.Such content cannot be cached.However, we will look into how you can take advantage of Amazon CloudFront as CDN for such type ofcontent as well to provide performance optimization.In general processing request at AWS is location closer to the viewer instead of Origin servers, Andby using the AWS global network, it can dramatically reduce the number of network that user's requestsmust pass through.As we discussed earlier, where you need to go, multiple ISPs, etcetera, in which it significantlyreduced latency and improve the user experience.And this is where the caching, SSL termination, persistent connection and other capabilities thatcan be performed at the edge location can help.Let's dive deeper into this connection that we highlighted in high level that's flowing from a userto a CDN.To understand more about how the cache content is served to the user and how this help optimizing performancefrom bandwidth latency point of view, starting with the basic DNS request, this will be abstracted.So we discussed earlier more detailed DNS request analysis is with Amazon, DNS Route 53.As part of the Amazon Route 53 section.So now the request goes to the local resolver, let's say your broadband DNS server and that goes tothe root server, then the top domain server.And then let's say it point back to the Amazon Route 53 as an authoritative server for, for example,name server for the domain you are requesting.And and here because you have that domain associated with CloudFront, then it will be forwarded toCloudFront DNS and then the cloudfront DNS typically were rather requested a pop that can best servethe user request pop or slash edge location.This location is not only based on the nearest cloudfront edge location in terms of latency.In fact, it performs intelligent routing decision, so it will look at performance.So let's say here the latency for the user, which is very key aspect to look at it.And we always refer to it in terms of the best latency location.However, there are other considerations that the decision take into account before forwarding whichlocation or pub the user should go to, which is the actual pophealth server capacity over there aswell as network capacity.And these are very important because think about it, maybe you are in a city where there is let's assumehypothetically there is a large event happening and the edge location in the local city or nearest oneto you, it's saturated with in terms of server capacity or network capacity.So even though in terms of latency, it's the closest.But in terms of providing the required caching, maybe it is not the best.And here, here where this intelligent routing decision helps because it will redirect you to the nextclosest pop or edge location that has the required capacity and should be the next best location interms of latency.So with this logic, then the connection established to the proper edge location, if the content alreadyin the edge location with the lowest latency and other factors that we just described, CloudFront deliversit immediately to the user.If not, if the file, for example, are not in the cache, the pop go to the nearest regional edgecache to fetch it to fetch that object.So what is a regional edge cache that we just mentioned?