# 15.8 Centralized Security and Control part 1

And this session and the next ones, we will discuss some design models related to security controlusing multi VPC architecture.As always, let's start with a simple design model.And this design, as you can see, there are two internal vehicles that need to connect to the internetusing an ingress egress VPC.Or you may call it the shared services VPC.It's named here as a page of where you may have certain security controls, including third party virtualappliances, to meet your security traffic inspection requirement by using a transit gateway with Turoktables inside the either with transit gateway.And as we discussed previously in other video sessions, you can achieve such operation in which herewe do not need to direct communication between these internal VCs, but with the of PC only.Therefore, as you can see, there are table and green associated with VPC A and B Transit Gateway attachment,which has only default row that uses a target as the urge VPC when there is traffic distance to anythingdefaulter out of mean matching anything.Once the outbound traffic reach the age of VPC, the rollout will be based on the age VPC rock tablesconfigurations.The return traffic from the internet will come from the age VPC in the design, and when it hit theA.W. Transit Gateway, it will use a different route table that is associated with the age of BPC TransitGateway attachment, as you can see it here in Orange, or can say that light orange color and thisright table there is a specific internal VPC route or so the ranges of those abuses propagated to thattrial table in which the traffic can be forwarded to the role of MVP based on the destination IP.Let's zoom in more and the design and look at some of the roundtable's and forwarding in more detailsto make it easier.Understand the design discussion where and which later on, when we discuss more advanced or biggerdesign, it will be easier for you to understand how the traffic is being processed around.We will focus on a packet source from the BCA to the internet first.The packet typically will determine the destination based on the VPC Iraq table, and this VPC roundtabletable trial points to transit gateway attachment after the packet reached the transit gateway.It will be forwarded based on the ad to attach table to that attachment.They agree Iraq Table here, where the target of the default throughout is the age VPC attachment.Once the packet reached the age of VPC, there are table associated transit gateway attachment and theage VPC will decide where the package should be forwarded.So typically this is going to be the roundtable table associated with that subnet.As you can see, the default trout in this subnet round table points to the virtual appliance firewallelastic interface interface.The packet will be inspected by the firewall and then if it's permitted, it will be forwarded to theinternet through there.Typically, the alagbon interface that is typically here based on the design associated with a differentsubnet that is there, possibly that outside subnet that could be a public subnet with public IPS,et cetera, where we can see in that subnet round the default trial point to the internet gateway ofthat BBC.You may also perform that network, address translation at the virtual firewall appliance, or you coulduse not gateway if it's required, we'll discuss some design options.Also in this section, we have a dedicated video session about knotting design options that you mayalso watch it and correlated to this design and other design that we'll be discussing.Their return traffic from the internet should follow the same logic and the opposite direction.Let's add to the design a shared services VPC.This is very common architecture where sometimes there are some services need to be used by differentVPNs like centralize authentication, etc. in which a dedicated VPC can be used to house and provideaccess to these services were the transit gateway.Their design can be simplified.Taking advantage of our tables are our propagation control, etc. and this design we can attach theshared services VPC to the orange round table, in which the shared services VPC will be able to reachinternal his prefixes as presented in the orange round table.Also, we need to add the shared VPC cider block IP ranges to the green rock table by using static crowdor propagation.In order to provide access from VPC and B to the shared services subnet.If we need the shared services to access the internet, we will need to make or ensure the shared servicesside, our blog or the required subnet present and in the Orange Draw Table, as well as well as thedefault crowd that need to be presented in the Orange Draw table.For that point to the age, VPC for access to the internet, typically here.So let's expand on the design and change some of their requirements.Here we have two shared services Pepsis internal and external.This architecture might be used in scenarios where external or edge VoIP schemes use a different sharedservices VPC to meet certain security compliance requirements, while internal share services VPC isused by internal VCs and applications.And this design, the internal VPC should not be connected to the public internet.While the external shared services VPC should.As you can see here, we added in your our table blue RA table and this round table.Only the internal VPC side, our blog.I present it well for the external shared services.VPC Insider blog presented in the orange table, as well as default throughout is added there, pointingto the edge VPC.A quick note here, although this design discussed here from Transit Gateway Point of view or our tabledesign, it's not allowing the communication between VPC Amby.However, there is a possibility of fraud happening that can happen at the Edge Services VPC, for example.Here, the traffic will head the default trial point to their edge services VPC.And then when it goes to those virtual appliances, though, virtual prices may send the traffic backto the other PPC.So and the next video session, we will analyze the situation and how it can be avoided by just wantedto highlight it to keep in mind, maybe need to think about it, how this can be stopped or optimize.And in the next video session, we will analyze it further.Let's expand a little bit on the previous design.Let's assume now we have this architecture where there is a VPC and we have a couple of subnet.And as you can see here, we have, let's say, two types of instances in terms of requirement so thatthe red instances and they're on subnet, they require to access the internet to the outside via theedge of IBC, as well as to access internal VPNs that are connected to a second 2G W Transit gatewayat the bottom.While the green instances, they should only access other VPNs via the second transit gateway, butthey should not be able to access the internet via the transit gateway.Let's look into how we can achieve this by using graph tables or rock tables across the VPC where thehosts are located, but there is important note here, so technically you can attach a based based onthe $A documentation at the time of recording up to five transit gateways, per VPC.So this is designed for simplicity.Let's look into the routing table.We'll look into how we can steer the traffic because all the control through there, I think they.So where the initial attachment of the transit gate with one and two.Typically, there is nothing special and even the the default throughout here is not needed becauseyou are not forwarding.But this is just think about it to distinguish.This is egw on and to and for the green instances, the subnet associated with it.And that round table we have default throughout point the transit gateway to.While they're ready, a Aniston's here, they're all table has the full throttle that point to try toget with one where it goes by the age of VPC.While the most specific crowd, let's say here we assume other BBQs roar.Just in theory, it's one seven two six zero zero slash 16.It could be multiple subnets, could be bigger subnet and you can't summarize it.So in such a way, we can steer the traffic where it goes.So this is just to show like, OK, we can connect a couple of tries a gateway.But is this design recommended?I would say if you can do it with a single transit gateway and multiple rock tables, that's more efficientand even it will reduce the cost because, as you know, with transit gateway, you pay for the attachmentand the for the data processing.So if you can do it with a single transit gateway and transit gateway kind of scale to thousands ofpieces, then do it with multiple tables.If there are certain requirements where you really can achieve it in such a way, or maybe there isan interim or temporary solution where the design going through maybe companies, the merger and acquisitionstype of scenarios where you need to do such a design temporarily, that's something doable.So the idea is to highlight the possibility of doing such a design, but not to recommend doing it unlessit's only achieved by doing it this way.