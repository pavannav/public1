# 7.1 Route53 Introduction

Welcome back.In a previous video design session after discussing designs, the question was if the design requiresa global level resiliency across different regions where in each region there is at least a VPC withan ELB could be application load balancer, network load balancer or even classic load balancer.One of the resilient and proven design options to meet such design requirement is to introduce a DNSlayer, specifically Amazon Route 53 with DNS routing policies.Let's discuss more about it.First, let's define Amazon, Route 53 in a high level.So.Amazon Route 53 is a highly available and scalable cloud domain name system DNS Web service.Amazon Route 53 effectively connect user requests to infrastructure running in AWS such as Amazon EC2instances.Elastic load Balancing load balancers or Amazon S3 buckets.In addition to that, you can also use it to route users to infrastructure outside of AWS.So this is not even within AWS.However, our discussion will be mainly focused around AWS because we are discussing networking andinfrastructure design, etcetera within AWS and as well as hybrid scenarios.There will be some discussions around the hybrid type of DNS setup as well.You can use Amazon Route 53 to configure DNS health checks as well to route traffic to healthy endpointor to independently monitor the health of your applications and its endpoint.In addition to that, Amazon Route 53 traffic flow or routing policies make it easy for you to managetraffic globally through a variety of routing types.So that could be based on latency, geolocation or proximity or based on weighted type of policies thatyou define.We will discuss this in further details as part of this section.So just stay tuned for that.Or maybe you can just jump in to these sections if you are familiar with the definition and high levelof Amazon, Amazon, Route 53.Amazon Route 53 used the concept of host zones.In which you can have public and private type of hosted zones.Public type of hosted zone typically for outside resolvable domain names, while the private is morefor internal to your VPC vpcs and also for hybrid DNS setup.If you have like hybrid setup with on premises with private connectivity such as direct connect, AmazonRoute 53 is the only service that offer 100% SLA.It's really interesting and this is key because DNS availability means customers applications are reachablefrom name to IP resolution point of view.So let's discuss it in more details and find out how Amazon Route 53 can offer such level of SLA aswell.Let's start with the very basic DNS resolution, logic and concept and find out how, for example,when a user make a request, get a reply.Let's start with the basic concept of DNS request or name to IP resolution.Typically, a user sends a request, let's say, for a website like WW dot example.com, and that requestgoes to the DNS resolver, let's say using Internet, and this goes to your service provider, DNS,IP resolver for DNS.That DNS will forward it to the DNS root to ask okay, where I can find the IP address of example.com.So the route might not know about it, but it will reply will say okay, check with the top level domainof the.com.Okay.And then that DNS resolver will go and check with that top level domain.com.So the top level the TLD for.com doesn't know the the resolution IP of that specific domain.However it knows like who's the authoritative DNS server or servers or nameservers for the example.comin which it will share the request to be sent to the Amazon Route 53.Because in this example is the authoritative authoritative DNS server for Example.com.Okay.So once that send to Amazon Route 53, the DNS resolver will get back the IP address from one of thename servers of Route 53.Then the connection can be made, for example to the Alb.So as you can see here, there are a few or like for here in this example name servers allocated.Okay.As part of the DNS setup of the hosted public public hosted public zone.Okay.For example.com.So there is 1.com.net.org.co.uk for example.And the question that some of my ask, okay where are these Amazon roughly three name servers located.So these name servers are responsible for that hosted zone where they are located.If you remember previously when we talked about AWS global infrastructure.So as part of these, there are edge locations and along with these, there are pops that is distributedglobally.And in these pops there are servers responsible for name resolution that belong to Amazon Route 53.Okay?So this give it a global reachability and also close proximity to a lot of users across the globe indifferent cities.Now if we expand this further, let's look at one name server and how it can be distributed and advertisedto the outside world from the Amazon 53 Pops.And this example, we will look at the Dotnet name server and as you can see, it's distributed acrossmultiple pops globally.And also the same IP is assigned to these name servers of the Dotnet server.Typically there will be many of them for for scale.And this IP is advertised to the Internet.This means DNS resolvers will use the closest pop to reach this nameserver.And if there is any issue with the closest pop, the next closest one can be used based on internetrouting.Since all using same IP for the same name server and as you remember the authoritative name server werefrom different top level domains like.com.org.ET cetera.And those name servers are distributed at a scale in the same manner which even increase the availability.These name servers or reachability to any of these name servers to a very high degree.So this is one of the key characteristic of Amazon Route 53 architecture that make it very or 100% SLAprovided by AWS.So after this brief overview, it's time now we dive into Amazon Route 53, specifically the policies.So next we will dive into the Amazon Route 53 routing policies or request routing policies.There are multiple options you can choose from, and sometimes you may combine more than one policyto design large scale, resilient solution.You need to have a good understanding of these policies.