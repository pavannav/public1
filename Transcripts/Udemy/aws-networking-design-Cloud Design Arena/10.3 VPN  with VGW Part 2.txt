# 10.3 VPN  with VGW Part 2

In a very oversimplified way if you are not experienced with BGP.And when we talk about as path as numbers, etcetera, simply the route source for the same prefix fromdifferent BGP peers with the less number of as numbers is a preferred.So this is keep it as a rule of thumb.Technically there are more attributes and there are more logic before the as path might be considered.However, let's look at the oversimplified logic when we talk about as path and prepending.This is what we are referring to.If we add multi exit discriminator to the mix as another BGP attribute for path selection.So typically when the as path are matching, then the multi exit discriminator comes into the picture.I think about it something similar to link cost is something that you need.You can use it, for example, to prefer one path over another.However, there is an important aspect that technically or by default we can say traffic or route shouldbe sourced from a single BGP.So if we look at the second example here, the mid is not compared, however, depends on the vendoror certain specific configuration.This behavior can be changed.That being said, in order to simplify design with routing, we will look into different design scenarios.And this recommendation now I'm about to mention, will be applied where possible, but it's not alwayspossible.Maybe sometimes we use it partially for certain throughout selection, but and sometimes we use it completelyfor the route selection and sometimes it might be impacted by the limitation of number of routes canbe advertised in which it it may not be the optimal way to use it.However, in general, always advertising more specific route.Let's say here with BGP can influence route selection.And as you remember, in every service that we discuss on every route, priority selection, alwayslongest match or longest prefix is preferred.So that is a way that we can guarantee that path will be preferred where applicable.Let's look at this architecture quickly.Again, this may let us think of another scenario What if these customer device gateway each belongto a different site?So if you have multiple site to site VPN connection, you can provide secure communication between siteusing VPN cloud hub.This enables you by using multiple remote site to communicate among these remote sites and not justwith vpcs.So now here we are creating a hub and spoke architecture that you can use with or without vpcs.So this design is suitable when you have multiple branch offices and existing internet connection andwould like to implement a convenient, potentially low cost hub and spoke type of architecture for primaryor backup type of connectivity between these remote offices.In addition to that site that uses direct connect connection connected directly to the virtual privategateway through private virtual interface also can be part of this hub and spoke architecture.So we can add this to the mix.Remember that you cannot do this with direct Connect Gateway, but this option available when you haveVif directly attached to a GW in which that you can have VPN to direct connect cloud hub use case.We will elaborate more around this because now you might be wondering how this compare to Transit Gateway.Later on we will be discuss and how this may be compared to other architecture.So just hold this thought and we will elaborate and discuss it in further details in which that youcan make the right design decision based on certain requirement and certain architecture attributes.