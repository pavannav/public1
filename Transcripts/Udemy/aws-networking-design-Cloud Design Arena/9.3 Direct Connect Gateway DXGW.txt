# 9.3 Direct Connect Gateway DXGW

The next network service endpoint that we are going to discuss is the direct Connect Gateway, whichis a globally available logical object where you can create the direct connect gateway in any regionand access it from most of the other regions, excluding some regions like China region.So it's better like you refer to the documentation for the specific exclusions of the region that isnot acceptable, accessible globally.However, you can say almost the typical regions are all accessible.The direct Connect gateway at the time of recording, it's mainly for direct connect connection.So a direct connect connection linked to an AWS direct connect gateway can be associated with eithervirtual private Gateway, which is directly attached to the VPC.As you can see in this architecture where VPC can be in the same region or different region of the directconnect connection location.Or it can be associated with Transit Gateway.If you don't have knowledge about the direct connect virtual interface types, don't worry because wewill going to cover the different type of virtual interfaces.VFS here presented as abbreviation in more details in a specific sessions.When we start discussing direct connect, there are some basic design considerations that we are goingto start with which are related to the direct Connect gateway.Starting with the Cidr Block, so you cannot have overlapping Cidr blocks with regard to the associatedVpcs.Also direct Connect Gateway does not allow direct communication between Associated Vpcs.As well as does not allow hairpinning a type of traffic connection where you may send the traffic toan on premises network and route it back over the same link that is associated with the direct Connectgateway and distance to another VW or VPC as it's shown here in the yellow traffic flow line.That being said, if this is something that you need as part of the design, we will look into a designscenario in a separate session with as part of the multiple vpcs architecture sections of how you cando type of routing related to the one presented with the yellow line.But we're going to use multiple gateway for that design.So don't get confused with that just now.Focus into the single direct connect gateway that does not allow such traffic flow.Last but not least, from scale point of view, each direct connect gateway can connect to up to tenvpcs.All right.So let's zoom in into that part and discuss it a little bit further.Details.So the direct connect gateway can connect to ten vpcs across different regions and each direct connectconnection can connect to up to 50 direct connect gateways.Which means you can scale a single physical connection.To connect to up to 500 vpcs.However, this is something you need to think about and analyze from architecture or design point ofview.Is the connection performance based on the expected communication from the vpcs to the on prem.And vice versa.Sufficient for such scale because the solution may offer high scale in terms of number of vpcs, butmay maybe the connection performance in terms of throughput is not enough, in which you may requiremultiple connection in such case.In other words, don't only think of a scale from the number of vpcs.Think about it in terms of other aspect as well, like the connection capacity scale, as well as drawthe complete picture of the IP communication model to have the most optimal design.Also with Direct Connect Gateway, the Vpcs connecting through the GW, the Virtual Private GatewayAssociation can be in different AWS accounts.This is important when you have multi account architecture and you want the vpcs and these account touse same direct connect connections with Transit Gateway, we will discuss it and the subsequent section.But in general, the transit gateway can extend and share the connection with multiple accounts as well.From an architect point of view, you need to know this is a supported design, especially with largescale organization where they may have hundreds or maybe thousands of accounts.It becomes critical to have the ability to share a connection with multiple accounts, not only vpcsin one account.All right, let's dive a bit deeper with the AWS Direct Connect Gateway and look into the routing operationand behavior when it comes with connecting via direct Connect Gateway.When you associate a virtual private gateway with a direct connect gateway, you specify a list of AmazonVPC prefixes to advertise to the direct Connect Gateway.The prefix list act as a filter that allows the same Cidr or a wider Cidr.So here, when we say wider side, I mean larger scope of ranges to be advertised to the direct connectgateway.In which you must set it.Set it either same or larger in terms of coverage.So let's look at an example here.We have it prevented, because if we look at the VPC, Cidr is 10.100 slash 16.However, if we put this allowed prefixes as 10.100 24, which is less than the Cidr or 20.100 slash16, which is something not same as the existing Cidr.So in such case the Vgg will not receive root.On the other hand, if we change it with same prefix length or maybe a smaller in terms of the prefix,but it is a bigger range which is slash eight.In such case it will receive the root.For a Transit Gateway Association.You provision the allowed prefixes list on the direct Connect gateway.The list is used to route traffic from on premises to AWS into the transit Gateway.Even if the VPC attached to their transit gateway do not have a signed Cidr with the one that you'regoing to put it in.In this list, prefixes in the direct Connect Gateway allow the prefix list to originate on the directconnect gateway and advertise to the on premises network.Let's look at an example.This is very important to understand this behavior.So if we advertise like this list of prefixes, okay, which is ten 200 2410 0008192.The route will be re originated and advertised from the direct connect gateway back to on premises.So if we look like here, we advertise something larger than what we have.We advertise something even does not exist, which is 192168.So what that means, that means the on premises router will receive this route over BGP and for example,it may send traffic to 10.101 and that will be propagated back to direct connect sorry, to the transitgateway through the direct Connect Gateway.So typically this is a black hole routing.You need to be aware of such behavior in which that you'll be very wise and specific when you advertisetraffic through such capability that is available when you have a design using Direct Connect, Gatewayand Transit Gateway.We'll look into some design scenarios related to that.But now it's important you understand this behavior.And as we mentioned earlier, prefixes in the direct Connect gateway allow the prefix list originateon the direct Connect gateway and are advertised to the on premises network.So that means it does not have the dynamic relationship with what is being received in actual routepropagation from Vpcs, for example.Last but not least, route priority or preference with direct Connect Gateway is BGP based, which meansit is based on BGP attribute like as path, local preferences and where we will discuss it in more detailsas part of hybrid connectivity design scenarios.To simplify it and look at look at it from the perspective of practical designs and the same for theother services as well.However, because this is mainly BGP based and just dealing with direct connect, so we will not coverit.Now just look into the design scenarios where we will look into different use cases so you get the pointabout it.