# 8.7 CDN - Security part 3

Access control.It's an important aspect when it comes to security.So we will look briefly into this.It's not related specifically to networking, maybe a couple of component.However, as long as we're covering the capabilities of a CDN network, it's a good practice that wehave knowledge about what additional capabilities that provide us in terms of solution, because alwaysas architect or designer, we need to look at it as a system and infrastructure, not only from a singleangle.So when it comes to Amazon CloudFront Access Control, there are two key common approaches that usefor access control, which are the signed URL and signed cookies.Both provide same basic functionality.They allow you to control who can access your content if you want to serve private content through CloudFront,however, they support different capabilities that translate to different use cases.So with the signed URL, typically you are restricting access to individual files, and what it doesis as signature to the URL or query string, this scenario is commonly used by client or user that donot support cookies, while with cookies you can restrict access to multiple files and give you theability that you don't change anything.When it comes to the used URL, the signature is added to the cookie.Additional way of access control provided by CloudFront is that the ability to provide geo restrictionin which that you can do a restriction based on geographical client IP location.So this IP location based type of filtering also when it comes to infrastructure filtering that youmay do, this is related to networking.So it should be more exciting to you.The ability to use Origin Origin Security Group, we whitelist the IP addresses or allow the IP addressesof CloudFront edge locations which which you can obtain it and then allow it in the security group thatis in the application load balancer in which the application load balancer is not accessed directly,only can be accessed through edge locations.When you have such architecture integrating your application load balancer with CDN, CloudFront asan origin.Another technique to prevent user from directly accessing an application load balancer and allow accessonly through CloudFront is by using custom Http header where you need to configure CloudFront to adda custom http header to request that it sent to the application load balancer and configure the applicationload balancer to only forward requests that contain the custom Http header.So this is another way that you can form it to make sure that request only from from the cloudfrontare accepted and forwarded to the backend system.Last but not least, this is important just to mention it.If you are not aware about it, if you are familiar with AWS, I'm pretty sure you are familiar withthe OAI or called Origin access identity.So this is mainly used to restrict accessing S3, specifically an S3 bucket where you have, for example,certain object that you cache it in.CloudFront could be videos, could be images.Okay, so that bucket or these object cannot be accessed directly with such security mechanism or restrictionin terms of accessibility.Instead, they can only access through or can be only access or phished only by the Amazon cloudfrontedge location.So this is also a technique for providing access control restriction.