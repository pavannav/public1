# 14.1 Design Scenario -1

All right.Time for a design example to simplify what has been discussed around traffic engineering.This example design scenario discussed traffic or route engineering use case.And this scenario A company has two data centers in Sydney and Singapore.As you can see, and they have a VPC or multi Vpcs built in Sydney region.As you can see, there are two direct connect connections, one gig to Sydney Data Centre and ten gigto Singapore data Centre.There is a shared IP prefix between these two on premises data centres, which is 192168.10 slash 24and as you can see there is direct connect between.Sorry.Data centre interconnect between the two on premises data centres.So might be layer two connections or overlay using whatever third party solutions to provide a seamlessintegration between these two subnets as a single shared subnet between the two data centres.A BGP number used for the on prem side of the Dhcp solution using BGP as number 65001.And the shared the prefix advertise over the two direct connect connections.This company would like to use Singapore Data Center site to be preferred for the traffic going fromAWS VPC to this network prefix.The shared prefix.Therefore BGP as path prepending.Use as part of BGP configurations to make the path longer via the Sydney.Path typically with the prepending.All right.And shorter through the Singapore side.So by keeping it with the default it will be through Singapore side looks like coming from through singleas number which is 65001 while three x prepending So here it should be 650016500165001.And here three x so we are missing one more 65001.Anyway, the idea is as long as long, even if they are two as numbers, that's it.It's considered long.So this is just to provide the logic that as path prepending is used to make it look longer from routingcontrol plane perspective.However, once traffic started transmitting, Sydney path was a preferred path.So why is that?The reason, If you remember, based on the route priority with Direct Connect Gateway, there is anetwork of path cost that is considered before BGP as Path as a BGP attribute and the route preferencedecision making.Let's look at it again.So if we look at number two.There is a preference for the lowest network cost in terms of VPC to DCS location and typically theVPC in Sydney and this direct connect location in Sydney.So this can make an expected behavior if you are not aware of such route priorities or preferences.That's why understanding the logic of traffic engineering is important.So how can we optimize this design to meet this company requirement?By looking at the rot preference or priority list, it's obvious that by using BGP local preferenceattribute, we can have better control because it's the first in the priority list.And for sure this is after considering longest prefix match.Here we are discussing same prefix length advertise over two paths.With AWS, you can use certain BGP community values to assign specific BGP local preference values forroute preferences.We discussed BGP community values usage with the public virtual interface public VRF here, the privateVRF.We can also use it, but these are different values and achieve different goal.So they still the same type of values in terms of BGP attribute or tagging or community values.However, these are used with private lives and to achieve different goal, which is to assign localpreference and path of preference.So by assigning BGP community value to the prefix 19216810 slash 24 as it advertise from the customerrouter to the direct connect router in Singapore, the location, technically it will be given higherlocal preference BGP value and makes it more preferred path from the direct connect gateway point ofview.Therefore traffic from VPC in Sydney distance to the on premises network 19216810 slash 24 will usethe Singapore path after this change.