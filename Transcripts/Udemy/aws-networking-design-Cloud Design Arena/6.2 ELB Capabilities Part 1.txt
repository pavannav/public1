# 6.2 ELB Capabilities Part 1

So before we dive into each of these load balancing options, let's look into the key common principlesof how elastic load balancing works.Then we will dive into each type with its specific capabilities.In general, NLB uses the concept of a target group where you can register multiple target groups throughoutthe request to a group of resources.While a target group is a pool of similar resources that serve similar client requests.From connectivity point of view, any LP port is configured as a listener port.Such as TCP port 443 for Https.This is how you expose application on that port and it can be reached by its IP or IP by the DNS domainname where you can create an alias record and the Amazon Route 50, for example.Then point to the ELB.The DNS entry is controlled by Amazon because your load balancers are in the Amazon Aws.com domain.The Amazon DNS servers return one or more IP addresses to the client.These are the IP addresses of the load balancer nodes of your load balancer.In addition, you can utilize Autoscaling concept to add elasticity to the solution where instancescan be added based on predefined metrics such as an increase CPU or memory utilization over a certainthreshold.This is how you can optimize the design that we started with that has no unified entry point.Like I'm using an ELB now.ELB is the unified or central entry point from client connection point of view, and the backend canscale out and in without without the need to do any tracking to the new instances.IPS For example, because this is handled by the Alb, then traffic rebalance and distributed accordingly.Also ELB Perform performs health checks against resources in a targeted group and only send the trafficto healthier resources and the targeted group.And the health check is a periodic request sent to each resource and the target group to test the resourcescapability to serve client request.And this is something configurable based on the application, port and nature, whether you want todo it also at layer four or application layer checking for that health check.Instances for groups that use ELB health check are considered healthy if they are in the running stateand they are reported as healthy by the load balancer.Let's expand more on Autoscaling and its use cases.For autoscaling.Typically, you will need to set minimum and maximum number of instances and create a policy to setmetric that determine the scaling.Trigger and timer in which, for example, if the utilization goes above a certain predefined threshold,new instances can be added.On the other hand, you may don't need more instances for your setup however you still want.For example, take advantage of the health check associated with the auto scaling group and replaceany malfunctioning instance with healthy one.This use case also known as auto healing.Instances are assumed to be healthy unless ec2 auto scaling receive notification that they are unhealthy.This notification can come from Amazon EC2.Elastic load balancing or custom health checks.After an instance has been marked as unhealthy because of a health check, its almost immediately reschedulefor replacement.So it depends how you want to use it in the design and architecture, whether for scaling auto healing.So this is just to give you an idea of the different use cases or common use cases that you might considerin your design.If we look again at this design we just discussed.We can see it is obviously using a a single availability zone.And we know this is not a recommended design because the availability zone here represents a singlepoint of failure and this architecture.And this is where you can take advantage of the safety and reliability of geographic redundancy by spanningyour ELB functionality with autoscaling groups across multiple availability zones within a region.And this enables you to distribute incoming traffic across different Azs.Incoming traffic is distributed equally across all availability zones enabled for your load balanceror as the routing model is set up, such as, for example, you might do weighted traffic routing withyour application load balancer, where you can send a percentage to certain targeted group and differentpercentage to others.You can either use separate single zone autoscaling groups as shown in this design.Or you can expand the the group across multiple availability zones.When one availability zone becomes unhealthy or unavailable.Amazon EC2 Auto scaling launches a new instance in unaffected zone.When the unhealthy availability zone returns to a healthy state, Amazon EC2 Autoscaling automaticallyredistribute the application instances evenly across all the availability zones of your auto scalinggroup.Amazon EC2 Auto scaling does this by attempting to launch new instances in the availability zone withthe fewest instances.We define the health check from ELB point of view.Now let's zoom out and look at health checks from an architecture point of view.We can look into different layers of health check rather than just one layer.When I say one layer here, we will divide it into ELB health checks and DNS or Amazon Route 53 healthcheck layers.Typically, there should be at least two layers.As you can see here in this design of health checks in a multi-az architecture with Elbs and DNS.You may have more depends on the architecture.You can have different layers of DNS than you can have different layers of elbs that goes into morecomplex architecture.But let's keep it with this most common and basic architecture.We should have DNS layer and ELB layer in terms of health checks with active health checks.The load balancer periodically sent a request to each registered target to check its status.Each load balancer node checks the health of each target.This is where unhealthy instances are detected and can be replaced with new healthy ones automaticallyat the DNS layer.More specifically, Amazon Route 53 level the health check.Look at the bigger picture in terms of the health check of the ELB or might be multiple elbs.We will look in a future sessions how we have at a global scale multiple elbs with Route 53 architecture.However, technically making a decision about the health of an LP endpoint is more complex than healthchecking.A single IP address, for example.What if your application is up or running on an EC2 instance target, but the load balancer itself isn'treachable, or if your load balancer and your EC2 instances are functioning fine.But there is an issue at the application itself.Route 53 DNS failover handles all of these failure scenarios by integrating with ELB behind the scenes.Once enabled Rule 53 automatically configure the manage health checks for individual ELB nodes.Rule 53 Also take advantage of EC2 instance health checking that ELB performs by combining the resultof health check checks of your EC2 instance and a target group and your elbs.Rule 53 is able to evaluate the health of the load balancer and the health of the application runningon the EC2 instances behind it.And this presented design.Let's assume the load balancer node in ASB, for example, facing an issue in which detected as unhealthyfrom the Route 53 point of view.Typically, Route 53 will resolve.To help the ILB node IP address only in which request will all be sent to the other Alb node in Asaand then the the traffic will be eroded across to the different instances that belong to the targetedgroup.So this is just a sample scenario to visualize the logic, how exactly it works.It depends into the number of layers that you configure, your DNS, number of layers that you have,your elbs as well as the way that you structure your targeted group in the back end.I would say this is something that you consider it as a foundation and then you can expand it.So always start with something clear, simple, and then go into the more complex one because the conceptis still the same, but you just expand on it.And typically this always should be driven by applications and business requirement and resiliency requirements.So don't start with complex.Always start with the simple one.With health checks at different layers, we typically achieve better resiliency.To optimize the solution, resilience and performance.Further, to work with the ELB elasticity seamlessly, you need to make services stateless where possible.Therefore, services should either not require a state or should offload state of different client requestsin which there is no dependency on local stored, locally stored data on a disk or memory.A key benefit of a stateless application here is they can scale horizontally because any of the availablecompute resources can service any request, and this enables services to be replaced, added or removedas part of autoscaling without causing any availability impact.Amazon Elastic Cache or Amazon DynamoDB are good destinations for offloading application state here.