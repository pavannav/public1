# 10.1 Connectivity Overview

After the different AWS network services endpoint defined on discuss in details.Now it's time to start cover the other component of hybrid connectivity building blocks, which is theconnection.As you can see, there are two primary connectivity types VPN and direct connect.Let's start by defining VPN and a specific site to site VPN, which enables two separate networks orsites to communicate in a secure manner with each other over another network that might be an untrustedtransport such as the public Internet.Or it could be a trusted transport over an Mpls provider.Or later on we'll discuss over direct, connect, etcetera.The two interconnected sites could be vpcs within a region or across different region as well as itcan be a VPC and an on premises data center site or branch site.And that's what we will focus in this section, which is more about the hybrid architecture and siteside.VPN can be achieved in two approaches managed VPN and customer managed VPN.Managed VPN service refers to the underlying service infrastructure, software, etcetera, in whichprovide the required infrastructure and software and all of the components required to build the servicealong with the pre-specified availability SLA of the service endpoint.This can be accomplished by using either VPN with Virtual Private Gateway or VPN with AWS Transit Gateway.