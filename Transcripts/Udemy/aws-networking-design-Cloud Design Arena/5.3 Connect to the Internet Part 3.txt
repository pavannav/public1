# 5.3 Connect to the Internet Part 3

With this.Now let's move into another use case still with the connectivity to the Internet.Now let's look into a use case where we need only outbound to the Internet access.If you remember from the previous session, we highlighted about such use case, but we did not divevery deep here.We're going to dive much deeper.So.The question here.We had instances in a public subnet.Now, what if I want these holes to be, for example, in a private subnet?Okay.But I want to communicate to the Internet.Maybe I don't want the communication to get back to these instances as well.What can we do?So maybe I have only software update on these instances.I want them to connect, but I don't want anyone to connect directly to those instances.So if you remember from the previous session, we mentioned that Nat Gateway can make life easier.Nat Gateway in which that we have like the host in a private subnet and Nat Gateway is in a public subnetthat act as the target for host in a private subnet to access the internet.If we look at the route table B where the Nat gateway is the target for the default route for theseinstances and in this design, because we are doing nothing at the Nat Gateway, we don't need to haveingress route table because the GW, the Internet gateway, will see the traffic coming from the NatGateway IP and it will just perform nothing at the public IP level and send it back to the Nat gatewaydirectly.However, there are a couple of aspects we need to be aware of with such design.So from documentation that you can refer to, you can find that inner gateway can support up to 55,000simultaneous connection to each unique destination.Okay, unique destination.Its mean single flow typically a single IP address.This limits also apply if you create approximately 900 connection per second to a single destination,about 55,000 connection per minute.So here it's a scale consideration that, for example, maybe it's fine with you or maybe, for example,you need higher.And the other aspect is how can I get, for example, for this design?So now in addition to performance, how can I get to such design?Because now I'm having like my instance is distributed across two availability zones.However, the Nat gateway here is the single point of failure because it's in a single AZ.So for example, if there is a any service interruption in ACB, even my instances in Asia will be impacted.So now I'm not really achieving my goal in terms of reliability.This is where we can provision another Nat gateway.So if you look at here, the design, we just have two Nat gateways and just look at the route tableB and C, each one pointing to a different Nat gateway to a local nat gateway that is located in itsown AZ.So with this design we achieved.Essay or because of their failure one as we still survive with another as and also if there is performanceimplication with the mentioned limit earlier now your work your traffic volume will be distributed acrosstwo Nat gateways and for sure you can spin up more and then you can have higher performance and throughputin such case.The other question like from design perspective, you may ask, okay, can we integrate this use casewith what we discussed in terms of gateway load balancer and network Firewall?So now, okay, nodding, it's fine and it's doing what I want.However, now I want to have another layer of inspection.So can I use what we have just discussed?The answer is 100% yes, it's possible.So what we discussed in terms of Gateway load Balancer, this architecture where we have gateway loadbalancer, any provisioned in its own subnet and use as a target and the host in a public subnet.Now with a Nat gateway architecture, the instance is moved to a private subnet and the Nat gatewayis a place in a public subnet.And then we keep the gateway load balancer in a in its own subnet.And the difference in terms of routing is that the instance route table has a default route pointingto the Nat Gateway.And the Nat Gateway point to the Gateway Load balancer ENI for the default route and the Gateway Loadbalancer.ENI uses the Internet gateway for exit.And for sure, we will need the ingress route table here to make sure the return traffic to go to theGateway Load balancer ENI to go for inspection as a return traffic and get back to the Nat gateway.Then the instance.Okay.And from traffic volume perspective, it will look like something like this.So from the instance not knotted and then to the gateway load balancer and back and the return trafficwill follow exact same path.Here you might think, okay, what if I have a load balancer?Okay, so now we are discussing nothing.So load balancer is something we will discuss in a future session for providing making our applicationaccessible.However, I just want to highlight that you may have a load balancer instead of a Nat that is frontend in your application, and the load balancer will use the same architecture as well.Still in the same architecture, but it's a slightly different design option here.Like the question is, okay, now we did the Natting, then we send the traffic to the security servicesfor inspection.What if I want first to do the inspection and then I do the natting in which the firewall virtual firewallwith the network firewall or Gateway load balancer with firewalls will look at the source IPS beforeNatting and then the Natting is being performed.