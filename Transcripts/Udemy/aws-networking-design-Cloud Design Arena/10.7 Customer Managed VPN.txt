# 10.7 Customer Managed VPN

Okay, time for the customer managed VPN.So with the customer managed VPN, the customer typically is responsible for the provisioning and managingthe entire VPN solution.Typically running a VPN software on an EC2 instance or instances, or it could be a VPN virtual appliancefrom AWS marketplace, including Sd-wan Solutions.Also, the design and deployment of the availability of this VPN setup need to be taken into considerationfrom the AWS VPC side as well as on premises side.Some of the solutions available at AWS Marketplace by the independent software vendors ISVs may offerautomated provisioning of virtual appliances along with its redundancy.Still, with this option, you need to take care of any software or patch updates, additional instancesto manage as part of the VPC in addition to any required licensing from the ISV.However, depends on the use case scenario in terms of requirement and constraints, you may find itmore feasible.Option.For example, this option can be used in conjunction with AWS Direct Connect to achieve different routingscenarios such as backup path with direct connect or over direct, connect to provide encryption intransit.The design shown here provides connectivity to a single VPC.Let's expand on this design and see how it can be used to connect to multiple vpcs.This design model, commonly known as Transit VPC, in which the VPC where the VPN virtual appliancesset up, operate as transit nodes to connect on premises over IPsec tunnels, over the Internet, anduses again IPsec tunnels over AWS infrastructure that terminate on the vpcs that need to connect whereeach user's virtual private gateway.As shown in this design, it's obvious that the end to end throughput is constrained by the IPsec Tunnelscapacity, as well as the throughput capacity of the VPN virtual appliances.Also with this design, the more on premises sites and vpcs you have, the more complex the solutionwill be to build and to build and manage as well as to maintain a good resiliency.Because now you'll need to deal with a lot of connections in terms of VPN tunnels and a lot of routingcontrol.ET cetera.So it's not a simple solution, this design to scale it.The other possible design option is to use AWS Transit Gateway instead of using IPsec Tunnels from thetransit VPC to each single VPC.So this design offers more scalability and easy to manage solution.However, it's still constrained to the bandwidth capacity of the VPN virtual appliances, as well asIPsec Tunnel throughput.In addition, the management of the VPN solution, since it's customer managed, it's responsibilityof the customer to maintain it, to build the availability, resiliency.ET cetera.That being said, it for sure better than the previous option because Transit Gateway can offload alot of complexity or abstracted in which that you can have, as you know, up to 5000 vpcs connectedto a single transit gateway.And you can have the flexibility of using multiple route tables and you do all of that route controlsand security communication if required, where you have, for example, specialized security VPC forinspection, etcetera, with the previous option.That can be very, very complex if you want to do it, or you may even want to use integration withyour existing sd-wan solution.So the design option are more flexible and scalable with Tgw compared to previous ones that uses IPsectunnels.And now when using Sd-wan to the transit gateway, it is even more flexible because in general, asthe one solutions offer more simplified management to the VPN connectivity and technically or commonlyreferred to the Sd-wan overlay at scale.However, there are a few key capabilities differ from vendor to vendor, and this is where you as anarchitect or designer helps to identify the most suitable option for the targeted solution or design.With the Sd-wan integration with the AWS Transit Gateway data plane or forwarding of traffic happensover tunnels while control plane where the routing information is exchanged is performed using BGP.Let's zoom in more into this part of the integration and analyze it further.First, let's look at the element of the association between the VPC Connect, where the Sd-wan virtualappliance reside and an AWS transit gateway.So typically you'll have transit Gateway, VPC attachment, and on top of that you will have Connectattachment that uses the VPC attachment as a transport, and then you will establish the connect peeringusing GRE tunnel with BGP to exchange routing information.Now let's look at some performance routing limit at the time of recording.So with the transit gateway VPC attachment, you can get up to 50 gigabits per second of throughputwhen it comes to connect attachment, multiple connect attachments per transport attachment are supported.We will know a bit why multiple connect attachment might be needed and when it comes to the connectpeering that use tunnel, you can get up to 55 gigabits.That's one peering, one connect, peering over a tunnel.However, you can have up to four connect peering tunnel per connect attachment.That means if you have four and the design that terminate on four appliances and you use equal costmultipathing, you will get aggregate of 20 gigabits.And if you remember, we mentioned about multiple connect attachment.So now each connect attachment can handle four.So if I have to, I can have eight.So that's something related to scalability.With regard to routing information, you can see from the virtual appliance, you can send 1000 routeand you can receive from transit Gateway 5000 route.Don't take this number of routing information for granted.It's fixed because, as you know, always improve and enhance.So always refer to AWS documentations for the specific number.However, one thing that we obviously can notice here, this is for sure way, way more than using,for example, when we use direct connect that transit gateway, the amount of route that each transitgateway send 20 route and direct Connect Gateway send 100 route.So this is something very limited compared to using such overlay.So this is one of the drivers sometimes for customers or enterprises to use an sd-wan overlay to havethe ability to exchange higher number of routing information.Let's look at some BGP routing considerations.So this consideration, it's a bit of configurations related, however it's good to be aware of.So if you are using BGP as number as n different than the transit gateway on your virtual appliancesof the Sd-wan, you need to configure BGP.Multihop So typically because here you are not using same directly connected subnet and you're hoppingover a different hop or maybe a couple of hops.That's why even you need to set the TTL value to two.And if you want to achieve equal cost multipathing that we discussed about.So in such case you can get benefit of higher throughput each each tunnel or peer connecting can canprovide up to five gig.So now we can get ten gig as aggregate bandwidth.First of all, you need typically to advertise same prefix length from the virtual appliance to thetransit gateway that's given.Now we have same prefix length.Then we need to make sure that the BGP as Path and the autonomous system numbers are matching.This way you can achieve equal cost multipathing over the available paths.