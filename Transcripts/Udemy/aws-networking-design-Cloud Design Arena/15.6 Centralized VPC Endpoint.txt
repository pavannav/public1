# 15.6 Centralized VPC Endpoint

In a previous session, specifically the connecting your VPC to the outside section, The connectivityto public services was discussed and one of the options discussed was the VPC interface endpoint.And this video session we focus on the centralization of these VPC interface endpoints to provide accessto AWS services and a specific private access for multiple vpcs in a centralized manner.So the typical architecture is shown here where we're going to have a hub and spoke type of setup.The VPC interface endpoint provisioned at the Hub VPC at the top and spoke Vpcs here in this designneed to access these vpc VPC services privately over transit Gateway or might be using VPC peering viathe hub VPC where the service endpoint provisioned.But remember here, if we use peering, the peering will be direct with the hub or the VPC where theinterface endpoint provisioned.As you remember, peering is direct, non, non, non transitive.And in such design we need mainly and first of all to ensure there is layer three IP connectivity betweenthe spoke Vpcs and the hub VPC at the top where the interface endpoint provision because later on thatIP will be shared through DNS and then without having IP reachability, the connection will not happen.And this design also can be extended to a different region with the transit gateway peering or VPC peeringover multiple region.If we are using VPC peering as part of this design and as we mentioned, peering is direct, not transitive,through an intermediate VPC in the middle and in the same manner, the connection can be extended toan on premises side.As long as we have the layer three reachability specifically here for the on prem to the interface endpointIPS, then it shouldn't be an issue from communication point of view.Now let's look into some DNS consideration with Amazon Route 53 to enable such communication model.As always, our focus is design in this course.However, sometimes configuration related details are discussed if it can help to understand the conceptbetter.And here the details can help to understand the role of DNS to enable such connectivity model.For simplicity, let's look at a single VPC with a single interface endpoint and the hub VPC where thisVPC endpoint provisioned.You will need to disable DNS for the VPC endpoint at the Hub VPC and then you create a private hostelzone with Amazon Route 53 using the full service endpoint name with the region along with an alias record.And then you can share this private hostel zone with the spark Vpcs in which they can resolve the serviceDNS name to that private endpoint IP, or you may use Route 53 resolver instead.Let's find out how.So Route 53 resolver and brief provide inbound outbound DNS services in a VPC.It allows you to resolve domain names for AWS resources in the region where the Resolver endpoint isdeployed.It also allows you to forward DNS requests to other DNS servers based on rules that you define.We will need to have resolver, outbound rule or rules for the service or services endpoint name ornames at the spoke vpcs and inbound resolver at the hub VPC.So these outbound rules should follow the DNS request to the appropriate inbound route 53 resolver andthe hub or central VPC with the endpoint.So typically the rule matches the DNA, the DNS name of the service requested, and then it should forwardit to that inbound resolver IP address.And typically once the Route 53 resolver receive that request, it should respond with the private IPaddress of that service interface endpoint that is located in the local VPC.For hybrid connectivity part, we again need a layer three connectivity through the VPN, direct connect,etcetera, or via Transit Gateway, or it could be via virtual private gateway.Depends on the design.As long as providing the layer three connectivity, then you need to create inbound route for the resolverendpoint, or maybe the same one that was created and that VPC.And then because here we are having a hybrid architecture, typically you will need to do some changesto the on premises DNS server in which the.You need to set up a forwarder from the DNS server that is residing on premises side that point to theinbound resolver endpoint in the central VPC.Typically for the related domain names of the servers that need to be accessed from on premises side,and this should provide the required reachability in such architecture.With that, let's look into some design considerations.So we discussed a couple of options and this is specifically for centralized services, endpoint access.So first of all, scale, there are multiple factors to look into in terms of VPC, interface, endpoint,private hosted zone, etcetera.So at the time of recording VPC Peering, we know there are limits around VPC peering.How much, how many VPC peering that you can have as a max.This is where you can refer to AWS documentations.You may find like you can have 50 active and you can increase that limits up to 125, etcetera.And also you may have limit around maybe around 100 associated vpcs per private hosted zone, even thoughyou can request increase around that.But this is a limit that also you need to look into and also the number of interface endpoint per VPC.This is also something that you need to look into it if you want to deploy many of them.When it comes to cost, it is more cost effective.You can say, with higher number of services or vpcs when you have many of them rather than you.You for example, provisioning these VPC interface endpoint across different vpcs and there will bemany and it will increase the charges, but it might be less efficient with high volume of traffic becausespecifically, let's say here we are passing traffic as inter-region traffic or through the transitgateway.And if it's high volume of traffic now you need to add to the mix how much the data processing costwill be, etcetera.So this is something you need to look into it.You need to analyze it further so it cannot provide like a standard rule, but it is like point to lookinto it.Then you can evaluate which option can be more cost effective.When it comes to complexity.Typically this will be reduced because here we are centralizing, aggregating these interfaces intoa single VPC.And with Transit gateway we can have very simple, easy setup of a hub and spoke communication as youremember.In terms of performance, this is obvious that you will be limited to the capacity in terms of throughputor performance of those interfaces in a central VPC.So again, look into the design.If it's like there is high volume of traffic and you have many vpcs, then you might look into somethinglike distributed or maybe you do a centralized per region so it can be different option deployed basedon the volume, complexity, scale and cost.So you need to take all of these considerations into account when you're building the design.