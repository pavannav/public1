# 11.4 Public VIF Routing Design

As we discussed in the previous video session, by using direct connect connection with public virtualinterface public.Typically you are doing public peering with AWS in which you can access public resources, includingAmazon.com.However, this access in terms of geographical scope or coverage can be controlled as desired.You can limit it to only the local region where the direct connect connection is located.In terms of that connect connection location.You can limit it, for example, to the continent, to the where the region, the direct connect connectionlocation is located.Or maybe you can open it to the entire global infrastructure globally with constraints around Chinaregion and government region that require different approvals and connectivity setup.First, let's look into how your advertise IP prefixes or prefix, maybe limited number that is typically.Public IP range can be controlled from being advertised to contain to to certain scope as shown in thisdiagram that we have the locker region, the continent and global.First you will need to have BGP peering as we discussed, to establish with AWS over the public virtualinterface.Then will perform packet filtering based on the approved and expected IP source or sources from yourIPS to ensure that is legitimate type of traffic.So that is done at the side.The router side.After that, the route is propagated or advertised to the desired scope that will be based on BGP communityvalue, which is simply you can think of of it like a label or a tag.You assign it to the route you advertise using predefined values from AWS.And technically you assign it on your router, the customer router as part of the BGP policies configurations,you pick the desired value based on the geographical scope that you need your IPS to be advertised to.As you can see in this diagram.And then based on the community values, the route will be propagated to that geographical scope.Also, if you notice here in this diagram, there is a cloud at the top representing peering with ISPsor other customer with public.With such design.The question is will also announce.External announce your IPS to external network.The simple answer is no, simply because does not support transit routing in which it does not act asa transit BGP as like, for example, a typical behavior of internet service providers ISP behavior.They can act as a transit as for example, Tier one or Tier two, etcetera.But here with AWS, it's not an ISP.So you get connectivity to AWS and you have access to AWS and Amazon resources, public IP ranges,but not external network, including other customers.Unless you want to reach, for example, another customer, public IP assigned, for example, to theirown EC2 instances or load balancers in their VPC.But this is not transitive routing because these resources within AWS global infrastructure are justthe IP addresses are public, are reachable through public Vif.Now let's look at the other side of the coin.Now what if, for example, you want to filter the IP ranges based on the geographical scope so youwant to filter only accept only route from local region or continent or global?Technically, as you can see in this diagram, assigned BGP community values or tags that you can useas part of your filtering based on the required geographical scope that you want to accept and use.This makes it easier for filtering because you don't need to look into the specific IP ranges, andthese IP ranges can change by time.Instead, you just perform the filtering based on BGP community tags as part of your BGP configurations.In addition to that AWS assign BGP community called no export.This is if you have experience with BGP.I'm pretty sure you know, what does this do?So in a brief this value tells the receiving BGP number, in this case the customer BGP to to not re-advertisethis route to another BGP number, another BGP domain.This is a mechanism to avoid having public ranges to be re-advertised to outside of the customer BGProuting or BGP as domain, even though technically ISPs will not accept advertisement of public IP ifit's not owned by you by now, you should be at a very good level of understanding how the routing occursover there.Connect with public Vif in which you can design and be able to validate designs with public virtualinterface easily, which is the ultimate goal of such discussion.