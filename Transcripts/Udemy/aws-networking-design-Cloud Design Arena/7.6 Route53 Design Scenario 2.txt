# 7.6 Route53 Design Scenario 2

Now we will discuss another example as part of this Route 53 Advanced routing Policies.In the previous example, we discussed how geolocation can be used with failover routing policy to achievecertain requirement.In this example, we will analyze a bit more complex scenario where latency based routing is used withweighted routing policy.In this design, let's assume the solution consists of multiple regions.For simplicity, we will take or focus on two regions and the DNS request, and this design or solutionwill be routed to EC2 instances, not not to ELB.So here in this sample example, we have two regions each with multiple EC2 instances.The goal is to route the client DNS requests to closest region and then at that region level, the routingto the EC2 instances is going to be weighted based on preset ratio.Let's look at the design in a bit more detail.So as mentioned earlier, in this scenario, the design will operate in two layers.First layer is based on latency based routing decision, which is typically based on the user locationand the closest region in terms of latency measurement, as we discussed previously, this with theDNS subnet extension, support by the client or resolver can provide more accurate location about theuser.Otherwise it will be based on the resolver, IP address or location in terms of geography.And the second layer is.Weighted based routing.So once the region is selected now the request will look into the resources in that region or VPC dependson the configuration and then will perform weighted based routing based on preset ratio.We will analyze it in more details, but it's important let to look at it first in terms of the highlevel logic and flow.So now we have two layers to take into consideration.Like in the previous example, the deployment, typically it's bottom up.So and the key difference here, for example, is that you need to configure the health checks for eachinstance because it's not ELB, it's not record with, for example, AWS resource in which it's a checkbox.You enable the health check, you need to create them manually and you set the required policies, etcetera,to report it, whether it's healthy or unhealthy.And then you create two record for the same name.As you can see here, it's name like us to example.com, and you set the routing type as weighted andtypically you set the desired weight for the routing for each record.And this name will be used as a resource for the higher routing policy.Which is the going to be type of latency based routing and your replicate the same concept for the otherregion.And if you notice the latency records, they have the same name while the region level record, theyare region independent.So if we look at the logic how it works, this will make it easier.So in this scenario, let's assume a user in Germany.Trying to access the domain example.com.Route 53 Based on the latency issue, draw this request to London region EU S2.However, as discussed before with latency based routing is not 100% guaranteed.The closest always will be the lowest in terms of latency.It depends on the latency measurement over the internet at that moment.But logically between Europe and Asia Pacific, it should be almost always the closest is somethingin Europe.Okay, so what will happen in terms of DNS routing decisions first?Route 53 Select the latency Alias record for the EU S2 region.So second, because this resource has underneath two two records typically and they are configured weightedand they have different weights.Okay.And because there is a health check evaluation so that typically the record will check the health ofthese instances.So if it's healthy, okay, based on the weighted, it will be handled here.However, let's assume this instance is not healthy, reported, not healthy.So what will happen now?It will try the other record.Okay.Because it has less weight.So now we have still another option and then it can handle it in terms of requests.Let's assume now even this one is not healthy.Now, in this case, what will happen now?It will go and try the other latency record, which is in different region and then it will use thesame logic.And let's assume now this instance is still healthy, so the traffic is handled and the request is resolvedand the IP address of that instance will be shared with the client.So this is another example how you can have tiered type of design of policies, the different requirement,different policies we've seen like we had geolocation plus failover.Now we discussed the latency plus weighted so you can do a lot of mixes.The thing is not about doing something advanced, complex and fancy.It's more about business requirements.So what type of resiliency that you need to provide, What are the different requirements in terms ofapplication regularity, whether it's language, whether it's geolocation?ET cetera.So all of that need to be first identified.And again, this could be part of your resiliency.And there are options in the cloud, whether it's warm, standby, active, active, active, standby.So by analyzing all of these different requirements, what is the strategy you are trying to take toachieve that requirement?And then you can design the DNS routing policy based on that to be considered technically the firstlayer of handling such request resiliency and for example, could be load balancing because as you know,once the request has handed to, for example, a load balancer, load balancer has its own logic toperform also resiliency type of traffic crowding.Because as you know, with the load balancer, you can also have cross zone load balancing and you canhave back end instances distributed across different availability zones and you have autoscaling andall of that.So now you will have it in layers from the request level all the way to the instance and traffic handlingand performance, etcetera.