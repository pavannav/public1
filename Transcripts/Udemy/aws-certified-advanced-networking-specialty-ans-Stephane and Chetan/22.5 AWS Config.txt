# 22.5 AWS Config

Hello and welcome to this lecture,AWS Config.So we already saw a couple of AWS managementand governance services including AWS cloud formationand AWS service catalog.Now the purpose of cloud formation and service catalogis to provision the AWS resources,that means the infrastructure for your solution.And then once that is done,probably you don't need to then lookat AWS cloud formation again.Now, what if you have a lot of resourcescreated either through AWS cloud formation automaticallyor manually created resources are there in your AWS accountand you want to know exact state of your resources,and then what changes have been madeto these resources over the time?Now this is typically required for,again, maybe for audit purposeor something went bad with your AWS account,somebody deleted somethingand you want to find out the root causeor probably who did that from where the API call was made.So if you really look at this problem,there are multiple services involved.First of all you should know exactlywhich resources were deleted and when,and then who deleted that resource.So if you know already, AWS cloud is really the servicewhich logs every API call that you make, right?But then there should be a servicewhich really looks at all these thingsand then can give you a bit of viewof what happened to a particular resource over the time.And that's what exactly AWS conflict does.So AWS config is all about tracking the state changeof your AWS resources.Just an example here, you have VPCand a lot of components provision in your AWS account.Like VPCs, NAT gateways, EC2 machine, databases,security group, flow logs, route tables,a lot of things, right?And what you can do then is you can enablethe AWS config for these resources.And once you enable the config, then config would make surethat it is tracking the changes happeningto these resources.And you can enable and disable the trackingfor individual resources or you can just enable itfor an entire AWS account.So for example, there is a user now who goesand changes something into your security group,maybe user adds port 80 in your security group.Now your security policy says that port 80 should neverbe opened, right?Now, how do you detect that?So AWS conflict can also help you detectthis kind of deviation from your standard practices.So you could have a desired configuration.In AWS conflict terms it is called rule.So rule will check, are there any security groupwhich has a port 80 open?And this rule will be run across all the security groupsin your account.And if there are no security groupwhich has this kind of port open,it will say that your configurations look okay,but in case it finds some security groupswhich are deviating from the standard configuration,then it'll raise an alert.Now this alert can then further be seen through SNSor it can be fetched to an API call for AWS config,can be shown in the dashboard,it can go as a cloud watch event.And then cloud watch even can thenfurther trigger further action,the chain of lambdas or this event can also be storedin S3 bucket, and then it can be evaluated further.So what you do with that event is a different thing,but ultimately AWS conflict can runthose kind of desired configuration ruleson your existing resource stateand then it'll tell you if there is any problem with that.So in short, AWS config does track the changesfor configuration happening to the infrastructure.And when to apply this rule, that means when to runthis rule can be trigger on some actionor it could be periodic.You may want to have like every six,I'll say, run this kind of rules so that it'll scanall my AWS resources and tell me whether the rulesare compliant or not.And there are already 280 plus predefined rules out there.So AWS is coming up with managed rulesso that you don't have to reinvent the wheeland rewrite the rules.These are some standard predefine rules.You can either use thator you can also write your own custom rules.And for that, you have to write a lambda function.So that's what you can do with AWS config.So I hope the overview of AWS config is clear,why it is used and what it exactly does.Now, there is one more thingto AWS conflict, that is auto remediationor manual remediation.What it means that if any rule fails,you could also trigger an action.Typically it is done through AWS systems manager, right?So you can write SSM documents.SSM documents are systems manager document.That means it's kind of script that you can write.Like for Linux based machine,it could be a shell script,for a Windows based machine,it could be a power shell script.But ultimately, when some rule fail,you can trigger that auto remediationand then this SSM document will go and fix that problem.Now, SSM document can do anything.You can make an API call through an SSM documentwhich then ultimately fix that security groupif somebody accidentally opens port 80or if there is an EC2 instancewhich is using an AMI which is not approved AMI,then you could write an actionlike delete that EC2 instance all together.So all that can be done using auto remediationor manual remediation in AWS config.So if you look at any compliance of a resource over time,AWS config will give you that view.Like at certain day, the resource was compliantbut then it became non-compliantand then again it became compliantbecause some rule was triggeredand then auto remediation was triggered.Similarly, you can view the configurationof resource over the time.So you can really see at what time,what changed and who did that change.All that can be capturedinto the AWS conflict dashboard itself.So this is a screenshot for AWS security group changes.Likewise, you can monitor all your resources.Now you can also see AWS CloudTrail APIwhich made that change so that you can traceon who was the user who made that API call.So in summary, AWS config is a service which helps yourecord the configuration and changes over the time.And remember that AWS configdoes not prevent actions from happening.That means it doesn't say that do not allow adding port 80to the security group,it can't do that because it's a reactive action, right?After somebody adds that rule,then AWS config rule can be triggeredor it can be set in a periodic manner.And then it'll detect that your security groupis deviating from the defined rule.So config can't really help prevent actions.Now, the questions that can be solved by AWS config,here are a few examples.Is there unrestricted access to my security group?Are my buckets public?Or has my ALB configuration changed over time?If these are the questions that you want to answer,then AWS config should be used.You can also write custom rules that we have seen.And for that, you would have to define the lambda functions.Like for example, evaluate if each EBS discis of type gp2 or evaluate if EC2 instance is t2micro,if instance is not t2micro maybeor to terminate that instance.Now finally, AWS conflict is a regional service.That means it is enabled for the resourcesin a given region, but then all the data it collectsaround the state of those resources can be aggregatedacross multiple region and multiple accounts.And that becomes inventoryof your AWS resources state basically.So it's kind of CMDB you can call.If you heard that term,configuration management database of your infrastructure.So that's it about AWS conflict service.And this is again, a good tool to experiment with,it's more aligned to the DevOps again,but important if you want to really track changes,what's happening into your AWS networking spaceas well, right?If somebody changes some roots into your root table,then you can go back to the AWS conflict serviceand check what happened at what time and who did that.So that's about AWS config.Thanks for watching.