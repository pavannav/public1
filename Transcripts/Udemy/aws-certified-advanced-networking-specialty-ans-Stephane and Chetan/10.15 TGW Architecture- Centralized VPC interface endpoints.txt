# 10.15 TGW Architecture- Centralized VPC interface endpoints

Hello, and welcome to this lecture.Now, in this lecture let's seehow can you use Transit Gatewayto centralize the VPC interface endpoint accessby connecting multiple spoke VPCs to a centralized VPC,and in that VPC you will provisionthe VPC interface endpoint.Now, we have a dedicated sectionwhere we learn about interface endpoints, why they're usedand different architectures.But here, as we are talking about Transit Gateway,it's important to cover this architecturebecause this is also somethingthat can be asked in your exam.So, what are interface endpoints?Just for a quick recap,interface endpoints allows you to connectto AWS services endpoints privately from your VPC.Which means if from instance, A in VPC Aprobably you want to make an API callto EC2 service or say SQS service, KMS,likewise, there are 120 plusAWS services endpoints available.So you don't need outbound internet accessor a NAT gateway in your VPC,but with VPC interface endpointyou can directly connect to these services.Now, the way it works is using private link.So it actually provisions ENI into your subnet.And then through that ENIyou can reach to the AWS service public endpoint.So that's in a nutshell is an interface endpoint.Now, as I said, there is a dedicated topicand a section for interface endpoint.But now look at the architecture here.What if we want to centralize the access?What that means is right now, I have one VPCand I want to connect to three endpoint services, right?As shown in this architecture.So ultimately I would create three VPC endpointsand it will create three NIS into my VPC.Now, this works fine, but what if I have more VPCs?Say I have four VPCs altogether.And if from those VPCs as wellI want to access AWS services endpoint.Then I would've to again, create three endpointsin every VPC.Now, if you see, number of endpoints are increasing andover the time, if you have multiple VPCs and you havea lot of endpoints that needs to be created.As of now, we just have three here, but maybeyou need a private access to maybe 50 endpoint services.Then you just multiply those numberand those many endpoints you need toprovision and manage that a lot, right?So transit gateway can help you solve that problem.What we can do is instead of provisioning this VPC endpointinto every VPC, let's call them spoke VPCs.You provision these endpoints in a centralized VPC, and thenthrough transit gateway, you connect all this spoke VPCto this centralized VPC.And in this casewe just need to create now three VPC endpointsfor three AWS services endpoint,and then all the spoke VPCscan privately access the endpoint services.So transit gateway greatly simplifies this architecture.Now, everything works finehowever, we also need to understand howthe AWS services DNS will be resolved in this case, right?So that's the question.Now, what do I mean by that?Suppose you are running some application on instance A hereand this application wants to connect securely toSQS API Endpoint.Now for that you'll use SDK or CLIas a part of your application, running an instance Aand that CLI or SDK will make an API callwith the DNS name, say SQS dot region namewhich could be ap-south-1.amazonaws.com.Now, you don't have outbound internet connectivityso this DNS will be resolved to a public endpointof SQS endpoint by default, but as there is no NAT gatewaysand there are no internet gatewaysyour traffic will never go out to the SQS serviceand your call will fail.Now, what essentially we want isif instance A makes an API call to SQSthen that DNS resolution should be resolvedto the private IP address of your VPC endpointwhich is available into this centralized VPC.Now, how do you really do that, right?So let's look at the solution here.So ultimately what you want is, in all these spoke VPCsif there is a DNS resolution request goes forsps.ap-south-1.amazonaws.comit should be resolvedto the VPC endpoint, private IP, right?And what really happens when you create a VPC endpointinto a centralized VPC is thatyou can enable the private DNS for that VPC endpoint.So let's take an example.You enable it for say, SQS endpoint.Now what AWS does in this case,it creates one private hosted zonewith the same name as your API endpointwhich is sqs.ap-south-1.amazonaws.comand then it attaches that private hosted zone with this VPC.Now, if from this VPC there is an EC2 instance hereand you try to resolve sqs.ap-south-1.amazonaws.com,it will automatically get resultsto the private IP of your VPC endpoint ENI.And this is what worksbut that works only within this VPC, right?It is not resolvable in the spoke VPCs.So all these VPCs still cannot resolvethis sqs.ap-south-1.amazonaws.comto the private IP of VPC endpoint.So that's the problem.If you want to centralize access to VPC interface endpointsbut there is solution to that as well.What you would have to do in this case isfirst, you disable this private DNS, right?Now, disabling this private DNS does one thing, right?It doesn't automatically create thatprivate hosted zone in the name of SQS dot whatever, right?And then what you can do isyou can create your own private hosted zonewith the same name as your API endpoint.In this case, sqs.apc-south-1.amazonaws.com.And then you create Alias record into your hosted zonewhich says the root domainthat is sqs.ap-south-1.amazonaws.compoints to the VPC endpoint DNS.Now, you get this DNS from VPC endpointas you create, you will get, you know, multiple DNS namesfor the same VPC endpoint.And we'll talk more about that later.But you can manually create these Alias recordsinto these private hosted zone.And then you should also create corresponding hosted zonefor other VPC endpoint services as well.So SQS is just an example.Similarly, you can do it for KMS, EC2and 120 more such services.And ultimately after you create this hosted zoneyou attach these hosted zones to all these spoke VPCs.Now, as soon as you attach this private hosted zonesto the spoke VPCs, now within the spoke VPCsyou can resolve the DNS name for AWS public service.And that means if any application runninginside this spoke VPCs can automatically resolveto the private IP address of your VPC endpoint.Now there are certain important things to knowabout the DNS and VPC interface endpoints.When you create an interface endpointit will provide you two kinds of the DNS.One is original DNS entry, and one is easy level DNS entry.Now, while these two entries, because, many a timeswhat you would want is the trafficfrom a particular AZ into your spoke VPCsonly goes to the VPC endpointin that particular AZ in the target VPC.When I say target, it's a centralized VPCso that you don't have the inter AZ data transfer charges.Now, what you can do definitely is you can use thenAZ specific DNS name into your route 53 hosted zoneand you can then manage it at the application levelwhich DNS names you want to resolve to, right?Now, if you look at the regional DNS endpoint,it ultimately resolves to this easy level DNS endpoint.So this is an example here.After I created VPC endpoint, it gave methis regional DNS name for that endpoint.And if I do NS look up to that, it will give metwo DNS records.And it results to the private IP addressfrom corresponding availability zone in whichthe endpoint has been created.Now, again, as I said, in order to save someinter-AZ transfer cost into the spoke VPCsyou could use this easy, specific DNS nameinstead of region specific DNS name.So if you just try to resolve the easy specific DNS namefor the input it will results toone single private IP address.And this choice has to be made by the client, right?So that's about the VPC interface endpoint.You now understand how you can use AWS transit gatewayto have centralized access to these endpoints.And more importantly, you should know how to resolvethe DNS names for AWS servicesusing the privacy hosted zone.Okay now let's also apply the cost lensto the same architecture.Now, let's go back to the original architecturewhere we had three VPC interface endpoints into each VPC.Now, VPC interface endpoint comes with the costand there are two types of the cost associated.One, per hour charged for the VPC interface endpointand then how much data flows to that VPC interface endpoint.Now, if you have multiple VPCyou will just multiply this cost with multiple VPCs.So it's a lot of cost again.Okay now, if you do the same thing using transit gatewaythat doesn't really mean that you are saving a lot of money.Though you might just see three VPC interface endpointsinto the centralized VPC.Of course we'll pay for that.But then the connectivity from the VPCto the transit gateway doesn't come free, right?There is additional charge per attachment per VPCas well as there is a data processing chargeas it flows through your transit gateway attachment.And this cost is much higherthan the VPC interface endpoint cost.So if you look at the cost hereyou'll be paying for transit gateway attachmentas well as data processing charge at every attachment.And on top of it you will pay for the VPC interface endpointwhich are provision inside your centralized VPC.So, in order to really look at the cost clearlyyou should consider all these factors.You should consider how many attachments are there,how much data is getting processedand then ultimately should come to the decision.So it's not in black and whitethat this is cheaper, and this is costly.You should really take that decisionbased on your architecture.Okay, now, one more architecturethat I wanted to discuss here isinstead of using transit gatewaysyou could also use VPC peering connectionsbetween spoke VPCs and the centralized VPC.Now why this, right? When transit gateway is there?Now, one of the obvious reason is cost, again.You saw that transit gateway comes with a lot of costfor the attachment, as well as for data processing charges.And in VPC pairing case, there is no such charge.VPC pairing comes for free.That means if you have limited number of VPCsyou can only have up to 10 VPCs connectedto the centralized VPC.You need to check the current limit thoughbut it was 10 earlier.Then there is a limitation, right?You can't connect more than that to the centralized VPC.And if you look at this architecture, this very well worksand you would have to do the exact same thing that you didfor the DNS solution.That means you would've to create a private hosted zoneand then associate that with all the spoke VPCs.Also you need to disable the private DNSfor the VPC endpoint so that AWS doesn'tautomatically create AWS service, private hosted zoneand associates that with this centralized VPC.So, very well both the architecture will workbut if you need more flexibility and there are a lot of VPCswhich goes beyond the VPC pairing limitationthen always go for the transit gateway.So that's it for this lecture.And I hope you understand how the centralized accessto the VPC interface endpoint can be managedusing the transit gatewaysand you know the pros and cons of the architecturesthat we discussed today.This is interesting and you might find some questionsaround this into your exam,so make sure you understand these concepts well.Thanks for watching and stay tuned.