# 5.5 Hands On- VPC DNS with Route53 Private Hosted zone

Hi.So in this exercise, let's try to see how the private DNS works for the VPC using route 53 privatehosted zone.So our setup will look something like this where we have VPC and then corresponding subnets.And in that we need two EC2 instances one as app server and one as the DB server.Now we want to use Amazon provided DNS server.So we are not going to change anything in the Dhcp option set as a name server.But because we want our domain name to be core dot internal, then we will create a new option set wherewe'll just change this domain name.And then we will associate this Dhcp option set to the VPC.And to be honest, changing this domain name to CORBA internal in the Dhcp option set is optional andI will talk about that while we implement it, but that's what the Dhcp option set will look like.And then the most important part here is to create the private hosted zone with the same name as ourdomain name that is corp dot internal.And then we'll create corresponding record sets in that with the app and db server pointing to correspondingEC2 instances.And then from this app server, we will try to reach to the DB server using its DNS name.And then the DNS resolution will happen using this flow.Right.So that's what we are going to implement.I have also listed all these steps here.So you can also just look at these steps and try to implement on your own.Now to save some time, I have already created the VPC and created a public and private subnet.But let's now create the next step that is Dhcp option set and then launch this EC2 instances and thencreate the Route53 private hosted zone.So let's do that now.So you can see I have a VPC.And then it has two subnets as of now public and private.And that's it right.So now the next thing that we want to do is we want to create a Dhcp option set.So this is a default one.What we really need is this server because we want to keep the same server as Amazon provided DNS.So let's create a new Dhcp option set.And let's call it say corp dot internal because that's the name that we want to use.And same here.Now the most important part here is use this string because it points to route 53 resolver and restof the setting.You can just keep as it is and that's it.Right.But you have to also associate this with your VPC.So let's go to the VPC and just modify this to the corp dot internal.And that's it.Let's save it.So we are done with the VPC settings.And next we want to go to the route 53.And if you go to the route 53 there are hosted zones.So we want to create a new private hosted zone.So let's say create a hosted zone with the same name as Corp Dot internal.And it's a private one.So you have to associate that with your VPC in the Mumbai region.So this is my VPC and let's create that.Now we'll again come back here when we launch EC2 instances.Because for creating these records here we will need private IP addresses of the EC2 instances.So let's go here into the EC2.And let's launch our public EC2 instance.Let's call it app instance.And I'm going with all the defaults, just that in the networking, make sure that you use your VPC,and I want to launch it in a public subnet.And I want to have a public IP because I'll be connecting to this instance over SSH to test the DNSresolution.Now with respect to the security group, you can create a new security group.And you should allow the SSH from say my IP.And also you should have all ICMP IPv4 from the VPC, Cidr itself.That is ten .0.0.0 slash 16.That's it.And let's launch the instance.Now.Similarly, we also want to launch another instance but in a private subnet.So let's launch that instance.Let's call it a DB instance.And.Let's edit the VPC.And in this case we want to launch it in a private subnet.And we don't want a public IP.However security group we can use the existing one that we just created.And that's it.Let's launch the instance.Okay, so two instances are there app and db.They will come up soon.And rest of the things we are already done with.Now last thing we want to do is to use their private IP address to create the record sets in the route53 hosted zone.So this one is for the app.So let's go here.Let's create a record and let's say app dot Corp dot internal points to this particular private IP.And similarly we need to do that for DB instance.So let's go here pick its private IP.Let's go here.Let's create a record say DB dot corp dot internal points to this IP.Right?As simple as that.Now let's go to our instances and pick the public IP of this instance and let's SSH into it.So this is my Mumbai session with the Mumbai key and I'm putting the public IP.And I'm just logging in here.Done.Let's also look at a few settings like cat, etc. Resolvconf and you will see that the search domainis coreboot internal here, and the name server is ten .0.0.2, which means if I do now ping dot db,dot core, dot internal, it should go to my database server.You can see very well it is doing the same.So this is now being resolved using the route 53 private hosted zone.Now also as my search domain is core dot internal, I can also do ping db and it still resolves becauseit is looking at this particular domain name.Now that's the thing where I said it's optional to configure this into the Dhcp option set.If you don't change this into the Dhcp option set then it will be AP South one dot compute dot internal.And then if you say ping db it will ping to db dot AP south one dot compute dot internal.And it won't then resolve because in route 53 hosted zone you don't have that domain name.But now because we added core dot internal in the Dhcp option set, you don't even have to define thefully qualified domain name here.Just DB works.Right.So this is just another thing that you need to understand.Right.So that's what our exercise was and it was pretty simple.Now if you are also doing this exercise, please don't forget to terminate your instances because youmay be charged for that.So thanks for watching.And in the next lecture we are going to set up our own custom DNS server and let's see how that works.