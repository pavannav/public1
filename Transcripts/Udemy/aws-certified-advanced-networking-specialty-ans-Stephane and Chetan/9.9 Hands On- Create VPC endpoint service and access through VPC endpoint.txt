# 9.9 Hands On- Create VPC endpoint service and access through VPC endpoint

Okay.So now let's do this exercise for VPC endpoint service.And this is relatively long exercise.But hopefully it gives you an entire view of how VPC endpoint service works and VPC endpoints work.So as we discussed in the previous lecture, in order to create the VPC endpoint service, the firstthing that you need is a provider VPC.And in that you will create a network load balancer with right availability zones.And then you will have your actual services running on the EC2 instances or say ECS or on ECS that youwill register to the network load balancer.And now this network load balancer will be set up as a VPC endpoint service.And as you create this service, you will have many options.For example, whether you want to allow access to this service from other AWS region, whether you needthe required acceptance so that the VPC endpoint created in the consumer VPC can connect to your serviceor not.Similarly, you can enable the private DNS so that you can have custom domain name to access your services.Then support for IPv4 or IPv6 or both.And also to allow access to the other AWS accounts.So all these settings will be there.So if you do that you will create the VPC endpoint service in the service provider VPC.And then in the service consumer VPC, you would have to create the VPC endpoint for this particularservice right.So typically this is how the architecture should look like.Where in service provider VPC you have the network load balancer.And there will be say a dummy EC2 instances on which you will run some services.And those will be exposed through this network load balancer.Then you will create the VPC endpoint service.And then in the consumer VPC you will create the VPC endpoints across this two availability zones forhigh availability.And there will be a client through which you will test this connectivity.Now further in order to connect to this client, typically you will have bastion host so that you canlog in over ssh.And then from here you can log in to this.But as you understood in the previous lectures, we are now getting away with the bastion host setupbecause AWS provides easier way to directly connect to this EC2 instance which is in a private subnet.So for that we use EC2 instance connect endpoint.And additionally we can just refine this architecture and probably have more subnets such that we havededicated subnet for VPC endpoints.This is how you should actually do it in production.So on the left side as you see in the consumer VPC, I have two subnets dedicated for VPC endpoint,one subnet for the client and one private subnet for EC2 instance connect endpoint.And out of this I think we already had this three subnets created in the previous exercise.So I'll just reuse the same VPC.But additionally I would have to create this fourth subnet in second availability zone.Now we don't have anything in the service provider VPC.Let's call it VPC b in that we'll need this for subnets, two for network load balancer and two forthese EC2 instances on which we host our service.Now one interesting thing that you will note in this setup is that there is no internet gateway.All subnets are private, but still we are able to do this exercise.And for the same reason, there is one small challenge.Right now we are saying that this EC2 instance should have a web server, but you know that when welaunch EC2 instance and install httpd service, it needs to be installed or downloaded from the internet,right?So if we do that, this EC2 can't really download anything from the internet.Rather we can't even reach to this EC2 instance from outside, right?Unless we also create the EC2 instance connect endpoint.So how this EC2 instance will have web server.Now for that typically you will have an Ami Amazon machine image which would already have the web serverrunning, and then you will just launch EC2 instance using that Ami.So in order to have this web server, first thing let's create an Ami which is already having the webserver.And then for actual setup we will use those Amis.Right.So let's do that as a first step.And first let's set up the right side that is VPC b.And then we will come to the VPC a right.And I have also listed all the steps here.So you can do all these steps on your own by reading this.And with that first thing as I said let's create EC2 Ami right.And for that you have to use the user data so that it is easier to create the Ami.And this EC2 instance, which first we launch will be in a public subnet.And normally C2 instance will create an Ami.And then we will go for creating the VPC, b and launch those EC2 instances using this particular Ami.Right.So let's do this and let's follow the steps now.Okay.So let's launch the EC2 instance.And this is a test EC2 instance.We just want that for creating the Ami.And I will launch it in a default VPC so that I can connect over the internet.So all the settings as it is just that.We also want to test the HTTP traffic so that we know whether web server is running fine or not.And in the advanced details, we want to have basically a user data and you can copy it from the slides.But what essentially it does it install the httpd service and then just adds this index dot HTML file.Right.So let's launch the instance and let's give it one minute and then I'll come back here okay.So instance is running.So let's pick its public IP and verify that the web server is running.And you can see that it is running here.Right.So next what we want to do is just we want to create Ami out of this.So let's go to the images and create image and let's give it a name.Say VPC endpoint exercise am I.Web server am I.Right.And you don't want to reboot the instance.That's okay.And let's create the image.So if we go to the Amis you will see this Ami being created.So right now the state is pending.So let me probably pause this video for a couple of more minutes and I'll come back here.Okay.So it took around two minutes for Ami to be available.So now we can just go and terminate this instance because this is not required.So let's terminate that okay.So now we have the Ami.And if we see the architecture we need to have now target VPC or service provider VPC and these foursubnets.And then we will create these two web server and the network load balancer.Right.So let's do that now.Okay.So let's go to the VPCs.So there is this consumer VPC already.So let's create a service provider VPC.And let's give it a name say VPC b for easy recognition and Cidr you can give same as VPC A becauseVPC endpoint works even though the VPCs have overlapping Cidr.But just to make it a little simple and identifiable, let's use 10.20, right?Rest of the settings as it is, and let's create the VPC.Okay.Now let's create the subnets for VPC b.So let's select vpc b and we need to create four subnets.So let's call it vpc b.So first one is for NLB.So let's call it NLB private one.And it's in first AZ and the Sidr.Is this right.Again.Let's add one more subnet.Let's call it private two for NLP.And this time let's select AC two and the cider range as 1.0 slash 24.Let's add one more.And this is for my EC2 instance.So let's call it EC2 private one.And let's call it 10.2.2.0 slash 24.And the fourth one.So this is for EC2 private two in second AC.And let's call it 10.2.3.0 slash 24 okay let's create the subnets.So subnets have been created.Now all these subnets would follow the main route table of VPC.But for consistency let's create a separate route table which is the private route table.Nothing special about it, but let's create that.So this is private route table in VPC B, let's create the route table.And the most important thing is to associate that with all these four subnets.As I said, even if you don't do this it should still be okay.Okay so we have four subnets now.And now let's uh, launch EC2 instances in those two subnets in VPC B.So this was a test instance that we had terminated.And we had created that for Ami.Okay.So now let's create the first web server in the VPC B.So let's call it web server one.And important thing is the Ami because we want to launch it from the custom Ami that we created.So it lists all the Amis.So I'm selecting the Ami that we created and then key pair.And the most important is the networking.We want to have that in VPC B, and it should be in the EC2 private one subnet.And then let's create a new security group for this.And we just want HTTP traffic basically nothing else.So HTTP from anywhere is okay.Or we can restrict it to vpc, Cidr because EC2 instance will get only traffic from the Nlbs.Right.So let's leave as it is.And that's it I think.Let's launch the instance.And now similarly let's launch Web server two in another AC.So same steps just that we need to use another subnet.And here also we need to use our own Ami.So make sure that you select the right Ami key pair.Then the networking VPC IP And uh, subnet will be EC2 private two.And then we will choose the existing web server as G because it will be common.And that's it right.So let's launch this instance as well.Okay.So if everything works fine we should have two EC2 instances Web server one and Web Server two.And now the next thing let's create the network load balancer and then attach these two instances.So for that let's go to the load balancer and say create.And the middle option is network load balancer.So let's create that and let's give it a name say VPC endpoint exercise NLB.And it should be internal.Because we are not exposing it to the internet.We are going to expose it using the VPC endpoint service.Now IP address IPv4 is okay but otherwise it supports IPv6 as well.And we want that VPC B across both AZS.So this is for the first stage.We need to select the NLB private subnet.Similarly for another AZ.It should be NLB private two.Now with respect to the security group earlier NLB did not support that.But now it does support that.And by default it will be default security group.But we are going to use this NLB for the private link.And in that case these security groups are not really used.Right.So either we can keep it default or rather I would suggest we remove that.Okay.Next we want to have a listener on port 80.That is a TCP listener because it's NLB only supports the TCP, UDP and TLS.So that is there.And we want to send all the traffic to the EC2 instances.And for that we need to create a target group.So let's create a target group for NLB.And target is instance target group name is say TX and protocol is TCP.And on port 80 of the instances IPv4 traffic and it should be in the VPC.B I think rest of the settings as it is.And now let's include both these instances into the target group.And let's create the target group okay.Once we are there let's go back here.Refresh it.And we should see this target group.And additionally I don't think so.We need to do anything else.Let's create this network load balancer okay.So it will take maybe a couple of minutes for this NLB to be available.So I'm pausing this video okay.You can see that NLB is active now and I can't access it from the internet.Otherwise we could have tested it.But so NLB is there.And one interesting thing that you see here, uh, so we talked about AZ mapping.Right.So AZ name AP South one A and then there is acid right now if you see for AZ a the ID is this.But for AZ b id is These apps.One is three.So that means that name and acids do not directly map in different AWS account.It can be different mapping.Okay.So this is done.So coming back to the architecture we are done with most of the settings on the right side.Now important thing is to create the VPC endpoint service using this network load balancer.So let's do that.So for that let's go to the VPC console again.And here there are endpoint services.So let's create the endpoint service and let's call it demo endpoint service.And the load balancer type is network load balancer.And it shows up your network load balancer.Now further this is a new option now where you can allow different regions to connect to your endpointservice.Otherwise VPC endpoint was only a regional thing.But now it can be accessed access across the region, right?So for us, the consumer VPC is in the same region, but if it is in a different region then you wouldhave to allow that here right.So remember that.So I'm not choosing as of now then this is the load balancer that it is sending traffic to.And here we say acceptance required because we are creating the service.So we want that anybody tries to connect to our service we should approve that request right.So that's where we are keeping it.Acceptance required.If you turn off this it will be directly connected which is not that secure.Now enable private DNS.As of now I'm not using that.But if you have time, we will also see this as a part two of this exercise.Now supported IP address type.As of now it's IPv4 only.So let's go with that and let's create the service.Now if you see this service has been created and there are details as in which availability zone theservice is available.And here you can also allow other AWS accounts to connect to your service, right?So you would have to whitelist basically those accounts, all that you can do here.And from endpoint Connection you can monitor which all consumer VPCs are connected to this endpointservice.Right.So all those kind of things are here in this dashboard.So I think we are done with the right side of our setup.And now let's move to the left side of the setup.So now in case of VPC A uh we already had this three subnets, if you remember just that there shouldbe this additional fourth subnet so that we can have the VPC endpoint created in this subnet.So let's do that as a first thing.Then we will launch this EC2 instance.And then we will create the VPC endpoint for the VPC endpoint service that we just created.So let's do that.Okay so let's go to the VPCs.And there should be this VPC A and B.And let's go to the subnet and let's filter it by VPC a.So there are only these three subnets.So now let's create one more subnet.So that will be for VPC endpoint basically another subnet.So this idea is two.So let's use the next one of this.So let's create the subnet for VPC a and let's call it say vpc b.VPC endpoint subnet two something.And it should be in ACB.And Cidr should be 3.0.Okay let's create the subnet.And again it will be by default linked to the main route table.But it's better if we associate the specific route table.So let's do that as well.And let's select all those.Sorry This is for VPC b.This one is for the VPC a.Let's edit that and also add this subnet.Now in this entire exercise we are not playing around with the route tables.It's not required.So it doesn't matter whether you create a specific route table or not.But this is for consistency.Okay.So we have the fourth subnet as well.Now let's go to the EC2 instances.And uh let's create the client EC2 instance basically.So let's launch the instance.So let's call it client EC2 instance from Amazon Linux.And the key pair and the network setting.We want it in VPC A and we have specific subnet for this EC2 private subnet.We don't want public IP and we want to create a new SG.Now here we need to connect it over SSH over the EC2 instance connect.So let's have just ssh.Anywhere that should be good enough.And let's launch the instance.Okay.So this is also done.Um, and let's test the connectivity to this instance as well as it comes up.So let me pause this video for maybe a minute and I'll come back here okay.So instance is running.Now let's connect to this instance using EC2 instance connect endpoint that we already have.And let's connect now once it is connected we will just try to say ping google.com and it won't workbecause there is no outbound connectivity in this instance cannot connect to the NLB that we createdbecause these are isolated VPCs right now and everything is private.Okay, so that's done.Now the next important thing is to create the VPC endpoint for the service, right.Which will create this connectivity between these two VPCs.So let's go to the end points.And sorry.Before that we need to have the endpoint service name that we had created.So this endpoint service and there is this service name.So we want that for creating the endpoint.Okay.So let's create the endpoint and let's call it say VPC endpoint service exercise endpoint.I mean this little confusing but this is what we are doing.And this time we want to create it for one particular AWS service right.So endpoint services that use NLB or Gateway Load Balancer and the service name is the one that we hadcreated.Let's verify that.And it says that it is verified now.Do we want to enable the Cross-region endpoint?No.At this point, because both VPCs are in the same AWS region.Now we want this endpoint to be created in VPC A, and optionally we want to enable the DNS name.In that case as you know, it will create the private hosted zone and it will attach it to your VPC.So as of now, we don't want that.But as I said, we can also try to do an exercise where we use a custom domain name for accessing theVPC endpoint service.Okay.So DNS record type IPv4.And then we want this to be created in both the AZS and we had AZS specific subnet for that.So this subnet one and this is subnet two right.That's it I think everything is okay.And here the security group we need to create the security group because it should allow the inboundtraffic from the EC2 instance.So let's create a security group and then filter it again here.Okay.So rather let's create another tab with the VPCs.And let's go to the security group.And let's create a new security group and let's call it VPC endpoint SG in the VPC a and we just wantinbound rule and we want to allow HTTP traffic from anywhere is okay.Otherwise from the EC2 instance or VPC, Cidr is also okay.Let's create that.And let's go back here and refresh this.So we have this VPC endpoint security group.That's it.Let's create the endpoint.Okay.So first thing it says that the pending acceptance because this is happening in the consumer VPC weactually need to go to the VPC or the provider AWS account and approve this request.Right now because we are using the same AWS account, we will see this pending request, uh, in theendpoint services uh itself.Right.So here if you go to the endpoint connection it shows that it is pending for acceptance.So let's go here and accept this request.Okay.So it will take maybe a minute or so for this to happen.So I am just pausing this video.Okay.So this is now available.And if you go to the endpoint you will see that this endpoint has been created.And there are different DNS names.And that's what we learned in the previous lectures as well.There is one DNS and then there are DNS as per the AZS.So because we created it in two AZS there is one for one B and this is for one a right.And we can access this VPC endpoint service using any of these DNS.So let's pick the regional one rather right.And now if everything is okay we should be access the endpoint services using this particular DNS.So going back to our architecture I think we created everything that you see here in this architecture.So from this client EC2 instance, now we should be able to access this VPC endpoint service using theendpoint specific DNS.So let's do that okay.So here what I just need to do is say wget http and the endpoint DNS.So let's paste it.Sorry there are some extra character.And you see that it got the index.html.Or you can do rather curl right.That would be better.And let's copy this.And you see that this service is for VPC endpoint demo.This is coming from the right side of the VPC and it is working as expected.Additionally, if you do say Nslookup for this endpoint DNS, then it should also give you the privateIP address from the consumer VPC, right?So it is 10.10.3.143.So this is right now coming from the fourth subnet that we created.And sometimes you may get a second subnet because it's a regional DNS.It will provide the IP addresses in round robin fashion.So what I mean by that this IP could be from this Eni or this Eni.But if you use zonal specific DNS then it will always resolve to a specific Eni.Right?So I hope it is clear now how this entire thing works with respect to the VPC endpoint, service andendpoints.And we are not really done with the entire exercise because I have one small exercise where I will showyou how to use private DNS name or say custom DNS name for your endpoint service.For example, if in the client VPC you want to access the services using, say app dot example.com,then how to do that?Now for that you need to own the domain name.So if you have it then you can only do that exercise.But if you don't have that then I would suggest that you delete everything that you created.In this exercise, which includes three EC2 instances, there is a network load balancer.There is a VPC endpoint service VPC endpoint and everything has a cost.So better delete that if you are not doing the next exercise.But in my case, because I have the domain name with myself in route 53, I will continue with thissetup and extend it for the custom domain name.Right?So with that, we'll stop for this exercise and let's go to the next exercise.Thank you.