# 19.25 Hands On- Route53 Resolvers - Part 1 - Setting up VPN

Hi.Now, this is one of the most interesting exercises in this entire course because in this exercise weare going to set up a lot of things.And that's where this exercise is divided into three parts.And I encourage you to do this exercise on your own because if you could do it, I sincerely think thatyou understand how AWS networking works.Okay.So in this exercise we want to try and set up the hybrid DNS.Now when we say hybrid, which means DNS can be resolved on AWS side or DNS can also be resolved onthe on premises side.So in this exercise we are going to do both.So our lab architecture will look something like this.Now I know that this looks a little complex but let's try to simplify that.So on the left side we will have the cloud DNS with the name Cloud.com.And on the right side we have on prem.com as a domain name.Now what we want to achieve that if any query for the domain name cloud.com or the subdomains.For example, app dot cloud.com should go to AWS route 53 private hosted zone.And if there are any DNS queries for on prem.com domain name, then all should be routed to this DNSserver which is hosted in the simulated on premise network.And how do we do that?Now for that let's first start with the left side.So here as you can see we just have a VPC and a simple server which we will use to test our DNS queryresolution.Now in here because AWS has route 53 private hosted zone, we can just create a private hosted zonewith the name as Cloud.com.And then of course, you know that this app server, EC2 instance or any other instances in the VPCon the left side can directly query the DNS using the route 53 local resolver.Right, so this will work.But what about if the query is coming from the on premises network.Now for that, the first thing we need to have the site to site VPN connection or the direct connectconnection between this VPC and the on premises network.So we will have this site to site VPN connection.And this VPN server here will act as a router to send all the traffic for AWS through this VPN tunnels.Right.So that's the first thing.Second, this app servers on the on premises site, which are nothing but any normal application, basicallywill send the DNS queries to the DNS server, which is there in the on premises site.So now if you want to send all the DNS queries for the Cloud.com to this private hosted zone, the thingthat we need on the AWS side, as we learned in the previous lectures, is the route 53 resolvers,and it will be inbound resolvers.So on the AWS side, we will create inbound resolvers.And then on the on premises side, we need to tell this DNS server that if you receive any DNS queriesfor the cloud.com domain, then forward it to this IP addresses of this inbound resolvers.Right.So any application server there on the on premises, if it wants to hit app dot Cloud.com that DNS querywill be first sent to the DNS server.And from there it will go to the inbound resolver endpoint.And from there it will go to the private hosted zone.So that's where all the servers will resolve the cloud.com domain name Rthy.Right.So I hope it is clear.Now the second part, how can you set up and resolve the DNS queries for the on premises?Com.Now for that very first thing we need to have a DNS server on the on premises site.So we will set that up and we will configure it with a domain name as on prem.com.Now again if the DNS query is coming from the local application server on the right side, then theDNS query will be resolved from there itself, right?But if the DNS query is coming from the AWS side of the network, then we need the route 53 outboundresolver endpoints.Right.So for that we will create the outbound resolver endpoints.And in that we will have a conditional forwarder which says if the DNS query is for on prem dccom thensend it to this DNS server which is hosted on the on premises network.Then all this traffic will again go through this VPN connection and will hit the DNS server.And that's how on prem Dccom will be resolved.Right.So that's our entire setup.And that's our goal to achieve bidirectional DNS resolution.And we are going to now implement this.Now at this point I assume that you already know how to create VPCs and subnets and Nat gateways.So in order to save some time I have just pre-created some part of this setup.So for the AWS side of the network I have VPC with Cidr range 20.0.0.0, slash 16 and then public subnetand the private subnets as shown.So I hope you can also do the same.Similarly on the right side of the network I do have similar VPC with 192 Cidr range.And then I have a public subnet.And in that I have also created a Nat gateway.And then I have modified the route table of both the private subnets to send the traffic for the internetthrough the Nat gateway.And this Nat gateway is required because in this one of the subnets, we want to create a VPN server.And we need to download some packages from the internet.And that's where we need this outbound internet connection.Right.So I have already set this up.So let me just show you quickly in the AWS console.And then we will proceed with the next steps.Right.So as you can see I have this on premises VPC and the cloud VPC with the respective Cidr ranges.Then I have the subnets.Nothing fancy about that.And then I have the route tables.Again, these are very normal route tables that we have been creating and using all the time, right.As I said in the private route table for the on premises VPC, I have the Nat gateway entries, so justdon't forget about that okay.Now with that setup let's go to the next step.And in here let's launch these two app server on both the side.And these are very simple server.We don't need to install any software on that.They are there just to test the connectivity and the DNS resolution.So on the AWS side we will launch that in a public subnet.And on the on premises side we will launch that in the private subnet.And further if you look at the security groups.So I want to allow SSH because I can connect to that machine.And also I'm enabling ICMP that is a ping from the on premises network.So let's do that okay.So I am into my Mumbai region.So let me launch the instance and let's call it say cloud.Make sure that you give right name so that you can identify your instances.So Cloud App Server and Amazon Linux 23 Ami that is okay.The key pair that I have and the networking make sure that you select the cloud VPC and the public subnet.So let's select that.And let's also have the public IP so that we can connect to that.Now for the security group let's create a new security group so that we can have the specific rulesfor this instance.And in there I want SSH.And then additionally I want ICMP that is ping.And I want to allow that from 192.168 range to test the connectivity.That's it.And let's launch the instance okay.That's done.Now let's also launch the on premises side of the app server.And in here we just need to again create similar instance.But at this time we need to allow the SSH from this particular subnet or this particular VPC, Cidr,because then we can connect and try the connectivity.And again ICMP from the AWS side of the network.So let's do that.Okay going back to the instances and say launch the instance and let's call it on prem app server.Again make sure that you use Amazon 2023 Ami.Then select the key pair.And on the networking side make sure you select the on premises VPC and the private subnet so you canlaunch it in private subnet two.Then we don't need a public IP.And then we need to create a security group for this as well.So let's use similar name now ssh I want to allow from either anywhere or you can be specific with 192Cidr.So let's do that.And additionally let's allow ICMP, IPv4 and the range should be ten .0.0 so that AWS network can pingto this instance.Okay.That's done.Let's launch the instance.Right now both the instances are launched.And now the most important thing is to set up the site to site VPN.Without that, you cannot really access this private instances across these two networks right now.How to set VPN.We have already done that exercise in the site to site VPN section, and if you remember, you can doit on your own.But if not, let's follow the steps here and let's quickly set up this VPN connection and then testit.Now, one thing about the VPN is that it really depends on the underlying operating system and thenwhich VPN software you are using.So as of today, I am using Amazon Linux 23 as an Ami for this VPN server, and I am using Libreswanas a VPN server.Now, in your case, if you are watching this video in say, 2025.Maybe there is a new army and these settings might not work, so please try to adhere to the same settings.Right.So now let's quickly set up the VPN connection.And for that first thing we need is this VPN server.And for this VPN server we will have the security group with the SSH open from anywhere.So that I can connect to that and configure the VPN server.And further, as you know, this VPN server will also act as a router to send traffic or receive trafficfrom these instances, which are in a private subnet.And that's where in order to send the DNS queries to the cloud or send the ping request to the cloud,we will allow ICMP, IPv4 and UDP traffic on port 53 from this subnets or say entire VPC Cidr range.Right?So now let's create this VPN server and configure that okay.Now back to the EC2 console.And we should already have two servers.Now this is our third server.And let's call it say on prem VPN server and make sure that you use Amazon Linux 2023 Ami and the keypair.Whatever you have in the network, we should have it in the on premises VPC, and it should be in thepublic subnet so that this VPN server can communicate with AWS side of the Virtual Private gateway.Now we need a public IP, and then we need to create a security group for this VPN server.As we learned.And in there I'm allowing SSH.And on top of that we need to have the DNS traffic.So DNS UDP.And this should be from 192 168 range.And also we are going to add ICMP traffic from the same range.Right okay.So everything is done.Now let's launch the instance.Okay.So we should now have three instances.And in order to set up the VPN connection we need the public IP of this VPN server which is this.Right.So we created just the VPN server.But we need to actually set up the VPN connection.And for that on the AWS side we need to create this virtual private gateway.We need to create the customer gateway with the public IP of this VPN server.And then we need to create a site to site VPN connection.So let's do that okay.So for that let's go to the VPC console again.And on the left side if you just go down here the first thing we need to create a virtual private gateway.So let's call it cloud VW and rest of the settings as it is.So this is our VW.And then we need to attach that to the VPC.So it won't allow us until it is fully created, but by that time, let's also create the customer gateway,which is the other side of the VPN network.So let's create the customer gateway and let's call it on prem.GW and the IP address should be the public IP of the VPN server.Right.That's it.Let's create this customer gateway.And let's go back here and see if it is created.Yes.And we need to attach that to our VPC.So let's select the cloud VPC and attach it right.So once it is attached then we can create the VPN connection basically between the virtual private gatewayand the customer gateway.So let's go here and let's create a VPN connection.And let's call it cloud on prem VPN connection.Now from AWS side we need to have the virtual private gateway.And on the customer gateway side we already have it.Now routing is static.We are not using BGP and here we need to.Provide the Cidr range from the on premises network.So let's use this range.And rest of the settings we can leave as it is.And let's create this VPN connection.Aethes okay.Now again it will take maybe few minutes for this VPN connection to.Be up and running.And by that time we can set up our on premises VPN server.So let's select this and download the VPN configurations.Now here the vendor will be open Swan right.So let's just download this and I'm just saving it somewhere.Maybe on the desktop.Let's save it now.Next thing we need to open this configuration file.And we need to use those settings to configure on premises side of the VPN server okay.So I opened this file which is a simple text file.And then we need to follow all these steps and few additional steps to set up the VPN connection, andall the settings needs to be done on the on premises side of the VPN server.So in order to do that, let's log in over SSH to the on premises VPN server and configure this settings.Arthur.Right.Okay.So let's go to the EC2 and pick this public IP again.And I'm just opening my putty session.And I will just put this IP address and I will load my SSH keys.Which is this one.And I can optionally also save this session so that I don't have to load my key again and again.And with respect to the color, I want to have the white background and let me sorry.Let me save this session say AWS with Chethan key.Right.And I can load this session.The user will be EC2 dash user.Right.That's done.Now we need to install the VPN software.Right now, all the commands for setting up the VPN server are there in the slides.And as I said, please try to adhere to the same VPN server settings, right?Okay, so let's install the Libreswan as a VPN software.And then we will additionally do some settings by referring to the text file that we downloaded.Okay.So let's do sudo yum install liberation.And just press y and then it will install the Libreswan VPN server.Okay that's done.Now next thing we need to refer to our text downloaded file okay.So it says that we need to add these entries into slash etc. Sysctl dot conf file.So let's do that.So let's do sudo.That is for the root permission VI, etc..Sysctl.Conf and just go to the end and paste this lines.Let's save it.And then we have to do sudo sysctl minus p to apply these changes.Sorry.Right.So that's done.Next we want to make sure that this line is not hashed out in this etc. IPsec dot conf file.So let's verify that.So I'm just copying this and let's do cat right click.And you can see here this line is not hashed out.So this is what we want right.Next we need to create this file which has all the VPN configurations.So let's copy this first and create the file and then paste this contents there.Okay.So let's again do sudo VI this file and now I'll paste the content.Okay so I pasted the content.Now a few things you need to change here.Right.Very first thing the left subnet that is the left side of the network.And that is our on premises network because we are doing these settings on the on premise server.The left is itself right.So let's configure that.And the right side is the AWS side of the network.So that is ten 000 slash 16.Now other settings we need to remove this auth equal to ESP.And then for the algorithms we need to change this alias 128 to 256 and mod 102 4 to 2 048.Right.And we need to do the same here.So 256 and here it should be 2048 Right.If we do these settings, I think we are good.Further, we need to just create this secrets file and then we need to add the line which is there.So let's do that.Okay.So let's do sudo VI this secrets file.Press I for the insert mode.Right click and escape colon wq.Right.So it has been saved.Okay so the VPN configurations are done.The last thing we need to do is to just start the VPN service.And for that let's do sudo systemctl start IPsec dot service.Okay.It has been started.And just to check the status let's do this.And if you see this kind of message that means your VPN server is running fine.And in order to verify that let's go to the AWS side of the VPN tunnels and lets see if one of the tunnelis up.Okay, so lets go to the VPC again and inside to site VPN connection.Lets select this and lets go to the tunnel details.So right now it's down, but lets wait for a couple of minutes.So I'm pausing this video and I'll come back here okay.Generally it takes up to 30s for this status to turn up here okay.So which means our VPN is working.And now let's proceed with additional settings with respect to the VPN routes so that we can test theconnectivity between the on premises side and the AWS side.Ether.Right.So we have done everything which is listed on this slide.And now let's configure the routes.So on the AWS side we need to tell all these subnets that if the traffic is going to or coming fromthe on premises network, then all the traffic should be routed through the virtual private gateway.Now how do we do that.So either you can add the static routes into the route table itself, or you can also propagate theCidr range automatically into this route tables.So let's do that.Right.So if you come to the VPCs and go to the route tables, there are two route tables for the cloud andtwo route tables for the on premises network.So if you go to say cloud route table here then you will see there is only route for the local Cidrrange.Right.And here we need to add a route for virtual Private gateway.Now as I said one option is to just add a static entry for the destination as the on premises Cidr range.But the better way is to propagate that route automatically.So for that let's go here edit the route propagation and enable this.Now what it does is it automatically brings this particular Cidr range here in the route table.Right.So that's something that you need to do.Second we will do the same thing for other route table on the cloud side.And here also let's go to the route root propagation and enable that right.And you can see that this entry has been added automatically.So remember this step.Okay.So cloud side of the VPN configuration has been done.Now let's go on the on premises side.And in here we need to do few settings.First we need to disable the source destination check on the VPN server.And that is because it is going to forward all the traffic ultimately to this server.So which means that traffic is not for this VPN server.And we learned that earlier in the EC2 section as well.So we will disable that.And second, we will update the route table for the private subnets to send or receive traffic throughthis VPN server.Right.So let's do that okay.So let's go here.Let's select the on premises VPN server action networking and change the source destination check.And let's say stop Right.And second, let's go to the private subnet for the on premises site which is this one.Then let's go to the routes, then edit the routes.And let's say if the traffic is going to or coming from ten 000 slash 16, then it has to go throughthis Eni of the VPN server.So instance and the VPN server.Right.So at this point we are done with the entire VPN setup.So from the cloud EC2 instance we should be able to ping to the on premises EC2 instance using its privateIP and the other way round.Right.So for that we need to log in to one of the server.So let's go to the cloud App server and we can just log in with the public IP.So let's do that.And here what I can do is just I can load my this session and just change the IP address so that I don'thave to load the keys again and again.Right.So EC2 dash user.And now let's ping to the on premises servers private IP address which was 192.168.1. something.So let's see that that is 157.Right.And you can see that I can ping.That means our VPN connection is working perfectly.Okay.So we need to test this connectivity.So we tested the first one that is from the cloud to the app server on the on premises side.Now we need to do the other way around as well.Now for that we need to log in to the app server.And it's in a private subnet.So first we log in to the VPN server.From there we can SSH into the app server on the on premises side.And from there we can try to ping to the cloud EC2 instance using its private IP.And further from the app server we can also test the internet connectivity because that traffic willgo through the Nat gateway.Right.So what do I mean?So from this app server we need to first test connectivity to the app server in the cloud.And then from app server to the internet through this Nat gateway.Right.So let's do that.And for that I need to first log in over SSH to this VPN server.Then I need to bring in the SSH key which is a private key.And then using that private key I will ssh into this particular instance.Right.So let's do that okay.So I am already onto the VPN server.So let me do VI.And I need to create the PEM file for the app server.And I'll copy the content here okay.So this is my ssh pem file.I'm copying it and I'm just pasting it here escape colon wq.Then I need to change the permission to 400.And then I need to ssh using this pem file ec2 dash user at the rate 192.168.1. I think 157.Yes.Okay, so I am into the app server and from there we need to ping to the cloud server.So let's check what's the IP address of the cloud server.So for that let's go here.So cloud app server IP address is 163.Right.And I'm able to ping which means the connectivity from both the side is working fine.Finally let's try to just ping say google.com.And it is also working.That means traffic is going to the internet.Okay.So this means we have completed our setup for the VPN.So let's stop here for this particular lab.So that was our first step basically.And now let's move to the second step where we are going to configure now the DNS on both the sidesright.So now let's go to the second step.Thank you.