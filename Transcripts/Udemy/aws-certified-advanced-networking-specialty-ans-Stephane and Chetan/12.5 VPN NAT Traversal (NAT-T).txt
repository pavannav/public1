# 12.5 VPN NAT Traversal (NAT-T)

All right.The another important feature that you should know about AWS site to Site VPN is the Nat traversal,also written as Nat T.Okay.So in this lecture let's understand more about Nat traversal okay.So this is a typical architecture as you can see here.And important thing to note is that the customer gateway where we are terminating the VPN is publiclyaccessible.So you are creating the VPN tunnel between the virtual Private gateway or the Transit gateway and thecustomer gateway, which is publicly accessible.So in this normal case, in order to have the IK handshake, you need to have UDP port 500 open at thiscustomer gateway.And that is inbound and outbound port.So this is for Ike negotiation.But for actually sending the traffic through this IPsec tunnel.It uses ESP 50 protocol.And the ESP is encapsulating security payload.And 50 is a protocol ID, for example TCP protocol six.Likewise, it's a protocol, and for ESP there are no ports.But you have to open the particular protocol in your firewall if it is there for this customer gateway.So in the regular case, if your customer gateway is publicly accessible, then you must allow inboundand outbound traffic for this two protocols and the ports UDP with 500 ports and ESP 50 protocol.So that's a normal case.And now let's look at the scenario when your customer gateway is behind the Nat device.Right.So something like this.Now if this is the case basically Nat device cannot handle the ESP 50 protocol.And that's because ESP handles your encryption authentication and integrity of the packet.Now you know that Nat basically accesses the IP header replaces the source IP with its own IP, andif you do that at this moment, then ESP integrity check will fail.And that's where you cannot directly use the ESP 50 protocol here.Right now you have to encapsulate this ESP 50 inside the UDP header.So in order to support the customer gateway behind the Nat, you have to do a couple of things.First you have to enable the Nat traversal that is Nat T on your customer gateway device.And second on your firewall or on the Nat device.You have to allow UDP traffic on port 4500.And once you do that on AWS side, you don't have to do anything because during the initial handshake,AWS understands that it is talking to the Nat device.And it all happens as a part of Ike negotiation.And then AWS will start wrapping up this ESP 50 inside the UDP packet, and it will send request overport 4500 and later that will handle the UDP packets and will send the packets to the customer gatewaywhere you can terminate the VPN.So whenever customer gateway is behind the Nat, then you have to enable the Nat traversal and you haveto open these ports.So in this case, as I said, you have to allow inbound and outbound traffic for both this UDP ports.Okay.So I hope it is clear.Now with that let's just summarize what we discussed.So AWS automatically detects the Nat during key negotiation and switches to UDP for 500 instead of directlysending traffic using ESP 50 protocol.So for Nat traversal, no configuration is required on AWS side, but on the customer gateway side.If you are not using Nat, then you must allow inbound and outbound for UDP 500 and ESP 50 protocol.But if your customer gateway is behind the net, then you must allow inbound and outbound traffic onUDP port 500 and UDP port 4500.Right.So that's basically is Nat traversal where your customer gateway is behind the Nat.And from the previous lecture just I want to bring up one topic.You know that when you enable the accelerated VPN using AWS Global Accelerator, you have to enablethe Nat traversal on the customer gateway device.And that's for the reason that AWS Global Accelerator doesn't support the ESP 50 traffic.So you have to send the traffic inside the UDP packet.And that's where Nat traversal has to be enabled on the customer gateway side.Right.So I hope it is clear what is Nat traversal and what configurations you need on the customer gatewayside.So I hope you will remember that because in your exam, there might be one question around this Nattraversal, and I am sure you can now answer that.So with that, let's stop for this lecture and let's go to the next lecture.Thank you.