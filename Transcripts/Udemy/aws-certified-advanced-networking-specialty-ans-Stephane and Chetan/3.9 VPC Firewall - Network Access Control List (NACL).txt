# 3.9 VPC Firewall - Network Access Control List (NACL)

Hello and welcome to this lecture on network Access Control list.So in the last lecture we saw how security Group works.As you know they work at EC2 instance level.Now let's look at the other firewall in VPC that is network ACL.So as I said network ACL works at subnet level.That means whatever rules you add to the network ACL, they will be applied to all the, uh, EC2 instances,right.Whatever traffic is coming in to the subnet.So while you compare network ACL with uh security group, there are subtle differences.And you need to know this uh, very clearly for your exam.So let's see what network ACL looks like.And as and when we progress uh, into the course we will also try to demonstrate how network ACL works.So first thing network ACL works at subnet level.As you know it applies to all the traffic which goes inside your subnet irrespective of uh which EC2instance is going to.They are stateless.Now, if you remember from the last lecture we said like security groups are stateful, which means,uh, whatever traffic is coming into your security group from a given IP, the return traffic goes withouthaving the outbound rule into the security group.Right.And the other way around, if traffic going out, uh, the return traffic will come back without havingthe inbound rule.That's a statefulness of security group.But we are saying now the network ACL are not stateful, they are stateless, which has to be then handledproperly.If you misconfigure network ACL, then your traffic may not flow.So let's look at what this statelessness means.So again a similar example.We have an EC2 instance.And we have web server which is running on port 80.And now as we are talking about network ACL we will talk about the subnet.So we have this EC2 instance in say a public subnet.And then there is one network ACL sitting here.Uh now by default whatever network ACL comes in it allows the all inbound and outbound traffic.But now we are talking about, say custom network ACL.You went and modified this network ACL.And then here is a client which has some IP address.Let's take an example 1.2.3.4 for simplicity.Now again the question is what inbound and outbound rule you need to have in this network ACL.Right.So of course, the inbound rule, uh, is similar to what EC2 would have, right?You need to open port 80, uh from this IP address and then the traffic can go in.So that means I would have similar rule port 80.Open it for one, two, three, four.Now the question comes what about the return traffic.It will hit the EC2 instance and now the traffic will go back from the EC2 instance.It will hit network ACL and it will check.Is there a outbound rule.Now this is required because network ACL is a stateless.So if you see currently there is no outbound rule.That means traffic won't go back to this EC2 instance.Right?That means you also need to have the outbound rule here.Now the question is to which port this traffic will go back.Now, uh, just bear with me.I'll explain this, but imagine another case where there is just one EC2 instance and it is trying toreach to the outside world, say w w w.Now in that case, clearly it's an outbound traffic and in the network ACL the rule will be I.This EC2 instance should be able to reach to internet on port 80 of the internet server.So this will be the outbound rule for that kind of outbound traffic.But in our case it's not really a traffic which is originated on EC2.This is a traffic, uh, which came from this machine.And now the traffic is going back.It's a return traffic.Now, one thing to understand is that we can't really have if you add just this rule port 80 and thetraffic is 1.3.1.2.3.4, it won't still work.Can you guess why this is?Because when my machine.Or originates the traffic to the web server.It does not use port 80.Here it uses the ephemeral port range to establish this connection to port 80.And if you know, ephemeral ports are typically in this range three, two, seven, six, 8 to 6 five,five, three, five.So that means randomly one port is picked from here.Let's take an example say 3 to 800.So this port is three two 800 and this port is 80.That's where the connection is established.That means if the traffic has to go back, you have to actually, uh, send the traffic back to thisport, not the port 80 of this originating machine.That means you don't need to have port 80, but you need to specify the whole range of this ephemeralport, because you are not sure which port will be used, uh, when the new connection is made.So you need to actually add this rule port range this and the IP is this.Then the outbound traffic will go back to the machine and your connection is closed now.So this is how the network ACL works.And if you miss this rule then your traffic won't go back.So it is very essential that you know how to troubleshoot this kind of issues.So that's called a statelessness of your network ACL.I hope that is clear.So let's go back.Um next is it contains both allow and deny rules like in the security group.If you saw we could just add the rule.And which means a given IP is now allowed.Right.So in this EC2 instance, whatever IP address or machine one two, three we could just add the allowrules.We can't say stop the traffic from this machine using the deny rules in security group.But in network ACL you can do that.You can say this IP 1234I want to deny the traffic two right.And then traffic won't flow.That means it allows both this type of the rules.Rules are evaluated in the order of the rule number.So in security group uh, typically what happens uh, it evaluates all the rules.And if it uh, somewhere if you have the allow rules, of course it will.Irrespective of the, uh, sequence of the rule, it will allow the traffic, if anywhere, it findsthat.But in network ACL, it will go by the rule numbers.So maybe it'll start at 100 and then one zero, 1102 kind of thing.If, uh 100 says the traffic is denied, it won't go next and it won't check for the same source IPaddress whether it's allowed or not.So which which your rules, uh, matches first, it will take into the effect uh, default network ACLallows all inbound and outbound traffic.As I said earlier, uh, if you are launching your EC2 for the first time, you won't even realize thatthere is something called network ACL.But in security Group, you have to go and allow specifically traffic for say, 22 or 3 389 for RDPbecause by default, um, security group does not have any inbound rule, but network ACL, defaultnetwork ACL, which comes with the subnet, has both the inbound and outbound traffic allowed.So the benefit of network ACL is that you can still block the IP address at the subnet level.Now this is a very important.Now consider a case where you have your web server again hosted on EC2 machine.And being a website for example it is running on say port 443.And of course in security group you will say so security group rule will say uh, allow port 443, whichis Https for whole world, right.And anybody can access this website.But then what happened?Maybe there is a, uh, bad bot.Right.And it's trying to maybe do a DDoS attack or any kind of attack onto your machine.Now you know this IP address.Let's take some example, say 9999 is a bad IP address.It's hitting your website now with security Group.You can't block this because security Group does not have any rule to block a given IP address.So only thing you can do is you can add your network ACL at subnet level.Right.So this is network ACL.And you can then add a um entry say port 443 for this source IP.And you want to deny.Right.And that's where everybody else can access your website.But this particular IP cannot.So remember that you can add a deny rule only in the network ACL.So if you look at the network ACL rules this is an example of the rule.So in this case you can see there is a rule number.And this rule number could be 1232766.Typically it is recommended to have the rule numbers in the order of tens or hundreds.That's because in the future if you want to say insert some of the rules, you keep enough buffer inbetween, right?So it's always, uh, good to have some gaps in the rule numbers 100, 200, 300 is good enough as longas you don't.Exhaust.Uh, this limit of 3 to 7, six, six.So here you can see there is a rule number what type of the traffic it is, protocol port source andwhether the traffic should be allowed or denied.So in this case, the first line says that any traffic from this particular IP address has to be denied.So if you are receiving any traffic from this IP address, it will be straightaway denied because itwill match the first rule.So even if the next rule says that four, four, three traffic is allowed, uh, that's an Https traffic.But this is already blocked at the first rule.So this won't be allowed by the second rule.And this is how you can block the traffic from a particular IP address.Or you can block it from a particular Cidr block.So this is very important uh, with respect to the network ACL.Now just to summarize the differences between the security group and network ACL, very first securitygroup operates at instance level.Uh, now with the knowledge that we have so far, we are saying that security groups operate at instancelevel.But in reality instance has Eni attached and the security groups actually works at any level.That means if you are easy to instance, has multiple elastic network interfaces, then there wouldbe multiple security groups attached to those.Enis.But for simplicity we are saying it operates at the instance level.However, network ACL works at the subnet level.That means all the EC2 instances in that subnet would be affected by the network ACL rules.However, if you modify one of the security group, then only the instance to which it is attached willbe affected by that security group.Security group supports only allow rules.We have seen that there is no deny, but in network ACL both allow and deny rules are supported.Security groups are stateful.That means you don't have to explicitly allow the return traffic.It automatically goes back.But in case of network ACL it's stateless, which means that you have to explicitly have a rule in yournetwork ACL for the return traffic.Now, when it comes to rule evaluation, there is no deny rule in security groups.That means if there is any match for the allowed traffic security group will allow that.But in case of network ACL, it goes by the rule number starting with one to maximum number.And then uh, if rule matches, it will just uh do that action whether allow or deny and then furtherwon't evaluate next rules.And this feature of network ACL allows us to deny or block the traffic from particular IP address,which is not possible in the security group, because in security group you just write the allow rules.There is no deny rule.And there are some more additional things listed on this link.If you are interested, you can go and read more about this.But as of now, this is sufficient, uh, to know about security group and network ACLs, and we'llsee more advanced feature of the VPC going forward.But I hope you understood this part.So thank you very much and stay tuned.