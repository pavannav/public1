# 14.12 Transit VIF

Hi.as of now, we have seen the public VIF and a private VIF.Now let's quickly talk about the Transit VIF.And then in the following lectures,we will see architectures using the Transit VIFand private VIF along with the DirectConnect Gateway.So in this lecture,we will just cover the features and thenwe'll dive deep into the following lectures.So as you already know,Transit VIF allows the connectivity betweenthe DirectConnect and the Transit Gatewayand this Transit Gateway will be inside your AWS account.And as you know, Transit Gateway are the Region routers,that means every Region would have the Transit Gateway,they can't go across the AWS Region.And we will again talk about this architecture shortly.Now,the way you connect Transit Gateway to the DirectConnect isthat first you have to provision the DirectConnect Gateway,create a Transit VIF attached to the DirectConnect Gateway.And then you can associate DirectConnect Gateway tothe Transit Gateway in your AWS account.Now, with respect to the Jumbo frames,you know that on a private VIF,you can enable the Jumbo frame of 9001 bytebut in case of Transit Gateway and Transit VIF,you can support 8500 byte size of the Jumbo frames.And you have to explicitly enable this while creating the Transit VIF.Now,you can also attach multiple Transit Gateway to a single DirectConnect Gateway.So if you want to see how it looks,now, this is a DX connectionand there is a Transit VIF, you attach that to a DirectConnect Gatewayand then you can associate multiple Transit gateways acrossdifferent AWS Region to the same DirectConnect Gateway.You can also attach multiple DX gateways to a single Transit Gateway.Now again, this is an architecture patternwhere you have multiple customer sites and all sites needsto communicate with your VPCs in a particular Region.For example,in that case, you can set up the architecturewhich does thisand just one more thing to remember is thatbecause there are now 2 gateways,Transit gateway and DX gateway,you have to make sure that the AS Numbers for those are different.Otherwise you can't associate these two gateways together, right?Andwhat I mean by that?So this is an architecture where you areconnecting multiple DX gateways to the same Transit Gateway.And with respect to the ASN it should be different when you associateTransit Gateway and the DX Gateway.So those are the things to remember for Transit VIF and the Transit gateways.And I hope that is clear.And now in the following lectures,we will dive a little deeper intothe DirectConnect Gateway and some more architectures.Thanks for watching and stay tuned.