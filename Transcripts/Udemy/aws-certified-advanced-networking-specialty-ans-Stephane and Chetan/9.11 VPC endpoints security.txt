# 9.11 VPC endpoints security

Okay.So in this lecture let's focus on the VPC endpoint security.And again this is a important topic for your exam right.So this is now the familiar architecture to you where we have the private subnet we have the client.And then you can access AWS services or your own services hosted as VPC endpoint service uh in yourown VPC.Right.And to access AWS services, as you know, uh, for S3 and DynamoDB you can use gateway endpoint.And for that you have to modify the route tables, uh, and otherwise rest of the AWS services or saymost of the AWS services can be accessed through the interface endpoint.And that is Eni there.Right now, if you apply the security lens to VPC endpoints, then there are two layers of security,one at the network layer and second at the permissions level that we refer to IAM in AWS.Right.So with respect to the network, as you know, mostly it will be the security groups through which youcan control the access to the respective, say, endpoint or endpoint services.So from the client perspective, if the client wants to reach to any of the service, say S3 or thisnetwork load balancer, then of course you would have to open the security group outbound rule there,right?So for example, for S3 you will open the security group say for four three.Right.That is outbound security group.And if you have this NLB which is uh, having the listener on four, four, three or support, uh,any custom port like 80, 80, then also you would have to have that, uh, port in your outbound rule.So that's about the EC2 outbound rule, right?Now for the gateway endpoint there is no Eni.That means there is no security group.So here you control rest of the traffic using the route table entry that you add here, right?I hope it is clear and you remember that.But for the private link or say interface endpoint there is Eni.And that means there is also a security group.And in here you have to control the inbound security group.So you will allow the same ports and the source will be the EC2 client IP address that is a privateIP address.Or you can have entire VPC Cidr range as the source.Or you can have the security group of this EC2 instance as a source.So overall at network layer these are the two security controls probably you can have while using theVPC endpoint.And we have already talked about this in the previous lectures.However, in this lecture we want to focus more on the permissions.And as you know in AWS that is done through the IAM policies.Right.So in case of VPC endpoint there are different IAM policies that you can apply.And those are called VPC endpoint policies.So as you can see here there is a gateway endpoint and the interface endpoint.So there is something called VPC endpoint policy.And it follows similar to the IAM policy format basically.So you can apply these policies.And shortly we will see what kind of permissions you can manage using this policy.So VPC endpoint policy is one of the security controls that you get.But apart from this you can also apply something called AWS resource based policies.And this is only applicable for the AWS services which support resource based policies.So if you heard about S3 bucket policy, that is a resource based policy.Similarly DynamoDB supports that.API gateway supports that.So all these are AWS resource based policies.And there also you can define from where or who can access this particular S3 bucket.And you can define that S3 bucket can be accessed only from this particular VPC endpoint.So likewise you are getting now double control one at the VPC endpoint level.And for some of the AWS services at the resource level as well.So next in this lecture let us talk about those policies.Okay.So if you do nothing and you just create the endpoint and you must be seeing that while we did the demo,there is always a default VPC endpoint policy that is attached.And it has this kind of format.That means it allows access to, you know, respective AWS services.There are no restrictions applied, right.So this is a default VPC endpoint policy.And uh, otherwise custom VPC endpoint policy can also be applied if respective AWS service supportsthat.And how do you really check whether respective AWS service supports the VPC endpoint policy or not?So for that you can just fire a CLI command something like this.And here you see there is a VPC endpoint policy supported.So if the value is true that means that service supports the VPC endpoint policy.Otherwise full permissions will be granted from the VPC endpoint perspective.And then uh, you can't really change this policy right now.Also, when you create the VPC endpoint service, that means you are hosting your services behind thenetwork load balancer.There also, this default policy is applied and you can't customize or you can't define the policy there.Right.So the bottom line is the VPC endpoint policy is applicable only for the services which supports theendpoint policy.So for example S3 bucket supports that.DynamoDB table supports that SQS supports that.Similarly API gateways and there are so many services.So if you run this command you will know which all AWS services support it.Right.So that's the default policy.But as I said you can always customize that for the respective AWS services.So just an example.If you want to restrict access to a particular S3 bucket through the endpoint, then you apply thispolicy to the VPC endpoint while you create that.And then only access to this particular S3 bucket and only for this action will be granted.Similarly, you can do that for the DynamoDB table, right?So I hope it is clear.Now further, you can also restrict access to particular AWS account or a particular IAM user or roleas well.So it checks which is the user who is making those API calls through the VPC endpoint.So here you can see the principal account should be this or otherwise.You can go granular.And you can also define the IAM rule or the user here.Now just one small thing to note here is that here principal should always be star.And you have to define these accounts or users in the condition block and not in the principal block.Right.So that's in a nutshell is a VPC endpoint policy.And now let's move to the next type of the policy which I said you can also apply the policy on theAWS resource itself.And this is called S3 bucket policy.So if you take an example of S3 then you can basically define different conditions in S3 bucket policysomething like this which says if the traffic is coming from a particular VPC endpoint with the endpointID, then only it will be allowed.Now similarly, you can also define the source as VPC itself.And in that case, all the endpoints within that VPC can access the S3 bucket.So rather than adding individual endpoint IDs, basically you can define the VPC as a source.Further, this AWS source VPC condition only applies in the context of VPC endpoints.That means if traffic is not going through the endpoint, then this condition doesn't make any sense,right?So just remember that.And further there is also one more condition element that is AWS source IP.So you can define the private IP ranges from where the endpoint is being accessed.And this is also applicable only in the context of VPC endpoint.Otherwise AWS source IP generally refers to the public IP from where S3 can be accessed, right?So that is another condition that you can add to your S3 bucket policy, right?So with all this knowledge, if you have to create a sample S3 bucket policy, then it may look likethis where condition says that source VPC endpoint should be this right?Or you can say it can be entire VPC and all the VPC endpoint can access that.Now here the effect is deny and condition is not equal.So it's a reverse kind of only allow from this endpoint.Ultimately it's double negation basically.Right.So these are some of the bucket policies that you can apply.And similarly you can do that for other AWS resources like DynamoDB or API gateways which we are notcovering here but very much possible.Right.So I think we covered almost everything with respect to the VPC endpoint security.And just to recap, at the network layer you have the security groups.And in case of gateway endpoint there is also route table.But at the IAM level there are two kinds of policies.One is VPC endpoint policy itself and otherwise.On the AWS resource side there is resource based policies.And that's it really.So that's it about VPC endpoint security.And with that now let's go to the next lecture.Thank you.