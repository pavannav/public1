# 12.11 AWS Site-to-Site VPN Architectures

Hello and welcome to this lecture.Now we understood about how AWS Site-to-Site VPN worksnow let's understand few architectural patterns.So, this would involve high availabilityof your VPN connection as wellas how do you connect, say multiple branches of your officeor multiple data centers to AWS VPCusing multiple connections.So, let's see what all we are going to talk here.Now, very first thing,having a single VPN connection from AWS VPCto your office networks or your data center.Now, we have seen that that can be very well donewith virtual private gateway,and that's what exercise also we did,but there is one more networking component in AWSthat is transit gateway,and we have altogether a different section for that.But for completeness of the VPN architecturewe are also going to talk about transit gatewayin this lecture.So, that's about having one connection between AWSand on-prem data center or your branch office network.Now, you could also havemultiple Site-to-Site VPN connection.That means you can connect one VPCto multiple branch offices all together.Now, that is like having multiple VPN connection.And we'll see how the architecture would look like.And finally, we'll see howto have redundant VPN connections.Now, why that?We already understoodthat there are two VPN tunnels for high availability.And that should actually suffice.But on customer gateway side,what if the gateway device itself goes downand both the VPN tunnels terminateon the same customer gateway.In that case probably having a redundant VPN connectionwould provide you much more resiliency of the connection.So, let's look at these architectures one by one.So, this is the architecture we have been talking aboutthroughout this lecture.We saw this and we had done an exercise around this.So, here you have VPC and virtual private gatewayand on the right side you have customer network.And then you connected thisto our Site-to-Site VPN connection.And for high availability,there are two VPN tunnels flowing through.So, this, you know very well.There is a slight change in this.Instead of virtual private gatewayyou can also use a transit gateway.Now transit gatewayis a relatively new networking component.It came in 2018,but now transit gateway you'll see everywhere,wherever you want to connect multiple VPCs togetherand having the architecture,like connecting VPCs to your on-premise network,then transit gateway is useful.Now, as I said there is a separate lecturewhich covers transit gateway,but in current exam at leastI haven't seen transit gateway coming as a topic.But it's always good to know about that.So, what I mean by this architecture is thatinstead of having a virtual private gatewayyou can also create a transit gateway,attach your VPC to that.And then transit can do the same thingthat virtual private gateway does.You can connect or set up a Site-to-Site VPN connectionbetween customer gateway and a transit gateway.So, that's another networking component you can use here.So, that's pretty much it about havinga single Site-to-Site VPN connectionnow let's look at other architectures.What if I have multiple corporate data centersand every corporate data center would haveits own customer gateway, right?Maybe how branch offices there in the USat multiple locationsand I want that all these branch officescan communicate to my cloud workloads.And those workloads are somewhere in,maybe (indistinct) into one VPC.Now, I don't have these branch offices connectedwith each other, just to find an example,but I want to make sure my VPC connectsto every branch office.Now I don't need multiple virtual private gateways here.And you can't even have,you can't connect multiple VGWs to a single VPC.You just need to have one virtual private gateway here,attached to a VPCand then you can have multiple VPN connectionto every customer gateway here.So, all these will terminateon the same virtual private gateway on AWS site.Now, while having this setup,remember that the aggregate bandwidththat virtual private gateway provides is limitedby 1.25 Gbps.So, it does not mean you have full connection.That means the bandwidth will be multiplied by four, no.It would be 1.25 Gbps only.So, accordingly, you can imagine how much datayou can transfer if you have,say up to eight or 10 connections your bandwidth,here at virtual private gatewaywould be shared by all these VPN connections.Now, there is a better option to that.As I said, there is a transit gateway as well,which does not have that kind of bandwidth limitation.So, you can very well replace this with transit gatewayand again do the similar setup.So, the pointis on VPC side you can haveeither virtual private gateway or transit gatewayand then you can connect itto multiple branch officesusing Site-to-Site VPN connection.Okay, with that, let's also talkabout one more architecture,which is high availability for your VPN connection.So, as we understood we could have one private gateway hereand then one customer gateway and customer data center site.And then there are two tunnels.The problem is what if the customer gateway itself goes downor you want to have some maintenancefor that customer gateway device.In that case, both the tunnels will go downat the same time.So, rather what you can do,you can have multiple customer gateways,at least two on the right side,and then you can have two VPN Site-to-Site connectionsbetween your virtual private gatewayand the customer gateways on the other side.So, this would provide you ultimately four tunnels.And that means if one customer gatewayhas a maintenance going on,you still have the connectivitythrough other VPN connection.And all that outing and everything,you can again configure in your out table,which routes to be taken.We already saw that;how to influence those routing decision.All that would apply here as well.So, you can have a redundant VPN connectionfor high availability.Okay, so these are few architecture I wanted to talk firstand then later I will take some example,like I'll provide a scenario,like this is a scenario:I want to have this kind of connectivity.Then what would the architecture look like?So, once you understand everythingabout the VPN connections,we'll get into that lecture shortly.So, that's it for this lecture and thanks for watching.