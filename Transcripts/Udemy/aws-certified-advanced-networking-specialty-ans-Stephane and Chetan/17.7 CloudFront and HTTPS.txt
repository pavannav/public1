# 17.7 CloudFront and HTTPS

So now as talk about CloudFront and HTTPS.So we have two connections happening,one within the client and the edge location,and one within the edge location and the origin.So the Viewer Protocol Policyis how the client connects to the edge location.So it could be HTTP and HTTPS.And we can also have the option to tell CloudFrontto redirect any request that is HTTPinto an HTTPS request for security.Or you can say for example,that you only want to allow HTTPSand refuse anything that is HTTP based.For me, personally, I like the second optionbecause it allows even bad requestto be transformed into good request into HTTPS.Now, for the Origin Protocol Policy,this is how the edge location connects into your origin.And you have HTTP only, or you have HTTPs only,or you have a match viewer,which means that if the viewer is doing a request on HTTP,then it will match the viewer policyand so therefore send a request in HTTP form to the origin.Or if the request of the viewer is HTTPS,then also match that and send the request to the originin HTTPS form.Now, a few things to noteis that your S3 buckets static websitesdo not support HTTPS so you have no other choicebut to use the HTTP Origin Protocol Policyfrom your edge location into an S3 bucketsetup as a static websites.And also if you want to have the CloudFrontto connect to your origin correctly,they need to make surethat your SSL or TLS certificates on your origin are validand recognized by CloudFrontbecause CloudFront will access publicly these end points.Now, for Alternate Domain Names.So this is when you want to use your own domain nameinstead of the domain name assigned by CloudFrontto your distribution.So instead of using a CloudFront URL that looks like this,you want to maybe use ww.example.com,which is a domain name that you own.So in this case, CloudFront will be set up by youwith alternate domain names.For example, ww.example.com or star.example.com,and we will have to load a proper SSL or TLS certificateonto CloudFront.We set up these domain namesdirectly onto the CloudFront distribution,and then our users are able to accessour CloudFront distributionusing a URL that we would expect.For this, we must have a valid SSL/TLS certificatethat will cover your domain nameand any alternate domain name that you set upas part of your distribution.It is possible for you, for simplicity's sake,to use a wildcard certificateand cover a wide array of domain names.Okay, now let's talk about these SSL certificates.So there's the default CloudFront certificatethat we get by default whenever we create a distribution,which is star .cloudfront.netand this is, for example, one of these distributions.So this is where we get HTTPS out of the box with CloudFrontwhen using a CloudFront URL.And if you want to load your custom SSL or TLS certificate,then you would serve it in two ways.So either CloudFront will serve the HTTP request using SNI,which recommended,so CloudFront will know which SSL certificate to usebased on the request it receives,or we can have a level of security that is expensive,which is to use dedicated IP address in each edge location.So for this, we can use the certificatesthat are generated directlyfrom AWS Certificate Manager or ACM,or we can upload it third-party certificatesdirectly into ACM or the IAM certificate store.But in this case, you have to manually rotate themwhen they expire.Now, these certificates must livein the US East Northern Virginia region,and the reason is because CloudFrontis assigned to that regioneven though it is a quote, unquote global service.Then you are also able to similarly,as we did it with the ELB,to set up a security policyin which we specify the minimum protocolfor the SSL/TLS in ware as well as which ciphers to use.So that's it for HTTPS security on CloudFront.I hope you liked it,and I will see you in the next lecture.