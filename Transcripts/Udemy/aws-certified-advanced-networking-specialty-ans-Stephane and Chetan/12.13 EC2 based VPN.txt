# 12.13 EC2 based VPN

Hello and welcome to this lecture.So, so far we talked about managed site to site VPN connection.We saw how to set up that VPN connection on site.You have Virtual Private Gateway and other side, you have customer Gateway.And then we also saw features, transitive routing, also various architecture patterns while usingthe site to site VPN connection.And we know that site to site VPN supports IPsec Protocol.Now that does not mean you can solve every other problem with the managed VPN connection.In certain cases.Probably you would have to look beyond that.And one of the possibility is to have your own VPN solution deployed on EC2 machine and then you managingthat on your own.And that's where we are talking about managing or setting up the EC2 based VPN connection.That means we are not going to use AWS managed VPN.So let's look at some of the features consideration and then various architectures when you should useEC2 based VPN connection.So the architecture would look something like this.You have VPC and you have your applications running here on this EC2 instances.And now instead of attaching the virtual private gateway to your VPC, you would actually terminateyour VPN connection on one of the EC2 instance.That means this EC2 instance should have the VPN software and then the connection from outside.It should terminate on this EC2 instance and then it will route your traffic to respective applications.So the first question is why would you want to do that instead of using the managed services?Because of course managed services provide you two tunnels for high availability, makes sure that VPNconnection is on and all the features you get along right.So very first reason could be you don't want to use IPsec protocol.There are multiple protocols for VPN like GRE or dmvpn.Now if that is the requirement site to site VPN cannot be used for that.So you will think of using the EC2 based VPN or you are having the overlapping on site and your customernetwork side.Now for using AWS site to site VPN connection, you need to have non-overlapping CIDR, but with EC2you can have the overlapping.Why?Because with EC2 you can you can have the IP table rules at OS level, which can do the source nettingand then your right side of the VPN connection.That means customer gateway connection can be netted using this EC2 instance and that or that allowsyou to use the overlapping on VPC side and customer network side.Which site to site VPN connection cannot allow.Then comes the transitive routing.We have seen in one of the session that through VPN gateway, if traffic is coming from on premise network,then you can't access Internet via IGW or NAT Gateway or you cannot connect to a peered VPC connection.That's because site to site VPN connection does not support transitive routing.Now that's not case here because your traffic is terminating on EC2 instance.That means it's terminating in some ENI into your VPC.It will allow you to go out from the VPC through IGW or NAT Gateway or VPC Peering Connection or VPCEndpoint Services.So it's as good as you are originating that traffic from within the VPC.So it's a benefit of using the transitive routing when you use EC2 based VPN.Then further, if you want some advanced features like most of the customers or enterprises would alsowant to deploy some kind of advanced threat protection along with the VPN software.Now manage VPN only provides VPN as a service, but it does not provide the threat protection for that.Maybe you would have to set up one more threat protection software inside your VPC and route trafficthrough that.But with a single EC2 machine you can terminate your VPN connection there as well as you can put advancedthreat protection on the same EC2 machine.And finally, when you require bandwidth more than 1.25 GB, that's an aggregate bandwidth that siteto site VPN connection provides, but with EC2 it would be limited by EC2 maximum bandwidth and fromearlier lecture that is network performance lecture.We know that EC2 can provide up to five GB of bandwidth when traffic goes from VPC to the outside.And if you need more bandwidth than that, of course you can also use multiple EC2 in say horizontalscaled environment and we will see that architecture in the next lecture as well.So with that, these are the reasons for which you might want to use EC2 based VPN rather than usingAWS managed VPN connection.So you need to have some, you need to consider few things while you use EC2 based VPN.And let's understand that.So to enable the routing of the traffic, of course EC2 would terminate the VPN connection and it willroute the traffic to respective applications inside your VPC.So it must have the NAT kind of ability.So you have to disable the source destination check on that machine because the destination is trafficis not really for that EC2 machine, but it is for your application servers behind that EC2 machine.So you should disable source destination check and also you should enable the IP forwarding like wedid while we set up the customer gateway in the exercise where we had a VPN connection set up, we hadenabled the system IP forward equal to one no same setup you have to done on this EC2 machine as well.Now in case of hardware failure, right now it's your responsibility to make sure EC2 is always up andrunning so that VPN is up and running.Now, as you know, EC2 provides you an ability to auto recovery and for that you can set up Cloudwatchalarm with the status check.And if status check fails, you can trigger the auto recovery of EC2 machine.So this will kind of try to recover your EC2 instance automatically and make sure your VPN connectionis established again.Then bandwidth limitation.As I said, you get up to five GB outside your VPC.With VPN managed VPN, you get 1.25.GBS now does not provide or maintain third party software VPN appliances.What that means is you have to choose which software you want to use in that EC2 machine for VPN termination.And there are many, right, Many leading network enterprises like Cisco or Juniper would provide thosekind of softwares.They also provide this software in the form of AMI Amazon machine image.You can just go and deploy that in your VPC and you are ready to set up that VPN connection so you canuse partner VPN solutions, as I said, from AWS marketplace.And this solution can also provide you highly available and self-healing setup.And for that you need to go to the AWS marketplace and check their documentation, how the setup lookslike, how the high availability is achieved.But most of the heavy lifting will be done by the partner solution and you have to pay for your licensesfees for using that software.So moving on, you also need to understand that the VPN bandwidth on the EC2 will depend on the EC2instance type and size.So if.You think that the network performance is low?That may be because your EC2 instance is really small size and you know that as you increase the EC2instance size, the network bandwidth as well as CPU performance can be better, which can give youbetter performance.Sometimes it also required to scale the EC2 VPN horizontally because one EC2 instance is limited byfive GB outside of the VPC bandwidth.Right.So if you want to have the higher VPN bandwidth, then you might have to have the horizontal scalingdone for those EC2 machines.Now in that sense there are multiple different architectures that you can build like active, activeor active passive.But I don't really think that exam would focus on EC2 based VPN.It should be more around AWS managed, IPsec VPN, client VPN and those kind of solutions.But just if you want to see how this kind of architecture can be set up, then you can see there aretwo EC2 VPNs here and what you can do is for your workload instances, you can have the route tableswhich says the default is going to first VPN, but by any chance this VPN server goes down, then youshould automatically update the route table for those EC2 machines and the traffic should go to theVPN too.Now how do you really update those route tables?You can have some kind of cloudwatch alarm when EC2 instance goes down and then on that alarm you cantrigger a lambda function which goes and update those route tables.So all those kind of architectures are possible.But as I said, you don't really need to worry about all these architectures at this moment for thisexam.Finally, you also need to know that if you are implementing the EC2 based VPN you might think of youcan use NLB and then load balance the VPN connection across multiple EC2, but it is not supported becausefor IPsec it's Protocol 50, which is not supported by NLB.NLB only supports the TCP protocol.Right?So that means NLB cannot be used.So these are few things that you might need to understand.First of all, why EC2 based VPN will be required and we already talked about that and then we alsotalked about some considerations.So that's it about EC2 based VPN, and now we'll get into other topics of the VPN, something like atransit VPC.Thanks for watching and stay tuned.