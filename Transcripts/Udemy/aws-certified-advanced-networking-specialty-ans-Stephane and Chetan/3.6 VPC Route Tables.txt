# 3.6 VPC Route Tables

Hello and welcome to this lecture subnets, route tables and the Internet Gateway.So in the earlier lecture what we have seen, uh, how to address the VPC using Cidr notation and whatthat Cidr means.And then how to subdivide the VPC address range in the form of subnets.And we saw how to size your subnets.Now let's deep dive further into the subnets route tables and the internet gateways.So when you create a VPC the first thing you need to decide is which region you want to create thatthe scope of the VPC is region level.Now as you know, every region comes with multiple availability zones.So VPC can have uh, its presence in uh, multiple availability zones.And it is decided by subnet.Subnet has a scope at easy level.That means when you create a VPC and when you create a subnet, you have to at that moment decide thesubnet would be in AZ one or AZ two.Now subnet cannot be in two phases at the same time right?Subnet would be restricted to a given availability zone.Now when you create a VPC it also creates two things for you.One there is a local router.Now this router is responsible for all the communication between all the subnets within the VPC.And then there is a main route table which actually contains the rule which dictates how the trafficwould flow between the subnets inside a VPC.So what you do is you create multiple subnets.Now here you can see I created two subnets uh subnet A in AZ one and subnet B in AZ two.And I have allocated uh cider range uh like we saw in the earlier lecture.Now the question is when you create this kind of subnets and then suppose you launch EC2 instances withinthese subnets it will get uh IP address.It's a private IP again from the subnet range.Now by default it will pick a random IP address from the subnet range.But if you want you can also choose the one of the IP address from the given subnet range.But there are some IP addresses which you cannot use.And we'll see that shortly.So uh, these are the two instances in two different subnets.Now the question is can instance a for example talk to instance b in subnet B.So how it is decided that whether the network uh is can be established between these two instances.So of course when instance A uh wants to communicate to instance B, uh it will look at the subnet routetable, right.Whether there is a route exist between a traffic from subnet A to subnet B.Now if you closely look at the route table which is here right.It just has one entry by default which looks something like this.It has a destination address as VPC address.And the target is a local router.Now this local router is responsible for any communication for which the destination address is insidethis range.Ten .10.00 16 now from this instance, if I do a ping to this machine, the target address is this machine'saddress, which is ten .1.1.11.Now if you closely look at this, this address is a part of my VPC address range.That means when this instance initiates the connection or a ping request and it will go to the routetable, it will check is there a route to reach to this IP address.So it will look at this address.This is a superset of this address.So yes there is a destination and the target is local.So it will take the path from local router.It will reach to the instance.That means by default whenever you create a VPC and you create multiple subnets, every instance inevery subnet can communicate with any other instance in any other subnet.That's because there is always this rule in all the route tables within the VPC.Right now there is just one route table which is a main route table.But even if you create a custom route table, it will always have this entry.And you cannot remove this.That means there will always be a network connectivity between all the subnets within the VPC.So this could be a question in your exam sometimes.And you need to remember that always all subnets uh and EC2 instances within the subnet can communicatewith each other by default.And we are still not talking about the firewalls.Uh, what do I mean by that?Is maybe instance B can stop the network traffic from instance A at firewall level.But we are not talking about firewall.We are talking about the network connection which is always there.So these are two different things.And please remember that okay.So that means instance A can talk to instance B uh through this local router and by following the mainroute table.Now the question comes can my instances communicate with the internet.Right.Of course the answer is no.But why.So there are multiple reasons to it.First, this VPC itself has no gateway which can connect this VPC to the internet.If you remember in the last lecture I talked.About VPC is a private IP address isolated private IP address.Right.So that means, uh, this network is not connected to internet directly.If you want to do that, the first thing you need to do is to, uh, add an internet gateway attachedto your VPC.And, uh, it enables the internet connectivity for your VPC, but just adding internet gateway, doesthat solve the problem?Does that mean my instance here can talk to internet now?So again the answer depends on now uh next part of the things.Right.So if you want to communicate with the internet your instance in a public subnet, it must also havethe public IP address.So assuming we also grant or attach a public IP address to this instance how to do that.We'll see later.But assume that along with this private IP it also has a public IP.Now can this instance talk to internet.Right now.The next part is it will look at the main route table.Again.It will check.Is there a route to go to the public internet.So for example you are trying to reach to Amazon.com and it would have some public IP address.So is there a way I can reach to Amazon.com IP address by looking at this route table?No, there is no entry to that.That means this instance can still cannot talk to internet because there is no route for the internetIP address.Now what if you add this IP address?You know that any address can be matched with 0.0.0.0/0.That means now I'm adding a route entry in this route table.And I'm saying if the traffic is for this IP address range that is internet IP address range, uh,traffic will flow through the internet gateway.And I'm modifying this main route table.So if I do that and if I my instance has a public IP, actually I can communicate with the internet.Right.So this is how, uh, internet connectivity can be established.Now please pardon me for the public IP address.I haven't shown that.But assume that along with the private IP, there is also a public IP.Right.Uh, only with private IP directly.Uh, your instances cannot talk to the internet.Okay.So that's, uh, uh, that's what we wanted to see.Now, the next part is, uh, what if I only want, uh, this instance to talk to internet and not thisinstance if I modify the main route table and add an route entry for internet gateway, my both theinstances in both the subnets would be able to talk to internet.That's because both these subnets are following the main route table by default.Right.So many a times it would happen like you would you would want to control, uh, a traffic differentlybased on what kind of workloads you have and whether they are part of a public subnet or a private subnet.For an example, if it's a database, you won't put your databases in the, uh, public subnet.Right.You will keep that, uh, in a private subnet and only instances maybe your web server, which is therein a public subnet, can talk to your database in a private subnet.In that case, you don't want to expose your database to the internet.So you want to make sure you control the traffic from your subnets.And in that case, what you will do rather than following the main route table, you can also createyour custom route table right.So you can create a custom route table for subnet A and attach it to the subnet A.And then you can add corresponding route here say if traffic is going uh, to internet then uh routeit through internet gateway.And now as soon as you attach this route table to subnet A it, it stops following the main route table.That means now it no more looks at the main route table.It will always follow the, uh, its own route table.That's the first thing.But as as of now, the subnet B will still follow the main route table because we haven't created acustom route table for the subnet B.So now, uh, you can also create a dedicated route table for subnet B.And as I said you don't want to enable the internet connectivity for subnet B.In that case it would have again a single entry, which uh comes by default with when you create a newroute table.Right.And it would have the entry ten .0.00 16 and route the traffic through local router similar to yourmain route table, but just that we, uh, explicitly created one route table and attach it to subnet.Right.So if you create, uh, two subnets now and attach to your uh, corresponding, uh, subnets, uh,these subnets would not follow the main route table.Now, any changes you make to the main route table will not affect your this subnets routes in any way.Okay.So I hope, uh, this is clear.That means you can create a custom route table, and you can add and remove the routes as you want,except this route, which will always be there in any of the route table within the VPC.Okay.So that means, uh, my instance B actually cannot talk to the internet.And now subnets are not following the main route.Okay, so I hope this is clear.So, uh, what is route table?It contains the rules to route the traffic in and out of your subnet.The way we just saw, there is always a main route table at the VPC level, which you cannot delete.Uh, if you create a subnet and do not create a dedicated route table for the subnet, your subnet wouldalways follow the main route table.Now you can create a custom route table at the subnet level.And uh, all the route tables within the VPC where there is a main route table or a custom route tablewould always have an immutable local route entry, uh, where the destination address is same as yourVPC Cidr address range, and you can modify even the main route table as well.That means if, uh, you know that all the subnets in my VPC want to have the same kind of network connectivity.So rather than creating an individual route table for every subnet and adding the same rules there,uh, it would make sense that you just modify the main route table and add all the routes there.Right.And then you don't need to create a custom, uh, route table at subnet level.So this is up to you how you want to design, uh, your VPC and the route tables.So again a few things you need to remember.Subnet has two terms I can say a public subnet and a private subnet.So the distinction is just about the route table.Basically if the subnet route table has an entry for Internet gateway uh it is called a public subnet.So typically you will have your Nat gateways or web server or load balancer inside the public subnetbecause they need to communicate with the internet directly.So that's a public subnet.And the subnet which does not have a direct route for the internet gateway is called a private subnet.So here I'm just showing a public subnet a subnet route table.Uh always there is a local route as you know, as per the VPC address range.And there will be IP address range and corresponding internet gateway.Similarly private subnet no route to internet instances receives only private IP addresses, and theyare typically used uh uh for, say, databases or application server, which need not be accessed frominternet directly.Right.So that's the purpose of private subnets.So it would only have by default the route uh for uh VPC destination.Uh, as you know, we cannot even remove this route.So that's essentially a public and a private subnet.So just last part of it, this very important that you remember this part.Any subnet that you create five IP addresses are reserved.And you cannot use those five IP addresses in a given subnet.Now if you create a subnet of Cidr say ten .0.0.0, slash 24, then first four addresses and the lastaddress in this subnet cannot be used.Uh, when I say cannot be used, that means you cannot allocate that IP address to EC2 instance, whichalso means in this subnet, uh, total IP addresses are 256 and five are gone.So you can only use 251 addresses.And that means you can only host 251 EC2 instances in this subnets.Maximum.Right.So you need to remember that the first address is a network address.The second address is uh reserved by AWS for inter VPC communication.Address plus two is for DNS.And we'll see that in a DNS lecture.Uh address three is reserved by uh reserve for a future use.And you know, in the network typically the last address is a broadcast address.And AWS does not support the broadcasting.And that's where even this address cannot be used.So remember that, uh, when you want to select the subnet Sidr, uh, prefix, for example, whetherit should be 24 or 20 5 or 26, you need to make calculations.Suppose you want 29 IP addresses, uh, in a subnet.Then you cannot, uh, choose the subnet size with uh, 27 because as you know, no, uh, it wouldmean that 32 -27 is five.So two raised to five.You would have 32 IP addresses if you create the subnet with prefix 27.And out of this 32 five IP addresses you cannot use, that means only 27 addresses would be, uh, useful.But you need 29 addresses.Right.And which means you need to create a subnet with uh, at least slash 26, uh, prefix.So if you create with slash 26, you get 59 usable IP address, actually 64 minus five which are reserved.So 59 and 59 of course is more than 29.So you need to create a subnet with prefix slash 26.So this kind of questions might come where given uh the requirement of number of IP address within thesubnet, you have to choose the minimum uh Sidr prefix.Right.So make sure you consider these additional five IP addresses.Right.You cannot use it.Now that means the smallest size subnet you create is slash 28.Right from the last lecture you might remember, uh, with slash 28, uh, you get two raised to four.That is 16 IP addresses in the subnet.And out of 16 five IP addresses you cannot use, that means only 11 EC2 instances you can create ina subnet with the Cidr slash 28.So these are just a calculations.And I hope now you can make those calculations.Uh, given a scenario okay.With this.Let's stop here for this lecture.In the following lecture we will see different types of the IP addresses.Uh, in a VPC, uh public private elastic.And also we will see IPv4 and IPv6 addresses.So thanks for watching and see you in the next lecture.