# 12.2 IPv4 and IPv6 traffic over AWS Site-to-Site VPN

Okay, so in this short lecture, let's quickly cover how AWS Site to site VPN support the IPv4 andIPv6 traffic.And we are talking about this because in your exam there might be questions around this.And also while doing the exercise you will see some of the terms that we cover in this lecture.Okay.So this is how typically the VPN works.As we said right on the left side you have either virtual private gateway or a transit gateway.And on the right side you have the customer gateway.And then there is a VPN connection between those two gateways.Now when you actually set up the VPN connection, you have to configure your VPN tunnels.And as we said, there are two tunnels for high availability.So there is tunnel endpoint one and there is tunnel endpoint two.And then you have to connect this customer gateway to this both the tunnels.Now in order to connect to this tunnel endpoints of course, you need to have the IP addresses assignedto this tunnel endpoints, and AWS will assign those IP addresses.And this will be public IP addresses because otherwise from customer gateway you cannot connect to AWS,right?So there should be some public connectivity for this tunnel endpoints.And then once the VPN is established then you can send traffic through those tunnels privately.This is how it actually works.So when you create this tunnel connection for that, as I said, you need public IPS and it is alsocalled as outside IPS.So you will hear this term outside IPS inside IPS during the exercise.So here we are talking about this public IPS which are the outside IPS.So every tunnel will have its own IP.And of course customer gateway also will have its own public IP which is the outside IP on the customergateway side.So with these two outside IPS you can create the IPsec VPN tunnel, right?So that's about the first tunnel.Similarly there will be another tunnel another outside IP.And then you create another VPN tunnel.So here just remember that there are these outside IPS which are public IPS on both the side.So basically when you create the virtual private gateways then AWS will automatically create these twoIPS and it will provide you as a part of VPN configuration.Okay.So that's about the outside IPS which is used to create this tunnel connection.However, in order to actually send the IP packets, these outside IPS are not used.These outside IPS are only used for the connection.That means at IP layer the routing happens.So for example from the corporate data center, if you have to route traffic then this IP address willbe used.But in order to actually send the traffic through this tunnel, there will be inside IPS on both sidesof the tunnel.And this inside IPS are allocated from the private IP address.Range 169.254.0.0 16.So if you talk about this first tunnel, then there will be a private inside IP, Cidr range on AWSsite and on the customer gateway side.And this range has to be with slash 30 prefix.So it can be 169.254.0.1 30.And this is just for an example.Similarly there will be similar IP range on the customer gateway side.And same for another tunnel as well.So once you have this inside IP addresses configured then actual IP packets will flow using this insidetunnel IP addresses.So the bottom line is in order to create the VPN tunnels, you need this outside IP addresses and foractually sending the traffic, you need this inside IP addresses.And that's where it really depends which type of IP addresses it is.Whether we are talking about IPv4 addresses for the outside IP or inside IPS, or we are talking aboutIPv6 addresses for outside IP or the inside IPS, because that decides whether this VPN tunnel willsupport IPv4 or IPv6 traffic.So overall you can see there could be four combinations.For example, all IPS are IPv4 addresses.Something like what you see on the screen or all IPS can be all IPv6 addresses, right?Or outer IPS could be IPv4 and inner IPS could be IPv6 or outer IPS could be IPv6, and inner IPS couldbe IPv4.Now all these combinations are possible when you create the VPN tunnels and depending on that, IPv4or IPv6 traffic is supported through these tunnels.So let's quickly look at those four options and their implications.So if there is IPv4 outer IP addresses which are also called as tunnel addresses and IPv4 for innerpacket transmission, then it's a default configuration of AWS site to site VPN.And it is supported on virtual private gateways and Transit gateway.And for AWS cloud Wan.Now we have covered AWS cloud Wan in the separate section.So just hold on until we reach there.But the point here is that this is a default VPN configuration and it supports IPv4 traffic of course.Now if you have IPv4 addresses for outer tunnel and IPv6 for the inner packets, then it allows IPv6traffic within the IPv4 VPN tunnels.Right.Which means actual traffic will be IPv6.And this configuration is only supported on the Transit Gateway and AWS cloud Wan.So it is not supported if you are using Virtual Private Gateway.Further, if you have IPv6 addresses for both outer and inner configurations, then it allows completeIPv6 traffic.So if your application is completely IPv6 based, that means there are no IPv4 addresses being used.Then you should use this configuration.Whereas if you are not using IPv6 addresses at all, then you should use this first configuration,right?And finally there is this fourth option where you have IPv6 outer tunnel and then IPv4 for inner transport.In that case it allows the IPv6 routing.That means your applications can be reached over IPv6 addresses, but tunnel can only support IPv4 traffic.And again, this is only supported on the Transit Gateway and AWS cloud van.Okay.While you can configure everything that we talked about.But it means that at any moment of time, either VPN tunnel will support IPv4 traffic or IPv6 traffic.So it won't support both IPv4 and IPv6 traffic at the same time.So just remember that.And if you want to support both the traffic through VPN then you have to create a separate VPN connection.Okay, so I hope it is clear how the IPv4 and IPv6 traffic is supported on AWS site to site VPN.And with this knowledge, now let's do an exercise where we will set up IPv4 tunnels from AWS to simulatedon premise network.So with that now let's go to the exercise.Thank you.