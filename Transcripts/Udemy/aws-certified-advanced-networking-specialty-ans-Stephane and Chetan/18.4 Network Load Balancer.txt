# 18.4 Network Load Balancer

So now let's talk aboutwhat you need to know from an exam perspectivefor the network load balancer.So here is the classic architecture of a NLB.So the NLB sits between two AZs.The clients are connected to the NLB.The NLB has fixed IP addresses, so it's connected to ENIswith static IPs.And then the ENI are going to send the traffic directlyinto our EC2 instances that are sitting in the target groupthat can span of course across multiple AZs.So the network load balancer operates at the layer 4,which is TCP or UDP protocols or Secure TCP so TLS.It's meant for really high performance,so it can handle millions of requests per second.And as you know, the NLB has one static IP per AZand you can assign an Elastic IPin case it is an internet-facing NLB,which is very helpful if you need to whitelist IP addressesfrom a firewall perspective.Now, load balancing is possible to be doneon multiple applications running on the same machine.So for example, containers with Amazon ECS,so it has the same port mapping feature as the ALB.There is less latency than the ALB,so we get a hundred milliseconds latency for the NLBversus maybe 400 milliseconds for the ALB.It supports the WebSocket protocoland you would use an NLB in these cases mostlyif you need extreme performance or you have a custom TCPor UDP traffic protocol.And in case, you're using also AWS private link.And this is used when you want to privately exposean internal service to another account in AWS for example.So target groups of the NLB can be EC2 instances,in which case these EC2 instances can be managed by an ASGor ECS tasks managed by the ECS service itselfor IP addresses and they must be private IP addressesfor TCP listeners only.So this is going to be for example, the on-premises serversthat are going to be connected to your cloudusing Direct Connect or a VPN connection.Also IP addresses can be for inter-region peered VPC.So this is a cool feature (indistinct) rememberin case you have a VPC in another region,it is possible to have it connectedto an NLB in another region using a IP address mappingand also peered VPCs.So an important point you need to know is thatyou cannot register your EC2 instances by ID on your NLBif these EC2 instances are in another VPC of your NLB.Even if that VPC is peered with NLB, this is not possible.Instead, you need to register these instancesby IP address directly using the IP address featuresof your target group.In terms of health checks,you have the supported protocol of HTTP, HTTPS and TCPand you have two types of health checks.The active health check will periodically send a requestto the registered targets,whereas the passive health check will observehow targets respond to connections and then they will...This is a feature for you that allows youto detect unhealthy instancesbefore the active health checks are being made.So you cannot disable this or configure it.Now, if we have a look at the architecture of your NLB,you could have multiple rules and listeners,but this works for both TCP and UDP traffic, okay?Why am I saying this?Well, because there is something very important to learnabout client's IP preservation.So this is the architecture we haveand we want to understand if the client's IP addressis going to be forwarded to targets or not.And this is something you have to remember by heart.So, if you are registering your instances by IDor within your ECS tasks,then automatically the client's IP will be preservedand your EC2 instances will see traffic originatingdirectly from your clients.If you are registering your targets by IP addressesusing TCP or Secure TCP, so TLS protocol,then by default the client's IP preservation is disabled.Now if you are using the UDP or the TCP_UDP protocol,then by default this is enabled.Now, if this is disabled, for example,for the IP address register registration for TCP and TLS,you can enable it, okay, by using the Proxy Protocol v2.and this is a setting you have to enableon your network load balancer.We'll see this later on in another lecture,but these Proxy Protocol v2 will add headers to the requestand these headers will be giving the opportunityfor your EC2 instances to understandwho was the originating clients.Okay, now in terms of mapping of listener and targets,we can have TCP to TCP or TCP_UDP;TLS to TCP or TLS and then UDP to UDP or TCP_UDPand TCP_UDP to TCP_UDP.Now, I haven't seen this to be helpfulfor an exam perspective, but I'm just leaving it out therejust in case this is necessary.So now some very interesting fact in networking regardingnetwork balancer and availability zones.So say we have a target group with EC2 instancesspread across two availability zones,then you need to know that you must enable an AZon the NLB itself before traffic is sent to that AZ.That means that if you only have an NLBwith Availability Zone 1 enabled,it will only send traffic to the EC2 instances in AZ 1.It will not send traffic to the second AZ,even if your EC2 instances are in the target group.So to have this NLB send traffic to AZ 2,you must add or enable an AZ and this can be doneeven after the network load balancer is created.Very important because the exam will ask you this.Also, you cannot remove an AZ after it is enabled, okay?You can add over time, but you cannot remove.If you wanted to remove an AZ,you would have to recreate a new network load balancer.Now you may say, what about Cross Zones Load Balancing?How does that work?Well, they only work for the AZsthat are enabled on the NLB.So now if we consider three availability zonesand target group with EC2 instances in all AZ,then what's going to happen is that say we enabledAZ 1 and AZ 2 with our NLB,then thanks to Cross Zones Load Balancing,the traffic can go from AZ 1 to AZ 2 at the NLB level.But again, because we have not enabled AZ 3,then even if we have Cross Zones Load Balancing,it's not going to send traffic to AZ 3because it is not enabled.So if you understand this,you understand the key networking conceptson the network load balancer.Now another one is around Zonal DNS Name.So if you resolve what's called the regional NLB DNS name,which is the DNS name you would use by default,then it's going to return all the IPsfor all the NLB nodes in all the enabled AZs.So this is your NB DNS name.And so if we have the NLB here enabled in three AZand we do a DNS query for this NLB DNS name,then we're going to get three IP as a return,which will correspond to the three AZ that are enabled.But sometimes you may want to resolve the Zonal DNS nameand the idea is that the NLB has DNS namefor each of their nodes so based on the available zoneand using this Zonal DNS Name,we can resolve only one IP address,which is the IP address of each node.So for example, if you wanted to knowwhat was the IP address of the node in US-east-1a,then you would add US-east-1a, to your DNS name,and then it would return only the first IP.So why would you do this?Why would you want to resolve a specific nodewhile this could be, for example,to minimize latency or data transfer cost?For example, say you have an applicationand it's deployed on your VPC in three different AZand it's going to then talk to an NLB.Then you absolutely want, for example,your instances in US-east-1a,to talk through the NLB node in US-east-1a.Because there is going to be no data transfer costand lower latency.So in that case, you would resolve the Zonal DNS namefor your NLB and only find the one IPthat will ensure you don't have any networking cost.Because if you use the regional NLB name,then there's a chance for youthat your application sends traffic from one AZto the other one, therefore incurring some cost.So to make sure your application sends trafficto the right NLB node,you need to implement application specific logic.Your application needs to understandin which AZ it is operating fromand then how to query the right DNS name for your NLB.Okay, so some things are good to knowfor your network load balancer.You cannot disable or remove an AZafter you create it for your NLB,and you cannot modify the ENIscreated for the NLB in each AZ.You can only view themwhile this is because we have a fixed IP.You cannot change the elastic IPsor private IPv4s attached to the ENIsafter you create the load balancer.So that means that everything is staticfrom the moment you create your NLB.Your NLB supports 440,000 simultaneous connectionsper minute per target.If you go over that, you will get a port allocation error.And the solution to this is to just add more targetsto the target group.Finally, and this is very, very importantfrom a networking perspective.So in case, you have an internet facing load balancer,then the subnets for you to deploy this intomust have at least 8 IP addresses that are available.So that means that your subnet sizemust be a minimum of /28.In case you're using an internal balancer,this is only relevant if you let AWS selectthe private IP addresses from these subnets.So remember, 8 available IP addresses for the NLB,which is a /28 CIDR for your subnets.So that's it. I hope you like this lecture,and I will see you in the next lecture.