# 18.5 Connection Idle Timeout

Now, we're going to discuss all the settings relatedto the load balancers, and the first one is going to bethe connection idle timeouts.So, this is a timeout that happens in two places.The first one is between the clients and the ELB,and the second one is between the ELB and the targets.So, the connection will be closed for the ALB for example.If no data has been sentbetween the clients and the ALB for 60 seconds.And same, it will be closed if no datais being sent between the ALBand the EC2 Instance for 60 seconds.If data is being sent, obviously,maybe just as little as one bites,then the connection will be kept open.And this may be more efficient,especially on the ALB to the EC2 Instance side,because connections can be then reused.So this setting applies to the CLB, the ALB and the NLBAnd it can be configured for the CLB and the ALB.By default it is 60 seconds,it cannot be configured for the NLB.It's going to be 350 seconds for the TCP connectionsand 120 seconds for the UDP connections.So you would tweak that setting the connection idle timeoutfor when you have, for example,file uploads and you want to avoid at timeouts.You need to make sure that some data thatis always being sent in your connection.So also it is very recommended to enablethe HTTP keep-alive settingfor your web server of your EC2 Instances.Which makes the EC2 Instance keep the connection aliveto the ALB and not hit that connection idle time out.So that's it for this short lecture.I hope you liked it and I will see you in the next lecture.