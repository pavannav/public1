# 16.8 VPC lattice section summary

Hi.So quick summary about the VPC lattice section.So as you know, VPC lattice enables service to service communication across VPCs and AWS accounts.And that's where it eliminates the need of having the complex networking using say, VPC peering ortransit gateways or VPC endpoints or Privatelink.Now with respect to the target, it supports EC2 IP addresses, ECS or EKS behind the Alb or NLB andthe Lambda functions further like the application load balancer.It also supports different dynamic routings.So you can have the path based routing, the method based routing.Or you can have weighted routing.So all those conditions you can write in the listener rules.Further VPC lattice also supports the service discovery.So it provides its own domain name.But again you can use route 53 to have your own custom domain name.Now, with respect to the association of the VPC lattice, we saw that there are four types of the associationthe service association.The resource configuration association.The VPC association and the VPC endpoint association, and out of that.VPC endpoints allows the outside network to access the VPC lattice.Service using some ingress VPC.So you should first create that VPC endpoint in the ingress VPC.And you can then connect other networks to that VPC.And then they can access the VPC lattice network.And further with respect to the authentication it supports the IAM policies.So you could have the IAM policy applied at the VPC lattice network level or the service level.Right.And by default the policy is none.So the authorization is allowed.But if you have enabled the IAM authentication then by default the access is denied.In that case you must have the allow permission for the user or the client to access the VPC latticenetwork or the services.Right.So that's about the VPC lattice.I hope you enjoyed this section.So thanks for watching.And with that now let's go to the next section.Thank you.