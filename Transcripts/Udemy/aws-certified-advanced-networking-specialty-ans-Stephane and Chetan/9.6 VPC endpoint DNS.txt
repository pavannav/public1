# 9.6 VPC endpoint DNS

Okay, now let's look at another important thing about VPC endpoint that is VPC endpoint DNS.And it is really crucial to understand how DNS works for VPC endpoints okay.So let's get started.Right.So when you create the VPC endpoints basically AWS assigns a DNS name for that endpoint.And that DNS name resolves to the private IP of that Eni that this endpoint creates.So in the consumer VPC, basically you can also access the VPC endpoint using that DNS name.Now in this case AWS creates two different types of DNS.One is a zonal DNS and another type is regional DNS.Zonal means per AZ.It creates different DNS.So if you create the VPC endpoint in two AZS, then there will be two zonal DNS, and there will bethen regional DNS.And it will provide the IP addresses of those DNS across all the availability zones.Right.So there is this kind of regional DNS.Now this is for example is for Athena service endpoint.As you can see here, there is US East one.So that's a regional one.But additionally it creates the zonal one.As you can see here US East one A that is in one of the availability zone.Similarly if you have two AZS then there will be additional zonal DNS.Now the way it works is from your client which is sitting in consumer VPC.It need not send the traffic to the IP address.Rather it can use this DNS to reach to the VPC endpoint.And from that endpoint, traffic will go to the target service.That service could be AWS service as we are talking about.Or it can be your own service deployed in another VPC.Right.So that's how the DNS work.And optionally you can also enable the private DNS for the endpoint.Now what does that mean.So let's take an example that you are creating the VPC interface endpoint for, say, SQS service,right?So it ultimately takes you to the SQS service endpoint, which AWS manages.Now without private DNS, you would have to use this regional or zonal DNS to reach to the SQS service.But if you enable the private DNS, then you can use the same public DNS name of the SQS service endpoint,and it resolves to the private IP address of the endpoint inside your VPC, right.So that means your client does not need to change anything.They can just use the normal CLI or API call, and it will be resolved to the private IP address ofthe VPC endpoint.And it will go to the SQS service privately.And for this to work you have to enable the DNS host name and DNS support for your VPC that we alreadylearned in the DNS section of the VPC.So let's now try to understand this more Diagrammatically.So assuming that you have a consumer VPC and you want to create the VPC endpoint for accessing Kinesisservice.Right.So you will create a VPC endpoint for Kinesis.And it will create an Eni.And it will have some private IP address from your VPC.Now at this point, if this client, which could be EC2 instance, may want to access the Kinesis serviceusing the AWS CLI or SDK or API call.Right.And in that case it will use Kinesis Service endpoint.Right.So in this case let's first see when you disable the private DNS for the endpoint what happens.Right.So private DNS disabled.And in that case you know that there will be zonal and regional DNS created for these endpoints.So now if client makes an API call or CLI call to the Kinesis service, first the DNS should be resolved.But because it's a public DNS for Kinesis service, it will return the public IP of the Kinesis Serviceendpoint.Right.And traffic will go over other internet.And if your VPC does not have internet gateway, this traffic won't even go to the Kinesis service.So that's the problem when you, you know, disable the DNS and you are not using the VPC endpoint specificDNS.So this is just like accessing the Kinesis endpoint with a public DNS right.So this won't work.So with private DNS disabled the API or CLI calls to the AWS service endpoint resolves to the publicIP.And this is what we want.We want that we get the private IP of this Eni.And then traffic goes through this private IP to the Kinesis.Now for that what you can do instead of using the Kinesis Public endpoint DNS, you can use this VPCendpoint DNS to connect to the Kinesis service.So now your EC2 instance will make a DNS query.But for this particular DNS right.And it will resolve to the private IP of this VPC endpoint, and then traffic will go to the Kinesisprivately.So this works right.But this means that the application that you are running inside your EC2 needs to now use custom DNSto reach to the Kinesis service.Right.So that's kind of not desirable.So we can use the endpoint specific DNS name.It resolves to the private IP.But this is not desirable.What is desirable is that my EC2 instance or application shouldn't be worrying about, you know, usingthis DNS, but rather should use normal Kinesis DNS.But it should resolve to the private IP of this Eni here.So how do we do that?So for that we have to enable the private DNS for Kinesis service.And as you do that what AWS does it creates another private hosted zone in route 53 with the same nameas Kinesis Service Endpoint.Because it's a private hosted zone and it associates that with your VPC automatically.Now, as this happens now your client can make a request for the Kinesis Service public endpoint.It first resolves to the VPC endpoint specific DNS.And then again it makes a query and it resolves to the private IP of VPC endpoint.Now your client can reach to the Kinesis service over this private link connection.So this is what it means when we enable the private DNS for the VPC endpoint.So with private DNS enabled application inside VPC using API or CLI or SDK do not need to change anythingfor accessing AWS service.But otherwise if you don't want to use the private DNS enabled, then if you are using aws CLI, youhave to append the additional parameters which says endpoint URL.And then you would have to provide this particular VPC endpoint specific DNS.And we will talk more about that as we do an exercise for this scenario, but otherwise I hope it isclear now how the DNS work for VPC endpoint.And also remember that there is original DNS and there are zonal DNS for every AZ.Okay, so let's quickly summarize.With private DNS enabled, the public hostname of AWS service resolves to the private endpoint interfacehostname, and then it creates the private hosted zone associated with your VPC.And then it resolves to the VPC endpoint DNS.And finally it resolves to the private IP of your Eni.And for all this to work at consumer VPC level, the enable DNS hostname and enabled DNS support mustbe true, right?So just remember this.And with this knowledge we are soon going to do an exercise for this scenario so that you can experienceit right.So we'll stop here for this lecture and let's go to the next lecture.Thank you.