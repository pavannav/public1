# 23.1 VPC Sharing

Hi.Welcome to this lecture that is VPC sharing.Now we have added this lecture recently because as per students feedback, we found that there couldbe some questions which are related to the VPC sharing.Now so far we talked about various networking features, for example VPC peering connections or transitgateways.But then there is also this feature of VPC through which you can share the same VPC with other AWS account.And then those AWS accounts can launch their resources in the same VPC.So in this lecture let's try to understand all the features of VPC sharing and then when to use thisfeature.Right now another thing to note that it is AWS organizational feature.That means VPC sharing can be enabled only within the AWS accounts which are part of same AWS organization.So multiple AWS account in an AWS organization can share a VPC to launch their own resources.So account B can launch EC2 instance in the VPC, which is created by account A that is the VPC sharing.Now in this case, there is the account which owns the VPC called as owner, and there are other accountswhich share the VPC are called the participant accounts.Now, in order to enable the VPC sharing, you have to enable that feature at the AWS organization level.And you can do that through AWS Organization Management account.And I will shortly show you how to do that.Once you enable this setting, then in the owner account you can create your VPC and additional resources,for example subnets.And then using the Resource Access Manager that is Ram.You can share that subnet with other AWS accounts.So for that you create a resource share.And once you create this resource share, you can share it with other AWS account or different organizationunits within the organization or with entire AWS organization.In that case, that VPC or subnet will be available for all other AWS accounts.So you can have this level of sharing within your AWS organization.And finally, important thing to understand that you cannot share the default VPC.So you know that in all AWS regions, AWS creates a default VPC.But then VPC sharing cannot happen with the default VPC.For that, you have to create your own custom VPC, and then you can share your VPC with other accounts.Now essentially that's the crux of this lecture.So if you just understand this I think this is good to answer a lot of questions.But here let's try to understand more about VPC sharing architectures and some pros and cons so thatif there are any deeper questions you can answer those questions.So with respect to the architecture, you could have multiple AWS accounts which are part of same AWSorganization.And here you can see that in every AWS account there is a dedicated VPC.But with VPC sharing you don't need to do that.Rather, you can just have one VPC in the owner account.And then you can additionally create the subnets which are shared across all these accounts using theResource Access Manager.Now all these three accounts can launch their resources, for example EC2 instances or RDS databasesor redshift cluster in the same private subnet.So you don't have to have VPCs in other AWS accounts.But the question is what's the benefit of this?Right.So of course, the benefit is you don't have to have those many VPCs and those many subnets in all accounts.And there are additional cost related benefits as well.For example, here, instead of having three Nat gateways in every account, you just have one Nat gateway.Similarly, there could be other resources.For example, you could have VPC endpoints and any other resources which you can now share with allother AWS accounts.So cost wise as well, this is a good approach.Now quickly, what are the benefits of sharing the VPC?As I said, it simplifies your design.You don't need to have those many VPCs in every account.So fewer managed VPCs.It avoids the Cidr overlap problem.It also provides you some segregation of duties.For example, the owner account could be owned by the networking team and all the development or applicationteam can share the same VPC.So networking team controls what resources needs to go there and which subnets to be shared with theapplication accounts.And it gives you better control over your IPv4 addresses.And you fully utilize your Cidr ranges because you don't have to allocate Cidr range separately in theparticipant accounts.Finally, you can have the reuse of your resources, for example Nat gateways or VPC endpoints, which.Saves you a lot of cost.And finally, if you design your architecture such that in all the AWS account, you use the same availabilityzone when you share that subnet.In that case, you also avoid the inter AZ data transfer cost.And I'll quickly explain about this in more detail.So these are the benefits of sharing the VPC.Now with respect to the architecture there are typically two patterns you will see when you share theVPC.So here as you can see there are four different accounts.And there is an owner account here which has the Nat gateways in a public subnet and then some VPC endpoints.And now you want to share this with all other AWS accounts.So what you can do is you can create private subnets inside the owner VPC.And then this subnets can be shared with all other AWS accounts.So this is kind of one of the approach where one subnet is shared with all other AWS accounts.So there are pros and cons of this approach.Of course the pros is that you are using your IP addresses effectively.Right.But there are some cons.For example noisy neighbor problem.What if this account D launches more resources in the same subnet and it exhausts all the private IPaddresses?In that case, the other accounts will not be able to launch their resources.So that's called noisy neighbor problem.Similarly there is a higher blast radius.So say there is some problem with the route table.Somebody removes the route table entry for this private subnet.In that case all the resources in this subnet will be affected at the same time.So higher blast radius is there.Right.So another approach is instead of sharing the same subnet with all the accounts at the same time, yourather create a dedicated subnet for every account.So this will look something like this.Now for account B you are creating a dedicated subnet.Similarly you are doing for account C and account D.Now there is no problem of the noisy neighbor and you have better control over this subnets.But again there is some problem which is like low IP address utilization.Right.Because you have to create as many subnets now and every subnet would need their Cidr.So there is always a trade off whether you go with the first design or the second design.But you just should know that there are these different patterns through which you can share your subnetsacross different AWS accounts.So I hope that is clear right now.I had also talked about some inter AC data transfer costs.Now when you create the subnet share, basically you have to make sure that you are using the same availabilityzone across these different AWS accounts.Because if you don't do that, then if there is any data transfer between these resources across differentaccounts, there will be data transfer cost because they are crossing the AZ boundary.And for that, there is one important thing to understand.Whenever you talk about the AZ, for example, US East one a this AZ may be different for another accountin the same region.Now what do I mean by that?So here for example, this is US East one region.And if there are three AZS.So in account a US east one A could be this AZ.But for account B US east, one A could be a different AZ.And why AWS will do that.This is because probably AWS want to make sure that the resources are created evenly across all theAZS.Otherwise, if all the customers of AWS will launch all their resources in, say, US East one A allthe time, then this will be the populated AZ, right?And that's where this mapping is created.So with respect to the VPC sharing, if you are sharing your subnet with other accounts, you make surethat you use the same AZ ID rather.Right.So along with the AZ name there is also AZ id.Now this AC id is constant across different AWS account.They mean the same AZ.So always look at the AZ id instead of AZ name when you share your VPC.So that is one thing that you should know while you share your subnets.So this avoids that inter AZ data transfer cost.Now just to show you again there is this Nat gateway and there is this private subnet.Now all these instances are using the same Nat gateway for the egress traffic.And if this Nat gateway is in different AZ then this resources here.Then all these accounts will pay for that data transfer cost across AZ.So if this Nat gateway is in this acid, then all these resources should also be created in the sameAZ it.So that's where it will save you inter AZ data transfer cost.So I hope that is clear.Right now you might have a question around is it a replacement for Transit Gateway.So.Answer is no.Actually, the purpose is different here, right?You can use both VPC sharing and the transit gateway at the same time.For example, in a given business unit you can just have one VPC.And all the development teams can use the same VPC.But when it comes to communication between these business units at that time, you can connect thesedifferent VPCs using the transit gateway.Likewise.And further, this transit gateway can be extended to your on premise network over a Rd or, you know,VPN connection.So you can use all these features at the same time.So this is just one kind of architecture that you can design using both VPC sharing and the transitgateway.Right.So I think those are the essential things that you should know for your exam.And additionally I have also provided some kind of permission models as in what permission the VPC ownerhas and what permission VPC participants are.So I'll just recommend that you just go through this list once so that if any questions come aroundthis, then you know the answer.Similarly, there are billing related things that you should know.For example, who pays for what.And typically data transfer cost will be paid by the participant and the resource cost.For example, Nat gateway per hour running cost or VPC endpoint per hour running cost will be paid bythe VPC owner.And there are some best practices with respect to.If you want to control with which account you can share this resource or not.You can do that using the AWS organization controls, for example, service control policies or alsocalled as SCP, and also using the Resource Access Manager.So these are few additional things that you should be knowing.Now, before we wrap this lecture, I just want to quickly show you in AWS console where you enablethe resource sharing.So let's quickly do that.So I am in my AWS management account of AWS organization.And then I have multiple accounts.So what you have to do is you have to go to Resource Access Manager.That is Ram.And here there are settings for example here and it says enable the sharing of the AWS organization.And you have to change this setting.Now once you do that then you have to go to your main account.So this is my main account for example.And I go to my VPC.So this is my management account.And then I have few VPCs.So I have to choose the Non-default VPC.For example this demo VPC which I might have created earlier.And then I can say share the subnet.Now so far we haven't created any sharing.So we have to use the resource share which takes us to the AWS Resource Access Manager.And here I will say share my subnet.That's the name of my subnet.And then what are the type of the resource.So it's a subnet.And then which subnet we want to share which is this.And within that subnet if there are many resources for example if you select EC2 there could be additionalthings that you want to share.But here it is just a subnet.So we have to select that and say next.And here what all the actions that you want to allow other accounts to do on this subnet.Right.So as of now we are saying that we'll go with the default.And here do you want to share with account or a particular user in the account.So we want to do that with particular AWS account within the organization.And you can also select the organization structure.So I have this another AWS account within the same organization.And I want to use that principal ID to share my subnet.So I say next and I just create the resource share.Now this will take maybe a couple of minutes for this resource share to be created.And then I should have this subnet available in my target account which was this account.So I'm pausing this video for a minute and I'll come back.Okay, so I am now in my different account.You can see this account number.And now if I go into the same region that is Mumbai I see two VPCs.Now I to be honest did not create this second VPC.And you can also see this account owner which is my management account, which means I have also accessto now this another subnet which comes from this this account.Right.So this VPC and it clearly also says shared here right now if you go to the EC2 instance then you canselect that VPC and this subnet to launch your resource.And this is how you create the subnet share.So I hope that is clear.Now with that we'll stop for this lecture.So the point is you can share your VPC with other AWS account.And it provides you a lot of benefits like right usage of your IP addresses.And you can also save some cost with respect to the resources like VPC endpoints or Nat gateways.And of course uh also intra data transfer charges.Right.So that's it.Thanks for watching and stay tuned.