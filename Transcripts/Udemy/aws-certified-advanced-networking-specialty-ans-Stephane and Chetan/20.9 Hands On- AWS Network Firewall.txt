# 20.9 Hands On- AWS Network Firewall

Hello, and welcome to this lecture.Now that you understood how AWS network firewall works,let's try to implement this on our own.And you know that network firewall works differentlyand you have to modify route tables for your subnetas well as for internet gatewayand all these we'll see in this lab.So what is the objective of this lab?We want to host a web server on EC2 instance,that's the basic requirement.And then using the network firewall,we want to implement few rules.For example, we want to add stateless rule blockto block the ICMPthat is a ping requests to our web server, right.And then we also want to add the stateful rulesso that from my web serverI can only go to a certain domain namefor example, aws.amazon.com, that's my outbound request,and that's to only using the TLS connection.So I shouldn't be able to usea plain HTTP outbound connection to this domain nameand then all other traffic should be blocked, of course.Now this scenario is based on the AWS block, which is here.I just slightly modified this requirement hereand accordingly I also modified my rules.So I'll talk about that as we proceed with the labs.So this is high-level requirement.And now if you look at the architecturethat we want to build to achieve this,it should look something like this, right.You should have a VPC, in that VPC you would have to subnetsin one subnet, which is here, it's a public subnetin which you would have public web server.And then there will be a firewall subnetin which you will provisionthe AWS network firewall endpoint.And there will also be internet gateway, of course.Now,if you look at the route tables for this three components,of course, for the public subnet,all the outbound internet trafficwill go through the firewall endpoint.Now, in case of firewall endpoint,all the traffic will go through the internet gatewayand for the internet gateway ingress trafficfor the web server, the traffic should also be routedthrough the firewall endpoint.And this is an ingress trafficbecause your internet gateway will do the knotting.It will map the public IP of FIPs over to the private IPand this private IP is from this subnetand that's where you see this entry.So at high level,these are the route tables that you need to configurefor your network firewallto be able to intercept that traffic.So I hope that it's clearand now let's understand which rules you want to add herein case of stateless and stateful rules.So for stateless,I will add one rule which will drop the ICMP traffic,and then rest of the traffic,I want to route through the stateful rule groups, right.Because then I want to take decisionin the stateful rule groupswhere I want to allow the TCP traffic or not.And here in case of stateless rules,I will attach the priority as 10 and 20so that the first rule that is a drop trafficwill be evaluated first.Now, in case of stateful rules, I have added four rules.The very first rule is for allowing the SSH,that is because from this web server,we want to test the outbound connectivityso I also want to SSH to this machine.Second, I'm only allowing the HTTP traffic,that means I have a web serverso I should be able to reach to web server over port 80,that is an HTTP traffic,and I should only be able to access my web serverusing the AWS provided DNS name.And that's where you see that,you know, the DNS name ends in dot amazonaws.com.That means I cannot access my web serverusing the public IP of the web serverbut only through the DNS name.Third, I will add this entrywhich allows the outbound request from this web serverto only the domain name that is aws.amazon.com.And additionally, the traffic will only be the TLS traffic.That means I cannot use plain HTTP traffic or a TCP trafficto reach to this aws.amazon.com domain name.And finally,I have the rule which is drop all other TCP trafficso that there is no other trafficthat can flow in and out of my web server.Now you'll be wondering if that is the casethen how you are allowing the, you know,this HTTPS traffic to the aws.amazon.com.Because when the PCP handshake happens at that moment,the traffic is not DLS, right, it starts with TCP.Now, in that case, this drop rule should be appliedand your traffic will be stopped.But for handling this use case,we have added this flow.established as a parameter,which allows the traffic to be droppedonly when it is not an established traffic,that means for this TLSthe TCP handshake has already happenedand that's where it won't use this routeto drop that traffic.But any other traffic, which does not meet this requirementwill be dropped by this fourth rule.So overall, these are our requirements for firewall rules.Now, if I want to set this up,I need a basic setup to start with, right.That means I will need a VPC,I will need EC2 machines and all.Now, if you follow AWS blogs,everything can be set up using a cloud formation template,but here I have done that manually.So if you want to start, this is very simple.There is no AWS firewall, right,I'm just creating two subnets and one subnet would be publicin which I launch a web serverand then there will be a route tablewhere the traffic goes to the internet gateway.So there is nothing fancy here, right,it's all, you know how to create this setup.Just remember one more thing that enabled the DNS hostnameand DNS resolution on your VPC,because otherwise AWS won't locate the public DNS namefor your EC2 instance.So in order to handle that,please enable these two settingsbecause in our firewall rules, as you have seen,we are allowing HTTP traffic only for the web serverwhere the DNS name ends in amazonaws.com.Okay, so for simplicity,what I've done is I already created this VPCand this subnets and internet gatewayand I'll start from there.Now, once I have that set up,I will just SSH into this web serverbecause there is no web server, as of now,it's just an EC2 instance,and I will install the HTTPD web server thereand we'll start the HTTPD service.Now by launching EC2 machine,I also need to provide the security group,but I will just allow everything in the security groupso that I know that traffic is not really getting blockedat the security group leveland I'm not touching the network ACL as well.Now then verify that you are able to access the web serveror a browser using the EC2 instance IPand our public DNS.Then also verify that you are able to ping to EC2 instanceusing the public IP or the public DNS.Now, this is a simple setupwhere there is no network firewall.Then we will create the network firewallin its designated subnet.And then we will also addcorresponding stateful rule groupsas well as stateless rule groups.Finally, once the firewall has been added,we need to modify the route tables, right.As I said to you, a route table has to, you know,take all this traffic through the firewallwhether, you know, going to the internetor coming from the internet.And finally, once that is done,we will test the ping to the web server from local machine,it shouldn't work.And then we'll SSH to the web server,which should be successfulbecause we have added that in our stateful rule.And from there, we will try to get some datafrom say, aws.amazon.com or HTTPS, which we should succeed,but any other domain names shouldn't work, right.That would be our final test.So if this works, that means our firewalls are working well.Now, as I said, for route tablesyou need to have the route table,something like this for your public subnetwhere your web server site,you will have the route table entry.You'll say is all the internet trafficwill go through the firewall endpoint,for firewall subnet, all the traffic will be routedthrough the internet gatewayand for internet gateway,any traffic that is going to your protected subnet here,it will go through the firewall endpoint.Now,as of now we haven't seen the route table for the internet.Now this is an ingress route table for the internet.And how you do thatis you have to first go to the route table,you have to create the route table,and then when you associate the route table,there is an additional entry there which is age association,and at that moment, you can sell it internet gateway thereand then that route tableis associated with the internet gateway.Otherwise, if you go to the AWS consoleand go to internet gateway,you don't see an option to add a route tableto the internet gateway, right, so just remember that.So with that, let's get startedand now I'll just jump to my console.Okay then,you can see that I already created this demo VPCwith DCIDR 10.0.0/16.And in this, I created two subnets,one subnet for the firewall and one subnet for the web.And this CIDRs are 10.0.100.0 and 10.0.0.0.Now, for the subnet for the web serverI just modified the route tableto add entry for internet gateway and this is very simple,I'm not adding the firewall as of now, as I told you, right.So it's a public subnet,and I haven't touched this firewall sub-net as of now.And then if you see the route tables,I have two route tables, one for the web serverand I have the route to internet gatewayand then I also attached to the corresponding subnet.Now for the firewall, also I created a route tableand I just also need to attach itto the firewall subnet here.So I'm just doing that.Now, I'm not really launching anythingin the firewall subnet as of now,so it doesn't really matter,but the setup is really very simple.Then what I additionally did is I launched a web serverinside my VPC in the web server subnetand I associated the public IP as well.And on the VPC level, I also have the enable DNS resolutionand the hostname setting stepswhere it also got this public DNS.Now, what I did is I just connected to this instanceor an SSH and now I'll just install the web server.So let's do thatand then also let's start the web server.(keyboard clacking)And finally, let's just create one dummy fileso that we can access this web serverand some web pages displayed.This is my web server,that's it.And now if I access this web server or an IP address,it should work.Right, it works.And similarly, if I get it public DNS name,it should also work.So using this DNS name I can also access my web server.Okay, so that's the test that we did.Now, our setup is ready with the starting pointand now let's get into the, you know,mood where we want to set up everything else, right.So we want to create now firewallsand corresponding group tables and everything.So let's do that now.So I'm going back to my console and AWS network firewallis part of the VPC console, so let's go there.There's already one firewall, I'm not going to use that,so let's create a new firewalland let's give it a demo as the nameand VPC, we want to choose the one that we created,which is demo one, the easy, I want to just have one easyand the subnet we have 10.0.100.0, this one.Okay, and we want to create a firewall policywhich is empty.To start with, let's give it a name demo firewall policyand also just let's change the name to demo firewall here,that's it and let's create the firewall.So it will take a couple of minutesfor this firewall to be ready,by the time I'm just pausing this video.Okay, you can see that the firewall status is readyand now let's go to the network firewall rule groups.And there're older rule groups, I'm not going to use those.Let's create the stateless rule group,and let's call it demo stateless.And you have to mention some capacity,how many rules you want, so let's keep it 100.So you can add a priority, as you know,the lowest the prioritythe rule will be evaluated in that order.So, first we want to have the rule with ICMPand here let's give the priority as 10so that we can insert a rules later as well.And the source is anything and the destination is anything.And I want to drop all this traffic, right.So I'm just adding this ruleand then I'll add another rule, which say priority 20and all protocols for any IP address,I want to just forward this trafficto my stateful rule groups, and let me just add that.So just this two rules for the stateless rule groupand we are just creating that now.And now also let's create the stateful rule groups,and in that case, let's select the stateful,let's call it demo stateful, define the capacity as 100.Now, in this case, we are not using the 5-tuple format,we are not using the domain list,but we are using the Suricata compatible IPS rules.And you can grab this from the downloadable contentor you can also just type it here.So let me get this rules from my presentation.So I have this four rules hereI'm just copying those and I'll just put it here.Okay, you can see that first rule is about allowing the SSH.Second is for allowing the HTTP trafficso that I can access my web server or the browser.And third rule is for connecting to aws.amazon.comfrom my web server.And fourth is to drop anything else,which is our TCP traffic.So let's create this and now I have this two rule groups,but understand that as of now,we haven't added these rule groups to our firewall policy.So let's go to the firewall policyand let's add this two rule groups.So the first one is the stateless, so let's select that.And similarly, let's also add this stateful rule group here.So I'm just adding the demo stateful rule group here.Okay, so on the firewall side, we are good,everything is set up and now what's remaining, if you know,is our route tables, right,we need to modify all these three root tableso that traffic can flow through this firewall endpoint.So first let's modify the route table for a public subnet.Now, in this case, all the traffic to internetshould go through the firewall endpoint.Now, for that, we need to first get the firewall endpoint.So if you go to the firewall and go to it's details,you will find the firewall endpoint there,and we will need this firewall endpoint for our route tableso I'm just copying this firewall endpoint.Right, now let's go to the route tablesand the web route table, routesand I'm just now modifying this.Now this is a public subnet when we created earlier,but here instead of internet gateway,I want to use my firewall endpointand just save the changes, so that is done.Now for the firewall route table,I want to have all the trafficgoing through the internet gateway, right.So let's do that and save the changes.If you're confused, just look at this architectureand you should be able to understand what I'm doing.Now, the third thing which is new to youis having the route table for the internet gateway.Now, in order to do that,let's create a new route table first.So let's go to the route table and create a route table,say demo igw route table in the VPC,and just create that route tableand then as in routes, we will again add,if the traffic is going to the web server subnet,which is $10.0.0.0/24,again it has to take the route through the firewall endpointand save the changes.But as of now, this route tableis not associated with the internet gateway.In order to do that, as I said,you need to go to the age associationand then you need to attach this to the internet gateway.So remember, this is important step, right,and save the changes.Okay, so I think we are done with the architecturethat we want to try.Now, let's verify what's happening here, right.First of all,let's verify if we are able to access the web serverusing the IP address.If you just refresh, this is not working.Now, this is because in the stateful rules,we said that only HTTP traffic for the domain namewhich ends in amazonaws.com should be allowed.Now, if I refresh this page, ideally it should work,I can also test this like this and this is working, right,that means our stateful rule groups are working.Also let's verify if ping is working or not.We had blocked all the ping requestsso let's go to the command promptand let's say ping to the IP address and it is not working.Also, let's ping to this public DNSand you can see this is also not workingbecause in a stateless rule groupwe have defined that block the ping request.Now, one more thing we want to tryis we want to log into this website or our SSHand then we want to see if we can reach to, you know,aws.amazon.com or our TLS connection.So let's go to this session,I'm just going to duplicate thisand you can see I'm able to log into the web server or SSHthat's because I have allowed thatin our stateful rule groups.Okay, so I'm here, now let's try to do something like thiscurl, first let's try, http://aws.amazon.com.Now, even though it's an aws.amazon.com DNSthat we have white listed in those stateful rule groupsthis one work,because we said the condition has to be the TLS.That means it should be an HTTPS connection.And if I do that, I get the page of aws.amazon.com.And at the same time, if I do something different,maybe let's try to do https://google.com.So it won't workbecause we are white listing only the domain namethat we are interested in.So this is what we wanted to achieveand I think this is working perfectly as we expected.And that's it about the Network Firewalls.So I hope you enjoyed this lectureand you understood how these things work.Now, make sure that when you do this exercise,you delete all your resourcesbecause everything is charged, right.There is an EC2 instance, there's a network firewalleverything has a price.So make sure you delete all those resources.So thanks for watchingand stay tuned for more exciting stuff.Thank you.