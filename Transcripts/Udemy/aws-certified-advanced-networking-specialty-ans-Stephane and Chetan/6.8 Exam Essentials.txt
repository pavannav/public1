# 6.8 Exam Essentials

Okay.So with respect to the network performance, there are a lot of things that we saw.And let's try to remember what you need to know for your exam.So as an exam essential understand that the MTU values first of all what is MTU.And then the various MTU values within the VPC.And typically when the traffic flows out of VPC.So if the traffic stays within the VPC MTU size of 9001 byte, which is also called jumbo frames, aresupported or otherwise if the traffic is going over the internet or if it is over the site to site VPNconnection, which terminates on the Virtual Private Gateway or the Transit gateway, the jumbo frameis not supported and the MTU size is limited to 1500 bytes.Now, when it comes to the VPC peering connection, the Inter-region VPC peering supports jumbo frameof size 9001 bytes, but for the Inter-region VPC peering it is 8500 bytes.Now, understand that when the packet per second is a bottleneck, that means your CPU is having thelimited capacity and it can't process more packet per second.In that case, increasing the MTU would give the better throughput because you are now processing thelarger packet at once.Right.So that's one optimization technique that you can apply now while using the enhanced networking andplacement group.Ultimately, it lowers the latency between the EC2 instances and the hypervisor.But if you are using Dfdc in that case it improves the packet processing at the operating system level.So understand the difference between enhanced networking and the DP DQ.So whenever dp DQ understand that its OS level optimization.Okay.Moving on.If you talk about the network bandwidth that EC2 can get, the maximum network bandwidth it gets, itdepends on a lot of factors, right.Those factors could be instance family whether it's M family, C family.So every instance family would have different network bandwidths as well as the size of the instance.That means what CPU capacity it has, what memory it has, how much disk input output IO it has.Right.Also whether the instance supports enhanced networking on not all this factor will ultimately decidethe maximum bandwidth that EC2 instance can get.But in general, if you want to look at the bandwidth, then you can visit the AWS website documentation.And then there you'll see a particular instance type.What is the maximum bandwidth it supports.Now also understand that the bandwidth when we talk about it's an aggregated bandwidth across multipleflows.Now one flow is one, uh, connection between EC2 and the other end, right, which is a five tuple,typically protocol source port or destination port source IP and destination IP.So when we say maximum bandwidth, we are not talking about just one single connection between the twomachines, right.We are talking about, uh, aggregate bandwidth using multiple flows.So uh, bandwidth of uh, aggregate flow within the region would depend on the EC2 instance.Maximum bandwidth.So, as I said, uh, EC2 would provide different bandwidth depend on EC2 instance sizes.Now if you go to the AWS documentation.So here is the documentation.If you go to this page I have given the link as well.It states that within the region you can get the full network bandwidth of EC2 instance.And if you just click here and go to general purpose instances.In that case you can see here for example, the there are different instance types first.And then if you go to the network performance you can see T2 instances.Maximum network performance is one GB.But if you go down and see other instances like M6 this is M6 a it provides 12.5 GB and in some casesyou get up to 100 GB maximum bandwidth across multiple flows.Again, remember that right with any you have to use any in that case.Now some instances support Intel virtual functions, some support Ina.And accordingly the bandwidth is decided.So that means there is no straightforward answer.What is the maximum bandwidth for a given instance?It depends on a lot of factors.So always visit this page and you will see the maximum bandwidth.Similarly, there are other instance types as well, and you can go and see their bandwidth as well.Okay.Now if the traffic is within the AWS region, then you can get the maximum bandwidth of 100 Gbps withthe Ina that we learned about.Also, if the S3 bucket is also within the same region, then also you can get up to 100 Gbps of bandwidthbetween S2 and S3, again using the multiple flows.Now outside of the region to the internet or direct connect, the maximum bandwidth you can get is maximum50% of the available network bandwidth.Now that will also depend on the generation of the instance as well as the size of the instance.The size of the instance has to be greater than or equal to 32 vCPUs, and it should be current generation.Then you can get up to 50%.That means, uh, the page that we just saw, if it says, say, 20 Gbps maximum bandwidth, but ifthe traffic is going outside of the region to the internet or direct connect, then you can get up toten Gbps as a maximum bandwidth.However, if the instance is not the current generation or the size is less than 32 week CPU, thenthe maximum bandwidth you get is five Gbps only.So remember that.And now the last thing you need to remember is that the single flow bandwidth is typically limited tofive Gbps.If the traffic is between two EC2 instances, which are across VPCs or within the same VPC, and ifthe traffic is going out to the internet or anywhere, then it is limited to five Gbps.But if the instances are within the same cluster placement group now I hope you know what is the placementgroup.Then you can get the ten Gbps in a single flow.Now this is typically required for high performance computing kind of workloads.Like if you are running some, you know, CAD applications or simulations, then you need that levelof bandwidth.And in that case you will use the cluster placement group.Now in that case per flow you can get ten Gbps.And the aggregate bandwidth again is limited to 100 Gbps with the Ina that we already talked about.So remember these things okay.so that covers the important points.What you need to remember for exam.So if I want to summarize this again, understand that the maximum network bandwidth between EC2 instancesand other networking endpoint is 100 Gbps that you get with the Ina.And uh, also understand that within the region you can get full bandwidth of EC2 instance and per flowbandwidth is typically five Gbps.But then within the cluster placement group you can get up to ten Gbps as well.Now outside the AWS region, if the traffic is going to, then the maximum bandwidth would be 50% ofthe maximum available bandwidth for that instance.And uh, otherwise, if the instance sizes are lower than 32 vCPUs, then the maximum bandwidth is fiveGbps.Now when we talk about maximum bandwidth within the region, it could be between Ec2's.Or it could also be between S2 and S3 as well.That means you can get 100 Gbps bandwidth with multiple flows between S2 and S3 as well.So that's it about this lecture.Thanks for watching.