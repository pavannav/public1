# 3.7 IP Addresses - IPv4 vs IPv6, Private vs Public vs Elastic IP

Hi.So in this lecture let's talk about how to use or what kind of IP addresses are available when you arecreating a AWS VPC and when you are launching your resources inside the VPC.Okay.So a quick refresher before we talk about VPC IP addresses.There are two types of IP addresses as you know IPv4 and IPv6.Now if you go few decades back there were only IPv4 addresses.And if you see these are 32 bit addresses.And that's where you could have say unique around 4 billion addresses, that is two raised to 32.But over the time because of, say, internet of things and every device used to have their own IP addresses,we exhausted all IPv4 addresses.So in 2011, there was a last public IPv4 address range that was allocated.So which means there are no anymore available IPv4 addresses.And this was the problem that was probably foreseen by the experts.And that's where IPv6 addresses were introduced.Now, IPv6 addresses are 128 bit in size.That means they can address two raise to 128 addresses, which is this big number which I cannot read,but it is said that it is more than sufficient for the foreseeable future for us.So eventually what you will see that most of the applications website devices over the time will useIPv6 addresses instead of IPv4 addresses.Okay, so that's about IPv4 and IPv6 addresses.And if you want to see this is how they look, IPv4 addresses are represented as four octet as you cansee.And IPv6 addresses are represented in the eight blocks where every block comprises of four hexadecimalnumbers.Something like this.Right.So that's about IPv4 and IPv6.And in this lecture we will primarily focus on IPv4 addresses because for IPv6 addresses.And to dive deeper, you need to really know other concepts in AWS, like using Nat, using DNS.And that's where we will introduce IPv6 in this lecture.But we are going to dive deep into IPv6 and the support for IPv6 in AWS in the later section of thiscourse.Right.So I hope it is fine.Okay.So if we talk about IPv4 addresses.So you could have your VPC which has the IPv4 Cidr range as you can see here.And then you will have subnet which is also have a part of that range as subnet Cidr range.And now inside this subnet if you launch say an EC2 instance, the first thing that it will get is aprivate IP address.Now that address it gets from the subnet Cidr range.So it can be say this address now along with the private IP address.If you want this EC2 instance to communicate with the internet directly then you will also assign apublic IP.Now that really depends on the subnet setting.So on the subnet if you enable auto assign public IP then EC2 instance will also have a public IP.Now this public IP will be allocated from the Amazons pool of public IPS.Right.So it will have that public IP.And now you can communicate with this instance over the internet using this public IP.Okay.Now what happens if you say stop the EC2 instance and start again.So in that case the private IP will still remain with the instance but public IP will change.So now it has got a new public IP.So that's the behavior when you use public IP with your EC2 instance.If you stop the instance and start the instance this IP will change.So just remember that.And here we are talking about IPv4 addresses okay.So I hope it is clear.Now what if you don't want this behavior.Rather even if you stop and start the instance, you want that public IP to be with the instance.Now for that you use something called elastic IP.So elastic IPS are nothing but public IPS.But these are static public IPS.That means they remain with your EC2 instance unless you explicitly disassociate that right.So elastic IP is allocated again from the public pool of IP addresses.But it remains in your own AWS account, so it remains there until it is explicitly released.Now further, even if you stop and start the instance, this IP will remain with your EC2 instance.And one more important thing to know is that you can disassociate this elastic IP from one EC2 instance,and you can associate that with another EC2 instance.Now let's take an example.Say this EC2 instance uh has some problem.It crashes for example.Right.Now in that case you still want your application to be running on the same IP addresses because probablyyour users are using that elastic IP.Now in that case you can bring up another EC2 instance.And then you can just associate this elastic IP with the new EC2 instance.That means your user will continue to use your services.So that's another benefit of using the elastic IPS.Now just one small thing to understand is that when we say assigning IPS to the EC2 instance, actuallythese IPS are assigned by something called Eni.That is elastic network interface that comes with your EC2 instance.And we are going to talk about that in the later section.But for simplicity we are saying that IP addresses are assigned to EC2 instance, but actually theyare assigned to the elastic network interfaces.Right.So with that this is what the IPv4 addresses looks like in AWS.And if you compare this, this will be the table which talks about the difference between these threetypes of IP addresses.Now we already talked about this.So I'm not going to go over it again.But hope you understand the difference.Okay.With this.What we talked so far is mostly around IPv4 addresses, which is 32 bit.But because we have now officially exhausted all the public IPv4 addresses, we have to now start adoptingIPv6 addresses.And AWS does support that.So AWS VPC supports IPv6 addresses.And these addresses are 128 bits in length against the 32 bits in case of IPv4 addresses.And that's where you can imagine how many addresses you can have with IPv6 addresses.So that is two raised to 128 bits.And that's a huge number.Now when you want to assign the IPv6 Cidr ranges to your VPC, then you can assign this range with theprefix in the range slash 44 to slash 60 in the increment of slash four block.Now earlier AWS used to have the fixed Cidr prefix that is slash 56.But recently AWS has changed that and for the subnets inside the VPC, you can assign IPv6 Cidr rangein the range of slash 44 to 64.Now, when you want to assign these IPv6 ranges to your VPC, you get couple of options.For example, you can assign your own IPv6 range and which means that you own that range.Or you can use iPam, that is IP address manager, which we will cover later.Or you can ask AWS to assign the IPv6 range.And if AWS assigns this IPv6 range, then AWS uses the prefix of slash 56.And that is fixed.So it depends on how you are allocating this IPv6 range to your VPC.And accordingly this prefix choices will differ.So just remember that.All right.Now there is one fundamental difference between IPv4 and IPv6 addresses.And that difference is that IPv6 addresses are globally unique and they are public, so there is noprivate and public IPv6 addresses.All addresses are public.And further the behavior when you stop or start the instance, the IPv6 addresses will be persistedso they don't change even if you restart the instance.All right.And finally, is it mutually exclusive to have IPv4 or IPv6 addresses inside your VPC.So the answer is no.Rather, you can use both IPv4 addresses and IPv6 addresses at the same time because VPC supports dualstack mode.So you can have both Cidr ranges to your VPC and then resources.For example, EC2 instance also can have both IPv4 and IPv6 addresses, and this is called dual stackmode.And let me further explain this with the help of diagram.So if you see this VPC you can see that there is IPv4 range as well as IPv6 range.Now within that VPC you can have IPv4 only subnets or dual stack subnet, which has both the addressranges and then IPv6 only subnet as well.So VPC does support dual stack mode.But just one thing to remember is that you cannot have IPv6, only VPC.The VPC need to have IPv4 Cidr range.However, if you see this subnet, subnet can be IPv6 only, but not the VPC.So remember this important difference.So with that I'm just providing a simple table which compares the IPv4 and IPv6 Cidr ranges for yourVPC.So if you want just pause this video and you can read through all these important differences.All right so that's about the IP addresses in VPC.At very fundamental level.And going forward we will again revisit these IP addresses in the context of different AWS networkingcomponent.So with that now let's go to the next lecture.Thank you.