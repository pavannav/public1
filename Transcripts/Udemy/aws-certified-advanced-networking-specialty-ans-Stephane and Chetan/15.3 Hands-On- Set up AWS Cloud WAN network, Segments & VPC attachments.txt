# 15.3 Hands-On- Set up AWS Cloud WAN network, Segments & VPC attachments

Hi.So in this lecture we are going to actually set up the AWS cloud Wan network.And more importantly we will see how to create the cloud Wan segments, how to create attachments andhow to configure the routing between the segments.So what we are going to do here is set up something like this.So we want to create a core network which is enabled for three AWS regions that is Mumbai, London andNorth Virginia.And let's assume that in Mumbai and North Virginia region, there are development VPCs and productionVPCs.And in London there is a shared services VPC.So of course we need to have three network segments like development production and shared segment.And we want to connect our development VPCs to the development segment and likewise for production andshared services.And then our aim is to, you know, launch EC2 instances just for testing the connectivity in everyVPC.And then we want to test that from development instance.In North Virginia, we are able to communicate with the development instance in Mumbai and similarlyfor production.And of course, what we don't want is that development instance can communicate with the productioninstance.So this connectivity should not work.And additionally what we want is that development instances can communicate with the shared servicesinstances.So for that we have to share the segment.So we'll share development with the shared services segment.And we will then have this connectivity.Similarly we also want production instances to also access the shared services segment.Now this is typically required if shared services segment has kind of, you know, centralized egresskind of routing configured or it connects your hybrid network, then typically you will see this kindof architecture, right?So which means now we will also share our production segment with the shared services segment.And then this connectivity will also be there.Right.So this is our target.And now if just I want to show you how exactly it will look like.So it will look something like this.And the process to set this up will be like creating all these five VPCs, creating instances insidethat.And then first we will create AWS cloud, Wan Global Network.Inside that we will create a core network.We will enable this core network in this three regions.And that's where you see this three core network edges provisioned by AWS.Once that is done, we will create three segments as you can see.And then on that segment we will create five attachments for this five VPCs.Additionally, we will share the development segment with the shared services segment.And similarly production segment will be shared with the shared Services segment.Now after doing all this, the last thing that we want to do is to update the VPC route tables.So we have to tell this VPC how to go to the other region VPCs.Right.For example, for this North Virginia development VPC, we would have to configure the route which sayshow to go to the development VPC in Mumbai or Shared Services VPC in London.So we will update the route table for all these five VPC subnets and we will add corresponding routes.Now in this case the target of the route will be core network Arn.Right.And for testing the connectivity as I said there will be EC2 instances.So we will log in to this EC2 instances over SSH.And then for testing the connectivity we will ping to this target EC2 instances using their privateIPS.Note that we will be using private IPS because otherwise if this EC2 instances has public IP, thentraffic can also go over the internet.So that is what we will be doing now in order to save some time.What I have already done is I have already launched all these five VPCs, and then I have created publicsubnets into each VPC, and I just launch one EC2 instance in each VPC, right.So that, you know, we can just start from there.And all these steps that I am just describing are also part of the next slides.So you can also refer to that if you want to.But otherwise if you just look at this architecture you should be able to implement the same.Now with that let's go to AWS console where I will first show you what setup I have already done.And then let's start creating this core network.Now if you are also doing this exercise, then just word of caution is that there is a cost for creatingthe core network in different regions.So there is a cost per hour for this core network edge.So as soon as you are done with the exercise, make sure that you delete all attachments.You delete your core network and you also delete your global network.And no need to say you need to also terminate your EC2 instances and I will be doing the same afterthis exercise.Okay.So if I go to my VPC.So I'm into Mumbai and you can see I have two VPCs.And more important is their Cidr ranges.If the Ssid starts with the ten then they are development VPCs.And if it is 172 then they are production VPCs.Similarly, I have these VPCs in North Virginia region again one development and one production.And then in London I have shared services VPC with 192 range, and I have launched two machines in Mumbai,one in each VPC.And then similarly in North Virginia.You can see here.Let me refresh.Yeah.Two instances and then one in London as well.So what I have also done is I have just logged in to this Mumbai EC2 instance over its public IP, andfrom there we will test the connectivity to the development instance in the North Virginia region usingits private IP.So to this instance, using its private IP, that's our target.But before that of course we need to create the core network and everything that we talked about.So for that let's go to the network manager.That's the service of AWS where you need to go to.And it's a global service.And let's go to the global networks.And as of now there is none.So let's create a global network.Let's call it demo global network.And just say next.Now here you need to create the core network.Now you can also create the core network a little later.But this is just, you know, you can do at the same time you are creating the global network.So let's create the core network with say demo core network.And here you need to provide the ASN range that we had talked about.So let's go with the default that is 64512265534.And this ASN ranges are used to assign it to the core network edges.Now is the edge locations, which tells that in which all AWS regions you want this core network in.So let's choose North Virginia, then Mumbai and London.So which is here right.And now the default segment.So for us it's a shared segment.So let's create the shared segment as we create the core network.That's it.And now let's create this.Right now.At this time, it will take maybe 5 to 10 minutes.So I'm pausing this video and I'll come back here.Okay, so it took more than ten minutes for me.And you can see that the global network and the core network have been created.So global network is created immediately.But the core network actually takes long time.Okay.So if you now go to the global network then you will see core network under it.And there are cool visualization where you can exactly see where your core network edges are.So it shows these three regions.But this kind of visualization will get better as you create segments and then attach VPCs to thosesegments.So we will revisit all these visualization parts uh, little later.But as of now let's focus on what's there in the core network.So let's go to the core network.So this tells us that there are three edge locations.And there is one segment that is a shared segment that we had created in this core network.Now it also shows the topology graph and topology trees.As I said, this will be more interesting to see when we set up our entire network.So we will come back here again.Okay, going back to the core network.Now, the next thing we want to actually do is to edit the core network policy.Okay.As you can see that there is this, uh, first version of the policy and under which it has deployedthe core network edges in the three regions.And it also created a default shared services segment.Now let's edit this policy and add additional components.Now you can also do this through this visual editor.Or you can also do that through the Json document.So if you see this you will already see that there are these Json ranges and three edge locations.Of course you can create additional edge locations if you want.Right now what we want to do is to create some additional segments.So there is already this shared segment.And now let's create the development segment.And I'm just leaving everything as default as of now.But I'll revisit this again after we create the segment.So let's create this and let's also create the production segment.So we created this three segments.And as of now that's fine.Let's create the policy.And you can see that the new policy version will be created.So let's wait for this policy to be ready for execution again.It takes hardly few seconds.Typically you should refresh this page.So let me try that.And it's ready to execute.So let's use this policy.And at this moment you have to view the policy changes and apply the change set.So let's apply the change set.What it's going to do is it's going to create this two additional segments that is development and production.And also if you want you can, you know verify the status of this policy.So it tells you like at which step it is currently.So you know it is creating those segments like that.So you can see here that it's creating those segments in all the regions.So again let me pause this video here and I'll come back soon.Okay.So again it took around five minutes for this step to be completed.So let's go back to the policy versions.And you can see all steps are completed.Now what we want to do is again we want to set up the routing for the segments.Right.So let's just wait for this to be execution succeeded status.So again I'll just pause for a few seconds maybe and come back.Okay.So again it took maybe a minute or so.And that's where I said that the UI is not that fluid as of today, but hopefully it improves over thetime.Okay, so we have got second version of the policy and we want to now configure the routing for thesegments.And essentially we want to share these segments.So as of now we have these three segments.And what we really want is that, you know, we should be able to control which segments can share routeswith each other.As of now there is no such restriction, which means if development and production segments are shared,then the routes will also be shared.But we want to be more specific where what we want is.So let's use this and we want that.The development segment should be able to be shared only with the shared services segment.So that's what we are doing using this allow selected.And let's edit this.Similarly what we want to do is for production as well.We want that the production segment can only be shared with the shared services segment.So it automatically populates that.I don't know why, but I would have expected that I should select this setting.Okay.And again let's do that.And in the shared services segment we want to make sure that this is only shared with the developmentand production.So let's choose this two.And we are good right?Now additionally what you can also do is you know, you can have the deny list instead.So in the production you can say deny the access to the development segment.But when at the same we are saying that production can only be shared with the shared services segment.And I don't also want this require acceptance so that we can do this quickly and optionally, you knowthat you can restrict that.This segment is available only in few regions.So if you want to do that, then you can select only couple of you know edge locations.But I'm not doing that okay.So this is done now.Next and most important thing is the segment actions which tells us which segments we want to sharewith each other.So let's create the segment that is shared segment.And we want to share this segment with the development and with the production.And while we do that, the routing between the both shared and development will be exchanged and sharedand production will be exchanged.But development and production cannot communicate with each other.And we already talked about this point in the previous lecture.Right.So let's create this sharing.And yeah we are good with the segment sharing.And the next is attachment policies.Now this will define how you connect your attachment to the segments.So we want to typically create you know rules here.So let's create our first rule which says say rule number one.And this is for my development segment.And it says that if the VPC is attached to this segment.So it should be with the tag value with say environment.Equal to development on the attachment.Then it will be automatically attached to the development segment.So that's the first policy or a first rule.Similarly let's create the another rule.And this time let's say this is for the production.And here the environment should be production.Now let's also create one default policy which says if any condition is there, that means attachmentdoesn't have right tags or value.It is attached to the shared services segment.So typically you should have this kind of, you know, rules if it doesn't matches any of the previousrules.So let's do that.Let's call it say rule number 100.And here let's say shared.And here we'll say any that means any condition.And one more thing.If you remember we talked about attachment as a tag value.So you can also create a generic rule which says you know, if the particular tag value is there onthe attachment, then it will be attached to the respective segment.Right.So this is our default rule.So also let's create that.So I think we are good with the attachment policies as well.So now let's create another version of the policy.And this time we are adding the segment sharing and this attachment policies.So again let's wait for this policy to be ready to be executed.And.Once that is done, we will apply the policy changes.Okay.So let's go to the policy and let's view and apply the chain set.So this will be quick.I think maybe in a minute or so this will be completed.Okay, so it took less than a minute.Now we have everything that we wanted.And the next important thing is to create the attachments.So now we want to create five attachments for five VPCs.So let's do that one by one.So let's create our first attachment say Mumbai development.And this will be in the AP South one that is Mumbai.And the VPC id is this.And the most important part is you need to put right tag so that this attachment can be connected tothe development segment.So let's call it development.And now I'll be repeating this for all the rest of the four VPCs.So I'm just pausing this video and I'll come back.Okay, so so far I have created four attachments for, you know, both production and development environmentsin Mumbai and North Virginia region.Now I want to create the last one for the shared services attachment.So let's call it London Shared and the location as London.Now here I'll be selecting this VPC.And I don't need to put any tags because it will be then evaluated with the default rule that we created.And let's create this attachment.So now we have all the five attachments.So all are getting created now if you see it automatically been attached to the segment.So this to the production segment and development segment.And it also tells you which rule match because of which it was attached to that respective segment.So rule number one two and for shared it was rule number 100.So which means that the rules that we had written worked on these attachments okay.So again it will take few minutes.So I'm pausing this video till this attachments get created okay.So everything is done.Now let's go to our core network.And now let's look at the visualization.So it should tell us how the entire setup looks like.So there are three edge locations three segments.And it will show you in the topology graph here.So as you can see, there are now these edge locations.There are VPCs.And if you enable the segments it will also show you the segment.I think the better view is the topology tree.So it really gives you a nice tree kind of view.Right now you can definitely see everything that we just did.So core network three regions.And then in Mumbai there are two segments and then there are two VPCs.And in London there is just one segment and shared services VPC.So this is a very nice view.Now another important thing to see is these routes.So it exactly tells you which attachments can communicate with each other.So if you just now look at the development and AP South one that is Mumbai.So it will tell you from the Mumbai region you can reach to another development VPC in another region,which is this IP address, and then also to the shared services segment.And these routes are propagated routes, right.Similarly, if you look at the production you will see the routes for the other production VPC in differentregion and also for the shared services VPC route.So if you want to troubleshoot networking issues, probably you can come to these routes and verifythese routes here.Right.So if you go back to this diagram, everything is done.And what is not done is this route tables for every VPC.Without that, you cannot communicate with each other.And let's also just verify that.So at this point let's test the connectivity from Mumbai to North Virginia development VPC.So for that let's go to the North Virginia and just pick this IP address.And I have already switched into the Mumbai development EC2 instance which is this.So let's try to ping to North Virginia, which doesn't work of course because we don't have the routein the subnet route table of every VPC.So let's do that for the Mumbai development VPC.I will show you that.And then I will be repeating the same for the rest of the VPCs.Right.So let's go back to the VPC console for Mumbai.And let's pick this VPC.And rather we want to do the updates in the route table.So this is Mumbai Dev public route table which has just two routes.One is the local and another is for Internet gateway.Now let's edit these routes and say if the traffic is going to any of this ten .0.0/8, then it hasto go through the core network.And it will automatically populate this aahn.Now you must be wondering that there is already this 16 route, but as you know, the longest prefixmatch takes the precedence.So if this EC2 instance initiates the traffic within the VPC, then this route will be taken.But otherwise, if anything falls into this range, the core network route will be taken.Now similarly we want to do that for shared services.VPC that is 192.168.0.0 slash 16 because we don't have any other range in the second place.So I think that's fine.And let's use core network and again use the same Arn.Note that we are using the Arn here and not the gateway IDs.That's one difference when you add this target for the core network.I think that's sufficient.And we are not going to add the production routes here because we don't want that connectivity.And even if we would have added it wouldn't work because we are not sharing the development and theproduction segments.So let's save the changes.Now I will do the same on the other side of the network that is the North Virginia development VPC.So let's go to the North Virginia.Let's go to the route table and let's go to the North Virginia development route table.And here let's again add a route which says ten 000/8.It has to go through the core network.And let's also add a route for shared services.And it will also go through the core network.Right?So if everything is done as expected, we should be able to connect from Mumbai Dev to the North VirginiaDev instance.So let's test that.So you can see that traffic already started flowing, which is great.Now let's also test the connectivity to the shared services VPC EC2 instance.So for that, let's go to the London EC2 instance and let's pick its private IP which is this.And let's go back to our terminal.Let me clear the screen and let's try to ping to this IP.And this doesn't work.Can you guess why?Of course.Because we don't have the VPC routes in the shared services VPC route table.So I'm going to now add that.So again back to the VPC console for London region.And in here we need to again add the routes.And we are saying if the traffic is going to the development sides then it should be through the corenetwork.And similarly if it is going to the production side again we can use the generic range so that it coversboth Mumbai and North Virginia.Then it should be through the core network and save the changes.Let's go back to our terminal and you can see that the traffic started flowing.That's great.Let's also make the final check whether can we ping to the production instance from here.So for that let's grab the IP address of the say North Virginia production instance.And of course it shouldn't work because segments are not shared.And we also did not add the routes in the VPC subnet route table that's expected.Okay, so that's it for this exercise, I think we are able to successfully set this up and test theend to end connectivity.Again, as I said, I have provided all these steps here and you can watch these steps and implementon your own.But at the end of the lab, make sure that you delete everything, because otherwise you will be chargedfor all these segments, attachments and, you know, core network edges.And I'm going to do that now.So I'm going to terminate all EC2 instances, delete all the attachments, delete core network, andfinally delete the global network.So I hope now everything is very clear to you how exactly the cloud Wan network works and how to setup the routing and configuration.So that's it for this lab.Let's stop here.Thank you.And stay tuned.