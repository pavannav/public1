# 18.17 Security Groups Outbound Rules & Managed Prefixes

So let's talk about security groups,outbound rules for a second.By default the outbound rules allow anywhere with this CIDR,but we can remove and just allow specific prefixes.So here is an example of an outbound rule,and as you can see, it authorizes the port 443of a destination to be Amazon S3 in the US East 1.So it looks like from this we have used a prefix destinationso that the only amount authorized is to S3.And so as such, we can introduce the conceptof managed prefix lists.So it's one or more CIDR blocks and it makes it easierfor you to configure your security groupsand your route tables.So in your account, for example,you can define your custom prefix list.For example, this CIDR 1, CIDR 2, and CIDR 3.You can share them to another accountsuch as in the security group, inbound rules.You can actually reference the prefix listthat you've defined in Account A.So these customer manage prefix lists are a setof CIDR that you define and that you manage yourself,and they can be shared with other accountsor within your organization.This allows you, for example, that if the Account Awants to modify the prefix list once,it will be applied to many security groups at once.The other option is to use the AWS managed prefix list.So it's a set of CIDR for AWS services,but you can't create, modify, share, or delete them.You can only use them.And some of the services that may be importantthat you can already reference using this managedprefix list are Amazon S3, Amazon CloudFront,DynamoDB, and Ground Station, and they may be others,but those are the most important.All right, that's it for this lecture.I hope you liked it and I will see you in the next lecture.