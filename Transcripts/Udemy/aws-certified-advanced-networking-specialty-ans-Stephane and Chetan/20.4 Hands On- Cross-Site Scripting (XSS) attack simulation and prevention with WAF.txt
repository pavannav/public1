# 20.4 Hands On- Cross-Site Scripting (XSS) attack simulation and prevention with WAF

Now, let's see WAF in action.Now, in this case, what we are going to do iswe are going to just launch a simple web application, right?In that case, what we will do iswe'll just create one simple, easy to machine installthe HTTP server, which is a simple web server,and then we'll copy the content from this Git repository,XSS example, and then we will just start the HTTPD server,and see how an XSS attack happen, right?Now, this is a very simple example,where XSS is not attacking other users,but the one who is really sending that XSS request,but, in real world,maybe if you have a more sophisticated attack,then you can also get data from other users, right?So, this is a simple example we'll now get into.Okay, so I'm into my AWS console,and I already launched EC2 instancewith Amazon Linux machine.I'm sure you know how to do that.And what I did is, after I launched this instance,I just SSH into it, and just installed the HTTPD serveras well as I installed Git in order to clone the repositorythat I just told you.Now, let me take you to my SSH session.Okay, so this is my SSH.Now, let me just change myself to sudo,and then I will just do cd /var/www/html,and here you can see that I have index.html.Now, this one is the other one.I just need to move this to new,and just move this to the original one.So, when you copy, you will actually seestyle.css, index.html, app.js.Under this XSS example repository,you just have to move under the /var/www/html directory.So, I'm sure you know how to do that.Once you do that, make sure thatyour HTTPD service is running.sudo service httpd statusYes, it is running.And then, let's get to the browser,get the IP address of EC2 machine,and see what it looks like.Okay, so IP is this.I'm just opening another tab now.Okay, I got the server IP.I'm just putting that.And this is a simple web application.What I can do is I can say just, "My car,"and, for the image, I can provide some random image,and I just got one image there.From the internet, I just put it there,and just sent the message.So, what this application is doing,just showing my comment and image there in this.So, this is so far so good.Now, if we want to just reproducethe cross-site scripting attack,what you can do is, if you look at the code of this app,you will understand whythis is cross-site scripting is reproducible,because everything that we enter hereis interpreted as HTML text.So, now what we are doing is, instead of giving that URL,we are just trying to close this example.com/any-image.jpg,and then we are saying,on error, you have to execute this script,which is, "Alert, this computer is hacked."Now, the code is written in such a way thateverything that you provide as image is taken as HTML,and that's where, if you do that, say, "My car2,"and send the message, you see this pop up here.Now, this is because the browser is executing thisas a JavaScript, right?Because not a proper input validationhas been done for this code.Now, this is not very sophisticated attack,because this is something you are entering,and then you are getting affected by this.But imagine that, instead of this script,you put something like,get me all the cookies of the users currently logged in,and probably it displays it here.Now, for that, we need to write a different code,but we don't want to get into that level of depth hereas of now.But now, this is how you understandhow the cross-site scripting attacks are done.Now, with that, let's move to another example,where we'll see how the Web ACL can preventthese kind of attacks.Now, for that, I'm going to the same EC2 machine,but I'm just putting a very simple webpage instead of this,because, otherwise, we would have to just play arounda lot with the code here,but let's make a simple single page web application,which just says, "Hello world."So, let's go to our EC2 instance.And, in this directory, I have just made another HTML file,so I'm just backing up the earlier one as .xss,and I'm just making another file,which, if you open, has nothing very interesting, right?This is a sample sample, right?This is a sample website, and that's it.Now, if I refresh my page in the browser,then I should see this.Okay, so far so good.Now, what if you put something in the URL string,something like, say, script and alert again,say hacked, right?Something like this and this, right?As of now, we don't expect anything to happen,because there is no resource under this URL, right?So, not found, but the request has reached the server,and it tried to find that resource.Now, if you want to play with this,we can probably put a Web ACL in front of our EC2 machine,but, for that, we have to use Application Load Balancer.Now, we'll create Application Load Balancerand add EC2 instance behind that.But, before that,let's just make sure we are usingthe right availability zone and right VPC,so this has been created in this VPC, 0a3,and then it is in 1a availability zone.So, let's go to the Load Balancerand just create a Application Load Balancer.Let's call it, say, xss-lb, internet phasing, IPv4,the right VPC has been selected, and we'll use same ACas our EC2 machine.And then, for the security group,let's create a new security group.And for that, we'll just allow traffic on port 80.So, let's do that.So, it's lb-sg,and this is in this VPC,and we'll say HTTP traffic to be allowedon port 80 from anywhere.And that's it.xss-lb as the description, and let's just create that.Okay, so security group has been created.We can just take its ID and go here, just refresh it,and we will just pick the same one.Okay, we'll remove the default one.And now, listener is port 80,and we want to forward it to one target group.As you know, we need to create the target group first,so let's do that.And, for that, let's go hereand create one new target group.So, the target is instances.It is also sending the traffic to port 80in the default VPC.HTTP1 traffic, health check on slash, that is okay.And then we are just going to give some name, xss-tg,and we are going to add this instance into the target group.That's it, we'll just create the target group.We'll go back to the load balancer,and then we'll just refresh this,and select the right target.I think we are done with the settings.This is what we have to do,and just create the load balancer.We have to select at least two ACs for load balancer,you know that.I missed that.So, that's it.The load balancer has been created.It will take maybe a minute or sofor load balancer to be active,and then we'll just checkif we can access the applicationthrough the load balancer URL.Okay, so load balancer is active.Just let's grab the DNS name, and try to hit that.Okay, so it is working.My website is visible.Now, as you know,if you put some kind of script there,again, the same result I'm expecting,where it will say the resource not found,and this is expected, right?But this is not really preventing any kind of attackas of now.It is reaching to the server,and then it's not finding the right resource.Now, let's find the web application firewall service.Here it is, and let's open that.Make sure you are using the latest version of the firewall,which is WAF v2,and now let's create the Web ACL.Okay, let's give it name xss-waf, or anything.And then we want to have it applied tothe Application Load Balancer,that's where we'll select that,and the region we want to put is in AP South 1,that is Mumbai.Now, Web ACLs are global in that sense,but then you can apply it to the particular region,as you can see here.Okay, our names are gone for some reason, but that's okay.And now, here, you have to associate thatwith a particular AWS service.So, we are just going to selectthe Application Load Balancer,and this is what our load balancer is.So, I'm just adding that.Now, once that is done, we have to define the rules.And, as of now, we will just add the existing rule.So, manage rule groups, which are owned by AWS.So, let's select the core rule group,which contains the XSS as well.So, AWS manage rule groups,and, under that, we will select the core rule set, right,which has 700 plus rules already,and it covers the OWAS vulnerabilities as well.So, I think this is sufficient for our Web ACL as of now.We'll just say add rules, and that's it.We will just give the right IM rule,and we'll just go with the rest of the things as default.Okay, everything is done.Let's create the Web ACL.So, as I said, once you create the Web ACL,it takes up to one minute for the Web ACLs to be active.And let's pause this video for one minute.Okay, so my Web ACL is active now.You can see here, if you click here,it will also show you which rules it matches,and it collects a lot of metrics from the CloudWatch.So, here you can see thatI tried just once this rule, and it has matched,but then, let's see it again, right?So, as of now, let's go to our EC2 instance.I pinned this script, as you know,this will still give the resource not found,because it is not going through the Web ACL, right?Because we applied Web ACL or a WAF ruleto the load balancer.So, in that case, let's get to the load balancer,let's pick the IP address, and just put it here.So, as of now, you can access it,because this is not a cross-site scripting attack.But, as soon as you try to do something like thiswith the URI string here, and append this string here,you should get the forbidden error.That's what it does, right?403 forbidden, because Web ACL has found thatthere is some kind of XSS attack.And what you can do isyou can go here into the WAF overview,and just refresh these metrics once,and it will show you that you are trying to executethis URI, which it detected as blocked,because it detected as an XSS.So, this is what the Web ACL can do,and this is a very simple example.In real life, you can do a lot more.You can match actual string,or Regex, SQLs, everything, right?So, we'll stop here with these demos,and now we'll move to the next security service.So, thanks for watching.