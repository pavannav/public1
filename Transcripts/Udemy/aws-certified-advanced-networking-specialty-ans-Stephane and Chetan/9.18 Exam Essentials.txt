# 9.18 Exam Essentials

Okay.So exam essentials.Okay.So first thing VPC endpoint enables the private connectivity between VPCs and AWS services or customerservices hosted inside a VPC that you already know.Now if we are using the VPC endpoint, then the consumer VPC or even the service provider VPC doesn'tneed to have the internet gateway or the Nat gateways.And that's where all the communication is secure, scalable, and cost efficient.Further, you should know that there is a VPC gateway endpoint and it enables the private connectivityto S3 or DynamoDB in the same AWS region.And in order to use that, you have to update the route table so that the traffic can be routed to S3or DynamoDB using the prefix list.And remember that VPC gateway endpoint cannot be accessed over the Direct Connect or the VPN or VPCpeering, right?They can only be accessed from within the same VPC where you created the gateway endpoint.Moving on.There are different types of endpoints powered by private link.And essentially as of today, there are these four types of the endpoints.And out of this we are interested in interface endpoint.Right now interface endpoint creates any into the consumer VPCs across different subnets across availabilityzone.And that's where it receives the private IP address from the subnet Cidr range.It also uses security group.And you have to make sure that security group inbound rules allows the traffic to come from the source.Source could be your EC2 client or any host in the on premises network.Further, VPC endpoint supports both IPv4 and IPv6 traffic, so your client can be IPv4 or your clientcan be IPv6, and your services could also be IPv4 or IPv6.And it also supports both TCP and UDP traffic.So until now UDP was not supported.But recently support for UDP has been added.Next VPC endpoint can be used to connect VPCs with overlapping sides, and that is one of the key benefitof it.Um, over using the VPC peering connection or the transit gateways and VPC endpoints can also be accessedfrom other networks, for example, peered VPCs or transit gateways, or a VPN connection or the DirectConnect.Now VPC endpoint also supports the Cross-region access, where VPC endpoints can be in different AWSregion, and VPC and VPC endpoint service can be in different AWS region and VPC.Now remember that VPC endpoint or a private link feature is unidirectional.That means traffic can only be initiated from the consumer VPC.Further, with respect to the DNS, VPC interface endpoint receives the regional and zonal DNS name,and if you want to keep the traffic within the same AZ, then you should prefer to use the zonal DNSname right and otherwise for high availability, you can also use regional DNS name.And finally, there is also VPC endpoint policy that you can define.And in that policy you can define basically which AWS resources you can access through this endpointand who can access.So whether it is specific S3 bucket, whether it's specific DynamoDB table or in some cases, you mightbe able to define different resources depending on which AWS service you are accessing.And also with respect to the principle, you can define whether the access is allowed for a particularAWS account or a particular user in that account.So you can apply this IAM policies while creating the VPC endpoints.And it applies to both VPC gateway endpoint and the interface endpoint.Right.So that's it about VPC endpoints and Privatelink.So I hope that all these concepts are clear to you.Uh, if you have any questions just drop a question in the Q&A section and I will be happy to answer.So let's wrap this section here.And with that let's go to the next section.Thank you.