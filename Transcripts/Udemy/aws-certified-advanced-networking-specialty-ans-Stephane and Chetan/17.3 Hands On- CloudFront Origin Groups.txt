# 17.3 Hands On- CloudFront Origin Groups

Okay, so let's practiceorigin groups in CloudFrontSo the first thing I'm going to dois to create an S3 buckets,and on that S3 bucket I'm going to uploada simple index.html file.So let me create a bucket,I'll call this one demo-origin-groups,and then we can create in the eu-central-1 region,this is fine.And then we are not going to,we can unlock Block all access to public.just to make things very, very, very simple.We are going to just create this buckets.My bucket is now created,so I'm going to go in it and I'm going to upload a file.I will add a file and this file is going to bemy cloudFront folder under index.html.So this is a file that is very simple, it should just say,Hello this is index.html from S3.So super simple file.Now we're going to make this file public, very quickly,so I'm going to do Action and then Make public,and this is to make sure that this filewill be publicly accessible.So this is good.Next, we're going to go into our EC2 instancesand we're going to create a new instance, okay.In the same region we're in, in eu-central-1,and it's going to be an Amazon Linux 2,and we're just going to install a very simple web server.So at the very bottom, I have the user datathat I get from before.Now, this web server has a little bit of a distinction.So as we can see here, we have two files, we're creating,we're creating index.html and index2.html.And the idea behind this is that we wantthis EC2 instance to be the fail-over of our S3 bucketsin terms of origin group.So this is why we have a second index2.htmlas you'll see in the hands-on it will be very helpful.So we're going to Add Storage, Add Tags,configure security group,and just make sure that you have SSH and HTTP accessfrom anywhere and then Review and Launch,Launch and yes,Launch my instance.So now we're good.We have an EC2 instance and an extra bucket ready.So what I'm going to do is go into the CloudFront serviceand create a distribution that will have these origin group.So let's go ahead and create this CloudFront distribution.So my origin domain is going to bedemo-origin-groups.amazons3, Okay,And then for a path, this is optional,but we'll leave it as is, the name is fine.Sorry, the name is fine.We can use an OAI if you wanted to restrictthe bucket access only to cloudFronts,but because we made the extra bucket publicand we made the file in our S3 bucket public,we can just right now not using OAI,but the best practice is definitely to use an OAI, okay,then we will scroll down and then we'll lookat the behavior is fine.Everything should be left as default.So let's keep it, everything as defaults.And then at the very bottom, I will create my distribution.So my distribution is now creating and in the meantime,let's just test a little bit, a few things.So if we go into our public IPfor our EC2 instance,we get the index.html, which is fine,which is the same as doing /index.html.And then if we do index2.html,we get a second file, this is index2.html page.So now let's waitfor the cloudFront distribution to be created.And then once it's deployed,then we'll get back to this.So my cloudFront distribution is now ready.I can copy the distribution domain name,and then open a new tab and make sure you do /index.htmlto access the index.html file.If you don't do /index.html,you're going to get an Access Denied error.So once you do /index.html,you get the file back from S3.So, Hello, this is index.html from S3.That means that things are working.And if you do /index2.html,you're going to get an Access Denied message,because that file does not exist on our S3 buckets.So now let's go ahead and set up a cloudFront origin groupto solve that problem.Okay, so let's go ahead and create an origin,so we're going to create our second originby clicking on Create origin,and then we need to enter a domain.So you go to your EC2 instanceand what you have to take is the Public iPv4 DNS.So please take the DNS and not the IP address,otherwise this will not work.So take the DNS, you enter the domain hereand then automatically it is recognized as HTTP.And then for the Name, we'll just leave it as is.click on Create origin.And here we go,our second origin is created,and now we're going to group them togetherinto an origin group.So the origins we want to add to the groupis obviously going to be the S3 buckets,and then we'll add the EC2 instance as the secondary origin.So the primary one is our S3 buckets,the secondary one is our EC2 instance.So I will call it DemoOriginGroup,and then we need to specify a fail-over criteria, okay,So fail-over criteria, we'll select also any kind of errorsthat comes back from the S3 bucketsshould be fell over into my EC2 instance, okay.So this is good. Let's create this origin group.So now we're good to go.And next, in terms of the Behaviorsof our cloudFront distribution,we need to edit this one.So we'll edit this behavior,and instead of deploying to this origin right now,we're going to deploy to distribute to the origin group.Now we'll click on Save changesand we'll have to wait until this is deployed.So let's just wait for the deployment to be over.Okay, so my distribution is now updatedand let's have a look at it, so if we go back to this URL,if we go to index.html, as we can see, we get a,Hello, this is indexed.html from S3,but now if you go to index2.html,as you can see, we get, this is,Hello, this is index2.html page from the EC2 instance.So it tried first to go to the S3 buckets,get an error message,and then forward things to the origin grouprequested the exact same file from the EC2 instanceand it works.So that's it for this demo, it was pretty cool, right?I hope you liked it, and I will see you in the next lecture.