# 9.14 VPC Endpoint architectures - Accessing from peered or connected VPC

Okay.Now let's look at how can you access VPC endpoint services from other networks like we have seen theconsumer VPC?But what if that consumer VPC is connected to say, some remote networks like appeared VPC, right.Or from the on premises network.So how you can do that.Now when it comes to accessing this VPC interface endpoints, there are typically two categories.You can access it from within the AWS.And what I mean by that.If you have VPC interface endpoint in one of the VPC, can that interface endpoint be accessed fromthe peered VPC.Right.So that you can centralize the VPC endpoint access.Or what if your clients are there in the remote network, like in your on premises network.So in both these cases let's see how end to end traffic flow works okay.So very first thing let's say you have a consumer VPC in which you have VPC endpoint.And then you can access say Amazon S3 as a private service, and that is through interface endpoint,not through the gateway endpoint.And then you have your own VPC endpoint services as well.And for that as well you have interface endpoint in the consumer VPC.So until now we have been accessing the VPC endpoints from within the VPC right.So you might have client in the central VPC itself.But what if your clients are in different VPC.Then can they access this VPC interface endpoint.Now based on our discussion they can right.Because VPC interface endpoint has the Eni and it has IP address.So as long as this remote VPC is connected to this VPC over VPC peering connection or the transit gateway,it can reach to this IP address and can access the target service.So I hope that is clear right.So of course the layer three connectivity works.But what about the DNS resolution now.That's really the important thing here to consider.So let's talk about that.Now you have this remote VPC and you have a client inside that.Now if this client has to access S3 service privately through this VPC peering connection or transitgateway, and then through the interface endpoint, how that traffic will look like.So the first thing VPC endpoint will also have the DNS right.That's a zonal and regional that we talked about.And another thing to know is that this DNS can be resolved publicly.That means even if this EC2 instance tries to resolve to this DNS, it will resolve to its private IPaddress in this VPC.So if there is a private connectivity, this EC2 ultimately can reach to the target service, right?So using private IPS of VPC endpoint Enis or using the VPC endpoint DNS, this EC2 instance can accessthese services.So I hope it is clear.But there is one problem.So let's assume that you want to access axis for simplicity the S3 service through this interface endpoint.Okay, so now we are talking about this client EC2 instance in the remote VPC.So if it tries to say upload some data to S3 using this S3 URL, what will happen here is that the CLIwill hit the public S3 endpoint that is S3 dot whatever region dot Amazon aws.com.And this resolves to the public IP of S3 endpoint.That means traffic will be routed through the internet gateway.If it is attached to this VPC, if there is no internet gateway, traffic doesn't reach to S3 endpoint.So how do you solve this problem in the remote VPC?So if you remember, we said that if you want to use the VPC endpoint DNS instead, you can add thatas an endpoint URL to your command.So now on this EC2 instance, if you run this command then this endpoint URL DNS will resolve to theprivate IP of the interface endpoint in the consumer VPC, and then traffic can privately go to theS3 service.Right.So it will work.But this is not desirable right.You don't want every command to have this endpoint URL.You actually want this first command to work.And still the traffic should go privately to the S3.Now how do you do that.So let's look at that.So the same architecture.Now what if you enable the private DNS for this S3.And in that case you know that there is a route 53 private hosted zone created.And it would have this kind of alias record.And then it will automatically be connected to the consumer VPC.Right.So this is connected to the consumer VPC.But still this private DNS or hosted zone is not associated with the VPC.So this remote VPC cannot still resolve this public DNS to the VPC endpoint DNS.So that's still a problem.So how do we solve that.So for that what you should do is first you should disable the private DNS so that AWS does not createthat private hosted zone.And which means that you should now create your own private hosted zone with the same name as S3 endpoint.And in that private hosted zone, you should create an alias record or a CNAME record pointing thisS3 public DNS to the VPC endpoint DNS.Now, because this is your own private hosted zone, and because there is another VPC, you can associatethis private hosted zone with this remote VPC, something like this.And now because this VPC has the private hosted zone, now this EC2 instance when it makes the DNS requestfor S3 dot AP South one dot Amazon Aws.com.It will be resolved using this private hosted zone to the VPC endpoint DNS, and then VPC endpoint DNSresolves to the private IP of the interface endpoint.Something like this.And then the traffic can flow from this EC2 instance to the interface endpoint to the S3 service.And the same flow applies even if you have your own VPC endpoint services deployed here in this VPC.Right.So I hope it is clear.And just to show you how exactly it works.Now if you fire this command, the endpoint DNS will be resolved to the VPC endpoint Eni IP.And then it will be routed privately to the S3.Right.So that's it about this DNS resolution.Um, if you got it, how it works.Congratulations.But even if you did not get it completely, don't worry.I don't expect this level of details into the exam, but it's always good to know how things work,right?So with that, we'll stop for this lecture.And now let's look at some other architectures.Thank you.