# 9.7 Hands On- VPC interface endpoint to access Amazon SQS queue

Okay, now let's do an hands on exercise where we will access one of the AWS services using the VPCendpoint.Right.So the scenario will be something very similar that we are talking about.So we will have the VPC with private subnet.And we will have a client EC2 instance.And from there we want to access one of the AWS services privately using the VPC endpoint and privatelink.And in order to do this exercise, one thing that you need to understand is how the DNS work for VPCendpoint.So I'm just flashing this slide here which we already covered.So we are going to really use what we have already learned about right.Okay.So now with respect to the architecture we will have private EC2 instance in one of the subnet.And if you remember at least this two subnet we had created in the earlier exercise that we did forVPC gateway endpoint to access S3.So we can continue to use the same VPC.And in that we had this EC2 instance connect so that we can privately connect to this EC2 instance.And this time we are accessing SQS service.So there should be IAM role which allows the permissions to put message or get message from SQS queue.And further we would also have to create one of the SQS queue.Okay.Now in order to create this endpoint we need another subnet.Now you can very well create the endpoint in one of the subnet here which is already there.But it's a best practice to have a dedicated subnet for your VPC endpoints right.So we will create an interface endpoint which will create one of the Eni in this subnet.And then we can use that Eni to access the SQS service.Right.And as you create this Eni it will have its own security group.And then of course EC2 will also have its own security group.So for traffic to go from EC2 to this Eni, we need to add a SG outbound rule here in this securitygroup of EC2 and SG inbound rule for this any security group.And further, of course, in order to receive the traffic from this EC2 instance connect endpoint,you also need to have the inbound rule in this, uh, SG for EC2 that we already talked about in theprevious exercise.So once we do that, then we should be able to access the EC2 instance over SSH.First from the AWS console.And from there it will be Https traffic to the SQS service endpoint.And while we do that, we will also enable the private DNS for the SQS service so that we can accessSQS service endpoint without modifying the CLI command.And I will also show you without enabling the DNS.And after enabling the private DNS for SQS.So that's the setup.Uh and there are all these steps listed.So if you want you can pause this video and read through this and can try to implement everything onyour own.But otherwise let's implement this.So here I will start with creating third subnet because I don't have that right now.And then let's follow along the additional steps.Okay.So as you can see here I already have two subnet one for EC2 instance connect and one for EC2 itself.And now let's create subnet in the demo VPC.And it should be the VPC endpoint subnet right.As wise we will keep the traffic in the same AZ as EC2 so that there is no cross AZ data transfer charge.And the subnet Cidr will be 10.10.2.0 24.That's it.Let's create the subnet.And let's make sure that we are using the same route table for all these three subnets.So there is already this demo private route table.And there are only these two subnet associated.So let's also add the subnet so that it doesn't use the main route table.However, we are not going to change anything in the route table in this particular exercise becausethat is required in the gateway endpoint, not for the interface endpoint.Okay.Third subnet has been created.Now let's create a couple of security groups.So one for the EC2 instance.So let's call it say create a new security group say EC2 for VPC endpoint demo SG.And it should be in the demo VPC.Now inbound traffic we need to allow the SSH traffic from the you know instance connect.So let's keep it everywhere so that we don't have to change it.And in the outbound traffic as well we need Https traffic going to the SQS service.But I am keeping it all traffic because I also want to make sure that this instance doesn't have thepublic connectivity to the internet.So in order to test that we need to use ICMP protocol that is ping, and that's where I am keeping alltraffic.Otherwise we could only use Https.So that's it.Let's create this security group.And now let's create another security group for the VPC endpoint in itself.So again let's create VPC endpoint security group.And as I said it should only have the inbound traffic.So let's add the inbound and it will be for Https traffic.And the source will be EC2 instance.So either we can put the VPC Cidr or 0000.Anything should work.Or we can even have the security group of EC2 instance.So we can refer the security group.As you know we could also do that.Now outbound rule we don't really need so we can delete it.And that's it.Let's create this security group.So we are done with the security group.Now IAM role we need to create that for EC2 instance to access the SQS service.So let's create the role.And this is for the EC2 instance.And here the permission should be SQS full access.And next.So let's call it EC2 role for SQS for endpoint demo.That's it.And let's create the role okay that is done.Now also we need to create the SQS service itself or SQS queue itself.In Asia Pacific Mumbai region there is already one queue.So I can use it.Or let's create a new one and it's a standard queue and let's call it say my queue.That's it.Nothing else needs to be done.We are going with all the default things and let's create the queue.Now for this queue there will be this endpoint URL of the Q using which we can send a message to theq.So we will need this endpoint URL.So I'm copying it here.Okay.So q has been created as well.Now let's launch the EC2 instance.So let's go to the EC2 console in Asia Pacific Mumbai region.Let's launch the instance.Let's call it EC2 for VPC endpoint client.Let's use Amazon Linux because it has EC2 instance connect agent installed already.And here let's have the key pair.And it should be in the demo VPC.And it should be in the EC2 private subnet which is this.And we don't need a public IP.And we have to have the existing security group for EC2 which is this one.And then in the advanced details let's select the IAM role that we had created.Dated.So EC2.Roll for SQS I think.Yeah this one.And that's it.Let's launch the instance.Okay.So instance has been launched.Uh it's impending.So let me pause this video for a minute and then let's connect to this instance.Okay.So until now we just launched this EC2 with the IAM role.And now we are connecting to this EC2 over this EC2 instance connect endpoint.And we haven't really created this VPC endpoint for SQS.So that will do little later.But let's at least go to this EC2 and check if we have outbound internet connectivity, which is not,but let's just verify.Okay.So let's go to this instance.Say connect using the connect endpoint.And let's connect.Okay, I'm into the EC2 and now if we do say ping to google.com, of course it won't work because thereis no outbound connectivity.And also let's try to, you know, make an API call to the SQS service, which also won't work.But let's just make sure.Now we need an CLI command for that.So we can just go to the AWS CLI documentation.And you can pick this command from there.So I'm just doing the same.And then we need a queue URL right.So let's say dash dash queue URL and that URL that we had copied.And then additionally we have to have the message body.And we can say this is test message one and region.It's AP South one.And again, this won't work, of course, because there is no connectivity, right?So now let's create the VPC endpoint.First we will have the private DNS disabled and let's see how it behaves.So let's go to the VPC again.So for this demo VPC let's go here edit VPC settings and enable both these settings and save it.Now let's go back to the endpoint.And let's create the endpoint.So let's call it SQS endpoint.SQS service.In the demo VPC and we want to disable the private DNS.And we want to create this in one of the AZ.And we already had a subnet created for this endpoint which is this one right.And with respect to the SG we want this VPC endpoint SG which allows the inbound 403 43 traffic.Policy wise, it is very similar to the gateway endpoint policy, where it restricts access to a specificVPC endpoint, but we are not doing that.So let's create this endpoint and it will take again a minute.So let me pause this video.Okay.So as you can see here the endpoint has been created.And there are two DNS.So this one is the DNS.And this one is the basically the zonal because there is this AP South one A as a availability zonename.So we can use either of these DNS.But let's copy this regional one as of now.So everything is done.Let's go back to the EC2 instance and let's see if we can still access the SQL service.So what do we expect now?It won't work because right now SQL service endpoint will resolve to the public DNS.And we can verify that using Nslookup and SQS.Dot AP South one dot Amazon aws.com.And you see this is a public IP.This is what we want.But rather if you do nslookup for this VPC endpoint rather right.This is a regional endpoint.Then it resolves to the private IP which is the IP of the Eni.Right.So if you want to send a message using this DNS we have to use the same command.But we have to add this additional parameter that is endpoint URL and just paste it there.And if you do that you can see that it has worked.But there is some problem which says the scheme is missing.Okay.So endpoint URL should also have the Https as a scheme.It should be Https rather.Right okay.So the message has been sent to the queue.So we can just go to the queue and verify that.So this is a queue.So go to the send or receive the message and poll for message.And you can see there is this already a message.Right.That means it is working.But what we really wanted is without having this additional endpoint URL, we should be able to senda message to the queue and how to do that.So we learned that for that we have to enable the private DNS for the endpoint.So let's go to the endpoint.Let's go to the action and modify private DNS and enable for this endpoint.Let's save the changes.So it is pending.So again let me pause this video for a minute.Okay.So you can see that endpoint is now available.So let's again go back to the EC2 console.And first let's try to resolve the this public endpoint of the SQS service.And you see that it has now resolved to the private IP.And this is what we wanted, which means we can now send the message to the queue without using thatendpoint URL.Right.So let's remove this.And let's call it message number two.And it works right.We can very well verify that by going here and pull messages.So this was the first one and this is the second one.That means it works right as expected.So this is what we wanted to do in this exercise.I hope it is clear.Just before stopping make sure that you terminate your EC2 instance and you also delete your VPC endpointbecause there is a cost for the endpoint.So let's delete that endpoint.Okay.So that's it about this exercise.And now let's move to the next lecture.Thank you.