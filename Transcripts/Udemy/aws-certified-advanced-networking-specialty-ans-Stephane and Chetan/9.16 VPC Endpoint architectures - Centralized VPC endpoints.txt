# 9.16 VPC Endpoint architectures - Centralized VPC endpoints

Okay.This is probably the final architecture that we will see for VPC endpoints.And this is kind of one of the favorite architecture because it really simplifies your overall network.It reduces the cost.And it is a way basically to privately access many different AWS services using centralized VPC endpoints.So what do I mean by that?Imagine that you have a lot of consumer VPCs.And in any big enterprise there will be hundreds of VPCs, right?And if all these VPCs wants to access the AWS services or other VPC endpoint services privately, thenyou would have to create those VPC endpoints into every single VPC.So just imagine if there are just three services here.Then every VPC will have these three endpoints created.So you just multiply that by the number of VPCs and those many endpoints you would have to manage.And at scale it's a problem.and hence exactly.To solve this problem, what you can do is you can centralize your VPC endpoints and you can do thatbecause as we just learned, VPC endpoints and in particular VPC interface endpoints can be accessedfrom the remote networks.Right.So if you centralize the VPC endpoints here in the central VPC, and if you associate this VPC withthe other VPCs over VPC peering connections or the transit gateway, then using just these three VPCendpoints, you can grant access to all these services to all other VPCs.And end to end traffic is all private.So the benefit is one.The management is simplified.Number of VPC endpoints are less.And of course every endpoint has a cost.So that cost is also less.But just that there are additional VPC peering connection.There will be some additional cost for that, but overall I think it reduces your cost significantly.Now here I'm showing VPC peering to connect these two VPCs.But you can very well use Transit Gateway as well.Just that transit gateway has some additional cost for data processing as well.So as compared to the VPC peering connection, it might not be as cost efficient, right?So irrespective of whether it's VPC peering or transit gateway this is how the architecture will looklike.So at layer three I think the connectivity is there.Everything works.But now let's look at the DNS resolution because that will be the most important thing to understandhow it will work in this case.Okay.So um, if you enable say private DNS, as you know, it creates a private hosted zone which has thealias record to VPC endpoint DNS.But the problem is this private hosted zone cannot be resolved from the, Though or remote or say,peered VPCs right now, if you have to do that, as we learned earlier, you have to first disable theprivate DNS for the endpoint.You have to create the private hosted zone, and in that, you have to create the alias or CNAME recordfor the public endpoint, which points to the VPC endpoint DNS.And then you have to associate this private hosted zone with all the spoke VPCs.Right.Once you do that now these all VPCs can resolve this DNS using this private hosted zone.And at layer three there is already this connectivity.Right.So traffic will flow from your spoke VPCs to ultimately the endpoint services.So this is how it will work.Now this you will do for every endpoint.So these are basically the steps to do that for all the centralized endpoint services.Right.So here we used the private hosted zone and we associated that with all the spoke VPCs.But you can also do that a little differently.So let's look at the other option to resolve the DNS.And that is using the route 53 resolver endpoints.So again let's start here.And in here we will have the private DNS enabled which creates the VPC endpoint DNS.And we have to now allow these VPCs to send DNS traffic to this inbound resolver endpoint.So this is very similar to the earlier architecture where your hosts were in the on premises network.But here what we will do basically we will create the inbound resolver as well as the outbound resolver.And then we will create the forwarding rule which says that all the traffic that goes to say SQS service,it goes to this inbound resolver.And what we can additionally do We can share this forwarding rule using the Resource Access Managerwith all the spoke VPCs.Something like this.Now, if any host inside this spoke VPCs try to resort to this DNS, we'll send all this DNS queriesto this inbound resolver in the centralized VPC.And the DNS will be resolved to the VPC endpoint DNS, and further to the private IP address of theEni.Right.So this is how end to end DNS resolution will happen.So this is just another way to resolve the DNS centrally.So that's it about centralized VPC endpoint.So with that we will conclude the architecture discussion here.And now let's summarize what we learned in this section.And what is important thing to understand or remember for your certification exam.So thanks for watching.And let's go into the next lecture.