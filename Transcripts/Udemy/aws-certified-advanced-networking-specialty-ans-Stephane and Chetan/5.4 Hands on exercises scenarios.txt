# 5.4 Hands on exercises scenarios

Hi.So with the knowledge that we have, let's do some hands on exercises.So we want to try two scenarios where first we use the default Amazon provided DNS server as our DNSresolver.And second, we want to use our own custom DNS server.So for the first scenario the setup will look like this.For example, you have VPCs and a couple of EC2 instances.And we want to have our custom domain name which is core dot internal.And then one of the EC2 instance will be app dot core, dot internal and other one will be DB dot corpdot internal.And we want to connect to DB dot core, dot internal from the app server using its DNS name.Now here our DNS server will be the default DNS server.That means in the Dhcp option set we don't need to change the name server.However, we can change our domain name from the region dot compute dot internal to core dot internal.And then we need to create a route 53 private hosted zone with the same name as core dot internal.And then we will create multiple records which points to the private IP addresses of these EC2 instances.And that's it.Then we should be able to use this internal domain name for the communication between these EC2 instances.So that's the first scenario.And then in the second scenario we want to use our own custom DNS server instead of Amazon providedDNS server.And the most important part here is to update the Dhcp option set where the name server points to thesame IP address as our custom DNS server.So in this case, the EC2 instance will send all the DNS queries to this custom DNS server.And here we will create a zone file for this DNS server, which has the similar record as route 53 privatehosted zone, where the domain name points to corresponding IP addresses.So that's our second exercise.So I hope that is clear.Now with that, let's get into the hands on mode and try to implement this on our own.So let's get started.