# 9.4 Introduction to VPC PrivateLink powered endpoints

Okay, now that you understood VPC gateway endpoints, let's look at the more important part of theendpoints that is VPC endpoint and private link.And here onwards whenever we refer to the VPC endpoint please understand that we are not talking aboutthe gateway endpoints.We are talking about the endpoints which are powered by AWS VPC private link, and it differs the wayit works as compared to the VPC gateway endpoint.So let's now completely focus on VPC endpoint in context of the private link.Okay.So going back to this architecture, we already talked about the gateway endpoint to access Amazon S3or DynamoDB from within your VPC.But in the rest of this section let's focus on the interface endpoint which is actually powered by AWSVPC private link.And as you can see here, as you create the interface endpoint, it creates an Eni into your VPC.And then you can get access to different AWS services endpoints as well as your own application whichmight be hosted in another VPC.Right.So we are talking about two and three.Now these are interface endpoints.But in reality there are again additional types of endpoints which are powered by private link.While in this section we will be focusing on interface endpoints only.But just for your knowledge, you should also know that there are other types of the endpoints whichuse private link feature of aws VPC.Okay, so what's new in that space?So if you look at this diagram the first block shows accessing AWS services which is interface endpoint.Second is you are having your own services deployed inside a VPC front ended by network load balancer.Now that becomes the endpoint service.And that you can also access using the interface endpoint.Now third type is the SaaS service.So maybe you are consuming some SaaS application from third party provider.Now they also expose their private services using the interface endpoint.And then there are additional types of endpoints as I said for example resource endpoint and servicenetwork endpoint.So types of endpoint.The most important is interface endpoint.And that's what really came first.But over the time and in the recent time rather AWS has launched something called Resource Endpointwhich allows you to access any VPC resource privately.So for example, if you have RDS database inside your VPC and it is not exposed to the internet, youcan also access that using this resource endpoint.Similarly, there is also service network endpoint which is a feature of VPC lattice.And we have talked in detail about this in the VPC lattice section.So in this section we are also not going to talk much about this service network endpoint.As I said we will be primarily focusing on the interface endpoints.Okay.Other than that there is now support for both IPv4 and IPv6 traffic, which means that your client,which is using the services through this interface endpoint, can have IPv4 or IPv6 traffic, and yourprovider services could also be IPv4 or IPv6.So all this translation from IPv4 to six or 6 to 4 is done using this interface endpoints.So you don't need to really do that natting yourself.Further, in 2024, Reinvent AWS announced that VPC interface endpoint can now be accessed across theregion.And this is really exciting because until now, the scope of the endpoint was regional, which meansthe VPC in which you create the endpoint should be in the same region as the service which you are accessingusing the endpoint.So this is something new.And now it also supports the UDP traffic over both IPv4 and IPv6.Otherwise only TCP traffic was supported.But now UDP traffic is also supported.So these are the new features.And at this point at least in 2025 I don't expect say Cross-region VPC endpoint related questions.But over the time as the exam questions are refreshed, you might find these additional topics in theexam.Right.So I hope it is clear what is VPC interface endpoint and all these additional features.So just to reiterate we are talking about now VPC private link enabled endpoints.And in that we are focusing on the VPC interface endpoint which allows you to access AWS services endpointsor your own service which is hosted behind the network load balancer or any SaaS service provider servicewhich is again hosted behind the network Load balancer.But you don't really see that.What you see is VPC endpoint service to which you connect through this endpoint in your VPC, right.So we'll focus mainly on this interface endpoint.Okay.So that was a quick introduction about VPC endpoints and Privatelink.And now let's get into little more details about the VPC endpoints.Some important features to know and some additional advanced concepts.So that's it for this lecture.And let's go to the next lecture.Thank you.