# 9.12 Other VPC endpoint types - Gateway Load Balancer, Resource, Service-network

Okay, so until now we have seen two types of the endpoint.One was VPC gateway endpoint to access S3 and DynamoDB specifically, and other was interface endpoint.So all the exercises we recently did was all about the interface endpoint.But lately AWS has also launched other types of endpoints which works on the same principle as the privatelink.So they are very much similar to the interface endpoint, but they have been created for special reason.Right?And in this short lecture, let's try to just go over that at very high level.And further, we are going to dive deeper into these endpoints into their respective section in thiscourse.Okay.So there are other types.And the first one is gateway load balancer endpoint.And as the name suggests this endpoint is required to access the gateway load balancer.And as you might already know, Gateway Load balancer is used to host or send traffic to your trafficinspection appliances.Okay.So we have dedicated section on Gateway Load Balancer.And there we'll dive a little deeper.But ultimately what it means is that in the consumer VPC or in the application VPC, you can have thisgateway load balancer endpoint, which points to your inspection VPC, in which you have gateway loadbalancer itself.Right.So this is how the architecture would be.And further this gateway load balancer will send all the traffic that needs to be inspected to this,um, network appliances.And then traffic will come back.And if there are any anomalies then it will be detected.So for using the gateway load balancer you need this gateway load balancer endpoint.So that's about it.Now another type of the endpoint which is very new is resource endpoint.Now until now we saw the interface endpoint where we had VPC endpoint service hosted behind the networkload balancer.And then we could access that using the interface endpoint.But what if you have say AWS resource for example RDS database which is launched in a private subnetin your VPC.Now how to access that database directly from other VPC.Now with the resource endpoint you can do that.Okay.So as you can see here in the provider VPC, you may have your database which is in private subnet notaccessible otherwise from the outside network.Now if you want to access this database privately from this consumer VPC then you will create somethingcalled resource configuration.And if these VPCs are across different AWS accounts then you need to share this resource configurationwith this consumer account.Further you have to create something called resource gateway which is a logical gateway basically.And then you create a resource endpoint in the consumer VPC connecting to this resource gateway.And once you do that you can then access this database using this resource endpoint from the consumerVPC.And for that we are going to talk about these resources or a resource gateway in VPC lattice sectionbecause it's more relevant for that section.But otherwise just understand that you can have this kind of private access to resources inside yourVPC.And these resources need not be just database.These resources could also be private IP for ranges from that VPC or IPv6 addresses.And as you can see here, you don't need any NLB in the provider VPC as against the interface endpoint.Now one thing to note is that if you want to do that across AWS account, then you need to share thisresource configuration using Resource Access Manager.That is Ram, right?So that's it about the resource endpoint.I think this is good enough for you to know at this moment.Okay.So the last type of endpoint that we want to discuss is service network endpoint.And when we say service network it means VPC lattice service network.Now we are having later a dedicated section for VPC lattice.So at this moment let's try to just understand at a very high level because we are going to dive deepinto VPC lattice later.Right.So VPC lattice is basically application to application networking.So as you see on the right side there is a VPC lattice network.And then you can have your VR services running across different VPCs.And all these services register or say are shared through this VPC lattice network.Now, if any consumer VPC wants to access any of those services, those consumer VPC needs to eitherdirectly connect it to the service network, or you can create the VPC endpoint for this service networkin the consumer VPC.Right.And as you create that endpoint, then client in this VPC or client which are sitting on premises orconnected over to other VPCs can access your VPC lattice service network.So this is how it works.It creates Eni similar to the interface endpoint.And through this Eni you can access the service network and which also means you can access other serviceswhich are associated with this VPC lattice service network.Now we are not showing on the right side all those services.And as I said we will talk about that in the VPC lattice section.So VPC endpoint of type service network connects VPC to this service network.And now because this endpoint is a part of VPC and there is Eni, this Eni or the endpoint can alsobe accessed from other networks.Right.So whether it's a peered VPC through the transit gateway or maybe a client sitting on premises and connectedover VPN or DX.So that's what is the service network endpoint.And all this traffic can flow to the VPC lattice network right.So I hope it is clear.So just to recap we talked about three more endpoint types.First one was gateway load balancer endpoint.Then there was a resource endpoint.And now it is a service network endpoint for VPC lattice right.So with that we'll stop for this lecture.And now let's talk about some advanced architectures that emerge using the VPC private link.And these are really important to understand.If you work on VPC private link in your day to day job.So let's now look at those advanced architectures.So thanks for watching and stay tuned.