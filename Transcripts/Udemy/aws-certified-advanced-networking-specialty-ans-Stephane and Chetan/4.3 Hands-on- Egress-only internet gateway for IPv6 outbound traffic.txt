# 4.3 Hands-on- Egress-only internet gateway for IPv6 outbound traffic

Okay, so let's do this.Hands on exercise for egress only.Internet gateway.So in this exercise, what we need to do is create a VPC.And this time we'll create a VPC with both IPv4 and IPv6 Cidr range.Once we create the VPC let's create the internet gateway.And then let's create a public subnet.And I will explain why we need public subnet.And as we create a public subnet there should be the Cidr range also for IPv4 and IPv6 cidrs.And the route table for this public subnet should have entry for internet traffic through this internetgateway.Then we will also create a private subnet.And again we'll have the Cidr ranges for both IPv4 and IPv6.And it will also have the route table where it will have these two local routes by default.Now after this we will launch just a bastion host because we want to get access to the EC2 instancein the private subnet.So we will have bastion host with, say, a public IP that is IPv4 address.And then we will have a private EC2 instance which has the private IP and IPv6 address.Right now after that we will ssh into the bastion host.And from there we will ssh into this EC2 instance.Now if you see this architecture it does not have Nat gateway which means now from this EC2 instance,if I try to reach to say ping google.com on IPv4 address, it won't work.And at the same time, even if I do IPv6 ping to google.com, it will also won't work because thereis no outbound connectivity for this instance to the internet.And in order to do that, we will then create egress only internet gateway.We will update this route table for IPv6 traffic.And then from this instance we should only be able to reach to IPv6 address and not IPv4 address.So this traffic should work.So that's the goal of this exercise and all the steps for this exercise.I have also listed in the slides.So you can also refer to these steps.So with that now let's go back to this architecture and let me create that in my AWS account.Okay.As you can see here I'm in my Mumbai region VPC console.So let's create a new VPC and let's call it say demo VPC.And let's give some IP address range say ten 000 slash 16.That is IPv4 range.And as you know IPv6 range will be provided by AWS itself.So let's take this option here.And that's it.Rest of the things will be as default.So let's create this VPC okay.Next thing let's create the internet gateway.And let's create a new internet gateway.And let's give it a name as demo.And let's attach this to the demo VPC.So this is a very regular thing that we always do.Okay.Now let's create the subnet.So let's first create the public subnet in the demo VPC.And let's call it say demo public.And so here we only need IPv4 address.So let's give it ten 000 slash 24.And basically we don't need IPv6 Cidr at least for the public subnet.Right.So let's create this subnet.And now let's also create private subnet.So that's again the demo VPC.And let's call it demo private.And again let's give the cider range 10.0.1.0 24.And this time we also want the IPv6 Cidr range for the subnet.So this is my VPC Cidr range.And the subnet Cidr range should be slash 64.So sometime UI doesn't work as expected.So you have to just try a couple of times or something like this.Okay.So that's done.And let's create the subnet.And the next thing we have to now create the route tables and associate those with subnets.Right.Let's filter with the demo.Let's go to the route tables.Let's create the route table.So let's create the public route table.And in that let's add the route for internet.And this should go through the internet gateway.And we have to associate that with the subnet.So don't forget this step.Similarly let's create another route table for the private subnet.Let's call it demo private.And here there will be these two default routes which is okay.This is what we want.And we want to associate that with the private route table subnet.Sorry.Okay.So everything is done.And now we have public subnet private subnet.So let's go to the EC2 and let's launch those two instances one bastion host and one in the privatesubnet.So let's go to the EC2 instances and let's launch the instance.Let's call it bastion host.Key pair whichever you have.And in the settings VPC should be the demo one.The subnet should be the public.And it should have auto assign public IP which is by default disabled at the subnet level.But let's enable that.And in the security group we just want SSH.So that's from anywhere.Or you can also do my IP.And that's it.Basically let's launch the instance.By the time let's also launch the private instance.So let's call it EC2 private.And again the network settings.So let's select our VPC subnet should be private auto assign public IP should be disabled.And we want IPv6 address.So make sure that you enable this.Now security group again we want SSH.And this should be from the public subnet.So instead of anywhere we can say that it should be vpc Cidr range IPv4.So 10.0.0 slash 16.That should work.Rest of the settings as it is.And let's launch the instance.Okay.So next we have to login to the bastion host or SSH.So let me take its public IP.And as I'm using windows I'll use putty to log in over SSH.If you are using Mac or Linux then you can also use your terminal to SSH into the bastion host.Okay so I'm just putting my public IP for the bastion host.Or rather, I should first load my key and let me log in as EC2 user.Now from here I have to SSH into the EC2 instance.And for that I need to bring the SSH key here.So let me do that.Okay, so I pasted my PEM file here.I need to change the permission to 400.And then I have to use this key to login to private EC2 instance.And for that I need to get the private IP.So let me get the private IP address which is this.And let's login.Okay so I'm into the EC2 instance and the quick test.Let's try to ping to google.com.And it shouldn't work because we don't have Nat gateway.And also if we do say ping six google.com which uses IPv6 address then also it doesn't go there.So that means the traffic is not going.So the next step of course is to create egress only internet gateway and then update the route table.Let's go back to the VPC.And again let's go here.Egress only internet gateway.Way.Let's create that.Let's call it demo egress.In the demo VPC and let's create that.Okay.That being done.Now let's go to the route tables and let's go to the private route table routes edit routes and addroute for IPv6 traffic.And it should go through egress only internet gateway which is this one.And let's save the changes okay.Having done that let's go back to our terminal.And now let's try to ping six google.com.And you can see that it is working.Now one other thing we should do is let's see if there is ping for command which specifically uses theIPv4 address.So it is not there.But I think we can use the flag which says it is IPv4 address.And for that I think we have to use minus four as a flag.Yeah.And you can see that it is trying to go to this public IPv4 address of google.com.And it doesn't work.So which means what we expected is working.Now in your case if you just do ping, it is possible that it is using IPv6 address by default.And you can very well see that here.And it will work in that case.So please remember to use minus four flag to test IPv4 connectivity.Right.So that was the exercise.Now just last thing terminate everything that you created.Because there are two EC2 instances which are running.And there will be cost for that if you are out of your free tier quota.And I am also going to do that now.So make sure that you select all your EC2 instances which you don't need and you terminate that.Right.So that's it for this exercise.I hope you could also do that.And if so, I'll see you in the next lecture.Thank you.