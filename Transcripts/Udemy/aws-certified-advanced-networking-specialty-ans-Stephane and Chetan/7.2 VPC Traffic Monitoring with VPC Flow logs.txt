# 7.2 VPC Traffic Monitoring with VPC Flow logs

Hello and welcome to this lecture, VPCtraffic monitoring with VPC flow logs.Now, in this section introduction lecture,we talked about various tools which you can use for traffic monitoring,troubleshooting and analysis.And out of those VPC flow logs is one of such IAMportant feature.So let's talk about what is VPCflow logs and then how to use it?So in a sIAMple term,VPC flow logs allow you to capture the information aboutthe IP traffic that is going in and out of yourENIsENIsare elastic network interfaces.And these VPC flowlogs you can enable at the VPClevel atthe Subnetlevel or individualENI level,which means if you enable the flow logs at the VPClevel,then the flow logs will be enabled for all theENIs in that VPC.Now, what's the purpose of VPCflowlogs? So typically it is used to monitor and troubleshoot any connectivity issues.For example, you are not able to reach to particular EC2 instance,which means all your connections request are rejected.Now, in that case, you can go and check the VPCflow logs and see which ENIis denying that traffic.And then of course,you can check the security group roles orNACL roles and fix that connectivity problem.Another use case is to see if any maliciousIPs are trying to access your IP addresses.And in that case,you can also perform analysis like this is my ENI or this is my IP addressand show me how many requests has hit this IP address.And if you know the malicious IPs,then you can also filter those results using that malicious IP as a source IP.So a lot of such analysis you can do once you capture this VPCflow logs.And later in this lecture, we will see some of these scenarios.Now when we enable the VPCflow logs, they are not only enabled for the EC2 ENIs,but they are also enabled fortheENIwhich are created byAWSservices.For example, you know that when you use AWSservices like elastic Load-Balancer or RDSdatabases orAmazon workspaces or NAT gateways,they also create the ENIinto your VPC.So flowlog will also capture the logs from theseENIs.And finally,one IAMportant thing to know is that enabling the flow logs doesn't IAMpact yourENI's network performance. So do not worry about that.OK. With that, let's see how to publish the VPC flow logs. So if you have VPCand multiple subnets and then multipleEC2 machines,of course,EC2 will have the ENIs and there could beother networking components like load balancers or NAT gateways,which also has ENIs inside your VPCVPC flow logs can then be sent todifferent systems and can be analyzed in different ways.Now, in this lecture,we will particularly focus on delivering the flow logs to the cloudwatchand then we will analyze that using the Cloudwatch logs insights.OK. So when you enable the flow logs, then they have a particular format.Now that's called a default format of the VPCflow logs.And that format looks something like this.And we will shortly talk about this format.But the default log format for the VPCflow log is version 2.But since then,AWS has launched new versions of VPC flow logs.So currently up to version 5 are supportedand accordingly VPC flow logs can have different fieldsdepending on which version of VPCflow log you are using.So this format, what you see here is the version 2 format.Now this is a sample flow log.And if you see here there is a version number 2, then this is AWS account ID.This is ENInow there is a source address from where the packet wassent and then the destination address where the packet is goingsIAMilarly the source port and the destination portand then network protocol.So you know that TCP is represented as number 6,then how many packets were sent how many bytes were sentthe start tIAMe, the end tIAMe.And the IAMportant thing here is this "action".So action is accept that means this packet was accepted by the ENIand finally the log status, which is OK,which means these logs have been captured in the destination.So this is a default log format and we will also see this in console.Now VPCflow log format can also be customized.And as I said, depending on which version of VPC flow log you are using,you can have additional fields.So there are all these additional fields which you can usewith custom VPC flow log formatand there are more fields.But I think these are the fields which are interesting to you.And in the next slide, I will explain some of these fields.Sothe record will look something like this.Now here we are using the custom flow logs format.So this is not a default VPC flow logs format,but we have added additional fields.So in here, if you see the version is number 5,because we are using the maxIAMum fields here.Now additional field also supports the type of the packet. So it's IPv4and thennumber of packets 14 and number of byte is this,then there will be AWS account id.And additionally, you can also specify, I want to have the VPC ID as well as the Subnetid.And along with that you can also include the instance ID.And then of course, you can have the ENI ID itself.And additionally, you can have AWS Region, AWS availability zone.And additionally, there are fields like sublocation type and sublocation ID.Now this represents the Amazon wavelength zone or outpost or local zone.So if your ENI resides in any of these AWS locations,then these fields will be populated,then the action which is like whether this packet was accepted or rejected.So here it is ACCEPTand then there are additional TCP flags.Now this is all about the TCP handshake and then you can referAWS documentation what 19 means.Now additionally, there are these two fields which are packet source addrand packet destinationaddr.Now these areused to actually know the original packet IP address.Now this is because if you are having intermediate devices in the flow, for example,the NAT gateways,then you know that the packet source address changes.But if you want to know the original IP from where the packet was originated,then you can use this packetsource addrand same applies to the packet destination addras well.Further, there is additional field called traffic path, which is used for theegress traffic. And in this case, it is aningress traffic and that's where you see this field is not populated.And finally,other IAMportant field is the traffic flow direction which tells you whether it wasingress traffic oregress traffic. Now, in this case, it's aningress trafficand finally, the log status, which is OK.So you can very well have your own custom log formatand you can include as many fields as you want.So this is just an example of one of such record.Okay, So I hope that is clear.Now with that, let me quickly show you how to capture the VPCflow logs and how to see that in Cloudwatch logs.So for capturing the VPC flow logs,there are some prerequisites because you are going tosend these logs to the Cloudwatch log groups.So first you need to create corresponding Cloudwatch log groupsand then you need to create an IAM role for VPC flow logsservice to send it to the Cloudwatch.And I have included this IAM role policy which you need to attach.Now let me go toAWS console and show you these things,okay. So I'm into my cloudwatch console in Mumbai Region and in the logs, I alreadyhave this logs group and I had just created this log group, right?And as I said, you also need to haveIAM role. So I had created this VPC flow logs role. And if you see the policy,it allows to write to the Cloudwatch logs andthe trust policy is for VPC flow logservice.So these are the prerequisites before you enable the VPC flow logs.And of course, then you have to just go to your VPC.Now,what I have already done is that I have created a VPC with onepublic Subnet and inside that I have launched a single EC2 instance.And then I just went here and I said,create the VPC flow logs and most of the settings as defaultjust that you have to select the destination log group and the correspondingIAM role.And that's where I have the VPC flow log created. Now let me show you theEC2 machinewhich I had launched and this machine has a web serverand it has a public IP address.So in order to generate some traffic, what I can do is I can just go here andjust hit this web server over port 80.And then this page appears that means traffic isgoing to my EC2 instance over port 80.So that's the setup I already did.And now we want to analyze these logs using cloudwatch logs insights.So for that, let's go here and let's go to the logs insights.Now logs insights provides you the customquery language which is native to Cloudwatch.And if you know about the log monitoring tool like Splunk,it also uses similar language,but this is very powerful in the sensethat it automatically detects your log record format.And then based on some deli-meter,it extracts automatically the fields from the logs.And then you can do a lot of operations with this field,for example aggregation, sum or filtering allthat is possible with this query language.And if you need some help, then you can just go hereand then you can look at some sample queries, for example VPCflowlog queries. Likewise, right.OK. Sothe first thing that you need to do is to selectthe log group on which you want to run these queries.So I'm selecting my log groupand now this is a default query. Now, out of this, I don't want few things.For example, I'm not interested in logstreams.So for VPC flowlogs for every ENI,the cloudwatch will create a separate log stream.So if you're interested,you can query a particular log stream or it will query all the log streams.That means all the ENIsso let's just remove this and run this query.OK. So if you see it shows the entire record.So it is similar to what we had seen earlier,the default format with 2 as the flow log version, theAWS account ID, the ENI IDand the source IP, the destination IP. And this IP is EC2 private IPand this is my public IP.So if you go to what is my IP address, it will show you that as IP addressand that's where in the flow log you will see this IP.Now you can also see other IPs and probably this issomeone else is trying to connect to my EC2 instance.And that's where you see that.So this record is exactly similar to what we already sawand it has all these fields that we already discussed about.So likewise, you can run many other queriesand I leave it up to you to explore and run more queries.And for your reference,I have provided some sample queries as a text document along with this lecture.OK. So going back to our discussion. So we already saw how to create VPC flow logs.And these are sample queries that you can use.Now,one more thing like sometimes it happens that your security group orNetwork-ACL blocks some traffic or say they do not allow the traffic.And in that case,you can use this action field which is like ACCEPT or REJECT and you can analyzeyour traffic flow and understand that whether thistraffic was blocked because there was no security groupor because there was no Network-ACL rule. So let's take an example.So for the incoming request, as you know, it first hits the NACLand from there it goes to the EC2.So one scenario is if your inbound traffic is rejected,in that case,the reason could be either SG or NACL,right?But if your inbound traffic was accepted, but the outbound was rejected.Then the problem is withNACL,that's because security groups are stateful,which means they automatically allow the return traffic.But NACL are stateless.And that's where if the inbound packet was accepted, but the outbound was rejected,then it's because of the NACLrules.And also the other way around for the outgoing request.If the outbound traffic is rejected, then the reason could be either SG or NACL.But if the outbound traffic was accepted,but the returning inbound traffic was rejected, then again, the problem is with NACL.So I have included this scenario because wehave seen question in the exam around this.Also, it is worth if we just go toAWS console and try to simulate this scenario.Now, here we will create a custom VPCflow logs and thenwe will see what impact theNACL rules have or the security group rule have.And for that, we will use this query which just filters the traffic formyIP as a source or destination address.Okay. So first thing let's create the new log groupand let's call it VPC flow logs,custom formatand rest of the settings as default.And now let's go to the VPCflow logs actions and say create a flow log.And let's call it VPC flow logs custom format.And we want all three type of the traffic, whether accepted, rejected or alland the aggregation level as one minute.Now, depending on this aggregation interval,you might have more records or less records.So as granular as you go, you will have more records in your VPC flow logs.Now, as a destination, we are sending it to the Cloudwatch logs.And here we are going to select our custom format log groupand the same flow logIAM role that we had created earlier.Now, the format is custom. So let's see which fields are of our interest.So we need account ID, we need "action" AZID not interestedbytes okay, source and destination address,start and end time. Flow direction is important where it's ingress oregressinstance id, interface id, log statusnumber of packets.Now, here we are not dealing withAWSservices. So it doesn't make sense to have this. And also we don't have intermediategateways. So I'm not selecting this packet source or destination addrprotocol is important, Region we already know. But let's select thatsource address, source port, start time,sub location IDs we don't want because we know that we are operating in Region. SubnetID makes sense. Traffic path is important.Then type is IPv4 or IPv6, then version ID and the VPC ID.OK. So all these are our fields and that's it. Let's create the flow logs.Okay. So flowlogs has been created and we have to wait maximumup to 10 minutes for these logs to be delivered.Now until then we can just hit some more traffic from my IP address to this web server.And that's this EC2 instance, right?So let's do that.And let's go to the Cloudwatch logs againand let's go to the Cloudwatch logs insights. And here this time,let's select our new log group. Now, it doesn't show up here.So sometimes you have to refresh.So let's see.Let's go to the Cloudwatch logs insights. Andhere, let's see if we have this custom format. Yes, we have itandlet's try to run our query.So mostly it won't have any data as of now,which is expected because it takes up to 10 minutes for these logs to be delivered.So I'm pausing this video for almost 5 to 10 minutes and I'll come back here.OK?So it's already been five minutes and I havejust changed the query a little where I have includedthe field names because otherwise there will be toomany fields and they could be in different order.So let's run this queryand you can see thatthere is the result.So it tells me what's the interface ID, source IP address, destination address,which is the EC2 instance IP address,then source port, destination port, flow direction, action and log status.Now, what we are really interested in here iswhere the source IP or the destination IP is my own IP address.So let's say filtersource addrequal tomy IP address, which I can get from hereor we can saydstaddrequal tomy IP address.And let's run this query again.So it will show you that I'm trying to hit to port 80then it responds with theegress traffic on ephemeral port and bothingress andegress traffic is accepted because I am able to hit this web server rightnow. Let's quickly do one thing.Let's go to the Network-Access-Control-List and then try to block the trafficthrough the NACLrules. So if you go to the public Subnetin which I have EC2 instance, and if you go to the Network-Access-Control-List,which is here,let'sremove the outbound rule andlet's remove this rule. Now what this means. Nowthe inbound traffic is allowed but the outbound traffic is not allowed,that means the return traffic.And hence now, if I just open a new window,thenit shouldn't really work. Now you see this coming up because it had cashed the page.But if I do this again,then you can see that it is not able to connect.Sowith that, if we run the cloudwatch logsquery again,then it should show us both the accept and the reject traffic.But as you know, it will take some time.So I'm again pausing this video for maybe five minutes and then I'll come back.OK. It's been almost three minutes. So let's try to run this againand OK, there are many recordsand OK, if you see this. Sothis entry is like I'm hitting on this EC2 instance on port 80.And then the next entry should be the response from the EC2 to my IP address.And this is a destination ephemeral port from whereI had requested this connection.And itsaystheegress is rejectedbecause as you know, the Network-ACL is blocking that traffic.So this is what we wanted to simulate.Okay. So that's what we wanted to see.I haven't really done the security group rule thing.But if you are interested, you can try that in your environmentand just advise hereif you are trying this exercise, then also, you know,disable the VPC flow logs because otherwisethere will be a charge for Cloudwatch logs groupand I will also do that after this lecture,but just be aware of that.OK? So just few more things about the flow logs limitationthat VPC flow logs currently do not record the traffic for AmazonDNS server which runs on VPC base address + 2 IP address.And you already know about Amazon DNS server. So these logs are not capturedany logs,which is about the communication with EC2 metadataservice or Time sync service is also not captured.TheDHCPtraffic is not capturedand same with windows license activation server or mirror traffic.Now, in the next lecture, we are going to see how to mirror the traffic.Now, apart from the native tools like VPC flowlog, you can also use traditional networking tools to capture the packets.For example, you can use wireshark ortcpdumpor you can use other tools like traceroute whichshows you hop by hop the connection to the destination.You can usetelnet or nslookup kind of tools for DNS resolution.And simplest of all these to check theconnectivity is the PING protocol which gives youthe RTT that is round trip time and it uses the ICMP protocol for that.Now ICMP is not a TCP or a UDP protocol. It is internet control message protocol.And for allowing the ping inyour security group and Network-Access-Control-List,you have to explicitly open the ports for all ICMP-IPv4 or IPv6.So just remember thatbecause sometimes you try to ping to the EC2instance and despite everything is all well setup,but still you are not able to ping because security group ornetwork ACL doesn't have ICMP rule added in that.OK. With that, we'll stop here.So I hope you now understand what is VPC flow logs andhow to enable VPC flow logs to capture the packet information.