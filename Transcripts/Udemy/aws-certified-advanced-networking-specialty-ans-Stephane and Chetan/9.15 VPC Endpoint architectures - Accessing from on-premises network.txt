# 9.15 VPC Endpoint architectures - Accessing from on-premises network

Okay.Now this is very similar architecture to what we have seen earlier, just that this remote network isnot inside AWS, it is outside of AWS.So you want to access the VPC endpoint through the VPN or Direct Connect, which is connected to youron premises network.Now again, with respect to layer three connectivity, there is this connectivity.So as long as you can access this private IP address or the VPC endpoint DNS, you can access the VPCendpoint services.Right.So let's start again.So if you have the VPC DNS it will be resolved from the on premises because it can be resolved publicly.But as you know it will resolve to the private IP address of the interface endpoint.And the connectivity should work.But the same problem, right?You don't want to add that endpoint URL to your commands when you fire that from your on premises host,right?That's not desirable.So what do you do now?Now in this case, we can't follow the same approach as previous because in that case, we had associatedthe private hosted zone with VPC because VPC is a AWS construct, right?And as you know, this DNS cannot be directly resolved from the on premises network.So what do you do for that?For that you have to create route 53 inbound resolver endpoint.And we have covered that in detail in the route 53 section.So basically it allows you to send the DNS traffic to your VPC.And if your VPC has a private hosted zone, it will also resolve the DNS from that private hosted zone.So now as you have the route 53 inbound resolver, you should also configure the DNS forwarder on yourside, which sends all the requests for S3 dot AP South one dot Amazon Aws.com to this route 53 resolverinbound endpoint IP address.Okay.So you would have that.And then your on premise server will get the DNS resolution using this DNS forwarder.And it will get the DNS of the interface endpoint, which will further be resolved to the private IPof this interface endpoint.And finally, your on premise server can access the VPC endpoint privately over a DX or a VPN and ultimatelycan access the VPC endpoint service.So in order to enable this DNS, you need to have this route 53 inbound resolver endpoint.Right.So that's another architecture.And those are the things that you need to take care if you are allowing access to your interface endpointsfrom the remote host, which is hosted outside of AWS.So I hope it is clear.And with that, now let's go to the next architecture.Thank you.