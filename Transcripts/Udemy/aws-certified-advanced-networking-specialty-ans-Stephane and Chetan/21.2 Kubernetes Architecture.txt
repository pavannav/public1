# 21.2 Kubernetes Architecture

Hello,and let's first see the Kubernetes architecture,and here we are going to talk about Kubernetes open source.Now, at this moment,I hope that you already know what are containers,what are microservices, and why Kubernetes.But if not, I recommend you to watch the videofor which I have already provided you the link.Okay, so in this lecture,let's talk about the Kubernetes building blocks,because if you talk about Amazon EK Service,it is nothing but the wrapper over open source Kubernetes.So it's important for usto first understand the Kubernetes architecture.Now, as I said earlier,the purpose of this course is to focus onlyon the networking part,but I think it's essential to know all those core componentsso that when we talk about the Kubernetes networking,you understand what those components are.Now, originally, Kubernetes was developed at Google.But then Google made it open sourced,and now Kubernetes as a project is maintained by CNCF,which is Cloud Native Computing Foundation.Now, CNCF was founded in 2015,and the purpose was to really innovatein the container space.CNCF has a lot of projects,but Kubernetes is one of the top projectwhich CNCF maintains as of today.Okay, with that,let's start with Kubernetes building blocks.Now here we are talking about open source Kubernetesto start with,and then later we'll get into the EKS part of it.Okay, if you look at Kubernetes from the top,Kubernetes is deployed as a cluster,and this cluster consists of control plane and a data plane.As you might have guessed,control plane is something which controlswhat happens in the data plane,and your actual applications runs in this data plane.So control plane consists of master nodes,and these nodes could be one or more,but for high availability of those all control processes,it is recommended that you always run multiple nodesin the control plane.Similarly, data plane consists of something called nodes.You can treat them as like hosts,so that when you deploy your application,they are spread across multiple nodes.So the nodes in this data planes are called worker nodes,and the nodes in the control plane are called master nodes.Now, when it comes to hosting the application,these applications are hosted on the nodes,but they're hosted as something called pods.Now, pods are nothing but a smallest deployment unitin Kubernetes.Now, you always heard about the containers,but then Kubernetes has the deployment unit as pod.Now, one pod consists of one or more containers.So basically you could have multiple containersinside one pod.But for Kubernetes, it always deploys pod,not the container.So it's very much possiblethat you just have one container in the pod,but more often or not,you could have more than one container.Maybe one application container and supporting databasesor cache.So this kind of pods you can build,so that application can work efficiently.So pod ultimately representsa group of one or more application containers.So this is an high level architecture of the Kubernetes.Now, let's see which components lies in the control planeas well as in the data plane.So this is a closer look at the cluster.Now, on the left side, you have control plane,and on the right side you have data plane.And there are other components on the right.You can see here this is a cloud control manager,and we'll talk about that as well.Now, important thing here to know thatwhen you are sitting outside of the cluster,of course, as a person,you would want to interact with the Kubernetes cluster.Now, how does admin talk to the Kubernetes cluster?So Kubernetes exposes the Kubernetes APIsfrom the API server, which is hosted here,and then admin will use either Kubernetes APIsor there are some command line tools like kubectl,which can be used to communicate with the Kubernetes.And we'll see the entire deployment flow at high levelas we understand more about the control planeand the data plane components.So there are a lot of services or objectsin the Kubernetes term,running in the control plane and the data plane,and let's now understand those components.So very first is kube-apiserver.So as the name suggests,kube-apiserver exposes the Kubernetes APIs.That means if you want to communicatewith Kubernetes cluster from outside as an admin,then you make an contact with API server,and then API server will talk to the data planeto make those adjustment to change the stateof Kubernetes cluster.So it's a front-end for Kubernetes control plane.Second is etcd, also called ETCD.Now, this is a key value store as you know.Right now,this key value store maintains the state of your cluster.For example, if you're deploying, say, different pods,and what are the configuration of those pods,you are exposing those pods as a service.Now, what are the portson which these services will be accessible?What are the port on which the pods will be accessible?This is all the configuration informationthat you provide in the form of manifest fileand all this configuration information,and you can say the state of your entire clusteris maintained in the key value store as in the etcd.So it maintains all your cluster data.So whenever some changes you have to make,API call will make those changes persistentinto the etcd key value store.And also if you want to get the state of your cluster,API server will get that state from the clusterand give it as an output on the command line or in the UI.So etcd maintains the cluster informationinto the persistent store.Kube-scheduler, as the name suggests,it actually keeps looking for the right place for your podon the nodes.So maybe there are some nodes on which there is less load,so kube-scheduler will decide how to launchthis particular pod on this particular node.And there are a lot of parameters,not just the compute hardware,based on which it takes decision.You can also change that behaviorbased on node affinity and tented node.There are many features in Kuberneteswith which you can decide this pod should go to this nodeor that node.Maybe some nodes have GPUs and your containers need GPU.In that case, you can also change the behavior,how scheduler will schedule that podto be running on the nodes.So that's the job of a kube-scheduler.Similarly, there is kube-controller-manager.Now, kube-controller-manager consistsof multiple controller processes.Now, as the name suggests, controller process does some job,for example, if you want to schedule a job to be runningat certain time of the day.So job controller will do that.Similarly, if one of the node goes down,then the node controller will identifythat the node has went downand it'll make sure that another node is spun up.Similarly, replication controller,if there is one replica of the podand you want to make it clear replicas, for example,then the replication controller will do that.Endslice controller,it basically enables the services to the pod communication.So whenever you expose your pods as a service,then there is a networking component called kube-proxy,which does that.And then EndpointSlice controllerwill make sure that this communication or linkis established between the service and the pod.Now, similarly, there is also cloud-controller-manager.Now, Kubernetes can be done anywhere.It can be done on premises,and it can be run in the cloud as well.And Kubernetes has extensible architecture,which means you can integrate Kuberneteswith the cloud resources.For example, Kubernetes has a load balancer as a service.But then you can use instead,cloud provider's load balancer.For example, you can use application load balancer,or network load balancer in Kubernetes.Now, Kubernetes itself doesn't knowhow to create application load balancer.And for that you have the cloud-controller-manager,through which you can extend the Kubernetes capabilitiesto the cloud.So AWS will provide AWS cloud-controller-manager.Similarly, other cloud providerwill develop their own providers,and you just plug it into the Kubernetesand then it can provision those resources for youwhen you make some deployments into the Kubernetes.So likewise, these are the important processeson the control plane side,and this is very extensible, as I said.You can put as many add-ons, which are available,open source or third-party developed.But these are the core Kubernetes control plane component.Now, with that, let's move to the data plane.As you know, data plane consists of nodes.Nodes are nothing but the hosts.And then nodes have essentiallythree important components running.As you can see there, nodes host the pods,pods are nothing but one or more containers,and which essentially means it runs your application.Then every node has something called kubelet.What is kubelet? It's an agent which runs on every node.And kubelet is a agent which takes commandfrom the API server.For example, API server says, "Run this pod."Now how API server communicates with this node?Because there is a kubelet running on that node.So it's an agent that runs on each node in the cluster.So it makes sure that the containers are running in a pod.Similarly, kubelet also register the state of your podsto the API server.Suppose one of the pod goes down,then kubelet will report it to the API server,and API server will then add that to the ETCD, likewise.Kube-proxy, now, this is very important.Kube-proxy basically enables the network communicationto the pods from network station,inside or outside of your cluster.What it means that pods can communicatewith each other, right?And when you want to expose pod as a service,because maybe there are multiple pods for the same service,of course, in order to scale that, right?So you can expose your pod as a service.So kube-proxy enables that creationof the service basically.And then you can also expose your servicesto the external world, similar to the load balancerand EC2 machine behind the load balancer.You can also have your pods behind the load balancer.So kube-proxy is the native Kubernetes networking component,which enables all this communication.Finally, container runtime.Because you're running containers,you need a container runtime engine inside this node,for example, if you're using Docker,then you would be having Docker engine.Now, Docker underlying use Containerdas a container runtime.So in Kubernetes, on every node,you need to have the similar container runtime.Now, Kubernetes supports a lot of container runtimes,not just Containerd,but it also supports CRI based,or CRI-based container runtime engine.CRI is nothing but Container Runtime Interface.And then there are many implementation of CRI.So Kubernetes can very well use those as well.But as of today, Containerd is the most popular one,which is used by most of the leading container technologies.So that's what the data plane looks like.Now, let's look at the processwhen you want to launch any pods, or say application,inside the Kubernetes cluster,then what that flow will look like.So for example, there is a developer on the left side here.You have your Kubernetes cluster on the right side,and there are users of the application sitting here.So developer, what it will do,is write a code for your application, of course, right?This code will then be unit tested, compile, build,which ultimately produce some artifacts.So maybe if it's a Java application,it could be JAR or WAR file.If it's a C or C-plus-plus code,then it could be some binaries and libraries.So that's an artifact which will ultimately be deployed.Then you have DockerFile.Now, DockerFile basically builds the Docker image.Now, if you are aware of the Docker,you must very well understand that, but ultimately,from the instructions of the DockerFile,where my code is or my deployables are,and which port this container will be running,or all this information is there in the DockerFile.And basically DockerFile takes those instruction,takes your code artifacts,and build the Docker image out of it.It will install required libraries, binary, everything,and it will create a DockerFile out of it.So you run the Docker build command,and then it will create the image,and then you push that image to the registry.So Docker Hub is a public registry that you can use,or if you're operating in AWS, then you can use Amazon ECR,Elastic Container Registry.There are many such registries available.But ultimately you push your imageto the container registry.So you build the image,and now you want to deploy this imageinto the Kubernetes cluster.Now, for that developer or maybe the ops personwould write a deployment file,also called as Kubernetes manifest file.So this is a declarative file in the YAML format.Basically tells Kubernetes how to deploy this applicationto the Kubernetes cluster.So somebody will write this deployment manifest file,which will tell Kubernetes control planehow to deploy this application.So to tell Kubernetes API, you use some kind of tools.Either you can use Kubernetes API,which are available over HTTPS,or Kubernetes has a command line tool call kubectl,or kubectl, whatever you call it.So kubectl will take your deployment fileand it will talk to your API server.Of course, there will be authentication,but we are not getting into that as of now.So with kubectl and Deployment file,it will reach to the API server as an instruction,and API server will then communicatewith control manager ETCD scheduler,and if required, to the cloud-controller-managerto take a decision what to do about this deployment.So it will make those calls,and then finally it will talk to the kubeletto deploy this to one of the node.Now, kubelet receives those instruction.What kubelet will do,kubelet will communicate with the container runtime engine.For example, Containerd in terms, in case of Docker,container runtime engine will understandthat there is one Docker image,which is available in Docker Hub,which needs to be deployed as a container or as a pod.So container runtime enginewill pull that image from the Docker Hub.Now, for that container runtime engineor a node would require permissions,all that would be a part of authenticationand authorization.But ultimately it will get that imageand then it will run it as a pod.Now, once the pod is running,of course it will be exposed to the external worldusing the kube-proxy, or load balancer service,but some kind of service will be createdand then the end users could access those over this service.Now this is how you deploy the application,and this application is available to the end user.So this very high level flow,and there are many steps in between which are skipped,but hopefully you would understandhow typically things work in Kubernetes world.So with that, we'll stop here.For Kubernetes architecture,I hope it is clear that Kubernetes has control planeand data plane,and what are the componentsof those control plane and data plane.With that, now, we'll move to the EKS architecture.EKS is nothing but the Elastic Kubernetes Service in AWS.And it is very much conformantto the open source Kubernetes.But we'll see how the architectureof the Kubernetes cluster looks into the AWS world.So thanks for watching and stay tuned. Thank you.