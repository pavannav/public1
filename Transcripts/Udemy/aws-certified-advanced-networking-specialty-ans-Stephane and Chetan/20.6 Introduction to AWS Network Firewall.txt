# 20.6 Introduction to AWS Network Firewall

Hello and welcome to this lecture,AWS Network Firewall.So, as of now we have seen a couple of firewalls already.You know how security group worksand you know how the network ACL works.And we briefly talked about AWS WAF and AWS shield as well.Now, considering that we have so many firewallsalready in AWS, then you must be wonderingwhy this new firewall, that is AWS Network Firewall.Now, before we really jump into the detailsof AWS Network Firewall, I want to let you knowthat there are two types of firewalls typically, right?And we also talked about this in contextof security groups and network ACL.So, one is the stateful firewalls.Now what the stateful firewall means?It means that it filters the trafficbased on network packet context,as well as connection state.But what does that mean, right?So the context means that it involves the metadataof the packet including the IP addresses, ports,packet length, information about, you know, reassemblyand defragmentation flags, TCP sequence number.So it's not just about source IP addresses,source port destination IP addresses, destination port,and protocol, right?It is much more than that.It actually looks at your connection, its state,if you're using say ICMP then, is your packet allowedto be defragmented, and similar things.So that's a context of the packet.And state refers to the connection of the state.For example, during the TCP handshake,you know that there are SYN flagsand acknowledgement flags are set, right?And then the handshake complete.So stateful firewalls can also look at these flagsand can decide whether you wantto allow that traffic or not.And opposite of the stateful is stateless firewall.So when we talked about network ACL,we said that network ACL is a statelessbecause network ACL rules operate in isolation.That means it doesn't care whether it's a traffic,is a part of a connection, which is already approved.It always looks at the packet in isolationand apply all the rules that you write there.So at high-level, these are the two typesof the firewall that you will find.And if you talk about AWS Network Firewall,it supports both stateful rules as well as stateless rule.But overall, you can consider the AWS Network Firewallas a stateful network firewall,and which is also an intelligent intrusion detectionand prevention service for your VPC, right?So if you look at this architecture,you know that inside VPC you have subnetsand then you launch EC2 machines,and then you want to put a firewall.The first thing you will use is a security group,which works at an ENI level.And if you want a subnet level protection,then you will put network ACL.Now, this is very simple.The traffic which is coming from or goingto the EC2 instance, it has to passthrough both these firewalls.So this works well.But if you look at these two firewalls, right?Network ACL and security group, they just allow you to,you know, provide the source IP address rangeor a destination IP address range for outbound rulesand then the port numbers, right?The ports of the EC2 machine where the traffic can reach,or the port number of the destination machine,where the outbound traffic can reach.It doesn't really consider, you know, the payloadof the data, the header information, all this is not checkedby these two firewalls.And that's where you can't really dothe deep packet inspection with these two firewalls.So the solution to that is of course,AWS Network Firewall.The way it works is you provisionthe network firewall endpoint inside your VPC.And when you say VPC, that means again,there are subnets and then all the trafficwhich is going to your machines inside your VPC,it should be routed through this firewall endpoint.So first the traffic will land into firewall endpoint,it will then go to AWS Network Firewall.It will inspect your packet as per the rulesthat you have written, and finally,the traffic will reach to your instance.And same applies for the return traffic as well,or the outbound traffic, which initiatesfrom your EC2 instances, right?So this is how the network firewall worksat very, very high-level.And now let's try to answer the question,why one more firewall, right?So you already have security groups,you already have network ACL,you already have WAF, but then what is differentabout AWS network firewall?So first thing protection at,at which level it provides the protection,security group, EC2 instance level,or you can say at ENI level,network ACL provides the protection at subnet level.WAF is an endpoint which protects youat load balancer and CloudFrontand you know that it works at layer seven.However, network firewall will provide the protectionfor all inbound and outbound trafficto and from your VPC, right?Stateful or stateless,you know that security groups are stateful,which means that any traffic which is coming in itallows the return traffic and in case of network ACL,you have to look at the packet in isolation.That means it's stateless.WAF is also stateless and network firewallallows both stateless and stateful rules.So, OSI layer security group and network ACL worksat layer three and layer four.WAF works at layer seven.Network firewall will work from layer three to layer seven.Now if you talk about the features,again, security group allows you to filter traffic basedon the IP port and protocol, same with the network ACL,and in case of WAF, you can write various rules, right?Whether cross-site scriptingor blocking particular IP address or rate-limiting rules,but in case of network firewall,it is much more intelligent.So you can write a stateless rules or stateful rules.You can block access to a particular domain,or you can detect the protocolslike whether it's a TLS connection or not.And it can also allow you to have a large IP blocksto be, you know, whitelisted or blacklisted.So network firewall will give you all this protection.Now with respect to the flows, security group allows,or you can apply the security group for both ingressand egress traffic, same with the network ACL.In case of WAF, as I said,only the ingress traffic is filtered.And in case of network firewall, again,you can apply the firewall rules in both the directionsand it is applied at the perimeter of VPC.That means whether traffic is flowingthrough internet gateway or virtual private gateway,or whether DX or VPN, or between the VPCto VPC communication, right?So if you look at this network firewallhas much larger scope than the other firewalls.Okay, so I hope that is clear.And now let's look at quickly,what are the features of network firewall?So it filters the traffic at the perimeter of the VPC,such as the traffic going to or comingfrom the internet gateway, NAT gateway,over a VPN or a Direct Connect.And for stateful rules specifically,you can use something called Suricatawhich is an open source rule set,which has been developed by the community.So Suricata is an open source-based intrusiondetection system and intrusion prevention system,in short IDS and IPS.And you can use all the Suricata rulesinto your network firewall.It also allows you to filter the domain names.That means if you don't want to allow accessto any other domain than to aws.amazon.com,you can do that with the network firewall.Also it can block access to the bad domain.You can also use wildcard into a domain name,and it also allows you to do the deep packet inspection.So what that means is it does not only just lookat the packet header, but it can also lookat the packet payload and maybe edit out informationabout the packet, right?So that kind of deep packet inspectionyou can do with the network firewall.Okay, next is about the protocol detection.So you know that when the traffic is goingover 443, then you assumethat it's an HTTPS protocol, but that isn't necessarilythat you can run different services alsoon the 443, but the network firewallcan intelligently detect that the protocol is reallyan HTTPS and that's where you can say any other trafficthan the HTTPS you want to drop it, right?You don't want to allow that.So this is possible with network firewall.And finally, you can import your rules which are writtenin a common open source rule format like Suricata,as well as, if there are third party partners,you can also integrate network firewall with themand you can, you know, import those rulesfrom those solutions as well.So in short, that is what the network firewall is.And now let's look at the firewall components,routing, VPC architecture and how to write those rules.So, thanks for watching.