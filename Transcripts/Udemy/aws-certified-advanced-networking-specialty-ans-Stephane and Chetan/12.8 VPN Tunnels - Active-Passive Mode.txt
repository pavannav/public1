# 12.8 VPN Tunnels - Active-Passive Mode

Hello and welcome to this lecture.Now in this lecture,let's look at some additional scenarios,when you have configured your site-to-site VPN tunnelsin active mode or passive mode.Now, you know that AWS site-to-site VPNprovisions two tunnels, that's for high availabilityand it's up to you how you configure this tunnels,weather in active-active mode,which means both the tunnels are up at the same timeand you can also configure it in active-passive mode,that means only one tunnel is up at any moment of the time.Now in this case, let's see how the routing happens.So, whether you're using the static routingor dynamic routing for your VPN connectionwould affect the flow of the traffic,now let's try to understand that.Now this is a very much familiar architecture,where you have VPC and a corporate data centerand they're connected using site-to-site VPN connection,on AWS side, you have virtual private gatewayand on the customer network side, you have customer gatewayand we have shown two tunnels here,that's what VGW does, right?Now in this case, this is a static routing scenario,that means you have already configured the routeson both sides of the networkwith the destination CIDR for respective network CIDRs.Now if from the corporate data center,if the traffic initiates, it goes to the CGWand from there, based on your routing configuration,it would flow through tunnel one or twoand it reaches AWS,then it would reach to the destination EC2 instance hereand from there, the return trafficwill again head back to virtual private gateway.Now there is one problem,when VGW receives this traffic from AWS EC2 instance,it has choice to send this traffic backthrough tunnel one and tunnel two.And in case of static routing,it would make that choice randomly,that means it can either send this trafficthrough tunnel oneor it can send traffic through tunnel two.Now, if it sends traffic back through tunnel onethere is no problem,even if there is a firewall hereit would understand that this is a return trafficfor the request which I had just sentand it will allow that trafficbecause we are considering the stateful firewalls.Now what if the traffic flows from tunnel two this time,the return traffic we're talking about?Now here, if there is a firewall on customer gateway sideand it's a stateful firewall,it would probably reject this traffic,because it does not have the state of the requestwhich was outgoing earlier, right?It will say that, I don't understand this trafficand it will probably drop this trafficand this routing is typically called as asymmetric routing.And that's a problem in some of the firewallsand some firewall might support itand in some firewall you might have to explicitly enablethe asymmetric routing.So if you are using both the tunnels with static routingin active-active mode,then make sure thaton the customer gateway side of the firewallyou enable the asymmetric routing.So that's one point.Now, if your traffic originates on AWS side of the network,for example, this EC2 instance herewants to send some traffic to any of those serversin corporate data center,it will send the packet to virtual private gatewayand then this VGW will randomly selectone of the tunnel,either tunnel 1 or tunnel two,you can't really influence that decisionin case of static routing.So this is just to make you understand thatin case of static routingthe tunnel selection happens randomlyon virtual private gateway sideand for the same reason,you would have to have the asymmetric routing configuredon the other side of the network.Okay now let's move on and look at a different scenario.Now, again, we will look at the static routingbut this time we have active-passive tunnels.Now what that means is,only one of the tunnel up at any moment of the time.So tunnel two is for example down,if you remember our hands-on exercise,we had set up opens one tunneland we had configured only one tunnel at that time,we did not configure another tunnel.Now this is a similar situation.Now in this case,let's walk through the same scenario again,from your corporate data centerif traffic goes to AWS first,it will go to customer gatewayfrom there only one tunnel is up,there is no choiceand you have to get to the VGW,VGW will send traffic to EC2and from there return traffic will come to VGW,now VGW will see that there is only tunnel up,so there is no choice to go through another tunnelof course it will send traffic back from the same tunnelthrough which the traffic came.So in that case,you don't really need to worry about asymmetric routingbecause there is only one active tunnel.Now in this case what happensif this tunnel goes down, right?There is no high availability.Now, that's your responsibility that if tunnel one goes downyou'd have to make sure tunnel two comes up automatically.And that is more to do on your side of the network,so many routers like Juniper or Ciscowould have some mechanismwhere it can automatically detectthat the tunnel one is downand then they will bring up the tunnel two automatically,in case of open tunnel two,we could have written some scriptswhich continuously monitor the status of tunnel oneand if tunnel one goes down for some reason,it will bring up the tunnel two.Now that's active-passive tunnels in that sense.AWS does not have to do anything there,this is completely in your control,which tunnel should be up at any moment.So that's about the active-passive tunnelsin case of static routing.Okay now let's move to the dynamic routing, what happens.You know that in case of dynamic routing,the routes are selected based on various BGP preferences.So let's see how it affects your traffic flow.So, this time, we have active-active tunnelsand it would make sense to have only active-active tunnelsbecause then there is no purpose of having two tunnelsfor high availability, right?If it's an active-passive,then it's kind of static routing you're doing,if tunnel goes down,you would have to manually bring up the tunnel.But with dynamic routing,you can use both these tunnel at the same time,in highly available fashion.What that means isyou can influence the VGW side of the tunnel selectionand say, always use tunnel one probably for some CIDRor maybe using some BGP attributes that we had learned,so VGW will always use tunnel oneand in case tunnel one goes down,it can automatically then switch over to tunnel twofor all the traffic,now that's the benefit of using dynamic routing.So in this case what we are saying is,again, corporate data center is originating the traffic,it goes to tunnel one and tunnel twobased on your configuration,it will reach to virtual private gateway.Now, if you know that if you look at the static routing,same thing might happen here,the VGW might send the traffic back through a tunnel twowhich will cause asymmetric routing.But with dynamic routing,with BGP you have ways to influence the decision hereon the virtual private gateway side,saying that, always use tunnel one,because maybe I am pushing the more specific routesthrough tunnel a one.So advertise more specific prefixesover tunnel one tunnel two.What that means is,maybe this server fallsinto a subnet range of 192.168.0.0/24and so for this BGP sessionyou can publish the CIDR as 192.168.0.0/24and for tunnel two,you might want to advertise 192.168.0.0/16.Now /24 is more specific than /16,so VGW will always prefer the tunnel onefor the return traffic, right?As you know,you can also influence the routing decision in case of BGPusing something called ASPATH,that means if tunnel one has shorter ASPATH,VGW will always prefer that particular pathrather than tunnel two pathif it is longer than the tunnel one is ASPATH,so you can also influence that decision.When ASPATHs are having the same length,then you can use third BGP parameter,that is MED, multi-exit discriminator.What that means isthe lowest MED value would take the preference.So from BGP end, from the customer gateway endyou can advertise different MED valuesfor both the tunnelsand here, VGW of course will choose the tunnelwhich has lower MED values.Now this is only possible in case of dynamic routingbecause you are having to BGP peers,one at VGW and other at CGW end.In case of static routing there are no such BGP parametersand that's where all that problem occurs.But in this case, if you have lower MED values,of course VGW will use tunnel one alwaysas a preferred tunneland then the traffic will go back to your data centerand there will be no asymmetric routing.Now the best part is,if tunnel one goes down for some reason,you have another tunnel up all the time,so all the traffic now will be routed through tunnel two.So we are basically influencing the tunnel decisionon AWS side of the networkusing this BGP parameters.So this is very interestingand very important to know for you,may not be directly asked in a exam,but maybe in your day to day workyou can analyze why you have asymmetric routingand how to fix that.So, that's it about the active-activeand active-passive routing.So just a summary in case of static routing,active-active tunnel mode,AWS randomly select the tunnelfor the traffic originating from AWSand make sure that you configureor enable the asymmetric routingon your customer gateway side of the firewall.In case of static routingwith active-passive tunnel mode,that means only one tunnel is up at the moment,you don't have to really worrybecause always that tunnel will be usedfor both sides of the communication.But it's your responsibility thatif one tunnel goes down for some reason,another tunnel is brought up in possibly automated fashion.Now in case of dynamic routing,of course it will make senseto have active-active tunnel mode onlybecause, active-passive,you are just not leveragingthe high availability of the tunnels.So in that case, all the BGP parameterswill come into the picture,so you can influence the decisionon AWS side of the connectionthat which tunnel to useusing maybe a more specific route,more specific prefixesor using shorter ASPATHor lower multi-exit discriminator value.So, that's how you can influence those routing decisions.So that's it about this lecture,I hope you understood the network flowand thanks for watching.