# 20.11 Gateway Load Balancer and Traffic flow

Okay so,now let's see how Gateway load balancersolves these problems, right.Now here we are showing two VPCsbecause this is what the ideal architecture would be,but you can very well have just single VPCand you could have Gateway load balancerand endpoint and applications and firewallsin the same VPC as well.Bare minimum, it is required that your application sub-netand Gateway load balancer endpoint subnetshould be different.And what that means, I will explain that shortly.But to have better architecture,I'm showing here the two separate VPC for application VPCand network appliances with VPC.And I hope that is fine.So on the left side, you have application VPC,which obviously would have your application,typically a web application,probably running in a public subnet.but let's not go into the detailsat the sub-net level as of now.Let's try to understandhow Gateway load balancer architecture looks like.So on the right side,you have a network appliance VPC,which means there will be multiple network appliances.And obviously as the name suggestsGateway load balancer should be able to balance the loadacross this network appliances.So in the network appliance VPC,you would have the Gateway load balancer provisioned,which ultimately sends traffic to this network appliances.Now, in order for the application VPCto send the traffic to the Gateway load balancer,you need something called Gateway load balancer endpoint.This is very similar to the VPC endpoint.So you might recall from the VPC private link lecture,where you could host your own VPC and point services.And then in order to access these services,you have to provision the VPC endpointinto your consumer VPC.Now, similar architecture here, in that case,you have Gateway load balancer endpointin the application VPC.And this sets up the private linkwith the Gateway load balancer.There is one difference thoughbetween the Gateway load balancer endpointand other VPC endpoint.You can add the Gateway load balancer endpointas a next half in the route table.Which you can't do with the VPC endpoints otherwise.And that's where it allows you to, you know,redirect traffic from internetto the Gateway load balancer directly.And we'll talk about that flow shortly,but this is one difference that you will notice here.Now, if you look at the traffic flow,as the ingress traffic hits the Internet Gateway,somehow you will modify the route tableand will redirect all this trafficto the Gateway load balancer endpoint,instead of directly sending it to the application.And then Gateway load balancer endpoint,will send that traffic by defaultto the Gateway load balancer,and then Gateway load balancerwill forward that traffic to the network appliances.And once the traffic is returnedfrom the Gateway load balancerto the Gateway load balancer endpoint,then it will hit the application.And that's where every packet will be,go through this network appliances.Now, one thing to understand, that Gateway load balanceruses something called Geneva protocolto send traffic to the network appliances.Now, if you've never heard about the Geneva protocol,it is basicallyGeneric Network Virtualization Encapsulation protocol,which is a wrapper over L3 traffic.So as you get the packet at L3 level, right,it encapsulates that trafficand puts its own header on top of it.And the benefit is that your packet is unchanged.That means the sourceand destination address of the packet remains the same.So your network appliancesactually knows that the trafficis coming from a particular IP address.It can remove those encapsulation headersand actually can see the sourceand destination traffic IP'sand the ports for your packet, right?So that's a Geneva protocol.Now, there is some history to why Geneva protocol was made.Earlier there were other protocolsor maybe a different vendors have their own protocollike Cisco developed GRE,that is Generic Routing Encapsulation.It's a tunneling protocol, as you know.And there are certain wide variety of the, you know,such internet protocol for Encapsulations.So in order to really have a standardized wayof this encapsulation,Generic Network Virtualization Encapsulation,that is Geneva protocol was developed.Okay, back to the original flow of the traffic.The network appliances will send the trafficback to the Gateway load balancer.It will come back to the Gateway load balancer endpoint,and then it will again hit your application.And application will then return the trafficto the Gateway load balancer endpoint.And the similar flow will apply now.Instead of directly going to the internet Gateway,the traffic again, will go to the Gateway load balancerto the network appliances, will come backand finally Gateway load balancerwill send it to the internet via this internet Gateway.So if I talk about this architecture, it's simple,low-latency, scalable,because here there is a fleet of network appliancesand HA as well.HA because Gateway load balancer supports the health checks.And if one of the network appliance is not healthy,it will automaticallysend that traffic to another network appliance.Now, good thing about the Gateway load balancer is that.it does what other protocols cannot.For example, you know ECMP, right.Equal cost multi-part.Now the problem with ECMP is thatit cannot really maintain the symmetric flow of the traffic.That means if the traffic isgoing through one of the tunnel,it may come back from another tunnel, right?It's asymmetric routing typically.But Gateway load balancer maintains the symmetric flow.It knows the source of the flow.And it will send the trafficto a particular network appliance only.And we'll send the response back to the same flow.So this allows sending the bi-directional traffictransparently over the same consistent route, right?And this is similar to, you know, bumping the wire target.Like it also keeps the stickinesswith the network appliancesand that's where it makes it a great choicewhen you have to maintain the symmetric flowand the stickiness.And which also means you could have multiple VPCssay multiple spoke VPCs,all sending to the trafficto the same Gateway load balancer,but the traffic will be routed back to the VPC,which originated that traffic.So this is what Gateway load balancer provides you.Now let's see how the traffic flow looks like.Now I am using here one availability zone,but for high availability, as you know,that should be multiple availability zones.So Gateway load balancer endpointshould be provision across multiple ECbecause scope is as EC level, right.On the right side, you have network appliance VPC.Here I have shown two EC'sand then network appliances are across multiple EC's.And then Gateway load balancerwill send traffic to this network appliancesor port 6, 0, 8, 1 using the Geneva protocol.Okay. So there are users hereand they want to access your application herein this EC2 machine.Now let's look at the route tables,how they should be look like, right.Now user will hit the Internet Gateway.Internet Gateway knows where my EC2 instance isand it converts your public IP traffic to the private IP.And it typically redirectsthat traffic to your EC2 instanceshaving the public IP, right?But in this case, you want to modify that behaviorand say, if the traffic is going to the EC2 instances,it should first go to the Gateway load balancer endpoint.And that's where you would have an ingress routing tablefor your internet Gateway.And you will say if the trafficis going to this particular sub-net,it should go to the Gateway load balancer endpoint first.Okay, now let's look at the other route tables.So there is a route table for yourGateway load balancer endpoint, subnet as well.Now in this sub-net,there is an route table entrywhich says the traffic going to the internetshould go through the Internet Gateway.Now this will be used for the return traffic.As the ingress traffic will hit the load balancer.We'll come back, we'll go to the EC2 instances.And then the return traffic will come back.Then it knows how the traffic should go back to the usersvia this internet Gateway.And that's where this entry is.Now, if you talk about the application server route tables,you need to think right,how the traffic will go back to the internet.It shouldn't directly go back.It should again go back through thisGateway load balancer endpoint.And in that case again, you would have entry,which says if the traffic is going to the internet,it should go to the Gateway load balancer endpoint, right?So this is how the route table should look likefor your application server subnet as well.Now, if you look at the traffic flow,there will be users,who will be hitting the public IP off your EC2 application.It will hit the internet Gateway.Internet Gateway will redirect that traffic toGateway load balancer endpoint,and it will further take this trafficto the Gateway load balancer.It will distribute that traffic to the network,with your appliances.It will get the traffic from there.Now Gateway load balancer, we'll look at the route tableand we'll see the traffic isthe destination for the EC2 instances.It will send the traffic EC2 instances.EC2 instances will response backto the Gateway load balancerbecause there is this entry.And in this point, a key in the Gateway load balancerwill send traffic to the Gateway load balancer.It will return the traffic.And now this route tablewill be used to send the traffic back to the user.So this is how N2 inflow look for the ingress traffic.Now you must be wonderingwhy the Gateway load balancer endpointwill only apply this particular entryfor the return traffic, right?Why not for the incoming traffic?Now Gateway load balancer endpoint understand thatthe traffic has been originated within the VPC,or it's been originated by the Gateway load balancer.If it's a Gateway load balancer,then only it applies this route table.Otherwise the direct traffic,which is coming to the Gateway load balancer,which is not a return traffic,will, it will directly send it to the Gateway load balancer.I know this could be a little confusing,but if you look at this architectureand understand this flow,I hope it will be clear to you.So this is how the ingress traffic flow works.Now, if EC2 machine wants to access the internet,that is an ingress traffic for this,similar flow would apply.So in this case, the EC2 instances will send the trafficto the Gateway load balancer endpointbecause there is this route table entry.Now Gateway load balancer will then again,send the traffic to Gateway load balancerto the network appliance.It will come back to the Gateway load balancer endpoint,and finally it will apply this route tableand we'll send the traffic to Internet Gatewayand out the internet, right?And then the reverse flow for the return traffic will apply.That we just talked about for the ingress traffic.So this is how the overall architecture should look likefor your Gateway load balancer.So I hope that is clear.And with that,now we will look into some of the propertiesof the Gateway load balancerand then we'll also do some labs around it.So thanks for watching.