# 16.1 VPC lattice introduction

Hello and welcome to this new section Amazon VPC lattice.Okay, so until now in this course we have talked about different networking services.And how do you connect different networks.For example we talked about VPC peering connection.We talked about transit gateways, site to site VPN connection direct connect everything.Right.But if you look at this networking connectivity it is typically network to network connectivity.And there we did not really talk about application to application connectivity.Now what is the difference.So difference is that when we talk about network to network connectivity, that means any host in onenetwork can communicate with any host in other network.And that works well when you want to connect to networks.Right?But what if you only want point to point connectivity between the applications?And one of such application networking we had seen was VPC endpoints, but still it doesn't really workin ideal way.The ideal way would be where application can only communicate with only required application.Irrespective of where they are located and in order to solve this problem.In year 2023, AWS had launched Amazon VPC Lattice service.So VPC lattice is the application networking service and it simplifies.Service to service communication.Further, Amazon VPC lattice provides the secure and consistent connectivity without you required tohave, say, VPC peering or transit gateway.And in the following lecture we will see how exactly it works.Now, other than the connectivity, it also provides the service discovery capability.And that is typically the capability of the service meshes where there are the sidecar proxies usingwhich you can communicate transparently with other services.And further it also has the dynamic routing.This is very similar to how load balancers work, where you can have, say, path based routing or.Weighted routing.So all those features are there in the VPC lattice as well.And finally, this is not just about the networking, but it also provides the authentication and authorizationfor the connectivity.So basically it provides the zero trust architecture where you can manage the access to different servicesusing AWS IAM authentication.So overall this is what is VPC lattice.And typically you will see VPC lattice useful in the microservices architecture where there are differentservices.And typically these services are built by different teams.These services might reside in different VPCs or across different AWS accounts.Now if you want to enable the communication between all these microservices, then probably your architecturemight look something like this, right where different Microservices are running on different computeenvironment, so some microservices might be running on EC2 instances.Some microservices on the ECS or EKS or even as a Lambda function.And further, as I said, these services may reside in different VPCs.So if I just have to redraw this picture, then it could be something like this where different serviceshave different domain names as well, and they are located across different VPCs.Now at this point, if you want to enable this communication, you will think of say VPC peering ortransit gateway, right.So without VPC lattice you might have VPC peering where you have point to point connection between theVPCs.But as you can see here, as the number of microservices increases, number of VPCs increases.This adds a lot of complexity.And further, as I said, this is network to network connectivity, which means if this VPC has someother resources, you might be able to reach to those resources from your another VPC.And typically for application communication we don't want that right.So clearly VPC peering is not a great solution for this use case.Now you might be thinking then we can look for transit gateways.Now again Transit Gateway simplifies the networking architecture.But there is a same problem here.Also we are connecting two networks and not really connecting to applications.Okay, so even the transit gateway is not ideal for this use case.Then let's look for VPC endpoints right.In that case you might be exposing all these services through the endpoints.Something like this right.And then in all other VPCs from where the service needs to be communicated, you will create VPC endpointfor that Privatelink service.Now this might work, but you can imagine how many endpoints you would have to create because it isbetween one service and one VPC.So for every service you would have to create endpoints in every other VPC.So while it is pretty close to what we want, but again it is very complex architecture and it is notat all scalable.Further, it doesn't provide you the service discovery.And also it doesn't provide you IAM level authentication.And for the same reason, we need more sophisticated service which allows this point to point applicationto application or service to service communication.And exactly for that we have Amazon VPC lattice.Now in this case you create your services and you attach these services to the VPC lattice network.Now once all these services are part of the network then all these services can communicate with eachother.And there you can also apply some authorization policies so that you can control which services cancommunicate with each other.So while using the VPC lattice, typically there are two personas the administrator and the developer.So the administrator will create the service network and they will define the access control for thatnetwork.And then developer will create their own service and associate this service with the VPC lattice servicenetwork.So you can see that VPC lattice is more developer friendly service where developer can take their owndecisions to create the service.And then enabling the connectivity for that service.Right.So that's in a nutshell is VPC lattice.I know that we just talked at a very high level.But as we go through this section you will understand how exactly it works.And then what are the architectures that evolves using VPC lattice service.So in the following lectures let's dive deep into the VPC lattice component and how it works right.So we'll stop for this lecture.And with that I'll see you into the next lecture.Thank you.