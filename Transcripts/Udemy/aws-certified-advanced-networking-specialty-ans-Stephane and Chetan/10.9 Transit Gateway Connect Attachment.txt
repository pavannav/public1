# 10.9 Transit Gateway Connect Attachment

Hello, and welcome to this lecture,Transit Gateway Connect Attachment.So you already know that Transit Gatewaysupports different type of attachments.The very first stage VPC attachment,then you could also have Direct Connect attachment,where you connect Direct Connect to Transit Gatewayusing Direct Connect Gateway.Then you could also have the VPN attachmentwhere you can terminate your IPsec VPN tunnelsonto the Transit Gateway.And then there is one more attachment typewhich is Transit Gateway Connect.Now this attachment is used for connecting Transit Gatewayto the virtual appliances, such that your SD-WAN routers.And this is essential whenever you want to extendyour existing SD-WAN network to AWSusing the Transit Gateway.And that's where it becomes important attachmentto learn about.So at high level, you can see here,there is a Transit Gatewayand then you could have your virtual appliancesrunning inside a VPC.And now this virtual appliances can be connectedto the Transit Gateway using this connect attachment.And for establishing this connect attachment,you need to create a GRE tunnelbetween Transit Gateway and this virtual appliance here.So again, you can create Transit Gateway Connect attachmentto establish a connection between Transit Gatewayand third-party virtual appliances,which are running in a VPC,or it can also run in on-premise network.And we'll talk about that architecture as well.Now for connecting this Transit Gateway Connect attachment,you need a transport attachment first.Now, what is transport attachment?In order to connect this appliancesto the Transit Gateway before creating this GRE tunnel,there should be a transport mechanism.So there are essentially two mechanismsby which you can connect these appliancesto the Transit Gateway.One is a VPC attachment.That means the VPC in which this appliances are runningcan be connected to the Transit Gatewaywith normal VPC attachment.And this is a case when your appliances runninginside a VPC, but if your appliances are runningon-premises, then you can use Direct Connectas your transport attachment.And then it supportsGeneric Routing Encapsulation tunnel protocol,and it provides you higher performance.And it also supports BGP.In fact, for connect attachment,it is must to have the BGP peering.Now, if you look at this architecture here.On the left side, you have VPCs,which are connected to the Transit Gatewaywith regular VPC attachment.And our appliance is running inside a VPC.And this VPC is also connected to the Transit Gatewaywith regular VPC attachment.And that's the transport layer as of now.Now once you have these attachments createdfor Connect attachment, you can create on topof this transport mechanism.So one end is Transit Gatewayand other end is this virtual appliance inside of VPC.And you can create now the Connect attachment.And as you create a Connect attachment,then GRE tunnel gets established and you needto also have the BGP peeringbetween these two end points here.So ultimately, there will be a BGP session configuredbetween the Transit Gatewayand the network appliance running there.And there will be BGP configurationslike inside IP addresses as well as external IP addresses.So Transit Gateway will have a unique IP addresswhich you can assign to the Transit Gatewaywhile you create itor you can modify that address later as well.And appliance would have its own IP addresswhich is called peer IP address here.Now you know that BGP also need inside IP addresses.So you can allocate an IP address rangefrom 169.254.0.0/16 range.And out of this 169.254.0.2.5 are reserved.So you can use any further IP addresseslike 169.254.6.0 onwards for BGP peering station.And this is what is shown in this architecture.Now let's understand how you can use VPC attachmentas the transport mechanism.And how can you use Direct Connect attachmentas your transport mechanism.Now, in the first case, your applianceis running inside a VPC.So architecture will be something similarto what you see here, but in case of Direct Connect,your appliances are running in on-premises network.So let's look at the first casewhen the VPC transport attachment is used.So you can see on the left side,we have VPCs connected to the Transit Gateway.And then we have virtual appliances running inside a VPC.And this VPC is connectedto the Transit Gateway or the VPC attachment.And on top of it, we create the connect attachment.We form the GRE tunnel and there is a BGP sessiongoing in between.Now in order for your on-premise networkto connect to this VPC, you can use different mechanisms.So one mechanism is you can use either a Direct Connector you can have internet-based connectivitybut ultimately, you are extendingyour on-premise network to the VPC.And then from there,you are creating this Connect attachmentto the Transit Gateway.Now, this architecture makes sense when you have to runthis virtual appliances inside AWS.Similarly, if you have your virtual appliancesrunning on-premises network, then in that case,you can connect your on-premise data centerto Transit Gateway.Now, you know that in order to connect Direct Connectto the Transit Gateway, you need data connect gatewaywith the transit weave.And once that connection is established,that is used as your transport mechanism.And then you can do the same thing again.Once that connectivity is establishedyou can create then GRE return on top of it.And then you can set up a BGP peering session.So ultimately, your Transit Gateway connectcan be used as a third-party branchor a customer gateway applianceturning in an on-premise networkthat uses AWS Direct Connect as a transport.So these are two different architecturesfor connect attachment.And remember that whenever it talksabout connecting Transit Gateway to the virtual appliances,then probably will go with one of these architectures.Now, a few things to rememberabout Transit Gateway Connect attachment.It does not support static routes.Only BGP support it for the Connect attachment.And then if you talk about the bandwidth,every GRE tunnel supports up to five Gbps of the bandwidth.And likewise, you could have four connect peersestablished on the same connection.Ultimately, it provides youup to 20 Gbps bandwidth per connection.Now, if you compare this with the IPsec VPN,then IPsec VPN only provides 1.25 Gbps per connection.But then you would've to have many more VPN connectionin parallel to really get 20 Gbps bandwidth.And you would've to also do EC MPNall those configurations.Now here as well, if there are multiple GRE tunnels,you would have to configure the equal cost multipath.But then you just need four such tenants in this case.And that's it about Transit Gateway Connect attachment.Again, you could find some of the scenarioswhich talks about connecting Transit Gatewayto the virtual appliances.And in that case, you should knowthat you can use Transit Gateway Connect attachment.So thanks watching.