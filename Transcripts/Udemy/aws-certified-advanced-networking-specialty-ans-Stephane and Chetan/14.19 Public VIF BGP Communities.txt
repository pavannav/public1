# 14.19 Public VIF BGP Communities

Hello, and welcome to this lecture.Now that you understood the routing scenariosin case of using public VIF,now let's understand how BGP communities influencethe route advertisement for the public VIFs.So in this lecture will also learn about BGP communitiesand then how BGP communities can be used in a public VIF.Now understand that BGP communities play different rolewhile using the public VIF and a private VIF.So this discussion is for public VIF only.So as you know, the BGP communitiesare used to control the scope of your routes.Now let's understand what do I mean by controllingthe scope of the route.So typically when you receive the on-premises prefixesor routes or a BGP session on AWS site,you have a choice or you have a controlwhere those routes can be propagated to all AWS regionsor you know particular AWS region, for example,and the requirement could be,you only want to have the connection with one AWS regionbecause your company do not operateor do not use other AWS regionsso there is no point in opening the routesfor the other regions to communicatewith your on-premise network.Now in that case you can control that advertisementthat I am sending these routes from my customer gateway,but these route should be advertised to onlythe primary AWS region where the direct connectionis established and do not advertise it to other AWS regions.So this is a kind of example,and you can completely control that route advertisement.So as with the public VIF,there are different community that support itfor inbound connection, there are three BGP community tagswhich are supported 722491001900 and 9300.That means this will be applicablefor the routes which are receivedfrom customer network to AWSand similarly, AWS also attaches the BGB communitywhile AWS sends those route prefixesto your end or the customer end.And now we'll understand the effect ofusing different community tags.Apart from this community tag,AWS also supports the NO_EXPORT BGP community tag.And again, we talked about this brieflyin the last lectures,but we will explicitly now understand what that means.So if I just want to show here,whenever routes are advertised from customer routerto AWS customer route can set any of this BGP communitiesand when AWS sends the route of AWS public IPSand prefixes to the customer network,it can also set this BGP community tags.And now let's understand what that means.So first look at the inbound communities.Now, the prefixes received from the customeron public VIFs are advertised within the AWSas per the BGP community tags.As I said, you can set the BGP community tagthen you advertise the routefrom the customer network to AWS, right?So if you set the community tag 72249100,then those prefixes or routes are advertisedonly local AWS region where their direct connectionis located, right?That means those routes are not advertisedto other AWS region.Now, if you're send the BGP community tag with 9200 instead,then get outside propagated to corresponding AWS continent,or there is a continent and multiple regionsthat included those in those continents. So for example,there is North America as a continent.So most of the AWS regions in North Americawill be a part of that continent.So your routes will be advertisedto Northern America continent.As similarly, there is Asia Pacific,as well as there is a Europe, middle east and Africa.So these are the three continent currently supported.So now this again,depends on how far do you want your routesto be advertised within the AWS regions?If you think that you operateor use only North America based US regions,then you will set the BGP community tag is 9200.And similarly, there is one more BGP community tag,which is 7224:9300and this means that everything that AWS city seesas the routes in a BGP session,it will advertise it to all AWS regions.And this is the default option as wellfor your BGP communities.That means if you do not attach any BGP communitywhile advertising the route,then 72249303 it's consideredand routes are propagated to all AWS regions.So this is what the inbound BGP communities looks like.Now, if you are using the community tag as 92249100,then all the routes received are only publishedor advertised to the local AWS regionin which the Direct Connect connection is located.So in this case, my Direct Connect location is a Frankfurt.So only this particular region would receive that route.And which means that you can't really accessthe public AWS services from other AWS regions,because they don't know how to route traffic backto on-premises. They do not have your route prefixes.Now, if you change it to 9200,then as I said, it goes to per continent.So Ireland and Frankfurt are part of a same continent.So route will be advertised to both of these regionsand any additional regions if they fallinto that particular continent.And you can look at the AWS documentation aroundwhich region falls into which continentand this list will ever grow, right?Because number of regions are increasing.So that's not the point. The point is thatwith 72249200 BGP community tag,routes will be advertised to a particular continent.Similarly, if you send 7224:9300 as a BGP community tags,then anyways, it will be advertised to all AWS region.And this is also the default BGP community tag.So if you don't send anything,then also the same thing will be done.So this is how the inbound community tags works.Now let's look at the outbound community tags.Outbound means, for the routeswhich are sent by AWS to on-premise router.Now, in this case, AWS applies different BGP community tags,and let's understand what those are and what that means.So AWS can send you know 72248100.And this means that the routes are being sentfrom the same AWS region in which the AWS Direct Connectpoint of presence is associated.Now, this is something like,when you receive the routes at your end,you can understand thatthese routes am receiving from a local AWS regionin which I have Direct Connect connection.So AWS will always associatethis particular BGP community tag.And if the routes are being sentfrom that continent on the AWS site,then AWS will also attach this additional BGP community tagthat is 72248200.So now at your end,you understand that if you see on the routes,you see this BGP community tag, you understand that,these routes are getting published by the continent.So it can be a local reasonor it could be another region from the same continent.And that's where on your site,you can then put a filter, right?If the outside from local regionor outside from continent.Accordingly, you can take decisionwhether you want to accept that route or not. Right?Similarly, if the routes are being advertisedfrom region outside of the continent,then AWS does not put any tag,that means there is no tag and then at your end,you understand that this route is comingfrom the global AWS region,which is not part of a continentin which you have the Direct Connect connection.So ultimately looking at this BGP community tags,you can understand that whether I'm receiving these routesfrom the local region or the continent,or from the global AWS region.And now it's up to you now,whether you want to accept those routes,or you want to drop those routes so that you can controlthat your customer network can talk to a local regionor multiple regions in the continentor all the regions, which AWS has.So that decision is with youby using this BGP community tags.So hope that is clear.And now, apart from this tags,AWS also advertises another tagwhich is no export community tag,and I just talked about that briefly earlierthat NO_EXPORT means,you should not further advertise these routes,which you receive from the AWS to other networksout of your network, right?Because this is a private connection between AWSand your network. So there is no needthat you advertise these route,which you receive from the AWS tothe other network, which you are connected to,but AWS is not connected to.So that's where that NO_EXPORT Community tag is puton all the routes with AWS sends.Then the customer router can choose which prefixes to acceptby filtering the routes basedon the BGP community associated with this routes.And I just explained that based onwhether you want to have a connectionor the communication between local regionor whole continent or to all public AWS regions.So now let's try to understand the same thing here.Okay. Now let's try to visualize what we just said, right?So maybe you have your Direct Connect connectionin the Frankfurt region.And then there is a AWS Frankfurt regionand there are public services,and this is a European continentas for the distinction that AWS has done.And there could be other regions in this continent, right?Now, in this case for the outbound trafficor the outbound routes from AWS to your network,you can see that AWS publishes the routesas well as the BGP community tag.Now, this is a local region because the connectionis in the same region.AWS will advertise the prefixes.Now this prefixes means the public IP address,prefixes of all AWS services in the Frankfurt region, right?And along with those routes,AWS will also associate this particular BGP community tagthat is 72248100,which means that the routes are coming from a local region.So you're connected to local regionas in Frankfurt via Direct Connect.So routes are being sent by the Frankfurt region.Now, if you have another region like Ireland,it falls into the same continent.Then also you will receive those routes,but then there will be a different BGP community there.That's what we just learned.And there could be also routes sentby the public region like North Virginia,which is not part of the same content,in that case from Ireland, when the routes are received,it has the community tag as 72248200.And if they're outside from North Virginia,then there will be no tag.Now, along with all these different tags,it would also have the NO_EXPORT tag.So whenever routes are receivedalong with whatever BGP community tag it has,along with that, additionally,there will be no export tags.So they, they have like multiple tags on the same route.Now, what you can do is you will receive this routesas well as BGP communities at your endand with the NO_EXPORT,you can't really have this advertisedto other route table or a public route tablewhich are all over the internet, right?You should keep those routes within your network.That means that's what this NO_EXPORT means.Now, additionally you can filter the trafficas we are saying, right?So you want to only have the communicationwith local AWS region in that case, what you'll see,you'll see the routes coming in, say for example,routes from the Ireland region,and you will see that there isa BGP community tag 8200but I don't want to have my communicationwith the Ireland region. So you drop those routes.You don't accept those routes.You don't publish or further advertise those routesto your route table, right?So all this kind of filtering you can dowhile you receive those routes from the AWS end.So this is how the BGP communities workfor our public interface,and I hope that is a little more clearwhat is the purpose of BGP communitiesand how the scope the route advertise meantfor inbound as well as outbound route advertisementor BGP session.So with that now,I think we covered everything about the public VIF,the first talk about the routing policies for public VIF,these are various architectures like active, active,and active passive connections.And then we saw the BGP communities,how you can have the different scope of the routeswhile using the inbound as well as outbound BGP communities.So I think that's it for the public VIF routing policiesand BGP communitiesand now let's look at the private VIFs routing policiesand the BGP communities.So thanks for paying attention.And now let's get into the next lecture.Thanks for watching.