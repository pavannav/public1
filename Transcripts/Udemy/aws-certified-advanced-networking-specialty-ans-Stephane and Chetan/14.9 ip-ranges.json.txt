# 14.9 ip-ranges.json

Hi. So before we look into the publicVIF, it is important to understand this one concept calledip-ranges.jsonand I will also explain why.So as you know,the public VIF allows the connectivity between your data center network andall AWS public IP address space and we will dive deep into thatinto the next lecture.But essentially what it means that all the public IPs on AWS site,you can access over the public VIF.So when you make this VIFactive, AWS will publish all the routes for all the publicIP address ranges which AWS uses for its own services.Either you can accept all these routes and then all the traffic going toany of the AWS public IP then will flow over this public VIF.That means over to this DX connection.Now, if this is the situation,then what you need to do is, you need tofilter out the IP addresses at the Firewall level.So on corporate data center sideover your DX connection,you can set up a Firewall on your side of the network.And then as you receive these routes from the AWS side for all the public IPs,including for all AWS services.And this is useful not just for the public VIF DX connectivity,but even if you are accessing any other AWS services without using the DX,that means over the internetand you want to restrict access to certain AWS public services,then also you can use ip-ranges.jsonJSON file.So which means here now if you're accessing,say Load-Balancer service of AWS from youron-premises data centerover the internet,then you can use this file to filter thetraffic and you say only traffic going to loadbalancer servicein a particular Region or globallyis allowed rest of the traffic, you can block at your Firewall.Now, for that reason, also,you will need IP ranges from AWS site to configure the Firewall rules.So now let's look into this file in a little more detail.So AWS publishes all its current public IP addresses in the JSON format.And this file ip-ranges.json can be downloaded from this web link as well.You can filter the IP addresses by Region or AWS service or whether it's IPv4or IPv6address.And ultimately, you can use it to filter your traffic as I said.Now ip-ranges.json would often change becauseas AWS expands its services or scale its services,there will be new IP addresses added, right?So ip-ranges.json would be updated. Now, if you are using ip-ranges.jsonfileto filter your traffic, you must also update your Firewall rules.So how do you get notified?AWS has configured this simple notification topic, SNS topic on which they will publish the messagethat there has been changes to ip-ranges.jsonfileSo what you can do,you can subscribe to this topic so that you get notified over an email as well.Or you can also set up a Lambda functionand that lambda will trigger and probably pull the ip-ranges.json from this web link and then update your Firewall rules.So you can set up that automation as well.So if I show you this file, then the syntax of this file looks something like this.It has a sync token which says when the lasttime this file was updated and then creation date.And the important part is IPv4prefixes and then IPv6prefixes.And then there is a prefix, the corresponding Region, network group. Typically it's Global service or regional and then actual service name.And as I said, this file contains the prefixes for all AWS public services.So here is the list of full AWS services.You can use any kind of filters to just filterthe prefixes for the service in which you are interested.And you can then updatethe Firewall rule at your site.If you are using Linux as a platform, there is a nice utility called JQand you can write a command line to filter thisJSON file based on the service or a regionthe way you want it.Ok. So that's it about ip-ranges.jsonfile.And we talked about this becausewith respect to the public VIF, it isused in most of the cases to filter your routes.So thanks for watching and stay tuned.