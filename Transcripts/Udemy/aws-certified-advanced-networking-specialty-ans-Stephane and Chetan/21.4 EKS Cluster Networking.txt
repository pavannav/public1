# 21.4 EKS Cluster Networking

Hello and welcome to this lecture,"Amazon EKS Cluster Networking."So in the earlier lecture,we understood the EKS architecture.We know that control plane is managed by AWS,and data plane is managed by the customer,that means by you.Now in this lecture, let's understandthe cluster networking.So far, we are not talking aboutthe EKS or Kubernetes pod-level networking.That means how pod communicates with each otheror how to expose pod services to the outside world.That will be covered in the following lecture.But here, we are talking about EKS Cluster Networking,that means when you create EKS cluster,then how does your network look likeand what are controls you have over that network?So this is about cluster networkingand not really about the pod networking, right?So let's start with EKS Cluster Networking first.EKS has the control planewhich resides in AWS managed VPCas you can see here on the top.And then the data plane, which means the worker nodesare launched in the customer VPC.So this is a VPC which you manageand this is inside your AWS account.So control plane is launchedin AWS managed account and VPC,and data plane nodes are launchedin a customer account and VPC.So the question is, if that is the case,then how does the control plane talks to the data plane?Of course, there should be some mechanismand you are already aware of thisif you know the requester managed ENIs.So if you worked with services like Amazon AppStreamor Workspaces, or for that matter,database service that is RDS.Now RDS databases are managed by AWS,but still you can control the security groupfor your database.You can configure those rules.Now, how it is possible?In that case, behalf of AWS service,it creates ENI into your VPCand then it gives you control over that ENI.And then security groups are attached to the ENI,and that's where you get control of managingthe traffic for those ENIs.In the same fashion,EKS control plane provisions the ENIs,typically 2 to 4 ENIsdepending on how many availability zones you useinto your customer VPC.And this ENI enables the communicationfrom control plane to the data plane.So this is the traffic from control plane to the data plane.And then also sometimes your data plane needs to communicateto the control plane as well,and we'll see how that communication happens.There are different ways and we'll cover that.But here you have ENIs, which are owned by the EKS,but they're provisioned inside the customer VPC subnet.Now if you look at this architecture,typically it is advised thatfor this ENIs, you should have a dedicated subnet.Now you could very well use these worker node subnetsto provision these ENIs,but you know, it's always better to have better controlover those subnets so that you can define the NACL rules,you can have the routing rules for your subnetand then you dedicate these subnets only for these ENIs.And then EKS doesn't need lot of IP addressesfrom this subnet, only six IPs are required,but you know, in AWS you can create the smallest subnetas /28, which gives you 16 IP addresses.So it's recommended that you have the smallest subnetsallocated for these ENIs.Now further, when you create EKS cluster,it also creates security groupand these security groups are attached to these ENIs.Now this security groups are also attached to the nodes,if you are creating these nodesas a part of managed node group.And you can see here,there are a couple of rules in this security group, right?So there is inbound rule which allows all protocolon all ports and it references to the self,that means wherever this security group is attached,the traffic can flow through all those ENIs to each other.And then there is outbound rulewhich allows the outbound connection to IPv4and IPv6 addresses, all the addresses,and then you can very well change those as you need.But this is a default security group which gets created,and then you also get controlwhere you create security groupand then you provide the security group IDwhile you create the cluster.That means it's configurable,but this is a default security groupwhich gets created if you don't provide your own.Okay, next thing, how do you access the control plane?Now, here is the thing to understand.When we talked about the Kubernetes architecture,we said that Kubernetes APIs serverskind of the heart of the communication.If you want to talk to Kubernetes cluster,you need to make an API call to the API server.Now in this case,that API server is a part of this EKS control plane.And if you're using some kind of client,Kubernetes client like kubectl on your workstation,then you need to connect to this control plane API serverand then you can deploy your pods, you can create services,you can attach nodes to the Kubernetes cluster.All those are Kubernetes-level operationand for that, you need to hitthat Kubernetes control plane API.So by default, this API serveris accessible over the internet.And as you can see, this is managed by AWSbecause it's inside AWS managed VPCand then AWS will allow the internet access to this.Now, there are ways to disable the internet accessand we'll see that in the same lecture,but this is what happens when you create the EKS cluster.Now, one thing to understand that don't get confusedbetween the Kubernetes API serverwhich is used to manage the Kubernetes cluster components.For example, launching pods, launching services,and then there is something called EKS APIs,those are AWS EKS APIs.That means if you want to create the cluster,you need to make an API call to AWS.For example, if you launch an EC2,you make an API call to EC2 service.Similarly, you are now making call to the EKS serviceof AWS and then it creates the cluster for you.That's AWS API,we are not talking about that at this moment.We are talking about the Kubernetes APIto really manage the internal components of the Kubernetesand to make the deployments into the Kubernetes, right?So by default, this API server it's accessibleover the internet.Finally, you can have both IPv4and IPv6 IP addresses assigned to your podswhich are running on this worker node.But thing to remember is thatit doesn't support dualstack mode.That means, pod cannot have both the IPs at the same time,either they can have IPv4 addressesor they can have IPv6 addresses.So that means both the options are available to you.Now, with that, let's look into the particular use cases.Now to start with, let's seewhat are the options you have when you sayyou want to access the EKS Kubernetes API server?So here is the same architectureand then you have kubectl as a clientwhich you can install on your workstation,assuming that you have permissionsto connect to the Kubernetes API serverthen you can very well go over the internet.Now this is a default settingwhere EKS cluster endpoint public access is enabledand the private access is disabled.And we'll talk shortly about what is private access.But if that is the case, you create the clusterthen you can connect from your workstation.So EKS will provide you the DNS,some uniquename.eks.amazonaws.comand which results to the public IP,and then you can connect it over the internet.Now, how do then worker nodes connects to the control plane?Because as I said, this ENIs by defaultwill only allow access from control plane to the data plane.But then data plane also needs to communicateto the control plane.For example, to let control plane knowthat some of the pod went down,so there should be a replacement pod.So for the reporting purposeor to get the instruction from the control plane, right?So by default,if you enable the cluster endpoint public accessthen this worker nodes hereand it runs kubelet, as you know,needs to communicate with the control planeover the internet as well.So the traffic will go from here to the control plane.Now, for that, as you might have understoodyour nodes needs to be in public subnetand then you should be allowed to connectto the internet through internet gateway.And we'll talk about more patterns of publicor private network going forward.But this is what happensif you go with the Kubernetes default public access.Okay, so obviously this works well,but then it means that anybody can connectto your control plane.So do you have any control over who can connectover the internet? Yes, of course, you havebecause AWS EKS provides you abilityto provide the CIDR block to whitelist the IP addresses.So you can restrict that which IPs can accessthis control plane.So if you know the customer side of the CIDRthen you can provide thatand only traffic from those IPs will be allowed.Okay, but in certain cases, maybe you want thatyour worker node doesn't have the outbound internet accessand still you want these worker nodes to communicateor say data plane to communicate with your control plane,or the private network.Now in that case, there is an optionwhere what you can do is you can enable boththe public and private accessfor this control plane endpoint.So in that case, a client can connect over the internet,but the data plane can be in the private subnetand there is no internet gateway here.And then it can access the control planevia this EKS owned ENI.So when it tries to resolve the DNS,it results to the ENI private IP.In that case, you don't need outbound internet accessfor your worker nodes.So that's one case where you can enableboth the public and private access.Now, in this case as well,you could still have the CIDR blockand you can whitelist the public IP rangefrom which you want external client to make connectionto the control plane API server.Now, while this is good, maybe in certain cases,you don't even want this public connectivity.You want that all the connection from your networkto the Kubernetes control plan is private.That means it's secured,so it could be over VPNor it could be over a Direct Connect.If you have that kind of requirement,you can completely disable the public accessto your control plane cluster endpoint, right?But in that case,you need to have some kind of Layer 4 connectivitybetween the data center and the customer VPC.Typically, it is turn over a VPN or a Direct Connectand then you have client inside this network,and this client can then reach out to the EKS owned ENI,and then the traffic can go to your control plane.Now, in this case,we have disabled the EKS cluster endpoint public access,but we have enabled the private accessso that it can only be accessed from this EKS owned ENI.So these are the ways in which you can control the accessto the EKS cluster endpoint.Now, at the same time,we also need to look at the AWS EKS APIs.Now, for example,if you're creating the EKS cluster altogether.Now for creating EKS cluster,you don't need access to the cluster endpoint, right?Because it is after creating the cluster.But for that, you need access to the EKS service of AWSlike EC2 as I said.Now, typically, all the servicesare available publicly, right?You can access that,but in certain cases you want to only allowthe client inside a VPC, for example,or client inside a private networkto create the EKS cluster.So in that case, if you need to do that,you can use VPC interface endpoint for EKS service.Now, what we are doing here,in customer VPC, we are creating a VPC interface endpointfor EKS service of AWS.And then your client are sitting here,but this time they're not using kubectl,but they're using other tools like eksctl,which is used to create the Kubernetes clusterand eksctl talks to AWS EKS service endpoint, right?So in this case, it will fire AWS EKS APIand the traffic will flow from the customer locationto the customer VPC via VPN or a Direct Connect.And from there, through VPC interface endpoint,it can reach to the EKS service endpointand then you can fire all the EKS-related APIs.To start with, it will create the cluster,and once cluster is availablethen the kubectl will be used to connectto the control planeand then perform all the operations.Now, this is how the VPCs look like and the, you know,networking look like for your EKS cluster.Now, few additional things, maybe recommendationor best practices, what you can do inside your customer VPC.So of course, you could have public subnetsinside your customer VPCso that you can launch the elastic load balancerslike ALB or NLB,and then you can distribute traffic to your pods.And we'll cover this in detail in the following lectures.And also for the outbound access to the pod,you can do that through the NAT gateways as usual.So for that, also, you could have your public subnets.Now if you're allocating IPv6 addresses,you know that IPv6 addresses are public, right?So you don't need NAT gateway,but then if you want to allow only outbound accessthen you need egress only internet gateway.Now, what is egress only internet gateway?It only allows trafficfrom IPv6 addresses to go out, right?But from outside,traffic cannot reach to those IP addresses.So this is particularly used for IPv6 addresses.But if it's IPv4 address, as you know,you should have the NAT gateways.And then NAT gateway should be in public subnet,so traffic goes from your pods to the NAT gatewaysto the internet gateway, to out to the internet.Further, you can also enable the VPC endpointsthrough private link to access other AWS services.For example, pod wants to send some logs to the S3 serviceor pod needs to access the EKS service,so all these endpoints are availablebecause we are still operating as a normal VPC, right?So all those VPC-related servicesare available in the customer VPC as well.Further, you can connect your VPCsto other networking component.For example, you could connect it to on-premise data centeror a VPN, or a Direct Connect.You can peer multiple VPCs.You can peer your VPC to the transit gateway.So all this is possible.That means you leverage all your VPC capabilitieswhile you deploy your Kubernetes data planeinto your own VPC.So we'll stop here for this lectureand now we'll slowly get into the pod-level networkingand that's the most important part of this sectionbecause you will have a lot of questions relatedto the pod networking.How EKS assign the IP addresses to the pods?How to expose pod services externally?All that will follow,but I hope the basic building block is clearhow the Kubernetes control planeand data plane networking looks like.So thanks for watching and stay tuned.