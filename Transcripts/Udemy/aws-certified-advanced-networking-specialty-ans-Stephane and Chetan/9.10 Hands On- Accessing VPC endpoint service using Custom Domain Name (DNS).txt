# 9.10 Hands On- Accessing VPC endpoint service using Custom Domain Name (DNS)

Okay, now let's try something interesting.Now, in the previous exercise, you access the VPC endpoint service using the DNS that is given byAWS, right?That DNS or original DNS from this client.Right.Now what if we want to use custom domain name for example say app dot example.com.Now if you have to do that first thing you need to own that example.com domain.And you have to prove the domain ownership.And then uh if you want to use that domain or a subdomain, you have to enable the private domain namefor the VPC endpoint service.And also you have to enable that in the consumer VPC.Right.So for this to happen you have to enable the private DNS for the VPC endpoint service.Same for the VPC endpoint.In the consumer VPC and VPC label, you have to enable the DNS hostname and enable DNS support, right?And as I said, domain should be owned by you.So that is also a prerequisite.So let's try to do that.And here I already have a domain name that I had purchased.And I have that in a route 53.So let's see how to do this.Okay.So let's go to the VPC and the endpoint services.And in here we have to modify the private DNS and let's say private DNS name say app dot I have a domainname that's here.That is AWS Training center.com that I had created in the past.So I'll use the same domain name as a sub domain.Basically app dot this.So let's save the changes.And now we need to verify this domain ownership.So let's go here and verify the domain ownership.And here for verifying the domain ownership we need to create this text record.And this will be the key and this will be the value.So let's go to the route 53.And this is my hosted zone.So let's create a record.And, uh, the record type should be text and name should be this and value should be this.Okay.So that's done.And let's create the record.Now once that is done let's verify that.And it says that it may take up to ten minutes for verification process.So let's wait.Okay.So I'll wait for maybe a couple of minutes and I'll come back here and I'll see if the verificationis done or not.Okay.So it took around three minutes.And you can see that the domain verification is verified.So this is what we want.Okay.So that's done on the service provider side.Now let's go to the EC2 console first.And let's try to see if we can access the application using the custom domain name so it appears trainingcenter.com.And it won't work, of course, because we haven't really enabled the private DNS, right?And there is no public record for this.So next thing let's go to the VPC endpoint that's in a consumer VPC.And here let's enable the private DNS.So let's enable for this endpoint and save the changes.And as I said for this to happen the VPC should have the enable hostname and DNS.True.Right.And that we had done in the previous exercise itself.That's where we don't need to do it again.So let's wait for this status to be available basically.And we will then test the connectivity.Okay.So it took around 4 to 5 minutes for this endpoint to be available.And at this time I expect that everything is done.So if I go to my EC2 instance session, I should be able to resolve apt AWS training center.com.And one more thing that you should see is that it automatically detected this private DNS name for theendpoint in the endpoint.We never told about this, right?It was set at the endpoint services.But because this endpoint is for this particular service, it automatically gets that private DNS nameright.So with that now let's go to this and let's try this again.And you can see that we are able to access the service.In fact if you do nslookup with app dot.This domain name then it resolves to the both the IPS basically for the endpoint DNS right.So ultimately what AWS does is it creates a private hosted zone with the same domain name that is app.com,and in that it creates the key name which points to the regional, uh, DNS of the VPC endpoint.And that's where you see both the IPS being given because we are trying to resolve the DNS.So this is how it works.So I hope you are also able to do that.And if not you are able to at least follow what we did.So here we basically use the VPC endpoint service using our own domain name.Right.So I hope you enjoyed this two exercises.And with that we are very close.To wrap up VPC private link section.Uh, we'll cover some additional architectures.And then we will move to the next section.But before we stop, please remember that you have to delete all the resources that you created in thistwo exercises.Right.So with that we'll stop here and let's go to the next lecture.Thank you.