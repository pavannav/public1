# 19.23 Route 53 - DNSSEC

Let's talk about Route 53 DNSSEC.But before I introduce a solution,let me expose in a simplified version the problem.So we have DNS poisoning or spoofing.It's a type of attack.So for example say we have our web server,it's hosting the website we want, for example.com.And it is at a specific IP 9.10.11.12.So the web browser of a clientis going to ask the local DNS server for example.com.Then the local DNS server needs to actuallyget the record for it.So it's going to figure outthat Route 53 has, on its DNS, the solutionfor where example.com is located.So it's going to ask, for example.comand Route 53 is going to returnwell, it's the IP 9.10.11.12.Okay. Fair enough.But then a hacker through some processmanages to inject the value example.com equals21.22.23.24 in our local DNS server.And this is because, to very much simplify it,the DNS works on the UDP protocolwhich makes it somewhat easy to hackand also when we don't have a DNSSECthere is no cryptographic DNS verification process.So therefore our local DNS server has been spoofedand now is going to return to our web browser, the wrong IP.So 21.22.23.24.And therefore the web browseris going to think it accesses the correct serverbut actually it accesses a malicious websiteat a different IP.So that's call DNS poisoning and spoofingand DNSSEC actually is going to help mitigate that issue.So DNSSEC is for DNS Security Extensionand it's a protocol that helps you secure DNS trafficto verify the DNS data integrity as well at its origin.It only works when the hosted zones are public,so for public domain names.So Route 53 is going to support DNSSECboth for domain registration and signing of the DNS records.So the DNSSEC signing is the ability to validatethat a DNS response actually came from Route 53and has not been tempered with.So no record has been injected.And how does it work?Well, Route 53 is going to sign each recordin the hosted zone using a private key.And then the clients can verify with a public keythat the signature is correct.So the client needs to have some sort of logicto understand the DNSSEC process.So we have two keys happening within the DNSSEC process.And the first one is managed by youand you must remember it.It is the key signing key, the KSK,and it's based on an asymmetric CMK that you create in KMS.So this key is managed by youand then you have another one managed by AWSwhich is call the ZSK, the zone signing key.And you must know which one is yours.So the one we manage is the KSK.The ZSK is managed by Route 53 directly.So when you enable Route 53there's going to be some DNS signing in DNSSECand so thereforeyou can verify cryptographically your records.And there's also a second measure of securitywhich is that the TTL becomes at most one weekfor all records in the hosted zone.And if you have a TTL of less than one week,then they're not affected, of course.So the exam may ask you,"How do you enable DNSSEC on a hosted zone?"So first one you need to prepare for DNSSEC signingthat needs make sure that the zone availabilityis working good.That means that the DNS is responding correctly.And this is done through customer feedback.Then you lower the TTL for all your records to, for example,one hour and you lower the TTL of the SOA to,the SOA minimum, sorry, to five minutes.So this is to prepare for DNSSEC signing.Then you are going to enable DNSSEC signingon your hosted zoneand you're going to create a key signing key, a KSK.So to enable DNSSEC you can use the console or the CLI.And then Route 53 is going to create a KSK for usand we link it to a customer managed CMK from KMS.So once this is done, we have enabled DNSSEC signingbut that's not all.You need to establish a chain of trust.And I will show you in the next slidewhat that means, visually.So chain of trust means that the hosted zone,where we enable DNSSEC,is going to tell the parent hosted zone to trust it.And this is done by creatinga delegation signer, DS, record in the parent zone.So it's very important.The parent zone must have a record to trust the child zone.So the DS record contains a hash of the public keythat's used to sign DNS recordsand that's how the trust is established.So your registrar that needs to trust your child zonecould be, for example, Route 53,or it could be a third party registrar.So once we've done that, we are signing all the recordsusing the key from step two.And from step three, the parent hosted zoneknows to trust that signing keythanks to the delegation signer, DS, recordthat we insert in the parent zone.And then finally, a very good to have in this setupis to monitor for errors using CloudWatch Alarmsso you can create CloudWatch Alarmsfor two specific type of metricsand they can come in the exam.We have the DNSSEC internal failureand the DNSSEC key signing key needing action.Okay, so all these things allow you to enable DNSSEC.To remember them, okay:step one, step two, three, and four.And what is a chain of trust just to understand?So in a DNS way, when we, for example,query for a specific record,we're first going to go to the root DNS serverwhich is going to point to the TLD DNS server's .com.Then the registrar,for example, for example.com on Route 53,then the hosted zone, which contains the records.And maybe if we have a second hosted zone in therethat is a sub.example.com,then we may have children of children of hosted zones.So the chain of trust can only workif all of these DNS servers along the wayare DNSSEC aware.So what's happening is that the root DNS serveris going to have a trust on the TLD DNS server.And then the TLD DNS server is going to trust the registrar.And then we set up DNSSEC on the hosted zoneand we insert a DS recordso that the registrar trusts the hosted zone.And then if we have a sub hosted zone,we insert, again in the parent hosted zone,we insert a DNS DS record to trust the sub hosted zoneand so on.And so that means that your clientwhen it does its queries,is going to query every DNS server all at once.And then all along the way, thanks to this chain of trust,we can verify each DNS response and we can make sure,and this is of course some logic implementedin the libraries you're already using,we can verify that all these units responseshave been encrypted correctly and signed correctly,thanks to the associated public key.And this is how DNSSEC works.So that was a pretty long lecturebut hopefully now you understand how this works.I hope you liked it.And I will see you in the next lecture.