# 16.2 VPC lattice components - Lattice Network, Service, Resource and more

Okay.So now let's understand different components of VPC lattice.So at high level VPC lattice contains these five components.That is VPC lattice service network service resources service discovery and auth policies.So as you can see in this diagram, there are different applications or services hosted across differentVPCs.And there is also a lambda function which doesn't typically sit into the VPC.Now in order to connect all these different services together, you have to create the VPC lattice servicenetwork.And then you have to configure all these applications as VPC lattice services or the resources.And in the rest of this lecture we will see what is service, what is resource and how to really configurethose.Now once you create this service network and associate your services to that network VPC lattice alsoenables the service discovery so that all these services or resources can be accessed using the VPClattice provided DNS name.So we will also talk about that.And finally, in order to enable the authentication and authorization you can apply the auth policies.So let's also talk about those right.So at high level these are the components of the VPC lattice.And now let's dive deep into individual components.So the service network as we said it's a logical boundary for a collection of services and resourceconfiguration.So you create a lattice service network and you associate the services or the resources or the VPCsto this service network.Now we have a dedicated lecture which talks about all these associations.So just hold on until we reach there.But just understand that VPC lattice network connects all the services and resources together.Next is the service.Now if you look at the service it is made up of multiple things right.So it's an independently deployable unit of software that delivers specific task or function.That's the definition.But what it means.It means service can be hosted on, say, EC2 instances or on the application load balancer or ECS EKScluster, or on a Lambda function, or even on the IP addresses.Now, how do you create a service?For that, you have to create a listener.You have to create the rules.You have to create target groups.And finally your actual service will run on any of these targets that we just talked about.So if you remember, this setup is quite similar to how we configure the application load balancer.But here we are talking about the VPC lattice service.So it has a target group listeners and the rules.Now further target groups can have weights like you can send 10% traffic to first target group and 90%to another target group.And then there are rules which has priorities and also conditions.Now again this is very similar to how we configure the application load balancer.Right.So that's in a nutshell is VPC lattice service.And we will see this in action when we will do the exercise.Okay.Next is VPC lattice resource.Now resource is quite similar to service but not exactly a service.So VPC lattice resource is an entity which can be Amazon RDS database or cluster of nodes behind theauto scaling group.Or it can be a simple instance or an application endpoint, domain name or IP address.So it can be anything basically.So that even you can bring in your on premises servers as a part of your VPC lattice network.And for using this resource you have to create something called resource gateway, which is like aningress endpoint for that resource.And finally, if you want to associate this resource gateway with the VPC lattice network, you haveto create resource configuration association.So like you associate the services you can also associate it.Resources.Using this resource configuration association.Right.So we talked about VPC lattice network VPC lattice service and VPC lattice resource.Next is auth policies.So VPC lattice auth policies are fine grained authorization policies that can be used to define theaccess to different VPC lattice services.And this auth policies can be attached either VPC lattice network level or individual VPC lattice serviceslevel.Now it is not necessary that you always define the auth policy.You can go with the default that is none policy.And in that case by default the communication will be allowed.So auth type can be either none or aws IAM.And none means you are not applying any authorization or authentication policies.And when the auth type is aws, IAM, then by default all the requests will be denied.So if you understand how IAM policies are evaluated, it is exactly the same thing.If you do not explicitly mention the allow policy, then the access will be denied, right?So when one service talks to another service, there are some evaluation criteria.For example, the requester should have the IAM policy which allows to access the VPC lattice service.So these are called IAM identity based policies.And the requester can have this access using either IAM user credentials or IAM role.Further VPC lattice service network or the individual service auth policy should explicitly allow theaccess to the requester.And that is called principal.Right.And in that case the access will be allowed.So just remember this about the VPC lattice auth policies.And if I just have to show you some examples then it could be like this right.So here we are saying that you are allowing the communication between the services inside this particularAWS organization.So all services can communicate because principle is star.Whereas if you see this policy, we are saying that you can only invoke the Get method for this particularservice.And for that the requester or a principal should be having this IAM role.So you can go that granular with respect to the communication at the IAM level.Right.So this is how you use the auth policies with VPC lattice service network or the service right.So with that I hope now you understand what are the different components in the VPC lattice.And now in the next lecture let's talk about the VPC lattice associations.We have talked about that at a very high level.But now let's closely look at those associations.Okay.So with that let's stop here.And now let's go to the next lecture.Thank you.