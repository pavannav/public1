# 18.15 gRPC & ALB

So just a quick lecture on gRPC supportfor the Application Load Balancer.So gRPC in a word in a sentence is justa very popular choice if you wantmicroservice integration using HTTP/2.So that's the newer way of writingAPIs using the newer version of HTTP.And the Application Load Balancerwill fully support gRPC which is great because beforewe had to use a Network Load Balancer.So how does it work? Well, we have our deployment.We have, for example, EC2 instances,that are running in an Auto Scaling group.And these EC2 instances represent a microservice,and on the instances we are running an applicationthat is fully supporting gRPC.So the users can now access directly our ALBusing the gRPC protocol, which is behind the scenes HTTP/2and the ALB will know how to do routingto your microservice using the gRPC protocol as well.Everything is gRPC native and HTTP/2 native.So why am I saying this? Well, because we are using an ALB.That means that we can do health checks directly fromthe application balancer directly into your target group,and that supports the gRPC protocol of course.Also you can do content based routingto route to the appropriate serviceif you have multiple microservices behind a single ALB.It supports all kind of gRPC communication.So because we are using HTTP/2we can get, for example, bidirectional streaming,and that is a feature that is providedas part of the ALB, and this is gRPC specific.And you can only use on the ALBthe HTPS protocol, nothing else. Okay? For listeners.Now. gRPC would also work with any Network Load Balancer,but the advantage of using the application balancerover the Network Load Balanceris that the application balancer provides youa lot of what I call HTTP specific featuressuch as the health checks, the content based routingand so on. Okay. That's it for this lecture.I hope you liked it. And I will see you in the next lecture.