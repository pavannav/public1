# 3.17 NAT Instance (EC2 based NAT)

Hello and welcome to this lecture.So in the earlier lecture, we saw how NAT Gateways workand it is a managed NAT service from AWS.But in certain cases, as I saidyou might need your own EC2 Instance setup as NATor maybe for cost reason or you need to havecertain softwares on that EC2 Instance,like IPS intrusion detection systemand intrusion prevention systems.And in that case, you can also use your own EC2 Instanceand configure it as a NAT.So for this, you need to do some additional configurationslike in NAT Gateway, there is hardly anything we did.We just launched NAT Gateway in a public subnetand modified the route tables for private subnet.Here, while we launch our own EC2 Instance,we need to make sure that the Instance has been launchedusing NAT AMI'swhich are there publicly available to be used.Then we also need to disable the source destination check.Now, that is important to understandwhen you have your own EC2 Instance, right?And you want to configure it as a NAT.Now understand that other EC2 Instance,which has to access the internetwill send the traffic to this Instanceand it will send this traffic to internet, right?And from there, the traffic will come backand it will come back to originating EC2 Instance.Now, for this instance, by default,it only accepts the traffic that the destination packetis its own IP address and that is a setting at VPC level.That means by default EC2 can only accept the trafficwhere the packet destination address is itself.But if you look at this, this EC2 is accepting the traffic,which is for sale google.comAnd then forwarding the traffic here.So at Instance level,there is a setting called source destination check.And you have to disable thatin order to configure an EC2 Instance as a NATso that it can also accept the packetwhere the destination is not itself. Right?So remember, this is very important.Again, this EC2 must be in a public subnet, like shown here.And again, you need to modify your route table,but this time, the destination of the route tablewon't be a NAT Gateway, but it would be an ENIwhich is attached to this Instanceand during the exercise, we'll see that as well.So quickly, let's do that.Let me go back to my earlier setup.So this is my NAT Gateway.Let's do one thing, let's delete this NAT Gatewaybecause we need to have the NAT Instance instead of this.So I'm just deleting this.And also let's modify the route tablesand remove the route which we had added for the route table.Now you see this, right?This is interesting, when your NAT gateways is deleted,you see there is a black hole herebecause we say the traffic has to go through thisbut this device does not exist.So many are times it happens during troubleshooting,you will find this kind of black holesand then you need to remove that and save the routes, okay.In that case, let's go and create our own NAT EC2 Instance.I'm going to my EC2 console now and launching the Instance,but this time I'll search for a NAT AMI here.So I'll just search for NATand I will search in the community AMIs.And you see here, I see this NAT AMI.I will just select, that is a NAT.I will select into micro, now as you know NAT needs to bein a public subnet.So I'm making sure it is in my public subnet.It also has a public IP.Now you can also attach an elastic IPbut for me, it is okay.I'm not going to run this setup for longso I'm just using the public IP,storage I'm going with default.Tag, definitely I want to add a tag, say NAT EC2.Now security group, this is interesting.Now, again, look at this diagram.This Instance will accept the trafficfrom all the Instances in a private subnetand that traffic could be anything for that matter.It could be HTTP, HTTPS, Bing.So mostly you want to allow all the trafficfrom your private Instances not SSH for surebecause we are not doing the SSH outbound,but let's open other traffic.Let's open ICNP that is for sure we need it.And we need it for BPC CIDRso that any Instance can access that using the NAT.Also, let me add maybe HTTP traffic and HTTPS traffic.So I'll just go and add that.Also, let me add HTTPSso that we can pull some content from the internet.Okay, so that's a security group for NATand we'll just go with the defaultand we'll launch the Instance, okay?So Instance has been launched, but as you knowwe need to do additional settings.The very first thing you need to make sureis you disable the source and destination checksfor this Instance.So let's do that, let's go to this NAT EC2 Instanceand let's go to Instance settings.Sorry, actionsand in the networking, we would have this setting calledchange source destination checkand we need to just disable the settings.So let's wait for some more time.Okay. So as of now, you can see that it is at theNetwork Instance level, ENI level.So lets stop this and save.So let's wait for some timefor this Instance to be functioning.And by that time, let's go to our route tablesand now just verify if I can access the google.com.I cannot, this is from my private EC2 Instance.I still cannot.Now let's go back and modify the route tablesfor this particular private subnet.Edit the routes and sayif the traffic is going to internet,I want to rather use my Instance,which is my NAT EC2 Instance.So it will just give me the dropdown.Let's pick our NAT, okay, and save the routes.Okay, let's go back and check if it is working.And yes, you can see the traffic nowis flowing through the NAT EC2 Instanceand I'm able to access the internet.So basically what we did is justmade this setup available for us.So that's in short is NAT EC2 Instanceand just to make sure we understand the differencesso NAT Gateway if you seeit is highly available within the AZand you have to create multiple NAT Gateways in multiple AZsto make sure it is resilient to AZ failure.NAT EC2 Instance you can manage the failure using the scriptwhere all the traffic is routed through one EC2 Instanceand if it fails, your route table gets modifiedand the traffic can be routedthrough other NAT Instance as well.With respect to the performance, NAT Gateway can scale up to45 GBPS bandwidth, when it comes to the NAT instanceit depends on basically Instance configuration.Typically it is five GBPSand that would also depend on the instance type.So it is not really scalable up to 45 GBPS.Maintenance, as you know,NAT Gateway is completely managed over AZ.NAT EC2 Instance, you need to manage the high availability,any patching or anything you want to do on that Instance.So for performance software is optimizedfor handling the NAT traffic.And it is now, in case of Nat instancea generic Amazon Linux AMI is used to perform the NAT.Cost or perspective also NAT Gatewayis having two kinds of charges.One for our running cost as well ashow much data it processes.And NAT Instance charges are typically based onthe instance type that you select.Again, IP addresses,NAT Gateway should only have the elastic IP addresses.Now NAT Instance can have elastic as well aspublic IP address.Security wise NAT Gateway does not have security groups.That means you can't really control the traffic therebut again, you can use network ACL, as I said.However, for NAT Instance as it is nothingbut another EC2 Instance inside your subnet.You can apply both the security group as well asnetwork ACL rules there.You can't do port forwardingor you can't use NAT Gateway as a bastion host,but being an EC2 Instance, again,you can do same thing with NAT EC2 Instance as well.So these are primarily the differencesbetween the NAT Gateway and NAT Instance.So with that, you should be able toclearly make out your choices given the scenariosand mostly it would be the NAT Gatewaysbecause that is what is recommended.But in case there are certain scenarios given,maybe you need to know all the settings.Typically, the source destination check has to be disabledand it should have been public subnet,all those kinds of NAT Gateway related stuff.So that's it for this lecture, thank you.