# 14.23 DX Failure detection with BFD

Hello and welcome to this lecture.Now that we have seen some of the architecture when it comes to the high availability of direct connectconnections.Now let's understand how to failover fast, which means if you detect the failure in one of the link,how fast you can failover to another link.And that is important because then ultimately it would reduce your network downtime or a network outage.So for direct connect, there is something called BFD.If you enable the BFD that is bidirectional forwarding detection, then you can minimize this failovertime to a secondary link.So in the earlier lecture, if you remember, we already saw what PhD means in generic sense, and ithelps you to take the failure in the network links and it detects that real quick so that you can failover to the secondary link.And now in this lecture, let's understand how BFT is applied for the direct Connect connection andthen how much failover time you would have if you enable the PhD and if you don't enable the BFT.So while using the multiple connection or using VPN backup, it's always important to have fast failureto a redundant connection.So if you don't apply the BFD, so by default BGP waits for three keeps alive to fail at a hold downtime of 90 seconds, so it takes up to 90 seconds to detect the failure if BFT is not applied.However, as you know, with BFD, that time can be reduced to as low as one second.Now, BFD is not something which is, you know, enabled at your end.Automatically, however, will enable the BFD by default on DCS connections.But as I said, you need to make sure that it is also enabled at your end.And if you talk about the calculation of one second typically sets the BFD liveliness detection intervalto 300 milliseconds and then BFD liveliness detection count is three, which means if there are threeconsecutive failures, that means the peer does not send the response back for three consecutive packetsin the interval of 300 milliseconds.Overall, around in 900 milliseconds, you will detect that failure.So you can think around within one second.And these settings would differ as per your router type.Now, if you are using the Cisco router, then you have to fire this command to set your BFD intervalto be 300 millisecond and then three is the count of the failure detections.So this is what the BFD means.And as I said for your exam, it is good to know that with BFD you can lower the network failure detectionto as low as one second.So that's it about BFD.And with that now let's get into the security side of the direct connect connection in the next lecture.So thanks for watching.