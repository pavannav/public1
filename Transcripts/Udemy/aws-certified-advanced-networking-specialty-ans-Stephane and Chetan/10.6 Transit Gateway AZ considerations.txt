# 10.6 Transit Gateway AZ considerations

Hello, and welcome to this lecture.Now, so far for the transit gateway,we had just a simple setupwhere we had three VPCsand then we just have one private subnet in every VPC.And then we created the transit gateway attachmentusing those subnets.Now, what transit gateway does isit also creates the ENR in that particular subnet.And of course that means in a particular availability zonein which that subnet resides.However, there are certain peculiar thingsabout the transit gatewayand you need to design your VPCsand the subnets accordingly.So let's see how the design should look likewhile using the transit gateway.Okay. Now, if you see this architecture,there are public subnets, there are private subnets,and I also have a separate set of subnetsfor connectivity to the transit gateway.Now, this is an ideal setupwhere you dedicate one private subnetin each availability zone for the transit gateway.And when I say in each availability zone,it doesn't mean it's an ideal,but this is how it should bebecause transit gateway has a limitationwhere if you create the transit gateway attachmentin a given AZ,then only the subnet from that AZwould be able to communicate with the transit gatewaythrough that attachment.Now, this is not the something that we have seen earlier.If you create, say ENI,all the subnet in the VPC are typically able to communicatevia that ENI.But this is not true for the transit gateway.And that's where if you need connectivityacross three availability zones,then you should have transit gateway attachmentcreated in three availability zone in your VPC.And that's what is shown here, right?And it is also good to have a dedicated subnetfor transit gateway attachments.Sometimes you also have to, you know,use the transit gateway for centralized IPS and IDS systemsor you want to have the outbound internet connectivitythrough the transit gateway.In that case, it makes sense to have a dedicated subnetso that you can have the corresponding routesattached to this private subnets.Okay, so the Availability Zone considerationsfor transit gateways.So when you attach VPC to the transit gateway,you must enable one or more AZsto be used by transit gatewayso that the traffic can be routedto the VPC subnet in those particular AZs.So to enable each AZ,you specify exactly one subnet,typically in the range of /28,because you don't want to waste unnecessarilythe IP addresses inside VPC./28 still gives 11 usable IP addresses.Now transit gateway placesnetwork interface in the subnet using one IP addressfrom the subnet, we know that.But remember that after you enable the AZ,traffic can be routed to all subnet in that AZ only.So resources that reside in the same AZ,where there is no transit gateway attachmentcannot reach to the transit gateway, right?So that's one important considerationwhen it comes to the transit gateway.Remember, in whichever AZ you are creatingthe transit gateway attachment,only the subnets from that AZwill be able to communicate to the transit gateway;other subnets will not be.So that's it for the consideration of the VPCsand subnet for transit gateway.And now we'll see some more architecturesfor the transit gateway.Thanks for watching.