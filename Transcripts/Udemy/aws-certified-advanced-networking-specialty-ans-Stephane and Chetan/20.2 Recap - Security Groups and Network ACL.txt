# 20.2 Recap - Security Groups and Network ACL

So in this section, as we are talking about the security, I thought, uh, it would be good to recapthe security group and network ACL first before we move to the other, uh, network firewall that AWSprovides.So quickly, let's, uh, revisit what security group and network ACL has to offer.So security group, it can be applied to EC2 instances.And these security group allows you to have the allow rules.Right.And security groups are actually associated with the Enis right.Whether that Eni is for EC2 or RDS instance or Lambda function inside a VPC.Now security groups are stateful.That means any traffic that is coming in the outbound traffic is allowed automatically, and the otherway round, if any traffic is going out of your Eni, then the return traffic from the destination willalso be allowed automatically.And that's what the stateful means.Now security Group can reference the Cidr notation when you provide the source IP address range in inboundrules or destination address range in the outbound rules, and additionally it can also refer to anothersecurity group.And this is a typical scenario where.Suppose you have the Alb and your EC2 instance is behind the Alb.Then you want to allow all the traffic from Alb to reach to the EC2 machine.In that case, in EC2 security group, you will allow the you know the traffic from security group ofyour Alb.Also, security group can be referenced across VPC peering.So if there is a VPC peering connection and EC2 instances are launched across this VPC peering connections,then one EC2 instance security group can refer to other EC2 instance security group in another VPC.Now similar use case like Alb that I just explained.Now by default there is no inbound rule in the security group and all outbound traffic is allowed.Now this is true when you create a security group and you don't add any rule by yourself, right?That's a default behavior.Now in case of network ACL, as you know they operate at the subnet level right.And they are stateless.That means you have to define the rule for both inbound and outbound traffic as well.So it's not like a security group again.Um you can reference a Cidr range but not the host name.Now by default it denies all inbound and denies all outbound traffic.Now when we say default, which means when you create a new network ACL, then there are no inboundand outbound rules.That means the traffic cannot really flow.Because at the end there is a deny rule in the network ACL.Okay.So that's the basics about security groups and network access control list.And just thing to remember is that the security groups are stateful and network ACL are stateless.And other important thing is that security group only have allow rules, whereas network ACL also supportsthe Deny rules, which also means that if you want to block a traffic from certain IP address, thenyou cannot use security group.But you would have to use network ACL because imagine a case that your server is experiencing some attacksfrom some malicious IPS.Then if you want to only block those particular IPS, there is no provision in the security group becausethere you only have allow rules.Right now with that context, let's look at some of the architectures that may come up when you usesecurity groups and network ACLs together, and primarily in the case where you want to block thosemalicious IP addresses.Okay.So this is a simple use case where there is a EC2 instance with public IP.And this is being accessed over the internet.Right.And EC2 has a security group.And now if you want to block a particular malicious IP, you know that security group cannot do that.And hence in that case, you would have to add a rule into the network ACL inbound rules and deny thetraffic from that malicious IP.So this is very simple use case.Now another use case when you have Alb in front of your EC2 instances.Now in this case again because EC2 receives the traffic from the Alb.In that case, if you want to block traffic at EC2 subnet level, it is not going to help, right?So having the network ACL at this subnet is useless, which means if you want to block again trafficfrom the malicious IP addresses from the client side, then you would have to put the network ACL atthe Alb subnet level, right.So I hope it is understood.And further, if you want to make sure that this EC2 only receives the traffic from the applicationload balancer, then in the EC2 security group, you will allow the traffic only from the Alb securitygroup.So you are sure that EC2 doesn't get traffic from anywhere else.But otherwise for blocking the traffic you would have to use network ACL, right?So I hope it is understood.Now what if you have the NLB right?So when you have the NLB things are quite similar.But you know that NLB also does client IP preservations.If the target is for example, EC2 instances, which means that EC2 instance directly sees the clientIP address, right?So in this case, if there is a client IP preservation, then you can also apply the network ACL atthis EC2 subnet level.And you can block that traffic.So this is very much possible.But one downside is that all that traffic will still flow through the network load balancer.And there will be cost for that traffic as well.Right.So while this is possible, what you could also do is of course you can also have the security Groupfor network load balancer.Now this was released in summer 2023, but otherwise earlier network Load Balancer did not have thesecurity group.But in this scenario, as we discussed, security Group won't help to block the traffic and it is idealto block this traffic at the network load balancer subnet level.Right.So again very similar to what we did for the application load balancer.Okay.So I hope that is also understood.And now just one more scenario.What if you have Amazon CloudFront in front of your application load balancer right now.Because application load balancer now receives the traffic from CloudFront IP addresses.Again you cannot use the network ACL at the Alb subnet level because in that you would have to blockparticular IP address.But Alb is only seeing the IP address of CloudFront distribution, right?So even if you put the network ACL and add the deny rules.It is not going to help, right?So what do you do in this case?So in this case generally what you will do you will use different AWS firewall service.And that is AWS WAF web Application firewall.Or there is also AWS shield which is used for DDoS protection.And in the following lectures we are going to talk in depth about this additional AWS firewall services.Okay.So before we stop for this lecture, there might be one question that might be coming to you is howdo you really make sure that this application instance only receives the traffic from this particularpath.Right.So in this particular scenario, what you can do is you can use security group rules, as we discussedearlier, where EC2 security group will have the inbound rule and the source will be the applicationload balancer security group.So this makes sure that EC2 only receives the traffic from the Alb.But how do you make sure that this Alb only receives the traffic from the CloudFront, and you takecare of the scenario where end client or say, malicious IPS are directly hitting the Alb?How do you really block that traffic?Right.So in that case, because this network ACL is not useful anyways, what we would have to do is in theapplication load balancer security group, we would have to whitelist or allow CloudFront public IPaddresses.Now again CloudFront is not a, you know, small service.It might have hundreds and thousands of IP addresses.Right.So you can't, you know, list all those IPS into the security group inbound rule.But for that we have already discussed AWS has something called IP prefix list.So there are specific prefix list for different AWS services including CloudFront.And it is mentioned in IPS JSON file.So I think we have covered this somewhere else right?So in this case, what we would have to do is in the Alb security Group, we would have to add the allowrule with the CloudFront prefix list.And the prefix list for CloudFront IP addresses is identified with this particular name.Now once you do that and once you have this AWS WAF with CloudFront which blocks the malicious IP,this is end to end secure, because now Alb is only receiving the traffic from CloudFront and EC2 isonly receiving the traffic from the Alb.Right?So this is how you basically use the security group and the network ACLs together to either allow orblock the traffic.Right.So that's it for this lecture.And in the rest of the section we are going to talk about other AWS networking security services.And we will see what they offer.Right.So we'll stop here.And with that let's go to the next lecture.Thank you.