# 5.7 Introduction to Route53 DNS Resolver Endpoints (Hybrid DNS)

Hi.So before we wrap up this section, I wanted to quickly talk about route 53 resolver Endpoints, whichis a relatively new feature of Amazon provided DNS server.And there is a purpose why AWS launched this feature.So let's try to understand that.And then we are going to dive deep into resolver endpoints into the route 53 section.So what are these resolver endpoints.So before that let's understand the problem.So we already saw the right side of this picture where we have the VPC and then private DNS.And then this route 53 DNS resolver can resolve the DNS queries which comes from the EC2 instances insidethe VPC.But as you know, in the real world, you might be operating across your on premise data center andthe VPC.So some servers could be in data center, some servers are in the VPC, and you would want to have acommon DNS records to assign the domain name for all these servers.So there could be three scenarios.First, maybe you want to have your DNS server on premises.So all the DNS queries from these EC2 instances should go to your on premises DNS server.Or second, you want to manage that in Amazon route 53 private hosted zone, for example, and all theDNS queries from on premises are forwarded to Amazon route 53.And third, it could be bidirectional, so you might have some part of the DNS hosted in AWS and somepart in on premise data center.Now, if that is the case, you might be assuming that if both these networks are connected over a VPNor a direct connect, then very well.This DNS server can reach to your route 53 DNS resolver and then everything will work.But that's not the case.By default.This route 53 DNS server is not accessible from your on premises network.And that was the problem.And exactly to solve this problem, AWS launched route 53 DNS resolver endpoints.And these endpoints can be accessed over from this corporate data center.Because these endpoints have the Enis elastic network interfaces which are created inside your VPC.So let's understand what these endpoints are and what problems they solve.So in almost 2018 or 19, the dot two resolver was officially named as route 53 resolver.And this provides both inbound and outbound endpoints.And of course, as the name suggests, the inbound endpoint receives the DNS queries from your on premisesnetwork, and outbound endpoint is used to send the DNS queries from the VPC or from the AWS to theon premise network.So with respect to the architecture, this is the architecture for inbound endpoints where you havethe DNS resolver, but additionally you are configuring the inbound endpoints which creates the Enisin multiple subnets for high availability.And then all the DNS queries from your on premises network can be forwarded to these IP addresses whichthese Enis have.And then these Enis will ultimately send it to route 53 DNS resolver, which can of course resolve theDNS queries from public or private hosted zone or public DNS.So here you are managing your DNS in the AWS.Similarly, if you have the DNS management on the corporate data center side, then you will have outboundresolver endpoints configured in the VPC.And then any request that comes from the EC2 instances will first go to the Amazon provided DNS server.And there it will see if it matches any of the private hosted zone.If not, it will send it to the outbound resolver.Or you can also configure the conditional forwarder, which checks what is the domain name is, andaccordingly it will send that request to this outbound Eni.And this outbound Eni will send the request to the on premises DNS server.So that is the other way around.So I would say this is the latest way in which you should think of hybrid DNS in AWS.Now earlier we saw that we could have Dhcp option set in which you can configure the IP address of yourown DNS server.But with this route 53 resolver endpoints this is much simplified.Now you can have both inbound and outbound DNS connectivity if you use this endpoint.So at this moment that's it about this resolver endpoints.And as I said we will cover this in very detail.And we will also do some exercises in the route 53 section.So I hope that is clear.And with that we will wrap up this section with quick exam tips.And we will move to the next section.So thanks for watching and stay tuned.