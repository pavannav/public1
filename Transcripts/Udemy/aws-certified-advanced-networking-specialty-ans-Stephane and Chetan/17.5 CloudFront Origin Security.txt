# 17.5 CloudFront Origin Security

So now let's see how we can secure CloudFrontin our origin.So for Amazon S3 buckets, we've seen this architecture,and the idea is that we want to be able to preventdirect access to files in our S3 bucketsso that they're only accessible through CloudFront.For this, we know we should use an Origin Access Controland associate it with your distribution.This was previously known as an OAI, but now it's an OAC.And then, once the OAC has been defined on CloudFront,then you edit your S3 bucket policyso that only the OAC has the permissions to doget object operations.And then any user trying to access your S3 bucketsfrom outside your CloudFront distributionwill be denied because of the bucket policy.Now it's going to get a little bit trickierwhen it comes to custom originsbecause there is no built-in way to do it.So the idea is that, for example,say we have an application balanceror we have an HTTP custom origin, maybe a web server,and we only want them to be accessible from CloudFront.So we have two ways of doing it,and one is going to be an application logic.So let's take the example of a user accessingour CloudFront edge locationand is going to give it a few headers.Now, CloudFront is going to be set upso that what we get is that we're going to havea custom HTTP header added to the requestthat it will be sent into your ALB or your custom origin.So the idea now is that there's an X-Custom-Headerthat is customly added with some random content in it,and this gets passed onto the origin.Now the origin can be configuredeither at the ALB level or maybe your application levelto only forward requeststhat contain that specific custom HTTP header,otherwise, it'll refuse the request.So in the ALB, you can set up a filter and saying,"Hey, if the request has this header,then please forward it to your downstream instances,else, just refuse the request."And the idea is that if a usertries to do a request directly against your ALB,then your ALB will not findthe custom header it expects from CloudFrontand, therefore, will deny this request.So, this will only work if the custom headername and values are kept secret, but this should be fine.Now, you can also have a second level of securityinto this setup, as we saw before,in which we can restrict the originto only have a networking accessfrom the CloudFront locations.And the CloudFront public IP addresses are availableat the URL on the bottom right corner.So this gives you an application-level security,as well as a network-level securityto access your custom origin.And if you wanted to post this architecture one step deeper,we can leverage WAF as a service.So the idea is that now we have the same architecture,so just simplified with an ALBbecause WAF is integrated directly with ALBs.And so we can have a WAF ruledirectly in front of CloudFrontas a first level of security.And then CloudFront will add a custom HTTP header,for example, the X-Origin-Verifyand whatever secret value you have in mind.And then, before reaching the ALB,it's going to reach a second level WAF.And this WAF will have a filtering rule set in placeso that only the request that are having this HTTP headerare passed on to the ALB.Then, if someone tried to directly access the ALB,it will have first to access itthrough your web application firewall.And then WAF will deny this requestbecause it does not contain the custom HTTP header.So this time the filtering logic happensnot at the ALB level, but at the WAF level,which may be easier to manage.Why? Well, because we can set upthat secret header name and value in Secrets Managerwith an auto-rotation policy for enhanced security.And then once in a while,this Secrets Manager will invoke a Lambda function.And this Lambda function will update the custom HTTP headerwith a new value in CloudFront directly using an API call,but also update it in WAF folder filtering rule.Meaning that we can probably rotate this every 30 daysto have maximum level of security.So that's it for this lecture.This gives you a good ideainto how you can secure network accessinto your origin by limiting in CloudFront.I hope you liked it, and I will see you in the next lecture.