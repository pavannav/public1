# 19.26 Hands On- Route53 Resolvers - Part 2 - DNS configuration

Okay.Now this is step two where we will configure the domain names on both the sides of the network.And for that we need to first start with the AWS site where we'll create the private hosted zone withthe name Cloud.com.And there we will create the A record with the IP address of the app server.And then we will test the DNS resolution from the cloud side itself.Right.So let's do this okay.So for that we need to go to the route 53 and create a hosted zone with the name say Cloud.com.It's a private hosted zone.And that's where it has to be attached to our VPC on the cloud side.So it's in Mumbai region and it has to be attached to the cloud VPC.And let's create the hosted zone.Now next thing we need to create a record of the type A with the name say app dot cloud.com.And it should be the private IP address of the app server itself, which is 163.So let's add that and create the record.Okay.So this record has been created here.Now one more thing that you need to remember is that for resolving the DNS through route 53 local DNS,we need to enable the DNS resolution for the VPC.So for that let's go to the VPC settings action edit VPC settings and enable this both the settings.Then only the DNS resolution happens with private hosted zone.So let's do that.Right.So that is done.Now let's go back to the cloud app server and try to resolve this DNS.Okay so I was already logged in there.So let me try Nslookup then app.cloud.com.So it takes some time.So let me wait for maybe a couple of minutes and then I will come back here.Okay so I waited for a couple of minutes.And in your case if it is not resolving you can also reboot the EC2 instance so that new DNS settingschange into the effect.Right.And you can do that using sudo reboot command.Right.So let's do nslookup and then app.cloud.com.And you can see that it is resolving this to IP address.So which means cloud side of the DNS is working fine.But this is just on the cloud side.We actually want to do that from the on premises side.Now for that first we need to set up on premises side of the DNS server and view additional configurations.So now let's do that.Okay so we just completed this step.And now let's do the other part.And here we will launch this DNS server again on the EC2 instance.And then we will configure it.Now here as you know we need to open the security group for SSH so that we can configure and then DNStraffic all the way from cloud side and also from this particular subnet or the entire VPC Cidr.Right.So again let's launch this instance and then let's configure it.So for that we need to install the bind and bind utils which configures it as a DNS server.So we will do that.And then we need to create some additional configuration files okay.So going back to the EC2 console.So we have this three servers.So let's launch one more and let's call it on prem DNS server and Amazon Linux 2023 Ami.Then the key pair as it is.And then we need to launch this in on premises VPC and in a different subnet that is a private subnet.So for security group we need to allow the DNS traffic mainly.So let's do that.So SSH from anywhere.That's okay.And here let's say dns UDP from 192.168.So that other application server in the same VPC can query to the DNS and additionally.From the cloud side.So for that ten .0.0.0 slash 16.Right.So that's done.Let's launch the instance.Okay.So now we need to log in to this instance.Now this instance is in a private subnet.So we can't directly log in.But we can SSH from the VPN server because VPN server is in a public subnet.Right.So let's take a private IP of this instance which is this.And let's go to the SSH session of the VPN server.And from there let's log in to the DNS server.Right.So I had this putty session already to log in to the app server.So I can just duplicate this session which will get me into the VPN server first.Right.And you can verify that using the IP address range.And from there I can use the same SSH key which I had created.And now I will log in to the DNS server.So the IP was 192.168, then dot ten something.So that was 31.Sorry I keep forgetting about these IP addresses.Right.So 31 let's say yes and then I'm into the DNS server.Now next thing I need to install the DNS packages.So first thing let's update the yum packages sudo yum update minus y.So it will take some time to update the entire yum repository.Okay, surprisingly there was nothing to update.And now let's install the DNS packages.So sudo yum install bind and then bind utils as well.Yes.Right.So that's done.So we did these steps.Next we need to create zone files for on prem dotcom.And for that I have provided a text file because it is easy to copy from the text file.So we need to create a zone file in this particular directory and paste this content and replace thisparticular IP addresses with the IP address of our app server.So let's do that.And this is the text file that has been provided with this lecture.So you can just copy it from here and replace the IP addresses as required.So let's create this var named on prem.com dot zone file okay.So again sudo viswa named on prem.com dot zone file insert.And here we need to replace this IP addresses with the app server IP address which is 11 dot I think.One 5711 dot 157 let's verify that once.Yeah that's correct.So here a few things.You might see some text misalignment.So if you want to correct that you can do that escape colon wq right.So that's done.Next we need to configure Etsy Named.conf and paste this content again here.We need to replace this with the IP address of the DNS server itself.So let's do this.So again I'm going back to the text file and just copying this content from here.Now one thing to note is that because you have multiple putty session open, make sure that you arein the right server and that you can verify using the IP address.Right?So let's do sudo VI slash, etc..Named.conf.Just delete everything so D and shift G deletes everything.I for insert mode, right click.Sorry.Let me copy the content again.And here make sure that you replace this with the IP address of the DNS server itself.So it is dot ten.And then something which is 31 because we are on the DNS server itself.Right.So that is done.Let's save this file.And now let's start the named service which is a DNS service basically.So so for that we can use systemctl command.So let's say sudo systemctl start or restart anything works name t dot service.And if you don't see any error then that means service is probably started well.And you can check that using the status command.And it says that it is running and active, and it has also imported this zone file so it can resolvethe DNS for on prem.com.Now in some cases, if you miss some syntax in the zone file then this service will not start.And for that you can use named dash check conf command.And you can provide the path of the configuration files.And it will tell you if the file is okay or not.So if required you can do that okay.So this is done.Next thing we need to make sure that this app server sends all the DNS queries to this DNS server.And for that on this app server we can say the name server is the DNS server private IP address.And we can do that using the result dot conf file.So let's do that okay.So I am on to the app server.And you can verify using the IP address.And at this point if I do say ping app dot on prem com it won't resolve because it doesn't know right?Until we update the name server.So let's do sudo VI slash, etc. resolv.conf.And in here the IP address should be the IP address of our DNS server, which is 10.31 I think.So let's save it and let's try again.And you see the DNS is being resolved to this private IP address which is itself.But then it is happening.Right.So which means DNS is working fine on the on premises side as well.Okay.So which means we have configured the DNS but they are still isolated.That means cloud DNS on the cloud side on premise DNS on the on premises side working fine.And this is one of the milestone.So congratulations.Now that was step two.And now in step three let's actually set up the hybrid DNS.So we should be able to resolve the DNS from across both the networks right.So let's stop here and let's go to the step three.