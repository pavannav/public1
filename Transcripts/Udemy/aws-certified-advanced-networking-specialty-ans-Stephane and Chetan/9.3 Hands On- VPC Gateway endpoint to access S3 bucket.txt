# 9.3 Hands On- VPC Gateway endpoint to access S3 bucket

Okay.So now let's look at how to use VPC gateway endpoint to access Amazon S3.So this is your say region in which you have S3 buckets.And then you have VPC with Cidr range of 10.1.0.0 16.And there is an internet gateway.There is a public subnet so that you can have a bastion host.And then you have private subnet in that you will have your say application which would basically accessS3 bucket privately using the VPC endpoint.Right.And for granting the S3 permissions we would also need IAM role for this EC2 instance that we will associatewith that.Once that is done, we will create a gateway endpoint for S3.And we will update the route table so that traffic for S3 using that prefix list can go through thetarget as VPC endpoint.And then to test this we will ssh into the bastion host.And from there we'll ssh into the EC2 instance, and from there we'll try to say upload some file tothe S3 bucket.So that should be our goal, right?While this was something I had planned initially to do, but if you are following these exercises inthis course, we always do this where we first log into the public EC2 instance and then go to the EC2instance.And that's for the reason that we can't directly get into the EC2 instance.But this time I want to do things a little differently.And that's for the reason that AWS has now capability where you can directly access this EC2 instanceusing AWS console itself, and that is done using something called EC2 Instance Connect.Now, that's not a part of this discussion, but I think this is a good time where we could experimentwith that so that you learn something new about how to access EC2 instances.But rest of the settings, they remain as it is where we will access the VPC gateway endpoint from thisEC2 instance.So with the EC2 instance connect, The setup is now changed.We don't need internet gateway here for this VPC, but rather we'll have just two private subnet.In one subnet we have this EC2 and in another subnet we will create EC2 instance connect endpoint.So this is another way to directly access private EC2 instance from AWS console over the browser.And there is no cost for EC2 instance connect.So you can very well use that for your own workload as well.So in here we will create this EC2 instance connect and then we'll have EC2 Instance Connect servicewhich is managed by AWS.And from AWS Management Console we will connect to this EC2 instance connect endpoint.And from there we will connect to this EC2 instance.And then we will test the connectivity to the S3.Now one thing to remember that whenever you create EC2 instance connect, there is also a security groupbecause it has an IP address, right?So for EC2 instance connect in specific, we will have the outbound security group rule, which allowsthe traffic of SSH to this EC2 instance.So that's one rule you need to take care.And for this EC2 instance we will have two rules.Inbound should allow the SSH traffic either from this subnet, this VPC or this particular securitygroup of EC2 instance connect.And then outbound rule should allow the traffic going to the S3 over port 443.So this is something you already know.But for instance connect we need to also allow SSH inbound right.So I hope it is clear.And with that the steps for our this demo will be all this.If you want you can pause the video and read these steps.But essentially in my AWS account I have already a VPC with two private subnets.And that's it.Rest of the things we will create on the fly.Right?So let's go to AWS console and do these things okay.As you can see I have a VPC.And this VPC has two subnets one for end point and one for EC2 itself, right?So the first thing, let's create a couple of security groups.One for the EC2 connect endpoint and one for EC2 itself.Right.So let's go to the security groups and create a security group.Let's call it EC2 Connect Endpoint Security group.Right.And allow SSH outbound.That is what is required.VPC is the demo one.And in the outbound we need the SSH traffic going to the same VPC.You can say or EC2 security group right.Let's go with the VPC Cidr so that it's easy.Now we don't need inbound rule here.So let's create this security group okay.Similarly let's create the security group for EC2 instance that we are going to use.And this will allow the inbound and outbound Https right.This should be in our demo VPC and inbound rule is for the SSH and I'm keeping it as 00000.This is because when you use EC2 Instance Connect, you can change the setting that you have to preservethe client IP.And in that case it will be IP of my machine or my laptop.But by default that is turned off, which means EC2 will see the incoming IP of the EC2 connect endpointitself, which will be the private IP from the VPC Cidr.So rather, let's keep it 0000 so that it allows all the traffic.Now outbound we need https.That's where the traffic will go to the S3.And in here we can use the prefix list for S3.So you can do something like this so that traffic is restricted only to reach to the S3 bucket.Right.So let's create this as well.So we now have both the security groups.Next we want to create an IAM role for EC2 instance so that it can access the S3 bucket.So let's go to the IAM console.Let's go to the roles and let's create a role that is for EC2 service.So let's select that next.And we need S3 full permission as of now.Ideally you should have the restricted permission to either upload or download depending on your usecase.But I am granting full permission and let's call it EC2 S3 full permission for endpoint demo.And let's create that role.Okay, role has been created.Next let's also create the VPC EC2 instance connect endpoint first.So for that we have to go to the endpoints.Now this is not a VPC gateway endpoint that we are really testing.This is just to connect to the EC2 instance directly from the browser.So let's create the endpoint and let's call it EC2.to connect endpoint.And here you will see different options.In fact there are these new options for service network resources.All that we will talk about a little later.But here we have to select the EC2 instance Connect endpoint.And the VPC is demo VPC.And the security group is the one that we had created.So let's see if it can search for that.Yes it can.So let's select that.And the subnet is EC2 connect endpoint private subnet.Right.And let's create this endpoint okay.So it will take maybe a couple of minutes.By that time we can go and create our EC2 instance.So let's go and launch the instance.So I'm into the Mumbai region and let's call it EC2.And I'm using Amazon Linux because Amazon Linux already has the Instance Connect software installed.If you are using different flavors of Linux, then it may not be there.So you need to install the instance connect.Okay so EC2 instance type.Let's keep it as it is my SSH key pair network.I want to have it in a demo VPC, and I want to have it in separate subnet that I had created for thisinstance, which is this security group.I'll select the one that we had created that was EC2, SG.And then in the advanced settings let's attach the IAM role that we had created for EC2.So I think it was having the long name.Yeah S3 full permission for endpoint demo.And I think that's it.Let's launch the instance.Okay this is done.So let me wait for a minute for this instance to be running.And then we will first login to the instance and see if we can access the S3 bucket okay.So this EC2 connect endpoint took at least 2 to 3 minutes for it to be available.So you would have to wait.Now it has been created.So I'm expecting that now we can connect to the private EC2 instance using this endpoint.So basically we have done all this inside this VPC.We have this instance connect and private EC2 instance.And now let's login to this instance.Okay.So for that let's go to the EC2 and let's select this EC2 instance let's say connect.Now in here you can connect using two methods either directly with the instance connect service or usingthe endpoint.So we have to select this endpoint here and rest of the settings as it is.And let's say connect.And you can see that it is trying to connect to the instance directly.And it has connected.So without having the internet gateway and without having the instance in a public subnet, we couldstill connect directly.Okay.So we are into the instance.Now let's create a dummy file so that we can test the upload to the S3 bucket.So let's say echo this is a sample Pull file and let's call it sample dot.TXT right.So you can see this file here.And let's try now.AWS s3 cp sample.Sample dot txt and s3.And my S3 bucket is this.So name is this.So let me copy this.And sorry copied some junk character.So make sure that it doesn't do it in your case.And here I'm trying to copy this file.Now if you do this it won't work.The reason this EC2 instance does not have outbound internet connectivity.And by default this traffic will go over to the internet.This is what we learned right.So I'm just pausing this or suspending this okay.So next we want to create a VPC gateway endpoint.And then let's see if it works right.So let's go to the VPC console again and let's go to the endpoints.Now let's create an endpoint.Let's call it S3 gateway endpoint.And this is for AWS service and service name is S3.And here you see both these options as I told earlier gateway and interface.Now in this case we want to use gateway endpoint.And this should be in our demo VPC.And for this which route table has to be modified.So there is already a route table for this subnet in which we have the EC2 instance.So same route table will be automatically modified where the prefix list will be added to the routetable.And I will show you shortly.What do I mean by that?Further, there is no policy.So we are granting the full access to all the S3 buckets basically to reach to S3 bucket rather.And it doesn't mean that the EC2 can put the data to all S3 bucket.It means that VPC gateway endpoint can reach to all S3 bucket.But if you want, you can restrict this to reach to a specific S3 bucket as well.And that we all learned in our previous lecture.Right.So I'm not doing anything here.Let's have a full access and let's create this endpoint.Now ideally we don't need to do anything else because everything is set up.This endpoint is also available.Let's check this route table and see if we see that additional entry.So let's wait for some time and refresh this.So you can see that for this prefix list the endpoint has been added as a target.So which means if we now go back to our EC2 and try the same command, it should work.And you can see that the upload has been successful.We can verify that by going here in S3 bucket.And let's refresh that.And you see that it has been uploaded.Right.So that's what we did in this exercise.So basically we have tested the connectivity from this EC2 instance to this S3 bucket using the gatewayendpoint.Now please remember the rest of the things.This is not related to this exercise, but we could also connect to the EC2 instance using this EC2instance connect endpoint, which is a new type of endpoint and there is no cost for that.And you then save the cost of your bastion host.So I think this is also a good way to avoid some cost and directly access your EC2 instance, right?So I hope that is clear.And if you are also doing this exercise, please delete everything that you had created.Terminate your EC2 instances.Uh, you may want to keep the instance connect endpoint if you are using the same VPC for other exercises.And I am also going to do the same.I'm just going to terminate the EC2 instance.Right.So that's it for this demo and I'll see you in the next lecture.Thank you.