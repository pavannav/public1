# 7.7 Walkthrough- VPC Network Access Analyzer

Hi.So as discussed in the previous lecture,now let's look at the network access analyzer.Now this works similar to the reachability analyzer,but just that the purpose is different.Now, we are saying that wewant to analyze if there are any noncompliant paths exist in our network configuration.So what's the purpose of VPC network access analyzer?So it identifies unintended network access to theAWS resources.Now, this is the other way around in the sense that you have to define thatthis is the connectivity that you don't wantand then you specify as a network scope and thenthe network access analyzer will figure out whetherthat kind of connectivity is there in your deploymentand it will flag it as findings and we will see a short demo of that.So you can use it to say isolate network segments.For example, there is a development VPC and the production VPCand you want to make sure that there is no connectivity between these two VPCs.Now you can go as granular as Subnetlevel or the ENI level or EC2 instances level.All that is possible when you configure the network scope.Another use case is to see if there isno internet inbound or outbound connectivity for your resources.For example, if there are database instances,then they should not be reachable from the internetand the other way around.So that can also be analyzed using the network access analyzerfurther, you can also look at the trusted network paths.That means when the traffic goes from the source to the destination,you want to make sure that it crosses through a particular device, for example,through the NAT gateways or through a particular Firewall.Now, this can also be verified using network access analyzer.And finally,you can have the trusted network access in the sense that you can mention that thetraffic can only come from a particular IPaddress range or a particular port or particularAWS resource.Now, in order to do this, you have to define the network scopeand then verify that whether it meets your compliance requirement or not.I know that just from the slides,it doesn't really tell you what exactly it does and how it does.So better, let's look at the example and see that in action.So let's try to analyze one of the use case.So for example, we have two VPCs and VPC-A is kind of development VPC and VPC-Bis production VPCand we don't want to have any network connectivity between them.So to begin with, we will have these two VPCs isolatedand then we'll create the network accessanalyzer scope which says that these two VPCsshould be isolatedand then we'll see what the result comes out to be.And later,we will set up a VPCpeering connectionso that we violate that rule.And then we will again analyze this network scope and see the findings.So that's the purpose.And now to save the time,what I have done is I have alreadycreated these two VPCs which are completely isolated.And currently there is no VPC peering between them. So let me take you to theAWS console and show you thatokay. You can see myAWS VPC console. I'm in Mumbai Region andI have two VPCsand then I have one public Subnetin each VPC. And further I have launched EC2 instances in both the VPCs.Now these are isolated VPCs. That meansthistwo machine cannot reach to each other over the private IP address, right?And now in order to go to the network access analyzer,you have to go to the network managerconsole which is here.And if you go on the left side, you will see this option here.Now make sure that you are in the Mumbai Region.Okay. So before we create our own network access scope,let's talk about these default scopes which comes along with yourAWS account.AndAWS hasprovided these scopes for us for easy troubleshootingSo if you just read the purpose of this scope, if you just talk about this one, right?AWS VPCIngress.So what it does is it identifies theIngress paths into your VPC from internet Gateway or peering connectionor VPC endpoints or VPN or Transit Gateway.That means any VPC resources which can be reached out from any of these components,then it will flag it as finding.Similarly, there is this egressIGWpath which is any resource in your VPC.If it can reach to the internet as an outbound traffic,then it will flag those network interfaces.Similarly, there is VPC egresswhich is corresponding to the first scope,which is, if your VPC resource can go out to theinternet or the peering connection or to the VPC endpoints.Likewise, then it will flag those resources. And similarly the fourth one.So to start with, let's look at the first one.So let's select this one and analyze this.Now it will take maybe a couple of minutes for this analysis.So I'm just pausing this video here.Okay. So it took around one minute for this analysis to finish.And if you read this, it has detected 2 findings, right?So if you go down, you will see this 2 violations for this rule.So first one is thiswhich sayssorry, it directly opens the network interface.But if you just go down here, it will say that from the internet Gatewayyou can reach out to this ENI which is attached to this particularEC2 instance,which we don't want because network scope is all about what you don't want, right?So it has flagged it as your violationand it also checks about the destination ports.As of now, you can reach to only port 22.But if you open the security group and allow port80 then it will also talk about that port,right?And similarly, this is for anotherinstance in VPC-Bright? So it has flagged both these as non-compliant rules.And now similarly, you can also run the analysis for rest of the scopes.Okay. So I hope that is clear.Now let's do our bit. That means we want to create our own scopeand thenAWS provides some templates to get started with.For example, if you want to locate databases which are accessible from the internetor non HTTPS access to the web server, all these are, you know,violation to our security policies.So you can get started with any of these templates.But here let's start with our own empty templateand let's call it sayno connectivitybetweenDave and PROD VPC,right?Andwe have to then define the match conditionsas in what is the source VPCand what's the destination VPC.So let's add a match condition and our source is VPC.So let's say resource id and the type is VPCand thenresource id is VPC-A.And now you can also optionally define the traffic type,whether it's a TCP or a UDP or what's the port, which allows this communication.But here we are just talking about generic connectivity between the two VPCs.So we won't add anything here.And now you have to define the destination.So again, the destination is the resource id of the type VPC.And here the destination is VPCB,right?That's it and say next.So it will give you the Jasonandcreate this scope.Now, once you create the scope, you just say analyzeand again, it will take maybe a minute or so. So I'm just pausing this video again.Okay.So analysis has finished and you see there are no findings,that means both these VPCs cannot communicate with each other.And this is what we wanted basically.So everything is fineand this is what we had expected.Now, let's do one thing. Let's go here and let's create a VPCpeering connection between these two VPCswhich is here.And let's create a peering connectionwhich is between VPC A andVPC B,right? And then let's create the peering connection.So as you know, the peering has to be accepted, so let's accept the peeringand that's it.Now we just have a peering connection,but we haven't added anything in our route table.So at this point, let's go here and analyze this again.So again, let me pause this video for a minute.Okay. So you see that still there are no findings even though there is a VPCpeering connection,but traffic cannot flow because there areno route tables which takes the traffic fromVPC A to VPCB.So now let's finally go to our route tableandlet's go to the VPC A route tableand sayif the traffic is going to VPCB which is 172.160.0016,then it has to go through the VPCpeering connectionand save it.And similarly, let's also go to the VPC B root tableandlet's edit this route and say if the traffic is going to VPCA,then it has to also go through the peering connection.Okay. That's it. And now let's come back here and analyze this again.So pause this video.Okay.So now if you see there are findings detected andthere are two network interfaces which are in violation.So start is VPC and A andVPC Band the traffic can flow from VPCA to VPCB.And if you see this is a VPCpeering which is allowing this traffic,so traffic can go right from VPCA instanceENIto the security group, outbound ruleto theNetwork-ACL of the VPCA Subnetto the route tableto the VPC peering connectionto the Network-Access-Control-List inbound rule of VPC B Subnet,then the EC2 instance B security group to the EC2 instance Bso end to end traffic is nowallowed which we did not want.And that's where it has been flagged off.So this is how you define the scope and then you runthis analysis to find out if there are any findings detected,right?So I hope that is clear.OK, just word of caution, if you have also implemented this,just go and terminate everything thatyou created specifically these two instancesbecause you might not be in the free tier quota.And once you do that also tear down your VPCpeering connection and also delete your VPCs.And just one more thing which I should have probably stated earlier,there is also some small charge for this excess analyzerthat is $0.002 perENI that is being analyzed. Now, in our case, there were twoENIsand hence you multiply that by this number for every analysis,which is not a very huge price, but still there is some charge.So just bear that in mind.Okay. So with that, we will stop here.So just to recap in the last couple of lectures,we saw two ways to analyze the network traffic.One is through the VPC reachability analyzerwhereit tells you that whether from the source you can reach to the destinationand if not, then what is blocking that traffic.And the second is network access analyzerwhich can flag the unintended network path between the source and the destination.So that will stop here and we'll move to the next lecture. Thank you.