# 3.5 VPC Addressing (CIDR)

Hi.So now let's get into the details of the first component that is Sidr.This is how we address our VPC and believe me, most of the people find it most confusing thing to understand.But after this lecture, I'm sure you will feel much confident about the VPC.Cidrs.So very first thing before we understand VPC Sidr is understanding the IP addresses.So I'm sure you understand the IP address scheme.So there are two types of the IP addresses IPv4 and IPv6 addresses.Now so far IPv4 addresses were sufficient.But with the advent of Internet of Things, IoT and so many other applications and massive digital transformation,we know that we are exhausting the IPv4 address range.And that's where now IPV six range has been introduced, which is much, much larger than IPV four addressrange.And for foreseeable future, we can't exhaust IPV six addresses because they are 128 bit in length asopposed to IPV four addresses, which are just 32 bit in length.Okay, let's start with the IP.Why you need IP.So IP is Internet protocol, and every machine on the internet should have the IP address so that wecan reach to it.Right.So IP address is an identity of the host in the network.Now as I said there are two types of the IP addresses IPV four which is 32 bit in size.And then IPV six address is 128 bit in size.Now every bit represents the power of two.That means for IPv4 addresses we have two raised to 32 those many IP addresses available.And for IPV six it is two raised to 128.So in this lecture we will mainly focus on IPv4 addresses because it's easier to demonstrate.But same logic applies for IPv6 addresses as well.So just a quick refresher on IPV four addresses how they are represented.So you know that there are 32 bit.So in that there are four octets.So here you see 12.34.56.67.So there are four eight bits.And every eight bit octet has been separated by a dot.Right.So it ultimately represents the decimal value of that octet.So for example if you have an IP address 192.168.56.212.In that case, if you want to represent that as a 32 bit, then you would have to convert it into thebinary as an each octet.And then you can see the binary value of that.So 192 can be represented in a binary.As you know this calculations you have to turn on the bits which makes 192.So bit number seven and bit number six.And if you look at the binary then it should look something like this.So that's 192.Similarly you can have 168.56 and two and two.So this is just a binary representation of the IPv4 address.So as I said, maybe in the college days or before that you might have learned different addressingscheme.And that was address of class A, class B or class C.So if you talk about class A address first octet used to be reserved for network addressing.And rest of the three octets were reserved for the host addressing in that network.Similarly, different combinations for class B and class C type of the addresses.But now this scheme has been replaced with the Sidr.And in case of Sidr, you have the base address and you have something called prefix.So here 192.168.0.0 is a base address and slash 16 is the Sidr prefix.But what is this slash 16 and what does it mean.So let's try to decode that.So this is the Cidr notation.And now if you represent that as an binary IP address then we just learned that you can convert everyoctet into binary.And this is how it will look.So there are 32 bits.And we are saying the Sidr prefix is 16.So what.It means that 16 bit we are locating to the network address and rest of the 16 bit out of 32 we areallocating for the host addressing inside that network.So this 16 prefix tells us how many bits we are allocating for the network.So network address is this.And within that network you have this many hosts.So if you create a VPC then this is a network address.And individual host can receive any of the IP address from this range.What that means is for the VPC, 192.168 is a fixed address.And then as you go on creating the host inside the VPC, that means EC2 machine.Then every machine will receive the IP address from the range 0 to 2, 55, and 0 to 255.And shortly we will see with the whiteboarding how this addresses can be allocated to the differenthosts and how many IP addresses you can have inside a VPC when you create that VPC with this kind ofSidr range.But from this slide, I want you to understand that whatever prefix you mention, those many bits areallocated to the network and you can't change that.But rest of the bits which are available to you can be used for individual IP addresses inside thatnetwork, and those can be assigned to the host.Let's say an example.I have a VPC and now in this VPC I give some address.So this is why VPC I give it address ten dot ten .0.0 slash 16.Now as I said, when you create a VPC with this address range out of this network address, first 16bits are given to the network.This 16, which is the prefix that means for this VPC.I cannot change this for any of the host, which I'm going to launch inside this VPC.What I can change is only this two octets, right?So if you see the first address in this VPC will be ten .10.0.0.Now I can change only this part.So the next address will be ten .10.0.10.20.3.Likewise, maybe up to 255, because we are talking about IPV four addresses and this is one octet,right?Eight bits.Now my addresses does not stop here.Once I'm done, I will again write ten dot ten dot now 255 is done.So I need to increment this bit now, so I'll make it 1.0 again.I continue 1.11.21.3.Likewise my.Address here would be ten .10.1.255.Does my address end here?Not really.Right.I can now go on incrementing this bit again.The next address would be ten dot ten .2.02.12.2.Likewise 2.2 55.So that means I can go on increasing this bit now after every 256 addresses.So how many addresses I have in this network.So I have 256 in every block.Likewise I have 256 blocks.So 256 into 256 which is 65536.Now you can make the same calculations here itself.When you create a VPC with this Sidr 16 bit you are giving to the network, that means rest how manybits you have for the host addressing 32 -16, which is 16 address bits you have for the host.So total number of hosts you can have in this network is two raised to 16, which is again 65536.It's as simple as that.So the formula is whenever you see some Sidr for example.Now let's take another example ten dot 100 .0.0 slash 24.If you see this sidr and someone asks you how many IP addresses would be there in this Sidr.So just look at this number 24.That means this many bits are reserved for my network.I only have 32 -24 equal to eight bits available for my host addressing.So total number of host I can have is two raised to eight.That is 256.That means as big this Sidr is, the smaller your network is, because those many bits you are reservingfor the network and rest of the bits are available for host addressing.So I hope this is somewhat clear.Now let's dig it further.Again.Let's see we I have a VPC right.And.Similar address ten .10.0.0 slash 16.But as I said, when you create a VPC, you can't use it as it is.You need to first create subnets if you want to launch a EC2 instance within that VPC.Now what I decide is I need a subnet, and every subnet that is a smaller network within VPC shouldbe of size 256 each.So excuse my drawing here, but my first subnet should have 256 addresses.So let me just write down those addresses.So first address of course the range from which the VPC starts.Then ten .0.012 up to 255.Now this is my first subnet.My second subnet would be ten dot ten .1.0.So this is exactly same what we did earlier.But just we are now calling it as one different subnet than the other.Likewise it would have 255 I can just go on.Adding more subnets.So the next subnet would be ten .1.2.0.And then it would have up to 255 256 total IP addresses.Right now, if I want to write a Cidr notation for this subnet, you can very well write the first.Always write the first starting address there that is ten .1.0.0.And then understand how many IP addresses are there 256 256 means two raised to eight.So for host we have eight eight bits.That means we are giving 24 bits to the network.So.My subnet Cidr will be ten .0.0.0.That is starting address slash 24.And you can, uh, very well uh, cross verify this.If you look at this whole IP address range, this three bits are not changing.So the three octets are not changing for this subnets.They are fixed.And that's where it's 24.Only this part is changing.That is eight bits.And ultimately you can make the same calculation 32 -24 given to the network eight bits are for hostaddressing.So total number of host equal to two raised to eight equal to 256.Right now the same for this subnet.This subnet starts at this address.So we need to write a Cidr ten .10.1.0.Right.Which is my starting address.Now again this subnet is of size 256.So you need to write a prefix of 24 again.So now this is my second subnet.Similarly my third subnet would be ten .1.2.0 slash 24.Likewise I have many such subnets and my last subnet would be ten dot ten dot here 250 5.0 slash 24.Now again I have 256 size of each subnet and 256 such subnets.So again the total size is 256 into 256 which is 6553 sorry three six.Right.And the same calculation you can do that from the VPC out of 32 bits I am giving 16 to the network.So I have 16 for my host addressing within that VPC.So total addresses 655 uh three six.Right.Exactly.Same.So it really depends on us how we divide this VPC into form of subnets.It's not necessary that you always create subnet of size 24.You can very well create a bigger subnet with say 23 or 20 2 or 21 or even the 16.So you can very well just create one VPC, right, which has an address range, say ten dot ten .0.0slash 16 and I.And then you can say I don't want to create multiple subnet.I'm sure that I just need one subnet in in this VPC.Uh, I can't think of the use case, but just let's imagine and then I can just create one subnet.So right insert this say subnet A.And I can have the same IP address range as my VPC now which means this subnet starts from this address.And this subnet ends at this address.And there is no other subnet because we just created one subnet right now.That's interesting.Right.That means it's up to you how many subnets you want as long as you do not reach the VPC limit.Now let's take one more example.And with that I'm sure, uh, the Cidr will be much clearer.Uh, so again the same example we have VPC with say ten .10.0. 16.Now this time I want to create even the smaller subnets.I don't want subnet of size 256 as I want subnet as small as I can because then I want to create many,many subnets, not just 256.Right.So the smallest subnet you can create inside VPC is with slash 28 prefix.Right.And why.So I'll talk about that.But let's understand if you are creating subnet of size now uh with prefix slash 28 how many IP addressesyou would have again in the subnet.Uh 32 -28 which is four bits.So two raise to four two raise to four equal to 16 hosts or IP addresses you can have in this subnet.That means my first uh subnet.Sidr would be this.This is slash, and the first address would be this .1.2.Now, how long it will go?Look at the number of, uh, IPS.The subnet would have only 16 IP addresses and we started at zero.That means the last IP address in this subnet would be this.So it will start at ten .10.0.0.And it will end at ten dot ten .0. 15.Now that's my subnet right.Because I'm creating a smaller subnet.Now what would be the next subnet again ten dot ten dot after this.Right.Zero dot 16.Now next subnet would start here again prefix slash 28.It will go on like this 1718.And of course again it would have 16 IP addresses total.So ten dot ten .0. 31.Now this would be the last IP address in this subnet.Right.So now my subnet ends here.Likewise now you're creating a smaller subnet right.And then how many subnets you can create.You know total IP address in this VPC is this.And every subnet is of size 16.So whatever number it comes those many subnets you can create uh and you can go on as long as you hitthe last uh address in this VPC.That is ten dot ten .255.255.Right.So with that, I hope if given any sidr, you can very well make out, um, what how many IP addresseswill be there in that network?And when you create a network with slash 28, you have to raise to four equal to 16 IP addresses.If it's say 27, it's two raised to five, that is 32.Similarly, if it's 26 then two raised to six equal to 64.So all this calculation you can now easily make.So uh let's try to solve this.If you have the IP address range 19216800 slash 24 again 24.That means 32 minus right 32 -24 equal to eight bits for the host.So two raised to eight equal to 256 IP addresses I would have.It will start with ..0.It will add it dot 255.This is what we just understood.Slash 16.Of course you would have 65536.That is 16 -16.That is 16 bits available for the host addresses two raised to 16.Is this number.Now this is interesting.134 5678 123 slash 32 now make the same calculation 32 -32 equal to zero.So two raised to zero equal to one.That means in this network I can have only one IP address.And it is itself right.This the same IP address which is given here.So that's that's what the slash 32 means.So if you want to specify a single IP address using the Sidr notation it would have slash 32.Similarly 0000/0 is interesting.This means all IP address.You are not reserving anything for the network right.So anything on the internet can be, uh, really matched with this IP address.So typically you will see in the security groups whenever you configure um, uh, firewall rules.And if you want to open that port for the whole world, like for example port 80, that is Http, youwant to open for whole world, you will open the source IP as 0000.That means any IP it can match.Right.So remember this, uh, and if you want to make your calculation and make your life easy, you can goahead and check this website.Uh, and you can just enter your Cidr range and it will exactly show you which IP addresses, uh, willbe there in that, uh, given Sidr.But I hope with this explanation you can make out, uh, on your own.So that's it about the VPC Sidr.And now let's look into the other components of the VPC.Thanks for watching and stay tuned.