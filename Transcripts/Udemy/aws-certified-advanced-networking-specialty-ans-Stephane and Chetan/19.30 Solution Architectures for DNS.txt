# 19.30 Solution Architectures for DNS

So now let's talkabout some solution architecture around DNS,and one of them is called a split-view DNS or split-horizon.This is when you want to use the same domain namefor internal and external uses,and that means that the public and the private hosted zoneswill have the exact same name.For example, example.com.So imagine a VPC, we have a public hosted zone example.com,and we'll also have private hosted zoneassociated with a VPC for example.com as well.Now, if we enter some records in the public hosted zoneand we enter other records in the private hosted zonewith different values, obviously,then what's going to happen?Well, if a client is outside of your VPCand issues a DNS query for app.example.com,there is going to be sent automatically,of course, to the public hosted zonebecause it doesn't have access to their private hosted zone,because it's outside of the VPC.Therefore the IP returned by this recordis going to be a public IP.And maybe this is the public IP of an EC2 instancein a public subnet as so the client knowshow to get to this EC2 instance.If the client is training for db.example.com,of course, that record does not existin the public hosted zone,and it's not going to be forwardedto the private hosted on either.So the records in the private hosted zoneare not going to be accessible from the internet,so there will just be an empty response from the DNS query.Now, if we consider instances or systems in our VPC,for example, EC2 instance in the workspaces.Now, if workspaces is going to ask for app.example.com,it is within your VPC,the VPC is associated with a private hosted zone,and so therefore, what's going to happenis that this workspace is going to get a new replyfrom the private zone with a private IP,and is going to be redirected for example,to another EC2 instance in the private subnets.So as you can see, the same DNS querymade from two different placesis going to return two different IPs,and therefore send us to two different EC2 instances.So, why do we do this?Well, maybe because sometimeswe want you to serve different contents,or we want to require different authenticationfor internal and external users or whatever.There are tons of efficacy for this,but as you can see, it is doable in AWS.It is completely fine to have a public hosted zoneand the private hosted zone for the same domain,and this can come up again in the exam.Another architectureis around at multi-account DNS managementwhile using Route 53 Resolvers.So imagine we want to extend what we learned beforeabout hybrid DNS,but now we have a lot of different accounts.We don't wanna link all these accounts together,and create resolvers in all the accounts.That would be highly inefficient.So how do we do an architecture with that?Well, we have our on-premises data center,which contains our server and our DNS resolverat onprem.private.This is connected obviously to your central DNS accounts.And then in our DNS accounts,we're going to have a classic architecturewith a private hosted zone.A resolver inbound endpoints, a resolver outbound endpoints,and by default, the Routes 53 Resolvers.So this is something we've seen from the past lectures.But now what's going to happen is that we want to delegatethe sub domains to the other accounts.So we're linking two lectures together right now.So in each account,we're going to create a private hosted zonefor a sub domain.For example, account one dot blah, blah, blah,account two dot blah, blah, blah, and so on.And we're, of course, going to associate them togetherby creating an association,and also making sure that the name servers have matchedbased on the sub domains.So once this is associated,what's going to happen is that any requestthat comes inbound, of course,is going to be redirected to my private hosted zone.The private hosted zone knows about the name serversof the other private hosted zones, and so therefore,the inbound requests from on-premisesinto our multi-accounts will be working just fine,and they will be managed, obviously, within each accounts.So these three EC2 instancesare now accessible from your on-premises data center.Now for the outbound is a little bit more tricky,because the private hosted zone is to go up one levelto the private hosted zone in the main DNS account,and then all the way to the on-premises data center.And to do this, well,we're going to create the forwarding ruleson the central DNS accounts, of course,saying, "Hey, for on-premises data center,"please go to this IP."But these forwarding rules do not have to be recreatedin every single accounts,otherwise that would be extremely painful.So there is the AWS RAM service,which allows you to share resources with other accounts.And some things that can be shared with other accountsare forwarding rules.So we can share these forwarding rules with other accountsand associate them, of course,with the private hosted zones and so on.And the idea behind this is that nowthere's only one central place in the central DNS accountswhere these forwarding rules are going to be managed.And then therefore, the account A, B and Cnow have the capability of also resolving the DNS namesof on-premises data center servers, for example.And this is how you do multi-account DNS managementusing resolvers.So I hope that makes sense.I hope you liked this lecture,and I will see you in the next lecture.