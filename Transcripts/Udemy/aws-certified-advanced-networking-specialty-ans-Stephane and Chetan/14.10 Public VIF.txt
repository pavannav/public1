# 14.10 Public VIF

Hello, and welcome to this lecture.Now that you have understanding of the VIFs at high level,now let's dig deep into individual VIFs.So let's start with the public VIF.So as you know,the public VIF is used to accessthe public endpoints on AWS site.Now, when we say endpoint,that means AWS services,as well as public IP addresses on the AWS site.And one thing to note here is thatthese IP addresses are accessiblefrom across the globe where AWS has regions.That means it's not likeif you have Direct Connect connectionin only US region,then you can only access public IPs in that region.You can access public IPsfrom across the regions where AWS has presence.So that's one clear distinction betweenthe public VIF and the private VIF.Now in case of private VIF,if it is connected to the virtual private gateway,which is attached to the VPC,you can only access the VPC in the same regionin which you create the Direct Connect connection.And in case of public VIF, it is not the case,you can access all public services across the globe.So just to explain it better,this is how your connection looks like.Now, if you have Direct Connect locationin the US region,then also you can access all these services from AWS,which are available as a public endpointfrom across the regions.That's what this means.Moving on,so you can access services outside VPClike S3, SQS, Dynamo DB,and all other public IP addresses.Now, if you remember our discussions about the VPNfor high availability,VPN creates two tenants,and those two tenants have public IP addresses.Now those public IP addresses also fallsinto the category of the public IP addressesthat you can access over the public VIF.Now, why we are talking about this at this moment,because later we'll see,how can you create the VPN or a Direct Connect.And that's an important architecturethat we we'll discuss later,but ideally is thatall public IPS are accessible over the public VIF.Now in order to create a public VIF,you need to provide the IPv4 addressesso that there is a BGP peering happeningbetween AWS router and your side of the router.Now for that, AWS expects you to provide /30 CIDRof the public IP addresses.Now, ideally this public IP addressesshould be owned by you if you are providing that CIDR,and if you do not have those public IP addressesfor BGP peering,then you can request AWSto provide those CIDR IP addresses.So in case of you don't have it,then ask to AWSand then AWS will provide you those public IP addressesin the range of /31 range.So one IP will be allocated to AWS side of the routerand the other IP will be allocatedto your customer router.Also, as you know, when you create public VIF,you also need to provide the IP for address prefixesthat you want to advertise.And we briefly touched upon thatin the last lecture as well.This is how AWS knows your IP ranges on your site,and then it can route the trafficfor the destination addresseswire that public VIF.When you provide this IP for public addresses prefixes,AWS verifies that you own those IP address prefixes,because it is possiblethat you just advertise any randompublic IP4 address prefixes,which could be belonging to some other customersand in that case, AWS will think that it's your addressand it will route all the traffic to your prior public VIF.And that's not really the right way to do that.So AWS verifies that you have authorityto advertise those prefixes.Now, moving onAWS also advertises all Amazon prefixesor the BGP session.So the route exchange always happensfrom both the in that means you provide your prefixesand AWS will also provide or advertise its own prefixesfor all public IP ranges.Now these include the prefixesfor services like EC2, S3and including Amazon.com public IP addresses.So AWS won't advertise the non Amazon prefixes.That means all these prefixes will be owned by AWS.Now, we talked about the ip-rangesjsonand this is where it is applicable.Now, suppose that through public VIF,you only want to allow the communicationbetween on-premise data centerand AWS particular regional leap.You don't want your on-premise data centerto access services from a remote region.Now, as you know,public VIF will provide accessto all public IP across the globe,but then how do you restrict that access?Now in this case, what you should do isyou should look at the ip-ranges.jsonand filter out the IP ranges only for the regionyou are interested in.And then at your firewall,you should only whitelistthose IP address ranges from the ip-ranges.json.And this is where you can now filterthe traffic accordingly.Now at customer router firewall,you can then restrict access toand from specific Amazon prefixes.So one thing to understand is thator to remember is that from customer router (indistinct)when you publish or advertise your BGP routes,maximum you can advertise 1000 routesand that's a hard limit,which means that if you sendor advertise more than 1000 routes from your end,then it is possible that AWS route tablemight not be able to cope up with thatand there is some problem with the BGP session.BGP session may also go downand in some cases routing may not happen appropriately.So that's the limit you need to understand.This is one important point with respect to your examso remember that in case of public VIF,1000 prefixes from on-premise network.Now, sometimes if you have more than 1000 prefixes,then what you can do is,you can aggregate those prefixes as one.So for example, if there are two /28 prefixeswith the same base CIDR,then maybe you can combine two /27 prefixes.So all those things you can do at your end,but make sure it does not cross 1000 limit.So now let's look at the public VIF creation, walkthrough,what all parameters it takes,and then we will move to the private VIF as well.So let me get back to the console.So I'm into my Direct Connect consoleand now, as you know,we already had one connection,which is in downstatebecause of course there is no connectivityat the Direct Connect location.Now we'll go to the virtual interfaces,so far I do not have any virtual interfaceso let's create the one.And in this case I will create public virtual interfaceand of course you need to give some names,say my public VIFand which connection on top of ityou want to create this VIFso I can select my connection.Now, as you know, you have to provide the account ID,whether my account ID or another account ID,and in that case, it will be a whole state VIFthat means another account would have to acceptthe VIF request in that account,but in this case, it's my own AWS account.Now the VLAN tag, I can say a 100.BGP ASN, now this could be a public or private,now, if it's a private,I can give something likeany number in the range of what number you see here.And then address familyas of now, we are dealing with only IPV4 addressesand for BGP peer IPs, as you know,you have deployed public IP ranges herein the /30 CIDR range.Now we don't have these IP addressesthat's why we can provide this,but if you have it,you can provide that IP ranges here.If not, you can submit a ticket to AWS,and then AWS will grant you /31 public IP addresses.Note of which, one IPyou can keep to your routerand another IP you can give to AWS side of the router.And finally,the prefixes that you want to advertiseas you know that it should be maximum 1000or over a BGP session,but in this UI, you can only providea 100 prefixes as of now.So that the setting that public VIF takes,and additionally,you can also have the BGP authentication key.As I said, you can put your own authentication key,and if you are not providing it,then AWS will generate that authentication keyand then it will just put that in the configurationsand when you export this configurationsyou will see the randomly generated authentication key.So essentially those are the parameters it takesfor creating the public VIF.Now, we don't have those IP addresses,so we can't really create,but essentially you should be able to do itif you are willing to set this in the real world.So that's it about this lecture,just remember few things which are very importantwith respect to the public VIF.While other things are also important,but just three things.Remember that public VIF enables your networkto connect to all AWS public IP is globally.And then in order to create a public VIF BGP session,you need to provide the public IP rangesin the range of /30 CIDR,if you don't have it, ask AWS to provide that.And third from customer router into AWS,maximum 1000 route prefixes can be advertised,you cannot advertise more than that.So that's it about the public VIFand now let's get into the private VIF.Thanks for watching.