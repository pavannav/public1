# 9.5 VPC PrivateLink - Important to know features

Okay, now let's understand some important things about the VPC endpoint and private link.And please again don't get confused between VPC gateway endpoints.Now whenever we talk about VPC endpoint we are going to talk in the context of VPC private link.So we will use these terms interchangeably.So please make sure that you don't get confused.Okay so let's get to this now important thing that you should know that when you create a VPC endpointspowered by Privatelink, it creates Eni into your subnet inside your VPC, right?And that Eni has the private IP from your subnet.So it has a local IP address through which you access the target AWS services or the network.And we already talked about that.Another important thing to note is that because you have the local IP address, now you can connectto the services inside a VPC which has overlapping cidrs.So your consumer VPC might have the same Cidr as your Target VPC.And shortly we'll talk about this further.For high availability, you have to create this VPC endpoint across multiple availability zones in theconsumer VPC.And again I will show you what do I mean by that.And VPC endpoint having Eni.It also has the security groups.And for us it is the inbound rule which comes into effect.And I will show you how the security group rule should be.Further it supports both IPv4 and IPv6 traffic.We briefly talked about that.And it also supports TCP and UDP traffic.And finally VPC endpoint being Eni can be accessed over from other networks.That means if you have a consumer VPC in which you have VPC endpoint, and that consumer VPC has VPCconnected over VPC peering connection or the transit gateway, for example, then that endpoint is accessiblebecause there is an IP address for that endpoint.And again we will see some scenarios around this.So these are all the important points that you should know about VPC endpoint with private link.And now let's go over all this one by one so that you really get it right.So first thing now imagine that there is a provider VPC and consumer VPC.So without endpoints what you will typically do you will connect this to VPCs or say VPC peering connectionor transit gateway.But in both the cases you should have non-overlapping ideas for those two VPCs.Otherwise you can't connect using the VPC peering connection or transit gateway.But VPC endpoint solves this problem.And this is possible because this endpoint is having the local IP from this VPC.So here let's take an example that in the provider VPC you have your EC2 instances having the servicesrunning.And these instances have similar IP address as the consumer VPC say 10.0.0.15 here.And now you create this VPC endpoint.And that's an interface endpoint.And it also gets one IP address.is now.This client can access this service using this local IP address.So it doesn't matter what IP addresses are there in the service provider VPC.So this is one of the biggest advantage of using the VPC endpoints with private link.Right.So I hope it is clear.Next let's talk about the availability zones.Now this is an architecture where we are showing two types of the endpoints that you access.One is AWS service endpoint and another is your own service hosted in another target VPC.Right.So for this you will create say VPC interface endpoints into your consumer VPC.And you can access these services on the right side.Now here we are not showing the availability zones, but in reality every VPC interface endpoint shouldbe created in multiple availability zones.So now if assume we have two subnets across two different availability zones inside this VPC and thereare two clients, and both these clients needs to access both this S3 as well as the So this VPC endpointservice hosted in another VPC.So ideally you should create VPC endpoint in these two subnets.And when you create the interface endpoint it will ask you in which all subnets you want this to behosted.So as you can see here now there are two endpoint Enis created across two subnets right now.In that case this client will access say S3 service using this particular Eni.And client two will access it through the local subnet Eni basically.And it will work similarly for this particular VPC endpoint service you will create another endpoint.And there also you will create that across two availability zones right.And then traffic will go like this.So this is what is recommended.But it's not necessary.Which means that you can also create the VPC endpoint only in one of the subnet.And in that case the client two can access the Eni into another subnet.Right.But the problem is, if you do that, there will be cross zone data transfer charges as well as it won'tbe highly available.If there is some outage in the AZ, then you cannot access the target services because that is not functional.So for both the reasons high availability as well as to not to have the inter AZ data transfer charge,you should have multiple Enis across availability zones.Right.So I hope it is clear.Now moving on.What about the security group.As I said because it's Eni it comes with the security group.And if you see this security group inbound rules are applied here because this client is sending requeststo the Eni.And that's where there should be an inbound rule.Say for example, if you are accessing S3 over 403, then 443 traffic should be allowed from the clientprivate IP address.So here we are putting the VPC, Cidr so that all the clients can access the VPC endpoint Eni.Right.And that's about it basically.So remember that interface endpoint any should have the inbound rules from the client okay.Next what about the IP protocol.So now it supports both IPv4 and IPv6 protocol.And as we already discussed client can have IPv4 or IPv6 or both addresses.And the service which is hosted behind network load balancer can also have both IPv4 or IPv6 addressor both.That's in a dual stack mode.And then it supports both IPv4 and IPv6 traffic.Further, with respect to the network protocol, it supports both TCP and UDP traffic.So that's about the network protocol.And the last important thing about the VPC interface endpoint is that it can be accessed from othernetworks.So what do I mean.So here is the consumer VPC right.And there are Enis created.And these are the VPC endpoint services or AWS services which you are accessing privately.But because this is Eni and it has IP address from the VPC, it can be accessed from say peered VPCor from the transit gateway, or even from the on premises network which is connected over VPN or DirectConnect.Right.So that's a fundamental difference between VPC gateway endpoint where you can't access it from othernetworks, but VPC interface endpoints or in general VPC endpoints powered by private link can be accessedfrom the remote network.Right?So I hope it is clear.And with that again just putting this forward, these are all the important things that you should knowabout the VPC endpoint and private link.Okay.So we'll stop here for this lecture.And now let's get into a little more advanced concept.That is how do you handle the DNS for VPC endpoint.And it is really really important to understand so that it is very clear how to configure the VPC endpointsin real life.Okay.With that we'll stop here.And let's go to the next lecture.Thank you.