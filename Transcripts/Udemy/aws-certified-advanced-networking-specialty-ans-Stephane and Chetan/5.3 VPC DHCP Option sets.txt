# 5.3 VPC DHCP Option sets

Hi.So in this lecture let's understand how Dhcp option sets work and what kind of attributes it has whichaffects your DNS configuration for the VPC.So as I said every VPC comes with a default Dhcp option set.And it provides basically a dynamic host configuration protocol attributes for the VPC resources.So through Dhcp option set you can set what should be the private domain name for your EC2 instances.Then what should be the domain name server which is like route 53 resolver by default.Then additional thing like network time protocol server that is NTP and net bias node type.But out of this I think we are interested mainly in domain name server.Because using the Dhcp option set, we can tell your VPC that do not look at the AWS provided DNS server,but rather you can look at some other DNS server for resolving the DNS queries.And we will do that exercise little later in this section.But all these configurations are managed at the VPC level using the Dhcp option sets.So as I said, when you launch the EC2 instances, this Dhcp option set parameters are automaticallyapplied for the EC2 instances as you launch them.So it has the default domain name server which points to Amazon provided DNS, which is nothing butthe route 53 DNS resolver that we talked about in the last lecture.And it also automatically configures the internal domain name for your EC2 machines.So the typical format is private IP of the EC2 instance, then the region name and then compute dotinternal.And we are going to again talk about this in further detail.But overall this is what Dhcp option set does.So I hope that is clear.Now just to show you this is one of the Dhcp option set which is a default one.And you can see here that it is created in Mumbai region.And that's where the domain name is AP South one dot compute dot internal.And the default domain name server is Amazon provided DNS.And this Dhcp option set is automatically associated with your VPC.And then as you launch EC2 instances they will have corresponding private DNS name something like this.So what it does basically it sets the host name for your EC2 instances.Now this host names could be private host names or the public host name.Now it really depends on the DNS attribute where your EC2 instance will also receive the public hostnameor not.But there are always this private host names.So it sets up the host name which is in a format like IP address, dot region, dot computing internal.And additionally the Dhcp will also set the Resolv.conf.If it's a Linux machine which tells your EC2 instance, what is the name server for DNS resolution?And here if you see the cursor, it has the name server 10.10.0.2, which is nothing but VPC base addressplus two.That means all the DNS queries from the EC2 instance are going to this name server.And we already talked about this DNS server in detail in the earlier lecture, so I hope that is clear.Now with that, let me take you to the AWS console and show you some of these configurations.So I just created one VPC called demo VPC.And if you see here it is associated with Dhcp option set which is the default one.And if you just go to this Dhcp option set then you will see the domain name as this.And the name server is Amazon provided DNS.And for this VPC I did not make any changes to the default Dhcp option set.What I did is I just went to the EC2 and I launched two instances this instance one and instance 2 in2 different subnets.Now instance one has a public IP because it's in a public subnet, and instance two is in a privatesubnet.Now if you look at the instance one, it has the public IP and it has a private IP.Now along with this private IP it also has a private DNS name which is assigned by this Dhcp.But if you notice there is no public DNS name for this EC2 instance.Now that's for the reason that there is a setting at the VPC level, which tells whether the EC2 instancewith the public IP will receive the public DNS or not.So if we go back to our VPC and just go here and look at this VPC attributes.So this first parameter enabled DNS resolution tells VPC that it can resolve the DNS using the Amazonprovided DNS server.And the second attribute which is enable DNS host names.It basically control whether the EC2 instance will receive the public IP or not.So let's just enable this and save it.But now, if we launch another instance in a public subnet with the public IP, it should also receivethe public DNS.So let's quickly do that.And while we wait for that instance we will also see some other things.So let me quickly launch this instance and let's make sure that it is in our VPC.And it is launched in a public subnet and it has a public IP.Rest of the settings I'm keeping mostly as default and launching this instance.Okay.So it will take some time for this instance to be launched.Okay.Now if we come back here and see that there is this instance one and it automatically got its publicIP.Now because we have enabled this setting at the VPC level that is enabled DNS hostname.And for the instance three it also got this public IP.Now we also have instance two which is in private subnet.And it just has a private IP.Now what we want to quickly do is from any of these instance one or instance three.Let's try to resolve the private DNS of instance two.And it should resolve to its corresponding private IP.So I'm just getting the public IP address.Or I can use public DNS as well to connect to this instance from my putty session.Okay.So I'm putting this DNS name I'm loading my key and just opening this session.And you can see I am into my session now.From here, let's try to ping to our EC2 instance two using its private DNS.Now for that, let's go here and let's pick this private DNS and just try that.And you can see that it is resolving to its private IP.So this is being resolved using the Amazon provided DNS server.And this is going through our Dhcp configurations.Now I also want to show you one more thing.That is what if you try to resolve the public DNS of the EC2 instance from, you know, within the EC2instance itself.So let's try that.So let's get this instance three public DNS which is this.And let's try to ping to that.Now you see it has resolved to its private IP.It hasn't resolved to its public IP.Now that's because we are making the DNS queries from within the VPC.However, if you do the same thing from your local machine.So let me try to go to my CMD and say Nslookup.Then you can see that it has resolved to its public IP because we are querying that from outside ofthe VPC.So that is how public DNS resolution happens.Now one more thing I want to show you is how that resolv.conf looks like.Right?So if you see Resolv.conf on this machine, the name server is 10.10.0.2, which is the VPC base addressplus two.So all the DNS queries are being sent to this particular DNS server.And this is the search domain for your EC2 instances.Which means if you don't provide the complete DNS name, it will automatically look at this as a suffixof your domain name.Now what do I mean by that?Instead of pinging to an private DNS which ends in this, you can just say, you know, the first partof the DNS, which is this.And if you do that, it still resolves because it is looking at this search domain.Now again, not important for your exam, but something that you should know.Right.So I hope this is clear.And you can definitely try all these scenarios on your own.So with that, let's go back to our presentation.So we saw all these settings, and we also saw how EC2 instance gets private DNS and the public DNS.Just there is one difference to know that if EC2 instance is in North Virginia region, that is US eastone, then the syntax is little different, but otherwise the syntax is IP dash private IPv4 address,dot region, dot compute, dot internal and similar domain name for the public address as well.And we already talked about domain names, the private and the public DNS.So I hope that is clear.Now, one thing to note here is that you cannot modify the existing Dhcp option set, but rather youwould have to create a new Dhcp option set and then associate that with your existing VPC.So something like this.Now in here we are changing our private domain name to finance dot internal.And once you do that the search domain for this VPC is set to finance dot internal instead of AP South1.4. internal.Which means now all these EC2 instances can reach to each other using the domain name, something likeIP dash, private IP address, dot finance, dot internal.But you have to wait for a few hours.Typically for this or otherwise, depending on your operating system, you can run some Dhcp commandto refresh your Dhcp lease.So that's another thing that you need to note is that Dhcp option set cannot be modified.You have to create a new one and then associate that with your VPC.Now here we talked about changing the domain name.But very well you can also change the name server.So instead of using Amazon provided DNS, you can point your DNS queries to your custom domain nameserver.And we are going to see that in the hands on exercise.So which means you can change all the parameters in Dhcp option set.And accordingly it will affect your DNS configuration.So I hope that is clear.Okay.So let's move on.And let's again talk about the VPC DNS attributes which we already saw in our exercise.So there are two attributes one enabled DNS support.And the default is true.And basically decides if the DNS resolution is supported for the VPC or not.So if it is true, then all the DNS queries are sent to AWS DNS server at the virtual IP or say baseaddress plus two.The another attribute is enabled DNS hostname, and it governs where your EC2 instance with the publicIP will receive the public DNS or not.So by default this attribute is false, but typically you should turn it on as well.So if true, it assigns the public hostname to the EC2 instance which has a public IP.Now with the combination of this true, there are a lot of things that may work or may not work.For example, if you are using Amazon Route 53 private hosted zone for managing the private DNS forthe VPC, then you should have both these attributes set to true.If any of these is false, then it won't resolve your DNS queries.So these are a few things which probably you should remember.And again if you want to try, you can try this in your AWS account where you set this parameter.And you can check that whether the EC2 instance with public IP has got the public DNS or not.We have already seen that in our exercise.And finally, a few things that you need to remember for your exam.So once you create a Dhcp option set you cannot modify the existing one, but rather you can createa new one and associate that with the VPC.At a given time.You can only have one DSP option set associated with the VPC.Now you can also have the VPC and say I don't want to have any DSP option set.But the problem is in that case, VPC doesn't know where to send the DNS queries and it cannot resolvethen any DNS, which is okay if your instance can communicate with each other directly using the privateIP addresses.Further, if you are changing your existing Dhcp option set and associating a new one with your VPC,it may take a few hours to take in effect.And it depends on your Dhcp lease.But if you want that this take into effect immediately, then you can probably run some Linux commandfor example Dhcp client and it refreshes your Dhcp lease.So this will really depend on your operating system.So this is for the Linux and for other operating system.There will be different commands.So you can refresh your Dhcp lease on demand.Otherwise it will happen over few hours.So these are all the important things that you should know for your exam.And with that we will see few things in action so that you understand it completely.So thanks for watching and stay tuned.