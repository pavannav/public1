# 9.17 VPC Peering vs VPC Endpoints

Okay, before we move to the summary, I thought let's talk about this topic quickly because many atimes a lot of learners ask this question, as in when to use VPC peering and when to use Privatelink,or when to use a transit gateway.So let's try to, you know, understand that and then we'll go to the summary.Now technically speaking you know what is transit gateway.What is VPC peering connection and what is privatelink.But it is always important to understand when to use what right.So you can consider VPC peering and transit gateway almost same right.They provide, you know, VPC to VPC connectivity.So here what we are talking about VPC peering uh also applies mostly to the transit gateway as wellright.So VPC peering is useful when there are many resources that should communicate between the peered VPC.So if you have say two VPCs and lot of hosts inside those VPCs needs to communicate with each other.So that means you are opening a wide connectivity between VPCs at layer three.Then you should use VPC peering connection, right?But if there is a one specific service that needs to be accessed from a lot of other say.Spoke VPCs or the client VPCs.Then you should use Privatelink, right?So you should use it when you want to allow access to only a single application hosted inside your VPCto other VPCs without peering that VPCs.So this works very well for the SaaS kind of applications, where SaaS provider is hosting their SaaSservice in their VPC.And now all the consumers in different VPC can access the same SaaS service.Now in that case, SaaS service provider does not need to pair their VPC to all the consumer VPCs.They can expose their services through VPC private link.Further, when there is overlapping Cidrs, you know that you cannot connect them to the transit gatewaysor VPC peering.But in that case, you can use VPC privatelink because overlapping SIDs do not matter to them.Further, there are also some limitations when it comes to the VPC peering connection.As in you can connect single VPC over, say 125 VPC peering connection only.But in case of private link, a single VPC endpoint service can be accessed from thousands of VPCs.So it is really ideal for that SaaS kind of environment, as I was saying.And finally, VPC peering enables the bidirectional traffic, which means that if two VPCs are peered,any host in any VPC can initiate the traffic to the other side of the network, right?But in case of private link, there is always service provider and service consumer.So only service consumer can initiate the traffic and then return.Traffic comes, but it is still unidirectional with respect to who can originate that traffic, whichmeans service provider cannot initiate the traffic to the service consumer.So in that sense it is one directional.So please also remember this difference.So these are some of the primary differences.And I hope this can help you take decision over selecting the private link over VPC peering connectionor the transit gateways.So with that we'll stop for this lecture.And now let's go to the summary section.Thank you.