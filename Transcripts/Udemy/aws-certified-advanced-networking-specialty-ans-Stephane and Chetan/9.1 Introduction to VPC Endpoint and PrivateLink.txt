# 9.1 Introduction to VPC Endpoint and PrivateLink

Hello and welcome to this new section, VPC endpoint and Private link.Now in my opinion, this is again one of the most important section because here we are going to talkall about the private connectivity from your VPC to other AWS services or the services which are hostedin VPC.And you will realize that over the time, your network architecture would have more and more of thisVPC endpoints because it really simplifies your AWS network architecture.Also, it reduces cost and also it improves the security of your architecture.And that's where it is one of the important topic.Okay.So the first thing what is VPC endpoint and what is private link.Right.So this is kind of familiar architecture to you.Where you have AWS region you have VPCs.And in that VPC you might have EC2 instance which hosts some application.And then you have AWS services like S3 or Kinesis SDKs.These are managed AWS services.Now if your application, which is hosted on EC2, needs to connect to this AWS services, typicallythe EC2 instance will make that call over the internet.So all that traffic goes over the internet.Even though both your EC2 instance and AWS services are part of the same AWS region, right?So this works, right?But because everything is in AWS, probably we can do a little better here.Now this is one use case where EC2 instance is public and then it can directly access the internet.But in most of the cases your application servers are in a private subnet, right.And in that case if you want to access AWS services, for example, if you want to upload some datato S3 or download data from S3, you will need Nat gateway in a public subnet.And then instance can connect to those AWS services.Right?So this works.But again, if you see this architecture, all the traffic is within AWS region, but still you arepaying for the Nat gateway charges and you might have to apply some security policies because trafficis going out from the internet.And sometimes you might also face the Nat gateway bandwidth challenges if you are downloading or uploadinghuge amount of data, and there are multiple EC2 instances in your VPC.Now to exactly address this problem, AWS had launched VPC endpoints.So VPC endpoints and AWS Privatelink.And there is a distinction between these two terms.So we'll understand about that.But basically both endpoints and privatelink work together to establish a private connection betweenyour VPC and AWS services.Or you could also host your own services inside a VPC.And you can access those services from this consumer VPC privately.You don't need, say, Internet Gateway or Nat gateway to access these services.So that's essentially is VPC endpoints powered by Privatelink.And if you set up this kind of connectivity then all the traffic between your consumer VPC to the providerVPC or provider services will be inside AWS.It doesn't go over the internet.So it's a private connection now right?So that's the benefit.So let's answer the question why VPC endpoint.So the VPC endpoint enables the private connections that we already talked about.It reduces your security risk because all the traffic remains now within the AWS region.And you don't need additional devices like internet gateway or Nat devices.So of course it's a less work for you and you're not exposing your network to the outside world.Third, it supports a lot of different AWS services as well as customer owned services, which are hostedinside VPC.And I will talk shortly.What do I mean by that?Right.So these are the reasons for which you should use VPC endpoints.Okay.With that now let's look at the different types of endpoints.And then we will start with one of the type of endpoint.So there are different types of endpoint.And the very first is VPC gateway endpoint.Now this is a different kind of endpoint from rest of the endpoints that we are going to talk about.And that's where we will cover VPC gateway endpoint in its own subsection.And rest of the endpoints we will cover separately.And this is for the reason that rest of the endpoints are powered by something called Privatelink,which basically establishes the private connectivity between VPC and the target network.And these endpoints, which are powered by VPC Privatelink, work little differently than how VPC gatewayendpoint works.And in the subsequent lecture, let's understand about those Differences.So as of now, if we talk about the VPC private link, then there are different types of the endpoint.The fundamental endpoint is VPC interface endpoint.And we will spend more time understanding about that.But lately AWS has also launched additional type of endpoints.So there is a gateway load balancer endpoint.There is VPC resource endpoint and VPC lattice service network endpoint.And further we are going to also talk about these endpoints in the respective section in this course.So in this section we will primarily focus on VPC gateway endpoint and VPC interface endpoint.Right.So with that now let's understand how these endpoints simplifies your network and what benefits theyprovide.So again VPC with a private subnet and instance with private IP.And assume that this instance needs to access AWS services endpoint something like this.Or it needs to access other VPC services which are hosted behind a network load balancer, and or itneeds to access some AWS services APIs, for example API gateways or SQS queue, all those things,right?Now, one thing that may confuse you here is you can see the S3 and DynamoDB both are here, as wellas in the bottom side of this architecture right now.That's for the reason that for S3 and DynamoDB specifically, you can access that from VPC using eitherVPC gateway endpoint or using VPC interface endpoint.And the use case is if you need to access these services from within the VPC, then use a gateway endpoint.But otherwise, if you want to enable the access to S3 from the VPC.But then this VPC can then further be accessed from some other remote networks, for example on premisesnetwork or peered VPC.Then you will use interface endpoint.And in this section we are going to talk in much deeper about all these scenarios, so just hold ontill we reach there.Okay, so let's get back to this topic again.So traditionally without VPC endpoints, as I said we need to have Nat gateway and internet gateway.And this traffic goes over the internet to these services.And that's where all those challenges about the cost and bandwidth comes into play.Right.But with VPC endpoints, as I said there are different types of the endpoint.So the first VPC endpoint type is a gateway endpoint for which you have to update your route table ofthe subnet, and then all the traffic to the S3 or DynamoDB only for these two services, traffic goesprivately from this instance to this AWS services.Right.So that's a gateway endpoint.Further, as I said there is interface endpoint.Now interface endpoint works a little differently where it provisions and Elastic Network interfaceinto your subnet.Right.And that's where it has an IP address.And now all the traffic from your this private EC2 instance can be routed through this Eni to this AWSservices or the customer services which are hosted inside the VPC.So traffic goes privately now to this target networks.So that's interface endpoint.And as you can now see once we have these endpoints we don't really need these Nat gateways and internetgateway into your VPC right.So I hope it is clear.And for the benefits as in gateway endpoint is completely free.And interface endpoint has small charge.But it is much lower than using Nat gateways.Right.So it is always beneficial.Okay.So let's quickly summarize what we understood in this lecture about VPC endpoints and privatelink.So VPC endpoints allows you to connect to resources in VPCs or AWS services using private network insteadof public network.They remove the need of internet gateways, Nat gateways to access AWS services, and all these deviceswhich powers these endpoints are managed by AWS and they are horizontally scaled, redundant and highlyavailable.So you don't need to worry about, say for example, Nat gateways high availability.You have to have multiple Nat gateways in different AZS, but you don't need to worry about that forthe endpoints.Now again, with respect to the types, there is a gateway endpoint which only enables the access toAmazon S3 and DynamoDB.But otherwise if you need access to other AWS services and customer managed or owned services whichare deployed inside the VPC, then you will use interface endpoint.And further, as I said, AWS launched many other different types of endpoints.So there is something called gateway load balancer endpoint VPC resource endpoint VPC lattice servicenetwork endpoint.And maybe in the future there will be additional endpoints as well.But our focus will be primarily on the gateway endpoint and the interface endpoint.Right.So I hope it is clear at the high level what is VPC endpoint and the private link and how they worktogether.And with that now let's get started with the VPC gateway endpoint.And this is a small topic as compared to the interface endpoint for the reason that it only allows youto connect to the Amazon S3 and DynamoDB to only these two services.Right?And the benefit is it's free.There is no charge for using gateway endpoint, and that's what you should always use it in my opinion.And we will talk more about that in the following lectures.But this is where we will start.And then we will spend most of the time in this section for the interface endpoints.Okay.With that, let's stop for this lecture.And now let's see more about VPC gateway endpoint.So thanks for watching and stay tuned.