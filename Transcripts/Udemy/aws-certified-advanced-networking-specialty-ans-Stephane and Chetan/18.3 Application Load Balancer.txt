# 18.3 Application Load Balancer

So now let's talk aboutyour application load balancerand what you need to know from an exam perspective.So, your ALB can have multiple listenersand each listener can have one or more rules.So in this example,Listener 1 one has one ruleand the rule could be to sendtraffic into a target group.And a target group may have multiple targets.It could be for example, EC2 Instances,and also it could have health checks.And here's an example of a health checkon your target group.Now, as I said,listeners can have multiple rulesand we'll see this in this lecture.And the idea is that, well,these rules can route to different target groupsand each target group may have multiple targets.Sometimes targets are even shared between target groups,all those, a common practice,and each target group again has health checks.So the ALB will operate at the Layer 7,which is for HTTP and HTTPS, WebSockets,HTTP 2 and the gRPC protocols.And this will allow you to do load balancingacross multiple HTTP based applications or target groups.The load balancing can also happenon a single target on multiple ports.So for example, if you have an EC2 Instancethat has 10 containers running on it,then thanks to a dynamic port mapping feature,the ALB is able to send trafficinto each of these containers on the right parts.There's also support for sending directlyfrom the ALB, a custom HTTP responsein case you don't even want the trafficto reach your EC2 or your target group as a whole.And it supports redirects. For example,if you want it to automatically secureyour HTTP traffic into HTTPS,you can support redirecting any requeststhat is HTTP based into an HTTPS request.Okay, so now let's talk about your target groups.So it could be made of multiple things.It could be made of EC2 Instances, and in which case,these instances can live in an auto scaling group.So this is for HTTP traffic.Then we have ECS task and the meansthat the tasks are managed by ECS itself.And we have HTTP traffic again, Okay.And this is cool because thenthis will leverage directlywith dynamic port mapping feature of the ALB.And then we also have Lambda functions.So in this case, any HTTP trafficis going to be converted into a JSON eventthat will be passed on to your lambda functions.We also have IP addresses and in which case,these IP addresses must be privates.So there could be EC2 instancesin peered VPC or on-premise serversaccessed over a private connectivity,So either direct connect or VPN connection.It supports weighted target groups,that means that you can send trafficto multiple target groups at a time.We'll see this in this lecture as well.This will allow you for example,to test multiple versions of your applicationor to perform blue-green deployments.The types of health checks can be HTTP or HTTPS,WebSocket is not yet supported.And each subnet must have a minimum,and this is very very important,a minimum of /27 size for the ciderand at least eight free IP addresses,and across all subnets,a maximum of 100 IP addresses will be used per, per ALB.So this is very important because you needto remember that the subnets must have a minimum size,must have a minimum of free IP addresses.And this is, and you need to remember again,the number of maximum IP addresses,because these are common troubleshooting thingsthat the exam may test you on.So remember the last two absolutely.Okay. Now let's talk about target groups.So we can route two target groups on multiple aspects.We can route based on the path of the URL,for example, / reserves or / posts,we can route based on the host nameof the incoming request, for example,one.com and other.example.comor *.example.com and then example.com just on its own.We can route based on Query Strings parameters,HTTP Headers, Source IP address.So here's an example for Query String parameters.So id=123 and order=false.And as I mentioned before,well the ALB should be a great fitregarding your Amazon ECS micro services,because it's going to leverage the dynamicport feature mapping from the ALB.That would not be possible to use if you wereto use a CLB because CLB does not havea port mapping feature.Okay, so now let's talk about Path-based Routing.So this is very simple.Here's an ALB that has two target groups.The first one is a target group from EC2 Instances.And then we have a second one,which is maybe a target group made of Lambda functions.And so what we want to do is that we wantto route all the users that will goto the Route:/user into the user's target groupand route all the users on the search target groupinto our Lambda target group.So we can define path based routing using this method.We can also use for example,Query String or Parameters Routing.So here's an example.We have a target group on AWS made of EC2 Instances,and we have a second target group.This one is made of On-Premise servers.So this is going to use a private IP routing.And here we're saying that anythingthat has the query string and platform=Mobileshould be sent to the first target group.And the Query string and platform=Desktopshould be sent to the second target group.So now let's have a look at some ALB features.So first of all, the ALB is able to authenticateyour users before routing the requestto the registered targets in your target group.So that means that for example, you can havean integration between your ALB and Amazon Cognitoto make sure that your users are indeed logged inusing Cognito before the request is passed onto your EC2 Instance and effectivelywhat this is doing is that you're movingthe authentication logic out of your application,into the ALB, and you're preventing your EC2 Instancesfrom having to deal with that complexity.It can also be a Microsoft Active Directory or OIDC,SAML, LDAP, OpenID.It can be social identity providers such as Amazon,Facebook, and Google.So you have lots of different waysof authenticating users, but all of thoseare integrated with your ALB.You can also load SSl or TLSor SSL certificates onto your ALB.And so that you need to have multiple listenersand you have an SNI, server name indicationfeature on the application button.So what this means that you can terminateyour connections directly on your ALBand using ACM, the ACM integration,and then send un-encrypted traffic, for example,to your EC2 Instances.So this is a very simple table.We have two types of Listener, either HTTP or HTTPS.If you do enable HTTPS as a listener on your ALB,you will need obviously to use ACM.And then internally, again,the same as before for the CLB,you can send HTTP or HTTPS traffic to your EC2 Instance,but in which case, if you do sendHTTPS traffic to your EC2 Instances,you need to make sure they havetheir own certificates loaded on them.Okay, now let's talk about the Listener Rules.So a listener can have multiple rules,one, two, and three, and then the default rule.And so each rule will have a specific action.For example, Rule 1,based on some criteria, for example,for path or for query string parametersis saying, hey, redirect to Target Group 1,Rule 2 is going to sayredirect to Target Group 2, and then, and so on.And then the default rule is saying,hey, no matter what happens,if none of the rules are matched,please redirect to Target Group 3.So they're processing orders.So the last one is going to be the default ruleand it supports multiple actions,for example, forward, redirect or fixed-response.So forward is to send the traffic into a target group.Redirect is to, for example,redirect the traffic from HTTP to HTTPSand fixed response is to send, for example,a fixed 404 response as your last role,if in case, well, this was a bad request.So the rules conditions are going to behost-headers, http-request-method, path-pattern,source-ip, http-header and query-string.So these are all the conditions you can haveand combine to make sure that a ruleis matched specifically for a specific target group.Okay, now let's talk about target group weighting.So this is very helpfulfor blue-green deployments and so on.So we can specify multiple target groupsto send traffic to, on a single rule.The idea is that you can test multiple versionsof your application for a ruleand then perform blue/green deployments.This allows you to control how much trafficis going to go to a specific target group.And then you can test whether or notthe target group is working effectively.So in this example, we have the usersconnecting to an ALB. The ALB has one rule,and this rule is saying that 80% of the trafficis supposed to go to the Target Group 1,which is the blue target group.And two, as a weight, So 20% of the trafficis supposed to go to my Target Group 2, so effectivelywe're doing is that we're sending 80% and 20%.And imagine that the Target Group 2is your new application version.Then we are getting a gradual shiftof weights onto it, and we can test whether or notwell the Target Group 2 is functioning as expected,and this is done from within a single rule.So this allows you to do blue-green deploymentsdirectly on your ALB.So that's it for your high level overview of ALB.I hope you liked it.And I will see you in the next lecture.