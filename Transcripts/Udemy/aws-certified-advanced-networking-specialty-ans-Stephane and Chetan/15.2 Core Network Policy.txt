# 15.2 Core Network Policy

Hi.So this is one of the most important lecture in this section, because everything that you do in cloudWan is defined by the core network policy.Now in the earlier lecture we just saw the components of the cloud Wan.But let's now understand what are the core elements of core network policy.And then how do you define your entire network using a simple policy document.So let's get started.So core network policy looks something like this.On the right side.This is just bare minimum policy I would say which just has few components.But of course as your network complexity will increase, this Json document will look different.So there are some high level elements of the core network policy which includes network configurations.And then we are going to dive deeper into this shortly.But essentially there is a generic network configuration for the entire cloud Wan.And then you configure segment specific settings in the segment section.And then you have attachment policy for all the attachment that you will be creating in your core network.Now let's try to understand what all options you have or parameters that you can provide while you configureall these settings.So very first thing is network configuration.So on the right side again you can see one of the network configuration that you have to provide whileyou create your core network policy.So here very first thing that you will provide is the ASN ranges.Now these are the autonomous system numbers that will be used for the core network edges that you willprovision into different AWS regions.And also it will be used for any kind of network connectivity.As you connect and set up the BGP peering connection between the other end of the network.So you can provide either 16 bit or 32 bit ASN ranges, as you can see here.Next is edge locations.And this is the most important setting I would say because accordingly the core network will set upthe core network edge that is C and E into every region.Now, if your company operates in, say, three regions, then you will only select those three edgelocations.And this core network edges are just like a transit gateways.But if I have to define that, I would say it's a managed transit gateway, in which case you don'tget direct access to the APIs for that.So you don't manage that directly.It's completely managed by AWS.So as many regions, you want those many edge locations you would have to select.Next you can define the inside Cidr blocks and these are required if you are connecting the connectattachment to the core network edge.So basically this inside cider blocks are used to set up the GRE or tunnel is tunnels for the transitgateway connect attachments.So either you can define the complete range at the core network level, or you can have this rangesdefined per AWS region level.And optionally you can also enable or disable the equal cost Multi-path support for the VPN attachment.So these are a few important parameters that you should know that you can provide for your core network.Okay, so after you define the basic parameters for the core network, the next most important thingin your configuration is defining the segments and the segment policy.So as many segment you want to create, of course you will define those many blocks in your policy.So for each segment you have to provide the segment name.And then you can optionally provide in which all AWS region or edge locations you want to enable thissegment.Now this is interesting because while creating the core network, you already provided a list of edgelocations, right?So if you don't provide this at the segment level, then segment will be available in all the regions.But maybe there is a situation where you want, say, a production segment available only in coupleof regions, because maybe that's where only you deploy your production workloads.But for development, you might be using all your regions which are defined in the core network.So you can this way just enable the segment for a particular set of regions only.Again, this is an optional parameter.Now another feature is isolate attachments.So the default behavior is that if you have a segment and you attach either VPC or VPN or connect attachments,all these attachments will be able to communicate with each other.So they automatically exchange the routing information.But many a times you want that on that segment.I don't want my attachment to even talk to each other.In that case, you can set this parameter to true and then attachment cannot communicate with each other.However, if you want to override this behavior, you can also manually create the static routes.And then of course you can enable that kind of connectivity.And we will talk again about the static routes.When we talk about the segment sharing.Next you can enable the required attachment acceptance attribute to true or false.Default is true.This means that if anyone creates the you know, attachment and try to attach to your segment, youcan accept or reject that request.And this is a good feature in the sense that if there are multiple teams who might want to, you know,create an attachment to your segment, they will create that attachment and you as a network administratorwill allow or deny that request.So in that case, you can have better governance over your network policy.Next is Allow or deny.List of the segments.Now for this, it is important to understand that whenever you create multiple segments on the corenetwork, this segments are isolated, so all the attachments on those particular segments can communicatewith each other, but the attachment across the segments cannot.And that's the basic fundamental of isolating the network using the segments.Right.But many a times you want to share your segments so that attachment across the segments can communicatewith each other.For example, development segment can communicate with the shared services segment.And in that case you will first share the segments.But you also want to control that.I shouldn't be sharing my segment with every other segment out there on the core network, and that'swhere this allow or deny list will help you control that behavior.So in the allow list, you can mention that I am okay to share my route with this particular segment,which are there in the allow list.Or you can say, I don't want to share my routes with all these segments which are there in the deniallist.So these are mutually exclusive list.That means you cannot provide both allow or deny lists at the same time.But just remember that allow and denialist only makes sense if you are going to share the segments.If you are not going to share the segments, then they won't have any impact.And now let's talk about how do you share the segments.So for that you use segment actions.And in that segment action you can define that.I am going to share this particular segment with list of other segments.And at that time the routing information between the segment will be enabled to be exchanged.So your attachment from now both sides of the segment can communicate with each other.Now there are few things that again you need to understand.For example, if there is segment A that you are defining and you are sharing segment A with segmentB, C, and D through the segment action, then segment A will exchange route with B, C, and D, andalso B, C and D will exchange the route with segment A, but this doesn't mean that B, C, and D willcommunicate with each other.And if you want to enable the sharing between B and C, then you have to explicitly do that.When you define the policy for segment B and C kind of right.So I hope that is clear.Now many a times you don't want to share the segment, but you still want some of the attachment fromthese segments to communicate with each other.And that's where you can create the static routes.So this is kind of overriding, you know, the default segment sharing behavior and creating the routesto enable, you know, that specific kind of connectivity.So I hope that is clear.So these are all important parameters you need to consider when you create the segments.And we are going to have a short walkthrough of how to create the segments and how to set these parameters.So these things will be much clear when you actually see that in action.Right.With that, let's move forward.After the segment you need to understand what are the attachments.So attachment in general are of the four types which are supported as of today VPC attachment VPN attachmentconnect and connect peer.So this is very similar to the Transit Gateway Connect where you have your SD-Wan appliance hosted inthe VPC.And then you associate that VPC with the core network using the connect attachment.Once that attachment is there, you then create a connect peer, which means you actually create thetunnel between the core network and your SD-Wan appliance.Now, with respect to the transit gateway, it supports only the GRE tunnel, but AWS cloud Wan alsosupports the tunnel less protocol, which is much more performant than the GRE.And finally, in order for connect attachment to work, you need to have the BGP routing between thetwo ends.And for high availability, core network will have two BGP sessions.So this is a third kind of attachment.And there is fourth kind of attachment that you can have for your core networks which is transit gatewayroute table attachment.Now this is not a transit gateway attachment directly.But this is like after you peer your transit gateway with the core network, then you can create a routetable attachment to the particular network segment.And we will talk more about this in the following lecture where I will show you how do you connect yourtransit gateway to the core network.But just remember that these are the four types of the attachment that you can have with the core network.Right now.Let's quickly look at the attachment policies.And these attachment policies allows you to have some kind of automation.So you can have kind of rules.And these rules are numbered.So lower the number takes a higher precedence.And then as soon as the first rule matches it stops the execution and scanning the next rule.But what are these rules for.So whenever you create any attachment it will see if there is a attachment policies which matches tothat rule.If it matches, it will attach that attachment to the segment which is defined as a part of the rule.And in the next lecture, we are going to perform a lab where I will show you how this exactly works.And then you have the required acceptance parameter similar to the segment one.And this takes only in the effect if you have disabled this at the segment level.Otherwise it just inherits the value from the segment level parameter.Further, as I said, rules have the logical conditions and you could have different conditions.For example, this attachment can only be connected to the segment if region is x, or this is for aparticular VPC id, or this attachment has a particular tag key or tag value, or it is coming fromparticular AWS account.So all these conditions and or logic can be implemented to have the automatic connectivity between yourattachment and your segments.And when you do that, basically you can define that the method of associating your attachment to thesegment is a constant or it's a tag based.So constant means that whenever you write the rule in that, you define that.I'm writing this rule for a particular segment, for example development segment.So if all the conditions matches for that attachment, it will be attached to the development segment.But then for every segment you would have to write those many rules.But if you want to write a generic rule then you can define that the segment should be the value ofparticular tag key.And the tag key could be say any moment.So whenever you create the attachment you will define the tag with say environment equal to development.Then that attachment will be automatically associated with the development segment.But now while creating another attachment, if you say tag key equal to environment and tag value isproduction, then that attachment will be attached to the production segment.So just using the same rule you are able to attach different attachment to different segments.So that's the purpose.And again we will talk about this behavior when we do our exercise.So these are all the important parameters when you create the core network policy.And if you have all these policy elements well defined, then of course you can use this policy to deployyour network.So let's now look at what's the process of, you know, deploying your network.So very first thing you will create the policy document.Either you can use the UI from network Manager or you can write a Json document and use it for yourpolicy.Once you generate this policy, AWS will validate whether the policy is correct with respect to itssyntax and all the parameters that you provide, and then it will be in pending generation status.After AWS does, basic validation policy will be ready for execution.But if there are any errors, then its status will change to failed generation.Now, when the policy gets into the ready to execute state, it also provides you change set, whichmeans from version 1 to 2, what all changes you are making to the policy and how it will affect yournetwork.So this is a good time where network administrators should carefully inspect what are the changes thispolicy is going to make, and then approve that change and apply the change into live environment.So you will apply that change and the policy will get executed.So the status will be execution succeed or failed.Now this is how typically your policy change flow looks like.And every time you change the policy its version is pumped up.So if first version is v1 then there will be new version created.And after this version is deployed, that version is tagged as the latest version.And this also means that for any reason, if you think that the changes that you have just applied arenot good enough, then you can revert back to the previous version.So AWS cloud Wan in true sense, I will say, is the software defined network where you can completelymanage and deploy your global networks using simple network policy.Right.So I hope this is clear now for your exam.I think it's good to understand all these parameters that we talked about.And in the next lecture we are actually going to implement the core network.So I hope whatever we just talked, you will see that in action and you will be able to relate it better.So that's it for this lecture.Thanks for watching and stay tuned.