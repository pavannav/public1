# 3.4 VPC Building blocks - core components

Hi.So in this lecture let's understand the basic building blocks of the VPC.You can also call them as core components of the VPC.And then in the next section we are going to dive deep into the advanced concepts of the VPC.So everything starts with AWS region.So whenever you create a VPC first you have to select the region.And every region comes with three or more availability zones depending on which region you are choosing.Now here for simplicity, I'm just showing two AZS or availability zones.Now you create a VPC and VPC sits into your AWS region.Now whenever you create a VPC, the first thing that you have to do is to assign the IPv4 or IPv6 addressrange.And that's the IP address range for your VPC.Now that address you assign in the form of Cidr notation also called as classless Inter-domain routing.So for this VPC I am putting IPv4 address range, for example ten .0.0.0 slash 16, which gives me around65,000 private IPv4 addresses for this VPC.And in the next lecture, we are going to talk about how to calculate the Sidr and how to know how manyIP addresses you can have inside the VPC.Right.So once you have the VPC, the next thing you create is the subnet.So you are dividing now a VPC address range into a smaller networks in the form of subnets.And as you create the subnets you have to associate them with a particular availability zone.So the scope of the subnet is at A-Z level.And that's where when you launch any EC2 instance or any other resources which are AZ bound, you haveto launch inside a particular subnet.For example, you are launching the EC2 machine either in subnet A or in a subnet B, so subnet is smalladdress range from within the VPC.And again you assign the Cidr range for your subnets.Now once you create the subnets of course you need to have the routing.That means how the traffic flows across these subnets.So whenever you create a VPC.So AWS will automatically create a main route table which is applied to all these subnets.And we are going to see what this main route table looks like and how to change the route table entries.But many a times you would want that every subnet has their own routing mechanism.For example some subnet has the workload which are exposed to the internet.Some subnets host the database machines which need not be exposed to the internet.So you might have more control over your routing.And that's where you can also have these route tables at the subnet level.Again we are going to see examples of that.But Essentially, route table defines the routing configuration for your VPC and your subnet.So as of now we are talking about the VPC which is like a private address space for you.Now imagine you are sitting at home and you launched an EC2 instance in this VPC.Can you reach to that instance so far?No, because this VPC is not connected to the internet.So another important thing for VPC is the internet gateway which connects your VPC to the internet.So that is called internet gateway.And it enables that connectivity to the internet.So we are going to also talk about the role of internet gateway and how to configure routing in thesubnet to allow this traffic to flow to this EC2 instance from the internet.Right.So this is all about the routing or network configurations.So now let's also talk about the security of the network.So for the VPC.AWS provides two basic firewalls, that is security group and network ACL.So security Group works at the EC2 instance level, which means any traffic that goes in or out of theEC2, it can be controlled using the security group rules.So Security Groups works at the EC2 instance level.And the other firewall that is network access control list works at the subnet level, which means therules in the network ACL will be applied for all the resources which you launch inside this subnets.And there are few other differences that you need to know between the security groups and network ACL.And we are going to cover that in detail in the following lectures.But at high level, these are the two basic firewalls that you can have inside your VPC.So I hope that is clear.Now, one more thing, which I will categorize as a part of the core component of the VPC, which isthe DNS.How does this EC2 machine resolve the DNS as it reaches outside this VPC or within the VPC?So you know that DNS is the most important part because it resolves the domain name to the IP addresses.So how AWS resolves the DNS queries from this EC2 machines.And that is done using something called AWS DNS server or also called as route 53 resolver.Now, though, it's a core and basic components of the VPC, we are going to have a separate sectioncovering the AWS DNS server because it's very important for your exam.So that's about the building blocks of the VPC.I hope this is clear.And now we are going to dive deep into individual components.So let's look at those now.Thank you.