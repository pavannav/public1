# 5.8 Exam Essentials

Hi.So let's try to recap what we learned in this section.And this is important to remember for your exam.And we already talked about this.But just trying to summarize this in this lecture.So very first thing VPC has a default DNS server which is identified as Amazon provided DNS in the Dhcpoption set.This server is also called as route 53 DNS resolver.Now this DNS server runs at VPC base address plus two, and it is also available on the virtual IP 1639.254.169.253, and this virtual IP can only be accessed from within the VPC.Now, Amazon provided DNS server resolves the DNS from the route 53 private hosted zone VPC internalDNS, which are assigned by the AWS, and then any public DNS.The DNS configurations for the VPC can be changed using Dhcp options sets, and this option sets cannotbe edited.If you want to change any setting, you have to create a new Dhcp option set and then associate thatwith the VPC, and at one time you can only have a single Dhcp option set associated with the VPC.Further, there are the DNS attributes.That is, enable DNS support and enable DNS host name.The enable DNS supports enable the VPC to send the DNS queries to Amazon provided DNS server and withenable DNS host name.The EC2 instances with the public IP also receives the public DNS name, and if you are using AmazonRoute 53 Private Hosted zone to manage the private DNS, then you should have both these attributesset to true.And we are also going to talk about these attributes when we talk about the VPC endpoints and many suchfeatures, which connects your VPC to the other AWS services.And finally, for hybrid DNS resolution, you should use Amazon Route 53 resolver endpoints.And there are two types of the endpoints.The inbound and outbound.Of course inbound are for receiving the queries from your outside network to VPC and then outbound forVPC to the outside network.So in hybrid scenarios you should use route 53 resolver endpoints.Now with that we will stop for this section.I hope you learned about the VPC, DNS, how it works and what are the important features.And as I said, we will revisit this in the route 53 section.So that's it for this section.Thanks for watching and stay tuned.