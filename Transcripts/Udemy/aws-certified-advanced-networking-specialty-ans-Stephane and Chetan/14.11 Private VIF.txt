# 14.11 Private VIF

Okay. Let's now, let's turn the private VIF.And, in case of private VIF, as you know,it is used to access the private resources inside the VPC,and, that's where, let's see the other properties,with respect to the same.So, it enables the network connectivity,from your data center,and on-premises network to the VPC,using private IPs for resources, like EC2, RDS, Redshift.All these are part of your VPC,and then all these resources,you can access using their private IP addresses.You must attach your VPC to the virtual private gateway,and, by creating the private VIF,you should then associate that VGW to the private VIF.Now, understand, that private VIFand virtual private gateway,must be in the same AWS Region.And that's one distinction,between the public and private VIF,that we are talking about.You can only access the VPCs in the same Region,if you're attaching it,to the virtual private gateway, directly.Now, lately, AWS has also launched Direct Connect gateways,which allows you to access,or connect VPCs, across the Region,but then we'll park that as of now,and let's consider the private VIF connection,with virtual private gateway.Now, when you have set up your BGP session,customer router will also receive all the prefixes,of your VPCs, right?Similarly, you can also advertise your side,of the prefixes, or the private VIF.Now, we understood, that in case of public VIF,you can maximum, advertise 1000 prefixes,but, in case of private VIF,you can only advertise 100 prefixes to the AWS.So make sure you don't advertise more than that,otherwise BGP session will go down.Now, the routes that have been advertised,from your end or the customer end,these routes can be automatically propagated,into the route table subnets,if, of course, you have enabled the route propagation,enabled at the Subnet level.So, in that case,all the routes will automatically get into your subnets,and any traffic which falls under that IP address ranges,will be routed through the private VIF,if the traffic is going from AWS,to your side of the network.Now, in order to advertise more than 100 prefixes,you can do route summarization,similar to what we have explained during the public VIF.So, instead of having small prefix ranges,you can just advertise one large prefix range,which, ultimately, covers all those small address ranges.And, note that the propagated routes will take precedence,over the default route, into your subnets.That means, if you have a route for the internet,0.0.0.0,is the destination and the target is internet gateway.And, if there is,a match, happens,with your propagated route, for the target,which resides in the customer network site,then instead of internet gateway,it will choose the route through the private VIF,because it's a propagated route,which your subnet learnt,over the BGP session from the customer network site.And now, let's see, what do I mean by that?So, this is the architecture,where you have the Direct Connect connection,and then you create a virtual private gateway,attach it to your VPC,and then you have a private VIF,attached to the virtual private gateway.Now, we have considered that CIDR,for your VPC,is 10.10.0.0/16,and for on-premises,it's 192.168.0.0/24,for this particular network.And then there could be other network,inside this data center as well.With respect to that, we have now two subnets here,and the two EC2 machines inside the subnet,and the subnet would also have the route tables,and for internet connectivity, we have internet gateway.Now, you know that when you create a subnet,there is always a local route, right?For the local VPC CIDR. And if it is a public subnet,you will also add a route of internet destination,that is 0.0.0/0.And the traffic will roll through the internet gateway.And these are both the static type of the routes.Now, when you create a private VIF,and then you have a virtual private gateway,you can modify your subnet,and say that all the traffic,which goes to the 192.168.0.0/24,has to be going through the virtual private gateway, right?Now, either you do it statically, into your route table,or, as you have attached the virtual private gateway,to your VPC, you can just enable the route propagation,for this virtual private gateway.And then all the routes,that it learns from on-premises network,will be automatically propagated to your subnet.Now, for example, I have shown that,if it learned about three such prefixes,then, automatically,it says the traffic, for this destination,should be going through the virtual private gateway,and ultimately, through private VIF,to the corporate data center.And that's where, suppose now,you're EC2 is trying to reach to this machine,which has an IP address of 192.168.0/10.Now, you know that this address,does not fall into the local address,so, this part will not be taken,but then there is an internet gateway,which matches all the IP addresses.So, technically it matches this IP address,but, because there is a propagated route,through virtual private gateway,for the same IP address range,under which this IP address falls, it will take this part.So, the traffic from your subnet,to this particular IP address,will take the path through virtual private gateway,and through Direct Connect connection,the traffic will flow.So, this is what I mean,when I say the propagated route will take precedence,over the internet gateway routes.So, just remember that.And finally,the private VIF supports MTU of 1500, by default,but then you can enable it,to support,the jumbo frame,of size 9001.And, this is applicable only for the propagated routes.So, essentially, that is what the private VIF is all about.And, just,to make it,more clear, what you can access inside VPC,and what you cannot,let's look at that.So, when you have this connection,you have Direct Connect location,you have Direct Connect router, here,and from there you create a private VIF,and you attach to the virtual private gateway.Now, inside your VPC,you will be having multiple networking components,as well as your resources.There will be databases, as well as EC2 machine.There could be VPC endpoint, gateway endpoint,as well as interface endpoint.And you know that VPC base address, plus twowill run the local DNS server,which resolves all the DNS queries,using the Route 53.Now, when this is the case,let's understand what you can access,from your on-premises network and what you cannot.So, while you can access all EC2 machines,and databases, inside your VPC,you can't really access this DNS service endpoint.That means you can't resolve the DNS,the private DNS for your VPC,from your corporate data center.Now, in order to do that,there is a different mechanism.You can use a Route 53 resolver,but that will be covered in the different section.And the second component you can't access,is VPC gateway endpoints.Now, if you remember from our earlier discussion,VPC provides two types of the endpoints,interface endpoints, and the gateway endpoints.In case of gateway endpoints, you can access AWS services,within the same Region, like S3 and DynamoDB.And you have to modify your subnet route tableto route that traffic through the gateway endpoint.And there is another type of the endpoint,that is, interface endpoint,where it creates an ENI into your VPC,and then you can access other AWS services endpoints,which are supported by interface endpoint.Because, because this is,again, not the transitive routing,you can't access the gateway endpoint via your private VIF.However, because there is ENI created into your VPC,you can still access the interface endpoint targets,but not the gateway endpoint targets.So, remember these two things.Just to show you, here.So you can, access EC2 machines, databases.You can access interface endpoint,but then you can't access DNS,as well as gateway endpoints.So, overall, that's what is private VIFand understand these restrictions.Now, let's try to create a private VIF,or at least let's see what parameters it takes,while creating the private VIF.So, I'm into my Direct Connect console again.And, as you know, I have one connection,and I don't have any virtual interfaces.So, this time, let's try to create virtual interfaces,as a private interface.First thing, you need to give the interface names.So, you can say, MyPrivateInterface,and then you have to select the connection.Again, if it's a hosted VIF,then you would have to select another AWS account,otherwise, it's your own AWS account.Now, here is the gateway type.Now, this option wasn't there for public VIF,if you remember,but private VIF can be attached,to the virtual private gateway.And this virtual private gateway,needs to be attached to the VPC.And then there is a new option,to connect your private VIF to the Direct Connect gateway.Now, we will see that in the next lecture,where we discuss, in detail,about the Direct Connect gateway.So these are the two options you get.Now, if you are selecting the virtual private gateway,of course you need to have it created,but right now it is not there.And then, similar to public VIF, you to give the VLAN IDs,and the BGP ASN for the customer router.Now this will be a private BGP ASN,and, additionally,you can specify the BGP settings,like the BGP peer IPs will be IPV4 or IPV6.And, in this case, you need to provide the BGP,address range, in the range of,169.254,.0.0/16.So, you can provide the range between, this,uh, CIDR block,for both your side of the router IPs,and Amazon's side of the router IPs.And then, optionally, you can provide,the BGP authentication key.If you do not provide it, AWS will generate automatically.And remember that all BGP peer IPs,are also optional in this case,but in case of public VIF,either you have to provide the public IP range,for your BGP peers,and if you do not have it,you can ask AWS and then you would have to provide that.In case of private VIF, these are optional.If you don't provide it, AWS will generate it for you.And then finally, you can see here,by default, private VIF will supportthe MTU of 1500 byte,but you can enable the jumbo frame on this connection.And then for propagated routes,9001 byte of the MTU will be used.So, essentially, those are the parameters for private VIF.I'm not going to create it,because I don't have the other settings.Those are required to do this,but, in general, remember all these parameters.So the key takeaways from this session.That private VIF enables the connectivity,between on-premise network and AWS VPC.And that's where you can access the VPC resources,or the private IPs.And thing to remember,is that you can announce maximum 100 prefixes from your end.Do not advertise more than 100 prefixes.Now, with that, in the next lecture,we will get into the details of Direct Connect gatewaysand the transit gateways,but as of now, that's it about the private VIF.So, thanks for watching.