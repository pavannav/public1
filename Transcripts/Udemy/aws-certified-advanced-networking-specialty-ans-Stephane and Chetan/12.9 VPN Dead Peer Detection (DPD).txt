# 12.9 VPN Dead Peer Detection (DPD)

Hello, and welcome to this lecture,Dead Peer Detection, that is DPD.Now, DPD is a mechanismthrough which you can detect the problemin your VPN connectionwhether the other end is working or not.And this is important because many times probablyyou want to fail-over to another channelor another network for your VPN connectionor for sometimes, maybe you want toclose that connection due to security reasons.So DPD helps you to address that problem.So, you know this architecturewe are talking about the same setup all the time.But then if you talk about the DPD,it's a method to detect livelinessof your IPSec VPN connection.Now, what that means is,on AWS side of the VPN connectionand on CGW side of the VPN connectionif you configure the DPD,that means it's optional settings.And this is decided duringthe phase one of IPSec negotiation.So VPN peers can decide thatwhether they want to use DPD or not.Now if DPD is enabledthen what AWS side of the virtual private gateway does isit sends something called (R-U-THERE) DPD messageto the customer gateway every 10 seconds.And then it would wait for an acknowledgementR-U-THERE-ACK.And it would check if there is no responsefrom the other end for three consecutive time,that means in 30 seconds,that's the default timeout.Essentially it will terminate your VPN tunnel.It works at tunnel level.Now, how it works?.As I said,VGW will send R-U-THERE messages continuouslyand your customer gateway should response to those messages.Now, if customer gatewayfor some reason is not responding to your DPD packetsfor three consecutive times.The VGW will terminate the tunnelthat's what the DPD is.Now, few more things you need to understandabout the DPD is that.By default, when DPD occurs,your gateway would delete the security associations,which it creates during the IPSec handshakeand during that processthe alternate IPSec tunnel can be used.If you have configured both the tunnelin Active-Active mode.Now, default DPD timeout is 30 seconds,but then you can set it higher than the 30 seconds.DPD uses UDP port 500 and in case of NAT traversal,port 4500 to send this DPD packages,so make sure that these ports are enabled.Now, when DPD occurs,that means when the DPD timeout occurs,there are three actions that you can seton AWS side of the VPN connection.You can say that, when DPD occurs,maybe you clear the connection.That means in the IPSec session,stop the tunnel and clear the routes.Or you can say restart the IPSec IKE phase one,or take no action. So this is up to you,how you configure it, but the default actionis to clear everything,that means you end your IPSec tunnel.Customer, which uses the Dynamic Routingmust support the DPD at their router.That means if your router does not support DPDand you are using Dynamic Routing using BGP,then that router won't be ableto make the connection with AWS or VGW.DPD is about checking the health of your VPN tunnel.But for inactivity of your tunnel,in the sense that if you're not sending any dataacross your tunnel, even though the tunnels are up,everything is working,but there is no server on either sidewhich is continuously or which is sending the data throughthat tunnel, now this is called idle connection.Now, there is a setting called default idle timeouton customer gateway side of it.And if you set that timeout time,there is no packet flowing during that timeout periodthen also your VPN tunnel is terminated.Now,if you're using a Cisco router or Juniper,there are various values you can set.Like maybe a 30 minutes or one hour or two hour.And when this timeout occurs, in that case,you have to re-establish the VPN connection.Now, one workaround for this isto have some hosts on either side,which is continuously sending some ping requestto other hosts in at other end of the VPNthat makes sure there is always some data flowingthrough the VPN tunnel.And then your term connection won't be terminated.So that's some of the way in whichyou can keep your tunnel up and running all the time.So, that's it for this lecture.Just quickly,DPD is Dead Peer Detection and it is used tocheck the liveliness of your VPN tunnel.If you decide to use DPD, then on AWS side,it will continuously send R-U-THERE packets.And if there is no response to three consecutive requests,the AWS by default will terminate the VPN tunnel.You can set different action,like a restart or none,but the default is to clear the connection.So that's it about DPD important for your examto have a fail-over scenarios.So remember this,thanks for watching.