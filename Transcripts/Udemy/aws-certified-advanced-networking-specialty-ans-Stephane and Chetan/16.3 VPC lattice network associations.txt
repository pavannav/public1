# 16.3 VPC lattice network associations

Hi.Now in this lecture let's look at the VPC lattice network associations.And this is one of the important lecture in this section.So when we say VPC lattice network association we are saying which all things can be connected to theVPC lattice network.And in there there are four types.First is a service association.Second the resource association.Then the third type is VPC association.Now this is the association which allows clients to access VPC lattice services and the resources.So these are the three primary associations in the VPC lattice network.And the fourth type is VPC endpoint association.And it is used when you have to access the VPC lattice network from some remote network.Right.So these are the four types of the network association.And let's begin with service association.So once you create the service and associate that with the VPC lattice network then that service isdiscoverable and it becomes the part of that VPC lattice network.Further, this service can be in the same AWS account, or it can be shared by some other AWS accountusing the Resource Access Manager.Okay.So that's service association.Similarly there is resource configuration associations.And for that you have to first create the resource gateway.And then using that gateway you create resource configuration association with the VPC lattice servicenetwork.So it works exactly in the same fashion as service will work.And you can also share the resources across AWS accounts.Okay.So far so good.So these are the simplest form of the association.And now let's talk about the VPC association.Now here I said that if you want to access the VPC lattice network and the services from a particularVPC, then that VPC has to be associated with the VPC lattice network using the VPC association.And once you do that, then the clients in that VPC can access or they can initiate the request forthe VPC lattice services.And all this communication happens over the local link address inside the VPC.And if you know this addresses start with 169.254 range.So for VPC lattice that range is 169.254.171.And then some IP address.So when you create the VPC association AWS will have this IP configured.And this particular local link IP is accessible only from within the VPC.Which means if there is another VPC which is peer to this VPC, it cannot access this IP address.And that's where only client inside this VPC can access the VPC lattice service network.So please remember this right.So once you do this VPC association.Now this client here in this VPC can access other VPC lattice services and the resources given thatthere are write auth policies.So the communication would happen something like this okay.The next association type is VPC endpoint association.Now as we discussed if you have say some shared services VPC through which you want to allow accessto other networks for the VPC lattice network, then the VPC association won't help because this IPaddress here is not accessible from this network.Right.And hence in this case instead you have to create VPC endpoint association.So this is the VPC private link or the endpoint feature where you are creating the VPC endpoint in thisVPC for the VPC lattice service network.Now because this is a endpoint, it creates the Eni.And there is a private IP address from the range of the VPC.And now this IP can be accessed from the clients which are connected over VPN or DD or VPC peering ortransit gateway.So once you do that, these clients can access the VPC lattice network.So remember this.So in order to access the VPC lattice network from the remote network you have to create a VPC endpointof type service network.And it connects VPC to the service network.Further clients traffic that comes from outside of the VPC over all these connections then can reachto the VPC lattice network.Now when you create a VPC endpoint in a VPC, as I said, IP is from the VPC are used to establish thisconnectivity, right.So I hope this is clear.So how do you create VPC endpoint.So you go to the VPC console.And there in the endpoint you can create the endpoint with the name this com dot Amazon AWS.And then region dot VPC lattice.Right.So we talked about all four types of the association.Now before we stop for this lecture I just want to bring up an important point.So in this lecture we said that VPC associations allows the client to access the VPC lattice networkand services.And these green arrows represents the same.But what if say service A wants to communicate with service B through the VPC lattice network.Now it is not allowed.That's because these services are just associated with service association.This VPC here and the VPC lattice network are not connected over the VPC association.So remember this thing.And this also means that if service A needs to initiate the traffic for service B, this VPC has tobe associated with the VPC lattice network over the VPC association.So you would have to do something like this.And now service A can also initiate the traffic.And at the same time service B can only respond to the request from service A.So service B will not be able to initiate the traffic to service A.That's because there is no VPC association between this VPC and the VPC lattice network.So this is very important to understand because many a times people will just set this up and they expectthat all services should be able to communicate with each other by default.It doesn't happen like that, right?So with that I hope it is clear.And now you understand all these different types of the associations and with this knowledge.Now let's go to the next lecture where we will see end to end traffic flow, where client initiatesthe traffic flow or the request for the VPC lattice service.Right.So with that let's stop for this lecture and let's go to the next lecture.Thank you.