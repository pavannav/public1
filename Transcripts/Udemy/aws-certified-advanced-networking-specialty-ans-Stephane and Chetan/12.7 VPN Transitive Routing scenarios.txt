# 12.7 VPN Transitive Routing scenarios

Hello, and welcome to this lecture.In this lecture, we are going to seesome VPN Transitive Routing Scenarios.Now, if your corporate data center is connectedto AWS VPC using site-to-site VPN connectionvia virtual private gateway,then if you're sending trafficfrom your on-premise network to AWS,now let's see what happens,suppose if you want to reach to a peered VPCthrough that virtual private gateway connection,or you want to reach to say internetvia internet gateway on AWS side of the network.Now, is that possible or it's not possible?So let's look at some of the scenariosand that would help us really choose the right answer,given a scenario where the traffic will flowin that situation or not.So let's see few scenarios here.Now, this setup is very much what we have seen so far.There is a corporate data center and customer gateway,and then there is AWS VPC and virtual private gatewayattached to the VPC.And these are connected over site-to-site VPN connection.Now on VPC side, you also have IGW attached through IGW.You know, you can reach to the internet,but now the question is,if your traffic is coming from your corporate data center,it flows through customer gatewayto the virtual private gateway,can it reach to the internet via this IGW,which is attached to your VPC?And what we mean that on corporate data center,you might not have outbound internet connection,but you're trying to reach to the internetby going through the VPN tunnel,and then from the IGW, is that possible?So the short answer is no, that's not possible.And there is a reason to thatbecause virtual private gatewayis actually a gateway device,it's not ENI which is provisioned by AWS,where, because ENI is a part of your VPC,it's a part of your subnet.That means for the traffic which lands into that ENI,follows all the routing rules of your subnet.But in this case, VGW is not ENI, it's a gatewayand that's where it can't route the trafficdirectly from VGW to IGW.So in that sense, this is invalid scenario.That means you cannot reach to the internetvia the VGW, remember this.Now, moving on, what if now,instead of we said that you cannot go directly to the IGW,but what if we introduced NAT gateway in between?Because you know, if there is a public or a private subnetand there is a NAT gateway in a public subnet,you can reach to internet, at least from within the VPC.Now, that could have been possible if we had more controlover that NAT device, but in this case,NAT gateway is again a managed serviceand it does not allow to change any configurationon that NAT device.And due to NAT gateway limitations,we cannot still reach to the internetif the traffic lands to VGW, and then from there,we can't really go to the internet via NAT gateway.So this is also invalid scenarioin that case, remember this.Okay, now that being said,what if I am having my own NAT EC2 instance,and then I configure it the way I want?Now, if you look at that scenario,now you have all the control over the NAT instancewhere you can remove all those restrictions of NAT gateways,and of course this NAT instance is in public subnetand it follows the routing ruleswhat do you define in a public subnet.In this case, it is possiblethat whatever traffic comes from your corporate data centervia the VGW can go to the internetvia this NAT EC2 instance.So remember, for sending the traffic to internet via IGW,you can't use internet gateway directlyor NAT gateway directly,but you can have your NAT EC2 instanceand then that traffic can flow.Okay, I hope that is clear.Now let's look at some other scenarios as well.Now, in this case, I have another VPC,which is attached to my primary VPC,where I'm terminating my VPN connection.Now think again, right, here, also, as you know,VPC peering does not support the transitive routing.That means if traffic is landing into this VPC from outside,this traffic cannot flow to the other peered VPC directly,unless you are originating that traffichere in the primary VPC.Now what traffic we are sending again,is not originating from this VPC,and that means this connection is also not possible.So remember that thing.Okay, now let's look at some more scenariosand then we'll conclude this section.Now, what if I have VPC gateway endpoint attached to my VPC?And as you know, we covered thatin a VPC endpoint section through gateway endpoint,you can reach to AWS services like S3 and DynamoDB,and for using VPC gateway endpoint, of course,you need to modify the route table of your subnet, right?So that traffic can reach to S3 or DynamoDBusing the prefix list.Now, again, a similar rule applies like VPC peeringto the VPC gateway endpoint as well.Traffic is not originating from this VPC,and that's where it can not follow those route tables.So in this case as well, this is an invalid scenario.This traffic cannot flow through virtual private gateway.And if you remember,we had covered this scenarioin VPC endpoint lecture as well.We said this is an invalid scenario,where traffic is coming from on-premisethrough virtual private gateway, or say VPN connection,you cannot reach to AWS services through a gateway endpoint.Now things change a littlewhen instead of VPC gateway endpoint,you are trying to use VPC interface endpoints,which are powered by PrivateLink.Now, if you remember VPC interface endpointallows you to reach to AWS services endpoints APIs,like you can hit the EC2 API's load balancer,or you can send a message to the SQS privatelyfrom your VPC, and this is powered by PrivateLink.In that case what happens,is this VPC interface endpoint creates ENI,Elastic Network Interface into your subnet,and through that ENI,you can reach to the respective services.Now, if you see, I have some destination in my VPCwhere the traffic can land,and from there through that ENI,I should be able to go to the respective services.So in case of interface endpoint,it is possible that I can access these servicesfrom on-premises via the private gateway.So this traffic is allowed.So that's a difference betweena gateway endpoint and an interface endpointwith respect to transitive routing.So hope that is clear.Now let's take the other way around.This is more, we talked abouta traffic coming from on-premises to AWS,but now the other way around,the traffic is originating on AWS side of the network.And again, through virtual private gateway,through customer gatewayit is reaching to on-premise data center.Now, what if I want to reach to internetusing on-premise NAT devices?Now, if you look at this,you can completely configure the way you want, right,on corporate data center side of the NAT devices.So if you allow that in your routing rules for this NAT,of course you can reach to the google.comor any internet websites.So there is no such restrictions.That means when a traffic originates from VPC,all the routing rules which we configured here would apply.And if you want to allow connection to internet,allow connection to some other third party networks,all that would be possible.So this is very well a valid scenario.Okay, so that's what I wanted to cover.Let's quickly recap what we just talked about.So if traffic is originating from your on-premisesand going to AWS via virtual private gateway,then you cannot access internetthrough a VPC attached internet gateway,you cannot access internet through a NAT gateway,you cannot access peered VPC resourcesthrough peering connection,and you cannot access S3 and DynamoDBvia the VPC gateway endpoint.However, you can definitely access AWS services endpointsusing interface VPC interface endpoint,which is powered by PrivateLink.And also you can access internetif you're using self-managed NAT instancein a public subnet,then also you can access internetvia the virtual private gateway.Now, other way round,if the traffic is originating from AWSand going to customer gateway,then you can access whatever traffic you wantto access on the customer network side of it, right?There is no such restrictions.So that's about the transitive routing of VPN,and this is good to remember all these scenariosor at least think in what cases it will be possible.So the key is if traffic lands into your VPC that is ENIor an EC2 instance on which the traffic force lands,then most probably the traffic is allowed.If it is just a gateway and then you haveto route traffic using route tables,it won't be allowed,that essentially allows or denies the trafficwhich flows through a VPN.So that's it about this lecture,I hope that is clear, and thank you.