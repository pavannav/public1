# 4.5 Elastic Network Interface (ENI)  deep dive

Hi.So in this lecture let's understand about the elastic network interfaces or Enis.Now as the name suggests these are network interfaces.And you know that network interface is a basic building block of any network which provides the IP addressand which ultimately enables the network communication.Now in AWS world it is called Elastic Network Interface.So as of now we are talking about EC2 machine and corresponding IP address.But how EC2 instance gets that IP address.Now that IP address is assigned using the Eni that AWS creates when you launch EC2 instance.So Enis are the logical component in the VPC that represents the virtual network card.Now this is very similar to the physical network card that you have in your laptop or in any servermachine.So in AWS world you have EC2 and whenever you provision the EC2 instance, AWS will create automaticallyan Eni and it will be associated with that EC2 instance.So here you can see that Eni has the address eth0 and it acts as a primary network interface for yourEC2 instance.So which means whenever we talk about the IP addresses, we are actually talking about the elastic networkinterfaces.And one more thing is that you can attach multiple Enis to your EC2 instance.And shortly we will see how many Enis you can attach to a given instance.But like there is ETH zero, you can also add one more Eni and associate that with the same EC2 instance,right?Okay, now let's understand a few more properties of the Enis.So Enis are bound to a specific AZ, which makes sense because EC2 instance scope is also AC level,which means if you create Eni in one AZ, you cannot associate that with the EC2 instance in anotherAZ.Okay, moving forward, let's understand the attributes of the Enis.And there are a lot of properties of the Enis.So we'll just quickly talk about that.And I don't think you need to remember all those.But I will highlight the things which are important to remember.So every Eni that you create has of course the primary IPv4 address that it gets from the subnet inwhich you create that Eni.And that's where EC2 instance gets the private IPv4 address when you launch it.Now similarly, Eni can also provide the IPv6 address.And that really depends whether your VPC has IPv6 Cidr block.Now so far we are just talking about the primary IPv4 address.But Enis can also have secondary additional IP addresses.Now here we are not talking about secondary enis but secondary IP's of the same Enis.That means when any can have multiple IP addresses.So one or more secondary private IPv4 addresses from the range of the VPC and the subnet in which thoseEnis are provisioned.Now, along with private IPv4 address, Eni will also have one or more elastic IP address, one publicIP address, and one or more IPv6 addresses.Now other important thing that you should remember is the security group that you attach to EC2.Instances are actually attached to the Enis.So Eni can have one or more security groups.Now there is a limit of five security groups so far.And this limit can change over the time.Now another feature of Eni is a mac address.And this is important because a lot of software licenses are bound to a mac address.So if your instance terminates for some reason, then probably you would have to buy another softwarelicense.Now, in that case, you can preserve your inis and then you can launch another instance and can attachthis eni to that instance.That's where your license will still work.So whenever you see questions around licensing and Mac addresses, remember this thing.And finally, you can also have source destination flag for the Eni.Now if you remember in the Nat exercise where we had configured Nat on the EC2 instance, we had disabledthe source destination check.And in that case we actually disabled that on the Eni of that EC2 instance.So that feature actually comes from the Eni.And because the Inis can be attached and detached from the EC2 instance and we are talking about thesecondary Eni, then you can definitely detach that secondary Eni from one EC2 instance and can attachto another EC2 instance.Now you can't do that with the primary Enis Nice because that's primary.But definitely you can do that with the secondary.Ines.Okay.Now one question that I did not answer is how do you know that?How many Ines or IP addresses your instance can have?Now that really depends on EC2 instance type.And this is available in AWS documentation where it clearly defines that depending on the instance type,how many network interfaces you can have and how many IP addresses per network interface can have.So accordingly, you can make your calculations that how many maximum IP addresses you can have on thatparticular instance.And this becomes essential or important whenever you work in say, Kubernetes world where Kubernetespods get a IP from the secondary IP addresses of your Ines.And we are going to talk about this in very detail in the ECS networking section.But point here is that number of IP addresses that you can have for EC2 instance actually depends oninstance type and corresponding Enis, right?So I hope that is clear.Okay.Now let's look at some of the use cases of the Enis.So there are many use cases.But I will highlight four use cases which are important for your exam.So the use cases are requester managed Enis.Then how you can have your EC2 instance as a dual home.Then how can you have a low cost solution for high availability.And then how can you use the secondary IP addresses of the Eni for the EKS pods?So let's talk about these use cases okay.So very first use case is requester managed Enis.And this means that AWS creates the Eni into your VPC on behalf of some other AWS service.So if you have ever used rds, DB instances or say Nat gateways, you have used requester Managed Ennis.And to explain it a little further, when you create a RDS DB instance, you know that it's a fullymanaged AWS service, which means AWS takes care of its provisioning, patching, maintenance, everything.But still, you can control the traffic to the DB instance using the security groups.Now here AWS creates the Eni on behalf of RDS service into your customer VPC.And that's how it can control the rest of the activities for that DB instance.And similarly, if you ever used ECS service that is Elastic Kubernetes service, entire control planestays into AWS managed VPC.Now in this case, even the instances for that control plane is created in AWS managed VPC.And this control plane communicates with the data plane which is inside your VPC using those requestermanaged Enis.So essentially there are two types of resources the resources which are created in the customer VPC.And that's an example of the RDS db instance.And the other use case where the underlying compute service is provisioned inside the AWS managed VPC.And it allows you to access that service using this requester managed Enis.Right.So that's the second example.And third example could be the Lambda service or say Amazon Workspaces or AppStream service.So AppStream and workspaces are like managed compute instances which you can use as your VDI environment.And in this case as well AWS creates the requester managed Enis.And just to show you how exactly it works.So as you can see here, the actual workspace instances which are nothing but underlying EC2 instancesare launched in AWS managed VPC.However, you can access your workspace instances from within your VPC, or you can even connect yourVPC to the corporate data center and then can access these instances from this network interface.Now for this AWS creates this Enis which are requester managed Enis.Now another example is using the Lambda functions.Now when you create a Lambda function, AWS also asks you whether you want to provision this Lambdafunction inside your VPC.And that's for the reason.If through lambda function, you want to access a rds db instance.Now in that case, AWS provisions this Eni and then associate that with the Lambda function so thatLambda can access the resources inside your VPC, and which also means if you don't use requester managedEni, then Lambda will be provisioned in a separate compute environment which is completely managedby AWS.And then Lambda won't be able to access your resources inside your VPC.So to enable that communication, AWS uses requester managed Enis.So I hope it is clear how requester managed Enis work.Okay, so moving on.Another example of the Enis as you know, is creating dual home instances.Now in this case you have this EC2 instance and one Eni is providing access to the application overthe internet.So you can put right security groups here.But at the same time, if you want to manage or connect to this EC2 instance from your on premise network,then you can have another Eni which is in a private subnet, and then you can connect to it over Dheor a VPN connection.So this kind of implementation can happen using multiple Enis.And now further another use case where you can have high availability solution which is low cost.Now typically for Ha you might have multiple instances running at the same time.But here you can have one instance which is serving the traffic.And there is this Eni.And now if for some reason this EC2 instance goes down, you can preserve your Eni saying that don'tdelete the Eni when instance terminates and then you can launch the replacement instance, which isalso called as hot standby instance.And then you can attach this Eni to this instance.Now while doing so your client doesn't understand that the EC2 instance has changed.You don't need to change any DNS configuration and everything works now.Momentarily you could have the traffic loss, but if it is not super critical business application thenthis is better with respect to your cost of running multiple instances.So as I said, you can attach the Eni to another instance and no other changes are required.And finally, you can use the Eni secondary IP addresses to allocate IP address to exports.And we are going to dive deep into this architecture in the EKS networking section.But that's another area where Enis secondary IPS are used.Okay, so before we wrap up, I just wanted to summarize what we learned and some important thing toremember for your exam.So number of IP addresses or any depend on the instance type.Then with respect to the Eni attachment or detachment you cannot detach the primary Eni.But you can definitely do that with the secondary Eni.You actually associate security group with the Enis and not with the EC2 instances.And as of now, you can associate up to five security groups to a given.Any further.If it is about the Mac addresses and the licensing thing, then you can use the same Eni and your licensingwill still work.Further, you can have multiple Enis and you can have architectures like Multi-home.Of course EC2 has to be in the same AZ, but then this Enis can also be created in other AWS accountand that's how the requester managed Enis work.Now Eni cannot be used for Nic teaming.Now we haven't talked about that.So Nic teaming is a general feature of the networking where you can combine multiple Enis to providethe higher performance and higher bandwidth.But in AWS world this is not supported.So which means if EC2 has the maximum bandwidth limit of five GB, then you cannot have two Enis andthen it will become ten GB.This is not supported.And finally, a word of caution.When you have multiple Enis from the same subnet attached to the instance because it may create theasymmetric routing.Now asymmetric routing means that the incoming traffic comes from one Eni but the returning trafficgoes through another Eni.And in many cases it may create a routing problem.So instead, if you want to have multiple IP addresses in the same subnet, then you just use a singleEni.And then you use multiple IP addresses of those Eni for that purpose.So that's it about the Enis and I hope you learned something new.So thanks for watching and stay tuned.