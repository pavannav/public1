# 22.3 AWS CloudFormation

Hello, and welcome to this lecture, AWS CloudFormation.So, as I said, from the management services,we'll be looking at few services here,and we are talking about AWS CloudFormation,because we have seen some question,one or two times maybe around AWS CloudFormation.So, first of all,let's understand what is AWS CloudFormation service.You can say, it is infrastructure as a code service.So, instead of manually creating everything,you can just write a code for that.So, if you're doing a lot of manual workfor creating AWS infrastructure,like creating VPCs, and the Subnets, and the VPN connection,it is no more required.And if you're working in AWS,I'm sure, you know AWS CloudFormation,because it's pretty much, very powerful service in AWS.And then, you can always create these resourcesinterpretable manner in different AWS region.Maybe for day one test purpose,You are launching your resources in US, East one region,because cost is relatively lower there, but for production,you want to launch it in, maybe Singapore region.Then you can use the same CloudFormation template,and just deploy it in Singapore, right?So, you can deploy same resources in another region,another AWS account.And, even if within the same region,if everything was deleted accidentally,you can recreate that using the CloudFormation.So, wouldn't it to be great?If you could do that as a code, of course, it'll be great.And that's where, it's AWS CloudFormation service,that helps you with.So, it's a declarative way ofoutlining your AWS infrastructure for any resources.Now, when we say any resources,typically what I have seen is,whenever AWS launches new AWS services,they will also have a support for that servicein AWS CloudFormation.And if you compare this with, maybe a third party tools,for example, you know, Terraform, you know, Ansible.So, these are not native to AWS, right?And in that case, I think it will take some longer time for,you know, resources support to be available in those tools.So yeah, most of the AWS resources are supportedin AWS CloudFormation as of today.Now, why would you use AWS CloudFormation?Maybe you want to create everything, right?VPC, Subnets, internet gateways, security groups.So, we have been doing a lot of exercises, right?In this course,and you know that we are creating all this one by one,sequentially, manually,and it takes certain amount of time.So, of course, we would wantto use AWS CloudFormation instead,because it creates your resources in right order.Because for example, if you're creating EC2, it knows that,it has to first create a subnet,and before subnet, it has to first create VPC.So, all these dependencies typically managedby AWS CloudFormation internally.You don't have to really worry a lot,but there have been cases where you need to managethe dependency and we'll talk about that.But then it creates those resources in the right order.So, what are the benefits of CloudFormation,of course, infrastructure as a code,no resources created manually.That means no manual errors,code can be controlled using the git kind of repository.So, it's source controlled.That means you could also see or track the changes happeningto your infrastructure through the code, right?So, if somebody changes your template,which has been added to git repository,you can review,what changes have been made to your infrastructure,just looking at that template change.So, it is reviewable.Cost. You could also geta cost for per CloudFormation template,ultimately templates create resources.So, what's the total cost of all those resourcesinside my AWS CloudFormation template,that cost you can get.Because CloudFormation adds automatically attachedto every resource it creates.That means you could also, you know,estimate how much this CloudFormation template will cost,if somebody deploys that.Further, you could also have a saving strategy,for example, in development environment,you want to delete everything at 5:00 PM,because it's a development,and you don't want those resources to be runningthroughout the night.So, you could have this kind of automation set,because you created your all resources using CloudFormation,and you can make one API call to deletethe CloudFormation stack that it created.And then, you are saving a lot of cost there.So, how does it work?So, it's very simple.You write a template.Template could be JSON and YAML,and you just store that template in S3 bucket,or you can even have it on your machine.You upload that in that case.And then, CloudFormation will create all the resources,which are part of that template.So, this is a simple templatefor VPC peering in the same account.So, this is a type of the resource.You define it.And there is a documentation in AWS,which tells you what the resource name is,and what are the properties of the resources are.So, here we say, VPC ID is a current VPC,which is making and request for other VPC to peer.And then, there will be peer VPC ID.So, after you deploy that in a CloudFormation,there will be a VPC peering,between this VPC and other remote VPC, basically.Now, this is in the same AWS account,but that could be another AWS account as well.And in that case, you add additional parameters here,who is the owner of that account?What is the rule that the other account will be using?What tags I need to add?So, not all the properties are mandatory.Most of the properties are optional, as you can see here,but then, depending on what resources you create,there will be different properties.The best way to really check is to go to AWS documentationfor CloudFormation and look at all those properties.So, this is just one example out there.Now, some feature and components of CloudFormation,which are again important to know.CloudFormation Designer.As I said, you can provide the template,and CloudFormation can show you the diagrams on.You can also basically create those diagrams,and automatically it will createthe CloudFormation template for you.ChangeSets, what it means is,if you have one CloudFormation template already developed,and then you deployed a stack using that CloudFormation.Now, you want to change something.Maybe you want to add one more security group rule,for example, right?In that case,before making changes to your live environment,you want to make sure that you understandwhat changes your template is going to maketo your environment.So in that case, you create a ChangeSet.Then it'll tell you thatit's going to add a new security group, is that okay?Then you review it, and then you go,and apply those changes.So, this is like a pre step before you apply new changes,or you update the template, right?Before that you can look at this ChangeSets.StackSets.Now, if it's a same stack,and you want to deploy it across multiple AWS account,or multiple regions.Maybe you are a SAS provider and you want to deploy,you know, per customer one stack,exactly same stack in different regions or accounts.Then you can use StackSets.So, you don't have to repeat those things for every account,rather StackSets will take care of that.Stack Policies.Again, important one.Maybe you are creating CloudFormation stackwith a lot of resources,and you want ability to delete those resources.You will just go and delete the CloudFormation stack.But if you're using say a database, right? RDS.And you don't want database to be deleted,because there is a lot of data in there,and that's valuable to you.Even if you are deleting the environment,you want to reuse that data somewhere.Or maybe if you're launching again an application,you want to use the same database.So, you could provide a Stack Policies, which says,don't delete this particular resource,delete everything else, but don't delete this.So, all that can be done using Stack Policies.Few more, Cross Stacks.So, Cross Stacks are something like, you know,you have a base, stack basically.So this is more, you know, programming kind of thing.Every other stack uses the same base stack.For example, you're creating one VPC.And in that one VPC,you want to launch multiple applications.Now, instead of every application launching their own VPC,you use one VPC stack.As a separate stack, it'll just launch that VPC,and required subnets.And then, all the other stack will use the same VPC.So, they can refer the resources in the centralized,or base stack into their stack, basically.So, that is a Cross Stacks.And then, Nested Stacks.It's more about,you can reuse your existing stack.So, instead of creating the RDS database,or including the code for RDS databaseinto every CloudFormation template,you just wrap it into one separate CloudFormation template.And then, you could have one big CloudFormation template,which just refers to other template.So, you are not rewriting the code, basically.You are reusing your other CloudFormation templates,which you have already written.So, it also provides you some kind of standardization.You want to make sure the DBAs write this RDS stack,and nobody changes that, right?So, you just refer to those stackfor creating the database rather than writing your own code.Okay. Moving on.Many a times,it happens like in CloudFormation, you write a template,and then you want to make sure,before creating one of the resource,some other resource should be created.Now, most of the time AWS manages that,but there are certain scenarios where AWS can't decide,whether you want to create resource first,and then add the entry somewhere,because it's all, not directly related.For example,if you want to create a VPN gateway in your architecture,and you also want to add a route table entryfor that VPN gateway for route propagation.Now, if you want to really enable that route propagation,first the VPN gateway has to be created.Now, CloudFormation natively you cannot manage that,but then CloudFormation provides you some levers,like for example,there is something called DependOn as an Attribute.Now, what it does is,you can say,before creating the route table propagation,it DependsOn the VPN gateway resource.So, what CloudFormation will do it,before creating this root propagation,it'll wait for your VPN gateway attachment to happen.And then only, it will enable that route propagation.So, this kind of conditions,helps you really manage the dependenciesof the AWS resources.Now, there is one more such condition like DependsOn,which is WaitCondition.Now, WaitCondition is interesting.Many a times, you would want that,you launch EC2 machine.And also in that EC2 machine,you would want to install some applications.Now, AWS won't know, what kind of applicationyou're installing in EC2 machine.Maybe you're using user data of the EC2 to, you know,download some packages, and install.And before you move to the next resourcesto be created through CloudFormation, you want to wait.Because maybe then, EC2 also making a call to a database,or there is some dependencies between, you know,all these things.So, what you can do is,you can create a WaitCondition,and then, through your EC2 user data,you install everything,and you can send signal to the WaitCondition.And then, WaitCondition moves forward.Otherwise it'll wait for the maximum time amount period,that you have defined.So, you can give your EC2 machine,to have maybe 10 minutes of, you know,boot strap time, right?If you put that under the WaitCondition.Now, without WaitCondition, what would happen?CloudFormation will create EC2 instance.So, as soon as EC2 instances up and running,CloudFormation thinks that, yeah, it is done, right?Let's move to the next resources to be created,or other parallel resources to be created.But if there is some kind of dependencies,then it can't handle that.In that case, you would have to use WaitCondition.Now, we are not using DependsOn clause here,because again,DependsOn just looks at the AWS resource creation.It doesn't really wait for EC2 to be become active,which only, you know,when the EC2 installs all the scripts insidewithin the user data, right?So, you have to externally make a signalto the WaitCondition,and there is a way to do that, right?It gives you one URL,you send a signal to that URL,which is S3 based URL,which WaitCondition handler gives you.And then, once that handler receives the signal as success,then only, CloudFormation will proceed from there.I know, little mercy here, but then,if you're interested,just read more about it throughCloudFormation documentation.But then, DependsOn and WaitConditions are the two things,we have seen,coming into your exam.Okay. So, that's about the CloudFormation basically.And just, as I said,there is an extension to the CloudFormation,which is Cloud Development Kit.Now, CloudFormation itself is a template,which you write in a JSON or YAML format.But if you want to really write a real codeusing Java, or a TypeScript, or Python.So, it will create then CloudFormation.Now, that's really the powerful, right?That means if you want to have some kind of,loop structures, if else condition,that is very difficult in CloudFormation,but then, with AWS Cloud Development Kit, which is CDK,you can write a code in these languagesto generate the CloudFormation template.And then, you can deploy using the CDK CLI, basically.So, you write your code,you synthesize that code through CDK CLI,which generates the template.And then, you deploy that using the CloudFormationto create the stack, basically.So, that is CDK.So, you can write a code in Java, Python, as I said.So, this is the new way of doing the things.And then, you can also create libraries.It is called as CDK Construct.So, you can share your Construct with other developers.You could add that to your open source code, right?So, all the developers can use your code,and change it as required.So, instead of really sharing the template,you are now sharing the code,which generates those templates.And the process is like your cdk init,which folks a project into your repository.Then you build that, you synthesize that,which creates the template.If you are making an update to existing template,you can also look at the cdk diff,which is like a ChangeSets in CloudFormation.And then you say cdk deploy.So, ultimately same thing, but you know,doing it more in programmatic way.So, that's about CDK, and that's about the CloudFormation,that you need to know for your exam.I hope, most of the things are clear.So, thanks for watching.