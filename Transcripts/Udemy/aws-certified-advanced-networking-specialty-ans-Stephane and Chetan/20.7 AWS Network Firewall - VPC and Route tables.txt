# 20.7 AWS Network Firewall - VPC and Route tables

Hello and welcome to this lecture.So, now let's look at the VPC setup for Network Firewall.And how would you configure your Route tableso that traffic can be inspectedusing the Network Firewall.So, we have VPC and then we have multiple AZs.And for simplicity, I'm just usingone availability zone here.And then you would have your machines,your databases inside your protected subnet.Now, this could be a public subnet or a private subnet,but we are just calling it protected as of now.Now, for using the Network Firewall,you would also need a Firewall Subnetin which you will provision the Firewall endpoint,which ultimately has the connectionwith your AWS Network Firewall in your AWS region.Now, when your traffic is coming from, say internet,then you how to make sure that now all the traffic landsinto a Firewall endpoint first,and from there, then it goes to your machines, right?So for Inbound traffic, for example,whatever traffic coming from the internet,you have to tell that if traffic is goingto the workload subnet, which you have protected,the traffic has to go through somethingcalled the endpoint for your firewall,which has a syntax like vpce- some number.So, once you do that,your traffic goes to the Firewall endpoint.Now, you must be wonderingthat how you can have the Route tablefor the Internet gateway?BUt Internet gateway does provide somethingcalled Ingress routing Route table,where you can define these routes.Now second, when the traffic landsinto the Firewall endpoint,it has to make sure that traffic ultimately goesto your target machines, right?That's where you will seethat any local traffic will be routedthrough the local VPC routerand this IP is a part of that.But any outgoing traffic,from firewall will go through the Internet gateway,and this is essentiallyfor the return traffic from your machines,or the outbound originating traffic from your machines.And finally your protected subnetwould again have the local route entry,as well as the entry for any outbound traffic,which is say 0.0.0.0.It will always go through the Firewall endpointand then Firewall endpoint furtherwill forward this traffic to the internet,through the Internet gateway.And this is how the end to end traffic will look like.So this is the Inbound flow.And if you talk about the sequence,you get the request from internet.It goes to the Internet gateway.It will be routed through Firewall endpoint,Firewall endpoint will talk to actual AWS Network Firewall,and then it will get response from there,and finally, traffic will be routed to your machine.Now, this machine will also respond,and the traffic will go to your Network Firewall,and from there, again, the traffic will goto the Internet gateway to the internet back.So this is how Inbound flow would look like.Now if you talk about the Outbound flow, things are similar.Same Route tables.And in this case,traffic will be originated at your machine,goes to the Firewall endpoint.From there goes to the AWS Network Firewall.It will respond.From there, it will goto the Internet gateway, to the internet.And then you'll get a response from the internet,Traffic will again land into the Network Firewall endpoint,again to the Network Firewall,and back to your machine.So, you see, whether it's an Inbound or Outbound traffic,every time it has been interceptedby your Network Firewall endpoint.And that's the whole purposeof using the AWS Network Firewall.So, that's it about the Inbound and Outbound flow,and the route tables for your VPC that you'll designfor using the Network Firewall.So, thanks for watching.