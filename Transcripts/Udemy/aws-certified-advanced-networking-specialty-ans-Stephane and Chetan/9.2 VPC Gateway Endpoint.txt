# 9.2 VPC Gateway Endpoint

Okay.Now let's look at the VPC gateway endpoint to begin with.And as I said in the previous lecture, VPC gateway endpoint is little different.The way it works as compared to the other end points like interface endpoint which are powered by privateLink.So let's first discuss VPC endpoint quickly.And let's also do some exercise so that you exactly know how it works.Okay so we are talking about the gateway endpoint.As you can see here which is used to access Amazon S3 and DynamoDB.These two services privately from your VPC.Right.And as you can see here, in order to access gateway endpoint, you have to modify the route table ofthe subnet.Right.So in this lecture let's talk more about this gateway endpoint.Okay.So it enables the private connection between VPC and S3 or DynamoDB.And for using the gateway endpoint you have to modify the route tables so that there is an entry inthe route table which takes the traffic from the subnet to Amazon S3 or DynamoDB table.But you must be wondering what kind of entry you will add, because for adding the route table entry,you need to know the IP addresses of Amazon S3 and DynamoDB.Right now these are the services which are managed by AWS, and I'm sure there isn't 1 or 2 serverson which, say S3 or DynamoDB service will be hosted.There will be hundreds and thousands of maybe machines on which these services are hosted.So ideally, you should add all those IP addresses to your route table, which is not really practical,right?So how do you do that?Now for that, what AWS does is it creates something called prefix list.So it's called IP prefix list where it combines all those IP address ranges for say S3.Or there could be a prefix list for say, DynamoDB.So you don't need to individually add those IP addresses to your route table.You can just add that prefix list.So this is AWS managed prefix list.But if you want you can also create your own custom prefix list.But in this case because we are accessing S3 and DynamoDB services, we can use AWS managed prefix list,right?So as I said, it's a collection of IP addresses for these services.And it is in the format PL dash some ID and it becomes available so that you can add it to your subnetroute table or also in your security group.And we will see that shortly.So your route table entry finally looks something like this where the destination is this prefix listfor S3 service.And the target will be the gateway VPC endpoint.Right.So please remember this.Next you also need to modify the outbound security group for the EC2 instance, which is accessing theS3 or DynamoDB.Right now.By default, you know that all the outbound traffic is allowed, but in case you are using a customsecurity group in which it is not allowed, then you should also open the port for https and http forthe same prefix list.Right?And that's where traffic can go out from EC2 instance to respective AWS service.Further, one more important thing about VPC gateway endpoint is that they can be accessed from withinthe same VPC in which you have created it, and you can only access the AWS services in the same region.Now, if you see this architecture, I have region, I have VPC and there is a gateway endpoint therewhich connects the VPC to S3.And then this VPC is connected to Corporate Data center via VPN or Direct Connect.And also I have VPC peering connection to another VPC.Now the question is from my on premise data center or from the peered VPC.Can I access S3 via this gateway endpoint?Now here is the trick.In general, if the given endpoint is having, say, a Eni into your VPC provisioned, then most probablyyou can do that because your traffic is landing into VPC and from there it can go to a respective destination.But as you understand the VPC gateway endpoint, there is no Eni created.Now that is true for interface endpoint.And that's what we are going to see in the next lectures.But in case of gateway endpoint there is no any that you manage.You just modify your route table of your VPC and that's it.You get that connection to S3.Now in this case, if traffic is flowing from VPN or DD, it does not know where to go because the routetable is for your subnet.It is not for the traffic coming from VPN.And that's why the in this case, you can't access this S3 bucket via this gateway endpoint.So the connection from the VPC is not allowed.Similarly, if the traffic originate in on premise data center, it cannot flow through the gatewayendpoint to S3.This is also not supported.So these are invalid scenarios for accessing S3 or DynamoDB using the gateway endpoint.So remember that.Okay.And finally one more important thing about the gateway endpoint is that it's free to use.So there is no charge for, you know, how many hours you create that gateway endpoint or the trafficthat flows between, say, EC2 and S3?Because VPC and AWS services are in the same AWS region.And that's where it's recommended that you always use VPC gateway endpoint whenever your applicationhas to access, say, Amazon S3 or DynamoDB.Okay.So that's it about VPC gateway endpoints.And before we get into say, security of the gateway endpoint and some other things, let's first doa quick demo where you will see how exactly it works.And then we will cover VPC interface endpoint which works differently than the gateway endpoint.And then we will see the security aspects of the endpoint because it's common for both gateway and theinterface endpoint.So with that let's stop for this lecture and let's go to the next lecture.Thank you.