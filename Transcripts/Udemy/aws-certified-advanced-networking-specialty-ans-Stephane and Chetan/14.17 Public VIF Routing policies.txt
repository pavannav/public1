# 14.17 Public VIF Routing policies

Okay, so now let's look atthe public wifi routing policies first,and let's see how it influences the traffic,which flows from AWS to on premiseas well as from on-premise network to AWS.So, there are two type of the policies,inbound policies and outbound policies.As I said, inbound policies meansthe policies which are applicable to the traffic or routesflowing from customer router to AWS.So in this case,of course, when you create a public wifi,you remember you have to provide the IPv4 prefixesor IPv6 prefixes, right?To advertise over the BGP.So what that AWS learns,which are the IP addresses on the customer insight.So you must specify those IP addressesand you must also own those IP addresses.So AWS verifies that you own those IP prefixes.And this is important because, you know,if you advertise the IP prefixes not owned by youand maybe some other customers own it,then AWS would wrongly send this traffic to youinstead of sending it to the other customers.And that's where this is very importantthat you own these public prefixes.Now traffic must be destined to AWS public prefixes.That means all the traffic that you are sendingfrom customer router to AWS should also have the destinationas one of the public IP addresses on AWS site.The transitory routingbetween the connection is not supportedand I'll talk more about that shortly,but essentially what it means isonce AWS learns about your side of the prefixes,AWS does not further share these prefixes with,you know, other customersbecause AWS would be having direct connect connectionwith multiple customers.So AWS does not advertise it further to the other customers.AWS also performs the inbound packet filtering nowbecause AWS knows your side of the IP prefixes.It makes sure that AWS is receiving trafficonly from those prefixes.So if you send the packet from a new IP prefixes,which is not advertised over BGP,AWS will reject that traffic.So let's look at through this architecture.On the right side, of course you have corporate data centerand on left side, you have all AWS regions.Now, because we are talking about the public wifi,then you can access all AWS region as you know.So on your corporate data center side,you might be having some network ranges.One of the range is this, 192.168,and there could be additional network ranges as well.Now you want to advertise these routes to AWSover a BGP session.So first thing you must own these networksand AWS will verify thatbefore setting up this BGP connection.And once that is done,you advertise these routes and these routes get advertisedto all AWS regionsbecause public wifi allows you toconnect to all AWS regions.Now once AWS learns about these prefixes from your end,AWS could also propagate these routes to the other customer,but as transitive routing is not supported,and this is not something that you want, right?You don't want your routes to be publishedto some other customers, right?So that's where this is not supported.That means transitive routing is not valid.So that's one thing that you need to understandwith respect to the inbound routing policies.Now let's talk about the public wifioutbound outing policies.Outbound means the traffic,which is flowing from AWS to the customer router.Now in this case, how AWS takes that decision,like I want to take one path over the other,and this is essentially applicablewhen there are multiple direct connect connectionsbetween AWS and your on-premise networks, right?So the longest prefix match takes the precedence.That, that is obviousand we have been seeing this in the various section,like a VPN as well, right?So what essentially this means is thatif on AWS side, you have multiple routes in the route tableand say, one route is with a slash 16 prefix,another route is with slash 28 prefix.And if there are two connection to reach to the destination,which route it will take?So if your IP address falls in slash 28 range,then it would take that prefix,even though the IP address also falls into a slash 16 range,but slash 28 is more, you know,longest prefix, I would say.More specific, right?In that case, that route will be,or that path will be selectedfor sending the traffic to customer router site.And the other thing is ASPATH.We learned about ASPATH in BGP session as well.Essentially, this means how many hubs are thereto reach to the destination.Longest ASPATH, less paper path it would be, right?So these are the outbound policies,which also influence the routing decisionfor outbound connection.Now, AWS also advertises all localand remote AWS region prefixes to your network.And this is important because otherwise,if your network does not know aboutthose public IP prefixes,then it does not really help youto route the traffic to AWS.AWS will also advertise all the public prefixeswith something called NO Export BGP communities.Now what that means isonce you learn the routes from the AWS,you should not further export these routesto the other networks outside of your network.Not similar to what AWS does as a non transitory routingin case of inbound, right?We said that AWS does not share those routeswith the other customers.Same thing applies to you as well.You should not further export these routesto the other networks.Now, additionally,AWS also advertises some BGB community tags.Now, what does BGB community tags mean?We will see shortly,but what the purpose of this community tagis to filter the traffic at your end.And in the start of this section, I told you that,you know, you can filter whether you want to talk toonly one AWS region or you want to the multiple AWS region,that can be done using this BGP community tags.And we'll see example of that shortly.Now, the prefixes advertised by AWS Direct Connectmust not be advertised beyond the network boundariesof your connection.We just talked about that when we saythere is a No Export BGP community tag,which AWS sends along with the routes.And finally,with the combination of this ASPATH and BGP communities,you can actually designdifferent type of the network architecture.That means if you're multiple direct connect connectionto reach to the target,you can decide if I want to do a load sharing of the trafficbetween these two connection,or maybe sometimes you want to have one connectionas an active and one connection as a passive.Now this can be done using the combination of,you know, routing policies as well as BGP communities.So now with basic understanding of how the routingand BGP communities work,let's try to build some architecturewhen it comes to the traffic flowingbetween the AWS and on-premise networkand when there are multiple direct connect connections.So how do you make a choicefor one connection over the other?So let's get into that.Thanks for watching.