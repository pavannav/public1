# 19.29 Route 53 DNS Firewall

So let's talk aboutthe Route 53 Resolver DNS Firewall.So this is a managed firewall,and it enables you to filter outbound DNS requeststhat are going out through your Route 53 Resolverfrom your VPC.So if you have a look at this diagram,we see we have an EC2 instanceand is going to ask our Route 53 Resolverfor the DNS query for example.com.Now we also have set up a Resolver DNS firewall,and so the Route 53 Resolver is going to performthe DNS query on the firewall saying, hey,what about example.com?And it's possible for us to set up rules on the firewall,for example, to block this kind of domain,we don't want to query for this domain.Therefore the resolver will say,back to the firewall, will say back to the resolverthat it's blocked and so thereforethe EC2 instance will not be able to resolvethe domain example.com from our resolver.So why would we do this?Well, this is to Blacklist any malicious domainsor to Whitelist trusted domains.And what is the risk behind the DNS?Well some attackers, they like to use the DNS protocolto send data out.So for example, the malicious attackerwill set up a DNS and through DNS queriesis going to send data to itand it's called DNS exfiltration.So it's just the little hack that can happen.And so therefore you can manage a Whitelistor a Blacklist of domains directly from your DNS firewall.Now you can also manage the configuration of all thisfrom the overall service called AWS Firewall Manager.And in case you wanna look at some loggingwell you have integrations with CloudWatch Logs,and also the Route 53 Resolver query logs that we just saw.Now, one important piece of informationand configuration need to know for the examis the DNS firewall configuration.And you have two ways of configuring it,Fail-close, or Fail-open.So Fail-close is going to make sure that if you don't haveany response for your DNS firewall,for example, if it's down or there's a problem on AWS,if there is no response then the resolveris going to block the query.So this is to have security over availability.That means that if your Resolver DNS Firewall is downthen all DNS queries will just block, okay.But if you have Fail-openthat means that if your DNS firewall is downthen the resolver will allow the query,and this is to make sure that you haveavailability over security.So this is something the exam will ask youand you'll have to choose between Fail-openand Fail-close at some point during maybe a question.Okay, that's it for this lecture.I hope you liked it.And I will see you in the next lecture.