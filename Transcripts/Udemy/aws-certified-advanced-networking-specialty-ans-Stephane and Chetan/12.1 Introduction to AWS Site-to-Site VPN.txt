# 12.1 Introduction to AWS Site-to-Site VPN

Hello and welcome to this new section, AWS Site to Site VPN.Now, if you ask me to pick three topics for AWS Certified Advanced Networking Exam, then I'll pickAWS Direct Connect, AWS Transit Gateway, and then AWS Site to Site VPN.So please make sure that you understand AWS Site to Site VPN really well and you can design the architecture.You can answer the questions related to different scenarios which we will be covering in this section.Okay.So let's get started with this section.And in this section let's understand the fundamentals of AWS site to site VPN as in what it offers,why to use that.And then going forward we will do some exercises as well.And we'll understand more about AWS site to site VPN.Right.Okay.So let's get started.Now we already talked about this maybe in bits and pieces, but let me put things together right.So if you want to access any AWS resources, for example, your EC2 instances or your databases, thenof course you just connect to AWS console, or you get a public IP of that AWS resource and connectit over the internet.So that's the, you know, very common way to access AWS over the internet.Now, while this works, not many companies would want to do that always, right?Because maybe they don't want to expose their AWS resources to the outside world.So rather they want to access those AWS resources privately, right?Now, when it comes to accessing the AWS resources privately and securely, then there are differentways to access AWS resources.So primarily there are these three ways in which you can access AWS resources, like your databasesor the EC2 instances which are inside your VPC and which doesn't have the public IPS.is now.If you want to access those, you can use either AWS Site to Site VPN or AWS client VPN or AWS DirectConnect.Right.So this connectivity is more secure and it is private.And in this particular section we will be primarily focusing on AWS site to site VPN.Okay.Now further, if you divide this secure and private connectivity again you can divide that into twodifferent categories.So AWS Site to Site VPN and AWS client VPN.Traffic still goes over the internet and there is one star mark there.And I will talk about that a little later.But this traffic goes over the internet.But it is secure and it is encrypted.And that's where it is called private connectivity.However, if you want a dedicated connection to AWS, then of course you can use AWS Direct Connect.And we have the following section where we dive deep into AWS Direct Connect.So in this lecture, let's focus on the connectivity over the internet.And out of that we will focus on AWS site to site VPN.Now what is the difference between site to site VPN and client VPN.So it is as simple as this.If you have your corporate data center and if you want to connect that privately to the AWS VPCs, thenit will be AWS Site to Site VPN.However, if you are working from home and if you want to privately access some VPC resources, thenyou will have the client VPN.So your machine becomes part of this network so that you can access these resources privately.That means using the private IPS without those resources required to have the public IPS.So that's the basic difference, right?So I hope it is clear.And with that now let's understand how AWS site to site VPN works okay.So as you can see here on the left side you have AWS regions.And inside that you will have your VPCs.And inside VPCs you will have your EC2 instances, your databases, everything right.And on the right side you have your corporate data center.Now what we are saying that AWS Site to Site VPN allows the private connectivity between these two networks,right?So VPN connection is established between AWS network and the customer corporate network.Now how do you set up this VPN.Now for that there are two ways.First, you can have a virtual private gateway, which is also called as VPN gateway, which is attachedto your VPC.And on the customer side.Also, you need a router and VPN endpoint.So once you have that, you can create a secure VPN connection between these two endpoints, right.So that's the one way to set up a VPN where you have the VPN gateway, which is attached to your VPC,but you can also use AWS Transit Gateway to terminate your VPN connection on AWS side.So in that case, the architecture will be something like this.Where in AWS region you will have transit Gateway, and then multiple VPCs might be connected to thistransit gateway.And then you create a VPN connection which terminates on this transit gateway.Right.So I hope it is clear now, further, with respect to the high availability of the network traffic,the VPN connection is actually made up of two VPN tunnels and the two tunnels, of course, for highavailability.So if you just double click into the VPN connection, you will see that there are two different tunnelsand you can configure these tunnels into different mode, for example active active or active passive.And we are going to talk about that later in this section, but just understand that these two tunnelsare for high availability.So if one tunnel goes down, your traffic can still go through another tunnel, right?So just to recap, there is a VPN connection which you can terminate on AWS side either on a virtualprivate gateway or VPN gateway.And other way to connect to AWS is using the Transit gateway, right?So I hope it is clear.With that, let's just cover the basic fundamentals of VPN.And then we will do some labs around that so that you actually see what we are discussing in this lecture.Right.So as we said VPN allows host to communicate privately.When we say host that means host which are there on premises and the host or EC2 instances or databaseswhich are there on the AWS side.So they can communicate privately over the untrusted intermediary network, which is nothing but internet.But this traffic is in encrypted format.That means even if there is a man in the middle attack, nobody can understand what that traffic is,right?So you are safe.Further, when we say site to site VPN, it is layer three VPN.So if you look at the OSI model of networking, there is a logic link layer that is layer two.And then there is IP layer that is layer three.So this VPN works at IP layer.Further, as we said, VPN has two forms site to site VPN and the client VPN.So of course site to site VPN connects two different networks and client to site VPN connects clientdevices like laptops to the private network like AWS VPC.Further, there are also VPN types and AWS supports IPsec VPN as of now.So IPsec is IP security.And this is what we are talking here.But other than IPsec VPN there are also other types of VPN.For example, gray that is generic routing encapsulation, and typically it supports multicast or broadcast.However, if you talk about the IPsec VPN, it supports only unicast.That means it connects just two networks and it can only transport the IP packets.However, gray VPN can also transport non IP packets as well.Further, there is dmvpn that is dynamic multipoint VPN which is like hub and spoke model of the VPNwhere even the spoke branches can communicate over the VPN connection.Now again, these are not supported by AWS managed VPN.And if you really want to do that, then you would have to use your own VPN solution.So for example, you can deploy those VPN solution on EC2 instances.Then you can have probably GRE or Dmvpn, but AWS managed site to site VPN is IPsec VPN, right?Further, a few more things to understand about AWS site to site VPN is that, as we said, it can beterminated on the virtual Private gateway and this gateway is attached to your VPC.So you can only attach single virtual private gateway to a VPC.Then AWS site to site VPN supports both static and dynamic routing.And we are further going to see this in the following lectures.But in the earlier section we already talked about what is static routing, what is dynamic routing,what is BGP and how the routes are exchanged.So AWS VPN supports both the static routing and the dynamic routing.Further, when you configure your VPN in the BGP mode, that is dynamic routing, you have to providethe autonomous system number that is ASN for your virtual private gateway.So you can provide either two byte ASN or the four byte ASN.But if you don't provide anything, then AWS will use a default ASN of number 64512.Now, sometimes in your exam you might have this kind of question around what is the default ASN thatAWS assigns to Virtual Private gateway?So that number is 64512.So just remember that.Now just one more thing which is worth mentioning is that when you create the VPN connection and actuallywe will do an exercise to set that up.During that.There are two tunnels, as we said.Right.And in order to set up that connection on both the sides, you need some kind of authentication forthose tunnels.So for tunnel authentication, AWS supports two mechanism, the Pre-shared keys, which we will actuallyuse during our exercise or otherwise.It can also use private CA certificate which are issued by AWS private CA authority.So both these methods are available.And as I said during our exercise we will see how Pre-shared keys are used.Right.Okay.So I think this knowledge is sufficient to do an exercise where we'll set up actual VPN connection.But before that, I think one important thing to know is that how does AWS VPN support the IPv4 andIPv6 traffic?Because that really depends on which IP addresses you are using on both sides of the VPN connection.So for example, on Virtual Private Gateway, which IP you are using, and on the customer gateway sidewhich IP you are using.So let's quickly discuss that in the next lecture.And then we will get into the exercise.Okay.So with that I hope you now understand the basics of AWS site to site VPN, how it works and basic componentsof that.And now let's understand more about IPv4 and IPv6 traffic.So that's it about this lecture.And now let's go to the next lecture.Thank you.