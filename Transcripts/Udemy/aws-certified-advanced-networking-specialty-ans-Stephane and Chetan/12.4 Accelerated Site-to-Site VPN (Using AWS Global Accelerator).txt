# 12.4 Accelerated Site-to-Site VPN (Using AWS Global Accelerator)

Okay, now that you know how AWS Site to Site VPN works, let's look at some of the features and oneof the feature of site to site VPN that is important to know is accelerated site to site VPN.And for that you can use AWS Global Accelerator.Now we have covered AWS Global Accelerator separately in the CloudFront section.And that's where in this lecture we are not diving too much deep into the global accelerator.But let us see the application of global accelerator for accelerating the site to site VPN connectivity.Okay.So in general, when you create the site to site VPN connection, typically you will have your customernetwork somewhere at the distance place and then you will have your AWS region.And then you will have either virtual Private gateway or the transit gateway on which you will terminatethe VPN connection.Right.Now all this traffic goes over the internet, as we already discussed.And this internet is generally made up of lot of different networks.That means the packet has to travel through all those different networks.Something like this.Right.And you don't necessarily control all these intermediary networks.And that's where many a times you may face challenges, for example having increased latency or thereis congestion in the traffic, or there is some network disruption somewhere in any of the network.And then your VPN connection might get terminated or it might not give the required bandwidth.So all this is happening because all the traffic is going over the different networks, right?Now how do you solve this problem?So for that you need to use accelerated site to site VPN connection.Now in this case what happens now between your data center and AWS region.Right.Where you want to set up the VPN connection you can route this traffic through AWS edge locations.Now again, in the CloudFront section, we have covered in detail what is edge location.But in short, these are like the networking hub, which can be used by CloudFront service to deliveryour content at lowest latency.And they can also cache your content.But in the context of site to site VPN, they are used to optimize your network path.And that is because all these edge locations are connected to AWS region using AWS backbone network,which means when now traffic flows to the transit gateway over the VPN, first time it will hit thenearest edge location.So there is very less network path which goes over your network service provider.But as I said, this edge location is connected to AWS region over AWS backbone network.So this traffic is completely congestion free.It is managed by AWS and that's where it can give you reliable connectivity.So in order to have this kind of VPN, you have to use AWS edge location to route that traffic.And how do you use edge locations?So for that you use AWS Global Accelerator Service.So all these edge locations are part of AWS Global Accelerator.Now question you might have where do you really enable this VPN acceleration.Right.So let me quickly show you that in AWS console.And then we will talk about some important things that you should know about.Accelerated site to site VPN connection.Okay.So I am into my VPC console.And now if you go to the VPN section and if you go to the site to site VPN connection, and if you tryto create a new connection here you can choose where it terminates.Now remember that acceleration is only supported on the transit gateways, so it is not supported onVirtual Private gateway.So you have to select the transit gateway.And I'm not selecting all other option.What I wanted to show you is this.So it says enable acceleration.And that's where it uses AWS Global Accelerator Service.Now another thing to remember is that you can only select this option while creating the VPN connection.Once you create that you can't change that later, right?So just remember that.Okay.So going back to our discussion, a few important things that you need to know is that it is only supportedon the transit gateway.That means if you are terminating your VPN on the virtual private gateways, then you can't have thisVPN acceleration.Second, it can only be enabled while creating the VPN connection, which means that you can't enablethat on the existing VPN connection.So in your exam, if there is a question which says that you want to accelerate the VPN, in that case,you can't really do that on existing connection.So the answer should be you should create a new connection and that too should terminate on AWS TransitGateway.And while creating the connection you have to enable the acceleration further.How it works.So again, as I said, we will cover that in detail in the CloudFront section.But AWS basically provides two anycast IPS for the global acceleration.So whenever you create the VPN tunnel, you have to use these two IPS for setting up that VPN connectionand not the one which by default Transit Gateway will provide otherwise.Right.So these are anycast IPS.That means they route the traffic to the nearest edge location.The IP still remains same, and it uses then edge locations to route your traffic to AWS region.Now another important thing for having the VPN acceleration you have to have Nat traversal enabled.Now what is Nat traversal?How it works.We are going to talk about that shortly, but just remember this thing further.There is always this IP negotiation that happens which means who initiates the VPN connection requestright?It can be started from the customer side, or it can be started from the AWS side.But for accelerated site to site VPN, it has to be started from the customer gateway side.But otherwise, if you are terminating your VPN on the Virtual Private gateway without acceleration,even you can configure that AWS will initiate the key negotiation.So this is another thing that you should know.And finally it comes with some additional charge.Now because the pricing change I'm not quoting how the charge is, but just remember that it cost youlittle extra when you enable the acceleration on your VPN connection, right?So I hope it is clear what is accelerated site to site VPN, and what is the purpose and what are someimportant thing that you should remember.So thanks for watching.And with that, let's talk about some other important features of AWS site to site VPN connection.So thanks for watching and let's go to the next lecture.Thank you.