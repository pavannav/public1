# 8.1 Introduction to VPC private connectivity options

Okay, so far our discussion is more towards what happens within the VPC and how you can create variousroute tables and everything for the traffic within the VPC.Or how do you take this traffic out to the internet.But now let's expand our networking beyond the VPC, and let's see how you can connect VPCs to othernetworks, for example, to other VPCs or to the on premises network, or say within the AWS.How you can reach to different AWS services, but not using the public IPS, but rather reaching overto using the private IPS.And this requires special networking plumbing between these networks.And in this section let's try to understand that.So in this section we are going to talk about how do you connect to VPCs and how do you then accessthe machines over the private IPS across the VPCs.Further, we are going to cover how can you reach to AWS services from the VPC without VPC having theinternet gateway or the Nat gateway?So if you remember in the first lecture of overview of AWS networking services, we had touched uponthe VPC endpoints.So let's understand about VPC endpoints.There are different types for example gateway endpoint interface endpoint and as well as interface endpointextension to the private link.So the fundamental question is what are the benefits of having this kind of private connectivity.So let's try to answer that.And then we will dive into individual networking components here.So why do we need private connectivity.That's because if there is no private connectivity the traffic will flow over the internet.And for that you would have to expose your application over the public IPS to the internet.So what are the disadvantages if you do that.So of course traffic flows over the internet and it is considered as less secure.Now depending on at which layer of networking you operate, you might have to then put additional layerof security.For example, you might have to encrypt the traffic at layer three or layer four, or at layer sevenusing Https, right.So you can do that, but there is additional overhead.Second, internet may not provide you the consistent bandwidth and the lowest latency because trafficmight be routed through multiple hops.And in that case, if there is a latency sensitive application, then it doesn't guarantee that it willhave the best user experience.So for that reason, also having private connectivity might improve the experience.Now, if you remember our Nat gateway discussion, you can provide the internet access to the instancesin private subnet using the Nat gateways.But there is additional cost, right?What if your instance wants to reach to some other VPC instance which is also in AWS?In that case, is there an option to bypass, you know, the Nat or internet gateway.So we will see what kind of architecture you can implement for this kind of use case.And as I said, you don't need to unnecessarily expose your servers publicly if they don't need to.So it is recommended that your blast radius is minimum by exposing minimum application to the internet.So in this section we are going to talk about how to solve this problem using various networking featuresof AWS.That is, VPC peering, VPC endpoints, and VPC private link.And I am not talking about the transit gateway so far because I try to put that in a hybrid networkingbucket.But Transit Gateway otherwise links everywhere, right?But in next few section, let's just try to focus on these three features okay.With that, let's get started.