# 12.3 Hands On- Setup AWS Site-to-Site VPN

Okay, so I hope you are ready to do this exercise that is setting up AWS Site to Site VPN.Now for setting VPN connection.You know that we need two networks.So we need AWS side of the network, which is AWS, VPC.And then we also need a customer network which will represent as another VPC.This is because we don't have actual customer network.But then we'll just create a VPC and we'll treat it as a customer network.So we can call it like simulated customer network.Now on the left side we will have a private subnet and EC2 instance.And this instance will just have a private IP address.Now you know that we can't reach to this instance from over the internet because there is no internetgateway.And EC2 also has a private IP.And the purpose of this exercise is to get access to this EC2 instance a privately from the corporatedata center.Now for this we need a VPN connection and on AWS site VPN terminates at Virtual Private Gateway or itis also called a VPN gateway.So you create a VPN gateway, you associate that with AWS VPC, and then on the customer side, youneed a VPN server.Now here we will just launch an EC2 machine, and we will install a VPN server on that machine.And then we will establish a VPN connectivity between a VPN gateway and the VPN server on the customerside.Now for that we will need EC2 machine.We need to connect to that machine in the corporate data center and then configure it as a VPN server.So of course we need a public subnet.That means we also need to have internet gateway attached to our VPC.And then we will have EC2 machine with a public IP.Once we have this EC2 machine, we will connect to it over SSH and install a VPN software.Now there are a lot of VPN softwares.So you can have Openswan, Strongswan, On one, or if you have access to the commercial softwares likeCisco VPN, you can also use that.Now for this exercise, we are using Libreswan because on Amazon Linux 2023 you can install Libreswaneasily and AWS Amis doesn't support Openswan.Okay, so once you have VPN software on the customer side, you will create IPsec VPN between thesetwo endpoints.And once this connection is there, then from this EC2 VPN machine we will ping to this EC2 with itsprivate IP.And if it is successful, that means our VPN is working as expected.So that's the goal of our exercise.Now, because you have done all previous exercises, I expect that just by looking at this diagram,you can at least set up this network the way it has been shown.Now how to configure a VPN server?I will share those steps, but at least looking at this network diagram.Try to implement this on your own.Now, what I have not shown in this diagram is just a routing table and the security groups.So let's just cover that and then we'll see the steps of configuring the VPN connectivity.So of course this local entry comes with the route table which is obvious.But most important is the second entry which says if the traffic is coming from or going to corporatedata center, then it should go through this virtual private gateway or a VPN gateway.So this route table entry is the most important one for the traffic to flow over this VPN connection.So don't forget about that.Now on the right side it's a public subnet.So you just need to have an entry which says all the traffic to internet goes through internet gateway.Now here we don't have this entry as virtual private gateway because there is no virtual private gatewayon the customer side in real world.And all the VPN traffic, as we just learned in the previous lecture goes over the internet.That means from customer data center side it goes over the internet, but just that the traffic is encrypted.So this routing table is good enough for the customer data center side.Right.So I hope that is clear.And just final thing, what about the security groups.So for this EC2 instance a the traffic can only be sent from the corporate data center.So you should have rules like ICMP that is ping allow from this network.And on the customer gateway side you should be able to SSH so that you can install the VPN server.And that's where SSH from either my IP or you can open it for world.And again from this EC2 instance, if you want to ping to this machine then you can also enable thissecond inbound rule.So these are the security group inbound rules for this machines.So that's it about the basic networking setup.And I hope you can do every step on your own and at high level.I have described all these steps in the slide as well.Now where you actually need help is understanding the VPN configurations and then creating the correspondingVPN configuration files.And for that, I have listed all the detailed steps in the PDF document, and this document is availablealong with this lecture.So do refer to that document right.So with that let's go to AWS console and perform these steps.And this time I'm going to perform all these steps really very quick because we have done that maybe100 times.Right.So in the VPCs I'm in Mumbai region I'll just create a VPC.I will call it VPC, AWS.Just copy this name and I will put my Cidr as anything which works for you and create VPC and in thisI don't need internet gateway.I just need a private subnet.So I will create private subnet.Let's call it private one and availability zone.I will select the first one because I want to launch T2 Micro instance, which is not available in anotherAZ of AWS Mumbai region and the Cidr range.I should be giving ten .0.0 slash 24.And create the subnet.Let's go to the route table.You know that by default it looks at the main route table.But it's always good to create a dedicated route table for your subnets.And I'm selecting the right VPC and creating the route table.After you create route table don't forget to associate that with the subnet.Many students miss these steps and then routing doesn't work as expected.So this step is important.Okay I'm done.On the left side of the VPC configuration.If you are not sure, just go back to the diagram and check.So I think we have done everything.Just this EC2 machine needs to be launched.So let's also do that.So let's go to the EC2 console.Let's open it in a new tab and make sure you are in Mumbai region.Go to the instances and launch an instance.Let's call it EC2 a and most of the settings as default, just that.Make sure you select the right key pair.Now this is part of your prerequisites how to create key pair and then how to save it.So please refer to that lecture.If not you can create a key pair here as well.And if your workstation is Mac or Linux, use PEM format as a download copy.Or if you are using windows like myself, then download it as a PPK file.Okay, so in network setting let's select our VPC and there is just one subnet.We don't need a public IP and the security group, let's call it EC2, a security group.And we need a couple of rules.We know that we are just going to have the ICMP IPv4, and it will only come from the corporate datacenter side which is this.So let's add that rule.I think rest of the settings are good enough.So let's launch the instance T3 okay.So that's done.Now let's go back to our VPC and let's do the VPC configuration for the another region which representsthe customer network.So this time I'm going to the North Virginia.And let's go to the VPC and create a VPC.Let's use the name as VPC as data center.And the Ssid is 192168.0.0 zero slash 16 and create a VPC.Now, if you don't know what to do, just refer back to the architecture diagram again.So if you see here we need VPC and we need a public subnet.So that means we need internet gateway as well.So let's do that.Okay.Let's create internet gateway again.Let's give it a name VPC d c g w.I'll just copy this name so that I can paste it while creating subnets.And let's attach it to this VPC.Okay.Let's go to the subnets and create a subnet in this VPC.Let's call it VPC DC public subnet.That's first subnet.So number one and you can create it in any availability zone.And the Cidr range which is the first one is 24.Okay let's create the subnet.And as a best practice let's also create our own route table.And again let's call it public route table.In our VPC and let's create it.Being public, we need to make sure that we add a route for the internet.And it goes through the internet gateway.And very important don't forget to associate this with the subnet that you created.Okay.We are good here.Just that one more setting you can do so that you don't forget to assign public IP which is auto assignpublic IP for this subnet.So let's go to edit and say auto assign public IP.You can avoid this.And while launching EC2 machine you can override this attribute.But this is better.Okay we have done that.Now let's launch another EC2 machine in North Virginia region.So again let's go to the EC2 console And launch EC2 machine, which acts as our customer gateway.So no instance is running.Let's launch an instance.Let's call it EC2 VPN.And this time we are selecting Amazon 2023 Ami.Now this is important because the VPN software also depends on what Amis you are using.So we are using the latest Amazon Ami which is available.And here instance type is T2 micro Key pair.Again you need to use what you have.If you don't have create it.And most important is network setting.Let's select our VPC with our public subnet and auto assign public IPS on.Let's create another security group.Let's call it EC2 VPN security Group.And if you know the rules, we need SSH.But from my IP so it automatically detects my IP.And then if you want to allow ICMP from the other network that is your AWS network.Then you should add this rule as well.Okay.Good enough.I think we are good.So let's launch this instance Cths.Now let's go back to the diagram and see if everything is there.So right.We have done most of these things.Now we need to create this virtual private gateway in the Mumbai region.Attach it to this VPC.And then we need to create a customer gateway.So customer gateway just represents the on premises network.And for creating the customer gateway you need to provide the public IP of this EC2 machine becausethat's the other end of the VPN.So let's copy this public IP because we'll need that.So this is a public IP.I'm just copying it here okay.Now I'm going back to the Mumbai region.And if you scroll down there is the customer gateway virtual private gateways.So let's go to the virtual private gateway and create a new gateway.So let's call it VPC a aws GW.Now rest of the settings will be default.So let's just create this.Okay.So make sure you see that.And it is in detached stage.So let's go and attach it to our VPC on AWS site.Aethes okay that's done now for VPN connection.As I said, we also need a customer gateway which represents the right side of the network.So let's create a customer gateway and let's call it um DC Customer gateway.Most of the settings will keep as default.Now IP address is the most important.So this represents the EC2 machine.So we need to put the public IP of that EC2 machine again rest of the settings as default.And let's create the customer gateway.Okay, it is created.And now the most important part is to create a site to site VPN connection.So let's go here and create a VPN connection.So connection is between AWS and DC.So let's give that name.The target type on the left side is Virtual Private Gateway.So let's select that and customer gateway.Also let's select this one.So make sure that you select the right one.Yeah IP address is the correct one.Now routing we need a static routing.As of now dynamic routing requires BGP.And for that your customer VPN server should also support BGP.But Openswan Strongswan doesn't support the BGP right.So only static routing is supported.That means you need to predefine the right side of the network Cidr range.So what are the prefixes on the right side.So we know that the right side is 192 160 800 slash 16.And here you will again tell what is the range of the Cidr.So you can either leave it blank or you can provide here the again right side of the network.192.168.00 16.And here the left side is AWS network range.Which is this.Right.Okay.So rest of the settings again will leave as default.But what you can do is you can have your Pre-shared keys defined if you want to do that.But let's leave it and let AWS generate that for you.And let's create a VPN connection.Rthy.Okay.So it will take maybe a couple of minutes for this VPN connection to be up by that time.Let's go back to the diagram and see if we are missing anything okay.So we have done everything I believe from this diagram.What we haven't done is probably on the routing side.If you just recall, we just have this private route table with local entry, but we don't have thisentry.So that's only the missing piece.The rest of the things are there.So let's just go to the private subnet route table and modify this entry.So let's do that okay.So let's go to the route table and.Select this route table.And here let's say if the traffic is going to or coming from this corporate data center network it shouldbe going through virtual private gateway which is this.And save the changes okay.All good I think we are good to now proceed with the next steps.So if you look at this document we have done all these steps up to maybe step number four.And now we need to configure the EC2 instance on the right side as a VPN server.But for this we need the VPN configuration file from the AWS.So we need to perform step five which is downloading the VPN configuration file.So let's do that.So let's see if site to site VPN connection is created.Yes it is available.So let's grab that and download this configuration.Now here you need to select the right vendor.So in our case it will be Libresse one.But Libresse one is just a fork of Openswan.So we can use this Openswan and download this.So I'm just storing it on desktop and just save it.And then you can open it this file in the notepad.So let me do that.So I opened it in a notepad.And this is how it looks.Basically it has all the information to set up a VPN tunnel and all these steps you need to performon your VPN server.Okay, so all these steps again are there in your document.So refer to those.First thing I need to do is to connect to VPN server over SSH.So let me do that.So of course for that I need a public IP of my VPN server.So let me take that and let me log in with Putty as my VPN client.I'll put this public IP and I'll go to SSH auth credentials and browse to my keys.It's in North Virginia region.So I'll just open it.And the user is EC2 dash user okay.So I'm into the VPN server.Now if you just go back in time then with Amazon Linux two Ami, you could install Openswan as a VPNserver.But then with 2023 Ami, AWS is not supporting Openswan anymore.So we will rather install Libreswan.And earlier we had to install the fedora repository because in Amazon Linux 23 there was no libreswanpackage.But now if you see the Libreswan is a part of the package, so instead of the command that you see inthe console, you can just use sudo yum install libreswan command.So the prompt will be a little different here.But please ignore that and it will take maybe a minute or so for installing the libreswan.So I will just pause this video until it happens.Okay, so it was quicker.So Libreswan has been installed and now let's do other configurations.Now next step is to have systemctl configuration file to have these entries.So let's do that.So let me do sudo VI slash etc. sysctl dot conf.And here we need to add those three entry.Now this is required for this machine to act as the VPN server and just escape colon wq.Okay.And then you have to do sudo sysctl minus p which applies.This changes basically.Okay that's done.Next we need to make sure that there is this file aecae IPsec conf.And it has this entry which is un hashed so that it can look at all the files which are present in IPsecdot d directory.So let's quickly check that.So I'm just doing VI to this file.And if you go at the end you will see this entry here which is not hashed out.So this is what we want.So we are not doing anything and we are just coming out of the console.Okay.So this setting is good enough.And now let's look at the next steps.Now here onwards you need to refer to the VPN configuration file that we downloaded.So let's open that file.So I'm here and it exactly mentions all these steps that we did.And additionally it says that now we need to create this file etc. IPsec doors conf.And then you need to update this with this data here.So let's do that.Let's do sudo VI.And let's put all these connection details for tunnel one.So I'm copying this as it is.You need to press I to get into the insert mode.And just right click to paste.Now the most important thing here is that this settings will be different depending on the VPN softwareand as well as Amazon Linux version.And I have to keep changing these settings because there are different versions of Openswan or Strongswanor Libreswan that you use in Amazon Linux.So as of now in this file you need to remove auth equal to ESP line and further for phase two algorithmand the AI key algorithm, you have to use these specific settings.So in my screen in the background you might not see the same settings because these have changed.So as of today just use the settings that you see in the document or on the text prompt on this screen.So please make a note of it okay.That is done.One other thing you need to do is to add this local network and remote network.Now remember that local network is the customer side of the network because you are configuring thisVPN server on the customer side.So the idea is 19216800 16 and the remote network is AWS network.So we have to provide the Cidr range of the VPC.Okay.That is done.And now let's create a secrets file.And this is used for the authentication of the tunnel.Now this value should be again taken from the AWS VPN configuration file.So let's refer to that instead.So here if you go it says create this file with this entry.So let me copy this and create IPsec dot d AWS dot secrets file.So let's do that.So sudo VI slash etc. IPsec dot t aws dot secrets.And let's add this entry here.Now the name of this AWS can be anything.Just that it should be same for both configuration file and the secrets file.So what I mean if you just see this this file name of AWS should be common between these two files.That's why it knows that we are referring to this credential for this VPN tunnel configuration okay.So everything is done.Now.The last step that we need to do is to start the IPsec service.And how do you do this.Just use this command systemctl start IPsec service okay.So let's do that.And it started how to check the status.Just replace this with the status command.And you can see that the tunnel has come up.It shows all these details.So if the response is something like this which means your IPsec tunnel is up and running okay let'sgo back to AWS console and see if we see the tunnel as up.So here let's go to the VPN connection which we had created in Mumbai region.And if you now go to the tunnel details I think it might not have refreshed.And you can see that one tunnel is up.Now, it may take maybe a couple of minutes for this tunnel to be up.So just be patient.But if it doesn't work, um, after say five minutes then probably there is some problem.So in that case re verify the steps.And I have also provided the troubleshooting guide okay.So the tunnel is up.So last thing that we want to check is can we connect to the EC2 instance in Mumbai region over a privateIP address.So let's pick this private IP address and let's check that from our VPN server.Let me clear the screen okay.And just let's ping to this instance.And you can see that the traffic is flowing.That means I am successfully able to connect over the VPN server.You can also verify that by looking at the VPN CloudWatch logs.There are metrics like how many bytes in and out of the VPN connection, but I'll defer it to you tocheck, right?Okay, so we are able to connect.Everything is working.If you could do this, congratulations.It's a big thing.And that's it for this lecture.Now before we wrap up this lecture, I just want to make sure that you terminate everything becausethere is a charge of VPN connection per hour and it doesn't come under the free tier quota.So don't forget to terminate everything.Right.So let's see what those steps are.So for deletion first terminate both EC2 instance.Delete the VPN connection.Then detach your VPN gateway with the VPC.And then you delete the VPN gateway customer gateway and delete both the VPCs.So make sure you do this.Okay, so I hope that is clear.And you enjoyed this lecture.Again refer to the PDF document.It has everything and it also has the troubleshooting steps in case your VPN connection is not working.Thank you.