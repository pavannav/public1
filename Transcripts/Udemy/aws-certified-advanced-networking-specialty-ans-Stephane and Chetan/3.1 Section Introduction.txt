# 3.1 Section Introduction

Hello and welcome to this first section of this course, Amazon VPC fundamentals.Now, if you are planning to go for your AWS Advanced Networking Certification exam, then I expectthat you already know everything that we are going to talk in this particular section, but many a timesstudent directly come to this course, and that's where it becomes important to cover the basics ofthe VPC.But if you have already previous experience working in AWS and if you know these concepts already,then I would suggest that you directly go to the next section that is advanced VPC features, and thatwill definitely save you 2.5 hours of your time.But if not, or if you want to refresh your knowledge about the VPC, then feel free to go through thisentire section.Okay, so what we are going to see in this section is basically what is VPC.So we will start from the scratch.Then we'll try to put a perspective around how different AWS services has a scope with respect to theirplacement in the region or availability zone, or at the VPC level.This becomes very important, especially if you are just starting with AWS.For example, if you are launching EC2 machine, then you should really know that it is bound to AZand then you are creating that inside the VPC and that VPC is inside AWS region.So I think this kind of relationship helps you build that logical picture in your mind around the placementof different AWS networking services.Then we will see what all AWS services typically resides inside the VPC and what are outside of yourVPC.And then the most important part, the VPC addressing.How do you use classless Inter-domain routing to address your VPC and what that Cidr prefixes mean,and how many IP addresses your VPC can have.Further, we will talk about everything about the VPC routing.How do you create the public subnet private subnet how you can reach out to internet.So we will cover that in the VPC route table and subnet lecture.Then we will talk about different type of IP addresses that you can have in AWS.So essentially with respect to the IP address scheme there are IPv4 and IPv6 addresses.But then further you can have private IP addresses or public or elastic IP addresses.And then we will cover the very basic fundamental firewalls inside the VPC which are called securitygroups.And at the subnet level we can also have next level of firewall called network ACL.So we will talk about that.And then we will close this section by visiting the Nat concepts.And in AWS world there is a service called Nat gateway which provides the outbound internet connectivityto your machine which you launch inside the private subnet.But don't worry, we are going to see everything that I just mentioned and there are a lot of handson labs as well.So again, if you are not familiar with these concepts, also feel free to do these labs on your own.So let's get started.