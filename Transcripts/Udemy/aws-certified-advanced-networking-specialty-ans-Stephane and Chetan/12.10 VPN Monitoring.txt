# 12.10 VPN Monitoring

Hello and welcome to this lecture.Now let's see VPN monitoring.So, it is important that you knowwhich CloudWatch metrics are availablewhen you're using AWS Site-to-Site VPN.And this would also be a part of your exam questions.So, let's understand there are not manybut those are important to remember.So, you can do VPN monitoring using CloudWatchbecause AWS Site-to-Site VPNis a native service by AWS.It uses virtual private gateway.You can very well get some metrics fromthis virtual private gateway using CloudWatchand then you can do all the regular stuffthat you typically do.For example, you can create alarmsand then you can get notifiedwhen something goes wrong.So, in particular, AWS Site-to-Site VPN providesthree important CloudWatch metrics.Very first is TunnelState.So, the state of the tunnel,which indicates whether tunnel is down or it's up.So, zero indicates that tunnel is downand one indicates that tunnel is up.Now this TunnelState could also have different values,anything between zero and one.And in those cases it would indicatethat at least one of the tunnel is not up.So, zero means both the tunnels are down.One means both tunnels are up.But if it's between zero and onethat may mean there is one tunnel which is not up.So, that's a TunnelState CloudWatch metric.Then there is TunnelDataIn,the bytes that through the VPN tunnel.And then TunnelDataOut,the total bytes sent through the VPN tunnel.All these metrics are collectedby default using CloudWatch.You don't have to do any additional settings for this.And as you have these metrics availableyou can definitely create a CloudWatch Alarm.For example, when TunnelState goes downthat means value becomes zero,you can say:set an alarm, send a notificationvia AWS notification service, that is SNS,and you can send either email and SMSto the receiver or administrator.So, remember these three CloudWatch metrics:TunnelState, TunnelDataIn and TunnelDataOut.Those are available for you to monitor via CloudWatch.Now, there is one more way in which you can dothe monitoring of your VPN tunnel,and that's through a Health Dashboard.Now, as you know,AWS provides something called Personal Health Dashboard, PHDand any changes to the VPN tunnelyou can see that automatically comingto your Health Dashboard.So, AWS Site-to-Site VPN automatically sendsthe notification to AWS Personal Health Dashboard.And whenever there is a,say tunnel replacement notification,for example, out of two tunnelone tunnel goes down for maintenance,then that notification would also come here.And if there is, say out of two tunnelonly one tunnel is working.And how it detectsthat if one of the tunnel is downfor more than one hour in a day,it would consider that your VPN is workingonly on the single tunnel.Now, that notification,in terms of single tunnel VPN notificationwould also be coming here in the Personal Health Dashboard.So, these are the mechanism through which you can monitoryour Site-to-Site VPN tunnel.More important is your CloudWatch metrics.Remember these three CloudWatch metricswe just talked about.That is TunnelState, TunnelDataIn and TunnelDataOut.So, that is what you want to rememberfor monitoring your VPN tunnel,and thanks for watching.