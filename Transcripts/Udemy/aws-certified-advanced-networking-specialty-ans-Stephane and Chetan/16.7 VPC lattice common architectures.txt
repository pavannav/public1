# 16.7 VPC lattice common architectures

Okay.So in this lecture let's see some of the common VPC lattice architecture that you will see in the realworld.And these architectures might give you good understanding when the problem is given to you around theVPC lattice usage in your exam.Right.So there are typically four architecture that we are going to talk about.And we have already talked about that in the previous lecture.So it should be very easy for you to understand right.So very first and a common architecture is to allow the connectivity between different VPCs.And we have already done the exercise around this.So nothing very special about this architecture as you can see here.VPC lattice is in the middle.And then you connect your VPCs services with the VPC lattice network.Now important thing here to look at these arrows.So the green arrow represents the VPC association.So the client in that VPC can access the services.And here as you can see both VPCs are connected using this VPC association.That means all services and client can access the VPC lattice network and the services.Right.So a very common architecture that you will see.And if you want to read more about this architecture then you can visit this AWS blog.Right.Now the next architecture.If you want to access VPC lattice services from your on premises network.And if you remember, we said that if you want to access VPC lattice from outside the VPC, you haveto use VPC endpoint for the VPC lattice service network.So if you see this is my ingress VPC.And in that there is this VPC endpoint.And this VPC is connected with the on premises network using over say IPsec VPN or Direct Connect orsay Transit Gateway.The point is, you should be able to access the private IP of this VPC endpoint from your on premisesnetwork.And if that connectivity is there, then you can access the VPC lattice services.So this is another architecture that you will see right.So in this architecture our services are hosted in AWS and we are accessing it from on premises.But what if we want to use VPC lattice.But the targets are there in the on premises network.And you know that VPC Lattice Services supports IP address as a target.And that IP can be from your on premises application.So in that case the architecture will be something like this.Now here in this VPC we are saying that VPC lattice has the target and the target IP or DNS is therein the on premises data center.And there is a connectivity between your this VPC and the data center again over a VPN or a direct connector a transit gateway.Now another way to do that is you can also have your VPC lattice service behind NLB or Alb.And for NLB and Alb the target can be the IP address.So if you want to load balance the application then you can also use this NLB or Alb here.So this is the other way around where your target is sitting in the on premises network right?So I hope it is clear.Then the final architecture is when you want to access the VPC lattice services from the internet rightnow for that as well.Again, first thing you need to create the VPC endpoint for the lattice network.And now you have to expose this endpoint to the internet.And how do you do that.So you would have to bring in some publicly facing entity.And that could be your network load balancer or the application load balancer which are sitting in thepublic network.Then through the internet gateway, the traffic will go to the load balancer and the target will bethe IP address of this VPC endpoint.Right.And this is how you expose your VPC lattice services to the outside network.Okay.So these are the very common architecture that you will see with VPC lattice.And I hope it is clear.So with that we will stop here.And in the next lecture we will just quickly summarize VPC lattice service.So thanks for watching.And let's go to the next lecture.