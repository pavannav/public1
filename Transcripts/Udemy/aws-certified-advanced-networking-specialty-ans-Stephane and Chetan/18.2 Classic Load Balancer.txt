# 18.2 Classic Load Balancer

Okay, so now let's talk aboutthe Classic Load Balancer.So this is what the architecture does look likewith a Classic Load Balancerwhere the clients connect to the CLB directly,which can be spread across multiple AZs.And then we have registered instancesin each Availability Zones as we want,and the idea is that the CLB will forward traffic.We'll send traffic to these EC2 instancesif they're deemed healthy.Else, if they're not healthy,well obviously the traffic will not be sent to them.So the CLB operates at the Layer4 and Layer7,and in terms of supportive protocols,we have HTTP, HTTPS, TCP,and then Security CP, also called SSL/TLS.The EC2 instances are registered directlywith the Classic Load Balancers,so there is no concept of target groups,and that's because target groupswere a concept that were inventedwith the second generation load balancers,such as the ALB or the Network Load Balancer.The health checks can be of multiple typesof HTTP, HTTPS, or TCP.And the Classic Load Balancer is the only load balancerthat supports the EC2-Classic networks as opposed to VPC,but this is a deprecated network.So it's just something to know,but the CLB is also sort of a quote, unquote,deprecated load balancer,so it goes alongside a deprecated network mode as well.Okay, so now some very important things to know.So the client is connected to the Classic Load Balancerthrough a listener and the Classic Load Balanceris connected to your EC2 instancethrough an internal traffic, okay.So you have different combinations you can dofor these things.And they are summarized here in a table.So we'll read the lines one by one,but they should make sense.And the reason I'm saying this is that, in the next slide,we'll see a very specific use case for itthat does come up in exam sometimes.So the listener can be HTTP.That means that the client will connect unsecurelyto our load balancer.But from an internal perspective,the CLB can connect to our EC2 instances using HTTP,so unsecured traffic, or HTTPS.And if we do want to use HTTPS,then our EC2 instancesmust have certificates installed on themto allow the connection to be encryptedbetween them and the Classic Load Balancer.Also, if we set up the listener to be HTTPS,then we have SSL Termination happening on the CLB directly,in which case we need to install an SSL certificateon the CLB.And then from an internal communications perspective,this iS the same as before,the CLB can connect using HTTP or HTTPSto the EC2 instances,but remember, there are two connections,because the first connectionwas terminated directly on the CLB.Now, if we go one level lower,so if you go to the TCP type of connection,so TC clients can connect directlyusing the TCP protocol to your CLB,and your CLB can send either TCP trafficto your EC2 instance or SSL/TCP secure trafficto your EC2 instance, in which case, yet again,you must install the proper certificateson your EC2 instances.And finally, you can also have the SSL terminationhappening directly at the Layer4 on your CLBand the internal communication to be TCP or SSL againfor your EC2 instance.So this table should make sense,but there are a few considerations to take into account.So if you enable HTTPS or SSL on your EC2 instances,this is what's called backend authentication.That means that there is an authentication happeningbetween your Classic Load Balancerand your backend EC2 instances,thanks to these certificates,because you have to load the public certificates on your CLBand the private keys certificates on your EC2 instances.Also, if you have TCP to TCP traffic,then this is a very special use casefor the Classic Load Balancer.And in that case, if you do have TCP to TCP,then there is no termination of connection happeningat the CLB level.That means that all the traffic will be passed on directlyto the EC2 instances.What this means is that, as you can see here,the CLB actually doesn't do any termination.It will pass them to traffic from the usersto the registered EC2 instances.And why do I say this?Well, because this is the only wayto do two-way mutual SSL authenticationin which the users have to authenticateto the EC2 instances,but also the EC2 instances have to be authenticatedto the users.So in this case,we'll have SSL termination happening on the EC2 instances,and the traffic is going to go across the whole way,and we can enable mutual SSL authentication.And this is the exam casethat can come up in the exam, okay?So that's all you need to knowfor the Classic Load Balancer.I hope you liked it,and I will see you in the next lecture.