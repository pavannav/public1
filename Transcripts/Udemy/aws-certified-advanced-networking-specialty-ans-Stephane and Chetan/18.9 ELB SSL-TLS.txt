# 18.9 ELB SSL-TLS

Okay, so let's talkabout SSL encryption for your Elastic Load Balancer.So this is our classic architecture,and the idea is that you could have terminationat the ELB levelby having an SSL or TLS certificate loaded onto it.In which case, while our users will access our ELBusing an encrypted connectionfor example, HTTPS, but there is going to be a terminationbecause then the ELB will just send HTTP trafficover your prior VPC into your EC2 instances.So you would load an SSL or TLS certificateon the ELB and to manage these certificates,you need to use ACM, which is AWS Certificate Manager.You can upload your own certificates as well onto ACMand then have them used by the ELB.For your HTTPS listener,you must specify a default certificateand you can have as well an optional list of certificatesif you wanted to support multiple domains.And as we'll see,there's something called Server Name Indication, SNI,to allow clients to specify the domainthe hostname they're gonna trying to reachand therefore your ELB will be able to send backthe correct certificates.You can also specify an SSL Security Policyfor compliance features, compatibility, or securityas we'll see in the very next slides.So now let's talk about SNI or Server Name Indication.The idea is that SNI will solve the problemof loading multiple SSL certificatesonto one single web serverthat will be serving multiple websites and domains.So we have our ALB and say, we have two target groups.One is www.mycorp.com, which has EC2 instances.And the other target group will be serving demo.example.comwith other sets of EC2 instances.The idea is that on our ALBwe're going to have routing based on hostname.And so therefore the hostname when indicatedwill be served by the right target group.Now, we're also going to load a lot of certificateson the ALB.So we're going to load demo.example.com,but also www.mycorp.com as well as maybe other onesand a default SSL Certificates.What's going to happenis that a client when it connects to your ALB,will indicate the server name, the hostname.So I would like to connect to www.mycorp.comand thanks to the SNI feature,the ALB will know which SSL certificateto use to establish the connection with the clientsand make sure it is correctly encrypted.So SNI is quote in quote, a newer protocoland requires the client to indicate the hostnameof the target it tries to connect toduring the initial SSL handshake.And therefore,the server will find the correct certificates.This only works therefore for the newer generations typesof load balancer, such as the Application Load Balancerand the Network Load Balancer.Now, these SSL certificates can be loaded onto the CLB.So the CLB supports only one SSL certificate.And if you wanted to support multiple hostnamesthrough your CLB, you would need to use SAN.So Subject Alternate Name on your SSL certificates.But the idea is that anytime you will needto add or remove a domain name,you will need to edit the SSL certificate with new SANs.And so therefore it is a complicated processbecause you will need to reissueand reassign these certificates over time.Therefore, sometimes the best option is to use multiple CLBsfor multiple hostnames with multiple SSL certificates.But if you can transition awayfrom the CLB and use the ALB instead,because the ALB has the Server Name Indication featurethat we just saw that will simplify your life.So talking about the ALB,it supports multiple HTTPS listenersand multiple SSL certificates.And then in order to make it workwith multiple hostnames,then we're going to use this Server Name Indication feature,the SNI feature we just saw.The NLB is similar.It supports multiple listenerswith multiple SSL certificates.And we're also going to use SNIto serve the correct SSL certificates.Now, finally, it is possible for youto set up a security policyfor your HTTPS or your SSL listener.So security policy is a combinationof SSL protocols, SSL ciphersand Server Order Preference optionthat's going to be supported during the SSL negotiation.That is when your client is connectingto your server and establishing a secure connection.So you can use some predefined security policiesfor example, the ELBSecurityPolicy-2016-08and then apply it to your load balancer.So if you consider the Application Load Balancerand the Network Load Balancer,then in case of frontend connections,so connections from the client to your load balancer,then you can use a predefined security policy.And for backend connections,then you must use the ELBSecurityPolicy-2016-08.It's going to be always used.So what can we set for predefined security policy?We have different ones.We have the default I just showed you.But also you can use the ELBSecurityPolicy-TLS policies.So in this case, you must meet complianceand security standards that requireto only use certain TLS protocol version.That may be something internally within your company.And also if you wantedto sometimes support older versionsof SSL and TLS for your legacy clients and application.The other kind of policies you may wantto use is the ELBSecurityPolicy-FS.And this is when you require Forward Secrecy.So the idea is that with this,you're going to get additional safeguardsagainst the eavesdropping of encrypted databy using during the negotiation,a unique, random session keyand that can come up in the exam.So anytime you see,how do we use a unique random session key for your ALB,think of a Forward Secrecy policy for your load balancer.Okay, so that's it for this lecture.I hope you liked it.And I will see you in the next lecture.