# 3.19 Exam Essentials

Hi all, welcome to this lecture.This is essentially the last lecture of this section,so we are going to summarize what we sawand we will also talk about the important thingyou need to remember for your exam.So we started with,how to map your physical network to virtual network,how the lands and the routersrelate to the network inside VPC.Then we saw the VPC and its components,the placement of VPC in regionand how VPC spans across multiple Availability Zones.Then we started with how to address the VPCusing the CIDR notation.And then we saw the significance of a prefix,how does it resolve the bitsfor your network addressing and rest of the bitsare used for the host addressing inside the VPC.Then we saw how the VPC can be subdividedinto a smaller networks in the form of subnets.And then VPC has a default main route table,but you can create a custom route tableand then attach those route tables to subnetso that you can take a better control over which trafficand how the traffic should be routed inside your subnets.We also saw that we need to have Internet Gateway attachedto our VPC so that it can communicate with the internet.So Internet Gateway has to be createdand then attached to your VPC.Then we saw different type of IP addresses,we started with IPv4 and IPv6 addresses,and then we also saw private, publicand elastic IP addressesand what are the differences between those.We also saw how EC2 instance get IP address,that is using the elastic network interface,and then we saw a couple of featuresof elastic network Interface,as in, it depends on your EC2 instance type,how many network interfaces you can attach.And also every network interfacecomes with one or more secondary private IP addressesas well as elastic IP addresses,so that's what we saw in this section.Later then we moved to firewalls,and inside VPC, we saw two kinds of firewalls,security groups and network ACLs.And we saw the placement of these two firewalls,and the differences betweenthe two importantly security group worksat instance-level and network ACLs works at subnet level.security groups are stateful, network ACLs are stateless.Security group can only have the allowed rulesand the network ACL can have both allow and deny rules.And then we also saw how the trafficcan be restricted using the network ACL.That means you can block certain IP addresses,which cannot be done using the security group.Then we actually set up the labs where we created VPCand public subnet and then private subnet,and we saw how to reach to the instance in a public subnet,and from there you can reachto the instance in private subnet.Also, we saw that private EC2 instancescannot access the internet,and the solution to that was NAT,so we understood how the NAT functionality worksand then AWS provides a managed NAT servicein the form of NAT Gateway.And then we created a NAT Gatewayand modified the route tables,for your private subnets to direct the trafficfrom those subnets to the internet via NAT Gateways.In certain cases, you might need NATas a your own EC2 instance.And we saw you need to take care of certain things,like for example,you have to disable the source destination check.Otherwise EC2 instance only accepts the trafficwhere the destination address is its own IP address,so we have to disable that setting.So in short, these are the very basic things about the VPCand you should be very well aware of all this conceptso that you can choose a right answer during your exam.With respect to the exam essentials,quick recap again, for VPC and the subnet,the maximum size of CIDR could be /16,and which allows you to create two raised 16,that is 65536 IP addresses inside the VPC.Similarly for subnets and VPC, the minimum size is /28,which means two raised to four,that is 16 IP addresses can be allocated,but those 16s are not usable of course,five IP addresses are unused in any given range,first four IP addressesand the last IP addresses is reservedand you cannot allocate that to EC2 instance.Now subnet is associated with Availability Zone,that means one subnetcannot span across multiple Availability Zone,however, one AZ can have multiple subnet.And we saw that in logical diagram as well.Now, all subnets by default followthe VPCs main route table.That's what we saw, we started with,but good idea is to havethe dedicated route table for your subnetsand then you modify the routes thererather than modifying the main route table.All the route tables have a default local routes,which is for which the destination addressagain the VPC CIDR, which allows the communicationbetween all the subnets within the same VPC.And whether you have the main route tableor custom route table, this local route will always be thereand you cannot remove that.That means by default at a network layer,at least all EC2 instances within a VPCcan communicate with each otherirrespective of, in which subnets they are created.So additionally, you need to know few more things,for example, if you want to block the IP address,you can't do that with security group,you have to do that with network ACL deny rule.Security groups are stateful,you'd no need to have explicitly outbound rulefor incoming return traffic, right.And in case of network ACL, they are stateless.That means for return traffic as well,you need to open the outbound ruleand typically it will be open for if few middle port range,and that's what we had seen.Now NAT Gateways or NAT Instance,must be created in public subnet,and they are highly available within the same AZ.That means if you need to have NAT Gatewaysto be resilient to AZ failure,you need to have multiple NAT Gateways across those Azs.NAT Gateways does not have security groups,and hence that traffic can only be controlledusing the network ACL, which works at subnet level.You don't get access to underline NAT Gateway machine,it's completely managed by AWS.So NAT Instance is a kind of replacementand it may be a cost effective solution,but it is not as performant and as resilient as NAT Gatewaybecause NAT Gateway is completely managed service.And remember for NAT Instance,you have to disable the source destination checkat that EC2 instance-level.So essentially these are the important thing you needto remember for the exam from this section,and with that, we will end this sectionand now we'll get into the advanced VPC concept.So thanks for staying during this lecture,and I hope you understood what we covered in this lecture.So thank you for watching.