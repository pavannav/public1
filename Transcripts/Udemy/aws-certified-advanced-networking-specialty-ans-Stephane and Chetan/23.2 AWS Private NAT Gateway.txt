# 23.2 AWS Private NAT Gateway

Hello, and welcome to this lecture,Private NAT Gateway.Now, I'm sure you've heard about NAT gateway,and it is used for giving the internet accessto the EC2 machineswhich are launched in a private subnet.And you very well know how NAT gateways work,but this is a private NAT gateway.And you must be wondering why this was introduced by AWS.Now, there is a special purposewhy private NAT gateways were launched.And from the name, as you can case,it does not provide the internet access to the resources,but then it still does the NAT-ing,network address translation,but it translates one private IPto some other or its own private IP.Now, what are the situation in which you will require that?So let's try to understand that first.So first of all,what's the problem with the private IPs?Now, you know that there is very well defined RFC1918 rangeswhich every company would useand allocate to their private networks.And there are different rangeslike 192.168, 172, 10.0.0.0/8.All these ranges are standard ranges.So in every organization,whether it's in a cloud or it's on-premise network,it will use this private IP address rangesfor all their servers.Now, whenever they wantto expose their servers to the internet,then comes the public IP in the picture,but otherwise,all these servers would have the private IPs.That means, in most of the cases, these private IPsneeds to be allocated again and again to different networks,even if they plan it well in advance,over the time as company grew,it's not possible that these private IPs are uniqueacross all your organization and organization units,because they work independently, right?So often it is requiredthat different business units which you have,they should be able to also communicate with each other.And also with microservices coming into life,more and more applications are microservices based.Every microservice servicewould have its own private IP address.And that means more and more private IPs are required.So what does it cost?Basically, if there are multiple business unitsand a lot of private IPs are used,then there is a problem connecting these networks.And the primary reasonis they have the overlapping CIDR addresses.So if one company has two business unitsand both business units are using, say, 192.168 range,it is difficult to connect this networkbecause IP addresses are same at both end.Now, what is the solution for this?Now, in the cloud world,we have seen that you could very well use AWS PrivateLinkbecause then you are really accessing the servicesfrom other VPC through the VPC endpoint ENI,which is provisioned into your VPC,and you don't really needto know the IP addresses on that side.So that's one solution,but, again, it primarily works in AWS.And you could also make it work in a hybrid world,but then it's purely AWS construct.And it is not feasible in all the cases.Then you could also use IPv6 addresses,you know IPv6 addresses are 128 bit in length.So you would have plenty of addresses to allocatebut then IPv6 addresses are globally public.That means you can't have private IPv6 addresses.Now, again, you are wasting a lot of addressesif you're locating these addresses to your internal network.And then another solutionis you have your self-managed NAT-ing appliancesand you do, you know, static kind of NATevery time you go from one network to another network.But then for this,you'd have to manage your own appliancesand additional operational overhead is there.So, to solve this problemwhere you connect two private networks,which has overlapping CIDRs,you can use private NAT gateway.Now what it does, it allows communication between the VPCsor VPC and on-premise network having the overlapping CIDRs.Now, networks which are in AWSshould be connected to on-premise networkusing the virtual private gateways or transit gateways.And as I said,private NAT performs the private IP address translation.This is not private to publicbut this is private to private.So we are not going to internet,but we are just going to another networkwhich is connected to your primary network.So for deploying private NAT,you would often need to divide your address spacein routable and non-routable address space,know what that means.If both side of the network has the same CIDRor overlapping CIDR,they are called non-routable CIDR,because you can't go from one network to another,because if you're trying to reach some machinewhich is available into your own network,it will be routed there itself, right?Now in order to make this connectivity,you need additional routable CIDRs.And what I mean, I'll just explain by this diagram.So let's take an example.You have two VPCsand both are having the same IP address range,100.64 and 100.64.And then you have private subnetswith the CIDR range prefix of 24, here and here.And now instance Awants to communicate with instance B.Clearly, this IP addresses are overlappingand then these are non-routable.And this is business unit one,and this is business unit two.Now, how do you solve this problem?Even if you connect these two VPCsover a VPC peering connectionor VPN whatever mechanism,but still, A cannot go to the Bbecause IP address is a local IP address from A.Now, if you look at the route tables for this,then you would understand that.Now in order to solve that problem,you'd have to bring another CIDR range to your VPCand you know that you can extend your VPC CIDRs.You can add secondary CIDRs.If you go back to some sections, we have covered this topic.So here, now we are adding one more CIDR to our VPCwith routable IP address range.So, which are not overlapping that means.So 10.0.1.0/24 here,and 10.0.2.0/24 here.That means you can gofrom this address range to this address rangebecause they are not overlapping.So this is one essential stepwhere you bring additional VPC CIDR,which are non overlapping.And now if you look at the route tablesfor this private subnet,you just have two local routes,one for this VPC CIDRand this is for secondary VPC CIDR.And then for this subnet,you again have the same route table.Similarly for another VPC.Now, if you have this architecture,you first need to connect these two VPCs together.Now, how do you connect that?You could do that using VPNor better ways, using the transit gateway.So now you create transit gatewayand you know that transit gatewaycreates transit gateway ENIs into the subnet.And now these subnetswill be your routable subnet with secondary CIDRs.So there are two ENIs now.Okay, so far, so good.And then you would have the attachment route tablesand we would have just common route tablefor both the attachment,which says how to go to VPC Aand how to go to VPC B,and that is through corresponding VPC attachment.So there is no special thing about this route table.Okay.Now still, can you reach to this VPC?No, you can't,because instance A wants to talk to instance B,but then IP address of these instances are overlapping.So now what you do is you introduce private NAT gateway,which is, again,sitting either in the same route table as TGW ENIor you could have a separate subnet.But for simplicity,they're just showing both in the same subnet.And additionally, what you would have to do is,you have to modify the route table for your subnet A,which is your non-routable subnet,and say if the traffic is going to 10.0.2.0/24,which is this subnet,it has to go through the NAT gateway.Now, we haven't really added the entry for 10.64,where exactly the B is running,because we can't directly reach to the B.We first have to reach to this subnet on the right side,and then from there,we should be able to reach to instance B.So what we are saying is,if traffic is going from A to 10.0.2.0/24,it has to go through the private NAT gateway.Now, as soon as NAT gateway gets the traffic from A,it would then do the network address translation.And we'll see how.Now, for this subnet, we have added a route tablewhich says if the traffic is going to 10.0.2.0/24,then it has to go through transit gateway attachmentwhich is obvious.Similarly, on this side, if traffic is going to 10.0.1.0,then it has to go through the transit gateway.And for this subnet just the local entries.So far, so good.That means from here,we can reach up to at least this subnet,though not the instance B.So what's the solution?If traffic is coming in this subnet,it should automatically go to the B.Maybe you have guessed already?For this, we need ALB, application load balancer.Now, if you look at this architecture,we have assumed that instance Ahas the IP address 100.64.0.10and instance B has 100.64.0.11.Just an example, it could be 10 as well.Doesn't really change anything here.But now if instance A tries to go to instance B,it can't directly go,it has to make an API callor network connection to ALB.Now, ALB has some addressout of this private IP address range, 10.0.2.0.So it would have some IP address, right?So instance A invokes an ALB, DNS rather,DNS will be resolved to some IP address,let's assume the IP address of ALB as 10.0.2.10, here.Now, if you trace down this packet,so A will look at the route table,it has to go to 10.0.2.10.So it has to go through the NAT gateway.The traffic will go to the NAT gateway.NAT will perform the network address translation.So source IP was a 100.64.0.10, which is address of A.It will just replace it with its own IP address.Now the NAT's own IP,private IP address is 10.0.1.15, just an assumption.And then with this route table,it has to go to 10.0.2.0.That is a destination address.So it has to go through transit gateway ENI.It will go to the transit gateway ENIand the transit gateway ENI has a local routewhich says if traffic is 10.0.2.0.It has to be routed through the local router.It will send it through ALB.And ALB has instance B registered as a target.So ultimately it will reach instance B.Now this is how entire flow worksin case of private NAT gateway.But another question is, does it also allows youto have instance B talk to instance A?So, the answer is no.This is one way traffic as of now.That means you get the written trafficbut A should initiate that traffic.If B initiates that traffic, it doesn't happen,because there is no ALB here which distribute traffic to A.And in that case, you'd have to do exactly opposite.So you would have to just create a NAT gateway in VPC B,in this private subnet, and ALB in VPC A.So if you need both direction connectivity,that means anyone can originate the traffic,then you would need two NAT gateways in both the VPCsand then two ALBs in both the VPC.And ultimately, this is how the end to end flow works.So I know that this is little complicated,but if you just go through this again probablyand just try to understand what's happening,this makes sense.And this is how the problem of overlapping private CIDRis solved using the private NAT gateways.Hope that is clear,and you enjoyed the lecture,and thanks for watching.