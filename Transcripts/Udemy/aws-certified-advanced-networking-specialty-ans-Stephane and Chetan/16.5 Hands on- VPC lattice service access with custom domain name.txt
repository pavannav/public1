# 16.5 Hands on- VPC lattice service access with custom domain name

Hi.Now let's do the exercise for VPC lattice.And this exercise is very similar to what we just saw in the traffic flow lecture.Right.So on the left side we will have the client VPC in which we will have a simple EC2 instance from wherewe will check the connectivity to the VPC lattice service.And on the right side we have two targets.One is inside the VPC.And that's a simple EC2 instance with a web server.And it will listen on port 80.Then we have another target which is the lambda function.And then we will have VPC lattice service which will route the traffic to corresponding target groupbased on the URL path.For example, if you enter say service name slash EC2 then it goes to this target.And if it is slash lambda then it goes to this target right.So we will create this app service.We will register this target groups and targets.We will create a listener.And then we will associate this app service with the VPC lattice network.At the same time, we will also associate this client VPC with the VPC lattice network using VPC association.And we want to access these services using the custom domain name.And that domain name could be say example.com.So we will create route 53 private hosted zone.And in that we will create corresponding records.So this is the premise of this exercise.And to be honest just looking at this architecture you should be able to implement this.And for that I have also provided high level steps here.So you can follow these steps and try to implement this.Now a few interesting thing here that you should note is that we are creating both the client VPC andthe application VPC with the same Cidr range.So VPC lattice solves the problem of overlapping cidrs right.That's one thing.Second, please pay attention to the security groups because as we talked in the earlier lecture, thereare different security groups that comes into play.So you have to make sure right, inbound or outbound rules are there.Right.Okay.So that is what we are going to do in this exercise.And just to save some time, what I have already done, I have created just these two VPCs.And in that I have just created two public subnets so that we can launch our instances.Now to be honest, we don't need these instances to be public, but just because we want to connectto this instance to test the connectivity.And also for app instance we have to install the web server.We are going to launch that in a public subnet.But as I said you can very well have them in private subnet.But then you will need Nat gateways and all those things.So to simplify the architecture we will create that in the public subnets.Okay with that.Now let's go to the AWS console and let's proceed with the steps.So as I said these VPCs and public subnets are already there.I'll show you that first.And then I will launch two EC2 instances in these two VPCs a client EC2 and the application EC2.Okay, so as you can see here, this is client VPC and the Cidr range is this same for the applicationVPC.Then I have two public subnets and you can very well check the routes for this public subnet.And it is nothing special right.It's a normal public subnet.And corresponding route tables are there and internet gateways are there.So all very simple right.Next as I said we will create the client EC2 and the app EC2 in corresponding VPCs.So let's go to the EC2 and launch the instance.Let's call it say client EC2 and we will go with the key pair.Then network.We want to create that in the client VPC.And I want a public IP so that I can connect and then can initiate the request to the VPC lattice later.And the security group wise Twice.I just need simple ssh.Right.Because that's what we are going to use for connection.And then we will initiate the ping request or HTTP request from here.Okay.That's it I don't think I need anything else.And let's launch the instance.Cool.Let's go back refresh.And now let's launch our app EC2 instance in the app VPC.So there is just one subnet.We want public IP here as well.And and we want to create a new security group.So let's call it app SG.Now with respect to its uh rules.So we want to connect over SSH so that we can install the web server.And additionally we want as of now HTTP traffic to be tested so that we can check whether web serveris installed properly or not.And later we can remove this rule as well.And I'll tell you why.So let's launch the instance.Okay.Instance has been launched.Let's go back to our steps and check what is next.Okay.Next is we want to create route 53 private hosted zone and associate that with the client VPC.And in order to resolve the route 53 DNS records we have to enable DNS settings for the client VPC.Now this is very important step so don't forget about that.Okay.So let's go to the route 53.And let's create the hosted zone.Let's call it say example.com.And it is a private hosted zone.And we want to associate that with the VPC in the Mumbai region that I'm using.And it should be client VPC right.So let's create the hosted zone.And as I said don't forget to turn on the DNS settings for this VPC.So let's go to the client VPC edit VPC settings and enable both these settings.And save it.Okay that is done.Now next we want to just log in to EC2 instance on the application side.And we want to make sure that web server is installed and running.So let's go there in the EC2.And this is my VPC.Let's copy the public IP.And now I will log in over the putty session.Okay so let's open the session with EC2 dash user.Okay.Now this is very simple.Just say sudo yum install httpd.Yes.Then sudo systemctl start httpd dot service and you can just create a file.Sorry.And say.Hello from EC2.That's it.Okay.And we can test the connectivity.Okay.So let's go here and let's say http colon slash slash and the IP address of the instance.And you can see hello from EC2.So it is working as expected.And as I said we just wanted the HTTP connectivity from outside only for testing whether web serveris running or not.So at this point I can even just go to the security group of the app EC2 instance and can remove thisentry, which makes this instance kind of private, right?We can't access HTTP service from outside.Okay, so that's done.So we have done up to step number six.And now let's create a lambda function with the default hello world kind of function.And then we will move towards the VPC lattice configuration.And in there we will create the target groups one for EC2 instance and one for lambda function.So let's do that.Okay so let's create the lambda function say author from scratch.Let's call it app lambda.So app dash Lambda.And it could be Python.Most of the settings as it is let's create the function.And by default if you see it will create hello world kind of lambda function.Right.So that's the lambda code.And if you just go and test this lambda function, it will just have this kind of message.And that is sufficient for us.So lambda has been created.Now let's go to the VPC lattice.So all the settings are on the left side.And there you see this target groups right.So let's go to the target group.And let's create a target group.Now the target is instance first.And the target group name is say EC2.And the protocol is HTTP on port 80.And this is for the app VPC.Now rest of the settings as it is say next.And we need to include this particular instance.Right now the health status is pending.But as soon as we associate this target group with the VPC lattice service, then it will make the healthcheck, which is very similar to the Elastic Load balancer if you remember.And it will turn to healthy.So let's create this target group for the EC2 instance.Okay, so target group has been created.Now let's create another target group.And this time it is for the lambda function.So let's call it say lambda target group.And next we have to select our lambda function.Rest of the settings as it is.And let's create this target group.Okay.So both the target groups have been created.And we are done with the step nine.Now let's go to the next step where we are going to create VPC lattice service.So in that we will also associate that with the custom domain name.We will create a listener on port 80.And then we will add rules to route the traffic to both these target groups.Right.So let's do that.And then we will proceed with the next steps.Okay.So let's go to the lattice services.Let's create a service.Let's give it a name say app service.And we are going to use a custom domain name and it will be app dot example.com.Now we are not going to have https.So we don't have or don't need to provide the certificate.Auth type is none as of now.And that's where the request from the client will be automatically allowed.So remember these steps.If you put IAM then you have to put corresponding IAM policies.Now we are not going to share the service with any other AWS account.So let's not do anything here and let's go to the next.And now let's add a listener.So for us the listener is port 80.That is HTTP.And we need to add a listener rule.So let's create a rule from here.And let's call it say lambda rule.The priority is ten and the condition is path condition.So we are saying if the rule is slash lambda then send it to the corresponding target group.That is a lambda target group.And the weight wait is one that is 100%.So let's add a rule.Now this is one rule.Now we could have added another rule which says EC2 goes to the EC2, but in that case it goes to thepath slash EC2.But in our case the web server is on the slash.So what we can rather do is we can just add a default rule.If it doesn't matches to slash lambda or anything else, then forward it to the EC2 target group.So things are same, but we are just configuring it in a different fashion.Right.Next.Now we don't have VPC lattice network as of now.So let's skip this.Say next and just create the service right.So VPC lattice service has been created.And now we will do the important thing that is creating the VPC lattice network.Okay.As we are going to create the VPC lattice network there are some prerequisites that we will first implementright.And the important one is creating the security group.When we associate the client VPC with the VPC lattice network.And I had explained that in the previous lecture.So we need to create a security group in the client VPC.And if you remember this security group is attached to the local link address right.So it should accept the HTTP traffic from the client VPC Cidr.Right.So what I'm talking about here is this particular security group right.So let's do that right.So let's go to the security groups.And let's create a new security group.Let's call it app.Now let's call it client VPC association security group.So it is clear.And this should be in the client VPC.And we need to add inbound rule which says for the HTTP traffic it should allow the client VPC Cidrrange which is ten dot ten .00 slash 16.That's it.And let's create this security group.Okay.That is done.Now we can go ahead and create the VPC lattice network.So let's go to the service network on the left side.And let's create the VPC lattice network.And let's give it say app lattice network.Then which services we want to associate.So we just have app service.So let's attach that.And we don't have any resources like you know RDS databases or anything.So we will skip that.And in the VPC association we are going to associate this with the client VPC.And the security group is the one that we had just created right.So that's done again the auth policies.We are setting it as none.And the rest of the settings I don't think so.We need to change anything.Okay.So VPC lattice network has been created and has been associated both with the service as well as withthe client VPC.And you can very well see that here right.So there is a service association.There is resource association.There is endpoint association.Then there is a VPC association.So we should see this VPC right.So it will take maybe a couple of minutes.So I'm just pausing this video and I'll come back here.Okay so lattice network and associations have been created.So if you see our architecture we have done most of it right.What we haven't done is probably some security group rule for this EC2 instance.But rest of the things are done.So at this point, let's try to just log in to this client EC2 instance over SSH, and then try to accessthe VPC lattice service using the VPC lattice DNS.Let's see if it works.So again grabbing the public IP of the client EC2 instance And just going to open the SSH session there.And in order to access the VPC lattice service we can use curl command and say http colon slash slashand the endpoint of the VPC lattice service.And for that we can just go to the VPC lattice network or the service.Rather we should go to the services and select the app service.And here we should be able to see the end point of this service.So right now we are just going to do that with the AWS provided DNS name which is this right.And if I go back to the putty session, if I do this should it work or not.So it doesn't work.Can you think why?And the problem is this particular security group of the EC2 instance.Because by default the target group will send the traffic to this particular instance, but which alsomeans we should be able to at least access the Lambda service, right?So let's try that first.So now I'm just doing this slash lambda.And it works.Right.Because there is no security group for the lambda function.So let's go back to the EC2 instance and add that missing security group inbound rule.Okay.So let's go here for the app EC2 instance.Let's go to the security.And let's open this security group.And in the inbound rule we should add HTTP traffic.And do you know what should be the source Cidr range.Now this is a range of VPC lattice internal network right.And I told you for that we need to have the prefix list for the VPC lattice.And do remember IPv4 prefix list because there are both IPv6 which is this.But we need VPC lattice IPv4 prefix list which is this?Right.So let's save the rule.And now let's go back to our client EC2 instance.And now let's try to just remove this.And you see hello from EC2.So traffic is going.So that's really great.That means we can access the VPC lattice service from the client.And now just let's try one more thing that is curl http app dot example.com.Should it work or not.It doesn't because we had created the route 53 hosted zone, but we did not create corresponding recordsin that hosted zone.Now for that, we need to have our domain name that is provided by the AWS.Which is this right.And we need to create corresponding record in the route 53.So let's create a record say app dot example.com.And it should be pointing as a c name To our domain name of the lattice service.Sorry.I think it did not get copied properly, but it should be something like this, right?So let's create the record.And now if we go back to the client.So let's try again.And still it doesn't resolve.So let's wait for maybe a second or so.And sometimes you might have to just reboot your client EC2 instance so that new DNS settings takesinto effect.Okay, so let me try to just reboot this instance and wait for maybe a minute or so.Okay.So let's just restart this particular session.So let's say restart EC2 user.And now let's try this again.So curl HTTP app dot example.com.And it works right.And you can also test slash lambda.And it is hello from the lambda.So it is working with the custom domain name as well right.So essentially we have done what we wanted to do.So we could successfully test the end to end connectivity using the client VPC and the app VPC.Right.And as I said we had the overlapping sides for the VPC as well.And VPC lattice allows that right.So we are done for this exercise.Now before I let you go there is an additional thing that you can try.Now if you just go or log in to an EC2 instance, then from there you cannot access the VPC latticeservice.That's because the app VPC is not associated with the service network using the VPC association.And we had discussed that in the previous lecture.So again the next step of this exercise if you want to do is to allow the inter services communication.So from the app EC2 instance, you should be able to access the VPC lattice service.And for that you would have to create that additional association.So I'll leave it up to you if you want to implement and test that.So I am talking about this green line here right which you need to create.Okay.So that's it for this exercise.Just last and most important thing clean up everything that you created.So please terminate the EC2 instances that you created the lambda functions, the VPC lattice networkservices, target groups.And then finally the VPCs and the route 53 private hosted zone.Otherwise there will be some charge for the VPC lattice usage.So remember this.Okay.So that's it for this exercise.So I hope it is now very clear how VPC lattice services work together.So thanks for watching.And with that now let's go towards some additional things about VPC lattice.And then we will conclude this section.So thanks for watching and stay tuned.