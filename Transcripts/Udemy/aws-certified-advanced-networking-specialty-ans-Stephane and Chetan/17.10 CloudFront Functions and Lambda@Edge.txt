# 17.10 CloudFront Functions and Lambda@Edge

So cloud front has more capabilitythan just a CDN.This allow you to run custom codeat the Edge to do some customization.So the idea is that you want to executeedge functions as close as possibleto users to make sure that you're verifyingsome early logics some early behavioror normalizing some Cache keys.So an Edge Function is a code that you writeand that you attached to a CloudFront distributionand it will run very close to usersin order to minimize the latency of whateveryou're trying to do.It doesn't have any cache behavior.It's just here to change the requestand the response sent by the users and back to the users.Now CloudFront has two different logic for Edge Functions.It has the CloudFront functions and Lambda@Edge.We'll see them both in this lecture.So the use cases for customization at the Edge,at the Edge is to manipulate HTTP requestand responses is to implement request filteringbefore reaching your applicationis to verify user authenticationand authorization is to also maybe generateHTTP responses directly at the Edge.Or do A/B testing by separating users into two groups.Or for example, doing Bot mitigation at the edge.The idea is that using Edge Functions.So either Cloud Front Functions and Lambda@Edgeyou do not have to manage any servers.Everything is deployed globally and it is serverless.Okay. So if we look at a traditional architectureof CloudFront,we have our clients to our Edge Locationstalking to our regional Edge Cache,talking to our Origin.Now Lambda@Edge Functions are going to be deployedat the Regional Edge Cache level.Whereas CloudFront Functions are going to be deployedon the Edge Locations.So as you can see two different levelfor deployments of these functions,and that will impact your choicebetween CloudFront Functions and Lambda@Edge Functions.So if we look at CloudFront Functions,they will interact with the viewer requestand the viewer response.These are the request and response that happenbetween the clients and the cloud front.It does not modify origin request or origin response.So these are lightweight functions return in JavaScriptand they're meant to do high scalelatency-sensitive CDN customizations.So it is Sub-millisecond startup time.So it's very, very quick to startand it can handle millions of requests per second.So we run at Edge Locationsand it's Process-based isolation.So you want to change the viewer requestand responses as I said.So what is a viewer request?Well, it's after CloudFront receives a requestfrom a viewer and your response is before CloudFrontforwards the response to the viewer.So the idea is that it's a native feature of cloud front.You manage the code directly from within CloudFront.And this allows you to do some very quick timesensitive transformations in CloudFront, okay.The idea is that you cannot call externalservices from the CloudFront Functions.You just want to perform some transformations,some computations or some normalizationin a very quick fashion.Now Lambda@Edge is a different mindset.So these were first what CloudFront was offeringas a feature CloudFront Functionsare new but Lambda@Edge has been existingfor a long time.And the idea was this is that you have Lambda Functionsthat are written in either NodeJS or Python.And you can scale through thousands of requests per second.So less scale than (indistinct)functions for sure.And they run on the nearest Regional Edge Cache.They're VM-based isolation.And they're used to change both CloudFrontrequests and responses.So we can change the viewer request,the Origin Request which is the requestthat CloudFront sends to your origin.You can change the Origin Response.So the response that CloudFront receivesfrom the Origin and as well as the viewer response, okay?Now, when you want to deploy Lambda@Edge Functionson CloudFront, you need to author your functionsin one region, which is us-east-1,and then CloudFront will replicate this functionto all its regional Edge Caches.Now, if we have a look at how these thingscan be used together.So we have Edge Locations and Regional Edge Cache.And so the view request goes all the way like this.Then it goes through the Origin,and then the Origin Response,goes all the way like this to the viewer response.Now your Cloud Front Functions are going to runon the Edge Locationsat the Viewer Request and Viewer Response level.Whereas your Lambda@Edge Functions are going to runat the Origin Request and Origin Response level.This is if you want to use both CloudFront Functionsand Lambda@Edge Functions together, okay?So this is one option.And we can see a clear separation of (indistinct).But if you want to just have Lambda@Edge Functionsthen in that case, what's going to happenis that the Lambda@Edge Functions are going to operateon both of your requests, response.And the origin request and response.But only at the original Edge Cache Level, okay.In this case we cannot use CloudFront Functionsbecause we cannot use both Lambda@Edgeand CloudFront Functionson the Viewer Request and Responses.So this is when you need capabilities of Lambda@Edgethat are not available with CloudFront Functions.For example, if you need a longer execution time,network access, third party API calls,these kind of things.So if we want to compare, you know, on the tablethe Cloud Front Functions and Lambda@Edgewhile the run time is support is different.So one is JavaScript, pure JavaScript code.The other one is NodeJS and Python.Where it gets executed is on Edge Locationsfor CloudFront Functions.Whereas it's original Edge Cache for Lambda@Edge.The CloudFront triggers are going to be the view requestand response functions and all of them for Lambda@Edge.The isolation is process based versus VM based.Now the max execution time is what I thinkis the most important thing.You only get one millisecond to executeyour CloudFront Functions.And therefore them must be very quick and very simple.Whereas Lambda@Edge, you get five secondsof run time for viewers triggeredand 30 seconds of run time for origin triggers.The memory of your CloudFront Functionsis low two MegaBytes.Whereas for Lambda@Edge, you have more memory.128 MegaBytes or 10 GigaBytes.The total package site is very smallfor CloudFront Functions,10 KiloBytes whereas Lambda@Edge has more space.There's no network accessno fast system access for CloudFront Functions.Whereas you have some for Lambda@Edge.You do not have access to the request bodyin CloudFront Functions.You have access to it in Lambda@Edge.So CloudFront Functions is reallyabout reading headers and so on.And the pricing is that there's a free tierfor CloudFront Functionsand it's one sixth of the price of @Edge.So if you can move your logicfrom Lambda@Edge CloudFront Functions,you're better off and Lambda@Edge as no free tier.And you get charge per request and duration.So when do you use a CloudFront Functionversus a Lambda@Edge function?Well, CloudFront Functions are going to be usedfor Cache key normalization.So you want to transform the request attributessuch as the headers, cookies,the query string, the URL.To create an optimal Cache Key.You also want to manipulate your Headers.So you want to change them.You want to insert, modify, or delete HTTP headersin a request or response.Or you want to do your rewrites,or redirects or to request authenticationand authorization directly at the Edge.So in which you can createand validate user generated tokens, for exampleJWT to allow or deny request very close to your users.Whereas Lambda@Edge is going to be giving youa much longer execution time, okay?Up to five seconds or 30 seconds.You get adjustable CPU memory.You get to have code that can dependon third party libraries.For example,you can use the SDK to access other AWS services.You can use the network accessto use external services for processing,and you have file system access.If you wanted to Cache some stuff locallyor you can even access the bodyof the HTTP request to manipulate the body itself.So hopefully the use Caches are clear and well defined.Now, if you wanted to do authorizationand authentication with CloudFront functions,the idea is that at the Edge Location,you would intercept the contentand you would check the token,the digital JWT token which lives in your headers.And then you can check whether or not it is correct.And then if it's not correctthen the client will get an error messagedirectly without having your requestgo all the way to your origin.Whereas your Lambda@Edge Function will operateon the regional edge Cache level.It could be intercepted by Lambda Functionswhich can do some custom logicand even issue third party API calls,to check whether or not the authorizationand authentication is properly well set up.Another very common use case for Lambda@Edgeis to load content based on the user agents.So we have an extra bucketan Amazon cloud front with Original Edge Cacheand we have users texting it from desktopand others just texting from mobile.Now they both wanna access a cat picture.So they do a request for cat pictureand the resolution is super high.So 192, 1, 9, 2, 0 and 1, 8, 0, 8.0 is 10 ADP and they request it from CloudFront.So if the request goes well,the request is passed all the way to the S3 bucketto get that specific image.But imagine that the same request is donefrom our mobile device.And this time the user agent is not Chrome.It is the iPhone OS Safari.Now, what we can do is instead of sendingthis big image back to our mobilewhich may be very inefficient,we can have Lambda@Edge Functionthat will inspect the User-Agent GTP headerand then redirect based on the device type.And so therefore, because our device is a mobile devicethen the image is going to be askedinto our S3 Bucket is going to be cat-640-320.Which is a lower resolution for our cat picture.And this is the picture that will be returnedby our S3 Bucket into our mobile device.So the Lambda@Edge Functionwas able to modify the requestto change the target of these request.So the other way you can use Lambda@Edgeis for a global application.For example, your client gets the websitefrom the S3 Bucket.And then for dynamic API requestsit sends it into CloudFront to leverage the Cachebut your Lambda Function can also be used for exampleto query data into DynamoDB table,which gives you a fully globaland serverless way to do an API in AWS, okay.So that's it at the exam level.What you need to know is understandwhen you need to use Lambda@Edgeor CloudFront Functionsand that should be good to go.But I want to give you a bit more detailsso you can really understand the use cases is behind it.So hope you like this lecture.And I will see you in the next lecture.