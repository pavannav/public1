# 18.16 Hybrid Connectivity

Okay, so just a few architectureswe need to seeto really make sure we understand correctly the balancing,because the exam will most likely ask you abouthybrid scenarios or DPC pairing scenarios.And so you need to understandhow load balancing works in this case. So.Let's say we have a very simple example right now,we have a VPC and ALB and it's redacting into a target groupmade of EC2 instances.Now we also want to do some load balancingacross a target group made of on-premises serversin our corporate data center.So to achieve that, what we need to do is to establish a VPNor a direct connect connection to connect our VPCinto our corporate data center.Once that connection is establishedand there is network connectivity between our VPC,and our data center.And therefore,what we can do is that we can start creating a target groupand that target group has to be using private IPs.So we would just do private IPs within a private group.And obviously the ALB using this private IPhas the connectivity to help check our on-premises server.Therefore it will work and the traffic can be sentfrom the ALB directly to our on-premises servers.This is very similar with peered VPCs.So, say we have an ALB and VPC A,and we have other EC2 instances in another VPC.Maybe it's another VPC in another account, who knows.The idea is that you would also want tosend traffic from the ALB to this other VPCand to do so, well we first need to establisha VPC peering connection.Which will make sure that private trafficcan flow between the two VPCs.And therefore we can define a second target group,again using private IPs to make sure the ALB sends trafficall the way to the target EC2 instances.Okay finally, just a distinction between internet-facingand internal load balancers.So, internet-facing load balancers look like this,where your ALB sits in a public subnet. Okay?And then sends traffic, usually to EC2 instancesin a private subnet, and is using IPv4 only.So the private, the EC2 instances see traffic originatingfrom private IP addresses onlybecause your ALB is terminating the connection.But what's going to happenis that you can also enable dual stack.So the idea is that right now it's IPv4,but you can also have IPv4 and IPv6for the clients to connect to your ALB.The idea is that now you will have different domain names.So you'll have, name,the first name is just your regular IPv4 domain name.The IPv6 dot the same name as your IPv6 DNS endpoint.So if you do a DNS query against that name,you will get an IPv6 as a result,or if you want it to just either be redirected to IPv6or IPv4 based on your compatibility of connection.You would use a dual stack end point,and dual stack is the dual stack dot the name.And this will give you either the IPv6,if it's supported by your network,or the IPv4.But regardless if using an IPv6 as an endpoint,the traffic that will be sentinto your EC2 instances will be IPv4.That's because your ELB does terminate your connections.Now, if you're using an internal ELB,you can only use it in IPv4 modeand all the traffic will remain within your AWS accounts,using private IP addressesand then the DNS name of your balancershould start by internal dashand then the name of your load balancer. Okay?So that's it for this lecture. I hope you liked it.And I will see you in the next lecture.