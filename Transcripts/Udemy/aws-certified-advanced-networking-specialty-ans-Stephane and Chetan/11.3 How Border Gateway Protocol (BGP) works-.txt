# 11.3 How Border Gateway Protocol (BGP) works-

Hello, and welcome to this lecture,Dynamic Routing using Border Gateway Protocol.So in the earlier lecture,we understood the difference between the static routingand the dynamic routing.And we understood that in case of static routing,you have to modify the route entries in your route table,and you have to add the new routesthat you want to send traffic to manually.And we also understood that in dynamic routing,these routes are propagated automatically.Now in this lecture,we are really going to understand how that propagationhappens and for doing that,you need to use a different protocol in this case,and that would be a border gateway protocol.Now border gateway protocol is a backbone of internet aswell because you know,internet is all mesh off a lot of networks.And then all these networks needs to exchange the dynamicroutes with each other,and that's where you can hit any IP addressand your packet reaches to the destination.Now, border gateway protocol is also importantwith respect to your exam.And in case of AWS,both AWS side to side to VPN, as well as direct connect,both support the border gateway protocol.So let's understand that.And in that case let's get started.Okay, so what is border gateway protocol or in short PGP?It's a dynamic routing protocol using path-vector.Now what that means is,in case of border gateway protocol,there is something called AS, autonomous systems,and we learned about that in the last lecture.So any network,you can call it as like ASand this AS is interchangethe routing information between each other.So in case of BGP, they work on path-vector protocol,which means they exchange the best partto reach to the destination.So if I know the parts of other AS's,how to reach them, what is the next stop?I will exchange that information with my neighbor BGP,and that's where the routes gets exchanged.Now, there are two types of the BGP.One is iBGP, or you can call it internal BGP.So it is like routing within one autonomous system.And what we are interested in, in eBGP,which is routing between the different AS'sbecause we are connecting multiple networks, right?And that's where the routing would happen betweentwo different AS's.So that's called eBGP.Now, how is this routing decision is influenced?Now what that means is,if I want to reach to a destination,what parameters I need to consider.Can I influence the routing?Suppose there are multipleroutes to reach to the same destination.So through BGP can influence that I want to take one routeor the other.Now there are certain parameters or features,or you can say attributes using which you can influence thisrouting mechanism.So one is called weight.So you can define the weight on the different paths,and then the routing would be taken to the highest weight,but that only works in the Cisco routers.And that's where we are not really going too deep intothis weight attribute.But there is something called AS PATH,which is like how many hops are thereto reach to the destination.Ultimately that is called ASPATH.So ASPATH would contain the number of the hops or whichAS's I need to cross in order to reach to the destination.So we'll see how AS PATH influences that.Then there is something called local preference.Now this works within the AS.And we'll talk more about that.So if you have multiple exit points to reach to the targetdestination from your source network,then within your source network,which router you will choose to go to that destination,that is basically a local preference.And then there is something called MED,Multi Exit Discriminator.Now similar situation, like a local path.But in this case, the MED value is external.That means the values are exchangedbetween the AS's as well.And it will be more clear as we see the scenario,how it works.So let's try to understand how everything works in the BGP.So let's assume you have the whole world at your disposal.And we are now trying to set up a network connectivitybetween the data centers at various locations.So there is one network you can call it one AS and it hasIP range ten, ten, zero, zero, zero, 16and AS value as 100.Now you understood that there is private AS and public AS,anything works here.But we are taking the number as 100.However, the actual number, you know,there is some range that you can look atin the last lecture.Similarly, there are other networks like one maybe Africa,and which has AS 200with a CID arranged 10, 20, zero, zero, 16.And then there is somewhere in Indiaand maybe somewhere in the Europe.So we have four networks hereand we have given them different AS numbers: 100, 200,300 and 400.Now every AS would be consist of multiple routers.That's what is shown here.And then there will be one router,which is connected to other networks,but that router has a private IP address from the networkaddress range, and that's what is shown here.Now what we have done is,we have connected this network oreither a MPLS link or other links.And there is a connectivity between AS 100 and AS 400,similarly between 400 and 300, 300 and 200,and then 100 and 200.So all these are connectedand there is a different networkbandwidth that they are connected with,and we will show you why that is so, as an example.And additionally,there is one more link between AS 400 and 200.So this is how the network is laid down.And now let's look at how BGP works in this scenario.In terms of the BGP neighbors.As I said,if the traffic is going between AS's, different As's,they are called E BGP neighbors.So these two guys are E BGP neighbors versus,if you're talking about router within the same AS,and they exchange the routing information,they are called I BGP neighbors.And as I said,what we are interested in is E BGP neighbors.Okay, so this is a network and now let's seehow the routing information is exchanged.So every AS would have their own routing table.We also call it BGP table.Now in that BGP table,there will be three things: the destination, the next hop,and the path, you can call it AS PATH.Now if you look at this AS,the destination is itself ten, dot, ten, dot, zero, zero 16.The next hop in that case would be zero, zero, zero,and the path would be I, I for internal.That means all this traffic is internal traffic.Now, every AS at initial level,it would have this kind of route table where the entrywould be for itself.The CID actually will be the self CID address.Now what happens, as you know this,all AS's are connected to each other.They learn the route from the other AS,like if from AS 100, I want to reach to AS 400,then what route entry I should have.So they would exchange those routesand that's where all these route entries are here.Now let's take an example from AS 100,if I were to go to AS 200which is this one, with the destination IP address of10, 20, zero, zero, 16, the next hop is this router,which is 10, 20, zero, one.And in this case, my AS path would be 200.So I'm adding that 200 and there will always be thisinternal entry.So AS PATH is 200 comma I.Now if I'm to reach to 400, of course,my next destination is 10, 40, zero, one,which is this router.And the AS PATH would be 400 comma I.Likewise, we addedor it gets automatically added all the AS entriesout table entries for all other AS's.Now if you see specially, this AS 400 and 200,they're connected to rest of the three AS's.That's where there are self entry and three more entries forall these three AS's.So this is very basic to start with.The route table would have this kind of entries.And now if you can see, if I want to reach to AS 400,any IP address 400, it will take that path.And if I were to go to AS 200 from AS 100,it will take this path.Now, what if I want to reach to ASA 300 IP address?Now, if you look at this route table,there is no route entry for that.That means right now,I can't reach to that destination.And that's where the BGP comes to the rescue, right?What it does is,whatever path you or other neighboring AS's understandor learn, they will pass that counting informationto other AS's.So in this example,if you see my AS 400 knows how to reach to AS 300using the hop 10, 30, zero, one, right?What it does, it just upends its own AS number here 400,and sends this information back AS 100.Now, if you see AS hundred got the destination address,10, 30, zero, zero, 16.And in this case, the next hop is 10, 40, zero, onebecause this directly does not understand the,it does not have direct connectivity with AS 300.So next hop will be a 10, 40, zero, oneand as packet reaches this destination,it knows how to route traffic to AS 300.So it will learn that entry here.Similarly, it will also get the same entry or similar entryfrom AS 200 as well.That's because AS 200 also connected to AS 300and it has its own entry.So now, AS 100 has multiple destination entries for AS 300.And that's what the BGP is.What it does, it exchanges routing information with allneighboring AS's.And in this case, we are just showing AS 100 path,but this AS 400 will also receive similar entriesfrom AS 100 and likewise.So it will be a lot of route getting exchangedbetween all these AS's.Now what's the benefit of this?Like AS 100 has now two entries to reach to AS 300.So what would happen?Suppose by default BGP chooses the,this path that is through AS 400 to reach to the AS 300and somehow this link goes down,that means there is some problem with that network.Now, BGP can't take other path because in routing table,it knows that there is one more path exists to reach tothe target destination.And that's where BGP can be used for fail-overkind of scenarios.So that's one of the possibilities and we are going to talkmore about such scenarios.But what I want you to understand from thisis that a BGP is all about exchangingthis routing information dynamically.Now what do I mean by dynamically?Suppose tomorrow this AS 400 gets connected to a new,say, AS 500 and then it would learn about that routeand there will be a route table entryin the AS 400 route table.Now it would also publish that route table information toAS 100 and AS 200 and AS 300.So that now all these neighbors also understand aboutthe new network,which got connected to AS 400 and that's where they canroute the traffic to, even that destination.So BGP is all about that.Internet is all about that.Okay, so that's basics of BGP.And now let's get into the attributes of the BGPthat we talked about.Specifically, the AS PATH, local preference,and then multi exit discriminator.So stay tuned.