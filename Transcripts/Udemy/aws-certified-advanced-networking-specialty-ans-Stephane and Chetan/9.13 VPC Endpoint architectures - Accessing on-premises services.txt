# 9.13 VPC Endpoint architectures - Accessing on-premises services

Okay, now that you understood how VPC endpoints work and Privatelink works, let's look at some ofthe interesting architectures that emerge using the VPC endpoint and Privatelink.So ultimately it simplifies your overall network.And you may look at these architectures.If you are using, say, too much of VPC peering connections or too much of transit gateways.So it would really help you simplify your network and also save your cost.And just a word of caution.Maybe some of the architectures are little advanced, so don't worry about that.You might not have the same architecture scenarios in your exam, but I'm sure this will help you inyour day to day operations if you are working in AWS networking, right?So let's look at some of the architectures.And very first architecture is what if you have your services deployed in your on premises data center.So you are operating in a hybrid mode.In that case as well, you can expose your on premise services using the VPC endpoint service.So in AWS you can see that you have consumer VPC and the provider VPC.And for creating the VPC endpoint service, you need a network load balancer.But the catch here is that the target of this network load balancer can be in your on premises network.Right.Something like this.And of course in order to connect to this on premises server or services you need to have some privateconnectivity.So that will be through either VPN or Direct Connect.So after having this private connection, you can add this application server using IP as a target tothis network load balancer.And then you can create VPC endpoint service using this network load balancer.And then in the consumer VPC you can create the VPC interface endpoint for this VPC endpoint service.And then client in the consumer VPC can access the services which are deployed in your on premises network.Something like this.Right.So that's one interesting pattern when it comes to hosting your actual services in your on premisesnetwork.So I hope it is clear.And with that, now let's look at some of the additional architectures in the following lectures.Thank you.