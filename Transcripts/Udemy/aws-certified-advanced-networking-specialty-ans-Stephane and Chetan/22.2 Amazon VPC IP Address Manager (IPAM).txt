# 22.2 Amazon VPC IP Address Manager (IPAM)

Hi.So in this lecture,let's understand how you can centrally manage the IP addresses across your AWSstate.And for this AWShas a tool calledIPAM.As the name suggests it is IP address management.So in this lecture, we'll talk about what's the problem with, you know,managing your IP addresses, then what'sIPAMand then what are the components of theIPAM to help you manage these IP addresses?Then I will just show you quickly how you can create IPAMpools and then use those pools to allocate IP addresses to your VPCs.And then we'll touch upon how can you track and monitor your IP addresses with IPAM.So if you are starting withAWS, probably you would have just a couple of VPCsand your on-premises networkand then you will create these VPCs allocate CIDRsand then make sure that these VPCs can communicate with each otherusing say Transit gateways or VPCpeering connection.Now, one thing that you need to take care is thatthe IP addresses orCIDR blocks for these VPCsare non-overlapping.That's for the reason that if they are overlapping,then there is a problem connecting these two VPCs.So it's the best practice to, you know,manage these IP addresses in such a way that no two VPCsprobably have the overlappingCIDRS.So that's better to manage your growing network in the future, right?Sowhen you begin,probably you can have simple Excel sheets in which youhave the list of VPCs and corresponding CDR ranges.So that whenever some new VPC has to be created,you can just look at your Excel and see, OK, this CIDRblock is not used.So for the new VPC, you can use a new CIDR rangeand that works well if yourAWS footprint is small,but over the time,if you are using multipleAWS regions, multiple environments and lots ofVPCs,then you can imagine that pain if you are managingthis IP address location mapping in some kind of Excelsbecause there will be multiple network administratorand then probablyit will be Excel hell going forward.Soexactly to address this problem,AWS has come up with this tool calledIPAM, which is IP address management.But at very high level,you create the pools of IP addressesand then you make sure that when you create VPCs,you allocate the VPC CIDR from those pools only.So thatirrespective of how many number of VPCs you create,you are sure that this VPC will never get the overlapping IP addresses.So that essentially isIPAM. Now, if you talk about theIPAM components, thenIPAM has something called scope.So IPAMscope can be public or private.So as the name suggests,public scope is for managing public IP addresses andprivate scope is for managing private IP addresses.And in this lecture, we will mainly focus on the private IP address space.Because for public IP addresses, you need to Bring Your Own IPs (BYOIP),which unfortunately, we can't do here.So when you enable theIPAM, the public and privatescopes are created for youand then once you have this scope, then you can create the pools.Now these pools are like collection ofCIDR ranges and then you can have layered pools.That means you can have a top level pool and inside that you can have sub pools.For example, if you see this diagram, there is one single Global pooland inside thatyou can create pools perAWS Region. Now that's just an exampleand it's up to you how you want to organize your pools.Butyou can create this kind of sub pools. And then when you create the VPC,you can allocate VPCCIDR from these sub poolsand this make sure that you are organizing your big address spaceinto a smaller poolsand then you're allocating your VPC,CIDR from those small pools.So that's how theIPAM works.Now, if you have a singleAWS account, which is not part ofAWS organization, then also you can useIPAMor if you useAWS organization,then also you can use IPAMto manage IP addresses across all member accounts in yourAWS organization.OK? Just to give you an example about the pool structure,so you can have a Global address space, which is again the collection ofCIDR ranges.And then you can divide this top level pool into regional pools.And within that, you can create a pool specific to a particular environment,for example, for Development, Staging or Prod.And now when you create a VPC,you first decide that this VPC will be for Development environmentin a particular Region.And then the VPC will be allocated IPaddress range from that particular development pool.And this is how you can manage your CIDRs for your VPCs.Now another example,you can also do the other way around. For example, you have theIPaddress space which is a Global pool.And inside that first you create the pools as per your environments, for example,production development.And within that, then you have the regional poolsand just to talk about another example,you can also create pools as per your line of businesses.And then you can have multilevel pools as you can see here for business unit one,I have regional pools and inside that I have environment specific pools, right?Soit really depends on how you want to create these pools.And if you're interested,you can just check out this blog whichtalks about some best practices for creating the IPPools.I hope that is clear.Now, earlier, I mentioned that you can use IPAMwithAWS Organization as welland for that, there are a few considerations. So let's talk about that.Now, if you see this diagram, this is a control tower based landing zone diagram.If you don't know what is landing zone or control tower, just read about it.But essentially, it is kind of best practices to manage yourAWS organization accounts so that there is a separation of duties,security is taken care,network is Centralized and all member accounts can have their own control.But still you are having the governance at the management account level.So this is a kind of, you know,AWS organization structure.And again, this is very flexibleand this is just one representation of control tower landing zone.So IPAMcan be integrated with yourAWS organization structure. And for that, what IPAMsuggests that you should have a separatenetwork admin account in which you enable theIPAM.And then using this account,you can manage the IP addresses for rest of the account in yourAWS organization.So there should be a DedicatedIPAMadministrator accountin yourAWS organization.And this account of course, should be a member ofAWS organization.Now, additionally,while using theAWS organization, what you can also do isyou can enable the SCP policies.Now, SCP is service control policieswhich enforces that all the members account in myAWS organizationshould not be able to perform,you know, some actions.And here it makes sense to have this kind of action which saysno member account can directly assign the IP addresses to the VPCs.That means all the IP addresses should be assignedusing the IPAM.Now if you put this governance policy in place,then you are sure that all the member accounts willhave non-overlapping IP addresses which is assigned by using theIPAM.So here this is just an example to enforce use ofIPAM in the member account.But then you can also modify these policies and say aparticularAWS account can use a particular IPAMpooloryou can also say that this particular organization unitin AWS organization can use a set of IPAMpools. So this is all flexibleand if you're interested, you can just look at theAWS documentation aroundhow to write these policies.So the point here is that you can use IPAMwithAWS organizationand you can enforce the use ofIPAM for the member accounts.Okay. So moving forwardIPAMalso allows you to write rules, which means when you create the VPC,you can specify that this IPAMpool should only be allowed to be used ifyou're having the right permissions. That means you have rightAWS account,you're operating in a particular Regionand you are requesting for, you know, particular VPC size, so minimum and maximumand your VPC has a particular tag.So inIPAM you can write the rules with all these conditions.And now when a user is trying to create a VPC, these conditions can be evaluated.And if all these conditions meet, then IPAMwill respond with theCIDR from theIPAM pool.And then you can use thisto allocate to your resources, for example, to your VPC.And if any of the rule doesn't meet the criteria, then of course, IPAMwon't be able to allocate CIDRand then you won't be able to create the VPC.And this doesn't really work only for the VPCs,but you can use thisCIDR for nonAWS resources as well. For example, if you are setting up your on-premises networkand you want to have some private CIDR range, then you can also ask IPAMto provide you the rangeand then that range you can use to allocate to your on-premises network.Okay. So once you have the IPAMpools created and these rules configured,then you can put everything into the automation.For example,in the beginning, maybeyou might have been asking your network admin for the CIDRranges and then you create a cloud formation template provide thatCIDR range and cloudformation will create the VPC.This is a flow beforeIPAM, I will sayand over the timeyou will use theIPAMand now developer can directly askIPAM for the CIDR range and provide it to thecloudformation and then cloudformation will create the VPC.But here you can completely take out even this step.And cloudformation itself can directly make an API call to theIPAMand get the available CIDR range and create the VPC.So this is a way in which you can automate using theIPAM.Okay. So with that, now, I just wanted to quickly show you how can you useIPAM in yourAWS account.So this is kind of one of the way in which we can implement IPAM.And as I said,it's up to you how you want to create this IP address space and how you want to thendivide into multiple pools.So if you see here, I have a Global poolwith /12 prefix which gives me around 1 million private IPS.And then I'm creating the regional poolwith /16 prefix which gives around 65,000 IPS.Now, within every Region,I would have multiple environment pools and these pools are created with /18CIDRwhich gives me around 16,000 IPS.And then while creating the VPCs, I can restrict that VPC can be created with maximumCIDR of /24.So that no VPC is more than 256 IP addresses.And which means that for every environment I can createat least up to 64 VPCs of size 256 IPaddresses.And now let's go to theAWS console and see this in action.Ok. So I'll just say IPAMand then you can go to the VPC IP address managerand then you land here. Now, very first thing we have to do is to create theIPAM.So you have to just make sure that you accept this.And then there are 2 pricing options.Now, the difference is that in free tier, you can only manage the public IPv4addresses.But in our case, of course,we want to manage the private IP addresses andthat's where we will go with the advanced tier.But which also means there will be some cost for this IPAMuse.Now here you can saythe IPAMname. So I will say MyIPAMdoesn't really matter.And then here in the operating Region,you should select all the Region in which you want to use theIPAM. So by default, Mumbai is selected because we are operating in Mumbai.So that's the base Region for theIPAM,but you should also add other regions in which you want to use IPAM.So I will also select us-east-1which is North Virginiaand then I'll create theIPAM.So this is just, you know, setting up the IPAM.And as you do this, it automatically creates two scopes, as I said,a public scope and a private scope.And now for the next part, we are going to use our private scope only.Okay. So the next step is to create the pools. So let's go hereand let's make sure that we use the private scope.So let's create a pooland let's call itthe Global pool.So rest of the settings as default. And just one thing here is a local.Now, local means that in which allAWS Region you want touse this pool. Now, this is a Global pool.So I don't want to restrict it to a particular AWSRegionbecause we will be creating a regional pool as a sub pool.And at that time, we will select the local.So as of now, let's leave it blank.Now see CIDRs. So as we know, we want to create 10.0.0.0/12,which gives me around 1 million IP addressesrest of the settings as default and let's create the pool.Okay. So this is my top level pooland now we want to create the sub pools.So let's refer to our CIDR allocation slide.So we just created this one and now let's create a regional pool.So we'll just do it for one Region, but you can definitelyreplicate this for other regions if you want.So in Mumbai, we will create a regional pool with /16 CIDRand within that, we will create environments specific pool with /18 CIDR.So let's do that.OK. So I'm in the pools and now I will say create a pool within this pooland I will say aMumbai pooland here most of the settings as default.And here in the local, I will say I want to enable it for Mumbai.So let's select that.And as an inputCIDR,I will say 10.0.0.0/16 for my Mumbai which gives me around 65,000 IP addresses.That's it. And let's create the pool.Now, once you create the Mumbai pool,now you can create the environment specific pools, right?Soonce it happens, we will create the next level of pool.Okay. So now if you go to the pools, you select the Mumbai pools and then in actionsay create a pool within this pooland this is mydevelopment pooland I'm just going here, I'mkeeping the things as default.OK?So this is my development environment and I want to allocate16,000 IP addresses out of 65,000 for the Mumbai pool.So I'll say 10.0.0.0/18.And at this time, I also want to make sure that all theVPCs which are created in the development environment,they are of size/24and they have the specific tag which says environment equal to development,so I can enforce this kind of rules for myIPAM usage.And if you remember we had seen that in the earlier slide.So let's configure the allocation rule setting for thisIPAM pool.So here we say that the minimum net mask for my VPCis /24. The default is also /24 and maximum is also /24.But if you want,you can define the minimum and maximum differentand then default as a different length.And additionally, we will sayevery VPC that you createshould have the environment tagas a development.Then only IPAMcan allocate thisto the VPC,right?And rest of the settings as it is and let's create the pool.Okay. So development environment has been created.So you can create, for example, testing with 10.0.64.0/18.And then you can also apply the similar rules for the taggingand VPC prefix size as well.But it's just a repeatation of what we did for development environment.So I'm skipping that part.Now let's go to our VPC console.So we have required pool created. So let's go to the VPCand let's go to the Mumbai Region and let's create the VPCand let's call it, say MyVPC.And here you can select that I want to create a VPCwithIPAMallocated IPv4 CIDRblockandit shows you all the blocks which are available.So within Mumbai, we know that there is a development block which we want to useand here it automatically picks /24 prefixbecause we had configured that as a rule.Now, rest of the settings as default. Now to begin with,I'm not putting the tag for the VPC and now I'm trying to create this VPCand it says that there was an errorbecause the resource is missing one or more resource tag required by the IPAMpool,right? Which was expected. So let's go here and sayenvironmentsequal to developmentand create this VPC.Okay. It did not work.Maybe because we made some mistake somewhere. So let's go back and check ourIPAMand in the pools, if you just go to the development pool,let's look at the compliance requirement and is key OK?So I made a spelling mistake there and that's where it's not allowing,sorry for that.But now let's use this as an environment.So let's go here and modify this and create the VPC.OK?So you can see that VPC has been created successfullyand it automatically allocated this CIDR range.Now let's do one more thing.Let's create another VPC in the same pool. So let's call it, say myproject XVPC. And we are allocating from the same development pool.And again, we need to add,I would tagasdevelopment and then create the VPC.And this time you see that it allocated the next available CIDR in /24 range.So this makes our life much, much easier. That means you don't have to worry about.Now, this CIDR ranges, it will automatically be allocated byIPAM.So that was a short demo for this.And now let's go back to our discussion.So if you are designingIPAM for your organization, what I will suggest that first you plan itand for that, I would say put everything onto the paper,like how exactly you want to create these pools and howmany IP addresses you should be allocating to these pools.And while you do that,don't just consider the currentAWS landscapebut also consider the future state as well as your on-premises networkwith that. Let's just quickly talk about a few more features of theIPAM.SoIPAMalso allows you to track and monitor the IP address usage.And for that, you can go to theIPAM dashboard and you can see everything out there.So I leave it up to you to explore theIPAM dashboard,you can also filter the CIDR ranges as per the resources.For example, if you want to see the allocation to VPCs or to the subnets or to the ENIsall that can be done through the dashboard.Now,IPAM is also integrated with cloudwatch,that means if you're close to exhausting the IP ranges from a particular pool,then you can create an alarm for that and then you can take necessary action.FurtherIPAM also allows you to see the IP address history.So you can track down for a particular resource which all IPswere used in the past or if you have an IP address,then you can see this IP was allocated to this ENI and then it wasallocated to some different ENIall that can be traced down using the IP address history.And finallyIPAM also provides you public IP insights. So this is applicable for all the public IPsthatAWS provides to you as well as the public IPsthat you bring in as a Bring Your Own IP (BYOIP),which means you get to see what all public IP addresses are there in use.And this is particularly useful from February 2024because AWSwill charge you for the use of public IPs.So using this public IP insights,you can figure out how many public IPs are there,how many are unassociated and then you cango and release those public or elastic IPs.So in order to get an entire view of public IPs, this feature will be very useful.OK? So essentially those are the additional features of theIPAM.Now, for completeness,I have provided few slides for every feature that I just talked about.So if you want, you can refer to these slides for the same.OK. So quickly, theIPAM summaryIPAMallows you to organize IP address space into separate domains.This could be business Domain or security Domain or Environmental Domain.It is up to you how you design yourIPAM.Then it automatically allocates the CIDRsto your VPCs with specific business rulesand then it also monitors your IP address space that's in useand monitor the resources that are using this IP against the set business rule,which means it can also show you the compliant and non compliant resources.Further, you can see the history of the IP address assignmentsand one more point that we probably did not touch upon, but you canshare your, bring your own IP addresses across the Region and acrossAWS accountusing theIPAM.And finallyIPAMprovides you two pricing option in free tier. You can manage the public IP spaceand for private IP space, you have to enable the advanced tierand there is a small cost when you enable the advanced tier.Okay. So that's it about the IP.So I hope this helps you for any questions in the exam around IP address managementin AWS.Thanks for watching and stay tuned.