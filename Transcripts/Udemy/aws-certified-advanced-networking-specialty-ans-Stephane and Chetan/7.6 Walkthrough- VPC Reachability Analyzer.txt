# 7.6 Walkthrough- VPC Reachability Analyzer

Okay. This is a quick exercise for VPC reachability analyzer.The setup is pretty simple where we haveVPC and a Subnetin which we have EC2 instance.And then we want to access this instance overSSH from our workstation.So the connectivity will look something like this.Now here we'll try a few scenarios.So very first thing that we will do is purposefullywe will launch this instance in a private Subnetand then see what the network reachability analyzer tells us, right?Then we'll modify theSubnet and make it publicand then further, we'll modify the Network-ACL rulenot to allow the traffic.And then finally, we will update the security groupand test the connectivity through the reachability analyzer.Now, for you, a word of caution,there is a charge for, you know, every analysis that you perform.And in my opinion, this charge is little higher than I would have expected.So for this lab,you might get four times this cost because weare going to run this analysis four times,right? So bear that in mind.So let's go toAWS console and set this upOkay. So I am in Mumbai Region and I'm in my VPC console.First thing I'm just going to do is to create my VPCwith the namedemoand some CIDRand just create the VPC. And again, I want to create internet Gatewayso that I can later connect over the internet.And I also want to attach this to my VPC,right?And now I want to create a Subnet and this time,I'm not going to create the public Subnetand that's purposefully because we want to see the connectivity.So let's call it a demo Subnetandin one of the availability zoneand let's give it some CIDRrange,that's it.And we also want to create corresponding route table.So let's call it demoroot table in thisVPC.And I'm not going to create a public route here,but just one thing, make sure that you associate this route table with our Subnet.Okay. Everything is done.Now, let's go to the EC2 instance and launch one of the instance.Let's call it demo again.And heremake sure that you use your key pair and edit this network to select your VPCand then your Subnet.Now, here we want to assign the public IP to the instance because ultimately,we want to connect over the public IP.And this is how typically, you know, when you start withAWS, your setup will look like, right?And most of the time some students make mistakeswhere they accidentally launch the instance in a private Subnetand then they are not able to connect.So I would say this is one of the, you know, very basic problem that most of, you know,newcomers in AWS face.And in that case VPC reachability analyzer can help you to detect such problems.Ok. Moving on.So we are assigning the public IP and we are going to create our own security groupand in the security group, we are going to openSSH from anywhere. That's it we want,and that's it. Let's launch the instance.Now at this moment, if you see the instance, it has a public IPand once it is in a running state, we will try to connect it overSSH.So let me pause this video for maybe 30seconds till the time instance comes in running state.Okay? So instance is in running state. I'm just grabbing its public IP.OK? So let me put the public IP and then load my SSH keywhich will be of course your private key. So I'm loading my PPKfile and opening this connectionand as expected, it won't workbecause we launched this instance in a private Subnet.Okay. So you may getsomething like this which is expected.Now let's go to the VPCreachability analyzer and also check from there.Now earlier this reachability analyzer was part of the VPC console.But now AWSstarted moving this kind of, you know, tools into a Network Manager.So in your case, it could be under the Network Manager.So open the Network Managerand if you go on the left side, you will see this reachability,analyzer under monitoring and troubleshooting.Now here make sure that you are in the Mumbai Region.Andwhat we want to do is to create and analyze the path. So let's give it a name, demo,EC2EC2pathand then source type is internet Gatewayand let's select demo, internet Gatewayand destination is EC2 instance which is here.And that's ourdemoinstance.Now protocol, it supportsTCP and UDP. So SSH works on theTCP. So let's select thatand now let's create and analyze the path.So it generally takes like 10-15 seconds for this path to be tested.So let's wait for a couple of seconds.You can just refresh this.So if you want, you can just wait and refresh this.OK? Soit has analyzed the path and it says it's not reachableand this was expected, right?And if you just go down and see the reason,it tells that the route table does not have the route to the internet Gateway.And that's exactly what's the problem.And if you just do something like reverse the path,it will tell you that this route table which is demo RTdoesn't have route to the destination.And this is how you exactly know where the problem is.Now, of course, what you can do is you can just go to your route tableand now modify this route table and say if the traffic is going to the internetor coming from the internet, then it has togo through the internet Gateway.Now, once you do that, you can just come here againand you already have this path. So you can just analyze the path again.So just say confirmand then it will again, start analyzing the path.So let's wait for again a couple of seconds.Okay.Now this time,it says the reachability status is reachable and itwill exactly show you how the path is getting traversed.OK. So this was expected. Now let's modify the Network-Access-Control-Listand then see how it affects the traffic.So let's go here and let's go to thesubnets.So we have this demoSubnet and there is a Network-Access-Control-List.So you can just go down here and click the Network-Access-Control-List.So it will take you here then look at the inbound rules.So this is inbound rule which allows all the traffic.So let's go and remove this rule and save the changes.Now again, let's go here and analyze the path and confirm,okay, it failed as expected.And if you just go down,it tells you that Network-ACL does not allow the inbound traffic from this Subnetto the VPC,right? And it is exactly the reason also shown or marked as in the red.Soyou know that what's the problem?Now, let's try the last scenario which is blocking at the security group level.So let's go back to the Network-ACL, let's go back to the inbound ruleand make sure that we add our inbound rule back with all the traffic from everywhere.And now instead let's go to thesecurity groups hereand we had this demo security groupand in the inbound rules, we had this rule.So let's now delete this rule and save the changes.Okay, let's go back here and analyze the path again.So it will start the test againand nowlet's wait again for a couple of seconds.Okay. So as expected, the result is not reachable. And here it exactly tells you thatSG rule mismatch, that means there is no rule which allows the inbound trafficand that's what we were expecting.So that essentially is the network reachability analyzer.And it is very helpful when you want to troubleshoot anykind of networking issues between any source and the destination.Finally, if you have done this exercise,just go and terminate your machines because you may be otherwise paying for those.So that's it for this lecture.And next, we will see network access analyzer which is quite similar,but the purpose is different.So see you in the next lecture