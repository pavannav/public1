# 6.1 Basics of Network performance - Bandwidth, Latency, Jitter, Throughput, PPS, MTU

All right.Now in this section let's understand the VPC network performance and optimization.And this is also one of the important topic for your exam.Because given a scenario you should be able to decide which will be the best option to optimize theoverall network performance.And when we say network performance, it could mean different things.For example, how much the network throughput should be or how much should be the bandwidth.So by choosing the right configurations, you can optimize the network when you have to typically transferthe data from the source to the destination.Now, before we get into the details of all the controls that VPC provides, let's first understandthe basics of network performance.And in that we will discuss some generic terminologies that we use to define the network performance.So very first is bandwidth.We talk about bandwidth all the time.So what does bandwidth mean.It means maximum rate of transfer over the network.So if you are getting say, 25 GB s bandwidth, typically it means 25 gigabits per second.Understand that it's not byte, it's bits per second.Now the another parameter that affects your network performance is the latency.And typically latency is measured in terms of delay between the two points in a network.And in further latency can be seen as the delay, which includes the propagation delays for the signalsto travel across the medium.Because you know that the signal cannot travel at the speed of the light, right?We can't achieve that speed, and this introduces additional delays further as the packet goes throughdifferent hops.There are networking devices like routers which may further add the network packet processing delays.So together these all constitute to additional latency.Then there is a term called jitter right.So jitter means variation in the inter packet delays.Now Jeter sometimes is not good for, you know, a seamless experience for the network.Maybe if you are watching a video, right?And if there is a Jeter, you will see that you are not getting a consistent, uh, rate, uh, network.Right.And that's where you will see the problem, right?Then comes the throughput.So throughput is the rate of successful data transfer.And it is also measured in bits per second.Now if you talk about the throughput uh, bandwidth latency and packet loss directly affects your throughput.So even though you have bandwidth of say, one Gbps, it does not mean that you will get a throughputof one Gbps, because there would be latency and there will be a packet loss due to which you have toresend the packets.Now all that ultimately results into the throughput.So don't consider that you have bandwidth of one Gbps.That means you will get throughput also of one Gbps.It's a rate of successful data transfer bits from one, uh, endpoint to another endpoint.then packet per second.This is more with respect to, you know, processing of your packets by your machine or a CPU.How many packet it can process per second.Right.Now it's possible that you have, say, a 25 gigabit per second of bandwidth of the network.But is your CPU of your machine is capable of processing those packets at it arrives into your machineor leaves your machine.So you would need to have corresponding compute power as well.Okay.Which means that the computing power that you have also affects the network performance.And now one more important thing with respect to the network performance is the MTU or maximum transmissionunit.And it means the largest packet that can be sent over the network.And it is not always the same depending on your network devices like routers and gateways.This MTU may change and as you can imagine higher the M2.That means you can send more data in a single package, and lower MTU means you can send less data ina single packet.So higher the MTU, more the throughput.Okay.So let's talk about MTU right.So consider there are two hosts host A and host B and host A wants to send some packet to host B.It could be an SSH.It could be a ping anything.Right.So host A initiates that connection and it sends a packet.Now typically that packet is uh 1500 bytes.That's a standard over the internet.Uh you will have the packet of size of 1500 bytes.But uh, if you increase this MTU, that means what if you want to send a larger packet right now,that would depend on, um, all the network hops you have or routers you have in between.Do they support the packet transmission?Uh, for the frame of size, more than 1500 bytes?If not, then maximum that M2 can have between two hosts would be 1500.But in certain cases.And we will talk more about that.You can also send the packets in the form of jumbo frames.Jumbo frames are basically a packet which are greater than 1500 bytes.And then we will see how in AWS you can use jumbo frames to improve the network performance.Because you can see now, right, if, say your jumbo frame is of size 9000, that means almost youare sending the six times the data.You would send in one packet of 1500 bytes.That means in fewer number of transmissions, you would have those many bytes delivered to the targethost.So most of the network supports the M2 of 1500 bytes.That what.That's what we just talked about.But the jumbo frames are the packets which are larger than 1500 bytes, and it goes up to 9000 bytes.In AWS it is 9001 and I'll talk more about that.Now, what are the benefits of using jumbo frames?Of course you have to send less packets.That means you will get more throughput, because sending every packet also means you have to send it,acknowledge it.If it's a TCP connection and there is a lot of processing out there.And then increasing the MTU increases the throughput.Because suppose you want to send a one GB of data across two hosts, and if size is 1500, you can doyour maths and understand how many packets needs to be sent.And then those packets needs to be processed at the receiving end or even at the sending end as well.So that's called packet per second.Now if you would have jumbo frames that means there are less less packets to process.So even with lower PS you can send the same amount of data across the host.So a jumbo frames gives you that benefit.So uh, now let's understand how the jumbo frame works frameworks, right?So how this M2 is decided when you send the packet.So for example there are two hosts.And from this host you want to send some packets to this host.Now you send first a packet of size say 1500.Now every packet has some flags associated with that.And those flags has certain meaning out of this.We'll be interested in one flag which is called DF or don't fragment.So when you send a packet with the df equal to one, that means don't fragment.Don't further divide this packet into a smaller packet.Right.Then router will check at its end.Uh, can I support the packet of size 1500?It is.It says okay.Yes I can process that.And it will take that packet and route to the next, um, uh, hop basically.Now it will reach to the next router in the path.And then again, the same question.Can I process the, uh, m2 of 1500?Now, for some reason, Maybe this router does not support the packet of size 1500.It can maximum processes 1000.Now in this case we have set the DF equal to one.That means this is.This router is not allowed to fragment this packet into two packets.Now like one maybe of size of 1000 and other.Maybe a size of 500.That's we are saying don't do that with this fragment equal to one.Now in this case you will uh, it will say it's not okay with me to process 1500.And even you have set the DF equal to one.It will send the response back to the originating server.And this is typically an ICMP response.Uh, and it will say change MTU to 1000 because I can't process that.So in this case now this host would understand that my network only supports the M2 of 1000.It will then send the smaller packet and it will say packet with size m2 1000.And then ultimately it will reach to the destination.Now in this case it means, this host A would have to send two packets instead of one packet.So you can also do this MTU path discovery using some Linux commands.And I'll show you how to do that.So if you see this we are talking about MTU of 1000.This network supports and this is just an example okay.So for MTU path discovery as I said the response goes over ICMP.So ICMP must be allowed between these hosts okay.Just remember that.All right.So until now we talked about the M2 and jumbo frames in general.So now let's talk about the jumbo frame in the context of AWS.So first thing AWS supports the jumbo frames up to size 9001 bytes.And it is not like for all the scenarios AWS supports this M2.And in the rest of this lecture we will understand different cases in which different Mtus are supported.All right.So this means that AWS supports jumbo frames.And with respect to the VPC, it is enabled by default.Now to be honest, it is like EC2 instances which will initiate the traffic.So you have to really configure that on the instance level when they transmit the traffic.But VPC doesn't restrict that.So AWS supports the jumbo frame within a VPC of say 9001 bytes.But if the traffic is going over the internet, through the internet gateway or the Nat gateway, thenthe packet size is limited to 1500 m2 only.Which means for the internet bound traffic, the jumbo frame is not supported.Now further, you know that you can also peer the VPCs, and these VPCs can be in the same AWS regionor across the regions.Now depending on that, if the VPC peering is within the same region, then the jumbo frame of 9001byte is supported.But if the VPC peering is across the regions, then the jumbo frame size is 8500 bytes.Now remember that until March 2025, Five.This scenario wasn't covered under jumbo frame, and you were limited to 1500 bytes for inter regionVPC peering, but now it has been increased to 8500 bytes.Further jumbo frames are also supported when the traffic goes over the direct connect between the VPCand the on premises network.And the size of jumbo frames really depends on how you are connecting over the direct connect.For example, if you are using the private VIF, public wave or transit wave and we will shortly discussthat next.You can also maximize the performance using the jumbo frames.If you are having your EC2 instances in a same cluster placement group, and which means that the EC2instances are launched together, typically on the same physical hardware, so that there is not muchnetwork hop and you can maximize the network throughput.So while using the jumbo frames, if you launch the EC2 instances in a cluster placement group.It gives you maximum network performance.And finally, remember that it's not like you just enable the jumbo frame while sending the trafficfrom EC2 instance, and it will increase your performance because you know that if jumbo frames arenot supported on the intermediary network devices, then you may experience the packet loss becauseif the packet of size over 1500 bytes are sent and the don't fragment flag is set, then that packetmay be dropped.So just remember this.Okay.With that let's understand how can you define the M2 on your EC2 instances inside a VPC.Right.So M2 also depends on instance type.Certain instance type supports jumbo frame.Certain may not support jumbo frames.And there is a list of EC2 instances.All typically current generation instances do support the jumbo frames.Jumbo frames or M2 is defined at any level, so if EC2 has multiple Enis, you can control or you canconfigure uh, how much, What should be the, uh, should it support the jumbo frame or not?Or what should be the size of the packet you want to send, uh, from this EC2 machine?Now, you can also check the path discovery MTU path discovery between your machine and the target endpointusing the command something like Trace path.And I will talk more about that shortly.Or we'll just see that in a console.And you can also set or check your MTU on your network interfaces for EC2 using a simple command likeIP link show uh, and then the network interface ID, uh.And finally you can also set your MTU, as I said, uh, using this command IP link set dev and thenyour interface ID and then you set your MTU values there.So important to understand here is that, uh, you can uh, the, the uh, sorry, MTU are defined atany level.And you can also set that, uh, using a Linux commands.Okay.With that, uh, let's, uh, do a quick demo.In this demo, what we are going to do is, um, we are going to launch two EC2 instances.Uh, and then we'll check the M2 using EC2 public IP.That means from one EC2 we will reach to another EC2 using public IP.That means the traffic goes over the internet, even though they are part of maybe a same subnet doesnot matter.And then we'll observe the M2 in that case.And then we will also, um, check the M2 when we are reaching over private IP.That means the communication is within the VPC.So we are um, assuming that the jumbo frames would be used with M2 9001.And in the this case probably we are expecting jumbo jumbo frames are not used and the packet size is1500 bytes only.And uh, we will just check the M2 on that EC2 interface as well.What values it shows.So let me just quickly go to my console.I have already two.I have two instances here.Nothing fancy about it.Both are in public subnet.And what I'm going to do is just to connect to the first instance.So this is a shortcut to connect to my instance.And what I'm going to do is just grab the maybe a public IP address of the other instance so that Ican check the MTU.So this is my second instance.I'm just going to grab its public IP.And what I'm going to do is just trace the path and the IP address.Now you can see that uh, initially it started with 9001, but then it defaulted to 1500 because trafficis going over to the internet.Now it will try 30 times and it will see all possible path to the target destination.And then it will show you ultimately uh, path MTU for uh, this communication.So I'm just watching this video for a couple of seconds.Okay.So this has been completed.And you can see the final, uh, MTU is 1500.And that was expected.Now, what?I'm, I'm trying to do is now just do the same thing.But this time using the private IP address of my second EC2 instance.And let me see if I have the private IP.No I don't, so I'll just grab it here.So this is a private IP address.I'm just copying it and just putting it here.And now let's see what happens.So again I'm going to just pause this video for a couple of seconds.And then I'll show you this.Okay.As you can see here my trace has completed.And in this case the M2 is 9001.That means it's using the jumbo frames.So this implies that within VPC, jumbo frames are enabled.And they are used for all within VPC communication.Right.Now one more thing I just wanted to show you how to see the jumbo, uh, how to see the MTU at interfacelevel.So you can just say IP link show and then your interface ID.And here you can see the MTU by default is set to 9001.So that's it.What we wanted to see here and just going back here, we just did this demo.You can try yourself as well.Uh, there are commands already given here.Now, finally, what we want to understand is that, uh, what are the different MTU values when, uh,there is a communication between VPC and other entities?Like it could be like over the internet, it could be within VPC, it could be over private network.Uh, like a direct connect to your on premises.So let's uh, see those values.So within the AWS if the traffic is within the VPC then jumbo frames are supported.So uh, imagine that there are EC2 instances and then these instances communicating with each other.They are part of VPC.So by default jumbo frames are enabled.Now if the traffic is flowing over through the VPC endpoint, that means if you are trying to reachto the other AWS services, for example, to the S3 or DynamoDB or, um, SQS kinesis over the VPC endpoints.Now in that case, the M2 of size 8500 bytes is supported.That means again, jumbo frames are supported.Now what are VPC endpoints?We will learn that in the next lecture.Now if the traffic is going out to the internet via internet gateway, then again the jumbo frames arenot supported.So you are limited to 1500 bytes only.Further, with respect to the VPC peering connection, the intra region VPC peering supports the jumboframe of size 9001 bytes, but if it's an inter region VPC peering, then the jumbo frame of size 8500bytes is supported.And remember, this is relatively new update.Now further, if you're connecting your VPC to the on premises network, then there are different scenarios.For example, you may connect over the VPN using the Virtual Private Gateway.Now in that case, jumbo frame is not supported and MTU is limited to 1500 bytes.And same is the case if you are using Transit Gateway to connect over the VPN.So in both the cases, jumbo frames are not supported.However, if you are using the Direct Connect, then depending on which virtual interface that you use,the MTU differs.So if you are using the private VIF using which you can connect to the VPCs directly, then the jumboframe of size 9001 byte is supported.And otherwise, if you are using the transit wave, which ultimately uses the Direct Connect Gateway,which connects to the Transit Gateway.In that case, jumbo frame of size 8500 byte is supported.And otherwise, if you are using the public wave which connects to the AWS public endpoints, then jumboframe is not supported and the M2 is limited to 1500 bytes.So this is all the MTU values that you should typically know while using the jumbo frames.So that's it about the jumbo frames and the M2.And with that now let's go to the next lecture.