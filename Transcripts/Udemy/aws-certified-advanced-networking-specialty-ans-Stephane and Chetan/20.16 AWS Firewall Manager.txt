# 20.16 AWS Firewall Manager

Hi.So in this section so far we talked about different firewalls starting with security group network ACLs,then AWS Network firewall, Web Application Firewall, and then AWS Shield and Shield Advance, andalso Gateway Load Balancer through which you can use third party firewalls.So it works well when you have, say, 1 or 2 AWS accounts where you can go and configure these firewalls.But imagine a situation where there are hundreds of AWS accounts, and then you have to have consistentnetwork security policies or firewalls deployed across those AWS accounts.And as you might have guessed, in that case, you need a centralized service which can manage thisfirewall across all these accounts.And that's where AWS Firewall Manager is used.So AWS Firewall Manager is a security management tool which simplifies the security, administrationand maintenance tasks across AWS accounts and resources.When I say resources, that means which AWS resource you are protecting.Uh, using the firewall.For example, security groups of EC2 instances or network load balancer or particular route 53 hostedzones or particular instance behind the network firewall.Likewise.So AWS Firewall Manager can manage the rules for AWS WAF shield, advanced VPC Security Groups, NetworkFirewall, and route 53 resolver DNS firewall.Now it works at AWS organization level.That means if you are adding new AWS account to AWS organization, then all the policies are automaticallyapplied to that new account as well.So if you are aware of the concept of AWS landing zone, this is kind of goes well with the landingzone concept, where you can centrally manage the firewall policies through the AWS Firewall Manager.And additionally, Firewall Manager also provides you the centralized dashboard for monitoring the DDoSattacks.Now again, this is across all your accounts within the AWS organization.So in order to use firewall manager, you have to do some prerequisites.For example, you have to enable the AWS organization.That is all features of AWS organization.You have to enable the AWS config service, which is basically, you know, inventory of your all AWSresources, and it tracks the changes to your AWS resources.For example, if you change the security group, it is recorded in AWS Config service.So you have to enable this service as well.And then you have to enable the sharing of AWS resources across your AWS organization.And that is done through AWS Resource Access Manager.That is Ram.Okay.So once you have the AWS Firewall Manager, then what you do, you write the policies and rules.In this.You define that what kind of firewall I want to deploy in which all AWS accounts and for which all AWSresources.So you define the scope and then once scope is defined, you can also define the actions if there areany compliance violations.For example, if you define the rules, say none of the security group in your AWS account should haveport 22 open to the internet.So if you now create a new security group which has the port 22 open, then firewall manager will detectthat and one it will trigger the notification saying that there is a compliance violation.And second, it can automatically remove that rule from the security group.Now it really depends on you whether you want to implement the automated remediation or not or otherwise.You can definitely have the detection mechanism in place.So this is how Firewall Manager works okay.So again with respect to the services that it supports.So it supports VPC security Group, Web Application Firewall, AWS shield Advanced Network Firewalland AWS route 53 resolver DNS firewall.Now we will see resolver DNS firewall in the route 53 section.But essentially that means network firewall can manage all the rules and policy for all these AWS services.Now additionally, you can also use third party firewall.For example, you can subscribe to AWS marketplace and you will see a lot of firewall options in there.So if you want to manage, say, Palo Alto Cloud, Next Generation firewall or Fortigate Cloud Nativefirewall, then they can also be managed centrally using the firewall manager.And as I said, firewall manager can detect the any violation to the compliances.And it can also perform the action to remediate that.So all this can be recorded in AWS Security Hub through the notifications from the firewall manager.And then in the security hub you can further integrate with your ticketing system.For example, you will automatically create a JIRA ticket or you can centrally, you know, view allthe violations in the dashboard that Security Hub provides.So this is how you typically work with AWS Firewall Manager.Right.So in this section we talked about a lot of things.And many a times it is overwhelming with respect to all the AWS security services out there.And we haven't still talked about few of the other AWS services.But often the question comes when to use what.So I just try to, you know, put some guidelines here.So for example, if you want to have a quick and easy way to deploy the firewall for protecting theweb application at layer seven, then you should rather use AWS Web Application Firewall, that is WAF.Now the keyword here is easy and quick.And layer seven attacks, right?Similarly, if you want to do that for the DDoS attacks, you might have guessed it should be AWS ShieldAdvance.And if you need more sophisticated firewall for your VPCs for IPS and IDs systems, then you shoulduse AWS Network Firewall.And if you need any third party IPS and IDs systems so that you can bring in into your VPC and you canroute your traffic through those third party firewalls, then you should be using AWS Gateway Load Balancer.And if you want to manage everything at one place centrally, then you should be using AWS FirewallManager.So Firewall manager itself doesn't provide you the firewall.It is just a management of firewall, which means all these AWS services that you see are not mutuallyexclusive, but you can typically use them together in different architecture because the purpose isdifferent.Right.So I hope this short summary also helps you build that mental model around different AWS network securityservices.And you should be able to.Use right answer in your exam based on what we covered in this section.Right.So with that we'll stop for this lecture and let's see what's next there for us.Thanks for watching and stay tuned.