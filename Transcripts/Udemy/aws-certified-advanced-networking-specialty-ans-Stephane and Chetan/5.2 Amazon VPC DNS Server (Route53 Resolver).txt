# 5.2 Amazon VPC DNS Server (Route53 Resolver)

Hi.So in this lecture let's understand everything about Amazon VPC, DNS server.Now for some historic reason there are different names for the same thing.So whether we are talking about Amazon VPC, DNS server or route 53 DNS resolver or local DNS serverfor the VPC or Amazon provided DNS server all means the same.Just that because AWS is adding more and more features, the name also got changed over the time.But essentially the purpose of this DNS server is to resolve the DNS queries for the resources insidethe VPC.And typically when we say resource, we are talking about the EC2 instances.So there is a VPC, there are subnets and then EC2 instances.Now if any of these EC2 instance has to resolve some DNS for say google.com or other, if you have multiplemachines within the same VPC and you want to access that machine over the DNS rather than IP address,then how that happens.Now, whenever you create a VPC, AWS will also create something called a default DNS server for thatVPC.And this DNS server sits within the VPC.But the question is on which IP address does it run?So we have talked about this briefly in the earlier session.But your route 53 DNS resolver runs on the VPC base address plus two.So if your VPC IPv4 cider is ten .1.0.0, then this DNS resolver will run at ten .1.0.2.And it can also be accessed over a virtual IP that is 169.254.169.2 53, but note that this IP addressis only accessible from within the VPC, right?So VPC base address plus two as well as this virtual IP.Now what is the role of this DNS resolver.Of course role is to resolve the DNS queries from the EC2 instances.So which all types of the DNS does this DNS resolver resolve?So there are typically three categories.Very first, if you need to have a private DNS for your VPC resources.So in the introductory section we talked about the private DNS where you can have say a domain nameexample.com for your VPC.And then for different EC2 instances you can have different domain names.For example app one dot example.com, app two dot example.com or db dot example.com.Now this you can configure using Amazon route 53 private hosted zone.So this DNS server can resolve the private DNS which you configure in the route 53 private hosted zone.Other than that it can resolve the Amazon provided DNS names.So if you remember, whenever you launch EC2 instance, it gets a private and public DNS names.Now, this resolver can also resolve those DNS to corresponding private IPS of the EC2 instances.And third, any public DNS name.So if these EC2 instances needs to communicate with, say, google.com, then this DNS resolver willforward all the public DNS queries to the public DNS server and will get you the IP address.Now this is also called as recursive DNS lookup.So it can do that.And just a thing to mention that whenever we talk about the public DNS, it means everything on theinternet, including Amazon Route 53 public hosted zone.Because public hosted zone contains the publicly resolvable DNS records.So that also sits in the internet or public DNS space.So this route 53 DNS resolver can also resolve any public DNS queries.Right.So as I said, it can resolve the DNS from route 53 private Hosted zone or VPC, DNS, which is managedby AWS and then any public DNS.And one thing to note that it does that in the same order.That means, for example, you have google.com as a private hosted zone.Then even if you are trying to reach to actual google.com, which is a public DNS, it will not lookat that.It will try to resolve that query from this private hosted zone.And we are going to do some exercise around this so that it is clear to you.So there is a preference.So first it will check the private hosted zone.If it exists then it will try to resolve the DNS query from there.But if you don't have private hosted zone then it will look at VPC, DNS and if not it will go to thepublic DNS.So that's the order.And at this moment we are saying that this DNS resolver is accessible only from within the VPC, whichis not 100% true.But just for this lecture, let's try to limit our scope to the VPC only.And towards the end of this section we will talk about the resolver end points, which allows this resolverto be accessible from outside the VPC network.Right.So I hope that is clear.Now before we move again quickly, every VPC comes with a default DNS server, also called as route53 DNS resolver.It runs at VPC base address plus two and also available on the virtual IP, and it resolves the DNSqueries from route 53, private hosted zone, VPC, internal DNS, and any public DNS.So with that, now let's try to see how this resolver resolves the DNS queries across these three typesof the DNS.So let's look into that.So very first scenario, how does it resolve the DNS queries from Amazon route 53 private hosted zone.Now for that, you need to first understand what is route 53 private hosted zone.So this is basically a you can say a private DNS for you.So if your VPC needs to have its own domain name, for example say example.com.And then this first server could be then app dot example.com.And the second server could be db dot example.com.So in that case app server doesn't need to know the IP address of the DB server.Rather it will say db dot example.com.It will be resolved using the private hosted zone.And then it gets the IP address.And then it will connect directly to that DB server.So this you can do using Amazon Route 53 private hosted zone.So of course you need to first create a private hosted zone.And you can use any name for that.Just an example.I'm using example dot internal.But as I said, you can even use google.com as your private hosted zone.It doesn't clash with the publicly available google.com.Now, once you create the private hosted zone, you have to create the records.So in these records, you map your domain name for a particular instance to its private IP address.So in this case you will have two records created which you say app one dot example.com which pointsto first ip and app two example.com which points to this second IP, something like this.Now at this moment, if first instance wants to communicate with the second instance, it will use theDNS name app two dot example dot internal.And in that case the DNS query will first go to this DNS resolver.It will check if there is a private hosted zone with the name example dot internal, and if it is there,then it will try to get the IP address for corresponding record, which is this, and then it will getthe IP address back.It will give that IP address to first instance.And first instance now will connect to the second instance.Using that private IP address.So this is how the entire DNS resolution happens right.So I hope that is clear.Now let's look at the second scenario.Now this is very much AWS managed DNS.As I said whenever you create EC2 instance, AWS may assign the private and public DNS names for yourEC2 instances.So that is also resolved by this DNS resolver.So the EC2 instance names are typically in the format IP, dash private IP address, then region name,dot compute, dot internal.And just an example like this or this.And then if any of these instance tries to communicate with other instance using this DNS name, theycan very well do that.And in that case the flow will be similar.The DNS query goes to the DNS resolver.It looks at the VPC DNS records, gets the IP address, and then the instance will connect using thatIP address.Now this is not very helpful I would say because typically you will rather use your own domain namerather than using AWS provided DNS name.So it makes sense to rather use route 53 private hosted zone instead of using this VPC DNS.But I just wanted to, you know, mention it here for the completeness of how it actually works.Okay.So we saw the two types.Now third type is the public DNS which is fairly simple.In this case the EC2 instance will actually make a DNS request for any public domain name.And this server is available somewhere in the internet.Now, in that case, your DNS resolver will make a recursive DNS lookup for, you know, public DNS.Now this public machines will be assigned this domain name using some domain registrar.And there are so many domain registrar out there.Depending on which domain registrar owns this domain name, they will be responsible for resolving thisDNS queries.So here even route 53 public hosted zone sits into this public DNS space.So you can have your own public hosted zone.And you can create your own DNS records which are resolved over the internet.And we are going to talk about this in more detail in the route 53 section.So you get the public IP back from the EC2 instance, and then EC2 instance will make direct connectionto the publicly available server.Now just remember that we are not talking about the network layer connectivity between the EC2 and thepublic server.For that, of course, your VPC needs to have internet gateway, or if these instances are in the privatesubnet, then you will need Nat gateway.All that is given here we are just talking about the DNS resolution.And for that if you see you don't need any public connectivity for sending actual traffic, you needthis internet gateway here.So remember this distinction right.So we talked about a lot of things though basics but very important for your exam because I'm sure therewill be some questions around this route 53 DNS resolver.Now before we stop for this lecture, I just want to make one more point.Now, you know that there is this DNS resolver in every VPC.But the question is how does EC2 instance knows that there is this DNS server and it is running on thisIP address that is VPC base address plus two.Now this is a part of Dhcp that is dynamic host control protocol.So every VPC comes with some default Dhcp configuration.And you can modify this information to change your DNS server.By default, VPC will have the configuration, which says that for this VPC, the DNS server is thisroute 53 resolver.But as I said you can modify this configuration.And all that happens using something called Dhcp option sets.So every VPC comes with a default Dhcp option set in which you would have these configurations.And in the next lecture we are going to talk in detail about this Dhcp option set.But in a nutshell this Dhcp option set tells the VPC resources where to go for DNS queries.And in that case that is Amazon provided DNS server, which is nothing but this route 53 DNS resolver.So VPC comes with the default Dhcp option set, which provides this dynamic host configuration to theinstances when they launch.And as I said we can change these configurations.So that's it about this lecture.Now let's look at the Dhcp options sets and other DNS related configurations for your VPC.So thanks for watching and stay tuned.