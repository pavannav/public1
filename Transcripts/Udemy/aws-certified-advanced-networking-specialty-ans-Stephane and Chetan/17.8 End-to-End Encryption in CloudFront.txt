# 17.8 End-to-End Encryption in CloudFront

So, how do you enable end-to-end encryptionbetween CloudFront, your ALB,and your EC2 instance to use HTTPS all the way?So on the end of CloudFrontyou must specify the Origin protocol policyto be HTTPS only.So CloudFront communicateswith the ALB using HTTPS and then your custom Originwhich is a load balancer in this instancemust have an SSL or TLS certificate that is valid.And the certificate domain name must beeither what we set for the Origin domain fieldwhich we configure in CloudFront,or if we're using the Host header from the clientsand it's forwarded to the Originthen the host editor must match the domainof the TLS certificate on your ALB.So, because the domain itself is going to be verifiedby CloudFront, you cannot use self-signed certificate.It must be a certificate issued, for exampleby AWS Certificate Manager or by a third party service.Now, the configurationon your application load balancer is that, as I said,you must use a certificate providedby the AWS Certificate Manager or third partybut then imported into the AWS Certificate Managerand then your load balancer will have correct configuration.And then of course you must setup your EC2 instance to have SSL.And so, as you know, or as you may knowthe AWS Certificate Manager Service, ACM,is not supported on EC2.You cannot export certificates from ACM.So therefore for your EC2 instance to set up SSL or TLSyou must use a third party,or you can use a third party SSL certificatefor any domain name actually,because there's no verificationor you can even use a self-signed certificateon your EC2 instance,because the ALB is not goingto verify the certificate itself.It's just going to use the certificateto encrypt the connection.Once you have all that in placethen you will have end-to-end encryptionbetween CloudFront, your ALB, and your EC2 instance.I hope you liked it and I will see youin the next lecture.