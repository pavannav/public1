# 19.27 Hands On- Route53 Resolvers - Part 3 - Resolver endpoints

Okay.So this is step three.Now where we need to set up the rest of the things.And that will be primarily around this route 53 resolver endpoints.Right.And here first thing we need to do is to make sure that on premises can resolve the cloud.com domainname.And for that we need this inbound end points on the AWS side of the VPC.So for that we need to create this inbound end points as I said.And these endpoints are Enis which means they also have the security group.So we will allow the UDP traffic on port 53 from the on premises VPC cider network range.So let's follow these steps and create these inbound endpoints.So let's first create the security group and then let's create these endpoints.Okay.So let's go here on the left side in the EC2 console or in the VPC console as well.You can go and create the security groups.So let's create a security groups and let's call it route 53 Inbound Resolver SG.Or you can also call this cloud site and the VPC should be the cloud one.Let's add inbound rule and say udp DNS 53 from the 192.168.0.0 slash 16.And that's it.Let's create the security group.Now let's go to the route 53 service.And here let's go to the inbound endpoints.And here make sure that you are in the right AWS region.Let's create the inbound resolver endpoint.Let's give it a name.The VPC should be the cloud VPC.The security group that we just created.This one and endpoint type is just IPv4.As of now we don't have IPv6 traffic and protocol.Let's keep it as it is.Now we need to create this in two different subnets and that will be in the private subnets.So let's select this private subnet.And we can also select another AZ and another private subnet.Alright that is done.Let's create this inbound resolver endpoint.Okay.Now endpoint has been created.Next thing is we need to configure this IP addresses in the on premises side of the DNS server to forwardtraffic for the Cloud.com to these IP addresses.And here are those two IP addresses okay.So we are done with this.Now let's go here on the on premises DNS server.And then add this zone for Cloud.com in the same named.conf file.Now for this again you can refer to this text file here which is there as a part of your lecture.So I'm just copying this and I need to replace these two IP addresses with the inbound resolver IP addresses.Now just remember, one thing is that you should be on the DNS server many a times.There is a confusion and you might set this up in the wrong server, so make sure that you are there.Okay so let's do sudo VI slash etc. name t dot conf.Let's go towards the end and paste this content.Now I need to replace this end points.So it should be ten .0.10. something.So it's a 10.2 24 and 11 dot 210.So ten dot 224 and then ten .0.11. 210 and then remove rest of the things.Right.So that's done.Let's save it.And we need to restart the service.So again let's do sudo systemctl restart nemty dot service, and you can also check the status.Yeah it is running.Okay so we are done with this.Now next thing we need to check if we can resolve the cloud domain name from the on premises app server.So again let's go to the app server and try to resolve app.cloud.com domain name.And let's check this Nslookup app dot cloud.com.And this doesn't resolve which is not expected.And it may happen right.Because sometimes the DNS settings change automatically.So for that you need to just check slash etc..Resolv.conf.And you can see it has been reverted back.Now, in order to permanently set this, there are few additional things you might have to do, butto save time, let's just modify this file again and configure it for the DNS server that is ten .31.And let's try again.And now it resolves to the app server on the cloud side.Right.And you can also do ping.And it works right.So that means from on premises to AWS cloud domain we are able to resolve the DNS queries.Now as I said, in order to permanently set this name server, you can either change the DHCP optionset for the entire VPC, or you can do that at the EC2 instance level by modifying this file here.So there is a file called Ifcfg for eth0.And there at the end you can add DNS one equal to 192168.10.31.And then this setting will persist basically.Right.So again we can resolve the DNS.And that's what we wanted to do.Okay, so we have won half the battle.And again congratulations for this.Now the last part where from the cloud side we want to resolve app dot on prem com domain name.Right.And for that we need to set up the outbound resolver here.And for that we again need to have the security group.And then we will create the outbound resolver.And then we will add a condition saying that if the domain name is on prem.com then the traffic shouldgo to the DNS server private IP address.Right.So let's do this.Okay.So let's go to the security groups again.And let's create another security group.And let's call it say cloud route 53 outbound resolver SG.It has to be in the cloud VPC.And we don't need inbound rules.We just need outbound rules.Generally, the default outbound rule is okay, but in order to make it a little specific, let's callit 19216800 16.And here again it should be DNS UDP traffic.So let's create the outbound rule.Next let's go to the route 53 outbound endpoints.Let's create the endpoint with the name that we had created in the cloud VPC.The security group for the outbound one endpoint type is IPv4 and protocol is the default one.Now again it will be in the same private subnet.So let's select both the subnets one by one.And that's it.Basically let's create the outbound endpoint.Now once the endpoint has been created now here let's create the rule let's call it say on prem ruleand traffic is forward.Domain name is on prem.com.The VPC is cloud VPC.That means it will only accept traffic from this VPC, and the endpoint is the one that is being createdand the target IP.So we know that it is 192.168.1.31.That's it.Let's submit this and let's wait for this endpoint to be created.Okay.So we are done with all these settings.And now if everything is okay then we should be able to resolve app dot on prem com to the private IPaddress of that EC2 instance.And we should be able to ping as well because we have the right security groups.So let's try this.Okay so I'm into the cloud side of the app server.And you can very well see that from the IP address.So let's do Nslookup app dot on prem.com.And you see it is resolving the DNS.Now That's the core exercise that we wanted to do.And optionally you can also try to ping to on prem com and it works right.And if it is working for you that's what we wanted to do.So we could resolve the DNS from both the sides.Right.And if you could do this and probably troubleshoot some issues on the fly then congratulations.You really did a great job.So it's really time to celebrate.But before you do that the most important thing do the cleanup.The first thing, terminate all EC2 instances and then terminate the VPN connection because VPN connectiondoesn't come in the free tier quota and also delete the route 53 hosted zone and everything that youmight have created in this setup.Right?So remember to do this cleanup and then you can probably celebrate a little.Right.So I hope you enjoyed this exercise.And that's it really.So thanks for watching and stay tuned.Ay ay ay.