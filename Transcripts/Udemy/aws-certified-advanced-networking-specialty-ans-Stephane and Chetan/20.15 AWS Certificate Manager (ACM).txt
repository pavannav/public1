# 20.15 AWS Certificate Manager (ACM)

Hi.In this lecture let's try to understand what is ACM and how does it help you to secure the network communication.Now if you ever heard about the SSL that is secure socket layer or the TLS Transport Layer Security,they provide the security to the layer seven traffic.Right.And to enable the SSL or TLS you need SSL X509 certificates and ACM provides those certificates.So I hope you already aware of these concepts.If not, I will just quickly talk about that and then we will see more about the ACM.So very basic example I can give is the Http traffic.And you know that you shouldn't transact if your bank website is Http or other, you shouldn't accessany website which is over Http.That's because Http sends the traffic over the plaintext.So if you are doing some transaction, your credentials can be easily hacked.Right?So this is not the great situation to be in.So what do you do.You turn to Https.Now when you turn to Https it underlying using the SSL or TLS protocol to protect your communication.So once you do that, all the communication goes over the encrypted channel.And to make this communication secure there are three basic principle the authenticity the integrityand the privacy of the communication.But here let's just try to understand the authenticity because it is helpful to understand the conceptof SSL TLS certificates.So authenticity means that client is talking to the right server.So server has to prove that it is a legitimate server.So how that happens.So that happens through something called SSL or TLS certificate.So server has that certificate.And server also has corresponding private key which never leaves the server.And the first time when client connects to the server the server will send this certificate back tothe client.So now the question is client has the certificate.And that certificate tells the client that it is communicating with the some bank.com.But the question is why will client trust that certificate?And for exactly that reason, this certificate has to be signed by someone to which this client alsotrust.And that's where certificate authority comes into the play.So this some bank.com will first get this certificate signed by the certificate authority and now sendit to the client.And client already trusts this CA.Now how this happens and what all other steps are there to have the integrity and privacy in the communication.I have explained this in very, very detail in my one of the video on the YouTube.So if interested please go and watch that.There is also link in the description of this channel for this YouTube video.But as of now, our focus is only on this SSL certificate which ACM can provide you.Okay, so what's the typical process to get these SSL certificates.So very first thing is you need to have your own domain name.And you can buy this domain name from anyone like Globalsign or GoDaddy or even from Amazon route 53.Once you buy the domain name, you need to request a certificate.Now for that.Typically, you will create a public key and a private key pair using open SSL, and then you will submitthe certificate signing request to the CA that is certificate authority.The certificate authority will validate that you own the domain name and will sign that request andissue you a certificate.Now there are well known certificate authorities, but we will be talking about AWS Certificate Managerhere.Now as you use AWS Certificate Manager, it's much simpler to get the certificate.Basically you just request a public certificate and it will validate your domain ownership and it willissue you the certificate.Now one thing to understand here is that here we are talking about the public SSL or TLS certificate,which means certificate which are trusted by all the clients, which connects to your application overthe internet.But many a times you need the internal communication to be made secure.For example, within your AWS VPCs there are a lot of machines like application server is talking tothe database or all other application server talking to each other.Now this is all internal communication to the VPC.Now in that case, you don't need to have the public SSL certificate, but rather you can have yourself-signed SSL certificate.Or you can get this private certificate from AWS private CA, which is a different offering than theACM.And in this lecture we are primarily focusing on the ACM.That is public certificate part of the Amazon Certificate Manager.So just to reiterate, there are two parts of the AWS Certificate Manager the ACM, which issues thepublic certificate.And this certificates can be deployed on AWS managed services, for example load balancers or CloudFrontor API gateways.Likewise.But for internal use you can use AWS private CA where you get much more control with respect to thecertificate validity.The domain names the root CA, the subordinate CA, the revocation of the certificate, all that ishandled by you using AWS private CA and this is typically called the PKI.That is public key infrastructure.You can also use this private certificate as a client certificate.That means you can issue this certificate into the smartcard.And then employee can log in with their smart card into their system.Or if you have IoT devices and you have mQTT endpoints, then also you can deploy this private certificateto your IoT devices.Now all these are the use cases of the private CA.But in this lecture we are focusing on the ACM which issues the public certificate for your AWS managedservices.So I hope this distinction is clear.Okay, so in general when we talk about the AWS services where you can deploy these certificates.So let's see that now the first one is you directly want to deploy your certificate on the EC2 machines.Now ACM which provides a public certificate doesn't allow that because you can't export the ACM certificateand deploy it on EC2 machines or ECS cluster.If you want to do that, you have to use your self-signed certificates.So this is out of scope for the ACM public certificates.But if your workloads are behind the load balancer or API gateway or CloudFront or Beanstalk, and thereare more services which are called integrated services with the ACM, then you can get these certificatesand deploy that on any of these resources.Now in this case, you can see the SSL termination happens over the load balancer.And from the load balancer to the back end the traffic can be over simple Http.It need not be encrypted because all these resources sits within the VPC.Okay.So let's quickly talk about few features of the ACM.And to be honest, the deep dive of ACM should be part of AWS security certification than the networkcertification.But let's here cover what is important for your network certification exam.Right.So ACM as you know, provides the SSL TLS certificates which you can deploy on this supported AWS services.And this is very seamless.For example, if you are using application load balancer you have to just create the Https listener.And there it will show an option to import the certificate, which is created in the ACM.You just apply that and that's it right?Nothing else needs to be done.Same for the CloudFront or API gateways.Now you can also request the wildcard certificate.For example you have your company domain name and then you can say API dot example.com or say web dotexample.com.Instead of having multiple certificates, you can just have star dot example.com.And same certificate can be applied for all those endpoints.And just one thing that you need to remember is that whenever you are using your certificate for theCloudFront service, which is a global service, you have to create this certificate in the US East,one that is Northern Virginia region.ACM is a regional service.That means if you are using it for the load balancer, you have to create that certificate in that particularregion.Right.So just remember this small bit here.And as you can see here, you have administrator who can issue the certificate through the ACM.And then this certificate can be deployed to different AWS services.And then users terminate their SSL connection on that endpoint.Right.So I hope that is clear.Now a few more things and then we will wrap this session up.So with ACM you know that you can have wild card certificate.But wild card cannot be in the middle of the domain name.So app dot star dot example.com is not allowed.But star dot app dot example.com is allowed.So remember this further all these certificates.And here again we are talking about the public ACM certificates not the private CA.All the certificates are signed by the AWS Trust Services CA.So AWS has a well known certificate authority which is trusted by most of our browsers and the devices.And that's where all these certificates, which are signed by the AWS root CAS, are automatically trustedby all leading browsers like Chrome and Microsoft.So you don't get that small warning that your certificate is not trusted because it is signed by AWSTrusted Root CAS.Further, the public certificate issued through ACM are valid for 13 months, but in case you need highervalidity or customize the certificates, then of course you need to go with the private CA which istotally different entity.And there is a lot of cost for the private CA.The public certificate through ACM are free to use with all those supported AWS services.Now for the renewal of the certificates.ACM will automatically do that if you are using it on the Elbe and the CloudFront.Likewise.Right.And it automatically renews that if you are using the domain validation method, if you are using emailvalidation, then it will start sending you the email about your expiration around 45 days before theactual expiration of the certificate.Then you have to take certain actions to renew your certificate.But again here ACM will renew that just that you need to tell ACM to renew your certificate.And finally you can also import your own certificate.For example, you already got a certificate which is signed by some well-known uh CA then you can importthat certificate along with its private key, so that ACM can then associate that with the AWS resourcesand can handle the TLS termination.But otherwise, for the certificates which are issued by the ACM, it never gives you the private keyit keeps with itself.Right.So that's the difference.So I hope that is clear.And you now know what is ACM and where to use ACM certificates.So with that we'll stop here for this lecture and see you in the next lecture.Thank you.