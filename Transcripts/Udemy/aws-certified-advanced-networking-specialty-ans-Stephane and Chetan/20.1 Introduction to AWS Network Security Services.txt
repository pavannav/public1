# 20.1 Introduction to AWS Network Security Services

Hello, and welcome to this section,AWS Network Security.Now, if you talk about the security in AWS,then I guess there are more than 50 AWS serviceswhich are related to the security.But in this section, we will primarily focuson the security services with respect to the networking.That means the services which can help youmaybe restrict some traffic, or identify some attacks,or secure your network connection.So all those services we'll cover in this section.Now, if you look at the AWS Network Security services,then there is a journey to this, right?When AWS first launched EC2 machines,after a while, AWS launched security groups, right?That's the full service AWS launched.But over the time, over these 12 years,AWS has then launched many other servicesto solve a particular problem, right?Because security is a broader thing,it's not just about restricting certain IP addresses, right?It is much more than that,and we'll learn about that as we go through this section.But broadly, there are two kinds of security services,one which you call preventive services,another, you can call it detective services.Now, preventive services,which you can protect your infrastructureat the network layer,and detective means it can detectby analyzing the traffic which is flowing through.It can detect certain patternsand can see if it is a malicious usertrying to access somethingor somebody's attacking your system.Now that will fall into the detective services.So first let's look at which all AWS servicesfalls into these categoriesand understand purpose of those services.Okay, so it all started with security group.As you know, security groups are attachedto the EC2 machines,and then you can define the allow ruleswhere you can say the traffic is only allowedfrom certain IP ranges and for certain ports.Later, AWS launched network access control list,which are applied at the sub-net level,and there is a fundamental differencebetween how security group and network ACL works.We have already covered that,and we'll revisit that again shortly after this lecture.Then AWS WAF was launched in 2015.WAF is a web application firewall.As the name suggests, it protects your application.So typically it works at layer seven,and then it can prevent you from the attackslike cross-site scripting,or SQL injections, or top OS vulnerabilities.Now, what is OS vulnerabilities?It is like defined set of vulnerabilities,which your application should always be protected from.Now, WAF can be applied to different AWS services,including load balancers, CloudFront, API gateways,or if you're heard about Absinthe,which is a GraphQL APIs again.So WAF works at application layer.And then, in 2016 around, AWS launched ACM.ACM is Amazon Certificate Manager.Now, if you heard about SSL and TLS connection,you have to make sure your serverhas the X.509 certificate,and then client gets the certificate,it validates that certificate has been signedby the right certification authority,so AWS itself is a certification authority,which gives you this certificate,which you can apply to different AWS services,again, like a load balancer,or API gateways, or CloudFront, right?And then your connection is TLS enabled, right?So either you can import your own certificate,or you can get this certificate from the ACM,and we'll learn more about what all thingsis employed shortly in this section.Then in 2017, AWS launched AWS Shield.Now, as I said, WAF works at application layer, layer seven,but then it doesn't protect youfrom the layer three, layer four attacks.And typically these attacks are DDoS attacks, right?Distributed denial-of-service attacks.That means attackers will overwhelm your serverswith so many requests that the legitimate userswon't be able to access your system.Now in order to protect your servers or infrastructurefrom these kinds of attacks,AWS Shield service is there,which comes again in two flavors,Shield Standard and Shield Advanced,and there are advantages using Shield Advanced,and we'll see what those are in this section as well.Then in 2018 around, AWS launched AWS Firewall Manager.Now Firewall Manager itself is not a firewall serviceor a preventive service,but it can manage other AWS security services.For example, you can manage AWS WAF rules,or Shield Advanced, or Security Group, or Network Firewall,which AWS launched recently.So basically you can apply all these rulesfrom a single console across your AWS account.Later on in 2020, AWS launched Gateway Load Balancer.Now, Gateway Load Balancer has a special reasonbecause a lot of AWS customer still use the third party IPSand IDS and network monitoring tool.And there was a problem.If you want to monitor every packetthat goes in or out of your VPC,you had an option to land this traffic to one machine,and from there, you have to send itto the right destination,but then you had a problem scaling that infrastructureas well as the traffic increases.Now, Gateway Load Balancer solves that problem, right?And we'll learn more about that in a separate sectionof Gateway Load Balancer,because this is also an important AWS servicefor your networking exam.So with Gateway Load Balancer,you can put IPS ideas as wellas network monitoring appliances.And then all the traffic will be routedthrough a single entry and exit pointvia this Gateway Load Balancer.And towards the end of 2020,AWS launched AWS Network Firewall.Now it is an advanced intrusion prevention systemand intrusion detection systemwhere you can write Suricata compatible tools.And we'll learn about all those rules, what you can write,but this is really, really, you know,a deep, intelligent network firewall that AWS launched.And we'll have a dedicated section,which will cover AWS Network Firewallas well as the hands-on exercises around that.And finally, there is something called DNSSEC validationat Route 53, so earlier, whatever Route 53 requestand response was there, it wasn't really signed.Now with DNSSEC validation,the client can verify that the response is legitimateand it is coming from the AWS Route 53 service.So Route 53 will sign the responseso that client is sure that whatever DNS responseI'm getting is from the legitimate AWS service.And this prevents you from the attacks like DNS poisoning,in which case the attacker may send you the wrong IP addressfor the DNS that you have asked forand then ultimately will hit the wrong IP address.So signing the DNS requests will save youfrom these kinds of attacks.So these are all the AWS security serviceswhich are preventive services.And we'll talk about all those in this section.And now if you go down to the detective services,you know the CloudWatch, right,most of the AWS services will emit the logs,which can capture by the CloudWatch.And then you can monitor these logs.You can also create alertsand you can also monitor the metrics,and then, you know what I'm saying, the notifications.Right?Then CloudTrail,which captures all your API activities in AWS.So whatever you do in AWS,that API layer is captured through CloudTrail,so you can monitor, right?If somebody has modified say a security group,that API call will be captured in CloudTrail.So you can again take that CloudTrail logsand you can find out who did what at what time.So these are, again, the detective servicesin case something goes wrong.Then VPC Flow Logs, as you know,whatever traffic flows inside and outside VPCscan be captured using VPC Flow Logs.We have already covered what flow logs are.So you can, again, analyze these VPC flow logs,and you can find out where the traffic coming fromor where the traffic is going to.Then GuardDuty, now GuardDuty is an interesting servicebecause it's a machine learning based service.What it does is it analyzes your VPC flow log,your CloudTrail management event logs,or CloudTrail for S3 data events or DNS logs.And it uses the threat intelligence such as listof malicious IP addresses or domains,and also applies machine learning to identify unexpectedand potentially unauthorized and malicious activitieswithin your AWS account.And we'll learn moreabout what kind of jigs GuardDuty perform.There are 70 plus jigs that that duty does,and then it applies machine learningto find out those behaviors.So that's, again,a very intelligent threat detection service.Then comes the traffic mirroring.If you want to capture all the trafficand then analyze it separately,you can always do that with a traffic mirroring feature.And we already covered that in the last section.And then finally we have Route 53 resolver query logs.So far, whatever Route 53 resolves the domain nameto IP addresses, the logs were not getting captured,but now with this new feature,all these logs can be captured.So Route 53 query log lets you log the DNS queriesthat originate in your VPCs.Now with this query logging enabled,you can see which domain names have been created.Right?And the AWS resourcesfrom which the queries have been originated,including, the IP address and instance IDs.Accordingly, you can see, you know,what's really happening in your AWS accountand inside your VPC.So these are what the query logs can enable you,and these logs you can store to S3 or CloudWatchor Kinesis Firehose.So overall, this is a portfolio of services,which secure AWS network.And then we will now see all these services in action.Now, if you talk about any architectureand where the services fit, then let's try to imagine that.So this is a VPC and you have a simple applicationwith EC2 machines and load balancing in frontand internet users will access over the internetthrough internet gateway, right?These are very familiar architecture to you.Now, if you want to apply some additional AWS servicesto maybe optimize the performance or to secure it,then you can always use CloudFront, which is a CDN service,as you know, right, so that the content can be cachedand locations can be used.And CloudFront itself brings some additional security layeron top of your existing application.Then for domain name to IP addresses,you would use Route 53 and then DNS squaredis really called to the Route 53.So this is kind of simple architecture, I would say.Now if you want to secure your EC2 machines,you'll apply the security group at the EC2 instance level.And in order to protect your application sub-net,you will apply the network ACLs.Right?And you will maybe only allow certain ports there.Then if you want to apply the additional firewall rules,then you can use network firewall.As I said, it includes the Suricata based rulesyou can write into the network firewall.There are stateful and stateless rules,which we'll cover in a different section.But then now this becomes your entry and exit pointfor all the traffic inside your VPC.Now, if you want to use your own third party appliances,rather than using AWS Network Firewall,then you can always use Gateway Load Balancerand your own network appliancesand third party IPS and IDS systems.But in that case, your entry and exit pointwill be the Gateway Load Balancer.Okay, so far so good.You understand where these services are used.Now, if I talk about application level security,I had talked about AWS WAF,which can protect your CloudFrontas well as application load balancer.Right?And any attacks like cross-site scripting,SQL injections can be protected with AWS WAF service.And finally, if you want to protectyour complete infrastructurefrom the distributed denial-of-service attack,that is a DDoS attack, you will put AWS Shield.Now Shield natively protects a lot of AWS serviceslike CloudFront and Route 53.But if you want to protect other AWS resourceslike your EC2 machines as well,then you can use AWS Shield Advanced.And that we'll cover more in a specific lectureabout AWS Shield.So overall, this is how your architecture looks likeif you want to integrate AWS security servicesinto your architecture.So with that, we'll stop here.And now we'll get into every servicethat we just talked about.So I hope that it's clear, and let's move forward.Thanks for watching.