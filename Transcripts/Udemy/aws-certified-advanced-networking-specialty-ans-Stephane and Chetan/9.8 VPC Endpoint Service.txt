# 9.8 VPC Endpoint Service

Okay, now let's look at how to access VPC endpoint service using the VPC endpoint and private link.So we haven't talked specifically about VPC endpoint service as of now.But essentially it means that you are hosting your services inside VPC.And you are exposing that to the VPC through the VPC endpoint and private link.Right.So in the last exercise we accessed AWS service using VPC endpoint.But what if you have your own services and you want to expose that privately to the other customers.And that's where VPC endpoint service comes into the picture.So this is very much like a provider VPC which is say owned by you.And now say your different customers wants to access that service privately.Now historically if you have to do something like this, then you would have to pair your provider VPCwith all those consumer VPCs.And then all those challenges about overlapping Cidrs and having end to end, you know, broader connectivitycomes into the picture, which is what you don't want.That's where what you do.Rather, you create your service inside a VPC using network load balancer, and then you expose thatnetwork load balancer as a VPC endpoint service.And then in each consumer VPC, you create the VPC endpoint for this particular endpoint service.So it's a more secure and scalable way to expose services to thousands of VPCs.As against, you know, having VPC peering.There are a number of connection limits, but here you can expose your services to thousands of consumerVPCs.And those VPCs can be across different AWS accounts.And we will see some examples of that.Then another benefit is, of course you do not need VPC peering or internet gateways or Nat gatewaysin the consumer VPC to access these services.And it requires network load balancer in the provider VPC, something like this.And then you will have your actual services running on EC2 instances or on the containers, but theyshould be behind network load balancer.And then you expose this network load balancer as a VPC endpoint service.So once you do that then you create the VPC endpoint in the consumer VPCs.And all these VPCs can access your VPC.And as I said these VPCs can be in different AWS accounts right.And this is how typically you will also access the SAS services which are available on AWS marketplaceor in AWS Partner Network VPCs.So this is kind of a scalable, secure way to access the services which are deployed inside VPC.So that's basically is VPC endpoint service.And now let's talk about a few important things that you should know about the VPC endpoint services.And then we will do another exercise.So important thing is you need to understand the AZ consideration.So because you are using the network load balancer in the service provider VPC, you know that you willtypically create that across multiple availability zones because your targets were actually servicesare running should be in different AZS for high availability.Now if you do that when you create VPC endpoints, they should also be created in different AZS, right.Something like this.Now in the previous lecture we briefly talked about that here in the consumer VPC.The instances can access this endpoint service using the local Eni into their subnet or in that AZ.But they can also access through another AZ.Eni.But in that case there will be cross AZ data transfer charges.So try to make sure that in the consumer VPC you align the availability zone with the service providerVPC, right?So service provider VPC should have target across multiple AZS.And then VPC endpoint in service.Consumer VPC should also be created in corresponding AZS.In consumer VPC application should use zone specific DNS so that traffic remains in that AC only, andotherwise, if using the regional endpoint, then DNS will resolve to the Eni IPS of this VPC endpoint.Dennis.In round robin fashion.So you might not keep the traffic in the same AC, and there could be additional charge for inter ACdata transfer charges.And finally, to make this solution perfectly highly available for the network load balancer, you shouldalso enable the cross zone load balancing so that if one of the AC is down, network load balancer cansend traffic to the target in another AC.Now this is all about the load balancer feature.But again, in this context you should also consider that.Okay.And just one last thing about the AC consideration, which probably not many people know is that ifthe service provider VPC and the service consumer VPCs are say in different AWS account, then the AZin one account may not be the same as in another account.Now let's try to understand that.So imagine that in service provider VPC you are using two availability zone.And if it's a North Virginia region then the availability zones will be say US East one A and US Eastone B.Now if in the same region you create the service consumer VPC but in different AWS account, then ofcourse you will also try to create VPC endpoint in the same AZ that is US East one and US east one B.But in reality that might be US East one C and US East one A for this particular AWS account.Which means the A-Z names in different AWS account may not point to the same AZ which is otherwise there.Now, this might sound a little confusing, but probably the reason AWS did this is because otherwiseall the customers will typically use first A-Z to launch their resources, and that might basicallyoverwhelm one of the easy only and rest of the ashes will be unused.So what AWS did?They probably randomly pointed the AZ names to different AZ in different AWS account.So how do you handle that now?Now for that, you need to consider rather easy IDs which are same across different AWS account.So always look at the easy IDs.So if in Service Provider account you are launching your network load balancer and resources in oneof the AZ, check its AC ID and then use same AZ IDs for the consumer VPC endpoints as well.Now once you do that, your traffic remains in the same AZ and then you don't pay for, you know, crossAZ data transfer charges.So the point is, even if you are using the same AZ in different AWS account, you may still have thecross AZ data transfer charges because AZS are not actually same.So I hope it is clear.And just last one more point about the VPC endpoint service is that if you are talking about this serviceprovider VPC, this targets behind the network load balancer need not be in the same VPC, right?Which means this target can also be somewhere on premises, which you can register behind the networkload balancer as an IP address.And of course, your provider VPC and the on premise network needs to be connected over a VPN or directconnect, something like this.So you have only network load balancer here in AWS, but your actual target or services are runningin the on premises network.So this is also very much, you know, valid architecture.So just remember that.Okay.Enough said about the VPC endpoint service.And now in the next lecture let's see that in action where we'll create our own VPC endpoint service.And then we'll create VPC endpoint in the consumer VPC.And we'll try to access those services right.So with that we'll stop for this lecture.And now let's go to the next lecture.Thank you.