# 14.20 Private VIF routing policies and BGP communities

Hello and welcome to this lecture.Now, let's understand the Routing Policiesand BGP communities for the private VIFs.So these policies and communities work differently,when it comes to the private VIF.Now in case of public VIF so that it influencesthe routing decision,and the scope of the route advertisement.But in case of private VIF, both the routing policiesas well as BGP communitiesare used to influence the routing decision.So there is no scope advertisementusing the BGP communities.So let's look into that.So now let's look at the Private VIF Routing Policies.Now, very first thing you needto understand is that in case of private VIF,the longest prefix match is preferred.Which means that if there are multiple private VIFsand multiple Direct Connect connectionsto reach to the target network,depending on the routes on those VIF which are publishedby the customer side of the network,if thetargetIP matches the longest prefix,that route is preferred over the other routes.And we'll see that by example shortly.Now, if this prefix is same,then which route is chosen?Now, let's take an example.Suppose you havetwo Direct Connect location of Frankfurt and Ireland, right?And then your VPC decides in the Frankfurt region.Now, in order to reach to the destinationwhich is our customer data center,both these Direct Connect locationshas a Direct Connect connectivity to our customer location.Now, which route it would take to reach to the destination.Both routes are thereand there are two private virtual interfaces.Now, in this case AWS uses the minimum distancefrom the AWS region and the Direct Connect location.Now, in this case it would be Frankfurt, right?Because you are sending the traffic from Frankfurt regionto the on-premise data center.And again,we will explain that through the architecture shortly.Now, in the case where,you want to steal route the traffic through Ireland,Direct Connect connection,then BGP communities are used.Now you can see here that you can also use BGP communities,but in this case they're used to influencewhich path AWS takes to sendthe traffic to the on-premise network.And there are three BGP communities that you can see here.7224:7300takes the highest precedenceor 7200and 7100and we'll see shortly how to implement that.Now we have seen three cases here,One, the longest prefix matchthat route is taken as a first preference,then if the longest prefix matches are same,for multiple connections then the shortest distanceis chosen from the originating AWS regionto the Direct Connect location.Now, if you want to change this equation,then you can obviously use the BGP community tags.But if you're not using the BGP community tags andthere is only one regionand one Direct Connect location,so say you are using the Frankfurt regionand within the Frankfurt region,you are using the multiple Direct Connect connections,then other choice is made.Now, in that case you can use again the AS_PATH attribute.So as you know the shortest AS_PATH is preferredover the longer AS_PATH,so you can again influence your routing decisionby setting up the appropriate AS_PATH.And we will understand that now with the architecture.So let's look at this architecture.So on the left side you have AWS region US Eastand you have DPCand on the right side you have corporate data center.Now for high availability you have multiple routersand then you have chosento have multiple Direct Connect locations as well.So that if one location fails for some reason,then you can dock the trafficthrough another AWS Direct Connect location.Now again, for high availability within the same regionyou have multiple private VIF connectionsand in another region you just have one connection.So overall, all these VIF terminateon the Direct Connect Gatewayand this Direct Connect Gateway is connectedto your VPC in the US East region.Now,if this is architectureand then you want to understand which path AWS will taketo send the traffic from AWS to corporate data center,it will depend on a lot of factors.Now that would depend on what kind of prefixes you advertiseor what this BGP session from your routers.Now let's assume that you are sending the same prefixesfrom both these routers and you can see herethe prefix is same VIFs last 16 CIDR.Now, in this case how AWS will decidethe traffic needs to be sent through which VIF.Now, if you can see here AWS makes a choicedepending on the distance from AWS regionto the Direct Connect location.Now, because VPC is in the US East region,it will choose US East as a Direct Connect locationto send the traffic through.And in that case,it would be sent either through VIF A or VIF B.Now, which one of that we can again controleither VIF A or VIF Bbut understand that it uses the homeor a local region as the first choicewhen the prefixes are advertised are the same lengthfrom multiple connections.So home region takes the precedence.That's what I'm trying to show here.Now let's try to understand what if you wantto influence this decisionand instead you want to send this trafficthrough a US west region.Now, with this architecture you can'tbecause prefixes are same but in that case,you can have the longest prefix advertisedover the other Direct Connect locationand through that private VIF.Now you can see here,we are advertising class 24 as a prefixand here it's last 16.So if from your VPC you are sending trafficto say IP address 172.31.0.50now that falls within this rangeand in that case VIF C will be selected as yourtraffic route, right?And that's what has been explained here. Okay.Now, as I said,if there is just one Direct Connect location,so let's take that example and then decidewhether VIF A will be selected or VIF B will be selected.So again, back to the first scenariowhere the same prefixes are sentand we already know that the traffic will be routedthrough VIF A or VIF B because US is the local region.In that case you can influence the decisionbetween VIF A and VIF Busing AS_PATH attributes of the BGP, right?So the shorter AS_PATH that path will be preferredover the longer AS_PATH.So in this case if say VIF A is havingthe AS_PATH attribute as 65001 three timesand VIF B has an AS_PATH of 65001 two timesand VIF C has same attribute,but the AS_PATH is shortestwhich is just once 65001.Now, just looking at this,you might think that private VIF C should be selected,but understand that the same prefixesare being advertised on all these three VIFs.So first preference goes to the local region,so that's where the US East will be selected,but within US east one you have two VIFsout of these the AS_PATH will take the preference.So shorter AS_PATH will take the higher preferencein this case VIF B has justtwo hopsVIF A has three hops.So private VIF B will be taking the precedence here.So that's interesting thing.Now, in the case where you can't just control the prefixesmaybe all prefixes are same,but still you want to route the trafficthrough private VIF C then what do you do, right?Otherwise it's not possible here.So in that case, you can use BGP communities.As I said, there are three BGP communities7224, 7100,7200and 7300.7300 takes the highest precedence and in this case,whatever the BGP community you setas the 7300,that particular VIF will be used for the connectionor the traffic from AWS to on-premise data center.So in this case, obviously it would be private VIF C.So this is first preference,then VIF A will get the second preferenceand B will get the third preference.So this is how the routing works.And with that you can also achievethe Active/Passive kind of traffic,because you can set the differenceBGP communities on these VIFs.So here, if you are using A as a primary,then set the 7300 as you are a BGP community here.If VIF A connection is down for some reason,then VIF B connection will be used in that case.So this is how the BGP communitieswill affectthe routeselection here.So with that I think we just covered abouthow the routing works in case of private VIF.Now, this is a little simpler than the public VIFas you can see,obviously the very first option is to havethe longest prefix that path would take precedence,but then if all the VIFs are advertising the same prefix,then the shortest distance from the AWS regionto the Direct Connect location that will be selectedand in case there are multiple VIFs there,in that case you can use the AS_PATH attributeto make a choice of one VIF over the otherand if nothing else,then you can use the BGP communities to,you know, set your preferences.So I think that's it aboutthe private VIFand the routing policies.Now I know that this is again, overwhelming thingthere are so many things we discussed,there are so many permutations and combinationsand I don't think really that you needto really get to know at that depth for your exam,but as you go into the field and implementand work on the Direct Connect,then probably you can set your routing policieswith clear understanding what you are doingand you know exactly how the traffic will flowin case there are multiple Direct Connect connections.So I hope that is helpful to youand with that, now we will pivot towardother section of the Direct Connectand that's it about the routingand the BGP communities as of now.So thanks for watching.