# 16.4 VPC lattice traffic flow

Okay.So we have talked about the basics of VPC lattice.And now before moving to the exercises let's quickly talk about VPC lattice traffic flow.And then we will actually see this in action.Okay.So as you can see here we have VPC lattice network here.And then there is a client VPC which is connected over VPC association.And then we have VPC lattice service which ultimately sends the traffic to the target in the serviceprovider VPC.Right.So this is a very common architecture for VPC lattice.Now here the client wants to actually send the traffic to this service a right.Now in that case let us see how this traffic flows and what are the different things which you needto take care to establish this end to end connectivity okay.So very first thing when you create a service in VPC lattice, as we said there is a service discovery.And for that VPC lattice will provide its own DNS name, and it is in the format as service name thatyou provide.And then there is AWS managed DNS, which looks something like this.And it ends in the region name on AWS.So if you don't want to use your custom DNS, then this client here can access the VPC lattice serviceusing this particular DNS.But as you know, you might want to have your own domain name for your services.And in that case, you can create your own route 53 hosted zone.And in that you can create the CNAME records to point your domain name to this VPC lattice providedDNS name right.So you have the DNS name.Now the first thing that client will do is make a DNS query.Right now this DNS query might be resolved by this VPC lattice DNS.Or it can be resolved by route 53 hosted zone that you created.And as a result of this DNS query, client will receive the local link address for the VPC lattice networkendpoint.And as we discussed, this local link address.Is accessible only from within this VPC.So now client has this IP address.Okay.So at this point client receives the IP address.And now client wants to send the traffic to that IP address.And there comes the security groups.Now there are two security groups one for the client for the outbound traffic.And then this VPC lattice association itself has a security group.So if you see the EC2 security group has to allow the outbound traffic.And this VPC association security group has to allow the inbound traffic for the HTTP or Https protocol.Right.So in this you should have this security group rules added so that traffic can flow from this clientto this local link address okay.So once it passes the security groups then it will check the auth policies at the VPC lattice servicenetwork layer and the service layer.Right.And in the previous lecture we learned that if the auth policy is none then the traffic is allowed bydefault.But if the auth policy is set to aws IAM, then there should be explicit allow statement for this trafficto flow right.So it will check the auth policies and then traffic will go to the service.Okay.So traffic reaches to the service end point.And at this point as you know service has the listener.And then service has rules and the target groups.Right.So according to the rule that you have specified, it will take action and will send that traffic tospecific target group.Now once it reaches the target group then it will be sent to the target.And at this point as well there is a security group, right.Now this security group should allow the inbound traffic from VPC lattice network Cidr range.Right.And that range is 169.254.But the best practice is to allow only to VPC lattice managed prefix list, and it pops up when youcreate that inbound rule in the security group.Now, once you do that, then this traffic reaches to the target, and then it goes back to the clientas a return traffic.Right.So I hope it is clear.Now just one more thing to understand.In this entire flow, there are no route tables.We haven't configured any route table.All this traffic flows just based on the DNS and this local IP address.So there are no route tables to be configured for this VPC lattice traffic flow.Okay.So I hope it is clear.And now you have sufficient knowledge to get into the exercise.So in the next lecture let's do similar exercise.What you see here we are client from one VPC will connect to the service in another VPC through theVPC lattice network.Right.So let's stop here for this lecture.And let's go to the exercise.Thank you.