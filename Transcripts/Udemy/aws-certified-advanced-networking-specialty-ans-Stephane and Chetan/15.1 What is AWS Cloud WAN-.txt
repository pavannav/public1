# 15.1 What is AWS Cloud WAN-

Hello and welcome to this new topic.AWS cloud Wan.Wan is wide area network.Now this is very interesting topic and it solves lot of problems which probably are complex to solvewith just using AWS networking components that you have seen so far.Now, we have already talked about connecting different networking components in AWS.For example, how do you connect multiple VPCs?How do you use transit gateways?Then?How do you set up your hybrid network using the VPN or Direct Connect?But if you really look at the large enterprises, they have their legacy network spread across the globe,right?And also then they operate in AWS.So often it is required that you have one single pane of network management.And as you might have guessed, that's what is AWS cloud Wan.So AWS launched AWS cloud Wan service, which allows you to build, manage and monitor the global networkacross AWS and on premises network.And we are going to dive deep into further what cloud Wan is and what are the components of the cloudWan.The best part of cloud Wan is that it's a policy driven.That means you define the network policy like you do with any software defined networks, and just applythat policy and it will take into the effect.And further, you get a single pane of dashboard through which you can monitor your entire global network.And this is done through AWS Network Manager.So we will also see how that network looks like and what kind of different views you can have, andhow you can monitor the performance and all the network links into your network architecture.So essentially cloud Wan is something like this.You connect your VPCs, your direct connect your site to site VPN or even your SD-Wan networks throughthe network attachment to AWS cloud Wan.And then you have centralized dashboard through AWS Network Manager.And as I said, you can change the network configurations through network policy.You apply that policy and it will be deployed across the globe.So if there are new AWS region in which you want to deploy your network, you just make that changein that network policy and then it will take into the effect.So I hope that is clear.Now let's go back to the basics and see why AWS might have launched AWS cloud Wan, what problems itactually solves and what are the complexities otherwise using, you know, other networking componentsfor your global network.So let's take an example.For example, there are two different AWS region in which you have your VPCs deployed.Now if you go with very basic construct, probably you will say there are just three VPCs.I will just go with VPC peering connection.If I need to have this VPC communicate to each other so it will look something like this.You connect this to VPCs in North Virginia region.And also you connect this VPCs to the other VPC in Mumbai region.And as you know VPC peering is non-transitive.So you have to also set up this link between these two VPCs.So this works well I would say for small number of VPCs.And you already know that if there are more VPCs, of course we move to the transit gateway.So there are now more VPCs.So logically what you will do is you will rather launch transit gateways.And transit gateways are regional components.And then you will connect these VPCs to the transit gateway in that region.And now because you want to have communication between VPCs across the region, you will pair this transitgateways.So I hope this architecture is pretty familiar to you.Now let's add some more complexity and add our third region here.Now again you can do the same thing.You can have Transit gateway, but again the same challenge that was with the VPC peering scaling.Right.That means now you have to have the peering between these two transit gateways and these two transitgateways, and also between the North Virginia and Mumbai Transit Gateway.So you can see that complexity is increasing.So as many region you will onboard those many peering connection will be required.And on top of this, if you want to connect your direct connect connections, that means you are goingnow hybrid.Then of course you would have to have your DCS connection going to the Direct Connect gateway over atransit VIF, and then you connect Direct Connect Gateway to the transit gateways in different AWS regions.We have already seen this architecture, and on top of which, for high availability, you might wantto have the VPN connection and SD-Wan connections as well.Now just a quick refresher.If you go back to the Transit Gateway section, we talked about the SD-Wan.In which case you have the SD-Wan appliance sitting on premises.Or you can also deploy that inside a VPC, and then you connect to Transit Gateway over the connectattachment.Then you create a GRE tunnel.And that's where your network can be extended to AWS.So if you want to add the SD-Wan to this network, again, you can connect it to the Transit gatewayover a connect attachment.And further, if you are using SD-Wan, that means you might also have different routing domain.That means you might have different network segments.So for example, a different network layer for development or production or shared services.Now as you connect your connect attachment to the transit gateway, typically all the routes and routingpolicies are propagated to the transit gateways.So it will be automatically propagated through dynamic routing.But you know that transit gateway peering doesn't support the dynamic routes, and you have to havethe static routes written into your route tables.And in that case, this becomes a manual task to also update your transit gateway peering.So overall, this is really a complex solution for managing this kind of the network.And as you might have now guessed, this problem is solved using AWS cloud Wan.So you just forget about everything that you see here and just replace all these layers with AWS cloudWan.Okay, so Cloud Wan is that one single global network that provides you the centralized connectivityfor everything that you see in this diagram.So before we dive deep into the individual cloud Wan components, let's first understand at a high levelwhat those components are.Which means what is this cloud Wan is made up of.And then we will also try to do a walkthrough where you will create a global network with cloud Wan.Okay.So what are the components of the cloud Wan.So if you double click into the cloud Wan there is something called Global network.Now this network consists of core network which we are going to talk about.And then Transit Gateway Network.Now transit Gateway network is a traditional transit gateways.So basically if you are already using the transit gateway you can still manage and see this transitgateway in the network Manager console.So global network has two components core network and transit Gateway network.But we are focusing only on the core network because that's what real cloud Wan is.And through this core network, basically you connect all these attachments that we just saw.Okay.So first thing is to create a global network.And then you have to create the core network.And as you create this core network it has core network edge which are the regional routers, whichsits into every AWS region that's called CDN or core Network Edge.And then in order to deploy this network you create core network policy.And we will see how this policy looks like.Now, once you have this core network, you connect all these regions together.And then you can divide your network into the routing domains.Now those are called network segments.And again we will talk about that shortly.So you can have development network production network shared services network the way you want yournetwork segmentation.You can do that.And for that network segment of course, you can define the actions as in which network segment canshare routes with each other, which are isolated network segments.All that is possible now after you create the segments, of course, you need to attach your VPCs andyour VPN to these segments so that they can communicate with each other.So next component is attachments.And with this attachment you can do some kind of automations as well.For example, if you create a new attachment for the VPC, you can tag that attachment.And then that VPC will be automatically linked to your development network.And we will see how that actually works.And finally, you can also pair your existing transit gateway to the core network.So this is not necessary.But if you have already your network setup in AWS using the transit gateways, and now you want to moveto the cloud Wan, then it is possible that you bring in your existing transit gateway and pair it withthe AWS cloud Wan.And one thing to understand is that the cloud Wan is a global service.But all this configuration data about your network policies and your dashboards, all this data is storedin your home region.And for cloud Wan, that home region is Oregon.Irrespective of in which region you actually deploy your networks, which means it is not necessarythat you also deploy your core network edge in the Oregon region.But it is just used to store all the metadata about your networks in the Oregon region.Right.So I hope that is clear.So now let's try to put everything together.What we just talked about.So you have this kind of architecture.Of course you replace everything with AWS cloud Wan.So when you create a cloud Wan you first create a AWS cloud Wan global network, which is combinationof core network and transit gateways network.We are not talking about Transit Gateway network as of yet.We are just focusing on the core network.So first thing you do is create a core network and you define in which all AWS region you want to haveyour core network, and then it will create the core network edges into those regions.And all this can be done using the core network policy, which is basically a Json document.But AWS also provides you a console and the UI to change your policy.Now, once you deploy your network, of course, you can then create the attachment to the core network,whether you have VPCs, you have SD-Wan, you have VPN, or you want to connect your existing transitgateway.In that case, there will be peering attachment.And now let's assume that some of the VPCs are for development.Some of the VPCs are for production.And then all these hybrid connectivity is your shared network.Right.So we are just naming the VPCs accordingly.And now you want to make sure that all these development VPCs are connected to a separate network.And they cannot communicate with the production network.And for that as I said, you can create different routing domains, also called as segments.And then you can create the attachment which are specific to that segment.So you connect all development VPCs to the development segment.Similarly, prod VPC to the prod segment and all your hybrid networks to the shared services segment.Now, please understand that the segmentation that I have shown here is just an example.And typically for your organization, you would have different requirement with respect to how you manageyour network segments.And of course accordingly you will have different routing policies as well.So this is just one example here.And all these attachments can be automatically added to the segments using some rules.And that's called attachment policies.And one of the way to do that is using the tags.And we will talk about that.And finally you could also have the segment routing in the sense that you can define my developmentsegment can communicate with the shared services segments, because maybe you want that your event developmentVPCs to be communicating with your on premises network, but you don't want your development and productionnetworks to communicate with each other.So again, this is just one kind of example where you can set up, you know, interoperability betweendifferent segments.So depending on your network requirement you will definitely decide whether two segment needs to communicatewith each other.Now there are other features as well.For example isolated segments.In that case, no other segment can communicate with a given segment which is marked as isolated.And we will also talk about that.So I hope that is clear.So that's the cloud Wan in nutshell.And going forward let's try to deep dive into the network policies for the cloud Wan.And also I will have a quick walkthrough about how to create the cloud Wan network.So thanks for watching and stay tuned.