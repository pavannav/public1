# 11.4 BGP Route selection - ASPATH, LOCAL_PREF, MED

Hello and welcome to this lecture.So in earlier lecture we saw how BGP interchangethe route information between the BGP peers.Now let's understand the BGP attributes,which would affect the routing decision.So first let's look at how ASPATH works.So, as I said, ASPATH is like number of hopsyou have to go through in order to reach to the destination,right?So this is the opening information, so far we have.We are really talking about the AS 100 hereand there are different routes informationthat it received from the neighboring AS's.And that's where this AS 300 information it receivedfrom both the AS 400 and AS 200.Now, additionally, if you could see,AS 400 is also connected to AS 200,and it has the routing information how to reach to AS 200.And this is this entry that I'm showing.Additionally, what it does,it will prepend its own AS numberand will send that route information back to AS 100.So now if you look at this,AS 100 has two routes to reach to the AS 200.One, it is directly connected.So it knows the next hop is 10.20.0.1,and it can reach to AS 200and other one is a longer route.It can go through AS 400and from there it reaches to AS 200.But if you look at the BGP as in protocol,it would rely on the shortest path, right?So that means if you want to send the trafficfrom AS 100 to 200, it will choose the shortest path.And in this case, the shortest path is this one, right?And that works perfectly in normal scenarios.But there is a caveat.Now, if you look at this network topology,the link between the AS 100 and 200 is 2 Mbps,however, between 100 to 400, it's 10 Mbps,and same between 400 to 200.That means if we talk about the bandwidth,we can get more bandwidth to reach from 100 to 200if it goes via AS 400.But as you know, BGP would always prefer the shorter route,so it will take this path.Now, how do you influence that decision, routing decision?And instead going from this path,let's try to go over the longer routebecause it provides much more bandwidth.In order to do that, you can use something called ASPATH.What you can do is you can update the route informationin AS 100 and you can add additional hop there,which are kind of dummy hops, you can say.So what I'm saying is you modify that route informationand you append the same 200 AS as multiple times.Now, if you see the length of this pathhas become 4, however, the other path,it's length is still 3.Once you do that, BGP understand that nowthis is not an optimal route probably,though it is.But it will then turn out to other pathwhich takes through 400 ASand then it would select the another path.And then the traffic will go from AS 200 to AS 400and from there it would be routed to AS 200.So basically what we did,we could influence the routing decisionby prepending more paths in the same route entry.And so that it chooses it over the another path.Now that in a nutshell is ASPATH.Okay.Now let's try to understand the next attribute in the BGP,which is Local preference.Now, in this case, the topology is little different.In case of AS 100, we have now two routers.One router is connected to AS 400and another router in the same AS systemis connected to the AS 200.Now let's take an example.We have this router, which is shown here.Now this router wants to send a traffic to AS 300.Now for that, there are two route table entries here.One is through 400 and 300and another is through 200 and then 300.Now, if the traffic has to go to 400,probably this router will send traffic to this router Awithin that AS system.And in another case, it may send it to router B here.The question is which route it should take.Either this route or this route?Now, in order to influence that decision,you can use something called local preference.And as I'm showing here,this decision is only within that AS.That means this local preference does not affectthe routing informationor this routing information is not sharedwith the neighboring external AS's, right?So what I can do is for these routes,I can set the local preference values,say 100 and 200.And the highest preference value would take the precedence.So in this case, as you can see,this router has the precedence as 200,then actual traffic will go through router A,from there it will go to AS 400,and from there it will go to AS 300.So local preference would affect the routes within the AS.So default value is 100,and then you can choose other higher valuesto have the more precedence.Now let's try to understand Multi Exit Discriminator.Now, here again,the network topology is little different.We have 2 AS's here.So from AS 100,we have now 2 connections to reach two AS 400through different destination routers in AS 400.Now you can see AS 100,the single router is connectedto two different routers in target AS.Now, Multi Exit Discriminator will come into the picture,when local preference is the same,when ASPATH is the same,then only this is MED is looked at.Now, if you can see,of course there is only one hop between both these,that means ASPATH is same.Now, Local Preference, we haven't really set right now.So default is 100.That means it is also the same.And now comes the MED.So lowest MED value will have highest preference.So now, if you set the MED value to 2 for this connectionand 5 for this connection,in that case, the lowest value would take the precedence.So traffic will go from here to here.So that is a MED.Now MED is between the external AS's.So there are multiple paths to reach to the same target AS,then you can have the MED valuesto influence the routing decision.So that's about the MED.So let's recap what we learnt about the BGPin last two lectures.So for influencing the BGP routing decision,there is something called Highest Weight,but this is only specific to the Cisco routers.And that's where we are not really interested.But then again, the highest weight takes the precedenceif there is a scenario talking about the weight,so make sure you choose the highest weight.Now, again, like you can see like a local preference,this highest weight information is not exchangedbetween the external BGP routers.It works within the AS.Then comes the highest local preference.As I said, it is not exchangedbetween the external BGP routers.It is set by the local AS for internal BGP routingand the default value is 100.Then comes the shortest AS Path.And you know, AS Path can be modifiedusing AS path prepend to make it appear longerso that another path can be taken up.Then comes the MED.This is again, a lowest MED will take the precedence.And this is between the multiple paths between the two AS's,in that case you can use the MED.And MED is exchanged between the autonomous system or AS's.So with that, I'm sure now you know about the BGP,how it works and the various attributes of the BGP,how they affect the overall routing.So that's it about this lecture,and now we will get into the AWS side of the VPNand the BGP.So thanks for watching.