# 5.6 Hands On- VPC DNS with custom DNS server

Hi.So in this exercise, let's try the second scenario where we'll be using the custom DNS server to configureand resolve our VPC private DNS now, which means we are not going to use Amazon provided DNS server,but rather set up our own DNS server inside the VPC.So at high level the setup will look something like this.Where inside a VPC, we will also have another EC2 instance which will act as our DNS server.And we then have to modify the Dhcp option set where we will point this Dhcp option set to this DNSserver when it comes to the name server, so that all the DNS queries from the EC2 are directed to thisDNS server, and this DNS server will have corresponding zone file, which has the entries for mappingthis app server and database server to corresponding private IP addresses of this EC2 instances andall these machines can natively reach to this DNS server because all this communication is within theVPC, right.So this is what we want to implement.So now let's look at our setup and all the steps that we are going to perform okay.So setup is pretty simple.Um we'll just have a couple of machines inside VPC.Now I'm launching these machines in public subnet because I want to connect and configure, uh, thesemachines.But in real setup you could have everything into the private subnets.Now you need to just create VPC and three machines as I'm showing here.Uh, just make sure when you open the security group rules, uh, you, uh, have the appropriate portsopen.So for app server, we just need to have SSH open and ICMP protocol open.And for DNS server, uh, apart from this we also need uh UDP 53, which is uh port for DNS.Right.Because all these servers are going to send the DNS queries to this server.So that's the only thing you need to remember.Now, what I did is uh, I already created this setup to save some time.So I have VPC and public subnet, private subnet and all these three EC2 instances.Let me quickly show you that in my console okay.So this is my VPC.You can see the IP address range is 1010 zero zero 16.Then I have two subnets inside this VPC one public.And you can see the routes.And then one private which has just um local route.Um that's it.I have internet gateway attached.And then I have three EC2 machines created.And uh, app server and DNS server are in public subnet and also have public IPS DB server is in privatesubnet does not have uh, public IP.So essentially that's what um, we have.Now let's follow the next steps.The first step is to create a DNS server and configure it.So let me connect to this DNS server.I'll grab its public IP and I'll just open a session.So I already have my partition ssh key loaded, and I'm just logging in there.Okay, let me change my cell to root.Now for configuring the DNS server you need to install some packages.So that's where I change myself to root.And then you need to make sure that you have the latest packages.So let's do jump date minus y.It will fetch the latest yum packages and install okay.Now let's install the packages for our DNS which is bind and bind utils.And I have provided all these instructions in the deck.So you could use variable those.Okay, so now we need to create a few files to actually configure the DNS server and all the steps I'vejust captured in the notepad so that I can copy and paste.The first thing we need to do is to create a file with the name as the domain name.So let me do that.And there is content for this file here.And you need to make sure that you are replacing the content wherever it is marked as x.So most important is like you need to put the IP address of your app server.So we are basically creating the A records like we used to do in the private hosted zone.So let's grab the IP address for the app server which is 180 1010 zero 180.And for DB server it's 1214.So let's do that.This is 180.This is 180.And this is sorry I forgot uh 214214.That's it.We are good.Just save this file.And now we need to also modify Iecee conndot file which actually configures the zone file ATC.So we need to just remove everything in this file.So I'm just removing everything and just adding the content here.That's it.And we need to make sure that we are adding the DNS server IP here.So let's get the DNS server IP which is 179.So 179.Now if you are using different Cidr range, of course you need to change the entire IP address.But I'm using the same as ten dot ten dot.That's where I don't need to change those things okay.Just save it.That's it.Those are the settings we need to just restart the named service.So let's do that service named restart.And we are good.So so DNS server has been now set up.Um now what we need to do next is to also configure the config so that if we reboot the server, uh,your DNS uh service also comes up automatically.So that's it.We have done that.So so far what we have done is just created everything and configure the DNS server.All the steps are given here.You can just follow this okay.So now is the time when we tell VPC that uh you need to use different uh domain name.And how do we do that is using the Dhcp options sets.Right.So in Dhcp option set we are going to create a new one called dot internal as a domain name and nameserver.This time we are going to use this DNS server as a name server.So let's do that.Let's go to the AWS console again.Let's get the name server IP address 1010 zero 179.Uh, let me go here and.Let's go to the Dhcp options sets and create a new one.Let's call it Carp dot internal and same as the domain name.Dot Dhcp and domain name is ten dot ten .0.179.That's it.Rest of the settings.You can leave as it is and we're just creating that.Now.Next thing we need to do is to use our VPC.And in this VPC we need to make sure that it's using the correct Dhcp option set.So we just go here and use our Dhcp option set okay.So this has been done.That means our machine should now redirect all the queries to the DNS server that we configured.But it takes time for this Dhcp lease to expire and refresh so that EC2 learns that.Now configurations have changed the fastest ways to just reboot the server.You don't need to stop and start, but just, uh, reboot it.It's a soft restart, basically.Uh, so you can do that from here as well if you want.And I'm just doing that for the app server.And similarly I'm doing that for the reboot it for the DNS server.Now.That's it.Um, if you want, you can also reboot for the DB server.It will take a couple of maybe seconds, for instance, to be up again, and I expect my connectionmust have been broken by that time.Okay, now, one interesting thing.If you could see here, if you have this DNS server, uh, it has got this carved out internal as ahostname.Similarly for app server, it has got this hostname and there is no public IP DNS because for VPC wedid not have the uh DNS hostname flag enabled.That's where you don't see it here.But we can still connect with the public IP which it already had.So let me just restart this session.So I'm into the DNS server right now.And similarly let me try to get the IP address of my app server and just log into that app server.Okay, so I'm in the app server.Now let's look at a few things.Let's look at uh resolv.conf file first because it reflects what my, uh, search domain is.So you can see automatically there is a called dot internal as a search domain.And the name server is 1010 zero 179.Now this is something which has been automatically, uh configured by the Dhcp option set.And now let's try to do say Nslookup and app dot core dot internal.And you can see it's resolving the IP address of itself.At the same time we can do DB dot corp dot internal.Because all these entries we had created in the uh DNS server configuration file.And it resolves to this two and four IP.That means whatever we had thought it's working fine.We can also do something like this ping db and it goes to the db.com dot internal.Because in our search domain it automatically adds this core dot internal.So that means everything is working as expected.Now in this case we are using custom Dhcp option sets with custom domain name server.We are not using the route 53 DNS server.So that's it.About this lecture I wanted to just make sure we can do it.And uh if you have time please try these things so that you understand everything end to end.So thanks for watching this lecture.That's it for this lecture.Thank you.