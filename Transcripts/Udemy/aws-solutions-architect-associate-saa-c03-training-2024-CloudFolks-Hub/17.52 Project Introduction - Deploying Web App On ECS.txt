# 17.52 Project Introduction - Deploying Web App On ECS

Hello and welcome to Cloud Folks Hub.My name is Bhavesh Edara.You are learning Elastic Container Service.Now you are aware about most of the ECS terminology like cluster, Fargate, EC2 Task definition, taskservice, etc. we already discuss about all this theoretically, but now we want to use all this terminologywith practical how we can do this.We are going to deploy one web application inside ECS cluster, and we are going to use all these facilitiesprovided by ECS.We are going to create cluster.We are going to set up task definition.We are going to set up service.We are going to integrate load balancer with service.We are going to set up autoscaling for the service as well.This is kind of practical implementation of ECS.Let's start our ECS project known as Deploying Web Application on ECS.I'm going to start with Project Introduction.In this project, we will host a web application on Amazon ECS.Fargate.Fargate.Let us run containers without managing server.Unlike the EC2 launch type, as you know that if you want to host your application inside ECS, thefirst step is we need to create ECS cluster.Now, ECS cluster supports two types of infrastructure Fargate which is providing serverless facility.Or if you want to add your EC2 instance as a container host.You can also add them as a part of your container host.Here we are not going to set up any EC2 instance.We are going to set up this cluster using Fargate which is actually providing us serverless service.Our app image is already stored in Amazon ECR, and it will also use Amazon S3 for storage.You know, if you want to set up a web application inside container, you must have container image.You already created container image in previous video and stored this into the ECR.So we are going to use same image over here.And our application is actually going to store data into the S3.So we will use S3 service as well.In this particular project we will expose the app through the application load balancer and set up autoscaling so it can handle traffic smoothly.In short, whatever we have completed so far about ECS, we are going to implement all of this now howwe are going to implement this?This is 100% practical process.So here I have execution plan.We have total nine steps.Step number one we will set up virtual private cloud for our web application.We are always doing everything according to the AWS best practices.And we are going to set up our VPC also as per AWS best practice.Then we will also set up security Group.We need to set up two security groups.One security group is used to protect our application load balancer, and another security group isused to protect our task.Then we will create ECS cluster.To integrate application load balancer with our task we have to set up Alb application Load balancer.We will also verify our ECR image and we will pull this ECR image from ECR to ECS.We will verify ECS task definition, create ECS Fargate service, enable service auto scaling and verificationof service.In short 1 to 9.This step by step videos will provide you all the details about ECS services.I am giving you promise.Once you will perform this particular lab or a project, you will be master of ECS and you can answerany questions related to ECS for sure.Thank you very much.From the next video we are going to start implementation.Goodbye.