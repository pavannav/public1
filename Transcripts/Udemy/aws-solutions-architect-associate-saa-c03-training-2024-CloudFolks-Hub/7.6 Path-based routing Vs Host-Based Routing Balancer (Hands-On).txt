# 7.6 Path-based routing Vs Host-Based Routing Balancer (Hands-On)

Hello and welcome.In this video we are going to talk about application load balancer routing policy.Please watch this video carefully because there is a chance that you may get question about this.Now I am going to explain to you the use case of the routing policy first.Then we are going to talk about two routing policy.Two explain you are about routing policy.I have one example of classic load balancer over here.Classic load balancer is outdated.It is the previous generation of load balancer.But to understand this routing policy it will help us.If you look at this diagram I have two load balancer right I have two load balancer.Why I have two load balancer because I want to forward traffic to the different EC2 instances or differenttarget based on the URL.If you look at this diagram, you will find out that if user is going to open images dot example.comthis load balancer.Load balancer one will handle this request.And this load balancer will forward this request to these two EC2 instances that we have in the differentavailability zone.If user is going to open order dot example.com.Load balancer two will fulfill this request.And it is going to send traffic or request to another EC2 instances that we have inside the two differentavailability zone.Now here we have to have two load balancer because we have two different paths.And based on this path we want to send request to two different targets as well.But this is a problem with The old generation classic load balancer.If we have an application load balancer, we will need to create a multiple load balancer.We just need to create only one load balancer.And using routing policy we can forward this request to the different targets.Now in the application load balancer we have two types of routing policy.And I'm going to explain you these two very easily.This is very important part because in the exam you may get question.So understand this carefully.Here I have mind map to explain you about two routing policy known as path based routing policy andhost based routing policy.Here I have routing criteria.Or you can just understand the whole situation using a very simple picture.If you look at over here, I have again same two targets.And based on the URL, I want to forward request to the different targets.Okay.But I have only one load balancer.Look at this.This is my application load balancer.This application load balancer will handle the request for example.com slash images and example.comslash orders.Now when you send a request for example.com slash images it is slash images.It is going to forward traffic to these two EC2 instances.If you forward the request for example.com slash orders, same load balancer will forward this requestto another EC2 instances.Here the main uh main part is example.com slash images.How you are using the URL.So if you talk about path based routing route the traffic based on the URL path okay.Which is This example.com slash images or example.com slash orders.Many companies are using this.If you look at over here in the example.Okay.If you want to give your AWS exam we are always using Pearson Vue website.And in the Pearson see they have home dot Pearson vue.com/cisco.Same way.Home dot Pearson vue.com/client/aws.So when you open Cisco it is going to open the whole Cisco websites.When you open clients dot AWS it is going to open the AWS website.So this is like an example of path based routing.Another routing policy we have is host based routing serving the same purpose but using different wayhere.Same as one load balancer.We have a single host or load balancer.Here we are using example.com slash images.But here we are using images, not example.com, right?If a user is going to send requests or images, not example.com, it is going to forward request tothese two EC2 instances.In the same way, if user is going to open order dot example.com, same load balancer will forward thisrequest to these two EC2 instances.Now famous website like Google.Okay, we have Drive.google.com.We have Mail.google.com, right?So Google is actually using host base.I am going to give you one simple, uh, detail that that will never confuse you about these two policy.See order dot example.com.Now order okay is actually hostname.So you just need to remember order dot example.com means it is a host base.Slash means path okay.This is what the path based routing policy.Now here Route the traffic based on the domain name.Domain name is hostname.The request is images dot example.com or does dot example.com.Okay, now this is one of the two criteria that we need to remember.And there are lots of differences between these two as well.Okay so here I have a major six differences between these two routing policy okay.If you if you talk about the DNS configuration let's say that if I'm going to set up path based routingokay.DNS configuration point to add no specific DNS setup for individual path.So if you are going for the path based routing okay.DNS setup would be very easy for you if you are going for the host based, because here we are usinghostname required DNS record for each domain or subdomain to point the Alb.So here you have to set up DNS records.But if you are using path based the whole process will be very very easy.As per the DNS perspective use case, useful for routing to different application or services underthe same domain, right?It is example.com only, but if you want to use multiple domain you can use this with host base.Okay.Ideal for routing different application or service based on the different domain or subdomain.Now subdomain means images dot example.com.Now images is a subdomain.SSL TLS certification.Because we want to set up https website in the path base, a single certificate can cover all the pathunder the domain in the host base may require multiple certificate if you are using different domainnames, so it is a little bit complex.Flexibility.Very flexible, limited to routing with a single domain and here host base more flexible supportingmultiple domain and subdomain.Okay, so if you have a single domain path based uh policy will be flexible.But if you have multiple domain then host based routing policy will be very flexible.And the last one complexity.If you talk about path based generally simpler to manage with fewer DNS record, we just need to setup one DNS record and with the host base can more complex due to the managing multiple DNS entry andSSL certificate.So this is all about path based routing policy and host based routing policy.But I know my audience.I know you guys.Without practical you will never understand anything right.So no need to worry.We are going to talk about all this practically in the next video.We will going with the practical as well.So don't worry about this.Thank you very much.See you in the next video.Have a nice day.Goodbye.