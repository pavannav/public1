# 18.25 REST API Lab - Part 3 Add Method Resources and Deploy API To a Stage

Hello and welcome to Cloud Fox Hub.My name is Bhavesh Katara.Now we are going to start part three of our rest API lab.In this particular part we are going to add method.And then we will deploy our API to stage.Let's start.First of all let's add methods.So I'm going to start with the node.In the http API lab we created routes and each route was directly connected to the Lambda function.In the rest API we do not create routes, instead we create method.Each HTTP method get, post, delete must be created separately and map to one lambda function.So in this lab we will create one method.For each lambda function we have three lambda functions.So we need to create three methods and how we are going to map this for post, we will trigger createuser lambda function for get.We will trigger get user lambda function and for delete again we will trigger delete user lambda function.Right okay.Now let's start with the first post method.Now this method we are going to create into the user right.So let's do this directly to the console.Here I provided all the details so you can follow this.First of all we need to create this method inside user.So I have selected slash user first.Then click on create method.Now which method we want we want to add record right.So we call it post lambda function.We want to enable this lambda proxy integration.So our process will be very easy.We are not going to touch any other options buffer.And everything is good.Now from here we need to choose our lambda function.And we have this create user function.So post and this is create user function.We are not going to touch any other option.So let's click on create method and it is done.Now we need to create another two method.First is going to get or we can say that fetch record.And here we are going to provide user ID.So we have to create this inside slash user slash user ID.So I'm going to select this create method.And this method is used to fetch means get enabling Lambda proxy integration.Here I am going to choose get user function and not going to touch any other options.Then same way when we are going to delete specific record we have to provide user ID.So here I am going to choose this create method.Then I'm going to use delete option over here Lambda integration.And here I'm going to choose delete user function.Let me click on create method.And now it is then we have all the three methods.Did it get and post.Now once you have everything ready you need to deploy API.But look at this.This is grayed out.Why?Because this action is not available while API is updating.Currently API gateway is updating my API.So it is going to take some time.Once this process will be over, this deploy API will be highlighted and then I can click over there.Right now.We have created all these three methods.But what is the use case of deploy API.Because during this http API lab we don't have this particular step right.So why in the rest API we are doing this.Let me explain you this note.In the rest API we must create stage and then deploy API to this to that stage to get invoke URL.If I'm not going to deploy API, I am not able to get invoke URL right.So I have to deploy this.And during the deploy I have to select my stage in http api.There is a stage also, but a default stage is created automatically.So no manual stage creation is needed.But here we have to do this.Now how we can do this?Here we have this particular step.Now I am waiting.Once I will get this deploy API option.Then we can deploy this right.Okay I got it.Let me click on deploy API.Now look at this.It is asking me for stage.Now I don't have any stage.So I am going to create new stage.Let's assign name dev and deploy.Now new stage has been created and my API deploy inside this stage.And look at this I got this invoke URL.This is a normal URL plus slash dev I have, so now I can use this particular URL to send requests tothis particular API.So stage has been created.Now what happens after deployment API gateway generates invoke URL.Yes we are getting it API all created method post, get delete become active and you can now test usingpostman browser and front end.So what we can say after this particular part we can say that everything is ready.Our rest API is now ready to work, but we have to test this.We have to test each and every functionality.It is working as per our expectation.Right.So how we can test this?You will get complete understanding about this into the next video.So please complete part number four to finish your lab.Thank you very much.See you in the next video.Have a nice day.Goodbye.