# 18.24 REST API Lab - Part 2 Create Rest API And Define Resources

Hello and welcome to Cloud Focus Hub.My name is Bhavesh Katara.We are doing our Rest API lab and this is part two.Let's start.In the part one we have created DynamoDB table Lambda functions.Now we want to set up rest API.In this particular video we have mainly two parts.First we are going to set up rest API and then we are going to create relevant resources inside thisRest API.So I am going to start with the create rest API gateway first.Here I have the step by step detail.I'm going to perform this lab directly from the console.So I'm going to the API gateway.I don't have any API so let me click on create API.This time we are going to create rest API.So go to the rest API tab and click Build New API.Here I have to provide name.Let me copy this name.API endpoint.We want to go with the endpoint.Here I am going to select most relevant security policy.This security policy is actually supporting one TLS 1.3 and 1.2.So it is most compatible with all the browsers.So we can test it very easily.That's why I'm going to select this endpoint access mode I'm going to choose basic.Strict is also there but we will talk about other options later.Let's go with the simple steps.So I'm going to choose basic and IP address type IPv4.If you want to go with IPv6 you can go with the dual stack which supports IPv4 and IPv6 both.But I want to make this lab very simple.So I'm going to choose IPv4 and let me click on create API.Now my API has been created.Once your API is ready in the rest API you need to create resource.So we are going to perform our next step.Define resources.Let's start with the note first.After creating the rest API, first create resources because rest API is based on the URL paths.In this lab we create one main resource slash user and then one child resource.It will be slash user slash user ID into the curly brackets.Because our API has two different URL path and two different types of operations.To perform operations we are going to use two URLs.One is very simple.Let's say that I want to fetch all the users, so I will use slash user.But if I want to fetch specific user, I am going to use user slash whatever the user id is.So I can able to get or fetch record of the specific user or delete specific user.So we need to create two resources over here one user and inside user.It is user slash user ID when we are going to create two specific resources means we are going to gettwo URL and during the test I will tell you that okay, these are the two URLs based on the resourcethat we have created.That's why in rest API design, each unique URL path must be created as a separate resource.One is slash user, another one is slash user user ID.That's why two resources.That's why we create two resources first and then attach method to them.Now here we have all the details about how we can create resources.So let me do this.Here we have our API.Now you need to click on Create Resource.This is a slash means our main parent default resource.We cannot delete this.We are creating sub resource inside this.So here we need to use less user.Now create and look at this.This is slash.User resources has been created.Now I want to create child resource right.So this will be slash user ID.Here we have this step by step detail.Now here we need to create this as a child of slash user.So you have to select slash user.Now click on Create Resource.Here we need to add this.Why we are using curly brackets over here.Because here we are going to add value.User ID is one of the.Record over here right.User ID is a primary key.So we are going to use this to access record.So we will add one zero 1 or 1 zero two whatever the user ID we have.So that's why we will use braces because here in the actual URL we will add value.Let me click on Create Resource and Resources.Are now ready okay.So this is done.Now we need to add methods.So once you have your resources ready we need to add add methods.In the next video we will add methods.Thank you very much.See you in the next video.Have a nice day.Goodbye.