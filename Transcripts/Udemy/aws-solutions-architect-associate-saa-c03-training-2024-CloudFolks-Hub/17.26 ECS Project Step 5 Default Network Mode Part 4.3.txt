# 17.26 ECS Project Step 5 Default Network Mode Part 4.3

Hello and welcome to Cloud Fox.My name is Bhavesh Katara.You are learning Elastic Container Service through the project.We have divided this entire project into the eight steps.And right now we are discussing step number five understanding ECS Task definition.Here I am inside ECS console and we are getting lots of options when we are creating task definition.We already covered so many topics about the task definition as well.Right now we are discussing about network mode.Here we have total five network modes and we already covered AWS, VPC and bridge mode.Today.In this video we are going to talk about default mode.So let's start.Now I am going to explain you about default mode More using mind map.I want to explain you some basics of Docker.For 2 to 3 minutes, just forget about ECS.Let's talk about Docker.We know that if you want to run container in our virtual machine, we have to install Docker.Now Docker can be installed in any operating system.So you can install this Docker inside Linux or in windows as well.Even if you want to use Mac OS, you can install Docker inside the Mac.But we are not discussing about Mac right now.So I have Linux docker host or it can be Windows Docker host.But if we are going to talk about default networking mode of Linux Docker host or windows Docker host.It is a different networking mode means how your container will be accessible from outside world.So in Linux Docker host the default network mode is actually bridge mode.Bridge mode is supported by only Linux.In Windows, the default networking mode is actually Nat, right?So here we have difference between Linux container host and Windows container host.Linux supports bridge mode by default.Windows supports Nat mode by default.Right.So this is like basics of Docker.Now let's come back to the.Let's say that if I have EKS cluster right EKS cluster.Now in this EKS cluster we can add Fargate or ECS instance.Now, Fargate is a serverless, so there is no need to worry about operating system.But if I'm going to use EC2, then again I have two option I want to add.Linux host means Linux container host or windows container host.So you can add Linux container host or windows containers according to your requirement.Let's say that if you want to host any windows based application, you can also add windows host.Right?Now.If I have Linux host and I am going to set up default network, I can choose bridge mode because itis the same here we have right in normal standalone Docker host.The default mode is bridge mode.more.Here I can also select bridge mode and we already discuss about this how bridge mode works.But if I have windows host, if I have windows host how I can choose.Net mode.For us this is known as default mode.So actually the default mode is specifically for Windows Container Host which supports default mode.Net right.So this is main use case of selecting this particular option.This is only supported by Windows Container Host.If your windows container host you can go with this for Linux.If you want to go with the default mode you can go with the bridge mode.So bridge mode is not supported by windows Container host and Default mode is not supported by Linuxcontainer host.Right.So this is the main explanation about this.Now let's understand using mind map in Amazon ECS.The default network mode depends on the depends on the operating system of your Docker host, and ECSsupports both Windows and Linux.If your ECS task runs on Linux server, it uses bridge mode.By default it is called bridge mode, but if it runs on Windows Server means Windows Container Host.It uses.Net mode because windows does not support bridge mode, right?So when you are going to select this default mode, first of all, if you have EC2 only environment,because if you have Fargate environment, it supports AWS, VPC, even EC2 supports AWS VPC as well.So I am always recommended to go with aws VPC, but we are actually understanding all this option.So that's why I'm talking about the use case.Now I just selected Amazon EC2 instance now.I have to go with the second third process.Do I have windows instance.If I have windows instance and I don't want to go with AWS VPC, I will go with the default, right?So the default network mode of windows will be selected.Now if your Linux then you don't need to worry about default.You do not need to select default.You will go with the bridge mode if you want to go with the default option.Otherwise we have aws VPC which is the best one.So default mode supports EC2 and windows container host only, right?So this is all about the default mode.I hope you got complete understanding about this.We are going to talk about more network options step by step.Keep watching videos.Goodbye.