# 17.67 Node Groups and Nodes in AWS EKS Cluster (EKS Classic Mode)

Hello and welcome to Cloud Fox Hub.My name is Bhavesh Katara.Now you are aware about quick configuration with X auto mode.You're also aware about custom configuration with X auto Mode enable, but you are not aware about customconfiguration with X Auto Mode disable which is classic mode.In this video I am going to talk about this particular mode, so let's start.Now before I am going to talk more about it, I just want to give you quick details about X configuration,auto mode and custom configuration x auto mode first, and then we will go for the auto mode disablewhen we are going to choose X auto mode in the configuration or custom configuration.Meaning is AWS is going to manage our worker nodes.Notes in the custom configuration with X auto mode.AWS will manage our control plane, but we have to manage our worker nodes.Now question is how we can manage these worker nodes.In this case we have to create node groups.So in the custom configuration with EKS auto disable we have to manage worker nodes.And we can manage them by creating node groups.I am going to explain you node groups and nodes and types of nodes available in this video.So we are going to start with introduction first.In Kubernetes nodes also called worker nodes are machines where your application workloads actuallyrun.In Amazon, EKS nodes are organized into node group and which is only available in the classic mode.Why?Because in auto mode we are not actually managing worker node, so we don't need to manage node groupA node group is simply a group of worker nodes that connect to the same EKS cluster and share the sameconfiguration.Each node in the group runs the Kubelet agent, which keeps the node register and sync with the controlplane.There are two types of nodes that you can add inside node group one EC2 nodes and another one is Fargatenodes.So in the classic mode we are actually responsible to add nodes into the node group.And we have two options one EC2 and another one is Fargate.Now let's understand the differences between these two.And this is similar as we have in the ECS right.So let's talk about this EC2 nodes.These nodes run on Amazon EC2 instance.You can choose the instance, type, size, and operating system as per your application requirement.You have full control to scale, update, and replace your EC2 nodes.Best suited for the workload that need custom configuration like GPU, high memory and local storagebecause it is giving us flexibility to choose our EC2 instance type.Now Fargate.These nodes runs on AWS Fargate, which is a serverless compute engine for containers.You don't need to create EC2 instances.AWS automatically provision resources for your ports.Best for small, lightweight and event driven workload where you don't want to handle server management.You just define your port and Fargate takes care of the running it securely.Now keep this in mind which is the most important part.Here we have this note.AWS Fargate is only available in EKS classic mode.Classic mode means you are choosing custom configuration and your auto mode is disabled and you aremanaging your workload manually, right?Otherwise, in the auto mode you are not going to get forget.You will get EC2 instance, but this EC2 instance will be managed by AWS.Okay now.Types of node groups.So in the classic mode we have node groups.And then we have two types of node groups as well.So the first we call is managed node group.And then we have self-managed node group.Now I am going to explain you the differences between these two.First of all I am going to start with managed node group.Introduction AWS manages EC2 nodes for you.So if I am going to create managed node group means AWS will manage my EC2 instances kind of auto mode.But keep this in mind it is not auto mode.I am going to clear your confusion about it in, uh within five minutes.But yeah, it we have to create uh, node group.But this is a managed node group.So AWS will handle everything.Everything means creation, update and replacement of EC2 instances.In the self-managed node group, you manually create and manage EC2 instance yourself and then connectthem to the EKS cluster so it is a complete manual mode.EC2 management created and managed by AWS EC2 management.Created and managed manually by you.Updates and scaling handled automatically by AWS.Update and scaling.Manually update and scaling means you are going to manage this integration zone and control in ECS consoles.You do not need to go to the EC2 console.Here you are creating EC2 manually and connect it with your control plan, so you have to manage thisseparately from the EC2 console node.Registration.It is automatic.Here we are manually joining these nodes to the control plan by running script control level less control.Because AWS is managing everything here we have full control because we are creating everything manually.Use case recommended for most of the users, and use case for the custom setup where you can chooseyour OS, config and networking.Now you have like the same level of confusion as I have.Yeah, managed node group and.Auto mode looks similar, right?Look similar.No need to worry about this.Here I have this particular point of the mind map which is going to clear all your confusion.So let me finish this.Your concept about EKS cluster will be crystal clear.So here I am going to explain you how it works in auto mode.Means I'm talking about how node, group and nodes work in auto mode, right?Okay.In EKS auto mode, you do not create or manage any node group yourself.So in EKS auto mode we can say that node group is completely Invisible to us.We don't need to manage anything.Right.In the auto mode.AWS automatically provision and manage compute layer behind the scene in auto mode.In managed node group means you have classic mode.You are creating your worker node and you have classic mode, right?So in the classic mode we have managed node group.In managed node group you create and managed node group.So if you have classic mode you have to create managed node group and you choose instance type sizescaling and setting.So here in the managed node group AWS is managing everything.AWS is going to create EC2 instance.That's true, but we have to choose instance type size scaling option.So managed node group is giving us some flexibility 80, right?And in the auto mode, we don't have node group because it is completely invisible to us.AWS is going to manage everything.Auto mode already includes its own AWS Managed Compute System, which replaces the need of visible nodegroups with node.Group is not visible to us, right?In short, auto mode is not equal to managed node group.Managed node group is only available in the classic mode.Auto mode is higher level of automation where AWS manages everything automatically, right?So now I think you don't have any confusion about any of these options.You have clear idea about configuration with EKS, auto mode, custom configuration with auto mode enableand custom configuration with auto mode disable.Still, if you have any confusion, I am just one message away from you.Thank you very much.See you in the next video.Have a nice day.Goodbye.