# 17.28 ECS Project Step 5 How Network Mode Affects ECS Task Definition Part 4.5

Hello and welcome to the Cloud Fox Hub.My name is Bhavesh Katara and you are learning Elastic Container Service.Inside we have to set up task definition.And then we are going to setup task definition.We have lots of options to understand from previous 4 to 5 videos.I am actually explaining you all this network modes.Now we already discuss about all these modes, but I want to talk about something related to the portmapping options based on this network mode.You know, this is one of the favorite interview question.So I thought like, let's understand port mapping related to the various network mode options.This is for your knowledge.You can say that not for your examination.So let's start.As you know that in ECS task definition, if you are going to choose Fargate, we have only one optionavailable AWS VPC.And if you are going to choose Amazon EC2, we have all these five options available.Now none means what if I'm going to select none means my container accessibility is completely off fromoutside world, so there is no discussion needed for none.So we are going to talk about all these four options step by step.I have this proper mind map.Let's start with this mind map.How network mode affects task definition I'm going to start with AWS VPC mode.In AWS VPC mode.Do you need port mapping?No needed IP type, own private IP.You know, when we are using AWS VPC mode, your task will have any directly connected, right?And your task will be accessible directly from the VPC.So whatever the port number you are going to assign to your task will be accessible directly.So if you look at over here, if I were to choose AWS VPC mode and going to scroll down my container,I am only getting container port right.So we just need to assign port number of the container.That's all.Specifically in AWS VPC mode now we have second option known as bridge mode.Now in the bridge mode I explain you earlier as well that we have virtual bridge running inside containerhost the bridge.Having a IP address and bridge is also going to provide IP address to all the task.In this case actually host Eni is said.Now if you want to access any particular task, let's say that this task is running on this particularhost with the IP 172.1.2 and port number 80.So how you can access this?You will use host port.If you want to access any application running inside this task T1 you have to use host port right.So you will use like this 172 1610 dot ten if outsider sending over here accessing this T1 task, youwill write like 172 1610 dot ten.It is going to use host IP.Then there is a port number.We call it host port and then IP address and container port.So specifically in bridge mode we are actually doing port mapping.We have to define host port and container port.Let's say that we have task tasked T2.How am I going to access this?172 1610 1080 81 now, 8081 is a host port, and we want to map this with 172 1.1.3 colon 80.So if you will access like if you want to access T1 application, you will write like this one 721610.10 the port mapping will work over here.So 8080 will forward it to 80 of one 7210 .1.2 and 8081 will be forwarded to the 10.3 colon 80.So in the bridge mode you have to go for the port mapping.You have to configure port mapping.Look at this here I'm going to select bridge mode.And once you go to the container you will get this host port container port y host port means.This one host IP.And then the port number that you are assigning over here.And the container port means this particular port, right?So in the bridge mode, port mapping needed 100%.Yes.Yes.Container port.Host port mapping IP type shared host.Because here we are going to share any of our container host among any tasks running inside specificcontainer host.Right.So yeah port mapping is needed in the bridge mode.Now here the third mode we have is actually default.Now default means what default means specifically netting for the windows container host.So if you are using windows container you are going to use default mode.Because Linux Container host supports bridge mode and windows container host is not supporting bridgemode.So it supports default mode means kind of netting, but it is the same like a breach, but it is foronly window.So in the default mode you have to set up the host port and container port, right?So port mapping is also needed in the bridge mode.Same way port mapping is also needed for the default mode.Bridge mode is for Linux.Default for windows container host, right?In both of these cases, you need to set up port mapping in the host mode.We are not going to forward anything.We are going to access our task directly using host DNA, right?Yeah, we are sharing host DNA, but here if you want to access any application, we are actually goingto use host Eni directly.Right.So here routing or or forwarding is not actually happening.Whatever the host Eni IP address you have we are directly going to use it.Give me a second.Let me clear this out so you will get better understanding.So if you want to access application running inside this container, the container will have same IP,right?One 7216.1010.Same IP as your host DNA.You just need to define port.You will say that 80.Now if you want to access this application okay.How you can do this.Let me clear this.I will access this.You will write like this one 7216 1010.Sorry.Ten 1080.It is going to access this particular application.If you want to access.This again the IP will be same because we are going to share IP.So one 7216 10.10.Colon 81.So if you will write like one 7216 10.10 colon 81.It is going to access this application running inside T2.But here we don't need any port mapping right.So if you go to this after selecting host mode and go to the container here, you are not going to gethost port.Why?Because host mapping is not needed.In short, in only two modes you need host mapping, port mapping, one bridge mode and another oneis a default mode.I hope you got complete understanding about this.Let's open host port mapping.Host port mapping needed.No mapping needed.IP type shared host.Right.So this is like a last video about the network mode.I hope you got complete understanding about network mode in task definitions.Till if you have any question I'm always available.Thank you.Goodbye.