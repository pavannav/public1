# 17.9 Add EC2 Manually After Cluster Creation

Hello and welcome to Cloud Folks Hub.My name is Bhavesh Katara.Again, in this video we are going to create ECS cluster.And now you are completely aware that while we are creating our ECS cluster, we have two infrastructureoptions, one Fargate and another one is Amazon EC2 instance.We already discussed about Fargate.Now we are actually discussing about EC2 instances.We also discuss that while you are creating your ECS cluster, and you are completely aware that youare going to use EC2 instances as a part of your ECS cluster, you can select this while you are creatingyour ECS cluster.Now sometime you are not sure about it, right?You thought like okay, right now let's go with the Fargate, which is having bad drops and having serverlessoption.So it is good, but if it is required in the future, I will add EC2 instances, right?So can I add this later?Yes, we can do this now.While you are creating your ECS cluster, how you can add EC2 instances and how it is going to react.We already discuss about it, right?In this video we are going to discuss about what if I want to add EC2 instance later after creatingcluster.Okay.So right now I am going to create cluster without adding EC2 instance.So my cluster will be ready.But now my company decided that we want to add EC2 instances as well in order to run some containers.How I can do this?I'm going to explain you entire process with practical in this video.So let's start.Now we are going to start with this option two.Here we are going to start with introduction.If you didn't selected EC2 while creating cluster you can still add it later in two ways.So here we have two ways to add our EC2 instance one.Launch individual EC2 instance and add them into the cluster.Another option we have is create your own auto scaling group manually.So if you don't want high availability and if you want to add individual EC2 instances into the cluster,you can do this.If you want to create Auto Scaling group, you will get high availability as well.And you can decide number of EC2 instances as well.So you can select both one of the option What we are doing right now.We are going to launch individual EC2 instance, and we are going to add this instance into the autoscale into the ECS cluster.Right?In this video we are doing this.In the next video we are going to set up Auto Scaling Group.So we will have both options available.And we are going to perform lab for both of this.Now here I have my cluster ready.This is my cluster.And if you look at this cluster here we have infrastructure and I have Fargate only I don't have anycontainer instances right.I want to add container instance to my existing EKS cluster.So first step is I have to launch EC two instance.Now when we are going to launch EC2 instance it is very normal EC2 instance.You can select size of any but you just need to take care about ECS optimized am I right?So.If you are going to the EC2 and if you are going to launch normal EC2 instance, it is also become apart of your cluster.But the problem is if you are selecting normal, am I right?You have to install container runtime.You have to install ECS agent.So every process will be manually.The better option is you can directly go to the browse mode Ami.Then you can go to the marketplace.Am I right here?You can search for ECS, optimize Amazon Linux Ami.Now there is a ubuntu option also available, but keep this in mind.You have to search ECS optimized, right?Advantages.You do not need to install container runtime.You do not need to install agent anything not required.Right.So everything is pre-installed that we can say now here we are getting so many.Amis.So what we can do here, we can directly search for Amazon Web Service right here.I am getting so many.But look at this I got it Amazon EKS optimized Amazon Linux okay.So I'm going to select this.Now when you select this here you will get some detail about the pricing and all.So you don't need to worry about this.It is a free tier.And if you go to the pricing it will so detail that if you are going to use with M5, then you haveto pay for this.For T2 micro it is completely free so don't need to worry about this.You just need to click on subscribe now and you are now going to use this particular Ami for your EC2instance, right?So now it will be listed over here.Now by default it is actually creating instance type M5 large.But we don't want to do this because M5 large is actually expensive one and we want to go with the freeone.So I'm just going to select T2 micro right.T2 micro is free tier eligible and it will work properly with our instance.Now we need to enable or select key pair as well.Key pair we are using in order to access our EC2 instance using SSH.All these options will be normal.We don't need to touch this.Let me launch instance.So here we have completed VPC.I have default VPC.So I'm just going to use this to make the ECS networking setup.Everything is done.My EC2 instance is now ready.Now once you have your EC2 instance ready, you need to attach one role with your EC2 instance.You guys are already aware about Im, so let me show you from where we can find this role.Here I just forget to assign tags.So let me assign this.Let's say that ECS.Cluster node one.You can give any name according to your convenience.So this is the name we have.Now you can go to the action go to the security and here modify IAM role.Right now there is no role attached.We have to attach one role.Let me explain you this first.And then we are going to perform related task to allow your ECS to register itself with ECS cluster.It must have permission to communicate with ECS.So we are attaching this role because now, uh, my my host container host is going to the ECS to registeritself.Right.And we need permission for this.So that's why we are going to attach the role.This is done by attaching IAM role.Now here we have the complete procedure that how you can attach role.So let me do this.So here I am going to this I am going to click on this list box.And I'm getting this ECS instance role.Look at this Is right now I am getting it.Maybe it is possible that you are not going to get it right.I'm getting it.Why?Because I already created cluster for some other stuff.So here we have this explanation.See if in the past you created ECS cluster with EC2 infrastructure selected, AWS automatically createdECS instance role for you, right?So in my case I'm always giving demonstration and all this.So it was there automatically created by AWS.But if you create a cluster without EC2 this role may not exist.So in your case you may not get this kind of ready to use role.If you don't have you don't need to worry about this.I'm going to provide you procedure.You have to go to the IAM and you guys are already aware about the IAM because we already completedIAM series, right?Then you have to go to the role.Now here you need to click on Create Role.Here you need to choose EC2.Then next.Now here you have to attach this particular policy with your role right.So if you are going to search this particular service over here you will find out.Now you may have one question over here as well.Like how you can find out this.I find out this from documentation of AWS right.Next.Now here you can provide name.You can provide name like ECS instance role.And once you clickcreate over here then role will be created.Then you have to refresh from here and you will get this I'm not going to create this role.I'm going to cancel this because I'm already having this role.So I don't want any duplication.So I'm just selecting this ECS instance role.and update IAM role.Then means my second task is actually completed.I attach role successfully and because of this, now this EKS cluster node one can register itself tothe EKS.Step two.Then in the state.Step one.Then in the state.Now let's go to the step three.Now step three is in the setup that we have to go inside my EC2 instance.So I am going to the EC2 instance.I am going to access this EC2 instance via SSH right.Okay so I have this key already in my computer.If you if you remember while I launched this particular EC2 instance.We use this particular key.Uh key name is actually UT key..You can find out the name of the key from here as well.Yeah.C key name u t key.Right.So I have this in my computer.So ssh this I UT key ec2 dash user.Now here I have to copy public IP.Let me paste over here.And I got access of my EC2 instance.Right now the process is very simple because we have ec2 ecs optimized Ami.So we have agent already installed.We have um container runtime also installed.So we just need to edit one file and the file is etc. ecs X.X.Right here it is actually supporting only VI editor.So we are going to use this.Otherwise you have option to choose Nano.But nano is not going to work.So I'm just going to copy and paste over here this command VI I want to add some detail into this particularfile.So I'm going to open this.By default it is going to create new file.No need to worry.Now here we just need to add this detail okay x equals to your cluster name.That's that's done.That is the only thing that we need to set up over here okay.So let me go to the X.Look at this.We don't have any container instance till now.Over here.My cluster name is my cluster.So I'm going to copy this and paste it.Then save the file wq.And what we can say that step three is also over now in the step four.Sorry.In the step three, we have to.Restart this service, right?C service is there.If you look at over here okay.See the status.Right.We are getting service tail.We just need to restart this.Right now check out because we already added this.We have now proper service.So let's see the status.And now it is active okay.So then and.Instead we have completed this here it is a typo.Let's say that in step four we are just going to verify this detail.So now I'm going to the ECS cluster.This is my cluster going to the infrastructure here.Now I'm getting the C container instance.And if you look at this instance id this is my instance ID A48.So.This is what you are getting over here as well.So now I added my individual EC2 instance into the EKS cluster.This is like a very simple process.Specifically if you are using ECS optimized Ami.But the problem is here we have single point of failure.If this EC2 instance goes down, any container or task that you will run inside this will be down aswell.So we don't want single point of failure.We want high availability.We want scalability as well.So in this case you can go with the second option.And the second option is create your own auto scaling group manually and add it to the EKS.How we can setup this.We are going to explore this option as well, but in the next video.Thank you very much.See you in the next video.Have a nice day.Goodbye.