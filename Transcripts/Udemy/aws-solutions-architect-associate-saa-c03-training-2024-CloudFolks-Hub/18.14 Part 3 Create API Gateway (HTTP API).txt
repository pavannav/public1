# 18.14 Part 3 Create API Gateway (HTTP API)

Hello and welcome to cloud folks.My name is Bhavesh Edara.We are doing our first API gateway lab and we have divided this lab into four parts.We already completed part one and part two.Now in this video we are going to explore most important part of this lab part number three becausehere we are creating API gateway.So let's start.First of all we need to understand that why we are creating API gateway.Over here we have our DynamoDB table.We have APIs.And these APIs are capable to add, remove and read records from DynamoDB table.So what is the use case of API gateway over here?Understand API gateway introduction.Our API logic is now completed.We already tested this.We have written the core backend code to create user, get a user and delete user.But remember Lambda can't talk to mobile app directly.Problem is we will use front end to add, read or delete record.So problem is actually here.My front end cannot directly talk with my all lambda functions.Okay so this is issue that we are solving right now using API gateway.Problem.Your mobile app wants to talk to Lambda and store fetch and delete data into the DynamoDB.But mobile app can't directly invoke Lambda functions.It's not secure.It's not allowed publicly.If you have lambda function, you cannot trigger directly from the internet or from the outside of yourAWS solution.API gateway.API gateway acts like a front desk.It's the entry point that accept requests from the outside world.Means from front end and then forward them to the Lambda function.Now keyword is correct.Lambda function right.So what will happen over here?Front end is going to send HTTP request to our API gateway.Now this request is totally based on Crud.It can be create get and delete.So what will happen over here.Front end will send HTTP request, HTTP request and based on the request, if it is a create it willtrigger our lambda function create user function which is going to add record.If it is get, it is going to trigger get user function and if it is delete API gateway will triggerthis.So we don't need to create multiple API gateway.All this three APIs will be triggered through the API gateway.And API gateway will be acts like a bridge between frontend app and our API hosted into the Lambda,right?Okay, so this will be the entire process.Now what are the responsibilities of API gateway?Accept HTTP request from the client.Route them to the right lambda based on the URL or method that we will discuss.Validate input, apply throttling or security if needed.Right now we are creating http API, so we are not going to get this validation and all this.But yes, if you are creating rest API, it is actually providing all this functionality as well.Once you will finish this lab, definitely we will create Rest API which will provide this validateinput functionality and all send back the response from Lambda to user.Now we have to map these functions right.So here we have mainly three process post which is going to add new record get which is going to readthe record from DynamoDB table and delete going to remove the record.So for post we have to set up route into the API gateway.Now it is going to trigger this particular Lambda function for get.This is another route.We have to set up this into the API gateway which is going to call get user function.And same way we have third route which is going to delete the record.Right.Okay.Now why not call Lambda directly.Why we need API gateway.What is problem?Why we are not able to call lambda function directly from our front end application Lambda.Don't have public HTTP endpoint.Security is hard to manage without API gateway.API gateway has structured the communication.So API gateway have all this functionality to provide secure communication.Right.So API gateway is a kind of middleman.Now that all three Lambda functions are ready, you can create API gateway to expose them as a realAPI routes that your mobile app and web app can call.Right?Okay, so now you understood this process.Let's create API gateway.We are going to set up http API.We now create http api in API gateway and connect it to our Lambda function.So these are the steps.step.First of all, I have to go to the.API gateway.Now, I don't have any gateway, so I'm going to click on create.As you know that we are creating http API.So I'm going to click on build.Here we have to provide name.So I'm going to provide name user API.You can use any name.No need to worry about this okay.User API we want to go with IPv4.Right now we are not going to configure any route or stages.If I'm talking about stages it is like you have dev stage or production.So for different kind of requirement you can set up different stages.Right now we are just starting.So we are going with the default stage.Let me click on next and create now our HTTP API is ready.We need to set up routes.So we are going to add route.Once you have created http api in API gateway, the next step is to define route.This means telling API gateway when request comes to this path.Using this method, send this to the Lambda function right.So you will get better understanding.Let's create first route.Create a new user.When we are going to add new user record, it is a Post request.So when lambda sorry when API gateway is going to get this Post request, we want to forward this tothis specific lambda function which is create user function.Right.So how we can do this?Here I am inside my API gateway.You will get this routes for user API.Here you need to click on create.Now we are looking for post request.So we need to choose post.Now how we are going to trigger this here we are saying that slash user okay you can any but we areusing user right now.When we will trigger this we will use this particular option user.Now let me click on create.Now when our API gateway is going to get this request by slash user.And it is a Post request we want to trigger lambda function.So here we have this attach integration and create an attach integration.Here we have lambda function.And out of three function we have this create user function.So we are going to trigger this function right create.So this is our first route.Look at the picture.You will get better understanding.When front end will send post request.API gateway will route this request to this particular Lambda function.Right?Okay.Now we have to create this forget and delete as well.So here we have these tabs I'm going to create this directly.So let's go with the get.Now when we are going for the get we have to provide user ID.So we are using slash user and get user ID.So let's go to the route.Here we have routes for user API.I'm going to click on create.Now I'm going to for the get we want this slash user slash user ID we will provide user ID.And here we are going to click on create.Now we are getting this because it is slash user get user ID right.So it is inside this.Now what we want to do here with the get we want to add At another integration.So we are going to click on Create integration here Lambda function.And which lambda function we want.We want get user function right.So it is going to read the record create.It is done.Now the third one which is going to delete this.Now this is the same thing that we have right user user ID.So let's go to the routes create.Now this will be delete.Slash user.User ID create same.Now let's choose delete.Attach integration.Create an attach integration.Now Lambda function.This time we want to go with the delete user function right and create.So now everything is ready and our API gateway is completely ready to get requests from the front end.And based on the request, it is going to forward this request to the specific lambda function meansit is going to trigger specific lambda function.Now once you create this okay here you will get this trigger URL.Using this URL you can send requests to your API gateway.Now question is from where I can invoke this URL.We need to have front end.So how we can arrange front end.No need to worry about this.In the next video we will arrange front end and it is very easy to test this entire process.What we can say right now our lab is ready.Just we need to test it.And in the next video, we will test it very easily, even without front end.Thank you very much.See you in the next video.Have a nice day.Goodbye.