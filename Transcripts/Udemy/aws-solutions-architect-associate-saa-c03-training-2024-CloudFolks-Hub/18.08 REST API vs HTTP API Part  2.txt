# 18.8 REST API vs HTTP API Part  2

Hello and welcome to Cloud Fox.My name is Bhavesh Edara.This video is part two of rest API versus http api.In the part one, we already discussed about authentication and authorization, security and trafficcontrol.In this particular part we are going to talk about all this features.So let's start.Request and response transformation.You know this is actually main feature of Rest API.This is actual decision maker okay.Now let's understand this.So you will get better understanding about why I am talking about this particular feature.Request and response transformation.Introduction.Modify the request or response before sending to backend.Convert the format like XML to JSON or add remove field.Example.Front end.Send name and phone.They can expect name only API gateway modifies it.You know that there is a front end who is actually sending requests.Then there is a back end which is actually accepting the request and then it is going to respond.Now in between them we have API gateway.If you have rest API it can modify this request.So this is actually decision maker.Let me tell you http API is not going to modify any request or response.Whatever it is coming from the requester, it is going to forward it to the backend.So in any case http api cannot modify your Request.Okay, so keep this in mind now supported by rest API.Yes http api.No, it is actually not going to modify.It means it is a faster.So http API is faster.Why?Because http api is responsibilities.Whatever it is getting from the requester, it is going to forward it to the backend without any kindof intervention.Right?Okay.Now input validation introduction check request before sending to backend.If user send Rome data API gateway reject the request.Example email is missing API gateway blocked the request supported by rest API.Yes http API know all the three features are actually supported by only rest API.And that's why I'm telling you it's a decision maker.Introduction mapping template used to format the data using Velocity Template language.Customize input output format using template example.Convert request body to a different structure supported by rest API.Yes http api no.Right.So you have to remember all these three points because you may get questions related to this.Now let's talk about performance and cost first casing feature.Store responses to to increase the speed and reduce back end load.If many user requests the same data, API gateway returns cache response instead of calling back end.So whatever the response your API gateway is actually receiving from the back end, it is going to storethis into the cache memory.So if for subsequent request it is going to send a reply directly from the cache.Yes, and it is actually refreshing the cache as well.For example, product list refresh every five minutes supported by rest API.Yes http api no http API is not supporting caching, but http API is already faster.It is not going to interact with your response, so it is actually faster.Right?So caching is not required by http API.Now coast introduction.How much it costs to run http API is much cheaper than the rest API.HTTP api costs 70% less than the rest API.Rest api expensive http api very cheap.Now let's talk about monitoring and logging.Monitoring and logging is supported by both, so no need to worry about this CloudWatch logs.Introduction, track request error latencies, API gateways and logs to the CloudWatch log.Example check why request is failing.Troubleshoot purpose supported by both access logs.Store logs of who access your API.Track caller IP time method status code.Example.Generate audit log for the security team supported by both now.Integration types.Lambda integration.Can you integrate directly with Lambda API directly cause Lambda function user sign up.Then it is going to access API.It is going to trigger lambda function.Save the data to DynamoDB means back end.We will go for this process practically, so don't worry about it.But it can directly call lambda function supported by both now http URL integration.Introduction API calls HTTP endpoint.For example, API your company's back end hosted on EC2 instance or on premises supported by both.Then Alb integration application Load Balancer integration API can follow the traffic to the applicationLoad Balancer API.Alb service supported by Rest API.No http API supports this.Okay, so keep this in your mind.Now let's talk about other features as well.Custom domain.Use your own domain like API or CloudWatch in supported by both course.Course is a feature allows frequent react angular to call API supported by both private APIs.API accessible only inside VPC supported by rest API.Yes, and http API.No.And as we discuss about this Rest API private create Rest API that only accessible within VPC.So that's why I said that okay other features are same.So we just like considering both of them same.The only difference is.Rest API private only accessible by your VPC.So that's why I said yes over here http api no.So this is all about the comparison and giving you guarantee that after this presentation no one canconfuse you about it.In AWS exam they're asking questions okay.And it is always confusing people.And that's why I thought like okay, I want a proper comparison.So my student will be very confident while answering these questions.Thank you very much.See you in the next video.Have a nice day.Goodbye.