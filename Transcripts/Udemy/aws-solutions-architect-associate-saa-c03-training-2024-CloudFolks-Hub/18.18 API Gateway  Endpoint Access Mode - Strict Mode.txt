# 18.18 API Gateway  Endpoint Access Mode - Strict Mode

Hello and welcome to Cloud Fox Hub.My name is Bhavesh Adara and you are learning API gateway.Our current topic is rest API.Right now we are exploring all the options that we are getting while creating rest API.Once we will understand this, we'll perform our first lab of rest API.In previous video I explained you about security policy.In this video we are going to talk about endpoint access mode.So let's start.Okay.Let's start with introduction.When you use enhance security policy in API gateway for example TLS 1.3 or post-quantum, you must alsochoose endpoint access mode basic or strict.Look, right now I am getting this endpoint access mode option.Why?Because here security policy is the latest one means I'm using latest TLS version.For example if I'm going to choose TLS 1.0 legacy old TLS version, this will be disappear, right?So you need to remember that this end point access mode is totally depend on the TLS version that youare selecting in the security policy.Now, if you look at over here we have two options basic and strict.Now basic is not providing you this kind of protection.Look at this.Allow all the client access the API.So there is no need to talk about it.The main protection that we are getting is from strict mode.So I am going to explain you strict mode properly right.So what is strict mode?Strict mode is a security feature of rest API.It is designed to protect your API from the advanced TLS based attack.One major attack is protect against domain fronting.See there is a attack called domain fronting.Now strict mode is mainly used to save your API gateway from domain fronting attack.Now if you want to understand that how strict mode works, you have to understand domain fronting.Now, I am not able to explain you in detail like what is this domain fronting attack?But I'm just giving you overview so you can understand how strict mode works.Okay, so here it is.Overview of domain fronting.Domain fronting is a technique where a TLS layer uses one domain name and HTTP request uses anotherdomain name.Now you have to understand this.What is this TLS layer and what is this HTTP request.So I'm going to explain you this using whiteboard.Now look whenever client is going to send request this is a client.Now this client is sending request to my API gateway.Now it is going to send two requests.First we call this TLS handshake.Second it is HTTP request right.Now first request is all about verification.So client will send this request and along with this request it will send server name indication calledSNI.Here in this first step, client is saying that look I want to connect with API example.com right.It is not actual request.It is just a message that client is sending for TLS handshake.Okay look I want to connect with API dot example.com.Now API gateway will will receive this and it will verify that.Yeah I am API dot example.com.So it is going to send that.Yeah we are.You can send your real request right.It is going to send certificate as well.Now the client will send real request.This is called hostname or HTTP request.Here we have this hostname.Now client is saying that hey I want to access API dot example.com.This is message before request during TLS handshake.And this is real HTTP request right in normal request.These two things are always matching means sni api example.com and hostname api example.com.You said that I want to connect with api example.com.And now really you are connecting to API example.com, right?This is normal.But in domain fronting attack what is actually happening.You are saying that I want to connect with API example.com.You are getting certificate and then you are trying to connect with invalid example.com.Now this is domain fronting attack.In normal request it is always matching.Here it is not matching means it is a kind of attack.Okay.So this is like overview of domain fronting.This mismatch can be used to hide and misuse traffic.These two are not matching in the domain fronting example tls sni api.com and then HTTP attacker dotTLS connection succeeds but the request is routed differently.Okay so this is domain fronting attack.Now how strict mode works so that we are we can able to understand.Now when you enable strict mode, API gateway performs extra checks before allowing requests to reachyour rest API.It ensures that request is coming from the correct type of endpoint, and that TLS handshake matcheswhat is expects.If this check fails, the request is rejected.Now let me explain you how it works.So first same endpoint type.The request must come from the same type of endpoint.The API is configured for.I already explained you that in the rest API we have endpoint type and mainly we have three endpointtypes regional, age, optimize and private.Right.So if the request comes from the different type API gateway rejected.Let's say that I have regional API means request come must come from the regional API.Let's say that I have private API.It means request must come from the VPC endpoint.If request is coming from other then this.It is going to reject this in the strict mode.Let's say that I have age optimized.It means request must come from the CloudFront.Now if request is coming from the VPC endpoint, it is going to reject this in the strict mode.This is the first thing.Now second SNI server and hostname matching.Now this SNI server and hostname matching is only available if your endpoint is regional and private.In strict mode, the domain name used in TLS must match the domain name used in the HTTP host header.SNI is equal to host domain mismatch request rejected.Match request allow.Right.I already explained you about domain fronting attack, and I said that in normal request these two arealways matching SNI and hostname.It is always matching.Strict mode is also going to verify that okay these two are matching.Yes, strict mode will accept this request here.If you look at this, SNI is not matching with the HTTP request hostname.Strict mode is going to reject this.It means there is no chance of domain fronting attack if you have strict mode right now.If you have it optimized then it is working differently, right?So for age optimized API's API gateway uses CloudFront protection to prevent domain fronting.Domain printing is a trick I already explained about domain printing.So keep this in mind that if you have regional and private, then domain fronting will be preventedthrough the strict mode.And if you have optimized then we are actually depend on CloudFront domain fronting attack protectionto protect our API gateway.Right.So this is strict mode.I hope you got complete understanding about this.And still if you have any question I am always available.Thank you.Goodbye.