# 17.64 Kubernetes Architecture On-Premises VS EKS Part 2

Hello and welcome to Cloud Fox Hub.My name is Bhavesh Katara.This video is part two of Kubernetes Architecture on Premises versus ECS.Previous video means part one.We discuss about control plane worker nodes and ports and container components of Kubernetes.In this video we are going to start with networking.So let's start.First of all, let's understand what is the role of networking in Kubernetes environment.Kubernetes networking ensures port service and users can talk to each other.When we are going to talk about networking here, there are mainly two use case of networking one howports can communicate with each other while they are running inside different worker nodes.Second, how we can access application running inside the ports from outside world.Definitely to complete or to fulfill these two requirements, we have to set up networking in case ofon premises.You design networking rule manually.Complex setup to make ports reachable across the nodes.Why?Because our ports are running into the multiple worker nodes.Here, when we are going to set up a Kubernetes networking, we have to involve physical networkingdevices as well.Because how to worker nodes are going to communicate with each other.Definitely we have to use physical switches or in case of there are virtual machines, we have to setup this virtual machine environment networking options as well.So this is like a little bit complex over here in the case of AWS Amazon ECS.This process is very simple.Why?Because AWS is actually going to provide us vpc cni plugin.Now this VPC cni plugin is going to work like a Nic see for our ports each ports will get IP addressfrom your VPC, works natively with security group and NaCl as well, so we can utilize all the featuresof AWS VPC.You can you can think like AWS VPC cni plugin is like same.We have in X.In X we call it networking mode aws VPC.Same way here we have aws vpc cni plugin.Right now we are not going to discuss more about AWS VPC, cni plugin and all.Why?Because we want to concentrate specifically on a X, right?We are not going to discuss about how vpc CNI plugin is going to provide communication and all.We will understand all this with some practicals, but our main component to understand is x.Now advantage of X simplified secure networking with native AWS VPC integration.What you need to remember over here.Our ports can get connectivity directly with AWS.VPC because AWS is actually providing us AWS VPC CNI.Okay, now the next we have is load balancing.Load balancing distributes traffic across the multiple ports and service.Now if I'm going to talk about on premises, you install ingress controllers or configure load balancingmanually.In AWS we have load balancer available.So we can directly integrate load balancers like we have application load balancer.We have network load balancer.So we can directly integrate this load balancer with our EKS cluster.A load balancer service in Kubernetes automatically creates an AWS load balancer and gives you publicendpoint.Again if I'm going to talk about EKS advantage auto ELB creation and no manual setup.Now.Third point is security and IAM.Kubernetes security means controlling who can access cluster and ports for on premises.You have to manually set up role based access control.You have to set up TLS certificates and you must have identity provider to control user access.In AWS we have IBM so we can directly integrate IAM with our EKS environment IAM roles for service accounts.So ports can assume IAM role.Let's say that if I have an application running inside the port, and this particular application isgoing to store data into the S3.Now in this case, I can directly assign role to my ports and using this port role application can storethe data into S3.So we have IAM roles that we can easily use over here.In case we need encryption, we can use KMS keys over here to encrypt data again.X Advantage Enterprise Grade Im.Means we have identity provider ready to use and encryption out of box.Finally how we are going to monitor and logging our environment.Monitoring checks cluster health logging captures the application and system events on premises.Need third party tools like.We have so many third party tools like Grafana and all.We have to use this.We have to set up this first and then we can integrate with our Kubernetes environment.In the EKS, we have built in integration with CloudWatch logs, container inside and X-ray.So again the advantage is no extra tool.In AWS.We have some native monitoring and logging tool that we can directly integrate with ECS.So these are like advantages of using ECS.I hope you got complete understanding about this.Still, if you have any question, I am always available for you.Just one message away.Thank you.Goodbye.