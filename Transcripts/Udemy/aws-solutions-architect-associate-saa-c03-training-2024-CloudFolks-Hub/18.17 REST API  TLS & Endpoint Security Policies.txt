# 18.17 REST API  TLS & Endpoint Security Policies

Hello and welcome to Cloud Fox.My name is Bhavesh Katara.You are learning API gateway.Now what we are doing, we are actually exploring all the options that we are getting while creatingrest API.First of all, we will understand all these options and then we will set up lab for rest API.Right now I am inside my AWS console.I am going to create rest API.Now you already discuss about this first important option API endpoint type.Now based on this API endpoint type here they are giving us security policy.Basically we are choosing security policy to protect our endpoint.Now it is going to protect our endpoint using TLS and using security policy.We can choose which TLS version we want to apply to protect our endpoint.Now here you will get some options.Normal and some options are grayed out.Why?Because these policies are actually based on your endpoint type.If I am choosing regional over here, it is going to highlight all these policies which is applicableto your regional endpoint.If I am going to choose age optimize now policy applicable to age optimize will be highlighted.Other policies related to the regional or private will be grayed out.Right.But the question is what is this TLS and which option or which security policy should I choose overhere?You don't need to worry about it.We are going to explore all of these options step by step in this video.So let's start.First of all I am going to explain you what is TLS.I know that most of the people having this idea, but let me explain you a little bit about TLS.TLS equals to Transport Layer Security.It protect your API.When client connects to the API gateway, TLS ensures data is encrypted.Attackers cannot read it.Man in the middle attacks are prevented.Client are talking to real API gateway.Think of TLS is like secure tunnel between the client and your API.Let me give you simple example.If you have one website http and another website is https, which website you are going to open httpsnow when you are using any https website.Actually, TLS is a protocol which is working in the background and it is making your website communicationsecure.Here when we are having rest API, we have endpoint.Now when user is going to access our endpoint we want to make it https.It means we need to use TLS right.So TLS is actually allowing secure communication between client and API.Now, what is the use case of this security policy?When you create Rest API, AWS asks which TLS security policy do you want to use it?So basically here you are actually choosing TLS security policy.This policy controls minimum TLS version allows cipher suite, allows whether Fips mode is required,and this applies to the default invoke URL.Now we will talk about this option number four.Let's talk about this right.Look at here.Here you can choose TLS version TLS 1.2 TLS 1.0.All these options are available over here.And there are some options like Fips.Then we have like Pkew Pkew.So I just want to provide you a little bit overview about this.Now for examination you just need to remember use case write API.Endpoint security policy allows you to select all these things.That's all.Now, if you are not going to talk about like what is a use case of TLS version 1.1 and 1.3, all theseare not actually required for examination.I am just explaining you this for your detail.In short, you can remember like this.If TLS version 1.3 is possible, go for this one because it is better than the previous version.Now let me give you overview of this TLS version 1.1.It is introduced in 2008.Older encryption method.Then we have 1.2 introduced in 2008.Strong encryption.Modern browsers.Modern app supports this.And then we have the new 11.3, which is a faster and introduced in 2018, so released in 2018, moresecure than TLS 1.2, faster handshake, better performance and becoming standard in modern apps.So in most of the cases, if your application or client supports it.Choose 1.3.Right.Okay.Now here we are getting some other words as well, like FS and PK.So let's talk about these as well.1.3 with tips.What is the meaning of this.Look at this.Here we have this option.Okay 1.2 FS then we have let me choose regional.So we'll get more options.1.3 normal.Then we have 1.3 tips.So what.What are the differences right.Fips federal information processing standard use is government approved Cyprus route required in USgovernment Department of Defense, banking and health sector.So specifically it is a specific use case and required by US government.So if your application require this kind of compliance you can go with this.Then we have FS.It is perfect forward secrecy BFS means key changes for every new connection.So even if someone steals key later, it only affects that one connection.All data stays safe.And then we have Pkew.Pkew stands for stands for Post-quantum cryptography.In the future we are going to use quantum computer.Right.So quantum computer may break traditional encryption PK encryption to quantum safe encryption cannotbe broken even by the future quantum computer.So this is like a specific differences between all of them.Now let's talk about this last line.This applies to the default invoke URL.So here I have not.It is actually explaining you this process.So the security policy you choose here applies only to the default invoke URL.Look right now I am creating rest API.Let's say that if I am going to choose this option now, this policy will be applicable to default invokeURL, right?We have this http api already in my system.We created this http API so it is there.Now this is my old http api.I'm actually explaining you what is this invoke URL right?So here we have this API.Now when you go to this API here you will get this invoke URL.This is HTTP.This is default invoke URL provided by AWS.We use this from the postman.We trigger this right now whenever you are going to choose the security policy while creating rest API.It is applicable to this AWS default invoke URL right AWS default invoke URL.Now if you later decide to use custom domain, you can also choose separate security policy for yourcustom domain.Now look in normal Testing phase, we can use this invoke URL.We don't need to worry about this, but in the production environment we want Bryan.We want that this invoke URL must be from my domain.I want like api.cloud.com.Right.So in this case they're also allowing us to go for custom domain names.In this case you will have two one AWS default invoke URL and custom domain names as well.Now when you will create custom domain for your API, you can choose security policy there as well.So you will have like a different policy for default AWS invoke URL and for your custom URL.There will be different security policy.Right now.Custom domain gives you additional security features as well.So default invoke URL is not giving these features.but custom URL is giving this extra features.We have different security policy as well as we have different like added security features as well.Like you can set up your own SSL, TLS certificate, Mtls mutual TLS authentication and hostname validation.Now we will discuss about this in upcoming video when we will set up our custom domain.So this is all about endpoint security policy.Now look at over here when you choose specific security policy let's say that if I am going to chooseover here uh TLS 1.3 and all, here it is asking me for another two options endpoint access mode.Now I can choose one basic or strict.Now what are the differences between basic and strict?When should I use basic?When should I use strict?We will discuss about Endpoint Access mode into the next video.Thank you very much.See you in the next video.Have a nice day.Goodbye.