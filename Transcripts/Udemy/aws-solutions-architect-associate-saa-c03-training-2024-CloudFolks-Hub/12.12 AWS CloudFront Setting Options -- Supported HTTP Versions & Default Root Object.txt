# 12.12 AWS CloudFront Setting Options -- Supported HTTP Versions & Default Root Object

Hello and welcome.If you want to learn AWS to just pass exam.These videos are not for you.If you are looking for shortcut then these videos are not for you.If you really want to become expert, if you want to pass exam as well as pass interview, then youare on the right place.I am just giving you one simple example why I am telling you.Let's say that we are learning AWS CloudFront right now.Now when we are creating CloudFront distribution, we are getting all these options throughout the seriesof CloudFront.I explain you all these options carefully.I explain you what is origin, what is default cache behavior, and what are the use cases of each andevery options that we have here.Same way I am going to explain you setting options right now.So let's start in 2024.You should need to learn Docker.But why?To get the answer.Join our free upcoming webinar link is in the description.Book your seat now.Thank you.After completing function associations, I am directly jumping to the settings.I am skipping this particular option of Web Application Firewall.Why?Because Web Application Firewall is a different service in AWS.And when we will discuss about security services of AWS, definitely I will explain you web applicationfirewall as well.Right now I don't want to give you a simple overview and and just going to close this topic.No we will understand firewall then web application firewall differences between all of them and howwe can use this with the CloudFront in upcoming days.So right now I'm just going to skip this.Then we have settings tab.Now in the settings tab I already explained to you about price, class name, custom SSL.If you don't know anything about it, no need to worry about this.You can start with this playlist.Start from the video number one to this particular video.You will understand all these options.Now here in this video we are going to talk about two points supported HTTP versions and default routeobject.So let's start with the mind map.The first option we have is supported HTTP versions.When configuring CloudFront distribution, you have option to specify which version of HTTP protocolyou want to support.If you are not going to select anything from here, it will support HTTP version 1.0 and HTTP 1.1 betaBetter version.If you are going for the higher version, definitely it will provide you better quality.So if you are going to select these two, it would be always better.Now let me explain you the differences.HTTP version 1.0 and 1.1 are supported by default.The options typically includes HTTP two and three, both of which offer significant performance andsecurity benefits over older HTTP 1.1 protocols.So selecting them will be the good deal.CloudFront will automatically use the best version supported by the viewer's browser or client.Now this is the most important part.Let's say that if I am going to select http two here, it is not like it will forcefully, um, provideHTTP two to the users.No, we have now four options compatibility available.If I am going to select these two HTTP version 1.1, 1.1, one, HTTP two, and HTTP three.Now, whenever a user will open a website, the CloudFront will verify that okay, which version ofHTTP supported by user's browser and then according to this, CloudFront will serve them.So if your browser is modern browser and if HTTP two is supported, CloudFront will provide you HTTPtwo version.If it is not there, no need to worry, you will get HTTP version 1.1.So it is always a good deal to provide better version to the users.So in this case, if you are going to select both of these, no need to worry about this, right?Because it is totally dependent on the compatibility of user's browser.HTTP two.HTTP two is an upgrade over HTTP 1.1, and it is widely supported by modern browsers.It improves the speed and efficiency by enabling multiplexing and header compression.Then we have HTTP three as well.Takes this improvement further by using quick, quick and deep, offering even lower latency and improvedperformance, especially on an unstable network.So it is actually performing way better than HTTP 1.1.If you are having a, uh, unstable network, so selecting this would be the better choice.Now it is up to you which option you should select, but yes, always go with HTTP two because HTTPtwo is now supported by most of the modern browsers.Then we have one another option known as default route object.Sometimes I am only going theoretical, but sometimes practical would be the better choice.So what I am doing right now I am going to explain you.Default route options practically once.You will understand this practically, then there is no need to go with the theory.But still we will look at this particular mind map point.Now let's understand default route object Practically.See here I have one distribution.In this particular distribution I am using S3 bucket as a origin, not S3 static website.I have S3 bucket as origin, not S3 static website.Okay, so look at this.Here I have one bucket okay.Which is actually origin of my.This particular distribution.Now I created distribution.Everything is good.But if I'm going to open URL let me open through the incognito.See I'm getting access denied error.Now what is the problem here.The problem is CloudFront is is is correctly configured.But the problem with the CloudFront is CloudFront is not able to find out root object.Root object means CloudFront.Don't know which object to open first when user is going to open website using CloudFront URL.Now I'm going to give you one example.Look at this.Here I have this bucket.And in this bucket actually index.html is my home file or root object.I want everybody to open index.html right.But it is not specified in the CloudFront.So CloudFront cannot able to open website.But if I am going to specify this object detail it will open this.So let's go to this URL.Now if I am going to specify this using this URL, I want to open index dot HTML okay.And look at this I am able to open this.So the problem is right now I am not able to open this web page using CloudFront URL because CloudFrontdon't know the root object.If you go to the setting of your CloudFront distribution, you will find out that the default routeobject is blank.And if it is blank means it cannot identify the the route object file.Here we have three files.So CloudFront do not know which file to open.So what I am doing here I am going to say that okay whenever the user is going to open this CloudFrontURL, my route object is index dot HTML.I want to open this.So let's click on Save Changes.Now when you click on save changes CloudFront will take some time.If you go to the distribution you will get this status it is deploying.So meanwhile let's understand the default route object from the mind map allow you to specify defaultfile, usually HTML file or a home file that CloudFront will serve when user access the route URL.Here it is.Example.Let's say that if you are not going to specify index dot HTML as a route object, you have to open websitelike w w w dot example dot index example.com.Slash index dot HTML.So it can identify this file.But if you are going to specify index dot HTML into the root file, you can open directly using theexample.com.Because now CloudFront is aware about which file to open first as a root object.Now what I am doing.Let's go to the CloudFront.Let's copy this CloudFront URL and let's go to the this incognito tab.And here I am going to write only CloudFront URL.I'm not going to specify index dot HTML because now CloudFront is already aware about the root object.And now I am able to open website by only specifying the URL.So sometimes it is really very important to specify default root object.I hope you got complete understanding about these two.Next two we will discuss about this in the next video.Thank you very much.See you in the next video.Have a nice day.Goodbye.