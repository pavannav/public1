# 17.15 ECS Project Step 2  - Build Docker Image for ECS - Part 1

Hello and welcome to Cloud Folks Hub.My name is Parvesh Haidara.To understand all the ECS functionality practically, we are actually implementing one project and theproject name is Deploy Scalable PHP Web Application using Amazon ECS.As a part of this particular project you already completed.Step number one create ECS cluster.If you look at over here, I am inside this ECS console and I have this particular cluster ready.Now we are going to perform step number two create Docker image from the code.So let's start.Before we are going to create image let's understand the use case goal.What are we doing in this step.Launch new Docker enabled EC2 instance.Now, you may have one question here like why we need to create EC2 instance?I'm going to explain you this.Don't worry.Clone the php s3 app from the cloud public GitHub repo.Install composer.What is composer?I will also tell you about this and build the docker image using docker file.See right now if you look at this I have.This application is already running.Now if you want to run this particular application inside your ECS cluster, you need to create task.Now task is a kind of container right?And container cannot run without image containers.Main requirement is to have image.So by using code of this particular application we need to create Docker image okay.From this image we can create task right.Now.To create this image we are actually doing all these things.Now, you may have one question here that why we are going to create separate EC2 instance, right?Why?Because ECS cannot build images.It is only runs them.Once you have your image you can run this image as a task or service.But you must have image is ECS don't have this facility to build image from the code.Okay, so we need to build image on a separate EC2 machine.Once image is built and tested, we will push it to the ECR for ECS to use.Now this is next step, right?Once the image will be ready then we will talk about this.But right now as I explain you, I have this particular code.I have to create image from this particular code.And then from this image I can run my application and the matter is, I cannot run or I cannot createthis image directly on X.So I need to create one EC2 instance.Or specifically we can say that one Docker host.Right?So now we are going to understand how we can create this image step by step.Now the first is you need to create one EC2 instance.This is very normal.I'm actually creating Amazon Linux 2023.And I'm just going to allow two ports port number 22 and 8081 8080.Then I will explain you later as well because we want to test application locally.Once we will test this then we will send it to the ECS.So I'm going to create EC two instance.Right now I am inside my EC2 dashboard.This is a docker uh one of my own instance for my other use.So I'm going to create new one launch instance.Now here when we are going to launch instance we will take this instance with the name build machineOkay.So I'm just going to give this name.I recommend you to give same name.Okay.Build machine I'm going to use Amazon Linux T2 micro.Here it is.Key pair.If you don't have you can create new one.We don't need to touch any networking setting.Yes we are going to the edit.And here I have port number 22 allowed.And I'm going to add port number 8080.Right.Then I want to allow this from any source.So 0.0.0.Very easy.No need to run anything else.Let's click on Launch Instance.Now it is going to launch my instance.And once my instance will be ready I have to access this instance using SSH.So what I'm doing let's make it ready ssh.This I, I have this key.So I'm going to use this EC2 dash user which is a default username.Now let's.go to the EC2 dashboard and let's copy public IP.Yes, this is my build machine.This is public IP.I'm just going to copy this and paste it over here.And I got this access of my EC2 instance right.So once your EC2 instance is ready and you got access we need to install Docker.Right.Because we have to create images using Docker command.So Docker must be there in your system.Now these are the commands that you can use directly to run Docker inside your uh build machine.So here I'm just going to copy and paste all this command.Now this last command is actually allows EC2 user to run Docker command.Right now it is going to take 1 or 2 minutes and it is going to install Docker inside my system.Now once Docker will be ready we can verify this by giving command docker dash dash Disney's version.So it is working.Let's go to the.Mind map.And here let's understand the next step right.Because it is installing Docker and it is going to take some time.So after installing Docker our next step would be install PHP and composer.As you know that we are actually installing PHP based application.So we need to run all these commands.I am going to explain you all these commands step by step.Let's talk about the first line.This is the first line we have in this particular first line we are installing PHP, unzip and git.So we are installing three softwares using this particular command.This y is actually indicating that do not ask about this install directly.So we are installing PHP, unzip and git using this command.Right?PHP is required because we are going to like our application is based on PHP.Unzip is to I decompress the file and git because in the next step, we need to clone a cloud fox publicrepository.Because I want to give you code using this code you can build image, right?Then the second command we have right is actually going to download composer.Now what is this composer.Let me explain.You see download the official Composer installer script right now you may have one question over here.That why composer or what is composer right.See, as a part of our project, we are building PHP based web application where image image is uploadedby user will be stored in Amazon S3 bucket.See this is application which is running right.I have sample application running.Now I can choose file and I can directly go to this.And I can say that upload now and it is going to upload this file into the S3.It means what is actually happening over here.My PHP web application is interacting with S3, right?So to interact with S3 from PHP, we are using official AWS SDK means here you have to use AWS SDK becauseyour application is going to store data into S3.Means your application is communicating with S3 right now.Whenever you are going to communicate with any AWS services, you have to use AWS SDK and AWS SDK forPHP is already available.The SDK is not built into PHP, it is a third party library, right?AWS developed this third party library that we need to use over here because we are communicating withS3 now.So to use it in our project, we must install it using composer, which is the standard dependency managerfor PHP.It is a dependency manager and using this manager we are actually going to install third party library.In our case it is AWS SDK for PHP.Right now let's install this.But let's complete all this process as well.Then we have this command PHP composer setup here.We just downloaded this.Now we are going to execute the installer to generate composer file.And then we are going to put this into the bin folder so we can execute it from anywhere okay.So let's go to the system here I think my docker is already installed.Docker dash dash version.Yes Docker is installed.Now let's do this.So here we are just setting up the composer right.We will use this afterwards.Okay one another thing that I want to tell you see uh, if you are going to set up this kind of projectso your programmer will help you, your programmer will guide you that we are using all these librariesand we need to use these libraries.And for this we need to use composer and.All right.So whatever the details that you are going to get it over here, it is actually possible with the communicationbetween two parties.There is a person who is setting up this thing, and there is a programmer who will guide you becauseyou are responsible to set up ECS cluster and you are responsible to create image for this.For example, right now you are not developer.So in this case you don't know that.Are you using PHP library or not?So we don't need to worry about this.The programmer will guide you and even in many cases programmer will provide you this image.Right.So why we are doing this?Because I want you to understand the entire process.That's why we are creating this image and learning all this process.Right.So don't worry about this.If you don't know about composer or AWS SDK, the programmer will provide you all these details.Right?Okay.So now everything is done.We install git, we install php.We also added composer.Now I am going to provide you code.Okay.So what you have to do over here.This is the repo cloud folks.Public repo.You have to copy.Code from this repo.We already installed git so you have to run this command.Now once you run this command you are going to get one folder which is php s3 app sorry directory.Now you can go inside this.And here you have entire code of your application.If you look at this I also provided you docker file.If you want to open this you can.Okay.This is a docker file.There is a index dot PHP code and everything.Right.There is a code to upload the files into the S3.Okay.This is Composer Files which is used for the libraries.Right.So it is going to provide dependency library and all this.So now you got this particular uh app as well.Now let's talk about install dependencies.Right.We need to install these dependencies.Why.Because once we install these dependencies we need to use this with the Docker image.Right.So we are going to run this particular first command sudo dnf install PHP simple XML.Simple XML is a built in PHP extension, but not always pre-installed.It allows PHP to read the files for the XML data easily.AWS SDK uses XML responses under the hood, especially when communicating with the certain services.So in the whole communication they are actually generating some XML files.And we need to read this file.So we are actually adding this particular library okay.You know this is for safety as well.If you forget to install this, it can create a problem.So that's why we are installing this now.It is done now it is a final one time step a composer install install the PHP libraries listed in yourproject.Okay, if you look at over here in this project, we have this composer, JSON and composer log filein which programmer provided all the details will be used by the composer install and it is going todownload required libraries.Okay, so it is going to download this required library.But the requirement and all this are actually provided by programmer to us.We are just going to use this okay.We just need to make sure that when we are installing this we must have this composer JSON and composerlog file.Okay.And the programmers are like a very smart people.They will provide you all this.So now it is going to install this.Now once it is done.So everything is good.And finally we are now able to create this image.So using this command Docker build it is going to create this particular image Docker Cloud for thisapp.And here it is a dot that I don't forget to do this because here it is going to find out docker fileinto the current directory.Right.So this is a docker file we have in the current directory.So dot is very important.Let me copy this and let me paste this over here.Then now it is going to create image okay.It is going to take 2 to 3 minutes to create our image.Now once image will be ready okay.We want to test this right now from this particular computer.So I will tell you how we can run this image, how we can test this particular application.And we are going to talk about something very important as well, which is very useful when we are goingto create X task, right?What is a use case of roles?What is a use case of variables that we need to understand as well?So testing is the most important part of the entire project.Now if you look at this here, uh there is no error.So my Docker image is ready.If I'm going to give command Docker images.Look at this.I'm getting this cloud folks app.I also provided you same step over here as well.You can give this command Docker images.And here we are.Getting this app means everything is good.Now let's run this particular application and test it locally.Let's understand the use case of ECS roles or IAM roles and how we can use this variable.Right.So step number 2.2 is the most important part of the entire project.But for this you have to watch next video.Thank you very much.See you in the next video.Have a nice day.Goodbye.