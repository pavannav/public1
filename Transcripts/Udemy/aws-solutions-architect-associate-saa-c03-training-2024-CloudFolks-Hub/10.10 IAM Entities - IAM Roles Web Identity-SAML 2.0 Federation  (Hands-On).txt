# 10.10 IAM Entities - IAM Roles Web Identity-SAML 2.0 Federation  (Hands-On)

Hi.If you want to become cloud computing expert, you have three options first AWS second Azure.Third Google Cloud Platform.Inside AWS we have many services.And one of the service we have is IAM Identity and Access Management.Inside Identity and Access Management we have three entities one user second group and third one isrole inside roles.We have five different use cases.And today in this video I am going to explain you role use case number three and four.So let's start this.Welcome back.So now let's talk about IAM role use case number three and four.First of all I want you to understand from where we are getting this option, then I am going to explainyou this particular option.Number three and four.If you go to your AWS console inside IAM dashboard we have this particular option of role.Now when you go to the roles here you can create roles.And here we have this five options.We already discuss about why we are creating this particular role for AWS services.Another is for AWS account.It can be internal and external.Today I am going to explain you about these two role use case together Web Identity and Saml 2.0 FederationAuthentication.This is very important for you to understand the concept of these two because you may get question relatedto this.So here they have same use case but there are two different identity system.So we are learning them together.And we need to understand the differences between them as well.So start with the mind map.So we are talking about web identity and Saml 2.0 Federation purpose aws IAM for Web Identity slashSaml 2.0 Federation is a security feature that enables secure access to your AWS, AWS resources usingtemporary credentials obtained through the IDPs.Okay.Now what is this identity provider here I am going to give you a very simple exam and you will get completeidea about this.If you look at this is my GitLab sign in page.Now here I can create my own user account.And if you look at this okay, here I have option to log in through Google login through GitHub, Hub.Log in through Bitbucket.Okay.Now these options we have available into the login screen of GitLab.Now this Google GitLab GitHub or login authentication.Bitbucket Salesforce.These are actually we call it IDP.Okay.So let's understand this.We have IDPs.I hope you got the idea.Okay.But let's understand this through the mind map.IDPs offer authentication service allowing users to prove their identity online using a single set ofcredentials.Let's say that I do not need to create GitLab account.I can login through my Google account as well.So I have my Google account created years ago that I can use over here in order to login to my GitLabas well.IDPs that use.OAuth 2.0 and OpenID connect or Saml 2.0 Federation to provide secure and standard way for web applicationsto rely on IDP for user authentication and authorization.Now, if we talk about IDPs, there are some IDPs.They are using OAuth 2.0 and OpenID.There are some IDPs.They are using Saml 2.0.Okay.So let's say that for Facebook and and the user authentication system that you are getting from here,most of them are actually using OAuth 2.0 and OpenID okay.If you have Active Directory connectivity, if you have corporate environment and for your corporateenvironment, if you are using Active Directory Federation service, then we are using Saml 2.0.Okay.So let's understand that these two as well OAuth and OpenID connect OAuth 2.0.This protocol handles authorization aspects, allowing applications to request access token from theIDPs to access user resources on the user's behalf.So, first of all, what are the resources that you are going to access is actually a authorizationor will be done through the OAuth 2.0.And there is a OpenID connect, which is which is a layer on top of OAuth for the authentication.So these two protocols are working together.OpenID connect is actually providing a authentication service.And OAuth is for the authorization.Authentication means prove who you are and authorization means after authentication which resourcesyou are able to access.It provides a way for IDPs to issue ID token tokens containing use user claim to the application.So once you let's say that if I am going to click over here, and if I am going to say that login throughGoogle, okay, it will redirect my request to the Google Home page.Then I have to provide my username and password.If my username and password is correct, Google will provide me one token and using this token I canable to login to this GitLab.Now this token process is actually transparent.We are not able to find out this token.We just need to authenticate and then the internal process uh will verify this token typically typicallyissue tokens for example JWT after successful authentication which can be used to access other web services.So once you log in through your Google account, okay, the whole communication will pass through theJWT token.When you are using a OAuth and OpenID, it is going to use JWT token.Okay.It is known as JSON Web Token.Okay.You need to remember this because you may get question related to uh related to this in exam.Okay.Example of IDP is using OAuth 2.0 and Oidc Google Facebook login via Amazon.All these are actually examples of OAuth and OpenID connect.And if you want to set up this kind of web authentication form, you can do this and you can selectWeb Identity over here.Okay.Keep this in mind.This is for OpenID connect and OAuth and OpenID connect.And OAuth is using JSON Web Token JWT.Okay.Same day.There is another option as well known as Saml Security Assertion Markup Language 2.0.This is a Saml 2.0 Federation.Okay, again use case is same, but here instead of OAuth and OpenID connect, we are using Saml authenticationprovider.Saml 2.0 is a version of Saml standard for exchanging authentication and authorization data betweenidentity provider and service provider.It is the same okay.It is particularly useful for enterprise scenarios for enabling SSO, allowing user to authenticateonce and gain access to multiple applications without re-entering credential.Now this is the most useful part.Okay, let's say that if a big organization like a huge organization, they have 10,000, 20,000, maybeone, one lakh users.okay.In this case they are always building for the single username single password.So if you are working with a company, you have only one username and password and you are able to accessall the services.Okay.But this is for the corporate user environment only.So in this case, let's say that if you are going to use cloud and if you want to access any resources.So in this case you do not need to create IAM user okay.They have their Active directory for the centralized user authentication.And you can uh, access all your resources using your Active Directory username and password.Okay.So this is like a main use case.The difference between these two is OAuth and OpenID connect specifically for Google Facebook kind ofpublic services.And Saml is for uh corporate companies.Saml does not use JWT token relying on XML for the Ascension.So this is like the keywords that you need to remember.And example of IDP using Saml is Active Directory Federation service.Okay.So this is the whole scenario.Now you may have this question that we got the idea, but how we are going to use this and we want tounderstand the full functionality okay.So look at this here I have one picture.And this picture is actually saying everything okay.So here these are the steps okay.First of all user login.Now user can be logged in through the continue with Google like a social media login.And or company like corporate user can log in with their own corporate user account.Okay.So user attempt to access web application then integrate with the IDPs.You will get forms like this.Now redirect to IDPs.Let's say that if you are going to log in through the social media, okay, it will redirect to theGoogle.If you are using corporate user account, it is redirect to the Active Directory.Okay.Then we have two different roles over here.So credential verification.The user enters their credentials username and password for IDPs login.The IDP verify verifies these credentials against its own user database.Then there will be token issue.So if successful the IDPs issue authentication token it can be JWT for social media and for Active Directory.For Saml it is XML containing user information.Then it is going to return to the application.And now if you look at this and then it is going to grant access.Now how you are going to get access now this is the most important part if you authenticate throughthe social media, okay, then you will assume this particular role.So here you have this role created from web identity.And according to this role you are going to get read only access to this bucket.So if you logged in through the Google account, you are actually assuming this particular role.And according to the policy which is attached with this role, you are going to get read only accessof this particular bucket.If you are corporate, corporate, user authenticated through the Saml, okay, you are going to getor you are going to assume this particular, uh, role.And according to this role, you are going to get full access of a S3 bucket.Okay.So this is how things are actually working.It is a proper example according to the exam.Now what are the benefits?Benefits are simple.Simplicity.Users enjoy single login experience using their existing Facebook or any account security.The app securely access the AWS resources without storing AWS credentials on the device.Let's say that social media users can access your S3 bucket.Now you know we have thousands of social media users, so you do not need to create the IAM accountover there.And you have very easy system to provide access to the users.And after authentication.And it is also scalable because if you are going to create millions of users, it will be a very hardto manage them as well.So we are not going to create millions of IAM users.We are just enabling social media user authentication using a service called Web Identity.Okay.And if you have corporate requirement we are going to use Saml 2.0 Federation.Right.So this is all about use case number three and four of role.Now you may be wondering that you know we want to go for the practical.No we are not going to provide any practical for this because it is not required.If I am going to provide you, it is going to waste your 2 or 3 hours.But yes, I will fulfill your requirement for sure.When.When we will learn AWS Cognito.So yes, there is a practical.I will provide you a whole process that how you can log in through the Google or how you can log inthrough your corporate environment, but not right now when we will learn AWS Cognito in upcoming days.So right now that's all about use case number three and four.In the next video we are going to talk about last use case of IAM role.Thank you very much.See you in the next video.Have a nice day.Goodbye.