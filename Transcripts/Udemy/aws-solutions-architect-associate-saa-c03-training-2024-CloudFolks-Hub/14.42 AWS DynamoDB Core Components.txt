# 14.42 AWS DynamoDB Core Components

Hello and welcome.If you want to understand how DynamoDB table works, you need to understand all the core componentsof DynamoDB.Here in this video, I am going to talk about all the core components of DynamoDB.We are going to learn these components theoretically and practically as well.So let's start.Now let's start with introduction.In DynamoDB, data is organized into tables where each table stores item.It's similar to rows in the traditional database.In traditional database like MySQL we are creating a database.First.Inside database we are creating tables and in the tables we are actually adding rows.Same way, when you want to use DynamoDB, you have to create a table directly from Amazon DynamoDBconsole.Now, each table having attributes, the tables having primary key, and in the primary key we havepartition key and sort key.Now how we are going to use all this together.I am going to explain you each component step by step.So let's start with the table.The first step to work with Amazon DynamoDB is to create table.The top level container that holds your data.It is similar to table in relational database but designed for high performance and scalability.I already gave you example in previous video that how we are using DynamoDB, and here speed is themost important part in DynamoDB.The data is presented in table like format with rows and columns, but actual actually stored as a keyvalue pair.You know, DynamoDB is NoSQL database and it is storing data into the key value pair.But yes, it is going to represent you this data into the table format.And here it is example that how table looks like right now I have one table already created for you.So if I want to go to this table I'm going to the tables.And here I have this one order table.And if I want to explore the content I can see this from here as well.Look at this.This is the same table I have in the picture as well.Right?I'm going to tell you how we can create table.But I am just giving you idea about this.Now in the table we have items.So an item represents a single record in the DynamoDB DB table.It is similar to row in the relational database.The table's primary key is used to uniquely identify each item.How that I will explain.You don't worry about this example table.Here we have this table.And look at this.Z001.C002Z003.All these are items inside the table.And right now I have this particular table as well.And I can add item according to my requirement.I am going to tell you how we can add item.But before this we have to understand attributes.Attributes are individual piece of data within an item.They are similar to columns in relational database.Attributes can store various data types of data, including string, numbers, lists, maps, and more.DynamoDB does not enforce fixed schema for the attributes.You know, when we are using MySQL database, we have to create schema first and it is fixed and wehave to add items means records accordingly according to the schema.But here when you will add item you are defining schema.So you can define attributes dynamically when adding record, which means item in the same table canhave different attributes.Now look at the records that I have over here.It will give you better understanding.So here I have customer ID, I have order amount.I have order date order ID but for this particular record of C is 004I have order item as well.So it is having different um attributes.And this is mobile number.So whenever you are going to add any record you can define attributes for this particular record.And then you can add record.Let me give you one practical demonstration of this.I have this particular table I want to add item.So I'm going to create item.Now here customer ID is considered as a primary key or partition key.Let's consider it as a partition key right now.And this must be unique.So let's say that if I want to add any record okay I have to maintain uniqueness of this particularitem.Of customer ID okay.So if I am going to add C001C002 or C003, it will not allow me because it is already there in the table.So that's why I am going to add C005 okay.And here now I can define this attributes.These are the attributes type.Let's say that I want to add order ID.Now this can be duplicate.No need to worry about this.I'm going to add C006.But this time I want to add something new.Right.Let's say that.Customer.Review.Right.And let's say that five star.So here I am able to add the whole new attribute for this particular record.If I'm going to click on Create Record see 005.But here we have customer review.So we have flexibility to define attributes according to our requirement okay.And this is what I have over here as well.So these are attributes.These are items.And when you are going to add items you are going to add it.You are going to define attribute first.And then you are adding items right.So I hope the concepts of all this are now clear.Now let's understand DynamoDB keys.The first is we call it primary key.When you create a table in addition to the table name, you must specify primary key of the table.Now this table is already created.So what I am doing I am going to open a new tab and here I am going to create new table.Let's say that order id new.Now here I have partition key and sort key right.Understand this.There are three main words primary key, partition key and sort key.Okay.When you create table in addition to the table name you must specify primary key of the table.Here I am not saying any word like primary key okay.No need to worry about it.Just understand the primary key uniquely identifies each item in the table so that no item can havesame key.DynamoDB supports two different kinds of kinds of primary key.One partition key and another one is partition key with short key.So here if I am defining partition key means partition key will be my primary key.Okay.You can add any.Let's say that customer ID the value must be string.So this attribute will work as a primary key.And I am only using partition key as a primary key.Okay.So.Let's understand these two.First of all partition key we also called simple primary key.Partition key.Simple primary key.A simple primary key composed of one attributes.Known as partition key.So you are just only using partition key as a attribute.Okay.So partition key will be your primary key.DynamoDB uses partition key value as an input to an internal hash function.The output from the hash functions determines the partition physical storage internal to the DynamoDBin which item will be stored.See, basically when we are using DynamoDB, DynamoDB is using cluster storage.So in the background, DynamoDB having multiple storage.Now when you are going to store any records, it is it is going to identify storage of this particularrecord, because in the background we have multiple storage.So based on your value of the simple primary key or a partition key, it is going to count one has.And based on this hash it is going to decide background storage of your record in the table.Now, if you have multiple storage, it is always a good idea to use multiple storage to store databecause it is going to improve your performance.So the idea is, if you are going to enter unique values in any of your attributes, consider it asa primary key.Because if you will enter unique value, it is going to calculate unique has and then it is going tostore this data into the unique storage.So you can distribute your records into the multiple storage.If you use good primary or good partition key.Now how it is going to count has we don't know about it how it is going to select storage.We don't need to know about it, right?So we just need to understand that whenever we are going to define any partition keys, it is goingto count hash value of the data that we are going to store in these keys.And based on this it is going to use storage running in the background okay.The table that has only a partition key.No two items can have the same partition key value.And here it is an example.Let's say that in our existing table that I am already having, customer ID is a partition key.I am only using single partition key, so it is a primary key as well.And now it will not allow me to add duplicate value for the customer ID.Let's check it out.Let's say that this is my existing table that I have.These are the values that I have okay.So if I'm going to create item customer ID it is partition key.So if I am going to say C0001 this record is already there.Let me add new string.Uh, order item.And here I am going to say watch.Right.Create item.See I'm getting this error.The condition request failed.An item with the primary key you provided is already exist, right?Because I have only one partition key working as a primary key, and it cannot be duplicated, right?So this is normal operation when we are going to use a single partition key or we call it simple primarykey.Now here we have one issue.Let me explain you this.See these are my customers.These are their order ID right.Now you tell me what happens if this customer sees 001 ordering again from my website.You have to add this entry over here, right?Because one customer can order items multiple times.So now previously the customer got watch from my website.Now he wants to buy mobile or something else.So there is a chance that same customer record I have to add over here.But customer ID is a primary key here.It will not allow me.Right?So I am right now in a trouble.Okay.If I am going to add new record for the same customer.See 001.It is not allowing me and it is possible that customer coming again and again to my website and placingnew order.So if you have a simple primary key, there is a chance that you cannot add multiple record for a samecustomer ID.So how to how to solve this problem?In this case we have composite primary key.When we are using composite primary key, we are using partition key plus short key.Let's look at it as a composite primary key.This type of key is composed of two attributes.The first attribute is a partition key, and second attribute is a short key.In the table there is a partition key and sort key.It is possible for multiple items to have same partition key value.However, those items must have different sort key value.And this is example.Now look at this particularly useful of our example of multiple customer ID.Now here combination of both will be considered as a primary key.And this must be unique.Now every time when the customer is going to place order, definitely customer ID will be same, butthe order ID will be new because each customer is customer is placing new order.If you understand this, the combination of these two can never be the same.So here.Customer place order with the order ID 001.After two days when he is going to place order the same customer we have, but it is actually placingnew order so it will have different order ID.Now in this case I can add orders for multiple customer and the value will be unique as well.So in this case composite primary key would be the better.Let's create one table over here with the same.In this particular existing table I have only one partition key as a primary key, which is problematic.That's why it is not allowing me to create new record for the same customer.Another thing is, once you have your table ready, you cannot add short key here.Okay, so right now if I want to use short key I can because my table is already created.So you have to define short key when you are creating your new table.So here it is customer ID.Here I am going to add order ID.In the string.We will talk about all these options.But let me click on Create Table.Now this table will be ready and I want to add records over here right.Look at this partition key customer ID.Partition key customer ID.But here we have short key.So combination of both will be used over here.Sorry.Let's go to the table.Let's explore the table item.Let's create the item.Now here I am going to add this c001.Order ID are 100.Okay let me add record then let me click create.See 001 order ID 200.It will allow me now if I am going to mix it right.Let's make it same.C001 order ID 100.Now the combination will be not unique.The condition requires fell item with the primary key you provided is already exist.So this is not possible.But there is no chance that this value will become same because every time whenever the existing customeris going to place new order, it will have new order ID, so I can fulfill my requirement by creatingcomposite primary key.Right?So I hope you understand the basics of DynamoDB.This is the really important part because now we can go with the different use cases.Understand the different use cases of DynamoDB.If you have any question, if you have any query, I am always available for you.Thank you very much.See you in the next video.Have a nice day.Goodbye.