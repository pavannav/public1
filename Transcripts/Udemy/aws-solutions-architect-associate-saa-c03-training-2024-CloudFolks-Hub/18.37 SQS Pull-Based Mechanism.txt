# 18.37 SQS Pull-Based Mechanism

Hello and welcome to Cloud Focus Hub.My name is Dave Shahdara.In this video we are going to talk about SQS pull based mechanism.We already discuss about how consumer is going to pull message from the queue, but we need more clarificationabout it.And that's why I have this video.So let's start.I'm going to start with introduction.SQS works on pull based communication model.This means SQL does not push messages to the consumer.Consumer must request means pull messages from the SQS.Here I have diagram.It is little bit different than the previous one.Producer is going to push message into the queue.But keep this in mind.Queue is not going to push this message to the consumer, right?Consumer is going to pull this message from the queue.Right.This is what we call it pull based mechanism.Now let's understand what does pull based mean.The queue just stores messages.The queue never send messages on its own.Means it is not going to push this message to the consumer.Consumer.Keep asking do you have any message for me?So consumer is going to the queue and it is requesting.Do you have any message that I can process or proceed.So consumer is going to the queue and it is asking do you have any message that that I can process rightnow this is called poly.Poly means consumer is going to contact queue and then asking for the message to process.Right.now.We will talk about polling in upcoming days as well because there is a long polling.There is a short polling as well.But right now you just need to understand that in the pull based mechanism, what is the role of polling.Okay.Now important rules on pull based mechanism.You need to remember this okay.First multiple consumers do not pull same message.First of all you need to digest this.You may have multiple consumers look at this.In this particular diagram I have two sorry three consumers.It can be easy.Two instances it can be lambda.Let's say that I have high availability requirement and I have multiple EC2 instances into differentavailability zone.Or you may have multiple Lambda functions for the load balancing.So you may have multiple consumers.Okay.That's the first thing that you need to remember.Now, if you have multiple consumers, then multiple consumers can poll the same queues.But one message is delivered to one consumer.Two consumers cannot pull the same message at the same time.We have multiple consumers.Multiple consumers can poll to the same SQS queue, no problem.But one message will be processed by only one consumer.Let's say that consumer one is going to pull this message.Other consumers cannot pull this same message.Right.So this is very important terminology.Two consumers cannot pull same messages at the same time.Second consumers compared message are not shared.All consumers poll the queue the first available consumer gets the message, other customers move tothe next message.Let's say that the consumer one is going to get message one.Now when the consumer two will pull, it is going to get message two.Okay.Does not decides.The consumer asks does not assign messages.Scarce does not post messages.Consumer decides when to pull.Control always stays with the consumer.Okay.So consumer is going to pull the message from the queue.So we have all the control at the level of consumer right.So this is all about uh pull based mechanism.I think this will be very useful in upcoming, uh terminology.And when we will talk about SNS, the other AWS services as well.So please understand this scenario.Right.Thank you.Goodbye.