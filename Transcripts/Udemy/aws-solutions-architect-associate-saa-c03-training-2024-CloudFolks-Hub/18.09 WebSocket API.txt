# 18.9 WebSocket API

Hello and welcome to Cloud Fox Hub.My name is Bhavesh Katara.Right now I am inside API Gateway Console and I am getting API types.Here we have four types http api, WebSocket API, rest api, rest api private.Now we already discussed about http API and rest API, but WebSocket API is completely different thanhttp api and rest API.Now what is WebSocket API?Why?It is completely different than our normal http api, rest API and what are the use cases of WebSocketAPI?We are going to discuss about all of this in this video.So let's start.First of all I am going to start with introduction of WebSocket API.WebSocket API is used for a real time two way communication between client and server.These are actually keywords related to WebSocket API.First, it is real time and second it is two way communication.Now this line will provide you complete detail about why we can say that WebSocket is different thanHTTP and rest API.Now look at this rest API.Request response and connection is closed.If it is only talking, one time request is coming back, end is responding this and the connectionis closed.Same things are actually happening with http api.Rest API supports more features.HTTP supports high speed communication, so these are like a common differences between Rest API andhttp API.Otherwise use cases are almost same.But if I'm going to talk about WebSocket API here.Once connections stay open, message can go in any time in both directions, so it is not going to closeconnection after just one request response process.So this makes WebSocket perfect for a live communication.Now what are the examples of live communication?Here I have some example use cases.For example in the live chat we need WebSocket communication.Same way for live notification or live dashboard gaming collaboration tools like zoom, whiteboard,Google docs providing live updates, required WebSocket communication and for real time tracking likedelivery by location updates in an application like Swiggy and Zomato.So you can you can attach the word live over here with WebSocket API.So whenever your requirement is two way communication.Your requirement is live communication.You can use WebSocket API.Now.We are already aware about features of Rest API and http api.You want to find out features supported by WebSocket API.So here I am going to provide you list of features provided by WebSocket API.First of all, WebSocket API supports two way communication.These are keywords of examination.Then it supports real time data and long live connection.If you are getting these three keywords in any questions, most likely the answer is WebSocket API.Then you can integrate a WebSocket API with the Lambda Alb.Integration is not supported.JWT authentication means JWT token supported by WebSocket API.Cognito authentication is also supported by a WebSocket API.API keys only supported by Rest API, so not supported by WebSocket API.Web application firewall not supported.Caching not supported or even not needed because it is for real time data communication.So guessing is not going to work over here.So guessing is not needed and that's why it is not supported.Then we have mapping template basic and routing.Yes.Now what is routing that we will discuss when we will go for the practical.So don't worry about it.So that's all about all this API supported by API gateway.Now when we will go for the practical when we will set up our http API, you will get crystal clearunderstanding if you have time.I request you to watch all the videos and complete API session today only.Thank you.See you in the next video.Have a nice day.Goodbye.