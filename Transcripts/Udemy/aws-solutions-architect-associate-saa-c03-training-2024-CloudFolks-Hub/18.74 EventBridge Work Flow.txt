# 18.74 EventBridge Work Flow

Hello and welcome to CloudForksHub.My name is Bhavesh Chaddara.If you want to understand how EventBridge works, you have to understand EventBridge workflow.And in this video, I'm going to provide you a high-level overview of EventBridge workflow. Let's start.Introduction To understand how Amazon EventBridge worksinternally, let's look at its core flow.It processes events using a simple sequence, eventSource, event, eventBus, rule, and target.Now what we are going to understand right now.We are going to understand all these steps with a little overview.Once we will complete this, we can understand the entire flow.Now after that, I will talk about each and every step in detail.So this video is actually providing you an overview of each step.Now the first step is eventSource.eventSource equal to eventGenerator.Example EC2 instance. Flow.When an EC2 instance stops, EC2 acts as the eventSource and generates an event.Now what is the event here? Instance stop. Right?So, event equal to change in state.Example EC2 state equal to stop.This event is created in JSON format and send it to the eventBus.So eventSource, whenever the eventSource will generate any event, it is going to create JSON document.Now this document or event needs to send to the eventBridge.How it can send it to the eventBridge?It is going to send this using eventBus.Now event can be anything and eventSource can be anything.For example, eventSource can be S3, eventSource can be EC2 instance, eventSource can be Lambda. Right?Right now, I am just giving you one example of EC2. Why?Because I just want consistency in the example. Okay?So it is not specifically for EC2 only. Now eventBus.EventBus equal to central hub, where events are received.EventBus is actually part of eventBridge, which is actually going to collect all the events.It can be EC2 stop, it can be EC2 start, it can be EC2 terminate. Right?Example, default bus, default eventBus.When we will talk about eventBus in detail, I will tell you about eventBus type.And one of the eventBus type is default.Don't worry about it right now.Right now you just need to remember that eventBus is used to collect all the events.Now flow, eventBus receives the event and forward it to the rules.Now what is rules?Let us say that I am getting two different events, EC2 instance stop, EC2 instance start.Now what I have to do when I am going to get EC2 instance stop event?Or what should I do when I am going to get EC2 start instance? Right?So rules is actually decision maker.So rules equal to filter plus decision maker.Example check, if the EC2 state equal to stop, then forward this event to the lambda.If EC2 instance event equal to start, forward it to the SNS.So it is actually decision maker.So flow, rule evaluates the event.If condition matches, send event to the target.Now what is target?Target equal to action executor.Example lambda is trigger, send alert.So target receives the event and performs the final action.Now let's connect all these steps so you will get better understanding about it.When an EC2 instance stop, the event source generates an event.This event is created in JSON format and send it to the eventBus.The eventBus forward the event to rules.The rules checks the condition and if it matches, send it to the target.And finally target performs required action. Right?So this is actually the workflow of eventBridge.Now is this the only workflow in the eventBridge? No.If you look at over here, you will get this option of eventBridge pipe as well.So in the eventBridge, we have one regular workflow and there is one another workflowas well known as eventBridge pipe. Ok.So that's why I have added one note.There is another way eventBridge works using eventBridge pipes, which provide direct sourceto target integration.Our focus will be on understanding the core workflow, eventSource, event, eventBus, rule and target.According to the syllabus, we are just going to concentrate on this particular workflow.There is another workflow, which is used to connect source and target directly.But in our syllabus, we are not going to concentrate it.So whatever we will discuss in upcoming video, it will be on the core workflow of eventSource,event, eventBus, rule and target.So that's like most important part.And now we want to talk about each and every steps in detail.Once we will understand this, we will create our own lab environment as well.Thank you very much.See you in the next video.Have a nice day. Goodbye.