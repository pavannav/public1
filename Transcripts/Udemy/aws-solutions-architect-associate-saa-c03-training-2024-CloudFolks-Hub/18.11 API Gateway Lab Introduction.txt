# 18.11 API Gateway Lab Introduction

Hello and welcome to cloud folks.My name is Bhavesh Shatara.We discuss a lot about API gateway.Now it's time to implement API gateway.We are going to set up our first API Gateway Lab.It's really very interesting and I am giving you promise that once you will finish this lab, you willget complete understanding about API gateway.So let's start.Here I am going to start with introduction of this lab.In this lab we will understand how API gateway works.In a real application, you will learn how a front end application can store data in a backend databaseusing API's created through the API gateway and Lambda.This lab gives you simple practical understanding of how API requests flow from client.Client means front end to API gateway.API gateway will forward this request to Lambda function, and then Lambda function will store datainto the DynamoDB, helping you to learn core functionality of API gateway in hands on way.Note in this lab we will use http api.In the next lab we will perform same operation using rest API, so you will get better understandingof http API and Rest API and what are the differences between them.So we will use same lab.Now let me give you some overview over here.Look if I am going to Amazon.com now if I want to purchase something, if I want to order something,I have to create my user account, right.Now, the first page of user account creation in Amazon.com, we call it like front end app.Now when you create your user account, Amazon needs to store this data your username, your password,everything.Now, Amazon or any e-commerce website.They are storing this data into the any database.In our case we are using DynamoDB table.So whenever user is going to create his or her account, or user is going to read his or her account,or when user is going to delete his or her account, front end is actually directly going to communicatewith backend.Right now, matter is front end cannot communicate directly with the backend like DynamoDB.So we need to set up API's.Here we will set up three API's based on the Crud request.I've already completed Crud create, read, update and delete.So here we are going to perform three Crud operation.Post going to create new record.Then get if user is going to read profile and then delete.If user is going to delete his or her account.Now we will set up these APIs over here and we will host this into the AWS Lambda.Now you can use any platform.Let's say that if you want to go with EC2 instance, if you want to go with X or X, you can host thisAPI logic into any of the platform.But here in our lab we will use this Lambda functions.Right.Now how you will expose this APIs.So front end can directly trigger them.We want to expose them securely.We want log of each and every trigger.So to expose them we will use API gateway right.So final flow will be like this.Our front end app will send HTTP request to our API gateway based on HTTP request.This API gateway will trigger specific lambda function, and then this lambda function will performthis particular operation with DynamoDB.So this will be our real flow.No need to worry about it.We will perform everything step by step and you will get better understanding in upcoming videos.Here for this lab we are going to use mainly three tools DynamoDB, AWS Lambda, and API gateway.Now to perform this entire lab we will use mainly four parts, right?So in the part one we are going to complete all the prerequisites.In part two we will set up API using Lambda.In part three we will create API gateway.And in part four we will test entire lab.So this small part will help you to understand entire process step by step.This is all about introduction video.Next video is part one.Prerequisites.Thank you.Goodbye.