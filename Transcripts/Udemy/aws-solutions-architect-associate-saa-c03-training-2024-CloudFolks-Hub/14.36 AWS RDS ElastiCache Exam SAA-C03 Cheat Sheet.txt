# 14.36 AWS RDS ElastiCache Exam SAA-C03 Cheat Sheet

Hello and welcome to become successful cloud professionals.You have to pass two stages.Stage one become expert in each and every cloud related services and second, pass a certification exam.Now stage one is right now going on.You guys are learning all the AWS services from our platform.What about stage two?Now stage two is little bit difficult because when you are going to give specifically AWS Solution Architectexam, they are asking scenario based questions and the words that they are using in the questions arereally twisted words.So we want to solve this problem for you as well.And that's why from this particular video we are going to start new experiment.For example, recently we have completed ElastiCache for RDS cluster series.This series will provide you all the details about ElastiCache, and we can consider it as a stage one.But what about stage two?Let's say that I have exam, I want to I want to understand all the questions related to ElastiCachein the exam.What should I do now?This video will help you over there.This is kind of summary.And also you can consider it.This video is a ElastiCache cheat sheet video for AWS Solution Architect exam.So at the end when you are giving exam okay you just need to watch this particular video.It will provide you all the terminologies details okay.Now if you like this particular experiment and if you want this kind of a cheat sheet videos for allAWS services, please make comment because your comment will be decision maker for this kind of series.Okay, so let's start.Now first of all, there is a common terminology update.Okay.This terminology update will really confuse you.So there is a word cluster and replication group right.Now here console update.Now what happened.See the term cluster is now used in elastic case management console for all Redis configuration.Now when you are going to set up Redis because memcached doesn't support cluster mode.So they are using the word cluster right now in Redis.If you are going to set up single node Redis setup or multi node setup with replication or sharding,they call it cluster.Now if you are going to set a same ElastiCache cluster from API or command line.Here they are using the word replication group.Right.So replication group refers the configuration with primary node and replica node.So specifically if you look at this okay.So what term cluster they are using it when you are going to use console and the term replication group.They are using it when you are actually going to create elastic environment using API or CLI specificallyin terms of reddis, a primary node and replica node combination or automatic failover is called replicationgroup, right?I am giving you this clarification.Why?Because right now let's say that if I am going to my AWS console and if I am going to create a elasticcache, okay, even if you choose anything, you will never get this option of replication group becausewe are doing everything from the console.So we have clear understanding about cluster.But we never heard this word replication group because we are not using API or Klee.Now in the exam they may use word cluster or they may use word replication group.But now there is no confusion because I clear this particular point.Okay.Now let's talk about the second point, which is multi-az in ElastiCache.As you know, that Multi-az is going to deploy your resources into the multiple availability zone.So you will get high availability.Now supported engines Redis fully supports Multi-az.Memcache does not support Multi-az.Look at this here.If I am going to create Redis here I am going to enable cluster mode.And if you look at this multi-az enable.Now if I am going to even disable cluster mode Multi-az enable.But if you look at the memcache option here and if you look at this multi-az disable.Okay.So keep this in mind.Memcache does not doesn't support multi-az and automatic failover because memcache is not supportingthis.So available in Redis Multi-az setup.Promote the replica to primary.If the primary node fails, which is only supported in Redis right now.This is one of the most important part that you need to understand because you know if someone is notteaching you like if you are learning from other platform and if they are not teaching you, don't blameon it because this is actually happening with with all the trainers, right?The problem is there are some terminology that they are using in the exam like that they are using duringthe question that they are asking.But we are unaware about this, right?So as a cloud professional, if you will, heard about this particular terminology and you don't knowabout it in the question, okay, you have like in dilemma in your mind that I don't know what is this.So how I can answer this, this is actually happening.So I thought like okay, let's add this strategy over here.So when you will read questions related to the ElastiCache you will feel comfortable and you are awareabout all this wording.And so I am adding this.But if if you are not getting this kind of wording on other platform, don't worry about it, okay.Because this is normally happening.So let's understand core caching strategies.First of all we are adding caching layer to improve the read cache and write cache.Now we are going with the read cache first.So here the first word is read.Through caching the cache automatically fetches data from the database on cache miss.It means the caching layer is responsible to fetch the data from cache miss.I am going to explain you this the whole process.Let me go with the second case.Aside caching lazy loading the application explicitly checks the cache first and update the cache onlyon miss.Now let's go back to the history.When I explain you caching on a very first chapter of this elastic Series.I gave you explanation using this picture.Here the user a finds the request.Now request is going to the application server.Application server sending query to the elastic cache.Now Elastic Cache don't have this.So it is saying that the cache missed.Now application is going to query does IDs provide a response.And then in application server going to the elastic cache to update the cache.Right.This is how I gave you explanation about it.Now look at this application server is going back to the Elastic cache and saying that hey populateyour cache right now.Because first time it was missed.And now subsequent caching request is going to the Elastic Cache.But look at this application is going to the Amazon Elastic Cache to update the cache, which is kindof example of lazy loading the application.Explicitly check the cache first and update the cache only on miss.Here you are not waiting for the for the application to cache your data.The case automatically fetches the data from the database on the cache miss.So it is a better performance.And now applications don't need to worry about this because your caching layer is actually, you know,caching the data.Another terminology.TTL expiration caching case data is removed after specific time.Inserting the state data is not saved, right?Then right cache strategy right through caching.Look at this.Read through.Right through.The cache is updated every time data is written in the database, right?Through caching is kind of simultaneous caching.Whenever a user is going to update the record, the caching is actually I'm sorry, data writing isactually happening simultaneously on a caching layer and on a database.Then we have right around caching, right?Go through the database.So it is going to write data into the database first, and cache is updated only when data is read later.Now when the data is read later, there is a two possibilities when read through caching means automatically,um, you know.Caching layer will cache this or lazy loading through.It is going to write the data into the caching that I already explained you.And the third one is right behind caching.Right?Are made to the cache first and update, then into the database at a later time in the background.So look at this right through simultaneously.Writing right around means data will.You are writing data into the database first, then into the cache.And right behind means caching layer is upper side.Data will write into the.You are writing the data into the caching first and then it will be in the database.Right?So please remember all this strategy.And now authentication and encryption.Redis already supports authentication token.Memcached authentication token not supported.ACL access is supported.It is one of the facility known as Redis ACL not supported.Encryption in transit.Supported.Encryption in transit.Supported.Redis and Memcached.And encryption at rest, which is one of the most important part, which is not supported in Redis.Encryption.At rest.Not supported.Sorry.Redis supports encryption at rest, which is the most important part because data will be stored insidethe storage as in encrypted format, which is supported in Redis memcache.Encryption is not supported.Keep this in mind.Memcached supports encryption in transit, but not encryption at rest.Okay, so this is the most important part.And Redis.Oh this is the word that they are asking in the question as well.So keep this in mind.Authentication token supported in Redis memcache.Not supporting any kind of authentication.I hope you got complete understanding.And now there is no confusion.I am waiting for your comment.If you like this idea, please make a comment.You will get this kind of cheat sheet videos for each and every AWS services.Thank you very much.See you in the next video.Have a nice day.Goodbye.