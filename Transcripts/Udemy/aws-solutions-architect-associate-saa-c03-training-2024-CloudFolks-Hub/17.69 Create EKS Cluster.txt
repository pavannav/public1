# 17.69 Create EKS Cluster

Hello and welcome to cloud folks.My name is Bhavesh Katara.In this video we are going to create our first ECS cluster.After creating cluster, I'm going to provide you some more information about it, which is really veryinteresting.So let's start.Here I am inside my console and if you want to create cluster you need to click on Create Cluster.Right now we are creating this cluster in auto mode I have to provide name.Let's say that my cluster.Then you can select your Kubernetes version.You need to create cluster IAM role.I already explained you that how we can create cluster IAM role.If you don't know, no need to worry.You need to click on create recommended role.Then it is going to open IAM console.It will create your role automatically.Here we are getting AWS service and it is going to attach this role with the EKS auto cluster.Next it will attach policies automatically.Required policy.So we just need to click on next.And then it is going to create this.Now I have this role already in place.So I'm just going to select this.Now same is applicable for node IAM role as well.Click on create recommended role.Now it is creating role for node.Here you will get this EKS auto node.Next it will attach policy directly.If you want to give permission to the specific service you can attach this policy.But right now we are just going with the default settings.Next here Amazon EKS auto role.I have this role already.So I'm just going to select this.Now you are actually providing detail about VPC.Right now we are not creating any VPC public subnet and private subnet.We are using default VPC.Here you are able to select VPC subnets.I'm just going to select AP South one A and AP South one B.Now when you click over here you will get list of the settings for your cluster.You will get X motor auto mode enabled.Yes.Now here you have all these options which is default auto mode.And some options are actually you can edit this.Some options are fixed.You cannot edit this.For example you cannot change VPC.If you want to change subnet you can right.So now I'm just going to click on create.Now when you click create it is going to create cluster automatically but it will take almost ten minutes.So for this video I have just created cluster already in advance.So it is going to save over time right?So this is my cluster.Now you have to wait.If you are creating cluster, you have to wait until you are going to get your cluster status active.So here I am getting detailed about my cluster.Now question is can I manage my entire cluster from here?No.There are two ways to manage your EKS cluster.What are these two ways?Let me explain you using my map introduction.Once you create EKS cluster, you must manage and control it properly.EKS gives you two main ways to manage AWS Management Console and another way is AWS cli plus kube control.Now question is okay what I can manage through the AWS console.So I am going to provide you this detail.First of all you will get detail about your cluster.Look at this.You are going to get cluster status cluster health Kubernetes version.So you will get all the detail about your cluster from the console.Second, access management who can access your cluster and all these things means IAM management canbe done through this console.You can go to the access.And here you have the detail about your IAM access entry.And you can add new entries as well.So access management can be possible using AWS console then add ons.If you want to add some add ons, you can add this by default with the default mode, auto mode we haveonly metric server.If you want to add more add ons, you can add this from the console.Then you can also manage node groups.If you go over here in the compute, you will get this built in node, group pool and node groups.Now here I just want to provide you some detail.We have created our cluster in auto mode.When you create your cluster in auto mode, you do not need to manage node groups.AWS will manage this automatically.It will add worker nodes automatically when it is required.And this is actually built in node pool.So it will manage worker nodes automatically.Node groups is only required if you are going through the classic mode.In the classic mode you have to manage node groups manually.And that you can manage from here.Now you may have this question like we are in the auto mode.So why we have this node groups visible.Because AWS is now giving us this flexibility that okay you have built in node pool in the auto mode.AWS will manage this worker nodes through this built in auto built in node pools.It is there.But if you want to add a node manually, you can also create node and add node manually, which is kindof hybrid environment.I recommended you because we are in the auto mode.Do not touch node group.We are going to use built in node group pool in which AWS is going to manage our worker node, right?Node group is actually required when we have.Specifically classic mode means we are managing our EKS cluster manually.Okay.Then you can also manage networking.Here we have this networking option.We have this VPC id we have the subnet okay.You can change the subnets.All this can be done through the console.And then we have some monitoring setting as well.So here we have this monitoring to the CloudWatch or third party tool.So all this can be managed.All this can be managed through the console.But what are the limitations of console.What you cannot manage from the console right now.This is the most important part.There are limitations in the console you cannot directly manage.Workload means if you want to host application inside your EKS cluster that you cannot manage throughthe console.Here you can view all this.Look.If you go to the resources here, you will find out the detail about poor templates.You will find out the detail about port or replica sets, but you can not create ports directly fromhere.Okay, now this is one of the main limitation of AWS console.See, you cannot deploy port service on ingress using container locks, scaling deployments, managingconfigmap secret, etc. these tasks cannot be done means real Kubernetes administration cannot be donefrom here.Now how I can manage this.To do this, we must have Kubernetes CLI tool known as Kube control.So where is kube control?I want to create pod.I want to host my application inside this Kubernetes cluster.How I can do this.So we have to install kube control into the separate computer.And from where we can manage this EKS cluster.Interesting.Right.And it is easy how we can set up kube control.What is kube control and how I can access this EKS cluster through the kube control and manage this.We are going to talk about everything related to the kube control in the next video.Thank you very much.See you in the next video.Have a nice day.Goodbye.