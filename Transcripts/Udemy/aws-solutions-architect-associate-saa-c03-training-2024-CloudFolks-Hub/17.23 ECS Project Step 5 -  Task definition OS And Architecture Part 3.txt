# 17.23 ECS Project Step 5 -  Task definition OS And Architecture Part 3

Hello and welcome to Cloud Folks Hub.My name is Bhavesh Edara.We are exploring all the options related to task definition.Why?Because creating task definition is the most important part of our project.In the previous video, I already explained you about task definition, family and launch type.In this video, we are going to talk about operating system and architecture of task definition.If you look at over here, we have this options available in this particular list box.Now how we can use this.What is our main use case of this.Let's explore all of this in this video.Let's start.This is step number 5.3 operating System and architecture.Let's start with point number one.When you define task definition in X, you must tell which operating system your task is built for,right?Mainly we have main two operating system.Either we are launching our task in Linux or Windows.If you have Linux based operating system, you have to choose Linux.If your windows based operating system like dotnet application, you have to choose windows, right?You can directly select this from this particular list box.Another most important part along with operating system is actually CPU architecture.We have x86 64 bit processor from the years, but now ARM based processor is also getting popularity.So maybe your application development is actually built for ARM 64.Now in this case you have to choose ARM 64 from 64 bit from this particular window, which is a kindof CPU architecture.Now this tells X.Now what is the use case?Understand this use case.You are.By selecting this option you are telling X my container is made for this type of system.Please run it on the compatible machine.There is a misunderstanding like people having this misunderstanding.So let me explain you misunderstanding to avoid.You are not selecting EC2 instance, OS or hardware.Here you are telling X.Please run my task on EC2 or Fargate host that matches this detail right?See, when we are creating our cluster, let's say that we already created this cluster into the stepone.Right here we have two options.Let me create this.Here we have this AWS Fargate and Amazon EC2 instance.Right now, when I choose this EC2 instance, we are actually talking about all the detail.Let's say that I have x86 64 system and based on EMI Ami, we can say that it is a Linux, right?So we have this kind of Docker host or container host already added into the cluster.Now when we are creating task definition, we are just informing ECS that launch this task into anycontainer host which is having Linux operating system and x86 64 bit processor.Now here I selected this.Now ECS will take care about it.When we will launch the task, it will launch this task into the only container host which is havingLinux operating system and x86 64 bit processor.Now, you may have one question over here that okay, let's say that if I'm going to select Linux Arm64,right.What will happen?I created cluster already and while I created this particular cluster I choose Linux plus x86 64 right.It was my ECS cluster infrastructure.Now while I'm creating this task I selected Linux Arm64.So what will happen?So when you will launch task from this particular task definition, ECS will find out that.Do you have Linux and Arm64 based EC2 instance?If it is not there, your task will be failed.It is not going to create new EC2 instance or container host whatever you are selecting over here.This type of EC2 instance must be there in your infrastructure.If it is not there, if there will be a mismatch between your task definition OS and your ECS clusterinfrastructure OS, your task will fail.Okay, so keep this in your mind.Now how ECS use this info.Once you set OS and architecture in the task definition, let's say that I'm selecting Linux plus x8664, right?Our application is actually based on this Linux and x86 64.It can launch my task into EC2 instance based on x86 processor with the Linux operating system.Or it can launch my task into the Fargate, right?Fargate supports this.Now, if I were to select Linux plus Arm64, right?So I must have EC2.Graviton instance means EC2 instance that supports ARM 64 bit processor, or it can launch the taskinto Fargate as well.So Fargate supports both x86 and ARM.Windows x86 64 supports EC2.Now let's say that you have windows based application DotNet or IIs based application, and you wantto launch your task into the windows based container host.You must have EC2 instance with windows Ami.Now if it is not available then your task will fail.Keep this in mind windows is not supported by Fargate.Fargate is not supporting windows, right?So relevant EC2 instance must be there.If X can't find compatible host, the task will fail to start.Okay so this is like important point that you need to understand.It is not going to provide you any error over here.But yes when you will launch the task it will fail.Here I am going to give you a real time example as well.You selected Linux plus Arm64 in the task definition.Let's say that I'm creating task definition and I have selected Arm64.But your EC2 container instances x86 64.Right now we have this only right.In this case, X will not schedule the task on that instance.It will wait or fail because there is a mismatch.Now what is the solution?You must register arm64 EC2 instance like AWS graviton in your ECS cluster.You know right now you have this cluster.For example, I have Fargate and, uh, x86 64 bit processor over here.And I want to launch my task into the graviton means ARM 64 bit.I can add arm based instance as well.Now you may have this question like how I can add this.Watch our previous videos.In previous videos, I already explained you that after creating your ECS cluster, how you can addyour EC2 instance later as a container host.So you have to perform same step.You have to create your graviton based instance and then you can add attach this particular instancewith your ECS cluster.Now once you add this, then you can run this task very easily because now in your task definition,whatever you have selected Elected means you selected Arm64 and relevant instance is there so it canlaunch this task.Okay, so the final summary you are telling X what kind of matching your task needs.And X will look for a matching X EC2 or Fargate host.If there is no matching host the task will not run.Okay, so this is all about operating system.Now in the next we have to select network mode as well.Now this is the most important part because this options will be different according to your launchtype.Let's say that if I'm selecting AWS Fargate, I'm getting only AWS VPC.If I am going to select Amazon EC2 instance, I'm getting so many options.We will talk about each and every options.But for this you have to watch next video.Thank you very much.See you in the next video.Have a nice day.Goodbye.