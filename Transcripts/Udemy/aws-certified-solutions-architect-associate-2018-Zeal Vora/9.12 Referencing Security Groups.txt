# 9.12 Referencing Security Groups

Hey, everyone.And welcome back.Now, in today's video, we'll be discussing about referencing the security groups.Now, generally, whenever you create a security group and its associated rule, what most of the organizationsthey do is that they create a rule based on the IP addresses.However, in security group, you can also create a rule based on the security group ID of the destinationEC2 instances.Now, this practice is specifically good whenever you are using a EC2 instance in an auto scaling group.So let's understand this with a diagram where on the left hand side you have two web servers under theauto scaling tier, and on the right hand side you have four EC2 instance part of the application tier,which is again part of the auto scaling group.So now let's assume that you have added because these two web servers will have to communicate withthese four application servers.So what you have done initially, you have added the IP addresses of these two EC2 instances withinthe security group associated with the application tier.Now, tomorrow, since this web server is part of the auto scaling group, let's assume that a new webserver is created because of a higher load.So that third web server, since it has a new IP address, will not be able to communicate to the applicationtier.So whenever you're referencing things via the IP addresses, it might not be the best way.So in order to avoid this scenario, what a lot of organizations they do, they allow the entire VPCcider block within the auto scaling group.Although that works pretty well even with the dynamic scaling.But that is not the best approach here.So the ideal solution to deal with such kind of a scenario.First one is to create two security group.The first security group is for web server and the second security group is for application server.So one security group will be common to the web server tier and the second security group will be commonfor all the EC2 instances under the application tier.So let's assume that the web server security group ID is SG hyphen web and the app server security groupID is SG hyphen app.So the security group rule that you'll have to put for the web server instances, you have to allowoutbound port 8082 SG hyphen app security group.So here we are considering that the application is listening on port 8080.So since this web server EC2 instance will have to forward the traffic to the application tier EC2 instances,you add an outbound security group rule which allows port 8080 to all the instances having the securitygroup ID of SG hyphen app.And for the app instances you create a new inbound security group which allows the traffic coming toport 8080 from the instances which have the SG hyphen web security group ID.So this is what the primary aim of this topic was.So let's quickly jump into practical so that it becomes easier for us to understand.So for today's demo, we have a EC2 instance called as Instance one and we have a RDS database.So let's assume that this EC2 instance wants to communicate with the RDS database.So what you'll do is within the database security group, let's quickly open this up and if you go tothe VPC Security group, which is the default one here.And if you go to the inbound, you will see that currently it is allowing all the traffic and the sourceis the security group ID here.So that basically means that if there are ten EC2 instances where this default EC2 security group isassociated with all the ten EC2 instances will be able to communicate with each other.So that is the primary purpose of this specific rule.So as we are discussing, if we want to allow this instance one to communicate with the RDS database,one way in which you can do is you can create a custom TCP rule for the port of 3306.The Ssid would be the private IP address since both are within the VPC.So you can create a rule with the private IP address, something like this, and you can click on save.So this is one way.However, if this instance is within the auto scaling group and tomorrow if there are two new instanceswhich gets created since your referencing by the IP address, that will not work, the new instanceswill not be able to communicate with the RDS.So the better way is instead of referencing via the Ssid, you can look into the security group associatedwith the instance.So in this case it is CP Labs hyphen firewall.Copy the security group id here and replace the cider block with the security group ID.All right, so now you see it says CP Labs Hyphen Firewall.So that basically means that any EC2 instance which has this CP Labs hyphen firewall security groupassigned to it will be able to communicate to the RDS database on port 3306.So I hope you got the gist into how security group can also be referenced within a security group rule.So let's go further and discuss the use case of how we can reference security group across the networkwhere you have VPC peering.So one good thing is that the security group can be referenced across a VPC peering connections as well.So let's assume that there are two vpcs within the peering connection and provided that both the vpcsare within the same region, then we can reference the security group IDs from the peer VPC.So let's quickly look here as well.So let's go back to the dashboard here.So currently I have two EC2 instance.One EC2 instance is part of a default VPC.So if you see the VPC ID is 2E1 and the second EC2 instance is part of the custom VPC.So these two EC2 instance are from a different VPC over here.So let's say that you want to allow a EC2 instance to to communicate with the EC2 instance one via theICMP protocol.Now again, one quick way is to allow the CI of the VPC to within the firewall rule.So let's look into it as well.So currently within the CLI, I am connected to the instance one via SSH.Now from the instance one, if we go ahead and try to ping to the instance two, you will see that youare not able to ping.So what we are doing, we are logged in to the instance one and from instance one we are trying to pingto the instance two and it is not working.So as expected, the first thing that you have to verify is the security group associated with the instancetwo.So within the security group, you only have one rule over here and there is no ICMP rule specific toallow the ping request.So what you can do is you can create a ICMP rule.Let's select all ICMP for IPv4.And here you can also specify the size of the instance one.So let's add the 32 block.We'll save it.And as soon as you save it, you see, you are able to ping it perfectly.Now, in a similar way, as we were discussing, you can also allow through the security group.So let's copy the security group ID,let's change this to the security group ID of the instance one belonging to a different VPC.Once you have done that, you can just try a ping again.And as expected, it is working perfectly well.So remember that this type of setup will only work if both the vpcs as part of the peering connectionare within the same region.Now, the third important aspect that you need to remember is about the Cross-account Security Groupreference.Now you can also reference the security group across accounts.So for example, if you want to allow a specific EC2 instance belonging to a different AWS account toconnect to the instance.Is in your account, then the format is the account ID of the second account, followed by the securitygroup ID.So let's say that there are two accounts and you have peered VPC across both the accounts and you wantto allow the connection.So if the Vpcs are in a different AWS accounts altogether, they will not be able to directly referenceby the security group ID.You will also have to add the account ID at the start.So this is very important part to remember specifically if you're referencing to cross account.So let's try it out.So within the security group ID, let's say that you want to allow on Port 22 and within here you canspecify a rule, something similar to this where the first highlighted section here is the account ID,and the second highlighted section is the security group belonging to the account ID that you have entered.Here you can click on Save and if you do not really have a security group ID belonging to this specificaccount, you will get an error, something similar to the one that you have seen.So for simplicity, what you can do is you can replace this account ID with the current account ID thatI have.So this is the account ID and also replace the security group associated with the security group ID.So let's do one thing.Let's copy this up.And the current security group ID is the one that is highlighted.So what you can do is you can add a new rule.Let's do 43306.We'll use the format where you have the account ID and where you are, the security group ID Once youdo a save, it works pretty well.Now here you see it automatically identified that the security group ID that we had added was part ofthe current account itself.However, if it was a different account then you would also see the account ID within the security grouprule.So again, one important part to remember when you're using a cross account reference is that you cannot reference the security group of a VPC that is in a different region.So in case if you have a VPC peering connection that belongs to two separate region, then you can insteadmake use of the cider block instead of a standard security group ID within the rule references.So that's the overview.I hope this video has been informative for you, and I look forward to see you in the next video.