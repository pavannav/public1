# 9.6 AWS Key Management Service

Hey, everyone.And welcome back.Now in today's video, we'll be discussing about the AWS KMS.Now KMS stands for Key Management Service.And the primary purpose of this service is to go ahead and allow us to perform the encryption and decryptionoperation of the data so this can be better understood with this diagram where you have a user on theleft hand side and the service on the right hand side.Now let's assume that the user has a sensitive data.In this case it is password one, two, three, four.So once the user sends this password one, two, three, four to kms, kms will go ahead and encryptthis data and will send the encrypted data back to the user.So instead of storing this sensitive data in his laptop or maybe even sending this sensitive data overvarious communication channels like Slack, email, others, the user can go ahead and instead sendthe encrypted data over the communication channels.So even if an attacker is able to fetch this encrypted data, he will not be able to do anything unlessand until the attacker has the access to the KMS service.So that's the high level overview.Let's go ahead and look into more detail.So currently I'm in the AWS console and if you go to the customer manage key.So these are the keys that I have created.Now here we have a key called as demo KMS key, and this key is basically will be used for performingthe encryption as well as the decryption operations that we will be performing.So for today's demo, let's go ahead and look into it at a high level overview.So for today's demo I have a text file called as Test dot txt and the contents of this test file ispassword is equal to one, two, three, four.And now we'll look into how we can encrypt the data with the help of KMS.Now one important part to remember is that if you want to perform the encryption using KMS, you donot really have any perspective.Like you can go ahead and upload the data here and KMS will go ahead and encrypt and give it to you.You need to make use of a CLI for that.So I have a CLI command for today's demo.So let's look into it.So you have AWS kms encrypt.So KMS has multiple operations, it can perform encryption, decryption and various others.So we are saying we want to perform the encryption operation.We have specified the key ID, so this is the key ID over here.So basically we have multiple keys.So which key we want to use for performing the encryption operation.So this is where the key ID comes into picture.And then what we are saying we are supplying a plain text file called as test dot txt.So if I go ahead and press enter, you see in the output we have received the cipher text blob.So this is the cipher text blob and this is nothing but the cipher text data or the encrypted data.So this encrypted data is what we have received from the KMS service.Now let's assume that after some amount of time we have this encrypted data that is stored in our laptopand we want to go ahead and decrypt it.Or you have sent this encrypted data to your friend who is within the same team and the friend wantsto go ahead and get the plain text data.So what the friend needs to do is he has to send this encrypted data again to the KMS service and providedhe has a right set of permission on the KMS service, the KMS will go ahead and send the decrypted databack.Let's look into it.So in order to go ahead and fetch the plain text data, now you are saying to us that this time I wantto do a decrypt operation and now you also have to supply the cipher text that you had received earlier.So let's go ahead and supply this.So this is the cipher text that we had received earlier.And now you see KMS has basically given us the plain text.Now this plain text does not really look like the plain text that we had as part of our text file.So within our text file we had password is equal to one, two, three, four.But this does not really look like that.Now do note that when the KMS sends us the plain text data, this is basically a base64 encoded data.So what you need is you need a tool that can go ahead and decode that data.Now generally Linux and Mac comes with the base 64 decode tool.However, Windows does not really come by default with base 64 decode tool.So once you go ahead and decode it, you should see it is password is equal to one, two, three, four.Great.So I hope at a high level overview you understood the basics of KMS.Now one of the great things about KMS is its integration with various AWS services.Now generally the question that comes is let's say you have large organizations who have thousands offiles.Now should they go ahead and supply each file individually to KMS and get the encrypted contents?Not really.Most of the organizations they store sensitive files in S3 Bucket or in the EBS volumes.So KMS also supports the integration with various services like S3.Bucket, eBay and so on.So if you have your files stored in S3 Bucket or EBS volumes, you can go ahead and perform the encryptionas well as decryption there.Now within the AWS console, if we look into the AWS managed keys, you see you have various keys associatedwith services like SNS, S3 Code, Commit, Lightsail and various others.So multiple keys can be created depending upon the services that are being used.However, one thing to note, all of these services are supported as part of the integration with KMS,so you do not really need to worry about encrypting the individual files.So that's a very high level overview about KMS.Again, we did not really create a KMS key from scratch and look into various configuration parameters.This was a very high level overview based demo video to understand the concept.And with this we'll conclude today's video.