# 7.7 EC2 Placement Groups

Hey, everyone.And welcome back.So in today's video, we'll be discussing about the placement groups.Now, there are two important features associated with placement groups that we need to remember.And these two pointers summarizes the overall features and the necessity of placement groups in AWS.So the first pointer here states that the placement groups are recommended for the applications thatrequire low latency, high network throughput.And the second point is states that the placement groups can be used to influence the placement of thegroup of EC2 instances that you launch in AWS.So let's do one thing.Let's go ahead and understand each of these points individually and we'll start with the first pointover here.Now, the easiest way in which I can give you an explanation of the first point is through this screenshot.Now this screenshot is a small road on the left and a highway on the right.Now you can easily imagine what this is.So if you're driving a vehicle within a small road, then the overall speed through which your vehiclecan go through is much more less when compared to the speed in highway.So similar to this specific analogy, if you now look into the first point, let's reread it.So the first point states here that the placement groups are recommended for the applications that requirelow latency, high network throughput.So any applications that really require the network throughput to be very fast, typically there canbe applications related to the high performance computing, which requires a very high network throughput.So for such kind of applications you can make use of placement groups.So let's go ahead and understand this in more detail.So we have one more analogy example that can allow us to explain the first point in detail.So let's say that this is a India region and there are three availability zones in AWS.Let's assume that there are three availability zones over here.Now what would happen is that if you have a server in this green availability zone and if you have asecond server in this red availability zone, and if you try to communicate between them, then therewill be a certain delay that will happen because the traffic needs to go from green to red and returnback.So further the distance, the more it might take for the packet to return back.Similar to that, the distance between the green and the blue is much more lesser.So the amount of time that it might take for the return traffic will be much more lesser when comparedto the green and the red.And similar to that, if both of your servers are within the same availability zone of green, thenthe speed will be much more faster because now you do not really have to go towards other availabilityzone.The traffic is traversing within the same data center, so that is what is the intention of the firstpoint within the placement group.So what we do here is that we try to launch the servers within a placement group in such a way thatit is within the same rack or maybe within the same underlying.Host.So then the overall capacity in terms of network throughput will be extremely high.So let's do one thing.Let's take one more example so that we can have a nice understanding of this point.So I have a server which is located within the same region of the place where I'm recording.So when I go ahead and ping this server, you will see that the overall TTL is 53.Now similar to that, if I try to locate a server like Amazon.com and if I try to ping them, you seethe overall TTL is 222, which is much more higher than what is compared to the server which is locatednear the place that I'm recording.So this is what placement group tries to offer.So what it tries to do is that it tries to co-locate the servers that you launch as close as possible.So the idea is that the servers, if they are very close to each other, may be within the same underlyinghost or within the same rack.Then the amount of data transfer speed will be much more higher.And that is what the primary aim of the first pointer associated with the placement groups are.So I'm sure now you understand what the first pointer is.Now let's go ahead and understand the second pointer as well.So the second pointer states that.The placement group can also be used to influence placement of a group of EC2 instances.So let's go ahead and understand this point as well.Now here your concept about virtualization will allow you to understand this point in much more betterdetail.So first important point here is that a single server can run multiple virtual machines.So if you have made use of VirtualBox or VMware or even KVM, you know that a single server or evena single laptop can run multiple operating system at once with the help of virtualization.So this can also be explained with this diagram below where you have a single host here.So if you see you have a single server and with this help of virtualization, there are multiple virtualmachines which are running.So these virtual machines in AWS are also called as the EC2 instances.So a single server can have multiple EC2 instances.Now how this point can lead to issue if you're running a cluster of servers.So let's understand this with an example.So the example states here that you have a medium core, which is an organization and it is runninga MySQL cluster consisting of two servers in a single availability zone, and the server are of typeM4 large.Now it can happen that these two servers which are running as part of the MySQL cluster are runningon the same host.So if you will see that you have a server over here, there is a virtualization layer and on top ofvirtualization layer there are three servers.Two among this are of type M4, large.So this can happen in a lot of scenario that if you are launching two servers in same availability zone,both of the servers or both of the EC2 instances can be part of the same underlying hardware.Now this kind of a scenario is risky because it can happen that due to some issue with the underlyinghardware, your server is down and if the server is down, all the virtual machines which are runningon top of it will also fail.And in such cases, if you look into this example, if this host has to fail, then both of the M4 largeinstance, which are part of the MySQL cluster, will break down and your database would be down completely.And this is one of the reasons why it is recommended to always prefer the Multi-az setup.Now, even with single AZ, there are ways in which you can tell to AWS that I want to launch two EC2instances in a single availability zone, but they should not be part of the same server or a same rackto be more precise.So with the help of placement group you can explicitly specify that two instances should not be partof the same server or you can say same rack of servers.So now, with the help of placement group, you will see in this diagram you have one M4 large instancewhich is being hosted in one server and you have second M4 large instance which is hosted in a secondserver.Now both of these are within the same availability zone, but you can explicitly define that with thehelp of placement group.Now in case if the first server goes down, then you also have a second EC2 instance which is runningin an entirely different rack so that you are safe.So many of you might be familiar with racks within the data center.So this is a typical example of a data center where there are multiple racks of servers which are available.So what you can tell is the first server might be launched in this first rack.The second server might be launched in this specific rack.So even if the entire rack has a issue, your server will not face any downtime.Great.So with this basic understanding of both the pointers, let's look into the types of placement groups.So there are three types of placement groups available.The first type is a cluster, so cluster placement group basically packs instances close to each otherwithin a same availability zone.So this can be better understood here.So in this diagram you see that there are two availability zones, but with the help of Cluster PlacementGroup, you try to place both of your instance within the same availability zone.Or to be more precise, it would be within the same underlying hardware or at least within the samerack.So then what would happen is since the servers are very close to each other, there will be a very highnetwork throughput that you can achieve.So that part can be attained with the help of cluster placement group.So let's do one thing.Let's go ahead and understand this in more detail practically so that it is much more fun for us tolearn.Now this is the EC2 console and within the EC2 console, if you go a bit down there is a tab for placementgroup.Now if you click on here, you will have a screen similar to this.So if you click on Create Placement Group, you will see that there are three types of placement groupsthat you can create.First is cluster, second is spread and third is partition.As we have discussed, the cluster placement group tries to launch the instance within the same underlyingserver or within the same rack so that the overall network throughput is much more faster.So that is the type of cluster placement group.So let's go ahead and try it out.So you can say K Labs hyphen cluster, the strategy is cluster and you can go ahead and create it.So you see now it has gone ahead and created a placement group called as K Labs Cluster.Now if you go to instances and click on Launch instance and let's go ahead and select the latest Amiand we'll use the instance type, let's select M5 dot large.Now there is a specific reason why we are selecting the instance type, which is much more bigger thana T2 Micro.So we'll be discussing about that in a moment.Let's go to the next screen and here there is an option of placement group so you can select this andyou can say add to an existing placement group and you can select the placement group over here.And within the number of instances you can say three.So now what will happen is AWS will launch this 3 or 4 EC2 instances within the same underlying hostwhich is available, or it will try to launch it as close as possible.So that is the main aim of the cluster placement group.Now do remember that it needs to be within the same availability zone.Great.So that is the first type of placement group.Second is the partition.So what happens in a partition placement group is that ensures that each partition within the placementgroup has its own set of racks.Now we have already discussed what racks are.So in this diagram you see that there are three partitions and each partition has three EC2 instances.So when you make use of partition placement group and there are three partition, you can ensure thateach of this partition is running on a separate rack altogether.Now if you're running in a separate rack, then there is a better fault tolerance because in case ifthe rack fails, so in case, let's say the first rack fails, then only the server associated withthe first partition will go down.The other two servers will still be running.So that is the importance of a partition placement groups.Now the third type is called as the spread placement group.Now within spread placement group, what happens is that each of the server that you launch will berunning on a different rack altogether.So let's say that there are seven servers which are currently in this diagram.Then seven servers will be running on seven separate racks.So for example, let's say that the first server will run here, second server will run here, thirdserver will run here, fourth here, fifth, sixth and seventh.So that is the overall idea about the spread placement group over here.So here the fault tolerance is much more higher because when you take an example of partition placementgroup, if one rack fails, then all the three servers associated with the partition will go down.However, within the spread placement group, if the one rack fails, then only one server will go down,all the other six will still be running.So let's do one thing.Let's go back to the console, let's go to EC2 and we'll go back to placement groups.So the first one we already discussed, let's go ahead and explore the other one within here.Let's take an example.If you are making use of a partition placement group.So if you remember here, you have to select the number of partitions.So if I select the number of partitions as five and if you launch five EC2 instances, then all thefive EC2 instances will be part of a different rack altogether.Great.So I hope with this you got a better understanding of what placement groups are and also the differenttypes.So there are few important pointers that you need to remember.The first one is associated with the cluster placement group.First one is that a cluster placement?The group cannot span across multiple availability zones.We have already discussed that the primary aim of cluster Placement group is to provide a high networkthroughput and the entire purpose gets lost if you are launching it in a two distinct availability zones.So in order to achieve a high network throughput, you launch the instances within the same availabilityzones.So that is the first point.Second point is that only a specific type of EC2 instances can be launched.This is because certain types of EC2 instances like T2, dot micro T2 dot small, they do not reallyhave a fast network throughput that you can attain.So there are specific EC2 instance types that you can launch within the cluster placement group.So if I have to quickly show you through the documentation, let's go to the cluster placement groupand we'll go a bit down.Now if you look into the cluster placement group rules and limitation, you will see that there areonly certain instance types that can be launched.You certainly cannot launch instance type like T2, dot micro t2 dot small because then the entire purposegets lost.So these are the instance types that you can launch within the cluster placement group.Again, if you look into the second point, cluster placement group cannot span across multiple availabilityzones.The third important point here is that a maximum network throughput traffic between two instances ina placement group is limited by the slower of two instances.So let's say that there are two instances which have launched within the placement group.The first instance is capable of ten Mbps.The second instance is capable of 20 Mbps.So the overall network throughput that you can attain between these two instances would be ten Mbps,which is the slower of both of them.So that is what the third point is all about.The fourth point, which is quite important, is that it is recommended to launch all the instancestogether.Now, when you launch all the instances within the cluster placement group together, then will tryto keep them as close as possible, typically within the same underlying host as well.So if you are launching a instance at a later stage, then there can be capacity errors.So if you are facing capacity errors, the recommended way is to stop and start all the instances inplacement group.So this used to be a classic example.Question where there is a scenario where someone is trying to launch more instances within a placementgroup and there is a capacity error.So now there will be four options in terms of answers and you'll have to select the right answer.So the right answer is you have to stop all the instances and start it within the placement groups.Great.So we have gone ahead and looked into placement group in great detail.I hope this video has been informative for you and I look forward to see you in the next video.