# 6.4 Practical - EC2 Auto-Scaling (Manual Scaling)

Hi everyone and welcome back.So today's video is going to be the practical video on the EC2 Auto Scaling service.So let's go ahead and get started.Now one of the things that we have to keep a note is that the configuration related to EC2 auto scalingresides within the auto scaling group.So within the EC2 console, if you go a bit down, you have the auto scaling groups over here.And any configuration that you have to create must be added as part of this.Now one more thing that the Auto Scaling group requires the launch template to be created and basedon the configurations that you add as part of the launch template, like the Ami instance type, securitygroup ID, etc. it will go ahead and launch the instance accordingly.So this is the reason why before you configure the auto scaling group, you have to make sure that youhave the launch template ready.So you can go to the launch template.Let's quickly create a launch template.Let's just call it as security template.And within here you can specify the ami id whatever army ID that you need.I'll just use the Amazon Linux one, then the instance type.Let's just specify the T2 micro.Whichever has the free tier eligible in the region.Then the key pair type.In case if you have you can use that.In case not, you can just ignore this.Along with that, the firewall depending upon what security group you want the instance to get associatedwith, you can go ahead and add that accordingly.So let's do one thing.Let's go ahead and create a security group for our use case.So I'll go to security group.Let's create one I'll just call it as a hyphen SG.It will allow all of the traffic.Just for simplicity let's verify this is outbound in the inbound.Also, let's go ahead and allow all of the traffic here from 0000.And I'll create a security group here.Let's verify the description.Let's say ASG security groups once.Then you can create a security group here.So now what we want is all of the instances that auto scaling Group creates.It should have this specific security group.So I'll just quickly refresh this.I'll go ahead and I'll add this specific one over here.Once this is done I'll go ahead and I'll create the launch template.Once the launch template is available now you can go ahead and configure the auto scaling groups.So let's create an auto scaling group here.Let's just call it as KP Labs Hyphen Auto Scaling Group ASG the launch template.Here you have to specify whatever launch template you have created.In case if the launch template has multiple versions, you have option to use that.But in our case launch template only has one version, so this is the default one that gets associated.Let's click on next.Now you have to specify the availability zones.So default VPC is selected.So in availability zones you can just use any two availability zones just for testing.Let's do a next.We are not going to specify other configurations will leave everything as default over here in termsof health checks.Let's just decrease this to say 90s.All right.This can help during the testing phase.But generally in production environments you can keep it either default or based on the requirements.You can set it in the next page.Over here you have to specify the desired capacity minimum and maximum.So let's say minimum is one desired.Also I'll just set it to minimum and maximum set of instances that I want the auto scaling to add isthree.So this is one.Now the next part is the scaling policies.So there are multiple set of scaling policies that you can add by default.At this stage we'll just set it as no scaling policies.I just want to give you an overview.We'll leave other things as default over here.Let's do a next let's do a next next.This is the review screen.We'll go ahead and we'll create the auto scaling group.All right.So once the auto scaling group is created at this stage the status is updating capacity.This is fine.Since the desired capacity is one it will go ahead and launch one EC2 instance as part of the region.So if you go back to the instances over here, you see there is one instance.The instance state is currently pending.So just give it a moment for the instance to run post which will verify the entire workflow.So on a refresh the instance state is now running.So within the auto scaling group, once it is created let's refresh this.In the instances you see it is showing as one.So this is good part.Now again there are a lot of other configurations like it can integrate with load balancing and othersthat you can set.Today's video is going to be simple to understand the entire workflow.Now one more thing that I wanted to share is that this specific demo was based on manual scaling.So in manual scaling, users manually adjust the desired number of instances based on the requirement.However, ideally in production environments, you should not set manual scaling.Manual scaling is good for initial set of testing to understand the workflow, but you should have othertype of scaling policies based on the requirements.So if you look into scaling over here, there are a wide variety of scaling policies that are availableover here.Again, ultimately based on the scaling policies, they are going to update the desired capacity accordinglywhen some alarm triggers and so on.So ultimately at the end this part will be updated.It can either be manual, it can be dynamic, or it can be based on schedule.So once you understand the manual, then when you create the other type of policies it will become simplerto understand.All right.So now at this stage we see that we have one EC2 instance that is currently running.Now one more point that we have to keep a note is related to the desired capacity.So here we know that desired is the target number of instances that you want to run under the normalconditions.Now, since you have added the desired capacity as one, the auto scaling will only launch one EC2 instance.However, auto scaling will also monitor the health check of EC2 instances to ensure that it is running.If the instance is not running, or maybe the application is failing the health checks or the EC2 instancesfailing the health check, auto scaling can terminate the instance and the new instance get get launched.So let me also quickly show you this specific part.Now let's go ahead and we'll manually terminate this EC2 instance over here.Alright.We'll see on how the auto scaling group will behave in such cases.So let's quickly wait for a moment here.So this specific instance is terminated.Let's refresh this.And now you see one more EC2 instance automatically got launched.So this EC2 instance again it got launched by the Auto Scaling group.How can you verify easily if you look into the security group here, automatically the AC hyphen SGis added, and you can also look into the tags.Within the tags you can see the auto scaling group name, the launch template ID and so on.So this is one of the quick ways.Now let's say that you want the auto scaling group to be available, but you don't really want the instancesto be running because otherwise it will take up the credit as part of the free tier.So in such case, what you can do, you can just edit this specific auto scaling group.You can change the desired capacity to zero and the minimum desired capacity also as zero.And once you have done that you can go ahead and click on update.So currently it is updating the capacity.The desired capacity and the minimum has been changed to zero.So this part will also be updated.So now if I refresh let's try it out I refresh.You see this specific EC2 instance is getting shutting down and it will soon be terminated as well.So that's a quick overview about the basic of EC2 auto scaling and the important workflow process.And with this we'll conclude today's video.