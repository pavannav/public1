# 13.4 SQS Dead Letter Queue

Hey, everyone.And welcome back.Now, in today's video, we'll be discussing about the dead letter queues.Now, the Amazon SQS supports dead letter queues, which other queues which are basically the sourcequeues, can target for the messages that can't be processed or consumed successfully.So let's understand this with an example.So let's assume that this is an SQS queue that you have, and along with this you have a lambda functionwhich acts as a consumer.So this lambda function basically goes ahead and it fetches the messages that are part of the SQS queueand it processes them.After the process is completed, it deletes the message from the original SQS queue.However, due to some reason, the first four messages have been successfully processed, but only thefifth message, it was able to fetch it, but it was not able to process that message due to some reason.So now what will happen is that this fifth message continues to be part of the queue, primarily becausethe Lambda function is not able to successfully process the specific message so that it can later deleteit.So since this processing is not completed, this fifth message continues to stay in the SQS queue andwhatever other processes that are fetching the messages from this queue, this message will always leadto an error.So in order to overcome this you have a feature of a dead letter queue.So the concept is that if there is a message that the consumer process are not able to successfullyprocess it, move the message to another queue, call as a dead letter queue.So this fifth message is now moved from the source queue to another queue college, a dead letter queueand then the developer can manually go ahead and inspect the message from the dead letter queue to understandon why exactly things are not working so the troubleshooting area can happen from the dead letter queue.So this is the basics of the concept of dead letter queue.Now let's go ahead and look into it in a practical way.So currently I'm with the SQS console and we have two queues that are available.One is the message queue and the second one is the dead letter queue.Now the message queue is configured in such a way that a maximum amount of receives is two.So after the message is received for more than two times, it is assumed that there is some issue withthe message, primarily because the consumer is fetching the message, but it is not able to processand hence that message will be moved to the dead letter queue.So let's go ahead and try it out.So I'll send a message here.So I'll just call it as this is a message one.All right.I'll go ahead and send a message now before we go ahead and configure it, let me quickly show you thesettings.So within the dead letter queue here, the maximum receives that you see is two.So we'll be able to get it two times.So let's go ahead and receive the message.So I'll poll for the message and as soon as we were able to poll, you see the receive count becameone.So if I'll quickly open the message you have, this is a message one.So at this stage, things seems to be fine.Let's go ahead and poll once more time.And now again, you see after the second polling, the receive count now has become two.Now at this stage, if we'll quickly go to the queue and if I refresh the page, the message availableis still one.So the message has not gone to the dead letter queue.It will only go when it is being queried for the third time.So now let's go ahead and receive the message for the third time.So now if you do a poll for message this time you will not see a message primarily because the receivecount has been increased to three and because of our configuration setting, the message now went fromthe message queue to the new dead letter queue.So now if I refresh the page, you see the message available in our queue is zero and the message thatis part of the dead letter queue is one.So now all that developer needs to do is that from the dead letter queue, he needs to go ahead andpoll for the message and this time he'll be able to look into the message and troubleshoot on why theconsumer process were not able to complete the processing on this message.So this is the basic concept behind dead letter queue.Now, in fact, let me quickly show you on how we can go ahead and create a dead letter queue.So first let's create a queue.So I'll just call it as queue one.And I'll go ahead and create a queue.All right, let's go back to the queue.So we have queue one.Similarly, let's go ahead and create a queue two and let's call it as dead letter.We'll go ahead and we'll create a queue.Perfect.So at this stage we only have a standard queue type.So the dead letter configuration needs to be configured in the queue.One So when you go ahead and click on edit, you have an option of dead letter queue.So we'll go ahead and enable this.And the queue target this time would be our queue.Two dead letter.All right, so this is the dead letter queue and the maximum receives here, let's just call it as twosimilar to what we were using in demo.I'll click on Save.Perfect.So now let's go ahead and send a message.I'll just call it as message one.Let's do a send message.Perfect.So the message has been sent.If we go ahead and do a poll, the receive count is now one.Let's poll again.The receive count is now two.And now let's poll again.And now you see the message no longer appears within the source queue.So let's stop the polling here and let's go back.So if you'll see here, there are only two queues that are appearing.In fact, this would be fixed after you manually do a refresh.So this is one of the issues with the SQS console.This is a newer console.Maybe it might be fixed after some amount of time, but let's refresh this and this time you see themessage available is zero in the queue one and within the queue of dead letter you have a message available.That is one that's great.So I hope at a high level overview you understood the basics of dead letter queue.Now before we conclude, there are a few important pointers that we need to remember.First one is that when a message moves to a dead letter queue, the timestamp remains unchanged.This is very important.So let's understand this with an example.So let's say a message that has been within the source queue for one day has now moved to a dead letterqueue.Now the message retention period within the dead letter queue is four days.So after four days the message would be deleted.So this first message, which had been within the source queue for one day and has now moved to thedead letter queue, the message will only stay within the dead letter queue for three days and afterthree days it will be deleted, primarily because the original timestamp of the message remains to bethe same.So although the retention of the dead letter queue is four days since the message was already therein source queue for one day, it can stay in the dead letter queue for a maximum of three days now.So it is considered as a best practice to always have a higher retention period for a dead letter queuethan that of the source queue.That's great.So with this we'll conclude today's video.