# 13.27 Practical - AWS EventBridge

Hi everyone, and welcome back.Now in the earlier video, we were discussing about the architecture of the EventBridge service andthe importance associated with it.Today is going to be the practical video where we are going to try it out.Now, as we were seeing in the diagram, as well as part of the EventBridge, there are three importantcomponents.One is the event source which will generate some form of an event.Then you have the target which will get invoked based on the event.And then there is a middleman, which is the EventBridge service that sits between the event sourceand the target.And in the EventBridge you can create multiple set of rules.For example, you might be targeting a very specific event, for example EC2 instance shutting down.So you don't really want to target other events like EC2 instance has started and so on.If you want to target very specific events, then based on the rules that you create, you can specifythe event pattern based on the pattern.The appropriate target will get invoked.So that's the overview.Let's go ahead and try it out.Now what we are going to do.First we require the event source.So within here I have one EC2 instance that is currently in the stopped state.In case if you do not really have any EC2 instance, you can create EC2 instance.So let me do one thing.I'll just quickly start this EC2 instance so that we have it running.Now again, this starting of EC2 instance is also a event that got generated, but as of now we don'treally have a EventBridge or the target, so it will not really do anything.Now what we are going to do, we are going to open up the EventBridge service and within the EventBridgeservice in the buses, we are going to create a new rule.So let's go ahead and quickly create a rule.Let's maximize this in the rule name I'll say prod server stopped.All right.So let's assume that in the production no server should be stopped.As soon as the server is stopped, you want the target to get invoked.Now that target can be SNS, it can be lambda function, etc. it depends upon the choice.For today's video, we are going to use the CloudWatch logs as the target so that we can actually seethe entire event that is received by the target.Now, whenever you create a rule, this rule can be of type rule with an event pattern.Or you can also schedule a rule.For example, we were discussing that there is a use case where you have to stop all of the developmentEC2 instances at 8 p.m. on Friday and start them at 9 a.m. on Monday.So in such cases, you will not rely on the event pattern based approach.You will rely on the schedule based approach.However, today's video is going to be the event pattern.This again is very important from the production perspective.So I'll go ahead and select it.Let's do a next.Now within here the important part is the event source.As we were discussing over here.In this case there can be a lot of event sources.For example you have AWS service AWS service also, it's not that there are only one service.There are hundreds of services.Each service will generate its own set of events.In our case, the service that we are targeting is EC2.So what we are going to do, we are going to select AWS and within here in the event source it is goingto be AWS services and which service you want to target.Let's specify EC2.Now even in the EC2 there are multiple set of events that can get generated.What category of events you are targeting is something that you can specify over here.Let's use this category EC2 instance state change notification.So in case if the state of EC2 instance changes, maybe it is transitioned from stop to running or runningto stopped or maybe stopped to terminated, etc..So whenever there is a state change.So here you have option of any state.We don't want any state.We'll specify a state.And these are the states pending running shutting down terminated stopping and stopped.So what I'll say stopped.All right.And within here you can either specify the instance ID or you can specify any instance.So let's say that in Ohio region all of the instances that are running are production service.So then you can specify any instance over here.And this has generated the event pattern.So this is the event pattern.Whenever event matches the appropriate pattern it will go ahead and invoke the target.So now comes the target.What is the target when event is matching.It can be lambda function SNS etc..So here we'll keep it simple.Again.As you see there are so many targets that are available.We are going to keep it simple.And we'll select the CloudWatch log group as the target this one.And within here let's specify log group as production EC2 stopped.All right I'll do a next I'll do a next.This is the review screen.We'll go ahead and we'll create the rule.Great.So there is one rule that is created.Let's open this.Let's verify in the target you have this specific target name.Now let's quickly go to the CloudWatch service.Within here we'll go to the log groups.Over here.There are multiple log groups which we had used for other demos.So the log group that we are targeting is this production.EC2 stopped at this stage.There will be no data because there is no EC2 instance that is stopped.So now what we are going to do, we are going to stop this specific EC2 instance.All right.So now the instance state is stopping.Now note that we had not specified stopping as the instance state.So this is the reason why immediately you will not get the event over here only and only after the stateis transitioned to stopped.So it is still stopping.After stopping it will be stopped.So let's quickly wait for a moment here.So now you see the instance state has transitioned to stopped.Now in the log group if we refresh.You see how fast it is.It did not really take 10s 20s delay.It was very immediate.As soon as the state transitioned to stopped, we got an event that got generated.And if you look into the event over here, let's open this.This is how the event really looks like.It shows you the region, it shows you the instance ID, and it also shows you the state which is stopped.Now this is just for reference.Now when you add the target something like lambda function, Lambda function will also get all of thisdata.So you can configure lambda function to extract the instance id.And the logic would be that lambda function will automatically start the EC2 instance with this instanceid automatically over here.So again there can be wide range of logics that you can add based on the requirement.But the aim here is to understand how the event really looks like and then the flexibility is available.So that's the overview about the practical aspect of the EventBridge service.And with this we'll conclude the video.Make sure that you go ahead and delete this specific rule.Because otherwise anytime you are doing testing and you are stopping the EC2 instance, the event willget generated as part of the CloudWatch log group.So that's about it.And with this we'll conclude today's video.