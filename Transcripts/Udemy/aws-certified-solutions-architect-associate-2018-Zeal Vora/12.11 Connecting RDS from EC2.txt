# 12.11 Connecting RDS from EC2

Hi, everyone, and welcome back.Now, in today's video, we'll look into how we can connect to the database that we have created inour ideas.Now, whenever you create a database, one of the important things that you need to keep a note of isthe database endpoint.So this is the endpoint where you will be connecting to.This is one important part.Second important part that you always have to verify is the security group associated with a database.For example, in this use case, I have created a database based on my SQL engine and the database supporthere is 3306.So in case if an application or if an easy to instance wants to connect to the IDs, then the port of3306 should be allowed in the security group associated with the database here.Now, in this case you have the default security group that is available.Let me quickly open this up now.In the default security group, in the inbound rules, you see that we do not have a rule associatedwith port of 3306.So this is the first thing that you need to allow.So I'll say let's go a bit down.You can see custom TCP port of 3306 and let us allow from everyone just for simplicity and I'll clickon Save.All right.Now for testing, I have a demo, easy to instance, that I have created, and from this easy to instance,we will be connecting to the database.So let's go ahead and quickly connect to this easy to here.You can use either the search connection from your laptop or you can directly connect whichever wayit works.Now, within the AC two instance, let's switch to the root user.In fact, let me just maximize this.Now, one of the important utility that you need to connect to the database is my SQL.So currently, you see, my SQL is not really available.Now let's quickly go ahead and install my SQL.So I'll say, why install Maria DB?So this should contain the my SQL related binaries.All right.Now if I just do my sequel, it is working.At this stage we are getting error.That is completely fine.Let's clear the screen.I'll run the my SQL command.Now you'll have to specify the URL of the database.So let's quickly switch to the IDs.Let's open up our database.Let's copy the endpoint.And let me piece the endpoint over here.Along with that, you'll also have to specify the user.I'll say admin and we'll specify hyphen p for password.I'll press enter.It is asking for a password.I'll specify the password of the database.Once.Then you see we are connected to the MySQL here.Now if we'll quickly do a show databases, it currently shows you the list of databases.You can also go ahead and create a database.So I'll say create database demo.If I press enter, it is successful.If I do a show databases yet again, you see we have the demo database that is available now from theexam perspective.You do not really need to know the exact my SQL commands.The only thing that you will be tested primarily upon is the ads and its associated configurations.So I'll just press control D so that we come out of the MySQL.So that's the high level overview.And with this will conclude today's video.