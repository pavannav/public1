# 14.5 Introduction to Route53

Hey, everyone.And welcome back.Now, in today's video, we'll be discussing about the Route 53 service in AWS.Now, just to revise the basics, in order for the DNS resolution to work, it is important to havea DNS server that is running.Now, whenever we do a lookup or whenever we open a website like Google.com in a web browser, ultimatelybehind the scenes, the request is always made to a DNS server and that DNS server is responsible forthe resolution.For example, if you open up Google.com within your browser, the browser in turn makes use of a DNSquery to a specific DNS server, and that DNS server in turn responds back with the IP address here.Now one of the question that comes is what are the softwares that are generally used for DNS server?Now Bhind is one of the very popular software which is used in a lot of organizations for DNS, forexample, let's say you have a EC2 instance and you want to create your own DNS server.You can go ahead and install Bind and you can configure the appropriate settings here and your DNS serverwill be up and running.So this is the quick way to get started.Now one of the challenge that comes is what happens if your EC2 instance or what happens if the bindinside your EC2 instance goes down, then essentially all of the resolutions that are being made, allof them will fail.So if a client sends a query towards the DNS server, there will be no response, primarily becausethe DNS server is not running and hence it is very important for the organization to decide if theywant to go ahead and use a managed DNS service or they are fine with a unmanaged DNS service, for example.In this kind of a setup, you have a EC2 instance, you install and configure bind and your DNS relatedconfigurations is up and running.So this is more of a unmanaged approach, an unmanaged approach.Not only you have to take care of installation configuration, you also have to take care of the overallhigh availability so that in case if one of the EC2 instance goes down, the DNS resolution should notstop.So that is an unmanaged approach.So this is generally good for learning.In fact, this is great for learning.If you want to go deep down into the DNS, however, it is not really recommended on a longer term unlessand until you have individuals in your organization who are very good into the bind and the DNS relatedareas.And this is one of the reasons why a lot of organizations, they use the managed approach where whatyou do, you let the service provider manage the DNS servers for you and you can go ahead and sleeppeacefully.Here.Now discussing about the managed DNS service in Amazon, Route 53 is a highly available and scalablecloud domain name system web service.So instead of a EC2 instance running bind, you make use of Route 53 and Route 53 benefit.As we are discussing, it is highly available and scalable, so you do not really have to install anyDNS service or configure it from scratch.Let Route 53 do everything for you.So let's do one thing.Let's quickly have a demo on it.So currently I am within the Route 53 console and we have a domain name of labs dot internal.So again, this is just for a demo purpose so that our basic understanding on the Route 53 service isunderstood.Now if you look into the labs dot internal here, there is one specific record of web labs dot internaland the value associated with it is one 7230 10. ten That means whenever a client sends the requeston web labs dot internal to this specific Route 53 service, it will respond with this specific IP.So let's try it out.So I have a EC2 instance here.Let me go ahead and connect to this instance.That's great.So we are connected here.Now if you do a nslookup on web dot Labs dot internal, you see within the output you get the IP addressof one 7230 10. ten very similar to what we have within the route 53 Now do note that this EC2 instanceis sending the request to route 53.The configuration is already done.If you look into the IP address one 7231 0.2.So this is the IP address where the request is being sent and this is the IP address where Route 53is also listening.Now let's also do a nslookup on a name which does not exist.Let's say DB Dot.Labs dot internal.And here it states server can't find this specific domain primarily because our Route 53 does not havea record.So let's go ahead and create a record just for our testing purpose.So I'll just say DB here and you have CP Labs dot internal and within the value, let's just say 192.168.0.5.I'll click on Create record.That's great.The record is created and from the EC2 instance, let's go ahead and do a lookup on Dbcp Labs dot internal.And you see the name resolution works perfectly.That's great.So I hope at a high level overview, you understood the basics of Route 53 and how it provides a greatamount of advantage in terms of a managed approach when compared with an unmanaged approach where yougo ahead and install and configure various DNS server related software like bind on your own.So with this, we'll conclude today's video.