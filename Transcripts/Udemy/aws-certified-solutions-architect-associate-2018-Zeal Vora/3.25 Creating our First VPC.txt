# 3.25 Creating our First VPC

Hey, everyone, and welcome back.Now in today's video, we'll be going ahead and will be creating our first VPC.Now, before we go ahead and look into it, let's quickly revise the basics of VPC.Postage will start with that practicals.Now at a high level overview, VPC stands for Virtual Private Cloud, and it enables us to launch theresources in a virtual network that has been defined.For example, if we have a VPC that is available here and in this VPC, you can go ahead and launchvarious resources like AC two instances and others.Now, since this is a virtual network by itself, one of the great benefits of VPC is that whateverAC two instances that you launch inside this, VPC will be able to communicate to each other using theprivate network.So no Internet will be required for the AC two instance communication inside a same VPC.For example, when you launch to AC two instances in a VPC, these instances will be able to communicateto each other using the private IP without requiring of any internet grid.So for today's practical, what we'll be doing will be creating this VPC and the name of the VPC willbe giving it as demo VPC.And this VPC will also have a CIDR block of 1070 700 slash 16.So this CIDR block basically determines what is the IP addresses that will be assigned to the resourcesthat will be created inside the VPC.For example, since the CIDR block starts with 1077 over here, this would mean that the IP addressesof the instances will also start from 1077.All right.So I hope that this you understood the basics.Let's go ahead and start with our practicals.So currently I'm in the management console from here.Let's quickly open up the VPC console.So this is how the VPC console looks like.You can either go ahead and create a VPC from here or from the left hand side.You can click on your VPC.Now, at this stage, there is a default VPC that is available.So generally, whenever you launch a new EC2 instance, as we know that the easy to instance are launchedinside a VPC, but if you have a new account altogether and you want to directly launch an easy to instance,so in such scenarios, a place by default creates a default VPC in all the regions.So you don't really need to worry about creation of VPC.All right.So this is referred to as a default VPC, and this has a side range of 170 to 31.So any easy, to instance, that you will create inside this default, VPC will have the IP addressof one 7231 onwards.Great.So now let's do one thing.Let's go ahead and quickly create our custom VPC.So within here you have a VPC only and you also have an option for VPC and more.At this stage, I'll be selecting the VPC only the name will be giving demo VPC.Let's go a bit down.The next important configuration here is the IPV four cinder block.As we are discussing, this CIDR block that is associated with the VPC determines what is the privateIP address that the AC two instances will receive.And this is one of the reasons why it is important for us to give the CIDR block here.Now, similar to our configuration, the CIDR block would be 1070 700, SLAs 16.So let's give it, I'll say 1070 700 slash 16 in the IPV six site at BLOCK.We'll just select it as No, we don't really need it for the time being.Once this is done, we'll go ahead and create our VPC.Great.So our VPC is now created and these are all the associated configurations.Now, if you go back to the VPC this time, you will see that we have the default VPC over here andwe have a second VPC of demo hyphen VPC.One thing to note is that the default VPC always will have this side block, which is one 7230 100 SLAs16, irrespective of which region you go into.So let me give you an example.So let's take a different region for today's example.Let's take a region of Tokyo.Now inside the Tokyo region.Also, you will see that you have one VPC that is created and this VPC has a CIDR block of one 7230100 16 grid.Now before we conclude, I have also included a pointer in our PGP so that you can reference it fromthe nodes.First important point is that every region has a default VPC with a side range of 172, 31, zero zeroand 16.This is something that we have seen.And the second point is what is the benefit associated with this?So benefit is that since the default VPC is created, it allows customers to directly launch the ACtwo instances in a device without having to manually create a VPC.So that's the high level overview about creating the VPC.And with this will conclude today's video.