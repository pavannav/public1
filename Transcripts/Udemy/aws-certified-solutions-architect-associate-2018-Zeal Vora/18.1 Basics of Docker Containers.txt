# 18.1 Basics of Docker Containers

Heroin and will come back into this video.We'll be discussing about the basics of Docker, the advantages that the Docker offers, which reallymakes it as one of the great technologies available and also a small demo before we conclude the video.So let's go ahead and understand more about it.Now before we go ahead and understand the advantages that the Docker offers, let's look into a typicalinstallation of software workflow.I'm sure that most of you might have already experienced this with your workstation.Now let's say that we have a Windows operating system and you want to install certain application oryou want to install a certain game.Now the first step is to download the installer.So installer would be a typical dot xy file.Now once you download the installer, you go ahead and double click on the XY file so that the installerstarts to run.Now during the middle of the installation, many of you might have already experienced that.You get an error message during installation saying that certain files are missing or certain dependenciesare missing, etc. So you start to troubleshoot the issue so that you understand the fix to solve thisspecific error message.Now, once you troubleshoot it and once you install a specific dependency, you run the installer andmaybe you get another error, which might not be the same as the previous one.Now again, you go back to the same cycle of troubleshooting it and re running the installer.Now I still remember this type of workflow you typically see specifically when you use to install theGTA game.Now it always used to say that there are certain DLLs which are missing certain dependencies which aremissing and you had to go through this software workflow at least a time or two to make sure that youhave all the dependencies and all the requirements to install the game.Now at this workflow that we were discussing over here, this is the end user workflow.Now there is also a workflow related to the development.Now let's say that this is a programmer who works very hard at developing a software, so he has developedthis specific software Now, depending upon the way in which he has created the software, it mightor it might not work out of the box in the operating system.So let's say that he has created a software through Visual Studio, so it might work directly in Windows.It will not work in Linux, it will not work in Mac.And this is a pain for the programmer because if he wants that software to work in Linux, if he wantsto make it work in Mac, it is really a pain.He might use Java.But if let's say he does not really want to use Java out of the box, it really becomes difficult forthe developer as well.So what he can do, he can create this offer, he can put it in a Docker container.Now he can deploy the Docker container in windows, he can deploy it in Linux, he can deploy it inMac, and he can be 100% sure that the Docker container software that he has tested in his local environmentwill work perfectly in Windows, it will work perfectly in Linux and it will work perfectly in Mac andall the operating system which the Docker supports.So this really becomes much more easier for the developer to push his software to a wide variety ofoperating system and also all the dependencies which are required by this specific software.Because during the second slide we are discussing that a software installation typically fails due todependency issues, etc. He can put all of these dependencies within the Docker container itself.So once he deploys the Docker container to any operating system, it will work out of the box and theend user does not really have to do anything other than running this specific Docker container.So with this, let's understand what Docker is in a definitive terms.So Docker is basically an open platform.Now once we build a Docker container, we can run it anywhere, we can run it in windows, we can runit in Linux, we can run it in Mac, whether it is a laptop, it is in a data center or it is in a cloud.Now it follows build once, run anywhere approach.So that basically means that once the software developer has built his own Docker container, he canrun it anywhere, he can run it in windows, he can run it in Linux, he can run it in Mac as well ina perfectly working condition, depending upon the test that he has performed while he has created theDocker container.So this is the theoretical perspective.Let me quickly give you a demo so that we have a good understanding about this.So for our demo purposes, we'll be taking the example of Engine X.Now Engine X is a very popular Web server in the market.Now we'll consider Engine X to be this software, which has been written by an organization.Now, if you look into the technical specifications of Engine X under the supported distribution, itdoes support Amazon Linux.Then you have S.A as debian FreeBSD.Then you have the redhat Linux, susy, ubuntu, etc. but it does not really support windows.And this is the reason why I intentionally.I have used this specific web server for our demo purpose.Now what we need to ensure is that if we try to install this in windows, it will not really work becauseyou will not have a file out of the box.So what you need to do is you can put this entire engine within a Docker container and once you havedone that, you can deploy the Docker container, even in Windows Linux as well as Mac.Now, again, Mac is not supported here, but if it is under the Docker container, it will work irrespectiveof whether Engine X is supported out of the box or not.Now, the first thing that will typically need to verify whether the engine x Docker container is availableor not.So this is the Docker Hub website and you see there is an engine X container which is available andthis is the official image.So let me click over here and if we go a bit down, you see this is the Engine X official image andbasically it is the command to pull the engine X image.And if you go a bit down, you will see various commands which are available in which you can run thespecific image.So generally there are two steps which are involved generally when you use Docker.The first step is basically this specific Docker container image.It would be in a specific website.Now Docker Hub is one such website, so you have to pull this image or I would say you have to downloadthis image within your operating system where you want to start the container from it.Now for that you have the command of Docker engine.Now, once the image is downloaded, you can go ahead and start the container from it.So let's look into how exactly this would work.Now let me copy this up and from my Windows CLI, I'll paste the Docker pull engine X command.So this command will go ahead and download the engine X image from the Docker Hub repository.Now do remember that before this command works, you need to make sure that you have the Docker daemoninstalled within your operating system.Now, Docker supports Windows, Linux as well as Mac.And basically if you see, oh, here I have the Docker daemon which is running and this is the reasonwhy my Docker command works perfectly.So let's quickly wait for a moment for our image to get downloaded.All right.So our engine image has been successfully downloaded.Now the next thing that you need to do is you have to start the container from that image.So for that, I'll write a simple command of Docker run.I'll specify a port mapping, let's say 88 to the 80 of the container.Let me specify the container image, which is engine X.So it has given us the long output of a year.But if we quickly do a Docker piece, then you should see that there is an engine x container whichhas started up now in order to verify I'm in my igniter mode and I'll put 127001 colon it and now yousee it basically is showing me the index, the HTML page of engine X, So that means my engine is perfectlyworking in the Docker container and I am able to access that within my Windows browser.So this is the great advantage of the Docker containers.You don't really need to worry whether it is supported or whether application is supported in Windowsor not, or whether application is supported in Linux or not.As long as you can put it under the Docker container, you will be able to run it in Windows, Linuxas well as Mac or in fact any operating system where the official Docker daemon is supported.So with this I hope you understood a T high level overview on what Docker is all about.Now one great thing, in fact, the advantages we just saw, one was Engine X was not supported in Windows,but through the Docker container it is perfectly working.And second is we did not really have to worry about installation of Engine X and taking care of allof the dependencies, etc. All we did was we pulled this specific Docker container of Engine X and thatcontainer itself had all the required dependency to run the Engine X web server.And once we pulled it, we went ahead and started the Docker container and that's about it.And this is the reason why Docker as a technology has become extremely popular.And typically if you look into the roles of DevOps or cities, having a good understanding of Dockeris a mandatory thing which a lot of organisations are looking for.And even as a software developer, primarily because of such advantages that it offers, a lot of organisationsare also mandating that the software developers should also have at least basic knowledge about Dockercontainers.So this is the high level overview about the introduction to Dockers.I hope this video has been informative for you and I look forward to see you in the next video.