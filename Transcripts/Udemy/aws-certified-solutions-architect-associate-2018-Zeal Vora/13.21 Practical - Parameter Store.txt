# 13.21 Practical - Parameter Store

Hi everyone, and welcome back.Now in the earlier video we were discussing about the basics of Parameter Store.Today is going to be the practical video.So let's get started.Now from the AWS console you can either directly go to the Systems Manager service or you can also searchfor Parameter Store.You can click over here.Or one of the alternatives is you can go to the Systems Manager service.So if you click on Parameter Store it will directly take you to the parameter store.In case if you are in the systems manager, if you go a bit down, then you have to search for the parameterstore.This will also take you to the same page.All right, now one of the first things as we are discussing is you have to create a parameter.Now what is a parameter.We have already discussed a parameter is any piece of data that is stored in the parameter store.So let's go ahead and create a parameter.Let's give a name.Let's just call it as demo string.Now whenever you create a parameter, you have to choose the type of parameter.So we'll use two examples.One is string and second is secure string.First, let's start with string.Once you have selected it in the value, you can provide any value.Let's say 12345.It can be any value over here.Once you have done that you can click on create parameter.This is a simple string based parameter.Now let's create one more parameter.Let's just call it as demo secure string.And within here the type will be secure string.As soon as you click on Secure string, it will ask you to select the appropriate KMS key, becauseit will use the KMS key to encrypt the data and decrypt the data when you try to read it.So I'll just choose whatever default KMS key that is available in the account.Now you'll have to specify the value.Let's just call it as Super secret password.and I'll create the parameter here.Now one of the differences, as we know, is that when you open up the string based parameter, youwill directly see the value over here.This is a simple one.However, when you go ahead and open the secure string based parameter here, you see the value by defaultis not shown.So you have to explicitly click here to decrypt the value.And this is the value.Now again you should have the KMS decrypt permission in order to be able to see the value.Otherwise things will not really work.Similarly, whatever application when it tries to fetch some values, for example the db password,then the IAM role associated with the EC2 instance where the application is running that IAM role shouldalso have the required level of KMS permission for the decryption to work.Otherwise you will get an error.All right.So that's a very high level overview about the parameter store practical.And with this we'll conclude today's video.