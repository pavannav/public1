# 4.12 S3 Versioning

Hey, everyone.And welcome back.Now, in today's video, we'll be discussing about the feature of S3 versioning.Now, if you have used Git, you'll understand the benefits that the version control system providesto us.Now, in a similar way, the feature of versioning is typically absent within the storage devices dueto which there can be a lot of issues that would be faced.And to overcome this, AWS has released the feature of versioning for the S3 storage.Now before we go ahead and discuss more in detail about versioning, let's look into the typical challengethat might be faced considering that the versioning is not available in a storage device.So let's assume that you have a storage device on the right and you have the file on the left hand side.The name of the file is a dot txt.So now what you do, you go ahead and store this file into the storage device and the file gets stored.Everyone is happy.Things are working fine.Now let's say some other user from your team only also has created the file with the same name but withthe different contents altogether.And he also decides to store the file in the same storage device.So now what will happen considering that the folder structure is same, the older file of a dot txtwill be removed and it will be replaced by the newer contents of the dot txt file here.The second primary challenge is related to the accidental deletion of objects, so let's assume thata dot txt is stored and the user has sent a delete file operation and this file gets removed.Now in case if there is a need to recover this file back from the storage devices, this is somethingthat does not really work well.So in order to overcome these two primary challenges, you have the feature of versioning within theS3 storage.So at a high level overview, versioning allows users to keep multiple variants of an object in thesame S3 bucket.So now here you see you have an S3 bucket and considering that the versioning is enabled, both theversions of a dot txt are stored within the storage system.Now, along with that, you can also make use of versioning to preserve, retrieve and restore everyversion of every object that is stored within your S3 bucket.So let's do one thing.Let's go ahead and directly jump into practical and understand more in detail about it.So currently within the S3 console, let's go ahead and create a new bucket.Let's just call it as my CP Labs hyphen version.Make sure to use a different name from your side.And here, let's create a bucket.All right, so our bucket is ready.Let's open this up Now, by default, the versioning is not really enabled within the S3 bucket here.Now, before we continue, let's quickly look into how Windows file system behaves for the objects withthe same name.So currently I have a new file with the name of a dot txt.Now if I try to create another text file with the same name, let's try to use the same name over hereand immediately you see it states there is already a file with the same name in this location.So apparently you cannot really have two different files with the same name over here.Now, similar would happen in the S3 bucket as well.Now in our S3 bucket.Let's go ahead and try it out.Let's do an upload.I'll add a file and let me add a dot txt over here and I'll do an upload.Perfect.So our file got uploaded.If we'll quickly look here, this is our a dot txt file.Now if you look into the contents of the a dot txt it basically has a simple content of version one.Now let's assume that we change this file content and we make it to version two similar to what we wereseeing.You have the base file of version one and someone from your team also creates the same file and triesto upload it to the storage device.So now let's go ahead and upload the version two.Let's upload it.Great.So the file got uploaded.And this specific file, this is the version two file.So now what happened?The version one got replaced with the version two of the file.And the version one of the file is lost in the storage device.And along with that, if you go ahead and decide to delete this content, let's go ahead and try itout.So let's say permanently delete.So now there is no way in which you can retrieve the file back.So these are the two challenges that we have seen.And in order to overcome this, we make use of the feature of versioning.Now if you go to the bucket properties and within here you have bucket versioning feature available,let's go ahead and enable it.So by default, here you see it is disabled.So let's go ahead and enable the versioning here and I'll click on Save.Perfect.Let's go back to the objects.And now let's go ahead and upload our object yet again.So I'll do an upload.Let's add our dot txt here and we'll do an upload.So currently one thing that you will see is that you got a new option of show versions over here andif you select it here along with the name of the object, it also shows you the version ID.So this is the version ID that is associated with the object.So the version ID ends with o X.So now let's go ahead and modify our a dot txt here.We'll change it to version three and let's upload the file.So I'll add a file.Let's upload the same a dot txt over here.Perfect.So here you see you have a dot txt.If you click on show versions here you see it basically shows you two versions of the file.One is the o x, which basically was the version two and you have the f h and this is the latest versionthat was uploaded in the S3 console in case if you really require the previous version, that is alsosomething that can be retrievable easily.All right.So that is the solution associated with the challenge.One where you can have multiple version objects of the same key.The second challenge that we were discussing is related to the accidental deletion of objects.So let's deselect the show versions.Let me go ahead and do a delete over here and let me type a delete.Now do note that this is delete and not permanent delete.All right, So now it states that the deletion is successful.Now at this stage it does not really show you any object, but if you select the show versions overhere, you will see that you have a deletion marker that is associated and it is also showing you theversions you have o, x and f h.So whenever you go ahead and delete the object, there is a deletion marker that is associated withit.And this is the reason why if you deselect the show versions, the object will not really appear.But in case if you want to retrieve it, this is something that can also be done.Now, along with this, you also have the option to permanently delete the file.So considering that the show version is enabled and you select the appropriate version and you clickon delete, this time it will be a permanent delete and this file cannot be retrieved.It is also giving you a warning stating that the deleting the specified objects cannot be undone.So there is a difference between the delete and a permanent delete and this is something that needsto be noted.Delete will happen considering that the show version is deselected permanent delete will happen, consideringthat the show version is selected and you try to delete a file.Great.So with this base set, let's also discuss some of the important pointers for versioning.First important point here is that once you version enable a bucket, it can never return to the Unversionstate.You can, however, suspend the versioning on the bucket.This is very important part to remember.You cannot go ahead and remove the versioning of the S3 bucket completely.So let's also look into it.So if you quickly look into the bucket properties over here, currently the bucket versioning is enabled,let's go and try to disable it.So when you try to disable it, it basically does not have a option.The only option that is present is suspend.So when you read the description, it states this suspends the creation of object versions for all theoperations, but preserves any object versions over here.So let's go ahead and try it out.Let's look into what it tries to do.So let's do a save change.So now what really happens here is that you will still have the show versions option, and if you clickhere, you will still see the data over here.But for the newer file that you upload, let's try it out.Let's upload the new text file.And now for this newer file, you do not really have the versioning enabled.So if I click on show versions here, you will see that for this new text file, the version ID is nullbecause there is no versioning associated with this specific object.Only the older object will have the versioning ID.Great.So that is what the first point is all about.The second point states the versioning state applies to all of the objects in the bucket and it doesnot work on the nivosum approach.So if you have enabled the versioning in the S3 bucket, all of the objects that you upload will havethe version ID and the versioning capabilities associated with it.Great.So with this, we'll conclude today's video.