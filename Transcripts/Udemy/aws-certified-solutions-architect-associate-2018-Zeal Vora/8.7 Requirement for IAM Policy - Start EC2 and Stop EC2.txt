# 8.7 Requirement for IAM Policy - Start EC2 and Stop EC2

Hi everyone, and welcome back.Now in today's video, we are going to take up one of the requirements related to the IAM policy thatwe have to create from scratch.And what is that requirement?It is related to start and stop of EC2 instance.Now this specific requirement will allow us to understand on how IAM policies are created and also someof the important details related to it.So if you understand the use case, it states that you have a Alice that is an IAM user, and thereis also a EC2 instance that is running.Now Alice should be able to perform the start EC2 and stop EC2 action on this specific EC2 instance.So what we have to do, we have to write a policy from scratch that allows the Alice user to performthese operations.Now your few things that needs to be noted is that in a given region, there can be, let's say other50 EC2 instances.So Alice should not be able to stop all of those 50 EC2 instances.Only one very specific EC2 instance that is running Alice should have the basic control over it.Now here it states that the instance id I example.So whatever instance id that this EC2 has, the Alice should have permission over it.Now let me also quickly show you this.So if you see whenever you create a EC2 instance in AWS, each instance has its own unique ID.So this ID ends with 531.Similarly, there is one more EC2 instance here called as Bob EC2 and the bob has a ID is AF4.So the requirement states that the Alice user should only be able to start and stop the EC2 instancewith a specific ID.Now note that whenever you create a EC2 instance in your environment, it might have a different ideathat does not matter.And this is the reason why we have a variable here of I example.Now as part of this requirement, these are the steps that you need to follow to achieve the end toend lab.Now, before we go ahead and proceed with the practical in the next video, I would really encourageyou to try things out so that you learn how the IAM policies are created.Now, if you look into the steps that you need to achieve as part of your lab, first one is you haveto create a EC2 instance for test purposes.Second is you have to create a IAM user of Alice.Then you have to create a IAM policy that allows start and stop EC2 instance for the instance that iscreated in the step one.All right.So you have to create the right set of Arn that matches the EC2 instance resource.And you also have to ensure that whatever base policy that you have written, it also works with thestart and stop actions that are performed through the AWS console.All right.So it should work both at a CLI level as well as the console level.Now before we conclude this video, I would like to show you a small demo on what exactly is expectedat the end of practicals.Now for today's demo, I am logged in with the Alice user in the AWS Management Console.Now if I quickly go to EC2, let's go to instances within.Here.You see, Alice is able to see both of the instances that are currently available.However, first let's go ahead and verify if Alice is able to start the Bob EC2 year.So when I do a start, you see immediately we are getting an error.This is fine and expected, so Alice should only be able to start the Alice EC2 instance.So let's also verify that.So when you try to start you see now it states successfully started.Let's quickly wait for a moment here.And now you see the instance state is running.So this is the one action that is allowed.The second action that needs to be allowed is stop instance.Let's verify.And you see it gave us the success message.And the instance now is in the stopping stage.All right.So I hope with this you understood what is the requirement based on which you have to create a IAM policy.Go ahead spend some time and create the appropriate IAM policy.And we look into the appropriate solution in the next video.