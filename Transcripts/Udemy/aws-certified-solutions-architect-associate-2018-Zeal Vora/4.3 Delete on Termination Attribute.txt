# 4.3 Delete on Termination Attribute

Hi, everyone, and welcome back.Now, in today's video, we are going to discuss about delete on termination attribute associated withthe EBS volume.Now, at this stage we already know that the EBS and ESI two instances are separated entities and theycan live independently of each other.For example, if we have an AC for instance, and there are two EBS volumes that are associated withit.Now when this AC two instance gets terminated, the volumes that are present, they live separately.So we can configure that volume can continue to stay irrespective of whether the instance is runningor terminated.Now, when we discuss about the independence of EBS volume and the AC two instance, there is one importantattribute of delete on termination that we need to know.So whenever an instance terminates or whenever you delete an easy to instance, depending upon the valuethat is associated with this specific attribute, which is delete on termination.Even in this screenshot you see you have a delete on termination set to Yes, depending upon this attribute,the EBS volume.The default EBS volume that is associated with the instance can continue to stay or it will be deletedaccordingly.So let's go ahead and look into it now from the AC console.Let me quickly launch an instance here.I'll just call it s test.Let's just select the key pair.I'll just say proceed without keep it.And we are more interested in this storage section.Let's expand the storage section here.Now with India, you have an option of delete on termination.So in such case, what will happen is the EBS volume that was attached to the ec2 instance during thecreation process.If the ec2 instance terminates, even the EBS volume is deleted.Let's go ahead and look into it.So let me just call it as a demo zero one just for simplicity, I'll just launch the instance.In a similar way.Let's launch one more instance.I'll just call it the Demo zero two.Let's go a bit down deeper.I'll just say proceed without.Keep it.In these stories.I'll just say at once, we'll expand this and this time delete on termination will set it to know andwe'll launch one more instance.All right.Let's go back.So there are two instances that are currently available.Now, if you look into the volumes, you see that there are in total of two volumes that are availableand both of them are in use.Now let's go ahead and terminate the easy, for instance, number one.And this instance had a delete on termination attribute set to Yes.So ideally what would happen is that if the easy, for instance, is terminated, even the EBS volumethat was attached to it by default, that volume will also be deleted.So if I refresh now, it states terminated.So if I go to the volumes here, let's refresh this.You see now only one volume is present.Now, in a similar way, let's go ahead and terminate the instance.Number two, let's quickly wait for a moment here.So the instance number two is also terminated.If we look into the volumes.Let's refresh.You see the volume continues to be present.Great.So I hope that this you understood the importance of delete on termination attribute and with this willconclude today's video.