# 4.22 Practical - S3 File Gateway

Hi everyone, and welcome back.Now in the earlier video we had a great introduction to the S3 File Gateway.Today is going to be the practical video where we will test the end to end workflow.Now for today's practical first let's go ahead and set up the file gateway.So for this we'll go to the Storage Gateway service.And within the storage gateway first we will have to create a new file gateway.Now here there can be a slight confusion because at this stage it shows that AWS Storage Gateway FSXFile Gateway is no longer available.So this is different from the S3 file gateway that we are discussing.So in case if you see it you can ignore it.So let's go ahead and quickly create a gateway.The name of the gateway.Let's just call it as S3 File Gateway.Now within the gateway options you have the S3 File gateway, tape gateway volume gateway.We'll be selecting the S3 file gateway.And this can either be an on premise because this is like a appliance.So you can either create a virtual machine in the on premise server, a data center, or even an EC2instance.So in the platform options, we'll be selecting EC2 options so that we can quickly test it.Once you've selected the EC2 options, then you can select the VPC and the appropriate subnet in caseif you'd like, and also the key pair in case if you'd like to log in.Once you have done that let's click on next.Now at this stage you see it is giving a slight error which states you must launch the instance beforemoving to the next step.So what this will do is it will create an EC2 instance with the appropriate file gateway software.So the setup will be created for you.So I'll go ahead and I'll launch the instance.So this is the file gateway specific EC2 instance.It will create the necessary security group and everything for us.And this makes the life simple.Alright so it took around one minute and our EC2 instance with the appropriate software is configured.Let's do a next.It is showing you the IP address.We'll just leave it as default in case if you'd like you can also make it VPC hosted.Currently we are making it publicly accessible.Let's do a next in the activate gateway over here will go ahead and activate the gateway.So it says successfully activated the gateway.This is good.Now you have some further configuration options specifically related to the CloudWatch that you cando.We'll keep it simple.We do not really need the logging because this is our test setup.We do not really need the alarm.Now one more thing before we proceed further, if you look into the EC2 instance for the file gateway,you see the instance type is M5.So this is a very big EC2 instance.So this is one part that you have to keep a note of.Even if you look into the storage you have two volumes.One is of 80 give second is of 150 give.So this is quite big volumes for the test setup.But generally for the larger setup it is expected.You can also have the caching related configuration that you can add once.Then you can click on configure.So at this stage our S3 file gateway is created.Now we can go ahead and create the file share.So basically once the file gateway is created this file gateway must be associated with the S3 bucket.So all of that configuration you will have to do.Along with that, the file gateway can also work at NFS protocol as well as the SMB protocol.So for which protocol you want it to work is something that you will have to decide.NFS is primarily for Linux, SMB for windows.So now let's go ahead and create a file.You can either do it from here or you can go to File Shares.You can create a file share.You can specify the gateway the file share protocol for NFS, which is for standard for Linux environment,and SMB, which is standard for windows environments.And now the most important part to which S3 bucket you want it to get connected with.I already have a lot of S3 buckets.I'll just use any one of them.Let's just use this demo website in case if you have any S3 bucket, you can select that.Let's go ahead and create a file share.Great.So once this is completed let's quickly go up.It says your file share has been created.Now the most important part is that you have to mount it to the Linux server.And here it shows you the command on Linux this is the command to mount it.So let's go ahead and test it.So for testing what we are going to do we are going to launch one simple EC2 instance over here.Let's just call it as demo EC2.We'll use a very minimal setup over here.Let's go ahead and launch this.Let's go to the EC2 instance here.So this is the instance which is demo hyphen EC2.You can ignore the EC2.This was used for one more other practical.So let's quickly wait for this instance to get created.Great.So the instance state is running.Let's go ahead and try connecting to the instance.Here I'll copy the public IP.Let's quickly connect using the appropriate key.Great.We are connected.Now let's go ahead and copy the mount command over here I'll copy this first.Let's go ahead and quickly create a folder.Let's call it as S3 bucket.This is a local folder.All right.So we are not in the root user.So that's the reason for this.Permission denied.Let's run the same command again.And now we'll run this mount command.And in the mount path we'll have to change it to the folder path that we had created.In our case it is S3 bucket.I'll press enter and that's about it.So if you now go to this specific path of S3 bucket, if I do a ls, you see all of the files that arepart of the bucket are now accessible.Let's also verify it in S3.Within here I have this bucket.Let's open it.And you see these are all of the files that are available now.You can also go ahead and create a file.Let's just call it as new file dot txt.Let's do ls.Oops we made some mistake here.Let's create one more file.Let's just call it as new file dot txt.And let's go ahead and add some data.Let's say hi to the new file dot txt.And that's about it.Once you have done that, let's go ahead and refresh from here.And you see you have a new file dot txt that got created.If you download this file, if you open it, you see we are getting the message.So that's the overview about the S3 file gateway practical.Now again in case if you have tested this in your environment, make sure that you go ahead and deletethe instance.Because these are very large instances.And in case if you forget, then at the end of the month you will get a very big bill.So this is essential.In fact, let me go ahead and do that so that we have the entire workflow tested.I'll go ahead and terminate both of the instances along with that in the file share.Let's go ahead and delete this specific file share.And then you can go ahead and delete the gateway.And that's about it in terms of practical of S3 file gateway.And with this we'll conclude today's video.