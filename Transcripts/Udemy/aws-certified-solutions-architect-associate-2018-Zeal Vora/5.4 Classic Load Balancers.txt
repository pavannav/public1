# 5.4 Classic Load Balancers

Hi everyone, and welcome back.In today's video we are going to discuss about the classic load balancers.Now, one important point to note is that the classic load balancers are the previous generation ofload balancers as part of the Elastic Load Balancing service.Now, when the service was first launched, the only type of load balancer that was available duringthat time was Classic Load Balancer.Now, there are various other types of load balancers that are available for very specific use cases.For example, when you go to the load balancers over here, whenever you create a load balancer, yousee there are multiple type of load balancers, application load balancer network.You have gateway and then you have a previous generation load balancer of classic load balancer thatis available now.Irrespective of what type of load balancer that you are using.The main aim over here to load balance the traffic.This part remains to be the same.It's just that the functionality related to it might change.For example, some of the load balancers like application load balancer, they can look into the HTTPheaders based on headers.They have a lot of flexibility whether to route the traffic, whether to return the fixed response,etc. so a lot of flexibility is available for today's video.We are going to have a short practical on the classic load balancer to understand the workflow.So let's go ahead and get started.Now what we are going to do as we are discussing for the load balancer, the first thing that you requireis the backend, which is the server.If you don't have a website that is running, what will the load balancer do?So today what we are going to do, I'm going to quickly create a new EC2 instance over here.Let's just call it as website hyphen EC2 from the Amazon Linux Ami whatever free tier eligible instancetype is there, you can use that for Keypair in case if you have Keypair, you can use that and restother things.I'll just leave it as it is.Let's go ahead and quickly launch the instance.Once the instance is launched, let's go back to the instance.Let's refresh.Now.One thing to note here is that the security group, because if the instance is hosting a website, websiteuses HTTP or Https, so you require the port 80 to be open.So whatever security group that is associated with the EC2 instance over here, this security groupshould also allow port 80 traffic from say everyone.And I'll click on save rules.All right.So this is the first important part.The next important part is that although the EC2 instance is running it does not really have any website.So what we are going to do, we are going to quickly install nginx on the EC2 instance so that we havesome form of website post which will be creating the load balancer.So let's go ahead and try to connect to the instance.Now again note that there are multiple ways to connect.You can either use EC2 connect.Sometimes this does not work for some users.So in such case you can use your private key to login to the server through SSH.We have already explored multiple ways earlier.So currently I am connected to the EC2 instance here.What I am going to do I am going to quickly install nginx, so I'll run a command called Yum.Install nginx over here.So we made a small typo.It should be nginx instead of this.I'll do a ctrl c.Let's try again.Yum.Install nginx.I'll just press Y.I'll press enter.Great.So nginx is installed.You can go ahead and start the nginx service.Once engineering services started now we are going to copy the public IP address of the instance toverify if there is any website from the new browser tab.I'll add the IP.However, one important part just add HTTP because some browsers they'll explicitly send a request tohttps.So I'll press enter.And you see we are getting welcome to nginx page.This is good.This means things are working.So now that we have the backend configured now we are going to create a elastic load balancers usingthe classic load balancer type.So now in the load balancers over here.Let's go ahead and create classic load balancer.So you have two options.You can either create the classic load balancer from here.Or you can click here.And from here you can create classic load balancer.Again AWS changes the UI quite often.So whichever way works you can use that.Now in the load balancer name let's just call it as classic load balancer.Now in the availability zone, one thing that you have to keep a note.In fact let's go back to the EC2 instance the subnet in which the EC2 instance is created.You have to select that as one of the availability zones.So within here the subnet is CF2.So whatever availability zone has CF2 that is one you have to select as a minimum.So this is CF2.So this is one that we'll select.And one more you will have to select.All right.So total two minimum.Once you have done that in the security group we'll just use the same security group that is associatedwith EC2 instance which is Launch Wizard one.So you can verify you had Launch Wizard one here.Now, why we are using it?Because you have port 80.That is allowed.And even at a load balancer level, you require port 80.All right.So this is one part.Now in the health check in the pink part instead of index.html you can just put a route over here orslash.Now the next important part is the instances in the ad instance.You can select this specific instance and confirm it.Once this is done let's go a bit down.Let's go ahead and create the load balancer.So it has created the load balancer over here.Now note that it can take a little amount of time in terms of provisioning.So let's go ahead and quickly click on the load balancer.Now within here it says zero out of one instance in service.That is fine.It takes light amount of time for things to be ready.So let's give it 3 to 4 minutes pause which will test the workflow.All right so it has been around two minutes.Let's go ahead and refresh.And here you see you have one out of one instance in service.This is good.Now the next part is you have to make a request to the load balancer and not directly to the server.This is important point to note.So you have the DNS name of the load balancer.You can copy the DNS name from a new tab.Let's go ahead and send a request to the load balancer.Let's press enter.And you see we are getting welcome to nginx page.And in a similar way you can make a curl request directly from your terminal.And you get the HTML page of Welcome to Nginx.Now one more point that you have to keep a note is that since classic load balancer is the previousgeneration of load balancer, it does not really have a lot of advanced features over here.And this is the reason why AWS recommends only the classic load balancer in case if you're using EC2classic.If you are using VPC, which is generally common in most of the organizations now, it is recommendedto use the next generation of load balancers, like the application or the network or the gateway basedon the requirement.For example, some of the features that are missing here is that it does not support the native HTTPtwo protocol.then the IP addresses targets is also not supported.Then you have path based routing that is not supported.For example, let's say you type example.com slash images.So you want that request to go to server one.However if someone types example.com that request should go to server two.So this type of path based routing, the header based routing etc..All of this is not really available in the classic load balancer.For this you will have to use the next generation of load balancers.Now before we conclude this video, one more very important point to note is that whenever you are workingwith Load Balancer, make sure that after you have completed the practical, you go ahead and you deletethe load balancer.Otherwise, even if you're using a free tier account, you will be charged at the end of the month.So this is one very important point that you have to keep a note of.And again the instance if you have you can go ahead and either stop it or terminate it based on therequirement.Now one more thing before we conclude in the official website related to AWS, they provide you detailsrelated to various load balancers and what type of features are available.For example, you see the application load balancer has so many features that are ticked.However, the classic load balancer does not really have them.And again, you see there are many of them.Slow start your IP address static elastic you have the privatelink support etc..So half of the things I would say, almost 70 to 80% of the things you will see is missing in the classicload balancer.Basic functionality is available.For example, this is something that we were discussing that supported network platforms.VPC is the newer one, and all of the next generation load balancer supports it.This classic load balancer, although it supports VPC, but it also supports Ec2-classic.EC2 classic is very old.Maybe some organization might still be using that, but apart from that, none of them uses it.But in case for them, the classic load balancer is the only choice.And again, as you are discussing, the amount of features are quite limited in the classic load balancer,but in a very specific use case it can be useful.So that's the overview.And with this we'll conclude today's video.