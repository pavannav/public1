# 12.6 Creating Read Replicas

Hi, everyone, and welcome back.Now, in the earlier video, we were discussing the basics of the read replicas.Now, in today's video, we'll go ahead and try it out practically and we'll look into the implementationsteps.Now, currently, I'm in the ideas console.The first thing that we need to create is the primary database.So once the primary database is created from there, we can go ahead and create a read replica.So let's go ahead and quickly create a database.Now, we'll keep it very simple.I'll just say my SQL.Will use the free tier one.Let's go a bit down.Let's go ahead and supply a password here.Let's go a bit down within the storage.Instead of 200, I'll just say 20 GB.Let's go a bit down.We'll leave everything as default.Let's go ahead and create a database.So the database is being created.It takes around 5 to 10 minutes for the creation to complete.So let's quickly wait till the database is available.All right.So it took around 10 minutes and our database is now available.So in order to create a replica, you can go ahead and go to actions and click on Create, read replica.Now, the read replica that you create, it can have a little different set of configuration from theprimary instance.For example, it can have a different DB instance class.So although the primary database instance might be the three micro four read replica, if you're expectingthat there are a lot of read requests that will be upcoming, you can have a little different instancetype.This is also something that can be done.Along with that you can specify a different destination region.So the primary database we had created in the Ohio region, So read Replica.Let me just select Singapore region just for simplicity and testing.All right.Once this is done, let's go a bit down in the read replica source.You have the database one in the DB instance identify you can give a different name.Let's just call it as database one read replica.All right.Once this is, then you can go ahead and click on create three replica.Great.So at this stage it states that the replica creation has been initiated.So let's quickly do a close.The status has changed from available to modifying a file.Quickly open up the database.Let's go a bit down.You should see that under that application you have the database one read replica that is availablein the Singapore region.Along with that, let's also quickly open up the database in one more tab.And we'll switch to the Singapore region.And you see you have the replica and the status is creating.Once this is available, then you will see the replication happening.So whatever you create in the primary database will be replicated in the replica as well.Great.So I hope with this you understood the basics on how you can set up the read replica in ads.Make sure that if you have initiated the practical, make sure to delete the read replica once it iscreated and also to delete the primary database to avoid the unexpected cost.So this will conclude today's video.