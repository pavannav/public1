# 9.28 Overview of EC2 Instance Metadata

Hi everyone, and welcome back.Now in today's video we are going to discuss about the EC2 instance metadata.Now this specific feature is quite important in wide variety of use cases specifically related to theautomation or dynamic set of applications.So with this said, let's go ahead and look into the basics of it.Now at a very high level overview.The instance metadata is data about your instance.Now, whenever you launch a EC2 instance in AWS, you see there are so many configurations that youwill have to set.You have Ami, you have instance type key pair.Then you have the VPC security group related configuration storage.Now in advanced details also there are so many options.You have the domain IAM role hostname.You see there are so many options that you will have to set to create an instance.If you do not set, there will be some defaults that will automatically be applied.So all of these things that we are seeing here is what is referred to as a data about your instance.So this instance metadata is data about your instance.Now how does it look like.For example you have a EC2 instance that is launched now through the instance metadata service.You will be able to see all of the metadata about the instance like the Ami id hostname, IAM role,the instance ID, the Mac address, IPv4 address, and so many other details about the instance.So let's do one thing from demo perspective.Let's look into how exactly this looks like.Now for today's demo I have a EC2 instance that is running.Let's quickly connect to it now.Again, note that this is a demo video where I just wanted to ensure that the overall concept is understoodproperly.Now from the EC2 instance, if you'll quickly do a curl on a specific address, let's say 169254169254.So this is a special address to access the metadata about the instance.You see there are a lot of metadata available now.For example, I want to find the instance ID associated with this instance or a Ami associated withthis instance to understand from which operating system this virtual machine is launched from.So you have a specific option of ami ID here.And if I press enter, you see, this is the exact emi ID, which is D0D.And if you want to verify this.You can see you have Amazon Linux and Ahmed is D0D.So all of this information that you see over here is also accessible through the special URL associatedwith the metadata service here.Now the question that comes is we are already able to see all of the details associated with the instancefrom the AWS console.Why do you need to use some kind of a special URL in the instance to fetch the metadata associated withit?Let's understand this with a use case where this kind of a approach is very beneficial.So let's assume that you have a sample company.And this sample company has designed a dynamic application.And why it is a dynamic application.It is because it adjusts its resource capacity based on the instance type.Now whenever you launch a instance in AWS, there are so many instance types that are available.If you see there are so many instance type.For example, T2 micro has one Gib of memory some instance type.If we go and explore, see five dot 18 xlarge it has 144 Gib of memory.So depending upon the instance type the CPU and the memory configuration will change.All right.So this specific dynamic application has the capability to automatically adjust the resource capacitythat it uses based on the instance type where the application is installed.For example, if the application is installed in a T2 based instances, it uses maximum of 50% of theCPU usage.If the same application is installed in a m5 based instance, it uses 100% of the CPU usage here, soto run the application optimally, it needs to identify the instance type of the EC2.Now the question that comes is how can the application understand what is the instance type of the EC2where it is installed?It does not have access to the AWS console similar to us.It can also happen that the instance does not have a IAM role, through which it can authenticate toAWS and fetch the data.In such cases, the application can access the instance metadata and can identify the instance typethat the EC2 is based on.So if you see in this screenshot also there is an option of instance type here.So you can also try this out I'll say instance type.And you see we got the instance type here.Similarly application can also send a request to this specific URL.Identify the instance type and based on which the resource capacity can be adjusted automatically.Now again note that it all depends whether the instance has the access to the instance metadata service.It can happen that these commands are not working for you, because the access to the instance metadataservice is not available specifically.So similar to what we were discussing, the application can send the request to the instance metadataservice to retrieve the instance type, and based on this it can automatically configure the resourceusage.All right.So that's a very high level overview about the EC2 instance metadata.At an overview.There are a lot of use cases where this proves to be very useful.We explored one use case related to the dynamic application.So with this we'll conclude today's video.