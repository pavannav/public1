# 18.10 AWS Fargate

Hey, everyone.And welcome back.Now, in today's video, we are going to discuss about a target.Now, before we discuss about Target, let's look into the traditional approach that used to be commonbefore target service was introduced.Now, in traditional approach, let's say you are using a container orchestrator.It can be X, it can be Kubernetes and so on.Now, for this container orchestrator to deploy an application, there needs to be an underlying resourcethat needs to be created, for example, easy to instances.And only after the easy two instances were created, then the container orchestrator would have beenable to deploy the containers inside the easy two instances in your environment.Now, although this is a pretty common approach nowadays as well, however, it comes with its own setof disadvantages.First one here is that you have to define and deploy the AC to instances in your environment.So let's say there is a lot of traffic that is coming to your environment.You might have to launch 20 or 30 C two instances or maybe more or maybe less and so on.This is one part.Second important part is you have to take care of the security of the AC, for instance, so it canhappen that AC, for instance, gets breached.So you have to take care of vulnerability assessments, security groups, the oral patch managementand so on all.And the third one is you have to manage the AC two instances in your environment and you also have topay for the AC two instances that are running.Now, although this is fine for most of the environments, the question that comes is, is there aneasier way to do this?And the answer is yes, true for good.Now, generally in forget what really happens is that all the AC two instances that you had to manageearlier, you no longer have to do that.Everything is taken care by the target.So at a high level overview, a target is a serverless PSG compute engine that lets you focus on buildingthe applications without managing these servers.So now container orchestrator can directly deploy the containers and forget will take care of everything.So this is a much more better architecture here.So let's go ahead and quickly look into it.Now for today's demo, I have an X cluster here and within here you see there is one task pending.So basically a container is not being deployed.So this is this cluster is trying to deploy a container.But the challenge is that there are no easy two instances that are available.And this is one of the reasons why you see it states zero out of one task over here.So this is one part.So let's go ahead and quickly delete this service.And now what we will be doing, we'll try and deploy the container on a target service.So let's also try it out.Now, again, note that this is a demo video.So generally you have an option of AC two.You also have option of target in case if you choose easy to.As we know, you need to have an AC, for instance, where the container can be deployed in case ifyou use forget, you do not really need to define anything here.Within you.Let me just select the far gate here.I'll just give the name of taste and let's go ahead and deploy.Let's refresh the page.As of now, it is trying to deploy.The status is in progress.So let's quickly wait for a moment.And if a refresh here, you see our container has been successfully deployed.Now, if you look over here, let's quickly open this up.You basically get a public IP.Let's copy this.Let me enter it over here.And you see, our application is successfully running in a target environment.So I hope that this at a high level where you you understood the basics of target.One of the primary benefit here is that since it is a serverless compute, we no longer have to takecare and manage the easy to instances for our environment.And this leads to a much more simpler approach.So with this will conclude today's video.