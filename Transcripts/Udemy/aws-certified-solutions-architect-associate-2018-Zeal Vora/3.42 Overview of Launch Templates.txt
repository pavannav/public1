# 3.42 Overview of Launch Templates

Hi everyone, and welcome back.In today's video we are going to discuss about launch templates.Now one point that we have to keep a note is that whenever you launch an EC2 instance, there are 30to 40 different configuration settings that you can add depending upon the requirement.Now, in most of the cases, user will simply use the default settings and launch the EC2 instances.Let me show you this before we proceed further.Now I'm in the EC2 console.Now when I go ahead and launch a EC2 instance, most of the times what we do is we add the name of theEC2 instance, we select the Ami, then we look into the instance type.Then we select the key pair.This is the most common settings that users will use.Maybe sometimes you might configure the security group from here itself.Now there are other configurations that are available.For example VPC subnet.Then you have the storage configuration.Now if you look into the advanced details you have settings related to domain joining IAM instance profile.Then you have the instance recovery shutdown behavior termination protection and so on.Detailed CloudWatch monitoring is also quite important for the production environments and so on.So the thing is, many of the settings that you see in the advanced details are very important fromthe perspective of production environments and in other environments.Also it will be useful.Now the issue is that every time user has to launch an instance, he has to go ahead and look and configureeach of the settings based on the requirement.And this can take time.So maybe DevOps engineer might know the importance of each configuration.But when you have a developer user he will not really know what exactly each of these configurationmeans.So what you would like is as part of the best practice, you want that whenever a user, maybe developer,user or any other user tries to launch a EC2 instance, these options are pre-selected based on thebest practices that the organization follows.So in order to achieve this, you have the feature of launch template that can be used.So at a high level overview, the launch template is used to store the instance launch parameters sothat you don't have to specify them every time you launch the EC2 instance.So whatever important parameter that you might have as part of your organization, maybe the Ami ID,the security group, the key pair name, the termination protection, shutdown behavior, etc. all ofthem you can configure as part of the launch template.And next time, whenever you want to launch a EC2 instance, you use this launch template as a referenceand create EC2 instance.So in such case, once the launch template is ready, any EC2 instance that is created from this templatewill inherit all of the configurations that you have added.So this is the basics.Let's go ahead and quickly look into it.Now from the EC2 console you have the option of launch templates.Again, note that this is a demo video.I just wanted to show you the importance of this specific feature.So when I click on Launch Templates, you see I have a specific launch template that is available nowif you see over here.In fact, let's maximize this.If you see this specific launch template has certain configuration like the Army ID, the instance type,the security group ID, the key pair name.Then in case if you would like, it can have storage configurations.It can have network interface configuration.Then in advanced details you can configure various things like terminate protection.You can have placement groups, shutdown behavior and so on.So lot of configurations you can add as part of the launch template.Now after the team has created the launch template to be used for the entire organization, what usercan do.User can simply launch a EC2 instance by referencing this launch template.And he does not really have to worry about configuring each of these values individually.For example, if I click on actions, I can launch an instance from this template.Now, when you launch an instance from this template, you see all of the configurations are pre-selectedfor you the EC2 key pair, the instance type, maybe the ami ID, and so on.So all of these configurations will be pre-selected for you.You don't really have to worry about that.All you have to do is you have to click on launch instance and the instance is successfully launched.Now if you click on Launch instance here you see this EC2 instance also has the tag that is associated.So you don't really also have to add the tag here automatically based on what the launch configurationhas.The values will be inherited for you.Now, before we conclude, one more pointer that you have to keep a note is that launch template supportsversion.So for each launch template you can create one or more numbered versions.And for each version you can have different parameters.For example you see you have a launch template.Now for this specific template you have three versions and each version has different set of parameters.So for example the version two has a parameter of t two.Micro version three has one of the parameter as t two medium for the instance type.So depending upon the configuration that you require, you can select the appropriate version to launchthe EC2 instance, and this makes things much more flexible for the users.For example, we were discussing about this specific launch template.Now if you discuss about this launch template, this has multiple versions.So you see you have version one, you have version two and so on.Each version can have different set of configuration.And user can select which version of launch template he wants to use.For example, when we launch an instance from a template, you see by default version two is used.However, in case required, user can also select the version one over here and the configuration willbe modified accordingly.Now, before we conclude this video, one of the best practices that ideally you should follow whenyou have launch template used in the organization is that you can control the configuration of EC2 instancesby specifying.Users can only launch instances if they use launch template.So if they try to launch the EC2 instance, they will be blocked only if they launch instances througha launch template.They will be allowed, and this will ensure that you have a very consistent environment that followsthe best practice that is set by the organization.Great.So that's an overview about the launch template and the benefits.And with this we'll conclude today's video.