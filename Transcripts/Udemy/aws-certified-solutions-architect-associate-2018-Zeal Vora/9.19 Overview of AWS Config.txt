# 9.19 Overview of AWS Config

Hi everyone, and welcome back.In today's video, we are going to discuss about one of the important service which is AWS config.Now one of the very important features of AWS config is that it allows us to record the configurationchanges that we make to the resources.So at a high level overview AWS config, it continuously monitors and record the changes that are madeto the resource.And it also allows you to see the timeline of the changes.So let's quickly understand this through a workflow.So let's assume that on day one in your AWS account you have a EC2 instance that got created.Now on the day four associated with EC2 instance, you have a security group called as dev hyphen SGthat got attached.Then on day six associated with the same instance, there was a IAM role that got attached to it.And similarly, in production environments you will have servers that would be running for years.So you might see some regular changes that might be happening to the server.So using this specific feature you can go ahead and see the timeline.For example you can see that this EC2 instance was created on day one.Then on day four there was some configuration that was associated, some changes that were made to EC2instance.Similarly, you will also be able to see that on day six IAM role got attached to the instance and soon.So this proves to be very useful.The next very important feature of AWS config is related to the area of audit and compliance.So at an overview the AWS config, it allows us to automatically detect any policy violations by analyzingthe resource changes.For example here you see there are certain rules that are available and based on the rules, it is statingthat whether the AWS account is compliant against that rule or not.For example, you see that there is a rule called as approved Amis by ID.So many times what happens is in the larger organization you have some approved Amis.You can refer it to its hardened ami or golden Ami, etc..Now, although most of the EC2 instances are launched to the Ami, but sometimes some EC2 instancesare launched through random Amis.So there is a rule called as approved Amis by IDs.So it will scan your AWS account to see if EC2 instances are created through the hardened Ami.If not, then this compliance will fail.It will also show you that there are two non-compliant resources over here, and when you click here,you'll also see what are the EC2 instances that are non-compliant against this specific rule.There is one more very important feature of AWS config called as Conformance pack.So at an overview, a conformance pack is a collection of AWS config rules that are built based on CommonFramework.Now we were discussing that based on some rules that you add, AWS config can analyze the resourcesand it can show you the compliance status or not.The great benefit of conformance pack here is that there are multiple set of categories, for exampleoperational best practices for PCI, DSS or operational best practices for NIST.So instead of creating rules from scratch, if you use a readymade conformance pack, this automaticallycomes with the relevant set of rules, so you don't really have to create rules from scratch for eachpurpose.So if an organization is going through a PCI, DSS compliance, then they can simply deploy this conformancepack as a discussing conformance pack.Behind the scenes it contains the necessary config rules, and that config rules will show you the compliancestatus.And you'll have a nice little detail that shows you about the compliance score based on a specific pack.So let's go ahead and have a quick demo.This will allow us to understand the AWS config service in a better terms.Now for today's demo I already have the AWS config service that is enabled.Now if I quickly go to the dashboard over here, let's maximize this.So one of the very important part and this helps a lot during the audit as well as the compliance process,is that it shows you the compliant as well as the non-compliant resource.For example we were discussing the rule associated with approved Amis by IDs.So if I quickly open this specific rule over here.So what does this rule do?Let's verify in the description.It states that.Check if EC2 instances are using a specific Amis.As we were discussing in most of the organization, you will have some form of a security hardened Ami,also referred to as the Golden image, and all of the organization's users are expected that they willonly launch EC2 instances from the Golden image.However, sometimes you will see some random user will go ahead and launch instance based on some otheroperating system that is not hardened.So this is where this rule proves to be very useful.Now, we already know that there is some non-compliant resources associated with this.So if I go a bit down, it will also show you what are the EC2 instances that are non-compliant.So you have this specific EC2 instance and this specific EC2 instance.Apart from this all other EC2 instances are compliant.So this proves to be very useful during the audit and compliance process.Now one of the first features that we were discussing is the ability to record the configuration changes.And this also helps a lot.So let's also quickly look into it.So within here let's go ahead and go to resources.So there are multiple resources over here.Let's go ahead and take an example of say EC2 instance.I'll search for instance over here.This is one.And we'll take one more example of say security group.All right.So we are focusing on this as of now.So at this stage it will list all of the instances as well as the security group.Even if the resources are deleted you can include them so that you can see what are the configurationchanges that were associated with them.So this is the first part.Now let's take an example.Let's go to the second page.Let's take an example of this specific resource which is the security group.So this is one of the security group that has been deleted.But you want to see what were the configuration changes that were made to this resource throughout thetime.So this is what we are aiming for Now, when you look into the timeline associated with this specificsecurity group ID ending with D63, which is the one that was deleted.So one thing that you have to keep a note that it is not necessary that only the resources that arecurrently running, you will be able to look into the timeline.Even the resources that are deleted in case.If you'd like to see the timeline, you will be able to do that.So let's go ahead and do that and see what exactly it shows.So within here if I go a bit down.So at this specific time there were some CloudTrail events.So if you see one of the CloudTrail event is Create Security Group, then you have authorized securityGroup ingress and then you have authorized security Group ingress.So what this means is that there was a security group that was created at 9 a.m. around, and therewas some ingress rules or the inbound rules that were added to the security group.Now the configuration change over here.This is what we are going to look for.So what it says here is that there was a security group that was created.This was the idea of the security group.The name of the security group was Test Hyphen SC.And from the CloudTrail event we saw that there were some inbound rules that were created.So this is what it shows you that there was one inbound rule on port 22 that was created, and the IPrange that was allowed is 0000.Along with that, there is also one more rule over here from port 80.So port 22 was open from 0000.Port 80 was also open from the same Cidr range.So this proves to be very useful.So this is the first part.Now it also shows you that whatever resource that was created.So for the security group it had two rules that allowed the entire internet to connect to it.So this was non-compliant.It was non-compliant against which config rule it is.VPC is open only to authorized ports.So this is the config rule.And whatever inbound rules that were added to the security group they were in non-compliance over here.So this also proves to be very useful.Now at 917 you see there is one more configuration change.What has happened here?Before we do that, let's look into CloudTrail event.CloudTrail event is revoke security group ingress and authorized security Group ingress.So what this means is that inbound rule that was added and one more inbound rule was removed.So if you look into the configuration change, you can see that this inbound rule of port 80 was removed.However, there was one more inbound rule on port 3306 which was added.So this also proves to be very useful over here.So let's minimize this now.At around 952, there was a CloudTrail event that got generated called as Delete Security Group.So here the entire security group was deleted.So this is what we refer to as the timeline of changes.You can see that the security group was created at 902 and it was deleted at 954.And you could also see what happened between them.What were the rules that were added?What were the rules that were removed, and so on.And similarly for other resources like EC2 instances and others, you will be able to see similar setof timelines.Now we were also discussing about the feature related to the conformance pack.Let's also quickly explore this from the demo perspective.Now if I go back you see you have the conformance pack over here.Now within here you will see that there are multiple templates that are available based on wide varietyof use cases.For example, for organizations that are going through HIPAA compliance, the HIPAA security operationalbest practices proves to be very useful.Again, these are the set of config rules that will evaluate the resources that are running in the AWSaccount, and it will show you whether they're compliant or not compliant.Then pci, DSS again this is very popular nowadays in the industry.A lot of organization has to go through this compliance.Then you have the nest, and there are a lot of other templates that are available which you can makeuse of.So this is one part.Now at this stage I have one conformance pack that is already created called as IAM template.Now if you see the score is 60%.For example, let's say an organization is going through a PCI, DSS compliance so they can use theconformance pack related to PCI, DSS, and they'll try to achieve 100% of the score.So this is one part that lot of organization aims for.You can also see what exactly is the reason behind such a low score.So if I'll quickly maximize this over here, it shows you details on all of the rules that are partof this specific template.For example, IAM password policy, conformance pack, root account hardware MFA enabled.So it is showing as non-compliant.So what exactly this means.Let's open this.Let's go a bit up.What it says checks whether your AWS account is enabled to use MFA hardware device to sign in it withroot credentials.Great.So with this, I hope at an overview, you understood the three very important features as part of theconfig.One is related to the timeline, second is related to the audit and compliance.Third is related to the conformance pack.Now in case if you would like, you can also go ahead and you can create the rule.For example you can go ahead and add the AWS managed rules.Over here.There are multiple set of rules that are available.You can use any of the rules in case if you would like.You can also go ahead and create a custom rule based on some checks that you might want over here.Now conformance pack, as we were discussing, it contains its own set of rules that are very specificfor a use case like say PCI, DSS or HIPAA, etc. so this is also used quite extensively.Now few more things at the overview that you have to keep a note before we conclude.One is related to the integration with the EventBridge service.So note that the Amazon EventBridge can capture the config rule evaluation events and it can then routethe events to various other AWS services or custom target.For example, you have AWS config EventBridge.Now, if EventBridge detects that there are certain rules that are non-compliant, a EC2 instance gotcreated with some random ami id, so you have the EC2 instance as non-compliant.So as soon as the EventBridge detects that EC2 instance is non-compliant, then EventBridge can go aheadand send the details to the appropriate target.It can be a lambda function.It can be SNS.For example, let's say that you have a requirement that whenever a non-compliant resource is detected,you should get an email.So in such case, EventBridge can connect to the SNS and SNS in turn can go ahead and send the emailstating that there is a specific resource which is non-compliant.So there is a lot of flexibility that you can achieve using AWS config and other services to achievea use case.Now, a few more important pointers that you have to keep a note, specifically the relation betweenAWS config and AWS CloudTrail.CloudTrail as we know it records the API activity in your AWS account so it can capture various thingslike the IP address, who is the user that has made the change, and so on.So CloudTrail is a very important service that should always be enabled.AWS config.As we are discussing, one of the features is that it records the point in time configuration detailsfor your AWS resource, and this helps a lot.So when you have two services that are quite similar, both can be used in conjunction.For example, you can use AWS config to answer what did my resource look like, say seven days back,30 days back, one year back, etc. at a point in time and you can use CloudTrail to answer who madean API call to modify the resource.For example, here we were discussing that in the configuration change we were discussing, that configis showing that port 80 was removed as part of the inbound rules, and port 3306 was added.Now which user has made the change?What is the IP address of the user?All of those important details is what you have to refer CloudTrail for.So all of these CloudTrail salts.But if you want to see on what timeline the resources were modified and so on.This is where config proves to be very useful.So again, both of them are used in conjunction with each other.One more point that you have to keep a note is that AWS config is not free.If you're using AWS config, you are charged based on the number of configuration items that are recorded,the number of active AWS config rule evaluations.So if you have 100 rules, then you will be charged based on the active rule evaluations and also thenumber of conformance pack evaluations within the account.So this is one thing that you have to keep a note of.Great.So this is the overview about the AWS config service and its importance.Again I would say that this is a very important service.And in most of the organizations that you will be working with, you will see that AWS config is enabledand it is used quite extensively.So that's the overview.And with this we'll conclude today's video.