# 3.15 Creating our First Website on EC2

Hi, everyone, and welcome back.Now, in today's video, we are going to launch our first website in EC2 instance.So overall, this is going to be a fun and a very interesting practical with a lot of learnings.So at this stage we have our EC2 instance that is already running and we are also able to log in tothis EC2 instance, either using the browser based SSH or the SSH client based approach.Now that the EC2 instance is running, there are a lot of things that you can do.You can launch your own website, you can install certain softwares and so on depending upon the usecases.And in today's video we are going to launch our first website.So what is going to be the overall aim?The aim is to launch a first website in a way that whatever website that is running is available throughoutthe Internet.So this is the primary aim.So with this base set, let's go ahead and get started.So the step number one is that we quickly connect to our EC2 instance using whatever method that youprefer.Once you connect to the EC2 instance, you will generally see this EC2 user.Now you want to install certain software required for the overall website hosting.So at this stage, since we are logged in at an EC2 user, we'll switch to the administrator user usingthe sudo SU hyphen command.So here you see that the user changed from EC2 user to root.So root is generally the admin user in Linux and it has all of the permissions.Now once you are with the root user, you can run this specific command that installs a software packagecalled as Nginx.All right.We'll run the clear command to clear the screen.Now, the next step is to start our engine.So at this stage, if you will quickly look into the status of Nginx, you see it is inactive.So what you need to do is you have to start it.So I'll say systemctl, start nginx, all right.And now if you look into the status again, you see it is active and running.So this is the primary step.The next important step is to allow this through firewall.Let me quickly show you.Now, again, note that from exam perspective, you do not really need to know the commands or in fact,you do not really need to know how you can launch a website in EC2 instance.We are going into a little detail so that we understand this from practical point of view and it willdefinitely help you not only from the certification point of view, but primarily the job and the realworld perspective.So when you run this command of netstat LP, you will see that we have a SSH that is running on port22 and the Nginx is running on port 80.So this 80 over here, you see this is where the Nginx is listening to.So what you need to do in the firewall or in the security group associated with this EC2 instance,you have to allow the port of 80.Now, we will be discussing in great detail about the firewall at a later stage, but at this stage,just follow along.So in the security, you see that there is a security group associated with this EC2 instance and itis only allowing the Port 22.So what we have to do, along with Port 22, we also need to add Port 80.So let's open up the security group here.Let's edit the inbound rules and within here we'll add one more rule.In the port you can specify port of 80, and within here you can just select 0000/0.All right.Once, then you can save this.So now the EC2 instance will accept the connection from both the Port 22 and 80 once, then go backto the instances and copy the public IP of the instance.So let's copy the public IP here and within notepad you can just type HTTP followed by the public IP,make sure there are no spaces, you can copy this and within new tab you can just add the IP addresshere and you should see Welcome to Nginx page.So this is the default website.Now in case if you'd like, you can also go ahead and modify this based on your requirements.Let's go ahead and try it out so that this practical becomes much more interesting.Now within our server, we'll use the clear command to just clear the screen and we'll go to a specificfolder which is usr share Nginx.HTML.So this is the folder where the website is stored.So if you run the LS command, which basically list all the files in the folder, there is a file calledas index.html.So all of the contents that you see over here is coming from that file.Now, in fact, if you will just view the page source, you see that this is a HTML file.You have doctype HTML in the title you have welcome to Nginx and so on.So all of these contents are nothing but the contents of the index.html file.Now let me in fact show you.So if I'll quickly do a cat on index.html cat basically shows you the contents of the file without havingto manually open it.So if you see the contents, you have the doc type of HTML title of Welcome to Nginx and so on.So for your website, if you have a HTML, you can add it over here.Otherwise you can also say something like Echo.This is my first website and you can add it to index dot HTML.So what this command does, it adds this highlighted line inside this index dot HTML and it will alsoremove the older contents of the file.All right.Now, if you do a cat on index dot HTML again, you will see it only states this is my first website.Now if you go ahead and refresh the page.You see it changed to this is my first website.Now, again, this is nothing fancy, but in real world environment, if you have a great little HTML,you can go ahead and add it to the index.html file or if you have some kind of a image, you can alsoadd it as part of the file.So this is the high level overview on how you can launch a first website in EC2.You will see that overall the steps are pretty simple and there are a lot of interesting things thatyou can do once you have the server up and running over the Internet.So with this we'll conclude today's video.