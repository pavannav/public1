# 16.9 Field Level Encryption

Kieran and welcome back.Now introduce we will be discussing about the field level encryption in cloud print.Now before we go ahead and understand it in detail, let's understand the challenges in a typical architecture.So a sensitive data which is travelling via a multi tier architecture, leads to a lot of security challenges.So we can understand this with a diagram here where on the top you have a cloud print distribution andthen there are multiple servers involved within your organizational architecture and the server threehas the responsibility to store that data within the database.Now let's assume that a user has basically sent a sensitive data, so this sensitive data needs to bestored within the database.However, when you have an HTTPS website, typically what you might do is you might terminate the HTTPSat a higher level, so you might terminate it at cloud and you might terminate it at the elastic loadbalancer level.So this data might be encrypted as part of the TLS encryption, but once it comes within your network,it will be in a decrypted format.So currently you'll see the same object with when it is traveling from server one to server two, maybeeven if it is an internal network, it is in a decrypted format, again from server two to server three.When it is moving, it is in a decrypted format and maybe the server three might have the logic to encryptthat object with a CMS and then store it within the data store.However, there is a risk over here, so let's say that a server one is compromised.Then this specific PII data can also be compromised.So that is one case.Second case is if the server two is compromised, then again this PII data is compromised.Again, if the server three is compromised, then again the PII data is compromised.So any one of the server within your entire network, if it is compromised, then this critical data,it can be credit card information, etc. can be compromised in an easier way.So this type of architecture, specifically when you're dealing with certain sensitive data, is notthe best of the architectures that you can design.Now, when we discuss about some of the possible solution, having an end to end encryption can be agood solution to the previous architecture that we were discussing.So with the end to end encryption, what will happen is that a user sends a PIA data.So this PIA data remains encrypted throughout the network.So when it traverses from the cloud front distribution to the Origin server, from the origin to servertwo, from the server two to server three and even from server three to the database, this object remainsin the encrypted format.Now, only when a specific service has a genuine business need, that service will only be able to decryptthe data.So now if you'll see that there is a genuine service over here and only when there's genuine servicerequest for this specific object which is stored in the database, it will be able to fetch the decryptedobject back.So now with this, what will happen is that the oral attack surface is reduced to a bare minimum.So even in this transit, even if a single server is compromised, the attacker will not see any datain plain text.So this type of architecture where a genuine service can only fetch the decrypted data, can be achievedwith the help of cloud friend field level encryption.So what happens with the field level encryption is that the cloud friend will go ahead and encrypt thePIA data before the request is forwarded to the origin.So what that will mean is that this specific object, which is the PIA data before this object is forwardedto this origin server, which is server one cloud and will go ahead and encrypt this specific data.All right.So that's what it is all about.Now, this can be better understood with this architectural diagram over here.So let's see.That user is basically sending his phone number, which are considering it to be the PIA data.So the user will send his phone number to the cloud distribution, cloud distribution before it sendsfurther.It will go ahead and encrypt that phone number.Now, it will encrypt that phone number with the public key, which you store it within the cloud distribution.So this encrypted phone number goes to the origin.Now, this origin can be easy to instances, it can be API, gateway, etc. So this goes to the origin.Origin will go ahead and store it to the Dynamo DB table.Any other data as it can be ideas, etc..So now you have the encrypted data which is stored within the Dynamo DB table.So now you have an administrator in the previous case.It is a genuine service, so we'll call it as an administrator here.So this administrator wants to look into this specific data in a decrypted format.So what this administrator will do, the administrator will make a call via the lambda function to theDynamo DB table.The Dynamo DB table will send back the encrypted phone number and the private key associated with thepublic key, which was stored in the cloud Friend.So this private key is stored in the parameter store.So this private key will be used to decrypt this specific phone number and the decrypted data will goback to the administrator.So that is what is referred as the field level encryption, where a specific field within a requestis encrypted.All right.So I hope you got the overview about this architecture.So let's again revise the high level steps.So in the first step, the application sends a post request with the PII data.As we have seen in the second step, the field level encryption intercepts, the post request.It encrypts that specific data with the public key and then forwards that encrypted data to the origin.Third step Origin takes the data, processes it normally and stores it at the Dynamo DB table.So here, even if this API gateway or this lambda function is compromised, there are no consequencesbecause already the data which is coming is in the encrypted format directly.So that is the third point for point here is the lambda function shows the data in the dynamo DB table.Now it is not necessary that lambda function needs to be there.It can be a simple, easy to install, but based on this architecture diagram you have a lambda functionwhich is taking the request from API gateway and then storing it to the dynamo DB table.So in the step four lambda function is storing that encrypted data to the Dynamo debate table.Now in the step file you have an administrator or it can be a genuine service which makes use of a secondlambda function.So you have a second lambda function.So the necessity of this lambda function is to retrieve the data from dynamo db table.So the second lambda function will retrieve the encrypted data from the dynamo DB table.Now, since it is an encrypted data, it is not of much value even for the administrator.So what needs to be done?Administrator needs to use the private key which is stored in the parameter store to decrypt the sensitivedata.And once the sensitive data is decrypted, the plaintext data is returned to the administrator.So I hope you understood the high level architecture diagram from this specific slide.So with this, let's do one thing.Let's take a quick demo so that this becomes much more easier for us to understand.Now, within the browser, let me go ahead and paste the URL associated with the cloud print distribution.So basically we have created a cloud distribution with the field level encryption enabled so that wecan understand this in a better way.So let me open the earlier.So this is a simple application all times to a place for this.So this application here has full name, it has the email address and it has the phone number.Now, among these three you can select which one among them you consider it to be the PII data.So whenever this application sends the post request with the PIA data, you can specify which amongthem is the PIA data over here.So when you go ahead and click on Submit, what will happen is the cloud friend will go ahead and encryptthis specific phone number.So for this application we have considered phone number to be the p i data warrior.So as soon as you click on submit, it will show you the response.So currently within the response you will see this specific phone number is within the encrypted formatand the same thing you will be able to see within the database as well.Now, within this diagram we had taken the Dynamo DB as the data store.So similarly we have the Dynamo DB over here.Let's open up the tables over here and within here you will see that there is one item here.Now within that item, the email address and the name are in the plaintext.However, this phone number you see it is automatically encrypted.So this encryption of the phone number which you have submitted via the post request is done with thehelp of the cloud friend field level encryption.Now, if you look into this diagram, this cloud friend needs to be associated with the public key becauseonly after you are associated with the public key, will it be able to encrypt.So let me also show you the cloud trend distribution so it becomes easier.So you have this cloud first distribution over here.And within here, if you go to the public key, you will see that I have a public key which is uploaded.So when you quickly do edit, you will see that there is a public key which I have added over here,and the same has been referenced to the cloud print distribution.So cloud will take this public key, which I have added and it will encrypt certain fields which yousend as part of the post request.Now the second part is the private key, because whenever you generate the RSA base keys, you havethe public key and you have the private key as part of the asymmetric key cryptography.So that private key is part of the parameter store.So let's quickly open up the systems manager over here.And within here if you quickly open up the parameter store.So within this parameter store, you see that there is one parameter which is available and it basicallyhas the value, which is nothing but the private key.So this administrator or this specific genuine service, if it wants to fetch the encrypted data, whichwas part of the dynamo DB table, it needs to use this private key to be able to decrypt that data.So if you are running an application within a server, you can associate a IAM role with that in sucha way that the IAM rule has a policy which allows request over this specific parameter store in caseif you are storing this private key within the parameter store.Great.So I hope you understood that a high level overview the importance of high level encryption in Cloudflare.I hope this video has been informative for you and I look forward to see you in the next video.