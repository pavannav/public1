# 18.4 Container Orchestration in AWS

Hi, everyone, and welcome back.Now, in this video, we'll be discussing about container orchestration in a class.So let's say that your organization have decided to move to Docker.So one of the important steps here is to choose the appropriate container orchestration tool.Now, as far as Aida is concerned, there are two primary services that you will see being extensivelyused in organizations.One is called as the Elastic Container Service.Second is called as the Elastic Kubernetes service.Now, globally, when you discuss about container orchestration, Kubernetes is the de facto orchestrationtool that is extensively used across the platform.However, for users as well, you have a good alternative of X that you can also select.Now, as we are discussing, there are two primary services years for containers.One is the elastic container service.Again, this can run Docker containers and can manage them.And second one is the Elastic Kubernetes service.Again, this can run containers pretty well.So let's go ahead and look into the differences at a very high level overview to understand the differenceand when you should use one among them.So the first important pointer is open source.So the EC is basically, since it is using Kubernetes, Kubernetes is open source X is not open source.When we discuss about complexity, Kubernetes is definitely it is more complex, is is less complexwhen compared with Kubernetes.And when we discuss about community support, community support wise, you will get great support forKubernetes.And when we discuss about X, the overall support is quite less.Now, you need to notice that Kubernetes is not just specific to a place you will see manage Kubernetesservice in various platforms like Azure, GCP, Digital Ocean and so on.So whenever you discuss about Kubernetes, you will definitely find great number of support in termsof community as well as great developments that are happening here.So choosing the right orchestrator for a let's look into some pointers.First one is that if you plan to work exclusively on a base, you know that you are not going to shiftto other platforms in the longer term.For simplicity, you can decide to choose X as it offers great integration when compared with X.Now, when X was initially launched, there were a lot of features that were missing in terms of integrationwith services, but now it is extensively launching great features for X in terms of its integrationwith the platform.Second here is that the organizations that have limited expertise and insufficient resource to investin learning Kubernetes can also go with X learning.Kubernetes is not a small job.It requires great number of efforts, a lot of things that are required here.In fact, we have three courses related to Kubernetes to cover the administration, Part two, coverthe security part, and so on.So Kubernetes, although it is one of the great orchestrator, but the learning curve is also a littlehigh.So in case if you have a very small team who do not have expertise on Kubernetes, you can switch toX.And third, if you plan to deploy containers across multiple platforms.In such cases, you can choose X, for example, your organization is using multiple cloud environments,maybe Azure GCP as well as a s.So in such cases, Kubernetes service is available in all of these three platforms.So it makes sense that you use Kubernetes across all three platforms.Otherwise you will make use of X in Kubernetes, in GCP and Azure.It will not really make sense.So that's the high level overview about the container orchestration tool is in place, and with thiswill conclude today's video.