# 12.12 Overview of Amazon Aurora

Hey, everyone.And welcome back.Now, in today's video, we'll be discussing about the Amazon Aurora.Now, Aurora is a great piece of database offering under the relational database service in AWS.Now recently is really pushing Aurora quite a lot.And specifically this applies in terms of certification.So when you discuss about certification like cloud practitioner, you have solutions architect, associatesolutions, architect, professional DevOps database, Specialty and others.Aurora is one of the common topics that you will see in all of them.So if you learn this topic properly, it will really help you not only in real world scenarios but alsoin your certification journey.So with this, let's go ahead and begin our journey in Aurora.Now, when you discuss about the database offerings, they are generally divided into two primary types.One is the open source database and second is the commercial or enterprise databases.Now, when you compare both of them, commercial database does come with certain aspects.It can be in terms of performance, security and others that are not found in the open source databaseofferings.So this is where the Aurora steps in.Now Amazon Aurora is a MySQL and PostgreSQL compatible relational database built for the cloud thatcombines the performance and availability of a traditional enterprise database with the simplicity andcost effectiveness of the open source databases.So this can be better understood with this diagram over here where you have a Amazon Aurora, whereAurora tries to have a performance availability, security, reliability of the enterprise databasewith the simplicity and cost effectiveness of the open source database.So it tries to be best of both and provide you the database offering in AWS.So Amazon Aurora is up to five times faster than the standard MySQL database and three times fasterthan the PostgreSQL database.Now again, it's not like out of the box.It is five times faster and three times faster than Postgres.But there are certain workloads where Aurora proves to be much more faster than the traditional MySQLas well as the PostgreSQL.Now again, as we are discussing Aurora, it provides security, availability and reliability of a commercialdatabase offering at one tenth of the cost.And this is what a lot of customers really need.Now let me quickly show you one interesting feature of Aurora.In fact, there are a lot of them.But let's begin with one.Now, generally in RDS, whenever you try to create a database, let's create with a MySQL engine overhere.So if you go down, you see that you have a option of storage type and you can go ahead and specifythe amount of storage that you need over here.So this is one example of MySQL.However, when you select the Aurora one over here and if you go a bit down, you will see that thereis no setting associated with storage at all.So what happens in Aurora is that you do not select the storage whenever required.The storage will automatically grow without hindering the overall performance of the database.So this is one of the really great features that Aurora offers.Now, before we go ahead and discuss more in detail about the Aurora architecture, let's quickly havea revision over a standard RDS multi-az and read replica based architecture.So this diagram will help you understand it in a quicker way.So you have three availability zones over here and in the availability zone one you have a primary RDSinstance running and this RDS instance is not only multi-az, but it also has a associated read replica.So when you discuss about multi-az, this works based on synchronous replication and when you discussabout RDS read replica, it works based on asynchronous replication and all of them in the underlyingthey make use of the EBS storage.So if you see over here you have a so you can consider with MySQL and Underlyingly it makes use of theEBS volume for storage.So now let's quickly look into the Aurora architecture at a high level overview.Now when you look into the architecture, there are two primary components that are involved.First one is the database instance.So this is basically the Aurora instance that is running.And second primary component is the storage cluster volume.So this storage cluster volume spans across multiple availability zones and the data is replicated betweenall of them.Now, since the Aurora instance and the storage layer are independent, we can scale the storage ina much more easier way.When you compare with the traditional RDS based architecture like MySQL, which basically makes useof the EBS volumes.So let's quickly look into the storage cluster, volume based architecture.So you have a storage cluster volume, which is basically a virtual storage volume which spans acrossmultiple availability zones.So in this diagram there are three availability zones and each data is replicated six ways across theavailability zones.So if you see over here, so you have one data, this data is available in Az1 Az2 as well as AZ three.Similarly, if you look into this green circle, the green circle is also available in all the threeavailability zones.Now in case if one of the availability zone goes down, there is nothing much to worry because the otheravailability zones already have the same data that is replicated now with this specific architecturewhere a storage cluster volume is used instead of a EBS volume, you can add the database instance quicklybecause Aurora doesn't make a new copy of the table data.Instead, database instance connects to the shared volume that already contains your data.Now this can be better explained with this next diagram where you have three availability zones andyou know that the data is already replicated across the storage of the three availability zones.So in case if you create a new Aurora replica, then all that happens is that a instance gets createdand it gets associated with the cluster storage volume.The data is already present beforehand in a similar way, if you create one more read replica in a differentavailability zone and maybe one more replica in a different availability zone, all they have to dois they have to connect to the underlying cluster storage volume and that's about it.The data is already present across all the availability zones for the cluster storage volume, and thisis one of the great advantage and it really makes Aurora much more faster.Now the same can be explained with this diagram.In fact, this diagram, I have directly taken it from the documentation where you have a primary instanceand you have multiple read replica in a different availability zones.Now you'll see that you have a storage cluster volume and each availability zone has its own set ofdata copies for availability point of view.Now when you discuss about Aurora, the concept of End Point is something that we need to understandso you can connect to Aurora cluster through end points.Now End Point is basically a Aurora specific URL, which consists of two parts.First one is a host and second one is the port.Now there are three primary type of end points that are available.There are more as well.So the primary type is you have a cluster level end point, you have a reader level end point, andyou have a instance level end point.So when you are working with the Aurora.Understanding this concept is very important.In fact, we'll be discussing about it in a detail in a moment.So let's go ahead and understand this with a simple diagram where you have an application.Now, this application can connect to a cluster endpoint.It can also connect to a reader endpoint.So cluster endpoint is the endpoint where you will be performing the write operation and the readerlevel endpoint is the endpoint where you will be performing read level operation.So you can consider reader endpoint as the endpoint for read replicas and the cluster endpoint as theendpoint for write specific operation.So if an application wants to write something into the database, then you will have to supply the clusterlevel endpoint for the same.And if there are certain use cases where you only want to perform, read only operation, then you willhave to configure the reader endpoint into that application.So speaking about the Aurora endpoints, let's discuss about the four types of endpoints that are available.First one is the cluster level endpoint.So cluster level endpoints are used when you want to connect to the primary database instance withinthe cluster, and it is used for performing the write operation.Then you have a reader level endpoint.These are the built in endpoints for read replicas.Now for multiple read replicas, this endpoint will also load balance the workload among all the readreplicas.So consider this diagram where you have to read replicas that are available.Now.It is not a best practice to directly add the URL of the read replica within your application.Instead, you can take the URL of the read endpoint, put it within the application and the reader endpointwill also load balance between the multiple read replicas that are available within the Aurora instance.The third one is the instance endpoint.So instance endpoint allows you to connect directly to the instance.So if you see over here there are two read replica instance over here.So instance endpoint directly allow you to connect to the instance in case if you need it.But the best practice states that you should not directly connect to the instance.You should directly connect to either the cluster endpoint or the reader endpoint.And then you also have the option of custom endpoints, which allows you to create custom endpointsfor your own requirement.So that's the high level overview as far as the theoretical perspective is concerned.So let's do one thing.Let's quickly have a demo so that this can be better understood.So I'm in the AWS console and I already have a Aurora instance running.Now if I maximize this, this architecture is very similar to the diagram that we were discussing whereyou have one primary instance and there are two replicas that are available.So within this, the first one is the primary instance.So if you look into the role, it is a writer one.So this is where you actually perform the write operation.And then you have two other instance which has a role of reader.So these are basically the read replicas over here.So let me in fact open up our Aurora.Now, if you look into the endpoints over here, you will see that there are two endpoints that areavailable.One is the cluster writer and second one is the reader endpoint.So if you look into the type here, this is the writer.So that writer is nothing but this cluster endpoint.So within the documentation you will not see it as a writer, you will see it as a cluster endpoint.So this is where you perform the write operation.And the second type is the reader.And we have already seen so this reader is nothing but the reader endpoint, which in turn has a connectivityto all the read replicas that are available.And it will also load balance the workload.Now within the Aurora, you will see that there are two Aurora Read replicas that are already available.So instead of using the DNS of each read replica, you can directly make use of this reader endpoint.Now, in fact, let me also show you the instance endpoint over here.So let's click on one among them and you will see that each instance over here has its own endpoint.So this is the first one.Then you have the read replica.Read replica also has its own endpoint and you have read replica to read replica to also has its ownendpoint.So when you compare it with the table, you have a cluster level endpoint, you have a reader levelendpoint, and then you have the instance level endpoint, which are nothing but the ones that we areexploring now.So these are also referred as the instance level endpoints.So that's the high level overview about the Aurora endpoints.Now, when you discuss about the features that Aurora offers, there are a wide range of very interestingfeatures that Aurora has to offer.Some of these includes global database.Serverless is a very interesting feature.Yet again, you have cross region replication, you have auto scaling, which is also a very interestingone.You have Aurora Backtrack, you have IMDb authentication, you have a feature for sharing a DB clusteror RDS proxy and so on.So there are a lot of interesting features that are available for Aurora.However, we'll be discussing them in the relevant section when it is required.So today's video was a very high level overview video for Aurora, the purpose for which the aurorais built.And at a high level overview the understanding about the Aurora endpoints.So with this, we'll conclude this video.I hope this video has been informative for you and I look forward to see you in the next video.