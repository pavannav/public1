# 9.15 Overview of Amazon Macie

Hi everyone, and welcome back.In today's video, we are going to discuss about one of the important security services in AWS whichis Amazon Macie.So with this, let's get started.Now one of the common things that you will see in organization that uses AWS is that they primarilyuse S3 and Glacier for their data backup and archival purposes.So when you have a lot of data that is getting backed up at S3, it can happen that the backup datamight contain some sensitive files as well.So it can contain some PII data, it can contain some SSL private keys, the AWS access secret keys,credit card information, and so on.So many organizations that goes through strict compliance like PCI, DSS, it is important for themto ensure that there are no PII data that are getting backed up in S3 in a plain text format.So in order to verify if no sensitive data is getting backed up in S3 bucket, you have the Amazon Macieservice that proves to be useful at an overview.Macie is a data security service that uses the machine learning and pattern matching to discover andhelp protect your sensitive data.So if you see over here in the screenshot, you have the Macie finding within your.The Macie was able to detect that there were two files in an S3 bucket.So these were the plaintext files which contains some sensitive credentials.And similarly it can scan all of your buckets and it can report you if it is able to find some sensitivefiles as part of the finding.This is one of the primary use case where Macie is used, along with the primary use case of Macie.Macie will also give you inventory of the S3 general purpose bucket, and it also evaluates and monitorsthe bucket for security and access control.So in case if Macie detects potential issue with the security or privacy of your data because ultimatelydata is in S3 bucket, S3 bucket might be publicly accessible, which you do not really want.So all of that also, Macie looks into it and it can generate a finding for you to review and remediateas necessary.So that's the basics of Macie.Let's have a quick demo to get better clarity.Now for today's demo.I already have the Amazon Macie that is configured.So this is how the dashboard really looks like.Now if you go a bit down you will see some of the policy findings.Some can be like S3 bucket encryption is disabled, some can be that S3 bucket is shared externally.As we were discussing that S3 buckets can contain sensitive data.So not only it can help you detect sensitive data, but in scenarios where there are some loose accesscontrols.So all of those findings also it can detect.Now let's look into some specific finding.So I have a bucket called as Cablelabs data backup bucket.If you look into the findings over here there are two findings related to the sensitive data credentials.It also shows you in which file it was able to detect the sensitive data.In this case there is a file called as config dot txt and one more file called as access secret 02.txt.So here there are three text files that are available and from the three text files it analyze the textfiles.And it was able to detect that there are two files which contain sensitive credentials here.Now, looking into the contents of the text files, the access secret 02. txt.It contains the access key as well as the secret key.So this was something that Macie was able to detect.And discussing about Config.txt it contains the RSA private key.Great.So now let's go ahead and discuss on what are the type of data that it can detect.So here note that by default Macie can detect several categories of sensitive data.Some of the sensitive data can include the AWS Access Secret key.This is something that we saw.It can detect the credit card expiration date, the driver's license, the bank account number of aspecific countries like US and Canada.The GPS coordinates, the mail address, the SSH private key.This is something that we saw.It can detect the phone numbers HTTP cookies, API keys and so on.This again is just an example.There are a lot of other data that it can detect from the S3 buckets.Now let's discuss about this point from documentation perspective.So within here if you look into the sensitive data that it can detect.You see, the list is quite extensive.There are a lot of things that it can detect over here if we go a bit down.These are the list of things that it can detect.Now in case if you would like you can also add the appropriate set of custom regex if you want thingsto be much more modular.For example, let's say that you have a bank account number.So here it detects bank account number for both Canada and US.But what about other countries like India?So let's say that entire organization is based out of India.And you want to detect any bank account number of India if it gets added in plain text as part of S3.So is there a customization that you can do?Answer is yes.Now, in addition to using the managed data identifiers that Macie provides, and this managed dataidentifiers can detect a lot of data that we just saw.However, there is a flexibility where you can build your own set of custom data identifiers.So primarily you can create your own set of regular expressions to detect the data.Let's also quickly look into it.So within here if you look into the custom data identifiers, you can create your own custom set ofidentifiers using the regular expressions, using the keywords and various other details.So there is a lot of flexibility that is available and using the custom identifier.If things are not really added as part of the default list, you can add it custom for your organization.Now, one more thing.Before we conclude, let me also go back looking into the type of findings that it can provide.So some of the findings as we were discussing is related to financial data.You have custom identifier.It can detect if S3 bucket is shared externally, S3 bucket, public S3 bucket shared with CloudFront,and so on.And also each one has the appropriate severity.So ultimately the aim of Macie is to make sure that your data is protected.That is the first part.And second part is to detect if any sensitive data is added as part of any S3 bucket as part of backupin plain text.So that's the basic overview about the Amazon Macie service.And with this we'll conclude today's video.