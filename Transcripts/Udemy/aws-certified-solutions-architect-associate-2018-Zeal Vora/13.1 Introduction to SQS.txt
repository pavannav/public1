# 13.1 Introduction to SQS

Hey, everyone.And welcome back.Now, in today's video, we'll be discussing about the basics of the Amazon SQS service.Now, before we go ahead and discuss in detail about SQS, let's take a simple use case that will allowus to understand the benefits that SQS offers.Now, the use case for today's video is an application that primarily works for restoring an image.So let's assume that a medium Corp is designing an application that will enhance and restore the imagesthat users submit to the online portal.So there are a lot of applications where you go ahead and give the image and they'll enhance it andmake it much more better.So that is what the Medium Corp is building Now as part of the application architecture, there aretwo components that are involved.First one is the image gatherer and second one is the image enhancer.So image gatherer is responsible for taking the image from the user via the upload button.So as soon as a user uploads the image, the image goes to the image gatherer application, and thenimage gatherer will send that image to image enhancer.An image enhancer is responsible for enhancing as well as doing some conversion on the image.Now this type of architecture sounds good.Now one of the challenges that comes is what happens when this image enhancer server goes down.So the image gatherer is working fine, the user is able to upload the image, but when image gatherersends the image to the image enhancer, the server is down.So this is one of the challenges that will be faced in a current set of architecture.Now, second type of challenge is related to the high availability.So let's assume that due to popularity of the application and huge spike traffic, Medium Corp has decidedto add more number of enhancer servers.So now you have two.So now the medium Corp has added two more enhancer servers.So now in case if one of the server goes down, then the image gatherer will be able to send the trafficto the other servers.So for the image gatherer to discover the latest servers that are running, what needs to be done,a database needs to be created or a configuration file with the relevant information needs to be created.So in this case, you have a configuration file that contains four servers.So you have server A server.B server.C server D and each server has its associated IP address.And what image gatherer does, It goes ahead and send the images across each one of the IP addresses.Now, although this seems good, but one of the challenge that comes is let's say that this server Ahas gone down.Now image gatherer has no way of knowing that the server is down.So there needs to be a additional logic at the image server that will check the health check of allof the servers.And if the health check is failed, then the configuration file entry associated with that server alsoneeds to be removed.So image gatherer application needs to do more than just the image gathering and this will introducea lot of challenges.So in order to overcome this, there is also a better architecture of using a queue.So what happens here is that you design a queue and what image gatherer will do.All it is responsible to do is to send the image to the queue and then the image enhancer.All the responsible to do is they are responsible to take the message from the queue and that's it.You do not really need to worry about anything else.So at the image gatherer level, you do not really need to get the latest IP addresses of the serversdoing the health check and creating additional logic.All right, so this is what queue is all about.Now, one of the main function of the message queue service is to take a message from a publisher andforward that to a consumer.So in our architecture, the publisher becomes the image gatherer and the consumer becomes the imageenhancer.So this is a high level architecture.Now, one thing that needs to be ensured here is that since this image gatherer will be sending theimages to the queue, the queue needs to be always available because in case if the queue goes down,then none of the images will be able to be taken by the image enhancer.So this is one of the most critical architecture component in this specific use case that we were discussing.And this challenge of having a highly available reliable queue is taken care by the Amazon SQS service.So at a high level overview, Amazon SQS is a fast, reliable, scalable and fully managed message queuingservice and it basically makes it simple and cost effective to decouple the components of a specificapplication.So now what you have to do, you already have a image gatherer, you already have a image enhancer.What you do is you create a highly.Reliable and reliable with the help of SQS service and configure your image gatherer to send a messageto the queue and configure the image enhancer to take the message from the queue.And that's about it.So this is the basic architecture.So let's go ahead and try this out so that this becomes better understood.So currently I'm in the Amazon SQS console and currently we have one queue with the name of messagequeue and currently there are zero messages that are available.Now we also have two EC2 instances for today's demo.One has the name of Image gatherer, second has a name of image enhancer.So for today's demo, the image gatherer EC2 instance will send certain messages to this SQS queue andthe image enhancer ec2 instance will fetch the message from the SQS queue.So that is the basic architecture, something to what we were seeing.So let's try it out.So currently within my SSH terminal I have two tabs that are created.First tab is associated with the image gatherer, second tab is associated with the image enhancer.So within here, let's go ahead and run a script and the name of the script is Send message dot set.So this will go ahead and send messages to the SQS queue.So let me run it.So currently here you see that it has sent five messages to the SQS queue.Now within here, if we will quickly refresh the page, you see the messages that are available arefive now from the image enhancer, which is responsible for consuming the message.We also have one more script available called as Receive Message Dot.So let's run it.So now at this stage you see it has received one set of message from the SQS queue.Now if you run once more it has received another set of messages from the SQS queue.So for example, let's say that the first one is the image.So it has taken the image, it has completed the work.Now again, it will go ahead and fetch new set of images that are part of the SQS queue and then itwill process the images.Once the processing is done, it will go ahead and fetch the newer images that are part of the SQS queue.So if I quickly refresh the page, you see the available messages has now reduced to two.Now in fact the messages that are part of the SQS queue, you can also view it from the console.So if I'll quickly do a poll messages here, you should see that there are multiple messages that areavailable and this is basically the message body Now that we understand the basics of SQS queue, let'sgo ahead and understand some of the important concepts specifically related to a tightly coupled systemand a loosely coupled system.So in a tightly coupled system, the component of a system architecture, they directly communicatewith each other and also have a hard dependency on each other.So for example, you have a producer and you have a consumer, so producer is directly sending a messageto consumer.So in case if a consumer is down, then the producer essentially will also start to send the error messageto the users.So this is also referred as a tightly coupled system.Now the second type of system is referred as a loosely coupled system in which the components of a systemarchitecture that can process the information without directly being connected.So you have a producer, you have a queue, you have a consumer.So now if the consumer are down, then the producer essentially does not really need to worry.So all producer can do is it can take the image from the user and it can send the image to the queueand then it can give a success message back to the user.That's about it.Once the set of consumers are back online again, they fetch the message from the queue and then they'lldo the necessary level of processing.So this is a much more better type of architecture.And in fact most of the architecture that you will see the new ones in the production environment arebased on the loosely coupled system.This is very important.That's great.So this is a very high level overview about the SQS service and with this, we'll conclude today's video.