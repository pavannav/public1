# 4.15 S3 - Cross Region Replication

Here you are.And welcome back.Now, in today's video, we'll be discussing about the S3 cross region replication.Now, in many of the organizations, there are compliances that has a lot of requirements related tohow the data must be replicated across multiple distances.So let's take an example where you have an organization and all of the organization's important datais part of the S3 bucket in Mumbai region.Now, in case as a worst case scenario, if this Mumbai region becomes unavailable due to some issue,then all of the data as part of the organization will become inaccessible.And this is the reason why.Compliances, they have a requirement that the data must be replicated across greater distances.So if you're storing your critical data in Mumbai region, also make sure that the data is replicatedin a different region, let's say Singapore.And this gives a better flexibility in case of a worst case scenario of a region going down.So this feature of replicating the data of S3 bucket across multiple regions is achieved with the helpof cross region replication.Now before we go ahead and start with our practical, one important part to remember is that for theS3 Cross Region replication feature to work both the source as well as the destination bucket must havethe versioning feature enabled.So with this base set, let's go ahead and try it out.So currently I'm in my S3 console.Let's go ahead and create a bucket.Let's just call it as my CP Labs hyphen Mumbai bucket and this will go ahead and create it within theMumbai region.Let's create a bucket.And in a similar way, let's also go ahead and create one more bucket.Let's call it as my labs hyphen Singapore bucket.And let's this time we'll go ahead and choose the Singapore region.Great.So both of our buckets are created.First, let's go to the Mumbai bucket here and within the management.Let's go a bit down.You have the option for replication rules.So if you go ahead and create a new replication rule here and here, immediately you get a warning statingthat the replication requires versioning to be enabled for the source bucket.So this is fine.You can go ahead and enable the versioning from here.Great.So it states the bucket versioning has been enabled.You can just give it a rule name, let's say Mumbai to Singapore.So this is the source bucket.And now you'll also have to specify the destination bucket, whether it is in this account or in thedestination account.So I'll just say choose a bucket in this account.Let's quickly do a browse S3 and we are interested in the Singapore bucket again for the Singapore bucketit is stating that the replication requires versioning, so let's go ahead and enable versioning forSingapore bucket as well.And once then the next important part is to choose a Iam role.So within here I'll go ahead and create a new role and then let's go a bit down, we'll go ahead andwe'll do a save Perfect.So our replication rule has been created, you see automatically a new Iam role also has been createdand here it basically gives you the details associated with it.So now let's go back to our Mumbai bucket here and just to test if everything is working, let's goahead and upload an object in the Mumbai bucket.I'll add a file so I have a temp data dot txt that is available.We'll be uploading this specific file.Perfect.So object is available in the Mumbai region to see if it is also available in the Singapore region.Let's open up the Singapore bucket now.At this stage here you see the object is not available primarily because it takes a little amount oftime for the replication also to complete.So let's quickly wait for a moment here.And now you see our object is also available within the Singapore region.Great.So I hope at a high level overview you understood the importance of replication across multiple regionsand how it can be achieved in S3 with the help of cross region replication feature.So with this we'll conclude today's video.