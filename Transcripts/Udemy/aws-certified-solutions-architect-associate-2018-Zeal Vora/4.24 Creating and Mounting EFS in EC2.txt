# 4.24 Creating and Mounting EFS in EC2

Hi everyone, and welcome back.Now in the earlier video we had a great introduction to the Elastic File System and we looked into thebenefits that EFS provides.Today is going to be the practical video where we'll look into the entire workflow.So the first thing that we are going to do is we are going to go to the EFS service.And within here you can create a new elastic file system.You can give a name.Let's just call it as demo hyphen EFS.And once this is done you can select the VPC.Now we were discussing that in EFS.It creates the mount target in different availability zones as part of the VPC.So you can select the VPC where you want the EC2 instance to connect to your file system.Since I only have a default VPC, we'll use that in case if you like, you can also do a lot of customizationso you have automated backups, performance, etc. we'll just keep it very simple.All right.So we'll just use demo EFS as the name.We have a default VPC.And we'll create the file system over here.And great.This was pretty quick.Our file system is now ready.Now, one more thing that you have to ensure is that for entire testing, you will also require theEC2 instance where you can mount your EFS.So let's also do that.So within the instances let's launch two instance over here for our testing.Let's just call it as app servers.We'll use the appropriate key.The number of instances will be two.And I'll launch the instance once the instances are launched.One more thing that you want to keep a note is that we were discussing that it uses the NFS protocol.So you have to make a NFS mount to your EC2 instance.So the security group should allow that.So let's do one thing for any one of the instance over here.If you look into the security there is a security group over here.Or if you're not able to find from here you can go to the instance console, open any one of them.There is a security group within your.Just edit the inbound rule and allow NFS from say, one 7230 100 16, which is the VPC Cidr block,and I'll click on save rules.Now we were discussing that there are mount targets that are created in each availability zones.Let me also show you.So for this elastic file system if I open this let's maximize this within the network.Over here you will see that I have three mount targets that are available each in a single availabilityzone.So Mumbai has three availability zone.And this is the reason why you get three distinct IP address as you can see.But if you would have created it in say, North Virginia, North Virginia has around six availabilityzones.So you will see six mount target.Each mount target has a different ID.So this is one point that you have to keep a note.Now once you have the basic things configured now it is time to perform the mount to the EC2 instance.So let's do one thing.Let's go back to the instances.And we'll quickly connect to both of the instance over here.In fact let's refresh.These are the two instances.Let's connect to the first EC2.Let me add the IP address over here.You can either use the EC2 key or you can also directly connect to the EC2 instance using the EC2 connectfrom the browser itself, whichever way works for you.So this is the first instance.Then let's copy the IP address of the second instance and from a new tab, let's add the IP addressover here.And great we are connected.Let's quickly switch to the root user in both the instance.Great.So now next thing what we are going to do.As of now if I just run the DF command.You see there is no elastic file system that is mounted.So first before we mount the elastic file system on the instance we have to create a folder.Let's just call it as shared storage.All right.Let's press enter.And the same command I'll run in the second instance over here.That is great.So once the shared storage is available now we are going to mount Elastic File System and associatedwith a shared storage, so that when you store some data in shared storage, it will get stored in theEFS.Behind the scenes for this, one of the important things that you will require is the DNS name.So this is the DNS name.And from here we can perform the mount operation.Now for easier reference I have documented the mount command over here.You will find this command after this video for reference.So what we are doing, we are using the mount to mount the NFS and there are some options related toit.The most important part is you have to replace the EFS DNS with the actual DNS.And this is the path of the shared storage.So in our case this was the path which is shared storage.So you can just replace this part over here.And you also have to replace the EFS DNS.So let's copy this and I'll just add it over here once.Then you can just copy this entire command and add it as part of the first instance.Make sure there are no errors over here.Similarly, in the second instance also we'll run the same command.So in order to verify if everything is working, what we are going to do, we are going to go to theshared storage.We are going to create a file called as server 01. txt.And from the server number two we'll verify.So in server number two also we'll go to the shared storage.If you do a ls you see the server 01. txt is available.Same in the server zero two.Also just verify server 02. txt.If you are able to create answer is yes and from the instance one just do ls and both of the files areavailable.Great.So I hope with this you understood about the EFS from practical point of view.Again, after you have completed the practical, make sure that you delete the resources.For example, if I run the mount command over here or the better way, let's run the DF here.You see that you have the elastic file system that is mounted on the shared storage.So what we are going to do, we are going to quickly unmount it.So let's just go back.We'll just unmount.the shared storage.If I run the DF command, the EFS has been unmounted.Same command will run in the server.Number two.It is showing devices busy.Why?Because we are inside the shared storage.Let's come out of it.And let's run the umount command here.And here also you see the file system is unmounted.Now, since the EFS is unmounted now although the folder will be present, but inside the folder thefiles will not be available because we know the files ultimately are within the EFS.And once I've done that, you can also go ahead and delete the EFS file system in case if not required.So let's specify the file system ID and we'll delete.So it is going ahead and deleting the mount targets that it had created for the file system.And also note that the instances that you have created make sure you terminate it after the practicalso that you are not really charged for it.So that's the overview about the EFS from the practical side.And with this we'll conclude today's video.