# 6.8 Practical - AWS Elastic Beanstalk

Hi everyone, and welcome back.Now in the earlier video, we were discussing about the basics of the Elastic Beanstalk service andthe advantages that it offers.Today is going to be the practical video on this.Now the practical workflow is going to look something similar to the image that you see.So first you have to create an application.Then while you are launching an environment inside the application, you can upload the appropriateversion and you can set the presets and associated configuration.And then you can manage the environments, the new updates that you make to the application accordingly.Now from the AWS console, let's quickly go to the Elastic Beanstalk service.Now within here, the first thing as we are discussing is you have to create an application.You can either do it from here or you can go to the applications from here you can create the application.Let's just call it as my first web app.And I'll go ahead and I'll create the application.Once the application is created inside the application you can create the environment.So let's set up the environment.So we'll be using this one, which is the web server environment.The application name is already pre-populated here.Along with that you will also require the DNS domain name.So this is something that you can add over here.Depending upon the requirement, you can just add any name that you'd like.Let's just call it as say Zalora just for an example.So this is going to be the DNS.You can check availability.It is available.So this is good.Now the platform type is the next important part.Depending upon how your application is designed you can choose the appropriate platform type.Just for simplicity we are going to use Docker.Now in the application code, you can either use the sample application that Elastic Beanstalk providesor in case if you'd like, you can also go ahead and upload your application code.Over here we are going to just use the sample application because we are looking into the workflow process.Now the next important part is the presets.Now do you want a single instance based environment or high availability based environment, etc..Generally, in production environment you will be choosing high availability.However, just for simplicity, for today's setup we are going to use single instance because we don'twant to be charged unnecessarily here.Once the preset is set, let's go to next.Now within here you have to create the service role and you have to create the EC2 instance IAM role.At this stage, none of them are created.So you can go ahead and quickly create the role over here.So for the service role you have AWS service.You have the Elastic Beanstalk environment.Let's do a next.Automatically the appropriate policies are selected and we'll create the role.This is the service role.Second is the EC2 instance profile.Let's also go ahead and do that.This time.The use case is a little different from previous.I'll do a next.Again a set of policies are automatically configured.The name of the role is also automatically added here.We'll create the role once both the roles are created.Now you can refresh this.You have the service role.You can refresh the second part.You have the IAM role.In case if you would like, you can also select the key pair.I'll skip it.Let's go to the next page.Now within here in case if you want the instances to be created inside a different VPCs, etc. thisis something that you can configure.However, this is optional.If you do not really add this automatic, the default values will be taken.Again, if you'd like database you can enable the database.Currently we are just keeping things default.However, we are just exploring what can be the possibility.The next part is related to the instance traffic and scaling.So if you would like to have CloudWatch monitoring the security group related data, this is the autoscaling group.Since we have selected free tier and single instances there, you cannot really select it.But in case if you have a load balancer environment, you can select multiple set of instances thatcan be created as part of Auto Scaling group.We'll just keep everything as default.Let's go a bit down.Now the instance type over here.Now this is where you have to be a little careful.So within here since it is a free tier environment, ideally you should see a free tier based instancetype.However Elastic Beanstalk does not do that at this stage.So let me just quickly give you an example.So I'll just open it up in a new tab.Whenever we launch a EC2 instance, let's go a bit down.In the instance type there is a specific instance type that has a free tier eligible.However, even though we had selected the free set of single instance free tier eligible, the instancetype that it is showing is not the free tier instance type.So you can just go ahead and select it over here.This is going to be the first priority.And the second priority we'll just say T3 dot micro.All right.This is one part.Next we'll just explore other areas.Am I?Everything will just leave it as default.The next part is related to monitoring.We'll just leave it as it is.You have the email notification in case if you would like.You can also have the platform updates, update level is patch only etc. so all of these integrationsare there.We'll go a bit down.We'll just leave everything as default proxy server.Currently nginx is supported for the Docker platform type.Let's go a bit down.We'll go to next.Now finally we are in the review screen.So these are all of the options that we have selected.Once you have verified it you can go ahead and create your Beanstalk environment.So now it is going ahead and launching the Elastic Beanstalk environment.You will also see the domain.So once your environment is ready you can open this domain and your application will be made availableto you.So let's quickly wait for a moment for this to be ready.Alright, so it took around 2 to 3 minutes and our environment.Health has changed too.Okay, let's quickly refresh.Things look good.Let's try to open the domain.And as expected, you see our application is working as expected.So you can also verify.In fact if you go to the EC2 console, if you look into the instances, you have the EC2 instance thatis created by the environment.So apart from that, you can also see all of the details as part of the central console.In case if you'd like, you can request the logs, you can look into the monitoring aspect etc. fromthe console itself.So this is the basics on the Elastic Beanstalk environment creation.Once you have completed the practical you can also go ahead and delete your application.So the way to delete it you can click on delete.And within here you can just specify the name of application.You can click on delete.So this will go ahead and delete the environment and the associated resources that you had created.So that's the overview.And with this we'll conclude today's video.