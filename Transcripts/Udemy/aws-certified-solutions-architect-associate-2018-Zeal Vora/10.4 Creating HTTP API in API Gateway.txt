# 10.4 Creating HTTP API in API Gateway

Hi, Adrian, and welcome back.Now, in today's video, we'll be creating our API through the API gateway and we'll look into the exactsteps that are involved.Now, for today's practical, the overall implementation architecture is very simple.We will be creating a HTTP based API and this API will go ahead and it will invoke a lambda function.So global users through the API can connect to the lambda function and execute some kind of a responseback.So let's go ahead and get started.Now the first step here is that we have to create a lambda function.So let's quickly open up the Lambda Service.And we'll be creating a very simple function.I'll just call it as halo function.The runtime would be Node.js.I'll go ahead and create a function.All right.So our function is successfully created now from the API gateway service.Let's also quickly open this up.Let's go ahead and quickly create an API will be using the HTTP API that you see over here.Let's click on Build.Now, within the integration, we'll just add lambda.And you can just select your helloworld function.And in the API you can just call it as HTTP API.You can click on next.In the resource part.You can just put a slash here.Let's do a next.We'll do a next.And we'll create it.Now at this stage we get to invoke URL.Let's copy this URL or in fact, you can just click on this.And in the output you should see hello from Lambda.So this is the very high level overview on how you can create a simple HTTP API through API gateway.Again, if you look, the overall step is very simple.So with this will conclude today's video.