# 3.38 New VPC Create Experience

Hi, everyone, and welcome back.Now in today's video, we'll be looking into the new VPC create experience in eight years.Now, at this stage, in a place whenever you want to quickly set up a VPC infrastructure, let's sayyou want to do some testing on a new VPC infrastructure.So when you want to set up a VPC, there are multiple components that you'll have to configure.For example, you'll have to configure subnets, route tables, net gateways, Internet gateways andensure the entire connectivity.So this not only takes time, but it's also a little cumbersome specifically when you want to performsome quick testing.So in order to avoid this, it has launched a new VPC create experience.So within this, using just a click of a few buttons, your entire VPC infrastructure gets created foryou.And this is one of the recommended ways for many of the testing environments as well as production.So let's quickly look into it.So currently I'm in the VPC console.Now, whenever you go ahead and you create a VPC, there are two options.First one is the VPC, only second one is VPC and more this VPC and more is where you can go ahead andset up the entire VPC infrastructure using few click of button.So if you'll see by default, they have added this specific CIDR block.So this is auto generated.And if you look into the infra force that is creating a VPC, it is creating a set of subnets, setof route tables and set of network connections.Now you can easily modify it.For example, you can say that we need one availability zone, the number of public subnet as one numberof private subnet, let's say zero.So you see this is automatically been updated.Similarly, you can also configure if you require a net gateway or not, I'll just select it as none.And similarly for VPC in point also you can select I'll just select it as none.So if you'll see, this is our infrastructure.Once you have configured this, you can go ahead and you can click on Create.Great.So our entire VPC infrastructure is created.So if I click on View VPC, this is our VPC.In fact, let's select our project VPC over here.If we go to SUBNETS, you see you have a subnet that is created.If you go to route table, a route table is also created.If you go to Internet Gateway and you see our Internet gateway is created, it is in the attached modeand also the appropriate routing is also configured.So this is the new VPC create experience quite useful.Once you have tested it out, you can go ahead and also go and terminate the entire VPC.Let's quickly do that.And great our entire VPC infrastructure is deleted.So with this will conclude today's video.