# 13.20 Overview of Parameter Store

Hi everyone, and welcome back.In today's video at a high level overview, we are going to discuss about the Parameter Store.Now at an overview parameter Store allows us to securely save and manage the configuration settingsand secrets in a central way.So let's go ahead and understand this through a diagram based workflow where you have a parameter storethat is configured.Now in this parameter store you can store various data like secrets, password, API keys, etc. soit can look something similar to this.So there can be multiple parameters like the host, the password, the db username, etc. which willbe stored and any application that is running in your environment.This application can send a query to the parameter store asking for example, what is the dev db host.So in this case the dev db host is dev hyphen dbcp.And this is the value that will be populated and sent back to the application.Now one of the great benefits here is that you don't really have to hard code all of these details insideyour application, and this can make things much more better and scalable.Now in the parameter store there are three different type of parameters that are available.Parameter is basically any piece of data that is stored in the parameter store.So there are three different type of parameters that are available.One is the string, second is the string list and third is the secure string.Depending upon the use case, you will choose one of these parameters.Type to store your data.Now if you look into the screenshot, I have three parameters that are available.Each one is using a different type.One is based on string, second is based on string list, third is based on secure string.So at a high level.Overview.When we create a new parameter, it is important for us to decide which one of the parameter type shouldwe choose.So let's go ahead and quickly look into each of these types.The first parameter type is the string.Again, this is the simplest one where it can be used to store the plain text data.Now this parameter type you can use to store some non-sensitive information like the environment name,any feature flags, etc. so in the screenshot here you see the name of the parameter.Here is demo hyphen string.And in the value you have simple 12345.It can be any other value as well.The second parameter type is the string list.This is basically a comma separated list of plain text strings.So in case if you have multiple set of IP addresses, multiple set of email addresses, etc., you requirea list type of structure.So in this case you can use this over here.So you see you have a comma separated values.The type is string list.And the third very important parameter type that we should definitely know is the secure string.This parameter type is used to store any encrypted value.So any secrets that you might want to store as part of the parameter store it should use the parametertype of secure string.Why is it secure?Because this parameter type makes use of KMS to encrypt the data.So this is the basics.Let's go ahead and look into it from the demo perspective.Now for today's demo I have three parameters that are already created.Note that we'll have a separate practical video.First, it is important for us to understand the concept.So here we have three parameters.Each parameter has a different type demo hyphen.String has a type of string.Then demo hyphen string list has a type of string list and demo hyphen secure string has a type of securestring.So let's go ahead and open the first one, the second one, and the third 1 in 3 separate tabs.First we are interested in the demo hyphen string.So if you will see the type here is string and this is the value 12345.Then you have the demo string list here.As we were discussing you have a list type of structure.So you have one comma two comma three comma four comma five.Similarly you can have list of IP addresses or list of email addresses, etc..So this is where string list is generally used.However, the most important of all that we should be knowing is the secure string.Generally, if you see the type is secure string, one thing that you see the value by default is notpopulated.So for string you could easily see the value the string list.Also, you could easily see the value, but the secure string, the value you are not able to see.So in this case you have to explicitly click on show decrypted value.So now it will decrypt the value using the KMS.And you will be able to see the sensitive value that you have defined over here.Note that here the KMS permission is important.If a user does not have KMS decrypt permission, then he will not be able to view this specific value.Now generally when you create a parameter, it basically gives you various information like the typeof parameter that you would like to choose based on the exact type of data that you might have.You might choose string, string, list or the most important is the secure string.Secure string.As we're discussing, whenever you select that, then you'll also have to choose the appropriate columnsbecause it will use the KMS to encrypt the necessary data.So this is also something that you have to keep a note of.Once you have selected that then you can just store the appropriate value.And that's the overview.Again we'll have a separate video.I just wanted to give you a gist.Now just for quick revision, I have also created a table so that at a later stage you'll be able tounderstand the difference over here.The table contains the normal use case for each type and that example value over here.So in the string list you have comma separated value secure string.The encrypted is yes.And example value can be anything.The only thing is that this value will get encrypted using the KMS.And while decrypting the KMS decrypt permission should also be present.So that's an overview of the parameter store.And with this we'll conclude today's video.