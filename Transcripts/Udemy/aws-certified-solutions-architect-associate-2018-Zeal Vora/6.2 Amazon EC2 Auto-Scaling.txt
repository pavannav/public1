# 6.2 Amazon EC2 Auto-Scaling

Hi everyone and welcome back.So in today's video we are going to discuss about the EC2 Auto Scaling feature.Now at a high level overview.Using the EC2 Auto Scaling, it allows us to automatically adjust the number of EC2 instances withintheir environment based on various factors like the load.For example, at this stage you see there are two EC2 instances that are running and they are managedby EC2 Auto Scaling Service.Now let's say that the load on the server increases.This can be say CPU utilization and so on.In such case, the EC2 Auto Scaling service will launch additional set of EC2 instances so that it canmanage the overall load.Now, in a similar way, if the load on the server decreases, then these set of additional EC2 instanceswill automatically be removed by EC2 Auto Scaling service.So let's do one thing.Let's have a quick demo so that we understand this topic post, which we'll be discussing about fewthings in more detail now.For today's video, I already have the EC2 Auto scaling configured, and if I quickly go to the EC2service over here, let's go to instances.At this stage You see, you have one instance of the CPL labs app that is running.Now.This instance is managed by the auto scaling.How do we know?If you look into the instance management you see this is the instance id c74 and it is the same as yousee over here.So EC2 Auto Scaling service is managing this part.Now as we were discussing that when the overall load on the server increases, then the auto scalingcan automatically add new set of servers.Let's also go ahead and verify this.Now what I am going to do, I am artificially going to increase the load on the server so that we understandthe end to end workflow.Now from the terminal over here, let me quickly switch to the root user.What we are going to do.We are going to run stress so that it increases the CPU load.And then we'll see on how the EC2 auto scaling will increase the number of servers accordingly.Now before we do that, one thing that I wanted to show you is that as part of this, there is one policythat is set over here which says that if the average CPU utilization is more than 30%, then it shouldincrease the number of servers.So let's go ahead and quickly increase the load on the server.We'll be running the stress command over here.Now let's also connect to this through one more tab.I just wanted to show you the load.So if I run the command over here you see the load has started to increase.And the command that is taking up most of the load is stress.So you have 25, 25, 25, 25.So slowly the load will increase.Now what will happen is the autoscaling.Since it is monitoring the CPU usage of the EC2 instances, it realizes that the load has increased,and then it will automatically trigger a event where additional set of servers will be launched.So let's quickly wait for a moment for this step to happen.Alright, so now the load on the server has increased significantly.So now let's just do a Ctrl C.And now what I'm going to do, I'm going to just refresh to see if additional instances are launched.And now you see there are two additional instances that were launched automatically by EC2 Auto Scalingservice.So this part of automatically launching the EC2 instances based on certain factors like the CPU utilizationor any other custom metrics.This is one of the great features of this specific service.Now, since we are going to decrease the load over here, I'll just do a Ctrl C so that our stress keepsthe server load below since we have closed it.Now if I run the top command over here, the load will slowly decrease so it will take slight amountof time.The load will decrease, it will reflect as part of the instance, and auto scaling will go ahead andremove the additional instances that were launched.So let's quickly wait for that moment.And till then let's discuss about other topics.Now, before we proceed further, one point that you have to remember is that the scaling or the aspectof scalability, it is basically the ability to increase or decrease the compute capacity of your applicationdynamically.Many times in exams you will see the scalability, which might be confusing.So this is one part that you have to keep a note of.Now when we discuss about the EC2 Auto scaling, it offers several methods to adjust the scaling tobest meet the needs of your application.So again, this is quite detailed.This is not a very simple service.There are a lot of configuration options that you can use.For example in EC2 auto scaling there are multiple ways to scale.One is the simple manual scaling where you can manually increase the number of instances and decreaseaccordingly.Then you have the dynamic scaling.Then you have the scheduled scaling.You also have predictive scaling.So there are a lot of options that are available over here.So let's go ahead and discuss about them at a high level.Overview.Manual scaling is where the user manually adjusts the desired number of EC2 instances.This is simple to understand.You have an interesting one, which is the scheduled scaling, where the scaling action can occur ata specific time based on the schedule that you define.For example, if you want to add instances at 8 a.m. in the morning and remove at 6 p.m. in the eveningevery weekday.So based on the scheduled pattern, the auto scaling will automatically increase and decrease the numberof instances.Then you have the dynamic scaling.In this case, it will automatically adjust the capacity in response to real time application demandor maybe CloudWatch alarms.So today's demo was based on the dynamic scaling where based on certain CPU usage over here, when itexceeded, then the new set of instances were launched automatically.You also have predictive scaling, which uses the machine learning to predict the future traffic andadjust the capacity ahead of time.So a lot of flexibility and depending upon the requirement, you can choose one of the scaling types.Now for today's video, if you will see, we had chosen this specific type, which is the dynamic scalingpolicies.However, if you go a bit down, there are other type of policies like predictive scaling policies thatare available.You have the scheduled actions that are available.This is something that we saw.So in schedule action, you see you have the start time.You have the end time and time zone aspects because you are setting the schedule.So this also proves to be very important.Now if I'll quickly do a schedule action, one of the important part is the recurrence.So whether you need it every five minutes, every week, every day and so on.And then you can also specify the cron.You can specify the start time over here as well as you can specify the end time based on the requirement.This is one part.Then we were also discussing about the dynamic scaling policy.So within let me also quickly show you.So the policy that we had set as of now was based on the CPU utilization.However there are other aspects as well.Like for example network in, network out, application load balancer request count per target.You can also have custom CloudWatch metric and so on that you can add.Again, dynamic scaling by itself provides a lot of options.However, we are discussing at an overview to begin with.Alright, so now since the load has decreased, let's go ahead and quickly refresh to see if instancesare still present.And now you see you only have one EC2 instance that is running.And if you see there were two more EC2 instances and both of them are terminated, you only have oneinstance that is currently running.Great.So that's an overview about the EC2 auto scaling and the benefit that it offers.And with this we'll conclude today's video.