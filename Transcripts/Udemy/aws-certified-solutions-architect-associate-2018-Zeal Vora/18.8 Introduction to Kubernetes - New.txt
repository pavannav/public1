# 18.8 Introduction to Kubernetes - New

Hi everyone, and welcome to the introductory video to Kubernetes.Now, before we proceed further, one thing that I wanted to share is that personally, I have useda wide range of container orchestration tools that are available in the industry.It can be Docker Swarm, it can be Amazon, EKS and various others that are available.But by far one of the best ones that is available in the industry is Kubernetes.So we are going to discuss about Kubernetes in great detail step by step.So with this base set let's go ahead and understand the basics of Kubernetes.Now at a high level overview one point to note is that Kubernetes is an open source container orchestrationengine developed by Google.So although it was originally designed by Google, but now it is maintained by CNCF, which is the CloudNative Computing Foundation.Now with this base set, let's go ahead and look into the architecture of Kubernetes at an overview.There are two important components of the Kubernetes cluster.First, very important one is the control plane.And the second one is the worker nodes.Now control plane is the actual Kubernetes cluster.So all of the main components that are part of Kubernetes, they are referred to as the control plane.And the worker nodes that you see on the right.These are the actual servers where the container applications run.Alright.So let's also look into the workflow at a high level overview.So discussing about the workflow, the user communicates to the control plane and provides the necessaryinstruction to run the containerized applications.For example you have a user.User will have some requirements.For example, user might want to run two containers of nginx, or user might want to run an applicationcontainer, but not on the smaller server on the larger server that is available here.So all of these requirements, the user will submit it to the control plane, which is the Kubernetesmaster node.Control plane will interpret the requirement, and it will go ahead and launch the appropriate containerin the notes that are available as part of the overall cluster.All right.So this is a very basic high level workflow.Now before we proceed further, let me also quickly show you on how exactly it looks like so that thingsare much more clear.So within one of the cloud providers I have a Kubernetes cluster that is running.So if you see this is the Kubernetes cluster.So when we discuss about Kubernetes it is primarily the control plane.And also the associated worker nodes where the control plane can assign the containers to.So if I quickly open this let's look into the worker nodes as well.You see there are certain worker nodes that are running.And these are the nodes where the containers will be assigned to.So let's also go ahead and look into it from the UI perspective.So this is a GUI associated with Kubernetes which gives you a basic overview on what exactly is runningin Kubernetes.Because it can happen that there are tens of thousands of containers that are running, and variousother things are set as part of Kubernetes cluster.So if you have a GUI, it becomes much more easier for you to go through.So if you currently see as part of this Kubernetes that is available, there are in total of three pods.So pods are something similar to the concept of containers.So you can consider that there are three containers that are running as part of Kubernetes environment.Now if you open this it will give you details associated with it.For example, it gives you the IP address associated with it and various other details related to it.Along with that, one more thing that I wanted to show you.In fact, let's go a bit down when you explore nodes.So as we are seeing that there are total of two nodes that are available as of now.So this is similar to the diagram where you have one node and you have two node, you have one nodeand two node.And it also shows you the overall utilization.For example, if I open up one of the node, it gives you great detail related to the allocation ata CPU level, at a memory level, and all of the important things.It is also showing you what are the containers or what are the pods that are running as part of thisnode.For example, as we're discussing that user when he submits the requirement to Kubernetes, statingthat I want to run two containers of nginx.Now Kubernetes has that intelligence to determine which will be the appropriate server in which thecontainers can run.And you can also see.So when you open up the appropriate node, you can also see what are the pods or what are the containersthat are actually running on this specific server.So let's do one thing.We are going to take a simple example just for this demo where at an overview, we want to run one containerof Apache as part of our Kubernetes cluster.So currently my local workstation is associated with Kubernetes cluster.So if I just do cube CTL get nodes.Now again note that this is a demo video.I just wanted to show you the basic.Now if you see as we're discussing, even in the G.We saw that in total there are two nodes that are available.And when you instruct Kubernetes that I want to run a specific container, Kubernetes can select oneof the node based on the scheduling constraints.So let's take an example here.So I'll go ahead and I'll run a command which basically will launch a container based on the Apacheimage.Let's go ahead and press enter.And it says created.Now note that currently I'm not logged into the Kubernetes server for example.Currently, when I submit a requirement to the control plane, you do not need to log in to the controlplane.Instead, you need to have the address of the control plane along with the authentication credentials,and then you submit the request to the control plane address, whatever URL is available, along withthe authentication to submit the overall request.So at this stage, this is the requirement that we have given to Kubernetes stating that I want to createa container based on the Apache image.Now if you run the kubectl get pods command.Now again, at this stage you don't really need to know the commands, just understand it at a highlevel.Overview.So you see that we have one pod or one container that is running over here.It was just started 67 seconds back and it is in the ready status.Now you might also wonder in which worker node is this container running.So let's also try and find it out.So if I go to node let's open up one of the nodes.Let's see if the Apache is running there.And in fact yes it is.So this specific container is running on the second node that we see over here.Similar to this you can also have wide range of requirements.For example you can say that I want to run a container but not in some random worker node.Only in a worker node that has minimum of four GB Ram available and certain amount of CPU capacity thatis available.So all of those scheduling related constraints also you can set as part of your requirement.All right.So at a high level overview, I hope with this you understood what Kubernetes allows us to do.Now the thing that we discussed as part of today's demo is a very, very basic example.Kubernetes has a lot of interesting features that are very useful from the production environment perspective.Alright, so at a high level overview, note that Kubernetes provides a lot of features and is designedon the same principle that allows Google to run billions of containers a week.In fact, I have taken this line directly from the official Kubernetes documentation.So here you see it says production grade container orchestration.And within here under the planet scale, it says that design on the same principle that allows Googleto run billions of containers a week.And this is truly amazing.And one thing that I can say is, yes, Kubernetes can scale very, very well.However, when you discuss about such a large scale, you should also have that level of expertise intoKubernetes to create and manage the overall environment.Now, when we discuss about some of the popular Kubernetes features, one of the very important oneis related to the pod auto scaling.So let's say that you have certain containers that are running that can serve the base traffic, butyou have a holiday season coming and suddenly you got a spike in traffic.So Kubernetes can actually scale in or scale out your containers based on the overall traffic, whichis very interesting feature and lot of organization makes use of that.Kubernetes also supports service discovery as well as load balancing.It supports self-healing of containers.It supports secret management, automated rollouts and rollbacks, and various others.Now again, at this stage, we wanted to keep it very simple, where we discuss at a high level overviewon what Kubernetes allows us to do.And in the upcoming videos, we are going to explore some of the very important features of Kubernetesin great detail, so that you can master this specific tool from the production environment perspective.So with this, we'll conclude today's video.