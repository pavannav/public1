# 4.16 Overview - Static Website Hosting with S3

Hi everyone, and welcome back.In today's video, we are going to discuss about one of the interesting features of S3, which is relatedto the static website hosting.Now a lot of organizations, they make use of this specific feature quite heavily.So it is important for us to understand on what this is all about and how to configure this.So let's go ahead and get started.Now before we look into the static website hosting, it is important for us to understand the challengethat organizations use to face before this feature was introduced.Now let's take a simple example where you want to host a static website.Now in such cases, where will you host it?You will have to probably create and manage an entire server and its related infrastructure on a cloudprovider.For example, let's say that you have a static website and you have a AWS account.Now, in such cases, what you will have to do, you will have to create a infrastructure in AWS.So you'll probably have to create an EC2 instance.And if you want the static website to be highly available, you will also have to create a load balancerand maybe few more set of EC2 instances so that even if one EC2 goes down, you don't really have toworry and the load will also get balanced.This is the first part.Now, just by launching EC2, will you be able to host static website?Answer is no.You will have to configure some form of a web server like Nginx Apache.Maybe you will have to configure certain rules on that web server and so on.The second part is the management.Just by running the EC2 instance is not enough.As part of security you will have to do regular vulnerability assessments, regular patching with thelatest security updates and so on.So all of this is a big hassle if you want to just host a basic static websites.So in order to make things much more simpler and also cost efficient, the S3 has a feature of staticwebsite hosting.So now what you can do is using this feature.You can upload all of your static website to S3 bucket, and you will be able to host the entire websitefrom S3 bucket.And this can make your life simple and very cost efficient.Now, before we look into the demo, few points that we have to keep a note of when we discuss aboutstatic website.In this individual web pages include static content, so they might also contain client side scripts.So static content can be HTML, CSS, etc. however, client side scripts using JavaScript is also allowedas part of the static website hosting.So you will be able to do various interesting things like you can create a to do list using the clientside JavaScript.So a lot of flexibility is available.Along with that, note that dynamic websites that relies on server side processing.This is not allowed for example or Python, JSP, ASP.Net all of these are not allowed as part of staticwebsite hosting.So this is the basics.Let's go ahead and look into how this works.Now for today's demo I have an S3 bucket called as KP labs my demo website.And within here I have two files that are available.One is index.html, second is todo dot HTML.Now let's say that these are some website files and I want it to be available over the internet, soI no longer have to create EC2 instance, install web servers like nginx Apache, configure them.Nothing is required.Just upload these files to S3 bucket and you have to enable the static website hosting.So let me also quickly show you.So within here you see I have static website hosting that is enabled as part of this.And it is also giving me the endpoint of the website.So if I quickly open this within here, you see we are getting our first static website.Let's host it in S3.So this is one of the very simple and cost efficient ways to host static websites without need to createthe servers.Now when we discuss about static websites, it is not just a simple HTML and CSS file.As we are discussing client side scripts, using JavaScript is also allowed.So let's also have a demo on this.So when I open two HTML over here you see this is S3 hosted interactive to do list.So within here let's say launch video on static website hosting.I can add a to do.You can say part two, you can do a to do and so on.So there is a lot of interesting use case that you can achieve using the static website hosting.I remember when I was working in an organization, we used to have an entire subdomain of the website,and everything was hosted as part of S3 using the static website hosting feature.So it proved to be very useful since we did not have to manage servers, it was cost efficient and secured.Now, before we conclude, one point to note is that for the S3 static website hosting to work, thereare two important areas.One is that the bucket must be publicly accessible because once you have your static website in S3,you want people to be able to access it, so the bucket must be accessible publicly.This is the first part.And then you also have to enable the static website hosting feature.As you can see in the screenshot, once both of these are properly configured, things will work perfectlywell.So that's the overview about this specific feature.And with this we'll conclude today's video.