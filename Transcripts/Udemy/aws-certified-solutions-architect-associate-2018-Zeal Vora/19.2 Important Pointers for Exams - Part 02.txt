# 19.2 Important Pointers for Exams - Part 02

Hi everyone, and welcome to the second part of the important pointers for exams.And let's get started.Now the first important thing that you need to know is associated with the identity and access managementwithin IAM.Again, there are multiple subtopics.First been the IAM user.Now we already know iam user represents a user and associated with this user.You can go ahead and assign permission.Some user might have administrator access, some user might have a three full access and others.So this is one part.Also note that whenever you create a IAM user, you can also create a access and secret key associatedwith this user.Along with that, by default, whenever you create a IAM user, that user do not have any permissionsin case if you want a user to be able to do something in a place, you have to add associated IAM policy.So this is where the second pointer comes into picture associated with the IAM policy, which allowsus to define a certain set of permissions and based on the IAM policy that you add, the access canbe granted or denied.You should also know the basics of the IAM policy.For example, in here the effect is allow action is easy to start and the resource is security group,so the user will be able to perform certain actions on a specific resource at this stage.Now, sometimes in exams they will give you a policy and you should be able to read it.The primary things to check is the effect, whether it is allowed or deny, what is the action and whatis the resource.Sometimes there is also conditional element that we'll be discussing in just a moment.The next important part is related to the IAM evaluation logic.This is something that you should know.Now first important part here is that all the decisions they start with the deny.So by default, if the user does not have any permission, he will not be able to do anything here.Now in case if a policy has both allow and deny, then deny will always take a higher precedence.Very important part to remember.If there is a allowing a policy and if there is no deny, then the final decision here would be allowed.Now, regarding IAM policy conditions are something that you need to know.So let's take an example.So let's say that a specific IAM user has two IAM policies.First policy, let's read it.So if it is a low action is easy to terminate instances and there is a condition of source IP of 1723110 zero slash 24 so this means that any traffic that is coming from this set of source IP addresswill be able to perform the easy to terminate instances here.Now, again, note it's not all the traffic, it's just if this policy is associated with the IAM userand if IAM users IP address is coming from this source IP, then the user will be able to terminatethe instances.So this is one policy.Now there is one more policy.Let's say that this is also associated with the IAM user.Now this policy has an effect of deny action of star, resource of star.So this will deny all the actions in place.But there is a condition.Condition states that if a string not equals and here easy to reach an episode one so this means thatdeny all the actions in a place except the one where string does not equal to the AC to region of APSouth one.So this region of AP south one of Mumbai is excluded from this specific policy.Now, when both of these policies are combined, then user will be able to terminate easy to instancesonly in the AP south one region through the source IP of 172 3110 or zero.In no other region user will be able to terminate the instances.So this is where the conditions and policy comes into picture.The next important part is the IAM group.Now we know that in the IAM group there can be multiple users that will be associated.So there can be a developer group, there can be a systems administrator group, administrator groupand so on and you can associate IAM policy to the IAM group.So let's say you have added a three full access policy to the IAM group.So all the IAM users that are part of the group will be able to perform full operations at S three level.The next important part is the IAM role.This is an important part of the exams as well.So note that IAM role can be associated with various services like AC two instances lambda and othersand you can associate iam policy to the iam role.Now if ESI for instance wants to perform some operation at the s three level, it is very importantthat you do not add the access and secret key in the AC two instance or in the lambda function.Instead, you add an iam role to these services and in the iam role you can add the appropriate IAMpolicy like S3 full access so that the application is running in an AC two instance or in Lambda willbe able to perform the appropriate operation.The next important point is related to the.Is organizations in.Yet there are two important feature sets that you need to know.One is the consolidated billing.Second one is the service control policies.Both of these are important and you should know what these are at.An overview.Now related to organization, there is one specific element in IAM that you need to know which is theprincipal or g ID or principal organization ID.So this validates if the principal accessing the resource belongs to an account in your organization.For example, if you look into this IAM policy, it is allowing the S3 three put object operation ona bucket call as a policy ninja.Now there is also a condition here now in your principal star and there is a condition which statesprincipal or ID is equal to the organization ID.So this means that it allows the account that is part of the organization to perform a specific operation.So in exams, if you see a use case where you want to allow access to a three bucket only to the accountsthat are part of the organization, this is something that you can make a use of.The next important part is related to the three service.This is an important service for exams.And again, there are multiple subtopics under s three.One of the primary ones is the storage classes.As we know, there are multiple set of storage classes available in S3, and you should know at a highlevel overview about each one of them.Now let's look into some of the important keywords that you can look into the exam questions to understandwhat are the storage classes that can be useful.First one is that in case if the files have an unpredictable pattern and you want to minimize the storagecost, then you can go with the S3 Intelligent tiering, because based on the pattern intelligent,you can go ahead and add the appropriate storage class, which can minimize your cost.And also note that the lowest cost storage is the glacier deep archive in case if a question statesthat you have large set of data and you want to insure that it goes to the lowest cost storage.Deep archive of glacier is the right option.The next important pointer is related to the versioning.At the overview, you should know the basics of versioning and how it protects against the accidentaldeletion coming to the deletion.Also note that you have an option of adding another layer of MFA.So this means that if you want to delete a specific object along with the basic IAM permissions thatyou need, you also need a multi factor authentication.Only after two forms of authentication, you will be able to delete a specific version or change theversion.So as three versioning plus MFA is something that can also be achieved.Along with that, let's also look into some of the three features.First one is related to the Cross region replication, which allows you to replicate an object acrossmultiple S3 buckets in different set of regions and for cross region replication, versioning is mandatory.Second is the lifecycle policies.You should know the basics of lifecycle policies and how it allows you to move the object to a differentset of storage class depending upon the oral retention period and other configurations that you add.Now, along with that, you should also know that the three object lock legal hold prevents an objectversion from being overwritten or deleted in case there is a use case where you do not want a versionto be overwritten or deleted.The next important part is associated with the glacier storage class.Now, when we discuss about the glacier storage class, again, there are multiple retrieval optionsthat are available.For example, you have an instance retrieval, you have a flexible retrieval and so on.And at an overview you should know when you should use one of them.Now, if you look into the instant retrieval, it allows millisecond retrieval of data.If you discuss about flexible retrieval, it arranges from minutes to two hour retrieval of data.And for glacier deep archive, you have 12 to 48 hours retrieval of data.Now, again in the exams you will have to take a look into the exact question on what exactly it wants.For example, if an organization is storing data in Glacier but they want an option where in case ifthey require data, the data should be available to them within few minutes.So in such case, you can make use of an instant retrieval option for such use cases, and similar goeson for the flexible retrieval storage as well.The next important feature of S3 is the transfer acceleration, which makes use of the cloud front edgenetwork to accelerate your uploads to S3.So in this diagram you can see that your way is three bucket.Now the users across the globe are making use of edge location to go ahead and transfer the data toS3.The next important point is related to the S3 encryption.There are three primary types.You have one with the managed keys, one with the CMS manage keys and third with the customer providedkeys.You should know the basics of each one of them.Sometimes in exams they'll give you a use case where you'll have to select one among these.So you should know the difference between these.The next important point is related to the.Pre signed Earls.At this stage we know that whatever objects that you store in S3, they're private by default.Now, if an external user wants to download a specific object and he does not really have an account,etc. in such cases, you can generate a pre signed URL which allows the user to go ahead and downloadthe object in a limited time frame.So maybe for next 24 hours he'll be able to download the object.The next important point is related to the static website hosting.So generally you should know that S3 does allow static website hosting.Now in case if you have websites like HTML, CSS, client site, JavaScript images, etc..Static website hosting works pretty well.Now in the exams, if a question states that a website is based on these set of technologies, thenstatic website hosting is a perfect fit.But let's say if example states that the website is based on Python, then this is not supported aspart of the static website hosting.So in such cases, the other options that you can use is either lambda or easy two instances and oneof the benefits of static website hosting is that it is cost effective solution when compared to ESItwo.Apart from that, you should also know the basics of the S3 even notification which allows you to receivenotification when a certain type of events happens within your bucket.Maybe delete event, put, event, get event, etc. and you can go ahead and supply it to Lambda Sansand others.Apart from that, you should also know the basics of the S3 object lock.And in this there are two primary modes the governance mode and the compliance mode.Now, one of the good things about the compliance mode here is that the protection once set it cannotbe removed by any user, including the root account.Sometimes in exams you might see a question which will test your ability and it will ask you based ona use case that is given in the question whether you should choose a governance mode and a compliancemode, which one you should choose.So you should know the difference between both of them and answer accordingly.The next important point is related to the routing policies of Route 53.There are multiple set of routing policies like latency, geolocation weighted geo proximity, ETA Overview.Just keep a note again in the exams you might be tested where you have to select one of the routingpolicies based on a given use case.Now, when it comes to load balancing, you should know the different load Balancing solutions ApplicationNetwork and the Gateway Load balancer.Note that if in exams you see use case where a UDP protocol is used in the application, then in suchcase application load balancer cannot be used.APIs are primarily preferred for websites and applications at a layer seven like HTTP and HTTP is.So any applications that are not running in layer seven like HTTP IDPs, network load balancer can beused here.And you also have a gateway load balancer which are primarily used for virtual appliance like IDs,IP firewalls, etc..So again, in the exams, if you see a question related to traffic inspection or inspection, VPC,this is where the gateway load balancer comes into picture.For example, if you see your in the diagram, the source sends the traffic to the destination, andbetween the source and the destination, there is a gateway load balancer and associated with the gatewayload balancer, you have a specific appliance which can do the traffic inspection.It can be a ISE, IP is appliance, firewall, appliance, etc. Along with that related to the Elbenode that you can redirect the HTTP traffic to HTTPS using the listeners rules that are part of theALB.So you have a website.Someone might send an HTTP request, but you want that HTTP request to go to the HTTPS connection.So this is where the listener rules can come into the picture where the redirect can happen.Along with that, you should also know the basics of CERT manager.Now when we discuss about CERT manager, one very important part to note is that the CERT manager cannotrenew third party cert in case if you have a third party cert and will not be able to renew, it willalso not be able to rotate cert certificates.However, the certificates that are generated at the ACM level, it will be able to renew the certificatesdirectly.Also note that you can make use of the s config to check whether the certificates are nearing the expirationdate in case.If they are, then you can get an alert along with that at an overview, you should know the basicsof auto, which is the recovery time objective and the recovery point objective.Sometimes in exams they might give you a use case associated with an RPO, so you should know the differencebetween both of them.Now, related to the database, first important one is the Dynamo DB under Dynamo DB you should knowthe basic capacity modes.You have the on demand mode and you have the provision mode.You should know the difference between each one of them and the benefits that it provides.Apart from that, in Dynamo DB, you should know the basics of point in time recovery, where it helpsyou in the accidental write or delete operations.So with the point in time recovery.You will be able to recover the table at any point in time during the last 35 days.The next important pointer is related to the Dynamo DB accelerator, which can act as a cache, So allof the frequent read requests is something that can be added and it can deliver up to ten times theperformance improvement.Note that this is more to do with the read operation than the write operation.Discussing about the database technology, the next important one is the relational database service,and under this the read replica is important area so at the overview read replica allows you to createone or more read only copies for your database that is running.So all of the read requests you can send to the read replica where the write request can go to the primarydatabase.Now, in case in architecture, if the database reads are causing high IO and slowing down the overalldatabase, you can go ahead and decide to launch multiple set of read replicas where the read trafficcan be directed to.Next, important point is related to the multi availability zone architecture.So in multi AC you have a primary instance and a stand by instance.Again, the replication is happening.If the primary instance goes down, then the client can be directed to the standby instance, whichis running in a different availability zone.The next important point is related to the snapshot.So in case if you want to take a snapshot of the instance, you can go ahead and take the overall snapshot.Now, sometimes in exams, if you see a use case, let's say where you want to run a database in a testenvironment only once a month and the cost is a primary factor.This is where the snapshot can come into picture.For example, you run a database after the testing is completed, you take a snapshot of the databaseand you terminate the database instance.After some time when you need the database again, you can go ahead and recreate the database from thesnapshot, and this proves to be quite cost effective.In case let's say you have a database and you stop the database, even though the database is stoppedin case if it has a provisional IOPS, you will still be charged for the storage, although the databaseinstance will not cost you.But the underlying storage is something that will cost you.The next important part is the IDs proxy.You should know the basics of IDs proxy and the benefits like connection pooling availability that itprovides.Along with that, you should also know the basics of IDs custom in exams.If you see a use case where the organization needs a modification to the database environment and theunderlying operating system adds custom proves to be useful and to support it.Engines.Here you have the Oracle and the Microsoft SQL Server.Along with that, you should also know the basics of the Oracle database cloning.So using the database cloning, you can create a new cluster using the same Aurora instance that youhave Now.Creating a clone is faster and more space efficient than physically copying the data, using other techniqueslike restoring a snapshot.So you take a snapshot, you restore a snapshot.This takes a lot of time.If you have Aurora, you can instead go with the Aurora cloning.Along with that, you should also know the basics of encryption.Now, in this encryption, one important point that you need to remember is that you can only enableencryption for days.When the instance is getting created.After the IDs is created, you will not be able to enable encryption for the instance.Now, in case if there is a requirement where you need the database instance to be encrypted in suchcases, what you can do, you can go ahead and take a snapshot and through your you can go ahead andenable the encryption.Once you have the encrypted copy of the snapshot, you can go ahead and restore the DB instance fromthat encrypted copy and that new DB instance will be encrypted here.The last service that we need to know for today's video is related to Athena.At a high level overview.Athena allows us to run the SQL query and analyze the data that you have in various storage deviceslike S3 Bucket.So that's the high level overview.And with this will conclude today's video.