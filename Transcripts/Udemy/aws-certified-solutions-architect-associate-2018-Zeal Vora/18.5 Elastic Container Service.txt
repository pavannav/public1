# 18.5 Elastic Container Service

Hi, everyone, and welcome back.Now, in today's video, we are going to look into the basics of the elastic container service alsoreferred to as X.Now, at a high level overview, Amazon X is a highly scalable and fast container management service,so you can consider it to be something like a container orchestrator.So let's go ahead and understand it in a better way.So let's assume that you have an X order.Now X, since this is a specific service, it will have integration with other services, for example,easier.So X can fetch the container images from the easier.And then from this it can go ahead and deploy the containers to the set of easy to instances.And the second point states that you can use it to run, stop and manage the containers on the cluster.For example, within this diagram you see that there are two ESI two instance, and in SGC two thereis one container that is running.Now let's say tomorrow you want to stop this specific container or delete the container, You can doit centrally from the x console.So let's do one thing before we proceed further.Let's quickly look into it.Now for today's video.I have the x cluster that is already configured and similar to this specific diagram, this X clusterhas deployed a specific container in an AC two instance, and I also have the AC to instance that iscurrently available.Now let me copy the IP of the AC to and let me add it in the browser.And within you see you basically get a message stating it works.Now tomorrow let's say I want to deploy a new set of applications.Let's consider this to be an application one.And now we have a second application that is available.So what you can simply do is you no longer have to connect to the AC.To instance, X can do all the tasks for you, so let's go ahead and look into it.So if I quickly open up the X order, you see that there is one service called as a service which isrunning the Apache Web server in this AC two instance.So this service is running.Let's go ahead and quickly delete the service.And at this stage the service is no longer present.Now, if I refresh the page this time, you see it is refreshing and now it states the site cannot bereached.So our application has been removed.Now let's go ahead and deploy one more application.And within here we'll just specify another application.We had earlier deployed our budget.This time we'll be using engine.Let's go a bit down.Let's go ahead and deploy it now.Note that this is demo video.The aim is to just understand X at a very high level overview.So if I refresh at this stage, it is still deploying the task.So let's quickly wait for a moment here.And now you see our deployment of task is successful, so let's copy the IP of easy to once more.Let me add it in the browser and this time you see we get the welcome to Engine X page.Great.So I hope that this at a high level overview, you understood the basics of Amazon X Now again, justto revise both the IT workspace that used to come earlier and this welcome to Engine X they are nothingbut the docker containers that are running inside easy.Two instances since X is a container service, it works at a docker level, so it supports Docker containers,Docker images and so on.Now if you look into the X architecture at a high level overview, generally what happens, let's sayon the right hand side you have a server so there is nothing but you can consider it to be easy to instancein this case.So this easy to instance has an X agent and it also has a docker that is configured and this agent integrateswith the Docker and based on it, the containers are created or deleted automatically.And this X agent, it connects to the Central X service.Now let me also quickly show you this.Now, currently I'm connected to the AC two instance, which is managed under this service.Now, if I quickly do a system, it's status X currently, you see we have this agent that is in theactive and running stage.This is one part.Along with that, you will also see that we have the Docker service that is running.So you have active and running and if you'd like to see the containers, you can do a docker.So here you will see that we have one container based on Engine X image that is currently running inthis easy to instance X console.Let's go ahead and delete this.I deleted.And now if I run the same command again, you see that the engine container is no longer present.The only container is related to the X agent.Great.So I hope that this at a high level overview.You understood the basics of the X and with this will conclude today's video.