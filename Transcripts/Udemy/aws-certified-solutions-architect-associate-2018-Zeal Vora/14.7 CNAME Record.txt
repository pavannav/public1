# 14.7 CNAME Record

Hey, everyone.And welcome back.Now, in today's video, we'll be discussing about the basics of name records.Now, before we go ahead and look into the name record, let's quickly revise the basics of a record.So A which basically stands for address is one of the most basic types of DNS record, and it pointsa domain to a IPv4 address.For an example, demo dot dot internal directs to an IP address as 192168 1020.So this is basically a a record.Now the next type of record that is important for us to know is the name record.So see name record are useful for pointing a domain name to another host name.So for example, let's say you have a domain of example dot internal, and what you want is any trafficthat comes to example dot internal needs to be routed to a domain college labskin.So what you can go ahead and do is since KP Labs Dot n is a host name, it is a DNS, you can go aheadand create a CNAME record.So example dot internal C name to KP labs dot in.So now what will happen is when you go ahead and resolve example dot internal, ultimately it resolvesthe IP address associated with the KP labs Dot in DNS and that IP address will be returned.In this case it should be 1921680.5.So this is the basics of CNAME record.Let's go ahead and try it out now within our Route 53 hosted zone, let's go ahead and create a newrecord.Let's just call it as sea name.kp Labs dot internal and within the record type instead of a wheels.This time we'll select Sea Name and within the value.This time, instead of giving the IP address, you'll have to specify the domain.So let's say domain is KP Labs dot in.All right.We'll go ahead and create a record.So if you'll see over here, you have a name, Dot Labs dot internal.The type is sea name.And the value here this time is not the IP address, but instead it is the domain.Now, from the EC2 instance, if you quickly do a nslookup first, let's do nslookup on the domain ofKP labs dot in.So you have basically you have a IP address starting with 60 5.0.Now let's go ahead and do nslookup on sea name.kp Labs dot internal.And again now you see it is basically giving you the same IP address.And also along with this it is showing you the canonical name, which is KP Labs in.So basically the request when it goes to the sea name dot labs dot internal, it goes ahead and findsthe IP address associated with the KP labs dot in and that IP address is returned to you.Now one of the question that comes is what exactly is the use case of a CNAME record?So you can go ahead and simply create a record?This is very straightforward.So let us go ahead and understand some of the use cases with sea name record is very important.So let's go to the EC2 instance and let me go ahead and create a load balancer.All right.Let me quickly create a load balancer.So the type of load balancer, let me go ahead and use the application load balancer, the name, let'scall it as demo, and we'll do a next let's also select some of the availability zones.Let's go to security settings.We'll do a configure routing, let's register the name, let's go ahead and give demo target group,let's register a target.We'll select our EC2 instance here.Let's do a review and we'll do a create.Now, again, you do not really need to do that.I just wanted to show you an example and that's why we are creating a load balancer over here.Now once the elastic load balancer is created.So currently it is still in the provisioning state.Here you will see that you are not basically getting the IP address of the load balancer.So if you see load Balancer does not give you the IP address, instead it is giving you the DNS nameonly.Now, for example, what happens if you want to route a traffic?Let's create a new record.Let's say you have a record called as application.kp Labs dot internal, and you want to route thistraffic towards this specific load balancer.How do you do that, considering that it does not give you an IP address?And the answer to this is the name record.Now, since CNAME record allows you to specify a DNS name, you can go ahead and choose a record typeof DC name and you can go ahead and add a value here once.Then you can go ahead and create a record and now you see a record is created.So now any time a user visits the application dot labs dot internal turn, the request gets directedtowards this specific load balancer.So this is one of the use case.Now in AWS, there are a lot of other services which will only give you the DNS name, for example,CloudFront distribution.So even in such cases, when you create a cloudfront distribution, you have to go ahead and use a CNAMErecord there.A record will not really work.That's great.So I hope with this you understood the basics of CNAME record.We also look into certain use cases which allows us to understand the benefits that it provides in productionenvironments.So with this, we'll conclude today's video.