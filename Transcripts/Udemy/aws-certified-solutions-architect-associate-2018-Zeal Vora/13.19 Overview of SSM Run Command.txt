# 13.19 Overview of SSM Run Command

Hi everyone, and welcome back.In today's video, we are going to discuss about one of the new tools as part of the Systems manager,which is the run command.Now from the name itself will be able to identify that the run command feature will basically allowus to execute scripts or some remote commands on top of the instances that are managed through the systemsmanager.So here you don't really have to manually login to the server through SSH, etc. you can specify thecommands or scripts that you want to run on top of the instances, and run command will do that.For example, let's say that you want to install antivirus in all 1000 instances that are running aspart of your region, so you can specify the exact command to install the antivirus and run commandwill take care of the rest.So this is the basics.Let's go ahead and quickly look into it.Now for today's practical, I already have one EC2 instance that has the SSM agent installed, and thisis managed through the Systems Manager.So from the Systems Manager console we'll go to the run command over here.Let's maximize this.Let's click on run command.Now if you see in the command document there are multiple set of documents.For example, if you want to configure Docker or if you want to configure certain packages like theAWS package or for example, if you want to install Windows Update, in case if you have a windows basedserver, or if you have an Ansible playbook, you want to run the Ansible playbook.This is also something that is very useful.So today we are going to look into a very specific one related to the run shell script.So let's open this.This will basically give you the description in case if you would like you can click here.And within here you can specify the exact commands that you would like.For example within your let's say touch temp antivirus dot txt.Again this is a sample command just to verify, but generally you can install Antiviruses.You can configure packages based on the requirements.So this is the command that I want to run.Now let's go a bit down.The next important part is the target selection.So within here you can specify instance by tags.Many times what happens is you have application server.So you might only want to install antivirus on the application servers.And application servers may be 100 to 200.So you can choose a resource group.You can specify instance by tags.So this can make things simpler.Specifically when you have a lot of instances where you want to run a specific command.Today I'll just keep things simple.I'll click on Choose Instance manually.I'll select this instance manually over here.Let's go a bit down here in the output we can send it to S3 bucket.We don't really want that.We want to keep it simple.The CloudWatch alarm, SNS notification everything will just keep it as default.Let's click on run.So currently the status is in progress.Let's refresh.So now you see the status is success.So now what you can do.You can quickly connect to the instance either through the Connect or Sessions Manager or the CLI terminal.Based on your preference you can go to the tmp folder.If you do an ls you see there is a file called as anti virus dot txt that automatically got createdthrough the run command.All right.So I hope at a high level overview you understood the benefits of run command.As we were discussing.It is not just to run a specific command, but in case even if you have an entire set of Ansible playbooks,you can configure that Ansible playbooks so that all of the instructions that you have specified, everythingwill run on the set of instances managed through Systems Manager.So with this, we'll conclude today's video.