# 7.2 Architecture of Gateway VPC Endpoints

Hi everyone, and welcome back.Now in today's video we are going to understand the architecture of gateway VPC endpoints.Now at a high level overview the gateway VPC endpoint.It targets the specific IP routes in your route table in the form of prefix list, and based on this,the traffic is diverted towards the destination DynamoDB or S3 service.So let's understand this in a diagrammatic fashion where you have certain set of private subnets here.So let's assume that these private subnets host EC2 instances which does not have internet connectivityat all.So what they will do to connect to the destination services?They will use a VPC endpoint.So your VPC endpoint are created.And the EC2 instances are using this VPC endpoint to connect to the S3 service.Now the question that comes is how does the EC2 instance know that to communicate with S3 or to communicatewith the DynamoDB service, they have to use VPC endpoint?What is the deciding factor here?And this deciding factor is at a route table level.So what will happen is associated with these two private subnets.There will be a route table.Now this route table will have a default entry of local which you see in the first line, so that theinstances in the private subnet can communicate between each other.Along with that, the second entry that you see is the most important here.So what this second entry is it is the prefix list.So this prefix list is nothing but a set of IP addresses that belongs to S3 or maybe DynamoDB service.All right.So this is a list of Cidr ranges.So whenever EC2 instance sends traffic to any of the Cidr ranges that belongs to S3 or DynamoDB service,automatically that traffic will be routed to the VPC endpoint here.Now the question that comes is how can a customer know what are the Cidr ranges or IP addresses thatS3 service or DynamoDB service is using?So the answer is pretty simple.We need not know this prefix list, and the IP addresses that are added here are automatically managedby AWS.So anytime when there is a new Cidr range or a older Cidr range needs to be removed, AWS takes careof that.Now, let me also quickly show you this, so that our basic understanding of gateway VPC endpoint isclear.Now in today's video we have a gateway VPC endpoint here.And this gateway VPC endpoint is associated with a EC2 instance in private subnet.So let's also quickly open up the EC2 service here.Now this private EC2 year.This is where the gateway VPC endpoint is associated to.Now if you look into the route table, the route table that is associated is private subnet route table.Now if I quickly open this route table here, there should be two entries.One is the local entry years and the second one is the prefix list.Now if I open up the prefix list, you will see that it has in the entry section.It has so many Cidr ranges that are available.All right.So these are the Cidr ranges associated with a specific service.If you see this is the S three service Cidr range.So anytime the private EC2 instance tries to communicate to S three.Now behind the scenes we know DNS comes into picture and the domain gets converted to IP.So these are all the IPS of the S three service in this specific region.Whenever EC2 tries to communicate with any of these IP addresses, the traffic goes through the gatewayVPC endpoints.And just for the sake of practical, let's quickly look into it.If I do a ping on google.com, you see it is not working.However, if I try to communicate it to the s three service, it is working perfectly well through gatewayVPC endpoints.Now, one more important part that you need to remember is that not all of the services are supportedas part of the gateway endpoint, there are only two primary services that are supported.One is S3 and second one is DynamoDB.Now generally, if you look into the prefix list that AWS owns, you should also see one of the serviceas DynamoDB.You see you have the DynamoDB prefix list also available, and if you look into the entries, the entriesare quite lower when compared to S3.There are only three primary Cidr ranges for DynamoDB for this region.Great.Now, for sake of completeness of the video, so that whenever you revise, you will be able to understandall the concepts.I have also added a slide associated with the prefix list.As we already discussed, prefix list is nothing but a set of Cidr ranges associated with a specificdestination service is great, so that's a very high level overview about the gateway VPC endpoints.And with this we will conclude today's video.