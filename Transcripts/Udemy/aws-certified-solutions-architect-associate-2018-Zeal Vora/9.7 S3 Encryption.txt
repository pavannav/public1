# 9.7 S3 Encryption

Hayden.And welcome back.Now, in today's video, we'll be discussing about the S3 encryption.Now, before we go ahead and discuss more about it, let's take a simple use case to understand theneed.Now, we'll understand this with a simple example of a external hard disk drive.So many of you might be using some kind of a portable external hard disk drive, something similar tothe one that is being shown.So generally what happens is that since it is portable, a lot of people, they carry it while they'retraveling across multiple places.In fact, one of my friends also used to have this external drive and during travel he lost it.And this drive had a lot of sensitive information.So actually he had to change his credit card, debit card pins, his username passwords, etcetera,across a lot of websites.So this is a risk because if the data is stored in plain text within your hard disk drive and if a attackermanages to fetch it, then a lot of unwanted things can happen.So it is very important to be proactive.And this is the reason why a lot of vendors like Western Digital, they come up with features associatedwith a hardware based encryption.So what really happens is that you can go ahead and encrypt your data within your external hard diskdrive with the features of hardware based encryption.So even if your portable hard disk drive is lost and attacker tries to connect to his laptop, he willnot be able to fetch any information because everything within this disk is encrypted.Now, similar to the use case that we were discussing of a portable hard disk drive, even S3 primarilybecause since it is a storage service and a lot of organizations, they makes use of S3 to store verysensitive data, including their database backups and others.It is important for encryption feature to be there.Now AWS S3 offers multiple approaches to encrypt the data which is being stored in S3.Now, there are two major approaches that you need to understand.The first is the server side encryption, and the second one is the client side encryption.So the server side encryption is where a customer can request Amazon S3 to encrypt your object beforesaving it on disk in its data center and decrypt it when you go ahead and download your objects.So when you upload your object to S3, which is having a server side encryption before that data isstored in the disk, behind the scenes, S3 will go ahead and encrypt the data.And before you go ahead and download the data, S3 will go ahead and decrypt that data.So that is more of a transparent way.So that is part of the server side encryption.You also have client side encryption where you go ahead and encrypt the data from the client side andthen you upload it to the S3.Now discussing about the server side encryption within server side encryption, there are three primaryoptions that you can use depending upon the use case.The first one is server side encryption with Amazon S3 managed keys.So this is referred as the SSI hyphen S3 You also have server side encryption with customer managedkeys stored in KMS, also referred as the SSI hyphen.KMS.Third one is the server side encryption with customer provided keys also referred as the SSI hyphenC.So let's quickly explore what each one of these options provide.Now coming to the first option of SSI with Amazon S3 managed Keys, what happens is that in this approach,each object is encrypted with a unique key.So if you upload ten objects within S3, all of that ten objects will be encrypted with a unique key.Now this approach of SSI hyphen S3, it makes use of a strongest block cipher to encrypt the data,which is Ese hyphen 256 So the approach can be simplified with a diagram where you have a data one andthis data one is encrypted with one key.Now you also have a data two, and this data two is also encrypted with a different key.So the keys to encrypt different objects are unique in this approach.Now, second option here under the server side, encryption is the SSI, with CNC also referred as theSSI hyphen KMS.Now, in this approach, the encryption of data happens with the CMK, which is stored in the key managementservice.So you can go ahead and choose your own custom CNC as well.So encrypting the data with own CMK allows.Farmers to create, rotate, disable the CMCs.You can also have granular access control on the same and you can also enable auditing over here.So the overall concept remains to be the same.So you can create your own CMK in KMS and that same key can be used to go ahead and encrypt and decryptyour data in S3.The third option under the server side encryption is the SSI with the customer provided keys, alsoreferred as the SSI hyphen C So what happens in this approach is that the encryption key needs to beprovided as part of the request, and S3 will manage both the encryption and decryption options foryou.So let's take an example where you have a plaintext data and you want to encrypt it in S3 before itis stored.So along with the plaintext data, you also provide the key.So this key is being provided by the customer.So S3, it receives the data, it receives the key and with this key, the AWS S3 will go ahead andencrypt the data for you in a similar way, whenever you want to decrypt the specific data, you willagain have to send a decrypt operation with the same key.This is basically the symmetric key, so you will have to provide the same key to both encrypt and decryptthe data.So that is what SSI C means.So enough of the theoretical aspect.Let's jump into the practical so that this becomes really interesting.So currently I am in the S3 console.Let's go ahead and create a S3 bucket over here.So I'll say sample bucket for encryption and we'll go ahead and we'll create the S3 bucket.Great.Let's go inside this bucket and let's click on Upload.We'll add a file and I have added a file called as one dot JPG.We'll go ahead and we'll do an upload.So by default, this object which is uploaded, it is not encrypted.So if you look into the storage class, the storage class is standard.And here the encryption, the encryption is none.So this is basically the plaintext data which is stored in the S3 side.Now, if you go to the properties here, there is an option of default encryption.Now by default, it is none.However, you can use the ease 256 as well as the AWS KMS.So this is 256.If you'll see, it is s s e hyphen S3 So this is the first one that we were discussing.And you also have the AWS KMS where you can go ahead and select the KMS key through which you want toperform the encryption and decryption operation.So let's take one example of the SC hyphen S3 So we'll use the S 256 and we'll click on Save.All right, So once you have enabled this, let's go back to the overview.Let's upload one more object and this time the name is D one dot JPG.Let's go ahead and do an upload.So let's look into the properties over here.And this time under the encryption you will see it is S 256.Now one important part to remember is that if you enable the encryption at a later stage in the S3 bucket,the existing objects which were uploaded previously when the encryption was none, they will continueto stay in the plain text.So if you open up one dot jpg, you will see the encryption is none.Great.So I hope at a high level overview you understood what this means.Now we'll also take one more example of SC hyphen C As we were discussing with this approach, alongwith the data, you will also have to specify the key.So the first thing that we need to do is we'll have to generate the key over here.So I'm in my CLI and the first thing that we'll do is we'll go ahead and generate a 128 bit key.All right.So here the output is basically the key that we intend to use.So this is the key that we can supply with the SC hyphen C operation.So the next step is to go ahead and upload some data in AWS S3 So currently I'm in my CLI and you havethe CP operation which is basically to copy the local file to S3 Bucket.Now within here, if you see that there are certain operations related to the SC hyphen C, so theseare the ones which are highlighted over here.So you have the SC hyphen C where you can specify S 256 You will also have to provide the SC hyphenhyphen key.So here you will have to provide the key while uploading the object.So let's try it out.So within the CLI, let's go ahead and create a file called as demo dot txt and this is the file thatwe'll be uploading.So let's go ahead and do a AWS S3 CP, specify the S3 bucket, which is sample bucket for encryption.And along with that we'll also specify the demo dot txt file.So in the first step we have basically uploaded the text file without any encryption over here and theupload was successful.So now let's go ahead and try to do it with the encryption option.Before we do that, let's go ahead and rename the file to c c dot txt and let's run the same command.However, this time we'll also have to specify certain operations.So the first thing, let's go ahead and change the file name.So this is the SC hyphen C and you will have to specify the options which were specified in the CLI.So the first one was you have to specify C hyphen C over here.Let's copy this out.And along with that you also have to specify the C hyphen hyphen key.And here you can specify the key which got generated.So this time when you go ahead and upload, you see the upload has been successful.So this is what is referred as the server side encryption with the customer provided keys.Now if you quickly refresh over here, you should see that there are two files demo dot txt and se hyphenkey dot txt.So the demo dot txt is basically associated with the server side encryption of is 256 since we haveenabled it within the bucket and you also have se hyphen key dot txt.If you open it up, if you look into the server side encryption it is se hyphen C great.So at a high level overview, I hope you understood what the server side encryption in S3 is along withthe three options over here.Now the next available option is the client side encryption.So in the earlier slide we were discussing that you have a server side and client side.Within server side there are three options and within client side, basically what you can do is youcan go ahead and encrypt the data at client side and then send the encrypted data to S3.So before the data goes to S3, the data is already in the encrypted manner.Now, this is a little different from the C hyphen C because within the C hyphen C, we provide theplaintext data and then we provide the encryption key.So AWS S3 has the logic to take the plaintext data, to take the encryption key and perform the encryptionoperation.So that logic is already present within the S3.However, with client side encryption, you do not use that logic.All that logic is stored in the on premise or at your application side, and you directly go ahead andsend the encrypted data to S3.Great.So that's the high level overview about S3 encryption.I hope this video has been informative for you and I look forward to see you in the next video.