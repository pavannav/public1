# 3.28 Challenge with Current Architecture

Hi, everyone, and welcome back.Now, in the earlier video, we had created a total of two subnets, and in each subnet we had launchedone AC two instance similar to what you see in the console.So although the architecture at this stage is fine, but there is one challenge.The challenge is that the VPC in which the subnet is created, there is no Internet connectivity thatis yet associated with this VPC.And this is the reason why, even though the AC two instance here has a public IP, you will not beable to connect to it primarily because the Internet connectivity is not there.All right.So, in fact, let me also quickly show you this practically so that we can understand this.I'll copy the public IP.And if you look into the security group here, we have port 22 that is allowed from every one.So ideally, we should be able to connect to it if things are working.So let's try it out.So from my terminal, if I quickly do a search AC two user followed by the IP address of the instanceand you see at this stage we got an error stating connection time dot connection time dot primarilyhappens when your client is not able to connect to the destination server.All right, so what will you be doing from the next video onwards?Will be looking into the components that takes care of the Internet connectivity at a VPC level andwill also configure the Internet at the VPC level so that we can connect to our AC two instances.So with this will quickly conclude today's video.