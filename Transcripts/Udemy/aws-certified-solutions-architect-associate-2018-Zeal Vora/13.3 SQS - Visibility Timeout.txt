# 13.3 SQS - Visibility Timeout

Hey, everyone.And welcome back.Now in today's video, we'll go ahead and discuss about the importance of a visibility timeout featurein SQS.Now let's go ahead and understand what is a challenge.Now, one important part to remember about SQS is that whenever a consumer receives a message from thequeue, the message still remains within the queue and it is not deleted.Now one of the question that comes is what is the reason behind this?Primarily because if a consumer receives the message, the message should be deleted and why is sucha architecture design?So let's look into it.So let's assume a producer is sending a message to the SQS queue.So you have a message M1 that is stored in the SQS queue.Now you have one of the consumer that is polling for the message and has now received the message.Now once this message has been received, assuming that, let's assume that now the message is deletedfrom the SQS queue.All right.So we are looking into the architecture which is not implemented to understand why it is not implemented.So assuming that the message M1 is received by the consumer and now it is deleted by the SQS queue.So now let's assume that the consumer is doing some processing on this message.M1 And while the processing is happening due to some reason the consumer went down.All right, So now essentially what has happened is that the message M1, which was part of the consumer,that message M1 is lost and the message M1 That was part of the SQS queue.That message is also lost.So essentially the entire message is now lost.And this is something that is not really good for the production environments and this is the reasonwhy SQS queue will not go ahead and delete the message even after the message is received by the consumer.So now let's look into the solution that SQS implements whenever a message is received by one of theconsumer.So first point, whenever a consumer receives a message from the queue, the message still remains in,the queue is not deleted.This is something that we already know.However, it remains in a hidden state for a specific interval and can be deleted by a consumer onceit has completed the processing.So let's understand this.So now you have a message M1, which is part of the SQS queue.You have Consumer one and Consumer one has received the message.M1 As soon as the message is received, this message becomes hidden in the SQS queue.All right.So it becomes hidden and it is not yet deleted.So now what will happen is the consumer is expected that it will go ahead and complete the processingof the message M1 And after the processing is completed, the consumer can go ahead and delete the messagefrom the SQS queue.However, let's assume that during the time when the consumer is processing the message, the consumerwent down, then what will happen is after a certain amount of time, this hidden message will be removedand the message will be back to the queue.So now another consumer that is running it can go ahead and fetch this message from the SQS queue andafter it has completed the processing of that message, it can go ahead and delete that message explicitlyfrom the SQS queue.So this is the basic concept.So let's go ahead and understand this practically.So currently I have an SQS queue and there are no messages that are available.I also have a EC2 instance that is running and will be using this EC2 for our practical.Now before we go ahead and run the SQS related CLI command, let me go ahead and quickly attach an Iamrole to this EC2 instance.So within the role, let's go ahead and create a new role based on EC2 within the policy.Let me just give a full access and let's call this role as SQS, EC2.I'll create a role.Great.So once the role is created, let's go ahead and quickly attach this role to the EC2 instance so thatwe can make SQS related CLI commands.So I'll specify SQS, ec2 and I'll do a save.That's great.So now let's go ahead and store some message in the SQS queue.So I'll do a send message and I'll say this is message one and I'll do a send.All right.As expected, if you go back to the queue, let's refresh it and you should see that you have one messagethat is available.All right.So we'll consider this message as N1.Now from the EC2 instance, I'll run a CLI command.The CLI command is SQS, receive message and we basically specify the queue URL.So the queue URL, you can easily get it from here.So this is basically the queue URL.All right.So once I go ahead and press enter, you should see that we.Have received a message with the body of this is message one.All right.So the consumer has now received this message.As soon as the consumer has received the message, this message will become hidden.So now if you run the same command again, you see you are not able to see anything in the SQS queue.However, after certain duration of time that is defined within the visibility timeout, if the consumerthat has received the message, if that consumer has not deleted the message, then it is assumed thatthere is some issue with the consumer.So the hidden message that was part of the SQS queue will automatically show up and that is the visibilitytimeout.So now from the CLI, if I press enter again, now you see the message has automatically come up andif you run the same command again, the message is now hidden.So now let's go ahead and look into the visibility timeout setting now within the SQS queue.In fact, let me go ahead and do a edit here.You have an option of visibility timeout, which is 30s and it can be anywhere between 0 seconds and12 hours.So what will happen is after 30s, from the time when consumer has received the message, if this messageis not automatically deleted by the consumer, the message that was hidden will show up again.All right.So that is what the visibility timeout means.However, depending upon the use case, for example, let's say a consumer might take around one hourto process the message M1 and after one hour it can go ahead and delete the message M1 from the queue.So in such cases it is important that you go ahead and set the visibility timeout to 60 minutes.So once the message is received by the consumer after 60 Minutes, if the consumer has not explicitlydeleted the message, the assumption is that there is some issue with the consumer and the message willagain appear in the SQS queue.And another consumer that is running as part of the fleet will now fetch the message.So that is what the visibility timeout refers to.Great.So with this I hope you understood the basics of visibility timeout.And with this we'll conclude today's video.