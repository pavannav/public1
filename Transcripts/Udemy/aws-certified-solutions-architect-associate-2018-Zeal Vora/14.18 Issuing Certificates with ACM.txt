# 14.18 Issuing Certificates with ACM

Hi, everyone, and welcome back.So in today's video, we look into how exactly we can issue a new certificate for our own domain throughcertificate manager service.So the first thing that you have to do is you have to go to the certificate manager service.And from here you can either go ahead and request the certificate or you can do it from your self.All right.So when you request a certificate, you are basically intending the public certificate if you have apublic domain.So we'll be using a public certificate here.I'll click on next under the fully qualified domain name.So currently I have one domain available where we can do testing purpose temporarily.So the domain is zeal for a dot com.Now, necessarily, you do not really need to follow this unless and until you have your own domain,because if you just add a random domain, the validation will not really work.If you have a domain, that's perfectly fine, you can try it out.If you do not have, you can just look into the steps here.So let's go ahead and specify, let's say demo dot z, vohra dot com.All right.So this is the end in here.And the validation method, you can either select DNS validation method, which is the recommended aswell as the email validation method will be using the DNS validation method.Here, we'll look into what exactly this is in just a moment.Once you select the validation method, all you have to do is you have to click on request.All right.So now at this stage, you see the status is pending validation.And if you'll quickly open this up, they'll basically show you on how you can validate it.So all you'll have to do is you'll have to take this, see name and see name value and add it as partof your DNS record.So let's go ahead and add a new DNS record here.The type would be seen in the name they have already mentioned.So this is the name, so I'll copy this up.Let me paste it and then you have the value you have to ensure that you copy the entire value.And LPs, the value and detail.You can either use the default one or you can say custom, let's say title to 600 seconds, or evenif you do not touch it, that is completely fine.And I'll click on Add record.So once the record is added to your domain, after some amount of time, you will see that this specificcertificate, the validation would be completed.It can take a few minutes.So this is one important part to remember.All right.So it took around one minute and you see the status is now issued.So now that the certificate is issued, you can go ahead and integrate it with any service that youwant.Now, if you look into the details, basically it states that the certificate that has been issued canbe used with Cloud Front, LP API, Gateway, as well as other integrated services that ADA provides.So this is a very high level overview on how you can issue a new certificate through ACM.And with this will conclude today's video.