# 6.3 Base Concepts - EC2 Auto Scaling

Hi everyone and welcome back.Today we are going to discuss about some of the base concepts related to the EC2 Auto Scaling servicethat we should be aware of.Now the first important area is related to Auto Scaling Group.So whenever you want to configure the EC2 Auto Scaling service, you will have to go ahead and configurethe appropriate details in the Auto Scaling group.So at an overview, Auto Scaling Group contains all of the rules and parameters.It can be health checks, instance specifications, scaling policies, etc. and this in turn tells theEC2 auto scaling on how the instances as part of the auto scaling should be managed.For example, within the EC2 console, if I go a bit down, you have an Auto Scaling Group option thatis available.If I click over here, let me in fact maximize this.So this is one of the auto scaling group that I have available.And this auto scaling group in turn contains all of the necessary configuration related to what is thetype of instances, how many instances should be running, what is the EMI, the scaling policies,etc. everything gets added as part of the auto scaling group and anything related to the EC2 auto scalingthat you want to manage, you will have to edit it as part of this.The second important area is related to the capacity management.Whenever you configure the Auto Scaling group, you will have to define three things.One is the minimum size.Second is the desired capacity.Third is the maximum size.Let's go ahead and understand all of these three.So the first one is the minimum size which basically means that what is the lowest number of instancesthat should always be running as part of the auto scaling group that you configure.Desired is basically the target number of instances that you want to run under the normal conditions.And third is maximum, which is the upper limit on how many instances the auto scaling can scale upto.Let's take an example to better understand this.So let's say that the minimum is one desired is two.Maximum is four.So in this case, since the desired is two, auto scaling will know that under normal conditions youwant two instances to run all the time, so auto scaling will go ahead and run two instances all ofthe time.Now let's say there is a higher load.So in such case autoscaling will scale up.It will add new set of instances.Now how many instances will it add?It will add up to the maximum that you have defined.In this case it is four.Now again it depends upon organization to organization.In a larger organization, these numbers will be much more inflated because four instances might notalways be enough when you have thousands of users who are visiting your application.This is just an example based on the requirement this part can change.Let's also take an example.If you look into the auto scaling group over here, the desired capacity is one.The minimum you see is one.The maximum you see is three.So based on this, how many EC2 instances do you think is running?And the answer is there should be one EC2 instance running because the desired is one.Now when you go to the EC2 service, let's go to the instances.And currently you see you only have one instance that is running.And as we were discussing this part, you can change, for example, when you edit it.So within here you see you have the desired capacity, the minimum desired capacity and the maximumdesired capacity.Now let's say that you want two EC2 instances to run all of the time.What you can do.You can change this desired capacity from 1 to 2.And you can just update the auto scaling group over here.So if you see the status is currently updating, the capacity since desired is two.What auto scaling will do?It will go ahead and have two instances that will be running as part of this.We'll be discussing this in just a moment.It can take slight amount of time.So you see the instances now is updated to two.And if I try to refresh here you see now you have two EC2 instances that are running.The third important area is related to the launch template.Now we must have already discussed about the launch template in the earlier part of the course, butat an overview, launch template is basically the set of configurations that is required to launch aspecific EC2 instance.Now, when you create an auto scaling group, you will have to specify the launch template as well.So whenever Auto Scaling Group adds new set of instances, these instances will be based on the specificationsthat you have added as part of the launch template.For example, this is the newer instance that got launched.Now for this instance, what is the ami ID?Because if you look into the security group, automatically a security group calls launch wizard hyphenfor got associated.Similarly, there must be some other areas like the amid the instance type is also T2 micro and so on.So how does Auto scaling Group know that whatever new instances that needs to be launched, it mustbe based on this instance type.It must be based on this security group, maybe a certain IAM role, and etc..All of this depends upon the launch template that you add.Now if you see for this specific auto scaling group over here, you have a launch template called SecurityTemplate that is associated.So if I'll quickly open this launch template here, this is the launch template where all of the thingsare configured.So here this launch template says that any instance that should get launched should be based on thisID, should be based on this instance type, should be based on this security group ID.Now if I just copy the security group ID and when I go to security groups let's search by this ID yousee it is launch wizard for.So all the new instances that the auto scaling service will create, it will have this specific securitygroup.The next very important area is related to the health checks.So here note that EC2 Auto Scaling service continuously monitors the health status of the instancesthat are part of the Auto Scaling group to maintain the desired capacity.So generally, what happens is that whenever a new instance gets launched and the launch is succeeded,then the auto scaling will continuously perform the health check on the instance to verify if it isrunning as expected.If the health check is passed, then it will move forward and continue with the periodic health check.If the health check has failed, then this EC2 instance will get terminated and the EC2 instance willget launched automatically.Alright, so note that when EC2 auto scaling determines that a in-service instance is unhealthy, itreplaces it with a new instance to maintain the desired capacity of the group.So let's say that at this stage, since the desired capacity is two, what happens if something goeswrong with one of the instances?Let me show you a demo.So this EC2 instance, let me go ahead and terminate it manually.All right.So let's assume something went wrong with the hardware or something went wrong with the instance dueto which the health check will fail.Now once the autoscaling determines that there is some issue with the instance, it will go ahead andit will launch one more instance so that this desired capacity gets maintained.So let's go ahead and verify this.So currently this is still getting shutting down.Let's wait for a moment here.So at this stage one of the instance has been terminated successfully.So now Auto scaling Group will realize that hey, something is wrong and the desired capacity has notbeen maintained.So it will go ahead and launch one more instance here.So after a minute you see there is one more EC2 instance that is automatically getting launched.And again, if you look into the security group, it will be based on the launch wizard for now.Again, this part has not been populated.Let's refresh.And now you see you have a launch wizard for that is getting associated with the instance.And again this is part of the auto scaling.Now all of these details you will also find as part of the auto scaling.So if you look into the activity over here let me go ahead and refresh this within your in the activityhistory.You see that at this time an instance was taken out of service in response to a EC2 health check indicatingit has been terminated or stopped.So it realized this and it realized that the desired capacity is not maintained.So it has launched in response to unhealthy instance, a new set of EC2 instance ending with nine BA,and this is the EC2 instance that the auto scaling has launched.Now one more part that you have to keep a note is that whenever you configure the auto scaling group,you also have to specify the appropriate scaling type based on the scaling type and the configurationthat you add.The EC2 Auto scaling will go ahead and launch the new instances accordingly.Here.Let me also quickly show you.So as part of the Auto Scaling group, in the scaling over here, you have various Scalings that areavailable like dynamic scaling policies, predictive scaling policies, scheduled actions, etc. andeven in the dynamic scaling policies, you have multiple type of policy types that are available.So depending upon what exactly you want as part of the auto scaling, you can select the appropriatescaling type over here as well.So that's the overview about some of the basic concepts related to EC2 Auto Scaling service.And with this we'll conclude today's video.