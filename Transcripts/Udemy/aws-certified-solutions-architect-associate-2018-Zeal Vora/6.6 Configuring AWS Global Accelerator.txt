# 6.6 Configuring AWS Global Accelerator

Hi, everyone, and welcome back.Now, in the earlier video we were discussing about the benefits that the global accelerator providesand some of the features related to health checks.So in today's video, we are going to go ahead and create the global accelerator from scratch and we'lllook into the configurations that are involved.Now, one of the prerequisites here is that you need to have certain set of EC2 instances running wherethe global accelerator can send the traffic to, and this EC2 instance should have basic website.So you can either install Nginx or Apache so that the basic website is running for us to be able totest the setup.Now for today's practical, I have two EC2 instances that are running one in Singapore region and secondone in the Virgina region and both of these EC2 instances have Nginx installed.So just to verify, let's copy the public IP of one of the instance in Virginia.And if I send the traffic to the IP address, it should basically show North Virginia region.And similarly if I copy the public IP of Singapore region EC2, let me just add it in the browser,it should show Singapore region.So this is one thing.So today we are going to focus on global accelerator configuration and how we can associate it withthe instance across the regions.So the first thing that needs to be done is you have to go to the global Accelerator service.Again, note that this is a global service, so it is not associated with a specific region.You see on the top.It is a global service.So you can either create accelerator from here or on the left hand side, you can open accelerator,you can create accelerator.Let's go ahead and create a new accelerator.Within here.You can just call it as demo global accelerator.Within the accelerator type.We are more focused towards the standard accelerator here.In the IP address type you have IPv4 as well as dual stack.Dual stack is basically IPv4 and IPv6.We'll keep it simple with IPv4 once done, you can click on next within the listeners, I'll specifyport of 80, the protocol of TCP.You also have UDP option once.Then let's click on next.Now you have to specify the region.As we're discussing, Global accelerator is a global service.It is not specific to region.However, the endpoints which can be EC2 instances, etcetera, these endpoints belongs to region.So here we will have to specify the region in which the endpoints are running.So the first region is the Virgina region where our endpoint is running.Along with that, we'll specify one more region of AP Southeast one, whereas second endpoint is running.Once this is done, let's click on next.Now within this region we have to specify the endpoint.For example, in the Virginia region, our endpoint is EC2 instance as we already know, that therecan be multiple set of endpoints, it can be ALB, NLB, elastic IP addresses, et cetera.So within the region of US East one, we'll add an endpoint of EC2 instance.And since we only have one EC2 instance it is being shown here.Similarly for the AP Southeast one region, I'll select the endpoint of EC2.Let's select the EC2 instance and Singapore region and that's it.Once you have done that, you can go ahead and click on Create Accelerator.All right.Now, as soon as the accelerator is created, you will see that there are two sets of IP addresses thatare available.Now, generally, let's assume that both of these EC2 instances are hosting your website.So what you will be doing for the domain that you have, it can be example.com, so you will associatethese IP addresses with example.com, or you can also associate this DNS associated with the domain.Now when I copy this DNS and if I quickly do an NS lookup from my CLI, it points to two set of IP addresseshere.44.1 65 130 1.2 37 which is nothing but the set of IP addresses.So if you have a domain, you can directly associate the domain with this DNS name so that whenevera user enters the domain in the browser, ultimately the traffic goes to the global accelerator here.All right.So it took around few minutes and now the status of a global accelerator is deployed.So now if I copy the DNS name of the global accelerator, let me add it in my browser.And this time the traffic is being sent to Singapore region.Similarly, if we have a user from, say, us, then his traffic will ideally go towards the North Virginiaregion.EC2 instance.This is one part.One more thing that I wanted to show you is that initially with the configuration that we had created,it might take a little time for the global accelerator to be deployed.However, in case, if you'd like, you can modify few things through which the creation process canalso be much more faster, for example.Let's edit this.The thing that we are interested in editing is the health checks.So here in the health check interval instead of 30 seconds and the threshold count of three.So this is something that you can play around with.You can say ten seconds for testing environment, you can just say one.However, in production environment, you should not use one.Maybe you can use the number of three.You can save the changes.Similarly for the second endpoint, also you can lower the health check interval.And save the change.All right.So this again, when we go to the accelerator, it will show in progress status.Let's quickly wait for a moment for this to change.Great.So our global accelerator is now deployed.Now, let's also go ahead and verify the second primary feature related to the health check options.As we know, since I'm based out of India, the traffic is being directed to the Singapore region EC2instance.So what we will be doing, I'll be stopping the Singapore region EC2 instance.And then we'll verify whether the global accelerator is sending the traffic towards the North VirginiaEC2.So once it is in the stopping stage, you can copy the DNS name once more.Again, note that it takes few seconds for the overall failover to complete based on the configurationsthat you have.So it will not be immediately like you stop the instance and after 2 or 3 seconds the global acceleratorwould detect it immediately.So now from the new tab, let me add the DNS here, and this time it is going to North Virginia region.Sometimes you will still see that traffic is going to the Singapore region, but that is due to cache.So you can do Ctrl F5 to verify if the traffic is going to North Virginia region.Second alternative is you can always use CLI, this is much more better.There is no caching related things that you will get troubled with unlike browsers.So you can send a curl request and this time you will see it is going to the North Virginia region.Great.So that's a very high level overview about global accelerator.One more important thing before we conclude, note that in Global Accelerator you will be paying anhourly fee.So do not leave the global accelerator running after your practicals.As soon as you have done it, immediately delete it.So let's go ahead and disable the global accelerator first post, which we will be able to delete it.So let's quickly wait for a moment here.Great.So our accelerator is now disabled, so we'll proceed with the deletion.Let's delete it.So the deletion process here as we see is started, if we refresh the status went to in progress andafter a few minutes the accelerator would be deleted.So that's a quick overview about the practicals.And with this, we'll conclude today's video.