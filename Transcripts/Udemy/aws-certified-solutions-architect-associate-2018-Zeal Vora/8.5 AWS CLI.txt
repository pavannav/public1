# 8.5 AWS CLI

Hey, Aaron.And welcome back.Now, in today's video, we'll be discussing about the command line interface, also referred as theAWS CLI.Now, whenever you're learning any new technology, one of the preferred ways to start is always throughthe console.And once you're familiar with the GUI and its associated concepts, then you can switch to CLI basedapproach.And in a similar way for AWS, initially we were doing a lot of things via the GUI, but at the end,whenever you intend to do a lot of automation or there are various use cases in which proves to be veryuseful.So the command line interface is one of the primary ways of interacting with the system in form of commands.So you execute some commands and you get the resultant output from a particular system.And it is also considered as one of the fastest way of doing things in a repeated automated fashion.We'll be discussing more about this just in a moment.So let's take an example of a simple use case to understand why CLI can prove to be very important.So let's assume that there is a new security check system which verifies the security of an EC2 instance.So what is security check systems?Does it logins via the private IP inside the EC2 instance?And it verifies if this EC2 instance has a security risk or not.Now it evaluates the system configuration only after logging into the EC2 instance.So first this system needs to login via the private IP, so login via the private IP and then it willgo ahead and run the check.Now in this diagram, since there are three EC2 instance, it is very easy to configure so you can manuallycopy the private IP of all the three EC2 instances.Put it as part of the security check system and security check system will go ahead and log in to theEC2 instance and perform the scan.However, in production environments there can be thousands of EC2 instances.So in case of thousands of EC2 instances, you cannot directly go to the console and copy the privateIP address of each and every EC2 instance and feed it to the system.This is practically not a good approach.So in order to overcome this you can make use of CLI.So let's do one thing.Let's jump into quick demo to understand more about this.Now as we are discussing, one of the rudimentary approach here would be to manually select the EC2instance, copy the private IP address and maybe feed into the system.Same goes with the second instance with the third instance and so on.So as we are discussing, this is a easier approach, primarily if you have a very less EC2 instances,but the environment where you have thousands of EC2 instances across hundreds of accounts, this approachis not really feasible.So in such cases, instead of going through the GUI, you can make use of the CLI as well.Now I already have the AWS CLI that is configured for today's demo, so let's quickly explore that.Now in the GUI, we used to manually click on the instance and all of these details related to availabilityzone subnet ID.It used to come in a nice, easy to read manner.Similarly in CLI you can go ahead and see all of these details with a click of command.So let's say I run AWS EC2, describe instances.And now you see you got a huge amount of information related to the EC2 instances that are running,including the IP address, the security group, the security group, ID, Mac address, network interface,and so on.So currently I'm in the AWS CLI documentation associated with the EC2.Now similarly in this EC2 console, you see that there are a lot of options that are available to performa specific use case.So even in CLI there are a lot of commands that are available to achieve a specific use case.Now let me quickly show you one of them.So let's say describe instances.So we'll use one of the sub commands over here.So beneath this you'll see that there are a lot of options and let's go a bit down.I wanted to show you a few interesting things.So in this specific example over here, what you are doing is you are describing the instances in sucha way that you get the instance ID, you get the name of the instance and you get the availability zone.So generally there are both the Linux as well as Windows commands that are available.So primarily in Windows you will see that this slash does not really work.So there is a little different convention that is part.So let's copy this up.And when you paste the command in Windows CLI.You see, you get a very nice output related to the availability zones, the instance and the name.So this is one you can also go ahead and filter things by private IP addresses.So here you have a private IP addresses and at the output you get the list of all the private IP addressesthat are associated with the EC2 instances.Now you can also quickly format it in a much more easier way in case if you intend to.So this time the output is in text instead of this curly brackets.So as we are discussing, depending upon the use cases like fetching the private IP addresses of allthe EC2 instances with the help of single CLI command, you can quickly do that.So this is one of the benefits of understanding and learning.CLI So just to quickly revise AWS, CLI is used for managing the AWS resources from the terminal andit makes room for automation and makes things much more faster.Now when we are discussing about AWS CLI, there are certain prerequisites that needs to be configured.First one is in order for you to make use of AWS CLI, you will have to download and configure the AWSCLI binary.And the second important part is you also need a access key or a Iam role to authenticate.So let's say that you have this AWS platform and you are configuring a AWS CLI command which createsa EC2 instance.Now before an EC2 instance gets created, there needs to be some authentication that your CLI must present.So for that authentication there are multiple ways like access keys or Iam role that can be configured.So let's do one thing, let's quickly try this out post which will be discussing about other aspects.So for today's demo, for simplicity, what we'll do, we'll make use of a EC2 instance to practicethe AWS CLI related aspects.So let's go ahead and launch an Amazon Linux related EC2 instance.We'll directly go to review and launch and we'll launch the EC2 instance based on our key pair.Great.So let's quickly open this up and I'll just name this as AWS CLI for quickly understanding the purposeof EC2 instance.Great.So the instance state is in running.Let's verify the SSH.So ssh is allowed.So let's go ahead and quickly connect to the instance.Great.So currently we are logged into the instance.Let's quickly switch to the root user now in Amazon Linux based instance, when you run here, you seeyou get a basic usage and also some of the help.So this basically means that AWS CLI is automatically installed over here.Now do note that depending upon the Ami that you use in AWS, CLI can be installed or it cannot be installedin case of the Amazon Linux based instances, AWS CLI binary is pre-installed here.Great.So our first prerequisite of AWS cli binary in EC2 instance is resolved.Second is you need to perform certain authentication before you execute certain commands.Let's try this out.So let's say I do a AWS EC2, describe instances.So this should basically describe the instances that are available.So here it states that you must specify a region now similar to the GUI on how we switch to a specificregion based on which the instances are shown.Similarly in the CLI, also you have to specify a region.Now you can configure it via the AWS configure that we'll be discussing in just a moment.Or you can also specify the region where you want to see the instances here.Now immediately you see it gave an error stating that unable to locate the credentials.So before you go ahead and run the command, you will need to specify some of the credentials.Again, it can be access key or a Iam role in case if you are running CLI in your laptop then you willneed to configure the access keys there.So let's do one thing.Let's go ahead and generate a access key for the Iam user.Now within the Iam user, let's go ahead and add one user.The name, I'll call it as AWS CLI.Just for our example, this time we'll be giving a programmatic access.Let's click on next for the policies.We'll be attaching a administrator access for simplicity.Let's go ahead and create the user.Great.So now you got the access key ID as well as the secret key ID so we can integrate this as part of AWSCLI.So to integrate things, what you need to do is you have to run the configure command and the firstthing that it asks you is the access key ID, So let me copy the access key ID, I'll paste it, Letme copy the secret access.See, I'll paste it.And then it is also asking for the default region name.Let me just call it as US East One, which is associated with the North Virginia region and I'll pressenter and that's about it.So now when you do the AWS EC2, describe instances, let's remove the region now.You see, you got the expected output over here.So any operation that you want to perform through the CLI will work perfectly here.As we are discussing here, that when you run this command it defaults to the default region endpointthat you had configured.However, in case some of the EC2 instances belongs to a different region, you can specify the regionfollowed by the region code.So AWS has certain region code that is available for each region.So for example, as we are discussing the example of North Virginia, so North Virginia has a regioncode of US East one.Now in a similar way, for every region you have a region code.So if you want to perform some operation on this region, then you can specify this region code overhere.Now as we had created our new EC2 instance in the Ohio region.Let's copy the region code here and let me go ahead and paste this.And you see, you will only see the EC2 instance that belongs to the Ohio region.Now before we conclude, let's discuss a few important aspects related to AWS CLI.First is that AWS CLI is supported for Linux Windows as well as Mac Systems.So you can go ahead and install the AWS CLI and the AWS configure another command that we had executedthere in the EC2 instance, you can do it at a laptop level.Second is AWS.CLI has multiple versions.One is the version one which is the older one and the newer one is the version two.So whenever you're working with AWS CLI, make sure that you work with the version two.Second is if you want to quickly look into the installation step.So here you see that there are multiple installation steps.One is the Docker image, second is CLI version two for Linux for Mac as well as for Windows.Anyways, so our primary aim for today's video was to understand AWS CLI at a very high level overviewand this is one of the reasons why we had used EC2 instance that came with the AWS CLI pre-configured.The last point for today's video is associated with the AWS CLI documentation.Do note that there is a separate set of documentation that is available for AWS, CLI and these setof documentation covers the list of all the options that you can make use of with AWS, CLI for theservices that AWS has to offer.Now let me also show you this.So this is the AWS CLI command reference.So this is the documentation that we are discussing.Now if you go a bit down within the available services, you see that there are a lot of services thatare available for you to make use of the AWS CLI commands.So let's say that tomorrow you want to work with Route 53 in AWS CLI so you can open up the Route 53from here.Let me in fact show you this.And now you see all the available commands that you can work with at the Route 53 level.And once you are aware that you want to perform a certain operations, let's say you want to deletea specific hosted zone, then you open this up and now you get the basic synopsis and you also see multipleexamples on how you can make use of the command here.Great.So that's the very high level overview about the AWS CLI.I hope this video has been informative for you and I look forward to see you in the next video.