# 6.1 Introduction to Auto-Scaling

Hi everyone and welcome back.Today we are going to discuss about an interesting topic of auto scaling.So nowadays in most of the organizations that you will work with, one of the common features that youwill see being used extensively is auto scaling.So let's go ahead and understand the basics of auto scaling and the advantages that it provides.Now one of the very common things that you would see is that organizations would generally provisiona set number of servers to manage the normal traffic levels.Now, although this is good for normal traffic, but sometimes when you have a traffic spike, thenthese servers can be inadequate.For example, let's say that you have the Enterprise Corp over here, and this organization runs twoservers most of the time because most of the users that opens up the Enterprise Corp website, theseuser traffic can be managed by two servers without any issues.So this is fine.Now the problem that happens is that when you have a traffic spike over here, like you have more numberof users who is visiting the Enterprise Corp website and you have a sudden burst in traffic for, say,maybe few days.So in such cases, these two servers will not be able to handle all of the traffic.So what the system administrator would have to do when he detects that there is a traffic spike andthese servers are not able to handle, they would have to manually launch few more servers to handlethe traffic.However, this is a manual process and it takes a lot of time.So in order to overcome this challenge and have a set of automation in place, you have the featureof auto scaling that proves to be very useful.So at an overview, auto scaling is a feature that automatically adjusts the amount of computationalresources based on the current demand.So same use case as the previous slide.You have set number of users and there are two servers that is able to handle the traffic.Now you have additional bursts of traffic that is coming towards your website, maybe due to holidayseason etc..So when you have a traffic spike this time manually, you will not have to launch the server automatically.The feature of auto scaling will launch additional set of servers to handle the traffic demand for you.And this entire part is automated and this proves to be very useful for organizations.Now, this topic of auto scaling can be understood in a much more easier way through an analogy of airconditioner.So let's say you have an air conditioner at home, and this air conditioner can automatically adjustits cooling power based on the room temperature.Now, in case if the room temperature gets very hot, the AC will automatically sense it and it willincrease the cooling power to bring down the temperature quickly.Now again, this part is automated.You can also do it manually as well.And in a similar way, when the room temperature is cool, then the AC will slow down its cooling powerand it might even turn off to save the electricity for you.So this works both the ways.When temperature is hot, it automatically detects and works upon it.When temperature is cool, it automatically works and detects upon it in a similar way.In the earlier slide, we were discussing on how when there is a traffic spike automatically, the autoscaling will launch additional set of servers to meet the demand.So that can also be referred to as the scale up operation.Auto scaling also works in a scaled down operation.So within here, when the load of the server decreases, the scale down operation occurs and the additionalservers are automatically terminated.For example, after, say, holiday season has completed and now the load on the server has been reducedand there are very few users who are visiting the website.So now there is no need of the additional two servers that were launched by the auto scaling.So auto scaling will automatically remove these servers over here.So both at a scale up operation when the load is high and scale down operation when the load is low.Auto scaling works perfectly well.So this is the basics of auto scaling.Let's go ahead and look into it from demo perspective.This will give us better clarity.Now for today's video.In a Digital Ocean platform, I have a server auto scaling that is configured.Now if you see in here it is showing the CPU utilization where memory is 35%, CPU is only 0.8%.And here it is also showing the minimum instances as part of the pool and the maximum instances as partof the pool.So what will happen is when the load increases, the number of servers will be increased by the autoscaling feature to the maximum servers that you have configured.Now let me also quickly show you.So within here you see you can set minimum size.For example when you have the normal traffic demand how many servers do you need.So that is the minimum.And then you have the maximum.When there is a traffic spike how many servers do you need?Maximum.Do you need five servers, ten servers, 20 servers, etc. all of those things can be configured.And we were discussing that the auto scaling automatically adjusts the amount of computational resourcesbased on the current demand.So within here the demand is based on CPU utilization.So more the number of requests, the more CPU utilization the server will get.And hence the target CPU utilization is 50%.What that means is that when the CPU utilization is more than 50%, the auto scaling feature will automaticallyadd new set of servers as part of the environment.Let's also go ahead and look into it from demo perspective.So I'll copy the IP address of the server.So currently I'm logged into the server.Now if I run the top command over here you see the load is very less.So the CPU utilization is hardly there.So now what we are going to do, we are going to manually increase the load of the server.Specifically we are going to target CPU utilization.For example, at this stage if I run the top command the load is very less.So let's go ahead and try it out.So I'm going to run the stress command.And we are going to increase the load of the server.So if I'll just open this.You see, we have the stress related process and the load of the server will slowly increase.So now let's quickly wait for the moment here.So now you see the load of the server has been increasing over here.There are four process.Each one is using 25%.So it is consuming almost 100% of the CPU as part of the server.Now what will happen is it takes slight amount of time.However, what will happen is autoscaling will detect this specific metric.It will determine that the CPU load has increased.So you see the CPU load will soon increase over here.And as soon as it reaches the threshold of 50%, additional server will be launched.So let's quickly wait for the moment for this metric to be updated here.Alright, so now the average CPU utilization is at 21%.Let's quickly wait for a moment here and this will increase significantly further.Alright, so now you see the average CPU utilization has crossed the 50% threshold that we had set,and as soon as it has crossed, it is scaling up.So scaling up operation has started.And you see one more server is getting launched over here automatically by the auto scaling.Now we were discussing about analogy of AC, where it automatically adjusts its cooling power both whenthe temperature is high as well as when the temperature is low.So here the first demo that we had when the traffic was high or the utilization was high, we know theauto scaling added new set of servers.However, we also have to verify whether it performs the scale down operation as well when the trafficor the utilization of the server is low.So now what we are going to do, we are going to remove the additional load that we are adding on theserver.And then we'll see on how auto scaling will also detect it.And the additional server that it had launched will be terminated.So we'll just close the stress over here so that the utilization will slowly decrease.And then we'll wait for the auto scaling to scale down.So till the scale down operation happens, let's also look into the benefits of auto scaling.One of the great benefits of auto scaling is the cost efficiency, and you only pay for what you use.So for example, additional servers got launched and it only ran for say 5 to 6 hours.So you'll only pay for that 5 to 6 hours after those servers gets terminated.You don't really have to pay for them, so you don't have to run expensive idle servers when the demandis low, the auto scaling will take care of this second primary benefit is reduced manual intervention.So now DevOps team do not have to monitor for the traffic spike and launch the additional server quickly.Auto scaling will take care of that.And this allows DevOps team to work on other priority projects.Third is the business agility, so you can easily handle growth or sudden changes in demand.So when you have some promotions or sales that are going on, then you don't really have to worry aboutserver capacity getting reduced because auto scaling will take care of things.Now, let us also look into some of the common factors that are used to configure the auto scaling toperform the scale up or scale down operation.One of the common ones is based on the resource utilization.For example, if CPU usage crosses a set of threshold, maybe 80%, 70%, etc., then auto scaling shouldadd more servers.If the CPU threshold is below 40%, auto scaling should remove additional servers.So this can be based on CPU usage, it can be based on memory usage, etc..Then you also have scheduled scaling where you can say that add new servers every Monday at 9 a.m.,because you know that on Monday you will have a huge spike of traffic and then remove the additionalservers at 6 p.m..Because after 6 p.m., no one logs in and keeping the servers running will lead to additional cost.So this is also configurable.This is referred to as the schedule scaling.Again a lot of organization uses this to reduce the overall cost, and the third quite popular one nowadaysis the predictive or the AI based scaling.So what happens here is that it analyzes your historical data and trends to scale in anticipation ofthe upcoming spikes.For example, let's say that every year, based on the data, the auto scaling software has figuredout that on December 25th there is a huge spike of traffic.So what it will do on the 24th itself, the auto scaling will go ahead and add additional set of serversand keep it ready for the upcoming spike that it anticipates.So here the historical data and trends also can prove to be very useful.So going back to the DigitalOcean environment now, you see in the auto scaling there is a scaled downoperation that is happening.And the additional server that was launched, that server is now terminated, primarily because theCPU utilization metric has gone below the 50% threshold.Now, before we conclude, one more important point that I wanted to share is that earlier, maybe tenyears back, auto scaling was primarily used with servers, so it only supported servers.But now there is huge popularity in other technologies, for example containers as well.So now auto scaling, although primarily it was used only for servers, but now it is used for widevariety of other technologies like containers, storage, database and many more.For example, if you are using Kubernetes as the container orchestration tool for Kubernetes pods thatyou run, you have the horizontal pod auto scaling that is supported.So automatically new set of containers or pods in the Kubernetes environment will be launched when itdetects spike in traffic or when you discuss about AWS.It also supports the RDS storage auto scaling.So this works at a storage level and so on.So again depends upon the cloud provider and the exact tool that you use.The feature of auto scaling will be available accordingly.So that's a quick overview about the basics of auto scaling.And with this we'll conclude today's video.