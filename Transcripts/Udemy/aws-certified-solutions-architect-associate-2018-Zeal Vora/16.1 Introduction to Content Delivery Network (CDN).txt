# 16.1 Introduction to Content Delivery Network (CDN)

Hi everyone, and welcome back.In today's video, we are going to discuss about an interesting topic of content delivery networks.So nowadays, from what I have seen, most of the medium to large scale organizations, they make useof CDNs quite extensively.So what are CDNs and why are they so popular?Let's go ahead and discuss about it.Before we discuss about CDNs, let's look into some of the typical challenges that organizations useto face, and then we'll look into how CDNs can address them.So let's take a simple example where you have a website, and this website contains a very popular imageof Mr. Crow.So you have a mr. Crow over here and everyone likes to see him.So what happens is let's consider that a website is very popular and you have 1000 users who visit thewebsite.And this website contains the single image of Crow.Now, in such case, when 1000 users are visiting the website, the server in which the image is hostedmust send that image 1000 times one to each user.So the workflow would look something similar to this.So let's consider that this is the server where your website is hosted.And this website contains the image of Mr. Crow.So now what happens is if you have a global set of users and if they are visiting for every request,the server will have to send back the image of Mr. Crow to each user.Now, although this is not much of an issue, but in a larger scale, this can lead to certain disadvantages.Now, one of the main disadvantage here is that the main server must handle every user request, andthis can create slowdown or outage during the heavy traffic.Second is higher bandwidth cost.Bandwidth can be expensive and if you have a lot of users, then the bandwidth costs itself can be morethan the server cost as well.Third is related to geolocation.So if users are far from the server.So let's assume that your server is hosted in India and you have a lot of users coming out of US andEurope, then they might experience slower load times due to latency.And fourth is this server is vulnerable to attack because the users are directly connecting to the server.So let's discuss about it from the security point of view.So we have a similar architecture over here.Now as we were discussing that this server is hosted on the internet so the users can directly connectto it.So one of the things over the internet that you should know is that attackers love the internet, specificallythe websites that are not secure and generally small scale website, large scale, medium scale website,all of them.You will see if you monitor correctly, you will face a wide range of attacks ranging from denial ofservice, distributed denial of service.Web application attacks are very common nowadays.So let's assume that since the server is over internet, you will have the bad vectors as well.And this attacker can attack your server so he can launch a denial of service attacks.He might launch a web application based attacks as well.So launching a denial of service attack is quite simple nowadays, and this can impact all of the usersbecause your server will not be able to handle the traffic.So one of the possible solution here is to introduce a middle layer.So this middle layer will contain all of the functionalities to protect the website against lot of attacks.Along with that, it can also include functionality related to caching of the most requested objectsfor better performance.So now what will happen is the caching can be done at a hardened middle layer.So in such case, all of the most requested objects, instead of going to the origin server, it canbe returned by this hardened middle layer back to the users, and this can improve the performance.In case if user is requesting for some object that is not cached at a middle layer, then the requestcan go to the origin server.So this middle layer that we were discussing about can be the content delivery networks.So at a high level overview you can consider the CDNs as a intelligent proxy that receives the requestand then forwards it to the back end system.And one of the most important part of CNNs is that many of the CNNs also comes with the popular featuresrelated to distributed denial of service attack protection, the web application firewall, the mostimportant caching, and so on.So all of the most requested objects can be cached at the CDN, so users, when they request for it,the image will go back from the CDN servers, back to the users.It will not hit the origin servers.So this is one of the great benefits of CNNs.So looking into the architecture, it would look something similar to this, as we know that CDN cansit between the server and the global users, and it comes with wide variety of benefits.One of the most important benefit of CDN you can consider it to be caching.It can cache some of the most requested objects that users request, and that can improve the performance.Apart from that, many of the CNNs, they comes with the web application firewall, so any attack thatuser might make, it can be blocked at a the CDN level and it will never reach the origin server.Similarly, DDoS protection also is very common if attackers are attacking your website using DDoS orDDoS.It will be absorbed at a CDN level.It will not go to the origin server.Apart from that, you have SSL, TLS termination, content optimization, and a lot of other benefitsthat CDNs provide.So let's do one thing before we proceed further.Let's have a quick demo so that we understand this concept at an overview post, which will be discussingabout other areas.Now for today's demo, I have an image of Mr. Crow that is available in the S3 bucket.Now again, this can be in a web server as well.However, just for simplicity I am using this approach.So let's copy the URL of this and from a newer tab if I quickly open this.You see we are getting the image of Mr. Crow over here.Now, similar to what we were discussing, if you have the middle layer of the content delivery network.It can take care of a lot of things and it can act as an intelligent proxy.Now for today's demo, I also have a content delivery network that is configured based on Amazon CloudFront.So this is the domain name of the CDN.And let's copy the domain name over here.And in the incognito mode of another browser I'll add the URL.And I'll also specify the name which is cro dot jpeg.You can just verify the name was cro dot jpeg.And before we send a request let me just open up the developer tools.This will give us the better understanding.All right we'll switch to network over here and I'll send the request.Now in a similar way you are able to see Mr. CRO over here.However, if you look into the response headers let's maximize this.So in the response headers over here you will see that there are certain headers related to CloudFront.So if you see this is one of the headers from CloudFront, what it says hit from CloudFront.So what it basically means is that this specific image was served from CloudFront itself.The request did not hit the origin server.So just to reiterate this CDN has cached this specific image.So when a user sent a request to the CDN, it did not send that request to the origin.Instead, from its local cache, it sent back the request to the user.So this is what hit from CloudFront means.So let's take one more example.This will allow us to understand the concept in a better way.So let's upload one more image.And I have an image called as Guardians dot jpeg and I'll upload it.So now what happens is if a user sends a request to a CDN and that request is for an image.Now if CDN does not have that image in its cache so it is not able to find it.So now what it will do, it will send that request to the origin server, from which the image willbe fetched, and later that image will be cached at the CDN, so that the subsequent request that mightcome to the CDN for that image, the CDN will serve that image from its cache instead of sending itto the origin server.So now let's do one thing.Let's copy the URL again in a new tab I'll specify Guardians dot jpeg, and before we do that I'll justopen up the developer tools.All right.And in the developer tools we are interested in network.So let's press enter.All right.So this is the image.So this is the photo that I recently took a day back.And these are the new guardians who are here to protect us.Now if you look into the network for the Guardians dot JPEG.So this was the image that got returned.Now within here you will see it is showing miss from CloudFront.Now why it is showing miss from CloudFront?Because this image we recently uploaded.So it was not really present in the CDN cache.And due to that, this request of Guardians JPEG went to the origin server.And from the origin server the request was served.However, what will happen later is that the CDN will add this image as part of its cache, and nexttime the image will directly be served from the CDN.It will not go to the origin server, so let's in fact try it out.I'll copy this now from the terminal.Let me send a curl request to the same page over here again.You see, our cache is missed from CloudFront.That is fine.So if I send again third time you see it is stating hit from CloudFront.So CloudFront has realized that there are multiple requests that are coming for that image of Guardians.So the CDN has cached that image and now it is serving that image from the cache.Great.So I hope you understood the importance of caching.Many a times what happens is organization.They specifically go for CDN because of the strong caching capability so that the website performanceis greatly improved.Now, one of the very important point specifically related to CDN that you should be aware is that itis not just one server, instead it is a global network.So it is a distributed network of servers that caches and delivers the website content as close to yourend users as possible.So if you look into the CDN network over here, it is across the world.And let's say in India, if you see there are so many edge servers in the India.So let's assume that 90% of the requests are coming from India and the image is cached in the CDN networkin India itself.So in case if your server is in different locations, say in US, it will not matter because the edgeservers of the CDN that are in India.That edge server is where the images will be cached, and from the edge server, the image can be sentback to the user, so it vastly improves the performance.Now that we understand about CDN, let's look into some of the popular CDNs that are available in theindustry.One of the very popular CDNs nowadays specifically for small to medium scale organizations is Cloudflare.A lot of websites I have seen they extensively make use of Cloudflare.Cloudflare again is a very popular and it provides wide range of features related to web applicationfirewall, SSL, TLS certificates, etc. so this is one area that you have to keep a note of.Second, popular CDN specifically for the AWS customers is Amazon CloudFront.And in fact, the demo that we just explored is related to the CDN of CloudFront only.So this is also quite popular.The third very popular CDN is Akamai.From what I have seen, Akamai is a very mature solution and generally for large scale organization.When we discuss about enterprises, this is where Akamai is generally used.Many of the large scale organizations that I have seen, they only use Akamai.Now, before we conclude, I have also added the tables related to the benefits.This can help in the revision.Now the first major benefit is the performance.As we were discussing some of the static assets like images, it can be cached at the CDN level.And since CDN has distributed servers across the globe, it can vastly improve the performance.The second is enhanced security.CDN generally comes with WAF, DDoS protections, etc. so you don't really have to worry about the originserver.The attacks will be absorbed at CDN level itself.Third is the bandwidth saving.Since the cache are coming from the CDNs, you don't really have to worry about high cost of bandwidth.Great.So that's a very high level overview about the content delivery networks.Now, one thing that I can say is that in case if you have a website just for learning purpose, I willhighly recommend you to bring that website behind the CDN.If you have CDN in place, there is a lot of interesting learnings that you will get and that will helpyou in your career as well.For example, many of the CDNs like Cloudflare, they also come with certain free plan features.For example, if you are using free plan, you have the DDoS protection.They also provide you the DNS resolver, they give you SSL certificate.And the most important part as we know is the caching.Now again, if you go with the paid plan, you might get the web application firewall, etc..So all of this is a great learning to have.Along with that, if you have the AWS account, then maybe you can also explore the CloudFront.CloudFront also comes with certain level of free tier.For example, one terabyte of data transfer out of AWS free tier is available.So this again can help you as part of your learning journey.Great.So that's a high level overview of CDNs.And with this we'll conclude today's video.