# 12.17 Overview of AWS ElastiCache

Hey, Ron.And welcome back.Now, in today's video, we'll be discussing about Elasticache.Now, before we go ahead and discuss more about it, let's take a very simple analogy that will allowus to understand the real use case.So let's assume that there is a new vegetable shop in the locality which has become quite popular.So every day you have 300 to 500 people who are visiting the shop and buying vegetables.Now each visitor is asking for the price of at least 2 to 3 vegetables before making a purchase.So imagine that you have 300 to 500 people asking a price to maybe a 2 or 3 employees.What would be the condition of that?So the condition of the employees would be something that is depicted within this diagram.So this is quite a pain.So in order to overcome this, the vegetable owner decided to create a dashboard that has list of allthe common vegetable prices which are requested by the buyers.So this is a price list that you see over here where the owner has specified price associated with themost common vegetables which users buy.Now, from this simple analogy, there are two important learnings.First one is that since the price list of common items was listed, users no longer need to ask theemployees about it.So this effectively reduces the overall load on the employee.So if a vegetable shop has maybe 4 or 5 employees and there are 400 or 500 visitors asking for the sameprice again and again, so that increases the load and they will not be able to concentrate on the otherimportant task.So with the price list, the overall load on the employee decreases.The second thing is that the visitors themselves can quickly go through the price list.So this improves the efficiency in terms of time for both the visitors as well as employees.Now, in a similar way, when you discuss about the challenges related to the database workloads, youwill see that a lot of users will create a similar query towards the database.So the first point as we are discussing, is that there can be a lot of common queries within the databasethat hundreds of users might request at a given amount of time.So if 100 users are requesting for a similar query, then ultimately it increases the load in the databaseserver.So this can also be depicted within this specific diagram.So in order to overcome this scenario, what you can do is you can introduce a caching solution.So with the caching solution, you can cache the response associated with the frequent queries thatare being made to the database.So if you see over here where you have a client, client has made a select query for user data.So the query is sent to the database and there is a response back.Apart from the response, this data is also stored within this caching engine.So what happens is next time when the client makes a same query yet again, that query can go to thecaching engine and the response can come back from the caching engine itself.So once the data is stored in the caching engine, the client no longer will have to send the same queryto the database.It can automatically be answered by the caching engine.So similar to the analogy that we were discussing, this not only improves the overall performance becausecaching engines are generally in-memory and they are extremely fast, so not only it improves the performance,but it also decreases the load in the database.Now when you talk about some of the popular caching solutions, two among them are very popular whenit comes to the database caching.First one is Memcached and second one is Redis.Now both of them are quite good.Among these, Redis is quite popular, primarily because it was introduced to overcome some of the drawbacksassociated with the Memcached engine.Now one of the challenges which a lot of organizations faces is that in order for you to use these engines,you will have to go ahead and install, configure, optimize and also secure the EC2 instances wherethese engines would be running.So you will have to take care of all the aspects related to the caching engine.And these aspects sometimes create a lot of issues.So what organizations were requesting is that if there was a managed service, which has, that wouldhave been much more easier because.Then the users would not have to do the installation configuration and so on.And that is where the Elasticsearch service comes up.So Elasticsearch is a fully managed service that makes it easier to deploy, operate and scale in-memorydata store or cache solution in cloud.So elastic cache is like a managed service and within few clicks you can have a in-memory caching layerin your infrastructure.Now, depending upon the configuration, elastic cache can also detect and replace failed nodes, thusreducing the overall overhead.So let's do one thing.Let's quickly look into the elastic cache service.So currently in my dashboard, let's go ahead and open up the elastic cache service.So within here you will see that there are two primary caching solutions which are available.One is Memcached and second is Redis.So you can take one among them.And when you go ahead and do a create, all it will ask you is to select certain configuration.So how many replicas do you need for it?So you can go ahead and select five replicas.What is the node type associated with that?Do you need a multi availability zone auto failure or not?So all of these things are point and click.You also have automated backup solution associated with that.So this is great because if you do not really have this service then you will have to configure allof these things manually.So you need to know on how you can modify the configuration file of Redis to include multiple replica,you need to have a solution to achieve multi-az with auto failover, you need to achieve a solutionto take automated backups and so on.So all of these things are quite pain and with the elastic cache solution from AWS, with point andclick solution, you have a ready made cache layer available in your infrastructure.So that's the overview about the elastic cache service.So the primary aim of today's video was to just give you a gist about what elastic cache service isall about.So with this we'll conclude this video.I hope this video has been informative for you and I look forward to see you in the next video.