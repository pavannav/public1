# 8.6 Installing AWS CLI for Linux and Windows

Hey, Ryan, and welcome back.Now, in the earlier video we were discussing at a high level overview about the AWS CLI.So in today's video, we'll be focusing towards the installation aspect of AWS CLI both for Windowsand Linux systems.Now as we were discussing that in AWS CLI, there are two versions that are primarily available.One is the CLI version one and second is the CLI version two.So for many of the EC2 instance, when you do a hyphen hyphen version, it will show you the versionof the AWS cli.So in this case the version is 1.16.So it is not the version two.So what we'll do is we'll go ahead and we'll quickly remove this specific version and we'll look intohow we can install the newer version of AWS CLI.So anyways, even at a later stage, if you see that the AWS CLI is already of version two, just removethe version two and we'll look into how the installation happens to have an overview of that.So when you do a which AWS, it basically shows you the path which is USR bin AWS here.So what you can do is you can go ahead and run the command on this specific path which is usr bin AWS.Great.And that's about it.When you do it says that the specific AWS cli binary no longer exists.So now let's explore the installation aspect.First, let's look into the Linux one.So here, let's go a bit down and here you have installed the AWS CLI version two on Linux and thereare certain commands that are available.Let's go ahead and try it out.So first we'll go ahead and download the Zip file which actually contains the necessary packages forAWS CLI version two.So let's clear the screen and we'll run the Curl Command.So when you do an LS over here, you should see that there is one zip file that is available next.Let's go ahead and unzip the compressed file.So let's quickly do that.So you see there are a lot of files that are available here.When you do an LS, there is a new folder that is created.And in fact, if I want to show you the contents of the folder, these are the contents.And inside these there are multiple sub files that are available.And the last thing that you have to do is you have to run the install command.So let's quickly do this.And that's about it.Now you see it states that you can now run the USR local bin version.So let's try it out.Let's copy this up.And now within the version you see, we have the version of two.So now after clearing the screen, when you run the hyphen hyphen version, you get an error statingthat no such file or directory in case you're doing this, one of the simplest ways is log out.You can just run the log out command.Let's quickly log in back to the EC2 instance.Let's switch to the root user.And when you do the AWS here, it works perfectly.Or when you do a version, you see things are working perfectly here.Great.So this is a quick way on how you can install the AWS CLI version two on Linux.Let's also explore the Windows based approach.Now the Windows based approach is very simple.All you have to do is you have to download the installer and through the GUI you can go ahead and install.So let's quickly explore this now before we run that.When you type over here, it states that AWS is not a recognized command.So let's close this up.Let's download this MSI installer.So you see, the installer has been downloaded.Let's open this up and let's go ahead and install it.Great.So the installation has been completed once.Then let's open up the command prompt here.And when you type ls, you see you are getting the option very similar to the Linux one.Now within the new command prompt, first thing that you have to do is you have to run the configurecommand similar to what we had seen earlier.Let me copy the access and secret key ID here.And let's also add the default region here.And next thing, when you run the AWS EC2, describe instances.You see, things are working as expected.Now, last but not the least, let's also quickly explore at a high level overview about the installationprocess for Mac OS.So personally, although I don't have a mac, but the installation is very simple here.Again, for the Mac you have the user interface based installation, so all you have to do is you haveto download this package binary.Double click it.You will see a installer similar to how we did it in Windows and you can go ahead and install the CLI.So these are basically the instructions at a very high level overview for Mac.Great.So that's the overview about the installation aspects of CLI.Now again, once you have installed the CLI, the overall CLI commands that you run would be same acrossboth Windows Linux as well as Mac.So with this we conclude today's video and I look forward to see you in the next video.