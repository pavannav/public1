# 7.3 Interface Endpoints

Hi everyone, and welcome back.Now in today's video we are going to discuss about the basic architecture and working of interface endpoints.Now one of the great benefits in terms of architecture is that interface endpoints makes use of a elasticnetwork interface, which has its own private IP addresses from the range of your subnet.So whatever this Ina that gets created, it gets created at a subnet level.So this Ina has its own set of private IP address here.And this specific ina, it acts as an entry point for your traffic that is destined to the supportedAWS services or a VPC endpoint service, for example, the traffic from your EC2 instance in a isolatedprivate subnet if it wants to reach the supported AWS services that this interface endpoint supports.The traffic first goes to the VPC endpoint here, and from the VPC endpoint interface it goes to thesupported destination services.Now at an overview.This type of a architecture is much more simpler and it provides great set of benefits here.Now before we go ahead and look into the benefits that this kind of an architecture provides, let'slook into how exactly this works.Now in today's video, I have an interface endpoint that is created.Now as we are discussing interface endpoint.In turn, it creates a elastic network interface in the subnet that it is associated with.Now if you'll see this interface endpoint is associated with a subnet here.And this is the IP address of the network interface.So if you in fact open up the network interface here, this is the network interface associated withthe interface VPC endpoint.Now for today's demo I have two EC2 instances.One is the public EC2 and second is the private EC2.Both of them are in a different subnet.And the interface VPC endpoint network interface.Here is created in the subnet where the private EC2 is running.All right.So let's go ahead and have a quick demo to understand how things work.Now from my terminal I have four tabs.The first two tabs are associated with the public EC2.And the third and the fourth tab are associated with the private EC2 instance from the public EC2 instance.When you run a command of AWS EC2, describe instances and let's specify the region of AP South onewhere our two instances are running.For today's demo.You see, we are getting the output as expected.All right, now when you run this specific command behind the scenes, a request is made to a specificset of APIs.Now let's do one thing from another tab.Let's run the Tcpdump utility.I'll say destination port of 443.So this will capture all the traffic that goes towards the port of four, four three.So at this stage it is saying no permission.That is completely fine.All I'll do is I'll run with sudo.Now when you run this specific command.All right.The command has run successfully.Now, if you look into the packet capture here, you will see that the request is being sent to an IPof 172 3142 .104.Now, before we proceed further, let's also replicate this setup in our private EC2 instance, whichdoes not have a connectivity at all to the internet.I'll quickly show you if I do a ping on google.com.You see, there is no internet connectivity, there is no Nat gateway, there is no internet gatewayat all.Similarly, here I'll do a pseudo TCP dump on the DST port of 443, and from here I'll run the samecommand as EC2, describe instances and the region I'll say as AP South one.Again, the command got successful.It is showing us the two EC2 instances that we have now in the packet capture.Here you see from the IP address of the instance IP address is 172 3130 4.1 65.Let me quickly show you, if I open up the private EC2, you will see 172 3130 4.1 65.Now from this source IP, the traffic is going to this specific endpoint, which is 172 3142 .104.Now if you look into the endpoint interface IP, it is 172 3142 .104 which is exactly matching here.So once you have the interface endpoint created for whatever service, then automatically your trafficdirectly goes to the interface associated with that endpoint.Now one more thing that I wanted to show you before we run this command.Let's do one more packet capture.This time I'll use another region AP South East hyphen one.All right.Note that this is a public EC2 instance, so this has perfect internet connectivity.Now in here we got the output.The reason why there is no output is because there are no EC2 instances running in the Singapore region.However, if you look into the packet capture, this time the traffic did not go to the interface endpointIP address.You see, this is a public IP address 15 .221.8.222.So depending upon where the request is being made, if the request is being made in the same regionhere, then the request goes to the interface endpoint IP address.If the request is made for another region, then it goes to another set of public IP address.So everything is very transparent here.Now if you run the same command, let's try the same command in the private EC2.All right.So this is the private EC2 instance will run the TCP dump command here.Let's run this for another region.Now this time you see there is no output here primarily because since a different region is selectedand there is no internet connectivity, this specific request fails.All right.So I hope at a high level overview, you understood the basic architecture on how the interface VPCendpoint works.Now one of the great benefits of interface VPC endpoint is that there are a lot of supported services.Now when we discuss about gateway VPC endpoints, there are two primary supported services, which isS3 and DynamoDB.However, for interface you have a lot of options.So let's also go ahead and quickly look into it.So whenever you go ahead and create a interface endpoint, let's search by the type of interface.You see there are so many services that are supported.There is a big list.One of the services that is supported is EC2 and this is what we were using in today's demo.And this is the reason why even from private EC2 instance, we were able to run the EC2 describe EC2instances command and the network interface got us the output.The second big benefit is the security group integration.Now as we know that the interface endpoint uses a network interface, you can associate a security groupto this specific Eni, and hence you will be able to restrict the access to the endpoint based on therequirements.Now I'll also quickly show you this.So in the demo interface endpoints that we have created, if you look into the network interface IDassociated with the network interface, there is a security group.You see.You can easily see even in the console there is a direct link to the security group whichever way works.But we are doing it through this way so that we understand the work flow.So associated with this network interface.There is a security group.Now when I open the security group, let's go ahead and edit the rule.At this stage you have all the traffic that is allowed.Let's go ahead and remove 0000.And I'll just add a random Cidr block which the EC2 instance does not belong to.Now, once we do that, let's clear the screen from the private EC2.When you go ahead and run the EC2, describe instances in the same region where the interface endpointis.Ideally, it worked earlier, but now you see it is not really working, primarily because the requestis getting blocked at a security group level of the interface endpoint Ini.Now, the third primary benefit of interface endpoint is that it supports on premise related setup.Now, as we know that a network interface gets created in the VPC itself.So any systems in your on premise data center through Direct Connect or VPN.For an example, they can directly connect to the interface endpoint Ini and from here the traffic goesthrough the AWS private network towards the destination AWS services.And this proves to be very beneficial.All right.So that's a very high level overview about the architecture of interface VPC endpoints.And with this we'll conclude today's video.