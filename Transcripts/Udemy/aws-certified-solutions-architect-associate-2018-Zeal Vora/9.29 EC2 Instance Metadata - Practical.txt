# 9.29 EC2 Instance Metadata - Practical

Hi everyone, and welcome back.Now in today's video, we will look into the practical aspect on how instance metadata can be accessedfrom EC2 instance.Now, it is not always necessary that the instance metadata is available while creating a EC2 instance.You can also set it to disable so that it does not work.So let's go ahead and quickly explore that.Now from AWS, whenever you are launching a instance, let's go ahead and try it out.Let's just call it as instance metadata.Now we'll just select a key pair.We'll say proceed without a key pair.Now if you look into the advanced details you have a specific option related to the instance metadata.You see metadata accessible.So this is what we are referring to now by default, if you do not touch this specific option it willbe enabled.So you don't really have to worry about that.However, if you want, you can also set it to disabled so that the metadata is not available for everyonealong with that, when a metadata is enabled, there are also two options that are available.One is the version one and second is the version two, version one and version two, which is the firstoption is where the token is optional.Version two is where the token is required.At this stage, we are not really discussing the difference between both of them.However, you have to ensure that you select the one which has the token of optional.Once you have done that, you can go ahead and launch the instance.Apart from that, just for simplicity, I'll also launch one more EC2 instance which does not have themetadata enabled already, so it will be in a disabled state.You do not have to replicate it, otherwise the cost will increase.You can just look into it from theoretical perspective.I'll say instance metadata disabled.I'll just say proceed without the key pair and let's go a bit down this time.Metadata accessible.I'll select it to disable.And I launch the instance.Great.So let's go back to the instance you have the one that is running which has the metadata enabled.So let's do one thing.Let's wait for both of them to be available post which we'll look into how things are working practically.All right.So it has been a minute or two.And both the instances are now running.So let's do one thing.Let's go ahead and quickly connect to the first instance where the metadata is enabled.So now that we are connected to the instance now there is a specific way in which the metadata is generallyaccessible.So what you can do, you can send a curl request to a URL of 1639254163925 for latest metadata.So you have to remember this specific URL.Again, it is quite simple to remember this one.Now as soon as you send a curl request here you will see all of these high level categories.Now in the high level categories also you will be able to find the details.For example, if you want to see the local IPv4 address then for example this specific one is what weare exploring.You can specify local IPv4 and you get the IPv4 address which is 32 .126.So if you want to verify this.You see it is 32.1 26.Similarly, if you want to see the Army ID, you can also do that.And now you see the EMI ID is available, which is 18A.Same thing you will be able to see here.Let's go a bit down.It is 180.All right.So through the instance metadata you will be able to see a lot of configuration options based on whichthe instance has been created upon.This is one part.Second part as we are discussing is the instance where the metadata is disabled.So if you'll quickly connect to this instance as well.Immediately.You see, you got an error which states failed to connect to your instance.Error establishing SSH connection to the instance.Now this error is coming because the instance metadata is disabled.So generally when you try to connect to the instance through the EC2, connect where you are loggedin to the instance through the browser, basically, behind the scenes, it pushes a public key associatedwith SSH.And based on this the connection gets established.Now this public key is pushed and available through the instance metadata service.And if the instance metadata service itself is not available, then a lot of options will not work.Not only curl, but even the EC2 connect related options will not work here.Now, fortunately, AWS has provided an option in which you can enable the instance metadata of a runninginstance as well.Let's go ahead and verify that.Now if we go a bit down you have the option of modify instance metadata option where you can enableit.And also note that there are multiple version of instance metadata service.At this stage you can use the version number one.So the version number v2 is optional here.So we are not compulsory using v2.Generally V2 has a little different approach where a sessions are used.We are not going to discuss this at this moment, but at an overview.V1 and v2 both allows you to achieve the same thing, which is the way to fetch the metadata about theinstance.The only difference primarily is that you need some kind of a additional step in V2 in order to accessthe metadata.All right.So I'll just quickly do a save.So here it states successfully modified.The process can take a few minutes to propagate.So this is one point that needs to be remembered.So let's go ahead and try to connect to the instance here.And at this stage we are quickly connected.Now, as expected, if you send a request to 1639254169254.Latest metadata.At this stage, things are working perfectly well.Now, just for simplicity, I wanted to show you one more thing.Again.You don't have to replicate it now.Since we had launched the EC2 instance without key pair, I'll just quickly add a key pair to the instancehere.All right, this is just for a quick demo.I just wanted to show you one more thing.I'll just add a key pair, my local key pair here, so that we can log in to the instance through theterminal and not through this EC2 instance.Connect.All right.So now let's do one thing.Let's go ahead and disable the instance metadata once again.I'll just disable it.And I'll connect to this instance through my local terminal of my desktop.So here I'll just add the IP address and we are connected.Now if you try to now run the 169254169254 latest metadata command.Immediately you see we are getting error of 403 forbidden.So this is where metadata is disabled at this point.Great.So that's a very high level overview about how you can connect to the instance metadata to fetch a widevariety of data about your instance, and also the options related to how you can enable and disablethe instance metadata service for a specific EC2.So with this, we'll conclude today's video.