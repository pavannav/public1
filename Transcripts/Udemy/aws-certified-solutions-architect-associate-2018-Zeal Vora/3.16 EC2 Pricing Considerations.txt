# 3.16 EC2 Pricing Considerations

Hi, everyone, and welcome back.Now, in the earlier set of videos, we have been working with the EC2 service and we understand thebenefits that it provides.Now, in most of the organisations that you will be working with, EC2 is one of the common areas wherethe organization hosts their websites and applications.And this is the reason why it is important for us to understand about the EC2 from the pricing pointof view as well.Now within the first important pointer to note is that for the Amazon EC2, the overall usage is calculatedeither at an hour level or at a per second level.And this depends upon the size of the instance, the operating system and the region where the instancesare launched.For example, let's assume that you launch instance like Linux, Windows and various others.Then the charges are at a per second level.For example, let's say that you run the EC2 instance only for 20 minutes, so you pay for 20 minutesonly.Earlier, what used to happen was the billing was completely based on an hourly rate.For example, let's say that you launch an EC2 instance for five minutes and then you deleted the instance.However, still you will have to pay for the entire hour.But now things have improved and per second billing is also an option.Now I remember during our college times everyone used to have a SIM for basic communication.Now what used to happen was the pricing typically during that time was based on per minute.So even if you call someone for ten seconds, you had to pay for the entire minute.But at a later stage there was a provider called as Tata DoCoMo.In fact, a lot of Indian users will be able to relate, which introduced per second billing.So if you call for ten seconds, you pay only for ten seconds.And Tata DoCoMo during that time became extremely popular, primarily because of the per second pricing,which proved to be very useful.And similarly, even for the EC2 instance, this also proves to be very useful.Now one very important part to remember is that if a EC2 instance is in a stopped state, you will notget billed for it.And this is the reason why many of the organisations they have a automated scripts that will stop theEC2 instances during weekends and those EC2 instances will automatically be started on Monday.So at least for two days in a week.The EC2 instance pricing will not affect the overall billing of an organization and this is a very commonpattern that you will see in larger organizations.However, when we discuss about EC2, one of the important component is a storage.So even if a EC2 instance is stopped, you will still be charged for the overall storage.For example, let's say that you have a EC2 instance with a one terabyte of storage and you have stoppedthat EC2 instance.Now, although the EC2 instance is stopped, this means the CPU, the memory, the network is not beingutilized, but the storage is still being utilized where all of the data is still stored and safe.And this is the reason why you will have to pay for the back end storage, whatever storage that isassociated with the EC2 instance.Now when we discuss about free tier, the free tier includes 30 GB of storage.So if you stop the instance, even if it has the basic default storage, you will not really get chargedfor it.So nothing to worry.But from the larger organization point of view, this is a considerations that you have to keep a noteof.So let's do one thing.Let's go ahead and quickly look into how we can stop a EC2 instance.So currently we have one EC2 instance here.The instance state is running.So in order to stop the instance, the steps are very simple.What you do, you go to instance state and you stop the instance.One more thing I wanted to show you is that when you stop and start a EC2 instance, the public IP ofthe EC2 instance will change.All right, So this is also something that will have a quick demo of.So let's copy the public IP of EC2 and within my notepad I'll just keep a note of it.All right, so now let's go ahead and we'll stop the EC2 instance.So immediately here you see the instance state has changed from running to stopping.It can take a minute or two for the instance state to be changed to stop.So let's quickly wait for a moment here.So it has been around two minutes.Let's go ahead and verify.So I'll quickly refresh.And now you see our EC2 instance is in stopped state.Now, as we are discussing, as soon as the EC2 is in stopped state, you will not get charged for it.However, for the underlying storage, whatever you have assigned to it, you will get charged.Now in free tier you already have up to 30 GB of storage, so you do not really need to worry much aboutpricing.However, if you have larger storage like 500 GB one terabyte, this is where the billing will startto increase.Anyways, so once the instance is in stopped state, you can go ahead and start it whenever you needit.So let's do it.So I'll start the EC2 instance.Now one thing that we need to keep a note of is this public IPv4 address.So this goes away as soon as you stop it.But whenever you start it, the new public address will be assigned.So let's refresh the instance state is pending.That is fine.It will take a minute or two to start, but this time you see a new public IPv4 address has been assigned.So this if I'll copy the newer address you see, both of them are different.So that's the high level overview about the EC2 pricing consideration.Again, if you're working in AWS environment, make sure that you stop the instance specifically ina development and test environments when you do not really need.I have seen in so many environments that developers or even other teams, they just keep the EC2 instancesrunning during weekends.Even in the test environment, the EC2 instances are running and at the end the organization gets ahuge bill.So this is something that we can avoid with a simplistic step of stopping the EC2 instance.So with this, we'll conclude today's video.