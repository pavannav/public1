# 14.6 Creating Hosted Zones in Route53

Hey, everyone.And welcome back.Now in today's video, we'll go ahead and create our own set of DNS records using the Route 53 service.Now, before we go ahead and do that, there are certain important concepts that we need to know post,which we'll go ahead and start with our practicals.Now the first very important concept specifically related to the Route 53 service is the concept ofhosted zone.So a hosted zone is basically a container for records and records contain information about how youwant to route the traffic for a specific domain so this can be better explained with an example.So let's say you have a Route 53 service and then you have a hosted zone.So this is a hosted zone, which in turn contains the mapping between the domain name and its associatedvalue.In this case, you have three mappings, one for example, second, for example, and third for demodot example.So this together forms a hosted zone.So in terms of Route 53, this is how the hosted zone looks like.So if you see you have a hosted zone of example.com, which in turn has multiple entries here for example.com,demo dot example.com, labs, dot example.com and so on.So within the AWS console, let's go to the Route 53 service.Now within this console, you can go ahead and directly create a hosted zone.However, on the left hand side, if you quickly open, it will also show you various other featuresassociated with the Route 53 where you can go ahead and click on hosted zone over here.And from here, you can go ahead and create a hosted zone.Now, one important thing that I wanted to show you, typically because the there is a little differencebetween console that keeps on changing.So generally earlier used to show this specific console.However, nowadays if you quickly open up the route 53 from the service, let's open it up in a newtab.Here you see it directly takes you to the dashboard.So this is one of the things that you should know.Now, before we go ahead and create a hosted zone, let me quickly give you a few examples related tothe dashboard here.Now, one of the things that you see over here is a registered domain.So in case if you want to create your own domain, that can also be done.So, for example, let's say you want to purchase a domain college labs dot in.So in this case it is already purchased.However, now once you type it, you can go ahead and click on check to verify if it is available ornot.All right.So in this case, since this domain is already taken, you will see it as unavailable.However, it is also showing you various related domain suggestions which are available, and it isalso showing you the price associated for the sale.So from here you can also purchase the domain name.However, this is something that we want to avoid as of now.So let's go back.Now discussing about the hosted zone.Let's go ahead and click on Create a hosted zone from here or as we are discussing, you can also goto the hosted zones and you can click on Create Hosted Zone.Now, whenever you go ahead and click on Create Hosted Zone, there is one very important configurationthat you need to know, and that is the hosted zone type.So there are two types of hosted zones that are available.One is public hosted zone and second is private hosted zone.Now, along with this, one of the first thing that it is asking is to provide a domain name.For today's example, let's go ahead and provide the domain name of internal.You can also go ahead and use the same domain name here.The next important thing is the hosted zone type.So let's go ahead and quickly understand what this means.Now, there are two primary types of hosted zone.One is called as a public hosted zone.Second is called as a private hosted zone.So public hosted zone basically specifies how you want to route the traffic on the Internet.So, for example, whenever you type in that traffic that comes to the DNS server is routed so thatrouting happens over the Internet.Second is private hosted zone, which basically specifies how you want to route the traffic in an AmazonVPC, for example.Let's say that we have specified this domain name called as KP Labs Dot internal.So this is basically a private domain name.We have not really purchased it.So for this use case, you can go ahead and select a private hosted zone.So now what will happen is all the query to this domain can only happen from the resources that arepart of VPC.No one from the internet will be able to send a query to this specific domain.This is one important part to remember.However, let's say if I already have a domain called labs dot in and there will be users across theInternet who will be accessing this domain, then in such cases you have to create a public hosted zone.Now do note that for public hosted zone, this domain name needs to be purchased.This is one important part to remember.For simplicity, since we are testing things out, you can go ahead and select a private hosted zone.Now the concept of public and private hosted zone can also be understood with the help of a simple analogywhere on the left hand side you have a walkie talkie and on the right hand side you have a mobile phone.So walkie talkie is primarily used for private communication between maybe two individuals or a groupof team.And mobile phone is generally used for public communication.So you communicate over Internet, you communicate over cell phone towers and so on.So the left hand side, you can call it as a private hosted zone and the right hand side, you can callit as a public hosted zone, primarily because once you have a SIM card, anyone across the world willbe able to call you.However, for the walkie talkie, no one except the ones that are associated with the same frequencyand range will be able to establish a connection with.All right, So just to translate that in a technical term, a private hosted zone is where the resourcesin a VPC can connect to the Route 53 and the resolution will work.Public hosted zone is where everyone from the Internet will be able to communicate with the Route 53for the resolution to work.So that is a high level overview.So once we do this, practically things will be better understood.So for the time being, let's go ahead and change the domain to KP Labs dot internal.You can also use the same domain.We'll use a private hosted zone.And now one of the important things here is you have to specify the VPC to associated with the hostedzone.As we were discussing that the private hosted zone will only be able to resolve if the request is comingfrom the resources that are part of the VPC.So you will have to specify from which VPC the request will be allowed.So let me go ahead and specify the VPC associated with one of the region.Let's just call it as US East one, and then you'll also have to specify the VPC ID, so I'll use thedefault VPC ID here once.Then you can go ahead and create a hosted zone.That's great.So now you see our hosted zone is created and there are certain records that are created by default.Now let's go ahead and create one more record here.Let's just call it as demo.kp Labs dot internal.So the right hand part is already filled.All you have to provide is the record name.Then you have to specify a value.You can specify, let's say 172 dot 31 .0.5.And along with that you also have a record type.There are a lot of record types.We'll keep it simple.We'll use the A record which basically converts the domain name to IP address.When a request is made, we'll go ahead and create a record.That's great.So not only we have our hosted zone created, but we.Also have created our first record.Now, we already know that since this is a private hosted zone, only the resources under the VPC willbe able to make requests to it and the resolution will work.So you cannot do it via the Internet.So in order to get it working, what we'll do is we'll go ahead and create a sample EC2 instance fortoday's demo.So let's go ahead and open up the EC2 service here.Let's go ahead and quickly launch an instance.We'll use the Amazon Linux to make sure you use the free tier eligible.Whichever instance type is associated with it will do a review and launch and let's do a launch.I'll acknowledge the checkbox and I'll click on launch instance.So let's quickly open up the instance ID and let me just quickly name it as learning DNS.We'll do a save.Great.So instance State is currently pending.Let's just quickly wait for a moment here.Now, let me go ahead and quickly connect to the EC2 instance and great, we are connected.Let's quickly switch to the root user here.Now one important thing that you need to remember over here.Let's quickly explore the details associated with this hosted zone.So this hosted zone is associated with one of the default VPC as part of the North Virginia region,which is US East one.Now, in order to quickly verify if this EC2 instance is part of the VPC, and here you have a VPC ID,so the VPC ID ends with CD four so you can quickly identify that this hosted zone is associated withthis VPC.So basically our EC2 instance will be able to resolve the records that are part of this hosted zone.So let's try it out now from our terminal.Let's go ahead and run the Nslookup command and we'll specify Demo Dot Labs dot internal.And immediately here you basically get an answer, which is the IP address, which is one 7231 0.5.So if you quickly verify it is the same IP address that you have added over here.Now what happens if you try to request the domain, which is not part of this record?So let's say I'll do Nslookup on blog dot Labs dot internal.So currently there is no record entry, which is part of our hosted zone and this is the reason whyit is basically giving you this error.So now let's go ahead and quickly create the record.Let's just call it as block and the value, let's just call it as 192 168 0.2 We'll go ahead and createa record.So now if you'll quickly do a nslookup on the blog Dot Labs dot internal.So currently again you see you basically are getting an error stating that the server can't find thisspecific domain.So this is primarily because of the caching issue in DNS.However, this should be resolved in a few minutes.So till that time, let's go ahead and create one more record set.Let's just call it as videos dot labs dot internal within the value, let's say 1060 60. ten and I'llgo ahead and create a record.Great.So now within the terminal, let's quickly do a nslookup on videos, dot labs dot internal.And now you see you are able to see the address.And in fact, after a few minutes, if you also try nslookup dot Nslookup blog dot labs dot internal.You see the resolution also works perfectly here.That's great.So I hope at a high level overview you understood the basic concept of Route 53 hosted zone and alsothe difference between a private and a public hosted zone.Great.So with this, we'll conclude today's video.