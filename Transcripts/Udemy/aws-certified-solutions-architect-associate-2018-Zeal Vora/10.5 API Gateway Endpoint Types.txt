# 10.5 API Gateway Endpoint Types

Hi, everyone, and welcome back.Now, in today's video, we are going to discuss about the API gateway endpoint types.Now, one thing to note is that whenever you are creating an API gateway based API, depending uponthe use case, there are multiple different types of API gateway endpoints that are available.It all depends upon from where the majority of your API traffic is coming from.Now, if we have to look at a high level overview, there are three primary endpoint types.One is called as the edge optimized endpoints.Second is the regional endpoints and third one is the private endpoints.So let's go ahead and quickly look into all of these.Now, in fact, before looking into each one of this, let me quickly show you on where do you findthese options?Now, generally from the API gateway console, whenever you go ahead and you create an API in the endpointtypes, you see there are three primary ones.You have original edge optimized and private.So in this video we are going to discuss about what each one of these mean.Now the first one is the edge optimized API endpoints.Now, generally from the diagram itself, you will be able to identify on what exactly this endpointtype does.So let's go ahead and quickly understand once more.First one is that an edge optimized API endpoint is recommended and best for the geographically distributedclients.So if you have clients across the globe, this type of endpoint helps.And why does this type of endpoint helps is because of the first service here, which is the cloud front.So API requests in this type of endpoint are rooted to the nearest cloud friend point of presence.And also note that this is the default endpoint type for the API Gateway rest APIs.The second one is the regional endpoints.Now in the regional endpoints, the cloud distribution went away.So this is primarily intended for the clients that are part of the same region.For an example, if a client is running on an easy to instance in the same region and it is making multipleset of calls to the API gateway, this is something that is often seen or when an API is intended toserve a small set of clients with high demands.Now the third type of endpoint is the private API endpoint, and this is more intended for the VPC basedresource.So this type of endpoint can only be accessed from the VPC using a specific interface endpoint.So you have an interface endpoint related network interface.The VPC related resources can go ahead and connect to the private API endpoint.So this is the high level overview about the API gateway endpoint types, and with this will concludetoday's video.