# 3.26 Overview of Subnets

Hey, everyone, and welcome back.Now, in today's video, we are going to discuss about the basics of subnets.Now, one important thing that we need to note specifically when it comes to VPC is that the VPC spansacross all the availability zones in a region.For example, let's say that a region has two availability zone.And when you create a VPC, this VPC will go ahead and it will span across both of these availabilityzones.Now, some region will have six availability zone, some region will have two availability zone, somemight have three, and so on.So depending upon the region, the number of availability zone will change.But the VPC that you create will span across all that availability zone that are part of that region.So this is one important part to remember.The second important pointer is that whenever you launch an AC two instance in a is a single.Easy to instance can be launched only in a single availability zone of your choice.For example, in this specific diagram, we see that there are two availability zones.So whenever you launch an easy to instance, a single easy two instance can be associated with a singleavailability zone.So this first you see two instances associated with availability zone.One, you're launching one more easy two instance.Now this is two instances associated with availability zone two, and let's say you launch a third ACtwo instance.So again, you'll have to decide that third AC two instance will be launched in which availability zoneit can be either in availability zone one or either in availability zone two.So this is more of a or statement either here or either there.Now, whenever you launch AC two instance, an important thing to note is that an AC two instance cannotdirectly be launched in a VPC, primarily because we have seen that a VPC spans across multiple availabilityzones.So if a VPC is spanning across, say, six availability zone in a region, you cannot directly launcha easy to instance in a VPC.So what you do, you divide the VPC into smaller sub networks and each sub network is associated witha single availability zone.So this can be understood with a diagram as well where you have one larger VPC.So you divide this VPC into multiple sub networks.So you have a sub network number one, this is associated with the availability zone of one, also referredto as AC one.You also have a second sub network.This is associated with the availability zone of two.And whenever you want to launch an easy to instance, this easy to instance must be launched insidea sub network that you have created.So if you look into this from a technical perspective, you will see that the VPC is divided into twosub networks.In this diagram, you have a sub network of one, sub network of two.Each sub network is associated with a single availability zone over here.So whenever you launch an easy to instance in a specific sub network, the AC two instance gets launchedin an appropriate availability zone that the sub network is associated with.So if you launch an easy to instance in sub network one behind the scenes AC two instance will be launchedin the availability zone one because the sub network of one is associated with availability zone ofone.So this sub network that we are discussing from a perspective we refer to as a subnet.So a subnet is a sub network, which is a subdivision of a larger network, in this case a VPC.All right.So let's do one thing.Let's go ahead and look into it from a practical point of view so all of the pointers can be betterunderstood.Now for today's demo, I have two VPC that are available.First one is the default VPC that comes by default in all the region.And second is a custom VPC college demo VPC that we have created.Now again, note that this is a demo video.So practical from your side at this stage is not required.The primary aim is to understand the concept.Now let's quickly go to the AC two console.Now what I have done this demo VPC that we have available, this demo VPC is not divided into sub networkyet.So let's quickly do one thing.Let's select a demo VPC here.Now, there are no sub networks in this demo VPC.So let's go ahead and try launching an instance in this specific VPC without sub networks.Now let me just call it as demo hyphen C two will go a bit down.I'll just associate a key page that we have in this region and in the network settings over here.Let's quickly do an edit and within here we'll select our custom VPC that we have created.Now you will see that you also have an option of sub network and within here currently there are nosub networks that are available.So if you try to launch a new AC two instance, you see immediately it gives an error stating that thesubnet specified is invalid and we have to go ahead and associate this easy to instance with a specificsub network.All right.So I hope with this you understood the basics on the requirement of launching an easy to instance ina availability zone and how the sub network divides the VPC into certain set of availability zones.Let's go back as we know that there is a default VPC that is also available.Let's search by the default VPC here.So this default VPC is already divided into sub networks.So if you go to the subnets over here, you see in total there are six sub networks that are available.The reason why six sub networks are available is because in the North Virginia region there are sixavailability zones.Now, if you go to another set of region, every region has a different set of availability zone.Let's switch to Mumbai region.And in Mumbai region you will see that there are only three subnets that are available, primarily becausethere are only three availability zones.Now, as we're discussing, each subnet is associated with a specific availability zone.So if you will see over here, let's go a bit on the right on the availability zone, the first subnetwork is associated with one a second with one C and third with one B.Each sub network can only be associated with a single availability zone.Now from our AC to console, let's switch back to the default VPC.It's termed as default over here within the subnet.So what you see, there are six subnets that are available.And if you want to launch your AC two instance in availability zone one C, you can select it from here.It depends upon your choice grid.So this will conclude today's video.