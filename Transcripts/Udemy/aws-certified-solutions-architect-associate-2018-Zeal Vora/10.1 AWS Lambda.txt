# 10.1 AWS Lambda

Hi, everyone, and welcome back.Now, in today's video, we are going to discuss about the basics of the Lambda Service.Now, before we look into the Lambda, it is important that we first understand the challenge that theorganization used to face post, which we'll look into the benefits that the service provides.So let's assume that there is a requirement in that organization where you have to deploy a sample applicationin production environment.So what are the things that you need to do in order for you to deploy the sample application?First primary step is you have to provision the EC two instances or servers where the application canbe deployed to.Then you have to take care of the high availability in case if you just deploy one server and one servergoes down, then your production application also goes down.So you have to launch multiple set of EC two instances where the application is configured.You also have to take care of the overall scalability.What happens if there are 10,000 requests to the sample application?Will one or two ESI two instances be able to handle all of them?Or you can make use of auto scaling to automatically scale when the traffic load increases, then youhave to configure the application environment.It's not that once you launch the EC two instance, you directly upload the application.The application environment needs to be configured.You have to look into what is the type of application, whether it's based on Java, Python, etc. andbased on that the environment needs to be configured.Then you have to take care of the overall operating system updates.You have to take care of security and so on.So just to deploy a sample application in production environment, so many things needs to be takencare of and this applies for all of the organizations.So in order to overcome this, A has released the Lambda Service, which is nothing but a serverlesscompute service that lets you run code without provisioning or managing the servers.So what do you do?Whatever application code that you have, you deploy that code at Lambda and that's about it.No longer.You have to create a EC two instance, configure the application environment, take care of scalability,etc. Everything lambda takes care for you and this is what is referred to as a serverless.There is no need of servers from the customer point of view.So let's go ahead and have a quick demo.Now, currently from the management console, let's quickly go to the Lambda Service.And within the functions.Let's go ahead and quickly create a function I just call it as first lambda function.And we'll go ahead and we'll create a function.And great you see it states successfully deployed the first lambda function.Now, one thing that you might have noticed, in fact, let's create a function one more time is theruntime.Now, depending upon the application that you have, you can select multiple runtime, you have dotnet, go Java, Ruby, Python and others.So you have great set of flexibility in terms of runtime.Now at this stage we did not really upload our own application code and this is the reason why thereis a default code.If you'll see that default code of Halo from Lambda, which automatically got created based on the runtimethat was selected by default.However, in case if you like, you can go ahead and edit this code.You can go ahead and upload your own set of code.So you see you have option to upload your own code and so on.Now one more thing is that you can also configure a lot of things.For example, you have configuration that you can edit associated with memory.So in case if you need higher amount of memory and the other storage related configurations, you cando that.You also have option of function URL, which is quite helpful.So in fact, let's look into it.So I'll quickly create a function URL here and you get a URL for your lambda function similar to howwhen you create ac2 instance, you get a set of IP address and the DNS name.Same here you get a function URL.So if you open the function URL, you get the output of halo from Lambda, which is the same that yousaw as part of the code.Apart from that, in case if you'd like, there are a lot of other configurations related to the VPCenvironment variables and others that you can configure depending upon the use case of your application.Great.So I hope with this at a high level overview, you understood the basics of serverless compute and theadvantages that Lambda offers, and with this will conclude today's video.