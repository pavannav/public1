# 16.6 S3 Transfer Acceleration

Hey, everyone.And welcome back.Now in today's video, we'll be discussing about the S3 transfer acceleration.Now at a high level overview, the S3 transfer acceleration, also referred as the SDR, allows usersto accelerate data uploads from all over the world to a centralized S3 bucket.Now, generally what happens is that you let's assume you have a set of user on the left and you havean S3 bucket on the right.Now this user can be global users.Let's assume that you have a bucket in the North Virginia region.Many organizations, they have global teams across the world, so they might have team in US, teamin Asia, maybe team in Australia, Europe and so on.And if this user wants to upload the data to S3 Bucket, then it goes via the internet.So you have multiple routings that happens via the internet.And when you have terabytes of data that needs to be uploaded, things can be also a little slow overhere.So with the help of S3 transfer acceleration, what happens is instead of directly going through multiplehops throughout the Internet, the user can send the data towards the CloudFront edge location.Now the CloudFront Edge location has a very optimized network between the S3 bucket and the edge location.So once the user uploads the data to edge, the edge can in turn take care of uploading the data towardsthe S3 bucket.And this is a much more optimized network with various optimized protocols as well.And this approach is referred as the S3 transfer acceleration.So let's quickly understand this with a small animation.So let's assume that you have an S3 bucket over here somewhere in the European region and you have usersacross the world.So you have users in us in South America continent as well as in the Australia.So a diversified users across the region.And if all of them has to upload the data again, it goes through the internet across multiple hops.Things can be also a little slow over here.So instead of doing that, what you do, you have the edge location, which are also distributed acrossthe world.So users connects to the edge, location and edge location through its optimized network, takes careof sending the data to the S3 bucket.So now if you see, instead of sending directly to the S3, the user will send the data towards thenearest edge location, whichever is present.So this is what the S3 transfer acceleration allows you to do.So let's do one thing.Let's try it out.Now I have a sample bucket called as my transfer acceleration.And if you go to the properties over here, if you go a bit down, you have an option of transfer accelerationover here.So at this stage it is in a disabled state.Let's go ahead and enable it.Now, one important thing that you will notice is that as soon as you enable the transfer acceleration,you get a new endpoint, which is the accelerated endpoint.Now do note that after transfer acceleration is enabled, the user has to send the data towards thepath, which is the S3 transfer acceleration path, which is through this specific endpoint.The bucket endpoint is also present.If the user sends the data towards the bucket endpoint, then the data will not really go towards thecloudfront edge location.All right.So this is important part to note once, then you can click on Save Changes.So as soon as the changes are saved, if you go a bit down again to the transfer acceleration, youwill get the accelerated endpoint over here.Now one important part to remember is that this works vice versa.So not only it helps in data uploads, but also when you go ahead and download the data.This is also helpful there.So let's try it out.So what we will do, we'll first go ahead and upload some data to this S3 bucket.So for today's video, I have a simple image called as farm dot Jpeg.So this is one of the pictures that I had recently taken.In fact, we were planning to buy a small plot of land to build a small house which also has a farm.So you can do a bit of farming in an area which is full of nature and definitely in the area where theinternet signal comes.So great place.So we'll be using this specific image for our upload process.So I'll click on Upload, let's add a file, I'll upload the farm dot Jpeg here and let's do an upload.Great.So now when you access this file, there are two ways to do that as we were discussing.One way is you can directly connect to the S3 bucket endpoint to access the file.And second is you can connect to the SDR endpoint.Depending upon both of these approaches, the results might differ.So before we do that, let's do one thing.Let's go to permission and we'll remove the public access setting over here.Let's do a confirm.And along with that, for just simplicity, I'll.You go ahead and make this object as public.Great.So now let's quickly look into multiple ways in which we can access this object.So the first thing here is you can go ahead and open this specific object.It will basically give you the object URL over here.So let's copy the object URL and let's do a curl on eye on this specific object URL here.And one thing to note over here is that the server that it is showing is Amazon S3.We'll just keep a note of this and we'll also try to send the request to the endpoint now.So we'll do a curl hyphen.I.So I think there is a small typo here.So if you see there is a comma and the dot and that is why you have a little different object URL here.But that is fine.That will not impact our practicals.So let's quickly go to properties.Let's fetch the URL of our accelerated endpoint.So this is the URL, I'll copy this up and at this stage it is showing you the 403 forbidden.This is as expected.So what you need to do, you will have to also specify the object URL.So let's copy the object URL here.Great.So one thing that you will notice here is that this specific content that is reaching us, it is comingfrom CloudFront.So the server is Amazon S3 but it is coming through the CloudFront distribution via the edge location.So since we have requested this object first time in CloudFront, it is getting a miss.However, after a certain amount of time it will be a hit.Great.So I hope with this you understood the basics of transfer acceleration feature.And with this we'll conclude today's video.