# 7.4 Overview of Endpoint Services

Hi everyone, and welcome back.Now in today's video we are going to discuss about the basics of VPC endpoint services.Now, whenever you're working with medium to large scale organization, you will see that there aremany third party integrations that are taking in place to achieve a specific use case.One of the very popular one is integration of a third party provider for various kind of monitoringsolution, including system, application and so on.Now, in order to achieve this use case, there are various popular providers that we see organizationsusing like Datadog, New Relic, etc. these set of third party providers to create these set of dashboards.What they need.They need the appropriate data.Now here this data can be related to system matrix.It can be related to the application data and so on.Now one of the challenge that organizations generally faces is that the matrix that we were discussingthat the third party solution needs to create a dashboard.These matrix are generally sent via the internet.So let's understand this.On the left hand side you have the consumer.On the right hand side you have the service provider.It can be Datadog, it can be New Relic, etc..Now for the service provider to generate dashboards and performance related data, what they will need,they will need the metrics from the consumer.And these metrics and these set of logs are generally sent via the internet.Now, in a smaller organization, this is not much of an issue, but whenever large organization comesinto picture, there are so many audit, there are so many compliances that comes into picture.And when you send a lot of important metrics and you send a lot of important logs to the service providerinstance through the internet, this leads to certain set of disadvantages.Now this disadvantage can be related to the overall cost.Because transferring so much amount of data through the internet, you will be charged.It can be related to a latency, it can be related to security and so on.So what is a better solution?Better solution would be if the data could be sent from the consumer to the service provider throughinternal network, bypassing the internet.So this is a much more better approach.Now if you'll see instead of sending the metrics and logs through the internet, now the consumer issending directly through the internal network.Now, since the consumer machine are in AWS, we assume that the internal network is AWS private network.Now, how can consumers send the data through the AWS Private Network to the service provider, whichis also hosted in AWS?So one of the possible approach that you might think about is the classic VPC peering, in which youhave a service provider VPC, you have a consumer VPC, you do a VPC peering between them.And from this specific peering, you go ahead and send all of the metrics and logs data to the serviceprovider.Now, if this approach works, I would say that this is one of the simplest one that can be done inquickest amount of time.However, VPC peering is not practical in a larger infrastructure.Let's understand this.So in this specific diagram you have a service provider VPC with a Cidr block of 1070 700 16 consumer,one VPC with the Cidr block of one 7230 100 16.Now, one important consideration is the larger service provider because Datadog, New Relic, theyhave tens of thousands of customers.So this is one important consideration to keep in mind.All right.Now when you have thousands of customers, service provider will have difficulty primarily related tothe Cidr overlapping issue.Now, in the first case since the Cidr block of service provider and consumer are different, this isthe reason why VPC peering will not create an issue.Now let's assume that there is one more consumer named as consumer two.Now this consumer two has the exact same Cidr block 1070 700 16.So this will not be able to achieve VPC peering with the service provider.And hence the approach of VPC peering for sending metrics and log data via the internal network is notfeasible.Now looking into this specific challenge, what is the consumer requirement?Because there are thousands of big enterprises that are hosted in AWS, and in all of them, they willhave similar kind of a requirement.And what is that requirement?The requirement is that the consumer and the service provider should be able to communicate with eachother through the AWS internal network, without having to worry about the Cidr overlap.This is one of the very common requirements that AWS has received from consumers, and in order to achievea solution, AWS has introduced.Endpoint services.So at a high level overview, using the VPC endpoint services, the consumer can make use of the interfaceendpoints to connect to the service provider VPC.And this traffic flows through the private network.Now let's understand this with a diagram here where on the left hand side you have the consumer.And on the right hand side you have the service provider.Service provider can be Datadog, New Relic, or any service provider which will be receiving data fromthe consumer.Now what happens here is that on the service provider side, service provider has to create a type ofload balancer.It can be network load balancer.It can be gateway load balancer as well.And this load balancer will go ahead and it will send the traffic to the service provider instances,whatever they might have.Now on the left hand side you have the consumer instance which might be sending some kind of a metricor log data.Now this consumer instance will send the data to the interface endpoint that is created in consumerVPC.And this interface endpoint in turn is connected to the network load balancer in the service providerVPC.And this is achieved using the feature of endpoint services.This connection that we see between the interface endpoint and the load balancer of the service provideris achieved using the endpoint services here.Now, in this kind of a solution, you don't have to do any kind of a VPC peering or setting up theVPN and so on.This is the direct connection that has been established.And this makes things much more easier, primarily because even if you have hundreds of consumers here,it does not really matter.All you will have to do is you have to connect the interface endpoint of the consumer to the appropriateendpoint services of the provider, and things will work perfectly well.So with this base set, let's go ahead and have a quick demo to understand how things look like.Now for today's demo, we'll take each part of this architecture step by step.What is the first thing in the service consumer side?It is the EC2 instance that will be sending some kind of a data to the service provider.So I have a consumer EC2 instance over here that is running in one account, which is the service consumeraccount.What is the second part here?It is the interface endpoint.So if you see I also have the interface endpoint that is currently running.Now this is the service consumer side and this is one account.Now I also have the other part of the architecture created in second account.And what is the architecture.It is a network load balancer and some instances.So if you see in a different account called as sandbox account, I have a network load balancer thatis currently running.And if we look into this network load balancer let me open up the DNS.This network load balancer is sending traffic to one of the EC2 instance that is running in the sameregion.Let me quickly show you.So this is the service provider EC2.So this network load balancer is sending traffic to this instance.So when will open the DNS of the network load balancer.You see, we are getting a very simple page.Welcome to Ingenix.Now, this can be some kind of a complicated application that is capable of receiving the metrics andso on, but we are keeping it simple just for our demonstration.All right.Now as we are discussing the interface endpoint that is created in account number one can connect tothe network load balancer in account number two using the feature of endpoint services.So this endpoint services is generally created at a service provider level that will be providing serviceto consumers.Now in the service provider account which is the sandbox account.You see I have the endpoint service which is created now.This endpoint service is associated with our network load balancer, which is also running in the sameaccount.And this interface endpoint is connected to that specific endpoint services.Now if you see this interface endpoint that we were discussing, you see it is also showing us the servicename over here.So this is the exact service name.It ends with 1930E.And if you look into this specific endpoint service it also ends with 1930E.All right.So this connection has already been established.So now it is important to understand whether this entire workflow works.So in order to verify that what we'll do we'll log in to this EC2 instance on the consumer side willsend the request to this interface endpoint.Now if things are working, this interface endpoint through the endpoint service would send the requestto the network load balancer.Network load balancer will send a request to the EC2 instance which is running nginx.And this instance will send a response back.And in this EC2 instance we should see the HTML page of nginx.So that is the entire workflow.So let's try it out.So I'll use the EC2 connect to connect to our EC2 instance here.All right.And for this interface endpoint we'll copy the DNS name.I'll clear the screen.I'll do a curl.In fact, let's just remove the eye for the time being.And you see, we got a nice little response here.Now, this is a classic HD email.You see, it states Welcome to nginx.Similar to the page that we have seen here.Now all that it has done this basically the HTML that you see over here, the same HTML we got in theCLI.So this means that the entire connection is working.Now one more thing that I wanted to share you is that both of the consumer and the service providerinstances are running in the VPC with the same Cidr block.Let me quickly show you.So this consumer EC2 has a IP address in the range of one 7231.So this is the default VPC.I'll also show you this.So in this region, you see there is only one VPC.This is the default VPC with the Cidr of one 7231.In a similar way.The service provider EC2 where the actual nginx is running.It is also running in the one 7231 range.I'll also open up the VPC here.You see, there is the default VPC with the exact same Cidr block.All right.So I hope with this you understood that even with the Cidr block being same, this specific approachallows us to connect applications so that the data can be traversed over the AWS private network withoutworrying about the overlapping Cidr blocks.Now, just for better reference, I have also taken the screenshot of the interface endpoint.So this endpoint is in the consumer side.And in here you see there is a service name.So this interface endpoint is associated with the service.And this service can belong to any other account as well.It is not necessary that it is in the same account.It can belong to different accounts as well.Great.So that's a very high level overview about the VPC endpoint services.And with this we'll conclude today's video.