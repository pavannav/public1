# 14.13 Overview of Routing Policies

Hey, everyone.And welcome back.Now, in today's video, we'll be discussing about the routing policies in Route 53.Now, routing policy basically determines how your Amazon Route 53 responds to the queries.Now, there are a lot of routing policies that are available in Route 53, and depending upon the usecase, you can select one among them.Now we'll be understanding this in more detail, but just at a high level overview, just know thatthere are multiple routing policies like simple weighted, you have latency failover geolocation aswell as multi value.Answer.Now before we go ahead and discuss about this in more detail, let me quickly show you on where do youexactly select the routing policy Now once you have a hosted zone in Route 53, when you go ahead andcreate a record while creating a record under the routing policy here, you see that there are multiplerouting policies that can be used.And it is important for us to understand at a high level overview what exactly these routing policiesare all about.Now, the first and the simplest among them is a simple routing policy where you have a plain 1 to 1mapping between the domain and the host.So for example, you have a record name of blog dot internal, which has its associated value.So this is a very simple routing policy based example.Now the second important one is called as the failover routing.So failover routing lets you route a traffic to a resource when the resource is healthy or to a differentresource when the first resource is unhealthy.For example, if Route 53 is routing the traffic to this specific EC2 instance, this is perfectly fine.But what happens if this EC2 instance goes down?Then with the help of failover routing, Route 53 can go ahead and send the traffic to a different resourcealtogether.So this is what failover routing refers to.So if first resource fails, Route 53 will have intelligence to understand the failure and automaticallyroute the traffic to a different endpoint itself.Now, the next important type of routing policy is called as a weighted routing policy.Now weighted routing policy basically allows us to route the traffic to multiple resources in a proportionthat we specify from our end.So you have a Route 53 and let's say that there are two EC2 instances and you have specified 5050 overhere.So that basically means 50% of the traffic will go to EC2 one and 50% of the traffic will go to EC2two.Now depending upon the configuration, you can send 10% of the traffic to EC2 one and 90% of trafficto EC2 two.So depending upon the weight that you add, the routing would differ accordingly.So that's what weighted routing is all about.The next is the latency based routing.So if your application is primarily hosted in multiple AWS region, then you can improve the performancefor users by serving the requests from the AWS region that provides the lowest latency.So here you have multiple servers across the region.Now it can happen that the first EC2 instance has a higher latency and the second EC2 instance, whichis in a different part of the world, has a lower latency.So in latency based routing, the traffic is directed towards the server which has the lowest latency.Now the last one for today's video is geolocation routing, which basically allows us to choose differentresources for different user based on different countries or continents.For example, if there are two users from India and USA, based upon the geolocation routing rules,Route 53 can automatically route the traffic to different resources.So for example, India user, when he visits the Route 53, Route 53 will direct him to the EC2 instance,which is based on the Singapore region.If an individual from us, he sends a query to Route 53, Route 53 might redirect him to the EC2 instancethat is based on the Oregon region.That's great.So I hope at a very high level overview you understood about the basics about the routing policies thatare part of Route 53.So with this, we'll conclude today's video.