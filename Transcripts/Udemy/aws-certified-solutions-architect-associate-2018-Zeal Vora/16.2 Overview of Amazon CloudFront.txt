# 16.2 Overview of Amazon CloudFront

Hi everyone, and welcome back.Now in the earlier video we were discussing about the basics of the Content Delivery Network and CloudFront.So today is going to be the practical video where we are going to deploy CloudFront distribution andwe are going to look into the entire workflow.Now whenever you're deploying CloudFront, one of the important configuration is the origin.So where exactly your back end is where exactly CloudFront will fetch the files from.Is it S3 bucket, is it the EC2 instance etc..So this is one thing that you need to consider.Now for today's video.To keep things simple, the origin that we are going to choose is going to be the S3 bucket.So from the AWS console first we are going to the S3 service.And within here we are going to quickly create a bucket.The name of the bucket you can give any name.All you have to ensure is that the name is unique because these names are unique across all the AWSaccounts.So this is one point that you have to keep a note of along with that once, then you can just go aheadand create the bucket.Great.So once the bucket is created, you can quickly open this.And what we are going to do we are going to upload some files.So I'll click on upload.Let's add a file.Now in terms of file I will be uploading this image file to the S3 bucket.You can upload any random image that you would like, but I'll also be attaching this jpeg as part ofthe resources so that you can make use of it in case you'd like.So I'll upload this specific file to the S3 bucket.Once this is done, let's do a close.And we have Guardians dot JPEG that is available now just for simplicity what we are going to do interms of block public access, I'll just click on edit I'll deselect it.Since this is going to be website, I'll just click on Save Changes and I'll do a confirm.And in a similar way there is a bucket policy.So we have to add one bucket policy.I'll add the exact policy after this video so that you can directly copy paste here.So the bucket policy looks something similar to this.So all you have to do is you have to replace the bucket name with whatever bucket name that you haveand add it.So I'll copy this and I'll add it over here.Within here I'll add the bucket policy and make sure that you replace the bucket name here.And that's about it once.Then you can save the change.Great.Everything is done.Before we start with CloudFront, just verify if you are able to open the image.So I'll just copy the URL and in a new tab if I open the URL you should see the image over here.So at this stage our origin is set.So now what we want is users Uses instead of directly connecting to the origin, we want users to connectto the CloudFront distribution, and CloudFront distribution will in turn connect to the origin.So now what you can do, you can just open the CloudFront console.And currently I have one distribution that is in the disabled state.We had used it as part of the demo.We are going to create one more distribution.So I'll click on Create Distribution.The origin over here is the important part within your will.Select the S3 Bucket Key Labs website 007.We'll leave everything as default.Origin access is public.We are not going to touch any of these in case if you'd like.There are a lot of configurations related to security that are available, but we are not interestedat this stage to explore them.So let's go a bit down.You have the WAF related configuration.Again, we'll not select this.Now.One of the important part here is the price class.You can either use all edge location which is the best performance, or maybe only North America andEurope, or maybe the third option.So any one you can choose.Let's just use the default one all edge location.Best performance.Let's go a bit down.We'll go ahead and we'll create the distribution.At this stage it is asking us to choose the security option.I'll just select Do Not Enable Security Protection at this stage.I'll go ahead and I'll create the distribution.All right.Now whenever you create a distribution you will get the distribution domain name.So this means that the users will have to connect to the distribution domain name.It can be a routing from your main domain.For example if you have a domain called as example.com, that example.com should point to the domainof the CloudFront.This is 1.2.Note one more thing if you go back to distribution at this stage the status of the distribution is enabled.However, in the last modified year you see it is stating deploying, so it takes a little amount oftime.Give it a few minutes for it to be deployed.Once this part gets updated, then our CloudFront is ready and we can test the entire workflow.So let's quickly wait for a few moments here.Alright, so it took around 4 to 5 minutes and our status is deployed.So now we can test the entire workflow.So now you can copy the domain name of CloudFront distribution.You can add it as part of your browser.And then you can specify the object name in this case guardians dot jpeg.So let's specify Guardians dot jpeg.In case everything is configured you should see the image.And yes, it is appearing perfectly as expected.Great.So that's a high level overview about the basic workflow of CloudFront.And with this we'll conclude today's video.