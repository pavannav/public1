# 8.9 IAM Role

Hayden.And welcome back.Now in today's video, we'll be discussing about the basics of Iam role.Now role is an entity that contains a set of policies, and any resource that is assuming that rolewill be able to have the same permission that is associated with that role.So let's understand this with a simple animation where you have this Iam role and this Iam role willhave a certain set of Iam policies that are associated with it.So you can consider it to be very similar to an Iam user on how each Iam user has a set of Iam policiesthat are associated with it.Now going back to the definition here, which states that any resource that is assuming the role willbe able to have the permissions that are mentioned in the role.So for example, let's assume that you have an Iam user and you have an EC2 instance and if any oneof these assumes this role or in a simplistic terms uses this role, then what will happen is both ofthem will have the same policies that are part of the role.So that is the basic idea.So let's go ahead and understand the need of Iam role with few use cases.Now our first sample use case, the name is nono scenario.So this means that you should not do this in production.So the use case states that you have an EC2 instance and this EC2 instance wants to fetch certain datafrom the S3 bucket.Now it can happen that there is some application that is running on the EC2 instance and that applicationrequires certain objects that are part of the S3 bucket.Now, from what it is observed in a lot of organizations, what they do is they hardcode the accesssecret key within the software itself.So within the application code, you will see that they have mentioned access or secret key, or maybethey had run the AWS configure command and they had added the access and secret key in the instancelevel directly.So once you have the access and secret key, that is part of the instance, depending upon the permissionof that access and secret key, the instance will be able to fetch the data from S3 Bucket.So although this scenario is something that you will see in many of the organizations, but you shouldnever use this type of approach.So instead of that as part of the best practice, you can always make use of Iam role.So what would happen here?You have an EC2 instance and you have associated a Iam role with that EC2 instance and the Iam rolehas the necessary policy for the application to be able to fetch or get the data from the S3 bucket.So in this case you are not creating any access or secret key, you are not configuring the applicationwith any access and secret key being hardcoded.So this is the basic concept.Let's go ahead and quickly jump to practical and understand both of these scenarios.So for today's demo, we'll be making use of an EC2 instance that is based on Amazon Linux two.And what we'll do is first, we'll be exploring this nono scenario and second, we'll be exploring thisIam role based scenario.So what we have in order to simulate the nono scenario, I have an Iam user and we'll be generatinga access and secret key associated with this Iam user.So these are the access and secret keys.Now as we are discussing what many of the organizations they do, they use this access and secret keyand they add it as part of the application code directly.So not only the developer will have the access to these keys, but all the users who have access tothe code will also have the access to these access and secret key.So you can consider it to be the public keys within your organization.And by mistake, if someone commits it online, then it would be a public key for the entire internet.So let's try it out.So I'll copy this access key ID and from the instance when you do a configure, you can add the accesskey or you can add a secret key and maybe the region you can select us East one or any relevant region.So this is the second most seen use case.First being the developer will add these keys within the code.Second is the organization might decide to hardcode the keys, something similar to what we are seeingright now.So once you have done that, when you run a query like AWS, EC2, describe instances, you see thingsare working as expected.Now in our case, if you look into the permission for the example, I have just added a EC2 read onlyaccess, but it can also have the S3 read only access something similar to what we were discussing.So instead of generating these keys altogether, what you can do is you can create an Iam role.So let's try it out.Now within the Iam dashboard, you can go to roles.You can go ahead and create a role.Now, since this Iam role will be used by EC2 instance, this role selection will have EC2 over here.Let's do a next and within the permission you can go ahead and associate a permission accordingly.So let me go ahead and add an EC2.Read only access and let's call it as first Iam role.So within the console we have this first Iam role and here you see that there is no provision of generatingaccess and secret keys at all.So there are no keys that are generated here.So once this role is created, what you need to do is you have to associate this role with the EC2 instance.So in order to do that, you can select the instance.Let's go to actions within the instance settings you have attach replace Iam role.And when you look here, you have the first.Let's go ahead and do an apply.Great.So now within the Iam role, you see that you have the first Iam role that is associated with the instance.So now let's do one thing.The access keys that we had added was part of the AWS credentials file.Let's go ahead and remove all of these.So when you press control K, you can remove the entire line.Let's go ahead and save this.So in order to verify when you do a Nano again, you see that this file contains no credentials.So now when you run the AWS EC2, describe instances as expected.You are able to see the same output.Now, in order for us to ensure that this is coming from the Iam role, let's go ahead and remove thisspecific policy.And now let's try running this again.And immediately, you see, you got an unauthorized operation.Great.So I hope at a high level overview you understood the basics of Iam role.Now, one important part to remember, as we are discussing in the first slide itself, which statesthat any resource that is assuming the role.So for today's example, we had used the resource of EC2 instance, but there can be many other resourceslike Lambda and others which can also make use of the Iam role for performing certain operations.So with this we'll conclude today's video and I look forward to see you in the next video.