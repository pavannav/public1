# 4.21 AWS Storage Gateway

Hi everyone, and welcome back.In today's video we are going to discuss about the Storage Gateway service.Now at an overview.Storage gateway enables the applications that are running in the on premise servers to use the cloudstorage by using the standard storage protocols.So let's consider an example where you have some legacy applications that are running in the on premiseenvironment.It can be some backup server, it can be some application server, etc..And what they do is they store the backup and all of their files in a specific folder.Now, what you want is we understand the advantages that AWS provides in terms of storage, services,etc. because services like S3, you can have virtually unlimited storage here.So what you want is you want the applications to start using the AWS storage services like S3 insteadof the local disk, or maybe the mounted disk in the on premise environment.Now, in order to be able to do that, what you will have to do, you will have to modify the applicationcode because currently the application is storing the data in some local mounted file system, or maybethe local disk, etc. but as soon as you want the application to store and access the data from S3,the application code will need to be updated.The environment will need to be updated, you will require access, secret keys, etc. so instead ofdoing all of that, you can keep the application code as it is because it is working perfectly well.So you don't really want to touch a perfectly working code.So what you can do is using the storage gateway.You can make the application connect to the appropriate storage services like S3 using the standardprotocols.Now again, all of this will make sense in just a moment.But first let's look into the architecture over here.Now let's take an example.So here you see I have multiple set of drives C drive, D drive, M drive etc. however there is onemore drive called as Z drive.This is based on ESXi.So what this means is that in here I have some files that are available.So whatever files that are create as part of this drive.This drive is not really created in the local windows system.This is a remote drive.So whatever data that I store in this specific drive, it will be stored in a centralized server fromwhich this part is mounted.So let me also quickly show you.So in the iSCSI initiator properties there is a specific IP address over here.So the actual data that we store in this specific drive actually gets stored in a server with the IPaddress of 192168244.1 60.So although for the application.So some application might be running on windows.So for the application to store some data in Z drive, it will be very similar to storing the data inM drive.All right.So from application perspective it does not really make any difference.But when you store the data in this drive as we were discussing it gets stored in a centralized storage.And that centralized storage might have say repeated backups, high availability aspect and so on ina very similar way using the storage gateway.The applications that are running in the on premise servers using the standard protocols, they cango ahead and store and fetch the data that might be stored behind the scenes in the remote serviceslike S3.Now, when we discuss about storage gateway, there are three types of gateways that are available.One is the file gateway, second is the volume gateway.Third is the tape gateway.Let's go ahead and quickly look into each one of them at an overview.This topic will make much more better sense when we discuss about the file gateway here.First, let's go ahead and discuss about the file gateway, which is the S3 file gateway.Here at an overview, the S3 file gateway, it enables the file system mount on S3, allowing accessto data directly in S3 using the NFS or SMB protocol.Now again, this might be a little confusing.So let's go ahead and understand this through a diagram.So let's consider that you have S3 bucket in AWS.And in the on premise data center you have one server that is running now, this server you want thatwhatever data that it stores or whatever data that it retrieves, it should do it from the S3 bucketinstead of some storage device in the on premise server.Why?Because you already know the advantages of S3.So if there is some application that is running in the server, you will have to modify that applicationlogic so that it can store the data and fetch the data.You will have to start using access secret keys and others.So in order to avoid modifying some legacy application which is already working perfectly well, whatyou can do, you can go ahead and introduce the file gateway over here.So this file gateway is basically a software appliance.And it in turn gets connected to the S3 bucket.And you can mount this inside the server using protocol like NFS and SMB.So what will happen is there will be some folder in the server.If application is storing the data in that folder automatically, that data will be available in theS3 bucket.Similarly whatever data that is available in the S3 bucket will be made available in a folder in thelocal server.Let's have a demo.This will give us better clarity.Now for today's video I am connected to a server and in here if I do a ls, you see that I have onefolder called as my S3 bucket.Now if I quickly open this folder over here and if I do a LRS, you see that I'm able to see some fileswithin here.Now are these files stored locally in the server?Answer is no.These files are part of the S3 bucket and they are made accessible inside the server using the NFS protocol.Let me also quickly show you.Now for today's demo I have an S3 bucket called as demo website.And within this bucket, how many files are there?You have three files admin, dot, txt, dot jpg and guardians dot jpeg and the same files you see areavailable here.Now let's say application might want to create one more file in S3 bucket.So all application can do is application can use the standard protocol instead of configuring the S3related configuration and say create a file called as legacy hyphen app dot txt.All right.If I do ls you see this file over here.So let's also store some data.I'll just do echo I'll say legacy app and I'll store it to this file of legacy app dot.TXT.Now from the S3 bucket let's go ahead and quickly refresh.And now you see automatically the legacy hyphen app dot.TXT is made available if you want to verify let's go ahead and download it so it is downloaded.If I open this you see within the notepad it is showing as legacy app.Great.So I hope you started to get a gist on what benefit storage Gateway allows.So in this kind of an architecture, the application which is designed to store some data in the localfile system in Linux, for an example.You don't really have to modify the application code, even if you want it to start storing and fetchingthe data from S3 bucket using the file gateway for application.It is just a folder, but behind the scenes file gateway will sync the data from that folder to andfrom the S3 bucket.Now here we were discussing about the protocol.So this is the NFS protocol.Now let me also quickly show you again you don't really have to know this from the perspective of exams,but I'm just giving you a gist on what exactly this is.So if I quickly do cat on proc mounts over here, you will see there will be one specific mount.So within here there is one specific entry.You see.This is the entry over here.So this is basically the NFS.You can see it is NFS four.So using the NFS protocol whatever data that is available in the S3 bucket using the File Gateway appliance,it is made available inside this specific folder which is root S3 bucket.Now again you can change this based on the requirement, you can change the folder, you can changethe S3 bucket, etc. but the main workflow would look something similar to this.Now let's take a step further to understand more in detail about the storage gateway.Now for today's demo.I already have a storage gateway that is configured, so if I open this gateway.So you see I have a demo file gateway that is available.The gateway type is S3 file.So if I open this gateway over here you will see that you have the IP address 172319.10.And this is the same IP address from which we had mounted the file system through NFS.All right.Now whenever you have a gateway currently I have also have file shares.So here this is the NFS based file share.In case if you are using windows then in such case the file share type would be SMB.So both of the protocols are supported.Now for easier reference, I have also added the screenshot of the slide from one of the AWS presentation.This will give us better clarity.So here we were discussing about the on premise setup, and we had taken an example of File gateway.File gateway is where the applications will go ahead and store the data in a specific folder mountedvia NFS or SMB.And behind the scenes, the data will be synced to the S3 bucket.Along with that, as we were discussing, there are multiple other types of gateways that are available,like volume gateway and tape gateway.In fact, let me also quickly show you.So whenever you create a gateway, there are three primary types of gateways that are available.You have the S3 file gateway.You have the tape gateway, you have the volume gateway.At an overview, the high level workflow might look something similar to the File Gateway demonstrationthat we did, but this one has a different purpose.For example, when we discuss about a volume gateway, it makes use of the iSCSI protocol over here.Now when we discuss about Volume Gateway, it presents the cloud backed iSCSI block storage volume tothe on premise application.So here you are working with the block storage volume directly.And one of the benefit here is that as we're discussing this time the protocol is iSCSI because thisis the volume and whatever data that you add as part of the volume, it can be stored as part of theS3 bucket.Now, before we discuss about the tape gateway, it is important for us to understand the basics oftape storage.Now, tape storage is primarily used in enterprises, and it is a method of storing the digital dataon a magnetic tape.Now, one of the great benefit here, although it looks old fashioned, one of the great benefit hereis that it is quite cheaper compared to the other options.Alright, and this is the reason why many organizations specifically enterprise, uses this for archivalbackup.Now the tape gateway option is primarily used to replace the physical tapes with the virtual tapes inAWS, and it acts as a drop in replacement for the tape libraries.So sometimes in exams, if you find a question where they want to replace the tape libraries, you wantto drop in replacement, the tape gateway proves to be very useful.Over here again.Over here you have a backup server.Using the appropriate protocol.It can store the data in the tape gateway.And this will be synced to the AWS side.So that's an overview about the storage gateway.Now before we conclude one more thing that I wanted to emphasize upon because here you see that in onpremise your file gateway volume gateway tape gateway.But what exactly it is, is it a hardware, is it a software or what exactly it is?We have to understand.Now, just for simplicity, you can consider this to be an appliance.Appliance is one of the options that are available.For example, here you see that I have an EC2 instance.So this is an S3 file gateway appliance.So appliance is basically you can consider it to be a software that is installed inside the EC2 instance.And this is what we use as a interface.So the NFS mount that happens it happens on the IP address of the appliance.For example here the IP address if you see it is 19.10 and the same IP address that you see as partof our server which contains the application.So NFS mount is happening based on this specific appliance IP address.So generally whenever you create a storage gateway you have multiple options.For example in platform options you can have EC2 instance.So this EC2 instance will have the appropriate software that interfaces the file gateway related configuration.So this software provides the necessary NFS that can be mounted.And also it will sync the data to the appropriate S3 bucket.So this is one option.You have the option related to the Microsoft Hyper-V.You have the option related to the VMware ESXi.So in this case you will have to download the image.And based on that you will have to create the appropriate set of virtual machine.And that virtual machine will have the necessary software that can act as an interface between the serversin the on premise and the cloud environment.All right.So this is one point that we have to keep a note of.That's the overview of the storage gateway.And with this we'll conclude today's video.