# 13.16 Configuring SSM Agent

Hi everyone, and welcome back.Now in the earlier video, we were discussing about the basics and the advantages that AWS Systems ManagerService provides.Now let's assume that you find that Systems manager is going to be very useful in your organization,and you want that all of the instances that are running it should be managed through systems manager.So by default when you enable Systems Manager service, is it going to automatically connect to allof the instances based on the requirement?The answer is no.So there is certain setup and certain configuration that is required at a node level for it to be successfullybe able to manage through Systems Manager.In today's video, we are going to look into it.Now the most important step number one is to install the SSM agent.So you have to ensure that SSM agent is not installed, but it is actively running inside the EC2 instance.This is very important because most of the features of Systems Manager service relies on the SSM agentthat is inside the EC2.So this is the first part that you have to ensure.The second important part is that after you have installed and the SSM agent is running, then thisagent will also require permission because it will connect to the systems manager service.So in such case, the EC2 instance will require an IAM role to be created and the systems manager agentwill fetch the access secret keys that are available through the role to connect to the Systems Managerservice and various other services on your behalf to perform a specific set of operation.And step number three is that whatever IAM role that you create an associate with the EC2, that roleshould also have the appropriate policy.So there is a specific policy called as Amazon SSN Managed Instance Score.This is a very important policy, and it basically provides a minimum set of permissions required forEC2 instance to be managed by SSM.So this is one thing that you have to ensure that is attached.So these are the three base important steps.Let's go ahead and try it out.Now the first thing that we require is the EC2 instance.So let's go ahead and quickly launch a EC2 instance.I'll just call it as EC2.Let's go a bit down.We'll leave everything as default.You can just associate a key pair in case if you would like rest.We'll just keep things as default.Let's click on Launch Instance.Now.Once the instance is launched, let's quickly refresh.This is the instance that is launched.Now you have to ensure that this EC2 instance has the SSM agent installed and running.And along with that, once the SSM agent is installed and running, it should also be able to connectto other services like the systems manager and others.So the EC2 instance, either you might configure the access and secret key, or you can add the appropriateIAM role.And this IAM role will have the associated policy.Now first let's go ahead and discuss about the SSM agent.Now one of the great things here is that if you are using instance Amis like Amazon Linux, then theSSM agent is pre-installed.You don't have to do it manually, but in case if you are using some different type of Amis, then insuch case you might require installing SSM agent in a manual way.So in fact, let's go ahead and quickly connect to this EC2 instance.You can either connect through SSH or you can use the EC2 connect as well, for example through theterminal using the appropriate key.I can connect to the EC2 instance from here.And sometimes if that does not work, then you can use the EC2 connect to directly connect to the instancethrough the browser.Whichever way works, you can use that.Now I'll be using the terminal session for our demo.So the first thing as we are discussing is the Amazon SSM agent.So if you verify if I do systemctl status Amazon SSM agent, you see the agent is active and it is runningperfectly well.So this is one of the great benefits of the Amazon Linux based Ami.However, here you will see that there are certain errors that you will see primarily because of thecredentials.So it is not able to find the credentials because the agent needs to connect to the systems managerservice, so credentials are required.So this is the reason why using the IAM rule is the recommended approach.Now in the step number two we are going to go to the IAM service.And within here we'll go to roles.We'll create a new role.The service is EC2 as we know.We'll go ahead and do next.We'll not add the permission at this stage.Let's do next.And the name of the role.Let's just call it as SSM role for EC2.And I'll go ahead and I'll create the role.Great.So once the role is created let's quickly search for this role.And inside this role I'll add a permission policy.Let's attach a policy.We are more interested in a policy.What was the name Amazon SSM managed instance score.So let's search for that.Amazon SSM managed instance score.So this is the policy I'll go ahead and I'll attach it to a roll.Once this policy is added to the roll, now we have to associate the roll with the EC2 instance.So let's go ahead and do that.So I'll go ahead and modify the roll from here.And the roll is SSM roll for EC2.I'll update the roll.Once you have associated a roll with the EC2 instance.In such case you can just quickly restart the SSM agent.Great.So all of our three steps are now completed.So now let's go to the Systems Manager service.And within the Systems Manager service if you click on Review Node Insights this is a relatively newconsole.You can click on either Enable new Experience or even if you click on Review Node Insights you can enablethe new experience from here.So let's go ahead and quickly do that.So at this stage it is setting up the Systems Manager integrated console, where you will be able tosee all of the instances that are managed the SSM agent version running on that instance, the operatingsystem associated with the instance, and so on.So this can take few minutes.So let's quickly wait for this to be completed.Alright.So at this stage it says successfully configured the systems manager integrated console.But this widgets the data associated with it.It takes a little amount of time.In fact, let's quickly refresh this to see what is the updated console that we see.So within here the node summary it says the widget is still configuring and for others no data available.This is fine.You can just give it a few moments here.All right.So it has been around 5 to 6 minutes.Now if I go ahead and if I refresh the page this time you see the data as part of the widgets has startedto appear.We can also see that we have one node based on the Amazon Linux.Apart from that, you will be able to look into other details.For example, if you click on Explore nodes you should see one node that is currently available.If I click here, it gives you details associated with the node, and there are various other thingsthat you will be able to do.For example, if you want to see what are the folders and files available as part of the node, youcan click on File System.And here you will be able to see the details associated with folders and files that are available.As you can see, there are so many files that are available inside, etc. this is one part.You can also go ahead and look into users and groups.So for example these are the users.And along with that it gives you information about the groups as well as you can see.Along with that you will be able to see what are the packages that are configured or pre-installed aspart of the EC2 instance.This is also very helpful in wide range of use cases.Along with that you can also look into the performance counters.So from the centralized console itself, all of these details will be available.One more thing that you have to be aware of is that in case for certain Amis SSM agent might not beinstalled, in such case, you can manually install the SSM agent.So in the documentation they have provided you the details on how you can install SSM agent.For example, if you are using Rocky Linux, then in such case the commands for installing SSM agentare provided over here.So this way things become simple.One more thing I wanted to share is that although many of these features are working for some very specificfeatures, there might be some additional configurations that might be required.For example, if you want to see the list of processes as part of the node.So this will not work in a default configuration.There are certain additional permissions KMS.All of these things needs to be configured, so it will just keep on connecting to node.You will not be able to see the list of processes.However, most of the features associated with the systems manager will work without having to changeany explicit settings as part of the systems manager over here.Great.So I hope with this now you understood on how you can go ahead and set up the node to be managed throughSystems Manager service.And with this we'll conclude today's video.