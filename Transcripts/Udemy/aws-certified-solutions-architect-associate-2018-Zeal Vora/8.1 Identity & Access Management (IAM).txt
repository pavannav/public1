# 8.1 Identity & Access Management (IAM)

Hi everyone, and welcome back.Now in today's video we are going to discuss about the basics of the Identity and Access Managementservice, also referred to as IAM.And we are also going to discuss some of the important features related to this service offering nowat a very high level.Overview.The IAM service allows us to control the access to the AWS resources that are running.For example, on the left hand side you have some AWS users, and on the right hand side you have someAWS services.It can be EC2.It can be any other service that is running in your AWS environment.Now the question that comes is these set of users.Will they be able to perform all kind of operation on these services, or they will be able to performsome kind of a limited operation on these services.So who will decide that?So that decision factor is what the IAM service controls.So in IAM you can have a set of rules where you can say which user can perform which kind of operationon AWS service.For example, John should have full access to all the services.So this is more of a administrative user.Alice should have only access to EC2 and Matt should not have access to any other services.So depending upon what kind of permissions you want to grant to the users in your environment, IAMallows you to create the policy rules accordingly.So just to simplify the overall architecture, all of these rules that we define are referred to asa IAM policies.And these policies you can write in Json.And these policies are added as part of the IAM system.So whenever a user tries to perform any operation on AWS resources, the IAM will verify if the userhas the proper permission, and only after that they will be allowed to perform the appropriate operationon AWS resources.Now, when you go to the IAM service, let's quickly go here.Now within the IAM service.On the left hand side, you see there are multiple set of features that are available.For example, you have user groups, you have users roles, policies and so on.Now in today's video, we are going to look into some of the very important features as part of IAM,primarily the users, the user groups and the policies.These are the ones that you will be using extensively in your environment, and these are the ones thatwe will be covering as part of today's video.Now the first important part is the IAM user.Now from here we can easily identify what this is.This is basically the user that you create in the AWS environment.Now even in the screenshot you see there are so many users that are available in my environment.And the IAM console also shows you various information about each of these users, like when was thelast activity that was performed by them?Whether the user has a multi factor authentication enabled or not, the password age and so on.Now from the IAM console let's go to the users.These are the users that are currently available in my environment.In case if you have never created a user, you will not see any one in the list.That is fine.Let's go ahead and quickly create a new user.Let's just call it as KP labs video user.Just for simplicity, if you want this user to access the AWS Management Console similar to what weare doing, you can select this checkbox within the user type.You can select the second option.I want to create an IAM user.The next part is to select a password.Let me just select a simple password.Here you can also click on show password.It will show you the password here.Whatever password that you have selected.Make sure that the password is a little strong.All right.So let's just add some random digits here as well.All right I'll copy this for reference at a later stage.Now you have the option of user must create a new password at sign in.Generally, if you're creating IAM user in your environment, you have to make sure that this optionis selected so that whenever the other person logs in, he can set his own password quite easily.Once, then you can click on next.You can click on next again.It will give you an overview.All you can do is you can create the user.All right.Once the user is created, it has given three information the console sign in URL.So this is the URL through which the user can log in to your AWS account.It has also shown you the username and the password.Now in case if you would like, you can email the instruction.Or you can also download the CSV file where all of this information will be present.So let's do one thing.Let's go ahead and copy the URL so that we can log in with the user that we have created.Now from another browser I just add the URL will copy the username.Let's add a username here.We'll copy the console password and I'll do a sign in.Alright, so the user has successfully logged in to our AWS account.Alright, so I hope with this you understood the basics of IAM user.Now the next important part that we need to remember is associated with the default permission of theusers that you create.For example, we just created a new IAM user and that IAM user also logged in to your AWS account.Now the question that comes is what are the operations that the user will be able to perform in theAWS account?Will he be able to create EC2 instances?Will he be able to terminate all the resources that are already running in the account, and so on.So that is the question related to the default permissions.And the answer to this is none.If no permission has been assigned to the user, for example, the user that we just created, we havenot really assigned any permission or any rules to that entity.And this is the reason why he'll not be able to do any kind of an operation.For example, you see there are so many access denied message.Now, if we say.EC2.Let's see what the user is able to see in the EC2 service.You see everything is giving an API error.If you go to instances, it is saying that the user does not really have any permission to perform thisoperation, even launch instances.You will be able to see the console, but even in the console there are so many errors, so you willnot be able to create any sort of instances.So basically this user can log in to the management console and that's it.He'll not be able to perform any other operations.Now the question that comes is what are the things that we need to do to ensure that the user has somekind of a permission in AWS account, depending upon his job requirements?And this is where the IAM policies comes into picture.So let's say that the user wants to perform some operations related to EC2 instances.How can we assign him permissions.And we can assign him permissions using the IAM policies, which are nothing but the rules that youassign.So at a high level overview IAM policy defines the permission that a specific entity has in AWS.So whenever you want to allow a user to perform operation, you go ahead and add the appropriate policy.So let's go ahead and explore that now.Let's go back now within the user, our KP labs video user over here you see last activity.It shows three minutes ago.That is fine.And this is a great way of exploring and evaluating the users.Now whenever you open up the user in the permission policies, currently there are no policies thatare assigned.In case if you would like, you can go ahead and assign the policy to the user.Let's click on Add Permission here.Let's click on Attach Policies directly.And here there are so many policies.You see there are 63 pages and even more.One of the common policy here is the administrator access.Now again from the name itself you can identify what this policy does.However, if you want the user, for example, to have permission on only EC2 service, then you cansearch for EC2.There is a specific option of Amazon EC2 Full Access.You can add this.You can click on next.You can add permission.All right so now for the CPU labs video user in the permission policy you will see Amazon EC2 full accesshere.So going back to the CPU labs video user, if I refresh the page right now you see that error went away.And even if you try and launch instances there will error that used to come over here.This error went away.Now one thing that you need to keep a note of is that this policy only allows EC2 related operation.If the user tries to access any other services, he will get a permission denied error.So let's try it out.So instead of EC2 we'll try some other service.For example S3 as soon as user tries to open up the S3 service.Let's see the S3 buckets that we have.And as expected, it states you don't have permission.All right.So depending upon what permission you want to assign to the user, you can easily add it from here.The next important aspect that you need to know of IAM service is related to the IAM groups.Now again, from the name itself, you will be able to identify what this does at a overview.IAM group is a collection of IAM users.So let's say that you have 20 people as part of the development team.So what you can do, you can create a developer group here, and you can assign all of the 20 peopleto the developer group.Now let's assume that all 20 people need access to the S3 service.So you're not going to go to each user as part of the development team, add S3, full access permission,and so on.This is quite cumbersome.So what you do, you assign the IAM policies to the IAM group level, and all the users that are partof the IAM group will be able to assume these specific permissions.All right.So the IAM policies that are attached to the group is associated with all the users that are part ofthe group.This is important part to remember.Let's explore this as well.Now let's do one thing.Let's go to the groups here.Let's create a new group.Let's just call it as developer Group.And let's go ahead and add our CPU labs video user here.You can also add any other users that you want.And you see there is also an option for attaching the policies in case if you'd like you can selectfrom here.Or you can also attach policies at a later stage.So when you open up the group, it will show you the list of users that are part of the group.And it also has a tab of permissions.So you can add permission to this group, let's say S3 this time.So we have Amazon S3 full access.You can attach the policies here.All right.So now what will happen is the policy that you have attached to the IAM user directly.That will also work.And the policies that you have attached at a group level that will also work.So if you have 20 users as part of this developer group and you have assigned S3 full access permission,all of the 20 users will be able to inherit the permissions that are defined at a group level.So now if I refresh this specific page.You see the error went away.All right.So I hope with this you understood the basics of IAM groups as well.