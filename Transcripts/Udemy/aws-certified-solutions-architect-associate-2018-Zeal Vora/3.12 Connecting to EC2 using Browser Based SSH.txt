# 3.12 Connecting to EC2 using Browser Based SSH

Hi, everyone, and welcome back.Now, in the earlier video we were discussing about the connectivity options for servers and how wecan either make use of various tools to connect to the server like SSH client as well as the browserbased SSH method.Now in today's video we are going to connect to our first EC2 instance using the browser based SSH connection,which is the simplistic one.So let's go ahead and quickly explore that.So at this stage we have one EC2 instance that is running.Now if you are making use of tools like SSH client, you generally need to copy the IP address of theEC2 instance and then connect through SSH.However, for browser based SSH connection, you do not really need all of those.Now, again, one important part to note is that sometimes it can happen that the browser based SSHconnection is not working.In such cases you can use the traditional SSH client as a backup.Sometimes it can happen that a SSH client based approach is not working.In such cases you can use browser based SSH connection as a backup.So since there are two primary options to connect to the server, one can act as a backup.So with this being said, let's go ahead and explore the browser based connection.So you select the EC2 instance that is created.You can click on Connect once, then you have to click on Connect here.So it is establishing the connection.And now you see we are connected to our EC2 instance.So if you run a command like LS Slash for an example, it basically shows you what are the differentset of folders that are available in Linux and all.So you can install softwares.You will be able to do all of those things from the browser based SSH connections.So this is the high level overview of today's video.In the next video we will be exploring on how we can connect to the server using the SSH client basedapproach.So with this we'll conclude today's video.