# 5.7 Network Load Balancers

Hi everyone, and welcome back.In today's video at an overview, we are going to discuss about the network load balancers.Now one very important point that we have to keep a note of is that in organizations there can be differenttype of applications that would be running, and not all of the applications that organization usessupports the HTTP protocol.For example, you can have DNS servers, you can have SMTP servers, FTP servers and so on.So when we discuss about this, all of this uses different protocols.And they are not based out of HTTP protocol.So when we discuss about load balancers application load balancers which again are very popular.But the downside here is that they primarily work with HTTP protocol and Https protocol.So if you have any application that is created that uses this protocol, for example, if you have aweb server created using nginx, it works with http, https.So for that application load balancer is fine.But for applications or any other services that uses other protocols, the application load balancercannot be used.So in such case, if organization are using some applications that uses different set of protocols,then there is a requirement of an additional load balancer type that can support them.And this is important.So the main aim here is that the additional load balancer type should work with non HTTP based protocolsas well.And this is where the network load balancer comes into picture.Now at an overview the network load balancer operates at layer four which is the transport layer ofthe OSI model.So if you discuss about the transport layer of the OSI model you have the layer four.So this is 1234.And in the transport layer you have various protocols like TCP, UDP etc..So the network load balancer works over here.One of the great advantages here is that since it works at a transport layer, it can support variousdifferent type of protocols as well.Like HTTP definitely supports it, will support FTP, it will support SSH protocol, SMTP, etc. thatare in the layer above it.Now, one more big advantage of the network load balancer is that it can handle millions of requestsper second.And this is one of the great benefits here.So let's take an example through Wireshark.It will allow us to better understand things.So if we open this because here you see the protocol is HTTP.So when we open the HTTP over here your various things like the Get request.You have the user agent, you have the host and so on.So when we discuss about the application load balancer, since it supports the HTTP as well as Https,then it can work around with this.For example, you can say that if a request comes a HTTP request comes with a user agent is skull.Then in such case block that request all of these additional things at HTTP protocol level, you cando with the application load balancer.However, the network load balancer works at a transport layer.In transport layer you have a transmission control protocol.You also have UDP and others that are available.So it uses the details that are available as part of the TCP or UDP to go ahead and make the appropriaterouting decisions.For example, it makes use of a flow hash algorithm that uses the sequence numbers along with otherthings, for routing the traffic to the appropriate target.Anyways, we not discuss about all of those at this stage, but just remember that the application loadbalancer works at the HTTP layer.The network load balancer works at a transport layer and in transport layer.One of the protocols that are supported is TCP.Now one more thing that we have to keep a note is that network load balancer target group supports followingprotocols.So you have TCP, you have UDP, you have TLS and you also have TCP UDP.So this provides a great set of flexibility for organizations where they are running applications thatdo not use the HTTP protocol.Let's quickly have a demo on this.Now for today's demo I have a network load balancer that is already created.Now in the network load balancer, if you see in the listener I have a port of TCP 22.Now what exactly runs on 22?It is the ssh.What does SSH use?It uses the appropriate SSH protocol.So now what is happening as part of the load balancer is the listener is 22.And it is forwarding to a target group called ssh hyphen tg.And if I quickly open this target group in the target we have one EC2 instance called as website EC2that is running.So if I quickly show you I have a website hyphen EC2 that is running over here.And this network load balancer is associated with it.So now what we can do is we can SSH with the URL of the network load balancer.And that request will be forwarded to the EC2 instance through TCP.Let's also quickly look into it.So I'll copy the DNS name over here.And from my local terminal I'll quickly run the SSH command.I'll specify my private key and will connect using the EC2 user and will specify the IP address or DNSnot of the EC2 instance, but of the load balancer.So if I add the URL of the load balancer, let's see if this works.If I press enter, you see I'm connected to the EC2 instance.Now why it is working?Because SSH is not a HTTP protocol.It is a different protocol altogether.Why it is working because of the network load balancer.Now if you look into the SSH protocol, you will see that it works at the application layer.So in application layer there are multiple protocols.One of them is HTTP as we were discussing and HTTP and Https is the application load balancer is whatit supports.Primarily there are other protocols like SSH, DHCP, DNS, FTP, etc. so all of these protocols theapplication load balancer does not really support.So in such case the network load balancer proves to be very useful.Now network load balancer as we were discussing it works at a transport layer.So it supports various things like TCP, UDP etc. now ultimately HTTP.Also it makes use of TCP because you could see that although this is a HTTP packet, it uses the Transmissioncontrol protocol.And this is the reason why the network load balancer will also work.So if you have a HTTP based application, network load balancer will also work there.You can also make it work with HTTP protocol.It is just that the network load balancer will not be able to read all of these details.For example, it will not read and it will not allow you to create rules based on other headers likeuser agent, header, host header, etc..So if you want to create rules based on headers that are part of the HTTP protocol, you will have touse the application load balancer.So let me also quickly give you a demo on how it would work if you have a website also.So currently I have a website that is running as part of the EC2 console.So in the new tab, if I open the IP address of the website, you are able to see welcome to nginx.So now what you can do is I already have a target group.This is a demo video.I already have a target group called as TCP hyphen tg.This target group sends the request to the website hyphen EC2.All right.So within here the TCP protocol that I have added is 80.Now again note this is a demo video.I just wanted to show you the basic concept.All right.So now you have two listeners, one for the protocol of SSH listening on the port of 22.And second one is port 80.So if I copy the DNS of the load balancer and from a new tab, let's add the DNS of the load balancer.Let's also specify HTTP.And you see we are able to load the nginx page.Great.So that's the overview about network load balancers.Again sometimes in exams if you see a requirement where organization wants to handle millions of requestsper second, and what might be the probable load balancer that can suit the requirement, the answeris going to be the network load balancer.So that's an overview.And with this we'll conclude today's video.