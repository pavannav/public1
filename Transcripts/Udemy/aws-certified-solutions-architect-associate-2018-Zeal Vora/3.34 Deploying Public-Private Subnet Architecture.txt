# 3.34 Deploying Public-Private Subnet Architecture

Hi, everyone, and welcome back.Now in today's video, we'll be going ahead and will be configuring the public and the private subnetfor the VPC that we have created.Now, when we discuss about the public subnet configuration, the overall configuration is very simple.First one for the public subnet, there are certain criteria here.First one is that the Internet gateway must be attached to a VPC.This is simple to understand.Second is the route table has a route towards the Internet gateway.So in total, when we discuss about route table, there will be two entries.First one is for the local and second one for the Internet gateway.In fact, the route table that we have already created already has the specific entry.So the AC two instances that we have created, these AC two instances are already part of the publicsubnet.So there is no explicitly new configuration that we need to create per se as far as the public subnetis concerned.The second one is the private subnet.Now in the private subnet, in the route table, generally the route towards the Internet gateway shouldnot be present.So you see at this stage there is only one route that is added.And since in this route there is no Internet gateway that is associated, you can refer to this as aprivate subnet.So when we discuss about the steps to configure the public and the private subnet, so in total therewill be two route tables that you will be creating.First round table will have this entry, second route table will have this specific entry, and thenyou will be associating this route table with each subnet and then the entire configuration completes.So let's go ahead and try it out.Now, at this stage we have two AC, two instances that we had created.Now both of these AC two instances are currently part of the public subnet itself.So in order to quickly verify if things are working as expected, so currently we have this 0000 rulefrom all.So let's go ahead and quickly ping the instance to verify the connectivity.So from the terminal I'll do a ping on the first instance it is working well.Let's copy the IP of the second instance.Let's do a ping.And this time we are getting a request timed out.So I feel very fair.Let's quickly verify the security group.The security group does not really allow the ping traffic, so you can explicitly add it in the inboundrules.So let's add all the traffic just for simplicity.All right.Now, if we try to ping again, you see things are working.So at this stage we can clearly state that both the AC two instances are part of public subnet.So now what we'll be doing will be creating two route tables, first one for the public subnet, secondone for the private subnet.Once the route tables are created, then we'll be associating that route table with each of this subnetand then we'll verify the connectivity.So for a demo VPC, let's quickly go to the route tables will be creating to route table here.Let's call it as public route.Table in the VPC will be associating it with a demo VPC.Once this is done, you can go ahead and click on Create Route Table in a similar way.Let's also go ahead and create one more table.I'll call it as private route.Table will be associating it again with a demo VPC and we'll go ahead and create the round table.Perfect.So at this stage we have two round tables that we have created.If I'll just expand this, we have this public roundtable and we have the private roundtable now inthe public roundtable will be adding two specific rounds.The first root of local is already present by default.So this is something that we do not really have to explicitly add.We'll be adding one more route towards the destination of 0000, which basically means anything thatdoes not match this.And the target this time would be Internet gateway.And we'll add our GW over here.I'll save the change.Let's go back and the private roundtable.At this stage, since there is no Internet gateway associated, this is completely fine.So our two route tables are currently created.So now what we'll be doing will be associating this roundtable with each and individual subnet.Now, by default, there is one round table that is generally created.So this is the main round table over here.So if you see in the mean it is stating as yes.So this main route table has these two routes and this is already associated with both of these subnets.So if you look into the subnet association over here.You see in the subnets over here, you see both of these subnets are associated with the main routetable.So let's do one thing first.We'll open up our public route table in the subnet association here.I'll click on Edit Subnet Association and I'll add the Demo subnet zero one and I'll click on Save Association.All right, now the private route table.I'll click on Subnet Association, we'll click on Edit Subnet Association, we'll add it to demo subnetzero two.So Demo Subnet zero two will be a private subnet.Demo subnet zero one will be a public subnet.I'll click on Save.All right.So our roundtable is currently associated.So to verify if things are working, you can also go to subnet.You can open up the demo subnet zero one.Within here in the round table, you should see that there are two round entries since there is an entryof internet gateway.We can clearly identify that this is a public subnet.And the second subnet, which is the demo subnet zero two in the route table, you see there is no Internetgateway entry.So this is a private subnet.Now you can go ahead and also rename it accordingly in case if you'd like.Let me do that.I'll just call it as public subnet so that it's easier to identify.And the second.I call it as private subnet.We'll click on Save.Great.Now let's test the Internet connectivity for both the AC to instance.First will connect to the instance number one.Let's copy this.Let's do a ping.At this stage we are getting a reply back as expected, because this is in the public subnet.Now let's quickly connect to the instance in private subnet.I'll do a ping.And at this stage, you see we are not really getting a reply back.Now let's do one thing.Let's also go ahead and connect to both of these instances.The oral connection, for the instance, number one, is pretty straightforward.Since Internet connectivity is there, we can directly do an SSH.So from my claim, let's quickly use our key to connect to the instance.All right.And we are able to successfully connect to the instance now to connect to the private AC to instanceover here.Since there is no internet connectivity will not be able to perform the SSH here.So what you generally need to do first, you need to log into the AC two instance in public subnet.Copy your private key here and from the public AC to you perform a SSH to the AC two instance in theprivate subnet.So I had made use of this specific key to connect to the instance.So what I have to do, I'll have to copy this key to the public AC two instance.Now, in order to copy the key, what you can do, you can either do a cat on E two hyphen key, hyphennew Pim.So this is how the private key really looks like.Now, again, this is something that you should not share with anyone.This is like a password.Again, since this is a demo, we are using this so you will copy this.And in the instance here you can create a new file colors ec2 hyphen key dot Pam for an example, youpaste the key here and you save the change.This is one way.Now, if you are using windows and you are not using mobile system, the cat command would not be there.For example, in the Windows terminal.If you do a cat, you see there is no such command in Mac and Linux.This is pretty straightforward since CAT is present.So in such case what you can do, you can open up your key file in a notepad or in whatever editor thatyou have.You can copy this entire key.And from the terminal, if I just open up a text file, you can paste the content.So this is how you can transfer the public key easily.All right.I will not save this speech because we have already created this easy to key dot pen.Now, the second important command is you have to change the permission to 400 to AC to key dot Pamand then you can go ahead and perform the search.A search this time will not be on the public IP of the private AC too, but it would be based on theprivate IP address.I like the private IP along with SSH.And along with that you will have to specify the key.And now you see you are connected to the instance in the private subnet and from the instance in theprivate subnet.If I do a ping on google.com, you see it is not really working at all because of no internet connectivity.Great.So I hope with this you understood the basics of public and private subnet, and we also looked intohow we can connect to the instance in private subnet in case if there is a requirement.So with this will conclude today's video.