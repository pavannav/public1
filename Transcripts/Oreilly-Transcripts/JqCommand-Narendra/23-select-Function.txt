[00:00:00] Friends. Here we are talking about select a function. See this select function is used to select elements based on given test condition. And here test condition can be formed using comparison operators, logical operators or with a hash function or with a contains function or with any other. But make sure that

[00:00:21] the result would be always either true or false from your test condition. Let me explain this on command-line. See suppose we have a file called demo dot json. So here in this object, let's say I want to validate whether question key is there or not.

[00:00:40] So if you remember there is a function called has using that we can validate whether butler key is present or not in your object, see the result you're getting true true means that key is present in your object instead of this version. Let me take small V version that is not there. That's we are getting false.

[00:01:00] Not only that, let me take the value of this key first. And in this value, I want to validate, let's say 2000 tool string is present or not. So for that you have a function called contains but

[00:01:20] this output you are getting using this filter. Then this output I want to apply as an input to a function called contains, then use pipeline but inside contains you already your required search string. Let's say 2012, they are getting output as true. That means that 2012 is there

[00:01:43] in this output. That is nothing but this one that's fine. Now let me take this one but string is not there. So if you observe in these two cases or here you're getting output as either true or false means this type of

[00:02:01] conditions you can take in your select function fine. Now let me simply apply dot filter on your demo dot JSON so that you are getting your output. Anyway, first of all, this is an object. So I want to

[00:02:20] display this object, I want to get this object from your demo dot JSON. If that object consists of version key, then in that case, you will take select function. See first let me write select function, then you will understand select function. But select function needs one test condition.

[00:02:41] The test condition you have to write inside this parenthesis. So now my test condition is from my demo dot JSON object. If version key is there, then only I want to display the output. Now see the result question key you're getting output.

[00:03:00] So now this select the function, selecting the element that is object. In this case from this demo dot JSON. If it has this version, suppose I'm taking instead of version, let me take some other key small V version. Now see the result you're not getting because

[00:03:22] this version key is not present in this. That's why selectf function is not selecting the data from this demo. Be clear, not here, this is an object. So an object directly we can apply to function in this way.

[00:03:40] Suppose if you have an R let's say I have an array called in this sample dot JSON. See here we have an add of course, that consists of some items and each item is an object. Now, my requirement is I need to apply my select function

[00:04:01] on this array nothing but on these items. Because in array, we have items in these items. And I need to get such a way that I mean, I need to get the items such a way that which are having, let's say label as dwarfs, then how we can apply,

[00:04:20] select a function on that because we need to select, which are having labeled as jaws means you need to apply, select a function on this. See how I'm going to apply JQ. See if it is an object directly, we can apply selective function but it is an array first get all items from your array. So for that we have iterator filter or array filter.

[00:04:42] Now you are not getting an error, you are getting simply items each item is an object. Now, on this output, you can apply our select function pipeline select. Now let me write a condition, test the condition. So see here the key value there is nothing but label, key value.

[00:05:02] If it is equals to let's set the S. So DevOps is a string. That's why we are writing inside double quotations see the result. So from all these items, whichever are having label as the wars, those only we are getting. Now be clear here, I have written dot label because

[00:05:22] dot label is the key label value I want, that's why I'm writing here dot label dot label, nothing but field filter instead of the verbs, suppose if I take aws sorry cloud because one more label is cloud, you're going to get these too.

[00:05:40] No. See here, I'm writing one more condition. Let's say ID means ID value. Suppose if ID equals exactly three, then only I want to get the items from my input. Anyway, with ID three, we have only one item that's we're getting.

[00:06:02] Now I want to get ID three and also ID equals to let's say four. Now you are getting those too. So this is logical operator are and this is comparison. These two are comparison. So comparison operator. So we can club with logical operators.

[00:06:23] And here instead of applying select the function on the output of iterator filter for your JSON array, we can also apply, select a function with the combination of map function. Let me show you that. So simply JQ map function our

[00:06:41] JSON file which is having array. Then inside parenthesis of map function, just write your select function. And this select function needs to test the condition. As of now, we are selecting the items from this JSON array which are having label as cloud. Let me write that condition.

[00:07:03] Now see there is a yes, we are getting the items which are having labeled as cloud fine. But here there is a small difference between these two syntaxes. And we already know one difference. That is whenever if you go with iterator filter, you're going to get

[00:07:20] output only items not as an array. But if you go with map function, you will get output as an array. That's fine. Second one is see you are applying select function on the final output of iterator because you're using pipeline. That means on the entire output on all items, only one time,

[00:07:42] single time you're applying select function and you're selecting your items. But map function is going to take first value from this sample dot JSON array. And on that first value, first, it is going to apply select function. And in case if this condition is matched, then it is going to give that item here.

[00:08:02] Then again, map is going to take second value. And on that second value it will apply again this select function. Suppose here in this JSR, if you have, let's say five values, then all five values individually, the select function will be applied. But here on all five values at a time,

[00:08:20] we are applying select function that is the main difference. And anyway, from this output also, we can get output in this way just because this output is an array then apply a filter on that final output simply pipeline then array filter. That's it. In case if you want, even from map function,

[00:08:40] if you want your output like your iterator filter and the final output of your map function apply once again this pipeline then dot iterator filter. That's it. Now finally, let me give the syntax for our select function. Be clear. If your file is having simply a JSON object,

[00:09:00] then directly you can apply select function in this way. Suppose if you have JSON array, then you have to go with either iterator filter or map function to apply your select function on your array. And here test conditions, we can form using comparison operators, logical operators with hash function or with contains functions.

[00:09:22] And in case if there are any, yes, we can use only condition is whatever the condition. Test the condition. Here we are writing that result would be always either true or false. That's it. Thank you.