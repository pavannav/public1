[00:00:00] Friends here, we will be talking about field filter and this field filter is useful to access key value or property value. See here before working with the field filter, we must know what is a key or what is a property and what is the value for it? See suppose if this is JSON data or

[00:00:20] JSON object. So here version is called key or property and this is the value for it. Same way statement is a key or property and for that key or property, this entire thing is a value. And if you observe somewhere here we have action, this is also a key and for that this is the value.

[00:00:41] Now we are ready to apply field filter. Now that field filter syntax is simply JQT key then file name of course, optionally, we can also provide our JSON stream or rest a response. Then this key we can also mention inside single or double codes.

[00:01:01] That means that field filter we can also apply inside single or double codes. Now let me go with practice so that we can easily understand. Now from this demo dot Json, I want to get this value. So this value is mapped with this key property. Now on this key property,

[00:01:20] if you apply field filter, then you will get this value. Now see that JQT version then demo dot JSON. Yes, we are getting it same way. Suppose if I want to get this entire value, then for this entire value, this is the key, just apply it and see the result.

[00:01:46] Yes, we are getting it. And here for this field filter, we can apply single or double quotes. And best practice is using single codes instead of double codes just in this way.

[00:02:01] That's fine. See suppose I have one more JSON data in this way. And here if you observe your key or property consists of some space, then in that case, how you can access the value? Hello, first, let me try with our existing syntax of JQ

[00:02:20] with filter simply dot With space. No luck. Let me try with single quotes or double coats. No luck. Then how we can access it very simple. See whenever your key consists of space or any special character.

[00:02:44] In that case, simply use single quotes for your field filter along with that just to provide double quotation for your key or property, then you will get it. That's it.

[00:03:01] See on our demo dot JSON file, simply we applied our field filter in this way, but we can also apply with the pipeline JQT version. Yes, it also works. Or if you remember we are also having one

[00:03:20] more syntax for our JQ command to provide input. Yes, it also works. Yeah, simply we operate dot But we need field filter. So simply key value. Yes, it's working, that's fine. And we can also chain these keys or properties together to get nested objects.

[00:03:45] So for that syntax is simply JQT key, one key, two key three and so on. In case if you have any number of keys recursively in your JSON data and then finally file name, let me go with practice see supples.

[00:04:03] Let me take one rusty api. Now from this response, suppose I want to get only this value. So this value is mapped with bio. Then apply your field filter with JQ dot BO and see the result.

[00:04:22] Yes, we are getting. But here I want to get this value only this value that is nothing but the value for your key called. But now initially if you observe your initial response, so this is the value that is mapped with but ray but this entire

[00:04:42] value is mapped with Bio. Now to access this value, you have to apply ricasoli freely filter that is like first you have to apply bio, then immediately birthday so that you will get this value. Let me apply that and see the result Bio body.

[00:05:01] Yes, we are getting it. So likewise, in case if you have recursively uh some keys and if you want to get somewhere your value, then in this way you can change your keys or properties. That's it. Ok, thank you.