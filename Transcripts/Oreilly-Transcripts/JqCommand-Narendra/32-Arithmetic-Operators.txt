[00:00:00] Friends. Here, we are talking about arithmetic operators using J two command. Let me go with command-line. So here simply without any input directly, I am providing two numbers and I'm doing the addition for them. Yes, you're getting output as mine. Now instead of both as integers, let me take one as float point. Yes, it's working.

[00:00:20] And here instead of directly providing your numbers, let me take them as variables. Let's say export X equals to four and export Y equals to sum two or any number. Now let me use those variables in our JQ expression so we can access them using ENV dot X plus

[00:00:41] ENV Y. Now see the result, you're not getting output as 9.2 instead of that, you're getting 4.2 because we know that en NVT X and ENV Y, I mean the variables which are from ENV always are strings. That's why simply

[00:01:00] these two are joining these two strings are concatenating. But we know that our inputs are numbers, then we need to add them. So for that, you have to convert your data into number using to number operation, but just observe the output.

[00:01:21] So you are getting an error like number four and string five or two cannot be added. So actually, you're applying two number function operation on both the variables. But here number four, we're getting, but here we are not getting a number, actually we are getting but because of operator precedence,

[00:01:41] so compared to pipeline plus has highest operator presidents property, that's why after finding this immediately, your plus is trying to add these two. But anyway, one is number one is string. That's why it is not happening. Now, compare to all operators parenthesis has highest priority. That's why

[00:02:02] see what I'm doing. I'm applying, let's say for this parenthesis, then this operation will be done first. Same way here, I'm applying parenthesis. No, your JQ command will execute this and this before this plus symbol.

[00:02:21] That's why first we are converting number here and here then those numbers we are supplying to place. Now see the result you're getting. Now you can also add now some string into this. Let's say I'm doing it this way. Sum equals to. Then we know that now this is a number, be clear

[00:02:42] and this is a string number and a string you can't add. Then if you want to add these two, then anyway, this string, we can't convert as a number. But this is a number. That's why we can convert them as a string by using two string function. But for that also see I'm using this parenthesis

[00:03:09] then on that, I'm applying two string. No, you applied this parenthesis for this operation. But after that, you are doing two string. But now again, compared to this, this place has highest priority to execute your operation.

[00:03:24] That's why for this entire operation, whatever you have that I'm writing inside one more parenthesis. Now see the is it, let's see now using this logic, just I implemented a simple shell just to find your AMA operations.

[00:03:40] So here I'm going to pass our data in this way, command-line arguments. Yes, you're getting suppose if you pass, let's say one number and one number as zero, then some subtraction, multiplication you're getting. But division is not possible. We know that some number by zero will get an Exception.

[00:04:01] That's what you're getting here. OK. Thank you.