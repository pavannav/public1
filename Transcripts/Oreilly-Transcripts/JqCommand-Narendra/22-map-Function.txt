[00:00:00] Friends. Here we are working with map function. See this map function is used to translate all items in an array to new array items. So here while translating your input array to output array that output may be same as input or it may be different. And that is based on the operation which we are doing on our

[00:00:22] input array using map function. Let me explain this on command-line. See suppose we have an array file array two dot JSON file. So in this we have an array. So this is an array. That's why on this we can apply mapfunction. Now let me apply map function.

[00:00:41] So like any other function, this is the basic syntax and to apply map function, we need at least any one operation like filter or function. Now we know that if we apply dot filter, there is no change in the input and output. Now I am playing God filter and see the reason.

[00:01:00] Yes, you are getting input and output both are same because you are getting an array, an array consists of your input values or items or elements. But how this map function is going to work very simple, this map function will take one by one item from this array. And on that one by one item,

[00:01:20] it will apply this filter or whatever the operation you are mentioning here. Then that result will be given here. So first you will take first item from this array to apply dot filter. Because of that there is no change in that item that is given here. Then it will take second then applying dot Then there is the second one.

[00:01:42] Then you'll take third value from this, then apply dot filter on that third value. But finally, that result will be given as an hurray. If you remember there is one filter or iterator filter on your arrays. This one here also same kind of operation.

[00:02:05] So this iterator will take one by one value from here and it is displayed one by one value from here, right till this play. But the result is not an array simply it is giving all values but map function is going to give an array. Now in

[00:02:20] this array assume that be clear in this array, each array is each value is an object. Now on that each object I want to apply the name filter because I want to get Apple banana and Kiwi as an. Then see if you apply

[00:02:40] name. What is the result? That's it. You are going to get an array and that array consists of from each item. The value of name that is nothing but Apple banana and Kiwi, not only name you can apply simply price

[00:03:00] fine. Now this is one function and inside that you operate a filter, not this result, you can also apply to a filter out to a function. Suppose this final result is an array right? In this array, I want to find the maximum value. See because of this operation only you're getting this array.

[00:03:22] Now on that result, I'm applying pipeline. Then max you know if you want to provide one function output to another function output, you can use pipeline symbol. See the result. Suppose here we are getting an array with these different strings, right?

[00:03:41] Different values. No, I want to join this array items with colon. Yes, see that pipeline join. Sorry, see the reserve.

[00:04:01] What happened? Yeah, we missed single quotation at the end. That's it. So this is the usage of map function. So finally you have to remember that input and outputs both are always array in your map function but that output you can use it to another function or another filter.

[00:04:22] So this is the syntax or if you want to club with the filters or functions, these are the syntaxes. That's it. Thank you.