[00:00:00] Friends. We know that from our previous videos that is we need JSON data to practice with JQ command. And that JSON data, we can create ourselves just by opening text data and copying pasting JSON data. Or we can use any commands where it provides output as JSON data like AWC commands

[00:00:21] or Q and these commands. Or we can also create some custom rest API so that the rest API response we can use to practice with JQ command or we can also use publicly available rest API to practice with JQ command. See, there are some public

[00:00:40] A pas let me open my browser and simply search for public trust A pas for testing Json and open any one of the link. So I'm opening the first link and here just click on the first API. See already we are getting output as some JSON data. Then simply copy this A P

[00:01:00] and go to your command-line, then run with cURL and rest API. See that you're getting some JSON data now to format it. There is a filter called JQT. Don't worry about that. We'll discuss it. See that it is formatting your JSON data,

[00:01:21] nothing