[00:00:00] You, Let's get into the lab for CLI based workflow. Now, we've already configured some of our setup. We did our organization and we did our project. But just to review, you need to make sure that you have a terraform

[00:00:24] cloud accounts and that you have created a new organization name as well as a project and create a workspace. And for that workspace you wanted to select CLI Driven workflow.

[00:00:40] So we already did that. So we're going to get into part one, but I'll just review a little bit in the browser. If you look at projects, you'll see any projects that you started. The one I'm working in is default project and you can see here that we have one workspace already set up. If we click on workspaces, you'll see that workspace. For me, it's called

[00:01:03] Workspace one and it's part of the default project. And I set this up as CLI driven. So that should be set up. If you don't have that already. We did some of this in the previous lesson. If you don't have that already, make sure you have all that up and running.

[00:01:22] And again, this is all part of lab 16 and I'm looking at the lab 16 document here. So let's get into part one. I've divided this lab into two sections, so two sub lessons. In this sub lesson we'll do part one and we'll start with

[00:01:42] configuring the main tf file. So let's take a look at our main tf and you'll see here that we have a terraform block and it's telling us to put the terraform cloud block here. We have to grab that terraform cloud block from the terraform cloud.

[00:02:01] Now, you could type it out yourself if you wanted to. It's pretty straightforward. If we go to our workspace and click on it, we'll see that it gets an ID number and we'll see that it's waiting for configuration. We haven't done anything with it yet. We haven't done any terraform

[00:02:20] plans or terraform applies. In order to do that, we need to show in the code our terraform cloud block. And so they'll give you that code here. Make this slightly bigger and you can see it's the terraform block with the cloud sub block.

[00:02:43] And you need to have your organization name. For me it's Prowse Underscore Tech and your workspace that you want to work in. For me it's Workspace Dash one. So I'm going to copy that over and bring that over to my main tf.

[00:03:02] There we go. Now, we could also just take the cloud block portion and paste that within our Terraform block. You may or may not need the AWS required provider block here. It may automatically realize that we're dealing with AWS code, but we'll leave

[00:03:23] that in. So for this lab, what I'll do is I'll take that cloud block and copy it directly within the terraform block and then get rid of that terraform block that I copied over from the cloud so that it will

[00:03:41] look like this here is the entire terraform block. And so inside that we have the cloud subblock with our organization proustech and our workspace, Workspace one, and then a separate sub block required providers and AWS as we've used before.

[00:04:05] Just make sure that all your delimiters are correct. This, the terraform block for me is using yellow curly braces. Those are the delimiters here and here. My cloud block is using what looks like sort of magenta curly braces here and here.

[00:04:24] And the required providers is using magenta curly braces here and here. So make sure that those are all correct and we'll go ahead and save the file. And now we'll bring up the terminal, make sure that you're working in lab 16. That's going to be our working directory.

[00:04:43] And we'll go ahead and run a terraform init to see what happens. Press Enter and look what it says. It's trying to initialize HCP Terraform because it sees our cloud block here. But we're getting an error. Required token could not be found.

[00:05:02] To do so, we have to type the Terraform login command. We have to actually log in to the Terraform cloud. So let's do that. Let's run Terraform login. Press Enter and it says Terraform is going to request an API token for app Terraform IO using your browser, whatever your default browser is.

[00:05:26] If this is successful, you'll get your credentials here. It'll store that token there in Credentials TFRC JSON within your Terraform D directory, which, by the way, was created for us automatically when we installed Terraform. I just haven't shown it yet. But we'll

[00:05:43] be dealing with that a little bit more later in the course. So we need to do this. We need to get this API token. You want to make sure that you are logged in to your Terraform cloud account, obviously in the browser first before you can do this.

[00:06:01] So we're going to say yes for this. And it's going to tell you, okay, there is a token in the browser now. So I'll switch back to my browser. And it popped up automatically, creating a user token. And you can call this whatever you want. I'm just going to leave it as Terraform login. And then we have an expiration by default, it's 30 days.

[00:06:22] You may want to make it less. Like an maybe one day. You might want more if you're going to do more labs with this login over time. So I'll leave it as 30 days for now, but for more security, you could always make it less. And then we'll click on generate token.

[00:06:41] All right, make that slightly smaller so we generate the token. And you can see this is what I just generated. Terraform login expires in a month. And this is the actual token. So I have to copy and paste that in. So we'll press the the copy button, go back to our terminal

[00:07:00] in VS code and paste that in with the Control Shift V and press Enter and there we go. We are in. All right. So just to review, I ran the Terraform login command you needed to grab an API token from the browser. We say yes to that and then we pasted it in and it stores

[00:07:23] that in your home directory in a hidden directory called Terraform D in a file credentials TFRC JSON. All right, and we see the Terraform logo and it says welcome to HCP Terraform. That's how you know that you've actually got in.

[00:07:42] And then you can follow some steps for an example configuration if you wanted to. But we're not going to do this. We're going to continue with our lab here. All right, very good. So the connection is valid. We could also take a look at our Terraform D directory right now,

[00:08:00] at that file. If you want, let's open a second terminal. And I just did that with a Control Shift backtick to get that new terminal and go to my home directory and take a look in here with an LS A. You can see the Terraform D directory right here. That's a hidden

[00:08:20] directory. That's why I used a dash A to view that. But we can change over to that and then look inside and you will see the credentials TFRC JSON. So that's where we have our actual credentials for our Terraform login. And if we take a look at that file, you'll see the actual token.

[00:08:43] So this is the kind of thing you don't want to show to anyone. I don't mind, because this will go away by the time you actually see this video. But this is the kind of thing you want to definitely keep secure on your system. All right, back to the other terminal.

[00:09:00] Good. Now let's go ahead and examine the main TF file a little bit further. So we have our terraform block. That's all good. And if we look further down, we'll see that we have a provider block,

[00:09:20] and that provider block is using the US East2 region. And it's calling on two variables. We've got var awsaccesskey and var awssecretkey.

[00:09:40] Right. So calling on two variables for the keys to allow us access to AWS via Terraform, then instead of using a separate variables tf file, I simply put the variables blocks right here as simple blocks of information with the names AWS access key, AWS secret key, and I'm putting nothing in the curly braces.

[00:10:06] These don't have a description, they don't have a type. They're not necessary for this lab. Ultimately, those variables values are defined in this project in the Terraform TFVARS file, which I've just created.

[00:10:23] And so you should see that in your lab. And so if we click on Terraform tfvars, you'll see that we have the two variable names from the variable blocks. And then it just says, enter your access key and enter your secret key here.

[00:10:40] So we're going to locate our AWS credentials and add those in here. All right, so we'll bring up the terminal again, and let's go back to that second terminal where we were working. I'm going to go back to my home directory and change over to Dot aws and from there we'll open up the credentials file.

[00:11:05] There we go. And we're going to copy over our access key and our secret access key into Terraform TFVARs. So we'll start with the access key and copy that in here. That's got to

[00:11:20] be within the double quotes because it is a string of information. And then the secret access key again. Remember, never show this to anyone like I'm doing right now. And there we go. We've got both. And we'll save the file.

[00:11:40] Now you may say, well, what do I do about these credentials? I don't want anybody to see them. What if I have to upload this entire lab or this entire project to my repository? Well, in that case, you would want to hide the Terraform TFVARS file, and

[00:12:02] you would do that with a gitignore file like this, you'd have a gitignore and then you'd add Terraform TFVARs to that and that would enable that to be hidden and it wouldn't actually get pushed out to your repository.

[00:12:21] All right, so back to the Terraform tfrs. That looks good. Going to save that, make sure it's good. And we'll go back to our other terminal here and everything looks good so far. So again, just to review, if we go back to the main tf, we've got our

[00:12:41] provider aws and you could actually put in your access key and your secret key here. That would be known as hard coding your credentials. And that's not a good idea because the main TF will most definitely be pushed out to a repository at some point if you're working with a team especially.

[00:13:05] So instead we're calling on variables and then for the variable blocks we're naming them AWS access key and AWS secret key, but not giving them a value here. Instead we're using Terraform TFVARs and we're hiding that with our git ignore.

[00:13:24] There's a couple different ways that you can go about this. With credentials, you definitely don't want to hard code the credentials right into your main tf. And there's a little sidebar here that goes over the different ways of passing credentials that hard coding not recommended whatsoever.

[00:13:44] Terraform TFVARs can be good. That's our option for this lab. But you have to make sure that if you're going to be pushing that code to a repository that's git based, you want to make sure you add it to the gitignore file.

[00:14:00] The next option is environment variables, but you want to be careful with that. Make sure that those are removed. If they're not necessary, keep track of those. And those are local environment variables that we store on our system and we've shown some of those already. Then you have Terraform cloud environment variables, which is recommended,

[00:14:21] and we'll show that in an upcoming sub lesson. And then finally you could use HashiCorp Vault, which is completely separate. That's highly recommended. And Vault is extremely secure for keeping any information, including things like credentials. So I chose terraform TFVARs so that we could run everything locally

[00:14:42] without having to incorporate any types of new technologies. And actually Terraform tfvars is meant for this purpose as long as you use it with a git ignore file properly. All right, so that is that. And just verify that everything is set up properly. Make sure

[00:15:01] that you have your terraform TF VARS file. You'll note that that also has the terraform logo next to it. And save it. And make sure your main TF has the cloud block for your organization and your workspaces. And in part two, we'll go through the run for our terraform code.