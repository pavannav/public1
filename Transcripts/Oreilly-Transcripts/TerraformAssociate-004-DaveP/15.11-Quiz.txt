[00:00:06] Time for the quiz. Here's question one. True or false. You need access to a particular terraform state for the organization example and the workspace prod.

[00:00:20] To accomplish this, you need to configure a data source block. Will the following code block work? True or false. And here is the code block. I'll give you a couple moments to review this. Feel free to pause the video and I'll be back in a couple moments with the correct answer.

[00:00:44] And the correct answer is true. This should work in this scenario to properly connect to the remote state. And by the way, that's what this is here. This is really referring to remote state, possibly on the cloud

[00:01:00] at say the terraform cloud or hcp Terraform or AWS or others. And we know because it says backend remote, right? In order for this to properly connect to the remote state using a data source block, you need to have the following arguments. You need that back end.

[00:01:23] That's the first thing. And we have that backend equals remote and that's a string in double quotes. You also need config and inside of config within these curly braces. We need the organization, which we do. We're just calling it example. But we've done this on the terraform cloud where I've had Krausetech

[00:01:42] and you might have used whatever name for your organization. You'll also need the workspace and the name of the workspace. And I'm just using prod here. So you would need all that information in order to get access to the terraform state, that terraform remote state.

[00:02:03] So this code block should work and so the answer is true. And question two. Which of the following parameters does the terraform import command require? Select the two best answers. A path B Resource address C Resource ID D Provider.

[00:02:26] Take a moment to pause the video and think about the answer. I'll be back in a couple with the correct answers. And the correct answers are B and C Resource address and Resource id.

[00:02:42] To import infrastructure, you would need to specify the resource address and the resource id, just like we did in the lab. Those are the required elements. So the command structure would look like something that we did before. And we'll just jump over here to our VS code

[00:03:02] and open up a terminal. That would be terraform import and then the address and the ID. In the case of our main TF within our Importing Infrastructure Lab here

[00:03:22] it would be AWS_instance.instance1, right? That would be the address portion. So AWS_instance.instance 1. That's the entire name. That includes the block label for AWS naming

[00:03:40] convention and our Terraform id. And then we would need that actual id. We don't know what that is. So you want to make sure that you know how to do that by importing with the Terraform import command. Or you could do that with an import block as well.

[00:04:02] And so resource address and resource id, those are going to be the correct answers for working with the Terraform import command. There you go. That's the quiz. And that wraps up the lesson.