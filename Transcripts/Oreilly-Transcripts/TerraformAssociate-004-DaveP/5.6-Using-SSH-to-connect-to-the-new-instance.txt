[00:00:06] Now it's time to SSH into our new virtual machine, our instance out on aws. I'm going to go ahead and make the terminal bigger. Now. When I do so, if I scroll back, I can still see the outputs,

[00:00:24] but you never know, maybe those outputs would disappear, maybe you'll lose those outputs. And if that happens, you can always get them back. You can view them with the Terraform output command.

[00:00:43] The Terraform output command displays the public DNS and the public IP and their values. So if I forget or I lose the information on my screen, I can always get those back real quick. And it's a lot easier than locating that info in the state file, although that's pretty easy as well.

[00:01:03] But I like to stay in the terminal when I'm working. So let's go ahead and SSH into that instance. Now we'll use the SSH command and I have to specify where the key is. The key is inside of keys and it's awskey to do that. I'll use the dash

[00:01:21] I parameter and give the path and the name of the key in double quotes. That's going to be going up one layer and then down to the keys directory slash and the name of the key. AWS underscore key, close the double quotes and then we'll connect. It's going to be Ubuntu because that's the default

[00:01:42] admin name that's given to Ubuntu instances on AWS at and you could connect either through DNS or through ip. I'm just going to connect via ip. I'll just copy and paste that because I'm very lazy and press Enter.

[00:02:04] Once again we get the authenticity of the host. We're going to say yes to this, but this time it connects. We are in and we can see that we're connected to Ubuntu and lots of information there at the bottom of the screen we see we're connected as

[00:02:24] Ubuntu and the host name, which is ip, and it gives the private IP name. There we go. We're into the Ubuntu system. Excellent. So if you were to look at the AWS console, you would see within SSH

[00:02:42] that there's an AWSKEY pem. But that extension will not work here because we built the key pair within open ssh. So keep that in mind. Let's take a look at the system a little bit. We'll do a CAT etc.

[00:03:03] OS release and we can see indeed this is Ubuntu 24.3. LTS very good. Now let's do a systemctl command and check if we have a web server

[00:03:20] running. Let's do Apache 2 and it says unit Apache 2 could not be found. So we're going to get an error because that's not installed on here. We will install this in an upcoming lab and we'll actually work with a web server that gets built automatically within the code.

[00:03:42] Excellent. So we can SSH in. Very good stuff. And that leads us to an exam alert. Know how to show individual outputs as well. So we ran Terraform output and that showed all the outputs.

[00:04:01] But you can show individual outputs by doing a Terraform output and the name of the output, like terraform output publicip. You can also connect via SSH using the output. And we could do something like this. SSH I path to the key username at and use the dollar sign for a variable

[00:04:24] and in parentheses we'd run the command terraform output and publicip and that'll actually insert the IP address in there. Let's show exactly that on our system. Now I'm going to disconnect.

[00:04:42] I'll do a control D to log out. And now I'm back in my local system within the instances directory. And I'll run the same command basically. But instead of connecting via ip, let's say I forgot the IP address. I could do an Ubuntu at dollar sign and then parentheses and run the

[00:05:04] command terraform output dash raw space publicip and close the parentheses. Let's check that SSH I the path to the key ubuntu and in parentheses Terraform output RAW public ip. Let's press enter

[00:05:21] and see what happens. And indeed it works. And we get placed directly in the Ubuntu server. So remember that Exam alert. Know how to show outputs with Terraform output, individual outputs with

[00:05:42] the command and the output you want. And also how to connect with a variable using this exporting that information in this fashion using the terraform output commands. Excellent stuff. That's it for this sub lesson.