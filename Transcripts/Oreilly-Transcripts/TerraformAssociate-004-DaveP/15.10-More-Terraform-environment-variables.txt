[00:00:06] Let's talk about some more terraform environments variables. Previously we showed the TFLOG and tflogpath environment variables when we were doing logging and troubleshooting. But there are more.

[00:00:21] Here are a few examples that you might run into. First, we have TFDatadir that can change the working terraform directory. So if you're in a subdirectory or a lateral directory and the code is somewhere else, you can use TF data dir to specify what that directory is.

[00:00:45] And then you could use that with the export command to have it work within the entire session and impossibly move that entire export TF data directory command to your bash RC to make it run all the time.

[00:01:01] All right? The variable would have to be followed by an equal sign and then whatever the directory is that you want to work with, and generally that will be a relative directory compared to where you are at currently. Then we have tf_plugincach directory that can change where the provider

[00:01:23] plugins are downloaded to and so you can specify the cache directory. We haven't shown caching yet, but we will show that in another lab. And so caching works very well. So you can just have your provider plugins in one place.

[00:01:41] This variable changes where they will be downloaded to instead of the default location. We also have TF in automation. This is designed for continuous delivery and pipelines, and setting it to say one if we did TF in automation equals one that would result

[00:02:03] in cleaner output without interactive suggestions. Right? So you might export this as well and then do a terraform apply and you'll just get much cleaner output. And this especially is good if you're working with scripts or you just don't want to have all this

[00:02:23] clogged information and interactive suggestions. It's similar to TF input. Why is this important? It prevents interactive prompts during automation. So let's say you're working in some type of workflow

[00:02:42] or pipeline cicd, you're using Jenkins or something like that, and you want to prevent it from hanging. If your process, when you do a terraform apply, asks for prompts, then that's going to stop everything up. That's going to cause some real problems for you.

[00:03:00] So using TF in automation or TF inputs can prevent those interactive prompts. So that can be really critical in CICD pipelines. And that's a common gotcha when you're automating Terraform. If you're doing it within a bash script or if you're using Jenkins or some automation tool.

[00:03:21] This is the equivalent to always using the dash input equals false flag when you do a terraform apply. That's another option that you can use with Terraform apply. Dash input equals false. Okay, so setting it to 1 would be cleaner.

[00:03:42] Setting it to 0 would have additional interactive suggestions as normal. And then we have tfvar and this could be used with just about anything, including secrets. And we did show that in the past with our AWS

[00:04:00] credentials. But you could incorporate anything onto this. You could say tfvar X whatever it might be equals this. And so you could create your own secret variables based on tfvar. All right, good. Some honorable mentions. There's also tfworkspace, which supports multi

[00:04:24] environment workflows, also TFCLI args, which can inject default arguments or flags globally. Or you could do that as a file with tfcli config file.

[00:04:43] And you can find all of the environment variables@development.hashicorp.com in the CLI section and just look for environment variables. You'll see the entire list of those, and that is the link for that, so feel free to check that out.

[00:05:00] But these are some of the more common ones that you'll work with. TF data dir, TF plugin, cache directory, TF in automation and TF input, and just TFVAR for just about anything that you want to do beyond that.