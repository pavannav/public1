[00:00:06] Let's run through a brief lab about Docker with Terraform. Now, first of all, I'm running my Debian 13 virtual machine, and I have Terraform installed. But we'll need to install Docker, and I don't have that yet.

[00:00:21] So I'll show how to do this for Debian, and we'll click the link here and you'll see that there are install options for Docker for all different versions of Linux. And then you'll have binaries as well, where you can grab the binaries for Windows and macOS.

[00:00:41] But I'm doing this on Debian Linux. So I'm going to close some of this and scroll down here and go down to our installation. First thing we need to do is set up Docker's APT repository. So I'll copy that and go to my terminal and paste

[00:01:03] that in, put in my sudo password, And we'll grab that repository, move on to the next step, and we'll install those Docker packages.

[00:01:24] And we'll say, yes, Good. And then we want to check it and make sure that it indeed is running. So we'll run a sudo systemctl status on Docker. I'll just paste that command in and it is active and running

[00:01:43] and enabled. If not, you need to start the service. And we also need to add our user account to the Docker group. So we'll do sudo usermod a capital G, and the group Docker, which was created automatically during the installation.

[00:02:02] And our user will just use the variable in Linux here. $user press enter. No news is good news. So that should work. Now, in many cases, you may want to reboot your system, and so I recommend doing that. So I'm going to do a sudo reboot right from the terminal here.

[00:02:28] And reboot our virtual machine. Log back in as the Dave account. Very good.

[00:02:40] Open up VS code once again. And we're back in code with our Docker lab here. So you'll need to install this for your particular system. So feel free to pause the video to do that. But you will need that installation of Docker for this lab to work.

[00:03:03] Now, let's go ahead and take a look at the code that I've provided for you. And that's here as a file Docker tf All right, let's take a look and then we can run the code. So first we have a terraform block, and in the terraform block we have the Docker provider.

[00:03:23] So Docker is used to run containers, and you can run little applications in those containers. In this particular lab, all we're going to do is run the NGINX web server as a container within Docker, and we're going to have Terraform actually do all that for us.

[00:03:42] But we need that Docker provider, and that comes from Cruise worker, slash Docker. We're using version 3.6.2, and if we scroll down here, we see the provider docker. We don't have any information in there. If you're on Windows, you may need to add what's in this comment, but we're working in Linux here, so we can just leave that blank.

[00:04:03] And then we have two resources. We have the resource Docker image, and that's the name for a standard image. That's the naming convention. It's Dockerimage, and I'm calling it nginx. And I want to grab the latest NGINX web server, and that's going to

[00:04:21] be placed in a Docker container. So you have your Docker images, then you have your Docker container that'll run the actual software for that. It's called DockerContainer. And again, I'm calling that NGINX and I need the full path for that, which is going to be Dockerimage NGINX image. So this container has

[00:04:43] to point to the image that we're going to download. I give that a name and then I give it a port that it will work on. So we're going to use our browser to connect to this Docker container on port 8000. And that's it. That's the basic Docker code. Pretty straightforward.

[00:05:02] We'll go to our terminal and you want to make sure you're working in the Docker directory. So I'll open that in an integrated terminal. Now we're in Docker. Very good. Let's do a terraform init that will download the provider plugin for Docker. Very good.

[00:05:21] And let's run a terraform fmt. See if I formatted everything right. Well, no, it did a couple changes here. And that's okay. We want that. Always want it to be formatted. Nice. So we'll save that and then we'll do a terraform validate. Go maximize on that success. Configuration is valid. And let's do a terraform apply.

[00:05:47] All right, very good. Let's take a look. Here's the command I issued. It's going to create the Docker container, and it's going to do that from the Docker image.

[00:06:01] So two to create, we'll say yes, and we'll let that create. And that was actually really quick for me. Just about three seconds and two have been added. Now those Docker images are so small and compressed

[00:06:20] that it will download that basic NGINX web server extremely fast. So even faster than it was to initiate the Terraform directory. Very good. So let's test that NGINX container. Let's jump over to our web browser and we'll put in our local IP.

[00:06:42] We're going to do an HTTP://127.0.0.1 colon 8000. So we're using port 8000 on this particular container. You could use whatever port you want in the code. I'm just selecting 8000, press enter and it says welcome to nginx.

[00:07:02] That is the web server container that's running within Docker right now. That's it. That's a little bit about using Docker. And so the code again is pretty straightforward. You've got your Terraform block, your provider block, and your resources for your Docker image and your Docker container.

[00:07:21] Then you can go a lot further. You can add a lot of arguments to the image and many, many arguments to the container. So definitely check out Docker with Terraform. We're going to go ahead and destroy this now, and that should be a real quick destroy. We'll say yes to to destroy the image and the container. You'll note that it destroys

[00:07:44] the container first because that relies on the image and image last. And that's reverse order from when we did the apply. Very nice. Now, if we go back to our Docker preview for our Lab document, you'll see

[00:08:04] that I also have some extra credit. You may also be interested in Podman. So I use Podman a lot as an alternative to Docker. One of the reasons that I do this is that it works very well in a lot of the Linux environments that I use it in.

[00:08:23] And it has native NFT support, so NFTables. And so all you have to do is install it and enable NFT and run your container. And you could do it that way if you wanted to with Podman. So feel free to do that as extra credit. But that's about it.

[00:08:41] A little bit of Docker with Terraform.