[00:00:06] So let's introduce Terraform variables. A variable is a symbolic name associated with a value and it works the same way in Terraform as it does in other programming languages or in mathematics in general.

[00:00:24] Here we have two different blocks of information. On the right hand side we have the variable block and this is where we assign the symbolic name for the variable and associate the value.

[00:00:41] On the left hand side we have a standard resource block and this is where we would call on the variable or refer to the variable. So two main pieces to the puzzle when it comes to variables in Terraform. But in reality it can be more complex.

[00:01:02] But we'll start with this as our example. So let's take a look a little bit further at this. On the right hand side we have our variable block and the name is variable. So it's a variable block and we choose what the symbolic name will be.

[00:01:21] In this case I'm choosing instance name that needs to be in double quotes. Then we have the delimiters, curly braces as we usually do for Terraform blocks. And then we have a description type and default.

[00:01:40] The description is just telling us more about what this variable block is. You don't need a description, but it can help you, especially when you're first starting out with Terraform. The second is the type. There's several types of variables in Terraform and we'll talk about those later in the lesson. In this case we're using a string.

[00:02:04] So that's an alphanumeric type of string of information which is normally placed in double quotes. In this case we don't actually need to type this. We don't need to have type equals string. It would work regardless, but we're placing it here

[00:02:20] for informational purposes. And then finally the default, that is the actual value for the variable. And what I have here is Lab 07 variables in double quotes. So all that goes into the variable block.

[00:02:40] Now on the left hand side we have our resource standard resource block. We've got an AWS instance and I'm calling it Lab 07. Then we have our AMI, our instance type and our tags. The only variable that we're calling on here is within the name tag. And instead of Putting this name Lab07 variables in double quotes here,

[00:03:02] instead of that we're referring to the variable we're calling on the variable. And so that will point to this. How does the naming structure work? We type it as var dot and the variable name or the symbolic name.

[00:03:24] So var dot and then instance name, which is this. That's how you Call on a variable. So in a nutshell, variables allow you to write more flexible and reusable configurations. Those are the keys here.

[00:03:42] Flexible and reusable configurations. And we can do many more variables if we wish to. We could include additional variable blocks and references. So, for example, we could have more variable blocks here

[00:04:03] and go as far as we want. And we could have variables for AMI and for instance, type, and for whatever we like. Generally, this would be stored. This block here would be stored in a file called Variables tf.

[00:04:26] It's a separate file where we keep our variables, and then over here we have our main tf. That's where our resources will go.

[00:04:40] So important, the whole idea here is to place any variable information in Variables TF and keep it separate from the main infrastructure code. What this means is the variables TF file is user definable. Users can go in here and change what the value is as necessary.

[00:05:03] But we're trying to keep the user out of the main TF file. So users keep out. We want to keep them out of this file, but it's okay for them to modify

[00:05:24] Variables tf, so they shouldn't be making any modifications to main TF and the other core Terraform files. Any individual variables that might change or values that can change over time should be called as variables from this block on the left.

[00:05:45] And their values should be listed within the file and the blocks on the right. So this is what really helps with reproducibility when we only need to make a couple of changes to our values.

[00:06:01] So to review, a variable is a symbolic name associated with a value, and so this would be the symbolic name here, instancename, and the value would be here for our defaults. Does it have to be listed here? No, it can be done in a variety of ways. But to start, we'll show it

[00:06:21] as a default value within the variable block. And finally, the reason to use variables is to allow you to write more flexible and reusable configurations among team members and with customers and so on. So there you go, a little introduction to terraform variables.