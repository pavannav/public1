[00:00:06] Let's talk about the Terraform Help system and if you go to the repository within Labs Lab 01, you'll see a file Terraform Help system and this will give you some information about the Terraform Help system and the commands that you can run.

[00:00:23] So follow along with that while I work in the terminal. First, you can use the Terraform Help option to learn more about Terraform. And you can access this with help or double dash help or just double

[00:00:41] dash h or single H. That's going to be the easiest way. So if we do a Terraform H, we can press enter and we'll get a lot of information here. Here's the command I issued and you'll see the usage. The command is terraform.

[00:01:00] Then we have global options, which you won't use that often. We'll show a couple of these towards the end of the video course. Then you have the sub command and some of the sub commands include init, plan, apply, and we've talked about those already. And then any additional arguments that you're going to work with.

[00:01:24] So Terraform H gives us information about the main commands which include init, validate, plan, apply and destroy, and some basic definitions for those. And then all the additional secondary commands,

[00:01:42] Console, fmt, graph, import, show stacks. And we're going to go through all of these as we go through this course. So take a look at this when you get a moment. Now, we mentioned global options. Global options would go in between the terraform command

[00:02:01] and the sub command, such as init. If we look down at the bottom here, we'll see the global options that we have. Well, there's help, there's dash version, and we've seen both of these already. And then finally you have chdir dir. This is for changing the directory.

[00:02:20] So let's say your working directory for Terraform is somewhere else. It's a different directory than the one that you're working in. You could use this to tell Terraform what directory will have your terraform files. And quite often you would do this in a scripted environment. And we'll

[00:02:40] show exactly how to do that elsewhere in the video course. So that is the Terraform H command. Now, what I recommend you do is I recommend you learn more about each of these main commands. To start, it'll be good to know a little bit about all the commands,

[00:03:01] but especially the main commands. To start. To learn more about any of these commands, you can type something like Terraform H and the sub command in question. So Terraform H init press Enter. That'll give us a lot of information.

[00:03:20] Here is the command I issued and you'll see the usage Terraform, any global options, which again are going to be unlikely during most of this course, and then Init and then any options for Init. Then we get information about the Init sub command. It tells you you can

[00:03:40] initialize a new or existing Terraform working directory by creating initial files, loading states, including remote state downloading modules, etc. And one thing that's very important that's not listed here is the downloading of provider plugins.

[00:04:00] Provider plugins are what allow Terraform to talk to the different providers APIs. And so if you want to communicate with AWS, for example, you have to download the AWS provider plugin. That's done within the code and that's done when you do a Terraform init.

[00:04:22] Among all these other things that are happening here, it says to you this is the first command that should be run for any new or existing Terraform configuration. Generally it's the first command that you want to run for your configuration. We might do a couple of other things as well before that, but generally Terraform init is the first thing and it says this command

[00:04:42] is always safe to run multiple times. Subsequent runs may give errors, but the command will not remove or delete your configuration. Now after that you can do options. Sometimes you'll need options.

[00:05:01] Let's say that a provider plugin has been increased in version number. Let's say we're trying to upgrade our provider plugin. We could go down here and go to the dash upgrade option and add that to our commands.

[00:05:20] So that would go ahead and install the latest module and provider versions within our configured constraints. And we're going to show all this. If some of this doesn't make sense, we're going to show it all step by step. But essentially we would do a Terraform INIT upgrade and a previously built infrastructure would now be upgraded with the

[00:05:43] latest provider plugins. That's just one example of the many options here. I highly recommend that you do this for each of the sub commands that I was talking about. Init, validate, apply, plan, destroy the main

[00:06:03] ones that were shown when we did a Terraform H all those main commands. So go through the help file for each of those and that's going to help you a lot. As you're working with Terraform, spend some time looking

[00:06:20] through the various help options. This is going to be one of the best ways to learn Terraform in addition to using the documentation. So that's it. For this sub lesson.