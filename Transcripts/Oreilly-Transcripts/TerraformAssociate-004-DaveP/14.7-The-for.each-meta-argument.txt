[00:00:06] Now let's move into the foreach meta argument. The foreach meta argument creates multiple instances as well, similar to count, but it can do this based on a map or a set of values.

[00:00:20] So this could be more powerful than count. You might need to use it sometimes. So let's get into it. First of all, we're working in lab 18 and we're in the for each directory here, let's take a look at the code. We'll go to main TF and what we want to do is we want to create the resource.

[00:00:42] In this case, I'm going to use IAM users because it'll be faster and we'll use the for each meta argument. So let's do that now. And we'll go down below the provider, just standard terraform and provider blocks. We'll create a new resource. I'm going to call it awsiamuser and

[00:01:02] we'll tab over. We'll call this accounts and we'll go down. And this is where we put in the meta arguments foreach. And you'll notice we have two options here. Foreach is what we want. So we'll tab that. And what I want to do is I want to have a set of information.

[00:01:20] I want to have a set of users. Four users, Alice, Bob, Charlie and Denise. To do that, I want to use a function I'm going to use to set. So another function here, and this is going to be within our meta arguments. Press the tab key for that. It sets up the parentheses for us.

[00:01:43] Inside the parentheses, if we have a list or a set, we need to use square br. So we'll create the square brackets and we will set up the four users. These are strings of information for the user names, for the account names.

[00:02:00] And so they have to be in double quotes and separated by commas. So it'll be double quote Alice and then we'll do double quote Bob and double quote Charlie. Gotta make that uppercase capitalized and double quote Denise.

[00:02:23] Good. And we want to make sure that we have the square brackets around the list of names or the set of names and then the parentheses around that for our to set function. And then we'll go to a new line and then we'll specify what the name

[00:02:41] will be. Now normally you would specify the name and just have double quotes and just have a str of information. But we have four names and we want to iterate among each one. We want to go through the whole set. So to do that we can use the each key option. Two options here. Each key and each value. In this case,

[00:03:02] we want each key for this to set function. Good. Now, you'll notice what happens when I do type that. All the red goes away because we have to have some type of iteration here. We can't just have a foreach meta argument and a two set function.

[00:03:20] We have to specify the name portion as well. So again, that's going to be each key, and that fixes everything for us. And we can put in some tags here as well. If we want to tags equals and create some tags. We could say time underscore created equals

[00:03:40] and we'll use another function. This time we'll do timestamp. Okay. And departments equals ops once again. There we go. That code looks pretty good for our resource block. Okay, looks good. So let's break down the code. We have foreach and

[00:04:02] two set that converts a list to a set. So normally if you have the square brackets, that's considered a list of information. But we're converting that to a set with the to set function.

[00:04:20] And then we're iterating over each with foreach. Each key is the current item from the set for each one. And this each refers to this each foreach each key. Fantastic. So this should create four separate IAM users from a

[00:04:41] single resource block. Let's save this code, bring up the terminal, run a TI for terraform init. Ooh, and notice what happened here. This computer has only 636 megabytes of disk space remaining. Aha. Why is that happening?

[00:05:01] Because I have these terraform Terraform hidden directories with all these provider plugins all over the place. Oh my goodness. The same thing could be happening to you if you have a 2030 gigabyte virtual machine. So feel free to clean up some of those. You can search for terraform

[00:05:20] directories to get rid of that. I'm also going to empty my trash right now, and I'll empty out some of these from the earlier labs a little bit later on behind the scenes. All right. But it has been initialized. We are good. Let's now do a T F for a terraform format and have that fix our main tf because this

[00:05:41] stuff wasn't all lined up and nicey nice. And we'll do a TV for terraform validate. Oh, we got an error here. What did I do? Something is amiss. All right, so on main TF line 26, it says this is no good. Why? Because I was moving too fast. That's why.

[00:06:04] And I didn't surround it by double quotes. Ops is actually a String gotta be in double quotes. Now, that didn't show up in VS code. It doesn't really show it as an error because it may think, okay, maybe this is just some type of

[00:06:21] different type of value or maybe a function or who knows what? It doesn't know until we do a Terraform validate or a Terraform Plan or Terraform Apply. So we got to place that in double quotes, save it, and then we can run that Terraform validate again and we get success.

[00:06:41] So a little troubleshooting on the fly. Why? Because I was moving too quickly. This will happen. Be careful when you're working in Terraform. All right, that is good. Let's do a TA for Terraform Apply Auto approve. And you could run through the plan as well if you wanted to, but it

[00:07:01] would just show that it's going to create the four users. And creation is complete. Very quickly. Let's go take a look at our AWS console and we'll go back to IAM once again and users. And there they are. Alice, Bob, Charlie and Denise are created all from one resource.

[00:07:24] You could also use the AWS CLI if you wanted to. And there's a command for that in the lab, I believe you could also view what we have as far as resources with the Terraform state list. Remember that command? We talked about this a ways back? And that'll show each of the IAM user accounts, right?

[00:07:44] Very good. All right, so that's it. Let's take a look at that code one more time. You can see here that we used a foreach meta argument to iterate over a whole list. And we converted that list in square brackets to a set by using the to set function.

[00:08:05] So the names for these accounts, we're using each key that'll use that foreach meta argument again to iterate over each of these. And each account will get a different name. So a little bit different than the count meta argument where you can

[00:08:21] just specify a certain number. Here we can work with entire lists of information. Very good. Let's go ahead and destroy this with a td. Make sure you destroy. And if you want to learn more about Foreach, you can find that at the

[00:08:43] Hashicorp documentation under Meta Arguments for each. And you'll see some usage and examples of that here as well. You'll note that they have name is each key location has to be an actual value.

[00:09:02] So for that we'd use each value. There's an example of that. Check that out as well when you get a chance. And that is it for this sub lesson.