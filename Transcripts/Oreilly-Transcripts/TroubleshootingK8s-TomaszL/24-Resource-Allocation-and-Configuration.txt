[00:00:00] In this video, we'll look at resource allocation and config aspects of logging back end. So, this is the graph on a dashboard showing resource consumption details of Elasticsearch pod. Now, if you look at memory usage 500 m limit is set for the pod, and current usage is already 496 mem, which is very close to

[00:00:22] the limit. Now, if the pod tries to cross the configured limit then it will get in killed and restarted resulting in loss of service. You can actually set alerts to notify when resource consumption crosses a specified threshold to take corrective actions, like increasing of configured limits, optimizing the two

[00:00:40] nibbles, and so on. So, we look at alerts in the monitoring section. So, let's spend some time looking at configuration aspects of the logging backend. The out of box setting should mostly work, however in some cases you might have to modify the config changes to cater to your requirements. In my al k stack

[00:01:00] I want syslog from nodes to be sent to the logging back-end for analysis. Now, to do that I need to ensure the configuration is set up to send syslog content. So, in the case of a LK file bit agent which runs on each node needs to be configured. So, let's look at the file bit configuration. So, that's the logging

[00:01:21] LK file wait - TS - country. So, as you can see in the config map only the pod locks are forwarded, which is your value of containers and all the logs inside dialog containers. So, recall the default pod locks which are invalid log containers. So, this is the basic logging files that are forwarded to Elasticsearch. Syslog, which is in where

[00:01:42] log messages is not forwarded at all, so we need to ensure that syslog file is made available to Filebeat daemon for reading and sending it to the logging back-end. So, this is the Filebeat DaemonSet. Now let's look at the Filebeat DaemonSet config. So, this is the

[00:02:01] spec that I will look at. I need to look at the hosts path and the volume mounts to see which files are being forwarded. So, as you can see files in containers, files in log pods, but I don't have the where log messages. So, what we need to do is we need to add the

[00:02:21] relevant config for syslog. So, what I will do is I will add the relevant host path and associated volume mounts to make the syslog file available to the Filebeat pod. Add this new host path which is where log messages, I'll name it as syslog. I need to also ensure that I set

[00:02:41] the volume mind appropriately, so I will need to add a volume mount entry for the well log messages. Okay, so this ensures that my Filebeat pod configuration handles the syslog. Now, after this I will edit the Filebeat config map, so there I

[00:03:00] need to access log as an additional input type. So, I will add syslog as an additional input type. Okay, so this configuration is done now. Let's save it and see if Filebeat pods

[00:03:20] are running fine or not. So, we have the Filebeat DaemonSet, and let's see, the post the config Filebeat DaemonSet will get restarted to take care of the new config. Okay, so we have

[00:03:40] all the containers that are available, and that's about it right. So, this will ensure that syslog is forwarded to Elasticsearch centralized logging back-end for further analysis. In the next section, we will look at monitoring and alerting.