[00:00:00] Your containers are working with Kubernetes engine. Your Kubernetes engine needs to work on every worker node that you are using. Of course, every node that you are using, regardless if it is in the cloud or on-premise will have finite number of resources including memory. And it turns out that memory

[00:00:20] errors are one of the most important things that you need to alleviate and you need to learn how to solve. So, in this video we'll be handing out of memory errors. Firstly, we will create pod definition with memory limit. That's important because limiting memory is very useful and very good practice, so

[00:00:40] you need to try to understand how much memory your container should take, and then you can limit it accordingly. Then, you will have better maintenance and maintainability of your pods and workers. Then, we'll start pod with the limit and you'll analyze memory limit of running pod, and you will see if it was taken

[00:01:00] into account or not. Memory limit YAML has a definition of pod that has memory limit defined. So, we can see that we are using pod and name will be memory data. Then, containers will be memory demo city are asked. We have image that this just Linux with some stress testing in embedded, and then

[00:01:20] the resource section is important for us. So, we are limiting memory, it will be equal to 200 megabytes and request memory to 100 megabytes. Those two limits will consist of memory that will be assigned to this container from the Kubernetes engine. That will execute commands stress so that command is doing

[00:01:41] some performance testing, and we are passing arguments to that command. So, one virtual machine should be run, we are allocating 150 megabytes to this specific process, and it should hang until it will finish. So, it means that our container has 200 memory allocated.

[00:02:01] My stress test process is taking 150, so it means that almost all memory will be taken but it should work ok because it is below limit. We will test that and see if that is the case. In handle out of memory of SH you have the comments that you could use to

[00:02:21] construct it. So, we will construct our memory limit using the apply function. Then, let's see and get running pod. So, we can see that it is not ready yet so let's wait a moment until it will be ready. Yes, it is ready right now. It's running from nine seconds. What we'll have that we can get full definition of

[00:02:42] the memory demo and we can get it in a YAML format, it is very useful. So, we can see when you will do it all the information will be printed out. And what was important for us, the section about specification. So, you can see that containers is taking those arguments. So, we have this megabytes parameters passed

[00:03:01] to the container. But what is more important, we have resource to lemis passed here. So, we can see that 200 memory for limits and for request 100 megabytes were passed and our container was limited to only 2 megabytes of memory. So, using that technique you can deploy many containers, many pods on one

[00:03:21] physical machine or on one cloud instance. It could be useful for you. Most you will do experiments. It is very useful to just heal and delete your pod so we will do it using delete Putnam or a daemon. After a while it will be deleted. In the next video, that will be the last one we will be handling garbage

[00:03:41] collection and eviction threshold. We'll understand those and see how can we configure them.