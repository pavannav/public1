[00:00:00] In this video, we'll look at Kubernetes resource management and how to troubleshoot resource management realated issues like pods not getting scheduled, pods terminating, and throwing em killed messages among others. We will toward a brief overview of Kubernetes resource management and thereafter delve into troubleshooting. So, what is a resource in

[00:00:21] Kubernetes. A Kubernetes resource is something which is requested by, allocated, or used by containers like CPU or memory. Kubernetes supports many different resource type. Let's see what's the most common types in Kubernetes. CPU, memory, huge pages, storage, device

[00:00:43] plugins like GPUs, and custom resources. So, the way we use resources is by specifying resource requirements while requests and limits in the pod spec. Now, requests indicate the minimum quantity of the resource needed for the container in the bond, whereas limits indicate the

[00:01:01] maximum allowed usage for specific resource by the container. This slide shows an example pod spec, where resource requests and limits for a container are provided. Limit is 2 CPU and 256 mb ram, whereas request is for 1 CPU and 128 mb

[00:01:20] ram. So, how this request and limits gets used. Kubernetes uses the resource request for pod scheduling. So, here is a quick rundown on what happens when you create a pod. The Kubernetes scheduler selects a node for the pod to run on. Then, how does the node get selected. Each

[00:01:41] node has a maximum capacity for each of the resources, like CPU, memory, and so on that can be provided to the pods. The scheduler ensures that for each resource type, let's say CPU, the sum of the resource requests of the shedule containers should be less than the capacity of the nodes. So, the key thing

[00:02:01] to understand is Kubernetes uses capacity based scheduling and not utilization based scheduling. So, in other words even though actual memory or CPU resource usage on nodes might be low the sheduler still refuses to place a pod on a node if the capacity check fails. So, this protects against a resource

[00:02:21] shortage on a node when resource usage later increases, for example during a delivery. For detailed explanation on the resource management topic I suggest you to go through the links mentioned in the slide. It gives very good overview of internals of the Kubernetes sheduler and resource management aspects. In the subsequent video, we will look at troubleshooting

[00:02:41] resource management related issues. This lists some of the common resource management related issues. Pod failing to get scheduled, pod restarting and showing in killed event, node showing system messages, the waste log which is available yd message or syslog, it's filled with out of memory errors. Now,

[00:03:00] before we delve into the troubleshooting scenarios just a quick list of common commands that we will use. The list node capacity, you can see what's the command to use that. It's Q that will describe node. Then get pod spec. Then get resource requests and limits for a pod if it is specified, and how to check the resource

[00:03:20] consumption for nodes or pods. In the next video, we will look at a scenario for insufficient cluster capacity.