[00:00:00] Welcome to the last video of our course in which will be troubleshooting application access issues. So, first we will create service with n instances. We will be troubleshooting access problem, and we'll define access problem as the problem with accessing, so getting into the pod. Then, we'll be solving problem with exposing service deployment and you

[00:00:21] will see that it will solve our previous problem. So, in troubleshooting dot SH you have a definition of what we will do. So firstly, we'll do capacity I'll run host names and host names will be the name of our application. We are passing that it should use server host name, it is available through Kubernetes GC r io.

[00:00:41] Labels will uniquely identify this instance, port will be used to get to that instance. It is important because this will be the public port in which we can use to access the container that has a private port exposed. Finally, we are specifying that three replicas should be

[00:01:00] created. So let's execute that comment and we can see that host names are created. We need to see and wait if all pods are running crud, right now they are not ready so let's wait a moment until they will be exposed. We guess that they are ready so we are at the point of looking at the service host names. SVC is

[00:01:21] a comment that allows us to see services registered and exposed to Kubernetes service registry. So let's see, and we can see that host names is not found. So, that is an access problem because without that we are not able to access our container. How can we solve that. We can

[00:01:41] expose our host names deployment but we need to give couple parameters to it. So, first parameter that we need to do is of course host name, so the name of the deployment. Then, port that is an internal pod in our container. So, container started on the pod 80 and we want to expose it on the target port, so it means

[00:02:02] on the machine on which Kubernetes is running on the nine three seven six. So, it will expose a deployment and then when you will do capacity I'll get SVC host names you will see but your host names deployment is exposed on a cluster IP and pod 80. So, you can see that it is exposed

[00:02:21] successfully. So, let's sum up this section of our course. At the beginning, we made services for Kubernetes explained. So, we created new service that was load balancing service and we expose that using service mechanism in Kubernetes. Then, we are using service discovery and routing. So, we created two services and we saw that we can register

[00:02:42] them within service discovery, and then we can retrieve them using labels. This is a very flexible mechanism of retrieving services. We made ingress resources explained. So, we took the service created in the video 1 and we made those resources available through

[00:03:02] the ingress traffic. Finally, we were troubleshooting application access issues and we saw that we can elevate those problems by exposing service using SVC command. So, let's sum up whole course that we just finished. At the beginning, we learned how to avoid installation

[00:03:20] pitfalls, so we saw how to install Kubernetes. Then, we're managing your cluster effectively, so how can we manage Kubernetes cluster using its commands. Section number three we were setting up logging, that is a very important aspect of your application. Section number four, we were monitoring your cluster, so we saw how it behaves

[00:03:41] under traffic. Section number five was about troubleshooting application deployment issues and related things like replica sets and DaemonSets. Finally, in the section number six we were configuring your services and we saw how can we troubleshoot access patterns and also leverage service

[00:04:00] discovery. So, I hope you enjoyed the course and bye. [Silence]