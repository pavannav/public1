[00:00:00] Let's look at some of the issues common to multi-platform clusters. Now, this is my cluster. I'd applied the following pod, my deployment object. Now this creates a pod with the following image, stress that PPC 64 le. Okay, so it's getting created. The

[00:00:23] status turned to error. Now let's see why the status turn to error. I'm describing the pod object using kubectl describe and the pod name. Now, what I see here is crash loop back

[00:00:42] off as reason and the state as terminated. I don't see any additional details which can help me to find out why the pod errored out. So, what I will do is take a look at the logs of the pod. Many a time logs gives good information on why the pod errored.

[00:01:00] This error, exact format error indicates a mismatch between the container image architecture and host CPU architecture. Let's take an example. Your container image is for Power Architecture and you are running it on Intel host. Then, this will result in the exact format error. Or, let's say your image is for Intel

[00:01:20] Architecture and you are trying to run it on armed host, you will get this error. Now, this error is very common in multi-platform clusters which consists of hosts with different CPU architectures. As a best practice, when using multi-platform cluster we should use image manifest having images for different architectures. Consequently,

[00:01:41] there will not be any issues with the pod referring to this manifest image running on any node in the cluster. Now, if you have an image manifest having image for a specific architecture and you are deploying it in a multi-platform cluster you need to tell Kubernetes Euler the type of nodes you want this pod to get scheduled.

[00:02:01] In this case, I am not using any scheduler primitives. No node selector, node affinity, or taints and toleration, nothing. The Kubernetes scheduler gets the request and whichever node is free goes ahead and deploys the pod on that node. In this case, the node happens to be an Intel node and the image is for Power

[00:02:21] Architecture and it fails to run. So, that's why we get this error exact format error. Now I will delete the pod. In the next video, we will look at a scenario where pending state is shown post deployment of the pod.