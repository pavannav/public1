[00:00:00] One of the most powerful concepts in Kubernetes are deployments. Deployments allow us to specify rules how our application should be deployed. So, in this video we'll be looking at the ways of using deployments. So, we'll see how to redeploy application and we'll see also that in such a case the rolling update

[00:00:21] strategy will kick in. We'll be using rolling update so it means that all the application will not be discarded until the new application will be running. So, there will be no point in time where we have zero instances. We will be also analyzing that process of deployment. So,

[00:00:40] let's go to code. So, we have am using deployments that as edge. Firstly, you need to deploy your NGINX deployment. If you have it already then that's good. So, we can see that NGINX deployment is configured. So, this is a YAML file that allows us to configure NGINX. If you are interested how does it look like you

[00:01:00] can go to the YAML file. But this is not most important thing for us right now because what's important for us is that we want to change the version of this deployment. So, we are searching in our deployment apps NGINX deployment that we just did, and we want to set image of the deployment to NGINX but we are changing version from one to two.

[00:01:21] So, in such a case the application needs to be redeployed so rolling up the strategy will kick in. So, let's start it and we can see that image is deployed. So, you can see that rolling status is that we are waiting for NGINX deployment, one of three new replicas has been updated. So, rolling deployment process is

[00:01:41] running. So, if you do get RS you can see that we have two NGINX deployments in which there is one of one instances and in other is 3 out of 3, but you can see that it will change after a while. So, we can see that right now the new deployment has three instances and old

[00:02:01] one has zero. So, that intermediate state was important for a rolling update strategy because this was the oldest NGINX deployment with version 1.9 dot one. So, until all instances of the new one will be ready we cannot kill order. So, when

[00:02:20] all were ready or have checks were ready and tells that application is ok, then Kubernetes is allowed to kill the old instance. So, after some time the new instance can be killed. But you can see that here is one instance and after some time there are 3, and old instances that have 3 right now has zero. It went

[00:02:42] from free to zero for the old instance, and from 1 to 3 for the new instance. And because of that we had zero downtime deployment using rolling strategy. You can get all pods. So, you will see that we have three instances of the NGINX deployment, and also you can describe

[00:03:00] your deployment and there will be information about strategy. So, let's find it out. So, we can see that we have rolling up the strategy with 25% max and available and max search is also 25%. So, it means that there should be no point in time where we have less than 25 percent of declared replicas. So, less

[00:03:21] than 3 and of course it is rounded up. So, if that 25 percent is lower than 1 for this case it will be run at up to d1. In the next video, we'll be delving and using DaemonSets to run code on multiple nodes.