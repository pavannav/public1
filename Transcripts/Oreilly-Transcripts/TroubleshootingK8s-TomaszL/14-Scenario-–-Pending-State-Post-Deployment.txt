[00:00:00] Let's see another scenario where we deploy pod and the pod status remains in pending state. As you can see, the pod spec has a node selected entry, beta dot Kubernetes dot io / arc BBC 64 le. Let's see what happens when I deploy this. The pod status is pending. Let's see

[00:00:21] more details by describing the pod. As you can see, the pod description shows that pod has failed. Now, since this cluster doesn't have any power are closed the pod fails to get scheduled. In other words, we don't have any node with the specified label. Let's check the node levels. As you can

[00:00:42] see, there are no nodes which are equals to e PC 64 le. What we have is beta dot Kubernetes dot io / arc equals 2mv 64. This is the reason why the pod is not getting scheduled and pod status is pending. This is a typical way to deploy application in a multi-platform cluster.

[00:01:00] In the spec, we specify node selector and which architecture I want to target. You can use any levels. Here, we use the label method or Kubernetes dot io / arc equals to whatever I want to specify based on my cluster setup, but you can use any levels. If you see pod status pending the first thing you do is describe the

[00:01:21] pod, see the errors and warnings, and proceed from there. Let's see another scenario with no definite labels. So, we will create a pod with no definite rules. The pod is in pending state. We will use kubectl describe and find out

[00:01:40] why the pod is in pending state. As we can see, it says reason is failed scheduling and the messages to motivate matched the node selector. What it means is Kubernetes scheduler failed to schedule the pod because it couldn't find a node which matches the node selector. Let's go back and look at the pods spec.

[00:02:02] What we see here is a hard requirement to deploy on node with label this guy because SSD. Since the schedule failed it means that there are no nodes in the cluster with that label. We can verify again.

[00:02:20] Let's get the node levels. And as you can see, none of the nodes have labels bigs type equals to SSD, and hence the scheduling failed. Now let's try to reply another pod which puts a soft requirement. I delete this pod for now and we deploy pod with soft affinity

[00:02:42] posited soft requirement. So this gets deployed. This is because the pod spec puts a soft requirement on specific node type. As you can see, it's preferred

[00:03:00] scheduling ignored muting execution, recall preferred during scheduling is a soft requirement. If no node exists with the label Kubernetes deploys on any available node. What you should remember is if the pod status is pending and if you see warning messages related to scheduling then you should go back and check the node selector or node

[00:03:20] affinity rules and the associated labels that exist in your cluster. Let me extend this scenario a bit. We'll use hard requirement again. However, we will add label disk type SSD places to one of the nodes and we deploy the pod.

[00:03:43] So, now node vm2 has the latex type equals SSD. And as we can see, the pod get cheated on the node vm2, which has the specific level. One thing I want to mention again is that

[00:04:02] some labels are added by Kubernetes itself. For example, the beta dot IO dot humanity slash Ark is the label that is added by Kubernetes itself. In the next section, we'll start with managing your cluster effectively.