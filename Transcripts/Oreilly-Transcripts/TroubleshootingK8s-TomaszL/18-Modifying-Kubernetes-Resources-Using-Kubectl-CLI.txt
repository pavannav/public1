[00:00:00] In this video, we will look at modification of Kubernetes objects using the CLI. So, many a times you will need to modify Kubernetes objects, like config map, secrets, deployments, etc to resolve issues in your cluster. Now, let's look at some examples of Kubernetes object modification using the CLI.

[00:00:25] So, this shows live editing of a deployment object and changing the replica count. So, the replicas is changed to two, the object is modified, and if I do kubectl get pods as you can see the

[00:00:42] number of ports for NGINX is two now. So, what I will do now is I will revert that replica count to one and you can see the number of pods reduces to one.

[00:01:03] So, for changing replica count we can also use the scale command. Now, this comes very handy when you want to scale out on and scaling the workload based on load. Now let's see some examples.

[00:01:20] So, I'm setting the replicas to two for the NGINX sample deployment, and as you can see the number of pods is not two. So, I increase the replica count to three and you can see that the number of pod increased to three.

[00:01:45] [Silence] So, similarly I can actually scale in, I can reduce the number of pods. So, what I will do is I will change that replica count so I make one. Now let's

[00:02:02] see the number of pods again. And as you can see, the only one NGINX sample part is running, the rest are being terminated. So, now let's look at ways to patch existing objects. So, let's say you want to change the image to a new one. Now, you

[00:02:20] can also use a dataset image command. [Silence] Using that edit command I change the image to NGINX,

[00:02:41] and if I list the pods you can see that the containers are getting recreated. And I have an image pull. So, this is most likely the image is not available and it's not able to pull the image. We can also use the roll-out command

[00:03:02] to see the status. So, this is the command to use the status of the deployment. To see the status, you can also use the rollout command with a new option to undo the changes.

[00:03:20] As you can see, the previous changes will be pulled back. So, one drawback of these methods is that you won't have any record of the changes. Now, hence it's recommended to use a declarative way of editing, by using a manifest file with the change values and then use the apply option. So, or else if

[00:03:43] you prefer to use the imperative approach you should at least use the - - save - console option to save the apply changes as annotations in the spec. Let's look at some more examples of modifying the Kubernetes objects. Now, in these examples we will use the patch

[00:04:02] command. Now, for many troubleshooting exercises you will need to update the config, restart pods, and so on so patch comes very handy here. So, let's say you have a detector config

[00:04:22] map and now you want to distend the pod so you can do the following. So, kubectl patch deploy and the deployment name, the namespace, and then what's the patch command.

[00:04:43] So, what I'm doing here is I'm editing the spec and adding any my material. So, as you can see the new pod is getting instantiated so what we will do is we

[00:05:01] will dump the spec and see if the variable will got added or not. So, we use the get option and use YAML. You can see the environment variable be stuck well you add it to the spec. So, this is the patch come on that field.

[00:05:22] We are taking the spec with this environment variable and you can see the same when dumping the YAML of the spec. So, let's see another example we are report getting home killed due to low memory limit and you want to increase the memory limit off the pod, so how you can use the patch command to do the same. So,

[00:05:41] let's create the pod, I will use the kubectl apply command, which is the declarative approach. So, let's check the pod and the pod is getting created.

[00:06:00] As you can see, the container status of the pod shows indeed. So let's look at the spec. So, if you look at the manifest you see the memory limit is set to 200 m and I'm

[00:06:20] allocating 250 m right, so which is more than the configured limit, and that's the reason the container gate gate and the status is okay. So, what we need to do is we'll update the limit right. So, for updating the limit we use the batch command.

[00:06:41] [Silence] So, what I'm doing is I'm patching the spec, bumping the memory limit to 300 M. So this is what I'm doing. So, we successfully applied the patch so

[00:07:00] let's verify the same by looking at the spec. So, again I will use the get combined with - YAML option. So, as you can see the memory limit is 300m. So, as you can see familiarity with the kubectl CLI is necessary to troubleshoot and fix issues

[00:07:21] in the cluster. So, in the next video we will look at an issue that is common when mixing imperative and declarative approaches for managing the Kubernetes objects.