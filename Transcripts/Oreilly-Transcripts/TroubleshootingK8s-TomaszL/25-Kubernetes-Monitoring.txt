[00:00:00] In this section, we'll take a look at Kubernetes monitoring architecture, the use and red method for monitoring, and the key metrics to monitor, and then look at alerts. So let's begin. In this video, we'll look at Kubernetes monitoring. This chart shows the Kubernetes monitoring architecture, a key component is Prometheus, which is the de facto

[00:00:21] monitoring solution for Kubernetes cluster. Gates Prometheus adapter exposes all required Prometheus metrics through the Kubernetes metrics API. The Kubernetes docs describe this architecture in details, however I find a task block very simple and easy to understand for anyone wanting to know the details of Kubernetes monitoring

[00:00:41] architecture. Please do check it out if you want to understand more on the architecture. If you are using turnkey or hosted solution monitoring will be set up by default and ready to use. If you are using custom solution with kubeADM then you will need to install and set up monitoring separately. The link mentioned in the slide has the requisite

[00:01:00] details for setup and install. We will not go into the details of monitoring setup, instead our focus will be how to make use of the metrics. Numerous metrics are available for node angle cluster, so how do you make use of it. How do you know if the values are indicative of any issues or not. This is where I would like

[00:01:21] you to introduce two two methodologies, the use and the red method. This slide talks about the details of the use method. Use stands for utilization, saturation, and errors. The use method focuses more on monitoring performance and is meant to be used as a starting point in identifying the root cause of performance issues and other systemic

[00:01:41] bottlenecks. It's more infrastructure focused. Utilization is the average time the resource was busy servicing work. Utilization is expressed as a percent over a time interval, example CPU running at 100% utilization, disk running at 90 percent utilization, and so on.

[00:02:01] Saturation is often expressed as queue length and indicates the degree to which the resource has extra work that it can't service. Since the request can't be serviced it will be queued. For example, the average CPU run queue length and so on. Errors are usually scalar counts. Example,

[00:02:20] the number of packet drops, collisions, etc. Error usually should not be ignored since they can eventually lead to degraded performance and other issues in the cluster. Use method was conceptualized by Brendan Gregg. I would recommend you to go through the link mention in the slide for more details. So, when using the use method for every

[00:02:40] resource check utilization, saturation, and errors. Kubernetes monitoring stack with Prometheus has built-in support for use method as we will see shortly. The next is the red method. The red method follows on the principles outlined in the four golden signals developed by Google site reliability engineers, or SREs, which focuses on measuring things

[00:03:02] that end-users care about when using your services. The red method requires application level tracing and we will not cover that in this section. To summarize, use method is for resources and the red method is for services. In the next video, we'll look at key resources to monitor

[00:03:20] and related metrics.