[00:00:00] In the previous video, we created replica set difination. So, we created a pod definition that specifies that we want to create free replicas. But up to this point we did not deploy it. In this video, we'll do a deployment live replica sets so this will be a next-generation replication controllers. So firstly, we will leverage the pod

[00:00:21] definition that we created in the previous video so I'll be creating service with an replicas, and it will be equal to three as you remember from the previous one. Then, we will get information about running replicas to confirm that we deployed as many replicas that we requested. This is important, and we'll see that there is

[00:00:41] some time between all replicas will be running because we need to have time to spin up those instances. So let's go to our terminal. So, we have a file that has definition of what we should do. It is called create RS replica set for the short. So, we'll use capacity I'll apply and will specify replica set file that

[00:01:01] we created in the previous video. So let's start it. So, you can see that the front end is created. At this time, we should expect to have three instances of the front end. So let's take a look at this. So, we can do Cooper City I'll describe our as is a shortcut for our replica set and you are to describe the front end. So, let's wait a second and

[00:01:22] we can see that there is a lot of the information here. So, at the beginning you will have a name space default because we didn't specify the specific name space, and we have selector and labels that was specified in the previous video. What's important here is replicas here. So, you can see that we have three current

[00:01:40] replicas and three desired. So, it means that the number of replicas that we requested were deployed successfully. Then there is a status of pause. We can see that three of them are running. Zero rating, zero succeeded, zero failed. So, if it will be a job there will be succeeded because job will finish in the future.

[00:02:00] But those are services that are running all the time, that's why we have state running. We'll look into job into one of the next videos. Then, at the end there is important aspect because it shows the events. So, we can see that 19 seconds ago there was an event to create a replica set for 3 instances,

[00:02:21] successful create event was emitted, then replica set controller acted to it and created pod from it, fronting and fronting. And we can see that prefix is equal to the definition that we specified here, tire. And then, we have suffix that is some random hash value. So, you can see that using the replica set

[00:02:42] you can scale your cluster and you can see that Kubernetes automatically creates as many replicas that you requested. In the next video, we'll be looking at jobs so there will be running jobs, run once and forget.