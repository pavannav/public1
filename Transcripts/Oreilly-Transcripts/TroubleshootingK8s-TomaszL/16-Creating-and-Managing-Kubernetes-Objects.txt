[00:00:00] In this video, we will look at ways to create and make changes to Kubernetes objects using the kubectl CLI. So, I recommend using manifest file, either YAML or JSON for your objects. So, this acts as a source of truth and can be version controlled as well. So, there are two approaches for creating objects in Kubernetes. Imperative approach. Now, this

[00:00:20] tells Kubernetes what to do. And then there is the declarative approach. So, this tells Kubernetes what you want and if you know what to do. So, consequently based on these two approaches there are two CLI commands. So, you have kubectl create and you have the kubectl apply. So, kubectl create uses an imperative approach. So, in kubectl

[00:00:40] create you specify what you want to create, delete, or replace. So, this is a sample deployment manifest file that will be used for examples. Now, let's use the imperative approach to create the objects. So, I will run kubectl create minus F and the manifest file and it gets deployed. When I want to use the

[00:01:02] declarative approach I will use kubectl apply. Your changes will be maintained even if you apply changes to it like object, so this is very important. So, let's now create the object using the declarative approach. I type kubectl apply minus f' and the manifest file,

[00:01:21] which is deployment YAML. So, once the object is successfully deployed you can view the last applied configuration by running the following command, kubectl apply then view - last - applied followed by the object name, which is the deployment slash

[00:01:40] NGINX - sample. Now, any config changes that are made to this object is added to the annotations field, and you can view them using the following command. So, kubectl get deployment / NGINX - sample - or YAML. So, we look at get command in more details in the next section. For now, we are just using it to

[00:02:02] view the live configuration. Advantage of the declarative approach is that it helps with the change review process, as well as provide an audit trail of the changes. See all the changes in the annotations field. So, you audit trail of what changes we intend with respect to the object. For delete, we recommend the imperative command right.

[00:02:21] So, the recommended approach is to use the imperative command as it is more explicit about what is being deleted and less likely to result in the user deleting something unintentionally. So, when you want to delete an object what you do is you use kubectl delete - f followed by the manifest filename. So, this

[00:02:40] is the infinitive, of course we are not using the declarative approach for deleting the object. Now, to summarize create manifest files for your object so that you can version control it, use a declarative approach to create and modify the objects so that whatever changes you do to the object is saved in a notation, and for deleting the objects

[00:03:00] used explicit imperative approach. In the next video, we will take a look at reviewing Kubernetes resources using the CLI.