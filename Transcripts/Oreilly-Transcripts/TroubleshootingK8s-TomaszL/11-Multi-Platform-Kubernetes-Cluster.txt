[00:00:00] In this video, we'll go through some of that aspects of multi-platform Kubernetes cluster and things to keep in mind when handling multi-platform Kubernetes cluster. We will also go through Kubernetes scheduling primitives for multi-platform clusters and troubleshooting some of the issues encountered in multi-platform clusters. So, what are Kubernetes multi-platform

[00:00:20] clusters. Today, Kubernetes is available for multiple CPU architectures. It's available for Intel, IBM Power Architecture, and mainframes as well. Kubernetes also runs on various OS flavors, whether it's Ubuntu, Debian, Red-Hat, Fedora, or on Windows. Multi-platform cluster is very common in practice. You

[00:00:40] might have cluster consisting of nodes having different CPU architectures, or you might have a cluster with nodes having special Hardware. For example, GPUs or nodes having FPGAs, or you could also have a cluster with a mix of Linux and Windows nodes, for example Windows nodes for running .NET workloads. So, multi-platform clusters are very common in practice, but when using

[00:01:01] multi-platform clusters we need to remember a few things. We need to ensure that we use Docker image manifest. It's also called fact manifest to handle multi-arch and multi-OS image. You can read through the links for more information on what are Docker image manifests. The thing to remember here is that Docker image manifest allows you to

[00:01:20] have multi-arch or multi-OS images and you will need it for multi-platform clusters. You will also need to use Kubernetes scheduling primitives, like node selector, node affinity, and taints. The link mentioned here will give more details on that. It's important to understand them since many issues in multi-platform clusters are due to

[00:01:41] incorrect handling of these primitives. So, let's dig a little deeper into the Kubernetes scheduling primitives. It's important because as I said before many of the issues that you will encounter in your multi-platform cluster will be related to some of these. The first one is node selected. So, what the node selected does is allow pods to select

[00:02:00] nodes based on node labels. Then, there is node affinity, which is similar to node selector but with more controls. You can define soft and hard requirements, you can even define relative weights for various conditions. Basic premise is that it allows pods to select nodes based on labels and various attributes of the node. The third one is

[00:02:20] taints. It allows the nodes to repel the pods. If you see the above to allow pods to select the nodes but what taints does is it allows nodes to repel the pods. We'll see more details about how it's used. Okay, so this slide talks about how node selector is used. Key thing to understand here is node selector will ensure pod gets

[00:02:42] scheduled only to nodes with specific labels. As you can see here, the node selector has a key our key calls to PP c64 le. So, this pod will be scheduled on nodes which has labeled arc equals to PB c64 le. Now, for example if no node is available with this specific label then

[00:03:02] you will see the pod in pending state. What it means is that the Kubernetes scheduler failed to schedule the pod because no node exists with that specific label. Now, the alternate is where you don't use any node select a level and pod get scheduled on any of the nodes. So, the way node selector gets used is cluster nodes will have labels,

[00:03:21] this will be typically done by the cluster administrator. Now, some labels are automatically generated by Kubernetes but you can always add custom labels based on your environment. Then your pods, deployments, and other Kubernetes objects will need to specify the node selector label in the spec. Kubernetes scheduler will take care of the

[00:03:40] rest. So, node affinity is similar to node selector but it's much more flexible and also provide soft and hard requirements. So, required during scheduling, ignored during execution is more of a hard requirement. Specified rules must be met for the pod to be shield onto a node. Then, we have preferred during scheduling

[00:04:01] ignored during execution. This is a soft requirement. If scheduler is unable to meet the requirement then it's fine as well. One thing to note here is the keyword ignored during execution. What it means is that if during the execution the node labels changes such that the rules are no longer met the pod will

[00:04:20] still continue to run on the node. You can see an example spec where node affinity is specified. You see both soft heart requirement is mentioned. You can as values node selected here, but as I said you can either use node selector or you can standardize on node affinity which is much more flexible and gives you more control options. The choice is yours. In the next video, we'll understand

[00:04:42] what taints and tolerations are.