[00:00:00] In this video, we will look at key resources and related metrics in a Kubernetes cluster, followed by viewing the metrics. The key metrics to monitor for node. For the key node resources, that is CPU, memory, disk, network, get the utilization, saturation, and error metric. Same thing for cluster but aggregated

[00:00:20] values for all the nodes in the cluster. From a pod point of view, in addition to the key resources, that is CPU, memory, network, we are also interested in the restart count of the pod. So, how do we view the matrix data for the key resources. We use monitoring dashboards as these make it very easy for monitoring of the cluster. Many turnkey

[00:00:42] solutions provide monitoring dashboards by default. For a custom solution based on Kubeadm you can refer to the link shown in the slide for set of instructions. So, this is the graph on a based monitoring dashboard for my cluster. Let's go to the terminal. I'm running kubectl get pods - and

[00:01:01] monitoring. As you can see, the monitoring stack is deployed in the monitoring namespace. You can relate to the monitoring architecture we saw earlier. You see Prometheus, alert manager, Grafana. So, these are the available default Dashboards. As you can see, we have dashboard using

[00:01:20] use method for node, cluster, followed by Dashboard for pod cluster, and so on. Let's see the node dashboard based on the use method. This cluster has two nodes. Let me see the details for node V m1. The CPU saturation and utilization are very high. Mem utilization seems to be

[00:01:42] okay. Mem saturation seems to be reasonable. So, let me change their timelines and see. In the last six hours, as you can see in the last six hours CPU

[00:02:00] utilization and CPU saturation is fairly high. Now, this indicates either their a capacity issues in the cluster or some group processes consuming all this CPUs. You can see the other metrics as well. One thing to note is that sometimes it's very difficult to get certain metrics. For example, getting the error

[00:02:20] home for CPU resources tricky. Is it the hardware error or something else, similarly for memory as well. So, let's get an idea of how the graph data gets generated. Now let's look at the CPU saturation data. A click on the twister and go to explore. So, it will show the

[00:02:40] underlying matrix for the graph. As you can see, the metric is Nord underscore CPU Orosco saturation underscore load one. So, let's go to the Prometheus dashboard and get the value of the specific metric. You can also see the available metrics. For example, if you type load : it will show you the

[00:03:01] available metrics for node, as you can see in this screen. Now, you can make use of any of this matrix in Grafana. So, let me go back to the home page. Now let's see the cluster level data. As we can see, both the CPU utilization and saturation is high for vm1, which is

[00:03:23] shown in green, and the yellow one is for vm 2 which seems to be okay. Now, vm 1 definitely seems to be having some issue. Now let's go to the basic node dashboard and here also we see CPU usages 100%, so we

[00:03:43] need to find out the cause. So, let's go to the dashboard for the pod and see the details. However, we'll need to select the namespace and then the specific pod to see the relevant details. Now, this is fine if we know the specific part to look at, otherwise if

[00:04:01] you want to look at all the pods it can be tedious. As you can see, the usage for this specific pod is pretty high. Now, this could be the offending pod causing high utilization for the node. Let's look at the computer resources

[00:04:20] cluster. It gives a good consolidated view of the cluster. Now, it is very helpful when operating the cluster in production. You see the utilization, limits, etc all in one place. In the next video, we'll look at alerts.