[00:00:00] In this video, we'll look at centralized cluster level logging in Kubernetes. Cluster level logging is about aggregating logs from all the pods across the nodes and having it in a centralized location for analysis and viewing. Note that Kubernetes doesn't provide a cluster level logging solution by default, like basic logging that you saw previously. Now, you need to rely on

[00:00:21] third party solution for cluster level logging. Turnkey solutions provide cluster level logging, either based on e lk or EF k as part of their offering. Now, if you are using custom solution based on kubeADM then you will need to set up logging by leveraging Community Supported options based on elk or EFk

[00:00:41] stack. There are lots of material available in the public domain to set up a lk or e FK stack for Kubernetes. So, first let's look at the logging patterns. The first one is to use the node logging agent. In this pattern, a logging agent runs on every node and picks up the pod log files and sends it to logging back

[00:01:01] in for processing. So there is a sidecar pattern. In this pattern, a sidecar container is attached to the pod to collect the logs and send them to the pod specific log file. Now, this is useful when the application doesn't make use of standard output and standard error for logs. So, once the pod log file is made

[00:01:20] available by the sidecar the node logging agent can pick up the same and send it to logging back in for processing. So, another pattern is the citecar container with logging agent. In this, there is no separate node logging agent, the sight curve container itself shifts the log to the logging back in for processing.

[00:01:40] In some cases, the application itself can directly send the logs to the logging back-end. Now, these are the most common patterns, there could be some variations or combinations of these patterns as well. So, now let's look at the logging backends. So, we have the EFk and elk stack. So, EFk consists of Elasticsearch,

[00:02:01] Fluent, and Kibana. So, Fluent is the entity which shifts the logs. Kibana is the visualization layer and Elasticsearch is the key component which indexes and processes the log. The other one is the ELK stack stands for Elasticsearch, Logstash, and Kibana. In the case of aLK, the

[00:02:20] agent that is responsible for sending the logs to Elasticsearch is Filebeat. Let's look at some of the examples. So, this is my Kubernetes cluster running a LK logging stack. Now, this is the Kibana dashboard. It's important to have some prior understanding of ELK. Now, this video will not go into the details of elk or

[00:02:42] EFk stack, the focus is more on using logging and the Kibana visualization to identify issues in the cluster. So, these are the available logs in Kibana. I want to see the logs for just four R's. You can see the same.

[00:03:01] Select an absolute time. You can do all those selections. So, what I will do is I will do view the logs for just last four hours. Now you can see the logs that were there for the last four hours. Now, in case of centralized logging you can even ship system logs like dialog messages and few are the logs to

[00:03:23] Elasticsearch. So let's search for errors in the log. So, I just search for errors in the log and I see all the logs with that text. So, like this you can search for various messages in the log. So, let's create a

[00:03:43] graph visualization to get a quick look of inner counts for each pod. So, I search for error and what I see is

[00:04:00] when I wanted to save it's already saved. As you can see, the visualization comes in very handy to get a quick view of unhealthy pods which requires attention. So, let me show you how to create visualizations on the fly. Alright, so let's go to the visualize tab and select

[00:04:21] the type of visualization. So, let's select the vertical bar. Now, I already have a saved search so you can create a new search as relevant. Now I'm selecting the error logs, this is what you get. Now a vertical bar with total error comes. Now, what I want is error count against

[00:04:40] specific pods so that I know the unhealthy pods in the cluster and take corrective actions as needed. So, for this I need to select the bucket type for the x-axis so I select terms as the aggregation type, and for the field key I select Kubernetes pod.

[00:05:00] So let me execute it. So, now this is better. I will add a custom label to the x-axis to make it clear. So, now you see the pod with the most number of errors thrown in the log. So, the highest width count 549 is the service catalog API server, and the next highest is the moon

[00:05:24] - app and so on. So let's take another example. So, I have a saved search for out of memory errors and let's execute it. No logs are available for out of memory. Now let's

[00:05:40] take another example. So, let me get the current logs. So this is the current logs. Let's say I want to find out pods which are showing security related errors. So, I am searching for security in the logs. So, nothing interesting is shown.

[00:06:02] Ok, let's say you want to find out what's throwing warning. You can search for one text and create a visualization to get a quick view of pods requiring attention. So, I see the pod with the wand looks. I can create a visualization as we did

[00:06:22] last time for error counts. So, all these are made possible due to centralized logging and indexing of logs by Elasticsearch. So, centralized or cluster level logging comes in very handy when managing Kubernetes cluster in production. In the next video, we'll look at common issues related to logging.