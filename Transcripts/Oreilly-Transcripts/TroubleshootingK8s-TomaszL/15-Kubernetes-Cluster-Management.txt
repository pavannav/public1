[00:00:00] In this section, we'll look at options available for managing the Kubernetes cluster. Specifically, we will look at the Kubernetes dashboard, the Kubernetes CLI, and go through some of the available commands and its usages. So, before we go into the details of dashboard and CLI a quick level set on Kubernetes cluster management. So, what do you mean when you

[00:00:21] say managing Kubernetes cluster. So, there are two aspects, managing the cluster lifecycle and managing the lifecycle of Kubernetes objects like pods, deployments, DaemonSets, and so on. Now, Kubernetes cluster management is specific to cloud providers and we won't spend time on it in this video. We will look at managing Kubernetes objects using the dashboard and the CLI. So, what

[00:00:42] are the typical lifecycle operations. So, these are the typical operations. So, creating the objects, deleting the objects, inspecting or viewing the objects, modifying the object, and so on. So, let's look at the dashboard. Kubernetes dashboard is a web-based UI for Kubernetes clusters. Now, it allows

[00:01:00] you to manage your cluster, as well as manage the applications running in your cluster. You can perform the lifecycle operations from the web UI itself. So, you can follow the instructions in the link shown in the slide to deploy the dashboard in your cluster. So, many of the turnkey and hosted solutions, like OpenShift, Rancher, etc provide their own feature-rich dashboards so we look at

[00:01:22] some of this later on. So, let's spend some time on the Kubernetes CLI. kubectl is a command line interface for running commands against Kubernetes clusters. So, you can download the binary for your platform from the link mentioned in the chart. So, kubectl needs a config file with access details to the cluster. So, it looks for a file

[00:01:41] named config in the home directory, under the home dot kube directory, or you can specify why the KUBECONFIG environment variable. You can also set this wire the kubeconfig flag. So, take a look at some of the links shown in the slide to set up kubectl. Set up autocomplete to make your life easier.

[00:02:00] Additionally, take a look at the kit sheet which provides a good summary of the important commands. Now, as a bonus look at KUI as well, which is a kubectl plugin and provides a graphical view of the CLI output. So, in the next video we will look that ways to create and manage Kubernetes objects via the kubectl CLI.