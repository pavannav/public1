[00:00:00] Welcome back everyone. So far. In this course, we have designed complete CICD pipelines, worked with real applications, used reusable workflows, integrated Docker Kubernetes Helm and even scale the runners at this point. You already know how to use github actions.

[00:00:23] Now, in this section, we will go one level deeper and learn how to create our own github actions also known as custom actions. This is where you move from a github action user to a github action builder.

[00:00:41] A custom action is a reusable unit of automation that you define once and use across multiple workflows, repositories or even share with the community instead of repeating the same shell scripts or steps across workflows, you package that logic into a custom action

[00:01:01] and just call it when required using use this keyword as shown here behind the single line could be multiple steps doing setup validation, security checks or deployments. You might ask

[00:01:20] why not just write transcripts inside workflows here is why custom actions matter in real projects. They are reusable, you write ones use everywhere they are consistent, they make your work flows clean, they enable versioning for your workflows with improvements

[00:01:42] being done without touching your actual pipelines. They are abstract. The team who is using these actions need not to know internal logic and most important they help in open source sharing public actions that we can upload on github

[00:02:00] marketplace and then share it with the commodities in large organizations. Custom actions are often standard building blocks used across hundreds of repositories. Talking about basic architecture of custom action. Every custom action in github actions is defined

[00:02:22] using a single file called action dot YML. This file tells github what the action is, what inputs it expects, what it produces and how it runs. Let's understand all key sections using a very simple and realistic example.

[00:02:43] The first key section is name name defines the display name of the action that appears in github actions, log and workflow steps description. It gives a brief explanation of what the action does.

[00:03:00] So users know its purpose in the workflow, talking about inputs, they help you to define the parameters your action accepts from a workflow, making it configurable and reusable outputs help you to expose values generated by action.

[00:03:20] So they can be used later in the upcoming workflow steps runs keyword or section tells github how that action should execute. That means it's which type of action it's javascript based action or Docker or it's a composite action or composite based action that we are

[00:03:43] about to use using specifies the type of action being created such as load Docker or compose it then steps contain the actual commands and logic executed when action runs.

[00:04:00] It's same steps that we have seen earlier when we were defining our workflows shell, it defines which shell the actual uses to execute commands such as bash or powershell. Custom actions should always be versioned properly using tags like V one

[00:04:22] V one V two, so on and so forth. Every change in your actions should be tagged properly. It's not recommended to use main branch in production document inputs very clearly in your read me. So that your end users are clear how to use your actions and what are the inputs to be

[00:04:45] pass on actions should not have multiple things to perform. They should be simple, clear and focused. It's always good to add basic variations and error handling in your actions.

[00:05:00] A well designed action become part of your organization's CICD platform. Your actions could be private which are internal for company usage or you can make them public by sharing them on github marketplace. Many popular actions that we have used while working with the workflows like checkout

[00:05:22] Docker login and many other, they are custom actions published by other users or communities as open source actions, github actions support three types of custom actions. Let's understand each one by one. First are javascript actions.

[00:05:41] These actions are written using no Js and executed directly on the runner. They make execution faster, they reduce container overhead and they are majorly recommended for A P A calls validations or metadata processing.

[00:06:01] You can typically use them when you're calling some github apis for automation. Some pr validation, you're doing some version parsing for your releases or some metadata checks the key files that we have to maintain for these kind of actions are action YML index dot Js

[00:06:21] and package JSON. We will understand and see that how to create these files when we will get into the demonstrations and will create couple of actions by ourselves javascript based actions are very popular because they are lightweight and easy to maintain. The second type of actions are Docker actions,

[00:06:43] these actions packaged everything inside a container image, they work consistently everywhere they support almost any language or tool. They are idle for your complex environments. Typical use cases for them are security scans, C

[00:07:01] based tools, multilanguage utilities or some heavy dependencies for Docker actions, key files again starting with action ML. But here we don't have any index dot Js. Instead we have Docker file and an entry script which replaces package dot json of your javascript based action.

[00:07:24] Docker action provide full control but they are little bit slow due to image build and startup process. Third and most commonly used actions are composite actions. Composite actions allow you

[00:07:40] to combine multiple workflow steps into a single reusable action without any dependency on javascript or Docker. They are easy to write. They are just plain yamls. Perfect for DevOps. Teams help in standardizing your

[00:08:01] workflow processes. And they are the most commonly used actions across multiple organizations. You want to do a tool set up, need to do a login or cloud providers. You would like to put a logic for your build test and push or you want to standardize your C steps.

[00:08:20] Composite actions are the best choice for that. You can treat them as you are creating the internal platform standards. Then they are the best choice for you. We should use a workflow or we should create an action first and then use it in our workflow.

[00:08:41] We should decide on that. You can create a custom action. Well, the logic that you are using is used across multiple workflows, the scripts that you are putting within your logics there by becoming repetitive. You want to enable

[00:09:00] platform level standards across org or teams, you are building an enablement layer for your teams to make use of github actions, easy, fast and standardized. These are a couple of scenarios you should create

[00:09:21] and use custom actions. Typically, if you're copying same steps into more than two workflows, it's a strong signal to create a custom action. The real power of custom action is that a complex logic lives inside the action while workflows remain clean readable and easy to maintain.

[00:09:43] That's exactly how platform teams standardize CICD at scale. That's it. For the theory. In the upcoming lectures, we will try to create our own custom actions Thank you and I'll see you in the next lecture. So.