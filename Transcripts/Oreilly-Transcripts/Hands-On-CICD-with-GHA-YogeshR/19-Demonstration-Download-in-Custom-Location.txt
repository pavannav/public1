[00:00:00] Welcome back everyone. So in the previous lecture, you have seen that how we can download the entire directory as an artifact in a job. But let's say if I would like to download my artifacts in a different folder, not in my current directory, how I can achieve that? That's also possible in github

[00:00:22] with the help of another input variable that we can pass on with our action called as path. So let's check that and see that in action for this. Let's again, edit our profile, thats open edit.

[00:00:43] Let's scroll down. I will leverage this number three because most of the things would remain same. Let's copy. Let's put it here will change job id to four job name as well. Become four. It again means job one runs on

[00:01:02] to latest. We check the current directory. But before that, I will create the custom folder where I would like to download my artifact. So for that, I can put, let's say creating

[00:01:22] custom folder for download, run. I'll show you both single file and directory download. So I'm going to create two directories here, mkdir

[00:01:40] hyphen P single five and MKD hyphen P download directory. So this will create two folders. When we do a SI L idea, these two empty folders should be visible to us. Soap.

[00:02:02] When we go here, it's a single, it's a download directory just to make it for the single one as well. I just copy this and I'll put this step as well. Here. Download single file, artifact,

[00:02:20] single file is the name of the artifact. And here we will hide path that I would like this to get downloaded inside this folder. So it will get downloaded inside this folder. Same way here. Also, we can act popt

[00:02:40] which will be download directory and to ensure that actually a got downloaded, we can put LS and LT and we can add single file and same thing, Ls hyphen healthy within our current folder that we are in.

[00:03:01] Just to create a segregation between the two. We can add one heo which is checking single file directory, echo checking directory artifact. So that way it will download

[00:03:20] my artifacts in the custom folders. The folders that we have already created are define. Let's roll up, let's commit updated to download artifacts in custom for this and Lex

[00:03:40] Comic, this will trigger the action again for us. Let's go to the actions. It's in progress. We have now job one which has three depending jobs, job two, job three, job four, job one is done. Let's wait for other three jobs to get completed.

[00:04:01] We will be focusing on job four. Let's click on zho it's done. Let's check. First, that custom folders got created or not. It has successfully got executed. Made the single file download directory. Let's check whether they are available or not.

[00:04:20] You can see that we have two folders, single file and download directory. First, we are downloading this single file. This single file got downloaded. One artifact got downloaded in directory. Also one artifact got downloaded. Now when we

[00:04:40] do the LSF and LC, the data available in our folders, we can see my artifact dot txt available in this single file folder. And these two files available in the second one that is a download directory. So that way you can change or customize your folder where you would like to get your

[00:05:00] artifacts downloaded in the next job execution. You want to do some setup for your test execution, which identifies which got generated in the previous job. Those test execution data needs to be placed in a specific folder. So you can do that with the help of this customization and it would be helpful for you for your next job execution.

[00:05:22] That's it for this lecture. And I'll see you in the next. Thank you.