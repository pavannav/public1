[00:00:00] Welcome back everyone. Till now, we have understood that how we can install the github runners, how we can delete the github runners and how we can use a guitar runner using a single label. But in many scenarios where you have multiple self hosted runners with different label

[00:00:23] and they are being installed for the different needs. In that scenario, you may need to configure couple of labels to filter out a specific runner for your need. In this lecture. We will see that how we can put that value for that. We will take

[00:00:41] two labels. For example, we have this github runner one which has a label called as self hosted Linux X 64 nodejs and Python. So what we can do, let's say I want to pick up a runner which has a label, self hosted as well as Python.

[00:01:03] Again, when you put multiple labels to select or pick up a runner that will become an ad condition. So github will search for a runner which carries both label, not just one label or either of them, it should have both labels available.

[00:01:22] So let's proceed. It's very simple to do that the way we have earlier defined runs on self hosted. It would remain similar. The only difference is you will cover up this in square bracket. After self hosted, you will put,

[00:01:43] then you put another name or another label and then close the bracket. If you want to have multiple labels, more than two labels, same process would be followed, let's say X 64. So that's another label that we have.

[00:02:02] So that way this will get picked up this Python and this and it will be and a condition. So let's commit this

[00:02:21] and updated runs on with multiple labels, let's commit. And, but this is also a change which will trigger the job.

[00:02:40] Let's go back, let's go back to the extra. It would be done by now, this is about it's completed. Now, demo chop two is the one which is using the self hosted runners. Let's expand that

[00:03:01] whose name is again, we have run runner name is Guitar runner, one so on and so forth. So it will get paid. So that's how you can use multiple labels to pick a runner for a specific need. That's very useful

[00:03:21] because most of the time self hosted runners are installed for the specific needs like special operating system or some license software which may or may not be available on the github hosted runners, which is only a reason of leveraging self hosted runners. So this kind of configuration would help you out to pick up

[00:03:43] a specific. So that's it for this lecture and I'll see you in the next one. Thank you.