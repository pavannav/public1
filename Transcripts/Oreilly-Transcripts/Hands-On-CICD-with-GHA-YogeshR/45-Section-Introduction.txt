[00:00:00] Welcome back everyone in this lecture. We're going to explore one of the most powerful and time saving features in github actions that is reusable workflows. So far, we have built individual workflows for different purposes like running tests, linting or deploying our codes. But what happen when multiple repositories or multiple workflows inside the

[00:00:24] same repository need to perform the same steps again and again, that's where reusable workflows come to the rescue. So what are reusable workflows? Let's start by understanding what reusable workflows actually are a reusable workflow is just like a normal github

[00:00:42] action workflow. But instead of being triggered by push or pull requests, it's being called by another workflow or workflows using the special keyword workflow underscore call think of it like a function in programming,

[00:01:01] you define it once and then you can call it from different workflows or even from other repos again and again, it helps you maintain consistency and avoid repeating the same logic everywhere. Now let's understand why we should use them as your project or organization grows.

[00:01:21] You might have multiple repositories like Frontend back end, different repositories for your APIS or Microservices and all of them might need similar workflows like running linting or unit tests, building Docker images, deploying the code or applications to cloud environments so on and so forth.

[00:01:41] Without reusable workflows, you would be copying and maintaining the same YAML files across different projects or repositories and that will quickly become a mess. So reusable workflows helps us in maintaining centralized common logic, reduce duplication, simplify maintenance

[00:02:01] and enforce best practices across teams. When we talk about reusable workflows, some benefits of using reusable workflows are they help in defining standards for linting testing, deployment or different steps or processes across projects

[00:02:22] and make them consistent. They are efficient as you define a workflow once and call it multiple times. They are easy to maintain as you update the main workflow once and it reflects everywhere they help in managing common secrets or environment settings or environment variables

[00:02:40] at a central place. They are easily scalable and makes them perfect choice for organizations which are managing tens or hundreds of repositories. Let's look at an example to make it more clear. Here is a reusable workflow defined in a file called, let's say github workflows.

[00:03:01] VML. Notice here that instead of on call, push or on call pull request, we are using on colon workflow, call this workflow won't run automatically. It has to be called from another workflow. We can also define inputs and secrets just like function parameters.

[00:03:24] Now let's see how to call this reusable workflow from another workflow. Let's say github workflows main dot YAML. This new workflow looks more neat and clear in comparison to the first workflow that we

[00:03:43] have created or the last workflow that we have created. While setting up our Python application, we are using the keyword uses colon to point to the reusable workflow either within the same repository or even from another repository. Like

[00:04:00] we can put uses colon the name of github organization repository name github workflows, name of workflow file at the rate name of branch from where you would like to call it. That way it will become quite easy to maintain different versions of same workflow if required

[00:04:22] when you're adding a new feature or a new test step or might be a new job which need to be tested. Before merging to the main reusable workflow. We can use reusable workflows for common build and test workflows which are shared across multiple services,

[00:04:41] centralized security scanning like SASS for dependency checks, unified deployment processes to multiple environments like dev staging production standard core quality checks, like sonar analysis, linting formatting, etc cross repo integration testing before merging major features.

[00:05:03] Basically any repeated process. A cross repository is a perfect candidate for using the reusable workflows. In last to summarize reusable workflows, let you define your automation logic once and reuse it

[00:05:20] anywhere they help you standardized, simplify and scale your CICD pipelines. They work just like functions in programming with inputs, outputs and secrets in upcoming lectures we will implement and use reasonable workflows from scratch and try to call them from multiple repositories.

[00:05:41] So you will get to see how it actually works in real time. That's it for this lecture and I'll see you in the next one. Thank you.