[00:00:00] Welcome back everyone. So in the previous lecture, we have customized our reusable workflow and make it more generic and it would allow us now to call different jobs based on the inputs being provided by us. Now, let's see how it would work.

[00:00:21] The workflow that we have updated. It has now couple of inputs being defined under the workflow claw which includes run unit test, build code, keyable, run sonarcube, run so on and so forth. Out of these six different inputs, only two inputs have before defined as true.

[00:00:41] That means by default, they will get executed if users will not provide any input to the same. So let's see what would happen when we call this reusable workflow. After making these changes, let's go to our application workflow

[00:01:00] in this. Now, we don't have any change. I don't want to make any change as of now, let's do a simple edict and let's add some lines to it. Let's say uh it seems this push and main branch. That's actually that what this is. This is just a comment I have already just to show you what would happen.

[00:01:25] Let's commit updated to run the workflow with updated

[00:01:40] usable workflow. Let's commit the changes and let's wait for some time. Let's go to the actions and let's go here. It got started. You can see that one job is coming as completed. But the sign here shows that it got skipped.

[00:02:08] This shows that skipped. When I click on that this job was skipped. You can see here these jobs got skipped. But now the point here is

[00:02:22] run grid test was being marked default as true. So it got executed. But we also have build image defined as true. Then why it didn't get executed? Let's understand that for that, we have to go back to the reusable workflow

[00:02:42] in the I reusable workflow. If we scroll down to our build job, it has two factors which are getting into consideration to run this job. First is condition that when run bill is true only then we have to

[00:03:00] run this option. That's the first condition and second factor is needs, it needs unit tests, Sonarcube and codeql to get executed only then it will run. So that's the problem because we have skipped Sonarcube and codeql.

[00:03:22] That is the reason being marked as true. It still got skipped because the upstreams are not getting executed, getting skipped. So that's the reason that bill job is also not running. Now, let's see if we want bill job to run what we have to do.

[00:03:40] Let's go back. Let's click on the workflow file and let's edit. Now, we can update those inputs by adding a new keyword with by default, your test is true. So I'm not putting it over here,

[00:04:02] but we need run codeql as true. So I want codeql to get triggered and I won't run sonarcube as true. So I have marked these two as true.

[00:04:21] Let's see what would happen this time when run code QL and this will also become true. Let's scroll up, updated to run codeql and Sonar scan.

[00:04:41] Let's commit. This will trigger the another workflow arm. Let's click on the actions. Let's refresh, it's in progress. Now let's click this time. You can see that unit test is running because it's a default marked as true.

[00:05:03] And this one is also running, which is our codeql because now we have explicitly mentioned or pass it on as an input that we want codeql to get executed. So it's running and as Sonarcube is also being marked as true from the input. So it's also running now but it was dependent on Sunita. So it took some time for

[00:05:24] Sonarcube to get started. Let's wait for these two to get completed. That is code Qn and Sonar because they both act as a dependency for a build image along with Unitest. Unitest is already completed. So that's fun.

[00:05:41] Let's wait, code is completed. Now, Sonarcube also got completed and now build image automatically got triggered because the default value for build or build image was true. So as soon as all the conditions got matched, it got triggered.

[00:06:01] Lets wait for this to get completed process is exactly the same. The way we have created the workflow that would remain exactly the same. The only difference is in a behavior or the calling, we want all jobs to get triggered, then we have to put all of them as true,

[00:06:20] excluding the ones which are default true, or we can put all true as well to make it easy to understand. And the rest of the things would be straightforward definition configuration. If conditions that's all depend up to you that how you would like to proceed with this thing. Let this be completed. Let's check it got completed.

[00:06:41] Let's expand that you can see that four jobs got completed and these two jobs got skipped because they are default false and didn't mark them as true while running the workflows. Let's edit the workflow again. Now we have built image by default as true. If we want to make it as false, it's the same thing we can do.

[00:07:03] We can edit the workflow and we can put the condition run, build as false. So this, we are making it as hard faults that by input, we are telling that you should not run it by default, it will run.

[00:07:22] Now we are saying you should not run it. Ah I forget to add the message but that's ok. It will again trigger the job let's go back to the actions

[00:07:40] and see what's happening. Workflow is in progress. It will again run same C pipelines with the unit tests and codeql, they will run in parallel unit tests are dependency for Sonarcube. So Sonarcube will get executed when this is successful. It's done. Sonarcube got started.

[00:08:03] Now we have this dependency bill job having dependency on three this time three got executed. But we have explicitly mentioned that build run should be false. So you can see that it got skipped. That way you can easily leverage a single workflow

[00:08:23] with different conditions and can easily be used with multiple repositories or multiple application workflows with an ease that will allow you to reduce the number of time you're writing the same code or same workflow, same steps again and again

[00:08:40] and help you out too easily scale your setups as and when needed, you want to add something new, just add the main workflow or the reusable workflow and easily get it called from the different jobs if required. So that's how the reusable workflows would work and help you out to manage your,

[00:09:00] get our actions in a more efficient and scalable way. That's it for this lecture. And I'll see you in the next one. Thank you.