[00:00:00] Now let's update our workflow to create a health chart from the files that we have added in the charts folder. Let's head back to the VS code. We have files available in this charts folder, lets enter name and uh

[00:00:20] create package and what we want. We want it to run. We can simply set it up as helm package. So this command will create the package where we need to create the package charts. I want the system to process the charts folder

[00:00:40] and then create the package. So this will create the package for us next command that I'm adding just to show you that package got created by typing simple LS hyphen LTR in the current folder so that we can see that yes, the package is available. So it's done added step two, create health chart

[00:01:03] that helm chart is being called as a package. That's why I've kept the name of my step as create package. So helm packet charts LS LFL to list down that package for us, let's commit and push. Let's go back to the actions its in progress,

[00:01:21] set up, check version linkt does the create package step? It got created. And in the bottom, you can see that a package got created. So that means now our workflow is ready, almost ready and it's linking the files and then it is creating the package.

[00:01:43] Now see if you are able to update the workflow and add a step that would update, I would like that step to update this values dot ML. So before creating the chart, I want my values do ML to get updated with the version

[00:02:00] so that it will always have the version of the image that we have just published. Let's see if we are able to do that or not. That's it for this lecture. And in the next lecture, we'll try to update and add this particular step. Thank you.