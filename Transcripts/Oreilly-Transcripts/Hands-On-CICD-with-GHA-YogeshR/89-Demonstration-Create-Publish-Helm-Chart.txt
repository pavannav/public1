[00:00:00] Welcome back, everyone. Now let's proceed and update our workflow to publish the health chart to our github container registry, which is also one of the OCI compliant registry as we are publishing the artifact or a package. So we have to do the authentication first,

[00:00:21] you already have access to write the packages. So this would work. Let's add the new step name, log into GHC. I'm just putting here as OCI that it's OCI compliant

[00:00:40] and in order to perform the login, it's a the industry we are login to. So callin echo. So what it's doing, it's echoing secrets dot github token. That's a token that got a sign for every workflow execution. And then we are proceeding helm registry

[00:01:00] login to GHC dot I hyphen U user is the current user and then password is coming as in standard input. Then name push helm chart run. We have created help, push thinks

[00:01:20] star dot PG, whatever the version is to this location, which is OC colon slash GHC dot IMA, that's the name that I have and dash charts. We can use this method and update the value here if required

[00:01:42] that this particular specific thing needs to be updated so we can do that as well. Let's copy this and update it over here. So helm push thinks we are publishing the version. So it comes out to be dollar curly races version. So this is something which will get published.

[00:02:02] It's a best practice to do a logout. So logout from GHC and registry logout pass the simple command that we need to execute. So this will log into the data container registry, which is one of the OCI

[00:02:20] compliant registry as well where we can publish our health charts, then it will publish the health chart and finally, it will do a logout. Let's update and let's commit added. Step two publish Haha in

[00:02:40] GHC. Let's go here, commit and push. If all is good, it should get published. Let's go to the actions, it's in progress, it got logged in pushed and then logged out. Let's take a present. The health chart is not the correct one. That's completely fine.

[00:03:01] Let's go to the packages and here we can see that package got published its health chart. Think nextx app when I click, that's the reset version or the most latest version that we have published approx one minute ago. So now our workflow, it's linting,

[00:03:21] then creating and then finally publishing the hand charts to the github container registry. That's it for this lecture. In the next lecture, we will update our workflow to run in a complete sequence starting from a CI pipeline ending onto the he chat publishing. That's it for this lecture. And I'll see you in the next one soon. Thank you.