[00:00:00] Welcome back everyone. So now we have the scale set controller deployed inside our CBA is cluster. So let's proceed and try to deploy are a skill set for us. As mentioned in the documentation, we have to configure it on your terminal

[00:00:21] as mentioned over here. So let's start, that's the command that we have to use, but we have to understand the requirement. We have to update the installation name value very carefully as it is the same value that we will use going forward as a value or an input value for our runs on

[00:00:40] that will let the system know that on which runner we have to run up jobs then name space where we would like to deploy it. Config you are in this URL for your repository organization or enterprise as runner scale sets are also capable to be available at org level.

[00:01:01] Earlier, the runners that we have seen they can only be used at the repo level only. So that's the configurer at what level you would like to configure it, then you have to set the github pad token with repo as well as admin org scope for repository and organizational runners.

[00:01:25] So because we're going to use these runners at our level as well, so we need to enhance the permission scope for our path. And then below is the example that how we can leverage it. So let's start by granting additional permissions.

[00:01:41] So that's the github action demo. Let's click repo we have the access, it's already there, but you have to ensure that you have the admin or excess enabled for your pack token in order to use the scale set and get them deployed at the odd level.

[00:02:00] Let's update the token token value would remain the same. I still have that in my notepad. So I would be leveraging the same. Let's go back installation name that set up the first variable. You can directly update in the command as well, but it's always good to deploy it as a variable for easy to manage

[00:02:24] installation name, what name uh Let's give the name as get her actions demo. That's the name of the installation or let's keep it at github actions scale set demo. That's the name of installation name space where you would like to deploy it

[00:02:43] for this. Let's check which name space we have used in the past cube CTL, get namespaces. So we have only used ARC system. Let's do one thing. Create a new namespace cube CTL.

[00:03:01] Create no need to create the put name space equals to a is that it and skill set. Art colors, skill set

[00:03:21] Ben config L in this, I would be adding my or URL which will be github.com/ Kols Andme. Then

[00:03:40] Pat Token, I am copying the token that we have generated earlier. It's done. And now we have the command installation name. When you have a name space, create a name space. It doesn't exist, it will create a name space. That's why I've skipped set git config URL, that would be the URL

[00:04:03] secret would be added. And then the scale set helm repository. Let's copy this command and let's spin it off and it's done. Let's check the status

[00:04:21] helm list. We have three. This is the one which is recently got created as a name space and that's the scaleset demo as a scale set name as our application name. So let's check if all the required qualifications are available or not.

[00:04:40] Qubec CTL, Getall hyphen N all colour scale set, no resources are found in this. That's completely fine. It's saying that you should have this configuration ready. So we have already checked, let's quickly have a look one more time,

[00:05:06] Helm list hypen A. So we have this skill set deployed. Now, let's check if we have a runner set listener available or not, which was not there in the past.

[00:05:21] Let's copy QC TL get pods hypen. In, in our case here, you can see that we have the getit have action scale set demo, this listener now available

[00:05:41] and it's in the running state that's what they are suggesting. That's the listener name. We have changed the name. That's why the name is a little bit different, but that's completely fight. So we have the scale set now being deployed. And in the next lecture, we will see that how we can use this skill set.

[00:06:04] That's it for this lecture. And I'll soon see you in the next lecture. Thank you.