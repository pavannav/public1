[00:00:01] Welcome back everyone. So now we have understood that why we should use the custom images in our runners. So let's try to build one for a Selenium test execution starting with I am currently in my reusable workflow repository

[00:00:20] in that repository. I will add the required code for building a custom image which includes Docker file, a workflow to build that and some dependency files. Let's start with that. I'm keeping that code in our main branch only. So let's start,

[00:00:40] let's add a new file, create new file. I would like to maintain all the required files inside a Selenium runner folder with the name Docker file. I have already created the content of file that which is this. So it's the same content that we have used earlier

[00:01:00] while we are doing the manual or the installation every time within the job itself. So our image is based on the action runner. Latest image, the image that is currently being used by a scale set runner. We're using the same image as a base. We're switching to a root user and installing all Python related packages we are installing here

[00:01:23] once done. Then we are installing Google Chrome, the stable version of Google Chrome, we are installing, we are initializing our virtual environment, setting up the path, creating a workspace to copy our requirement TXD

[00:01:40] that we are about to add in our repository as well. And then we will run the pip install to get the requirements or the dependencies mentioned in the requirements dot get installed. And then we are again changing our user to the runner, which is a user being used by

[00:02:00] action runners. So that's the Docker five. Let's commit once done. Next thing is a requirements dot TXT. So let's add another file which is requirements dot txt. It's the same content that we have maintained in our requirements

[00:02:24] test dot TXT, it's the same content. Let's commit this file. So these two files majorly act as our code. So let's add a workflow that will help us out to build

[00:02:41] an image and then push the image in the github container registry. Let's go back to the reusable workflows, root folder, go to github. What loose add a file, create a new file. Let's name it as

[00:03:02] Bill radium runner dot YAML. And starting with name C pipeline to be slim runner custom image. Then so if there is any change in this file only, then I want this file,

[00:03:22] this particular workflow to get triggered BUSH, we can keep it all branches if we want, let's keep it all branches. But here I am adding paths and I want this to get triggered when there is any change in

[00:03:41] Selenium runner folder. So if there is any change in the Selenium runner folder, trigger this job and then github slash workflows slash, that's the name of a file.

[00:04:00] So if there's any change in this file, then trigger it. So this will get triggered. This workflow will get triggered on push on any branch, but specific to these two particular paths, then we have a simple set up. So let's add the permissions. We need two permissions here. For contents, we need

[00:04:22] read and for packages, we need write as we need to publish the packages to the github container registry that Synta is wrong. So let's update. It looks fine. What's the issue here?

[00:04:40] But branches and paths look OK to me. Yeah, path is also bad. So this is the correct flow. So now we have the setup ready. Let's start with the job edition jobs. Let's name it as build

[00:05:01] and bush name build and push Selenium runner image. We can build it on simple two so we can keep it runs on open to latest

[00:05:20] runner spelling is wrong. Let's fix it. Now, starting with steps, check out code here. We will use actions that is actions,

[00:05:41] check out at the rate B five. That's the version once checkout is done. I would like to build a name, name for my image name. Let's keep it as Docker maria.

[00:06:00] It will use Docker metadata action wifi version with these inputs. So we are keeping image name as GHC dot I that's a github container history then repository which includes VME reusable workflow. And then Selenium line is art

[00:06:20] image name. We need to use the tags as well. Let's start with that type raw. Well, let's keep it later. So here we are not doing any calculations. We're just keeping it simple.

[00:06:41] Next thing that we have to do, it's very simple as we are done with the metadata. Let's complete our login process name, log in to GHC. So for that, we are using Docker login action version three registry is GC

[00:07:00] dot IO github vector. The one that is triggering and secrets is github token that permission we have already added over here. Next thing we have to set up the Docker environment. So let's proceed with that by using the Docker setup, build X section. And finally, we will add this step to build and push the image.

[00:07:20] So Docker build and push image Docker build, push action version six context is Docker Selenium. That's not the correct one. That's the correct one. So we have all the data available in the Selenium underscore runner folder. The file to be used is Selenium underscore runner

[00:07:41] Docker file. We have to push these are the tags and these are the labels cache from github action and cache to github action mode is max. So that will make our workflow ready to build and push Selenium runner custom image to our github container registry.

[00:08:04] So that's commit added c pipeline for building Selenium runner image. Let's commit and see if it's able to build and push it successfully or not. Let's go to the actions.

[00:08:20] There is a bill in progress. Let's check. Login is successful. Setup is in progress. Build is getting published. We're getting an error. Let's see, the error fail to build tag is needed when pushing to the registry. That means we have missed something as A T

[00:08:46] let's check our file workflow file. We have fast all tags but why it's not getting picked up

[00:09:01] steps, meta outputs tag. We have put adding the id here because it's picking up the id from here steps dot meta. You forget to add it. Let's go back, let's add it.

[00:09:22] We're calculating this stuff here. But here we forget to add the idea that we're going to liberate we're using in the doubt that's meta. So tags raw equals to raw. This tags type is raw value is latest.

[00:09:41] We are not calculating any labels so we can get rid of this as of now. So we are ready. Selenium Runner file is this pushes through and let's try to commit and commit the changes,

[00:10:04] check the actions again in progress, set up. It's now about to start building and bush, it's failed again.

[00:10:23] Path Selenium runner not found it's wrong. We have kept it Selenium dash runner. So let's put it that way,

[00:10:40] let's edit that. But lest everything is fine. Let's commit, let's commit the changes.

[00:11:02] It's in progress, login is successful, will set up is completed. Bill is in progress now. So image is getting built up.

[00:11:24] So till that rabbits building, I have already opened this package section. So once it's successful, a new image should start, should be available over here.

[00:11:40] It's installing all the required packages and dependencies which are needed. What we were earlier doing as a part of job, we have moved it to the custom image. No, it's pushing it now

[00:12:04] looks like it's successfully pushed. It's green. Let's go back here. Let's refresh and we can see that Selenium dash build is available. If I click, we have the latest version available.

[00:12:21] So that way we have completed a Selenium runner build creation, custom build creation and it's available now in github container registry for usage. We have understood how we can build it up. We have done couple of mistakes, we have fixed those mistakes just to show you that how things could be,

[00:12:41] things could look easy, but there might be something that you may miss. So that's it for this lecture. In the next lecture, we'll try to update a scale set runner definition that we have to use this build for all future pod creation. That's it for this next and I will see you in the next one. Thank you.