[00:00:00] Welcome back everyone. So now we know how we can leverage and use self hosted runners in our workflows, Lizzie, you want to delete a from your system. So there are two ways that you can do that. First,

[00:00:22] you can simply go to the runner's page depending where you have installed the runner at repository level or org level. Go there. You can click on this and click on remove rather. So it will ask you to remove the runner from here. You can select and then get the runner removed.

[00:00:42] If you want to remove the runner in a proper way, you can get inside the runner, click on remove, then insert this value and confirm it will give you the option for removal.

[00:01:00] So you can copy this command and then execute this command from your runner machine and get it removed gracefully, you want to do a force removal. All you have to do is just click on that. Enter the details like what's the runner name so on and so forth. The runner will get removed

[00:01:20] in this lecture. We will do it using a command. That's the command. Let's copy, we are removing github runner two, which is running in action runner one folder. So thats open the machine. We are in action runner one, which is Guitab

[00:01:40] runner one, we have to go one step back CD this and this is the command and let's run the command. This command will do a graceful removal. So runner remove successfully, remove all credentials and

[00:02:00] dot runner file. If I'll go back and I'll go back to the runners, you can see the runner is no more available in the runner sections. Once it is deleted, you can simply go here and do a clean up

[00:02:20] by executing this come up so that nothing will be left in the background. That will do a complete removal. So that's how you can easily remove the runners from your github repositories or github organization deletion. And installation would remain same

[00:02:41] for odd level or repository level. Only their availability will be different when you install at repo level. That is specific to that repository when you install at org level that is available for the entire organization. That's it for this lecture.

[00:03:00] And I'll see you in the next one. Thank you.