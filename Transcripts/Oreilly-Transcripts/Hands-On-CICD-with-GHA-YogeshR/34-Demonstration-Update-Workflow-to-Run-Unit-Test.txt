[00:00:00] Welcome back everyone. So our branch is ready with a simple application code. Let's now modify our workflow so that we can start adding the jobs or steps specific to a Python based application for that. Let's go to our workflow

[00:00:24] added the fine to start with. I'm just deleting everything that we have added under the jobs. In fact, I'm removing the environment as well as of now as we don't need them. And let's start with the jobs. So let's add jobs.

[00:00:42] So we are writing first job. This job is used to run the unit test cases. So I'm typing it as unit guests, then name

[00:01:00] what would be the name. Let's keep the name as running unit tests runs on seem one to latest and then finally, we will start adding the steps here. So this time we're going to add a couple of steps. The first step that we're going to add,

[00:01:22] it's what check out because now we need our application to get checked out before we start running the unit test cases. So what's the first thing name? The step would be check out code for this

[00:01:40] uses, we will use the action here, we can check, check out that's the action and lastly worship. So let's copy. So this win, complete the checkout for us on our,

[00:02:01] get to have it on a the runner which will run on the Ubuntu latest machine. Once checkout is completed, I have to ensure that Python setup is ready or Python should be available. So I'll put name as set up

[00:02:21] Python when use this, what I want to use or what I want to do. I want to set up the Python. So we what we can do, we can go back and we can search for set up.

[00:02:42] Python should be there here again by action. Let's click. It's with version 6.0 0.0. Let's copy and let's put it here. So here you can use

[00:03:01] this particular variable or this particular input if you want to install a specific version of Python. For example, let's say I want to install Python 311 or three. So I can copy this. I can press enter, I can type with and with what with

[00:03:21] Python version? And then I can put in a single body. The version, let's say I want three or 10 version of Python to be configured or set up. No, this way, download the code this way, set up the Python for us. Once Python setup is completed, we will install the dependencies that we have added in the requirements. Do

[00:03:43] TXT to run our application. So what we can do, we can add name install dependencies.

[00:04:00] Now, for this, we have to execute the command so we can't use uses. So we will use Ram. Generally, let's start with Python hyphen M install and let's upgrade the pay portion.

[00:04:20] If you're familiar with Python, this command will be clean command. So it will basically update the pip version. And later on we will use PEP to install are requirements using the requirements are TXT. So this will install the dependencies for us. This will download the code, this will do the setup. This will configure the

[00:04:43] or install the dependencies and finally name then you will test and simple one and we can type P test hyphen V. So this command will run the test cases for us. So now we have the changes.

[00:05:01] Let's commit, added job to run unit tests for I have let's commit

[00:05:20] this commit in depth will trigger the new workflow run to make it easy. All it's a take on the actions. It's in progress. That's our first job running unit tests. Let's click, it's completed.

[00:05:41] Check out will download the code. You can see that determining then it is executing the download and this is the shaft or the latest download lead is worship. It has configured the Python

[00:06:00] installed version 3.10 0.18 version got installed. Then it has installed the dependencies for us. All the dependencies and sub dependencies will automatically get installed. Finally, it is running the unit test cases for us.

[00:06:21] In our Testscore app dot py, we have four test cases, test home test, get task test, add, task test, add task without title. All four test cases got passed and it's a success. So that way we have started configuring our workflow, put a Python based application

[00:06:41] and it's ready in the upcoming lecture. We will see that how we can generate a report for this unit test cases and then upload that report as an artifact for a future reference. So I'm leaving you with this thought, try to implement it by yourself.

[00:07:00] We will try to do it together in the next lecture. Thank you.