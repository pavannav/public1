[00:00:00] Welcome back everyone. In the previous lecture, we have seen the use of legacy runners using the runner controller. In this lecture, we will deploy the runner scale set controller in our cert cluster and see that how we can use it to create the

[00:00:20] auto scaling capability within our action runs. So as earlier, the documentation that we were reading for the quick start guide, we have a warning that this documentation is for the legacy mode of arc. For the new autoscaling runner scale set, you have to go to the github

[00:00:40] docs available app this location. So let's open this document as a prerequisite. We should have a cluster and health three available. We have, we have both in place. Let's go down, add your hand chart, update the name space value to the location where you want your operator ports to be created.

[00:01:03] And the below command will install the latest version. We can use double hyphen version to deploy a specific version of the repository. We can use double hyphen version to deploy the specific version of a Turner skill set controller.

[00:01:22] In our case, keep CTL get name space. So that's the name space where we have deployed our previous uh legacy mode arc. So let's set up name space

[00:01:40] equals to this. Then let's because we know that it's not available so we can use and install and name. Let's keep the name as Ark scale set. Let's give them a name like this or we can keep it

[00:02:01] a skill set is fine because actions are a controller, skill set. Then the name space where you want to deploy this create name space if doesn't exist, we know that name space does exist but still.

[00:02:24] And that's the repository from where we need to download the chart. And let's hit enter, we got the output that it's successfully installed. Let's check

[00:02:40] he list. So that's the runner controller and that's the arc or the external controller scale set, which is now deployed. Let's check if all the required pods are running state or in the running state or not get

[00:03:02] pods hyphen N. So we have our scale set, github action runner scale set controller. That's the pod which is in the running state. So that means our controller is successfully installed.

[00:03:22] Next, we have to configure the scale set, right? So now we have the controller up and ready. And that's it for this lecture. In the next lecture, we will see that how we can configure a skill set runner the way we have earlier deployed the runner deployment.

[00:03:43] Same way in this case, we will deploy the skill set for our run. That's it for this lecture. And I'll see you in the next one. Thank you.