[00:00:00] Welcome back everyone. And let's start with the process now to build our custom action of type Docker for that again, we'll go back to our VS code and start adding the content one by one. We already have the content for the base image so that we don't have to build that image or install the packages again and again,

[00:00:20] whenever there is a change in our action until there is a change for the version. So for Docker type custom actions, we need two major things. First Docker file, which will help us out to create the Docker image that we would be using in our action

[00:00:41] and second an entry point which is basically what would happen when that action got C So to maintain them separately, we will add a new folder. Let's keep the name as

[00:01:00] action within that. I would be adding my Docker file that is Docker file and this particular file will be based on starting with from Jsr dot IU slash.

[00:01:20] We can take it from the from here. Let's go back to the helm base. It's based on this. So let's update this, that's the base repository. We already have all the data available in that. So we don't have to be worried

[00:01:47] to go away with this. We can add some specific version like V one V two as of now. We're keeping it latest. Then we'll start with what the Yeah, we are keeping it app.

[00:02:00] You can keep it github app or github workspace or whatever the name that you would like to keep, we would be copying the entrypoints. That's the actual script that will get triggered.

[00:02:21] We need to execute it or we need to make it as an executable. So we're changing the permission and finally, we are adding entry point which will be nothing but entrypoints. So that would be

[00:02:40] Docker file for our custom action. So that's the Docker file, which is ready. So for that, so now we need this entry point. So I'm keeping that also in the same folder. So I'm adding one more file here with the name, entry point, what this file will contain as a content.

[00:03:02] Let's try to understand that initially it would be same, it's a script. So it's be bash set. E that's the regular shell script, then we will add a couple of variables. Then what else we need to do

[00:03:20] for that? I'll go back to my existing workflow. So that's the workflow that we are trying to automate. Let's copy this entire stuff.

[00:03:41] I'm just keeping it here as a temp for discussion. Let's put. So from this, this would remain there so we can't change it. Let it be said, install BIQ and check. We don't need these anymore so we can get rid of this.

[00:04:00] They are the part of our base image. Now, the next thing that we have to do now, so let's start with the entry point out creation. We'll start with the basic ingredients which includes a

[00:04:20] user been in with bash and set by fail. Then we will add or define what would be coming as input from R action dot el. So when action dot el would be calling the Docker image, how it would be passing on the input. So what all inputs it would be passing on?

[00:04:43] These were the values. So we are checking up chart directory OCI registry OCI repository. They are based on what we have defined here. We have this so this could be different for other, this chart folder might be different for someone else.

[00:05:01] So we can, we can make it as a input. Same way we were publishing it into the OCI registry. We are publishing it in some repository within that. So what are those informations? We are adding those that? What's the registry? What's the repository, the prefix

[00:05:20] to be added by default. We're keeping it 010 the way we are passing it here. But if needs to be changed, we can pass on the value then push chart that would be the requirement whether we would like to push the chart or not update the report.

[00:05:41] If required target branch, input, update the repo that would update the current version that we were doing. So whether update the reports that this section is needed or not. So all this information we are fetching,

[00:06:01] then we need some git or get up specific information that we will be leveraging from the github set up with the environment variables, the default environment variables that we have for github runner. So get a workspace. That would be the workspace run ID. Sha

[00:06:20] all these values that we have used here. We are just defining them as variables for our usage. Once triggered, we will switch to target her workspace

[00:06:41] where we would like to work just to see that we have the correct inputs being printed, we would be acquiring them so that we can easily identify whether current values are getting used or not.

[00:07:01] We can add some sanity checks like o registry or repository. These details are being provided or not as an input or charge directory does exist or not. So we can pass on that value and accordingly we can be it. Post that in my entry. I would be adding these steps

[00:07:23] to calculate the sha that the sha that we have used a couple of times across our work like we have used it here. We have used it here and here so we can keep them or we can add them as a segregated section. So short sha that would be the value that we get

[00:07:43] image tag would be this final version would be this. So that way we have calculated. Now, the next thing that we will do, we will convert or execute these YQ commands to update our files.

[00:08:02] Like we have updated values chart dot el these were the files that we have updated. So let's try to, they put these things again in the entry point.

[00:08:20] So Echo, we are just printing the information so that it's pretty much clear that what we are doing, it's the same command YQ echo. You can see that over here. It's the same command that we have added. The only difference is we are converting it from those number of steps to a single entry point

[00:08:41] post that we were performing the lint that was here. We can go here and we can add this step for lending. It's again, same echo helm lint chart directory. We have already passed on that as an information it got

[00:09:01] linted. If there is any issue, it will fail. If there is no issue, it will pass. Once all is good, we can proceed with C creation. Thats again, simple echo that we are doing. We're creating an output directory where we would like to add it.

[00:09:20] It's exactly same what we have done here. Helm package. We might have created it somewhere upside, but that's something that we are creating. He pack also we're creating it in the.

[00:09:41] So we are reading it in the current directory. So we are just changing it so that we have a clear picture that where exactly the data is getting created if we need to debug it later on. So we're shooting at a tree creating the package,

[00:10:00] this getting the information if value is there, that means package got created successfully. If it's not there, that means there is some issue and it should get fill. Finally, we will proceed with push and what we will do, we will push our chart

[00:10:21] to the github Health Registry. We will publish it over there based on whether push chart is true or not. So that value is coming up from here, push chart. If you want that, you should always push it, you can enable that. But we are creating a chart which may or may not be pushed. So accordingly we can plan

[00:10:45] rest, all things are safe. We are checking whether our total is being provided or not. If not, it will throw an error. So that's what's happening over here. Finally, we are adding this step which will allow us to update our

[00:11:01] repository with the latest version of our package. It say we are just creating it more useful so that it will become more helpful and more generic with multiple options. Somebody is not interested in updating the report.

[00:11:21] They can skip, somebody needs to amit it, they can do that. Accordingly, it would work. So action is something which can be used by many Ts should not be hard that you have to perform these actions. You need, you don't need that accordingly. You can plan it

[00:11:40] then though it's not mandate, but we can set a couple of outputs if need to be used in future or in the upcoming steps. So that's how you can do that. So that's the entry point that we have added. Now based on this,

[00:12:00] you will create your action Y ML at the top level, add a new file with a name A dot VML. We can start with the same name.

[00:12:20] Let's keep it helm exs the name that we have given earlier description. Then you can add the author if it's not mandatory to add the author, but you can add the author if you want.

[00:12:42] So that way I can add it in the previous section, we haven't seen it. But if you want, you can add branding, for example, icon is package or cloud. That way which color you would like to go ahead with. You can add those things.

[00:13:02] Next important section or field is inputs, you need inputs, we need a couple of inputs that we have defined over here. We charge directory

[00:13:20] false by default, it is charged. It's not mandatory to pass on. You have to pass OCI registry, you have to pass o repository version prefix again, it's false. That's 010 but the one that we have used push charts by default, it is true. Update report is also true.

[00:13:41] Target branch by default, it's empty so you can leverage, it's not required the package directory dot slash by default that which will get created in the current repo. So it will get skipped if we have M it as force which is default D it is for. That means it's not mandatory

[00:14:00] and then we are passing on the token value. If there is any special token, it would be used as it will use it from this secret. You have underscored token. So that way we have the inputs available. We have published couple of outputs

[00:14:23] at the end which are these. So we can keep them in our action dot YAML as a separate section, we can close this, you can close this.

[00:14:40] And then finally, we are adding the part which says lunch using. So we are using it based on Docker. So it will become Docker

[00:15:01] then image which image you would like to use. So now here you can keep two things you can keep or provide the image name. If you want based on this Docker file, you can build and publish an image

[00:15:22] or you can directly put that based on this Docker file based on the Docker file that you have either way you can make it work. So that's your choice. Let's keep it based on the image.

[00:15:41] So we will build that image soon. Docker GHC dot IU Buchen Meer. Let's keep the name as Helm's current latest

[00:16:01] that this is the email that I would like to publish. And then these are the arguments that we are passing charge directory OCA registry repository. These are the inputs that we are receiving over here.

[00:16:20] Selasi configuration. They can, we can remove this. This is no more needed. So that's the setup. So that's how you can call it. So you have everything defined in your Docker file. This is your entry point now you can create and publish this image. So what we can do,

[00:16:43] we already have this HLINE image that we have completed. This is the workflow and I will add another workflow which says publish, let's see

[00:17:00] action image do V content would be more or less the same. So we can copy and paste, publish action image again on main. But action that means in action folder, if this file or this file got chain, you have to trigger

[00:17:23] work flows and nasty name. Latest checkout action check out for exact same action would be there. And here it will get replaced with

[00:17:41] action and the name that we have defined here, this name will be the part of my, this will be the name of my action image so we can update it here with this and this. So that way it's now

[00:18:00] ready. We have action dot YML, we have publish action, we have Docker, we have entrypoint, all these files are ready. So let's commit. The last thing should work.

[00:18:22] Is it cold for action? Let's commit and push. Let's come back here. And actions for the helm actions,

[00:18:41] it has triggered the action image workflow. It's based on this image, the one that we have prepared earlier. So this creation would be much faster, it's published. Let's go back.

[00:19:01] Let's go to the packages. Let me quickly refresh. So we have Helm Base helm Action. This is the one which has been published most recently and it's available. So now our action code is ready

[00:19:29] and the image that we're going to use, that image is also ready and published. That's it for this lecture. And in the next lecture, we will see that how we can

[00:19:40] Bob in the next lecture, we will publish it to the marketplace and then we will try to use it. Thank you and I'll see you in the next lecture. So.