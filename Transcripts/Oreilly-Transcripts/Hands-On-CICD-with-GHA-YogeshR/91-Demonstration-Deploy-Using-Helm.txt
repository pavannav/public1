[00:00:00] Welcome back everyone. So now let's start with the process and add a new workflow which will deploy the chart that we have created to our environment. Let's proceed. I'm saving it as a wl let's keep the name as a deploy

[00:00:20] help or let's keep it deploy health chart. Let's save it name. Let's say deploy and charting CBA is on now here we have to be precise. I can keep it workflow dispatch if I want it to get executed uh

[00:00:42] manually for this lecture, I'm keeping it. It's based on pull request main branch, but I'm adding a new filter in that which says types closed.

[00:01:02] So we have just added one more filter. If we need it, we can keep the filter like this as well. We would like to trigger this workflow only when pull requests got merged, then comes permissions. I need content with read

[00:01:21] and I need package with read. That's enough because we don't have to write anything here. We just need to download as we have used the environment variables in the past same environment variables here. So let me quickly do a copy so

[00:01:40] we can leverage here. These are the environment variables. Finally, jobs deploy Helen chart runs on. Now we want this particular job to get executed or run on our other

[00:02:00] skill set. Let me go back. I forget the name. So I'm just fetching the name. Uh that name is in a reusable workflow. So let's get that copied from there, reusable workflows and runs on

[00:02:20] guitar actions, skill set devil. So it runs on this here. I just noticed spelling is wrong. Its packages, we are putting a condition if we want this to be run only when get up event,

[00:02:40] event is pull, request is merged is true. That means there is an event happened. What is the event? A poor request got merged in the branch only, then it should trigger steps. We need to check out report, we will do it using checkout action.

[00:03:01] Then next step, we will set up helm using Azure setup, helm version three. And the way we have done in the past, we can use the same version here with version as 3.12 0.0 as though

[00:03:20] we have the access, but we still have to do a login for login. I can quickly go here because we have used the same process, publish a chart. And if we scroll down thats what we have done as a login. So lets copy, I'll put it here. So this will help us out to do the

[00:03:41] log in. Let me quickly do a word wrap secret github token will come from this OCI registry is this github actor is the one who is to it and that is the password. Next thing we have to do the deployment

[00:04:06] as of now, I'm running it to deploy the latest version. You can further enhance this workflow. If you want to get it deployed on the specific version that you have just uploaded, help

[00:04:20] upgrade, install pink nexts app or C A with this value and a name space name will get updated using environment environment is production. So

[00:04:40] I'm running it for production. That's why I have removed it from the main line. And this helm chart will always be used to get the deployment done in the production. That's it. Environment production runs on this. It will only run when merge is completed. First, it will check out, it will download,

[00:05:00] it will set up the helm, then it will log in and then it will deploy the hem chart helm, upgrade, install, thinks app OCI in this version OCI. This is the chart that needs to be deployed and we need to get it deployed in dollar env dot

[00:05:21] Naughty and weights va dot name space and we can keep it as so it will wait till the time this deployment is completed. And yes, one more thing this name space may not exist. So create name space.

[00:05:40] It's done, but we're missing one important step here. It's trying to deploy in the Cubans cluster. So we have to ensure that cute config also got configured. So before running this deployment, we can set it up. That is configure

[00:06:00] cube config. So run echo secrets dot config we have already configured it base decode, we can skip this if needed as we have already used it in the past. So I go to this and then ch mode 600 to this

[00:06:22] and we don't know whether this folder exists or not. So we can add a mkdir. And so now this is ready. Let's see whether it is working or not. So we have added this that we are going to do a deployment. It will only run when there is a pull request which is closed

[00:06:43] and it's in the main branch permissions. We have added, we have added some environment variables that we have referred in the steps below environment is production, it will run so it will run on our skillset runners. It will only run when merged is true.

[00:07:01] The event is pull request, got March, that's the event and then download the code set up, help login set up qcon for helm to use the same for deployment. And then finally, we're doing a deployment with using the upgrade and double hyphen install will install if

[00:07:20] it doesn't exist with the name think app, that's the chart in name space that's coming up from the environment, it will create the name space if it doesn't exist and it will wait for this to get completed. Let's go back. Let's clean. And if we quickly check

[00:07:40] helm list hyphen N or before that cube CTL get name space. As of now, we don't have a name space with the name production. So let's commit this newly created a workflow added workflow for

[00:08:02] deployment. I did workflow not for deployment to deploy helm chart in production. Let's commit,

[00:08:20] this will trigger a new job in our repository in the demo branch. Let's check that's triggered. Till that time. This is not completed. We can't merge, I pull requests. If we scroll down, we have enabled it. But I would like this to get completed before I will execute my March.

[00:08:45] Lets go back to the action. Sonarcube got failed again. Lets quickly go back to the Sonarcube. Go to the product quest

[00:09:00] thats the one three hot spots got included. It's all in the newly added workflow and it's same. So let's quickly fix them. Review save change status. We are using the secret asking for the same

[00:09:22] save change status, review safe chain status. It's all done. It's pass. Let's go back, rerun, rerun the failed jobs start again.

[00:09:40] It has started the process. So RCX got executed successfully. Let's go back to the pull requests, scroll down and complete the merge. Confirm

[00:10:01] we got some add up. Let's go back to the actions, deploy helm chart branch helm chart is not allowed to deploy on production due to environment protection rules. Yes. So we have added the protection rules due to which it's not running.

[00:10:21] So there are two ways we can do that. Either we will deploy it from a different branch or we have to remove the protection rule for time being. I'm just disabling the protection rules. Let's go to the rules rule sets. We have a main branch. Ah sorry, not this one. We have to go to the environments,

[00:10:43] production environment. We have three protection rules required reviewers. We have already done that wait timer is ok. Only deployment is enabled from one branch. Let's remove it for time being.

[00:11:02] But in a real time scenario, you can maintain it. Its done. Let's go back and rerun failed job. We have added a wait timer for one minute

[00:11:20] and we have added the approval. So that's the approval. So first let's approve production, approve and deploy because it's on production. It's done is waiting for the wait timer. So it will wait for one minute and then it will trigger the deployment for us.

[00:11:40] It's started and it's waiting for the deployment job is about to start running on the runner and it got reared. Lets see whats the issue. Run helm upgrade install. That's the command name that we were triggering release think

[00:12:02] does not exist that's fine. So it's creating, it could not locate a version matching provided version string. Let's check what we have done. We haven't provided any version but I'll check one more thing packages. It's helm

[00:12:20] chart slash think next app. Let's try to debug it. Let's go back here analyzing it. Did you get the answer for this failure? I know the reason reason lies in a statement that I have mentioned multiple times. OCI the registry that we're using. It's an OCI based registry. It's not a regular

[00:12:41] helm registry that we have. It's a github based OCI compliant registry, which is not able to resolve the version on its own. So we have to pass on the version without passing the version, it won't work. That's why it's throwing an error that could not locate a version matching provided version string. So we have to

[00:13:01] fix this issue. I'm leaving you with this error and see if you're able to fix it by yourself. If not, we'll do it together in the next lecture. Thank you.