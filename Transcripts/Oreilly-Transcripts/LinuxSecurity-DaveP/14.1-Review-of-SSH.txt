[00:00:01] Let's cover a quick review of SSH. On the slide here, we have two computers. We have an SSH client. That's where you're sitting, and what you use to connect to remote systems. And over here, we have an SSH server. This icon is of a actual server, but it could be any computer, or switch, or what have you.

[00:00:23] For the labs that we're gonna run, my SSH client will be my Debian client system, and the server's going to be my Debian server.

[00:00:41] Remember that by default, the server needs to have Port 22 Inbound open. On the client side, the outbound port is dynamically assigned. So it could be anything, but it's usually higher than 30,000. Previously we used passwords to make the connection

[00:01:01] between the client and the server, but in the next sublesson we're gonna show how to use keys to make those connections. And this way we don't have to type the passwords, and we can create keys that are as powerful as we need. So the client computer will be my Debian computer here

[00:01:20] and we'll make sure that SSH is indeed installed. And the server will be my Debian and server. And we want to make sure that that is installed, and also that SSH is running, so it is active and enabled. So now we're set up, and you know what SSH is. We use it to remotely control another system

[00:01:42] from the command line. So let's get into the labs for this lesson.