[00:00:01] Let's talk about how to install security updates only. Quite often on a server, you're only gonna want the security updates and not all the other updates to applications and so on. So a lot of times you'll just want security updates. And this Lab, Lab 13, shows how to do this.

[00:00:22] So if you wanna follow along, this is Lab 13, "Installing Security Updates Only." What we're gonna do, we're gonna show the DNF Security Update option in Fedora, really easy to use. But we'll also show how to install individual updates from a security perspective in Debian.

[00:00:41] So let's jump over to our Fedora system now. Bring up a terminal, and if you're working as root, that's fine, but if you're working as a regular user, make sure that you have sudo powers or wheel powers, depending on which distro you're working in.

[00:01:01] In this case, we're in Fedora, so we wanna make sure we have wheel powers. And make sure that you're using the sudo command before anything that we do here. So it's fairly simple to install security updates only and disregard the rest of the application updates in Fedora. We can do a sudo dnf check-updates,

[00:01:24] but then just add --security onto that. Type in our password, and that'll look through our Fedora repositories and look for any RPMs

[00:01:41] that are available based on security updates. We're not really worried about application updates, we just want security updates. And we find that there indeed is one, opensc for the x86_64 platform, which is us. If we're running an Intel

[00:02:00] or an AMD CPU on our physical system or on our virtual system, then x86_64 is what we want. So we have a couple options here. We can do a dnf install and then the name of that particular package. And, of course, we'd have to precede that with sudo,

[00:02:22] and that'll install that individual package. Or we could do a sudo dnf update--security, and that'll just grab the security updates. Let's do that. And it sees the one update, as we knew it would.

[00:02:41] So we'll say yes to that. It'll look for it, download it, install it, verify it, upgrade it, and it's complete. Very good. Now, security updates are usually a lot smaller than application updates, so that process should be a lot faster. So that was really easy, right?

[00:03:01] Now, one of the things you're gonna want to do is make sure that you keep abreast of the various security updates that are out there. So I recommend highly that you go to your distros security page for any updates. So, for example, I'm here at Red Hat

[00:03:21] at access.redhat.com/security/security-updates/. And this has all of the updates available to you. And you can see these say things like RHSA-2024, this year, and then the number for that particular CVE.

[00:03:40] And so you wanna keep up to date with what these are and make sure that you're updating your systems. And you'll notice that they have different levels of severity, like moderate or important, and higher levels. So definitely look out for this. But that's really it when it comes to doing the updates on this system for security only.

[00:04:03] Now, you can also do updates based on the advisory number. So for example, if we go back to our Fedora system, we could do something like a sudo dnf update, and then we would say --advisory and specify what that advisory is.

[00:04:22] So something like RHSA-2024:0773, okay? So that's very similar to what we were just looking at. This advisory probably already has been updated.

[00:04:40] Let's take a look. Okay, and it has been installed, no security updates needed, but it does say there are 24 updates available. I'm not really interested in that for this sub-lesson, I just want security updates. So it's complete because this particular advisory has already been installed.

[00:05:00] So that's another way to do it. If you're looking at these advisory numbers, you can do it by that particular number for your Fedora and Red Hat and CentOS systems. Now let's show how to install security updates only in a Debian-based system. There's several ways to do this,

[00:05:20] and they're more complex than in a Fedora-based system. Now the one system that I have here that needs security updates is my Ubuntu server. So I'm gonna use this as the example. So we're gonna do an apt-get for this, s, and the option is gonna be dist-upgrade

[00:05:44] in this case. Then we're gonna pipe that and we're going to filter with grep, and we're going to do "^inst. And the installations that we're gonna filter for,

[00:06:00] we're gonna do a second filter, grep-i, and we're gonna filter for security. Okay, so this should do an apt-get and look out to the repositories, but it should filter for just the security updates. Let's press Enter for this,

[00:06:20] and there you go. We get a bunch of security updates for jammy jellyfish Ubuntu:22.04. And we have Linux headers, Linux generic, Vim, Vim-common. And, yes, believe it or not, the Vim text editor does have security vulnerabilities,

[00:06:40] so these are important. So we found all of them, now if we want to install them, we can up-arrow to bring that command back, but now we'll just kind of do a little bit more finagling with what we want to do. First, I'm gonna use the awk command

[00:07:00] to grab the names of each of these. So, for example, the names are here, that is the entire name of one. And then we've got the name of another here and the name of another here. So those are the names that we wanna pull down. Those are the package names.

[00:07:21] We have to grab each of those, and those are the second field of each line item. So that's what we're gonna use the awk command to do. And we're gonna grab that second field from each line item. So this is gonna be an awk-F. We'll do "", and then we'll do {, ',

[00:07:43] and we'll say print $2'}, and close that. That'll grab the second field of each of those line items. And then we'll pipe that to xargs so that we can run the argument to do an apt to get install

[00:08:03] for all of those security items. So that'd be apt-get install. Let's see if this works. Okay, so dpkg was interrupted. We have to run a dpkg --configure -a,

[00:08:27] and we'll see if we can fix that and then we'll try that again. Okay, so that's finished, and now we can run our command again. There we go. And it's grabbing just the security updates.

[00:08:43] So what actually is happening here? Well, we use the grep command to filter for the security updates only. We use the awk command to specify the second field of each line item. And then we ran apt-get install

[00:09:00] to actually install them utilizing the xargs command. And this is one of those examples where you'd use apt get, or apt-get, if you will, instead of just apt. So that's gonna grab all of those security files only. And you may say, "Wow, that's a lot more work," right?

[00:09:20] And so a new Kernel's available here. I'm just gonna say Okay for that, and restart the services that are necessary. Basically just polkit. But yes, that is definitely a lot of work. There's other ways to do it. One of them is called the Unattended-Upgrades program, and then there's another one called Debsecan,

[00:09:40] and I talk about those in the lab document. So feel free to check those out as well, and you get options for how that'll work. But generally, this is how I'll do it. I'll just filter for the security updates and grab those only. And we can see that we're finished here with the Ubuntu system now. Another way to do this is to automate it.

[00:10:02] You could create yourself a Bash script with the command that we just did here, and you could incorporate that into a script and you could run that when you want in an automated fashion with Cron or Anacron, or other methods.

[00:10:20] And we'll talk about those a little bit later in this course as well. So lots of options when it comes to Linux to do these things, to make things easier for you. But definitely take a look at updating security only instead of installing all of the updates on your systems, whether they're Debian-based or Fedora-based.

[00:10:41] And that is it for this sub-lesson.