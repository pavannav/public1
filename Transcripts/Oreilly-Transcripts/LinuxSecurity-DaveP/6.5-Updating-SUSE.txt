[00:00:01] Let's discuss and demonstrate how to update SUSE. Now, you might be wondering what's the difference between SUSE and openSUSE? openSUSE is the free version that you can use and support yourself. SUSE, on the other hand, has paid support that is optional

[00:00:20] to you, and that's done with SLES. That's SUSE Linux Enterprise Server. We're gonna be running openSUSE here. I'm running the open one here with my own support. But either way, you are working with package managers and tools that can deal with the updates.

[00:00:40] The two main tools are YaST2 and also zypper. YaST2 is a software module that allows for updates either in a tab based user interface or in the GUI if you have a desktop. But more often than not, I'll use zypper, that is command line only.

[00:01:00] And so the zypper command could be used with refresh, with install, and with update. And when you work with these SUSE and openSUSE distros, you're normally working with .rpm and .repo packages. So let's take a look at our SUSE system.

[00:01:21] Now I've already booted it and I'm logged in as root. And so generally what we would run is the zypper command, and you could do a zypper refresh, which will refresh the repositories and make sure that it can actually view

[00:01:41] what these repositories are pointing to on the internet. You can also do zypper install to grab an individual program, or you could do zypper update to do a full update on this system. And it shows here some back ports have expired,

[00:02:01] but you can see everything is installed. There's nothing to do. And that's because when I created these virtual machines, I created them from golden images and I used Ansible to actually do updates on them once they were cloned and built. So no updates necessary here, but that is basically how you would do it.

[00:02:20] You could do a zypper refresh to refresh any repositories, and this is always a good idea in case you need to add a repository. Whenever you add a repository, you wanna do a refresh so that you can view those repositories on the internet. And then you would do a zypper update to do a full update

[00:02:40] of the system or a zypper install to install individual packages. So you can use YaST2 if you want to, which is yet another setup tool or zypper, which is more commonly gonna be the tool because it's command line based and it's very commonly used in the field.

[00:03:00] And if you're working with SUSE and openSUSE, I recommend that you get into their lists, their mailing lists, and you could do that at lists.opensuse.org/archives.