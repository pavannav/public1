[00:00:01] The uncomplicated firewall or UFW, which by the way is designed and developed by Canonical. Works very well on Ubuntu and it works well on Debian as well. It's not designed for Rel or its derivatives, but it works very well on Ubuntu and Debian. It's a front-end firewall utility that interfaces

[00:00:22] with IP tables and or NF tables. So if you look here, you'll see some of the basic commands. We have UFW status, which shows whether UFW is active or inactive. UFW enable, which starts and enables the firewall.

[00:00:40] UFW show added, which will list the UFW rules and UFW reset, which disables UFW and deletes all rules. So be very careful using that. So as you can see, the commands are very simple. By default, we have some global rules in UFW.

[00:01:00] And what UFW does is it by default denies all incoming connections. And that's an example of that implicit deny that we talked about. So it denies all incoming connections, but it allows all outgoing connections that may or may not be secure enough for you,

[00:01:21] but for a basic workstation, it's usually okay. I just wanted to show you those global rules. And here's an example of an individual rule, because if we deny everything incoming, then we won't be able to connect to the server from remote systems. If, for example, we wanted to SSH into a server

[00:01:42] that had a UFW firewall, we could allow that SSH protocol or the port number in this case. And we would do that with a simple command. UFW allow 22 slash tcp. So in the next sub lessons we'll show how to install and configure UFW.