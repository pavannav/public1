[00:00:01] Over the next two sub-lessons, we're gonna cover a lab. This is Lab 15, Creating and Repairing a Degraded System. And if you look in the repository, you'll find Lab 15. Feel free to follow along with the lab document as we go through this. We're going to cause a service to fail

[00:00:22] and then troubleshoot it and fix it. So we're gonna break and fix our system. So let's jump over to our Debian client now. The first thing I wanna do is run a systemctl status. Now by the way, this lab will work on

[00:00:41] just about any Linux distribution that is systemd based. So systemctl status, when we do so, we see our name of our computer, deb-client, and the state is running and it's in green. Green is good.

[00:01:00] That means that no services have failed. No C groups are giving us any issues. But I'm more concerned with services here. And you can scroll down and take a look at all this. There's a lot of information, but what we wanna see is that it is running. The other option is degraded in red, which is bad. That means that something has failed, usually a service.

[00:01:23] Whenever you have a server that you're working with, you always wanna check this and make sure that the system is clean and that it's running properly. So right now, this is running properly. If your system is showing degraded, well, we're gonna show how to troubleshoot that in this lab.

[00:01:41] So I'll press Q to quit out of this. And what I wanna do is cause a service to fail. I'm going to make the SSH service fail on this system. To do that, I'm going to break the configuration file. So let's do that now.

[00:02:00] We're going to open the sshd_config file, and that's located in /etc/ssh. Inside of there you'll see all kinds of good stuff, including the sshd_config file. That's the main configuration file for your open SSH server.

[00:02:21] You'll also see ssh_config, which is the configuration for the local SSH client. But the server side, we configure this, sshd. And then you'll see a variety of information for the different types of keys that you can use with SSH

[00:02:40] as well as moduli for elliptic curve math. So we're gonna modify sshd_config, though, and that's pretty much what we're gonna focus on in this video course. To configure that file, you'll need sudo permissions. So we'll do a sudo vim and feel free to use whatever editor you want to and sshd_config.

[00:03:06] Go into that now. And this is the sshd_config file. We're gonna be making a lot of modifications to this when we get into the SSH portion of the course. But what I wanna do for now is just go down

[00:03:21] to the port that's being used. Now an SSH server uses port 22 inbound by default. I'm going to remove that comment and I'm gonna add a bunch of junk onto the end. That is not a real port number.

[00:03:41] That is port 22 with a bunch of gobbledygook on the end. That effectively will cause the configuration to fail. It'll break the service. So I'm going to save that and quit. Anytime you make changes to a configuration file like this,

[00:04:02] you need to restart the service. So we'll do a systemctl restart on ssh or sshd if you like, and in some cases you may need to do sudo as well. Now when we do so, we get the red messages.

[00:04:21] This is expected as part of this lab. It says the job for ssh.service has failed because the control process exited with an error code. Well, we know why that happened. We are causing the issue. We are breaking the system. But this is good because we can show

[00:04:41] the troubleshooting process step by step. And they give you some information here. They tell you, see this command or this command for additional details. So we can use systemctl or journalctl to get additional details as to what caused the problem.

[00:05:01] Okay, so we have broken the system and in the next sub-lesson we'll start troubleshooting and we'll fix the system.