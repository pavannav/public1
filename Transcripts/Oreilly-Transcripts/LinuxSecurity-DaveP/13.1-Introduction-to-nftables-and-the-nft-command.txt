[00:00:01] If you want real control of firewalling, then you wanna work more closely with the NetFilter and the Linux Kernel in general. And that means using nftables, and sometimes the older iptables. If you take a look at the slide here, you can see a little diagram. nftables and ip_tables are actual IP packet filtering

[00:00:25] and NAT tools, otherwise known simply as firewalls. They're used to set up, maintain, and inspect the packet filter rules within the Linux Kernel.

[00:00:40] Let's look at this architecture of Linux from a high-level just for a minute. The Linux Kernel, meaning the core of the operating system, provides for the NetFilter. That's a networking framework that can allow for packet filtering, network address translation,

[00:01:00] and port redirection all directly at the Kernel level. The NetFilter is a set of hooks which alter how the operating system works. These functions will govern how packets of information are passed through the system. The older system for firewalling is ip_tables,

[00:01:22] and ip_tables includes four Kernel modules: ip_tables, ip6_tables, arp_tables, and ebtables. Those Kernel tables are administered by userspace tools, for example, iptables.

[00:01:40] So that would be the command in the command line. However, the newer packet filtering option is called nftables, and actually the first release of this was in 2014, so it's been around for a long time. This is administered by the nft userspace tool,

[00:02:00] so that's what we use in the command line to work with the nftables, those tables for our firewall. The commands nft and iptables are both known as backend tools, as opposed to FirewallD and UFW, which are front-end utilities or abstractions, if you will.

[00:02:24] But if you really want to get into proper firewalling security, you want to use nftables and the nft userspace tool.