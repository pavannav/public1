[00:00:01] Now let's begin the Lab for firewalld. We're gonna be working in Lab 23. And in this lab we're gonna do a lot. It's gonna be broken up into four sub-lessons. So a pretty lengthy Lab, take it step-by-step. But what we're gonna do for this sub-lesson is we're going to install and enable firewalld.

[00:00:22] And then we'll attempt to connect to the server from a remote system. So let's get to that now. And really the first thing I wanna do is try to connect to that server. What server am I running firewalld on? I'm gonna be using Centos and I'm gonna be working as root,

[00:00:40] but I wanna see if I can connect to it right now. So we'll do a ping to 10.0.2.61, that's my Centos server. We're getting replies. And let's try an SSH to that server as well. And we can get in via SSH as well. So we know that the firewall really isn't doing anything or anything much over there just yet.

[00:01:01] Let's go over to our Centos System and we'll log in. There we go. And so let's check if the firewall is actually running. We'll do a systemctl status on firewalld, and it is active and running and enabled.

[00:01:22] So that's usually the case, firewalld will be running, but it's a very basic firewall and it allows a couple of things to go through. So if you don't have it running, for example, if you installed it on Debian or Ubuntu, you'll want to do a systemctl --now enable firewalld.

[00:01:45] But ours is already enabled. So we're in good shape. Clear the screen. Let's go ahead and show the current zone. Almost all of the firewalld commands that you'll use will start with firewall-cmd.

[00:02:02] So firewall commands. So let's check out the zones that are available to us. We're gonna do a --get-active-zones. When we do so, we see that the current active zone that we're using is called public.

[00:02:20] It's not the most secure zone, but it has a bit of security inherent to it. So we can see public and we see the interface that that is working on, which is enp1s0 in Linux. Now if you're working on a Fedora server, you'll by default, see Fedora server as the Zone,

[00:02:41] and that's essentially the same rules as public. So zones are entities that hold different configurations. For example, there's Block and Deny, there's Public, there's DMZ and several others. Public is a bit less secure.

[00:03:00] Block and Drop are more secure. Let's take a look a little bit further at our information for our zone. So we'll do a firewall-cmd again, but this time we'll do a --list-all. When we do so, we see the public zone again, that's the active zone for our network interface.

[00:03:22] It's also the default zone. So if you make any changes, it'll default to this zone that we're working with. Now if we look here, we see icmp block inversion. No, that means that we can ping this server from remote systems.

[00:03:41] We can send ICMP messages. That's Internet Control Message Protocol, and we saw that, we actually did that from the Debian Client. In addition, we see services. So we have some services that are working and allowed through the firewall. We have cockpit, which is the web console,

[00:04:00] where we can take remote control of our server in a browser. We also have the DHCP version six client and SSH, which by default is port 22. Then you'll see one other thing that is not normally there by default, it's port 2222. And the reason that's there is because when we did

[00:04:22] our SE Linux Lab, we enabled that port on the firewall. This is that system where we did that. So port 2222 is also enabled. However, SSH is currently configured to use Port 22.

[00:04:40] So having that port 2222 is actually a security vulnerability. We should probably close that off. So you get a lot of information here when you do a firewall-cmd--list-all. There are many types of built-in zones that you can work with with firewalld. To see those, you can do a firewall-cmd --get-zones,

[00:05:05] and you'll see the list of those. And you can see we have block, DMZ, drop, external, home, and so on. Things like home and internal and trusted, those probably aren't gonna be secure enough for your needs.

[00:05:22] However, things like Block and Drop are gonna be much more secure templates that you'll want to use more often for your servers and your networks. And in fact, in this lab we'll use the block zone. You can also do a firewall-cmd list-all-zones

[00:05:48] and you should see much more information about each zone and what it allows and what it doesn't. To see that better, if you're working in the console, pipe that to less and then we can arrow down through that.

[00:06:01] We have the block zone, which is good. DMZ is okay, that allows for SSH. Drop, also very good, and all the others. And press Q to get out of there. So there's a little brief description of the zones that you can have. And these are just the built-in templates. You can build your own as well if you wanted to.

[00:06:22] And like I mentioned, you can assign these zones individually to each network interface on your system. Quite often servers will have multiple network interfaces, so it stands to reason that you may want to have different levels of security for each. Zones allows you to do that.

[00:06:41] That's it for this sub-lesson. In the next sub-lessons we'll show how to change the zones that we're working with and start modifying our firewall.