[00:00:01] Let's get into a brief introduction of firewalld. Firewalld is developed by Red Hat. It's designed for Fedora, Red Hat Enterprise Linux, and CentOS. And it's installed on them by default. Firewalld is a front end for the net filter framework via NF tables in Linux.

[00:00:22] And we'll talk about all that more in the NF tables lesson. But firewalld acts as an alternative to using the NFT user space commands. It's recommended for workstations, and it can work well for servers. But for servers and for firewalling entire networks,

[00:00:43] it's recommended that you use NF tables. You can install firewalld to Debian and Ubuntu systems as well, and I often do. I'll usually pick firewalld over UFW as a front end tool.

[00:01:01] But it may behave slightly different on Debian and Ubuntu. I mention this because I'll be using CentOS for the lab. So here's some commands for firewalld. And here we have systemctl status on firewalld. That checks the status of the service. We've already shown this.

[00:01:20] And then when you start working with firewalld, you'll be using firewall-cmd as your main command with various options and arguments. So for example, -list-ports will display any open networking ports, -list-services will show open services.

[00:01:41] You can add ports with the --add-port option, and then you can show the active zones with --get-active-zones. This will show the type of zone in use by each network interface on your computer. Now, this is one of the nice things about firewalld. It can work with different zones.

[00:02:01] And there are several built-in zones that you can use with different levels of security. You can even build your own zones, and you can assign different zones to different interfaces. So there's a little intro to firewalld. In the next sub lessons, we'll show how to install and configure firewalld.