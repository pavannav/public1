[00:00:01] Let's briefly talk about some of the different systemd states that you will encounter and these can be viewed with the system CTL command. If you look at the slide here, you'll see systemd states for units. Now if you remember we had a blue box for units,

[00:00:21] which is dealing with the immediate when it comes to services, whether they are active or inactive. And we're talking about starting or stopping those services. So we've got this blue box here with a variety of states for the units. Well, we showed active and inactive.

[00:00:41] You also have activating and deactivating if it's currently in the process of trying to become active or inactive. And this process could get slowed down by a variety of things. There could be dependent services, there could be systems slow down for a variety of reasons. We also have failed if it fails to activate altogether

[00:01:03] and perhaps that's because there's a bad configuration or another service is causing it to fail. Not-found and also dead. So we have a lot of different options here for the state of our unit. Let's go back to our Debbie and client for a moment

[00:01:20] and let's run that command one more time; systemctl list-units. And when we do so, we see all the different units. Now you get auto mounts, you'll get mounts, you'll also get paths and scopes. But again, for this lesson,

[00:01:40] what we're concerned with is the services. And as we look through here, we can see whether a service is active or not, whether it's running or not, or if it's active and it has exited. And again, the difference between that means if it's running, then it is using memory and CPU power

[00:02:00] and it's being monitored by a Linux Daemon. If it's exited, it's still active, but it's currently not being monitored and not using any memory. So you can view that with that systemctl list-units command. Now we've shown systemctl start

[00:02:24] and also you could do stop. We also showed restart when we restarted our SSH service, but there's another one called reload. And so reloads a little different. If you don't want to restart the entire service

[00:02:42] but you just want to reload the configuration, you could do that with the reload option. This works for some services. So for example, systemctl reload networking. We just reload the configuration, but it wouldn't actually restart the entire service.

[00:03:03] And if you want to reload all configurations, you could do a systemctl daemon-reload. And we'll need authentication for that as well. I'm gonna escape out of there, and look what it says, "access denied".

[00:03:20] That's 'cause we didn't use sudo, so we'll do that now. And that reloads all configurations for the services. The other side is unit files. And remember we had that green box for the persistent portion of services, whether we're enabling or disabling. And so we have a lot of states for that as well.

[00:03:43] Here we have enabled and disabled, we also have masked, which is completely disabled. And no start operation will work unless it's unmasked. And by the way, unmasked is one of the systemctl options. However, you may not be able to do this

[00:04:00] unless, one, you have sudo powers or two, you might not be able to do it at all, the system might have it masked and no one can unmask it. You also have a variety of others; linked, static, transient, generated, and bad. If you see bad, that means the unit files invalid or another error occurred,

[00:04:21] and that's when you have to look into reinstalling the entire program. You may have to do that or you may have to do a full reconfiguration. Most of the time though, you'll see enabled, disabled or masked, and the rest are less common. So if we go back to our debbie and client,

[00:04:40] we can do that command again, systemctl list-unit-files, and we'll see whether the services are enabled or disabled. So this deals with persistence.

[00:05:00] And like we said, apparmor is enabled, Bluetooth is enabled, and so on and so forth. And so know the difference between working with unit file commands where we would do a list unit files and persistence of services, enable and disable.

[00:05:21] The difference between that and unit commands with the list-units command and also starting and stopping services, and that deals with the immediate side of the services. This is all part of systemctl, and it's all part of systemd, so if you really want to get more into the guts

[00:05:41] and the core of Linux and how it works today on most systems, then you wanna learn more about systemd. But that rounds out this lesson. Definitely check out the systemctl command and do some practice on your systems.