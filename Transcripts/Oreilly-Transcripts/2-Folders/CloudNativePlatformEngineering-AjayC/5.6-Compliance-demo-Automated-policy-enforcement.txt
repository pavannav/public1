[00:00:06] So we will actually do a demo of the compliance at the point of change policy code one right after this. To actually get into the demo, I want to show you a quick platform reference architecture as to how this sort of works together in the

[00:00:22] demo. So think of it like the platform abstracting away the complexity of all these security layers. So we talked about our multiple four layers of security and, and we learned why that is important. Now, providing developers with a unified interface and a paved golden path is the way in which we have talked about how to make these things work better.

[00:00:41] So if you have a standardized tooling with a standard interface, it sort of imposes application owners to do things in a way that is seamless. They continue to do the way they have been doing things, but at the same time it gets you a better solution end of the day. So developers sort of interact, you know, with you really look at the

[00:01:01] developer touch points here and the developers interact with developer portal, Golden Path, CICD and all that. They don't need to know anything about opa, they don't need to know anything about falco, Spiffy or Cosine. And then the standardized tooling with those standard interface that you see here and that will allow the teams to get the platform team to solve

[00:01:20] those problems for them without them really having to know about it. So if you need to add a new policy, developers don't need to know about it. Right? You just go ahead and do that. And then if you need to upgrade Falco, you do that. It's completely transparent and the new CVE scanning just works. Right. So that's the beauty of having the platform team provide this as a

[00:01:41] platform capability to make the whole development process better without really having to have the developers spend their count load on it. So the, that is how all these things fit together in the grand scheme of things. All right, so we are going to do the code demo, but before we do that, as always, I want to try and walk you through this readme

[00:02:01] file. What does this do? So we talked about compliance activities. So let's look at the compliance demo of automated Policy Enforcement. So this demo will focus on OPA for preventive controls. So runtime display detection with FALCO is sort of introduced. Conceptually we're not going to do a demo of that, but you have the code to sort of extend that

[00:02:23] and include the FALCO aspects to it. So just to recap, we had four layers of modern governance. We talked about identity with zero trust, then the provenance prevention and the runtime security. So policy as Code is that number three is going to be your primary focus of this live demo.

[00:02:44] So just to understand how this demo is set up, if you could actually go into. You will find policies described here. And your inputs are described in this bad deploy and good deploy. And the scripts are here that says check sh.

[00:03:00] Let's first look at the rego file. So rego file sort of describes, you know, what are the kind of things that you should be looking for in your compliance. So without really getting into all the details of it, you can just look at it and say that hey, it's looking for various data surround things like what is happening during deployment if there is a missing CPU request, if

[00:03:23] there's a missing memory request and some of the missing port security context. So those are the kind of things that we are having here. And as you really look through and define and understand policy as code and start implementing it, you can take this and extend that to cover everything else you need here.

[00:03:41] And then what the script does is that it basically goes ahead and checks this, you know, runs uses OPA and just evaluates the rego file and provides the right kind of in outputs as to what is working and what is not. So that's basically the demo setup. And let's actually go and look at the actual demo here.

[00:04:04] So I'll go into. And I will go into inputs. Okay. And I will also open up my terminal. So and I have going to.

[00:04:20] All right, so I have this here. So let me actually first run scripts check and then we look at which input. So you look at bad deploy first. So you can see that when I tried to deploy it, it basically said that it denied. Right. So again make sure that you have installed the

[00:04:41] opa if you have not. And I have installed it earlier, which is why it didn't give me an error for that. But all good. So you can see that the bad deploy said that. Yeah, I mean so Ms. Tag must not be latest. And then missing label, missing memory limit, missing memory request and all that.

[00:05:00] Now if I actually go ahead and run this for my good deploy can say that. Yeah, so this is allowed and it's passing. So let's look at bad deploy and good deploy. So what happened in a bad deploy is that the part security context and we actually open good deploy side by side.

[00:05:21] So you can see in good deploy I'm specifically requesting my resource limits. My security context is clear. I'm not running as non root. That is what I'm doing. I'm not running as root. Whereas in here it's not set. It's not like they have said that I want to run as root, but that context is not set,

[00:05:41] which is the actual policy requirement. As you can see in the simpler example, we have a fairly bare bones bad deploy JSON file compared against the good deploy file with more of these contexts clearly defined. So that shows you how these two work. So what you can do is of course extend this. As I said, take a look

[00:06:01] at the your policy as code and then see what kind of policies that you want include here in your rego file and keep extending this to get to where you really want to get to. From the point of view of implementing this.