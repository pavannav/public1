[00:00:07] So let's do some demo scripts for lesson seven. This is where we are going to do the self service resource provisioning. So what we learned in this lesson was what platform APIs are and how to implement that well. So this basically enables that self

[00:00:20] service infrastructure using so very very well defined platform APIs instead of strict ticket driven flows. In this specific demo, what we are actually going to be doing is that we will sort of simulate that platform provisioning API we spoke about. And this will allow developers to request resources, receive immediate structured feedback, check status and you know, avoid dealing with all

[00:00:42] the things that back happen behind the scenes. Things like your Terraform, kubectl, cloudaim, all those things are completely transparent to the developers. And as you can see this is not about infrastructure automation tools, it's about like how platforms present capabilities and that is a current theme.

[00:01:01] As you can see. I'm making sure that when we talk about cloud native platform engineering we are never really talking about very specific tools. Yes, we are referring tools saying that how tools would be the ones that will give you more accessibility to get more things done. But it has to be around the understanding of concept of doing it.

[00:01:21] So as we are really trying to do this I'll switch to my versus code but just so that you know what we are trying to do is to make sure that you understand translate this simple flow to this simple flow. That is really the idea. And always remember API is your product interface to your platform product. Okay, so let me switch to my VS code and I'll go to my

[00:01:44] and I will open my terminal. So let's. First thing what we should do is we should go through the script. Maybe I'll just turn off my terminal and just show you the code and I'll just quickly walk through the code and then we'll come back and run some of these things.

[00:02:01] So let's look at what do we have in this code. So first function that we have here is. So I'll think of this like data models, right? So first data model is provisioning request. So provision request is. This is a fast API once as you can see I'm using fast API with pydantic based model. We saw that earlier. So this is the contract first mindset that we have.

[00:02:23] We have a few things in here. So this is the team is basically who owns the resource that is important. The ENV is essentially like you're going to have dev staging, prod, you know or any of your target environment resource type is. This is where you talk about things like your Kubernetes namespace S3 bucket.

[00:02:41] What is the resource that developer needs? Now comes the idea of 8020, right? Simple defaults for 8020 and optional advanced knobs for 20%, escape hatches for 20%. So your name, this would be optional, but you know, this would be like sort of if you want to override an automatically generated name, you might want to provide that.

[00:03:03] And this advanced is where you have your that 20% that we talk about those optional advanced controls kind of thing. So then comes the provision response. So this is where you would actually run this and what you get out of it would be more along the lines of a request ID or a status, like saying hey, it is pending or it is ready, it's failed kind of thing.

[00:03:24] Now once you have looked at that then. So that's the data model side of things. Now let's look at the validation. Right. So there should be a function for validate request. Okay, so you see validate request here. So what the validation does is that it blocks production provisioning for invalid names.

[00:03:43] So you can see that if the name is not right, production providing requires valid owning team. So for example, you see that the requested environment is prod and the team name is guest or unknown. You're probably saying that. Yeah, I don't know. The production can't be for a guest or an unknown, so it basically gets rejected.

[00:04:02] Same thing with something like kubectl. A raw kubectl command in the advanced field sort of enforces that guardrail that the platform hides that implementation details. Right. So that's on the validation side of things. Now let's look at provisioning.

[00:04:20] Yeah, so this is where we are simulating the provisioning process. So what this simulates here is a one second delay representing like real terraform, crossplane, kubernetes, whatever you end up using. And it sort of auto generates a resource name if nothing is provisioned. So let's look at where that is done. Yeah, Resource name is generated when nothing is provided.

[00:04:43] And what happens is that when it is a success it returns a clean results with some of the next steps and the platform sort of exposes the value and not the complexity. Then let's look at the endpoints. Yes, so if you really look at the endpoints here, there are multiple endpoints provided for provisioning requests.

[00:05:02] Yeah, so you have provision request that sort of creates a essentially a new resource request. That's what this is. Then we will have provision request should have provision request based request id. Yeah, so you can see a provision Request,/request ID. No.

[00:05:20] And this is a get. Should have that. That will get you. Yes, this is a get. So you can get the status of a specific request. Yeah, this is a create. This is a get. Now comes, you know, essentially getting. Listing the provision request. So you can do a get provision request and then you find

[00:05:40] that you can list all of them. So and then the last one, essentially the health. Right. I mean, so you're saying that, okay, you know, what is the health checker for all of these things? So that is what we have in the demo script. So now let's start running this and see how this works. I will quit this guy and expand this so that see it not less than 6.

[00:06:04] Go to lesson 7. Now the first thing I'm going to do here is I'm going to actually start a virtual environment. So I'm sure you have come across Python virtual environments, vn. So you can use vn, uv, whatever that you're familiar with, interested in.

[00:06:22] But VM is probably the more popular one. It has been the more popular one for a while. So we'll first create that virtual environment and then we'll make sure that we source this. So essentially this is. You can actually look at that in the folder,

[00:06:41] but it just sets up the environment. That's all it does. Right. So essentially, you know, resets all the old environments and comes up with the new ones. So good. So once we do that, I want to make sure that I have the fast API installed here. So I'll make sure that Fast API Con Pedantic, all those things

[00:07:00] are installed. See if that is good. Yep, it went through that and said that everything is good to go. Yep, everything is good to go. Now I can actually do UV on main app that lets me reload it and to port 8080.

[00:07:24] So basically I've started the application so you can see what happened there. Now it's running on port 8080 on my local host. And this is on in this folder that we are looking at. And it started the reloader process, started the server process. Good. Now

[00:07:41] let's come here and see if we can actually go to tat port and docs. Okay. So now we have just deployed our the platform API there. This is the Swagger ui. Right. So here, remember these were the four requests that we had.

[00:08:03] So you can actually have the provision request and we can have the response for that Same thing for the create provision. This is the listing provision request. You know, you can list everything that is needed. You Know you don't have anything extra provided here, but if you do, you can have that here.

[00:08:20] Then same thing for creating the provision request. So essentially this is sort of the schema there for creating that Kubernetes namespace and then can go ahead and execute it right away there. So that actually provides that. Right, as you can see now you can clear that, execute it and see what it does. So as you can see, this sort of shows you how

[00:08:41] that provisioning request thing works. Now we might also want to do now even if I come here to my VS code, right? So let's look at what it did.

[00:09:01] So what I did here was I provided that kubernetes namespace. So essentially from that documentation earlier, you can see that I got this create provision request, I executed it, I got that like information, I put that in here, I ran this on this and then I got, okay, my other

[00:09:22] window say that I want to create this resource type Kubernetes namespace for a dev environment for my payments team. And it just went ahead and created. So you can see that it provisioned successfully. So that's really the beauty of how we do this. Right, so now. So that's

[00:09:40] the first one. Now let's actually also look at some of that 20% part of it. Right, so the 20% here would be. Let me actually get the specific code in here. So how would that work? So let's actually run that. So this is a case in which the platform team had some very specific

[00:10:02] requirement. So what was the requirement here that the flexibility with the power user essentially can extend this, right? You have this advanced annotations, you know, with specific cost center and all those things, provided that is now available by running it this way. So I think that is the other thing that you would look at.

[00:10:23] Now let's also look at some actionable error messages. So what happens is that some guardrails that are being enforced by the API. But how do you actually see those ones? Right, so let's come in here and run this one. Hopefully you can see that.

[00:10:40] And so it's trying to say that, okay, I want to, there's a team here and the dev environment and saying that I need the S3 bucket. So what happens is that in this case provisioning failed because of the upstream quota being exceeded. So instead of just saying that it failed because this was a very simple request, instead of just saying

[00:11:00] that, yeah, what you said failed, you're providing an additional message saying that you know, your quota exceeded, you should try a smaller quota or contact the Platform team to actually get this done. So that is another way in which you could do. So the other thing that we saw here was about listing the resources.

[00:11:21] Where was that one? Provisioning request list. Provisioning request. Okay, so let's actually copy this guy. So this is listed. So one of the problems of course is if you just do curl, it gets you unformatted information. It's pretty easy for you to get these things directly from your document API documentation.

[00:11:42] So you could just come in here, put that and get these ones. And of course you can do a pretty print to see the kind of output it sort of creates with respect to all the namespaces. You can see various namespaces around kubernetes names, you know, various resources provision regarding various namespaces and kubernetes namespaces and these three buckets.

[00:12:02] So that hopefully gives you the idea of how to actually go about doing all of that. One other thing I would probably want to show you here is version request. We did that. We did that. Okay, so sort of like the get provision request kind of thing, right? So if we were to try this out and execute it, there is no. It sort of failed

[00:12:23] because there is no request ID here. So what is the request id? So let's actually go back and look at the request ID from the first one. Request id. Okay. What was the request id? Let's go back and look at our VS code.

[00:12:42] Okay, Provide a request ID in here and provide your request ID and execute it. What happens? Aha. You can see that now this is available. The request IDs available here.

[00:13:01] So let's go that and stick it into our browser. Do a pretty print. Excellent. Now you can see that this is the request that you made. Here is the request id, the status of the request, the namespace and the name of the team. And it's also the next steps, right? Which is the beauty of it. So you can just say, use your CI CD to deploy this to namespace check status

[00:13:23] where I get provision request. So as you can see, the power of being able to do these things is tremendous as you really look through this. So but a lot of these things enabled by the documentation, the Swagger API documentation, right? So think of it like if you can't provide that documentation, how difficult this would be to be able to do it,

[00:13:41] right? Because humans can't remember all these intricacies of it, right? They shouldn't have to remember. And even if they can, they shouldn't. And that is why the documentation becomes so much more important in the space. So few takeaways as we finish up Platform is that API, right? I mean, so essentially it's not Terraform, it's not Kubernetes,

[00:14:01] not the CLI tools behind it. So anybody who says that Kubernetes is platform engineering, this is the counter argument to that, then always remember that 8020 rule. Then hide complexity, expose value, make sure that your errors are actionable, and then make sure that all these things are self serve.

[00:14:20] So you made it self serve by making sure that you have your open API documentation, the Swagger UI to provide you all the kind of documentation easily accessible by the developers.