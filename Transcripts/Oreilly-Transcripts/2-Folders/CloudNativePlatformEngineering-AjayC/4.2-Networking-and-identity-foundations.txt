[00:00:06] So now let's get into this idea of networking and identity foundation. So let's talk a little bit about oidc. OIDC Connect is sort of the industry standard for doing idp. So let me explain why we use it.

[00:00:21] This is a authentication protocol built on top of OAuth 2.0 framework and it sort of allows the users to sign into applications using their credentials from a separate identity provider. This provides this battle tested industry protocol. Right, so let's talk about some examples first, right? If you think of login with Google

[00:00:40] and login with GitHub, which I'm sure you have seen all the in everywhere you have actually tried to create an account that's the OIDC in action, that's the real example of oidc. So the question is like why should you use OIDC instead of building your own authentication? So main thing is that this is something that

[00:01:00] has been tested so many times, so it's guaranteed to work well and it's been reviewed by experts and literally millions of websites and millions of people use that in thousands of organizations today. So on top of it it also supports every major Service. It supports AWS, GCP, Azure, GitHub, GitLab,

[00:01:20] Kubernetes, they all support Speak, OIDC. It also eliminates the need to build that custom authentication for each platform component. You know, recently, a few months back, I started a company and I was building up some solutions and I wanted to make sure that there's the right kind of authentication to do all these things. When people like that do it in smaller teams, building smaller solutions

[00:01:42] and all that, you always want to go with something that is proven that sort of reduces the friction for you and reduces the learning and reduces the kind of things that you need to build. So what I did was I obviously used OIDC to make that work. So you also understand that the security teams already understand the attack vectors, which means that they know

[00:02:03] how to mitigate it. So it's not just like they understand how it is. So how to fix it. So when you use oadc it provides you that option for your security teams to address. That idea here is very simple, right? Do not reinvent the wheel if you don't have to.

[00:02:20] Authentication is security critical, so use the standard. So how does OIDC work? There are four concepts in oidc. Number one is what I call as entity. So this is the idea of a user or a service requesting access. Very simple, right? Your user could be your developer, your service could be your CI CD pipeline, Or it could be many of those things, but that's an entity.

[00:02:43] Second thing is what we call as authentication. This is a process of verifying the entity's identity. You could say that, hey login with GitHub SSO, you know, with GitHub SSO. So that is the process that we are talking about. And you know, it could be like a username password. So it could be SSO

[00:03:00] with your corporate idp, could be any of that, right? So it doesn't have to be just GitHub itself. Right. So third one is what I call as the token issuance. This is like after your successful authentication. So you went through this process, identified the question of this authentication. After that authentication, the IDP creates a signed JSON web token or jwt.

[00:03:23] And that jwt, we will look at a JWT later. But that has got this embedded claims built into it. And now comes the last part which is a validation. So you asked for it, you defined it, asked for it, got it. Now the next thing obviously is to validate it. So platform services validate the token instead of

[00:03:41] ever managing passwords. So I think that is a process through which the whole IDP works. So we can also look at it the point of view of how do you scale this, right, because it's one thing to do this for a team of 10, but you know the example that we were looking at earlier, 100% engineering org with

[00:04:00] 10 services that can create potentially thousand, 1,000 potential permission mappings. So if you really look at the left side, you can see how messy it can get. And what you have in here are all the services that might require this authentication. And on this side you have all the different people who are actually requiring these things.

[00:04:23] So the way it sort of works is that if you provide a team based solution which we talked about, you know that your payments team requires certain kinds of access with respect to the kind of authorization that they need. Search team requires different types of authorization because all of them are authenticated and they're coming to the authorization

[00:04:43] phase. So like that each team requires different levels of authorization. So not every team require all the services. Sometimes you may, but in that case also it's pretty simple, right? Because you are doing it at a team level as opposed to an individual level. And this is interesting in the sense that the other aspect of it is that

[00:05:02] it's highly unlikely that permission changes are required at the team level. Because you know, if the payment teams require a certain kinds of permissions, that's always the case, right? It's not going to keep changing, might have some kind of say, okay, we have a new product, new way of doing things, sure, that comes in, but once it's set, it's set and

[00:05:20] that's the real beauty of it. So I hope this diagram sort of captures that idea of team based authorization that makes this whole process far more efficient. So this also maps very nicely with the idea of team topologies, right? Because if you have a set of stream aligned teams, your payment teams,

[00:05:41] your search team could be your stream aligned teams, where your infrastructure team or a data science team could be maybe an enabling team or like a platform team, you can actually define these relationships and these authorization based on the team topologies concept. So the idea is that as new team members inherit those correct permissions,

[00:06:02] that the whole process becomes seamless and nobody needs to think about it and it's never an issue as you move forward. It's also good to think about Kubernetes as an extensible control plane in this context, right? As you're really trying to build your control plane. So we talked about OIDC and all that. So question really ask is like, why use Kubernetes

[00:06:23] for it? Because anytime you talk about building an internal developer platform, the conversation on Kubernetes sort of comes up. So building a custom control plane from scratch means that you're reinventing the orchestration, the API management, you know, resilience patterns, everything gets apt to be rebuilt. Same idea with how we might

[00:06:41] be using, why we might be using yadc. Right? So you also need to build everything from like a declarative API. We spoke about reconciliation engine, that state, you know, state storage place where you actually save the state. Then some kind of a leader election, some kind of like high availability and extension mechanism.

[00:07:01] So all of those things are things, if every team were to do all of that, it becomes significantly more costly. And when you do that, obviously we're going to introduce our own bugs to be able to do that. And these are things that Kubernetes solved many years ago. So leveraging Kubernetes sort of gives you that at least

[00:07:21] 10 years of battle tested distributed systems that every engineering team has been using in the enterprises for many, many years. So primarily just to sort of summarize the three main things here, one is the resilience, the idea of self healing through built in reconciliation loops and that declarative state management.

[00:07:40] So this is where controllers constantly ensure that decide state equals actual state. Remember that we talked about what reconciliation is earlier? Next one is the extensibility so this is where a universal API can be extended with CRDs or custom resource definitions to manage any resource. And we'll talk about this in a subsequent lesson. So this, think of it like this is not just pods, right? It's not just

[00:08:03] Kubernetes pods we are talking about like databases, queues, certificates, anything that you need to do, you should be able to do extend at that level using the operators. Now comes the ecosystem. So massive industry standard tooling ecosystem is there with kubernetes, right? I mean, so this is something that has exploded and that's become like an industry standard. Which is why I'm

[00:08:22] sure you would hear when you talk about platform engineering, if you ask people, like, what's the first word that comes to your mind when you talk about platform engineering? Most people might say kubernetes, right? I mean, this is the reason that large ecosystem. So you can have GitOps tools like your Argo CD and Flex and you can have observability tools plugin.

[00:08:42] So even if you're not running application workloads in Kubernetes, you can use it as your control plane for other resources. And you know, one of the things, one of the tools that does these things is called crossplane. And crossplane uses sort of Kubernetes as a control plane to manage cloud resources and sort of across multiple providers. You know, it doesn't have to be AWS or Google Cloud or Azure. It can

[00:09:03] do it across multiple providers. So I hope this sort of gives you that control plane extensibility and why we typically use Kubernetes to do this. Another thing to think about is vendor managed service strategy, right? So should you run Kubernetes yourself or use a managed service?

[00:09:24] So this is something that you have to make a decision. But let's talk about a few considerations that you might have. Self managing kubernetes means that your team spends a lot of time with upgrades and patches. And this was one of the biggest challenges with Kubernetes early on, when we first started using it around 2015 or so.

[00:09:43] Because Kubernetes releases quarterly, right? I mean there's a lot of releases and it's very actively been being actively developed. So each release needs testing, upgrade planning, execution. So security patches come more frequently in a cloud native world. One shouldn't have to worry about that, right? So I've seen platform teams where 70% of their time goes into a

[00:10:01] Kubernetes maintenance, right? So it's not adding value to anybody, definitely not to the developers who are literally quote unquote, paying for the platform engineers. But the managed services, whether it is Eks, Gke Aks, you'd have heard of all of that Elastic Kubernetes cluster, all that. So you would find that Elastic Kubernetes services and Azure Kubernetes service and all

[00:10:23] that, you would find that if you use that, it sort of frees up your team to deliver more value for your developers. So let's look at this quick. Responsibility metrics, right? What you find is that if you look at the control plane, it's done by the vendor. If you look at the managed node groups done by the vendor, look at the

[00:10:41] cloud native add ons, any kind of load balancer controllers and all those things managed by the vendor. Again, anything that is developer facing that is where the platform team comes in. Because your cloud service provider may not know what your developer's needs are. So this is where the platform team can augment that. Similarly, on the identity integration and abstraction like we spoke

[00:11:01] about, you don't really want to use the vendor's identity provider. So you are actually going to have your platform team manage that. So any kind of value add platform products is where your platform team comes in. So this sort of tells you where you should be focusing on as a platform team as you're going through this.