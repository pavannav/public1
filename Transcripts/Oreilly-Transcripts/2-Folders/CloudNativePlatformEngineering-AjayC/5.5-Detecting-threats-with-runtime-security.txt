[00:00:07] Now we come to the layer four which is detecting threats at runtime. So prevention is not always good. As we looked at those four different layers early on, we found that prevention is not always good enough. Right. We must assume that the breach is possible.

[00:00:21] You should have mechanism to actually fix that. So zero day vulnerabilities, you have heard of them. And so those things sort of exist, right? Because sophisticated attacks are bypass anything that we do in static analysis. So you need to have some dynamic activities too. So we must assume that breach is possible and have mechanism to sort of detect these things and fix.

[00:00:40] So in this assume breach mindset, the idea is that you can't always prevent everything, but when those things happen you can be very, very agile and really responsive. So what are you really looking for at the runtime? Right. There are a few things that you'd be thinking. Number one is this whole

[00:01:00] idea of privilege escalation attempts. So this might be a case where a container process trying to become root, right? I mean we have seen some cases like that in the past. Then the other one would be an unauthorized file access, very simple. But something that goes and reads your etc strato or something like that or some sensitive files that you might have in the system.

[00:01:23] And you know, if, if that happens and if you don't really detect that while the runtime is happening, there is no way it can actually in a full proof manner detect those things while you are actually, you know, doing this in each of the three lives prior to this, another one would be the suspicious network activity. Right? So for example, some if there

[00:01:42] is an unexpected outbound connection, some kind of port scanning happening, that is something you want to know. The one thing that I've seen in some of the recent years is like this idea of a shell run inside a container or maybe a shell spawned inside a container, right.

[00:02:01] When that happens, it's almost never a good thing, right? Container should run one process. When you have a shell it means that it's spawning child process which is never a good idea. So this unexpected process execution, sort of like a web server spawning a cryptocurrency miner, that is something that has been reported in industry a lot. So these are all the anomalies,

[00:02:22] right? So they don't match that normal application behavior. So if any of those things happen, it says that yeah, this is something that has to be done now to do this, to do the layer 4 security. One of the most preferred tools is called Falco. There are some other alternatives but we recommend Falco. This is again another CNCF graduated cloud native runtime security tool

[00:02:43] that detects that overall anomalies within the system. So every action on a Linux for example, if you look at this in our diagram here, every action on a Linux system is a system call if you think about it, right? I mean so you read a system source is call, you know, open a network connection, it's a system call. So spawn a process, another one, right?

[00:03:02] So what FALCO does is that it sort of instruments the kernel to see all these syscalls. So it's really high levels of visibility that you can think of, right? So FALCO rules sort of describe what is suspicious and the rule files that is the FALCO engine that you see there, rule files when those, when you have a bash or a shell or Z shell or something that runs inside a container.

[00:03:25] And FALCO can really figure out how to alert log trigger any of those automated responses that you might need. So an integrated SecOps pattern would be like if you look at all of them, prevention is done by Gatekeeper that blocks that non compliant

[00:03:40] deployments at that API RunPoint detection is when any threats that bypass prevention. So FALCO does that detects that malicious behavior in runtime Then response is like yeah, FALCO can alert trigger automated or manual response procedures. So this becomes that overall SecOps flow. So anybody doing DevSecOps in a true platform engineering world today

[00:04:03] we'll be using Gatekeeper and FALCO most probably to actually with this kubernetes admission controllers to be able to do this the right way. So how does that really look in the large scheme of things, right? So if you look at this diagram, what you see is that the gatekeeper is that shield that you see here. So you see that this is the shield

[00:04:23] it sort of blocks all the bad things from sort of getting in, right? I mean it basically denies all the bad stuff coming in. And then you have the static analysis, the policy evaluation, admission control. So essentially saying the idea if it doesn't pass policy, it doesn't deploy. And then FALCO becomes the alarm right here essentially

[00:04:43] catches the bad things that got in anyway. And because of the runtime monitoring, anomaly detection and alerting if something suspicious happens, you sort of know immediately and then that gets notified into Slack or pagerduty or however you want to really want those alerts done.

[00:05:00] So gatekeepers think of it like gatekeepers sort of preventing those predictable problems, misconfigurations, maybe known vulnerabilities, perhaps even like policy violations of course. And then FALCO detects those unpredictable problems, right? I mean, these are not so Gatekeeper is those predictable and Falco is that unpredictable ones.

[00:05:21] So these could be zero day attacks, sophisticated attacks like insider threats and all that. So together with Gatekeeper and Falco, it sort of provides that comprehensive picture of how it should be done.