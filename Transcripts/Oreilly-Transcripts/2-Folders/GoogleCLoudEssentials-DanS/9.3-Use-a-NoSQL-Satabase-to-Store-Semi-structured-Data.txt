[00:00:06] - Now I wanna take a look at a NoSQL database and in this case we're going to look at Cloud Firestore. So I will simply type Firestore and navigate to that.

[00:00:21] Now NoSQL databases really are different from relational databases. So whereas you may be used to specifying table definitions and outlining exactly what attributes you want to track in each table, with NoSQL databases, we have much more flexibility with regards to when we define our data structure.

[00:00:40] And also we don't have to have a single data structure like every row in a table follows. Within Cloud Firestore, we have the option of having slightly different structures in our organization. Now instead of having tables in NoSQL databases like Cloud Firestore or

[00:01:02] in document databases in particular, we have collections and collections are somewhat analogous to tables. And we also have, within collections, we have sets of documents and documents we can think of as being analogous to a row in a relational database. So here's an example where I have created a collection.

[00:01:21] I call it my-fs-collection-1. And within this collection I have three documents. Now remember, a document is kind of like a row in a relational database and it has three fields, at least in this first document. We have a color, a list price, and a product name. So this is basically a very simplified structure

[00:01:42] for like a product catalog. Now if you'll notice, that we have three attributes, color list, price and product name for one document. For another document we have a different set although we have product name but we have material and last validate. So we have different attributes that we are tracking.

[00:02:01] And in our third document we have color, list price and product name. So we can vary what attributes we track by each document. So for example, we could add a document and we're gonna allow Google Cloud and Firestore to create a document name for us. So we'll add an attribute and we'll call it "color".

[00:02:23] And that is a field type string or data type string. And I'll add size and that can be a string. And let's have something else like quantity. And we can change that type and we can make that a number.

[00:02:41] Now you'll notice there are a number of different field types we can work with. So for example, we're gonna have booleans for tour falls, maps, which are sets of keys and values. We can have timestamps, geo point, for example, if we're working with geo location data. So I won't use any of those other types. Just know if you want to work with a NoSQL database,

[00:03:01] there are lot of opportunities to structure your data in ways that fit well with your application rather than trying to make your application fit into a very fixed kind of tabular structure that we often find ourselves doing when we're working with relational databases.

[00:03:20] So I'm just going to, oh, specified field values. So we'll specify our color will be red and our size. How about if we make that large? And our quantity? Let's say we have 20 and let's save this. So now I've added a document which again is analogous to a row to our collection.

[00:03:42] So we've added a document to the collection. I've done it interactively. Typically when we work with Cloud Firestore, we're doing it programmatically. So you might have a Python or a JavaScript program that might be working with a user interface collecting data. And then that program actually makes a call to the cloud Firestore API and actually inserts the data

[00:04:05] as a document into this collection. But for our purposes from a learning perspective, we're using the graphical user interface. So I will point out also there is a query builder in Cloud Firestore, which is not a SQL database.

[00:04:20] This is a NoSQL database. So it has its own query language that is specific to Cloud Firestore. So if you do wanna work with Cloud Firestore, I just wanted to point out that you won't be using SQL as your query language. So Cloud Firestore and document databases like MongoDB are really

[00:04:40] useful when you have applications where the kinds of attributes that you want to track may vary over time and it may even vary across products or other things that you're tracking within a database. So Cloud Firestore is a good option when you need that kind of flexibility with regards to the schema.