[00:00:06] - So we've seen with Vertex AI that we can create data sets within Vertex AI using data that we have in BigQuery, or cloud storage, or something we have locally. We've seen we can use Vertex AI training

[00:00:20] to actually build models, and with those models, we can get evaluation results, and we can also deploy those models to containers. And once we've deployed those models, then we can actually use them to generate predictions. Now, if we use BigQuery ML for building models, we can also use BigQuery for querying predictions using SQL.

[00:00:44] With Vertex AI, it's a little bit different, because again, vertex AI is more of a generalized platform that's more easily customizable. But there are basically two ways of generating predictions in Vertex AI. One is to use batch predictions.

[00:01:00] So for example, you might have a table full of data, or a file in cloud storage with a bunch of data, and you wanna maybe do some classifications or make some predictions. If that's the case, then batch predictions is a good option. And with that, you can set up within Vertex AI. You can specify a particular model and a particular data set, and you can basically ask Vertex AI to run that.

[00:01:22] And that's like a one-time job. Alternatively, you can use Vertex AI to deploy endpoints, and this basically creates essentially an API endpoint with a web address that is acting as a service and continually running and waiting for requests. So, for example, rather than send in a row of data,

[00:01:42] for example, like if we're doing batch predictions, we might make an API call and include in that API call a JSON payload, which contains, you know, four or five or several attributes that we wanna use for the prediction. The endpoint receives that payload request, runs the model, and makes a prediction,

[00:02:00] and then returns that prediction as the payload in the response. So the Vertex AI endpoints, again, act like APIs, which we can call. So if we want a more service-oriented architecture, then we can use endpoints. But if we want to do sort of batch predictions or bulk predictions, then in that case,

[00:02:20] we'd want to use the batch prediction option within Vertex AI.