[00:00:07] - Now, we're gonna turn our attention to logging. Now, the service in Google Cloud is called cloud logging. So, I'll simply look that up. And the dashboard or the main console for logging looks something like this.

[00:00:20] This is known as the Logs Explorer. And basically, it is a centralized place where we can go and view log messages that are collected from variety of different services and infrastructure. And what's really nice is the Logs Explorer is organized in a way that makes it very easy

[00:00:40] to quickly filter down and find messages that are particularly relevant to whatever topic we're interested in. So, first I just want to briefly show you some of the features of Log Explorer. Now, starting in the upper left, we'll see that something labeled "Last 1 hour". That's basically the time span that we're looking at.

[00:01:00] So, we can vary that. So for example, I could look at the last three hours, for example, and I could update that. And that will update the results that I'm seeing. And we also have options here at the top to limit the log messages that come back based on the type of resource we're interested in.

[00:01:21] So for example, I could take a look at just log messages from a particular Kubernetes cluster like GCE Cluster 2 or I could look at all of the log messages from all of the Kubernetes engine clusters or any of these other services.

[00:01:40] I could pick any one of these and I could have messages just displayed from that. I'm going to switch to GCE Cluster 2 and apply that. And what you'll see here is the list of log messages down here has changed because now we're filtering just two log messages from that particular Kubernetes cluster.

[00:02:02] Another thing I wanna point out is that as we're making these selections, cloud logging is actually building a query here that we can actually read and edit. And this is in the logging query language and uses a particular syntax that's, or specific to cloud logging.

[00:02:22] And it allows us to specify filters like the resource type. We're looking at just Kubernetes containers. And we're looking at a particular cluster called GCE Cluster 2. So as you become more familiar with cloud logging, you can actually type in your own filters here as well.

[00:02:41] But I'm just going to continue to allow cloud logging to populate this for me because I find it's really useful for quickly sort of filtering down and identifying the messages that are of most interest to me by using, I find it goes very quickly if I just use some of these features here at the top

[00:03:00] like selecting based on a particular log. I won't do that. But I am interested in severity. Now, I may be interested in only errors or warnings and errors, and anything that's more critical than that. So if that's the case, then I can click there. And what we'll see here is that again, the list of messages has been refreshed.

[00:03:21] Now, we'll also notice this histogram has also been refreshed. Now, the histogram basically shows the number of messages that are being generated over a particular period of time. And we'll see that there are sort of pockets where we're getting errors and then there's no activity, and then we're getting more errors.

[00:03:40] And so, there's some kind of periodicity here with these errors that we might wanna investigate. Now, if I change the level from warning to maybe info or higher, we'll see more messages. And now, we'll notice that in addition to the red in the histogram which indicates errors, we have significant number of other messages

[00:04:02] that are just continually streaming into the logs, which may or may not be of any particular interest to us. But again, we can filter down to our more targeted subset of messages by selecting a severity error or a specific container or a particular log. So, what we have here now is a list of messages.

[00:04:23] So, let's assume that I've kind of narrowed things down. I'm interested in this particular Kubernetes cluster and I'm interested in errors. And let's actually narrow it down to maybe the last 30. How about if we go last hour? We'll narrow it down even further. Okay. So there.

[00:04:40] So, now we have just a handful of error messages. In cloud logging, we can get quite a bit of detail in log messages. And so what we're seeing here by default is just a very brief message, just a truncated version of the message. But we can expand these messages by using this arrow

[00:05:00] and clicking down to see additional details. And what we can see here is basically the payload of the log message. And for this we're seeing that this message was written to the standard error output and it's about a container running in Kubernetes.

[00:05:20] The severity level is error. And then the text payload, which is really the message, this is usually where we get a lot of useful information. It indicates we're having some kind of error reading from a stream. So, depending on what applications we're running in our Kubernetes cluster, this may be something that we could safely ignore

[00:05:41] or it's something we need to drill down into. But what we're seeing here is that with cloud logging and our ability to sort of hone down the search criteria for particular log messages, whether it's by a particular period of time or a particular resource, or a particular severity level,

[00:06:00] we can pretty quickly identify messages that are relevant to whatever it is we're trying to troubleshoot.