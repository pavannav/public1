[00:00:06] - So, in a previous video, we created a very simple Google Cloud virtual machine instance. And what I'd like to do in this video is repeat some of those steps but actually dig into a little bit about how we can configure machine

[00:00:22] and really tailor it to our specific needs. So we're gonna create a new instance, and we will call this gce instance 2. And I want a label to this 'cause, again, labels are really useful

[00:00:40] when you have a large number of machines, but we don't need to do that. I will use us-west1, and we'll leave this in us-west1-b. Now, for this, let's take a look. Now, if we look at Compute optimized, let's just see what some of our options are. We have different series, and these are powered by different CPU cores.

[00:01:03] So if you're really interested at the side in controlling like what kind of CPU core you use in your virtual machine, you can do that by specifying a series. You can also specify different machine types. And depending on what you're looking for, so, for example, if you wanted something that was compute optimize,

[00:01:20] so a lot of CPUs relative to the amount of memory, then you might wanna choose a compute-optimized disk. So if you wanted to run, for example, a database and you wanted to have a lot of memory compared to relative to, say, the number of CPUs you were running, you might pick a different series. And so, for example, I might pick the M1 series,

[00:01:42] and I see there are many different options for memory optimize. And what I see is, for example, I could get a 40 virtual CPU, 961-gigabyte virtual machine. So I could configure this if I really needed

[00:02:00] like a production server that was pretty big. But you'll notice over to the right, Google gives us an estimate of the cost. So what you can see here is running a memory-optimized machine with 40 virtual CPUs and close to a terabyte of memory. It's gonna cost me somewhere in the neighborhood of $3,200 a month.

[00:02:21] So needless to say I won't be starting that machine, but I just wanted to point out you have a wide range of options when we configure virtual machines. So you can get very highly powered virtual machines running, again, and we want this sort of ability to tailor

[00:02:40] at the very high end because there are use cases where we need that much, that many CPUs and that much memory. For our purposes, we're gonna go ahead and go back to General purpose, and we'll just take a medium, e2-medium with two virtual machines, and let's just see what our other options are if there is something. Well, here's, why don't we try an e2-standard-4

[00:03:01] that has four virtual CPUs and 16 gig of memory? And we won't be using confidential VM service, and we won't be deploying a container. Right, so we're gonna change some parameters on the boot disk. And so because we're working with the boot disk,

[00:03:21] we can do things like change the operating system. So, for example, if you'd rather use CentOS, you have that option to change that as well. Now, we also have the option of choosing a particular version. I'm just gonna leave it at CentOS 7. We can change the disk type. So, basically, we have a few different options

[00:03:40] or several different options in Google Cloud when we want to attach virtual disks to our virtual machine. So, for example, we could use what are known as balanced disks. And balanced disks are balanced in the sense of you get good performance and good sort of cost-performance tradeoff. So balanced disk is always a good choice if you don't have either cost constraints

[00:04:02] or performance constraints that you really wanna optimize for. Extreme persistent disks really give you great performance, but they cost more. Standard persistent disks will cost you less but may, are not as performant. So you can decide which type of disk you need based on your particular use case.

[00:04:21] I'm gonna go ahead and leave this at balanced disks. Now, the size of the disk, we can edit here. So if we wanted to, for example, make it 40 gigabyte, we could do that, and I'm just going to select that. So now, you'll notice that we are running a, in the boot disk information, we're running a boot disk that is 40 gig in size.

[00:04:43] You may also notice that our monthly estimate has updated. So as I change the configuration, I can get a sense of what the impact is on the cost of running this particular configuration. So I'm gonna leave the various access options and firewall parameters the same.

[00:05:00] I will, however, open the Advanced options, and this allows us to see and configure a number of different sets of configurations with regards to the virtual machine. If we look in the Networking, we can do things like add networking tags. We can specify a host name for this particular machine.

[00:05:21] Now, Google will create one by default, but if you wanted to give your virtual machine a host name, you could do that here. And there are some other networking things that we can do. So, for example, you can add an additional network interface to some virtual machines if you're so inclined.

[00:05:41] We're not going to do that here. We're gonna keep the networking fairly simple. Now, there may be times where you wanna add a new disk. So you have your boot disk, but maybe for performance reasons, you don't wanna be, say, running a database on the same disk as your boot disk, you can go ahead and add a new disk by clicking New Disk.

[00:06:00] And here, you can specify information like a name of a disk, the disk source type. And here, you can install a disk with a particular image or a snapshot. So, for example, if you have created another disk and you created a snapshot of it, you can actually use that snapshot as a basis for this disk. So that's a great way to share information

[00:06:21] across multiple disks. If you wanted to have multiple disks starting with the same kind of image, you could use that snapshotting. And then you can also specify sizes, and you can also schedule snapshots. So if you want to have, for backup purposes, a snapshot created at certain time intervals,

[00:06:41] it's very easy to do that. You just configure it here in this set of parameters. So I'm not gonna add another disk, so I'll just cancel out of that. And under Management, we have some other options. These other options have to do with things like automation.

[00:07:00] If each time your virtual machine starts up you wanna run a particular script, you can specify that here. So that's one of the useful things you can do under the Management tab. And then finally, there's something known as sole-tenancy, and sole-tenancy works with only some types of machines. So it won't work with this type of machine, but basically the idea with sole-tenancy

[00:07:21] is that a physical server running in the Google Cloud data centers will only have virtual machines from a single project running on that physical server. So the sole-tenant idea here is that with, if you only want other virtual machines from your project

[00:07:40] running on the same physical server, then you'd want to enable sole-tenancy. Now, without sole-tenancy, it may be the case that a virtual machine in your project runs on a particular physical server. Let's call it server one. And then one of my virtual machines from my project is also running on that same physical server one.

[00:08:01] Now, the two virtual machines are logically isolated. So from a security perspective, they are logically isolated. But there are use cases, high security use cases, where you don't even want to depend on the virtual isolation. You wanna make sure that only virtual machines within your one project

[00:08:20] are running on the same physical hardware. And so that's what sole-tenancy can do for us. So let's go ahead and create this new machine. And now we'll see we have two machines. One of them, our gce-instance 2, is still creating,

[00:08:40] so it will just take a minute. Okay, so now we have two gc instances, and I'll just click on this just to show you we're going to get the same kind of information we saw earlier which is that we are running, the system is running, we're in us-west1-b,

[00:09:00] and let's see. Our network is configured as we expected, and the size of our disk is 40 gigabytes, so that's 40 now. So, again, what we've done here is now created a second virtual machine. And so those are the basic steps with creating virtual machines. You can either start a virtual machine very quickly

[00:09:22] by accepting most of the defaults, or you can do what we did in this video which is to spend some time kind of configuring it and tailoring it to our specific needs.