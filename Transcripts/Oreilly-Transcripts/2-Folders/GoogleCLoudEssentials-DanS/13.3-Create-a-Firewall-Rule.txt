[00:00:06] - When we're working with networking within Google Cloud sometimes we wanna restrict traffic to certain subnets or maybe ensure that some traffic can get in from different sources. And for that we can use firewall rules.

[00:00:21] Now, in Google Cloud to firewall rules are created within the VPC network. So for example, I'm at my VPC network page here and I'm going to navigate to firewall. So we have here a list of firewall rules that exist. And these firewall rules control different kinds

[00:00:41] of ingress and egress. We can customize firewall rules to allow certain traffic in and certain traffic out. Well, to do that we can create a firewall rule. So let's jump over to creating a firewall rule. And again, this is the kind of thing where there's a lot of planning that actually goes into deciding

[00:01:01] what the firewall rules should be. We're not covering any of that kind of discussion or how you go about making choices. I just wanna show you how you can use Google Cloud's create firewall feature to implement a firewall rule that you've already made certain decisions about. So let's say we just name our firewall rule

[00:01:21] GCE Firewall Rule 1, and this is a test firewall rule. And we can decide whether we want to leave the turn the logs on or off. Again, I'm gonna leave those off because that can get kind of expensive. We can choose a network that we specify

[00:01:42] this firewall rule for. I only have a single default network, so I'm going to use that. Firewall rules have priorities and they'll lower the priority number the sooner it gets evaluated. So if there is a rule that denies

[00:02:00] certain kind of traffic and it has low priority number, then that's gonna get evaluated first. So that will effectively deny the traffic. The maximum number is about 65,000 or so. So by default we've set priority rules at a thousand. And so you can adjust the priority number. And for example, that is a good example of one of the decisions you make in the planning stage

[00:02:21] when you're designing networks and you're planning firewall rules, you choose a priority that makes logical sense given the other firewall rules that already exist. And then we specify the direction of the traffic. So does this rule apply to traffic coming into the network or traffic leaving? And we can determine if there is a match,

[00:02:44] like the conditions match a certain traffic pattern, we can either allow the traffic to go through or deny it from going through. And then we can specify targets. So where does this rule apply? And we can apply it to all instances in a network. We can apply it to targets or instances in the network

[00:03:02] that have certain tags on, for example, the virtual machine. Or it can be applied to a certain service account. I'm just gonna select All instances in the machine. And we have the option of a source filter. And so for example, we can filter based on ranges or source tags or service account.

[00:03:21] I'm just gonna apply this to all IP four traffic. So I'm gonna say it's based on IP4 range. And this rule's gonna apply to any traffic. And so for that I specify 0.0 0.0/0, which is cider notation to indicate any IP4 address.

[00:03:43] And let's see, for protocols and ports, let's say we want to allow certain specified ports and protocols. Let's say we wanna allow, say for example, traffic to a PostgreSQL database that we're managing ourselves.

[00:04:00] Let's just say we, for some reason, we have maybe a really old version of PostgreSQL we need to run, and PostgreSQL listens on port 5432. So we're gonna allow traffic in on that. And then at that point, we have the option of disabling a rule, which is different from deleting it. We might want to have a rule and have a rule in place,

[00:04:22] but there may be problems and we need to debug some issues. So we might wanna selectively disable certain firewall rules to help us troubleshoot a problem. So that's better than deleting a rule because then we'd have to recreate all over again. So I'm not going to disable the rule and I'm just simply gonna create this firewall rule. And what we can see here,

[00:04:40] and then at some point it's probably lower down, we have this firewall rule. I'll just take a minute to find that. Actually, why don't I just search for it. GCE Firewall Rule 1. There we go. And that is our firewall rule. And so we have created a firewall rule here,

[00:05:00] and you can do this, again many times, you can have many different firewall rules. And actually we'll take a look at creating firewall rules in the exercise associated with this lesson.