[00:00:06] - The Cloud SDK, or Software Development Kit, is a library of programs that we use when we wanna work with Google Cloud from the command line. Now, we've seen in previous videos how easy it is to work with Google Cloud using the console.

[00:00:22] Now, console is great when we're doing something for the first time, we're investigating, we're learning. If we're in a more kind of production environment where we wanna do things repeatedly or we wanna standardize things, where we maybe want, say, a number of different people to run a script to set up an environment

[00:00:42] so that they all have exactly the same setup, the Cloud SDK is great for that. And the Cloud SDK works with both command-line tools and programming languages. Now, currently the Cloud SDK supports several languages, including Java, Go, Python, Node.js,

[00:01:00] and C++ and C#. Google sometimes adds languages that it supports in the Cloud SDK, so you might wanna just check the documentation if there's another language you'd like to use it with just to see if that might have been added by the time you start working with Google Cloud. The other part of the Cloud SDK is the command-line interface, which we often call the CLI.

[00:01:23] And the CLI is really useful if you want to, say, use Bash scripts if you're working in Linux or if you wanna use Windows scripts, the CLI allows you to basically issue commands that we can use at the command line.

[00:01:41] Now, another component of the CLI are local emulators. So for some services, like something called Cloud Pub/Sub and for Bigtable, there are actually emulators that software developers can use to run locally and develop locally. And so you can actually develop, say for Cloud Bigtable,

[00:02:01] without actually using the Bigtable service and paying for it. So there are several local emulators that are available within the CLI. Now, within the CLI, there are different commands that are used for different kinds of services. Google has a command called gcloud,

[00:02:20] which is a very generalized command which works with a lot of services. And so, for example, if we work with Cloud SQL or Cloud Firestore, we're going to use gcloud commands. Now, some of the older services, such as BigQuery and Cloud Storage, have their own special utilities.

[00:02:41] bq is the command line that's used with BigQuery. kubectl is actually, it's not limited to just Google Cloud, kubectl is a command-line tool for working with Kubernetes clusters. So kubectl is included with the Google Cloud SDK, although it really is part

[00:03:00] of the family of Kubernetes tools. And then finally, gsutil is a command line for working with the Google Cloud Storage. Now, Google Cloud has also added a gcloud storage command to the gcloud utility. So you can work with either gcloud storage or gsutil

[00:03:23] if you wanna work with Cloud Storage from the command line. Now, the steps for installing the Cloud SDK are pretty straightforward but they do vary by the operating system that you're using. So for example, if you're on Windows, you'll follow one set of instructions

[00:03:40] versus if you're on the Mac or Linux. So the instructions can be found at cloud.google.com/sdk/docs/install. And they're very straightforward. It's just a couple of steps, you know, again, depending on the kind of operating system you're using. If you're on a Mac, there may be different binaries

[00:04:02] that you would download or packages you would download depending on the architecture if you're using Intel versus ARM architecture. But again, it tends to be operating system-specific, so definitely check the docs for the latest information on installing the Cloud SDK.