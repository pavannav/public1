[00:00:06] - Let's talk about Kubernetes clusters. Kubernetes is one of the most significant changes in sort of architectural options that software developers and system administrators have had in quite a while,

[00:00:21] because it allows us to sort of coordinate or orchestrate a large number of containers that work together. And we'll talk about Kubernetes engine, which is a managed service in Google Cloud, that helps us deploy and use Kubernetes fairly easily, much more easily than might be the case if we were trying to run Kubernetes

[00:00:40] on our own infrastructure. So the first thing I'm gonna do is go to Kubernetes engine. And what we'll see here is we have the option to create clusters, and we will do that in a separate video. But before we do that, I just want to set

[00:01:00] the kind of the groundwork for understanding some terms in Kubernetes, which reflect the kind of a shift in the way we think about architecture and application architecture. So for that, I just wanna take a look at a couple of slides. And the first thing I wanna talk about is how containers are sort of packaged up and organized within Kubernetes.

[00:01:23] Now we understand that containers are like lightweight virtual machines. They allow us to package up an application and run it in an isolated compute environment and we can run multiple containers on a single server. Well, in distributed applications that use containers,

[00:01:40] oftentimes we have containers that work closely together. So for example, we might have one container that does a little bit of setup work and another container that does a big bunch of processing or there might be a container that acts as a proxy and does something on behalf of another container. Well, in those cases, those containers have very similar life cycles.

[00:02:01] We tend to create them at the same time. We tend to shut them down at the same time. So it's useful to treat those sets of highly integrated, tightly coupled containers as a single unit. Well, in Kubernetes, we have an abstraction for that. It's called a pod. And a pod is a set of one or more containers that we deploy together.

[00:02:21] So with Kubernetes, we often talk about deploying pods, and sometimes, in many cases, that pod only contains one container, but it may contain more. Well, those pods, when we are ready to deploy them, we deploy them onto servers. In Kubernetes, when we deploy something

[00:02:42] we could deploy it to a virtual machine or it could be a physical server. So Kubernetes has another abstraction called a node. And a node is basically some resource. It might be a a bare metal server, physical server that we run on, or it might be a virtual machine that we run on.

[00:03:00] But in any case, we can run pods on nodes and we can have multiple pods running on a single node. And again, the pod is made up of one or more containers that's running a particular kind of application for us. And then we can have multiple nodes. And those nodes all make up what's known as a cluster.

[00:03:20] And we talk about a Kubernetes cluster. We're talking about a set of nodes that run pods for us. And those pods are made up of containers. So Kubernetes clusters allow us to manage multiple nodes or multiple servers, as a single logical unit. And this has a lot of advantages

[00:03:41] in terms of designing systems that are highly available, highly scalable. So the key concepts to understand about using Kubernetes clusters is that a Kubernetes clustered is made up of multiple nodes and nodes can run on virtual machines or physical servers. And within those nodes, we can run multiple pods,

[00:04:01] and those pods can contain one or more containers each. Now, we typically use Kubernetes when we have a large number of containers that we want to run and manage together. So it's a little different from cloud run. Cloud run is really useful when we have maybe a small number of containers and we'd rather not manage

[00:04:22] a infrastructure of a cluster. But when we have large complicated systems like many enterprises do, Kubernetes engine becomes a better option. Also, with Kubernetes engine we can run stateful applications in our containers which we can't do in cloud run.

[00:04:40] So those are the key concepts we wanna keep in mind as we're thinking about Kubernetes clusters and deploying applications to Kubernetes clusters.