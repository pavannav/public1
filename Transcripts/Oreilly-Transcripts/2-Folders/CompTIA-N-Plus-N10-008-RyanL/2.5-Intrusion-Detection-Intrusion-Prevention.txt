[00:00:07] - So intrusion detection systems, IDS or IPS, this is one of my favorite topics. This is where I start to get really pumped about networking. The intrusion detection system is going to be performing deep packet inspection,

[00:00:20] like what we were talking about with the firewalls, and that is looking at particular flows and saying, if I see traffic that looks interesting, everything starts off with an if condition. Say, if I see traffic and you go do what? I say, well, if it's coming from anywhere destined for my web server,

[00:00:43] then what do we do? I'd say, well for traffic destined to the web server, and let's say, it's on TCP port 80, I want to send any of that traffic to the IPS. So what we'll do is we'll select traffic, you don't typically scan everything because IDS is resource intensive,

[00:01:01] it gets expensive to try to look at all the data. So what we try to do is find our most interesting data where there's most likely to be an attack and then pass it off to IDS. What's IDS going to do? It's going to monitor for malicious behavior. Now that IDS could be standalone, just a dedicated box, screwed into a rock somewhere,

[00:01:20] or maybe it's a running off in the cloud and you just feed it logs, or it could be integrated into another device. Lots and lots of devices today are mixed. They say, here's a single box that can do everything that you need. It's all of your threat control, it's one miracle box. Which is nice from a purchasing perspective.

[00:01:42] If you need to check a lot of boxes and you don't have much of a budget, but when you get to start turning these features on what you're going to notice is they've got performance stats on the box. These vendors going to show you this. Those stats are old. There's a meeting that you're having with the vendor and these stats were built long before that date.

[00:02:01] When those stats were built the box had a level of performance. But since then operating system updates have come out. New signatures have come out. What typically happens is we load more and more definitions or signatures under the box, making it smarter, but what happens to a database as it gets bigger and bigger, it takes longer to find things.

[00:02:22] So in other words, as an IDS or IPS looks at more and more traffic over time, it's going to always need more resources. Something we have to be very mindful of is resource management on these types of devices, very easy to oversubscribe them. So what we'll do is many times, if we've got the budget,

[00:02:41] we'll want to get a standalone device and we'll only send a traffic that needs analysis. We don't want to send it things that are benign. We don't want to send the things that are encrypted, they're never going to be decrypted. We don't want to send it copies of backups or big file transfers that we know are good. We want to send it things that are potentially suspicious.

[00:03:00] We will look at intrusion detection sensors, there's a lot of things that they can do, but most of it is going to be based on signatures. What's a signature? It means there is an attack that was used out there in the wild. We've seen it. It behaves the same way every single time and just like these if conditions we talked about for selecting traffic,

[00:03:20] my signature is also an if condition. It says, if you see some packets that look like this, they do a handshake like that, they try to do an authentication in kind of a funny way, then that's an attack. That's all that a signature is, is a rule. And these always come out after the attack. So the idea of finding something zero-day

[00:03:41] or pre zero-day, it's not very likely. We can do things, we can look for protocol anomalies, we can look for strange behavior that's not what we normally see on the network, but this takes a lot of tuning, takes a lot of training, it takes a lot of base lining. So most organizations I've seen are heavily reliant upon signatures.

[00:04:01] They're ambitious to do more than that, but it sometimes takes a lot of work and a lot of tuning and they don't always have a lot of time. So your IDS is only going to function as good as your signatures are. So in picking a vendor, take a look at how often signatures are being published and what the false positive rate of those signatures are.

[00:04:20] A false positive is when they tell you there's an attack, but there's not really an attack. It was legitimate traffic. We don't want false positives, those are miserable but they're better than false negatives. A false negative was when the IDS tells you, everything's fine and an attack just went by. So your IDS or IPS is going to get better with tuning.

[00:04:40] That's when we go in as an administrator and we define this is what I've got in my environment. These are the types of servers. This is what's important, this is what's not important. We turn off signatures that aren't relevant to us, we turn on signatures that may have been disabled, that are relevant. A lot of times this will come into things like alternative operating systems. Let's say that you've got some Solaris boxes

[00:05:02] or Sun architecture still around. It's well-built, runs forever, maybe it's got a proprietary app on it, seen this in a lot of hospitals. You get that type of equipment in your environment and it's running forever, but is it protected? Is it being patched? Are you still getting updates? A lot of times there could be

[00:05:20] something like a public exploit out there. So if my IDS knows that I've got that piece of equipment and it has a signature enabled for blocking the attack, I'm going to be pretty effective at protecting the device. If my IDS is just set up with the default configuration that's right for most users, it's probably not looking for weird hardware. Like my Sun server, probably doesn't have

[00:05:40] that signature enabled. Cause while IDS might have 60,000 signatures in it, they're not all going to be turned on at once. You've got to go through and say, this is what's appropriate, or what's not appropriate for my environment. And by doing this, you're going to get better performance out of the IDS. The goal of the IDS is to warn us about attacks,

[00:06:00] it says, hey, somebody is trying to do something nasty to your web server. We could typically then go in, try to protect the web server, maybe set up some dynamic blocking. Basically the IDS is a warning mechanism. One of the things that I've configured IDSs to do is to block subsequent attacks. So the IDS sees an attack and while it can't stop the attack itself,

[00:06:20] it can automatically SSH into another device and execute commands to set up a block, or it could communicate through API to a central controller and that controller can put out blocks. So an IDS can be used for alerting and warning. When we look at intrusion prevention,

[00:06:42] what's different about this is that operates in line, which means here's my router, I've got packets that come through my router and they're destined to my web server. The problem for the attacker is before you get to the web server I've got an IPS inline.

[00:07:00] So the packets have to flow through this extra appliance. Because this is an additional device that's inline, it has the ability to drop. That means if I see something and matches a signature, I can throw it away. So if I drop at the IPS, the attack never reaches my web server. With an IDS you're typically

[00:07:21] only getting copies of the packets, so you've got a tap setup, you're hearing about what the web servers getting and you go, whoa, somebody just attacked the web server. You couldn't block that initial packet, but you can talk to other devices and make sure that the offending host no longer talks to your web server. So IPS is a better solution

[00:07:40] but it has to be deployed inline. Target devices are better protected, this is why I say it's a better solution. Is it the best solution? Not necessarily, you got to see what fits your environment the best. A fail open or fail closed? If my IPS is doing a software update, right? You got to patch it and update it just like anything else.

[00:08:01] Well, if I'm patching my IDS, I can't scan traffic. What should I do? The updates is going to take 30 minutes. I'm doing it three in the morning, but should my website be down for 30 minutes? If you say yes, then you would fail closed. Do you say anytime the sensor can't scan traffic, we don't forward traffic.

[00:08:20] Or you fail open, this is anytime we can't protect traffic, let it go ahead and pass through anyhow. Much more lenient, but it could leave you exposed to an attack. Who decides this? Typically somebody at the executive level, because there's going to be a financial penalty. If you stop forwarding traffic, you're not going to be able to process sales.

[00:08:42] That said, if you fail open, an attack might go through. So this is a situation where we've got to, you know, kinda weigh our options. Looking at the IPS, the components inside of it, things we think about, IP reputation. What are we looking at? Well, it's a start packets. When a packet comes in, what's the source IP?

[00:09:02] And we go, oh, it's from this block of IP addresses that's been doing lots of malware attacks, lots of spamming, lots of ransomware. I go, well, I bet you what, (laughs) I'm going to probably throw this away already. Let's keep going what else do we see? We could do protocol analysis. Is the protocol SIP, HTTP, TFTP et cetera,

[00:09:21] is it behaving like it should? How do we know? Because there's RFC, requests for comments. There's white papers that say, thou shalt behave in this order, you do your authentication, then you negotiate the speed then you transfer the file. That's it, if you try to do it in a different order, it's no good.

[00:09:42] This will catch a lot of attacks that you don't have a signature for because somebody is trying to break the rules. Signatures, again, this is our main form of stopping attacks, these are the rules of what an attack looks like. They're very good. The downside is that they only come out after the attack is public and well-known.

[00:10:01] Anomaly detection is a little bit sexier. This is cool because it's smart. It's going to say, hey, we see somebody trying to do OSPF injection on your network. They're trying to forge bad routes. They're going to do an attack to be able to steal other traffic.

[00:10:21] I go, wow, how'd you catch that great news. And you go, well, you never have OSPF in the environment. You only run EIGRP. See if my sensor knows that I run a certain routing protocol, if it sees the other one, it goes, hey, that's not normally here. If I only run IPv4 and IPv6 shows up,

[00:10:40] hey, that's kind of funny. So an anomaly is just what the name says, it's something that is different from normal. But the trick to this, good luck, is you have to describe what normal is. And the better you can describe normal, the better it can define an anomaly. So this is something that requires constant tuning.

[00:11:01] It'll find false positives, where it goes, hey, there is an attack and we go, no, that's not an attack that's just a new file discovery mechanism in Linux, you know, that's just a new service that's turned on. So you'll have to go back and tune that out. So nominally detection looks for things that are bizarre. It can be very useful, but it tends to be pretty hands-on,

[00:11:20] we've got to train it. And your IDS or IPS is going to have a learning capability, told you we've got that funny word, normal. (laughing) So learning gives us the capability to define what's normal. What's a normal number of half-open connections? That's where somebody tries to do a three-way handshake, but can't quite pull it off.

[00:11:41] If normally I see three per day and I see 3,000, I go, wait a minute, it looks like somebody's running an NMAP scan so trying to connect to lots of ports that aren't open, which is responding in these maybe a SYN goes out in a SYN reset comes back, like I could look at that and I can say, hey, that's that doesn't look right. What's happening there with that particular application.

[00:12:02] And we only know that its behavior is strange because we've got this sometimes called a histogram, but a record of what's normal. What does traffic normally look like? What's normal number of UDP connections? What's number of failed connections or resets that I typically see. And by looking for outliers,

[00:12:20] I can say, hey, in the last hour, traffic has been pretty funny and I can just send an alert to the administrator and they can come back in and investigate.