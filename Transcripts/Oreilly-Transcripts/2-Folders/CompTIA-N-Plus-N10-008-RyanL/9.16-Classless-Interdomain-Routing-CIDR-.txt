[00:00:07] - CIDR, is not just the stuff of Renaissance festivals and holidays, But also, (laughs) of networks. Once upon a time, networks were classful. If you think of the routing protocol called RIP,

[00:00:22] RIP version one was a classful routing protocol. So basically, when you said I know about the Ten Network, it assumes the classful boundary of 10/8 for all things. So, you're like, I know the Ten Network. It's like, you must know Ten everything. It didn't get the concept of /28, /30's,

[00:00:40] being able to do whatever the heck we wanted with them. So, the concept of CIDR is what allows us to do subnetting today. Now we've been subnetting through this whole chapter, but it's the first time that we're talking about CIDR, classless inter-domain routing. This basically just says, that as an administrator, you can goof with the network bits, moving them from right to left, and from left to right.

[00:01:02] We aggregate networks by moving bits to the left. We subdivide networks by moving them to the right. This is where, again, I said it's kinda like a throttle. Where you go, "What do you want?" We move towards network bits or we move towards those host bits. So aggregation, if someone says the word aggregation to you,

[00:01:21] they're talking about collecting a lot of routes. Probably? (laughs) They could be talking about a few things, I suppose. But in terms of networking and addressing, aggregating is to say, "Hey, why am I using OSPR for EIG or RIP or whatever your routing protocol is, to tell all these individual entries to someone else? Seems cumbersome.

[00:01:40] Why have all these individual entries in your routing table if you could just have something simpler? Aggregation is possible and it's easy. When you've got a lot of networks together that you want to aggregate, and the network addressing is contiguous. For example, if we look in the third octet,

[00:02:01] we see we're going 10.1.0, 10.1.1, 10.1,2, 10.1.3, 10.1.4, 10.1.5, 10.1.6, 10.1.7. All these networks are described as a /24. Isn't there, like, an easier way that we could group these and make them all a singular network? If we wanted to, we could describe this instead of being,

[00:02:21] 1, 2, 3, 4, 5, 6, 7, 8 /24's. Isn't eight like an interesting increment? Isn't that one of those numbers we could hit? So if we modified our subnet mask, and we worked within, which octet? The third octet. And we wanna go how many bits over?

[00:02:40] Well, we wanna find an increment of eight. So we're gonna go 10.1. Now we're in the second octet. And, I normally count at zero, but I'm going 1, 2, 3, 4, 5, 6, 7, 8. And these are worth 128, 64, 32, 16, 8, 4, 2, and 1.

[00:03:02] So my subnet bits are gonna come over here and there's my interesting number. That's gonna be my increment, the eight. So, we had 16 bits to get to the third octet. Now, once we're in the third octet, use blue here, we've got 17, 18, 19, 20, and then 21.

[00:03:25] If we used a /21 mask, this is basically an increment of eight in the third octet. What does that mean? It means our first network is 10.1.0.

[00:03:42] If it's an increment of eight, the next network is 10.1.8.0. See how that works? What's the broadcast of the previous network? Well, we take 10.1.8.0 and we subtract. Gives us 10.1.7.255.

[00:04:00] That's the broadcast, that's the network. Everything that goes in the middle would be 10.1.0.1 to 10.1.7.254. Isn't that cool? And this is with a /21 mask. So instead of giving somebody eight individual routes, I'd say, hey if you wanna get to 10.1.0.0 /21,

[00:04:26] come talk to me. So this is one single entry that I will send to somebody else. That's one entry in the routing table, that's one entry in their OSPF database. Not eight, it's one.

[00:04:40] If this network goes down for some reason, I don't have to tell them about it. If it comes back up, they don't have to recalculate. They know basically anything that can be reached that's 10.1.0.0 /21 is over here in my direction. This is called aggregation. It's also called summarization. The moral of the story, the take away,

[00:05:01] is we've got a bunch of networks like this, and again, they're contiguous, we can group them together with a single subnet mask. What happens if that kept going? What if it was like, 10.1.8, 10.1.9? Well, we could group just the first eight, cause that was easy, and then go ahead and add two more /24 routes. What's that do? Still takes us down from ten routes down to three.

[00:05:22] They're being advertised as someone else. So there's different ways that we can kinda putts with the network mask to summarize our routes, to have a single entry that summarizes, or aggravates, many more smaller entries. And there's rules about the way aggregation or summarization works.

[00:05:40] For instance, with OSPF, you can only do summarization on your border routers or your boundary routers. So between two different areas, or between OSPF, maybe EIGRP, that's where you can do summarization, You have to be on a boundary. If you look at EIGRP, you can do summarization on any interface, anytime.

[00:06:00] So, there's rules within routing protocols about where and when you can do summarization, but just realize that it is something that's possible, and it's another handy benefit of using subnetting. Ooh! One more thing I wanted show ya. Come over here. Let me just bring up Firefox real quick.

[00:06:20] If you use the search engine called DuckDuckGo, you can type in a network like 10 dot, let's do 192.168.50.0 with a /21 mask. So let's see, 255.255, What network mask do we wanna do? Let's just say like a /28, just for example,

[00:06:43] 240? Actually, 248.0 I think is gonna give us what we want. So, what is shows us here is if I were to do 192.168. Oop, I did it wrong. Wrong octet. We'll get 255.248.

[00:07:03] This shows us that this contains 6 hosts, and if I come over here and I put the 248 in the third octet, 248.0, that's gonna give me my increment of 8. And it says that if you wanted to group everything from 192.168.48.0,

[00:07:21] through would be eight higher than that. So, like 192.160.57.0? 56 would be the next network. 48 plus 8 is 56. That would all be within our range. So here it says everything from, 192.168.48.1 to 255.254 are your valid hosts.

[00:07:43] So, if you wanted to see what a range would be, DuckDuckGo, if you put the IP address and the mask in, it'll tell you right beneath. Which is kinda neat, the way you can group things together like that. In our example, we were doing 10.1.1.0,

[00:08:00] and I said 10.1.1.0 /21, and again, 21 would be, 128, 192, 224, 240, 248. So let's say, what does it look like with 10.1.0.0, with a 255.255.248.0 mask?

[00:08:21] And they say that would include all IPs from 10.1.0.1 to 10.1.7.254. So, all the way up to the 10.1.8 network. Pretty cool right? So just different examples of how you can kinda check your math. I don't think it supports slash notation.

[00:08:40] Yeah, it will just ask you to use a regular IP subnet calculator. But if you type the mask in, for example, it'll go ahead and calculate it, which is pretty nifty.