[00:00:06] - Let's take a look at NetFlow and IPFIX. NetFlow is super, super exciting. After you've seen Syslog, after you've seen SNMP. Syslog is pretty good, right? It gets the alerts, that the router wants to send us.

[00:00:22] SNMP can also have some alerts. We're monitoring things like interface utilization, CPU and memory, maybe fan speeds, voltage on power supplies. But a lot of times what we're viewing is very, very high level. If I want to know what's going on with my router, how busy is it? It's going to tell me that my Gig zero interface,

[00:00:42] is it 61% utilization. That's it. It doesn't tell me about what's going through it. It just tells me a high level about how busy it is. Additionally, we use triple A, which gives us accounting, and the accounting function can give us some byte counts,

[00:01:00] in terms of like how much data has been processed by each user, in which ways. And it's pretty good, but it can be a little cumbersome. Now over time, they've given us some new tools to use as alternatives to just using SNMP. For example, one of them is called Sflow or Sampled Flow. And the first thing to be clear about

[00:01:21] is Sflow is not NetFlow, right? So I've talked to lots of people and I say, "This is switch to NetFlow ?" And they say, "No, but it can do Sflow". Those words sound alike, but they're super-duper different. When we look at Sflow, this is really just a sample. So it's not a full network tap. It doesn't have full visibility.

[00:01:41] It's a full packet capture, meaning, as data crosses the wire, if I'm looking at spoiler, we're talking about NetFlow, If I'm looking at NetFlow, it records what we call IP accounting. So we look at the source destination IP, source destination port number, the protocol,

[00:02:01] the time that it started, the time that it finished, the number of bytes sent. But we're not looking at the contents of the data. We're just looking at, like the metadata. Who called who, for how long? Now what Sflow is, is a full packet capture. It's gonna give you the contents within that one packet. It's probably not enough to be that useful,

[00:02:22] but you know what? It's just part of the way that it works. You get it. So in NetFlow, we don't have full packet capture. We just have the accounting details, the metadata about the connection. Sflow, gives us a full packet capture, but it's partial stream, meaning you might be looking at one out of a hundred packets.

[00:02:40] So just like, imagine you reach out into the river, you scoop up a little bit of water, that's not looking at every water molecule that flows down the stream. You're just getting one that you scooped out. You're looking at what you see. Maybe you put it back when you're done. Sflow is also vendor neutral, so it's something you'll find in lots of devices. This does give us visibility

[00:03:01] into what's crossing the network, but it's just a piece of it. It's like taking a sample out of a river. And this is what we're headed for, this is exciting one. NetFlow. So this is metadata only, you don't get the contents of the packet, but in my world, I don't think I've wanted it that often. What I get excited about, is that all packets are accounted for.

[00:03:21] You can't send a packet out of the network without me seeing it and recording it. I'm gonna have a detail of who talked. I don't know what they were talking about, but I can see the protocols, I can see the times, I can see the amount of bytes transferred. So Cisco built NetFlow, it's been around for years.

[00:03:40] Version 5 is one of the versions, it's pretty common. And what this is gonna start showing us is what's the interface? It's Gig zero. Alright, on gig zero, who's talking? Source and destination IP, source and destination port, the protocol that's in use, the type of service, we'll also see things like the byte count.

[00:04:00] Now NetFlow was Cisco proprietary. Other people out there, created their own implementations. So if we take a look at Juniper, for example, they created something called Jflow. If we look at Walway, they've got something called Netstream. So different people wanted to accomplish the same thing

[00:04:20] and started to do it in their own proprietary ways. Eventually everyone said, "You know what? this is a good deal. Why don't we all do it the same way?" And they do. And that's called IPFIX. There's lots of good documentation out there about the way NetFlow works. There's a lot of tools that call themselves NetFlow collectors, just because Cisco is so ubiquitous

[00:04:40] within the networking space. That said, the new standardized version, there was NetFlow version 10, basically IPFIX came out after. IPFIX is what we do and it's vendor neutral, so you can do this between Cisco, Juniper, F five. It doesn't matter. Everybody can speak IPFIX. And then we can take this information and put it into a central database

[00:05:01] to do all of our IP accounting. So once again, IPFIX is gonna be a standards-based implementation of flow collection. Just because something that you're reading about, doesn't say IPFIX, if it says NetFlow, a lot of what it's talking about in terms of capabilities,

[00:05:20] or in terms of, you know, what they are able to determine for troubleshooting or in forensics is going to be the same. It's gonna be true. So learning about NetFlow is still helpful. Although what you'll probably be implementing today is called IPFIX. It's not exactly the same, but it's similar in some ways. It's defined because it's standards-based

[00:05:40] inside of requests for comments or RFCs. You see the RFC numbers here. Now, an Exporter generates flow statistics. You go, "Hang on, what the heck is an exporter?" If I've got a router, that's got packets moving through it, I turn on IP fixer NetFlow. What's it do? Well, it generates flow statistics.

[00:06:01] Host A called host B for this long. What it does with this flow statistics is, it sends them to a collector. (cursor clicks) A lot of times people refer to that as a NetFlow collector, but we'd also call it an IPFIX collector.

[00:06:20] The collectors receive, and then store the flow statistics. So the router, the firewall, it's not busy keeping all this data, but we're keeping a record of everyone who's ever communicated, and we're sending that to the collector. An Exporter, that's a device that's generating that flow output, can send flow details to multiple collectors.

[00:06:42] And this works the same way. A collector may have feeds coming from many exporters. So maybe I've installed, there's a pretty cool tool called scrutinizer, so maybe I've installed scrutinizer, to collect all this NetFlow and IPFIX data. I can point my routers there. I can point my switches there.

[00:07:00] I can point my firewalls there. And I can get logging data from all of them.