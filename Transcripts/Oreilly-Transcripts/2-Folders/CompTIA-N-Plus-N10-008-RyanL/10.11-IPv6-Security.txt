[00:00:06] - Let's go ahead and take a look at IPv6 security. One of the things we'll discuss is first off is IPv6 more secure, is IPsec built into IPv6 and is Layer2 any more secure in an IPv6 environment. Things to consider.

[00:00:20] And the reason that we want to think about this is, well was IP version four secure? And typically we would say, no, it isn't there isn't any type of security, it's more about connectivity. And if I want security in IP version four, typically the tool that we use is called IPsec. It's an open standard to apply encryption to our data, leveraging ESP.

[00:00:40] So if we think about IPv4, pull myself out of this, you have an IPv4 header, which is our envelope, this is source and destination network traffic. Now at Layer4, you typically have TCP or UDP with an IPsec VPN. What is this Layer4 header? It's not TCP or UDP it's ESP.

[00:01:02] And then everything that this carries, is going to be encrypted. So that could have been your original IP header, your original TCP header, your original data. And then what we're actually moving from one place to another is an IPv4 header, that carries a Layer4 header that's ESP.

[00:01:23] So this is IP version four, let me get that four out of there, just so it's not confusing, Layer3 and Layer4 and we're carrying ESP. So when it says IPv6 any more secure than IPv4 is, that's kind of a broad statement, but what I would like you to think about is IPsec.

[00:01:41] This is our tool to make IPv4 secure. So a lot of, a lot of books I've read a lot of courses I've taken, say ESP support is built into IPv6, IPv6 has security. Well, yes and no kind of. Let's talk about how it works, we're not doing anything different in IPv6,

[00:02:00] than leveraging an ESP header. If you actually look at the header for IPv6, it has nothing to do with encryption. So you'll see this phrase that it's built in quite often. But if you actually look at the header, there's nothing there to do with IPsec, IPsec is leveraged in IPv6, the same way it is in IPv4. And that's through the use of a separate Layer4 header,

[00:02:22] that you have to point to. That header is responsible for encapsulation of everything inside of it. So in my opinion, it's just an opinion, I don't think it's a whole lot more secure, I think it works very similar in the same way. Is Layer2 more secure in IPBC network? Well, that's neat because it actually can be.

[00:02:40] So here's IPv6 header. We've got the version in version four it would say 4, version six, it would say 6. In version four, we had a type of service. The type of service field is leveraged for quality of service, quality of service is where we say, not all packets are equal that's bit torrent, that's voiceover IP. That's an email.

[00:03:00] We've got different urgency for each type of data. So instead of calling it type of service, they just call it traffic class. We've got a label here called Flow Label. It's a 20Bit ID to identify like a circuit. I haven't seen a lot of operating systems leverage this yet. It's just kind of there for future use, but it's neat because MPLS, if you looked into it,

[00:03:22] it supports a 20BIT label as well. And you could potentially do some neat things with traffic engineering, I just haven't seen anybody do it yet. So just think about that for future use the payload length. Well, that's what we're actually carrying with this header. It's an envelope. The next header field, this is what's pointing at Layer4.

[00:03:42] This is where we go, oh, I've got IPv4 inside of IPv6. This is where I say, oh, I've got ESP inside of IPv6 so I can actually perform some encryption. Over here we've got the hop limit. Hop limit is very similar to Time To Live and IPv4, we've got a hop limit so that if

[00:04:00] there's some type of routing loop, where we say which way is Canada? And one router says it's that way and the other router says it's that way. You could create a loop that packet goes back and forth and back and forth. This'll drive up CPU utilization. A packet will never make it there it's just a waste. So when routing loops occur, TTL prevents the packet from living forever.

[00:04:21] It detriments on each hop. Eventually it hits zero an ICMP message goes back, that says sorry, your packet didn't make it. So in IPv6, they took the concept of Time To Live and they call it Hop Limit. So it's exact same thing. One of the pieces that's different is it we're carrying 128BIT addresses both for source and destination.

[00:04:43] This is 256 bits total. Well, when we looked at IPv4 that had 32BIT addressing, so you had 64 bits of address. This is basically heavier. Why? it's just a big number to have to move back and forth. What does that do? It increases the header size.

[00:05:02] So the IPv6 header is a bigger envelope than an IPv4 header. And it also has fewer fields. You'll notice that IPv4 has got more fields because it supports things like fragmentation. All right. So in the event that we're going to use IPsec, you've got this next header, which says I'm carrying ESP.

[00:05:23] And the way that achieves ESP, is through something IPv6 calls an extension header. An Extension Header is an additional field of information. Well, that's very much like what we did in IPv4, where we needed to add a header for ESP. We say, this has all the fields the ESP needs

[00:05:43] to encapsulate and decrypt or encrypt information. And there we see it with ESP. So when we look at IPsec, there's two things that we want to understand transport mode and tunnel mode. And this is true with IPv6, that we need to understand these a little bit more so

[00:06:00] than with IPv4. The reason that I say that is with IPv4 Nat was heavily utilized. So we didn't have end point to end point communication across the internet as often. You could with publicly addressed servers, of course. With IPv6, there's a very good chance you're going to have end point to end point communication.

[00:06:21] If two hosts want to communicate and they want to encrypt everything between them. This is called transport mode. If you've got devices that don't know anything about encryption, like let's say phones and printers and just a general workstations within the office and let's say that's an office in Tampa. And let's say that we also have an office in

[00:06:40] well, let's say maybe Nashville. If I want to go from the Tampa office to the Nashville office. When I create a connection there, let me draw here a little bit, what's going to happen is it's going to be used in tunnel mode. Here's a router in Tampa. Here's, an, a router in Nashville. You got the internet in the middle. You've got un-encrypted traffic here.

[00:07:01] It comes into the router. The router has a security policy that says that traffic needs to be protected. We add the ESP header. We add a new IP header, that says it's going from this router to that router. When it gets to the Nashville router, the ESP header is inspected. We've got something there called a security perimeter index

[00:07:20] that allows us to find basically a configuration that says, this is how you decrypt those packets. This is the encryption algorithm you use. This is the hashing algorithm, this is the key that you use. And then what's going to come out of that is an un-encrypted data. So un-encrypted within the networks, encrypted across the internet.

[00:07:40] See why that's called tunnel mode? It's almost like driving down the road. You're looking around at the, you know, looking out the windows, looking at the sun, looking at the trees, then you hit this tunnel and you can't see anything. Well, that's very much what's happening between the two routers that traffic is encrypted, if there's devices in the middle they can't inspect it. And then it's decrypted the same way that we would come out of a tunnel on the other side of a mountain.

[00:08:02] So just to kind of talk about the transport mode, how that's different. If you have a server here, let's put an S on it, and I have a laptop over here and they both support IPsec, and they both support IPv6 and they're connected directly or without any type of anything, breaking it in the middle.

[00:08:20] I can go from one system to the other and use ESP the entire way. In other words, when I send data onto the network, it's not like what we saw in the first example, notice that it was un-encrypted data. You had a phone, maybe you had a printer, maybe you had an Xbox, just some simple device that isn't really thinking about crypto. None of the IOT devices are doing crypto

[00:08:41] like they should be. So this un-encrypted traffic goes out. We scoop it up with our tunnel and we say, this is traffic that should be protected. It is protected to wherever it needs to go. Then it comes out un-encrypted on the other side. Getting this transport is from end to end both systems support ESP, both of them understand that

[00:09:00] this traffic should be protected. And in this scenario the traffic is never in the clear. Now each of your host has to support IPsec. But when we say IPsec is built into IPv6, IPv6 implementations in most operating systems have native support for ESP. So that's what actually allows us. Let's take a look at last at the way that Layer2 works

[00:09:23] with IPv6 I said, is, is IPv6, any more secure? I didn't, you know, I couldn't jump on a soapbox or mountain top and shout from, you know, across the Hills that, yes, it's definitely better. It's very much the same. In my opinion, at least the way IPsec is implemented. When we take a look at Layer2 however,

[00:09:40] we've got this concept called ICMP Neighbor Discovery. ICMP is actually replacing ARP, ICMP does a lot of things for us in V6. Neighbor Discovery is where I can figure out what Mac address goes with the particular IPv6 address. We've also got this concept of Stateless Address Auto Configuration.

[00:10:00] That's where /64 just get assigned to you as a prefix. And the last 64 bits or host ID is just made up by somebody. This is not going to be secure, right? We don't have any logs or any records of who's coming or going . Contrast that with DHCPv6, here, we can have reservations, we can have a log, we've got a record of when people

[00:10:21] are coming and going, and we can record this so that if there's crime, if there's problems, we can at least have a Mac address to go based on. Additionally, we've got the concept of Router Advertisements, which are done through ICMP and they say, Hey, this is our network prefix, why don't you come join us? And when devices need Route Solicitations, cause these things are on a timer.

[00:10:40] Let's say an announcement just went out and I show up and they go, Hey, what the heck is going on here? I didn't, I don't, I don't see any IP addressing. Also not a Route Solicitation ,It's a multicast. And when the router hears that he goes, wow, I was going to send a, I was going to send one of these route advertisements in about five minutes, but because I got the nudge, I'll go ahead and I'll send one out now.

[00:11:00] So a Route Solicitation nudges router to send a Route Advertisement before it's timer is expired. It'll go ahead and just send those out anyway, but this will nudge it. So it's kind of like a little bit similar to DHCP in the sense that it is a request for IP addressing and it comes back. The difference though, is that again,

[00:11:20] that route advertisement. A lot of times, will just tell you the gateway, it'll tell you the prefix. And with mechanisms like SLAAC, Stateless Address Auto Configuration. You go ahead and you'll build that value on your own. So ICMP Neighbor Discovery, we talked a little bit about Route Advertisements, these are generated by the router.

[00:11:40] Route Solicitations, these are generated by end points that want to Route Advertisement. You have Neighbor Solicitations, which are used to discover Mac addresses, and you have Neighbor Advertisements that come back. Additionally, if there's a problem with the gateway, and we would like to tell you to use a different gateway, we can issue something called an ICMP Redirect.

[00:12:00] We say, well, normally you would use dot one, dot one is a little busy right now, or we've got an interface malfunctioning. Why don't you go ahead and use dot three? So the secure piece, which is really pretty nifty is called SEND or Secure Neighbor Discovery. And the idea of SEND is that we're going to use cryptographically generated addresses.

[00:12:20] What's that mean? Well, we're going to use asymmetric encryption. You go, well, what's that mean? Remember with symmetric encryption, we use the same key on either side. This is used in the bulk encryption of data. So if I've got a VPN, if I'm using a WPA2 or WPA3, of my wireless. You have a key that's issued on both sides, and that's what you use to encrypt and decrypt data.

[00:12:42] We call that a bulk data encryption. Bulk data encryption is at least, from what I've seen, always symmetric. Asymmetric encryption is where we use two different keys. So the idea of these two different keys is you have a private and a public key, anybody can know your public

[00:13:00] only you know the private. And anything that's been encrypted with the private can only be decrypted by the public, anything encrypted by the public, which everybody has, can only be decrypted by the private. So if I have a public key and I publish it, anybody could send me a secure message ' cause it could only be decrypted by me. Not super new, this has been around since the seventies,

[00:13:20] but if we look at symmetric encryption, this has been used for thousands of years, Asymmetric encryption, maybe in the last 50 or so. So Asymmetric encryption on its own, not really a new concept, what is a new concept of saying, Hey, we've got the 64 bit host ID that we're going to create. Why don't we use that?

[00:13:40] Then we got to come up with something anyhow, instead of using Mac addresses, why don't I just leverage my RSA key pair? I'll take a portion of my key space and I'll use this as my address. And also when I'm exchanging information with people, they can verify that this information really came from me because I'm the only one that has the private key. This gives us the ability to support

[00:14:01] public key infrastructure.