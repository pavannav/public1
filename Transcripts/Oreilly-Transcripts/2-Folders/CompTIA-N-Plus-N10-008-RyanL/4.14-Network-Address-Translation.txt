[00:00:07] - [Lecturer] All right. Let's take a look at network address translation. What is the need for NAT? Well, back in the nineties, we were depleting or running out of IP addresses because everybody was jumping on board. When they first designed TCP IP,

[00:00:20] they were trying to figure it out in their heads. How many bits to use, how much space should they create? What's enough. And they came up with 4.2 billion. They said, if we create a 32 bit addressing scheme, we'd have 4 billion addresses. You know, they're connecting a couple of computers at this point, there was like, there's no way we're ever going to reach 4.2 billion. Well, IP was more popular than they thought.

[00:00:41] It beat out all the other competing protocols. And there we were running out of IP address space. So they came up with this concept of network address translation. They said, well, you know, NAT is just going to be used as a temporary band-aid until we get IP version six created IPv6 is going to move us from a 32 bit to a 128 bit addressing scheme,

[00:01:03] what's that really mean? Well, 32 bits gave us 4.2 billion hosts. If you move to 33 bits, you'd have 8.4 billion. If you moved to 34 bits, you'd have 16.8 billion. If you moved to 35 bits, you'd have 32 billion.

[00:01:20] Holy crap. We started at four. Can you see how it's exponential? Every bit that we add is going to make a huge leap in the number of available addresses, just stressing that IPv6 gives us room for 340 undicillion values. It's kind of hard to imagine. I call it an intergalactic address, addressing strategy,

[00:01:42] more addresses than we can possibly use. And we mean it this time. So looking at RFC 1918, this is a concept that we're going to come up with IPV four addresses that should be used internally. So if you've ever seen like 1 92, 1 68 IP addresses on your

[00:02:00] home networks, this is part of it. 1 72, 16 through 32 is part of it. The 10 network, all of it is part of it. So they say, if you're Oracle, if you're Coke, if you're Pepsi, if your monster energy, if your Ferrari, Mercedes, BMW or Hyundai, everybody can use the 10 network internally. And then what we'll do is we'll translate you to a unique

[00:02:22] public IP when you're on the internet. What that means is that on your private network, you've got tons and tons of space that you can use. You address it the way that you want. Nobody cares. It's separate from the rest of the world. When you want to talk to the rest of the world, we'll translate a private address to a public address.

[00:02:41] So globally unique addresses are shared by the internal users. This is what's going to happen when we perform NAT on the gateway. Everybody's private address turns into a public one. So looking at RFC 1918 addresses, these don't have to be registered. They're built for internal use, they're not routable in the public internet.

[00:03:00] So if your network IP is 10 1 1 5, and you want to do a query to Google, that has to be translated to a public value, or you'll never get an answer back. Again, there's our 10 slash eight network. There's a 1 72 16 through 32, 32 is going to be outside the range. So the real limit is from 16 to 31. 2 55 . 2 55 .

[00:03:25] When you see a number that's written like this, think about the way it like at a store. I go, how much is that? And they say it's $32. No, I'm just kidding. In retail. They'll do things like 31 99, just to mess with you. We know that that's one 10th,

[00:03:40] one penny, less than $32, right? So when you look at an IP address, we know 1 72 32 is the next network. What's one penny, less than that. 1 72 31, 2 55 2 55. And of course, 1 92, 1 68, anything is going to be private space.

[00:04:00] So what we do with these private addresses is we change them. We give them a mask that used to call us IP masquerading, and we change the private to a public. We say that the internal host borrows the public IP for the session, the host presents itself as the public IP to the public system that it's talking to.

[00:04:20] Of course the response comes to that public IP. And then that gateway changes that response back to a private address when the response comes through. Whenever you're done communicating on the internet, it'll time out and the IP address that you were using returns to a pool. So just to be clear, there's different ways that you can perform address translation.

[00:04:41] In the old days, we used to do one to one, meaning that if I had 50 users on the inside, I' have 50 IP addresses on the outside, I have to contact my service provider, tell them the number of IP addresses that I need and paid monthly for access to those IPS or alternatively purchased IP addressing outright,

[00:05:01] which is called provider independent addressing not any cheaper, by the way. So with NAT, I had to have a one-to-one ratio. If I've got 50 devices inside, I need 50 public IPS. And those IP addresses are used on demand. So maybe I've got a hundred users,

[00:05:20] but I can have 50 simultaneously on the internet. So remember that NAT has a one-to-one requirement. What we do today, much more commonly, is referred to as port address translation or PAT, PAT is also known as multiplexing. And this is where we use one real-world address. This is whatever Verizon or Comcast or frontier

[00:05:41] is going to give me. And I share that real-world address by many tenants on the inside. This is how all of our home networks work going out through the cable provider. A single IP is a shared across thousands of simultaneous connections. How is that possible? Well, we look at the sessions and we track them based on port number.

[00:06:02] The port number is from one to 65, 5 35. So anything in that range can be used and can identify a unique solution or unique connection. Each session requires a unique port number. How does it look? You have a client on the left talking

[00:06:20] to a server on the right. The server is listening on 4 43 for HTTPS. When the client connects, it picks a unused random port number 3, 2, 1 8, 7, that'll work just fine. Anything that comes from 3, 2, 1 8, 7 to 4, 4, 3 is part of this flow. Any of the responses will go from 4 43 back to 3, 2, 1 8, 7.

[00:06:44] It's the source port that makes the communication flow unique and allows PAT to function. All of the sessions that go through the firewall or through your router are maintained inside of a table.