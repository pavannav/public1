[00:00:07] - All right. In this module we'll take a look at doing a lot of the same things we discussed in windows and Linux, but also in OSX. I'm not gonna use any unnecessary verbiage here. I'm gonna at least try not to tell you anything that I told you already,

[00:00:20] because I think a lot of it is just simply a repeat. So for example if I click here on Wi-Fi, one of the things that apple does for us is it shows us all the networks that we've previously connected to, which is kind of neat. It also has a list of the type of security that was used on each of those networks, and last but not least, you've got auto join here. So the auto join could be something that you wanna do

[00:00:42] or that you don't wanna do. Realize that attackers that are out there, they can spoof an access point, they could pretend to be Starbucks, they can pretend to be McDonald's, they can pretend to be your local airport. So whenever you come along and you see that SSID, you may automatically join it just because you think it's something that you know.

[00:01:00] So there you are on the opposite side of the town from the airport, and you see the airport's SSID, you'd go ahead and join it cause you think it's a familiar network. So that's one of the neat things we can do just in terms of security is disabled at auto join. Only use it when we mean it. Down here, there's this feature called remember networks that this computer has joined.

[00:01:20] I think that's pretty handy to use. And a lot of times I'll just disable the auto join. But if you wanna just say, I wanna do it each time unique and fresh, you can disable that checkbox. Now coming over to the TCP/IP tab, we've got the ability to, of course set our IP address, static or manually, and we've got IPv4 and IPv6 configuration

[00:01:40] both under the same tab. Next coming over to the next tab, we've got DNS. DNS will typically be discovered through DHCP for you, but if you want to manually define our own DNS server, you could do that here. Using the if config command is very similar to what we do under Linux, you'll probably need to use the sudo to run the command as administrator.

[00:02:01] And this just like when we looked at Linux, will show us each of our interfaces. So just like we've seen before, there's our loopback zero at 127.0.0.1. Here's my ethernet interface at 192.168.254.58. Here we see an arp -a again. So windows, Linux, or OSX, arp -a is going to show us our arp cash.

[00:02:21] Really, really handy. A lot of times if there's a system that I'm trying to track down, here's the IP address, here's the Mac address. If I wanna find that Mac address in my system, what I'll do is I'll log into the adjacent switch. I'll do show Mac address table, pipe and then include, just, this is kind of like grepping,

[00:02:40] but you can do pipe include in the Mac address that I'm interested in. And this switch will say, oh, that Mac address, it's over there off of this interface. Cool. I can do show CDP neighbors, or show LDP, see if that's a switch that's over there, if it is, log into that switch, show Mac address table, and then it's gonna show me where the Mac address lives. I can go device by device by device

[00:03:01] until I finally get to the end point. Here we've seen netstat. Netstat can be used with a lot of different parameters to show you lots of different output. In this example we're just using it to look at the routing table once again. So to view the routing table under OSX, one way to view it is netstat -rn. The nice thing about that

[00:03:20] is that command will work in different operating systems. So once we memorize it once, we can use it again and again.