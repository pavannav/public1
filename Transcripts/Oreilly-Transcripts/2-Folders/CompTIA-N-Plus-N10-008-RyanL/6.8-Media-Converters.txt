[00:00:06] - All right. Our last component to talk about in this lesson is a media converter and media converters are pretty cool. Just taking the particular format of communication, like RJ45 or Ethernet in and allows us to go out over fiber.

[00:00:21] That's it. You could convert from all sorts of different formats. So anytime I wanna move from one style to another, we leverage a media converter. This is effectively a layer one device that receives and transmits on one medium and then cakes and passes it through to another. This gives us the ability to extend networks

[00:00:41] beyond the 100 meter limit. Remember, there's limitations on copper and fiber can go much, much further. Some examples are a fiber to Ethernet adapter. It's gonna be copper on one side, glass on the other. Could be fiber to co-ax. This is another example of media translation that we require. Lot of times if we think about what a router does,

[00:01:01] router of course routes packets but it's also responsible for media translation. You'll see Ethernet coming in and you'll see interfaces or expansion slots for interfaces that allow us to leverage WAN technologies. We can leverage ATM, we could leverage cellular, we might leverage frame relay,

[00:01:20] we might leverage ISDN, we might do a T1 or a T3. So you're bringing Ethernet in and then you're going out over something that is not Ethernet. That's the concept of media translation or being a media converter. Realize that that happens as a capability of many routers but if you want a standalone device to perform media translation,

[00:01:40] the most obvious reason is to take maybe a copper signal and extend it a bit further, you could use a media converter to do that. Lot of times what we'll see on switches, is a port. So let's say that this is a switch, on the switch you'll have these ports. What goes into this port is called an SFP module.

[00:02:03] Small form-factor pluggable and you go, "Hey, Ryan! That was two Fs" I know, I didn't make the acronym. But small form-factor pluggable is what they call this. Basically they say you've got a socket, that socket will accept a module and that module is gonna do

[00:02:20] whatever it is that you need it to do. So if you want it to do co-ax, if you want it to do fiber, if you want it to do, you know Cat6, you'd get a module for that style of communication and then plug it into that slot. Additionally, you could have a translator from single mode to multimode if you needed it. Just putting that out there.