[00:00:06] - [Instructor] So as I mentioned in the previous module, I don't think v4 is gonna go away anytime too soon. But we've got v6 here on the horizon. A lot of us are using it, especially in mobile environments. We don't even know it. So dual stack is the process of using v4

[00:00:21] and v6 at the same time. You're probably doing it now. You might've been doing it for the last several years, and you just didn't realize. One of the easy ways to test is just to go into Google or your search engine of choice, and type, what is my IPv6 address? And it should come right back and it should tell you. Unless you're on a v4 only network, which is fine as well.

[00:00:42] Looking at simultaneous v4 and v6 is what we refer to as dual stack. And when we look at it, it's kind of funny, right? IPv4, it was normal just to have a single IP address on your device. Maybe 10.3.3.5 is my computer. I'll have a loop back of 127001.

[00:01:00] We all do. But that was basically it. When we look at IPv6, you've got those link local addresses. We talked about this previously. Does anybody remember what the prefix is? This first 16 bits? For link local. Remember these don't get forwarded between broadcast domains, a router doesn't pass these.

[00:01:21] These are those FE80 messages. And this is where, on a local network, just again, within your broadcast domain, you can do discovery of other services, and you can form adjacencies using these link local addresses. We've also got this concept of random IPv6, and that's just where we're gonna have

[00:01:40] an IPv6 address that's generated, and it's gonna constantly change. Microsoft does this for your privacy. And there's deprecated forms of this as well. So a lot of times you come across a device that says I support IPv4, IPv4 only. What we'll do as an end point, is support both v4 and v6. So in dual stack operations, realize that v6 is gonna be preferred to IPv4.

[00:02:02] Is it gonna happen by the user? Is it gonna happen automatically? What goes on here? Well, anytime you type in a domain name, so www.pearson.com, you're gonna get back in A record. What was that for? V4 or v6? If you said v4, you're doing good.

[00:02:20] And we've got the quad-A record, which is the entry we create that gives an IPv6 address. So you might ask, who is pearson.com? You'll get an answer that says, here's the v4 answer and here's the v6 answer. Your computer will look at its local routing table and interfaces, and if you've got a v6 interface, it's gonna try to go there through IPv6 instead of trying to connect through IPv4.

[00:02:41] So just realize it will always be a preferred. Ethernet itself, it doesn't really mind. That's the whole beauty of the OSI model. You've got layer two, with Ethernet, you've got layer four with maybe TCP or UDP. They don't really change a whole lot because IPv4, IPv6 is in the middle. In your Ethernet header, you'll see an Ether type that says,

[00:03:00] this is what I'm carrying as payload. And it points to the next header, it'll have a value there for v4, it has a separate value for v6. Again, TCP doesn't really care what it's riding on top of, it could get there via IPv4, or it could get there through IPv6. DNS, it doesn't work any different, somebody types in www.pearson.com, and the webpage loads.

[00:03:21] So for an end user, it doesn't change a whole lot. If we take a look at from an administrative perspective what has to happen, you're gonna have separate firewall rules. You're gonna have a separate routing table. You're gonna have separate access control lists, and you might have different types of application support. What's that mean? Well, applications are written to perform a certain way.

[00:03:41] Maybe they talk to databases on the backend, maybe they do some type of clustering. If clustering is built in, if redundancy is built in, or high availability or failover, those functions need to be modified to be able to support v6. It will have to have v4 rules and v6 rules for all of these. Just good things to remember.

[00:04:01] So dual stock addressing will look something like this. We've got an IP address of 10.1.100.13 We have a subnet mask of /24 who are 24 consecutive ones, which gives us 255.255.255, make that a five, 0.

[00:04:21] Our standard subnet mask. What's the gateway? .1. Standard class C. Looking at our IPv6 address, notice we've just got the kind of standard /64 prefix. So here's our IP, 2001:ABCD:4321:100::13.

[00:04:41] So remember that's all of those zeros squashed down. That would be the long hand representation. See how much easier that is to type if you were to do a ping, or if you're gonna try to define this, maybe an entry inside of a GUI. This would just be so long to write out, so that's why our shortcut is handy.

[00:05:01] Same thing, remember that we can use static IP addresses. If you like using gateways as .1, look how we can shorten it. In the scenario with dual stack, we have these different sets of addressing. Are these the only addresses? No, because you'll have your FE80 address for link local. Also, if this is a Microsoft system, and I'd assume many of you are on Microsoft systems,

[00:05:21] it by default is giving you a bit of privacy with IPv6. The way that it does this is it receives the prefix, the first 64 bits, it generates the host portion, and it uses this for several hours. At a certain point, it generates a new suffix or a new host ID, and all of your new connections

[00:05:40] are coming from that host ID. Let me show you what I mean. So that's my illustration of a computer. I'm building connections, and I'm just using IP address A. When I say A, imagine any IPv6 address that you wanna think of. Now let's say four hours later, I go, it's time to change to a new IP address.

[00:06:01] I create a new IP address, we're gonna call it B. And all of my new sessions, every time I create a new SSH session, a new web browsing session, a new phone call, a new software update, all of my new sessions will be sourced from the B address. But if you do a configuration lookup on your IP addresses,

[00:06:20] you'll see that you have two of them. You'll have IP address A, which we'll say deprecated. We're only using this for your older connections. Maybe you had a remote desktop session into a remote window sever, and while you were configuring that windows server, the operating system had a timer, and it said time to create a new IP.

[00:06:40] Well, it creates that new IP, it associates with the interface, but it still leaves the old one there for your existing connections. So looking at our gateway over there, we've got 10.1.100.1, and of course, 2001:ABCD:4321:100::1.

[00:07:00] These are the static IPS that would be allocated to that interface of our router or the VLAN interface of a layer three switch. Remember with DNS records, we've talked about this a few times, you have to update these to make it easy for your users. So just as we'd create an A record, which says www equals this IP address, the quad-A can do the same thing to IPv6.

[00:07:22] Your DNS services run on UDP 53. That's an important one to remember. As you go through and you learn a bit more about DNS, you'll see that it can also use TCP. TCP is used for something called a zone transfer. And this is where I can go to a DNS server and say, hey, DNS server,

[00:07:41] can you tell me about everything that you own? Tell me about everything that you know, like a full directory download. This is really good from one DNS server to the other. If you've got maybe multiple DNS servers and you wanna keep everything synchronized, but it's not something you wanna have available to the outside world. It could be a security concern. So it's a good one to know,

[00:08:00] a lot of people aren't familiar about the specifics. But if you're creating rules for what can come into your network from the outside internet, and you go, oh, I guess DNS needs UDP and TCP. Not true. You want UDP there for the look-ups, and you want TCP there only if you intend to do zone transfers to transfer all of the records that the DNS server knows about

[00:08:20] from the inside to the outside. If you're not trying to do that to external servers, shut off TCP. There is a weird scenario where you could need TCP for a massive reply, but it's kind of way out there in left field. And I'd say 99.59% of the time, you're doing everything over UDP 53. And again, your DNS service runs an IPv4 or IPv6.

[00:08:42] Meaning, here's my server on port 53, we're basically listening with the service. Maybe this is a Linux system and we're using bind. Bind is listening on UDP 53. Now, when you come in to talk to it, it doesn't matter what you used as a layer three transport.

[00:09:01] This could be IP version four. It could be IP version six. And either way, it's really just about getting the port 53. Why? This is the socket that's hooked to this process. Think of bind as like bind.exe. That's what it would be in Windows. It's an executable process that's running in Windows,

[00:09:20] or it's running a Linux in your memory, and it's processing these queries. So a query comes in, we do a lookup, and the response goes right back out. Doesn't matter whether it's an IPv4 response or an IPv6 response. It's an important concept.