[00:00:07] - So let's take a look at the public addresses, now that we know what private are. In the last section, I'd mentioned class A, class B, and class C, which may have been new terms to you. These are ranges of public IP addresses,

[00:00:21] and this is just the way that we've carved out the allocated space. So notice that class C goes from 192 all the way to 223. The next step, if you went to 224, is gonna be class D, and this is where we start getting into multicast addresses. This is IP addresses

[00:00:40] that are allocated for a different purpose. Instead of doing unidirectional traffic, or unicast traffic from one host to another, multicast is to talk to multiple devices at once, almost like a radio station. So realize that there's a class D for multicast, there's even a class E, that's experimental. But for now, we're just gonna focus on classes, A, B and C.

[00:01:03] This is what's used for publicly routing on the actual internet. These are defined in RFC 1366, and once again, these just refer to the concept of globally routable IPs. If you go to an ISP or an internet service provider, you're gonna have routers, their routers run BGP,

[00:01:22] border gateway protocol, which is basically the routing table of the entire internet. We'll leverage BGP to reach any of the destinations that you see here, from one all the way up to 223 networks. Now, each of these is intended to be a unique global address.

[00:01:40] One of the few funny examples to this, or I shouldn't say examples, but exceptions to the rule, is that concept of anycast. Anycast is a single IP on lots of hosts at the same time, which is a little bit weird. They're in different networks, they're in different data centers, but they basically exist as the closest instance.

[00:02:01] So when you turn on your cellphone and it finds the closest cellphone tower, you can do a trick like that around the internet with DNS servers. So when you want to reach a popular server, like 8.8.8.8, Sam in Tampa, Florida, I send it to my local service provider and they go, oh, cool, 8.8.8.8, right here in Miami, no problem. So there's a local reference to that.

[00:02:22] Now, if you're in California, there would be a different instance. If you're up in Chicago, there would be a different instance, if you're in New York, there's a different instance. So certain IP addresses can exist in multiple places, but again, I'm telling you what the weird exception is. That's kind of way out in left field, although it's something we use every day,

[00:02:40] you or I won't be typically configuring anycast, what we typically do is gonna be unicast. So one IP on each device, and I'd say something like 99% of the IP addresses are gonna be like that. So let's talk about the classes here. We see that there's classes A, B, and C. So if someone said, hey, the eight network, 8.8.8.8,

[00:03:02] that famous DNS server, what class is this? And what we realized that anything from basically, one to 126, is classified as a class A address. So if it was an IP address that started with 105, that would be class A.

[00:03:20] If it was something that started with 160, it would be class B. If it was 200, it would be class C. That's all there is to those, pretty easy. So there are blocks that are excluded from use, can you think of any? RFC 1918, which we talked about in the last section.

[00:03:41] So those 10 addresses aren't used. 127.0.0.1, that's reserved for your local loopback. Every one of us has 127.0.0.1, it's like your belly button. You can ping it, every mobile device, every phone, everything that does TCP/IP, has that loopback, it just means that it's you.

[00:04:01] Now, what's funny is they burnt an entire class A to do that. So there's something like 16 million hosts in there that we could have used for other things, but the entire 127 network is allocated, just for your local host. There's a 169.254 address that's used for something called auto IP,

[00:04:20] when you do DHCP and you fail. You don't have to remember all the blocks that are excluded, just realize that there are ranges within here, that are not used.