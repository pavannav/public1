[00:00:06] - Now, let's do a small lab with these packet analysis utilities. Your FTP server recently was hacked. You suspect that insecure passwords are being sent. Use the appropriate tools to investigate.

[00:00:20] Good luck. By the way, there's no setup script for this lab, I just expect you to install your own FTP server, and see how it is working and what you can do with the packet analyzer. All right, now let me set up this lab environment. I'm already opening a root shell to my second machine,

[00:00:40] which is on 223 currently, so that we have a client as well as a server. Now, on my main machine I'm going to install the FTP server. Vsftpd is the name of the package. Already installed, that's good.

[00:01:01] I don't wanna do SELinux now, so I'm using setenforce 0. I don't wanna have any problems. And, also systemctl stop firewalld. I don't wanna dive into firewalling right now. I just want to analyze packets and see everything coming in from other machines.

[00:01:21] Systemctl status vsftpd. It's not running, that's good. Let's have a look at the etc/vsftpd/vsftpd.conf configuration file. I wanted to check local enable is yes, so we have access as local users.

[00:01:42] So, do we have local users? We have user user. Well, that's good enough. So, we are going to do it as user user. Systemctl start vsftpd. And, now we need to make sure, of course,

[00:02:01] that Wireshark is running. We ignore the SELinux message because, again, this is not about SELinux, we don't care about SELinux. Now, let's get to the other machine, and on the other machine I am installing lftp, which is an FTP client.

[00:02:22] So, yes, this is okay. And, from this machine, I am connecting lftp user@192.168.4.168. Password, here it comes, my very secret password.

[00:02:41] And now, I have access to the user home directory. Let's have a look at Wireshark and see what Wireshark is thinking of it. So, what do we have? Here, we can see the packets and, oh, request, pass password.

[00:03:02] Now, what exactly is that? Now, if you click the packet then you can see the packet payloads, and the request command pass, request argument password, that looks like a plain text password. And, the answer is login is successful.

[00:03:21] Now, obviously, my password is just the string password, so that might not be too convincing, but I hope it's clear that you see that passwords are just being sent in plain text here. So, that is sufficient.

[00:03:40] You may also use the follow TCP stream here because in the follow TCP stream it's becoming obvious what exactly is happening. So, here we can see the initial contact where the FTP server is sending its features. Then we see user user coming in from the FTP client,

[00:04:02] FTP server answering, please specify the password, pass password, and login is successful. PWD, home user, so you can see really everything. Are you still using vsftpd? You shouldn't. You should use sftp.

[00:04:20] Sftp is part of SSH, and everything happening in SSH will be encrypted. And, if you don't believe me, here's Wireshark, try for yourself, analyze what's happening. Good luck with that.