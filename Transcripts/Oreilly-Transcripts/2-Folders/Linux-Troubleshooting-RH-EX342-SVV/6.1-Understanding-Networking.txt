[00:00:06] - In order to do network troubleshooting, it helps to know what it's all about. So let me draw a quick overview of the process of communication between a server and a client. Let's start with the server.

[00:00:20] The server is running an application. Let's say the application is a web server. So the web server is generating an answer to the client. So I'll draw the web server to generate an http packet. And after the http header it will boot the payload.

[00:00:41] And the payload is whatever the client has requested. Give me a web text, or any which, or video or whatever. So http is sending it down the stack. And the next step it encounters is TCP. And TCP will consider everything that is offered through the http header as payload.

[00:01:02] So in payload here, in TCP, the http header is included as well. From TCP it goes further down. And the next step is IP. And IP also will consider everything coming from above payload. Next we go to the network card.

[00:01:22] And at the network card we work with a mac address. And everything that follows the mac header is considered payload as well. So the interesting thing is, if you are analyzing the packet that is really transmitted on the wire,

[00:01:40] then you will see that the mac header is followed by an IP header, which is followed by TCP header, which is followed by an http header. And if you have insight in all of these, then you can analyze what is actually happening. From there, the packet is submitted on the wire. Well, wire, that depends of course,

[00:02:00] you can have wireless communication as well. And next it will be received by the client. So the very first part where the packet arrives, is at the network interface card. The network interface card, which works with the mac header,

[00:02:20] because that allows the client to recognize the packet as a packet, that was sent to this specific client. On the network interface card, we can have received buffers, as well. Received buffers can be used to temporarily store the packet before it is further transmitted up into the stack.

[00:02:43] And once the packet has totally been buffered, it can be sent forward to the IP protocol, to the TCP protocol, and from there it can go to the http protocol, until you can see the webpage

[00:03:00] that you have requested in your browser. But this is not everything. In order to understand network communications, it helps to distinguish between the different levels of communication. So first we can distinguish this specific level.

[00:03:20] This is the level of the application. And I know there is an OSI model that is described in much more detail. I don't want to go that deep. I just want to distinguish between the fundamental parts of networking. So the application, that's your web servers, that is your Firefox browser,

[00:03:40] is taking care of processing the http data. From the application, it is going down into the IP stack. And the IP stack is a part of the Linux kernel So TCP and IP both are a part of the kernel. Next from the kernel it goes down

[00:04:01] up to the level of ethernet. Ethernet, which is typically used to put packets on the wire, and to recognize packets that are addressed to your specific mac address. But still, that's not everything.

[00:04:22] There's also this thing called firewall. So in this diagram, we can easily draw the firewall. Because the firewall really is lowest level of networking that sits in the Linux kernel and that determines if the packet can further

[00:04:41] access whatever is happening in your stack. And there are other things that matter, as well, for optimizing, trouble shooting, analyzing network stack for example. Take packet size for example, which is an ethernet property. There is thing called MTU,

[00:05:01] the maximum transfer unit. Maximum transfer unit is what you look at at an ethernet level. And you must make sure that packets that are sent with the specific size here can be received at the same specific size in here. And all of this is a generic overview of what you need to consider when troubleshooting networking.

[00:05:22] Now let's continue with the next lesson and talk about networking in some more detail.