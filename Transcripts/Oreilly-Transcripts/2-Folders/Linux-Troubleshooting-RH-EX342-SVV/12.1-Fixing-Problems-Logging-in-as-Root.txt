[00:00:06] - In our first lab, you are going to fix problems logging in as root. So root cannot log in to your server anymore. Fix the problem. For all of the labs in this lesson, there is script that allows you to install the lab. The name of this script is lab121.sh.

[00:00:30] Alright, so there's a problem logging in as root, let me reboot to install the lab.

[00:00:42] So I can log in as usual, which is good. And let me see if I can open a root shell. So I can open a root shell, which is also good.

[00:01:01] And now let me try to log in as root chvt 2. I hope it is fixed now, so let's see if you can log in as root. Oh no. Login incorrect. So we have a problem logging in as root, but we can as US root.

[00:01:21] Let's think about the methodology. So first, we need to define the issue. The issue is that the root cannot login anymore with root, can still use su to become a root. We need to gather information. We've just done a couple of tests and that's sufficient information. We need to form a hypothesis,

[00:01:40] so what is included in the login procedure, what is not included in the su procedure. That will be related to pam, now wouldn't it. So we need to get to a pam environment. So let me log in as usual, and open a root shell.

[00:02:01] And let's have look at etc/pam.d/login. So that's my hypothesis. Something is wrong with pam. So in login, we can see that all starts with pam securetty. And then there is nothing else that really does matter. Now if we have a look at etc/pam.d/su by itself,

[00:02:27] then we can see that there's sufficient to root okay, and this is not so strict and it does not look at securetty. So I think there's a problem that relates to securetty.

[00:02:41] Let's have a look at securetty. Now we can see that securetty is quite small - zero bytes. So let me remove etc/securetty. Now let's see what's happening if we have removed securetty, chvt 3.

[00:03:08] And this is kind of funny. Apparently if pam calls etc/securetty, it will respect it, but if etc/securetty does not exist, then everyone is allowed in. That's not very nice,

[00:03:21] but at least we have fixed the primary problem. Now let's clean up some of the mess as well. And some of the mess means that we need to reinstate the etc/securetty file. So how are we going to do that? Well, rpm-qf etc/securetty might be a nice beginning.

[00:03:41] Here we can see that it comes from the seventh package. So let's try yum reinstall setup. Let's do ls-l/etc/securetty again.

[00:04:04] And oh no, I'm getting security on tap completion, so we don't have a securetty. Okay? Let's do it differently. I'm using yumdownloader to download the setup rpm package. And I'm using rpm2cpio to extract the setup package.

[00:04:28] An rpm archive is a cpio archive, which is like a turbo but different. An rpm2cpio is getting out the cpio archive, and I'm piping that to the cpio command -idmv.

[00:04:44] Need to be careful with the directory that I'm in because it will extract some files in the current directory. And there we can see it has extracted an etc directory including etc/securetty. So let's have a look at etc/securetty

[00:05:02] that has been extracted here. This is looking quite okay. So I'm copying etc/securetty to etc, and now everything should be alright again. So chvt 4, just to know that their virtual terminal to test, that still root can log in, and problem has been fixed.

[00:05:24] That's all.