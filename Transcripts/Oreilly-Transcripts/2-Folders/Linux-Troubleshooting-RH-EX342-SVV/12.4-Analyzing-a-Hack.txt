[00:00:07] - Let's move on with the next program. Analyzing a hack. So what's happening? So you suspect your system has been hacked. Use the appropriate tools to find out what has happened. Good luck. This lab also comes with script

[00:00:21] and the name of the script is lab124.sh. Okay so let's install the lab. And apparently it's installed. So good luck analyzing the hack. Now if that is all you know, I might have been hacked.

[00:00:40] What are you going to do? Well, you need to figure out if anything has changed on your system. And there's a couple of methods that you can use. "Aid" would be nice but "Aid" needs to be installed on before hand. And if you haven't installed it, you cannot use it. So you rely on common utilities.

[00:01:01] Like "Find" for example. You can use "Find" to find files that recently have been modified. But if the hacker is clever, then he has used stack on all your files and that will not bring you anything. So probably the best thing to start with is an rpm query. So I'm using rpm-va to verify all packages

[00:01:24] and then we wait. So while this is still running I can already see some stuff that I don't like. I can see that the permissions have been changed on var/log/gdm.

[00:01:41] I don't care too much because that is a log file. I can also see that the permissions have changed on ETC Shadow. I do care about that. Because ETC Shadow is a very sensitive file. So ls dash l on ETC Shadow, and what is that giving us? Oh no! ETC Shadow is 777.

[00:02:01] That's not good! Let's use chmod 000 on ETC Shadow. Because that is how it's supposed to be set. And another problem fixed.