[00:00:06] - In this short lesson I'm going to give you an overview of solutions that exist for tracking changes. In the following lesson, we will have a look at the different solutions in more details. So there are different solutions available.

[00:00:21] One solution is the Advanced Intrusion Detection Environment, or AIDE. The disadvantage of AIDE is that you need to install it separately. It doesn't come automatically and you need to configure it for it in order to do anything. Another solution is RPM queries.

[00:00:40] As you are using a Red Hat system, Red Hat installed software using RPM, and if you want to figure out if something significant has changed, which may help you in troubleshooting, you can use an RPM query to verify all packages. Next there is the auditing system. Linux by default comes with the AuditD system.

[00:01:02] And AuditD can be configured in a very advanced way to track exactly everything you need to keep track of. And this can be very useful for problems that are more complicated to solve and where you want to monitor specific aspects of the operating system. In the upcoming lessons I will go in much more detail

[00:01:22] about AIDE and about auditing, but let me give you a quick demo of RPM queries and how you can use them to verify the integrity of your RPM packages. So to verify what has happened on RPM packages, rpm-VA, for verify all,

[00:01:41] is what you want to do. And here you can see the checksums that have been set on different configuration files, and which show you what exactly has changed. So, this is very nice to get a quick overview of all files that have changes

[00:02:00] that have been applied so they can know where to go to, and let you also know if the file stilll is in the original state. Let me quit it here, and let me open the man page of rpm, because S and 5 and T and L- what exactly does that mean?

[00:02:24] Well, let's go to the verify option. Here you can see the things that are tracked, the size, the digest, the permission, the type, the owner and the group of all files. And you can use specific parameters to exclude any of these.

[00:02:47] And here we can see the list of the different items that have been monitored and which has changed. So something that you will often see is S,

[00:03:00] file size differs. I mean if it's a configuration file, you may have configured your system, so that's pretty normal. And if you configure your system and the file size changes, then 5 will change as well, which is the digest. It is the checksum that is put on a file.

[00:03:20] You should more look at items like the permission mode, or the user ownership, or the group ownership. But even the file size and the digest may be useful because it indicates that files have changed, and that can help you in your troubleshooting procedure.