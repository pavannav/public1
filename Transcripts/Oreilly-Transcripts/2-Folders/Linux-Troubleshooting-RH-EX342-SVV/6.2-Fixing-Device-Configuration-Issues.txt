[00:00:06] - So, let's start our network troubleshooting right at the bottom, and the bottom is the connection to the network cable, so the device. In this lesson, we are going to talk about device configuration issues that you may encounter.

[00:00:21] And, the first thing you need to understand is device naming. So, this new system is used, biosdevname, is the name. The purpose of biosdevname is to make sure that every network card has a name that says something about the physical location of the device.

[00:00:40] It's a default option, and you can either turn that option on or off. By default biosdevname=1 is used as a kernel boot option to switch biosdevnames on, and if you don't like that, you can use biosdevname=0. If you do that you will just get eth0

[00:01:00] and eth1 as the network cards. For biosdevname to work, the network driver is queried to figure out what type of device is used. And, you can use device names like this, eno0, where en is Ethernet, 0 is onboard. Or ens1, where s1 is PCI slot 1

[00:01:23] on the PCI express bus. Or enp2s0, where we are referring to PCI address 2, 0. eth0 is only used if nothing else could be found, or if biosdevname is disabled.

[00:01:40] The udev rule, which is in /etc/udev/rules.d/80-net-name-slot.rules takes care of the default netbios naming. And if you don't like the netbios naming, instead of using the kernel boot options, you can also link this udev rule to /dev/null, which will disable it.

[00:02:00] You can also create custom rules in the /etc/udev/rules.d/70-persistent-net-rules. Before we continue, let's have a look at these biosdevnames and how this is working. Now, how about these rules in /user/lib/udev/rules.d? You can find all of the rules that are processed

[00:02:21] when this system is booting. And, the rule that we are looking for is this one, 80-net-name-slot.rules So, if you want to understand what is happening, have a look at the rule. So, here we can see that the name is specified

[00:02:43] using net.ifnames, and after the net.ifnames, it is going to query the environment variable of ID NET NAME ONBOARD, or SLOT, or PATH. These are basically the three different options that are offered by biosdevname.

[00:03:03] In the initialization of the network card, first udev rules are processed by the udev process, and that generates the names. And, based on these names, the network service can process the configuration files in the /etc/sysconfig/network-scripts/ifcfg- whatever.

[00:03:22] These scripts match the network cards to the hardware address, and you can also use the devices to manually set and override the biosdevname name. Now, there can be configuration issues as well. And if you have configuration issues, then the troubleshooting procedure

[00:03:41] would be to start in runtime. In runtime analyze, we have got IP address show is a nice command. It will show you the current IP address configuration. Then, there is IP route show and cat on /etc/resolv.conf. The three of them will show you the essential components of the network configuration.

[00:04:01] For troubleshooting, you can also consider running dhclient because, if you don't know what type of address to use, often a DHCP server is available, and, no matter what you are using in the network configuration, dhclient will dynamically request an IP address from the DHCP server.

[00:04:20] At least, it helps you in troubleshooting connectivity issues. After you have done that, you can analyze the persistent configuration. And, the persistent configuration is in /etc/sysconfig/network-scripts/ifcfg. Or, you can use nmcli conn show for more detailed information.

[00:04:40] Let me show you a bit of this. So, a good way to start is by using the ip a command. So, the ip a command, which is short for IP address is showing me that I've got a device with the name ens33, which has the IP address set to 168.192.4.168,

[00:05:01] and we do not see the word dynamic here, so this is a statically assigned IP address. There's inet6 as well. That is IPv6. This address has been assigned because IPv6 is active by default and IPv6 as a plug and play automatically obtained

[00:05:20] an IP address configuration. You may just ignore that if you want to. The next part is ip route show. Ip route show will show you current information about the router. And, then there is cat /etc/resolv.conf. This file is automatically generated by NetworkManager, and in this file

[00:05:40] you can see that the name server is set to 8.8.8.8. Now, this configuration comes from the NetworkManager. And, the NetworkManager is a service on Red Hat that manages /etc/sysconfig/network-scripts, and, in that directory, you can find a configuration file

[00:06:00] for every single network interface card that you've got. So, here we have ifcfg-ens33. If you look at the contents of that file, then you can see all parameters that are being set. Let's pick out the interesting parameters.

[00:06:20] First of them, bootproto. If bootproto is set to anything that is not DHCP then it will assume a static IP address. If you set bootproto to DHCP and you also specify an IP address in this configuration, then we get an interesting situation where you will get both.

[00:06:40] So, you will get a dynamic IP address as well as a static IP address. Then, we get some more parameters, here is name, and the device that refer to the device that is being used. In NetworkManager there is a device name, which in this case is ens33, and there's a connection name, which in this case

[00:07:02] is ens33, and the connection is matched to the device. We can see onboot, which is set to yes, which is convenient, the IP address, including the prefix, which is the subnet mask, and the gateway as well as the DNS. So, this is the configuration

[00:07:20] as it has been written persistently. Let me also show you once more, ip a, one IP address, and now let's use dhclient. Dhclient is the DHCP client. As I've mentioned before, this can be convenient to test connectivity and to figure out what should be used in your specific network environment.

[00:07:42] So, here we can see it now is a secondary IP address, which is marked as secondary, but also as dynamic. So, that makes it easy to recognize this IP address as DHCP assigned. If you type ifconfig, by the way, some people are still doing that, I don't know why, but ifconfig first

[00:08:03] is giving too much information, but let's scroll up a little bit. And, there we can see that ifconfig only sees the primary IP address. There is no way that ifconfig is capable of displaying the secondary IP address, which is also why you should not use ifconfig anymore.

[00:08:22] Using ifconfig is an error by itself, so please don't do that. Let's go back to the slides for a minute. All right, let's talk about applying changes to configuration files. You may directly modify the configuration files in the etc/sysconfig/network-scripts, but, if you do so, then I would recommend you use

[00:08:43] nmcli conn reload, which is the appropriate NetworkManager command to reload the configuration. If the network card is already active, use nmcli con down eth0; nmcli con up eth0.

[00:09:02] Oh, and in case you are wondering, hey, is that a typo, the second time you are using con with one N only, no, that's not a typo. Nmcli is just a very smart command. It recognizes con with single N or double N both as a solution to refer to the connection.

[00:09:22] So, that will both work. Notice that you may have to start services as well after applying changes to the configuration. That is about services that may be dependent on your network configuration. In short, be very careful with manual modifications

[00:09:41] to the network configuration. You might be better off using good old nmcli.