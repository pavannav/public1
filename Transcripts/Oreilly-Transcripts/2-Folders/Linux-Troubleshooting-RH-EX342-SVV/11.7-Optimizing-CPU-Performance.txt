[00:00:06] - Now let me explain where you can really win on performance regarding CPUs. The problem with CPUs is the following. There is the I/O scheduler,

[00:00:21] and if a task comes into the I/O scheduler, the I/O scheduler will next make sure that the task will run on a CPU. If it's one CPU, then there's no problem, but often on modern systems there are multiple CPUs like CPU0, and CPU1,

[00:00:40] and CPU2, and so on. The problem with this system is that every time that a task will run, it will get a time slice. The I/O scheduler will put it on a CPU core, and next the task will get back into the queue until it can run again, and the next time that the task will run again, it may run on a completely different CPU,

[00:01:02] and that is not very efficient, and that is why you can do CPU pinning or IRQ pinning. In CPU pinning, that's something that you may use in KVM virtual machines for example, and in CPU pinning you may make sure that the specific task at any time

[00:01:21] will run on the same CPU, and why is that beneficial? Well, it is because it makes sure that the next time you run the task on the same CPU it uses the same cache and everything again so that data that previously has been stored in old CPU cache may still be there. It makes handling of the process a little bit faster.

[00:01:42] Same story for IRQs. IRQs are the interrupt lines, and the interrupt lines are handled by different CPUs as well at the moment that the IRQ occurs, and IRQs can be pinning to a specific CPU core as well, but I would suggest,

[00:02:00] even if this is very useful to know, don't put too much effort in it because, if you optimize your CPU successfully, you may gain nanoseconds. If you optimize your disk I/O channel successfully, you may gain milliseconds. That's interesting. The nanoseconds are only interesting

[00:02:20] if you are working in a realtime environment where it really needs to go as fast as possible. All right, let's have a look at CPU tuneables. CPU tuneables are mainly in the proc file system, and you will find them in kernel because the kernel is what sits closest to the CPU.

[00:02:42] The CPU itself cannot be tuned because that's hardware, and you need different utilities in the Linux operating system. The only thing that can be tuned is the way how the Linux kernel is accessing the hardware, and there is a limited number of parameters

[00:03:01] that can be used to influence that process. Mainly they are the scheduler parameters. They relate to the scheduler that we have just talked about like the scheduler migration cost, the minimal granularity, the round-robin timeslice. Now what is that?

[00:03:21] The round-robin timeslice in microseconds is an interesting one because it will tell you how long a round-robin process can stay on the CPU before it is moved away to make place for something else. Let's have a look at the value of that parameter. So here you can see it's set to 100,

[00:03:42] and how about the sched realtime periods in US? So that's in microseconds. That's set to one million. So this is where you can modify to determine

[00:04:01] how the schedulers will do their work, but by setting these parameters you will only gain a couple of nanoseconds. So make sure before you get in there that you really know what you are doing. In case you don't know what you are doing, I would say have a look at the man page of proc.

[00:04:20] If you search for /proc/sys, then you get documentation about everything that you can do in the /proc/sys file system. So this documents basically everything that is available. So if ever you find a parameter and you are wondering, hey, is this something that I can use to optimize my CPU working?

[00:04:41] Well, have a look at the man page and read what is in there. Also you may consider installing the kernel documentation. In the kernel documentation you will find additional information on what you can do to optimize your system, but now that we are at the end of this section about performance troubleshooting,

[00:05:00] I would recommend you to troubleshoot in the following order. First you look at disk, disk and file system, because that is where you can really win a lot. Next you look at memory because everything in Linux goes through memory. If you have bad memory performance, everything will perform badly. Next you will have a look at networking.

[00:05:23] Networking is becoming more complicated because networking depends on items that are outside of your Linux machine. Finally, you look at the schedulers because, if you are running a realtime processes and you are running them in the wrong scheduler, they may take away CPU cycles from everything else,

[00:05:43] and that's about it. You wanna know more about performance optimization, watch my Linux performance optimization video course because this was just an hour in the Linux performance optimization. We'll talk about stuff like this for more than 10 hours.