[00:00:07] - Sometimes your network problem is due to something else in the configuration. In that case, it may help if you can analyze network traffic. Linux comes with some excellent utilities to help you analyze the network traffic.

[00:00:22] Wireshark is a very convenient utility if you use a graphical interface. It allows you to do a live packet capture but in order to do live packet capture, you need to be a root user. It can import pcap files as well. Pcap files are packet capture files that can be generated

[00:00:42] to multiple network analysis utilities and you can use advanced filtering options like ip.src equals 10.0.0.10. Probably the most convenient option that you hae in Wireshark is the option to right click a packet and select Follow Stream

[00:01:00] which allows you to see an entire conversation. That means that you can see the conversation between this node and the other node from the beginning to end and follow all the packets as long as the live stream has been active. Then there is tcpdump. Tcpdump is not so convenient because it will give you a lot of output

[00:01:21] and it will dump by default on the screen but it's a utility that exists if you are on the command line and that can be convenient anyway. Here also, if you wanna do a live packet capture, you need to be root. And tcpdump can generate pcap files, so in general, how it works in an enterprise environment

[00:01:41] is that you have tcpdump on all of your servers and if you suspect that anything is wrong or if you just want to analyze, you generate the pcap file, copy the pcap file to a graphical workstation, analyze it with Wireshark. And let me demonstrate how to use these two utilities. So, let's start having a look at tcpdump.

[00:02:02] If you type tcpdump without any arguments, it is going to bind to something which in this case is virbr0. That's not really convenient. So, we need to specify where it is going to listen. Tcpdump --help shows you that different options are available including the option -i for an interface.

[00:02:24] So, tcpdump -i ens33 will start listening on ens33. Now, let me open a new terminal. Oh look, something is coming in already.

[00:02:41] So, what do we see? We see packets that look like DNS traffic. This is in-addr-arpa which is DNS and you can see that it's coming from trouble.example.com on port 53476 to gateway.domain

[00:03:03] and fortunately because of what we see here, we can recognize it as DNS traffic. You can see that even if you don't do anything specifically, a lot of stuff is happening like the ARP requests which is needed for communication within a network.

[00:03:21] Now, let me start a ping to server2. We should be able to see that as well and here you can see that it starts by sending an ARP and the ARP is for who has and then it's becoming unreadable quite fast

[00:03:42] and also we can see that server2 is giving us destination host unreachable. Let's do a fourth emit. Now, when you get a destination host unreachable, there's something else that you can do. Yum isnta, no, let's not do that.

[00:04:00] Now, let's use something else. Ping to 192.168.4.2, for example. And here you can see the ICMP requests and the ICMP replies. So, that is giving you a nice overview of basic tcpdump functionality.

[00:04:20] You can use some specific options as well. Tcpdump -c 10, for example. Obviously we need to specify the interface as well. It will capture 10 packets and after capturing 10 packets it will exit.

[00:04:40] Now, if you want to, you can also write the results to myfile.pcap. Pcap is the standard format for writing packet captures, so it will capture 10 packets on ens33 and once it has dumped, the result will be written to myfile.pcap.

[00:05:01] The advantage of using a pcap file is that you can later load it in some other utility. Now tcpdump is offering lots of options. If you wanna know how to use it, use the man page on pcap-filter.

[00:05:21] It will give you information about everything that you can do and take your time to read through it because really, it's very rich. Maybe you want to start having a look at the examples. So, here you can see examples, just type the examples, they will all work with tcpdump as long as you don't forget to specify the -i option.

[00:05:43] Let's have a look at Wireshark. Wireshark is a graphical interface, so here we can see the graphical interface. It needs a lot of place, so I would recommend running it full screen and at any time run it as root. In Wireshark, definitely you want to specify

[00:06:00] where you want to listen, so specify your default network card and then you click Start and then you wait for a little bit or you generate some traffic, of course. That will work as well. So, let's do an SSH to 192.168.4.2, for example.

[00:06:25] Oh no, connection refused but at least we can see that something is happening in Wireshark. It's only five packets but that's okay for me. Five packets is still readable and the nice thing about Wireshark is that it's giving you colors to make it easier

[00:06:41] to recognize what has happened. Blue if it's okay and depending on the protocol that you've been using, you may get a different color. Red if it's not okay. Now, if you want to analyze what has been happening, you can right click a packet and when you right click a packet, in this case, it's UDP, you can select Follow UDP Stream.

[00:07:04] So, what do we have in UDP stream? You can follow the entire conversation. It's a little bit boring because this conversation seems to be one packet only with just some binary formatted contents. Now, if you do have a packets,

[00:07:20] then you can click on the packets and further analyze what is happening. So, here we can see an NTP packet. The NTP packet is an upper layer protocol. You can also see, by the way, that the filter has been applied because I clicked on follow the stream of something that wasn't really a stream

[00:07:43] but if you want to see what is happening in the packet, you can go down all the way to the OSI stack and the most interesting thing, you can analyze the payload that has been sent in this specific packet. So, this is the NTP information and NTP apparently is sending binary

[00:08:02] or maybe even encrypted data and the interesting thing about Wireshark is that if you do have protocols that are sending data in clear text, you will see that as well. I would advise everybody to install Wireshark at least once on their servers

[00:08:20] and to analyze and try to get ahold on what is happening on the server. You might be surprised of the results.