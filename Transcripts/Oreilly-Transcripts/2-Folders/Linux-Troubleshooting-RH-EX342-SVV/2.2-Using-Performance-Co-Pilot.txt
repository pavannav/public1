[00:00:06] - So Cockpit it one very nice solution that helps you monitor your system. Another solution is the Performance Co Pilot. And I want to show you some of that as well. So let's start by installing it.

[00:00:27] And, oh, apparently it was already installed. That's even better. Let's use system CTL enable dash dash now on PMCD. It's not obvious, but the name of the PCP main demon

[00:00:41] is PMCD; PM for performance metrics. Let's verify system CTL status PMCD that this thing is running. Now we can see it is running. And it has been running for an hour and 17 minutes, so apparently this was already installed.

[00:01:02] That doesn't really matter. PM stat will show us metrics. Now this metrics can be useful as it is automatically refreshing and telling you what's going on on this system. So we can see information well, that is very

[00:01:23] IO stat slash VM stat similar. So we can see information about swpd memory about free, the load average of course, buffer usage, cache usage, pages in and pages out in swap. Block in and block out in the IO system.

[00:01:41] And then the interrupt contact switches utilization, the user space, system space, and idle time as well. This is a nice utility, but this is not why you want to install the Performance Co Pilot. Because we already have utilities that give you information like this.

[00:02:01] Then there is PMA top. PMA top or performance metrics A top is a specific port of the A top utility. And it's giving you an overall view of what your system is doing. It requires some study to figure out

[00:02:20] what exactly is happening. But once you are used to it you can really figure out anything that matters to the performance of your system. You can see it's providing information about processors, about CPU, about memory, about swp and really a lot of information.

[00:02:40] It's very convenient. PCP is also a logger. And if you want to use the logger then you need to make sure that the PM logger process is installed and enabled. So system CTL status PM logger is showing us that PM logger on this system

[00:03:01] has been up and running for some time already. The results will be written to var log PCP. As you can see, it has a subdirect PM logger. And in this PM logger we have information about everything is logged.

[00:03:22] The information is categorized by server name so here we have trouble dot example dot com. In which we can see the different moments that log files were written. Notice the way how it is written, so we have 20180424. That must be the date of today

[00:03:41] and then we have 18 38, and 18 33, and 18 41, and 44, and 55. So what time is it? Now currently it is 20 05. Well, we can see there is 19 55, so something has been logged recently.

[00:04:02] As you can see, different file are written. And the total size of these files is quite okay. 1.8 megabyte. Now if you want to do something with these logs you can use PM logger.

[00:04:20] Now the purpose of PM logger is to allow the PCP utilities to work with historic data. Typically that would be the dash A switch that works with multiple commands to analyze historic data as well. Now when you work with PCP there's metrics as well, that can be traced.

[00:04:40] PM info will show you a list of all the different metrics so you can hook in very specifically to the different metrics that are available, as you can see by default, no less than 1835 metrics can be used. If you want to get information about a specific metric,

[00:05:02] PM val is what you wanna use. Let's use PM val dash S five on proc N procs for example. You probably can guess what this is doing. This is looking at the number of procs. It will do it five time. Dash S five stands for five samples

[00:05:22] with a default of one second interval. The interesting thing about this is that once you suspect that something very specific is wrong on your system, you can look up and see if there's a PCP metric that can be traced and once you have found the PCP metic

[00:05:41] then you can monitor it using commands that look like the commands that I've just used. And this goes for all of the 1835 different metrics that are available. Let's get back to the metrics just to get an idea of what is available.

[00:06:00] Here you can see that it is very specific, so a lot of kernel metrics, memory metrics, relating to different areas of memory, network metrics and so on and so on and so on. So you can be very specific about it. Let me give you one more PM val command.

[00:06:21] So PM val dash T one minute. On kernel dot per CPU dot CPU dot idle. So what is this going to do? This is showing the idle statistics

[00:06:41] on the kernel per CPU, CPU idle parameter and it's doing that for one minute period. I think you get the idea. Using PM val you can also analyze historic data. And that is where it really is becoming interesting. It's a little bit more typing because you need

[00:07:00] PM val dash A on var log PCP on the specific server where you want to go to and next you need to enter a specific file.

[00:07:21] So this is where you really need to take care of what you are doing. So 18 dot, what do we have? Let's use the 18 44. Dot zero, there's an index, there's a meta as well,

[00:07:40] but definitely you need the dot zero, and if you want to figure out kernel dot all dot load, next we can specify the time range that we want to use. So dash S

[00:08:02] at Tuesday, April 24 18 45 zero zero 2018

[00:08:21] dash T at Tuesday, April 24, 18 55 zero zero 2018.

[00:08:41] And that is giving you information about this specific parameter which is the kernel all load parameter. It has been logged between the start and the end timeframe. You notice it's becoming a little bit more complicated because you need to be very precise, but it also allows you to filter out data

[00:09:03] that you wanna know more about. PCP is a very rich environment. And if you really want to use it, I can recommend it, but make sure you invest some serious time in getting to know it. PCP comes with great documentation.

[00:09:21] If you do a yum search on PCP you can see all the different packages that are available. You can see it also has plugins for different solutions as listed here. But what probably you are most interested in at this moment is the PCP doc package.

[00:09:41] So make sure you install PCP doc. And then use man dash K PCP after updating the man database

[00:10:00] so man dash K PCP and you get information about everything that's available about PCP. I can recommend PCP intro. PCP intro is a very nice and decent introduction into what PCP can do for you and how you can use it.

[00:10:20] But again, make sure you invest some time to get to know the Performance Co Pilot.