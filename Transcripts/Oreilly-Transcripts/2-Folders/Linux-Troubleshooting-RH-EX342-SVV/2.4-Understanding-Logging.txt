[00:00:06] - In order to gather information, logging is essential on Linux. RSYSLOG is one of the primary systems to gather information. RSYSLOG is compatible with SYSLOG

[00:00:20] that has existed since the early days of Unix, and in RSYSLOG, you will work with facilities, priorities, and destinations. The essence about RSYSLOG is that you need to define

[00:00:43] what it should log information for. So, which facility is it going to look at? Which priorities is it going to consider? And only if the facilities and the priorities have been defined, RSYSLOG will write the messages to the log destination,

[00:01:00] which often are files. On Red Hat, by the way, these files are written to the /var/log directory. So, one essential property of RSYSLOG is that RSYSLOG does not log everything. It only logs what the administrator has defined that should be logged, and as a result, you may miss information in RSYSLOG.

[00:01:22] The default configuration is good enough, but it may just not be deep enough, and that is where the second system may help, systemd-journald. As you probably are aware, systemd is the manager of everything

[00:01:40] in current Linux distributions, and as a manager of everything, it has a process called systemd-journald, and systemd-journald logs everything that's happening in the items that are managed through systemd, and that means that there is a gigantic level of repository that can be accessed

[00:02:00] through systemd-journald, but you should be aware that systemd-journald, by default, is runtime only, which means that if you reboot, it's all gone. I will show you how to make it persistent, which is not that hard, but it's something that you should realize. Also something that you should realize is that systemd-journald can be integrated with RSYSLOG,

[00:02:26] but if you integrate it with RSYSLOG, RSYSLOG is still going to apply different filters on the information that is received from systemd-journald. There is one reason why you still want to use RSYSLOG

[00:02:41] in bigger environments, and that is because the RSYSLOG has an option to do a remote logging, and a remote logging means that you are going to define a log server and that's very convenient if you have dozens of servers, and you don't want to go on to every specific server to figure out what has happened.

[00:03:01] In RSYSLOG, you can just have one remote log server that gathers all the information where you can go to figure out what exactly has happened. In the next lesson, I will show you how to configure logging on Linux.