[00:00:06] - In this video I will make a quick overview of what we are going to do with the AIDE, Intrusion Detection system. And then I will show you how to do it. So the essence is that we will start using AIDE

[00:00:20] dash, dash, init, and AIDE, dash, dash, init will create a sample database, and the sample database will be written to var, lib, aide,

[00:00:43] and it's well Fd name aide.db.gz. So this is the blueprint of what your system is doing, and this blueprint contains all the properties of the files that currently exist on your system. And how you use AIDE is pretty simple.

[00:01:01] You are going to use AIDE check. And if you are going to use AIDE check then you will create a new database. And the new database will be compared to the original database, and by comparing

[00:01:20] the properties of the files in the original database with the properties of the files in the new database, you will easily find out if anything has changed, and that can help you identifying changes to critical components of your operating system. Let me show you how to do this. In this lesson we will set up AIDE.

[00:01:40] I will show you. So, to start it you need to install -y aide, 'cause by default is not installed. AIDE conf configuration file and you can probably guess the name

[00:02:02] of the configuration file, it's aide.conf. And in aide.conf you are going to specify what you want to configure. So AIDE is working with the DB directory, the DB directory is where it's going to put the AIDE database. And there's a log directory which obviously is for logging.

[00:02:23] The location of the database to be read, that is, what you are going to use when you want to check your current system environment. So, I will show you in a minute how to initialize the database. When you initialize the database

[00:02:40] you will write a database with the name aide.db.new.gz. You need to rename it to aide.db.gz, and that is going to be the blueprint of your actual system. And everything else? Well, right here, it's not so very important.

[00:03:02] What is interesting is the rules that you can use in AIDE. So, this is what AIDE is going to monitor, all of these. And you can specify what exactly AIDE should be monitoring. So these are the permissions like p, and i, and n, and user, and group.

[00:03:20] And it can be very specific, all depending on what you want to do. And if you go down in the list then you can find how these permissions are set in the AIDE configuration. I would highly recommend that if you are running AIDE for the first time, and just starting to explore its possibilities,

[00:03:41] don't change anything, just run it with the defaults. Typically users that have been running AIDE for a longer time have figured out that some options do work, some options don't work. And if options don't work, then this is how it works. Look at the directory, boot for example.

[00:04:02] Next, look at the variable that is set and specifies the permissions. So Content EX, this is what you get in Content EX. And if you don't like what you get in Content EX, you set it to something else. It's pretty straightforward. As you can see, you can work with specific files as well,

[00:04:24] and define exactly how to work in your AIDE environment. So, probably by the time you've got it working, you want to go over this configuration file once more to decide what exactly you want to do. So, once you have an idea of what you want to do,

[00:04:41] you can use AIDE, dash, dash, init. This command, which will take a while, will initialize the AIDE database. So as you can see, the AIDE database has been initialized in var/lib/aide/aide.db.new.gz. We need to rename this database.

[00:05:04] The reason why, by default, it gets known as new.gz is that you want to make sure that it doesn't get overwritten by anything. But, once you have generated it, you will write to aide.db.gz,

[00:05:22] which is the default name that you will be looking at. Now let's go to etc and let's make a couple of modifications. Like chmod 777, let's do that recursive on

[00:05:40] everything cups. That is a modification that AIDE definitely should be able to find. Let's verify that in the aide.conf. So, what we should be looking at is etc/cups.

[00:06:06] So, here we have the configuration for cups, where we can see that it's set to Content on the score EX. So what exactly is Content EX? Well, if you scroll down a little bit,

[00:06:21] then we can find the Content EX right here which is set to p, u, g. p for permissions. Let's just check that as well in the list. There we go, p for permissions. So chmod 777 on everything cups should not go unnoticed.

[00:06:43] Now we can run aide, dash, dash, check because aide, dash, dash, check is going to compare the current state of the system, with the state of the system is logged in the database. You should notice, by the way, that in this quick demo I have copied

[00:07:00] aide.db.gz to file lib/aide. I would recommend to save it in a secure place, outside of this box because if ever you get hacked and the intruder wants to hide what he has been doing, and he notices that AIDE is available on your system, the aide.db.gz will be one of the very first targets.

[00:07:24] Because if that database is gone, then the intruder can do whatever he wants to do and you will never find out by using AIDE. We haven't done that in this demo, but in real life you would absolutely do that. Alright, here we can see the result of the AIDE commands

[00:07:41] and if you want you can scroll up to see everything that is mentioning. Of course it's given the results on everything in the cups directory. So, here we have the permissions set to six, four, four, currently set to seven, seven, seven.

[00:08:00] And we see that all around on this system, and that is how AIDE can help you in figuring out what has happened. So, if ever you need to troubleshoot a situation where you have been hacked, AIDE is an excellent solution.