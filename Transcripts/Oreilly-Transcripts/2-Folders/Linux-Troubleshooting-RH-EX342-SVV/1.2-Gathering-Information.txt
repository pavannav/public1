[00:00:06] - Let's talk about gathering information some more, and about the resources that Linux offers to gather information. So, there is journalctl. Journalctl on a systemd-based system is the most important source for gathering information

[00:00:23] because systemd-journald gathers information about everything that is logged by units of systemd. So, you probably can get required information using journalctl command, which interfaces systemd-journald. Notice that the information logged

[00:00:41] by systemd-journald is in memory only. It's pretty easy to make it persistent, but by default that doesn't happen. Also you can use systemctl's status. Systemctl's status command in fact queries the journald to get the most relevant and most recent messages

[00:01:01] that have been logged by specific services. And by using systemctl status, you easily get an overview of what has been happening. We have seen that in the previous lab where we fixed the problem with a remote user that was trying to log in. And then there is rsyslogd. Rsyslogd is the old syslog-compatible service

[00:01:22] that has been around on Linux for a long time. Rsyslogd writes to different configuration files and other destinations depending on the way how it is configured. Rsyslogd is a topic that you have probably learned about already. In this lesson I want to focus on systemd-journald.

[00:01:42] And I want to give you a couple of quick demos using the journalctl command and how you can use that command to filter out the information that you need to know about. So, let's start with the journalctl command. When you run this command by itself

[00:02:01] then it will show you everything that has been logged to the journal since last reboot. Which as you can see is quite a lot. Use Shift + G to go all the way down to the last lines that have been logged. Also convenient is journalctl -ef, which stands for end and follow.

[00:02:22] And the idea about end and follow, well, follow means that it will automatically refresh if new messages are written to the journal. It's like tail -f on /var/log/messages, for instance. Control + C to get out of there. Also convenient is journalctl

[00:02:44] _SYSTEMD_UNIT =sshd.service. This will show you messages that are generated by the sshd.service. Something that is very similar to that

[00:03:01] is journalctl - u sshd.service. These are messages that are generated by and about sshd.service. So, the -u is giving you more detail than the _SYSTEMD_UNIT.

[00:03:20] Which is convenient, because it's also less typing. Next we have an option to use with priorities, which are used in an rsyslog environment as well. As in journalctl -p, - p for priority, emerg..err,

[00:03:44] which will give you messages that have a priority between emergency and error. That way you don't get too much information about things that don't really matter, and you can focus on the things that do matter. If it's an error, and if your system is logging

[00:04:00] a lot of information, that can be convenient. Also, there is journalctl -xb, which is information from the last boot. So basically, this is boot information, and it's telling you everything that has been happening at the last boot.

[00:04:22] And as you can see, it's telling you in a lot of detail. Journalctl is also giving you an opportunity to filter information. But if you want to filter information, then you must make sure that your system has been up and running long enough. So, let's first verify when my journal is beginning.

[00:04:43] So, that's April 24th 16:07. And right now it's April 24th 18:28. So, that's only a little bit over two hours. If you start using filters like this, it should be within the time range, because by default journalctl is in memory only

[00:05:02] and will be cleared at every reboot. So, based on that we can use journalctl - -since 2018 04 24

[00:05:21] 17 :00 :00 - -until 2018 04 24 17 :05

[00:05:41] 00 so that you will see only messages that have been logged in these five minutes. This can be very convenient to look for information related to crashes of services where you know very specifically when it went wrong. And last, there is journalctl -o verbose.

[00:06:04] You can probably guess what that is doing, it's giving you verbose output. And as you can see, the verbose output really is verbose, providing a lot of information about the environment and other variables of systemd units that have been logging to journalctl.

[00:06:22] This is really useful if you want to get all the information, but really it's a lot of information. Now, something else that you need to know about when working with journalctl is how to make your journal persistent. In order to do so, there is a simple procedure.

[00:06:42] This simple procedure starts with the file /etc/systemd/journald.conf. So, in this journald.conf, we have this option Storage is auto.

[00:07:02] Storage is auto means that the configuration will be saved if there is a target directory. Let's create the directory using mkdir /var/log/journal. And on this directory you need to set appropriate permissions. So, chown root:systemd-journal

[00:07:26] on /var/log/journal will set the ownership that you need. And next the permission mode must be set to 2755 on /var/log/journal.

[00:07:41] So, the special thing about it is that you need set group id on the contents of this directory. Next you can use killall -USR1 on systemd-journald to send the appropriate signal to systemd-journald. And this'll make sure that next time that you reboot,

[00:08:02] after the reboot you still find the old journaling information as well. So, let's see if that works. Journalctl at this moment shows us the journal that begins April 24th 16:07. Let's reboot and let's figure out

[00:08:20] what happens after the reboot. So, now we have just rebooted. And let me open a root shell again.

[00:08:41] And type journalctl, where we can see that the journal still begins at April 24th 16:07. So, if you go to /var/log/journal, in /var/log/journal we can see this nice subdirectory. And in this subdirectory there is

[00:09:00] the system.journal and the user-1000.journal. So, it's logging two different files. And that is how you make the systemd journal persistent. So, now that we know a bit about systemd journal, let's do a quick lab with systemd journal. So, here is the situation. The systemd journal should be persistent.

[00:09:21] However, after a reboot you notice that it's not, fix it! In order to run this lab, you need to install the lab script with the name lab12.sh. So, let's do it. So, let's run the lab, which is lab12.sh.

[00:09:41] And now we need to reboot, because the problem is that after a reboot we noticed that the journal is not persistent.

[00:10:02] Alright, so we apparently have a problem. Let's talk about methodology. You know in the methodology, which should be applied at all situations, we first need to define the issue. Let's see if we have an issue.

[00:10:23] Oh, that's another issue. I cannot use su to become root, but that's for later. So, let's use a root shell using sudo -i. So, let's run journalctl. And we can see that where before we had a journal

[00:10:41] that began at 18:07, now the journal begins at 18:40. So, it is not persistent. So, how are we going to apply the methodology in here? Well, first we need to define the issue. The issue is the journal is not persistent. That's easy enough.

[00:11:00] Next we need to gather information. Well, if you want to gather information, you need to know a little bit about your stuff. So, what is this about? Well, we go into /etc/systemd and we open journald.conf. And we look for the default parameter Storage is auto. Storage is auto is set, so that's okay,

[00:11:20] that's not the problem. Next we need the second check, because there's only two things that are required to make the journal persistent. So, /var/log, let's see if we have a journal directory. And, oh no, we don't have a journal directory. So, we can form the hypothesis.

[00:11:41] The hypothesis is that the journal is not persistent because we don't have the journal directory. So, mkdir journal should probably do it. Oh, and that already brings us to test the hypothesis and fix the problem. On this journal directory of course we need to set appropriate ownership,

[00:12:01] so chown root :systemd-journal. And we need to set the appropriate permissions. And now we can apply the changes to the journal.

[00:12:26] Which would start the persistency right now. And after a reboot, we can verify if the solution was correct. So again, we have applied the methodology and we have been quite fast because after the definition of the hypothesis,

[00:12:44] we haven't really verified it because this was so obvious that instead of verifying the hypothesis by trying different things, we immediately jumped to the solution. Now the only thing that still remains to be done is to verify the solution.

[00:13:13] So, back in a root shell, journalctl. And we can see that the start time is the same as the start time that we just had.

[00:13:21] So, verified, solution applied, problem fixed.