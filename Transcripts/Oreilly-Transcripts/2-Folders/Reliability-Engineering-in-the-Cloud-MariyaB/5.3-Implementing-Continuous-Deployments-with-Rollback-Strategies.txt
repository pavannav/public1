[00:00:06] Now let's talk about implementing continuous deployments with rollback strategies. Let's get right into it. Our strategy is to automate the whole deployment process. The combined value of AWS codedeployed, AWS codepipeline and

[00:00:23] AWS Elastic Beanstalk lies in their ability to automate and streamline the entire application development and deployment process. So CodePipeline orchestrates the CI CD pipeline, enabling efficient code changes from development to production.

[00:00:42] CodeDeploy automates application deployments, ensuring consistency and reliability, while Elastic Beanstalk simplifies application management, allowing engineers to focus on code rather than infrastructure. Together, these services promote a lean approach to cloud

[00:01:03] reliability engineering or cre, by reducing manual intervention, enhancing deployment efficiency, and optimizing resource utilization, ultimately improving the reliability and resilience of cloud based applications.

[00:01:21] Talk about strategies and specifically on the deployment side, an N1 deployment strategy involves deploying a new version of the software to all but one of the available environments.

[00:01:40] The one environment that is not updated is typically referred to as the N1 environment, representing the previous version of the software. N1 deployments are often used as a risk mitigation strategy by leaving one environment running the previous version of the application.

[00:02:01] Organizations have a fallback option in case any critical issues or unexpected problems arise with the new release, so this minimizes downtime a potential disruption. For example, if you have three production environments A, B and C, you

[00:02:21] would update environments B and C with the new version of your software, leaving environment A running the previous version as the N1 environment. This is a great strategy to implement and create a mitigation plan.

[00:02:40] Let's talk about another strategy which is called the Blue green deployment. So blue green deployments involve maintaining two separate environments. The current production environment is often referred to as the blue environment, and the new version which is deployed with the new software and

[00:03:01] tested in isolation is often referred to as the green environment. So blue green deployments are used to minimize downtime and risk during software releases. The green environment allows testing and validation of the new release without affecting the blue environment.

[00:03:20] Once testing is done and is done successfully, traffic is switched from the blue to the green environment. We also have a strategy for canary deployments. So canary deployments involve deploying a new version of the software

[00:03:40] to a small subset of users or instances first the canaries before rolling it out to the entire user base or environment. This allows for a gradual testing and monitoring of the new releases performance and stability. Canary deployments are used to detect and mitigate issues early in the release process

[00:04:03] by exposing a small number of users to have a new version of your software, you can monitor metrics and gather feedback to assess if there is impact. If an issue arises, you can limit the impact to a smaller user group. For example, in Instead of

[00:04:21] deploying a new version to all users simultaneously, you deploy it to a small percentage of users or instances. You monitor performance, you get user feedback, and gradually you increase the exposure. If everything looks stable.