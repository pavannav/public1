[00:00:06] What does squashing mean? To squash in Git means to combine multiple commits into one. Let's say we want to combine C3, C4, and C5 into a single commit. Then we can use Git squash to create a new commit,

[00:00:22] which will be a combination of C3, C4, and C5. When you should squash commits, it really depends on what you have decided as a team. Instead of many individual commits, which might be unnecessary, with squashing, you only get a single commit appearing in your main commit history.

[00:00:45] This can be helpful in keeping commit history clean. There are multiple ways to squash your commits. The first way is using interactive rebase. We'll talk more on it in the interactive rebase section.

[00:01:01] The second one we have is using merge. You can squash all the commits of a feature branch while merging into the main branch. The main benefit of this is that history stays clean and there is no automatic merge commit. If you merge without squash, Git will create a merge commit.

[00:01:22] Another way to squash is using pull request. A lot of Git hosting platforms like GitHub, GitLab, Bitbucket, they provide an option to squash the commits when merging a pull request. So there is an option when you click on merge pull request, and you can choose squash and merge.

[00:01:42] This will merge and also squash all the commits of your feature branch into a single commit. Let's go back to our repository. So let's start by creating a new branch, git checkout, minus b, feature squash.

[00:02:05] Let's make three commits to this feature branch. Squash commit one. Let's also change the file to squash file two.

[00:02:22] Commit one. Let's add the changes. Squash commit one. Let's make two more commits.

[00:02:44] And change the commit message. One more commit, so we'll do file three, commit three, git add, and make the third commit. Now when we do git log, we have three commits that

[00:03:03] we made, commit one, commit two, commit three. Now let's say I want to merge this feature branch into my main branch. So for that, I will have to go back to the main branch We can do it using checkout command, get checkout main.

[00:03:21] We are on main branch. Now in order to merge, we need to run the merge command with git merge, name of the branch, which is feed squash. Now with merge, you can use a flag, hyphen hyphen squash.

[00:03:42] Now what squash does is that it brings all the changes to your working directory. So now all the changes related to those three commits are in your working directory, but you need to commit them. Now there are two ways how you can commit them.

[00:04:00] One way is that you can simply do commit and provide your own commit message. This is a combination of Last three commits. Or what you can do is just use git commit. And this time git created an automatic message which says squashed commit of the following,

[00:04:22] which contains squash commit three, commit two, commit one. So git also automatically generates a message for you. You can use the same one or you can write your own commit message. So let's use the same one. And now when you do Git log, you have just one commit,

[00:04:41] which is the squashed commit of the following three commits. So this is how you can squash while merging. Another way to do it is using pull request. Let's see how we can do using pull request.

[00:05:01] For that, let's create a new feature branch. Get checkout minus B, feature squash using pull request. Let's make the changes again. Let's make three more commits again.

[00:05:28] Squash with pull request. Commit one. Let's commit the changes.

[00:05:47] Commit one. Let's make two more commits. Git add all, git commit. Last commit, git add all.

[00:06:07] Git commit. Okay, so we have made now three more commits. Let's change the commit message of the last commit because

[00:06:22] I accidentally choose the same commit message. Git commit hyphen hyphen amend. And now we have three commits. Commit three, commit two, commit one. Now this time we want to merge using pull request and also squash.

[00:06:43] So first thing we need to do is we need to push this pull request, squash pull request feature branch to remote, and for that we need to do git push minus u, the branch name. I have to use minus u flag because this branch right now does not exist on remote.

[00:07:08] Okay, just try git push and use the same command. So now our feature branch is already pushed and there's a link to create pull request.

[00:07:22] Let's go to this link. Let's create a pull request, squash demo using pull requests. Best description. And then click on create pull request.

[00:07:51] OK, this branch has some conflicts which needs to be resolved. Let's resolve using.

[00:08:01] GitHub this time. Let's accept our changes on this branch and mark as resolved. Commit the resolved changes. So this time I just resolved using GitHub, but if you have conflicts, you need to,

[00:08:20] you can also resolve using your command line. So now this time, before clicking on merge request, I will click on this arrow and it says squash and merge. So let's do the squash and merge. And now it gives you an option to change the message as well.

[00:08:41] So I can do squash demo with pull requests. Okay, let's verify the changes in our main branch. Let's go back to our repository and then do git pull.

[00:09:07] Now we do Git log, and we can see we have the commit from the branch that we merged from the pull request. So this is how you can merge along with squashing using pull request.