[00:00:06] Welcome to this section, load balancing. If there is one technology that you need to master when it comes to reliable architectures, it's got to be load balancing. It's an extremely critical component when it comes to reliable architecture.

[00:00:22] What is load balancing? It's basically the process of distributing workload across multiple servers to minimize and optimize resource use. Maximizing throughput and minimizing response time to avoid overload on any single server.

[00:00:41] Basically what it means that you will receive the incoming requests And then spread the load across several backend resources in a pool. Load balancing is integral to several distributed architecture. In fact, you will not see any distributed architecture without a load balancer in the mix.

[00:01:02] So why do you use a load balancer? First, it ensures high availability. Among its backend pool of servers, if one server is down, the service is not impacted. The load balancer ensures that its traffic is sent to the available servers. Facilitates scalability.

[00:01:21] You could add or remove servers behind the load balancer in the server pool without even the users noticing it. Optimizes performance. Load balancers help with overloading one particular server. It avoids overloading by spreading the load among many pool members.

[00:01:43] Load balancers also enable intelligent routing. Later in this section, we will look at various load balancing algorithms that will explain how intelligent routing can be done using load balancers. Here is a pictorial depiction of how load balancers work at a very high level.

[00:02:02] The user connects to the load balancer, and the load balancer spreads the traffic among a bunch of servers in the backend. Notice that the user does not even see the backend servers. This is one of the major advantages of the load balancer. It hides the backend pool of servers.

[00:02:22] So if a server goes down in the backend pool, the user hardly even notices it. There are various types of load balancers. Hardware load balancer, software load balancer, DNS-based load balancer, and a cloud load balancer.

[00:02:40] It is so important that you understand each one of them. So let's look into the details of each one of these load balancers. First, hardware-based load balancers. These are specially designed physical devices. In fact, these were the original hardware-based load balancers that came to the market in the late 80s, early 90s.

[00:03:02] When F5 was released, for example, it was a groundbreaking technology in load balancing. Again, they deliver high performance using ASICs, application-specific integrated circuit in their hardware. This is an expensive option, though, because it is a proprietary hardware,

[00:03:23] but it will provide you the best performance. Examples of hardware-based load balancers include F5, CAMP, and Cisco. Again, you will see hardware load balancers whenever you need high-performance and mission-critical load balancing.

[00:03:40] But the downside of load balancers is that it is very expensive. Whenever we talk about load balancer, nowadays we usually refer to a hardware-based load balancer like this, like F5, for example, or a cloud-based load balancer that we will be seeing shortly. Software-based load balancer. What are these?

[00:04:00] These are basically programs that run on VMs or bare metals. They deliver varied performance, mainly because they rely on the performance of the underlying hardware, whether it's a VM or a bare metal, depends on the resources they have for the most part. But there is one advantage of software-based load balancers.

[00:04:21] They provide a lot of flexibility. Think of this as software-defined load balancing, similar to software-defined networking. Examples of software load balancers include HAProxy, NGINX. Now, many of you know NGINX as a web server, and it indeed is.

[00:04:40] However, it is increasingly used as a proxy, or a reverse proxy, to load balance across many backend servers. It is extremely powerful. DNS-based load balancers. What are these? These load balancers use DNS to distribute traffic,

[00:05:00] often used for geographical load balancing. They can route traffic based on load. When we say geographic load balancing, we talk about user's locality. The great example for this is Akamai, the content delivery network provider. Depends on where the user is,

[00:05:20] his request will be routed to the closest location to his geographic position. In this way, the request does not have to traverse through large distances. DNS-based load balancers are effective in geographical based load distribution. Finally, the cloud load balancers.

[00:05:40] With the advent of public cloud, the load balancer technology has skyrocketed. There are many load balancing solutions that are being provided by the CSPs. These are fully managed load balancing service. You don't have to worry about software-based or hardware-based or F5 or NGINX.

[00:06:01] The CSPs take care of this for you. The load balancers can automatically scale based on load, again, because it's fully managed load balancing service. It supports a variety of traffic types. Examples include AWS NLB and Azure Load Balancer.

[00:06:21] Even within cloud load balancers, there are varieties of load balancers provided by these CSPs. Talking about load balancers, you need to know the difference between layer four and layer seven load balancing. Layer four, as many of you know, is the OSI transport layer. The layer four load balancers work at that level, TCP and UDP traffic.

[00:06:45] Whereas layer seven, which is the OSI application layer, works with the protocols like HTTP, HTTPS, FTP, and so on. Layer four-based load balancers route based on the IP address and port.

[00:07:01] Layer 7 load balancers, on the other hand, route based on URLs, headers, cookies, et cetera, application data. Layer 4 load balancers are extremely fast because they do not look at the packet data a lot. Minimal packet inspection. However, Layer 7 load balancers can be slower compared to Layer 4 load balancers.

[00:07:24] That's because it has to do deeper packet inspection. Layer four load balancers are not application aware because they work at layer four, transport layer, whereas layer seven load balancers are fully application aware because they work at the application layer.

[00:07:44] So where do you use these types of load balancers? When you need a simple, high-speed traffic distribution, you would use Layer 4, especially when you don't need application-level routing. You would use Layer 7 load balancers when you need complex routing,

[00:08:01] mainly used in web applications and microservices. Note that many hardware and software and cloud load balancers are capable of operating at both layer four and layer seven. To complete the discussion on load balancers, we need to look at load balancing algorithms.

[00:08:20] There are six algorithms that I'll walk you through. First is round robin. This is simple and equal distribution in sequence. Weighted Round Robin assigns weights based on capacity, and it will help in influencing the target selection. Round Robin is the simplest. Weighted Round Robin adds a little bit more configuration to it.

[00:08:44] Leased connection, this is based on the number of active connections to the backend server. Leased response time, now here it takes the server performance also into account, based on server response time to the client requests.

[00:09:00] Least load, this is similar to least response time, but here the algorithm works based on the load reported by the backend server. Finally, least load with slow start, and this is an advanced algorithm that is used to prevent overloading a new server. For example, let's say you are in the process

[00:09:20] of adding five new servers to the pool. After you add the first server, if you don't pace the load to that server, you run the risk of overloading that server. So the slow start helps you with the slowly ramping up traffic to newly added servers. It is an extremely powerful algorithms.

[00:09:41] Great, we learned a lot about load balancers, what they are, what are the various types of load balancers, and some of the popular load balancing algorithms. Note that there are many load balancing algorithms, especially in public cloud load balancers, as they provide a lot of configuration opportunities.

[00:10:00] Now, as an SRE, you need to be fully aware of these load balancing techniques. This is what will make you stand out as a competent SRE compared to others. Learn all the load balancing algorithms that you need to and master them. In the next section, let's take a look at how do you handle failures.

[00:10:21] We will look at some of the application architecture techniques to handle failures when they occur.