[00:00:06] Four golden signals to monitor. When it comes to what to monitor in real life, these are the four golden signals that you should be after, and you should be monitoring it all the time.

[00:00:20] They will help you to increase reliability. Let's get started. These are the four golden signals, latency, error rate, throughput, and saturation. I'll repeat, latency, error rate, throughput, and saturation. Make sure you memorize these.

[00:00:40] These must be monitored all the time. Let's look into the details. Latency, what is it? It's basically response time. It's pretty straightforward, but the important point here is it's the response time that the user sees. You need to make sure that you capture the response time as close as possible to the user in your system.

[00:01:03] One thing about response time, especially if you are simply monitoring it from an access log, is to be cognizant about the response time of incomplete or failed requests. For example, let's say your access log has three milliseconds as the response time.

[00:01:22] And if you think everything is great and there is no problem with the user experience, you may be wrong in some instances. What if this was a fast fail? Some applications tend to do that. In which case, you will see three milliseconds or five milliseconds as a response time.

[00:01:41] You may be thinking that everything is fine, but in reality, user is actually experiencing errors. So you need to make sure you monitor the response times of the actual user requests. Error rate. This is an important signal to monitor. What is error rate? It is essentially failed requests divided by total requests times 100.

[00:02:05] It is a percentage of requests that are erroring out. This will directly show you what the user is experiencing, and that can be an indicator of a problem. Now, we need to explore errors a little bit more, because what error means is not the same for two different applications.

[00:02:24] For example, hard errors, HTTP 500 internal server errors, they are errors no matter what. So they need to be counted as errors. But not all HTTP 200, which is usually OK in HTTP protocol, can be seen as good.

[00:02:43] It could be 200, but the user may be actually seeing an application generated error in the screen. So that should be considered as an error. Then we have HTTP 200, but the response time took longer than usual. And when we talk about SLIs and SLOs later,

[00:03:02] we will look at the response time equation. But the response time need to be considered when you consider errors. It is quite possible that a user saw 200, but after a very long delay, they must be considered as errors.

[00:03:20] Finally, application generated error. If you are simply relying on your access log, you might miss application generated errors. So they need to be captured as well. So again, I want to reiterate that calculating errors is tricky. It depends on the application. You need to really sit with the developers, if you are not the developer,

[00:03:42] and really understand how to capture the errors. With that said, errors is one of the four golden signals that you should be monitoring all the time. Specifically, you should be deriving the error rate. That can be a better monitoring to monitor in a time-serious fashion where you can see the trend of error rate.

[00:04:03] The third golden signal is throughput. What is throughput? It's essentially the load on the system. It's the number of requests processed by the system in a given window. So this refers to how much are you really processing. And this is important to monitor for several reasons.

[00:04:23] Let's explore this. First, this tells you how much is the traffic coming to your system, and that can be valuable. For instance, if you are seeing a spike in the volume that is coming in, that can indicate a potential problem that is coming down the pipeline soon because the volume is more than expected.

[00:04:44] Second, it reveals the load on the system. So whenever we talk about what is the load on the system, throughput can be used as an awesome indicator. Usually, it is referred to as the number of requests per second. It can be per minute, also depends on your application.

[00:05:03] Lower throughput can indicate a problem in the network path leading to your system. And this is why it is important to monitor throughput. You might see that the throughput is low, and you may think that it is OK, because you don't see a lot of errors, the other golden signal.

[00:05:21] But low throughput can actually indicate a problem. For example, users may not be able to reach your application due to issues with other network components. The final golden signal here is saturation. What does saturation refer to? It's basically a measure of how constrained the resources are.

[00:05:42] Saturation is more of an infrastructure metric, not always. Even application can have saturation metrics. Let's explore this a little bit more. Saturation refers to capacity. If you are running out of capacity, you are saturated. The most common examples are CPU, memory, disk

[00:06:02] space, thread pool, and bandwidth, and so on. If they are getting saturated, somewhere along the pipeline in your architecture is an issue. So saturation can indicate that for you so that you can be proactive about it.

[00:06:20] Saturation usually leads to latency. So eventually, the latency, the other golden signal, might show you what's happening. Perhaps the user is experiencing poor response times. The saturation may indicate what is going on and maybe what is the cause for the poor response time. For instance, in this case, your servers may be running out of CPU.

[00:06:44] You are maxed out on CPU. You are saturated on CPU. Thereby, a solution could be to scale up. Saturation is often overlooked metric. In my experience, when we troubleshoot, we always look at metrics such as response times, error rates, and so on,

[00:07:01] but we never really look into Can we check if the network bandwidth is being saturated? Or can we check if the backend database's CPU is getting maxed out and so on? Again, saturation is more of an infrastructure metric, but it can be from application as well. But the point here is make sure you include saturation In your observability system.

[00:07:26] So those are the four golden signals that you should be monitoring all the time. They can help you with improving your reliability. Again, they are latency, error rate, throughput, and saturation.

[00:07:41] As a short form, you can even call this LETS, L-E-T-S. Latency, error rate, throughput, and saturation. Now that you have the background of four golden signals, in the next section, let's look at some of the most popular observability systems out there,

[00:08:00] and also let's look at the differences between monitoring and observability.