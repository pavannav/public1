[00:00:00] When cloning a repository you have a couple of different options. In this case I have a repository of mine on a github and when I want to clone something I'll go to the code button. Now you don't always need to go there you might know

[00:00:20] the address by default in this case is github.com slash alfredodesa slash python bootcamp dash duke. Now that's fine but I want to show you here what you should expect. Now what you want is the local tab you have also code spaces which is something different but what you want is to be right here and in this you would have three options HTTPS, SSH and the github

[00:00:46] CLI. You also have the capacity or the ability to download a zip, a compressed version of all of the files on the repo or open directly with Visual Studio or open with github desktop. I primarily focus on this one right here which is I

[00:01:04] cloning the repository with a specific address. Now HTTPS is useful when you don't own the repository and when you don't have permissions to do anything

[00:01:20] else for the repository itself. I tend to use SSH which is a specific secure transport protocol, a network protocol for retrieving files in this case. Now we can try with both the operation is the same for both. For SSH it will require to

[00:01:41] have your keys and your SSH keys in the repository. In this case I'm going to keep things simple. I'm just going to use HTTPS which doesn't require me to do anything. The full URL has a .git at the end so that .git at the end

[00:02:00] is useful because that's what you need to add for the whole address which is the same address as the one here in the URL. So I'm going to copy these to the clipboard and then I'm going to go to my terminal. So I'm going to switch over to my terminal and the command is in git clone and I'm

[00:02:22] going to do I'm going to paste that in HTTPS colon slash slash github.com slash alfredodesa slash python bootcamp-duke.git. So I'm going to run that and when I list the contents of my directory I had previously a my-repo there

[00:02:40] directory and I'm going to have python-bootcamp-duke. So if I go inside of there and I list the contents you will see that there's the readme file, the resources directory, and the rubric. If I go back to the repository here you will see that those are the same files and this is exactly the same content as

[00:03:03] they have. So essentially what I've done with cloning is get all of the files and all of the history, in this case there's just one commit, all the history inside or actually into my own machine. So I have these files now not on

[00:03:20] the website, not on the web the github platform, but I have it in my local computer as a directory. So that is those are the essentials of a github or cloning a git repository and in this case and doing it so with one of the

[00:03:40] options from the github platform which is using HTTPS from a repository that I own.