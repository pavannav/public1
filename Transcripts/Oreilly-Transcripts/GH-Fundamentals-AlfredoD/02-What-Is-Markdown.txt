[00:00:00] Markdown is a way where you can write a plain text and have it rendered nicely in various different outputs including something that will look amazing on a website. Now there's multiple different types of applications for markdown and in this case the github platform uses markdown explicitly for rendering its

[00:00:29] for doing all kinds of different things like describing issues, describing pull requests and even describing what a project would look like. So in this

[00:00:40] case if you wanted to have a powerful readme like in this case where we have this piece of documentation from the github website, the github platform, you can actually have something like that. So markdown is a type of markup

[00:01:02] language like HTML. Now it has several different types of syntax and rules that will apply and depending on where you're adding them it will render differently. Now the great thing is that whenever you're using markdown throughout the github platform you will be able to have it rendered in a

[00:01:23] very nice way. For example here we have my own personal repository and it does have a readme. This specific readme has a specific different things that will allow me to display nicely when it's rendered. This is how the plain text

[00:01:42] looks like and it looks very very rough. Now whenever this is rendered we will be able to have certain rules applied. All of these special characters like say for example this one right here will mean it has a special meaning. In markdown that means that this will be in italics and when you're using backticks it will

[00:02:04] be using monospace. Now this is a good way to have a specific syntax and having that displayed nicely when it's actually rendered. When it renders it actually looks pretty useful and pretty nice and you can even have

[00:02:22] things like buttons like this one or have emojis and have capitalized and titles when when you need it because you want to highlight certain things in a certain way. So that's what's markdown and we'll take a look at how that works throughout the github platform.