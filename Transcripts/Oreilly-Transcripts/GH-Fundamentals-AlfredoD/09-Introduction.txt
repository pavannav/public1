[00:00:00] The GitHub platform is based on this tool, this version control tool called Git. Before we dive more into what GitHub is, and understanding more of its concepts, understanding what Git is and how we can interact with a repository and how can we get the code

[00:00:24] into our local system, how we can make changes, and how we can effectively work with code when we're using version control, in this case, Git. So we will be using the terminal and command line tool to make some of these operations.

[00:00:40] So once we understand exactly how to operate with this, we can take more complicated, more involved steps later when we have to do other types of operations.