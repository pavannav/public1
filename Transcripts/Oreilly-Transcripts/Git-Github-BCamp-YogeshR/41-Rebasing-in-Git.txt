[00:00:00] Hello, everyone and welcome back in this lecture. We are going to explore kit rebasing an important concept that often confuses beginners but is incredibly powerful. Once you understand it, let's dive in. So first, what exactly is rebasing

[00:00:21] in simple terms, rebasing is a way of moving or combining a series of comets onto a new base comet, think of it as rewriting the history of your project so that your work appears to have started from a different point in time.

[00:00:40] Imagine this scenario, you and a teammate are working on the same project. Your teammate is making changes on the main branch while you are developing on a feature branch over time, the main branch progresses with new commits and your feature branch falls behind

[00:01:01] rebasing allows you to take your commits and place them on top of the latest main branch as if you had started working after those new changes were already in place. Now, why should we use replacing? The main reason is to maintain a cleaner and

[00:01:21] more linear project history in your git project. When you rebase, it looks like your feature branch was always built on top of the most recent changes, this avoids unnecessary branching paths and extra merge commits giving you a neat

[00:01:40] straight line of commits that's easier to read and understand. It also makes collaboration smoother. When you rebase before merging your feature branch, your teammates don't have to deal with extra merge commits. They will see your changes as if they were developed

[00:02:00] in direct sequence with the rest of the project. Now, you might be wondering, isn't this the same as merging, not quite merging, combines two branches together, but it retains the complete history of both. This often results in what's called a merge commit.

[00:02:24] You end up with a branching history that shows exactly when and where the two branches joined. Rebasing. On the other hand, rewrites history instead of showing a branch split and then merged back,

[00:02:40] it makes it look like your work was always based on the latest main branch. No extra merge commits just a smooth line of commits. So which one should you use? It depends if you want to preserve the complete history of how development happened,

[00:03:00] including all the merges, then merging is the right choice. If you prefer a simpler and cleaner history without the extra noise of merge commits, then rebasing is the way to go. Let's put this into a simple example. Imagine your main branch has three new comets

[00:03:22] AB and C. Meanwhile your feature branch has commits X and Y. If you merge your history will show commits AB C followed by a merge commit and then your commits XY, if you rebase your history will instead show commits AB C followed directly by X and Y

[00:03:46] as if your work started after commit C both methods preserve all the changes. But the history looks much cleaner with rebasing

[00:04:00] before we wrap up one quick word of caution, rebasing, rewrites history. If you rebase a branch that other people are already working with, it can cause confusion and conflicts. A good rule of thumb is only rebase branches that you haven't shared with others yet.

[00:04:23] For shared branches like main or master merging is usually the safer choice. That's the essence of git rebasing. In the next demonstration, we will practice this with some hands on commands. So you can see how rebasing works practically

[00:04:41] see you there.