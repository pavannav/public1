[00:00:02] Hello, everyone. Welcome back in this project. We'll put our learning into action with a practical demonstration that ties together all the concepts we have covered so far, we are using visual studio code for this demonstration.

[00:00:20] But you can use any type of ID. We have created a simple landing page using basic HTML and CSS which includes a logo, a navigation bar with links like home courses about and contact along with some simple content on the landing page.

[00:00:44] It's a minimal web website for now, but that's enough for us to focus on practicing it. The next step is to manage this project using it. Our goal in this demonstration is to walk through the complete workflow from initializing,

[00:01:01] get committing changes and pushing them to a remote repository to create branches for new features and merging them back. Let's get started. Let's switch back to the VS code in the VS code terminal. If we run git status,

[00:01:22] we'll notice the message saying this is not a git repository and we cannot see any of git folder in this directory. That's because git hasn't been initialized yet. So the first step is to run Getit in it.

[00:01:40] This initializes git in a project folder and creates a hidden git directory where git tracks all changes. You can see a few helpful hints displayed similar to what we saw when initializing repositories earlier. Also on the left navigation bar,

[00:02:02] all files appear in green with a U in front of them indicating they are unstaged. We see this because of the git extension in VS code. Now if we run git status again, it shows that we are on the master branch

[00:02:21] with no commits yet and all files are untracked to start tracking them. Let's add all files using Gadt running its status. Again shows that the changes are staged and ready to be committed.

[00:02:43] Next, we will commit these changes with git commit. Hyphen M initial commit. If we check the commit history using git log hyphen knife in one line, we can see our first commit, initial commit on the master branch.

[00:03:04] So far we have initialized getit added our files and created our first commit. Also, if we list all files in the folder, you can see the kit folder as well. Now let's connect this project to github

[00:03:23] for this demonstration. We will use a personal github account. We will understand in upcoming sections why we are using a personal account for this. Now, on github, let's create a new repository. We can do this from the plus icon or by clicking on the new button.

[00:03:45] Let's name it. Think Crook hyphen web site and add a short description. This repository is for think Groupb website. We will keep it public and won't add a read me

[00:04:02] dot gitignore or license file. Since this is just a simple static HTML project once created github provides a repository URL. Now this entire setup is being done on a personal laptop, not the Linux machine we used earlier.

[00:04:23] Since we have already initialized staged and committed our files. The next step is to push the changes. First, we need to add a remote. Let's go back to the terminal and clear the screen for better visibility.

[00:04:42] We'll add the remote using git remote add origin and the repository URL to verify we can run git remote hyphen V and confirm that origin is set up for both fetch and push.

[00:05:00] Now let's copy the next command to rename the default branch by default. A branch is called master but github now uses main as the default branch. So we'll rename it with git branch. Hyphen capital M main. Let's run git branch

[00:05:20] again to recheck. So our branch name is changed to me. Now, we will push the project to get up with get push hyphen U origin main git uploads all the files and our local branch is now linked to the remote branch on github

[00:05:43] running git status. Again, confirms that nothing to commit in a repository and the working tree is clean. If we refresh the github page, all our project files are visible along with the initial commit.

[00:06:00] Next, let's add a readme file to do this in a clean workflow. We'll create a new branch dedicated to this task we execute, get check out hyphen B read me hyphen branch. Now we are on the read me branch.

[00:06:22] We can confirm this by using git branch again. Now inside VS code, let's create a new file called Readme dot MD. A read me MD file is a plain text file that explains a project's purpose functionality and usage.

[00:06:43] It serves as the primary documentation for software projects in this file. We will add a heading like therook hyphen web site along with sections such as about usage instructions, technologies, used

[00:07:00] and contribution guidelines. This helps anyone looking at the repository, understand the project better. Once the file is ready, we will go back to the terminal and execute its status again. Here we can see the Readme file is unstaged.

[00:07:23] Now we will stage and commit it with get at dot get commit hyphen M added readme file, running its status again, you can see the working tree is clean.

[00:07:40] Now let's push the changes and this read me hyphen branch to get up with git push origin read me hyphen branch on github. We can now see the read me hyphen branch with the new file. Normally we would open a pull request to merge it into main.

[00:08:03] But for now let's handle it directly from the terminal we'll switch back to the main branch with git main, then merge the branch with git, merge, read me hyphen branch.

[00:08:23] Let's run git status. And here you can see our branch is ahead of one commit. So let's push the updated main branch to get up with git push if we refresh github, the Readme file now appears in the main branch.

[00:08:43] Github automatically detects the Readme file and displays the content on the repository page. So let's quickly recap the steps we have done in the project. We built a simple website as a base project.

[00:09:04] We initialized git in the project folder, we staged and committed the initial files. We created a remote repository on github and connected it to our local repository. We pushed our coat to get up.

[00:09:21] We created a new branch for the Readme file, committed the changes and pushed the branch. Finally, we merge that branch into the main branch and confirm the changes on github. This is a complete practical workflow for managing a project with git and github

[00:09:41] from initialization to branching, merging and pushing changes. That's it for this demonstration. In the next lecture, we will move into collaboration features and see how github supports team workflows.