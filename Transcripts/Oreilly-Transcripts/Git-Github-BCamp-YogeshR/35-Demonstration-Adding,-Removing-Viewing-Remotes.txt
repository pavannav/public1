[00:00:02] Hello everyone. And welcome back in this demonstration. We are going to learn how to add, remove and view remotes in git. If you remember from the previous lecture, we already added a remote called origin

[00:00:21] to view the list of remotes. We use the command git remote hyphen V. The hyphen V option stands for verbos. It displays the remote names along with their fetch and pull LS. When we run this command, you can see that the origin remote is listed.

[00:00:44] Now, let's try adding another remote. Imagine you want to connect your local repository to a second repository. For example, backup repository to do this. We will first create a new repository on github.

[00:01:00] Click on the new button, give it a name like backup hyphen repo, leave all settings at their defaults and click on create repository. Once the repository is created github provides us with the command to add it as a remote

[00:01:21] just like before copy that command and paste it onto the terminal. Here we encounter an error. That's because the name origin is already used by the existing remote. So we will use a different name for this remote.

[00:01:40] Let's call it back up. The command will look like getit remote and backup repository. URL. After running this command, check the remotes again with git remote hyphen V. You can see that both origin and backup is listed

[00:02:04] at this point. A backup remote is set up for both fetching and pushing. Don't worry if you're not entirely clear about fetch and push yet, we'll explore these concepts in upcoming lectures. Next,

[00:02:20] let's see how to rename a remote. For example, instead of just backup. Suppose we want to call it backup hyphen new. We can do that with get remote rename, backup space, backup hyphen new.

[00:02:40] When we check the list again with git remote hyphen V, we see that the remote name has been successfully updated. Finally, if you want to remove a remote, then execute git remote, remove backup hyphen new. And once again, if you list your remotes with git

[00:03:00] remote hyphen V here it shows that only origin remains. So that's how you can manage remotes and getit, adding new ones, renaming them and removing them when they are no longer needed. That's it for this demonstration.

[00:03:21] In the upcoming lecture, we will discuss pushing and pulling basics. See you there.