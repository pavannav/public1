[00:00:00] Hello, everyone and welcome back in this demonstration. We are going to learn about stashing changes in git. So at first, let's understand what exactly is stashing stashing in git is like putting your unfinished work into a temporary storage box. We are not ready to commit our changes yet,

[00:00:21] but don't want to lose them either. We can stash them. This allows us to safely set them aside and return to them later. It's especially useful when we are in the middle of something and suddenly we need to switch branches or handle another task.

[00:00:40] Instead of committing incomplete work, we can stash those changes, focus on the new task and restore them whenever we are ready. Now, for more clarity, let's see this in action. We are inside the Linux terminal where several repositories are available.

[00:01:02] Getit hyphen demo. Next hyphen backup and ssh hyphen backup for this demonstration. We will use get hyphen demo. Let's list all the files here inside this folder, check the status width git status

[00:01:21] as expected. The branch is clean and up to date. Now let's modify a file open. Hello dot TXT and add a new line that says add it for stashing changes, save the file and close it.

[00:01:40] Now view its content to confirm the update. And here you can see the line we added running git status again shows that hello dot TXT has been modified. Now, suppose we need to switch tasks, but we don't want to commit this change yet. Instead of leaving it uncommitted, we can stash it using

[00:02:03] git stash git confirms that it has saved working directory and index state wip on main some hash id and the message for this dash. If we check the status again,

[00:02:20] the working directory is clean. No pending changes opening hello dot txt shows that the extra line we added has been temporarily removed. So where did it go? It's now stored safely in the stash to see what's been stashed, we can execute

[00:02:42] git stash list. This shows an entry with an id like this stash at the rate, curly braces, zero, the branch name ma and some hash id with short message describing the stash changes.

[00:03:03] Now, let's say our other work is done and we are ready to bring those changes back. We can restart the most recent stash using git stash apply. This brings the stash changes back into the working directory

[00:03:21] here. You can see a similar response to get status, checking the status again, we can see that Hello dot txt is modified and opening the file confirms that the dashed line is back in place. Notice that kit stash apply, keeps the stash in the list,

[00:03:42] meaning it can be reapplied later if needed. If we run git stash list, we will still see the same stash present. However, if we want to apply the stash and remove it from the list in one step,

[00:04:00] we can use git stash pop. This is like taking an item out of the box and discarding the box afterwards. Let's run, get stash again. We can see the same message here. If we run git stash list, you can see two items in the list now and the below one is the latest in the list.

[00:04:24] Now, if we run git stash pop, we can see the same message where git shows that hello file is modified. And again, if we execute git stash list, notice that we have only one stash left now, the other one is applied and popped out.

[00:04:42] This is the difference between git stash apply and git stash pop apply, restores and keeps it while pop restores and deletes it. If there are multiple Stashes, we can also apply a specific one using its ID

[00:05:01] git stash apply and the stash ID, this gives us precise control over which task to restore. One important thing to remember. Stashes are local only. They are not pushed to github or shared with others. They live on a local machine

[00:05:21] making them ideal for quick personal saves, not collaboration. And that's how we can use. Get ST a simple yet. Powerful feature to pause our work, switch tasks and return to our progress without losing anything. That's it for this demonstration. In the next lecture we'll understand.

[00:05:42] Rebasing in git. See you there.