[00:00:00] Hello, everyone. And welcome back in the previous demonstration, we created a release in github using tags. But before we go further, let's take a step back and really understand what tags are because they form the foundation of versioning in kit.

[00:00:20] So what exactly is a tag? A tag in git is like a permanent label. You attach to a comet, unlike branches which move forward as new commits are added, a tag always stays fixed on the comet. It was created for this makes it very easy to identify and

[00:00:40] return to important points in our project's history. Git provides two types of tags, lightweight tax, annotated tax. Let's go ahead and explore both log in to the Linux machine where we have been running all our demos.

[00:01:00] Let's list all the files. We can see our think crook hyphen website directory. Let's navigate into this directory and check the status. It's saying the branch is diverged and suggesting to use git pull, execute,

[00:01:20] git pull, git pull is complete now. And if you notice git is also pulling the tag that we created now to look how we can check the tags in our directory. The commanders get tag enter.

[00:01:40] You can see V 1.0. This is the same tag that we created in previous demo list, the files. Again, everything looks good. So let's move ahead. Let's quickly create one or two files,

[00:02:02] stage it commit and push to the remote repository. So the files are pushed. Now, let's clear the screen. As we understood, we can add tags via two methods.

[00:02:21] First, let's see, lightweight tags. Suppose we have just completed some work and committed it. And we want to mark this point as version 1.1 we can do with a simple command git tag. V 11, this creates a lightweight tag named V 11

[00:02:43] pointing to the latest comet. If we execute git tag, we'll see both the earlier V 10 tag and the new V 11 tag listed to see details about a specific tag

[00:03:01] run git show V 1.1. This displays the commit details associated with that tag. So lightweight tags are straightforward. They are just pointers to commits without any extra information.

[00:03:21] Now let's move to annotated tag, annotated tags are more powerful. They store additional information such as the tagger's name, email, the date and a tagging message. Unlike lightweight tags,

[00:03:40] annotated tags are full objects in git's database. This makes them the recommended type for most project releases to create one. Let's first make a quick change in the demo file stage it commit it

[00:04:00] and push it to remote repo also, we can add a tag before pushing as well. But for the demonstration purpose, we are just adding it after the push. So once that's done, let's create an annotated tag with this command

[00:04:23] git tag. Hyphen A V 1 2 hyphen M version 1.2 release here. The hyphen A flag specifies that it's an annotated tag. And hyphen M allows to add a message describing the release.

[00:04:41] Now, let's run git tag here. We can see all the tags listed if we run, get show V 1.2. Notice this addition to the commit details. We can also find the tagger information and the message entered.

[00:05:00] This clearly shows why this version was tagged, not just where it appears in the project's history to list all tags in detail. We use git tag hyphen L now creating tags locally is just the first step to share them with others. We need to push them to the remote repository.

[00:05:26] There are two ways to do this. Push a single tag, git push origin B 12 or push all tags at once. Git push origin hyphen hyphen tags.

[00:05:43] Once pushed, let's go back to the github repository. Next, let's go to the releases. We can observe these tags listed under the tags section. And just like before we can use this tag to draft or publish new releases in github.

[00:06:04] For example, we could use the V 12 tag to create a new release with its own notes and mark it as the latest release. Next push this release, let's go back to the repository page

[00:06:24] and under the releases, you can see P 12 as the latest tag. Now, before we wrap up, remember this important distinction tags are markers in git history that identify specific commits

[00:06:41] releases built on top of tags, provide a more user friendly and shareable version often with documentation binaries or packaged files. And that's how we create both lightweight and annotated tags. Push them to get up and use them as reference points for versioning our projects.

[00:07:05] That's it for this demonstration. See you in next.