[00:00:00] Hello, everyone. And welcome back in this demonstration. We are going to learn about rebasing in git and see it in action. We are inside git hyphen demo folder. If we list all the files, we can see the same files we created earlier to start. Let's do a little project clean up

[00:00:22] by moving all the TXT files into a dedicated folder. We'll use the command git MV, asterisk dot TXT space, TXT. Once the files are moved, commit the change with a message git commit

[00:00:40] hyphen M move TXT files to TXD folder. Now, if we run git log hyphen and one line hyphen F and graph, we get a visual representation of our commit history here, we can see the latest commit. We just started

[00:01:01] and below that multiple earlier commits. We can also see that the line has diverged and then merged each star represents a commit, followed by the hash id and the commit message. If we notice closely after a certain commit, the line diverges,

[00:01:22] that means the history split into branches. A few comments were made independently and then everything merged back together. This happened earlier when we performed a merge in one of our previous demonstrations. And again, later

[00:01:40] when we created another branch made commits and merged it back. This is the typical pattern seen when using merges branches, diverge independent work is done and then they come back together sometimes requiring conflict resolution.

[00:02:01] Let's try this out first, let's check the branches in our working directory. We currently have two branches main and feature for this demo. Let's create a new branch called rebase hyphen branch. Get check out

[00:02:20] hyphen V. Rebase hyphen branch hit, enter git switches us to the new branch called rebase hyphen branch. We can confirm this by running git branch. And yes, we are. Now in Reba's hyphen branch at this point, the branch is an exact copy of mean,

[00:02:43] let's also check the logs in one line and graph view here. We can see the same logs that we present in the main branch. Great. Now inside this branch, let's create a couple of new files. First, let's create a file called rebase

[00:03:01] dot TXT touch. Rebase dot TXT. If we list files, we can see it added. Now let's stage and commit it. Getit add rebase dot txt git commit hyphen M rebase file added.

[00:03:20] Now if we check the logs again, we can see head pointing to the rebase hyphen branch with the commit message we just added now create another file with the name rebase two TXT. Touch. Rebase two TXT. Let's stage and commit this as well.

[00:03:42] Git add rebase two txt git commit hyphen M rebase to file added, checking the logs. Again, we can see both commits clearly added on top of the branch. Now let's go back to the main branch GIC main.

[00:04:00] We'll ignore the message about being one commit ahead and clear the screen. Let's check the logs again. Currently, the main branch doesn't have those two new commits. The latest commit here is the one we made at the starting of this demonstration.

[00:04:20] If we were to merge Rebase's hyphen branch into main, the comet graph would show a merge comet with branches diverging and then coming back together. But if we want a clean straight history, we can use rebase. Instead, let's clear the screen and check our branches once more.

[00:04:41] Good branch. We are currently on main and we want to rebase this branch. The command is git rebase. Rebase hyphen branch. This command takes the commits from Reba's hyphen branch and replace them on top of the main

[00:05:01] git confirms that the rebase was successful. Now, if we execute git log hyphen hyphen one line hyphen F in graph, we can now see the comets tagged neatly in a straight line, the commit that move the TXT files, the commit, adding rebase

[00:05:20] TXT file and the commit, adding rebase to TXT file. The history is clean and linear without any extra branching. So this is how you can rebase the branches in Getit. Let's check the status. Next, we can see we are three commits ahead.

[00:05:40] So let's push the changes. Since we have added the remote using the https method, it will ask for the user name and password. Let's add the credentials here and it's done. That's it for this demonstration on rebasing and get

[00:06:00] we'll continue exploring more advanced concepts in the next section. See you there. Thank you.