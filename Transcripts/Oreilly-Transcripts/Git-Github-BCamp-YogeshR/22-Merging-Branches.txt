[00:00:00] Hello and welcome back everyone in this lecture, we are going to learn about another important concept in git which is merging branches. Merging is an important feature in version control. Because in real world projects, it's common for multiple developers or even

[00:00:20] a single developer working on different features to work in separate branches. Eventually these independent changes need to be combined to ensure that the project stays up to date and coherent. This is where the process of merging comes into play. It allows to bring together different code changes,

[00:00:42] resolve conflicts and maintain a unified code base. Mastering merging is essential for collaboration, ensuring smooth integration of new features, bug fixes or improvements.

[00:01:00] Let's begin with the big picture. When we talk about merging branches, we mean taking the changes from one branch and combining them into another. Most commonly we merge a feature branch back into the main branch often called main or master.

[00:01:20] Now git actually supports a few different ways of doing this depending on the situation. The first one is fast forward, merge, a fast forward, merge is the simplest type of merge. Imagine you created a new branch called feature

[00:01:41] and you made a few commits there. Meanwhile, the main branch hasn't changed at all. No one else has added commits. In this case, merging is very straightforward. Git will simply move the pointer of the main branch forward

[00:02:00] to point to the latest commit of your feature branch. That's why it's called fast forward. Git just fast forwards, the branch pointer, no new commits are created and the project history remains clean and linear. The second type is three way merge.

[00:02:22] Now what happens if main has moved forward and your feature branch has also moved forward? In other words, both branches have new commits since they split. In this case, git can't just perform a fast forward merge

[00:02:40] because the two histories have diverged instead. Git performs a what's called a three way March. But why three way merge? Because git looks at three points in history, the common ancestor commit

[00:03:00] where the branches originally split the latest commit on the main branch and the latest commit on the feature branch using these three points. Git figures out how to combine the changes into a new comet. This new commit is called a merge commit.

[00:03:24] So let's understand what exactly is a merge commit. It's a special kind of commit that has two parents instead of one. That's Git's way of saying this commit was created by combining these two branches.

[00:03:45] It preserves the history of both branches and shows clearly where they joined. Now, sometimes when doing a three way merge git can automatically combine everything.

[00:04:00] But if both branches edited the same part of the same file, you will get a merge conflict. Don't worry, conflicts are normal and we will cover how to resolve them later in this course. Understanding these merge strategies is crucial because it

[00:04:20] helps you keep track of your project's history and avoid confusion when collaborating with others. That's it for this lecture. In the next lecture, we will take this a step further and demonstrate how merging works in practice, including what happens when there's a conflict.

[00:04:42] Thank you.