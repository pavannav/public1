[00:00:01] Hello, everyone. Welcome back in this demonstration, we are going to install and configure git on a Linux virtual machine. The configuring part is common for all the Os Windows, Mac and Linux. So you could skip the installation part if you already installed git in your machine.

[00:00:23] So I have already created a virtual machine on an awsec two instance. And here's the vm's IP, I will copy this IP and connect to the virtual machine using my terminal to connect. We will use the SSH command

[00:00:40] with the private key followed by the username, which is Ubuntu and the IP address. Now, here we are corrected as the root user. The first step is to ensure our system is up to date

[00:01:03] for that. We execute apt update and percent AM apt upgrade. Hyphen Y This will update all the package lists and install the latest security patches and package versions. The process may take a little time depending on your machine

[00:01:25] and once that's done, let's clear the screen again. Now let's change the hostname of this machine. For example, let's use hostname, CTL set, hyphen, hostname, thin Crook

[00:01:42] and then refresh the terminal using the bash command. This gives our server a more meaningful hostname. Usually git comes pre installed in most Linux distributions, especially in Aws, easy to run two instances.

[00:02:02] But let's check whether it's installed by running git hyphen hyphen version. As you can see git is already installed here with version 2430

[00:02:33] like pseudo app install git hyphen Y. So if we run this command, we can see that git is already the newest version message.

[00:02:46] Now let's try another command getty Aife in help. This displays usage information and a list of common git commands such as clone init

[00:03:00] add MV, restore RM, bisect diff commitlog branch and more. Don't worry about all these commands right now. We'll go through them one by one later in this course, let's clear the screen again.

[00:03:20] Before we start using git. There's one important step configuring our identity. Whenever we commit changes, get records who made those changes to do this? We need to configure a user name and an email address. The information gets stored in the commit history

[00:03:42] to check details about the config command. Let's execute. Get config hyphen hyphen help. You will see the documentation explaining how this command works currently. If we execute git config hyphen hyphen list, it shows nothing

[00:04:01] because we haven't configured anything yet. So let's configure our name and email globally using the hyphen hyphen global flag. This ensures the settings apply to all repositories for this user on this machine.

[00:04:23] First, let's set the user name, getit, config hyphen hyphen global user dot name and then specify the username. Next we set the email, get config hyphen hyphen global

[00:04:40] user email and then your email address. Now if we run git config hyphen hyphen list again, we can see the username and email are configured successfully. So that concludes this demonstration in the next lecture, we will move ahead with git architecture.

[00:05:03] Thank you.