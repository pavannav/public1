[00:00:01] Hello, everyone. And welcome back in this lecture. We are going to explore two very important commands and get that help us undo changes, get reset and get reward. By the end of this lecture, we will understand the difference between the two

[00:00:20] when to use each one and perform a step by step demonstration of how they work in practice. Let's get started. Imagine you're working on a project and you have made a few commits later, you realize something went wrong or maybe you just want

[00:00:40] to go back to an earlier point in time. That's where git reset and git revert come in. Both are about undoing changes, but they work in different ways. Let's begin with git reset. Git reset moves the current branch pointer

[00:01:01] backwards in time. Think of it like rewinding a movie. We are telling, get, forget about these recent comets. We want to go back to this earlier one. For example, if you have commits AB and C

[00:01:20] and you reset back to commit A, then commits B and C will no longer be part of your branch. But here's where it gets interesting. Git reset comes with different modes, soft reset mixed reset and hard reset.

[00:01:42] A soft reset only moves the branch pointer back but keeps all your changes staged, ready to commit again. This is useful if you want to rewrite history, but still keep your work. A mixed reset, which is the default, moves the pointer back

[00:02:02] and keeps your changes in your working directory, but unstaged. So the files are still there, but you need to add them again if you want to commit and finally a hard reset. This one is the most dangerous one.

[00:02:21] It not only moves the pointer back but also deletes all the changes in your staging area and working directory. It's like those commits never existed and the changes are gone. So be careful with hard reset. Now, let's talk about git, revert.

[00:02:43] Good. Revert works differently. Instead of rewinding history, it creates a brand new comet that undoes the changes of a previous comet. Let's say you committed something that broke your project. If you use revert,

[00:03:01] git doesn't delete that commit. Instead, it adds a new commit on top that cancels out those changes. This is much safer because history is preserved. Everyone on the team can still see what happened and nothing is lost.

[00:03:20] That's why revert is often preferred in collaborative projects while reset is more common when you're working on your own. So to quickly summarize git, reset, rewinds history and can even erase commits if you're not careful.

[00:03:40] But kit revert keeps history intact. But adds a new commit to undo changes now that we have understood the concepts. Let's see them in action first. Let's open our gitdemo repository.

[00:04:01] If we list the files, we'll see the items we created earlier. Now, if we run git log hyphen hyphen one line, we can see the comet history we built in the previous demonstrations. Let's clear the screen now to clearly demonstrate how git reset and git revert work.

[00:04:23] Let's create a new sequence of comets. We'll start by creating a new file called commits dot TXT. And inside it, we will add the text. This is commit. A

[00:04:40] now check the content of the file using cat. It's fine. Let's do git status and here we can see the file is not staged. Now let's stage and commit this file getit

[00:05:00] at commits dot TXT AM percent a percent git commit hyphen M commit A. Let's run git status. And here we can see the working tree is clean. Don't confuse with son.

[00:05:20] This is just a shorthand to run both the commands. If we check the log again, we can see that commit A has been recorded. Next, let's add two more comments. Let's append. This is commit B in the comets file.

[00:05:43] Let's check the content of the file. The content is appended now execute its status and we can see the file is modified. Let's stage and commit the change as commit be.

[00:06:03] Let's follow the same procedure with commit C let's append, this is commit C and the commit that as commit C if we check logs, we can see all the comets are present

[00:06:22] at this point. Our log shows three comets AB and C. Let's explore git reset. Suppose we want to move back from commit C to commit A. Each commit has a unique hash ID.

[00:06:42] So we'll copy the hash for commit A and execute git reset, hyphen hyphen hard hash of committee. The output shows head is now at some hash committee.

[00:07:00] If we check the log, notice that head is on commit A and commit B and C are gone. And if we open commits TXD, it only contains the line from commit A.

[00:07:20] This is what hyphen knife and hard does it moves the branch pointer back and removes the changes from your working directory and staging area. In other words, those commits are completely lost. But what if we don't want to lose the changes?

[00:07:41] Let's create commit B and C quickly, let's check these comets with git lock and we can see comets B and C are present. Now again, we want to go from commit C to commit A.

[00:08:00] But instead of hyphen knife and hard, we will run git reset, hyphen I and soft hash of committee. If we check the log commits B and C are no longer listed, but if we run get status,

[00:08:20] we can see the changes are still present and already staged. Opening commits dot Txt confirms that the contents from comets B and commit C are still there. So hyphen and soft removes the comets from the history but keeps the changes staged

[00:08:40] ready for a new comet. Now, let's see, the third option. Let's again, create a commit. But this time both commits B and C in the same comet. Let's do get log to recheck the comet's history.

[00:09:02] And here we can see commits B and C. Now this time, we don't want these changes to be staged like we did in hyphen hyphen soft option. Let's reset. Hyphen hyphen mixed option

[00:09:20] by default. Hyphen F and mixed is used if we don't provide any option. So let's execute, get reset. Hyphen hyphen next and hash of commit here, the comets are removed from history. But unlike hyphen F and soft, the changes are unstaged.

[00:09:44] If we check the content of commit's file, we can see all the lines present. Now, if we execute K status, we'll see we are in the master branch and the file is marked as modified but not staged.

[00:10:06] But when we used hyphen hyphen soft option with reset, the changes were in staged area. Let's clear the screen and commit the changes with the same message.

[00:10:22] Let's check the log, execute K status to check if anything is left, we can see the working tree is clean. So to summarize hard, reset, moves the pointer and deletes the changes completely

[00:10:43] soft, reset, moves the pointer but keeps the changes staged, mixed reset, moves the pointer and keeps the changes in the working directory. But unstaged. Now, let's look at git revert which is safer in collaborative workflows.

[00:11:03] Let's say kits B and C introduced a bug and we want to go back to the state of commit A. However, unlike reset, we don't want to rewrite history or lose comments. We just want to undo those changes while

[00:11:22] keeping the record of everything that has happened. This is where git revert comes in. Execute, git revert hash of commit B and C. This opens an editor where we can confirm the commit, message,

[00:11:43] add, revert, commit B and C done. Save it. If we check the log, we can see come B and C is still listed in the history, but a new commit has been added on top with message

[00:12:03] revert, commit B and C is done. The head pointer now points to this new comet. The history is preserved and the repository state is back to what it was in committee.

[00:12:24] This is the key difference. Git reset, rewrites history by moving the branch pointer. Git revert creates a new commit that cancels out the changes, keeping the history intact. And that's it for this demonstration.

[00:12:42] We'll see you in next.