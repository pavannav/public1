[00:00:02] Hello, everyone and welcome back in this demonstration. We are going to install git on a Windows machine. If you're working on mac Os or Linux machine, you can safely skip this demonstration and move to the next

[00:00:22] git is the foundation of everything we will be working with. So getting it set up properly is an important. First step. Don't worry. The process is simple and we will walk through it step by step first open your browser and head over to get

[00:00:42] hyphens cm.com. This is the official git website. When you scroll down, you will notice the option. Download for Windows. Go ahead and click on it. That takes you to the download page

[00:01:02] where you will see a link saying click here to download. If your system is Windows X 64 choose this option. If you are using arm 64 pick the other one. Since my system is X 64

[00:01:22] I'll click here. As you can see the download has started the file you will get is an executable file name, something like get hyphen 2.51 0.0

[00:01:40] X 64 hyphen. Once the download is complete, then double click the installer to launch it. This starts the gate for Windows setup wizard. The first screen is the license agreement. This simply tells you the terms of using git.

[00:02:03] So click next. Next, you will see the select destination location screen by default git installs in program files which is perfectly fine for most users. You can change it if you would like,

[00:02:22] but I recommend leaving it as is and clicking next. The next screen is select components by default. All the essential options are already selected. Git bash,

[00:02:40] get G I get LFs and so on. Git bash is the command-line tool you will use the most and get is for those who prefer a graphical interface. If you would like a desktop icon, you can check that box too

[00:03:01] for most people leaving the default works best. So let's click next. Now we are at the start menu folder screen. This just decides where the shortcuts will appear. The default option is fine.

[00:03:20] So let's click on next. Now comes an important screen, choosing the default editor for git by default git suggests whim which is a powerful editor but not very beginner friendly. Many people prefer to switch this to something

[00:03:40] like notepad plus plus or visual studio code. If they already have those installed for now, we will leave it as the default but just know you can customize this based on your preference. Then click next.

[00:04:00] Next, we can see the adjusting the name of the initial branch in new repositories. Git allows you to specify the default branch name when creating new repositories by default, it's set to master, but you could also choose main trunk or something else.

[00:04:25] Don't worry if this sounds new, we will learn about branches later in this course for now. Just understand that it's the name of the main line of development in your project. Next is the path environment setting.

[00:04:42] This option controls how kit will be available from the command-line. The recommended option is kit from the command-line and also from third party software. This makes git commands accessible from git bash,

[00:05:00] command prompt and powershell. It's the most flexible option. So let's keep it selected and click on next. The next screen is about choosing the SSH executable screen. This decides how git connects securely to remote repositories.

[00:05:25] The default option use bundled openssh, it is recommended and works well for most users. The other option use external openssh is for those who already have an SSH set up on their system,

[00:05:44] we will stick with the default use bundled openssh, select it and click next. Now you will see the screen for choosing the https transport backend. This is about how it communicates securely with remote repositories.

[00:06:05] The recommended option here is use the native windows secure channel library which works based on Windows and provides secure communication over the computer network. Let's keep that

[00:06:20] and click on next. Next up is configuring the line ending conversions. This is important when working across different operating systems like windows, Macos and Linux. The recommended option is check out windows style commit Unix style line endings

[00:06:44] which ensures compatibility between windows, Macos and Linux. We'll leave that selected and click next. Then we have configuring the terminal emulator,

[00:07:00] Gitbash needs a terminal to run inside. And the recommended option is use mintty which provides a nice and easy to use interface. But if you are comfortable with windows, CMD or console windows,

[00:07:20] you can go with the other option as well, but we will go with the default and move forward. The next option is choosing the default behavior of git pull this controls how git handles merging. When pulling updates,

[00:07:41] we will understand this deeply in the upcoming lectures. The default option is fast forward or merge which is the standard and works well for beginners. Let's go with this for now. Now we will see a few more advanced configuration options like

[00:08:02] choose credential helper. The recommended git credential manager securely stores your login details. So you don't have to re enter your username and password each time. Let's leave this enabled and click next

[00:08:21] on the next screen. You will see some extra options like file system caching and symbolic links. Both are enabled by default file system caching improves performance. So let's keep the defaults and click next.

[00:08:41] At this point, the installer is ready to begin installing git on your system, just click install and the process will start. It usually takes less than a minute when it's done, you will see the final screen that says the setup has finished.

[00:09:04] You will also see options to launch git bash or view the release notes. Let's leave the option to launch kit, batch checked and remove the check from the release notes. Then let's click finish.

[00:09:22] So here you can see the git patch terminal is launched, which means git is now installed on your Windows machine. Let's maximize the screen and increase the font size using control plus to confirm the git installation. Let's type git hyphen

[00:09:43] knife inversion in git bash terminal. Here you can see the git version which is 2.51 0.0 dot windows 0.1. Let's also run git command here. You can see all the commands of gate

[00:10:01] and that's it. We have successfully installed git in Windows in the next demo, we will configure it.