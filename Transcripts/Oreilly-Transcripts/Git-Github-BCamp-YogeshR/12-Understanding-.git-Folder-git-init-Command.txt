[00:00:00] Hello, everyone. And welcome back in the previous lecture, we understood the concepts of git folder and the git init command. Now let's perform a hands on demonstration to see both in action. First, let's create a new directory by executing the command

[00:00:20] mkdir getit hyphen demo. Now let's navigate into this directory. CD G hyphen demo. Then if we run LS hyphen L, you can see that nothing is present inside the directory. And next,

[00:00:40] when we execute with LS hyphen A which lists out all the files including the hidden files, you will notice that it's still empty at this point. The folder has no connection with get, it's just a regular directory. Now let's convert this folder into a git repository

[00:01:04] for that. We use the git init command git init here you will see hints in the response related to the branch and a message saying initialized empty git repository in slash root hyphen demo

[00:01:22] here slash root. Hyphen demo kit is the path of our current working directory. So let's understand what has just happened with the git command git, created a hidden folder called kit inside a project directory.

[00:01:43] If we run LS hyphen L again, you won't see it because dot git is hidden. But if we run LS hyphen A, you will see the kit folder. Now let's explore the dot kit folder. Let's clear the screen and move inside the dot git folder.

[00:02:03] CD dot getit, Alice. Here, you can see several files and directories that we discussed in previous lectures such as head branches, config description, hooks, info, objects

[00:02:20] and refs. So your head points to the current branch you are working on. We will discuss this in more detail when we reach to branch concept. In this course. Next is config stores repository specific configurations like username, email,

[00:02:40] default print and more objects store the contents of your files in a compressed form, each identified by a unique hash refs, store pointers to commits branches and tags.

[00:03:00] Next is hooks contain scripts that can trigger on certain git events. For example, before committing changes, description info, branches are other supporting files for repository metadata. So this is git folder.

[00:03:20] Basically the brain of K it stores everything it needs like your commit history, configurations and metadata. If you delete this folder, your project becomes just a normal folder again with no version control.

[00:03:40] Currently. If we run the tree command inside kit, you will notice that most directories are empty except hooks because we haven't committed anything yet. But as we start adding, committing and updating files, git will store all that information here

[00:04:02] to summarize. Getent is the command that turns a normal directory into a git repository. The dot gate folder stores all the commit history configurations and metadata. Without the dot gate folder, there is no git repository

[00:04:20] to summarize git in it is the command that turns a normal folder into a git repository. The kit folder stores all the commit history configurations and metadata. Without the kit folder, there is no git repository. That's it for this demonstration. See you in the next section.