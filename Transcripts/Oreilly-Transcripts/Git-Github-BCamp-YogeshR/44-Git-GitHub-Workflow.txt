[00:00:00] Hello, everyone. Welcome back. So far, we have explored several git and github concepts through our lectures and hands on demonstrations. In this lecture, we will connect all the concepts we have learned so far and understand how git and github work together in a complete end to end process.

[00:00:24] We will walk through each stage to see how a code moves from local development to being shared on github. So let's get started. Git organizes the flow into four main areas, the working directory,

[00:00:41] the staging area, the local repository and the remote repository. Each stage represents where our changes exist. At a particular moment. We begin by either initializing or cloning a repository. If we are starting a new project from scratch,

[00:01:02] we use the command git in it. This tells git to start tracking the current project folder. But if we are working on an existing project that's already on github, we use git clone followed by the repository link. This brings the entire project along with its full history onto your local machine.

[00:01:26] Once the setup is complete, we start working in the working directory. The working directory is simply a workspace where we create modify and delete files, git notices these changes,

[00:01:41] but it doesn't automatically track them to tell it which files we want to keep track of. We add them to the staging area. This is done using commands like git add file to add a specific file or get a dot

[00:02:00] To add all the files we have modified in the current working directory. We can also rename files using git MV or remove them using git RM. And if we have made some changes that we no longer want to keep, we can discard them and bring the file back

[00:02:21] to its last committed state using git restore file. At this point, those files move from the working directory to the staging area. The staging area is like a preparation zone where we gather all the changes we plan to save

[00:02:42] once everything looks ready, we record those changes permanently in the repository with the command git commit, hyphen M your message. This creates a snapshot of the project at that point in time. If we want to skip the staging step

[00:03:01] and directly commit all modified files, we use git commit hyphen A hyphen M your message. If we accidentally add something to the staging, we can easily unstage it with git reset file which sends it back to the working directory.

[00:03:24] Once committed the changes, move into the local repository. The local repository is where it stores the full history of a project on a system. Every commit made is saved here creating a complete timeline of our work.

[00:03:41] If something goes wrong or we want to go back to a previous version, we can use git reset commit to roll back to review differences between versions. We can run, get deaf to see changes since the last commit or get deaf head

[00:04:01] to compare our current work with the most recent snapshot. After our local changes are ready, it's time to share them with others through the remote repository. The remote repository is hosted on github. It's a shared version of a project where we and our team can collaborate

[00:04:25] to upload our commits to github. We use git push this, sends a local commits to the remote repository. If someone else has updates to the project on github, we can bring those changes into your local repository using git

[00:04:42] fetch to download them or GPU to download and merge them automatically into our working directory. And that's the complete workflow. We start by initializing or cloning a repository, make changes in our working directory.

[00:05:01] Stage them with git ad, save them in the local repository with git commit and finally push them to the remote repository using git, push every git command simply moves our work from one stage to the next, helping us track, manage

[00:05:20] and share our code safely and efficiently. That's how git and github together create a smooth and reliable workflow for version control and collaboration. In the next demonstration, we will see this workflow in action see you there.