[00:00:00] I hope you have enjoyed this video so far. There are two more topics in this part I would like to cover, starting from the concept of reusing workflows. And we will touch on environments later. Rather than copy-pasting from one workflow to another, you can make a workflow reusable. You and anyone with access to the reusable workflow,

[00:00:20] can then call the reusable workflow from another workflow. Reusing them avoids duplication. This makes workflows easier to maintain and allows you to create new workflows very quickly by building on the work of others. Reusing workflows also promotes best practices by helping you to use workflows that are well designed,

[00:00:41] have already been tested, and have been proved to be effective. Your organization can build up a library of reusable workflows that can be centrally maintained. A workflow that uses another workflow is referred to as the caller. The reusable workflow is the call workflow. One caller workflow can use multiple called workflows.

[00:01:03] Each call workflow is referred to in a single line. A reusable workflow can be used by another workflow if either of the following is true-- either both workflows are in the same repository, or the call workflow is stored in a public repository.

[00:01:21] Here is a sample code snippet. This workload is comprised of a single job running two different tasks. Task one is a call workflow which has a reference to a reusable workflow called workflow-A. Task two is also a call workflow which relies on a reusable workflow called workflow-B.

[00:01:41] But this time, it actually passes data into that. Workflow A and B could also be used as a standalone workflows, which nicely shows the use case we're reusing. Otherwise, this new workflow will be an almost identical copy of both A and B, having duplicate code which makes it even harder to manage and maintain.

[00:02:01] The last topic in this video covers the definition of environments. Environments are used to describe a general deployment target like production, staging, or development. When a GitHub action workflow deploys to an environment, the environment is displayed on the main page of its repository. You can configure environments with protection rules, secrets,

[00:02:23] and branch protection. When a workflow job references an environment, the job won't start until all of the environment's protection rules are passed. A job also can't access secrets that are defined in an environment until all the environment protection rules pass as well. Note, you can only configure environments

[00:02:41] for public repositories. If you convert a repository from public to private, any configured protection rule for environment secrets will be ignored, and you will not be able to configure any environments. The only way to use environments in a private repository is to purchase the GitHub Enterprise license.

[00:03:00] Let's have a look at this code snippet. You probably are very familiar with this. I'm specifying a new job which runs some sort of a deploy task. This is similar to all of the examples I've used before. Once you have your environment configured in GitHub, only thing you need to do is adding the keyword "environment" with the name of the environment you have specified.

[00:03:20] And that's it. You could also target multiple environments for specific jobs or tasks within the same GitHub action workflow. While environments are very powerful and useful, there is not too much to talk about. So let's look at this in action. All right, I'm in a GitHub repository.

[00:03:40] And I will navigate to Settings menu. From there, I'll select the Environment. And here you can see that there's no environment for this repository yet. So we're going to click on New environment. Enter a new name. Let's call this a Dev environment. And we configure the environment. First thing first, you can see that there are some environment

[00:04:00] protection rules. One of them is Required reviewers. The other one is Wait timer. The required reviewers is really good if you wanted to allow somebody to approve the workflow run before it is being run. And the wait timer just adds some timing before the deployment goes to proceeding. You can also specify which branches can be used for this environment.

[00:04:20] You can add environment secrets that we've reviewed for. And you can also add environment variable. We're going to review this later. Once you have got your environment set up, you can then go ahead and use that environment within your workflow as we've shown before.