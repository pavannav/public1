[00:00:00] Welcome to this next video in the Introduction to GitHub Actions series. After positioning GitHub Actions and clarifying YAML as its core syntax, let's make it a bit more practical from here on and walk you through several hands-on examples. In this video, you will learn how to build your first GitHub Actions workflow template, which can be used by you

[00:00:21] or anyone in your organization. Before switching to the actual creation, it is crucial to have a really good understanding of the core concepts of GitHub Actions workflow. Some have already been touched on briefly in previous videos, but I think it's a good idea to walk you through this a little bit more in depth.

[00:00:41] No matter how easy or complex your GitHub Actions workflow will be, it will somehow always have the following six components in there. Actions: Actions are the smallest portable building block of a workflow and can be combined as the steps to create a job. You can create your own actions or use publicly shared actions

[00:01:03] from the marketplace. Event: Events are specific activities which can trigger a workflow run. For example, a workflow is triggered when somebody pushes some changes to the repository or when a pull request is created.

[00:01:20] Events can also be configured to listen to external events using webhooks. Runner: A runner is a machine with the GitHub Actions runner application installed. The runner waits for available jobs. It can then execute. After picking up a job, they can run the job's actions

[00:01:40] and report the progress and results back to the GitHub. Runners can be hosted on GitHub or self-hosted on your own machines or servers. Job: The job is made of multiple steps, and runs in an instance of the virtual environment. Jobs can run independently of each other or sequential

[00:02:02] if the current job depends on the previous job to be successful. Step: A step is a set of tasks that can be executed by a job. Steps can run commands or actions. Workflow: Workflow is an automated process

[00:02:20] that is made up of one or multiple jobs and can be triggered by an event. Workflows are defined using a YAML file in the "github/workflows" directory. While it would be totally fine to create your GitHub Actions workflow from scratch using the core components I just talked about, know that GitHub offers the thousands of already

[00:02:42] available workflow templates. Think of templates as predefined workflow collections having the necessary jobs and steps to succeed. For example, if you want to interact with Docker containers, there are several templates available to build a Docker container, run all the tests,

[00:03:00] and then publish the Docker container to the Kubernetes clusters or more. Templates are provided as open source from the GitHub Actions marketplace or can be created as custom workflows by yourself. Most probably in real life, you will come up with a combination of both, starting from a generic template and adding

[00:03:21] some organization-specific tasks into it. Another nice benefit is that GitHub is relying on AI and machine learning, which is capable of making recommendations of what workflow templates to use based on the information it finds in your repositories. For example, if it detects Node.js application

[00:03:40] in your repository, it will offer a set of templates that work with Node.js specifically. Here's a screenshot of what the workflow template looks like for a Node.js application. With that, we are ready to build our first workflow template. As I want to focus on the actual workflow creation,

[00:04:00] I'm not going to spend too much time on the other files in the repository. If you want, you can go through this example yourself. Feel free to create a clone of the repository at this URL. Building this from scratch involves the following steps. Create a new repository, and give it a name, such as Hello World GitHub Workflow Template.

[00:04:21] Create a README file to share some information about the repository content. Create a folder called action-a, or any name you want, which will store some of the source code of our application as well as the actual action parameters. There will be a Dockerfile, which

[00:04:40] allows you to compile the Docker container's application. There will be an "action.yml" file, which has the actual action parameters, and the entrypoint.sh, which is a Bash script to provide instructions to the Docker container's workload. You also need to create a "github/workflows".

[00:05:01] And put a YAML file in there to call that action. Besides the Docker and the entry point file, which are examples of a containerized workload, let's focus on the two prime source files allowing us to run a workflow. These are action and main.yml files. The action.yml is stored in the subfolder by default

[00:05:21] and contains settings, parameters, variables that will be used by the actual GitHub Action workflow. Looking at the sample script, it contains a name and a description, and input parameters, some run parameters, and some branding information. The actual GitHub workflow is stored in the main.yml, which is saved under the "github/workflows"

[00:05:44] folder by design. This is the one and only location where GitHub Action workflows can be stored. Looking inside the workflow definition file, we detect some familiar sections. We first write the name for this workflow. Then define the trigger point using the on keyword, which

[00:06:00] means that the workflow gets triggered whenever we initiate a change in the source code. Once triggered, the jobs section kicks off, presenting a sequence of tasks, one of which is to call the action which is created and passing the parameter to it. Assuming most is already clear from the descriptions, I think it's becoming even more clear

[00:06:21] when we see this in action. So we are in our repository on GitHub. I'm going to open up the README file. And I will apply some changes and save those changes to our repository. Now, I can commit my changes. And as soon as the commit happens, we should be able to head back to the Actions tab and have a look at the current action run.

[00:06:41] Within that action run, you will be able to see all the steps that are running. And when the action is running and the run is finished, you should see the message that we printed out in our console.