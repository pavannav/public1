[00:00:06] - In this video, I'd like to tell you why compiling kernels isn't necessary anymore. So when Linux was first released in 1992, it used monolithic kernels. In a monolithic kernel,

[00:00:20] all the functionality you are ever going to need is compiled into the kernel. And that is kind of annoying because as a result, in order to add features to the kernel, it needed to be recompiled every single time for every single change. So you want to add a driver? Recompile your kernel. You want to enable a specific option? Recompile your kernel.

[00:00:42] Fortunately, that is no longer needed. With the Linux 2.0 release in June, 1996, the modular kernel was introduced. And since we have a modular kernel to add functionality to the kernel, drivers could simply be added and loaded. First with the insmod command

[00:01:00] and later with the modprobe command. In early modular kernels, you needed to do that manually. In modern kernels, drivers are loaded automatically. That's a topic that we will discuss in much more detail in lesson five in this course.