[00:00:06] - [Instructor] In this video, I'll tell you about 'btrfs'. So 'btrfs' is the B-tree filesystem. It offers advanced features such as volumes and Copy on Write, or CoW.

[00:00:20] And because butter comes from a cow, according to the main developer, 'btrfs' should be pronounced as, 'btrfs', not B-T-R-F-S or anything, 'btrfs'. So, 'btrfs' is the default file system on Fedora Linux among Solaris. On older distributions, ZFS and Stratis and LVM

[00:00:41] are providing features that are in 'btrfs' as well. One of these features is subvolume. Subvolume means that directories can be provided with properties that normally are set on storage devices. And that can be convenient. Now in 'btrfs', there's a couple of commands like, 'btrfs file system show', which will show current

[00:01:03] 'btrfs' file systems. Or 'btrfs filesystem label', which will show 'btrfs' labels, 'btrfs sub volume list' and 'subvolume create', allows you to list and create subvolumes. And you can even create a 'btrfs' based user home directory

[00:01:22] while using 'useradds' by adding the option 'btrfs-subvolume-home'. That will make that every single user has their own subvolume and that adds protection to some level. Now let's talk about snapshots, 'btrfs' comes it embedded snapshot functionality.

[00:01:42] The snapshot allows users to freeze the current state of files. It's not much different from what you have seen in LVM snapshot in the last lesson. So making snapshot is useful before performing tests, if desired it is easy to revert to the previous state. Just imagine, you test something, you figure out

[00:02:02] that you mess up all of your files, you just restore your snapshot and now you're back to the original state. Snapshots are created on subvolumes, using commands like 'btrfs subvolume snapshot/files/files snapshots'. And 'btrfs subvolume list', is a useful command in that

[00:02:23] which allows you to show subvolume IDs. And based on that, you can use a command like 'btrfs subvolume set-default 258/files', that allows you to set a snapshot ID as a new mount point for a directory.

[00:02:44] Okay, for this demo we are on Fedora for the simple reason that Fedora is using 'btrfs'. Let me show you. So here we have the mounts on Fedora and do you see what an interesting mix that is? The root volume is on 'btrfs',

[00:03:00] let's call the home volume is on 'btrfs' as well. Now we have boot on 'ext4', that's because, 'btrfs' is a bit complex and for that reason it's easier to boot from 'ext4' so that you don't have to rework your 'init ram fs', with its big 'btrfs' filesystem. And we have the 'efi' on 'vfat'.

[00:03:21] I don't know what you think about it, but three different filesystems on the default installation, I kind of have mixed feelings about it to be honest. But before we continue we need 'lsblk', as we need a block device. And the block device that I'm going to use is 'nvme0n1'.

[00:03:40] So on this system we have an 'sda' and an 'nvme0n1'. That doesn't make a real difference. We can use 'nvme0n1' instead of the 'sdb' that you can see on the slide. So I'm going to start using 'mkfs.btrfs' on dev 'nvme0n1'.

[00:04:03] And here we can see a dump of the metadata and most important number of devices is one and, there we go. One device 20 gigabytes in total. So let me create a mount point. And use 'mount -t btrfs',

[00:04:24] on dev 'nvme0n1/mnt/butter'. The 'mount -t', the option -t strictly should not be necessary, because the -t instructs mount to use the 'btrfs' driver,

[00:04:40] as this is a 'btrfs' system, we should be okay. But if you are using this on a system that doesn't have the 'btrfs' kernel module loaded yet, then the 'mount -t' might be very convenient so that the mount knows that it needs to involve the appropriate kernel module. Next, let me use 'btrfs subvolume'

[00:05:01] show on 'mount butter'. Where we can see the subvolume information. Wow, that's a lot of information. So I am going to create a new subvolume, 'btrfs subvolume'. Do you see by the way, how awesome the tap completion is?

[00:05:21] So, I'm going to use create again of '/mnt/butter/milk'. And in case you think, hey, you never created a directory 'milk', that's correct, 'btrfs subvolume create' is going to do that for me. So, now I can copy my 'etc hosts' to 'mnt butter milk',

[00:05:45] and that makes that we have one file on the subvolume. Wow. Now let's use 'btrfs subvolume snapshot'. I want to have a snapshot of 'mnt butter milk'

[00:06:00] to 'mnt butter milk snapshot'. And that shows that we now have a snapshot within the subdirectory. So now we can do what? Well I can use 'rm' on 'mnt butter milk hosts',

[00:06:21] and then I don't have the file anymore. Let's do a quick 'btrfs subvolume', list of 'mnt butter milk'. And there we can see the subvolumes including the subvolumes IDs.

[00:06:40] So we have the 'milk' using ID 256 and we have the 'snapshot' using 257. So I need these IDs in 'btrfs subvolume set-default 257' on '/mnt/butter/milk'.

[00:07:03] All right, so now I'm going to use 'umount' on 'dev nvme0n1' and 'mount dev nvme0n1' on 'mnt butter'. And then I'm using 'tree' on 'mnt butter',

[00:07:23] which is showing that the host file is back. So what did we just do? Well, we made a snapshot. We have set the snapshot as the default. In order to make that effective, we needed to unmount the 'btrfs' device, mount the 'btrfs' device again, so that the btrfs device is looking at the snapshot.

[00:07:43] And that concludes this quick introduction to 'btrfs' and 'snapshot' use.