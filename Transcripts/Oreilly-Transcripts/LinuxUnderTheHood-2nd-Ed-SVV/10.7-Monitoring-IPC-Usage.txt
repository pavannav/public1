[00:00:06] - OK so we talked about IPC a bit You can monitor them as well. So how does that work? Well there is the IPC comment. Which is a bit of an old comment to give you an overview of system five IPC's.

[00:00:21] But it's limited because not every process is using system 5IPCs ipcs -l will show current limits on the system five ipcs. You can also use LSOF minus P or the process ID. That will show all the open files and if your open file

[00:00:41] is an IPC it will list the IPC type. That's actually quite convenient. The alternative would be to see the same from /pro/PIDFD with lsof -p is giving a pretty decent interpretation. Then the other way to see ipcs is by using strace -P on the PID.

[00:01:01] So that attaches an strace to currently running process and it'll show you the different ipcs. You will see sockets and connect and bind if it's socket oriented you'll see shmget and shmat for shared memory access

[00:01:21] or get a msget and msgrcv and msgsnt for message queues. Let me show you. So first ipcs is showing what? Well showing that there's not much system five ipcs around on the system. Let's find the process ID of SSHD

[00:01:40] and I don't have an SSHD that's interesting. So system CTL start on SSHD. And now I can use LSOF -P PID of SSHD. And what do we see? We see all the open Files. And if it is an IPC we will see that as well.

[00:02:03] Now it happens to be that we don't have very many ipcs here but we have some unit sockets to the end. So the unit socket is the sockets that you will also find all TCP, which is the port basically. A port connection type-A stream and type-A

[00:02:20] datagram which stream with is connection oriented and datagram is UDP connectionless. So if you do this on the process like Firefox. Let me check if you have any Firefox running. Yeah 5008. There we should see a little bit more. LSOF -p on 5008 and there we have

[00:02:43] a lot of ipcs as well. Where you can see unique sockets. You can see how many unique sockets in fact. And what else do we see? You can see some FIFAs for instance that's pipes and scroll down and you will find

[00:03:02] all the ipcs nicely listed here using LSOF. Now a little bit more in-depth would be to use strace. Strace -p on any process ID like 5008. And that will print information and in the information

[00:03:20] you can see the actual system calls that are going on. And the system calls are providing information about the ipcs. But there is something that we can really only understand once we have investigated system calls, which will happen in the next lesson.