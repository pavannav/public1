[00:00:06] - So you now know about cgroups and about namespaces, two of the most essential building blocks for running containers. Now, how do we get from these Linux technologies to actual containers? Let's start by defining what a container is.

[00:00:23] So a container is a standard unit of software that packages code and all of its dependencies so that the application runs quickly and reliably from one computing environment to another computing environment. I took the liberty of copying the definition of a container from docker.com

[00:00:40] because after all, Docker started the container revolution. And containers offer all that is needed to run applications in the aforementioned way. Containers leverage Linux kernel namespaces and cgroups to run containerized applications in a secure way. Now, we can manually combine everything to run a container,

[00:01:04] but doing so is slightly cumbersome. All files that are needed in the mount namespace need to be copied over, and it's easy to forget something while doing so. And cgroup restrictions need to be set, namespace restrictions need also to be set,

[00:01:20] and that is why we have containers. Containers are actually making the process easier. And one of the important benefits of using containers is that while using containers, default images can be provided. And if you look at the Docker solution and also Podman containers, standard images may be provided

[00:01:41] through container registries. So that means that you don't have to manually copy anything in a tree rooted directory anymore. You just fetch an image, and the image is a table that contains all the dependencies that your application is going to need. Then there is also the container run time,

[00:02:00] and the container run time provides an easy interface to set resource limits and namespaces. So this run time is actually providing an interface that allows you to specify all of the cgroup and namespace information on the command line. Now, Docker has really made containers a success by combining cgroups and namespaces

[00:02:21] and adding the Docker registry for efficient image delivery. In this class, you are not going to learn about Docker. The mission here is to learn about the Linux technologies that have resulted in containers as we know them nowadays.