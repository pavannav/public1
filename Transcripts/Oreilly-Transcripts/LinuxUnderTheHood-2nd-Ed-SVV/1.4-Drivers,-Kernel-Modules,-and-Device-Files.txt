[00:00:06] - In this video, we are going to explore the relation between drivers, kernel modules and device valves. So hardware support is offered by drivers. In Linux, the drivers are presented as kernel modules and they are loaded on demand in most cases.

[00:00:22] You'll learn much more about that in lesson five in this course. Most device nodes are represented by files in the /dev directory, and they make that in user space. There's an interface to the drivers because that's a problem here. Drivers operate in kernel space. Users cannot access kernel space,

[00:00:41] and that's why the device nodes are coming in. Now, many devices only send bytes to a peripheral on the computer or receive bytes from the peripheral. Such devices work like pipes, and for that reason, they work well as character devices because it's basically a stream of character

[00:01:01] that you send to the device. All the devices work more like files. What you write to a specific location can later be retrieved from the exact same location. And these devices are represented by block devices. Network devices are a bit more complex as they work with packets instead of streams of bytes.

[00:01:22] And these devices are controlled by the IOCTL system call, which permits for more advanced operations and for that reason, doesn't work well with either block or character devices. It's a category on its own. That's also why you won't find any network devices in the /dev directory.

[00:01:40] Another special case is the video adapter. They also don't have device nodes, and that is because the kernel writes directly to the memory of the video adapter as this is faster. Video adapter is such an essential part of the operating system that we need the fastest kind of access that is possible. Now let me visualize how kernel modules

[00:02:01] and device nodes are working together. All right, let me give you an overview of how these device nodes and kernel modules work together. Essential again, this is difference between user space and kernel space, which by the way is also referred

[00:02:20] to as system space. That's the same. So in kernel space we have the kernel, and in user space we have the application and the application wants to write a file. So how does that work? Well, in the end, if you want to write a file, you end up to a device like /dev/sda1

[00:02:46] because files are written in some directory and these directories are stored on block devices. Now the device sda1 has what we call a major and a minor. The major number eight is information for the kernel.

[00:03:02] And that is what we need to address the kernel. So based on the major number eight, the user can make the appropriate system calls to get to the kernel. But hey, the kernel doesn't have the drivers included. So the kernel needs to connect to the appropriate drivers.

[00:03:20] And it's doing that based on the eight. The major eight is mapped to a driver for the SD device. And based on that, the kernel knows how to get to the appropriate device and then it will tell this device, hey device, I have a request for you on minor number zero.

[00:03:44] By the way, that's not entirely true. It's not eight zero, the minor is eight one. So the kernel will next talk to minor number one, and minor number one, that is resolved as what we know as sda1.

[00:04:02] Kernel doesn't know about sda1. Sda1 is user space representation, but the key to this entire story is that the kernel is addressable based on the major. And next, the more specific information that is passed to the driver, that's the minor information.

[00:04:20] And that is how the device nodes in the dev directory make a connection to the appropriate kernel drivers.