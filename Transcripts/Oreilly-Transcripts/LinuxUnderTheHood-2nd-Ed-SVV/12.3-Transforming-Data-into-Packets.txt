[00:00:06] - Let's talk a bit more about how data is transformed into packets. So applications generate data, and to send this data over the network, bits are needed, and different protocols define how.

[00:00:20] SSH, for instance as a protocol example, is used at the process application layer and it generates an SSH payload. And this payload is sent to TCP. TCP ensures reliable and guaranteed delivery. The TCP datagram is next sent to the IP layer, which takes care of proper addressing.

[00:00:41] And the IP layer sends the data to ethernet in local networks, which packages into frames that are sent to the appropriate MAC address. And then at the other site, the receiver is going to receive at the IP layer, reach the packet header, finds the IP data, passes it up to TCP stack until it can be read by SSH.

[00:01:03] Let me make a drawing to show you how this process is working. All right, so what are the ingredients in networking? Well, first there is the network. Let this be the network, ethernet network with a good old terminator.

[00:01:20] That's what we used back in the days to mark, physically mark the end of the ethernet network. Now let's say we have an SSH client, and this SSH client needs to communicate to SSHD. Then what exactly is going to happen?

[00:01:41] Well, first the SSH client is generating its payload. This payload is the data that an SSH client wants to send to the SSHD process that is next passed up to the TCP layer. So TCP is adding its header to it,

[00:02:03] and in that header, for instance, TCP is adding ports. And that is followed by the payload, the SSH payload. Now at the TCP layer, the TCP protocol doesn't care about the SSH payload at all. It's like the mailman.

[00:02:20] The mailman doesn't care about what you put in your letter. Next, the entire TCP part is sent to the IP layer. And the IP layer is adding a header as well with the destination address, with the source address,

[00:02:40] and everything else that is coming from the TCP layer is considered to be a payload once again. (marker pen squeaks) So once again, once the data is received in the IP layer, IP doesn't care about what TCP has been doing. It's just payload, it's generic information that needs to be sent to the other site.

[00:03:02] Then this is sent to the ethernet layer where we have a destination MAC address and a source MAC address. And you guess what is going to happen next. Payload is what we call it. So the process repeats itself, and in the end, it can be moved forward.

[00:03:24] And what is going to happen next? Well, from the ethernet layer, we are going to send zeros and ones on the network because in binary computing, it's all about zeroes and ones. Then the zeroes and ones are received at the other site,

[00:03:43] and the other site needs to do something with it. Well, here, the process is going to happen in the reverse order, and in the other site, we receive the the ethernet package with the destination address, with the source address, and with the payload.

[00:04:01] So at the ethernet layer, it is going to read the destination and source and do something with it to represent the payload at the IP layer. And at the IP layer, again, we have the destination address, the source address, and the payload. So based on the destination address and the source address,

[00:04:21] the IP layer can do whatever it needs to do, and it can pass the information up to the TCP layer. So what do we find? Well, we find a destination port and a source port and the payload as well. And then finally, the payload can be sent

[00:04:40] to the SSHD process. And that is how peer layers in networking actually are communicating to one another. And the fun is that all the peer layers are not really aware of anything that is happening below the current layer or above the current layer. Oh, and just for your information

[00:05:00] to keep it a little bit simple, I have just inserted the destination addresses and the source addresses and MAC addresses and ports, and not all the other information that you can have in a network packet header.