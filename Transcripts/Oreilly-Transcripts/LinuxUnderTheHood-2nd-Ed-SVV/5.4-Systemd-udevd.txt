[00:00:06] - A very important part in hardware handling on modern Linux is systemd-udevd. So systemd-udevd can be considered the plug and play manager on Linux. In fact, even before systemd-udevd did exist,

[00:00:21] systemd has just adopted and integrated it. Now when hardware events are detected, udevd processes rules to initialize devices. And these rules determine what exactly is going to happen with your devices. To learn what is happening, you can follow the process using udevadm monitor.

[00:00:43] The Linux kernel initiates device loading and then sends out uevents to the systemd-udevd user space daemon. systemd-udevd catches the event and decides how to handle it based on attributes that it has received in the event.

[00:01:00] And next, it reads its rules and acts based on these rules. There's default rules in usr/lib/udev/rules.d. You can also create custom rules in etc/udev/rules.d and the results can be monitored through the sys filesystem. Now let's have a look at what udevd is doing when you plug in a USB thumb drive for instance.

[00:01:24] So I need to start with udevadm monitor. I need to have it listen and then I can plug in the USB thumb drive. And there you can see, this is the initialization of the USB thumb drive.

[00:01:41] Now let's check out what is happening. I'm picking out a couple of lines. So here we have the kernel line and the kernel is starting by recognizing that something has been happening. A USB device and kernel passes the information over to udev. What you see right here is filenames.

[00:02:00] These are all filenames in the sys sudo file system. So the kernel detects the device, udev is initializing it, and once udev has initialized parts, the kernel can move forward. And the result, for instance, is right here. The USB storage kernel module is used.

[00:02:21] So the kernel module, we can see that right here, USB storage. And once USB storage has been used, the device detection can further proceed. And if you go here step by step, you can see exactly what has happened in this device. Here, for instance, you can see that the kernel is now far enough to initialize the block device, the SDF.

[00:02:44] So that means that we have a device node at this point. And once SDF is here, it can detect the partitions on SDF. So SDF1, we got it right here. That information is passed to udev and udev is making sure that everything is written to the sys filesystem and initialized the proper way.

[00:03:03] And now look at that. We apparently also need exfat. At this point, the kernel is far enough to detect what type of filesystem is used on the device. So exfat, and we can see right here that the exfat module is loaded. udevadm monitor is an amazing tool to analyze in depth

[00:03:23] what is going on when hardware is plugged in or when it's plugged out. There you go, you can see the unbind and the remove that is happening when I remove the USB thumb drive. But there is more, you can tweak the udev rules and that's what we are going to do in the next video.