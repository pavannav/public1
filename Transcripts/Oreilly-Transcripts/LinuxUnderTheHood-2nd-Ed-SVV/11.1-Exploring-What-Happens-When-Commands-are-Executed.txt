[00:00:06] - All right, so let's try to understand what happens when a command is executed. Actually, when a command is executed behind the scenes, different things need to happen. First, a command needs access to specific system functions.

[00:00:22] That's the connection between user space functionality and kernel space functionality. And that allows the command to do whatever it needs to do Next, the command needs to create its own execution environment. Here, it needs access to environment variables and other stuff that is required for the command to do what it needs to do.

[00:00:42] Obviously, it needs to load in memory, and it requests access to system resources like files. Then it gets specific feature access as well through libraries, and it needs to load library functions for common functionality. And all of these are provided

[00:01:00] by different Linux system components. And it's the system components that we are trying to understand in this lesson. So system calls provide access to key Linux functions. We have already seen system calls a little bit, and we've already talked about fork and exec ve which are used to create the execution environment.

[00:01:22] Next, virtual memory is allocated to run the command and parts are loaded in a resonant memory when they are needed. The command is executed in system space or in user space. And when executed in user space, permissions to access system calls are checked.

[00:01:41] These permissions are in the form of capabilities and the command next places pointers to required libraries in memory. And from the program code, specific library calls are issued as well. Now, in a learning commands, there is an important kernel component that plays a role and that is task struct.

[00:02:03] So from the process point of view, the process is the only thing that is running, and it has access to all memory and CPU. But again, that is from the process point of view, and that doesn't represent the reality. Because of this process point of view, documentation sometimes also talks

[00:02:21] about the Linux virtual machine model. Honestly, I don't like it very much if you call it that way because it is confusing and many people would think about hypervisor-based virtualization. And this Linux virtual machine model has nothing to do with hypervisor-based virtualization.

[00:02:42] Now, the kernel maintains this illusion of being the only one on the world in the task struct of the Linux kernel process table. So what is task struct? Well, task struct is the backbone of the Linux kernel management system. And the task struct includes various fields

[00:03:00] that hold process-related information and that shows in the different subdirectories in the /proc/PID. This also makes /proc/PID probably, the best way to find information from the task struct. Task struct is kernel internal, and you don't have a better way to get insight in it. It's not like a table that you can print.

[00:03:23] Now, when the scheduler switches the CPU from one process to another, that's a process that's known as context switching. It saves the current state of the CPU in its task struct and loads the task struct of the new process. And that's quite a bit of work and for that reason,

[00:03:40] you should try to avoid context switching. Let's have a look at the task struct information that you can find in Proc PID. So in proc, we have all these PID directories. Let's pick out more or less, random one, 5,008, which is what? Well, CMD line is showing that this is Firefox.

[00:04:01] So in this directory, we have this entire structure that contains the complete information about what the process is doing. And one of the nicest ones is status. In status, you can see the current status. So this is giving information about memory that the process is using, about parent process ID,

[00:04:22] about the number of context switches that the process has been doing so far. So all of this is quite interesting. So this proc PID status is probably the best interface to find out what is going on in the task struct kernel table.