[00:00:06] - All right. At the end of this lesson, I would like to show you some cool file system related tools. On the slide, you see the overview. Let's go check them out. So to start with, I need to install the bcc-tools package.

[00:00:27] In this bcc-tools, you will find some pretty cool utilities that I'd like to have a look at. Okay, so these tools are in the /usr/share/bcc/tools directory.

[00:00:43] And as you can see, there's a lot of them. I can really recommend having a look at most of these. There are some pretty interesting stuff that you can find, but before you can do it, use echo export PATH is $PATH

[00:01:02] and add /usr/share/bbc/tools to it. Do that in the tilde .bashrc and source tilde .bashrc.

[00:01:22] And then we can use stuff like tcptop for instance. Does that have anything to do with file system operations? No, but tcptop is giving information about TCP usage. So you can see a top-like interface that displays same kind of information

[00:01:40] that you will find in the uptime command. But hey, we should be looking at vfsstat. vfsstat is showing information about the virtual file system. So that's the VFS, the virtual file system, as a generic file system instead of showing specifics about your EXD

[00:02:01] or your XFS or whatever. Or how about filetop? filetop will show which files are used most actively. So here you can see the names of all of them and you can see the number of reads and the number of writes of all of them. Obviously, a file like libc.so.6,

[00:02:21] which is your C library, should always have a place because that plays a role in almost every command. Or what about cachestat? cachestat is giving information about the number of requests that could be serviced from cache. Of course, you need to create some activity for this

[00:02:40] to be more interesting. So if you open a new terminal, well, the new terminal by itself is already showing. There we go, couple of cache hits and you'll see cache misses as well in case you are doing something you haven't done before. Interesting utility. Likewise, there is xfsslower.

[00:03:04] xfsslower, which will print slow file system operations on specific files as well. So every XFS operations slower than 10 microsecond is shown here. So you can see which file is involved and that might help you in analyzing what is going on.

[00:03:22] And likewise, there is ext4slower, which is doing the same for ext4 and oh no file operations and we are missing a kernel option here. So kernel should be built with CONFIG_KALLSYMS_ALL. Well, that's about kernel building.

[00:03:40] Let's not do that here. But hey, xfsslower by itself is a useful utility. So usr/share/bcc/tools, check them out. Some of them are pretty obscure, others are pretty useful.