[00:00:06] - So let's talk a bit more about library usage. When Linux starts, the LD config command creates a library cache. This library cache is stored in the binary file etc ld so cache, you can use the command LD config minus P if you

[00:00:22] want to show the current contents. Programs that require access to library should update the library cache through the package installer. And in some cases it is required to manually run the LD config command to update the library cache. That would happen if you get a missing symbols error message

[00:00:40] while trying to load a program that has just been installed. The ltrace command can be used to print library calls that are used by a process. It's a little bit like strace and lots of output may be provided. You should consider adding the minus L library name, dot so dot version option

[00:01:00] to print library S from a specific library only. Otherwise you'll really be overwhelmed. You can write a result. Next, using redirection of the standard error to an output file or using pipe M percent like we have seen it for strace. Let me show you, so here we have LD config minus P,

[00:01:21] which is a print of the library cache. As you can see, it's quite a bit of libraries that are displayed. So let me also show you ltrace output. You might have to install it first. Ltrace is not always installed by default, using ltrace minus L on libc.so.6 on DLS command

[00:01:42] and let me pipe that to less. And there we go, here we can see all the library functions that come from the Glibc. And that's actually quite important because we can see malloc, the memory allocation, we can see read there. These are the wrappers that are

[00:02:00] provided by libc to get access to specific kernel functionality. And there's also more generic functions like set local, which getting the current language settings and so much more.