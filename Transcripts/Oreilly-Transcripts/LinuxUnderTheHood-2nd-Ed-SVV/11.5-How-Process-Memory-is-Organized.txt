[00:00:06] - Let's talk about the way how process memory is organized. So when a process starts, it allocates virtual memory, which are the memory pointers, and within the allocated virtual address space, different areas are used

[00:00:21] and I wanna talk about these different areas in this video. The code segment is where the program code is loaded. Its size is equal to the size of the process file. The data segment is used to sort data and hard-coded variables that are defined by the programmer. Then there is the heap, which is used for dynamic memory allocation

[00:00:40] in addition to the data segment where variables are allocated and freed during process execution. Memory mapped regions are reserved for accessing libraries and shared files, and the stack has a more static nature and is used for accessing function calls. Let's make a quick drawing to show you how these relate to one another.

[00:01:01] Alright, so when a process is starting, it allocates virtual memory. Let's call it VM for virtual memory. What do we have in the virtual memory? Well, on the top of it, we have the stack, and the stack in its size is growing downwards.

[00:01:22] Then we have the memory mapped region. This is where your libraries and such are loaded. Next we have the heap and the heap on memory need is growing upwards,

[00:01:40] so they are growing towards another. Next we have the data segment and we have the code segment. And these are the different areas of memory that processes are using. Now of course, this is virtual memory, and from the virtual memory, actual memory is mapped in small pages

[00:02:02] to the resident memory. And that goes for all of these areas because virtual memory is just a reservation of memory. Resident memory is what's really used. So let's talk about these memory areas a little bit more. Stack memory is fast accessible memory.

[00:02:21] It's used for small amounts of data. When processes start, a limited amount of stack memory is allocated and the size of the stack is limited, which is why it is used for small amounts of data like functions. It has a Last In, First Out or LIFO access structure. Programs automatically allocate

[00:02:40] and deallocate stack memory. And when stack memory is exhausted, you'll see a stack overflow error. Now we have heap memory. Heap memory is used to store data that is not tied to a specific function or procedure. It has no specific structure and individual parts of the heap can be addressed individually.

[00:03:00] Heap memory is dynamically allocated and deallocated during application run time, and the size of it is dynamically adjusted. Access to heap memory is slower than access to stack memory, and it is mainly used for larger structures. If processes add memory to the heap without ever deallocating it, a memory leak occurs.

[00:03:22] Now if you want to analyze this, you can address the /proc/<PID>/status directory. More details about the actual memory maps are in /proc/<PID>/maps And if you don't want to go through the proc files, then you have an interpreted view that is displayed

[00:03:41] by using the pmap command. When running pmap you'll notice that memory location change, and that is because of address randomization. That's a technique that protects program memory against specific attacks like stack smashing. Let me demonstrate how you can monitor memory usage. In the next demo you'll see virtual memory addresses

[00:04:02] with the lower addresses in the upper lines and the higher addresses in the lower lines, and you'll see the different memory sections where the text and code segment corresponds to the process name and the data segment and heap show as anonymous memory. The memory map regions contain the name of libraries and files, which in fact is also anonymous memory.

[00:04:21] And on top of that the stack is shown. You can also find the same information in /proc/<PID>/maps So to do this demo, let me find the PID of the secure shell process. Ah, we don't have secure shell process.

[00:04:41] Well, we can deal with that. 3941, so let me do a cat on /proc/3941/status which is showing generic information about the memory that is currently used,

[00:05:01] which is showing generic information about what exactly the process is doing. Now if we want more information about memory allocations, then we can use pmap on the PID of the secure shell process. And here we can see all the different memory addresses

[00:05:20] that have been allocated or mapped in virtual memory. That's information that you can also get directly from the proc file system. So if you would go to /proc/$(pidof sshd) and in there check for the maps, then you can see that the memory address lines

[00:05:41] are a bit less readable, but it is basically the same information. And if you want more detailed information, you can use pmap -x on S(pidof sshd) and oh, that's a lot of information as you can see. Maybe it makes sense to have a look at the header line.

[00:06:01] And in the header line you can see exactly for every single part of memory what exactly it is doing.