[00:00:06] - So it's often heard that in Linux everything is a file. And it's true. It really means that Linux communication happens through files. Device access, for instance, happens through device files.

[00:00:20] Pipes happen through temporary files that are created in the operating system. And file descriptors are numbers that are used by processes to keep a list of open files. All processes have at least three standard file descriptors, which is 0 for STDIN. This refers to the standard input device.

[00:00:40] For interactive processes, that would normally be the keyboard. 1 is STDOUT, it refers to the standard output device for an interactive process, it's your monitor. And for daemon processes, it's often the deaf node device because they don't have a standard output. And 2 is standard error. It refers to the standard error device.

[00:01:01] I'm pretty sure that you have used these while using redirection in a bare shell. A complete list of file descriptors can be found in the /proc/<PID>/fd directory and the lsof command gives an interpreted list. Let me show you. So if it is about file descriptors, I really like to have a look at Firefox

[00:01:21] because it's starting a lot. So using ps aux pipe grep firef where we can see the different processes and we are going to have a look at process 3823. So if you go to /proc/3823/fd, that's a file descriptive directory for this process.

[00:01:42] And ls -l is giving a list of all the files that are open. Oh boy, that's a lot. And that's true because this is a browser. And what do we see? We see zero redirecting to dev/null. We see one the redirecting to a socket. What's that? That's a standard output. And this graphical application uses a socket

[00:02:01] with this socket identifier to print the information on screen and we see more sockets involved and also a lot of files that are involved in Firefox for doing its work. I like having this look in the proc pid directly. You could also use lsof, but if you want to do it in lsof, then you need

[00:02:22] to figure out how, again, do we connect to a specific process? And I would say, well, good luck finding how to do this in lsof. There we go, -p. So what did we have? We had 3823.

[00:02:40] So let's do an lsof -b on 3823. There we go. Same information. Interpreted this time. So use either one of these if you want to find out which file descriptive certain processes have opened. You'll learn more about these as you continue in this course.