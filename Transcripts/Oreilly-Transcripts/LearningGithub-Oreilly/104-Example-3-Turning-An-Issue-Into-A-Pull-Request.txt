[00:00:00] For our final GitHub API, example will replicate a feature found in Hub. GitHub command line tool that wraps around the original get command to provide additional features in see tactic Comfort. It is the ability to turn an issue in to pull request. Sometimes create an issue for project and later on for whatever reasons. You end up providing the fix for

[00:00:20] it. You could open a new poll request and close the separate issue, but the best thing would be to turn this issue into a pull request to avoid duplication entirely. So we'll assume a starting situation where the issue exists and we push the branch either on a fork or on the original repo that fixes the issue. And now we want to turn the issue into a pull request for master based on a branch.

[00:00:43] Let's create such a situation in say, one of a Repose, I will create an issue in their new issue and I will call that my this is bad blah, blah blah. So obviously that would be an actual issue and now let us say that this is Pertain, maybe to the index.html

[00:01:02] later on, in time we actually got that covered. Maybe there should like fix how bad that was fixes S5. Not so bad. After all and I will actually create a branch or call that fix that issue and it will offer as usual to create the pull request. But let's say, I do not want to create a separate folder request. I would like to take the issue,

[00:01:22] make that a pull request. If we look at the GitHub zpi for this, we see that progress API does have Apple quest creation call, fortunately, and normally, there is a first way where you have the classic repo identification to owner and repo as usual and then it needs a title which is

[00:01:41] required the head and base, which are the usual term. The base is where you want the pull request to land. And the head is the branch that contains the changes and we can also provide a body for the pull request description. However, we are not interested in creating a request from scratch. Would like to hijack an existing issue and turn it into a pull request.

[00:02:00] Title, body, command thread, and all. Fortunately, there is an alternative input that indeed does away with the title and body entirely takes the issue number. That is exactly what we need. So let's look at the code for this as usual. This is like a small command line tool. It is in the bin directory, just like in previous lesson and we

[00:02:20] start with the colors and getting the few modules. We need messaging, the command line interface, we have in using and token these Are what we are looking for. We are only naming argument this time around and there is one argument called issue which is a number. So what do I have? I have - - n say is where I want to create that. Pull

[00:02:41] request that can be fully qualified repository name with owner or if it is not with its user will assume. This means, we will need to know my own login. There is this - issue that tells me, what is the issue that I need to convert into a poll request? And there is a - - using, which lets me tell which branch to use as a

[00:03:00] Source for the pull request. And if that Branch comes from another repo, that is a fork of the current repo I can use GitHub story tional Syntax for that prefixing, the branch name with the username for the fork and then as usual, creating the GitHub API client authenticating with the token because we still expect the token. And then we just look at our -

[00:03:20] - in augment to see whether it is a name with owner using a regular expression. If it does have a name that's great, I can call my create pure function immediately if you Does not have to ask my GitHub client wrapper to get me, the currently authenticated user. Again dealing with the arrows to just prefix that - - an argument with my login and a slash so that it is a

[00:03:41] fully qualified name with owner. And now there is the create pure function, it is just a call directly to the GitHub client. Now, what is nice on this one? Is that the GitHub client actually provides to syntax, is either create which creates the traditional one of the

[00:04:00] In body of create from issue which does not expect title and body, but once the issue number and this way, they can provide proper validation for both cases, which is quite a nice. And as we did in the previous example, once the operation is completed in confirmed, we will display the URL for the pull request and automatically open it in the users favorite browser. Let us

[00:04:20] see how that works. I could go node, then you should PR and my token, obviously now I need my n. So that would be O'Reilly GitHub demo because I'm not using my own name in here. It will have to request my current name to qualify this. The issue would be, let me check what is the issue? I created

[00:04:41] Issue Number 8. Okay, so they should be eight answer using equal and my branch is call fix that issue. So let's look at what that does. It worked it turned that issue. Number eight into a pull request. It used what was in there and I got my pull request now and I do not have an itch. Use against you, make sure cancer is back to zero. So I turned that issue into a pull request,

[00:05:04] that's great. Now I would like to wrap up this lesson by telling you if you haven't already noticed. When looking at the demo repo we used here and at the API demo code base specifically that the entire code base for it is intensely commented and used to produce what's called a

[00:05:20] full annotated Source. You have a link in the readme that looks like this, this is the readme and then you can get at all the parts of the code. Code shows the server entry point and the CI server core, all of that this is publicly accessible. This is on the GitHub Pages for that repo and that aims to explain to

[00:05:41] you again. Just like I would be sitting next to you and Whispering into your ear how that code works and white down the way it is. So that even if you're not really proficient, both at the GitHub API or at node, you can make sense of it and adapt that for your need and that is it for our GitHub API. Samples. I hope you could see that. It's pretty easy to use. Actually, if the script is in your

[00:06:03] usual cup of tea, I can guarantee you will find nice wrappers for you on preferred language. And in fact, this is the next lesson, we will give you a quick tour of the existing wrapper, libraries for the most common languages out there.