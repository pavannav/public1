[00:00:00] Moving on from our previous example. Let's now do a smaller one. We're going to look at small come online tool. That lets us take a number of files or directories and turn them into a chest to do this. We just need a single API, call the gist creation. One, it uses only three parameters with just one required one. The founds payload,

[00:00:20] as you can see, it's hash with file names as keys, and hashes with a single content property as values. So this only loads for textual files, not binaries. Also know that should avoid naming your files. Just found one just file to and the like as these are Keys used internally by the gist system. You will also notice there is an optional description

[00:00:42] parameter for the short textual description of what the gist is about. And there's a public parameter, because by default, it will create private gist as disease. A command line tool, it runs completely separate from the small server. We used so far so it will need a token or a username and password to work. Actually creating a public gist doesn't need authentication. It would

[00:01:03] be an anonymous just but I much prefer authenticated jests if only to then be able to revise them. So we will require a token. This can be your personal access token that you created earlier in this video series or even the oauth token from the previous lessons in this chapter use, whichever you want. We'll look for this token in the

[00:01:23] environment variables as token, all caps, but also love for passing it through the command line. The API call is pretty simple in the end, the demo, webhook repo, you clones contains a bin directory, with a create just script in there. Thanks to special configuration

[00:01:41] and a package.json file. If you installed this repo globally as an npm module, you would get a create gist. Command available globally mapping into this. So let us look at the code. This is the create just file in the bin directory of your API demo Branch. After enabling

[00:02:00] Color extensions for input and getting the modules. We need we start messaging the command line as the say we could have used your a more advanced command line for sure. And use like a fool declarative set of requirements and format. But I decided to go with a very minimalist composer and just do some manual non-magic come online interpretation.

[00:02:20] Basically we allow for tostring options, token a description and one Boolean option private as this tool prefer to create public, just by default. Or any other chunky. No common line will be regarded as a path. Either a file or a directory, having a token of label is considered mandatory and not having any eligible

[00:02:41] file to send is a problem to. So we're checking for this here with the token, looking at the argument first, if no arguments provided at the token environment variable. And then we're Computing the files based on Augie dot underscore which is the list of every single piece of the command line that did not match specific options defined earlier. Let's look quickly at the code that computer file lists compute finalists

[00:03:03] here. Let me just look at that function here. It's pure JavaScript. There's no GitHub involved. It's just a recursive algorithm, that looks at every path in turn, and if this is a file, it will attempt possessing. Edifices of directories will grab everything in there normalize the past, and call itself, recursively, accumulating more and more

[00:03:23] in the resulting payload. The ACT variable to the accumulator. And when you look at the smaller process file function, Can see that. We are actually checking that this fall is textual. We delegate here to a module called is text or binary. That does a lot of you ristic space on file, extensions and file content analysis

[00:03:42] to determine whether the file is textual or binary because the API does not allow for binary files. And if it is textual, we grab that file with a heuristic detection for its text encoding and we just add that if we skip a file, we will still log that to the console, just for user information. Onto the actual GitHub API Parts, actually pretty simple.

[00:04:03] We Define here, the GitHub client. Same thing, version, the timeout, which is much longer here, because possibly we send a lot of data. We use the different user agent and we used before to be more specific and with engage using a token and then we just call the actual API call. We don't have a GitHub Bridge here. We can just use the

[00:04:23] API client wrapper directly with the files because we build a payload that matches the API needs or description. Mint and public, unless we provided a private argument. And if that works, we display the URL. And we actually use a nice little module called OPN, which I only asked you to open. That

[00:04:42] was magically opens that URL for our users comfort, and that's all there is to it. So, let me just try that tool right now. I'm in my repo so I can use that on pass the token directly and this is the person likes it. So, can we created earlier otherwise I could have done an export token. Okay, Sequel on Windows set. Took an equal and this is

[00:05:03] going to be private. I will say just a demo and I'm going to actually send all of the JavaScript content for that repo. So that would be controllers, lib bin, and Serge's, and building all that, and it's sending that immediately. You can say, tells me our, here's your new Js. And there it is. And it opens out for me. That's a

[00:05:23] pretty useful tool. Really should probably package it up for Global installation and publish it. That is it. It for this again, example, this was considerably shorter and simpler will also build a small command line tool in our next lesson for third and final example, which will turn an existing issue

[00:05:41] into pull request once we have pushed the branch that fixes the issue.