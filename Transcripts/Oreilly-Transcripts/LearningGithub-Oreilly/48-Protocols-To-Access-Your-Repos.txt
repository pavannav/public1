[00:00:00] Let's talk for a minute about the network. Protocols, you can use to access your GitHub repository. There are really three you could go with. Let's get subversion out of the way. It is there only to make it easier on people. Still stuck in the subversion, Quagmire to be able to leverage GitHub as a host and thereby gain access to all the collaborative. Features around the

[00:00:20] code GitHub is actually the largest version host on the internet. Believe it or not, but we are not version users anymore, are we? So let's focus on the two other party. Protocols, get hybrid, Commons HTTP. It is the default position for every repo until GitHub notices, you switching to get over SSH. Instead, the reason for this is that most corporate firewalls or

[00:00:42] proxies will not let SSH and get protocols through on the net if boards. But obviously you do for HTTP, get helps. HTTP is actually smart. Both in read-only and read, write mode remains very efficient to use on par with the SSH base equivalent. However, if you all

[00:01:00] Using HTTP URLs, when pushing from the git command line, you will need to use a personal access token instead of a password to avoid the risk of exposing your main password. We detail this back in less than 4, or for the one on the Hub wrap tool. So, watch this video. Again, if you need to set this up, get over. SSH is the more traditional super

[00:01:20] safe way. It is mostly used for read/write Access and requires you public SSH. Key to be registered on your GitHub user account, we Word how to add your kit? You're profiling lesson. 2002 note that this is often possible to trick your corporate firewall or proxy by S searching over https. The network and

[00:01:41] points at github.com can deal with that if your firewall or proxy. Examined the protocol being actually used, it might still deny your connection but if you want to use a sage it's probably worth a try. And that's all there is to know about a protocol. You could use both are well secured although get over stage is perhaps more.

[00:02:00] More conducive to high security than HTTP as the latter. Lets you use and store, clear text passwords, unless you have enabled, second Factor authentication pick. Whichever works best for you. Let us now move on to the next lesson, which explains what the pseudo mud is.