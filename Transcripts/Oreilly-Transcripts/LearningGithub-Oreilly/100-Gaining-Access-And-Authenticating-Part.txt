[00:00:01] Let's talk about copes. Now, at the time of this recording and users cannot change the granted Scopes, you're requiring. It's an all-or-nothing proposition. However, GitHub API documentation, clearly states that in the near future, they will be able to selectively Grand fewer permissions at authorization time and it will even be allowed to revise. Granted permissions

[00:00:22] after the fact. Right now, all the can do with the granted Scopes is Bros the Scopes and Boca nap wholesale, if I go to my authorized applications here, I see for instance, code climate. I can see what copes, I gave and I can revoke the entire axis, but that is it,

[00:00:42] it's still pretty good from security and privacy standpoint. In fact, it's encouraged that your app offers them a direct link somewhere in their account Pages or something, to review. Their authorization. Such a link is easy to build. You just need your abs client ID every authorization. Authentication request comes. With a normalized list of currently granted Scopes. So you can verify that

[00:01:03] you have all the Scopes you need. And if that's not the case, you can always display a page to your user explaining, which additional permissions, you need to operate and why, and provide them with a new authorization link that request is extra permissions again. You can easily verify which Scopes your current touka has been granted. And compare that with the requested copes for any given API

[00:01:23] action by just issuing an HTTP header request to it. Let's see that in action with, okay, Cole Cole, for example, let me just add - capital I so that I only get the header not interested in payload. Now if you look at this you will see a specific

[00:01:41] header here called XO off scoffs and it tells you what Scopes your own took and is good for and then X except you to of Scopes may tell you what Scopes the current action requires that might differ from the ones you have. Now, the demo server that we fought in used in previous chapters,

[00:02:00] The webhook endpoint has some really cool, extra code available in it and it's API demo Branch. So let us check that Branch out and we'll install the extra penalties because you don't have those while it's burning. Let me tell you about what's coming up this server. Environment needs the app, credentials for the app. We just registered

[00:02:21] in order, not to have to manually export these to the environment again and again, every time we play with it, we will put that in an environment file that Will never ever be versioned that our server will automatically load and its environment. When it is not in production mode, you would probably configure your hosting to actually adjust the environment instead of this. But that's a really

[00:02:42] cool and common development approach. What we will do is we will grab those credentials from a developer application, here's my client ID to start with and then the client secret there we are. We will just edit a DOT and file. So that would be our client ID will explore that as client ID equal.

[00:03:03] This is the variable that our process expects and its environment and then clients Secrets which we can again copy so that fall is ignored by get it doesn't even show up. If I do get status, I don't see it but it is loaded by our server and we can now start the server again with npm start and then it

[00:03:23] goes. GitHub credentials properly loaded. It will actually check those credentials will see how but it tells me here that At my app, credential seem to successfully authenticate. The demo server is running on localhost four, five, six, seven, eight, there is a secret token, which was generated, which was going to be using later, and there we are.

[00:03:41] So, as you can see, this uses the port that we defined it registration time. If it's still available, it will go to a free port, a bit, higher up and tell you. So so you can adjust your apps registration and will tell you to adjust your absurd situation. You can open this root URL in your browser. Grab that load it in a browser.

[00:04:00] Here it is and we see this button which is actually a link and authorization link that will start what I like to call the oauth dance. When you click it, you'll get redirected to get hubs. Authorization screen goes. And you see, we get this redirection here to get up.com such login or authorize with the client ID, identify your app and the

[00:04:20] redirection URL and scope requirements. So we get this authorization screen. Something you've already seen. This is your avatar. This is the avatar. Of the account, owning the service, the developer application. So this happens to be the same one here and this is a detail of the permissions that I'm about to Grant. So I will be able to

[00:04:42] authorize this application and we'll look at a call back that the server gets. So there goes successfully obtain, your auth token and look at the information I got here. It tells me, okay, I got a get to authorize and then I got her access.

[00:05:00] Cocaine. And there were Scopes in there that got normalize, the normalized scope list, which I mentioned earlier, this is repo, use the Callback that access token. Actually, I do not want to have to reauthorize with it. Every time I run this example server, something, which you would never do in production. Normally, this is something that would be stored in your production database along with my

[00:05:20] user account information ideally in an encrypted way. But I will just do that in my hands will just stop the server with control C. The dot and again and just for the sake of demonstration and for the ease of testing here, the server code accepts loading that access to confirm the environment as well. Obviously this is going to be completely different token

[00:05:41] for you. Now, let's take a quick look at the code to see what it looks like. Our server is really here using node.js as we did in a previous lessons. So we went with one of the two most popular GitHub API rappers there. It's called the GitHub module.

[00:06:00] If you look at the server for instance, the main server is your typical almost bare bones Express web server app. Mostly sets up a couple of traditional middle way here for spreading form fields. In incoming requests logging maintaining a session and Flash messages stuff like that. It's pretty Bare Bones. The homepage

[00:06:20] simply renders here, static template, mostly just telling it whether we are authenticated or not because if we all tend to get it, as you can see, Here, we got a different set of buttons. If we are not authenticated we allow for through ization. I'm just checking that I do have an access token available here that's all and if I look at that template, you can see that

[00:06:41] if we are authenticated then we have this set of button if you're not. This is the authorize button and that button goes to slash auth / authorized and that starts the GitHub dance. If I look at Miser, can't cure. And look at that method or authorized goes to GitHub authorization. If I look at that method, just redirect

[00:07:03] here to an authorization URL, which is built by my GitHub Bridge. So let me just look at that method. The npm module for get her battery does not provide helper methods, for this kind of URL building for the of dance basically. So I'm just building this, providing my client ID and direct URI and Scopes that

[00:07:23] I want. Which is just a small re just saying, repo and a constant. So that's just us as per what the dark say. And now when we granted this permissions and GitHub it, redirected to that callback URL. Which was / water. /, call back - look at this. I drowned here

[00:07:41] / or / callback is exchanged oath for code for talking. Let's look at that method and it just did a gate to use anonymous method in the GitHub Bridge. So let's look at exchange hold for token in the bridge again. No real help from the module here. So I'm just doing a survey To server HTTP post request to that

[00:08:03] tendered URL. Identifying my application using its client ID and client secret. And providing the authorization code that GitHub at sent me in the clear. Now, it's going to return either errors. So there's a lot of boilerplate code here to just standardize the error or possibly display the error. Otherwise I get the access token. I save that in memory and display thats

[00:08:23] that perhaps I can put that in my development environment and I just switch the authentication system from The actual client API wrapper, provided by the npm module so that it uses the token now. So that's the access token. That's going to be used for that server until it just stops running. And that's really all there is

[00:08:41] to the authentication part. Now on the homepage, you can see several other buttons. There is, for example, one button. That lets you review the access the granted to your app, and it just uses a fixed GitHub URL completed with the apps client ID. If you look at the URL at bottom left, part of the screen you can This is a fixed rail with just my client ID at the end. It's going to serve as

[00:09:03] redirection so that I get immediately to the review of my access granted which could have caused a connection. So it tells me yeah, I can access private repos and that's it, and you can revoke that access to we've seen that screen for now, the second button here, let me verify that my token is still current and that is still valid.

[00:09:23] This is achieved through a server to server requests down. As usual with the apps, client ID, and Secret. Secret as credentials. We just provide the access token. Let's click it to see what happens. Just stopped my server, so I might want to stand that ever again. Now it's using this tool access credential, we just

[00:09:41] had told me just do that again, it's just me, my oauth token is still valid and if we look at the console, we see the payload that we actually got acid to contract response, right? We got all the information about the token, the token itself, it's hashed version. Some notes may be associated. That when it was created and by whom etc, etc.

[00:10:03] And the code for that again is not too complex. If I look at my user account here the check token, round goes to the check token method, and check talking messages, dedicated to the bridge. And if look at check token in the bridge, it's again just a simple URL building with my apps client ID and the

[00:10:23] access token. I just request that authenticating using my apps credentials and I get the body bag. That's all there is that's it for authentication and authorization with that out of the way, we can finally start coding real use cases. But before we do that, let's take a final moment in the next video to talk about the values

[00:10:42] feature areas that GitHub API covers.