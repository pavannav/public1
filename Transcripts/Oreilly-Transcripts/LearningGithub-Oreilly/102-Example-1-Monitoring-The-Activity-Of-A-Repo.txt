[00:00:00] The previous lessons in this chapter introduce GitHub zpi and it's called Senate's. It's voice feature areas. How to register or ABS, a developer deal with the oauth dance together. App use get-ups API on behalf of an end user which in this case is you and me as a first example of an actual feature will go for somewhat big fish, a kind of continuous integration server.

[00:00:21] Remember how in the previous chapter we had trayvis run or test Suite on every port? Standing status checks in a whole code cough would audit the latest comments. You know pull request to check that it off its vision test coverage. What we're going to do something similar. We're going to provide a service that scans all the comments involved in. Pull request to look for passwords, that shouldn't be here. Such red-flag words as

[00:00:42] utilize and leverage and Synergy or growth hacking. When people start using these, instead of simpler better words, you know, they're trying to bamboozle you. So in order to put this together we'll have to deal with a few aspects of the API. All in this one example, which makes it a nice case study first, we need our app to be notified. Whenever a pull request is open. And when it gets new comets in,

[00:01:03] we've already seen how to do that in a previous chapter. That's a web hook. However, in this lesson, we'll have to automatically set up the hook from the App instead of requiring, the end-user to do it manually just so it's nicer on them. Once the hook is set up GitHub will start sending events alway and we can start by letting GitHub know we're about to verify something by

[00:01:23] sending a pending status for that pull request, which touches the repository status API. We then need to grab the cam. It lists, for that pain request, as that event, won't have this type of detailed info and bedded in this payload. So there comes another part of the API this time from the pull request section, we can then analyze the messages for this comments

[00:01:41] looking for buzzwords and build up a data set of offending comets. The final step is to send another status to GitHub either a success or failure, possibly with a descriptive message. So the sounds pretty cool but probably involves quite a lot of code because you're not necessarily you know developer I'm not going to

[00:02:00] Sit through the typing and bugging of this. The code is already there in the webhook endpoint repo, you clown in the API demo Branch. We checked out in the previous lesson. I'll just tool used for the role of an API pages and pay that with the code, I rode as an example, use of these API features. In fact, if you want to create a violation of this repo know the languages such as Ruby python or Java. That would be swell. Go

[00:02:21] for it and when it's up, let me know about it. Let us roll back to the first step. Setting up the hook on behalf of or end user. ER, we already got the necessary permissions when authorizing the app in the previous lesson, the repository is webhooks API has just a call. We need, we need to pass in a name which for web

[00:02:41] hooks has to be web or lowercase, or the names are reserved for official service Integrations. Then we must specify, which events were interested in by default that would be pushed, but that's not what we're looking for here. We're looking for pull request, as we'll see. In a second, hooks are active by default, but you can explicitly.

[00:03:00] Estate. So finally there's a config object. Now as you can see in the example, just below for a web hook that config has a specific parameter list, it must include a URL field for the web who can point a content type to Define which format the incoming even request will use for its payload and possibly a secret field. For secret token used to secure the

[00:03:20] connection as discussed in a previous chapter. In our case, the URL will be our public facing HTTP URL provided by N Rock. The content type will be Jason and we'll use the secret generated by your server. The only thing left to decide is which repo to set up the Yukon. As you can see on the API URL, it

[00:03:42] requires a repo identification with the owner and repo Name by default our server. Generates a new secret taku not every run, which means we have to update the hook info all the time. That's not practical. So let's copy the secret token for this run and persist it in order to and file. So it's we used in later on.

[00:04:00] Much like our access token and the apps credentials. So let me just grab that secret token. I'll kill the server. I did the vi and and the secret token and p.m. starts again. And you see, we're still understand secret token. So it used the one that I stored. This way I won't have to reinstall the hook over and over again as I rerun the server.

[00:04:21] Now if you look at the homepage for our app you see a setup to hook button. So got a code. If I look at that template, I see that set of hooks. 's button, goes to /, webhook / set up. Okay, that's in a web hook controller. If I look at rounds, I see that right hook set up goes to render repo list for set, okay? So let's go to run, a repo list for set up

[00:04:41] and look at what's in there. This function, we see an interesting twist. We use a custom function of Oz finding Rock URL to figure out whether and rock is running locally and has mapping to app screen, local port to save or user the trouble of having to copy paste it. Now it so happens that in rocks web interface also a There's an API which lets us that semantically. Inspect, the running tunnels, the code is bread,

[00:05:03] is simple and outside the scope of this lesson. So I'll leave it to you inspection. If you're interested, let's move on to the next part with just delegated to or GitHub API bridge to get a list of the repositories or app can access on behalf of the user. When that listing is there, we just want to reform. So, let's look at the code for that, get repositories. It really, just very

[00:05:23] thin layer on top of a simple API called provided by the npm module GitHub, we extended Full page size to its maximum 100 which is plenty for this demo in production code, you'd probably handle pagination here. Let's click that button and see the result. I get a form where I select a

[00:05:41] repo and have to type the ngrick URL. What I can try now is run and rock and see how that goes. So let me just get there from new terminal and do the end Rock there goes. You can see I get a specific HTTP URL and this is what you have to. Let's refresh that and you can see it was a Detected by occurred. So that's super nice. Let's pick a

[00:06:03] repo that you don't mind since in your hook on and submit the form. I'll just grab get up demo setup that webhook and it tells me you were Hook was set up. Okay, so what happened, let's look at the code. It tells me that a post to web hook set up called set up web hook, okay? Let's look at that method. It just delegate to the

[00:06:23] create hook method of a GitHub API Bridge passing it. The two Fields the name with own Owner of the repo and then the and rock URL. So just look at the create hook method in the bridge. There's just a little more here. What it does. It just creates that payload

[00:06:41] according to what we just discussed. So the user and repo name that has to be web. The URL public URL that got passed content type is Jason secret. Is our secret token events is just pull request and we explicitly say that active is true. Then we just delegate to the npm module so that's very Layer looking at a web hook settings, we should be

[00:07:03] able to see that hook. And indeed, if we look at the console for our server, we can see that we got a ping event, which is the initial setup event for my hooks. We got that being and it was apparently properly sawing so that you could talk and thing just worked. Let's look at our repository web hooks here and if I

[00:07:23] just go to a box and settings, here goes, here's my webhook, it's been properly, set up. It's got everything I need. It's got all the right settings and I can see. Indeed, there was a delivery here, which was the Ping delivery. Now, how did we know which even type to use in a hook

[00:07:41] settings? How did I know that this was pull requests even if that sort of made sense? How did I know what the event payload might look like? This is what the event types and payload section of the API documentation is for. If you look at the pull request, even type, here we can see that it tells us, this is triggered when a poll question. Assignments before us. We have all the information about the action in their do multiple

[00:08:03] actions. There are actually just two of those that are of interest to us. And then the pull request number, we can see the entire payload as always, the docks are great. Now, we're really only interested in two actions here opened, which is when the pool Coast is first created and synchronize, which is when new comets, come in the pull request because of further pushes. So, we will need to filter

[00:08:23] for these before, passing the bucket to a serial code, which brings us to the Handler, by the way, this be The event was handled by or webhook Handler. So how does that work? If I look at high voltages and I look at the Callback URL that we send the / webhook. It's handle webhook event. Okay, let's look at that

[00:08:42] and what it does. It starts just like in the previous chapter, by making sure the incoming request is properly sawing for security purposes. And if it is, it can immediately respond to get Hub and then filter for the appropriate even type. So is that indeed, a pull request? Is that one of the pull request actions are Was it in just open or synchronized? And if that is, then it will

[00:09:03] delegate to CI service, but it will delegate to the show service using an, a synchronous system using nodes process next tick, which basically tells it do it as soon as you can. But not this time around this is useful. If the CI service were very heavy and like an intensive long, synchronous processing this way. I

[00:09:23] do not block my response to get home and I can ensure that I always respond within a few milliseconds. Well, Well, below, get hubs, 30-second timeout for webhook endpoints in other Technologies as know. You'll probably have to push a processing message to message queue and have it handled by some message queue workers on the

[00:09:41] side. This is not something I really need to do on my node server. That brings us to the CI service and to that handle event method. Let's look at that handle event. That function has to do a number of asynchronous tasks in sequence to give the code manageable. We use a single Jazz, which handles all the plumbing for this? I kept this function high-level enough but we'll dive into the

[00:10:03] details of each part in the moment. We start, by sending GitHub a pending status for the pull request. We then analyzed request Comet for band buzzwords and when we're done we either send a failure or a success status. If they are aware Search terms in the commit message than failure. If they weren't success, the

[00:10:23] final lines are just your for server side. Console logging what our status check actually do. Do it uses? The status is API to send a status to GitHub related to the pull request later. Search, our status is pertain to a very specific come. It's not to pull request,

[00:10:41] but a pull request uses the statuses of its head Comet for its status Jacks. So the API needs, as you can see the repo, identification, owner, and repo and then a sharp for the comet to talking about done, it needs the state which can be panning success error of failure error. Would mean service are they were on your end? The Talk. URL is an optional information URL like

[00:11:03] Travis Bill details, for instance and you can have an optional description message for the reason of your current status. And most importantly you need a context, you want that context to be very specific to your system. This is a way to identify the service and the kind of check running it. Perhaps you remember the

[00:11:20] context for Travis Jack's. We had Travis - zi / Bush for individual comments. Coming in and Travis - EI / PR for merge attempts of ongoing. Pull request. So let's look at the code and let's look at the status check method and it is just a method that takes a pull request payload, a state and a description message and it will fill in the status,

[00:11:41] info with the repo identification and the pool requests head Shah, and fixed context for this kind of status checks, which is this constant here, which identifies our service and then it just did a gates to the API Bridge, which in turn is a very thin wrapper. On top of the original npm, module for GitHub, back

[00:12:03] to the main algorithm than here, okay. So that is status. Check the next step is checking for buzzwords. How do we go about it? Let's look at that. Check for bandage words. Okay, so the first thing we need to is to get a list of the Comets involved in the pull request and the pull request API, lets us get that

[00:12:23] based on a repo identification. Pure number, we have that here least comments on a pull request. So as usual full You repo identification and the pull request number and that will send us back and re with all the comments in there, possibly paginate it. So, what we do here is that we just did a gate to the GitHub

[00:12:41] bridge to get these and then we'll transform that. So, how do we go about? Get commits for pull requests in a GitHub Bridge. Well, we really just delegate to the original API to very thin wrapper. We just grabbed the first 100 coming. This is a demo, so I don't deal with pagination, but in production, obviously where we produce. He's done that at that, I set in memory of possible, buzzword occurrences,

[00:13:03] that we usually red flag and the code for this for these detectors world and reduce its Purity as it has nothing to do with GitHub. So, I won't dive into it here but it's fully commented and documented and there's annotated source code for it. So you can dive into it if you're interested going back to the main algorithm, though, once we have that information, we computer failure. Message

[00:13:23] again, just pure JavaScript no GitHub involved and depending on whether there is a failure message or not. We see. Send either failure, check or success status now because we use the same context and we use the same repo and we use the same headshot. These new statuses will update. The original

[00:13:41] pending status is because there are the same context of the same sha. It's just a latest status wins. That sounds really cool. Let's see that in action. Does it even work? Let's try it out by creating a branch with, at least some faulty, comets pushing it to get Hub and making a pull request for it. So let me just clone that thing. Git clone Riley. Could have demo,

[00:14:03] gets that goes and now check out a buzzy Branch. I will make fake empty comments, just leveraging the Synergy here. Oh, this one's heavy and I'll do another one. That just says, this is a nice comment which it is and then let's utilize. Ooh, red flag growth hacking,

[00:14:24] double red flag. I'll just push that. And so my branches there, and here I am. On my repo. So I'm just go to the branches, there's a new Branch. There's my bestie Branch. Now, your girls are make pull requests for it. Yay! Just leave it as is, create the pull request and look my webhook did some analysis

[00:14:42] and the check failed. Now, on this repo had protected the master Branch so that prevents me from merging and this was so fast that we couldn't even see the pending verse was because my analysis GitHub reply, to my request, for comment detail so fast that I could really Immediately, send failure, that is on top of my pending status.

[00:15:04] So let's see how we could fix that though. I'll keep my pull request page on top and I would normally rebase interactive lie here and just change the messages using their we word interactive, Reba's action. But those are empty messages to rebase wouldn't know what to actually rebase because there's no difference in the

[00:15:20] code base between master and Buzzy. So what I will do is I'll just hard reset to master instead and I will rewrite these but perhaps more probably. So instead of leveraging I'll say taking advantage of and instead of sooner G I'll say API Harmony. For example, my second one was just great. This is a nice comment and

[00:15:41] the third one will go. Let's use social media in a smart way. And I'll push and as you can see on Top This goes, here's my pending and the test pass this time. I was okay. I like your commitment edges. I can see that used to be formally. An issue with that history. And you can see my failure message, which was

[00:16:03] computed, tell me which messages which terms, which are others. But now the new history actually tells me everything's Dandy, so that's it. And yes, yes, this was a force push. This is not something you would normally do, but we were just trying to trigger a hook with a clean history. All right. So free past this time in this

[00:16:20] demo that's it for first API example, which was kind of beefy in the next lesson. We'll tackle a smaller Problem by looking at a much smaller API use with really just One call that lets us automatically create a gist from the command line using local textile files.