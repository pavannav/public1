[00:00:00] Picking up from a previous lesson, where we use GitHub page generator to get a quick start out of GitHub Pages side. Let's now look into the underlying technology GitHub uses for this namely Jackal. So, what is Jackal exactly? Well, jacket is a static site generator. This means it lets you use a number of authoring and structuring features such as meta data,

[00:00:20] categorization, syntax coloring for cut bits which to display. Internal linking customs in taxes for markup such as markdown and much more. But Ends up building the final site out of the source material and the result is a set of static files, you can put anywhere you like on the internet as they do not require any special server capability. The main advantage of static sites is the can

[00:00:42] be blazingly fast and served in a highly optimized way which is quite good at dealing with traffic beats. Get Hype uses Jekyll internally. When you push the relevant brand to build an update your site but if you're just relying on them then it's going to be difficult or at least unwieldy to, right? Fine, tune and perhaps even debug your site. Every time you want to check out

[00:01:03] the result of your work. You would have to comment push the GitHub. Wait, a couple seconds for them to build the resulting site. Refresh the browser and start all over again on the next iteration. The recommended approach is much better installed Jekyll locally on your machine and use it for life, tuning of your site. In order to avoid mishaps, you just need to make sure you're using the same set of

[00:01:23] features that get a Pages do installing Jackal. And the extra features for get a pages is quite straightforward. Forward. Once you have Ruby installed on your machine, Ruby is a great programming language that Jackal is written in. It's pretty much a given on any Linux or Os, 10 machine nowadays. You can

[00:01:41] verify that by opening a terminal on OS 10. It's located in the utilities subfolder of applications and then you would type Ruby - V. And if you get a version number in there, you're good to go. Now you do not need to have the very latest will be installed, but make sure it's at least two dot 0, dot 0.

[00:02:00] Which is the minimum requirement for GitHub pages. On Windows. You may very well not have Ruby installed. You can download it at the official website, same thing, if you already have it, but inversion that is to all do a lot of installation ways packages and the run and managers Homebrew, lots of stuff

[00:02:20] on Windows. The easiest way is the Ruby. Interstellar just run the installer answer, a couple questions along the way and you're done. You can then open the command line. Upped the famous CMD program on Windows and run. Ruby - V you should see your version number. Now, the Ruby system comes with a package manager called rubygems accessible through the gem. Command

[00:02:42] will need to use a gem version manager here called bundler, which can install like so depending on your Ruby installation, you might need to run the sudo command before that gem command on Linux or stem. Now here it is but notice installed and that makes a new

[00:03:00] And available called bundle. You can verify that it's available by just typing bundle - V. We now need to start working directly on a relevant Branch for GitHub pages, will be working on a demo repositories, gah - Pages weren't here. So let's clone that. I'm on my desktop here, otherwise you can just see the into it. Grab your repo, grab, the Clone

[00:03:22] URL using https here. So it works for you as well across the wide variety of corporate environments. I'm going to Learn that I'm going to actually immediately, check out the gah - Pages Branch instead of Master and here goes. Okay I have no cloned and I can go into

[00:03:41] that repo, here it is. I'm on the gah - Pages Manch which is all cool. This assumes that you have given some locally, by the way, if you do not install it using any appropriate way search as denoting, the official Interstellar from the official site and then you can open fresh terminal and run. Command again on Windows favor. The freshly installed git

[00:04:03] bash over the built-in command prompt in general, as we're going to be using, get from the command line here, pushing back to GitHub and so forth. I would advise you look into chapter 4 of our previous video. Series learning GitHub, it covers all these aspects in detail, it's get Jekyll. Now we need to install the ruby gem for it will also need the gem for GitHub Pages.

[00:04:23] Which in fact does this Jackal as a dependency? So we don't need to specify both a good way to keep track of the Gen versions, we use. And the dependencies we need is to bundler which we installed a moment ago. Bundler uses a dependency manifest file called The Gem file with a capital G. Let's write one in a clone repo,

[00:04:42] will start by typing bundling it to create that gem file. And then I grab that directory in my favorite editor, am using Sublime Text here. This is the initial text for our gem file, and I actually am not interested in rails here. I'm interested in Have pages. And then I will just use bundle, which is

[00:05:03] short for bundle install. So this might take a while depending on what James, you might already have installed. If it's a fresh Ruby, installation is going to take a while depending on your bandwidth. Most of these germs actually already have. So it's just updating a couple of these and add some point. You're going

[00:05:20] to be all set and there is, I'm finally ready. So we can now start a local server for her, get a page. Has by running the following command bundle. Exact check, he'll serve it's running a server on Port 4000. Now you might run into a permission denied bind to error.

[00:05:42] If your local Port 4000 is already taken you just add a dash dash board option to the command line specifying, the port number for example, 4001. But most of the time 4000 is labeled. Let's just look at that side here in on browser and it works. I Do get my GitHub Pages site here, locally in my machine

[00:06:03] so I can start having an instant feedback. Really when the flopping it, the local Jekyll server will watch our faults and automatically update the locally published side as we go. So check it lets you add pages using any sported Sue syntax. This includes HTML markdown which in this case is GitHub flavored, markdown, textile and more

[00:06:24] let us add a column on Page in Morgan format to do that will create a new A column of on that MD file here and you can see that jkl picked up on it, but it just copy that file as is in the published site. There's no call from that

[00:06:42] HTML in order for Jekyll to understand them. As pre-process, that my file must start with what is called front matter, which is a segment of yeah, mobile data surrounded by two lines with triple dashes and even if I do not have any metadata whatsoever, I Still need to put those lines at the beginning so that Jacqueline, stand some pre-processing

[00:07:03] is in order. So, let me just type my mom down here. I just love gfm, you know, there it is. Save it, it's picked up by Chick-fil-A and it becomes available. So now, I should be able to access Local Host, 4000, /, colophon, that HTML and indeed, I get that it's

[00:07:23] HTML. It is formatted. Okay, that's cool. Just works. Now I would probably like that file. To share the same general layout. As my main page GitHub speeches generator, did not extract the layout as a resource table file, so we need to do that. First in Jekyll layout files are stored in the underscore

[00:07:41] layout root directory. Let's create that and copy paste, our entire index HTML page. Their first, I will create underscore layout subdirectory and I'll call that main dot HTML as this is the main layout. I'll start by copying complete index.html in there.

[00:08:00] And now, I will remove the content that is specific to the homepage and that content is actually what is right here in that section element. There goes, everything else would be the layout and in Jackie layouts, the coal contains for the page is available called content will use a syntax double curly. Syntax will talk about, in a moment to

[00:08:20] inject that valuable here, okay? You can see that Jack and server keeps picking that up in the background and updating it. Now, to wrap up the refactoring of a main page, Page. So that it uses the layout and just going to index.html. I will remove everything that was Leon beso, everything except the content of the section don't goes

[00:08:43] and just changing that will not really be enough. It will not automatically use the layout to make it use the layout. I need to put front matter at the beginning and use the layout key awesome main because this is manage CML. So let us verify that the home On page still uses the layout and indeed it does. I can just

[00:09:03] go now to my color font page and use the same layout information. And now Mike on phone uses the layout,