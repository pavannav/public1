[00:00:00] - Let's take a look at my network map once again. This time, we've added IP addresses and let's get into the IP schemes that I have implemented here. First, we'll start with the virtual network and the virtual machines in there. We can see we have all my virtual machines,

[00:00:20] Debian Server, Debian Client, Ubuntu Server, and so on. Each of them has a separate IP address. What is similar is the first three octets. 10.0.2 is the same for each of these systems. And so in this blue box here,

[00:00:41] you can see that the IP network is 10.0.2.0 with a /24. And the /24 means that we're using a netmask, 255.255.255.0. That also tells us that the network portion

[00:01:00] of the IP address is 10.0.2. It's where the 255s are. The host portion of the IP address is where the zero is. So that could be .51 or .53, .54, whatever, and I'll be showing all these IP addresses in the virtual machines as we go through the course.

[00:01:22] So that is the virtual machine IP network, and we'll talk a little bit more about the /24 in the next sub lesson. We also have the LAN that I'm working at. So, the local area network is where my main system is,

[00:01:42] and also my Debian Laptop and these other servers. Collectively, these are working on the 10.42 network. So we see 10.42.0.0/16. That is the network number.

[00:02:01] The /16 means 255.255.0.0. Once again, the 255s tell us what the network portion of the IP address is. It's 10.42. The zeros tell us the host portion of the IP address,

[00:02:22] and that's gonna be whatever it is. For example, I've got some wired connections on my Ubuntu Desktop. It's 10.42.0.15 through 20, so there's 10.42.0.15, 10.42.0.16, and so forth.

[00:02:42] Same thing with this Proxmox system, 10.42.0.11 through 14 and that's because they have a lot of wired network connections that we're working with here. Each one is using a different IP address. My main system has a lot of connections as well,

[00:03:01] and we'll show some of those as we go through the course also. You'll also note the cloud, but all systems on the cloud are using DHCP. So, all the systems in my virtualization network and all my physical systems here are using static IP addresses. That'll change over time.

[00:03:20] We'll show how to allow our virtual machines to obtain IP addresses automatically but in the cloud, everything is DHCP right now for my systems. Does it have to be? No, but that's what I'm using. More often than not, I'm gonna be working with the virtualization network and the systems on my local area network.

[00:03:42] Let's show the Debian Laptop now. And I have a connection to my Debian Laptop. This is running Debian 12, and we'll make the terminal size a little bit bigger here, make the font bigger, and let's run an IPA command now.

[00:04:00] Now the IPA command is great, but you can get just the information you want by using some options. For example, whenever I'm looking at a new system, I'll do an ip -br -c and then the a. So, let's press Enter for that and take a look at what we get. That strips out a lot of that extra information

[00:04:22] that we saw previously when we did an IPA command. And so what we get now is just the name of the network interface card, whether it's up or down, and the IP addresses. That's really the information that I want. So that's the command that I'm gonna run the most often,

[00:04:42] that ip -br -c a. - br is for brief, -c is for color, and then the a after that. Okay, so we see our wired network interface card, that's enp3s0 on this particular laptop,

[00:05:00] and its IP address is 10.42.48.10. 10.42 is the IP network. The host portion is the last two octets, and my DHCP server is giving out IP addresses within a range, 48.1 through 48.254.

[00:05:25] Okay? So that's the range for the DHCP server. So, this is obtaining an IP address automatically from a DHCP server on my local area network. The second network interface card on this laptop is the wireless card.

[00:05:40] And wireless cards will either show WLP or something similar. Usually, it'll start with a W. And you can see that that wireless card is up as well. And it has obtained a IP address automatically from a SOHO router that also acts as a wireless access point. The network is 192.168.41,

[00:06:03] and the individual host IP is .101. So, there's an example of some IP addresses on the network. As we look at the servers, we're gonna see more and more network interface cards and IP addresses, and it's gonna get more complex.

[00:06:21] And so, for example, if we go to my main system here and I run an ip -br -c a, you'll see a whole lot of connections. But that's why that command is good. It can help to simplify what you're looking at, especially on servers and power workstations

[00:06:41] where you have lots and lots of connections and IP addresses. Let's try doing a ping across the network from one system to the next just to see what's going on on this local area network. Well, we said that the wired connection for my laptop is 10.42.48.10.

[00:07:01] Let's try to connect to that from my main system. We'll do it now. Ping 10.42.48.10. And look, we're getting replies. Very good. And only in 0.2 milliseconds on the average, really, on the average 0.314.

[00:07:22] Pretty good for a local area network. Not bad. So there's a quick ping between my systems, and that's essentially what I'm running here. So those are my IP schemes that I'm gonna be using. I decided on the 10.0.2.0 network number for my virtual machines

[00:07:41] because that's what VirtualBox uses by default when you set up a NAT network. So that's what I'm using here as well. I'm trying to mimic that same IP address scheme. But on my local area network over here, this is one of my local area networks, I'm using 10.42.0.0.

[00:08:03] So keep that in mind as we go through the video course. You're gonna see these different IP addresses, but keep in mind which IP addresses are related to the virtual network and which IP addresses are related to the physical network.