[00:00:00] - Let's get into Lab 6 and that's going to be configuring a static IP address. And we're going to do this on our Debian server. Normally, servers will use a static IP address, but I wanted to show how to change to a DHCP based address and now, we'll show changing back to static. So this is Lab 6 in the repository,

[00:00:22] we'll go over to the server now. Clear the screen and we need to get to that interfaces file that we modified early. And we need to get to that interfaces file that we modified earlier. So we'll do the same thing we did before, vim/etc/network/interfaces.

[00:00:40] And by the way, tab completion is your friend, let's not forget. I just typed int and pressed tab, and it knows I want the interfaces file because that's the only file in the network directory that starts with int. So we'll press enter for that and that brings us back into our configuration. And all I'm really going to do

[00:01:01] is change it back to what it was. So I'm going to press I for insert mode. We'll back out DHCP and we'll change it to static, meaning a manually assigned IP address. We'll press enter and tab over once, and we'll say address is 10,

[00:01:21] and we'll type the IP address that I want to use. 10.0.2.51/24, important to have that CIDR notation for our net mask. Then we'll press enter and tab over once and place in the gateway address, which for me, is 10.0.2.1.

[00:01:43] Escape out to go into command mode, call in WQ to save and quit, and now we have our new configuration. However, if we do an ip a, you're still going to see that DHCP assigned address. Just like before, we have to down

[00:02:01] and up our network interface, so we have to deactivate and reactivate it. So we can do that now with an ifdown ens3. And now it says, "Hey, cannot assign requested address." Well, that's 'cause we have nothing at this point. If we run an ip a and take a look here, you will see for ens3, there's really nothing.

[00:02:22] There's no address whatsoever because it's down and because it's now configured statically. So we need to bring it back up. For me, that's ifup ens3, press enter. Now if we run an ip a, we will see our IP address.

[00:02:45] And here it is and we're back to .51. Again, as always, make sure that you test your work. So we'll do a ping example.com, you can ping whatever you want. And really a better thing might be to ping your gateway.

[00:03:01] If you can ping your gateway, then you know that you have a decent connection within your virtualization system or on your local area network. And we'll show more with the ping command later on in this course. So there we have it, we have a working static IP address. It works correctly.

[00:03:21] And you may have noted that I have some extra credit in the labs, in some of the labs. So if you look at the bottom here, you'll see the ifquery command. So that's another command that you can check out with your network interface. It displays the IP address, netmask, and gateway address.

[00:03:40] Check that out. And that's it for this sub lesson.