[00:00:00] - Time for another lab. In this lab, we'll modify the static IP address with the nmcli command directly in the Bash shell. We're gonna be working in my Debian client here for this. And you see an example in your lab documents. That's essentially what we're gonna do

[00:00:20] with the Debian client. So let's move back over to the terminal. And it's important to know what the network manager names are for your network interface cards. You'll need that information when making modifications. So the command we're gonna be using is nmcli connection,

[00:00:42] modify, so that's nmcli connection modify. We can abbreviate that to con mod or just to c m. And let's start abbreviating right off the bat. So what do I wanna modify? I want to modify that wired connection 1.

[00:01:02] Now here's the thing, A quick little bit here. The device name is ens3, but network manager is calling it Wired connection 1. That may be different depending on what Linux distribution you're running. For example, if you have CentOS, they might automatically use the same name

[00:01:21] as the Linux name. So be ready to see that in the field. Ens3 might be called ens3 or eth0 might be called eth0 or whatever the name is of your card in Linux, it'll show that for network manager. For this Debian client, it's showing wired connection one

[00:01:41] and I wanted to use that so we can see the difference between the names. But whatever that network manager name is, we're going to have to reference that. If it has spaces in it, we'll need double quotes. And so we'll do capital W and press the tab key

[00:02:00] and it should know the rest of the name. And then we're gonna make a modification here. So what do I wanna do? So I want to modify the IP address of this system. Let me open up a second terminal real quick and run the nmcli command and just verify this system is 10.0.2.52.

[00:02:21] Okay, that's good. So I wanna change it. I'm gonna change it to 152 just to show how to modify this. And so we need to know several things. We need to know the method that we're using, whether it's gonna be a statically configured IP address or one that's coming from a DHCP server.

[00:02:42] In this case we're choosing static. We also need to know the IP address and the net mask. And then we need to know the gateway and the DNS server that we'll be using. Those are the main components of our TCP IP configuration. We can add lots and lots more options,

[00:03:01] but those are the main things that we need. So first we need to know the method and the argument for this is ipv4.method. The method we're gonna choose is manual. If we wanted to obtain an IP address from a DHCP server, we would just set that to auto

[00:03:21] and it would automatically grab everything we need. And that would be the only configuration we'd have to do. But I don't want that. I wanna show how to set this to manual. What I'm doing here is I'm kind of considering this to be a server that I'd be working at. And the reason I'm using the Debian client is just

[00:03:40] 'cause it's a little bit easier on the eyes. The servers that I work with, you know they can have some kind of harsh looking text and so it's a little easier on the eyes when we work with a desktop environments. So just imagine that this is a server that we're reconfiguring.

[00:04:01] 'Cause if you need to change IP addresses, you're gonna do that with the nmcli command. And you're gonna do that on servers quite often that needs static IPs. So let's just imagine that that is the scenario here. So ipv4.method, we're gonna say that that is manual.

[00:04:21] We are manually assigning the IP, it's a static IP address. Next, what is the IP address? The argument for that is ipv4.address. And so the address I want to use here is 10.0.2.152/24.

[00:04:42] So that's our triple 255 net mask. Next, the gateway address, the argument for that is ipv4.gateway. And for me that's 10.0.2.1. And this is gonna word wrap at this point. If you don't want word wrapping,

[00:05:01] you can put a backslash in here and then press enter and it'll start a second line. But this is still gonna be one command. And so the next thing I need is the DNS server that I wanna report to. The argument for that is ipv4.dns and I'm gonna be using 10.0.2.1.

[00:05:20] But if you wanted to use a different DNS server, this is where you could do it. You could set it to whatever you want. I'll use 10.0.2.1 and that's it. Let's take a look at the whole command, nmcli connection modify, abbreviated as c m, the name of the network interface card.

[00:05:42] If it's just a single name like ens3, you don't need double quotes. But if you have a space here, you'll either need double quotes or you'll need to set it in place. Double quotes is fine. And then our arguments, ipv4.method. Well we're setting this as a static IP, so we state manual, ipv4.address.

[00:06:03] And this is the address I want. Ipv4.gateway and this is the gateway that we want. And then ipv4.dns. And that's the DNS server I want to talk to. Again, I just separated lines with a backslash so that it didn't word wrap across the screen.

[00:06:21] We press enter for this. No news is good news. So the command was taken, but that's not going to modify the actual connection just yet because we have to deactivate and reactivate the system. If we were to run an nmcli command once again

[00:06:43] in another terminal, it's still gonna show as 10.0.2.52. Okay, so let's down and up the network interface. We do that with the nmcli connection down and then the name of the connection.

[00:07:00] Do we need connection down? No, it can just be c d. We'll leave it as that though. So connection down, press enter and it says "Wired connection 1" is successfully deactivated. Good. Let's go to the other terminal again. And before we bring it back up, let's run that nmcli command once more. When we do so it's red, ens3 is disconnected.

[00:07:22] So keep that in mind. Sometimes if you're troubleshooting a system that runs network manager, you might see this red here and it could just be that the network interface card is not configured or maybe it just hasn't been activated for whatever reason.

[00:07:41] Or maybe some other software has deactivated it. So red is bad, like we said, right? We don't want red. We wanna make sure that we have everything green, so we have to bring it back up. So I'll just up arrow, control arrow over to down and change that to up. Or you could abbreviate that as nmcli c u in the name

[00:08:04] of the connection, press enter. Connection successfully activated. Go back to my second terminal, type nmcli again, green is good and we're at .152. Excellent. And for fun, let's add one more item.

[00:08:20] A lot of times a client will need to search for a specific domain name so that perhaps the client could become a part of that domain. And so we can do that with the nmcli connection, modify command with our wired connection. And then the argument would be ipv4.dns-search.

[00:08:45] And then we'll specify the domain name that we want to search for, for example. And it's gotta be in double quotes, "example.local" and press enter. Good, and we'll have to down

[00:09:00] and up that connection again. So I'll just go through my history to do so, down and up. Good, let's go to the other terminal and let's take a look with an nmcli one more time here. Now you'll see we are 10.0.2.152 like we should be.

[00:09:20] We see our DNS servers this, but now we also have the domain that we're searching, for, example.local. And so if we're using host names that are fully qualified domain names that include example.local on the end, now we can kind of work with that and look for that domain and possibly become a part of that domain later on.

[00:09:41] So be sure to practice with the nmcli commands now to modify your network connection.