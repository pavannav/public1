[00:00:01] - Let's briefly talk about how the OSI model corresponds to Linux. So here we have the seven layers and names, and we have some components of Linux. So let's take a look at this. Here we have userspace, and it's inhabiting layers five through seven. The Session layer, Presentation layer and Application layer.

[00:00:22] So what is userspace? Userspace is when the user is actually working in Linux and doing things, typing commands in the terminal, or running programs in the GUI. The layers of the OSI model that deal with this are the Session layer, Presentation layer,

[00:00:40] and Application layer. So for example, if you connect in your browser and type HTTPS, that's an Application layer type thing. If you're working in the terminal and you type Wget or cURL, that's an Application layer type thing. And the Presentation layer and Session layer are always involved in that as well.

[00:01:01] So that's all part of userspace in Linux where any user can be working. Then we have the kernel. The kernel is the core of Linux. It's the core of the operating system. Here we're talking about layers two through four, the Data Link, Network, and Transport layers.

[00:01:21] And so this is more administrative or system functions in Linux. Things that the typical user can't really mess with too much. And to do anything of importance, you'd have to be root. But generally it's the system that's taking care

[00:01:41] of this itself. So that's the kernel or the core of Linux. And then just like with any operating system, you have hardware, you have your network interface card, and your motherboard, and everything else. That's layer one. That's the physical layer. The connection to your network interface card, whatever patch cable you're using,

[00:02:01] or your radio transmissions on your wireless network interface card. So we have the hardware as layer one, kernel as layers two through four, and userspace as layers five through seven. This is a general representation of the components of Linux as compared to the OSI model.