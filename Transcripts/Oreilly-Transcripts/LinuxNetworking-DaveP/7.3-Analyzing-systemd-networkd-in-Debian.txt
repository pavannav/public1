[00:00:01] - Time for another instructor-led demo. There's no lab document for this one, but I just want to briefly show where you would go to configure your network interfaces if you're working in another system that runs networkd. So for example, here I am in a Debian system.

[00:00:20] And for this particular system, I actually used to run the networkd service, but currently I'm using NetworkManager here. Again, you can check that by doing a check on systemd-networkd. You can see it is loaded on this computer. It's been installed, but it is inactive.

[00:00:42] On my main system here, we are actually using the NetworkManager service. But in the past, I did use systemd-networkd because I have a lot of network interface devices that I needed to work with. And to work with those, you can go to /etc/systemd/network.

[00:01:06] Inside of there, you can create your configuration files that you wanna use for each network interface card. And so for example, I have one here called 10-43.static.network.

[00:01:21] The dot network tells you that it is a network configuration file. So let's take a look at that. We'll do a vim on 10-42.static.network and press Enter. And you can see that this is a fairly straightforward configuration file. It gives the name of the network interface device, ens6f0,

[00:01:42] the IP address, and the gateway address, and also a metric. We'll talk about metrics later when we get more into routing and the ip r command. But that's a very basic configuration file. It just applies the name and IP address. And we have others here.

[00:02:00] We also have 10-41.static.network and 192.168.122 and 192-static.network. We'll take a look at one other one real quick and you could see that this one is given a static IP of .254 with this gateway

[00:02:20] with a much higher metric, which means it has a lower precedence as far as using the network connectivity of the system. Okay, so that's where you would go to view information for networkd-based systems that are not Ubuntu server, that are something else.

[00:02:40] So if you're working in say a Debian system that's using networkd, or if it's Arch or something like that, this is the path that you would go to, /etc/systemd/network.