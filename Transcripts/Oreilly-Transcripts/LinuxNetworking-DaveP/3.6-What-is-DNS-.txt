[00:00:00] - Yet another question. What is DNS? Well, DNS is short for domain name system. It's a worldwide system of domains, hosts, and DNS servers, and it's further defined in the request for comments 1591.

[00:00:20] Let's take a look at that real quick. And here it is at rfceditor.org. That's RFC 1591, Domain Name Systems Structure and Delegation. So feel free to take a look at this and get more information about DNS, in depth information. For now, we'll just talk about it in brief.

[00:00:41] And so DNS servers do multiple things, but their main job is to resolve domain names and host names to the respective IP addresses. Okay? So for instance, example.com is the domain name,

[00:01:00] and the IP address for now is 93.184.216.34. A DNS server will use Port 53 Inbound by default, and that's what it listens on for requests from a DNS client. And a DNS client is any system that's trying to connect

[00:01:22] to a domain or a host and doesn't know the IP address of that domain or host. So here's basically how it works. Let's say we're working at a Linux computer. It's a client system, maybe our Debian client, and we wanna get out to example.com.

[00:01:42] The thing is this, people want to talk and communicate using names. Most people, at least. We don't usually go around using IP addresses, unless you're an admin, right? The average typical user is gonna use names.

[00:02:00] So we have a domain name here, example.com. And so we type that in into the browser. The problem is that computers don't communicate with names. They communicate with IP addresses, and ultimately they communicate by way of a bitstream of information, but they don't use names. They need to use IP addresses

[00:02:21] to communicate at the operating system level. So we need a DNS server to be able to tell the client what IP address example.com is. So the client types in example.com into a browser, and then makes the request of a DNS server.

[00:02:42] If this DNS server knows what the IP address is, it'll forward that back to the client. If it doesn't know, then it'll go out to the cloud. It'll go out to the internet to find another DNS server that possibly does know what example.com is and what its IP address is.

[00:03:02] And there's a whole hierarchy that's worldwide of DNS servers. And so this process can get pretty complex. If we can't find the IP address from the first DNS server, it'll go to the next DNS server and the next one and the next one. So there's a whole hierarchy involved with this.

[00:03:22] Once that IP address is found, that'll get forwarded back, back to this DNS server, that's the local one, and then that'll forward that information to the DNS client. At this point, this system will communicate with example.com, by way of IP address.

[00:03:42] Okay, very good. So at that point, the client can communicate directly with example.com out on the internet. .com, a TLD is a top level domain. So when we connect to example.com, .com is a TLD.

[00:04:02] It's kind of like an extension of a file. So we have .com for commerce, we have .edu for education, we have .gov for government and so on and so on. Every country has their own top level domain. There's lots and lots of them out there. My website is prouse.tech.

[00:04:22] .tech is the TLD less commonly used, but there it is. So that's what a TLD is. It's important to know that. Now, let's talk about FQDNs, or fully qualified domain names. A fully qualified domain name or FQDN is gonna be the combination

[00:04:40] of a host name and a domain name. So sometimes you might connect directly to example.com, but sometimes you might want to connect to a particular host on example.com. And so for example, here we have ns1.example.com.

[00:05:00] ns1 is the host, example.com is the domain. Put the two together and you get a fully qualified domain name or FQDN. In the field, a lot of techs will just refer to this as a DNS name. That's the simple naming for it. But more technically or more accurately,

[00:05:22] this collectively would be known as a fully qualified domain name, okay? So we'll be showing DNS quite a lot during this course. And if you're interested, you might be interested in my building Linux servers course,

[00:05:40] and we'll show that briefly here. That's on my website at prouse.tech/courses. And if you really want to get more into creating DHCP and DNS servers and also directory services in Linux, check out this course. So that'll get much more into depth about DHCP and DNS servers.

[00:06:00] But for now, to summarize, remember, especially the second bullet, DNS servers resolve or translate if you will, they resolve domain names and host names to their corresponding IP addresses.