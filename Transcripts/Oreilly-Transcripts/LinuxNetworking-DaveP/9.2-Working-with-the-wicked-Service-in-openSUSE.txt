[00:00:01] - [Lecturer] Let's work with the Wicked Service and YaST within OpenSUSE. And I have an openSUSE virtual machine here all set up. We just have to log in. Once again, I'm connecting as root and we'll type our password. And there we go. Have a lot of fun.

[00:00:20] So this is OpenSUSE, the open source version of SUSE. If you're gonna use SUSE in the field, you would use the actual version of SUSE because it normally incorporates paid support if you need it. But if you wanna run it on your own, you'd use openSUSE. Okay, so we're here as local host.

[00:00:40] You can see it's in red and we have the number sign. That means we're connected as root. So be careful what you do with root. So the first thing we're gonna do is check the status of the Wicked Service. We'll do that with the systemctl status wicked press Enter and we'll see it is active and exited,

[00:01:02] yet it is enabled. Okay, good. So it works. And we can see that eth0 is up. Very good. If we run an ip a command, we'll see in color that eth0 is using 10.0.2.61.

[00:01:23] So that's good as well. All right. So the service is functional and we've got our IP address listed. You could do an ip r to find out what the gateway is, in this case, 10.0.2.1. And the main thing that we work with

[00:01:42] when we're working with SUSE to configure the system is the YaST utility. That's yet another setup tool, y-a-s-t. So that's what we want to use here. Let's go full screen on this,

[00:02:00] make it slightly bigger and we'll type y-a-s-t or yast and press Enter. And that's gonna bring us into a nice tab delimited system that we can use in SUSE. Alright, so if we want to make modifications here, we can go down to System, then tab over to the right column

[00:02:20] and go down to Network Settings. That's gonna be where you want to go to make your changes. We'll press Enter for Network Settings and that'll read our device configuration. And then we can inspect what we have here, analyze and make changes if we wish to. So you can see here that we have eth0, that's our device

[00:02:41] and you can see the IP address as well. Right now we're in overview that's highlighted here, but we could change where we wanna go with the keys on the keyboard. So for example, if we press the Tab key, it'll highlight Overview for us. And then we could use the arrow keys to switch over to

[00:03:00] our Hostname and DNS settings. And so here we could change the IP address of the DNS server. Arrow over to Routing and that shows our default gateway. So we can modify that. Go backwards and we have some global options where we can set up DHCP and IP version 6.

[00:03:21] And also the service in question. And we see here that the service we're using right now is the Wicked Service. But this is where we would go to modify it if we wanted to use another service. If you needed to install Network Manager, you could do that in the terminal and then you could select that service here

[00:03:42] and use that instead of the Wicked Service. But Wicked works very well. It's wicked fast. And the main options though, you'd be modifying within the overview. So if we tab down, we could make changes here and we could press F4 to edit our IP connection

[00:04:05] and we could tab down and make the modifications here for the IP address and cider and the hostname from here. Okay, but I don't wanna make any changes. I'm gonna press F9 to cancel outta this and F9 to cancel again.

[00:04:22] And it says All changes will be lost. Yes. I have to tab to that and press Enter. And everything else you wanna work with is here in the YaST Control Center. So if we Shift+Tab back to the first column, you could go to Network Services and you could set up additional things like hostnames and proxy and so on.

[00:04:43] You could go to Security and Users and work with App Armor and your firewall and that kind of thing. A lot of the stuff that you see in SUSE is actually based on Debian work and some Ubuntu work. So this is where you would go to make

[00:05:00] all of your configuration changes. When you're done, you can press F9 to quit and that'll bring you back to the shell, back to the terminal. And so one more thing I wanna show here is the path to the network configuration files. And to do that, we'll go to /etc/sysconfig/network.

[00:05:21] And when we look inside there, you'll see the configuration files. For example, ifcfg-ens3 or eth0. Let's take a look at that one. We'll do a vim on ifcfg-eth0 and there you go.

[00:05:40] You can see the IP address and the mode is static as opposed to DHCP. But again, we normally wouldn't configure these files, we wouldn't work with the configuration files, instead you would work with the YaST program and control center. So there's a little bit of SUSE and openSUSE,

[00:06:01] so check it out if you get a chance. I'm running this locally but you could easily run it on the cloud just to see how it works as well.