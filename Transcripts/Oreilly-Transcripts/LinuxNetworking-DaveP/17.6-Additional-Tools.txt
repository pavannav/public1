[00:00:00] - Let's show some additional networking tools that you might be interested in. And we'll start with the iftop program. You might need to install that because you probably won't have it on your system, especially if you're running something like Debian. So we'll do a sudo apt install iftop and install that now

[00:00:21] and then we can run it. Now you may have to use the sudo command to actually run a program like this. And by the way, this software has no warranty. This is software that's outside of Linux, and Linux doesn't have a warranty either, so use this at your own peril. But I just wanted to show some different commands

[00:00:42] and programs that you can work with. So we'll do a sudo iftop, press Enter, and you'll see some information here. This is gonna listen for network connections and show data transfer rates in real time. And here we go. We have a connection from debclient,

[00:01:02] out to mdns. But let's go to a browser window and we'll just open another page here and we'll just go to example.com and go back, and now you can see all those connections from debclientexample.local out to whatever it is,

[00:01:22] whether it's Amazon or Google content or example.com. Here it is. And the amount of data that was transferred and also our data transfer rates. So you get some good information here. Check that out. That's iftop. If you want to get out, Q to exit.

[00:01:41] And you'll note that it shows the current interface you're working in, the IP address and the corresponding Mac address as well. So a nice little program. Now you might've seen that I've been running the top program sometimes, and top is a great analysis tool when you're working in Linux, but it doesn't show too much

[00:02:00] as for the networking connection. So I'm gonna quit out of that. Another program that I'll use is btop, and there's lots of top derivatives out there. And by the way, if you're interested in top and wanna learn more about it, I have an article on my website at Prowse Tech and a longer video using that program.

[00:02:21] So check that out as well. But there's lots of top derivatives. btop is one of them. So let's install it with the sudo apt install btop and we'll grab that and now we'll run it. Oh, our terminal size is too small.

[00:02:41] So let's quit out of here and we'll make it smaller and run btop again, and we should be able to make that a little bigger. There we go. As you can see, this shows a lot of stuff. It shows CPU usage. I only have four cores in here. And then it shows memory usage,

[00:03:02] disk or storage usage, all your process IDs, and some networking information, right? So we have our networking here and you can see how much was downloaded and uploaded for whatever network interface card you wanna work with. So you can modify this

[00:03:20] if you have multiple network interface cards. But this will show you, you can press the b or the end keys to go to the next card or go back to the last card and it'll also show you the IP address as well. So sort of graphical but in a command line way. So btop another interesting program there.

[00:03:40] Press Q to get out of there. The next program is ifplug status. And this also is not installed to Linux by default. So we could do a sudo apt install and it's called ifplugd. Once you grab that, you can run it with the ifplug status command,

[00:04:02] and you may have to run that with sudo as well. There we go. And all this does is it tells you if there's a link. So if you have lots of network interface devices, you can find out if there is an ethernet link. Are you getting an actual heartbeat from the ethernet network from the physical network?

[00:04:23] Yes, a link beat has been detected. That's a lot easier than going to the back of a computer or the back of a server that could be who knows where to see the link light on the network interface card. So nice little program there. The next one is ethtool

[00:04:42] and you need a physical system for this one to make any sense. So I'm gonna jump over to my physical system here and to install it, you do a sudo apt install ethtool. Once it's installed, you can run it on any network interface that you want.

[00:05:01] So for example, we could do this to see all my network interfaces and maybe I want to run it against this guy so I could run that command. I'll run it as sudo ethtool. And we already have one up here. Yeah, let's do that one ens6f0, press Enter.

[00:05:25] And you'll get a lot of information that's based on ethernet, more of the physical type stuff. This tool and ifplug status are gonna give you that more data link layer, physical layer type information.

[00:05:42] For example, supported ports TP for twisted pair. So we know we have twisted pair cabling. It's a standard RJ45 port. Supported link modes. These are the types of duplexing that we can do. And the version that we're doing right now

[00:06:00] is full, full duplex, meaning that this network interface card is sending and receiving at the same time and it's doing so at a thousand megabits per second. Remember that lowercase b means bits and not bites. When we're talking about network data throughput or data transfer, we're usually gonna use bits.

[00:06:23] So really this can send a thousand megabits per second in both directions at the same time. And we do see down here, twisted pair and so on and so on. So you can glean a lot of information with the ethtool. It's a good program.

[00:06:40] Okay, next one. And we'll go back to our virtual machine, Debian client. The next one is bmon. Do a sudo apt install bmon. Grab that program. We'll say yes, okay?

[00:07:00] And these are all available in the Debian repositories, so you can be relatively sure that they have been somewhat tested and won't break your Linux system. So just keep that in mind. There's no guarantees and there's no warranties, but you can be pretty sure that your system will be safe with these.

[00:07:22] Now we can run it with a bmon in command. No sudo required. And this is another command line based, but sort of graphical looking program. And let's open up a website once again. This time let's just open Prowse Tech again.

[00:07:40] Let's do it correctly. This time, let's open Prowse Tech again and go back and take a look and you can see the received information and the transmitted information. And that'll all show up here in this sudo graphical type output.

[00:08:02] Press Q to exit, and Y for yes. And finally, you might also be interested in how to identify network hosts using a variety of tools, especially by IP address. And I have an article and video on my website that shows a lot of different tools.

[00:08:20] That's called how to Identify IP hosts in Linux. You can find that at Prowse Tech/identify/IP hosts. So check that out. So some extra programs there for you to work with. Additional tools for checking out networking in Linux.