[00:00:01] - What is Curl? Curl is a tool to transfer data to and from a server. It works with almost all well-known protocols like HTTPS, FTP, SFTP, mail protocols like SMTP, also LDAP, the lightweight directory access protocol,

[00:00:21] and so on and so on. So it can transfer files using just about any protocol under the sun. Also, it's meant for scripts where you would embed the curl command into your Bash script. And this can be very beneficial when setting up a server,

[00:00:41] that requires different programs be downloaded, and installed automatically. So let's show how to use it to grab a webpage, also download a file, and to run a script. So first, we'll go to our terminal,

[00:01:00] and now I'm gonna use Tylix on my Debian Client virtual machine here just for fun, mix it up a little bit. If you don't have Curl, you can get it, but you can check just by typing the curl command. Okay? If you don't have it, you can do a sudo apt install curl,

[00:01:22] and just verify that it is already up to the newest version, because by the way, in Debian, when you do an apt install, that installs programs in the beginning, but then if you already have the program, it'll check to see if there's any updates for it as well.

[00:01:40] So it's kind of a multipurpose command there. All right, let's make a directory and change over to it. We'll do an mkdir, and we'll create a directory called test-dir, double ampersand and we'll change over to that test directory. There we go. So it's created and we've accessed it.

[00:02:00] First thing I wanna do is show a basic curl of a webpage. And this is kind of similar to Wget. Let's do it now. We'll do a curl on https ://prowse.tech. And what I wanna do is write this to a file.

[00:02:22] Now, otherwise it would put this into the standard output of the terminal. I wanna write this to a file. So we'll do a double alligator, and we'll call this index.html, press Enter and you see the progress of this.

[00:02:40] We'll go full screen on this and I'll take a look, and now we have index.html. Good. And if we do a cat on that, you'll see the entire index.html file, all the HTML for that. Good. So Curl can do kind of the same thing that Wget does.

[00:03:02] It can grab files in the same type of manner, and it can output that information by default, or you can have it write to a file the way we did with that command. We could also do a curl on 127.0.0.1

[00:03:21] to show an example of that. And it puts the actual HTML right in the standard output in the terminal. That's our website. So that shows the HTML that we just wrote. Very good. So that's how you can grab a webpage, a single webpage. But we're gonna use this for a lot more.

[00:03:40] The next thing I wanna do is show how to download a file. So we're gonna do a Terraform download. Previously we used Wget to grab all the Terraform files from a page. Now, let's go back and we'll go back to releases.hashcorp.com/terraform. I just want to grab one particular file.

[00:04:02] So let's go into the latest stable version, and we'll right click on the one that I want, which is Linux AMD64. Right click on that and copy the link. Good. Now, we'll go back to our terminal, and we'll type the curl command.

[00:04:21] And this time we're gonna add some options. We're gonna do a dash, capital L and lowercase O. And I need to state where I'm going to write the file to, and what the file name should be. Okay? So we could place it anywhere, but I'm gonna place it here.

[00:04:40] So I'm gonna do a ./, and I wanna call the file, terraform.zip, because it has kind of a longer name. So I'm gonna make it more simple. Just call it terraform.zip. And then we need that URL that we just copied. So Ctrl+Shift+V pastes that entire URL in,

[00:05:02] and it should grab that terraform 1.6.2 linux amd64.zip file. So we'll press Enter, and we can see the progress very quick. 52 meg, 23 megs,

[00:05:21] and got that pretty quick. And if we look inside here, we will see terraform.zip. Good stuff. Now, what did this do? The dash, capital L looks for new locations just in case the page where this file was being stored has changed,

[00:05:41] and it will follow that if there's an indicator for that at the old webpage. And the lowercase O will write the file instead of writing information to the terminal. And that's what we wanted. We wanted to write this to a file so we specified exactly

[00:06:00] what that file would be. Now, when doing this kind of thing, this is gonna be common in scripts. And another option that you might add is the dash, S option, which is silent mode so that the information won't be written to the terminal. So you may want to use that as well.

[00:06:20] So there's an example of a download of a file, and you could add that entire curl command that we did to a script so that a system that's being set up could automatically grab programs, and then you could have it install those programs as well. Speaking of which, you can use Curl to work

[00:06:40] with pre-made scripts to download information or install programs and so on. So let's show the installation of a Kea DHCP server. And so to do this we would run the curl commands with a couple different options. We would use dash, one,

[00:07:01] s, capital L, F. The one means that we want to use at least TLS version one for security. The S is silent mode. The capital L will look for new locations like we saw before, and the F will fail fast with no output.

[00:07:21] So if it fails, you're not really necessarily gonna know. But the idea is that this kind of thing is run quite often behind the scenes within another script. But here we go. Let's get the access that we need here. We need to put in the URL for this. To grab the Kea program,

[00:07:41] we can go directly to https:// dl.cloudsmith .io /public/isc, that's the maker of Kea, the internet systems consortium, / kea -, and the version,

[00:08:02] I'm gonna grab version 2-4. If you needed a newer version, you could change that here. /cfg/setup/bash .deb.sh. Okay. And then close out that single quote.

[00:08:20] So what this is gonna do is it's gonna grab this sh file, that is a script, that's a Bash script, and it's called bash.deb.sh. Okay. And we'll put a backslash in here so we can go to a new line so it doesn't word wrap, and we'll do a little bit more. We'll do a pipe and run the sudo command and then bash.

[00:08:43] And that means that it's gonna run this Bash script. So that's what we want, and that should run the entire script, which will make sure our system is capable of running Kea, and install any dependencies that we might need. So let's run that now.

[00:09:03] Okay, and it is checking for apt-transport-https. We don't have it so it installs it. It also makes sure we have ca-certificates, and gnupg for security, because you need that to get access to the repository

[00:09:20] for Kea. And so it does all that and we get OKs, and says, "You're ready to rock." Well, I've been ready to rock for a long time now. So let's do this. Now, we have the repository set up, and we can actually install Kea. So we can do a sudo apt install,

[00:09:40] and you have to know the name. It's kind of a long name. It's isc-kea, and then they make multiple programs. I want the standard DHCP4 server. So we'll do a dhcp4-server. And that should be it. Press Enter and that will install Kea.

[00:10:03] So that curl command was very beneficial in grabbing a script that could then be used to set up the system to later install what we need. So you'll see Curl used in conjunction with scripts that need to be run or it could also be run

[00:10:22] within a Bash script. All right, good. So now, we can do a systemctl status on isc-kea - dhcp4-server, and we see it's active and running already. Now, in some systems it might not be active, you might have to start it, but it is active and running.

[00:10:41] If we change over to /etc/kea, and look inside there, we'll see our standard DHCP4 configuration file and we can check that out. Here's some standard DHCP stuff here. Okay, so cool stuff.

[00:11:00] By the way, cd- in Bash brings you back to the last known good directory, which was our test directory. Use it. And so if you're interested in that kind of stuff, and learning more about DHCP, then feel free to go to my website at prowse.tech/courses,

[00:11:20] and you'll see building Linux Servers, DHCP, DNS, and Directory Services. So feel free to check that out. But remember that all of these curl commands can be used by themselves or can be embedded within scripts, and that's how I use it the most. That's the real purpose of Curl

[00:11:40] to work without user interaction. So definitely practice with it, and I'll leave you with one now. Try this on your system. Do a curl if config .me/all, and see what kind of results you get.