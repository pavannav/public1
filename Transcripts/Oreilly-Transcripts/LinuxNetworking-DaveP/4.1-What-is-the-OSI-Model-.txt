[00:00:01] - Yet another question for you. What is the OSI model? Well, the OSI model is a seven layer model that's used to define how data transmission works. It's useful in planning and analysis and troubleshooting of computer networks.

[00:00:22] Whenever two computers communicate with each other, the OSI model is involved. And by the way, OSI stands for Open Systems Interconnection. It's the OSI reference model. So it's a reference model, which helps us to categorize how data communications are occurring.

[00:00:44] If you look here on the screen, you can see we have a computer that's sending information and a computer that's receiving information. Whenever information is sent or received, the OSI model layers are in play. So for example, maybe we're sending an email message

[00:01:03] from this computer. When we do so, that message is gonna go down through the OSI layers, starting with the application layer, then through the presentation layer, session, transport, network, data link, and finally the physical layer

[00:01:21] where the information is sent across the physical plant, whether that's cables or over the air. And then it gets to its destination and it goes up through the layers, data link, network, transport, session, presentation, application. And then finally, that information is either displayed on

[00:01:42] the screen or stored in the storage drive or what have you. That's gonna happen every time we work with the OSI model. In addition, it's all about the TCP/IP protocols. So, for example, when this computer sends an email,

[00:02:00] it's going to most likely use something like SMTP for sending email. SMTP is an application layer protocol, and that's one of the reasons that we use the OSI model. We use it to define how the TCP/IP protocols are working in

[00:02:21] relation with each other. Now, this concept might be considered somewhat outdated by some, but it can still be useful from a networking standpoint. Remember, however, that the whole concept is an abstraction. The OSI model and OSI layers aren't the actual data

[00:02:43] that's being sent and received. Instead, what you should do is you should look at it as the OSI model is a way of categorizing and defining the data that's being sent and defining the protocols that are used

[00:03:01] and how those protocols work with each other. In the next sub lesson will define each of the layers a little bit further.