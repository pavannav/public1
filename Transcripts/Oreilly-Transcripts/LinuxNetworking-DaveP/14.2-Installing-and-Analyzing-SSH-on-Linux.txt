[00:00:00] - Now it's time for a lab. We're gonna show how to install and analyze SSH on Linux. We're gonna make sure it's installed properly, and verify that it's running and check the version of SSH. So let's move over to our Debian client here, and we'll change over to the terminal. Go full screen. And first thing I wanna do is make sure

[00:00:21] that the Debian client has SSH installed. To do that, we'll do an ssh-V, and we see indeed that OpenSSH is installed. We have version 9.2. That's not the latest version of OpenSSH, and I usually recommend using the latest version,

[00:00:40] and you can download that. But this should be fine for the purposes of our labs. And normally you want to have the same version of SSH on both systems, but you can get away with different versions on different systems if you have that. So we have it installed here,

[00:01:00] but the Debian client is gonna be the system that's connecting out to a remote system. So as you remember, the SSH client is gonna connect out to an SSH server. My SSH server is gonna be my Debian server. So let's take a look at that, and make sure that that has SSH installed.

[00:01:20] And that's very important. If it doesn't, then we won't be able to connect. So we do that and we run the same command, ssh-v, and we see indeed OpenSSH 9.2. So it is installed and it's the same version. Good. If, for some reason, you do not have SSH,

[00:01:40] you can install it with an APT install, and you're gonna do openssh-server, and replace your package manager in there. If you're not using Debian or Ubuntu, replace APT with your package manager of choice, whether it's DNF, or YUM or Pacman, or what have you.

[00:02:01] But for Debian and Ubuntu, apt install openssh-server, and you can grab that. If you're working on a system as a typical user account, make sure the user account has administrative privileges, and make sure you preface that command with sudo. Once you have it installed, you also wanna check

[00:02:22] and make sure that the service for SSH is running, and we can do that with the systemctl status ssh. Now, back in the day, you would do it with SSHD, but that's not necessary anymore. SSHD is actually becoming deprecated in a lot of the Linux distributions, and instead, we just go by SSH.

[00:02:43] So we'll do that now and take a look, and we can see indeed it is active, and running, and it is enabled. That's good, and if you look further down, you can see here we have the log information,

[00:03:01] and you can see that the server is listening on all IP addresses, all networks, all hosts on port 22. So it has port 22 open as we showed in the slide, and it's listening for incoming connections. So this server looks like it's ready to rock.

[00:03:21] We could do one other thing. We could do an SS command, and use one of the options we showed in a previous lesson, - tulnw, and when we do so, we'll see the currently listening TCP ports. And you can see here that port 22 is listening,

[00:03:41] not only for IP version four, but also for IP version six. So that is good. Those are the checks that you wanna do on your systems. So you wanna make sure that both systems are running SSH, and that the service is running properly. So we'll check that real quick on the client side as well.

[00:04:01] And it is active and running, very good. So we should be good to go to connect with SSH in the next sublesson.