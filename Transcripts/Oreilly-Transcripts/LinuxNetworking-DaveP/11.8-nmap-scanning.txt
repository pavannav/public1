[00:00:00] - Another instructor led-demo to close out this lesson. Here, I wanna show the basics of the Nmap program. Nmap is used to scan for systems on the network and then also scan open ports and find out more information about those systems. So let's take a look now.

[00:00:20] If you try to run Nmap and it tells you command not found, that means it's not installed. So you'll need to install that on your system. So we'll do that now on this Debian client. sudo apt install nmap -y for yes, and we'll install that now.

[00:00:40] And at this point, we should be able to run commands. For example, nmap -V, and it'll show the version that you're running. Definitely check out nmap.org when you get a chance, if you wanna learn more about Nmap. But let's show a couple things here and show how Nmap can be used.

[00:01:01] The first thing I'm gonna do is scan my network. I wanna take a look at my virtualization network that I'm a part of and see what hosts are available on there. So I'm gonna run the Nmap command. And as you can guess, with most Linux commands, there's tons of options that you can use. I'm gonna use the -sn option

[00:01:21] and I'm going to scan our network, which is 10.0.2.0/24. And when we do, so that starts up Nmap and it should scan three hosts all together. What do we see? We see 10.0.2.1, that's my gateway.

[00:01:40] 10.0.2.51, that's my Debian server, which is up and running. And 10.0.2.52, that is this system, this Debian client. Good. So that's a great way of scanning the network real fast to see what is alive on the network. But that just scratches the surface of Nmap.

[00:02:02] You can go way farther and way deeper and do a whole lot of stuff with this program. One example, we could scan for the open ports for our Debian server, separate computer on this network. And we could just do that with an nmap 10.0.2.51.

[00:02:22] By default, that's gonna look for open ports that deal with TCP. It's not gonna do anything with UDP unless we force it to, and those tests could take a long time. So it's gonna work on the first 1000 ports based on TCP ports.

[00:02:41] And for that Debian server, it found one port is open and that is SSH. The corresponding port number is 22 by default for an SSH server. So you see here it says 22/tcp. That means that this is a TCP-based protocol.

[00:03:00] It's guaranteed, it's connection oriented, and in this case, is using port 22. And we see it's open. So it stands to reason that we could SSH into that system and try to take control of it. Let's take a look at that server. This is the Debian server

[00:03:21] and let's run the ss command with the -tulnw to see the listening TCP ports. And you can see here that it is listening on port 22 and it's listening for any IP address that might connect.

[00:03:41] If we were to connect from the client and then ran this command here again at the server, it would show that client connection and we'll do that when we get into the SSH section of this course. Let's show a couple more Nmap scans to systems on my local area network here.

[00:04:00] And so we'll run an Nmap against, first, the Proxmox system, which is called Parallax. It's at 10.42.0.11. And when we run that, we see the actual host name, parallax.dpro42.local.

[00:04:21] So Nmap is giving us that information to start, corresponding to this IP. It scans the first 1000 TCP ports and it finds that, once again, 22, which is SSH, is open. Also, 111, RPC binds, that's remote procedure call,

[00:04:40] and 3128 for Squid HTTP. So we see there's a couple things that are open on this Proxmox system. Let's scan the other box known as telemetry, which is running Ubuntu Desktop. Press Enter for that, and here, we'll see telemetry at 10.42.0.15.

[00:05:04] Port 22 for SSH is open. Port 4000, which is called remoteanything here. And that's actually known as NoMachine and that's a program I use to remotely control all the computers on my network,

[00:05:20] be they physical or virtual. We'll show that later in this course. Then we also see 8200 for trivnet1, and 9090, which is called zeus-admin. And if you remember, that's actually the cockpit port that we connected to previously when we connected

[00:05:42] from a Debian client to a Fedora server via the browser. So that's known as cockpit port 9090. So we see, wow, that's running on my telemetry Ubuntu system. Very interesting. So you can imagine how much you can find out with Nmap.

[00:06:00] Another similar command that can show all of the hosts on your network is the ip neighbor command, which you can abbreviate down to ip neigh or just ip n. And when we do that, that's going to show connections that we can possibly make on our network.

[00:06:20] Right now, the ip neigh command shows our gateway, 10.0.2.1 and the Debian server, 10.0.2.51, but they're both shown as STALE because we haven't had an actual connection to them in a little while. If I was to do a ping against the server

[00:06:42] and then run that IP neighbor command again, it'll show these as REACHABLE because it had to go through the gateway, it had to communicate with the gateway as well. So it's gonna show these as REACHABLE now. So you get a lot of information with the ip neigh command,

[00:07:00] including resolution to the MAC address. So every host out there with an IP address, it'll show the corresponding MAC address for that system. So another cool command. So definitely check out the nmap command and also the ip neigh command.