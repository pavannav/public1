[00:00:00] Welcome back. Let's continue with the implementation of our custom javascript action. And right now, we're gonna continue the implementation of our index dot Js file. So far, we have done a very simple here, validation of our inputs just to make sure we are receiving and proceeding with some safe input and then we execute the npm update.

[00:00:20] And then if there are any changes on our package touches or the package lock touch file, we will then simply print something on the screen that there are updates available. Otherwise we'll print that there are no updates at this point in time looking over what still needs to be done. If there are modified files, then we should add and commit the files to the target branch and then create a P

[00:00:42] to the base branch using the octo API. So let's focus on topic or 1.4 for this lecture. Let's then scroll up a little bit. And what we want to populate is basically this part here. So we're gonna start by setting up the git credentials. So remember we always have a username and a

[00:01:00] user email here and we can simply leverage the exec dot exec function. And then we'll simply say here, git config global user dot name. And this, we're going to simply say GH automation here. We could say anything else we could actually. And perhaps it's best practice here to define the

[00:01:22] user name and the user email for the automation of our or for our CI automation as an organization secret or as an organization variable and then leverage it in all the repositories. But for us here, we're not gonna go that far. We're gonna simply say user dot email. That's just gonna be github automation at email.com.

[00:01:41] For example, this is just so that whenever we commit, we have some information that this commit was generated by our automation user. If we do not provide this, this is not fail our pipeline, it will still execute correctly. However, we'll have some weird information whenever we look at the author and add the email of whoever created our update commits.

[00:02:03] Once we do that, we can then check out here, we can say await exec dot exec. And once again here, we will say git checkout and then the branch that we want to check out here is going to be our target branch, right? So the target branch here is referred at the top at the target branch here

[00:02:23] and then it's retrieved from our inputs. And for the sake of or for um for us to be a little bit more on the safe side here we can also add another option and say mark this as required to true. Basically, this means that if at any point in time, we come back to our action dot the mo here and we remove the default

[00:02:41] and we keep this as required false. Then for whatever workflows that leverage this action and do not provide the target branch, then this would fail, right? So the required through here simply says, OK, whenever I'm getting the input, if I receive an empty value here, then I'm just gonna fail the workflow. This is another measure of safety.

[00:03:01] We wouldn't have to add this here at this point because we are adding a default in our target branch. So whenever this is not provided, it will default to this one. However, for the sake of here to have a little bit more resilient definition of our function, it's always better to also add the check here.

[00:03:20] Because if later on someone decides to remove this line, then we are also on the safe side for this one. So we're gonna also mark this as required here and the github token is actually required as well. So we're gonna pass this as required um to true. And we can also mark the working directory as required even though we could pass a default of the current directory

[00:03:42] if this is not provided. But since this is a custom action, then we would like to receive the inputs here as as extensively as possible. So we're gonna mark all of them as required. Now, the required here once again, doesn't mean that the user has to provide them at the workflow whenever they are using the action,

[00:04:02] for example here, right? That doesn't mean that they have to provide them here. It simply means that whenever we execute this index dot Js file these input values here, they have to come from somewhere if it comes from our workflow or if it comes from the default values in our action 0.0 that's totally fine.

[00:04:21] We could also mark this not required and then add once again another fallback value here with the R option for example or with the, with the null operator like so, so if it's null or undefined, we would then um provide another value. But I believe that the get input returns only a string. So for hover over here, it just returns a string,

[00:04:40] right? And then uh this means that it will um this operator here is not gonna work for us because this works only with null and undefined. So we would have to use the or here. And whenever we get an empty string, then we would fall back to the default value, right? So this is another possibility just showing you different ways of approaching the issue.

[00:05:00] But here, let's stick with the more traditional one. Um Now the next step that we need to do here, we are checking out our branch and we also need to pass here some more common options. So we're gonna say the current working directory is going to be our working there. And here you can see that we are using

[00:05:20] this current working directory for quite a few commands. And I would prefer to actually define this at the top here as a common exact right. So this here is just gonna contain our current working directory pointing to working there. And then we can leverage this, we can use this here at the bottom.

[00:05:40] Whenever we are using the working directory, we can simply use the spread operator and say the common exact opts. And then if we want to provide any additional options or any overrides, then we can do after the spread operator so that we overwrite the values from here. Now I'm just doing this because if we, if you later on want to extend, for example,

[00:06:04] the the options with some other options that are relevant for all the comments, then you don't need to go changing the code everywhere. You can just change this variable here. So I'm just gonna change a couple of things. And since we will actually execute this command or we will execute several commands here,

[00:06:22] then it does make sense that we have this um shared variable. So now we can, after we check out the branch, we can simply add our package dot Jason and our package lock dot Json files. And we can then commit this.

[00:06:40] We can simply come here and say git commit with the message. We can say core update dependencies, right? It's a very simple message here. We're not gonna try to define how many packages or which packages we are updating. Let's just say that this is a update dependency commit afterwards or the last commit.

[00:07:00] The last thing that we need to do here, we need to change, we need to push this, we need to push this to our um origin and then our branch here that we defined so that the the target branch, right? And then this would be a force push because it might be the case

[00:07:21] that we are running. Um We, we are basically updating this or running this workflow and there is already a branch here. Now, you probably would go for a more complete implementation. If you're doing this for the real life, you wouldn't directly do a force, you probably would do a rebase first. And then if that rebase is successful, then you would push the new changes.

[00:07:42] But here, let's not go too much into the implementation. Since there are already tools that do this for us, we're just looking into how we can implement quite a few interesting functionalities and quite some flexible custom action here with javascript. Perfect. Now, the last thing we want to do here.

[00:08:00] So basically we have done the 4.1 right. So the 4.1 here is done. And the last thing we want to do is the 4.2 which is to create Apr to the base branch using the Octokit API. So we need to fetch this Octokit API. And we're gonna do that after we do all the commits here, we can get the

[00:08:20] Octokit API. And I'm just gonna call this Octokit by using the github. And here I believe we need to import here at the top. So we need to github is equal to require add actions forward slash github and the github package, the actions forward slash github package it has here a function that allows us to get Octokit.

[00:08:43] And whenever you want to fetch this Octokit API here, then you can see that we need to provide a token. This is what the github token is for. So we already have this GH token parameter and this can then be passed here. Let's then just pass the github token here, right? So this github

[00:09:00] token, remember we have written down in our action dot YAML here that it must have access to content to so, right, access to content and to pull requests, right? So if it doesn't have right access, so basically, just not be able to push the code nor to create pull requests. And this is something that we will define in a

[00:09:21] little bit in our workflow for now we're just going to continue with the action and then we go to the workflow and make the necessary adjustments. And there is a little typo here just so let's be consistent with this. So Octokit with C so once we have this API, we can simply say Octokit dot rest dot dot create. And this enables us to create a pull request

[00:09:43] by providing here the repository owner. This is the organization, the repository itself. This is the repository. We want to create a pull request in. We would like to provide the title. This is an optional parameter, but we would still like to provide it. Nonetheless, we simply say update

[00:10:00] NPM dependencies and then we would provide also the body and the body. Here would be an interesting exercise for you to try to maybe define a more complex body in markdown format. But here we're not gonna do that because that would take a little bit too much time. So we'll simply say this pull request updates,

[00:10:21] npm packages. Maybe the interesting exercise would be to try to get the changes from our or within our package lock and then parse these changes and see which versions were updated here and then display this information in a table format. Now, this will be interesting to give it a try,

[00:10:40] but this is not nothing related to github actions. This is would be more a JavaScript or even a shell exercise for you to execute a couple of comments, get their outputs and then parse this into markup. So we're not gonna do that here. Uh We're gonna provide the base branch and this is basically just our base branch. I believe I have mentioned this here on the top.

[00:11:01] Yes, base brunch and target branch, right? So the base branch is the base branch and the head here is our target branch. And perhaps we could even do it a little bit better by changing target to head. And this will be or actually let's keep that for the next lecture. Next lecture, I do plan to do a little bit of clean up and add a few more things here.

[00:11:21] And we'll also do this renaming, right? So this is the information we need to provide. We are still missing the owner and the Hipo. And for this, we can actually use our github dot context dot Hipo dot owner and the github dot context dot Hipo dot Hipo, right? So this will be

[00:11:40] give us the the owner and the repository which are important for us. And here we are accessing some context information from the or via the github package. Now, it might be the case that this pool request call here, it fails because either the pool request already exists or there are few other changes that

[00:12:00] that we would have to implement here on our code for it to fully be like error proof. So let's just add around a catch a try catch statement and we will simply logger. So we're just gonna simply say here, core, not logger core 0.1 right? So we're just gonna do a one here and just a warning, not one. So the name of the function is warning,

[00:12:20] I will simply say here JS dependency update and then something went wrong while creating the pr check logs below. And then here we simply once again call dot warning and we simply see E dot message, right?

[00:12:41] So if this is so this is an error here and the error has a message, we could also just print the whole thing here. Um And we'll just give the error but this might print as an as an object. So let's just print the message for now. And this will just give us basically just some, some message that will, that, that will contain some information or hopefully will contain some

[00:13:02] information depending on what is returned from this package. I haven't done too much of testing to see what this message will contain. So just to be on the safe side, let's then after the message, let's just print the whole thing here. The error in case there is some Stack Stack trace that is interesting for us. All right.

[00:13:21] So I think that's actually this, let's just quickly go through the code. Once again, we are configuring it, we are then checking out the base branch or the target branch. We are then adding our package and package log files, committing, pushing it and then creating the pull request here to the base branch from the head branch to the base branch.

[00:13:41] If there are any errors, we'll just do a warning here. And we could actually also maybe we could even say of warning, let's just add the error, right. So this is an error in the end here. And then we would like to core dot set failed and we could also um I just do it like so, right? So we could also core dot set failed with the error message here.

[00:14:02] And this will be printed and then we will just print the whole error here afterwards, right? So just a little bit better if there are problems creating a poor request, we would like our action to fail. And I'm just thinking here reasoning a little bit, a little bit more about the issue. So we're gonna keep it as it is and part number four is done,

[00:14:22] right? So now we are done with this part number four and then five otherwise conclude the custom action. Then that's it. No big deal here. We're basically just gonna print that there are no problems or there are no updates at this point in time. So the number five is also done

[00:14:41] and we can remove this here at the bottom. And we are, I believe we are pretty much done with the javascript part. So now we can examine what is missing here in our workflow file because the way that it is, it's not going to work for the very simple reason that we are not passing this github token here. If we look into the workflow file, we see that there is no github token.

[00:15:02] So this is not gonna work. If we go back to our index dot Js and we go to the get Octokit documentation, you can see here that the token says the repository personal access token or this github token in capital letters here. This is a very, this, this is better practice if we want to pass something

[00:15:22] that has access only to the current repository to just use this github token here because this is generated by github and it's valid for a single workflow for a single repository. So this is more secure than just creating a personal access token. How do we retrieve this value? Well, this value is already available to us.

[00:15:40] We just can pass this by accessing our secrets context and then the github token variable. If you hover over this, you will see that it provides some some information here. So it is automatically generated for every workflow run is always included in the secrets context. And you can see here more information about the github

[00:16:02] token variable. So I opened the documentation here for us just to have a look at github token because I believe we haven't discussed this extensively um under the github actions, documentation, security guides and then automatic token token authentication. You will see that on the right here, we have permissions for github token.

[00:16:21] And this basically tells us that we can set, we can change between two modes, we can change between the permissive mode or the restricted mode and the restricted mode. If we look here on, on the middle column or on the middle column of this table, then we see that it doesn't have write permission to the contents and it also does not have permission to pull requests,

[00:16:43] right? If you have a default access of permissive, then you have a lot of permissions here. Um But by default, I believe that the restricted is the is the default option. And then here we do not have write permission to the contents nor to the pull request. So we need to somehow give this token permissions

[00:17:01] to content and pull requests from within our workflows. Luckily, for us, this is very easy to do. We just need to add a permissions key here and then define here contents and we'll say right and pull requests and we'll say right as well. And that's all we need to do

[00:17:22] to ensure that this generated github token here contains right permissions for contents and for, for requests. As you can see, that's a pretty easy way for us to extend the permissions of our github token. But once again, the github token is, is valid for the current repository if you were

[00:17:41] to access contents of all the repositories from within this repository here. Then this github token would not, would probably not have access to it and it would have to go with a fine grained personal access token or a github app. Since that's a more advanced, we're gonna, we're not gonna discuss this here. So let's just continue here

[00:18:01] with our action and the action looks good. The action dot EO file looks also fine. We haven't used the debug yet. We're gonna use that in the next lecture and our workflow definition also looks good. So I believe this is a good point in time for us to just check whether there are any changes here. No, everything is looking good.

[00:18:21] So let's add our changes and then commit with whatever message we see fit. I'll simply say add logic to create Pr and so now we can then push this to the remote repository and then go back to github here to try to rerun our action. Let's see if everything is working as expected. We're gonna click on run workflow.

[00:18:44] We're gonna say the base branch is main target branches, update dependencies and the working directory is already pre populated for us here. So let's just click on run workflow. We will need to wait a few seconds until the NP update command completes its execution. So let's wait a few seconds and then we will inspect the output. Perfect.

[00:19:04] So we have an error here. I already know what's the problem? I just want to inspect the output here with you. As you can see all the comments are being executed. No problem here. But then whenever we and, and we are also able to push the changes, but then whenever we want to create a pull request, we run in

[00:19:21] to an issue here that says that the github actions is not permitted to create or approve poor requests. We need to change that in the settings for our repository. So let's try to do that here. The first thing I would like to try is within our repository settings under actions here. And then general, if we scroll down a little bit,

[00:19:42] then we should see that we have this workflow permissions. Now, you can see that there is an option here, choose whether github actions can create pull requests or submit approving per request reviews and this is grayed out for us. We are not able to do that. And this is probably because I'm within the organization, this is not allowed,

[00:20:00] right? So you can see that this is also grayed out. So we cannot change this for the repository only. Let's then have a look at the organization. I'm just gonna open the organization here and go under settings and then on the left side here under actions general, then here we should be able, if we scroll down once again, we should be able here

[00:20:21] to then change the permissions, right? So as you can see, as we discussed a little bit earlier, the read and write permissions are available here, but the default one is just read permissions, the restricted ones, right? So here now we can click on allow github actions to create and approve requests. We're gonna click on save

[00:20:40] and this is the default workflow permission settings saved. So let's go back here. Let's try to refresh this page and see if there was any changes. And if we scroll down at the bottom here, then you will see that this is now actually validated. It's checked, it's selected and we can then deselect. So this is providing the the organization settings here

[00:21:01] are providing us with some default values, but we can then also change this here. If we wanted to not allow for a specific repository, we for us, we actually want to allow. So let's click on save and then I believe that this is all the changes that we need to make. Let's just have a look at the code for our repository here.

[00:21:20] And you can see that we have a new branch, right? So we have two branches. We have this update dependencies branch here, which was updated by our github automation user. And we can delete this branch or we can leave it as it is because we have the force push in our code. So now if we were to come here to actions, let's try to rerun our code and see if everything is working as expected

[00:21:42] as you can see. Now, the action completes successfully. If we scroll down here, we do not have any messages, but we have a forced update to our branch here. And that's because we have the force flag. And if we scroll up a little bit and click on pull requests, then we should see our new pull request here. It was opened by github actions

[00:22:02] and from our default target branch into the base branch here. And then we just have some files, change files changed as well as some description here. This is what we could customize. And if we scroll down a little bit or basically, I believe, I don't know if it shows here the diff the diff in on the browser since this is a large change.

[00:22:22] But then you can basically, if you click here, you can inspect and see what is actually changing on this file. Since the these are lots of lines. This is what I mean by just summarizing the changes here, the versions that are updated and then mark this or pass this as the body of the poor request so that it's easier to check that.

[00:22:41] Perfect. So everything is working as expected. I believe this is a good moment for us to take a pause since we have done a lot in this lecture. Let's take a short break and come back in the next lecture.