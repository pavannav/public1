[00:00:00] Hello, everyone. I just want to take a few minutes to go through the most important concepts in YAML in case you are not familiar with YAML yet another markup language. And we use that throughout the course to define our github actions workflow. So I believe it's good if you have a good basis of YAML to understand everything that we're coding.

[00:00:20] The basic structure of a Yamo file is basically we can think of a dictionary, right? So we have a key and then we have some value here. And that would be for let's say my workflow, right? So this is basically a key and then this key has a value and this value in Yamo, we can think of three primitive values just the basics. The first one is a string,

[00:00:41] the second one is a number and that would be just composed by digits. And the third one is a bull, right? So we also have boolean values, true and false. We have numbers, we have 10, we have 20 then we have strings that can be either written like this, written like this. And if we do not provide any double quotes here that's OK.

[00:01:00] But sometimes when we have special characters, then we might want to wrap them in double quotes or in single quotes. If we have, for example, any double quotes inside of our string, these are the primitive values. But in Yamo, we can also have dictionaries of dictionaries. So we can have also objects. And let's say here, for example, we would say config

[00:01:23] and then we would have, for example, the path and that would be first directory forward slash file YAML for example. And we could have here environment and this is going to be, for example, just staging just one example here. And we can see how we can define dictionaries in

[00:01:40] YAML. So basically this would become under this config dictionary, we then have another dictionary here which is composed of two keys and then each of these keys, they have a primitive value of a string. This is very common when we are defining jobs in github action workflows, for example, because then we need to provide such keys such as runs on

[00:02:02] and then steps here for example, and this would be the name of our job, right? So this is a very common and it's very clean for us to read this in yo. So I do find yo pretty neat language that we can use to create such files. Also important for us to know how to define a race in

[00:02:20] YAML. And for that, we can just simply think of a race like colors, for example. And arrays in YAML have this dash here before each entry. Now we can have entries which are also of primitive value. So red, green and blue, this would be three entries of a string, primitive value.

[00:02:40] We can also mix up the values, right. So there's no problem here. We can have numbers and we can have booleans and strings. So there is no strict type checking here in object. So we could say here, colors name for example. And then we could also say the hexadecimal code which would be FF 0000.

[00:03:03] We can have the same here for green where we have name green, X 00, FF 00. And the same thing for blue here where we have name blue and the Xa decimal code that would be 0000 FF This is just one example of how we can add array elements which are in

[00:03:20] themselves objects and they can have complex structures. And of course, this can be nested as many levels as needed. So within our array here, we could also have let's say labels here where we would have then experimental experimental accessibility or something like this and this by themselves,

[00:03:41] they can also be complex objects and we can start seeing here at the top how the higher it looks like, right? So we have an array of colors here. And then in the first object we have an array of labels and we are selecting the first value of this array. That's pretty much it actually for writing yo files.

[00:04:01] But a very important aspect of YAML is the indentation, right? So always be consistent. If you use two spaces, then whenever you add two spaces, that means that this array here is under this colors key. If we have something like this name, my workflow environment

[00:04:21] staging, this is valid. However, if we were to invent the environment here, then you will see that this is not valid anymore because ye o cannot really understand this for the name key. It's really getting a string as a value. However, we are also passing environment as a child key of this name key. So indentation is what yo uses to

[00:04:42] this child parent relationship and it's very important that we follow this. So if we provide a value for name, then we cannot add any keys as Children of this name key here. But if we remove this value, then we have a valid syntax, right? So indentation here that simply means that in the end,

[00:05:02] this would be something like name dot environment, we can think of a hierarchy like this. And if we were to just add the environment like so then we would access name and a sibling of this name would then be our environment. All right, I think that these are actually all the concepts that we need related to Yamma.

[00:05:20] We're not gonna enter into anything more complex here. Basically, this is all we need to define our workflows. I just wanted to go through this so that all these concepts are clear and you can identify them as we go through the exercises and as we write our workflows, let's then take a short break and come back in the next lecture.