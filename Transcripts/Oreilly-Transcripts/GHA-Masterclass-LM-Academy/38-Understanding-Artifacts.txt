[00:00:00] Let's now discuss artifacts in github actions and artifacts are another way for us to share data between jobs and also store data after workflows have completed so that we can use it later on. This sounds very much like caching, storing and sharing data between jobs. So let's understand the differences and the recommendations regarding using artifacts versus using

[00:00:21] caching artifacts. They are stored for up to 90 days. By default, we can also customize this, make it longer or shorter. They are managed via two actions. The first one is the upload artifact and the second one is the download artifact. Important thing is that whenever we are using the upload artifact action, we can then use the download artifact

[00:00:41] to download that same artifact we have uploaded only within the same workflow. If we were to try to use upload artifact in one workflow and download artifact in another, then this would not work. That being said after we use the upload artifact, this becomes available in github. And we will see that in a practical exercise

[00:01:01] and we can download that by ourselves as well. Artifacts are recommended when we want to store files that are likely to be accessed outside of the workload. For example, build outputs that we later on want to deploy or test results coverage reports that we would like to upload to some artifact registry so that it

[00:01:20] can be accessed by others and also logs. Now over to caching. Caching is stored for a much shorter period of time for up to seven days and it's managed via a single action. The cache action that we have already covered caching is recommended whenever we want to store files that are likely to be accessed only within the workflow. For example. And a very common example are the build dependences.

[00:01:42] We can install them once cash them and then fetch or retrieve this cache, which is a much faster operation. That being said there is no hard line dividing artifacts and caching. They both can be used to store a single file or multiple files and to share this data between jobs. I do like to think that the nature of

[00:02:01] these files defines or determines which one is best. So if we're thinking in terms of like build outputs, test results, logs and other types of outputs that we would like to access later on outside of the workflow and do something meaningful with them. Then artifacts would be my choice if we are just using data within the workflow that

[00:02:21] is necessary for these other operations such as building testing and so on and so forth, such as the build dependencies. Right? Then caching would be my choice. A very common use case for artifacts. Here is the uploading and downloading of build outputs for deployment. So it's very common to use artifacts to store such such build outputs.

[00:02:40] And then we can reuse them, we can download them and use them later on for our own deployments. Now that we are familiar with the characteristics of artifacts and the differences between artifacts and caching. Let's take a short break and come back in the next lecture.