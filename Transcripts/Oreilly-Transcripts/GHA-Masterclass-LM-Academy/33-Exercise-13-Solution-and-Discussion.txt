[00:00:00] Welcome back. Let's now go through our practical exercise for working with output. So let's just create a new file here. Outputs dot YAML name 12, working with outputs on once again workflow dispatch. And then we define our jobs. The first job here is going to be a build job

[00:00:25] and the second job is going to be a deploy job as a real job. Both of them will run on Ubuntu latest. So I'm just gonna copy paste this here and then we can go to our steps definition. Our first step for the build job is going to be to echo something

[00:00:43] to the github output file, right? So here, remember the workflow or remember the process for us to provide an output and then reuse it in a later job. Let's then start with our definition. Let's go step by step here. The first one is going to be to provide a step and then we need

[00:01:01] to provide an id to that step so that it can be referenced afterwards. And here we can simply run something. So echo, let's say, and this here the format that it has, right is a key and then this key is equal to some value. And then this is then appended to the file that is referenced by this github output variable.

[00:01:24] So this is a special variable here, if we want to, we can actually just beforehand for us to see what, what it actually contains. We're just gonna say here, print, get up output path and then we can simply run this and say echo

[00:01:41] dollar sign github output, right. So this here is just going to give us the value of our github output variable. And we will see that this is a path to a file. And then for us, whenever we want to define outputs for a certain job, we can then append key value pairs to this file

[00:02:01] that will then be processed after the completion of the job and then the outputs will be available to any dependent downstream job. So here we can simply say result and this is just one example or status, for example, success, right? So we're just gonna say, OK, provide the information that the build command was successfully executed.

[00:02:24] That's all we need. And then here we can access this down the road, right? So there are a few other things that we need to do here. And primarily in the build job, we need to define an output section. And then the here we can define this could be for example, build

[00:02:41] status, right? And then this here is going to reference our steps dot build dot outputs dot And then here it will be status. Make sure that whenever you are providing the outputs that you want to reference here, you are using the same key that you're providing on the left

[00:03:00] side of this key value pair. All right. So we have here on the build job successfully provided or successfully defined an output which comes from the build step and then is presented as a build status output for any other job that needs the build job. So we're gonna use that in the deploy job. We're gonna say needs

[00:03:20] build. And then here we will define a single step, the name is going to be deployed. And then we can use for example, an if condition here, an if condition and then we could say here the needs context and we have already some autocomplete the build job, then we have the outputs of the build job and

[00:03:41] then we can access directly here the build status, right? So if the build status in is then equal to success, then we can run this here. And we'll simply say echo deploy, right? Another possibility would be for us to put this directly at the job level because the needs context is also available here,

[00:04:03] even though it's defined at the same level here, whenever this expression is evaluated, the needs will be available. If we hover over the if key here, then we see that the available expression contexts include the needs context. So we can safely use that and we can also safely do some equality here.

[00:04:24] And then since we will always output this as a success, then this deploy job will always be executed. But if you want to give it a try, you can also provide here. For example, once again, a boolean input or even a string input that you could use to define the output here of our status, right?

[00:04:43] So, or you could provide a choice input, maybe we could actually do that. So let's just do it, why not? Uh So we're gonna say build dash status and this is going to be a of type choice. And the options here

[00:05:00] are going to be the first one. Success, the second one failure, right? So now in default here, let's just put success, default is success. And this is a nicer way for us then to use a free text input because free text input would then rely that the user provides exactly the necessary or the matching string

[00:05:22] for us. Here, we're gonna go with just using the type choice and then we can already provide the valid options and then any of the two is valid for us. So here, the only thing we wanna do is we wanna do a status and we're gonna do inputs dot build status, right? So

[00:05:40] the build status from the inputs has nothing to do with the build status of the output. They are completely different. So you can call them also something else here. I'm just using the same term, but they are independent. They don't need to be the same in any way. Perfect. So that's it. For this first version of our workflow. Let's commit our changes and let's push this to github

[00:06:02] to evaluate what happens. Perfect. Back in github expanding here on the left and then clicking on 12 working with outputs. Let's execute our first run with a build status of success. And let's click in run workflow.

[00:06:20] Let's refresh once again and inspect the output here. If we click on build, we can then see the steps here. And as you can see whenever we output the dollar sign, you have output, then we have actually the path of the file where the outputs are provided, right? So

[00:06:40] here the the this file here is going to contain several lines containing key value pairs. And then we, we can rely on github for processing it and setting our outputs. If you click here on complete job, then you will see that there is a step here, set output build status. And that means that we are actually setting the value here.

[00:07:03] Now on deploy, once you click here and we expand, then we see that we are running the deploying. And this means that we have successfully retrieved the value from the output of the build job if we come back to actions and then once again, expand go to number 12.

[00:07:20] And now we run the workflow with a failure status. Now, we should see the deploy job being skipped while the build job is going to or because the build job will technically exit with a failure status. Perfect. So you can see here the job has completed successfully. That's because we do not have any exit with a non zero code, but still the status was set to failure.

[00:07:42] And then here in our deploy, we can see that the deploy has been skipped. All right. I think we have discussed enough with this practical exercise if you want to try different ways of, of or, or different values for setting outputs or different operations with outputs, for example, instead of using within the if keyword, using somewhere else in the workflow, feel free to do so.

[00:08:02] But for now, let's take a short pause and come back in the next lecture.