[00:00:00] Welcome back. In the previous lecture, I mentioned that we would use Docker to create our custom action, but I didn't mention which programming language we would be using. So we're gonna be using Python. And in this lecture, I would like to go through a couple of steps that we can do here locally so that we can properly install as well as define all the dependencies for our custom action.

[00:00:21] The first thing you need to make sure that you have installed is Python. And then the version that I have here is 3.12 0.1 at the time of this recording, might a later version if you are watching this lecture a few months into the future, if you have anything from 3.10 onwards, I think that should already be enough.

[00:00:41] We're not making anything very complicated here. Just make sure that you have Python three installed anything from 3.9 or 3.10 I believe is already good enough. There might be some minor changes on the syntax, but I believe that that is not the case, right? So, and the first thing we need to make sure that we have Python installed

[00:01:01] and we would like to create a virtual environment in this folder here so that whenever we install our dependencies, we keep them a local. We don't want to install global dependencies in our Python package. And we also want to avoid using global dependencies because they can basically have conflicts with

[00:01:22] other Python projects that we are using, right. So the first thing is we would like to run the command Python and then dash M VM and then the name of the directory where you want to create this virtual environment. And this will create for us here under the VM directory. I just noticed that I'm not correctly under

[00:01:41] the Dockering URL directory. So let's change into that. I'm gonna say dot github and then the actions and Docker pink URL and I'm going to rerun this Python. You'll see that it has created a VM directory here. And actually, it also has another one here at the top level.

[00:02:01] So we can just go back to the top level and then remove this VM directory. We are not interested in this top level one. We wanna make sure that it's under the Docker pink URL folder. Now that we have removed the directory. Let's once again, go back to the Docker pink URL and within this URL or within this directory here,

[00:02:22] we then would like to install our requests, right? So requests is a module that we're going to use to execute our ping commands. And once we run that, if you already have this cached, in my case here, this is already cached and then it's just using that and then adding it to this virtual environment.

[00:02:41] If you do not have it cached, it will download and install it and might take a little, a few, few more seconds. Now it comes what we are interested in. We would like to freeze what the dependencies that we have installed and we're gonna save them in the requirements dot text file. Once we do that, we will see that there is a new file that was created here, requirements dot TXT.

[00:03:03] And then this contains all the dependencies that need to be installed as well as their exact versions. One small comment here. If you look at the side of the source control, there are more than 1000 pending changes. And that's because we have not ignored this virtual environment.

[00:03:21] This is something that we would like to do. So let's just come here under the dot Ignore our file and we're going to have the like. So now we don't need to commit the virtual environment folder here. This is different than our javascript file or than our javascript custom action where we had to commit the node modules if we are not building everything.

[00:03:43] But here we don't need to commit our local dependencies folder. This is because we're gonna use Docker to install this every time that we build the image and what is important for us is to have the requirements dot txt file. So this is going to be used then by Docker to install our dependencies and then execute our scripts later on.

[00:04:04] Perfect. Now that we have this file here, we can just quickly create a Docker file and this Docker file here, we basically just take from a Python and we're gonna use the alpine and the version is going to be 3.19. So you see there are quite a few here

[00:04:23] and I believe that the 3.19 contains Python 3.12 0.1. The same one that I have installed here is fairly recent. So it will stay actual for quite a bit of time. We will then set our working directory to be just forward slash app

[00:04:40] and we're gonna copy the requirements dot TXT file into the current directory. We then would like to just double check here the version of Python so that we can inspect that later on. And then we would like to run pip install. We can use the option, no cash door here.

[00:05:00] And then we will recursively install the requirements dot txt packages are the packages that are listed in this requirement dot requirements dot txt file. After we have completed that we can copy our main dot py to the existing directory here and we do not have this main dot Py as well. We could also copy everything else, right? So we could copy everything else

[00:05:24] into our current directory. And because we are not committing the VM, then this is not going to be copied once this is run in github actions. If we were to build this file here locally, then we would actually also have to add a dockerignore. And within this dockerignore file,

[00:05:40] we will ignore this virtual environment for the sake of completeness. Let's add it here. I would rather just have it as with the correct setup. So we're gonna ignore our VM director just adding this, adding this as it is, it's good enough. So this if we were to build this this image here locally, run the command locally,

[00:06:01] then we would not include the VM directory. Perfect. And now we would like to have our command here and this is going to be just Python and the next one will simply be forward slash app forward slash main dot py, right? So now we are missing here, the main dot py, we're gonna create that here

[00:06:20] main dot pi. And then we will simply have here very simple. If double underscore name, double underscore is equal to, then we're gonna have here the main and this simply means that the script is being executed directly instead of being executed as a module. And then here we simply say if name is equal main, we will then for now just print here.

[00:06:44] Hello world, right? So we are just executing a Python script. And if we were to just execute it here, Python main dot py, then you see that this is printed on the screen. And that's basically what we will do here with the Docker file, right? So to make sure that our that our setup here is correct, we can now

[00:07:04] add all our changes. Let's go back to our root directory here because we have changed the git ignore file and I want to include that in the commit as well. So let's just have a look at the status, see everything that will be, that will be added. The git

[00:07:20] ignore was modified and then there is the directory here that we will add. So let's add everything. Let's once again get status just to make sure that we are not committing our virtual environment directory. That's correct. Now, we can commit here with whatever messages fit. I'll simply say add action dot yo and Docker file

[00:07:40] and then we can push this to our remote repository. However, before we do that, I just noticed that we have not added a workflow to test this custom action, right? So now we have the custom action here. But if we scroll down a little bit, we do not have a workflow to test that. So let's just take the javascript workflow. We're gonna copy paste here

[00:08:00] and then let's open this copy and then let's rename this. So I'm just going to rename it. Here, we're gonna say, instead of custom actions, JS, we simply say custom actions, Docker and we're gonna name this with a number three. So now we have a couple of inputs here. We can just add here the URL for now

[00:08:21] and we don't need to provide a default. We can or rather let's just provide here, HTTPS, let's use the Google URL and we're gonna just add this between quotes like, so let's now then and change it here, custom actions. Docker

[00:08:40] and we're gonna remove the runs on and we're gonna remove the additional parameters later on. We're going to extend that. We're gonna make sure that we have all the inputs that we need. And the only job that we have here is going to be ping URL and we're gonna use actions, check out and then we're gonna say here ping URL and we're gonna use actions and then this is going to be Docker

[00:09:05] dash, ping dash URL. Let me just double check that the name is correct. Yes, that is correct. And then here, the only thing that we will provide is a URL and this will be inputs dot URL, right? So now we're good to go here

[00:09:20] and we can add this as an additional commit or we can amend this commit that we have just done here. And that's because we have not pushed it yet. If we had pushed it, then we would have to do a force, push and I'm not very I a fan of using force push, especially if you are using trunk based development. But anyway, so git status,

[00:09:40] just checking here that we have added a new file. Let's add this and then we can commit with dash dash amend and then no addit so that we don't change the message now that we have added our, now that we have added our workflow here, then we can actually test that in github actions. So let's push this and go back to github actions

[00:10:02] in github actions under our actions. Tab Let's now here show more workflow. We can see the Docker one here and we can click with run workflow. Let's just click on run workflow and let's see what happens after a few seconds. It should be completed. Let's then expand the pink URL here.

[00:10:21] And you can see that there is already some interesting action happening here. There's a lot of environment variables that are passed to a Docker run command. We have our hello world here and interesting as well. We have this building Docker image group here that if we expand, we will see eventually

[00:10:40] here at the bottom our commands being executed. The Python version that we have is again 3.12 0.1. And then we install our requirements and then we just finish it here by exporting this image, right? So by just wrapping this up and then we can run this here or github actions run this right after.

[00:11:01] Now, if we provide a Docker file as we did to our action dot YAML, so you just say Docker file, then this image is going to be built every time that we run this action or this workflow, every time that we run a workflow that references this action, it will build the Docker image. If this build process here takes several minutes, this can be very annoying, right?

[00:11:23] So this would be one use case for us to publish this in a private or in a public registry and make sure that we have the correct permissions here to then use this Docker image with without having to actually build it all the time for us. Here it takes just a few seconds and that's also why I prefer to use here.

[00:11:44] The alpine version, it's a very light image, just a couple of megabytes. So it's basically just takes a second to download and extract. It's very quick, right? But if you are having a more complex Docker image that needs to be built, then once again,

[00:12:00] I would look into publishing this and then you would have the the build and the publish process in its own workflow, right? You would probably have this image in its own repository and then you would manage it a little bit different. And what we are doing here important for us is that the Hello world is working. So we have our Python script being correctly executed.

[00:12:22] That means that we can now work solely on our Python script and on implementing the ping functionality. Let's then take a short break and come back in the next lecture.