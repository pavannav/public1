[00:00:01] Welcome back. Let's now go through our practical exercise for using expressions. Let's create a new file under the workflows folder. This is 07 expressions dot YAML. And our name is going to be simply 07 using expressions. Let's define an on key and the on key is going to be push.

[00:00:22] And here we also want to add another one. So the first one will be push and the second one will be workflow dispatch like so and then we can define here the jobs key so that we, we don't see the errors anymore. Let's simply call this job echo runs on

[00:00:41] Ubuntu latest. And now let's save the file for formatting. Before we proceed, I would like to define a couple of inputs in our workflow dispatch. And basically one input. This is going to be again our debug and the type is going to be boolean and the default is going to be false. Now remember that when we have a push event here, we cannot define inputs,

[00:01:03] we cannot pass inputs to a push event. So whenever we run this command from a push event, then this inputs the debug here is not going to be defined within our echo. We can then define the steps. And the first step here is going to be print

[00:01:22] debug information or rather let's change this a little bit. We're gonna add debug between square brackets here debug and then print information. And here maybe we can call this print startup information. And then we can have an if key here, remember that if key defines whether this,

[00:01:41] this step is going to run or not. If the value that from the expression of this, if key is truthy, then this step will run otherwise it will not. And here we can simply say we can access once again our inputs context dot debug, right? So if inputs dot debug, then print startup data like so

[00:02:02] and if there are some errors popping up, we can just wrap this with double quotes or with single quotes. And then here we need to provide a run and the run will simply see will simply be a series of statements we're gonna say echo triggered by colon. And then here we can just populate with a few things, right? So github

[00:02:22] dot event name is one, we can also add the the branch that we are triggering this workflow from. And it's going to be github dot ref and then we can also echo the sha commit, right? So commit sha here and then this is going to be github

[00:02:42] dot Sha. Now we can also echo some runner information. So we can say runner operating system. And then we can see here, Runner dot OS and we can also print any other information that we would like to write to runner dot And once I do the dot Here,

[00:03:00] there are a couple of couple of properties available for us. Let's keep it simple for now, let's just add the runner Os, let's add then a second step and this is going to be called, this will also be a debug step. And we say print when triggered from main. And then here the if condition will be a little bit more elaborate.

[00:03:21] And let's just add the run here so that we get rid of the error. And now the expression here will be a little bit bigger than the one above. This is simply going to be inputs dot debug. So whether this is a debug or it will run only when the bug is on. And here we are interested in accessing the ref and if the ref is then equal to graphs

[00:03:43] forward slash heads forward slash main, that means we are coming from the main branch. And then we want to echo this statement here just very simple. I was triggered from main, right? And then here in the end, we can just add whatever we want to. This is where our logic, extra logic will come from.

[00:04:00] Um We would execute some some specific steps here. So simply say hello world for now, right? So Echo hello world. And then we are interested in seeing how these expressions evaluate. Let's then save the the file, let's add everything and let's commit our code back on github. We have a new run here. This is the 07 run.

[00:04:23] And once we click on echo, then we will see that both the steps here that had the if condition did not execute. And that is because if we click on workflow file here, that is because once again, we cannot provide this debug input on push. So it's going to default to a false value

[00:04:40] if we click here on actions and then we select the 07 on the left side, we can run this workflow and we can mark the debug as true. And let's then click on run workflow. If we refresh this page, then we should have the new run. And now this should give us, this should print basically every step in the job, should execute everything.

[00:05:01] Perfect. The debug steps, they have been executed. So this gives us actually a nice way to maybe execute a couple of steps that in in addition, depending on the condition of the configuration that we provide to the workflow. So far, we have been using very simple inputs. Don't worry about it. We'll discuss inputs in more details. Later on,

[00:05:21] we can also try to create a new branch. So if we were to come here on the branch and then simply create the new branch here. We can say test one, it doesn't really matter the name of the branch. And once we click here, then we can actually come to the actions here. And within the action, we already have two workflows that were triggered.

[00:05:40] The first one here is triggered on push. And because we have pushed the new branch, then this has also triggered. And the second one is the one that we are interested. So if we were to click here, we will now see on the run that the step here did not execute. That's also because it's not a debug run. So let's just come here, run the workflow. And from the branch, we need to select the branch here.

[00:06:02] Let's select test one, turn on debug and then click on run workflow. Let's then refresh the page once again and have a look at the results. Now we can see that just the first debug statement was printed was executed. The second one was not right. And that's because here we have an end condition.

[00:06:21] Now, I would like to show you another thing here. According to the documentation, the if key here is already evaluated as an expression. So this special syntax here is not strictly necessary. OK? If we were to edit this and remove this the syntax here, then simply add the contents of the or write the contents

[00:06:43] of the of the expression here directly in the if key, then this will also work, let's commit our changes. And here we are in the test one branch and that's on purpose, right? Because we don't want to integrate these changes back. So let's just commit the changes here and have a look at our actions and we will see that it will execute

[00:07:03] successfully, right? So the steps are not going to run because this is not a debug run, but the run is going to complete successfully. Perfect. The last thing that I would like to show you, let's just do a little bit of clean up. We can come here to the branches, we can delete the branch that we have created. And now let's go back to the ide

[00:07:22] or rather we could also edit this on the browser. Let's click here on expressions and let's edit this on the browser. And it's a very useful feature that allows us to provide some default values in case a certain value is not available, right? So one example here is the input

[00:07:40] and the debug here and this is not available when we are running from push events. If we click again on the actions here, we have a couple of runs from our sixth workflow. The context workflow where debug is not available, basically just prints nothing because they come from a push event. So we can actually provide a default value for such for such situations.

[00:08:02] And one way of doing that is by let's again replicate the brand name here, that's going to be 07 dash using expressions, vertical bar debug dash. And then here we can use again an expression, right? And within this expression, we can use the inputs dot debug.

[00:08:21] And now what happens is we can simply say if the input debug is defined, then print on otherwise print off, right. So this is is a way for us to provide the default value, the default value is off, right. So if the bug is not on

[00:08:42] this expression here, the and, and or expression, so all this expression here will default to the value of, of if the bug is on, if it is equal to true or if it's a truthy value, then this expression here will evaluate as true and the logical end will then proceed to evaluation of the second expression and it will return the

[00:09:02] value of the second expression which is on. So this gives us the value when the debug flag is set to a truthy value. And if the debug flag is set to a false value, then the whole and expression here evaluates to false and then the logical or proceeds to evaluating the value of the second expression.

[00:09:23] This is a little bit of how the logical and and logical or work. This is also true for using such operators in javascript, for example. So it's not something that is too specific of github actions. I cannot promise that this is the case for every programming language. But it's a fairly,

[00:09:40] it's already a known behavior that whenever we have the logical end here, if the first, if the first operand of the end evaluates to false, then the whole thing evaluates to false. And then if we have an or if the first operand here, which is this whole thing evaluates to false, then it proceeds to evaluating the second opera.

[00:10:01] And even though we have it off here, so this is still a non empty string and then this gets returned from our entire operation. So this is a very useful thing for us because it allows us to provide default values. And as you can imagine here, we can also provide default values

[00:10:20] whenever we are defining information for our workflow. For example, we saw the end key in the previous lecture and we could, for example, Myvar with default. And then here we could use an expression and simply fetch the Myvar from our variable. And if this is defined,

[00:10:41] then we would use this value. If this is not defined, we could simply provide a or here and this or would then be the default value, right? So we don't need necessarily to have the end here. Basically, if this value is a non empty string, this will already be the return value from this expression.

[00:11:00] So basically the default value comes with the or and then whatever default value here, the expression here on the top is just a way for us to provide a certain value whenever the expression is true here, whenever the value of the input is true, and then we want to provide some custom value. And then also a default value in case it is false.

[00:11:21] As you can see, expressions can be very useful and they can be used in many different parts of our workflow. Let's just remove this environment variable. And let's have a look at how this behaves whenever we trigger the workflow, right. So now here we are once again in main, once we commit the changes here, then we can just add whatever message we want

[00:11:41] and we will just commit the changes directly to the main branch. Now here on our actions tab we will see how this is going to work and there you go. Now you can see that even though this was triggered by a push event, we still have the value of here because of that default value. If we were now to click on seven and trigger

[00:12:01] this workflow with the debug flag on, then we will see that the new one here is going to have debug equals on perfect, right. So this is again another very useful feature from expressions. And I do find myself using them quite often. So I just wanted to share it with you and explain how it works and

[00:12:20] that's it for this practical exercise. Let's before we wrap up. Let's just do a little bit of cleanup here. I would like to actually change the the trigger change this here to simply have workflow dispatch. Like so let's commit the changes. This is the first one. I'm just gonna copy this because I would like to

[00:12:41] also do the same change for the context one. And once we complete this, we can just commit here with 06 dash context, commit the changes and we are good to go. Thanks for your time. And let's take a short break and come back in the next lecture.