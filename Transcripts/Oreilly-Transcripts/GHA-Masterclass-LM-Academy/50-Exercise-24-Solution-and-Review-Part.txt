[00:00:00] Welcome back. Let's now continue with the implementation of our custom javascript action for that. We will also install two other packages. So let's change into the github actions Js dependency update folder. And we'll simply say NPM, install and actions forward slash exec. And the version we want here is 1.1 0.1

[00:00:22] and the other one is the add actions forward slash github and the version is 6.0 0.0. Let's then also add the save exact flag so that we all have the exactly same versions, make sure that you are inside of the Js dependency update folder. Otherwise the packages will be installed in the incorrect directory.

[00:00:43] Let's then return to our base directory here or to our root directory and we now can continue with the implementation. So I would like to just add a few comments here for us to basically understand what the logic is going to be. And once we are familiar with the logic, then we can step by step, implement

[00:01:03] the comments here or the steps that we would like to execute just to clarify before we move forward, we have installed two packages here and maybe it's not clear what they actually do. The exact package provides us with utilities to execute command-line scripts as well as capture their output and then use them in our actions.

[00:01:23] And the github package provides a lot of interesting and very helpful functionality to interact with the github API, for example, as well as to retrieve a lot of information from different contexts within our action. The first thing we would like to do here is to parse inputs and we do

[00:01:40] need to receive some information from the outside world telling us what is the base branch. So the base branch is the first one from which to check for updates we then have. So let me add this into a new line here. That's the first input we're interested. The second input would be the target branch

[00:02:01] to use to create the PR right. So if the target branch is, for example, dependency updates or some other target branch that we, that the user would provide to us, then we will use that as the branch where we will commit our updates. We then also need here a github token

[00:02:21] because since we will create new PS here, then we do need to execute some functionality. We need to reach out to the github API and we need a github token for authentication purposes. And this is to create PRS for example. And the last input that is relevant to us is the working directory

[00:02:42] for which to check for dependencies, right? So here we are in a in a repository containing multiple projects. For example, you can think of each of these individual directories here as different projects. And this very much resembles, for example, a monorepo right, where you would have a different projects within the same repository.

[00:03:02] And maybe you want to check dependency updates only for a single director, especially in mono hippos, you might have different languages within the same directory. And then it doesn't make sense for us to check for all of the directories assuming that they will all use npm projects, right? This, this might not be the case.

[00:03:21] So it's always good to allow to provide a certain and individual working directory so that we can check only for that directory. Once we have the working directory here, then we would execute the NPM update command within the working directory,

[00:03:40] right? And then the NPM update is basically just going to tell us whether there are updates within our project. Let's execute this NPM update command locally here within the 17 custom actions and then the React App folder. And let's see what's the output. So once we execute this and wait a couple of seconds,

[00:04:01] let's see what it prints on the screen. Once we execute that, when we scroll up a little bit, we will see that we have added 29 packages, removed some packages, change others. And then if we scroll down a little bit, we see that the package log file has been modified, right? That means that several of the applications or several of the dependencies we had here

[00:04:21] were updated to the latest allowed version based on whatever constraints we set here in our package to. So now a configuration of a React application there is a lot happening under the hood here. So not all the dependences are listed in the package Jason of our React app. But if you were to start digging into some of the node modules,

[00:04:42] the React script module here, then you would start seeing that within the React script. We will actually have another package dot Jason. Let me just scroll down a little bit for us to find it here within our React scripts folder here. Then if we expand it, we will see that there is a package dot Jason. And if we scroll down a little bit,

[00:05:00] then we start seeing actually all the dependencies of our project, right? So there are several 100 even more than 1000 dependences here. And running the npm update is going to make sure that we install the latest available versions considering the restrictions here. Maybe this is not enough for you, maybe you would like to instead of just

[00:05:21] checking for the versions given the existing constraints, maybe you would like to actually check for the latest available version even if there is a major update. That is the case for using for example, another package here which is called npm, check updates. And this will then check available updates for all

[00:05:40] the packages in our package dot Chase on file, right? So here we can see that there are some major versions as well as minor versions and patch updates. And this is a relatively more comprehensive way of running it. If you would like to check for the latest available versions of the packages, you can actually this command with NPX npm, check updates and then this will just replace the npm update

[00:06:03] that we will execute. For the sake of simplicity. Here, we will stick with the npm update but feel free to try it on your own and also see if it works correctly. Once we execute the npm update, comment within the working directory, we then need to check whether right. So we need to check whether

[00:06:21] there are modified package star dot JSON files. So basically the package dot Jason or the package lock dot Json files. And then if there are modified files, create APR otherwise conclude the workflow or conclude the custom action.

[00:06:42] All right. So here, basically we will check whether there are modified package JSON files and if there are, then we're gonna create APR using the target branch and the PR is going to be to the base branch using the target branch. And here we can also be a little bit more specific as well. I think that we can also

[00:07:02] write it, write this down as a series of steps. So let's say if there are modified files like, so then we're gonna just basically 4.1 here, add and commit files to the target branch. And then here create APR to the base branch.

[00:07:21] And then here we would actually using the Octokit API this, we can retrieve this Octokit. That's the github API, we can retrieve that via the actions forward slash github. The one that we have installed at the beginning of this lecture. Now here, otherwise, so if there are no changes, we will simply

[00:07:41] conclude the custom action. These are all the steps we need to execute. I believe we have done already a more comprehensive discussion than I had in mind. So let's add our files here. And before we do that, I actually want to revert the changes to our custom actions, the react application here, we can do that by running the git checkout

[00:08:04] and then package lock dot json. And once we do that, then the file will be reverted to its previous version basically here because we have not committed the changes. Then this will just undo the changes we have done with the NPM update.

[00:08:21] And then we can now run NPM C to ensure that we have the versions or the packages in our previous package, the dash lockfile installed locally. It's not strictly necessary that we run this command here because we have, we, we are not running anything locally in our, in our React app. But it's always good practice whenever you change the package, lock to

[00:08:42] Chason to run a clean install, to ensure that we have all these versions here. The exact versions as defined in the package lock, the Chason installed locally as well. Once we are done with this change, let's come back to the root directory once again. And let's have a look at the changes that we have to commit. So all of them are within

[00:09:01] the github folder, the github folder, and that's exactly what we want. So we have a few files that we have modified from our previous commit as well as some new files here that are not committed yet. Let's just add everything and let's then commit with whatever message you see fit, install missing packages

[00:09:21] and provide instructions for implementation, right? So now we know exactly what we have to do. We know the steps we need to implement. And if you would like to give it a try to implement it by yourself before watching the upcoming lectures, then by all means I highly encourage you to do so. Otherwise, if you just want to go through the exercise with me,

[00:09:41] then let's take a short break and come back in the next lecture.