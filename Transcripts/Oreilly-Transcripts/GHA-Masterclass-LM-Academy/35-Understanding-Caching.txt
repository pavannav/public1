[00:00:00] Let's now discuss how caching works in github actions and how we can leverage it to speed up our workflows. So caching allows us to store files and later retrieve them based on a key. Now, workflows can access the cache from their branch or from the default branch. In the case here, the main branch, the

[00:00:20] caching process is pretty much like standard caching process. And if you are familiar with caching keys and so on, then you should be familiar with what's happening here. Basically, let's have a look at the diagram and see how it works. So the workflow run starts, we come here to the steps and to the first step and then the the cash step will evaluate right. So here we are using the cache action

[00:00:43] and then we need to provide some parameters via the width keyword. And the first one is the path that we want to cache. And the second one is the key for the cache. And so if there is a matching key, so if there is a matching key, then we will restore the cache and we're going to skip this step here

[00:01:01] and we'll proceed directly to link and testing. However, if there is no matching key, then we will execute step and then only then proceed to executing linking and testing. Here, we can see whether there was a cache hit via the output of this, of this previous step here, the cache hit if it is equal to true,

[00:01:21] that means that a key was found or a cache for that key was found and then it's going to set the cache hit output to true, right? So the package log file is normally used npm project. Because if anything here changes, it means that any of the dependencies, either direct or indirect dependencies have changed.

[00:01:41] And then we need to re install. One very common use case is to really catch dependencies to speed up the execution by avoiding always having to download and install them. So sometimes there can be quite a few minutes necessary to, to download and install all the dependencies. And if we can cash and quickly retrieve them,

[00:02:01] that is much faster, especially because the dependencies are not something that change very often with every commit, right? So we can benefit quite a few quite a few times from using this caching function. Before we wrap up this lecture, I would like to discuss a few things. I'm on the on the documentation here about cash independencies.

[00:02:21] And the first thing I would like to discuss is that the documentation says that if we are caching package managers listed below, we can use their respective set up actions to require minimal configuration. Now, this is slightly misleading from my perspective because they don't really cache the the the installation folders of the dependency.

[00:02:42] So for example, this set up node here doesn't catch any node modules folder which means that it always re install the dependences. It does catch a few things but it's not caching everything. And then we still need to, if we want to catch the node modules folder, we still need to implement the logic ourselves.

[00:03:02] And the second thing I want to discuss is about providing partial keys or or a partial match for restoring the cache. It's possible for us to specify more than just the key parameter here. And if we were to actually want to restore the cache based on other keys,

[00:03:21] we can use this restore keys option here because then this allows us to basically go and provide other options for the cash function or for the cash action to download the cash even if there is no exact match. That's it for this theoretical discussion about caching and how to use this in our actions. Before we go to the practical exercise.

[00:03:42] Let's take a short break and we will come back in the next lecture.