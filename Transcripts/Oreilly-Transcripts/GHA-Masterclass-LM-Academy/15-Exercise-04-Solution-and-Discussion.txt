[00:00:00] Welcome. And let's now go through our practical exercise to understand how we can use actions in our github workflow. Let's start by creating a new file here. We're gonna call this 0 4 dash using actions dot YAML. And as always, let's give it a name. It's going to be 04 using actions

[00:00:20] and let's trigger this to begin with on push. We're going to have a single job. So let's specify the jobs keyword. This is just going to be built. And here we would like to run on B two latest and here we would like to do a little bit of work with a React application that doesn't exist yet.

[00:00:40] Don't worry, we're gonna, we're gonna spin it up soon and don't worry, you don't need to know anything about React. And if there are any doubts, don't hesitate to use the question section. But here we're going to keep things simple. The focus is not react. What we want to do is we want to install the dependencies of our application. So I'm just going to write this as comments here of our React application.

[00:01:01] And the second thing we would like to do is we would like to execute automated tests because it's a very common workflow here. Very common sequence of steps. We want to install the dependencies, then we want to run automated tests. And if these are successful, we would like to, for example, build and publish the application. Now, we need to have a react application and how we're going to do that.

[00:01:23] We're going to simply create a new folder at the root level that is going to be called 04 using actions. Just so we know that this relates to our workflow here and let's change into this directory 04 using actions. And here we're going to use the Create React app utility.

[00:01:40] So you need to have node and NPX or NPM installed in your, in your machine. The version that I'm using here is version 20. If you do not have it installed, I do have a video earlier in the course where I go through the steps to set up node and NPM. So if you go through that, then you should be able to have node installed and you should be able to also use NPX.

[00:02:01] So we want to use the Create React App utility here. And I like to use the typescript template. It's my own personal preference here. I prefer to use typescript whenever possible. And we're going to just create this inside of the React app folder. Let's do it like this. And once we do that then it will need to install this package. We need to type

[00:02:22] here. Now, this package is not going to be installed globally, but it does need to be install behind the scenes so that we can set up. We can use the package without having to run a global install. I always prefer to use NPX instead of installing it globally and then calling it via the command-line. This is again personal preference, but there are few advantages to that.

[00:02:43] Once we run this, we need to wait a few seconds, perhaps minutes so that all the dependencies are installed. At the end, we are going to see a screen saying that everything is installed here and we test if everything is working properly. Perfect. So if everything was successful, we will see here a success message in our,

[00:03:03] in our console and we will also see a couple of useful comments just to make sure that everything is working. Let's change into our React app. And if you look here on the side, we already see the files. Let's not get confused, right? So we are still within the React app folder. It is a little bit tricky from

[00:03:20] from visual studio code. Sometimes this gets hidden if you have provided here in our NPX command. If you have provided this section here, React app, then it's going to create the React application inside of this folder. If you do not provide this, then it's gonna create in the existing folder as soon as as long as it's empty, right? So

[00:03:41] here we are inside of React app. So now let's change into React app and let's run the NPM, run start comment, this should start our React application and we should be able to access it on the browser. After a few seconds, we have the address that we need to access here to visualize our application.

[00:04:00] And if we just open this address here on the browser, then we should be able to see that there is a minimal React application, right? So if you see this screen or something similar, right? So this already means that you have set up your React application and you're good to go with our github workflow section of of this lecture. Perfect. So back here in our in our ID, we can stop the application.

[00:04:24] We don't need to have it running. Let's just clear this and go back here as well as to the root directory. You can see that node modules is excluded here. This is from our very first setup lecture where we added the node modules to the get ignored.

[00:04:41] This is intended because we don't want to commit this just a small comment right now. We would like to proceed with implementing these two steps here that we have written as comments. However, there are a few things that we need to do beforehand, right? So the first thing that we need to do is we need to check out the code from the repository.

[00:05:01] So even though the workflow is defined inside of the repository, the code of the repository is not available by default to the workflow. So the workflow does not download does not check out the code from this repository, even though it is stored inside the repository. And this very simple because you might have a workflow that has nothing to do with the repository.

[00:05:22] But workflows, remember workflows have to be defined inside repository. So the first one is we need to check out the code from the repository and this is going to be our first step. So first step here is gonna be called check out code and this is going to use

[00:05:40] an action and this action is called actions forward slash checkout. And the version here is going to be version four. It's recommended that you also use version four so that we are on the same page here. But if there is a version five or later version, by the time you, you're watching this course, I'm gonna make sure that everything we discussed here is compatible with later versions. And if it is not, then I'm gonna provide some update lectures as well.

[00:06:02] So the first thing we need to do is we need to check out the repository. Once we do that, let's start by simply printing the directory structure, right? So printing folders and here we just want to run LS like. So let's see what's the output of this in github actions for that. We need to add all our files including the files

[00:06:23] here under our zero for using actions react app directory. And we can then commit here with whatever message you see fit once we commit that this will be a few more files than our previous commits. But we can then safely push this to our remote repository. And this is probably because I restarted my computer.

[00:06:41] So let's just make sure to add this identity here dot SSH forward slash udemy profile. And now we should be good to go to push once we do that. Let's come to the X tab here in our repository, the first version here or the first run. And then if we click on build and we print the folders here, then you will see that.

[00:07:01] Now we have the folders available at our working directory. So this is because we are checking out the code and you can see here that there are quite a few things that are done by this action for us. So we don't need to worry about anything. The interesting part here is fetching the repository and this is just executing git fetch.

[00:07:21] So you can have a look at each of these log log groups and you can see what this action is actually doing. So there are a few things and if we were to replicate that it would be quite a few lines of code. Interestingly or interesting for us is that we have now the files available. So we can actually run our commands inside of this folder,

[00:07:41] we can remove this and we can now here before we run our installation script, we can set up our node environment. The setup node stuff is going to use as well, actions forward slash set up node. And the version that we will use at this point in time is version three. And here we can pass as we saw in the example on the slides we can pass a node version.

[00:08:04] So this is going to be node version. And here we will simply take the latest version from the 20th major release, right? So whatever latest minor version is available, this is going to be used to set up node Js, let's now install the dependencies,

[00:08:20] this is going to be called installed dependencies. And here I'm gonna commit this with a with a common mistake that we do. And it is just to simply run npm CI, right? So our first tendency would be OK. Let's just run npm CI let's add this. I know that this will trigger this will throw an error. So let's commit this. And let's see what happens on github actions.

[00:08:43] Once we do that, let's just push the changes and go back to github actions here in our page, clicking on actions and then on our 04 using actions we already have it here. And let's have a look at what happens on our script. So it's now setting up node, you see that it's taking

[00:09:00] the node version 29 and the NPM version 10.1. And here you see that there is an error in installing the dependencies. And basically this is saying, hey, it can only install the dependencies if there is an existing package lock dot json or an npm shrinkwrap dot json, right? So here it's trying to run this NPM install or this npm clean install command.

[00:09:25] But the problem is that we are running this in the root directory. And if we look here in our code, the root direct directory does not have any, any package logs. Not where our React application is actually live because our react application is running is living inside of zero for using actions

[00:09:44] React app. So if you click here, this is where our package log file is present. We need to change that in our code here. And for that, there is a very useful command. There is a very useful key here that we can use. It's called working directory and the working directory we can simply provide

[00:10:02] here 0 4 dash using actions forward slash React app. Now this is going to execute all the commands or all. Basically this run command here, this is gonna execute this run command inside of this working directory, right? So instead of doing something like this, which would be valid if we were to do something like this CD

[00:10:22] 04 using actions React app and then npm C this is also valid. But here it's more clear what we are doing and what we need to where we need to run this command. If we use the working directory key. And a bit later in this lecture, we're gonna see a nicer way why this key is useful for us.

[00:10:42] So let's change this here. Let's add our changes or let's commit our changes and let's push them to github to see the working version of this installation script in our actions. Tab If we click here, then we should see that this is going to execute successfully.

[00:11:00] It will probably take a few seconds or minutes. But I think here it's gonna be in the in the realm of seconds to install the dependencies. But if we refresh this, we should see that the script is already running here for 18 seconds and there are already some logs which means it's executing correctly. Perfect. Now all the steps have completed, you see here that it took roughly 30 seconds to install the dependencies and

[00:11:21] we have a successful workflow run back in our in our workflow file. We can then after we install the dependencies, run some automated tests. I'm opening here the React app folder and within package dot JSON file. If you are not familiar with npm projects, we can find here under the script section.

[00:11:40] This defines all the scripts that we can run if we are, that we can run by prefixing them with NPM run and then the name of the script, right? So NPM run start is a valid script NPM. Run build NPM, run test and NPM run eject. So here we're going to come to our workflow file and we're going to add a new step.

[00:12:02] It's going to be called name, run unit tests. And we want to run here simply npm, run test, right? So this has the same problem as the previous step that we need to provide the working directory. Otherwise this is gonna run in the home, let's say quote unquote directory of our virtual machine.

[00:12:22] So we need to provide the working directory here as well. Now, this is a little bit of a repetition and we probably want to extract this to somewhere else and we can do that by using a very nice keyword here called defaults in our job definition. So this keyword defaults here is at the job definition level,

[00:12:42] meaning it's sibling to runs on and the steps. And then within here the defaults we can say run and then within run, we can give our working directory, right. So now what's going to happen here is whenever we execute this run command

[00:13:00] in any step, it's going to default to having this working directory as well. So now we can remove this from here and remove this from here. This is a very useful way for us to define some configuration that should be shared between different steps in our same job. And we don't want to include that as either part

[00:13:20] of the shell script that we are running here or individual in the configuration of each of the steps. If we were to change this working directory to something else, then this would apply to all the steps that are executed in this job. Perfect. So I believe this is already our working configuration. Let's save this and then let's add everything,

[00:13:40] commit and push this to the remote repository. After we push that inside of our actions that we should have our new run. And let's have a look at the output of this run. After a few seconds. We'll see that all the steps have completed successfully. And we can see here, install dependencies, the run with NPM C as well as the unit tests here.

[00:14:01] And this executes simply executes the NPM run test inside of our React App directory. And then this has a single test here. If you're interested and not familiar with the structure of a default React app, then you can just have a look inside of the source folder. There's an app dot test dot TSX and it basically checks

[00:14:22] whether this this React application is default React application has the text Learn React. So that's it for our exercise regarding using actions. Once again, this is our first encounter with, with actions here you inside of workflows. Don't worry about it. We're going to use them over and over again

[00:14:41] in later lectures. But the focus here was to understand how we can then include this out of the box functionality so that we can make a workflow that is shorter, more concise and doesn't repeat unnecessary code, right? So our focus here was on the checkout action, which is important if we want to use the code of our repository

[00:15:00] and the set up node action, which is important to set up node with a specific version. Remember that node is available by default in Ubuntu. Latest it is available. It's version 18 at the time of this recording. But here we can then fix a certain version so that we don't run into any compatibility issues, right? So here I'm just to clean up,

[00:15:21] I will remove these comments and let's change the push to workflow dispatch as always so that we don't trigger any unnecessary runs. Let's commit our code and then push this to the remote repository. And with that, we reach the end of this practical exercise. Let's take a short break and come back in the next lecture.