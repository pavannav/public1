[00:00:00] Welcome back. Let's now talk about functions and how we can leverage them in github actions. So github actions provides some out of the box functionality that we can leverage and use it to model complex behavior. There are two types of functions that we can think of or at least the ones that are available, we can divide them into two types for better understanding. The first one is the general purpose

[00:00:21] functions and we can think of them as as just a set of functions to interact with data from multiple contexts, right? So we can use this to perform certain checks or certain transformations. A couple of examples are these functions here. The contains which will examine whether one argument another argument

[00:00:40] you can think of it in terms of strings, for example, starts with and ends with these are self explanatory. It's just gonna examine a string, whether it starts with a certain sequence of characters or ends with a certain sequence of characters. The from JSON function, which is very useful when transforming a certain string into other data types. And you can actually use this to

[00:01:04] derived for example, full blown objects from strings you can store the strings as JSON format and then use this to parse this string back into json. So this is fairly powerful as well as the two json function, right. So this allows you to transform certain pieces of data into a

[00:01:23] JSON string. This is one use case for the from JSON function, we can use this to convert variables from the string type to different data types, right? So when we have environment variables in github actions, they are by default of the string type. So if you would like to have a boolean or a number,

[00:01:42] they will be of type string in their default format. But you can then use the from JSON function to convert between this string type and different data types. We also have the status check functions and these functions are somewhat special functions within github actions that allow us to use the status of a workflow previous jobs

[00:02:04] or steps to define whether a certain job or step should be executed, right? So this are four functions that we have. The first one is success. The second one is failure. The third one is always and the fourth one is canceled. And they mean basically this success here is if everything before

[00:02:20] um the the job where this function or the step where this function is called has been executed successfully, the failure one will return true in case any of the previous steps or jobs has failed. The always one will always return true and the canceled one will return true if the workflow has been canceled.

[00:02:40] Here is one example of using the canceled function or rather not canceled here. And this is a good practice when we want to execute jobs or steps, even if previous jobs or steps have failed. So instead of using the always function, which is a tendency we might want to follow. It's actually recommended that we use not canceled so that this function is executed

[00:03:02] if anything before fails. And if the workflow hasn't been canceled, if you want to execute a certain step or job, even if the workflow has also been canceled, then you would think about the always function before we wrap up this lecture and proceed to the practical exercise. I just wanted to spend a bit of time

[00:03:20] on the documentation page from github so that we understand all the functions that are available and you know also where to look for some documentation later down the road. If we scroll down a little bit, we can see this is the contains one that's the one we have discussed and we can provide a search and then an item, right? And these, if the search contains the item, then it will return true.

[00:03:42] We can also pass an array here and the function will return true if the item is an element of the array. Now, why I'm showing you this is because there are a few pieces of information here in the documentation that are used. For example, this function is not case sensitive, right. So if you were looking for a certain substring and relying on case to actually

[00:04:03] match this and if the case is different, you would expect a false, then this function is not the best one to use down. Here. There are also a few examples of how we can use the function with a race. And we can actually use the from JSON function to transform a string into a Json object, right. So this will be transformed into an actual array.

[00:04:25] And then we can use the the context github dot event name. So the piece of information, event name from the github context and then we can compare this instead of having a larger check here. This will also be possible but there is a um a shorter way from doing it.

[00:04:41] And it's this one, you could also store this, for example as an environment variable so that you can use it in more places. And if you need to update this later down the road, then you don't need to go through all the the extensive checks here and extend them with an additional or right. So this is another use case for having this stored somewhere else and then you can

[00:05:02] extend it as needed starts with and ends with I think are pretty self explanatory here. We also have the format and then we can use this function in case we want to format some string. This is useful whenever you are writing something to a log or whenever you are printing something that has

[00:05:20] a more complex piece of information. So it's possible to do that. Just it's useful to know that it exists. We have joint function to join a race with a certain optional separator. If you don't provide anything, the default is a comma, right. So with the two Json function and the from Json function, we have already discussed. And if we

[00:05:41] scroll down a little bit, we have a few examples here of how we can use the two. And the from JSON, I would like to have a look at this one here because this one actually touches upon what we have discussed previously regarding converting a certain environment variable to another data type.

[00:06:00] So if you see here, this continue to true and time to three, these are actually stored as strings in environment variables even though in we can use integer. And we can use booleans. These are data types supported by YAML. Whenever we are running the workflow, whenever this is then populated in environment variables or in the

[00:06:21] context, they are transformed into a string by github actions, right? So to convert them back to their original type, we can use the from JSON function. The hash files function is also very interesting because it returns the same hash for the set of files that match the path pattern, right? So if you want to get a certain file, so for example, let's take the package lockfile.

[00:06:42] If you, if the hash changes, it simply means that this package Lock dot Json has changed, right? So if we one use case here would be to use this hash files as the key for a caching feature. And we will do that when we discuss caching in github actions. So this is one use case and there are many others.

[00:07:01] So just keep that in mind the hash files, it can handle not only a single file but actually a set of files, right? So anything that you need to do um or to check whether the contents of files have changed, then the hash files is probably a good place to start and then we come to the status check functions. We have discussed all of them

[00:07:21] and this is basically how you use it, right? So you use an expression here and then you simply use the function names of success or always as we want to use here. So the canceled one once again, this is recommended to use, not canceled instead of always if you want to execute a certain a certain step. And I think the documentation here

[00:07:42] um also highlights that and it provides an alternative here instead of using the always perfect. So these are the functions here. There are no additional functions. There is also a nice action on object filters if you would like to work with more complex data types,

[00:08:00] right? And this also includes, for example, if we scroll up a little bit and go to the contains function, this also includes an example here where we would look at the labels of a certain of a certain issue. And whenever there is a possibility of having an array, so either labels or assignees anything that you can think of,

[00:08:21] and you would like to return a certain array containing one of the properties of this, of this array here of objects as the actual values. So this star syntax here objects filter can be used for, right? So think of it here, we have an array, an array of objects and then each object has a property.

[00:08:40] And then we would like to return just the values of the property as an array. Then we can use this fruits dot star dot name here and this will return all the names for us instead of us having to iterate over this entire object. So this is fairly flexible since you can also use it on objects. And I would like to share that with you that it exists.

[00:09:00] So that whenever you come across this use case, then you know that you can use this syntax. Perfect. We have discussed quite a few things about functions. Let's take a short break and come back in the next lecture.