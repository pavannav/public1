[00:00:00] Welcome back. Let's now discuss matrices in github actions. Matrices allow us basically to run several variations of the same job. Why is this important? Well, because maybe we want to execute the same thing, but we don't want to copy the job over and over again just with small changes in its configuration. So one way for us to address this is to add the strategy option here.

[00:00:24] And then under strategy you can see there is a matrix and then we provide a series of key value pairs. So each key here will become available under the matrix context. So you can see here the OS is the same OS here and the node is the same node as here.

[00:00:40] And then each value is going to be added to a combination. How will github do that? Well, basically it's just gonna take each value and combine with all the other values. If we think of a mathematical notation here, this would be kind of the Cartesian product between these two arrays. So between this array and this array. And so the result is going to be something like this.

[00:01:02] The first element is coming from the operating system. And the second one coming from the node version. And here I'm just illustrating combinations here for us to, to match the name, right. So this name here, we can then match it here. It doesn't follow the order that we specify first node and then operating system.

[00:01:20] This is not really important in terms of the combination is rather how we use and how we compose these values, right? So for the names here, basically this would lead to nine jobs where the 1st 31 will have Ubuntu latest, fourth to the sixth, we have mast and then the final three jobs will have windows latest and then it will alternate the node version.

[00:01:42] So as you can see, we can very quickly generate lots of jobs by using this matrix strategy. And then we can leverage this here as you can see via the matrix context. The matrix context basically allows us to retrieve the values here and then we can use them. So here's one example that we set the runs

[00:02:00] on based on the operating system of the matrix. And then the node version as well based on the node key of the matrix. One use case for this is imagine that this is a workflow for us to to develop an npm package. And we want to make sure that the npm package works on all the versions of node that are relevant and all the operating systems.

[00:02:22] So running test suites in parallel. For example, for multiple node versions before publishing an NPM package. Now, one thing to keep in mind is if you are using this kind of solution, this matrix here in private github repositories where the github actions are not unlimitedly free.

[00:02:40] They have a certain limit of of monthly minutes which are free from github. But then you need to pay for github actions. So each executed job counts towards billing purposes, right? So it means that we have nine jobs here that will be triggered. And a matrix generating nine jobs of three minutes each, then it it will lead to basically 27 minutes of build time in the end.

[00:03:04] So keep that in mind if you have jobs which or if you have workflows which take a long time to run, then this using this matrix here can lead to really an explosion in terms of minutes in, in your billing. And this is applicable to a few under under a few circumstances in github actions.

[00:03:22] We will discuss that later on when we talk about billing in github actions for public repositories, github actions is free. So we don't really need, we don't really have this concern, right? But this is just something that you need to keep in mind whenever you are using the matrix solution. Perfect. Now that we have a general understanding of how matrix works in github actions.

[00:03:42] Let's take a short break and come back in the practical lecture.