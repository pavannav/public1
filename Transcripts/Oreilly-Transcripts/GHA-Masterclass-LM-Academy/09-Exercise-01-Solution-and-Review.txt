[00:00:01] Our goal for this first practical exercise is to create a simple yet fully functional github workflow so that we can understand the main concepts we have discussed in the previous lecture. Let's start by creating our workflow file and in github actions for us to create a workflow file that should be created within a specific folder in our repository.

[00:00:21] So I'm gonna make a directory here and this is the dot githhub directory. And then within this dot github directory, we also will make another directory which is going to be called workflows. And this has to be this specific structure is required so that we can create our workflows here.

[00:00:42] And they are then identified by github actions and they are properly handled by it within this workflows folder. We can now create our first file and we'll simply call this 01 building blocks. And then the extension here is going to be Yamo. So every workflow that we create in github

[00:01:01] is a YAML file. And as soon as I open this, you see that we have an error here is most Quigley line and it's simply giving us some cryptic error message. This is because I have the github actions extension installed. Let me show you how to do that. And here, if you were to search for extensions, I have more extensions here because I do other things in visual studio code.

[00:01:23] But if you were to simply look for github actions, then the first one that is going to appear here is from github itself. If you open this, you will see there are quite a few downloads and it's provided by github.com. And you can then in your case, if you do not have it installed, you can just click on install here.

[00:01:41] And this provides lots of additional functionalities including the the validation of our files, right? So here, if we were to open our building blocks, again, we see that there is some error message, it's identifying that this is a workflow file from github actions and it is not valid. Let's then start by adding the first key here and

[00:02:02] it's going to be the name of the workflow. The name of the workflow is simply called 0 1 dash building blocks. Now we see there's still an error, there are few missing properties here. As you can see, it already identifies that there are two properties that we need to provide. The first one is the own property and this identifies the trigger of the workflow.

[00:02:22] And the second one is the jobs property as we have already seen in the previous lecture. This defines which jobs are composed or are included in our workflow for us to begin with. Let's add on here, push. And then this simply tells github actions that whenever we push to the remote repository,

[00:02:40] we would then would like to execute this workflow. And then under the jobs key, we're going to define a couple of jobs. The first one is going to be echo, hello. And then we will define where this job runs, we will say runs dash on and this is going to be Ubuntu latest. And for every, for pretty much every workflow here, except when we discuss

[00:03:04] different runners, we're gonna use Ubuntu because this is the option that is the cheapest in github actions. And it also is the option that we normally see that is used in most work flows out there. If there are any specific features that are required that require a Windows or a Mac

[00:03:22] Os runner, then feel free to change that. But for all our purposes here, Linux runners are more than enough then within the still under here, the echo LS key. And as we have also discussed, this will define all the steps that are run by our job within our workflows.

[00:03:43] The first one we're going to add with a name and this is going to be called simply say hello. And then you can see here that there is something missing. If we hover over, then it simply says that we need to add one of these properties. OK. So now here for us, we can simply add the one property and this is going to run a bash script.

[00:04:05] In this step, we simply say run echo hello world. And once we save this, I'm going to save this. So that it auto formats. For me, it's important that you observe the indentation here. It's very important that under the jobs we have

[00:04:20] two spaces or four spaces that's also fine. So this would, for example, be a valid file if we have Exel, right? For example, this is valid and here this is also valid. So the important thing is that the intention is the same for keys that belong to the same object in

[00:04:41] YAMLS defines a key and the value of this key is this object and this is an object containing one key. And then again, another object here and this object of hello world contains two keys that runs on and the steps and then the steps here is an array. And it's just important that we observe indentation because this is essential in

[00:05:03] YAML, whether you use two or four spaces, it doesn't really matter. In the end. I like to use two spaces because it makes things look a little bit more condense. But that's just a personal preference. Now, let's define the second job here. We want to define an echo dash. Goodbye

[00:05:20] and this echo goodbye is going to have a specific or a small difference here regarding the steps, I'm gonna see that in a bit. So this will run in Ubuntu latest and the steps are going to be as follows. The first step is going to have a name of failed step

[00:05:41] and it's gonna run a code that is going to actually exit with a nonzero code in bash. This means whenever you exit with a nonzero code, it simply means that that execution, that script that ran failed, that there was something that went wrong. We can simulate that by simply having an exit exit one. Like so

[00:06:01] and this is going to simply run and execute the exit command and then it's gonna provide a non zero code. Now we can also add something here and say echo, I will fail. And then after we print this on the screen, we're gonna exit with a nonzero code.

[00:06:20] Why are we doing that? Because I want to show you what happens afterwards. We will have an additional step here and this is going to be a step, say goodbye and we will simply say run echo, goodbye. Like so now what is going to happen here is that as github actions executes this first step, it's gonna run into a step that fails,

[00:06:45] that doesn't complete its execution successfully. Now, by default in github actions, whenever a step fails, then this whole job here is considered as failed and actually the whole workflow will be considered as failed,

[00:07:00] but more importantly, if a single step fails here, then any subsequent steps will not be executed by default, we will leave it like this here. This will be our entire workflow. It's a relatively simple workflow. It has two jobs. The first one is called echo, hello. And the second one is called echo goodbye.

[00:07:21] And then these jobs simply execute some very straightforward bash scripts. Another very cool thing of having the github actions extension installed here is you probably already saw that whenever I stop over some part of this of this workflow file, it gives us a couple of lines of documentation. This is

[00:07:41] very useful because for example, if we want to see what the on keyword does, then it really explains everything here. And then we can follow some of the links to see. For example, what are the different, different triggers that we can use for our workflow. Or for example, we have some more information about defining jobs or about the runners.

[00:08:00] And you can see here that there are a lot of information, maybe a lot of things here are not clear because we haven't talked about them. We'll talk about most of these as we progress through the lectures. I just want to show you how you can use this extension to actually clarify some of the concepts if they are not clear for you for now, let's leave it as it is, we're going to save this file.

[00:08:22] And then in our terminal, we will add everything and then we will commit with any commit message that you would like to for me and then for you afterwards to, to, to browse the commits more easily. I just wanna make it clear that this relates to the building blocks section of the course and that we have added the workflow file.

[00:08:42] So whenever you browse the commits of this repository later on, then you know what this refers to. Once we do that, we can then push our changes to the remote reposit story so that we can see them in action in github. Once we come to the github page, then you will see that there will be a small here yellow circle.

[00:09:02] As soon as you push the repository, it has already turned red to me because we have a workflow that is failing. So if we were to open here and then open the the the building blocks file, then we have an exit code of one here at the bottom of this fails, it is intended. But as you can see, we didn't have this before before pushing this,

[00:09:21] we didn't have this indicator here. And this refers to the actions that get triggered. Once we execute a certain push to the repository, if we were to then click on the action step on the top and then we have this one here and then we click here, then we will see that the echo hello step

[00:09:40] has executed or whether the echo hello job has executed successfully. We have the the hello world here and that the echo goodbye job has failed. And here I will say has failed quote unquote successfully because that's the intention and very important here. As you can see the say goodbye step did not execute.

[00:10:02] So this icon here means that this has been skipped and there are no logs here, meaning nothing was printed to the screen. This is very important for us to know how it works. Because if there are any steps that fail in our workflows, we would like to know which other steps are gonna be executed or not.

[00:10:21] Once we talk about controlling the execution flow in workflows, we are going to dig deeper into these relationships regarding failed, not failed, canceled successful or whenever we want to, for example, run a step always, for example, this complete job here has been executed despite

[00:10:40] the failed step step here exceeding with a non zero code. Now, how does that, how does that happen? Well, we will discuss that also later on in the lectures when we talk about controlling the execution flow in in our workflow files to summarize what we did in this first practical exercise. Let's go through our workflow file again and see what happened actually in github

[00:11:03] actions. So the first thing we have this name here and the name of 01 building block appears in our actions. Tab here whenever we click on all the workflow. So this is exactly matching this name here. This might appear on the left as well if it's a bigger window,

[00:11:21] but it might also appear at the bottom if it's a smaller window or if you put it a little bit minimized here on the side. Now, this workflow then happens every time we push to the repository for all the branches, it doesn't really matter, it doesn't do any filtering based on the branches. And then the each workflow run is gonna appear here for us to understand that

[00:11:43] let's go back to our ID. And now instead of having a failed step, let's have a successful step, successful step. And we'll say I will succeed and we will exit with a code of zero. Now we're gonna save this, we're gonna add all the files

[00:12:00] and then we'll simply say, get a commit and your commit message as you want to. Once we do that, we can then push the changes to our remote repository and go back to github back here in github. You can see that our new commit has already triggered a new workflow run. This workflow is skewed, meaning it is waiting for a runner to become available,

[00:12:24] then it goes to in progress and very soon this workflow should be a successful workflow after 12 seconds of execution, then we can see that the execution is actually green. And if we were to enter here, we will see that both the steps executed successfully.

[00:12:41] Now the successful step executed it has exceeded with a code of zero. And then because it was successful, then the say goodbye step was also executed. That's it for this first practical exercise. Let's now take a short break and come back in the next lecture.