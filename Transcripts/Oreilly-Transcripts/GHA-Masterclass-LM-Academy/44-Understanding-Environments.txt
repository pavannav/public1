[00:00:00] Hello everybody and welcome back. Let's now discuss environments in github actions. We have already touched upon environments in a couple of lectures, but I would like to dedicate a few minutes to discuss them in detail. So environments allow us to create multiple environments with different rules whenever we want to execute deployments to different environments,

[00:00:22] that could be, for example, a staging environment uat environment or a production environment giveth actions allows us to, for example, define required approvers before we can actually run a certain job that targets a certain environment. We can also restrict which branches are allowed and

[00:00:40] we can define variables on an environment basis. Let's take one example here, we can define an environment via the environment key under a job definition and then whatever the configuration for this environment is, it will get applied to this job. So if there are required approvers, for example, the job will be in a pending state until someone goes and approves that job run.

[00:01:02] This could be very useful. For example, if we want to restrict deployments to actually have a manual approval step before being executed, right. So this is how we specify the environment. And then for example, the variables that we specify here, they are available under the vast context and

[00:01:20] the secrets that we create within environments, they are also available under the secrets context. As already mentioned, one use case is to require manual approval before deploying to higher environments. So 11 higher environment would be production that would hit our end users. And even though we would like to target as much automation as possible,

[00:01:40] it's also very common to have manual approval steps. And then we can in this way have a little bit more control over the flow, right. So this would be one use case here for us. That's it for the short review of environments, they have some restrictions in github actions and actually for public repositories, all these features are available.

[00:02:00] But in most companies and in most organizations, except if you are doing a lot of open source, most of the repositories will be private and there are restrictions on private repositories. For example, if you have a private repository on github without using github enterprise, then you do not have available this required approvers feature. So there are a few restrictions that

[00:02:20] actually make this github actions environment, not so suitable for small or medium organizations that are not using github enterprise. That being said it is still a valuable tool if you want to define for example, variables that change based on the environment. So the set of variables are the same but their values might change on the environment and this can be useful for configuring your applications.

[00:02:42] Perfect. Let's take a short break and come back in the next lecture.