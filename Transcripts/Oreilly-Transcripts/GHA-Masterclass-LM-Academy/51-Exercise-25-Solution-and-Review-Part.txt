[00:00:00] Welcome back. Let's now continue with the implementation of our custom javascript function. We will start with the number one here where we will parse the received inputs before we can parse any input or rather before we can receive any input. We need to do find this in the action dot E file. So let's start with an input key and we're going to have the base branch input.

[00:00:23] The description here will simply provide a brief explanation. This is the branch, use it as the base for the dependency update checks, right? So this will basically just check out the base branch and then run the NPM update command on the base branch.

[00:00:41] We can provide the default here of main. And then we can also say that this is not required because if this is not provided, we'll simply use the default. If we do not wish to provide the default here and set this required to true. That's also fine, right? I just find that if we do not inform any base branch,

[00:01:00] then it's a reasonable choice to take the main or the master here. Depending on your choice as the default value, we can also provide a target branch here and the target branch will be the branch used or the branch from which the PR is created. And so then here we will execute the npm update and then we will check out,

[00:01:21] we will check out this branch here, the target branch and then we will create the pr from this branch into the base branch. Maybe a default value here would be just update dependencies and we can also mark this as not required. We then have mentioned the working directory and the working directory is a

[00:01:40] required input because there is no reasonable default that we can provide. And the description here will simply say the working directory of the project check for dependency updates. And then here we will provide that this is a required input. We then can request, we should request a github token for us to be able to create APR

[00:02:04] and the description here will simply say authentication token with repository access. We can also say must have right access to contents and pull requests. And this is because we want to both

[00:02:20] commit our our changes, push them to the remote github repository. So this is not just a read operation, we're also writing to the contents of the repository and we want to create pull requests. So we are not just reading the existing pull requests, but rather we are also creating new ones. That's why we need right, access to both contents and pull requests.

[00:02:43] This is also a required input. So we're gonna mark this as true. And also I do like to always have an option here to turn on debug messages, right. So here the description would simply say whether to output debug messages to the console. And here the default will simply be false.

[00:03:04] We can as if we write the default as false like this, then we will have a complaint because this must be a string, right? So if we hover over here, then we will see that it expects a string. So for us, a very easy way of doing that is just wrapping around single quotes. And this becomes a string.

[00:03:21] Later on, we can parse this value within the action to turn it back into a boolean. Once we have defined our inputs here, then we can come to our index Js file and start parsing them. We will use the core package for that. So let's start by just writing here constant base branch. This will be core dot getinput.

[00:03:43] And as you can see here, once I start writing the dot get and then we see that we have multiple getc functions here. The boolean input, for example is one that we can use to parse the debug input. So we say get input and then this will simply pass here as the base branch.

[00:04:00] This is the name of the input that we have defined in our action file. They have to match otherwise this will not be defined, right? So we have the base branch, we also have the target branch and we can say core dot get input as well here, target dash brunch. And then we also have the github token,

[00:04:20] which is core dot get input with github token. And then we also have the working door which is just core dot get input, I will say here working directory, right. So now we have all our inputs, we can also get the debug input is equal to core dot get. And then we can use the boolean input here with debug.

[00:04:45] And we can see the documentation by just hovering over and waiting a few seconds here. Then this appears on the screen and it basically says that it gets the input value of the boolean type in in the YAML specification, right? So if it is anything like true or true or like this,

[00:05:01] then this will also be in the boolean type, right? So basically this just saves us one line of code instead of just getting it as a string and then transforming it with the boolean operator in javascript or the boolean constructor. Then we can actually use it like this. There are a few things I would like to highlight here. And the first one is that this github

[00:05:21] token is a secret, right? So this should be treated as a secret. And if we use the secrets from our workflows here. Then this will already be considered a secret by github actions. But we cannot be more, we cannot be careful enough. And we can simply say core dot set secret for example. And then we can simply pass here the github

[00:05:44] token. And this will then be taken as a as or will be marked as a secret by our action here so that we have an additional layer of protection in case we incorrectly or inadvertently print or log this somewhere. We also receive both or all the base branch,

[00:06:02] target branch and working directories as inputs from the user. Which means and these are strings, which means that this would open up some surfaces for attack. So we just would like to check here that they follow certain conventions, right? So let's take the base branch and the target branch, for example, for them to be

[00:06:21] branch names, they can contain only letters, digits underscores hyphens and periods and we could also apply the same except the period, but we could use then letters digits hyphens and underscores for the working directory. While I want to be careful here is to validate them. It is because we will use them later on when executing scripts and they can then

[00:06:44] allow the script Injection problem or the security vulnerability. We will discuss that in the security section in details. But here I do not want to skip that. I wanna make sure that they are valid and safe input. So we will simply say here, we're gonna define two functions and we can define more functions here.

[00:07:02] It's no problem. So we say constant validate branch name, which then receives a branch name. And rather instead of using a parameter, I would rather use a or a string parameter. I do prefer object parameters. That's more of a

[00:07:20] personal preference here. And we can just use the destructuring here that allows us to access this. Now. Why do I prefer object parameters? It's because they are much easier to extend, right? If we wanted to add options here, then we can add them to the object. And then we don't need to have any, any

[00:07:40] change in the code. If we had parameter lists here, then we could have 5, 10 different parameters. But anyway, it's just a one matter of personal preference. And we can simply say here, we can create a red text test. So if and then we'll just define the red X here. It can contain any letters. So it can contain any

[00:08:00] lowercase letters, uppercase letters. It can also contain numbers, it can contain the underscore it can contain a hyphen and it can contain dots and they can also contain actually forward slashes because if we want to define the hierarchy, they can also contain forward slashes. So we're gonna have this

[00:08:20] and then we'll simply say if there is any of this one or more, then that is good for us and we can put a dollar sign here. So basically, if the branch name matches this pattern here, then this would be a valid name and then we can make this like, so dot Test and then pass here the branch name, right.

[00:08:40] So this, if this is a map, if we hover over the test here, it is a boolean. So basically whether the pattern, if this pattern here exists within this branch or if the, if the branch name follows this pattern here, then this would be a match. If there are any characters that do not match this pattern, then this would return a false here.

[00:09:01] And that is what we want, right? So if the, if the or actually we can just return directly here, we don't need to have this um entire if check. So basically all this is going to do is just take the branch name and check against a regular expression. So if this matches, if there, if these are the only characters contained within

[00:09:23] our regular expression, then this will return true. Otherwise this will return false. We can also say validate directory name here and then we can say their name. And the only thing that we don't want to allow here

[00:09:40] is the dot right. So this should also allow for forward slashes. It should allow for hyphens, it should allow for underscores and then we want to test here, the the name, we could go a little bit crazy here and add a pattern or use a pattern here where we can pass the the regular expression as a parameter and then build this validator. But anyways, I don't want to go too deep into the javascript implementation.

[00:10:03] This is already good enough for us. Since this is a fairly simple implementation. The only thing we want to use here is basically just if the validate branch name, for example, with the branch name of base branch is not true, right? So if this here fails, then we would like to output a message.

[00:10:24] So core dot error, I will simply say invalid branch name, branch names should include only characters, numbers, hyphens underscores

[00:10:40] and forward slashes. All right. So here, once again, we are trying to be as thorough as we want. And then we can say here instead of branch name, invalid base branch name. And we can also do the same here for our um base. So for our target branch, I will simply say invalid target branch name, branch names should contain,

[00:11:06] should include only characters and so on and so forth. So we're gonna use here the same input that we are providing. And then we can also have if and just 11 small thing here that should be not right. So if that's not valid, if that's not valid, if that's also not valid the directory name with their name

[00:11:25] being our working there, then here we will do the same thing. We're gonna have a cordon error and then we say here, invalid working directory name, directory names should include only characters, numbers, hyphens underscores and forward slashes.

[00:11:43] And here we can actually have also, I believe dots in the, in the, in the branch name. So we are gonna add that here as well, right. So we're going a little bit deeper into how we would do some good validation for our inputs to ensure that we are not gonna run into or we are less likely to run into any security breaches. Now,

[00:12:04] I'm not being super thorough here. I'm just doing a simple rejects verification. Maybe that's good enough for most cases. But the point here is that whenever we are creating custom actions, then we should always assume that our users might make some such silly mistakes.

[00:12:21] For example, as passing the github token, not as a secret, uh or other silly mistakes are less, less silly, but maybe more malicious mistakes as trying to run a script Injection attack in our custom action. We can also instead of just printing the message here, we can use another function which is called the set failed function.

[00:12:42] And this function will not only print the message, but it will also set the status of this action to failed. We could then here throw an error just to exit actually the execution of this script because if we do not throw an error or if we do or actually, we can just return here.

[00:13:00] We don't need to throw the error because we already have the set failed. So we can return here. We can also return here and we can also return here, right? So this is a little bit of a repetition. Maybe again, if you would like to think as a composing or, or from a composing mindset here, you could create a validator function that would take multiple validators

[00:13:20] and as well as their messages, their respective messages. And then they would just, we would just iterate over a loop and run these validators. Then we can get rid of this multiple ifs. But once again, we're not gonna too much into the design here. Three ifs are good enough for me and they are pretty clear what they are doing here. Perfect. We have done some basic validation.

[00:13:42] We could also output this on the screen. So let's use just co.info here. I will simply say Js dependency update. I will simply write the base branch is and then we can use actually back takes here to interpolate the value. So the base branch is

[00:14:00] and then we will simply write here base branch and then we will print also target branch is the target branch and then working directory is and then here that is the working there, right? So perfect, we have done some input parsing and validation.

[00:14:20] This is pretty much done for us here. This is pretty much done. And now what we can do here is to use the exact script. So let's add here the exact script, we're gonna require the add actions exec and we can use the exact script here to execute a command-line script.

[00:14:41] So if we scroll down a little bit, we can simply use this here. This is an asynchronous execution. So we do need to add a weight before so that it completes before we proceed with the rest of our function or the rest of our action. And all we will do here is just await exec dot exec and we can pass the command npm update

[00:15:03] and then here we can pass additional parameters to the command and then options to the command. The option we are interested in. So if we just hover over exact and come to its type definition, we have the arguments that we can pass to the command and then we have the options, the options here allow us to pass a current working directory.

[00:15:22] So that's nice because then we don't need to change into the directory by running a command-line script. We can just use this option here so we can just use the current working directory. CWD is going to be our working there, right? So now we have the working directory here and we

[00:15:41] will execute the npm update within this working directory. Once we run the npm update, we then wanna check whether there are any files that were modified by this comment and we can do that by having something like this. So we can say, get status and we can then execute the exact,

[00:16:00] get exact output. This will not only run the command, but it will also give us here, the exact output, which is an object. And it contains several interesting properties that we can inspect. If we click here, then you can see that we have access to the exit code to the into the S CD or of this, of this output or of this exact command here.

[00:16:24] And the STD out will basically be populated if there are changes. If there are no changes, this will just be an empty string. So what we will do here, we will simply say exact, get exact output and then we will pass the command git status S for short here.

[00:16:42] And then we are interested only in the package dot JSON files, no additional arguments. And the same thing here with the current working directory as our option. Perfect. So once we do that, we can inspect with, if git status do STD out

[00:17:00] dot length is greater than zero. So if there is something to the STD out of this command here, then we will simply core.info there are updates available. And so we will implement the logic for creating the PR and everything afterwards. Let's just print something on the screen here. And we'll simply say

[00:17:21] JS dash dependency update there are updates available else. So if the length is zero, then we'll simply say no updates, no updates at this point in time, right? So this is basically the logic for checking for updates and

[00:17:43] then checking whether there are changes to our package files. And we could of course, use these changes as well. If you want to go a bit deeper into this, you could get diff you could get the changes, the numbers that were changed and then use that to build the body of, of your pull request.

[00:18:00] But for us, we'll just keep it simple and check if there are changes. That's good. Then there are updates available. If there are no changes, then no updates at this point in time. So we have done both the second here and the third points. And I believe that's a good point in time for us to take a short break from the implementation here.

[00:18:21] Let's take a short pause and come back in the next lecture where we will update our workflow and then run a couple of tests with different values.