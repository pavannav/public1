[00:00:00] Hello and welcome back in this. In the upcoming lectures, we will explore how to create custom actions in github actions using Docker for that. Let's create a new folder here under our actions folder. Remember this actions folder is in the dot github directory. We're gonna create a new folder here and simply say Docker dash ping

[00:00:20] dash URL. So the action we will create is a simple action. It will receive a URL and a couple of parameters and then it will ping this URL and depending on the results that it receives, it will either succeed or fail. This can be useful. For example, if we want to wait until a certain URL is responding before we proceed with our workflows.

[00:00:41] And then if by any chance the URL doesn't become live within the expect within the expected period of time that we can define, then we can simply fail the workflow and inform that OK, this URL is not live. So we're not proceeding further similarly to the composite and to the javascript custom actions,

[00:01:00] we need to define an action dot YAML here under this directory and the structure will be very similar. So we start with a name and we simply say ping URL, we can then pass a description here and the description can be ping URL until maximum trials, maximum trials have exceeded if result

[00:01:21] is not 200 until then fails the action, right? So basically that just gives a high level description for us here, we can define a couple of inputs and the first input that we would like to receive is the URL itself. The URL will be of a description. So we'll just have a description URL

[00:01:41] to ping and it will be required, we will mark this as required true. The second input that we can receive is MAC underscore trials. And here we will pass a description. So the, this is going to be the maximum number of trials

[00:02:00] until action fails. We're gonna pass a default value here of 10. And we will say that this is not required, it's required false here. One small comment is whenever we are defining our custom action, if we were to try to pass something that is not a string here, then github actions or the schema would complain

[00:02:20] and it would simply say expected string, right. So here, if we added a for example, then yo would convert this into a string or it would interpret it as a string. But if we have only digits, then we can wrap it up around single codes to make sure that this becomes a string. The last parameter that we can define here is the delay

[00:02:40] and the delay will be simply so description delay in seconds between trials, right. So if a certain trial fails, how many seconds do we wait until we retry until we try again? And then we try until we have exceeded the maximum trial. So we're gonna set the default of five seconds

[00:03:00] and we'll pass this as a false or rather as not required. And here it comes the, the place where we define that we are using Docker, we will simply say the runs using Docker. And then we need to press an image here and this image could be, for example, it could reference the public Docker registry and then reference

[00:03:22] a certain container that has been built and published there. We could use that as the value here or we can simply say the Docker file. Once we pass the Docker file like this, then when we try to run this action, github actions is going to look for this Docker file within this folder here within our Docker ping URL folder.

[00:03:43] And then it will use this file to build our image. We're gonna keep it simple for now, we simply use the Docker file. This is the first step for creating a custom action using Docker. And as you can see, this is very similar to the to the javascript here. If we were to expand our action,

[00:04:01] it's very similar. It basically has the same things name description, a set of inputs and then here a runs key which contains a using and then instead of main, it simply contains here the image, right? So very similar structure. What changes is that is that now we are free to choose whatever programming language we want.

[00:04:21] And we can then as long as the Docker file is valid and it correctly builds our code and executes our code, then we can use that to create our custom actions now that we have defined our action. Do y let's take a short pause and come back in the next lecture.