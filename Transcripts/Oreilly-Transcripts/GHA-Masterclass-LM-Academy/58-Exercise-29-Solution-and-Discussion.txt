[00:00:00] Welcome back before we wrap up the custom actions section. And we move on to the next one. I would like to discuss the topic of setting outputs from within each of our custom action types. There are different ways for us to do that. And we're going to discuss all of them in this lecture. The first one is going to be from the composite type of custom action,

[00:00:22] which is defined all with in our action file similarly to the inputs, we can define the output here as a top level key outputs. And then here we can define the keys that will then become available as output. So for example, here we could say

[00:00:40] installed dependencies for example. And then we have to provide a value, right? So this value here and also a description, I believe a description like so, right? So then this is a valid, this is a valid syntax. If we do not provide a description, then you can see that we get an error here. And if we hover over then you see that it says missing property description.

[00:01:04] So we are required to provide a description and we will simply say here, whether dependencies were installed or not. And the value here, we can very much use what we already know from setting outputs of jobs within workflow.

[00:01:21] So we can retrieve this value here by simply looking at a specific step. And we can take for example, the cash dependency step and the value that we would retrieve here is whether there was a cash hit, right? So if the cash hit here is going to be, and then I'm just gonna use index from github

[00:01:41] and we will simply say steps dot cache dot outputs dot cache hit. So if there was a cache hit, the dependences were not installed. So we're just gonna negate that or actually here, since this is a string, then we can check whether this is different than true, right? So if this is different than true, that means

[00:02:00] there was no cache hit. This is the same condition that we have here. So if this is different than true, there was no cache hit and the dependencies were installed. If this is equal to true, then there was a cache hit and the dependencies were not installed. So the syntax is fairly similar. We are just referencing a step from within our

[00:02:21] collection of steps here by using its step id and then accessing the outputs of this step and then the specific output if we were to define outputs from within another step here, let's say by having just some random random output here and we would execute this

[00:02:40] as a shell. So we simply say here, shell bash and then we would say run. And all we will do is for example, echo random equals 123 into our github underscore output file. If we were to just add this to our github dot output file,

[00:03:02] and then we would have to provide here an ID as well by saying, let's say random output. Then we could also reference this within our outputs. And we can just here wrap this around double codes. And then we can use this here, let's say under our outputs, we would say random output.

[00:03:21] And then once again, we would pass a description some value and then the value here, it would then be accessible via our steps dot random output do outputs dot random, right? So I believe that's the, that's the value here.

[00:03:43] So basically we just reference it via the idea of the job of the step. And then we use here the value of or the key of the output that was printed that was appended to our file referenced by this environment variable. So outputs for for composite actions are fairly straightforward.

[00:04:02] And then we can use, I'm just gonna remove this because we don't need that for. Now, we can then use these outputs here within our workflow. So if we were to come here, custom actions composite and have a look at the step that uses our composite uh dependencies here, then we can pass an id

[00:04:22] and this idea would be here, set up deps for example. And then here we could say name, print, set up the output and we would simply say run echo and then simply say install dependencies.

[00:04:40] And we will then use here our, our special syntax. Let's then add this to a new line so that the o stops complaining. And then we can say here steps dot setup deps dot outputs dot installed deps, right? So here, now we are referencing an output from this step within another step.

[00:05:03] And if we look here, then we have to make sure that the key is correct. So, install deps is the key and then we can reference it here. Let's add this and let's commit our changes with whatever message you want. I'm just gonna say add output to composite action and then we're gonna push this and go back to github to test it back in github.

[00:05:24] Let's click and run workflow. Let's just click once again, run workflow and refresh the page and see what's going to be the output. As you can see here, the cache was not found for input keys. And that's because when I ran this, this workflow, it was more than seven days ago

[00:05:42] and the cash duration is actually seven days by default, right? So here it basically invalidates the cache and it does, it then has to install all the dependencies if we now expand this here, then we will see that we actually retrieve the output. We are able to retrieve the output here and print on the screen.

[00:06:01] Install dependencies are set to true. Perfect. So it's working and as you can see, it's fairly straightforward just going through it. Once again, we need to define the outputs here at the top level. And for composite actions, we have to define the value key and then retrieve the output from whatever step we want to,

[00:06:21] to retrieve this output. We need to fetch a step or retrieve a step via its id. And then we can use this here. We can define the ID here, right? So basically, then we use this ID and then we can access the outputs of that step and then fetch a specific output by its key. That's it for our composite action.

[00:06:42] And now let's move on to our javascript action and it's very similar actually under our action dot YAML here, we can also or we should also define our outputs at the top level here. So we say outputs and then we're gonna say here whatever we we want to

[00:07:01] to define as an output. So here we can just say here updates available, for example, right? So since that's what we do in our javascript action is to check whether there are updates. And then we need to provide only a description here whether there are updates available, the characteristic here is that we cannot provide a value.

[00:07:23] And that's because we do not have specific steps that we can reference directly. So what we have to do is from within our custom action. And here, let's say, let me just scroll to the top. I'm just gonna scroll down a little bit here. So you know where I am, I'm within the run function. And after the validation

[00:07:41] and some logging some debugging here, then we check whether there are updates and then if there are updates, we then proceed with actually setting up git and committing everything, creating the pull request. So this would be a good stage here for us to simply call core dot set output.

[00:08:01] And the set output, as you can see by the signature here, receives the name of the output that we want to set and the value, right. So the name here would simply be the updates available. I believe that's the name that I wrote here correctly. That's this one. And the value here that we would like to check or that we would like to set is just true.

[00:08:24] And so this here is going to be our our output. We can also add a logger here. If we want to logger debug, I will simply say setting or rather we can just write this as a string, normal string setting updates

[00:08:40] available output to true. And so here now actually perhaps a better way for us instead of just setting it once here to true, that would be to simply create a let and we will say here, updates available

[00:09:00] is equal to false. And then if there are updates, we simply say updates available is equal to true here. And then at the end instead of we instead um after executing everything here, we would within here, then just say that we are setting updates available output too.

[00:09:22] And then here we would retrieve our our variable here by using updates available, right? So now we are just setting this value here of the updates available to either true or false depending on where we are at this point.

[00:09:40] So basically, let me just collapse this if statement here. And so that we can have a better look, we start by defining updates available to false. And then if there are some outputs to this git status, we set the updates available to true. And then after everything, we just set the output here at the

[00:10:01] bottom updates available to whatever value is here. So if there are updates, this will be true. If there are no updates, this will be false. This is how we would go about it in our, in our javascript custom action. Let's now go to the workflow and the workflow is pretty much the same thing. So if we want to use the output of this custom action here,

[00:10:23] we would define an id and we will say update depth for example. And then here afterwards, we can retrieve that once again, print custom action output and we can simply run here and echo updated or updates available for example.

[00:10:41] And then we would simply use our github actions special syntax to retrieve that from our steps dot update, deps dot outputs dot updates available. So it's very similar. And once again, whenever we are setting the output within a custom action,

[00:11:00] using that output from the workflow perspective is the same thing. We're just accessing the outputs of a step that is using a custom action. So nothing changes for other steps here. Perfect. Let's then add or let's check the changes first, everything looks fine. Let's add everything and then let's commit here with whatever message we see fit.

[00:11:21] I'm just gonna add output to Js action and then we can push and go back to github to test this one back on github just to make sure that we don't have any open pull requests here. I ran into issues in the past if there are open pull requests. And I think I mentioned that and this would be 11 opportunity for improvement

[00:11:41] in our custom action to handle this case. So just make sure that we don't have any pull requests and then we can run the workflow here directly with all the the default values. And I'm going to turn on the bug in here. So let's just click on run workflow, refresh this and inspect the output. So after the run is completed here,

[00:12:00] we can see that the value is available as expected. So nothing new. We are just using the output as before and we are setting it via a call to the set output method. The last type of custom action, the Docker custom action is similar to the javascript one when we are defining the output here, but it is slightly different when we are setting it.

[00:12:20] So the outputs, we're gonna simply say for us here, since this is a pink action, then we will simply say website reachable. And the description here will be whether the website, whether the URL is reachable, right? So maybe here we can just say URL reachable.

[00:12:41] Now, the difference is that we do not have the the facilities, we do not have the github packages here that we have available in our javascript action. That means we need to go through the way that we already know how to set outputs. I'm just gonna open this here for us from the output lecture.

[00:13:01] And you can see here that this is done by echoing a key value pair or appending it to the file, which is stored here referenced by the path in this environment variable. Now, behind the scenes, there is a file here, right? That means that we can actually open this file in Python and simply append to this file.

[00:13:20] So for us to do that here, we can simply say, let's create a function. I will say set underscore output, I will simply say file path, key value. And then we'll simply say here with open file path. And the mode here is a pen mode that's important.

[00:13:41] And then we'll say simply here as file and then we'll say print, we can format, then this with our key is equal to our value. And then we will say here file is equal to file, right? So basically what we are doing is we are just adding this printing this key,

[00:14:02] printing this line to the end of this file, right? So once we have that here, we could then call this function. Let's say the set output here, right? So the set output here and then we would pass the file path that would be OS dot get

[00:14:21] and then the name here would be github underscore output. The key is the one that we have defined in our action dot yo file. So let me just copy paste here, let's copy and paste this. And the value here is simply going to be our website reachable.

[00:14:40] And so we're just setting the output and passing this is the file path, the which is stored in this environment variable. This is the key we want to set and this is the value we want to set. So all this is doing is just opening this file appending to it and then it's printing this line here at the end of the file. Once again, this can be referenced by just setting an id to this step.

[00:15:03] So let's add an id here. We're just gonna call this ping L and we will add, once again, our name is going to be print output from ping URL and we're just gonna run here echo. Let's once again, add a multiline statement

[00:15:23] URL reachable and then we will access the steps dot ping URL dot outputs dot L reachable. And that's it, right? So, pretty straightforward or nothing new here, we knew that we had to append

[00:15:41] to this github output or to the file that is referenced by this github output environment variable here. The only difference is that we are doing that through Python instead of through shell commands. So once again, our status looks good. Let's add everything. Let's commit this add output to Docker action.

[00:16:02] We can push and go back to github to test this last change. Let's then run the workflow twice once with the valid website. And another one, it could be, for example, with an invalid schema. So that is a little bit faster than having to wait for the for the maximum trials,

[00:16:21] right? We're gonna open both of them and we're gonna just quickly inspect the output. Perfect. So the value is available here is set to true and actually here if it fails, it doesn't really execute the next step, we could actually make sure that this step here executes after this action fails. But once again, the value is just going to be false. So

[00:16:41] no big deal for us here, we can already see that we are retrieving this value from Python because of the capital T here. So this is all working as expected. As you can see, these are the three ways that we can set outputs for the composite action by directly retrieving the value from our steps for the Java

[00:17:00] action by using the core set output facility, which behind the scenes probably also works with the github output file. And for any Docker action here by directly writing to the file referenced by this github output environment variable. Now that we have covered different ways for us to send outputs,

[00:17:20] I believe that's a good point for us to take a break and come back in the next lecture.