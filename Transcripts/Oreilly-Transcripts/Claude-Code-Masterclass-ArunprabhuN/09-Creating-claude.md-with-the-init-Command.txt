[00:00:00] Hello all. In this section we are going to look how to initialize the cloud code in your project. As I mentioned earlier, this is the sample project I have taken. In your case, if you want to follow along with this course, take your own project. it can be any project that you are working currently on and initialize the cloud code in that project. As we already know,

[00:00:20] it's simple to open cloud code you have to go to your terminal and just type cloud. Another way is you can just open any file and click on the cloud code plugin. And from this plugin also, you can start the cloud code. As I mentioned, there are multiple subscription models or in my case I'm using the anthropic console, just click on the anthropic console,

[00:00:41] it's asking me whether it can navigate to an external website. As we have already did, just we need to authorize this. If you have already logged in, then it will directly ask you to authorize. Once you authorize, you can just close the window and you're ready to use cloud code. As I mentioned, the plugin is more easy to use than a terminal,

[00:01:00] so it is more user friendly. So there is a concept of commands in the cloud code. For example, when you click on the forward slash, then there will be a lot of commands will be shown. The first command will be in it. That's the slash command they call it. each command will perform some operation. There will be a separate section on some of the most useful commands in cloud code,

[00:01:24] but first, when you are starting with using cloud code with a project, you can start with init command. Once you click on init, what it will do is it will create a working memory. Basically, if you see here, I will analyze this code base. So cloud is trying to analyze the code base.

[00:01:41] This I might have created myself or I might have used another AI also. So cloud is trying to understand the project. So it's going through all the files, all the file system, everything, and it will try to understand. See now let me read the key source file to understand the architecture. Also it will say, I am understanding this to create a cloud.md file.

[00:02:03] Basically this cloud. empty file is a set of rules. After cloud understands your project, it will create a set of rules based on your project. what is the architecture of the project? what are the files used, if you have some coding practices, everything it will create, and you can manually edit this file as well that we will look in some time.

[00:02:23] But first, let's allow cloud code to create this cloud.mppd file. It says now I have all the information. I'm going to create the cloud.md file. So now the cloud. empty file is ready. This they call as immutable system rules. So these rules always the cloud will try to follow

[00:02:42] on top of a user prompt. User prompt is you are asking the cloud to perform some action after collecting all the information and after planning how to do that operation, cloud will try to follow these set of cloud.md rules. Because each project is specific

[00:03:00] and each of us will have a different style of coding, different coding practices, different security practices, different style of testing practices, so this is not going to be common between my project and yours. That's why cloud creates an MD file within the project which it will use to follow as a guideline to make any change into your project.

[00:03:22] So it's asking me whether I can add this cloud.md file. You can click yes or yes and don't ask again no, whatever. let me click yes. So, it's trying to add this file. If you see here, it's already added. Let me open the file. So it says I have successfully created the

[00:03:40] cloud.md file for the flask-based course explainer application. If you have seen, I didn't mention that my application is using flask or it is a course explainer application. Cloud has gone through all the files myreadme.md file and all the codes, and it has understood what this project is and how it is developed.

[00:04:00] So it has created some high-level architecture. It has important architectural notes. So if you see here, let's open it as a proper MDV file. OK. So in this file it has done everything project overview, setup information, probably it read from my readme.empty file, how to run the application,

[00:04:20] how to run the test and the architecture design and important notes. So this file you can always edit. For example, I want to make sure always the unit test should happen whenever cloud code adds some functionality. Then I can add that command here. So simply just enter, put double hash.

[00:04:41] Something like ad units. So I can mention it Whenever you add any feature, add a unit test and make sure the unit test always pass. So, for example, this is my coding practice. I want always unit tests to be written in my project

[00:05:01] and those tests should be run and make sure it passes before adding any functionality. So like this you can add any number of instructions for the cloud to follow whenever it makes the changes so that we can be sure your organizational or your team level coding practices are followed

[00:05:20] when you are using cloud for making any changes. So these commands are called hash commands. That is another way to add these hash commands when you put a hash here and you add something, then those prompts will be automatically added into your cloud.md file. Because these are called hash commands and these are kind of a set of rules that needs to be followed always,

[00:05:41] so it will be automatically added into your cloud.md file.