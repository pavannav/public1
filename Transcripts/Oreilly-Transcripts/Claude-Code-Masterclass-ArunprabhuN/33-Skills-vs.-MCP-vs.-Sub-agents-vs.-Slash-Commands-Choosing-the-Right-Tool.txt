[00:00:00] Hello. Welcome. In this section, let's look what is the difference between skills and other features in cloud that we have already seen. First, what is skill? Skill is a specialized context-aware intelligence that is applied whenever it is needed. Cloud decides when to use a skill,

[00:00:20] so it's not manually triggered. One of the important features of skills is it is composable, that is, skills can use multiple resource files. We have already seen under the skills we have multiple files like scripts, reference.md or any file we can have or directories which Act as a resource for this particular skill.

[00:00:41] not only under this directory, you can reference files from any directory in your system. Similarly, you can make use of other commands as well that is already available. You can invoke inside your skills. Similarly, skills can reference MCP tools as well. We have already seen skills can invoke different scripts.

[00:01:02] Similarly, it can use any MCP tools that are available in your cloud. So a better use case for using the skills will be for reviewing the code, because for reviewing a code you might need multiple files. For example, when you do a frontend review, you might need architectural design, frontend components,

[00:01:21] whether the components are properly used, and many other files. Similarly, when you do a back end code review, you might need AP documentation and different. Resource files we don't have to load all these files into the context. If you create a skill based on the need, cloud will decide which resource file to use, and it can load it into the context.

[00:01:40] Similarly, we can use for architectural guidance any workflow that needs multiple resource files. We can use skills. Now check the difference between different features and skills. First, skills versus cloud.md versus hooks. We know skills provide specialized knowledge, whereas cloud.md is a global file which is available in

[00:02:01] the project and which provides information about the whole project, and it is loaded default into the context always. And we use hooks for triggering some action when a particular event happens, for example, when a file is saved or when a PR is created, we want to send a notification to our team's channel. or when cloud expects our input,

[00:02:21] we need a notification. In such cases we will be using hooks. so I hope there is no confusion. Between these three, let me jump into the next component. skills versus subagents. Skills are specialized knowledge, whereas subagents are an isolated context. Skills will be loaded in the main conversation itself.

[00:02:40] When Claude decides to use the skill, it will be loaded in the same context. Whereas subagent is for creating a separate isolated context, for example, when you want to review a PR using some standards, you can create a skill. So any agent that is creating a PR will use this skill,

[00:03:00] whereas we use subagents to mainly perform different actions. For example, You can create a subagent to review a code. parallelly we can run another subagent which might write the unit test cases or execute the test cases, and each of these subagents is going to have a separate context window. These subagents might use the skills.

[00:03:21] Let me go to the next one. Skills versus MCP tools. MCP servers provide cloud with access to external tools and data sources. so basically MCP provides the tools, whereas skills define how to use the available tools. A skill can use multiple tools. Consider MCP tool like a python code.

[00:03:42] A skill can reference multiple MCP tools based on the need, and it can make use of these tools. Next one is skills versus slash commands. This is a little bit overlapping area, so we will use slash commands when we want to run a specific repeatable action. For example,

[00:04:00] you might have a prompt which you might use regularly. Instead of copy pasting this prompt every time, we can create a slash command. So basically slash command we can use for repeatable actions, a simple repeatable action, whereas skills we will create for sharing specialized knowledge with cloud, and skills will be triggered by the cloud based on the need,

[00:04:22] cloud will decide which skill to use and it will trigger. But mostly slash commands will be manually triggered, as I mentioned for repeatable actions we can create slash commands and we can manually trigger it. So an example can be for reviewing PR we can use the skills we can use slash commands for something like deploying

[00:04:41] Demployment steps will be saved so you can create a slash command and execute that command. so whenever it is needed, skills can reference different slash commands also. Hope the differences are clear now. In the next section we will do a hands-on demo on creating a skill and how to use it.