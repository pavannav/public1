[00:00:01] In this section, let's look into how to use cloud code with Visual Studio code ID. If you're using any other ID, for example inteligent ID, then also the steps are same because we're going to access cloud code through the terminal, so you use any ID for your development.

[00:00:23] ID is integrated development environment. it can be intelli, Pyhom or Visual Studio code. The process will be same. First I will open my project. This is a sample project I created with only one python file, just to show how cloud code is going to work.

[00:00:41] Later on, we will create another project with a lot of files for our course. So right now if I want to open cloud code, one way is I can open the terminal. Now you can see, within my user folder cloud code course is open. Now let's assume this is my workspace.

[00:01:01] My project where all my project files are there right now I have only one file. Let me open the cloud agent. You know the command to open it, it's cloud. Enter cloud and hit enter. Now it's open and it's asking me the permission. permission for this particular folder. in order to work this folder,

[00:01:21] I need your permission. Just click Enter. So now you can see it's using Sone 4.5 API usage is building. And there is no recent activities for me, and there is a tips run you need to create a cloud.md file. We will be looking into these details in the next section because right now

[00:01:40] we are only looking at how to open the cloud in Visual Studio code. Once you open a cloud code in the terminal, most probably cloud code plugin will also be installed in the Visual Studio code. If it is not installed, how you can check whether it is installed or not is you just open any file in your project. For example, when I open this main.py. It shows the cloud code plugin here.

[00:02:02] If it is not available, then go to the extensions. search for cloud code. The first one, cloud code for Visual Studio code. install this plugin. If it is not already installed, just install it. It has some benefits. So as it is integrated with the editor,

[00:02:20] it knows what is the current file you have opened, and it will suggest auto completion of your code, as we have already seen with Github copilot, cloud code will also do auto completion, and it will know which file you have opened, and when you ask some questions, you don't have to enter any context about that class or method that you are asking the question about,

[00:02:41] because cloud code will automatically know which file you have opened and based on that it will give its answers. So now we are ready with cloud code installed in our system, API is connected, so we are ready to do the programming. In the next coming sections we will look into how to use cloud code for doing the programming.