[00:00:00] OK, let's do some hands on activity. let me open the cloud code. Now I'm in main branch. Let's try to create a branch and commit the changes that we have already did using cloud code. Let me ask cloud code to do that. I'm giving a simple prompt. Create a branch and commit the changes. That's it.

[00:00:20] As I mentioned earlier, you can create a slash command so you don't have to explain all these things. Even you can say it has to have a neat commit message and you can give the branch name as an argument. So when you call the slash command and send the branch name as an argument, it is going to perform the same activities. But right now, let me give you a simple user prompt create a branch and commit the changes.

[00:00:44] OK, now cloud has did the analysis. Now it is saying I need to run this command. Git checkout b commands added branch, the branch name that I have given. Now you can either give simply yes or yes and don't ask again. That means we are giving this permission

[00:01:00] to cloud always within this project. When you give yes and don't ask again. You can see here cloud has added a new file settings.local.json. If I open this file here, the permission allow. so we are giving the permission to git checkout always to the cloud. So you can manually add the permission here,

[00:01:22] whatever permissions you are giving the cloud to do by itself without asking your permission, you can say whatever you want to deny by default for everything it is going to ask. If you want to add something on top of this, you can add here as well manually you can edit these settings. As I mentioned earlier, in cloud, any settings is in two levels.

[00:01:42] Either it can be on a project level where the settings.local JSN file is created under the cloud folder within your project. If you are doing some process which you are only doing but you don't want to share it with your team, then you can save these same settings under the cloud folder under your home directory as well.

[00:02:00] Let me close it. now it is asking, I want to add this file. We can say yes and don't ask again so that it never asks us for adding every file. Each of these is a different git command. Previously it was git checkout command. Next is git add command. If you think any command under git, it doesn't have to ask you, then you can simply put git

[00:02:21] asterisk, the wildcard, so that whatever commands that is calling with git, it is not going to ask your permission. But it is always better to specifically add which command that you are giving permission for the cloud so that it doesn't do anything that you are not expecting. Like reverting your commit or stashing your changes

[00:02:42] which you think will be a problem, so it's better to give specifically which command you are allowing it to perform. now it is asking permission for the commit. If I simply give yes, then that command is not going to be added here because it's going to be asking permission from us for every time when it is going to do the commit.

[00:03:00] And if you see here it has automatically added a commit message as well. I didn't mention to add any commit message, but it has automatically added a commit message. That's where these LLMs are more powerful now. You don't have to specifically add each and every command. Some of the generic activities, they understand what they are doing and they can do by themselves.

[00:03:20] You don't have to specify, OK, you need to add a commit message every time. They will understand the changes that you are doing and based on the changes they will add a nice commit message as well. Now it's consolidate what it has done, created a new branch called commands Adder, added these files, and committed the changes as well. When you want to create a pull request,

[00:03:40] usually we will go to the GitHub and we will be doing the pull request, but with cloud code and GitHub client tool we can do even creation of pull request from our ID itself. You don't have to open your repository at all. From here itself you can create the pull request also. In the next sections we will be looking at how to do that as well. Also, one thing to note down here is

[00:04:01] this is going to be settings.local.jsn. So this is local to your own project. This will be ignored by the git because this permission is local to you. You might be giving some permission, but someone working in your team might be thinking, OK, every time it has to ask me. I don't want to allow all the permissions. That's why in cloud settings.local.json is something that

[00:04:23] will be ignored automatically by the git, whereas if you create something as settings.jn. Then you can add it to your report, you can commit those changes to your report, and everyone in your team can share those settings.