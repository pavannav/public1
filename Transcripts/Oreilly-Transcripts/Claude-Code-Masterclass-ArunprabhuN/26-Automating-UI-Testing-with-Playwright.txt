[00:00:00] In this section, let's look how to automate browser testing with cloud code and playr MCP tool. So we have already installed MCP tool and it is connected now in cloud.empty file, I have added one more step. So you know we already have the unit test step. Whenever you add any changes, perform unit test. This is the command we have given to the cloud.

[00:00:21] I have added one more step verify the changes with play rate, and it is mandatory. After implementing any new feature, you must start the flask application. If it is not already running, use the playwright MCP tool to connect to the application and navigate to the new feature what we have added. Take a screenshot, put the screenshot in the test output folder and verify it.

[00:00:41] So with this step we make sure the UA is properly developed. Cloud code will check the UA, test it, and also capture the screenshot as an evidence. Now let me open cloud code. You know this is our application. We have available courses Course 1, Course 2, Course 3. This doesn't look good.

[00:01:00] Maybe we can have the course name here instead of 123. Also, I want to add one more course here. So let's ask cloud code to perform this task. So Iment instead of course 1, Course 2, Course 3 in home screen, add name of the course for each hyperlink. Also add another course for golang. Let me press enter.

[00:01:22] As I mentioned, always review your code, but this is just a demo lesson, so I'm just accepting the code without any review. So now I see unit test has been added and it is asking to run the unit test. good.

[00:01:44] OK, now we can see it has completed the unit testing. Now it's saying it wants to verify the changes with playwright. So in the prompt we didn't mention anything about playwright. Everything it's referring from the cloud.md file. So it's asking my permission to navigate to the URL.

[00:02:01] Now we can see it has accessed the home screen and it was able to see the course hyperlink headings introduction to Python Web Development with flaskcontrol. Previously it was Cour 123. It's asking my permission to take a screenshot. Meanwhile, the cloud code has already started the application, so let's go and check the application by ourselves as well.

[00:02:21] Let me refresh it We can see now all the hyperlinks has been updated. Let me go back to the cloud code. OK, let me give permission to create a folder and save the screenshots. So now the playwright want to click the element and check whether it is properly navigating to the screens.

[00:02:40] So better I can give yes and don't ask again for taking screenshot. I can give the permission so that it won't ask me every time. OK, now it want to close the browser. So it says the screenshots are saved here. Let me go to the location, play rate MCP test output. So now we can see cloudcode along with player have taken screenshots,

[00:03:02] and this is a new feature that has been added, so it took screenshots of only go programming language ins screen. Now you can understand we have enabled Cloudcode, the AI application to access the application running in our local machine. So now it is able to interact with the application running in our system.

[00:03:21] so it is able to open a browser, close the browser, take screenshots and everything. How this is done is using the MCP tool playwright. So if you see the MCP tool, let me press enter. So there is an option called Viewtools. So if you just click on it, enter, so these are the tools that play it already have all these 21 tools.

[00:03:41] For example, it will allow you to close the browser, resize the browser, evaluate JavaScripts, upload files, press any key. Type text navigate to your URL. take a screenshot. Playwright is having all these 21 tools which allows cloud code to interact with the browser.

[00:04:00] So this is the power of MCP tools. There are a lot more MCP tools available which will improve the usability of our coding agents.