[00:00:00] In this section we are going to look into a very important concept that will be useful not only in cloud code but in any AI LLM or coding agent that you use. We are going to discuss about session and context. What is a session? A session is an interaction that you are having with any LLM.

[00:00:22] When you open cloud code, if you click on our history, first we created cloud.md file with init command that is one session. We have fixed a bug in the home screen that is another session. A session is some information that you are discussing with cloud code where the cloud code will have all the details in its memory.

[00:00:42] The memory is not carried over between different sessions. When you open a new session, it is like a new chat window where the information that you are discussing are not shared with other sessions. So for example, if I click on this new session, a new window will be opened and whatever I'm discussing here,

[00:01:01] it is not shared with the other session. So the information that you have shared in the previous sessions are not known to the new session that you are creating. If you have a question, what if you want to share some information between the sessions, like architectural design or coding practices that I want all the session to know about?

[00:01:22] That's where we are creating the tmd files like cloud.md which works like a guidance for the cloud code. so the cloud code will always refer this file whatever the user prompt that you are sending. If you ask the cloud code some question along with that question, it will always refer to this cloud.md file as well.

[00:01:42] so it's like a static memory across all the sessions. Every agent will refer this cloudcode.md file. That's why you have to add this cloud.md file into your repository. It's like a generic file for your whole project. That's why we are maintaining the architectural design, other common things within this cloud.md file.

[00:02:03] You might get a question, OK, if session is like a memory, why can't I keep all my interactions in a single session so that all the information can be shared with the LLM? There are two problems in this. First one is context limit. What is the context? Context is the information that cloud codes share

[00:02:22] to the LLM along with your question. So if you look at this diagram, this is the first turn where a user sends a message. This is the input to the LLM and text response. This is the output the LLM gives to the user when the user is asking the next question within the same session, then along with this new question,

[00:02:40] this is the second question the user is asking. Previous question is also sent because the LLM should have a context. Imagine any LLM as an interaction with a new developer. For example, when you want to implement some task, first you will discuss with the LLM how to implement it, what is the architecture we can plan. Later on you will say,

[00:03:00] OK, implement this. When you are saying implement this, it should know what you have already discussed in that session. So that's why all the information in a particular session will be managed as a context and it will be shared with the LLM every time the user asks a new question. So in turn 3, all the information from turn 2 will be going along with the new user message.

[00:03:22] So the problem here is any LLM will have a context window that is the limit of how much text you can share with that LLM. You know, in LLM the text is measured with tokens. For example, cloud allows you to share 200,000 tokens in a context. When your context exceeds this limit,

[00:03:40] then the previous message or previous data will be lost because this is the maximum limit that you can send in a particular session 200,000. Tokens approximately means around 500 pages of text. That is really a huge amount, even though you think you are not going to reach this limit. If you are asking, Shall I do different tasks in the same session?

[00:04:00] No, you shouldn't do that. Why? Because that means you are feeding the LLM with information that is not relevant to your current task. For example, first you are fixing some bug. Now you want to perform some other activity which is Exactly related to this bug fixing, then it's better to create a new session,

[00:04:20] or else you are going to include information that is not relevant to your current task. This will lead to sometime wrong answers from the LLM, so you have to be careful. You have to give clear information as much as details for LLM to answer a question, but you have to also make sure you are not sharing information that is not relevant to that particular question.

[00:04:41] That's why whenever you do a new task it's better to create a new session. There are some slash commands which will be helpful in maintaining the session when you know what exact functionality you are going to do. If you think I know the file where the changes have to be made, then it's always recommended and best practice to send the file in the chat.

[00:05:01] You already remember. Whatever file that you have opened, for example, if you have opened cloud.md it will be added here. If I go to app.py, it will be automatically added there. If you want to add some more files, for example, I want to add along with the app.py models python file and views python file, then you can put at command and you can add any files that you want,

[00:05:21] for example, models.py, views.py, and if you want to add the test case as well, you can add the test. So like that you can add the files. This will help the cloud code to minimize the search that it has to do in creating the context so that it will know, OK, the user is asking questions in these files,

[00:05:40] so it will be shared with the LLM and the answer will be more accurate. Also, if you have a big chat, for example in a session you have a lot of chat, if you think the context is getting increased, you don't want all the information in the context, but you want to just make it concise. Then you can use a command called compact. When you use this command, cloud code will try to go through all the information in your particular session

[00:06:03] and make it a concise and clear information for the LLM, so the context will be reduced, but the information will be available also. You have another command called clear where you can clear the conversation, so the whole context will be cleared and it will act as like a new session. as I have already mentioned,

[00:06:21] whatever the commands I'm showing here, you can use them in your terminal as well. Let me go to the terminal. For example, you know, we can select a session from this history. Similarly with the terminal, you can use a slash command resume which will allow you to select the previous sessions. So if you have stopped your work, you can resume from that particular task.

[00:06:41] So when you give resume, it will allow you to select which session you want to resume. So you can select the particular session that you want to resume and you can continue your task from there, so the context is maintained.