[00:00:01] Hello all. In this section, let's look into another setting called hooks. Hoooks allows users to define their own shell commands to be executed during a cloud code session. This allows you to control the cloud code's behavior. For example, you want to execute something before cloud codes starts working on some file,

[00:00:21] or once it is completed, you want to execute something like running unit test. Like all these things you can provide in a hook. One of the mostly used examples for hook is having notifications. For example, when you are asking cloud code to perform some activity, it might take a long time to perform the tack. It has to analyze the code. It has to design a solution and provide the solution.

[00:00:41] Till that you don't have to sit and look into your ID. You can start working on some other task and you can define hooks to send a notification whenever cloud code needs your permission or whenever cloud code completes the task. Similarly, auto formatting, when cloud code finishes some code, you can make a hook to autoformat the whole code.

[00:01:00] And another use case might be logging.log all the activities done by the cloud code itself. So the events that can trigger the hook is pre-tool use before running some tool or post-tool use or as I mentioned notifications. This is for any notification whenever cloud code wants to notify that it has run some command, this notification will be triggered. Similarly,

[00:01:20] session start and session end. So you can add the hook inside the settings.jsn. If you think you are going to share the hook with everyone in your team, then you can add it into your settings.json and add it to the repository. If you think you want some hook for yourself, like I have a notification sound to be played whenever cloud code expects my permission, then you can add it to settings.local.json so

[00:01:42] it will stay only within your local code. Let me go to the Visual Studio code. I have added the hook already, so this hook will be played post to use, so this hook. will be executed after the tool usage. The tool is to do right. To do right will be executed when cloud code completes all the activities,

[00:02:00] so I will know the cloud code has completed the task. Similarly, I have another notification. This will be executed whenever cloud code asks my permission or any other notifications. So I have added the playsound.py python file. Basically this python file is to simply play the sound. I have stored the notification.mp3 also. So this is the notification I have saved.

[00:02:21] you can save your own notification sound. Let's test it. let me open the cloud code. Let's test the hook with cloud code itself. It waited for 5 seconds and played the notification sound.

[00:02:40] If you see, I have made some typos as well, but cloud cloud is smart enough to understand what I'm mentioning, and it executed the command. And when it needs my permission, it played the sound as well. Let me press yes. OK, it played the sound once it completed the task as well. You can use the hooks in multiple ways.

[00:03:00] try it out in your project as well.