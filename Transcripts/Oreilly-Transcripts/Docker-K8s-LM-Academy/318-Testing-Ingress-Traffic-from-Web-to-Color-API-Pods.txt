[00:00:00] Once enough time has passed, then our managed certificate should be in an active state. So let's describe here, our Proud managed certificate here and the moment we do that, we should see that our certificate status is active, the moment this is active, then we are really good to go. It might take a few minutes still to for this to propagate or to take effect.

[00:00:24] So if you start seeing some errors related to certificates and handshakes like failed handshakes when you are accessing on the browser or when you are sending call requests, don't worry about that. Just wait a few more minutes and the error should soon be gone. Let's also have a look at our

[00:00:41] ingress here. So the Proud ingress and these are a few things that you will also see as the ingress is configured. So a few labels and annotations here, scroll up a little bit here. A few annotations. They will be automatically added by the ingress controller when taking care of the

[00:01:00] ingress or of setting up the ingress. You can also see here that there are a few sync reasons. And then if you see these logs here and you don't see any errors, then you are good to go as well. We can now confirm that this works by coming to the browser. And then let's just here instead of using our IP address, let's just use now our DEV dot color or so colorful, right?

[00:01:23] So, so colorful.com and the moment we do that, we can confirm that not only we get the response back from our DEV POD or color API here, but at the moment, we click here, we can also confirm that the connection is secure. So our certificate here, oops.

[00:01:42] So our certificate here is valid and we are good to go. We have successfully managed to set up TLS and ingress in our managed cluster. I also want to show here that if you come to the Google Cloud console and then you type here certificates until you come to the certificate manager and then you

[00:02:02] will have to come under the classic certificates tab. So just under the classic certificates tab, you will find both the provisioned the managed certificates. If you click here, you should also see a status of active and some more information here about the certificate chain. That's amazing. Congratulations on completing all the steps so far.

[00:02:22] I also want to show you that here. If we try to hit the matrix endpoint of our dev so colorful API we will get a response back and this is coming from the Prometheus metrics that we have exposed when we updated the color API image to the version 2.1 0.0

[00:02:40] if we try to hit this endpoint here in the www or in the just so colorful.com like, so then we will see that this is actually not defined, right? So we did not update our prod color API to the version 2.1 0.0 on purpose. And that's why we also cannot get the metrics here. Additionally, they are connected to two different databases, right?

[00:03:02] So if we were to try to retrieve the primary key here, this is not going to return green. If we hit the DEV dot So colorful, that's gonna return green. But the normal one, the production one returns blue because we did not set the primary key in our color API in the production environment.

[00:03:21] That's great. So with this, we can confirm that our TLS certificate and our ingress, they are working as expected and we are able to expose our application behind a secure domain name. That's great with this. We have managed to set up our TLS certificates and our ingress in both our DEV and prod environments here.

[00:03:41] So that's a great moment for us to take a short break and I'll see you in the next lecture.