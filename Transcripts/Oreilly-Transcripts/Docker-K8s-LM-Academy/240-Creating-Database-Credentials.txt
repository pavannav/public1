[00:00:00] Awesome. Let's then start implementing our MLIB project. I will create a new folder here as usual so that we have all the files under the same folder and no confusion with previous projects. Let's then create both the secrets and the configuration that we need for our database in this lecture.

[00:00:21] And then in the upcoming lectures, we'll also create the other objects. Let's then start with mongo root credentials or let's call this Mongodb root credentials like so, so root creds dot mo and then here this will contain the root credentials that will be used in our database

[00:00:40] initialization. That's going to be a kind of secret. So we can then just name this, let's call this Mongo DB root creds like so, and let's also add a label here. We're gonna call this app Mongod B. The type will be opaque and the data here,

[00:01:00] we're gonna pass both a username and a password, right? So now for the username and the password, it does make sense that we start the base64 encoded version of the string. We can also use string data like so to simply pass the username and the password here in plain text, but I'm just gonna use data here and the

[00:01:22] username will be root underscore password or sorry, root underscore user and the password will be root underscore password. Let's now encode this with base64. Let's open a new terminal here and I'm just gonna run this from within the id. So let's copy root user and then we will echo

[00:01:40] this and let's instead of just echoing the string. Let's also add the dash end here to prevent adding any new line characters, echo, prints a new line character by a default here. And this can lead to some problems when using these values with mongo. So let's just echo this and pipe the results into base64.

[00:02:00] And this is then what we will use here. I'm gonna keep this commented on the side so that we know the string value here. Let's do the same for root password. So we're gonna also use it here, root password like so and then this is also not adding a new line character. Let's just copy this here and let's paste it here.

[00:02:21] Oops, let's just keep the root password. Also commented here. Perfect. So in the real life, you would never commit this file to the repository. I will commit this because this is a course and you just, you might want to refer to the code outside of the lectures at your own time. So it does make sense that you have access to the entire code. But in real life.

[00:02:42] Once again, you would never commit such secrets. You would either create them via the cube cut C or you would store in an external secret solution like vault or secrets manager. And then it would also ensure that you set the proper protection, the proper access control around which users can

[00:03:01] actually retrieve the values for these secrets. Let's then copy, paste this because now we will create the credentials for our color db, right? So that's gonna be our color db, Alexa. And then here we'll change the name from MongoDB to color db credentials. So MongoDB color db credentials,

[00:03:20] the app will also be MongoDB here. But now instead of root user, we're just gonna use color db underscore user and we will use color db underscore password. Let's then do the same here. We're gonna paste the value here, call db user like, so let's then copy this, paste it here

[00:03:42] and let's do the same for the password. So let's add this here, call db password. Perfect. Let's copy this, paste it here and then we have our secrets created. Now, one reason here, just one comment around why we are actually splitting these secrets.

[00:04:00] And this is really from a cluster management perspective, right? So think about it from the perspective of whoever is administering the cluster and managing users and also probably deploying MongoDB services here that the applications can connect to that person would probably not want to give everyone or all the developers access to the root credentials used to create the database.

[00:04:24] This would be something that is very serious and very permissive. And anyone with root credentials could do a lot of operations in the deployed MongoDB. So what the administrator would probably do is to keep the root credentials here under a more strict access control rule or

[00:04:42] basically just give this access to certain users and the color the B credentials since they are intended to be used by applications, then the administrator would let's say give access to the developers working on whatever application needs to connect to call db, they would give access to these secrets,

[00:05:00] but they don't need to give access to the root credentials here. So this would not be possible if this both these sets of credentials, they were under the same file. That's why I'm splitting this year because in terms of managing access, it makes sense that we separate this because then we can be much more targeted and intentional regarding who gets

[00:05:21] access to which sets of credentials. And if you also were to deploy another database, another Mogo DB database, then it would create another set of credentials and it would give only those users that need access to that database access to these secrets. Perfect. So hopefully, this small explanation helps us understand a little bit

[00:05:42] more why I split this into two files. And hopefully, you can also understand the motivation regarding creating things that need to be protected at different levels in different files and different objects. Let's save this here. Let's then come to the terminal.

[00:06:00] Let's simply apply all the contents of our directory here and we can confirm that both secrets were created. So for to get the secrets here, then we should see that both of them are created. That's looking good to me. So let's take a short break and come back in the next lecture to continue with our project.