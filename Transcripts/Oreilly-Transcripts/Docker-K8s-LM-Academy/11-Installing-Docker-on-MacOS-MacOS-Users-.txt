[00:00:00] Hello and welcome back. Let's now go through the steps to install Docker desktop on Mac. You can just open the Mac install page here in the Docker documentation and you'll be presented with a choice between either installing for Apple silicon or for Intel. I'm gonna go with the choice that I have at the moment. And then after you have downloaded the installer,

[00:00:21] you can just click on it very standard, installing application, installing an application in Mac Os. So you can just drag and drop Docker into the applications folder. And after you have done that, you can just look for Docker and then open Docker desktop. Once you open Docker desktop for the first time, you will need to access, accept the service agreement.

[00:00:41] Let's just click on accept here and then we will be asked to finish setting up our Docker desktop. We're just gonna go with the recommended settings. Let then let's then click on finish and we might be prompted to add the password multiple times. And after we completed the installation, we will be presented with this screen, we can just click on continue without signing in.

[00:01:01] We can also skip the survey here and then that's pretty much it. We now have Docker installed in our system. We can now confirm that we have a Docker installed by running Docker version like so. And we can also try to run a very simple container Docker run Hello world. This should then go through and print a very simple message here and

[00:01:23] that's enough to confirm that Docker is successfully installed in your machine. Awesome. Let's then take a short break and come back in the next lecture.