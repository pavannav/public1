[00:00:01] Welcome to this lecture on running containers at scale. And this will help us understand how coerts comes into the picture and why Docker by itself is not enough. The question is why can't we just get by with Docker? So Docker and Docker compose alone, they are enough if we want to run individual

[00:00:21] containers or very small systems or create development environments. But when we look at the requirements of production workloads, then Docker and Docker compose, they really fall short in several fronts. Let's have a look at some of this. The first one is container scheduling. Whenever we want to schedule containers with Docker and

[00:00:42] we want to distribute them among different services. We have to do this manually. This is both time consuming and error prone, which means that Docker doesn't really encourage us to distribute our workloads across servers for higher resiliency Kernis.

[00:01:00] On the other hand, makes this process very easy by allowing us to define declaratively the criteria that we want to use to distribute the pods around different nodes. And this is in turn handled automatically by CNIS behind the scenes based on the resource definitions that we provide, we will mention a couple of terms here such as pods and deployments in cos.

[00:01:23] And if these terms are not entirely clear to you, don't worry, this first set of lectures is gonna cover all these architectural components as well. So hopefully by the end of it, you will be familiar with all the cobs terms are important for you to work effectively with it. Another challenge of production workloads is

[00:01:41] balancing the load across different containers. Docker has no built in load balancing mechanism and even though we are able to reach out to containers based on their name, there is no abstraction between the container and the actual name that we can use to talk to that container in

[00:02:02] Cnis. There is the concept of a service which provides us with this built in load balancing. We can basically reach out to the service and the service will be sitting in front of one or more pods and the service will take care of redirecting the requests and balancing the load across these pods.

[00:02:20] A third challenge refers to scaling applications. So how do we actually add more containers effortlessly to our system? Of course, we can always change the configuration manually. Here with Docker, we can create new containers. We have new names, you can update the application code, we can do a lot of adjustments, but these adjustments are primarily low level.

[00:02:40] So we need to really get very fine grained in the configuration for directing this load to newly created containers. Kernis makes this auto scaling process much more intuitive and much more hassle free by using what's called a horizontal pod, auto scalr. And this can scale pods horizontally.

[00:03:00] That means creates more instances of the same pod configuration. And this can be done automatically through real time metrics. Think about it. CPU memory or even some other custom metrics that you might choose to use self healing is another very important aspect. What happens when an application becomes unhealthy.

[00:03:20] Even though Docker has the concept of restart policies, these restart policies, they are very crude. They are just looking at the exit status of a container. That means if a container becomes unhealthy but it doesn't crash, then a Docker is not really able to identify, it's not able to detect nor to restart the container or the unhealthy workload.

[00:03:41] Kernis. On the other hand has many mechanisms that enable a very robust self healing system, health checks, continuous monitoring, as well as several other features such as rescheduling pods from a failing node. These all contribute view towards more robust self healing mechanisms in terms of service discovery, even though we have mentioned that yes,

[00:04:03] Docker allows us to communicate between containers by using their names. This is not really suitable for scale. There is no built in solution for service discovery in Docker. While in Cubans, we do have an internal DNS that allows for robust service discovery through services.

[00:04:21] Once again in this high level lecture. I'm just comparing the two here and showing which constructs which abstractions in Kernis can come to our rescue. I'm by no means expecting you to be able to create and implement these things already. Don't worry about it. We're going to cover all this throughout the course. But right now, it's important to understand which

[00:04:41] certis components relate to each of the challenges in each of its benefits for production workloads. But at least in terms of configuration management here, Docker does also require low level configuration and adjustments in case we want to really replicate configuration for larger applications or across multiple environments.

[00:05:01] While Kubernes provides several constructs such as config maps and secrets that allow us to decouple and to scale the application configuration. And really this is just a very high level overview and I'm talking here plain Cnis, right? I didn't really enter into the Cnis ecosystem. There are thousands of additional tools that can be run in

[00:05:24] Cnis. Kernis is very extensible by default. It is designed to be extensible. That means that beyond Kernis, there is a huge ecosystem that tackles even more production workload challenges. Let's then take a short break and come back in the next lecture.