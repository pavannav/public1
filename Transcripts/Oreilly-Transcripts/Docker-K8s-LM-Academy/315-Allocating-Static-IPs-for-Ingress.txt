[00:00:00] Hello and welcome back. So far, we are able to communicate with our caller API application via the IP address here. But we don't really have a domain that is actually mapped to this IP address and that we can use to provide our users with a user friendly, with a human friendly name that they can use here.

[00:00:20] Additionally, this is not secure, right? As you can see here, there is no certificate that is backing up this website here and certificates they are actually issued to domain names. So Google Cloud offers us a couple of ways for us to tackle that. And what we will start implementing in this lecture is the usage of managed certificates from Google cloud and the managed

[00:00:44] re also from Google cloud and from GKE to make this connection from our domain name to our underlying color API applications possible. So the first thing we need to do here is to get a good understanding of how this overall domain structure works,

[00:01:02] right? So if we have here, if we just add domain, we have cloud domains here. So now I have two domains, you will probably just have one if you have registered the domain here. So let me zoom out a little bit. I think it's a bit too much. And then if we come here to the actions and we click on edit DNS details,

[00:01:20] we will be taken to this page here. We have the cloud DNS zone mentioned here and that means that we can come to the cloud DNS here to have a look at the DNS records. So if you click on the So colorful.com here, we will see that we have 2D NS records here, two different types,

[00:01:41] but none of them is actually mapping to any IP address specifically. So the first thing we actually want to do is to have stable IP addresses that we can map a certain DNS name a certain domain here to that stable IP address in Google cloud we are able to require here.

[00:02:01] Let's have a look if I type IP, that's exactly here IP addresses. And then we have the option here reserve external static IP address. What this will do is it will reserve a public IP address that will be stable globally. So when we reserve a static address, we then need to specify a name.

[00:02:22] And then we can say here, let's allocate one for our DEV application and let's add here a description. So global static IP address for DEV color API. Now here let's scroll down. We can leave this as premium and let's now select global here,

[00:02:40] right? So when we are using the managed certificates from GKE and the increase from, we need to reserve a global static IP address. We don't need to worry about the pricing here. This is also covered in our free trial. Let's now click on reserve and this will take a little bit of time to provision.

[00:03:01] Let's also reserve another one here that's going to be for our prod color API PROD and then here just for us to stay on the same line. Let's just get here. The same description in the description here is just going to be global static IP address for the broad color API.

[00:03:20] This will also be premium IPV four here. Let's select global as well and let's click on reserve. So as you can see here, we have allocated two IP addresses. Let's now come back to our cloud DNS service here. Let's open this in a new tab and let's now select our so colorful dash com here

[00:03:41] and let's add a couple of standard DNS entries. So the first one is going to be without any prefix here. So just the apex domain and we're gonna redirect this to our call API product. So I'm just gonna copy this IP address from here. I'm gonna paste it here and we can also add another item or no, actually, that's not here.

[00:04:02] So that's just this one. Let's then click on create, we will add another standard entry here for the www and we will use the same IP address, right? So this is the one from the prod color API. Let's also click on create here and let's add a third one to the DEV dot So colorful.com

[00:04:24] and this one will actually reach out to the DEV color API IP. So let's copy this and let's paste it here and click on create. So now we have the DNS entries at our DNS zone here that it will actually redirect the traffic from

[00:04:41] the DEV dot So colorful.com to our underlying IP address. Now this is not going to work, right? So if we click here, this IP address is just created here. It is reserved, it is not in use. It is not really assigned to any underlying pods or services or ingresses in our

[00:05:04] Cnis cluster. So if we were to just try to visit this address here, dev dot So colorful.com, there is just nothing behind it, right? So it's just an like an IP address that will be resolved here. We can also see it not secure, but there is nothing behind this IP address that is able to serve http requests.

[00:05:23] So there are still a few things that we need to set up and we're gonna continue with that in the next lecture. But just before that, let's take a short break and I'll see you in the next video.