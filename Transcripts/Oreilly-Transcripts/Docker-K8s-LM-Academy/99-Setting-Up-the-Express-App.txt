[00:00:00] Hello and welcome back up until now, we have set up our database container, our Mongodb container. And in this lecture, we will start setting up our express application just before we jump into that. Let's confirm that by just running the start DB dot S script, we are able to get our database up and running.

[00:00:22] Perfect. So the command went through successfully. If we were to run a Docker PS here, then we will get the result that our container is up and running for already 11 seconds. We can confirm connectivity by running the Docker run RM

[00:00:40] name here. We can say Mongo shell or Mongosh. We would like an interactive shell. We would also like to attach to the network of this container and the network name is stored under our network file. That's key value net. Let's just copy this, come back to terminal, paste it here. And then we would like to inform also the image which will

[00:01:02] be just the same one as we get for the server here. We are just interested in running the shell but the image stays the same. So we have this image and then we would like to run the mongo command inside of this image. And we would like to connect to our local Mongodb

[00:01:20] installation. So let's just pass here, Mongodb colon forward slash forward slash. And when I say Mongo local Mongodb installation, I mean the container that we have here up and running, we would then like to use our user and password that we have created. This is inside of our start DB dot S

[00:01:40] file. Here is the name of the user. Here is the password and here is the name of the database. So let's just come back here. And we're gonna say key value, dash user colon, key value, dash password at mongodb forward slash key dash value, dash db. In the moment we do that,

[00:02:00] we will get a shell running in our local database here in our key value database. And we can confirm that we have permissions by running the show collections operation. If we were to use another database, the admin one for example, and we were to try to list the collections inside of the admin, we will get an unauthorized error.

[00:02:20] Perfect. So we can confirm that by running a single command, we were able to spin up our database. So let's have a look here. Once again, the database is up and running and let's keep it that way for now. So let's keep the database up and running and let's start setting up our express application. I would like to set this up inside of its own directory.

[00:02:40] So I'm just gonna make a new directory here, back end, let's change into this folder and the process here, you are already very familiar with it npm. In it dash Y this is gonna start an empty package dot JSON file and we're gonna install a couple of dependencies. We're gonna install express at 4.19 0.2

[00:03:01] mongoose at 8.5 0.1. This here is the library, the package we will use to interact with our MongoDB database. We would also like to send JSON requests and parse them. So we're gonna use body parser at 1.20 0.2. And we would like to save exact these dependencies so

[00:03:22] that these exact versions are saved in both our packagea and our package log toa files. Let's run this command and wait for the installation to complete. Perfect. Once the installation is complete. Another thing that we can do here, we can actually add the node modules folder already to our

[00:03:41] Docker dot Docker ignore file and this will just make sure that we are already immediately ignoring our node modules folder. Whenever we will build our image, let's now come back to the ide and let's start writing our code over there in the IDE. Let's open our backend folder and within this folder,

[00:04:01] let's create yet another folder that's gonna be called the source or SRC folder. And we're going to just have index dot Js or we could actually call this server dot Js like. So in the package dot json, we will start, we will add the script here, start and this script will do nothing more than just call our server dot JS.

[00:04:23] Perfect. Let's now open our server dot Js and we're gonna start coding our application. So we say express is equal to require express, we then also will import mongoose. So to say mongoose is equal to require mongoose

[00:04:41] and we will also import our body parser even though we will not parse anything in this lecture. I just want to already get the requires here at the top level all in order. Perfect. Let's then initialize our app. Constant app is equal to express. And we will say first thing app use bodyparser

[00:05:01] dot json and this is a function. So we do need to call the JSON function. Perfect. We can now try to connect here. So let's say mongoose connect and we would like to use the same strategy as we have used for the container that we were running. Basically, we would like to form the mongo DB name of the container that we have running for our database.

[00:05:25] And then we would like to expect this Mongos dot connect to be able to reach out to that container and connect to the database. Let's start by adding here just the address. So we will improve that in the future, we're just going to hard code it here. So we'll say Mongo DB,

[00:05:40] colon forward slash forward slash MongoDB. And then we will say our key dash value dash db. And here instead of passing the authentication in the connection string, we can actually use another parameter. We can use a few options here to our connect options. One of them is the off option.

[00:06:00] Let's add the off option here. We say username will be our key value dash user and password will be our key value password. We can also add a connection timeout in milliseconds here. Let's say 500 milliseconds. So have a second here. And once we have this,

[00:06:20] we also would like to get some feedback on whether we are actually connected to the database or not. So this returns a promise and we can hook into the resolved value of this promise by adding a then method here. And we simply say console dot log connected

[00:06:40] 2D B and if there are any problems. So let's just add here another catch method. If there are any errors, then we would like to console dot error, the errors that we receive. Perfect. Let's then create a single route for us here. We'll say app dot get and we'll simply say forward slash

[00:07:01] health here. And the only thing that this will do is this will receive here the request and the response object and all this will do will simply send a response status 200 dot send and we're just gonna send the up string here. Let's move this up to before the declaration or before our mongoose connect.

[00:07:25] So we're just gonna add this here. And now we would like to start listening not independently of this mongoose connection, but we would like to ensure that this mongoose connection is up and running here before we actually start listening.

[00:07:41] So we're going to hook up into the then method once again and within the then method here, we will say app dot Listen, so sorry, app dot Listen. And then we will say here, let's for now here, hardcode the port 3000. And then we'll simply say here console dot log

[00:08:00] listening on port 3000. And so this is a very basic setup here. What happens is we are declaring our application, we are setting up the routes, then we are trying to connect to our database. And if the connection is successful, if we are within this, then clause here,

[00:08:21] then we will start listening on port 3000 and receiving requests. If we were to start listening before the connection takes place, there might be a small delay and we would not be able to start serving requests that require database access from the get go. That's why we are first of all connecting to the database

[00:08:41] and then starting our server and starting to listen for requests. We can also add a few more console logs here just to see what will be printed on the screen. So we say connecting to db and then we can also say here in the error, we can say

[00:09:00] something went wrong. So let's say console dot log or console dot error, something went wrong, right? And then this will finally print the error here. So this is all we need for. Now, let's save this file. We have already defined our start script here.

[00:09:22] And let's see if this is gonna work by just running NPM. Start inside of the back end folder back in the terminal. Let's clear the console and let's simply run NPM. Start. And the moment we do that, we will see that we actually get a connecting to the B console or logged here to the console.

[00:09:40] However, we are not getting any feedback back. We are trying to find the Mongodb database to connect to. So basically, the Mongoose connection is trying to figure out where the database is, is trying to solve that address. But then after some moments here, after a few seconds and a few retries that happened here,

[00:10:01] then we will see that we get printed, something went wrong. And basically, we are not able to find the Mongodb address. This simply means that whenever we are informing this mongo DB host, if we were to come back here to the ide and open the server dos, whenever we are informing this MongoDB host here,

[00:10:21] by default, we are not able to really figure out where it is. And that makes sense because for us, we know that this is the name of a container. But within our machine, there is nothing really where we are not informing anywhere that this host here is to be resolved to our container. The way for us to do that is to build a Docker file.

[00:10:40] So to create a Docker file, build an image for the back end, create a container based on that image and then connect to the same network where our mongo DB server is deployed. So let's tackle this in the next lecture. But before let's take a short break and I'll see you in the next video.