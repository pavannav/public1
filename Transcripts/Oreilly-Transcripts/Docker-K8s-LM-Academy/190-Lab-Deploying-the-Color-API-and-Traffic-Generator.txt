[00:00:00] Let's now start exploring services and communication with running pods in a little bit more details. I've created a new directory here. The directory is empty. There is nothing here inside of the directory. So if you wish to also start with a clean slate, then make sure to do so. And let's jump to the ide to create a few Kubernes

[00:00:21] definition files within the same directory here. The first file that I wish to create, it's called color API deployment. So I'm just gonna call it like this dot YAML. And once again, we're just gonna follow the traditional API or deployment definition here. So the API version is going to be a

[00:00:40] forward slash V one. The kind is going to be a deployment. The metadata here will contain the following information, the name that's going to be color API deployment. And then the labels here will simply have an app and the app will be our color API.

[00:01:00] Let's now define our specifications for the deployment. So the number of replicas that we wish to have here, let's start with five replicas. And the selector that we will use to manage these replicas is going to be match labels. And then the label here is going to be app and then call API like. So

[00:01:22] the template we will use for a new pods is a fairly straightforward template. The metadata here will contain our labels, app call API as well. So once again, don't forget this here should match the label here that we are providing. Otherwise the deployment will not be able to manage

[00:01:41] the pods correctly and then the specification for our pod and we will simply pass a list of containers for now. So the name of the container here will be color API. And the image that we will use is LM Academy forward slash color dash API version 1.1 0.0.

[00:02:01] If you follow the previous videos and you have the caller API deployed and pushed to your own username, then by all means feel free to do so just make sure that it matches the version 1.1 0.0 that is available under my username. Then here we will also advertise the container port or ports here with a colon and then the container port

[00:02:24] will be port 80. That's great. Let's save this here. Let's come back to the terminal and let's now apply this file. So keep apply and we will simply pass a file here. The file will be color a deployment. Let's apply this, we can confirm that the

[00:02:43] pods here were created. So cube cut, get pods and I have a few additional pods here from earlier testing sessions. So let me just delete them so that we don't need to worry about them, delete pods. And I'm just going to pass here, Alpine cURL call API

[00:03:00] and then traffic generator. So this is just gonna terminate all the pods. It might take a while here. I'm just going to wait a few seconds and now we can clear the screen, confirm here that we have only the deployment pods up and running. If we get once again the deployments, we get only a single deployment. And if we get the replica set cuo

[00:03:21] get replica set, then we can confirm that the replica set is up and running as well. Perfect. I now wish to start a pod here which will generate some traffic to one of our pods that were created by this deployment. So let's just inspect this first pod here. Cube car

[00:03:40] describe pod and we pass the pod name. We're gonna get the private IP address. This is the IP address we are interested in. So I'm just gonna copy this here and let's go back to the ide to create the definition file for our traffic generator in the id. Let's create a new file. I'm just gonna call this traffic generator dot YAML

[00:04:03] and this will be a single pod, right? So we don't need to create any deployments with this here. So the API version will simply be V one, not apps V one but just V one, the kind here will be a pod, the metadata. So we can also leverage this. Let's already put traffic generator

[00:04:22] and let's now erase this additional duplicated stuff here. Perfect. Now we have the label, let's just change this to app like so and then we have here traffic generator. We don't need to advertise any containerports here. So let's just remove this. We will also not define any resources for now.

[00:04:42] So just the name, the image is going to be LM Academy forward slash traffic generator. And the version here will just be 1.0 0.0. Once again, if you are pushing this under your own username, then by all means, feel free to use that. And we also need to define a few arguments.

[00:05:02] So remember the traffic generator starts a script which expects two arguments. The first one is the end point for us to hit and the second one is the interval between the call request. So let's pass a list of arguments here. And the first argument will be our pod address like so

[00:05:22] I'm going to use the IP address and then forward slash API to call the forward slash API end point. And then the interval here that we will use is simply going to be 0.5 seconds. Let's save this here. And now let's go back to the terminal and let's create this pod

[00:05:40] in the terminal. Let's clear the console we're gonna run cube cut, we're gonna run apply once again. But now we'll simply pass the traffic generator dot YAML. We can then get the logs of this pod and we can also follow the logs by just adding the dash F here. And now, if we paste this here, then as you can see,

[00:06:01] this is exactly what we are getting right. So just getting here, the name of the pod back and the color is then the blue color. Let's try a little experiment here. I have split my screen into two. We keep having the logs here, but now let's just delete the first pod of our deployments.

[00:06:20] I'm just going to copy the name here. We're gonna scroll down and then we'll simply say cube cut delete pod and we're going to pass this here. And the moment we do that, you can see that our traffic generator, it basically stops working, right. So it's like not able to reach out to reach the pod anymore. And this is because

[00:06:40] the traffic generator is actually bound. It is now querying a private internal IP address of that specific pod of this specific pod here. If this pod is down, then whatever application was communicating, must talk to this pod is not able to talk to the pod anymore, it's not able to work. So our first step is to explore how cluster IP services can

[00:07:05] be used to provide a stable IP address that will abstract away the individual IP addresses of pods. You already know that even if we were to get here, the pods that are running in our system, we get a new additional pod here.

[00:07:20] We have a new pod that was created roughly a minute ago as a replacement to the pod that we have deleted. However, because our traffic generator is bound to the IP address of the pod that was deleted. It's of course not able to identify that there is a new pod with, of course, a new IP address.

[00:07:40] So it's not able to communicate with that. It's best for us if we can provide a layer of abstraction here. And that's what we're going to do with cluster IP services. Great. So let's do a little bit of clean up here. Let's just stop following the logs. Let's remove the traffic generator pod. So delete pod, traffic generator. Let's delete the spot. It's going to take a while here.

[00:08:02] We can leave the deployment running if you wish to do so, we can also delete that. That is up to you. If you delete the deployment now, you will need to recreate it at the beginning of the next lecture. So I'm just going to leave it running great with the traffic generator pod deleted. I believe that's a good moment for us to take a short break and let's come back in the next lecture.