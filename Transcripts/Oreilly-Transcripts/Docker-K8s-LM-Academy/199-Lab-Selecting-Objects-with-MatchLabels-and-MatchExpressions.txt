[00:00:00] Hello and welcome back before we get started. Let's just make sure that we delete all the resources we have created within our label selectors folder here. And I would like to go back to the ide and create yet another file to define a deployment in the ide. Let's create here. Once again, color deployment. Let's call this color

[00:00:21] depold dot YAML API version will be apps you want and then we will have a kind of deployment just gonna leverage the autocomplete here to help me with. That is going to be the color API deployment, right? So I'm just gonna use color API like. So let's remove the the application here on the top.

[00:00:41] You should be familiar already with all these things here. Let's just adjust here at the bottom. Make sure that the container definition is looking good LM Academy color A P 11.0 or whatever you are using here. We will also add a couple of additional labels.

[00:01:00] We're gonna say that the environment here is gonna be local and then we will also say that the tier will be back end like so, so very similar to the pods. We have created in the previous lecture. Let's also change the port here to port 80 I believe that the container specifications are good to go. And I want to spend a few minutes on this selector section here

[00:01:22] because this is something that we will see over and over again when working with CLIS and defining how to select and manage different objects under different circumstances. So here the example is a selector for a deployment to manage pods. But this is also applicable when we are, for example,

[00:01:41] establishing rules for pod placement in different nodes. So this pattern here you will see multiple times and it's good that we can use it to these two match labels and the match expressions configuration. Let's start with the match labels and this is very straightforward. So the match labels here, we just specify

[00:02:02] a bunch of key value pairs. This itself is an object or a map where each key is going to be a label of our pod and each value is going to be the value of that label. So basically, we're just going to copy all this and place it here under match labels.

[00:02:20] Let's remove this here like so and now we can also use more advanced filtering or selection operations here with the match expressions. So the limitation of match labels is for example, we cannot use in or not in operators, we cannot define complex matching rules and it might be the case that we are

[00:02:41] interested in using this more advanced logic here. One very common example that we come across often when working with infrastructure is that we have some sort of managed label that indicates that a certain resource is managed by some other application or third party software.

[00:03:00] So let's add this here, we're going to have a single, so the match expressions key here it takes a list, an array of objects instead of a single map here containing key value pairs, it takes a list of objects and each object has two or three properties. Remember the key property is here, this is going to define the label itself.

[00:03:23] We then have operator and the operator will define which operator we wish to use. So this can be in this can be not in, this can be exists for example. And if the operator is exists or not exist or actually this is does not exist, not,

[00:03:41] not exists. So if it is does not exist, then we only need these two properties. If the operator here is in or it is not in, then we need to specify another list here and it is a list of values, right? So values here that's going to be a list in itself.

[00:04:00] And then we'll simply pass here a list of values. This can be represented with the square brackets or it can also be passed as dashes here indented under values. So we could for example, say here match expressions and we could, let's say release and the release here is going to be stable. We could say that the release here should not be in Canary releases, right?

[00:04:24] So this deployment here is only going to manage stable pods. So it's going to exclude any release here. That is not canary. This might be a little bit tricky here, especially if we have multiple releases because this could end up leading this deployment here

[00:04:41] to manage other releases in addition to stable. Because let's say we have another release here, maybe some development release and this development release is then satisfying this condition, but it is managed by another deployment. So there could be overlap. It's very important that we avoid this overlap.

[00:05:00] So otherwise we are actually risking entering into very tricky territory here and Cnis might actually stop working as expected. One use case that I find interesting for this match expressions is to actually use the key managed here and just say, hey, if this key managed exists, then we are good to go.

[00:05:20] We are managed by a deployment. This might not seem much but the moment that we look at the color API here and the color back end, we can see that the set of labels with a color API environment. And here backend here, they are exactly the same as this set of labels here.

[00:05:40] So that would be an issue if we wanted to create this spot here. Individually from the deployment with this set of labels. So one way that we could go about it. And once again, I'm not saying that you have to do it. I'm just taking this as a learning exploring example. So one way for us to do that is to simply set the managed tag or the managed label here to true,

[00:06:03] for example, or to say deployment, it doesn't really matter the value that we add here because we are looking at the exist operator. So as long as the tag managed or as long as sorry, as long as the label managed is present in the resource, then this resource will match this expression here. All right.

[00:06:22] So let's see if we can save this. Let's come back to the terminal and let's try to apply the entire directory here to see what's going to happen back in the terminal. We're going to simply run cube cut, apply the entire directory here. And now let's have a look at our pods. And as you can see here, we have one pod color back end that was successfully created.

[00:06:43] And we have also pod that was created for the deployment. We didn't specify the number of replicas in a deployment definition. So let's go back to the ide and let's set this option in the ide. Let's set replicas here. 23, let's save this and let's come back to the terminal in the terminal.

[00:07:01] Let's now apply this once again. So cube cut apply. Let's apply the current directory here. We have configured our deployment, we should now start scaling things up and as you can see, perfect, this is actually working as expected. So the color back end here, even though it has the same set of labels

[00:07:21] that are present here in our deployment pods, it's not being considered by the deployment and by the replica set here because it is not setting the label managed to any value. We could then visualize this here with LL tier environment,

[00:07:41] an environment like so and dash L managed the moment we do that we will see here. Oops that is not very clear to see, but so let's just remove a few things here. So let's remove the environment. And I believe that this would be a little bit easier. No, still need to remove one more. So let's remove the app since this is the same.

[00:08:01] Perfect. And that's a little bit easier for us to see. So as you can see the color back and it is in the backend tier. And if you have a slightly wider screen here, you can also see that all the other labels are matching, but it is not being considered by the deployment because it does not have the managed label here set to any value.

[00:08:23] Now this match expression just to wrap up this lecture here, this match expressions can be very powerful. So we can add also other elements here, let's say key tier operator, oops, that should be just a value. So operator should be in

[00:08:40] and oops sorry for that. That's autocomplete. And the values here would be let's say back end, right? So now or we could also say back end and cache, maybe let's say something like this, right? So we are just, I'm just showing you here. I'm not saying that you should do this with deployments specifically. As you might see, this can be very useful when we are selecting nodes to place our pods.

[00:09:02] But this is not something we would do with a deployment, right? So deployments, I would recommend not keeping such set based selectors here. I would recommend keeping them to a minimum and to simple operations because otherwise it might be hard to identify what's the actual scope of the deployment and it's much harder to avoid overlap between deployments.

[00:09:23] So keep it simple, just be mindful that we can get very powerful with this match expressions, configuration option here. And that's what I wanted to present and discuss in this lecture. Let's take a short break and come back in the next lecture.