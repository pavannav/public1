[00:00:00] Namespaces are a great way for us to isolate our resources within the cluster and create logical partitions. So let's start exploring how we can work with them. Name spaces are objects that means we can create them using declarative files. We can also create them via the cube cut.

[00:00:20] C let's follow the declarative style. So let's just create a new file here. We're going to call this DEV NS dot YAML and this name space here has one of the simplest, the clarity or one of the simplest configurations. So once again, a P version is there that's going to be V one. The kind here is there that's going to be name space kind is name space like so name space

[00:00:44] and the name space object has no spec section. It has a metadata section which has then a name that's going to define the name of the name space and we can set this to whatever we want. Let's save this here. So as you can see very simple here,

[00:01:00] let's save this. Let's come to the ide and let's apply the changes in the ide. Make sure you are inside of whatever folder you created the name space file. And we can once again, cube cut, apply f with the current directory and we can see that the name space DEV was created. If you now run the cube cut, get name space,

[00:01:21] this is going to return all the name spaces that we have in our cluster. And as you can see, in addition to the one we have just created, there are four name spaces that are shipped here by Cnis. The default one cube node leads cube public and cube system. Once we run any commands here with for example, cube

[00:01:41] get pods, we will see that it returns no resources found in the default name space. We can specify the name space for this kind of commands here. Actually for most of the commands in cuo by simply passing the N for the dash dash name space flag here and then passing the value of the name space.

[00:02:02] So we can see the pods that are running in the cube system, name space. And as you can see, there are quite a few pods here. This is all cerni software cube scheduler is here. The cube proxy is here, the controller manager, the API server, the Accord NS the E CD store. So as you can see, there are lots of pods here that are running

[00:02:21] and delivering most of the functionality of the coin system. So let's now create a new pod inside of our DEV name space in the idea. Let's create a new file here. Once again, let's just continue with our color API. I'm not gonna do anything fancy here. So let's just directly go for a kind of pod that's

[00:02:40] gonna provide a lot of auto completion for us here. Let's replace my app with just color API like. So let's replace all of them image once again LM Academy forward slash color A P 1.1 0.0. Let's remove the resources and let's set the port of our container to port 80 like.

[00:03:01] So for us to create a resource within a name space, we need to pass the name space option here under the metadata. So let's as name space here, DEV and let's save this. Let's now come back to the terminal and apply this file in the terminal. It just clear the screen, let's run cube cut,

[00:03:20] apply once again. And we will see that the pod was created. Now if you run cube cut, get pod once again, the pod is not running in the default name space. Therefore, it is not returned here. So we can just pass the DEV name space and we can confirm that our pod is up and running. It might be very cumbersome here to pass a specific name space with every command.

[00:03:42] Let's say that then we want to actually describe the pod. So let's describe our pod here. For example, call API. And if we forget to pass the name space dash N or dash dash, name space here. Then we get an answer like oops, there is no color API pod and that's because it is looking at the

[00:04:00] default name space whenever we issue the comment. So we will have to come here and the name space DEV once again and then we get the response. Now, this can be very annoying, very quickly. If you are working with either different name spaces or if all pods are inside of one name space, that is not the default, then you need to keep specifying that. Of course, we can

[00:04:21] set the name space for all our commands from now onwards by updating the context under which the cube is operating. So for that the command is cube cut config set context and then we're gonna take the current context so we could actually just have a look here. So cube config current context like so I believe that

[00:04:42] this will give us back our current context. So now we can use the set context here to set properties of our context. We're going to pass the current flag here to take into account our current context which is Minicube. And then we will set here. The name space will be equal to DEV. Now this is gonna set the name space DEV for our current content,

[00:05:07] which means that any cube cut command that we execute from now onwards will operate under the DEV name space. So cube K get pods that's gonna return the pods under the DEV name space.

[00:05:21] And now if we wish to describe a pod, describe pod color API, we don't need to worry anymore about always passing the name space. Now, this can be very good, right? But it can also be tricky. So just make sure that you keep in mind which name space you are operating under because it could be very easily the case here.

[00:05:40] Let's say that we set our, so we use keep config set context name spaces. And I was like, OK, the name space prod here doesn't really exist right now, right? So we don't have it here. Nothing is going to work. But let's say that we set this, we are operating on pod doing some get and describe here cri and so on and so forth.

[00:06:01] And then suddenly out of nowhere, we just decide to delete something because we think, OK, yeah, I am done testing some stuff on DEV. Let's now just delete our deployment here and that's a very critical deployment, right? So very critical deployment here. And we run this command thinking that we are in the DEV name space.

[00:06:21] But because we have forgotten to set the context, for example, we end up deleting stuff in prod and then, well, you know, the consequences, everything goes down, we have a bunch of things to fix and hopefully we still have a job tomorrow, right? So it's like be very careful when you set this configuration like this and make sure

[00:06:42] that you are operating under the name space that you intend to. Whenever you are running operations, particularly destructive operations such as cube coral digit, we can view which context we are operating under by running cube cattle config view like so and that's gonna return a bunch of stuff here,

[00:07:02] but let's just scroll up a little bit, you will see here that there is actually quite a lot of information. So the cube cut can manage multiple clusters to begin with, right. So first of all, you if you are working with multiple clusters, maybe you have some cluster from work or different clusters,

[00:07:20] ideally even different clusters for prod and non prod, let's say so they are completely separate. You might have all of them listed here. So we might have a bunch of clusters listed under cluster and then we might also have a bunch of contexts listed under the context key. What's important here for us is to have a look at the current context.

[00:07:42] So that's minicube here and we take this value and we map this value here to the context that has the corresponding name. So the name here is Minicube. This matches the current context and then we go into this object here and we get the name space and then we can see that this is the pod name space.

[00:08:03] All right. So let's just change this once again. We're gonna set our context. So config set context current name space is gonna be DEV once again and then we are good to go. We can confirm that we have pods running here by running cube cat. Get like so

[00:08:20] and let's now explore what happens when we delete a name space. So let's set our context name space to default back here. We can confirm that we get no pods. And actually just before we delete stuff, let me just show you one thing here. You can let's clear the console. You can also get resources for certain commands here across all name spaces.

[00:08:44] You should add a dash, capital letter A here or uppercase A or a dash dash, all dash name spaces. And this is going to return all the pods across all the name spaces that we have here in our cluster. This might be useful in case you just want to identify

[00:09:03] actually which name space a certain pod is running on. So let's suppose here that you have a certain pod that you know the name of the pod, but you don't know the actual name space where the pod is. You might use the all name spaces or the dash A like. So to get a list of all the pods and then you can identify ah OK,

[00:09:23] the pod is actually under the DEV name space. Good. Let's then set here our context to DEV and then continue with our operations right. So just cancel this operation here. Just make sure to cancel not to run this. And you can confirm that we still have here. Let's have a look cube

[00:09:40] cut config or if you actually run it. So just undo it by running cube cut config set context, name space, default. And then we are good to go. All right, deleting a name space. So cube cut, delete dash F and then let's pass the current directory here or actually no, no, let's let's not pass the current directory.

[00:10:01] Let's pass just the DEV dash and S here. So DEV, namespace dot YAML and I'm passing only this file here because we have multiple files inside of our directory. We also have a color API pod and I want to show you what happens with this pod here when we delete the name space.

[00:10:21] So just gonna run cube, delete F DEV dash NS dot YAML. And this is going to delete the name space. Now, what's your guess what's gonna happen with the pod? Well, if you guess that the pod is gonna stay there forever, that is not really what's gonna happen. And if you guess that the pod is gonna be gone. Well, then congratulations.

[00:10:43] The pod is just gone. So if we run our cube car, get pods, we will see node pods on the default name space. That's fine. But if we add the A here, our pod, our color API pod is gone. Forever. If you have resources running inside of a name space and you delete that name space,

[00:11:02] then the resources will be deleted. So be extremely careful about deleting name spaces. You should also use security role based access control to really limit who can delete name spaces inside of your cluster, actually who can manage name spaces altogether inside the cluster.

[00:11:21] But this can be a very, very destructive operation. Be very careful about it. All right. So that's it for our initial discussion around name spaces. It is more clear now how to work with them both in the configuration site. How do we create a name space declaring it in YAML as well as in the cube cut

[00:11:41] side? How we can use name spaces by either setting the current context here to be restricted to specific name space or by passing this name space as part of our commands. Let's then take a short break and come back in the next lecture.