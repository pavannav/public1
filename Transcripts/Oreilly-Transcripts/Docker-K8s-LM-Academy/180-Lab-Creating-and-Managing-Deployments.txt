[00:00:00] Let's now explore different aspects of working with deployments in Cnis. I'm starting in a new empty folder here. So there is nothing inside of the folder just naming the folder deployments. Let's then open the folder inside of our ide. Let's create a new file here. I'm just going to call this engine X deployment. I'm just going to call this

[00:00:21] depot without anything else just for brevity purposes. Let's call this or let's add the extension to E here. And then once again, let's populate the four top fields of a Kubernes configuration file. The API version we will use here is apps V one, the kind of object we wish to create is a deployment

[00:00:41] and we can also press enter here to populate some of the fields automatically just going to write them here. We then have the metadata, let's name this an engine X deployment and let's add some labels here. We're gonna say labels and I'm gonna say F and GX, right? So once again,

[00:01:00] same principle, the labels that are defined here. They are not referring to any labels applied to the pods created by this deployment. Rather they are labels that are applied to the deployment object to the deployment resource itself. Let's now define the spec section and this is going to be very similar to the spec section of replica sets.

[00:01:22] First, we start by defining the replicas here and then we will define the selector. So this selector will be used to match the pods and keep track of the pods by the replica set. We're going to use the match labels option and then we will define the app engine X label like. So we can then define the template here.

[00:01:43] And this template will be used to create the pods or to create new pods. Whenever the number of replicas here is below or actually, whenever the number of running pods that match this condition is below the specified replicas here. So the template is going to use a metadata section.

[00:02:00] We're gonna pass labels here and the labels will include and they have to include the same as we have in the match labels criterion here. Let's then specify the spec of our pods and this will contain a containers array here or containers list with the container being named X

[00:02:20] and the image here. Let's just use our engine X colon 1 27.0. We can also expose here the port container port 80 like. So the default strategy to roll out updates is the rolling update strategy. So here we would define the strategy strategy like so

[00:02:42] and then the type here can be either recreate or it can be rolling update. The rolling update is the default one. So if we do not specify this section here, then the deployment will use a rolling update by default. Let's keep the default for now.

[00:03:00] Let's save this file and let's come back to the terminal to then apply this configuration. Make sure that you have no pods created or at least be aware of the pods that are already running in your machine here. Let's now apply. So let's run cube cut, apply and we're going to use the engine X deployment file. Once we create this deployment here, we now get access to the deployment. So cube

[00:03:25] K get deploy, we can run this command. This is short for deployments, deployment, lots of work, that's fine. And as you can see here, we have our engine X deployment with five ready, five up to date and five available

[00:03:40] if we were to describe this. So let's describe the deployment X deployment. We can see more information about the deployment. We can also see the selector here as well as the rolling update strategy. So we see the strategy type here, rolling update. And this also defines percentages of pods

[00:04:02] that may be unavailable during the update. And the search here specifies how many pods or how much percent of the desired amount we can actually go over the desired amount. Remember when we tried to add or to create a new pod to a replica set that already had the three desired pods.

[00:04:23] We wanted, that pod got immediately terminated by the replica set. That's why we need to allow here some room for us to create new pods and then temporarily go above the number of desired pods so that the new ones can be created before the old ones get terminated at

[00:04:41] the bottom. Here, if we scroll down a little bit more, we will also see that there is one event where we have scaled up the replica set in the next deployment and then a hash here to five, we can also confirm that we have or that Kubernes has created a replica set behind the scenes for us. Here. We did not specify any configuration or we didn't tell Kubernes

[00:05:03] a create a replica set. But once again, a deployment is an abstraction on top of a replica set that means the deployment creates and manages replica sets the same way that replica sets, create and manage pods. We can also confirm that we have several pods up and running.

[00:05:20] These are the five pods here and let's get the configuration of one of the pods. We're just gonna run the cube cle get pod, we're gonna paste the pod here and let's output this as a YAML configuration. Let's scroll up a little bit more here and then we will eventually get to the section that specifies the labels of this pod as you can

[00:05:43] see here under the labels we have the app engine X label that we have provided before, but we have also a new pod template hash label this specifies. So basically this is the same label here. If we scroll up a little bit more, this is the same hash that is in our replica set.

[00:06:03] That means that the deployment is actually looking at the template, the contents of the template section and it is hashing these contents into a unique or into a single string. If the contents of the template change, it will automatically lead to a new hash because that's the whole point of hashes.

[00:06:22] So hashes should change if the content of this template section of our deployment definition changes. This is also how the deployment is able to link pods to replica sets and to manage updates in the template in an automated fashion. If we scroll down a little bit more here,

[00:06:40] we will see that there is quite a bit of configuration around the specifications of the container and a bunch of other things here that we did not specify. And this is really all added and managed by Cnis.