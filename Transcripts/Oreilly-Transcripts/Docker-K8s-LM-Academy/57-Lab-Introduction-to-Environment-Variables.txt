[00:00:00] Hello and welcome back. It's very often the case that in locker, we want to pass some configuration or some information to our containers via environment variables. Let's then spend some time discussing the different ways that we can pass environment variables to our container. I'm starting in a new empty folder here.

[00:00:20] So if you wish to do so, you can also do that or you can also use the code from previous lectures and projects and try to also expand on it. Once again, I'm just going to start everything from scratch. So we're going to write a very simple express application and we will apply some of the concepts we have learned so far throughout the course. Let's start by switching to the terminal here.

[00:00:40] Make sure that you are within the same folder where you have open in the ide the TY folder. Let's run an NPM init Y here so that we initialize an NPM project and we can install NPM dependencies. Once we do that, we just get this message here on the screen. We're, we're not gonna do anything fancy regarding directory structure.

[00:01:02] Let's just install here, the express dependency and we will use the version at 4.19 0.2. So let's just make sure we all use this version. We are on the same page here. Let's add a flag save exact so that this exact version is saved in our package touches on file.

[00:01:24] After we do that, we have all the dependences we need for this little discussion. I'm not gonna actually parse any request body here. I just want an express application to respond to a single endpoint. If you are using and building on the code from the containerizing and express app project,

[00:01:41] then you can skip this step here. Don't need to install this again because this is already installed. Let's then come back to the ide here and we will see that there are a couple of directories and files created. Once we have initialized our project and also installed the dependencies, we can then create another folder. Let's let's call this source and let's create just a very

[00:02:01] simple index dot Js file within this source folder. What this will do it will just have an express app. So we're gonna say constant express is equal to require express. We will then initialize the app here. Constant app is equal to express and then we can define our port here.

[00:02:20] So the first thing we will do is we'll simply say port is equal to 3000 for now we're going to hard code it. And later on, we're gonna receive this via environment variables. Let's then define a route here app dot get forward slash and then just the request handler here. So let's say that's gonna be the request. This is gonna be the response object.

[00:02:41] And then whenever we are hitting this endpoint, we just simply say here, response dot send hello from express like so and now we just need to start up this application. So we will say app dot listen on the port. And then we have then here a callback function that will be executed once the listen is completed. And then we simply say

[00:03:04] console dot log server listening on port and then colon and here let's change this to PC so that we can interpolate. You could also just add a plus there and then sum up basically the value of the port.

[00:03:21] But we're just going to interpolate this here with port. Perfect. So let's now add here in our package to change on just a simple start command. Let's come under the script section and we're gonna add here, start and this start command will simply execute node source forward slash index dot Js. We're missing a comma here at the end.

[00:03:42] The moment we add that that should be fine. So now let's execute this in the terminal back in the terminal. We simply run NPM start and then we get that the server is listening on port 3000. If we were to split this terminal into two and send a call request to our localhost port 3000 here.

[00:04:01] Then we should get back. Hello from Express. That's perfect. Our application is working and we have all the code that we need to start exploring environment variables. Let's then stop our application and let's close this additional terminal here. And let's go back to the ID in the ide. Let's create our first version of the Docker file.

[00:04:22] So now we want to dockerize this and the process should be fairly already familiar to you since we have Dockerized express applications before. So we're just going to start with the node image here base image. Let's work with 22 alpine and then let's add the work working directory here to be forward

[00:04:40] slash. We are interested in copying the package and the package lock JSON file to the current directory. We will then run an NPM C command to install all our dependencies. And then let's just copy all the folders, the same directory. Now this once again, remember this will also copy Docker file and node modules.

[00:05:04] So we can also create here what we have just explored of a dockerignore. And then we can simply say I do not want to copy the node modules folder. And that's because this folder here will be created. It will be already created by the NPM command

[00:05:20] when we install the dependencies. So it doesn't make sense for us to copy it from our local machine. Additionally, when our application has lots of dependencies. Then the node modules folder might have several 100 megabytes. So copying it will also be a heavy operation and will make the build process slower. So we have here copy all the files from the current directory and mirror its structure,

[00:05:42] which means that the source folder will contain the index dot Js file. So we'll just simply have the CMD the command here. We're gonna execute node and then we will say SRC forward slash index dot Js. Perfect. This should be everything we need to build our image. Let's then jump to the terminal and try to build the image

[00:06:02] back in the terminal. Let's clear the console and let's run Docker build, let's tag this here. We'll simply say express dash app, dash M variables or M VS to be a little bit shorter. And we can also if you want to add the image here at the end, just to differentiate

[00:06:20] you can do. So for me, this is already good enough. So let's just add a dot here and wait for the build process to complete. And Oops Docker is not running for me. So let's just start a Docker here. And then once we run the build command, we should be good to go. Perfect. The build completed successfully. We have now our Docker images here. We have the Express

[00:06:40] app and vs and you can see this is actually a little bit dirty. This has some images from previous lectures. So I might do some clean up here if you haven't done. So please feel free to go ahead and do it. Let's just start up an Express application here. So we're gonna say Docker run dash D. Let's map the port 3000 here to port 3000 on the container.

[00:07:01] This is the image that we want to run. And once we do that, we get our container up and running on the background. So if we were to just run a cURL to localhost, we will get the hello from express. Great. So our image and our container are working as expected. Let's just copy the hash here because I want to kill this process.

[00:07:20] So Docker kill Excel. And now if we were to crawl once again, then we do not have anything running on port 3000. So we are good to go. Let's now come back to the de and let's define our first environment variable. Since this lecture is already getting a little bit long, let's just define a default environment variable value here for our port.

[00:07:42] So we have the run npm CI we then copy all the files. And then here we can define our environment variable and simply say N part is equal to 3000. When we add this instruction here, then this environment variable will be available at runtime for our container.

[00:08:01] So now in our index dot Js file, we can actually say that the port will be equal to process dot m dot port, right? So let's try to make this change here. Let's save this. This will have changed the index dot Js file. So now let's come back to the terminal. Let's rebuild the image and let's make sure that

[00:08:21] it's working and to make things even more clear, let's actually change the port because this 3000 is the same as before, right? So let's now here. Just put port 5000. Let's use the 5000 value here. And then we will, once the server starts, we should actually get the port logged here. Perfect. So back in the terminal, we're gonna run Docker

[00:08:41] build. Once again, we can use the same tag here or we could also pass an additional tag and say port. So port 5000, for example, port 5000, let's build this, let's clear the console, inspect our images here and you will see that we have now this express app

[00:09:00] and vs port 5000. So we would like to actually run this here and we will add a colon, we're gonna say port dash 5000. And now we need to change the ports that we map here, right? So let's map our port 5000 to the port 5000 of the container. We could also map our port 3000, the port 5000 of the container.

[00:09:20] That's possible, right? But let's not make things complicated. So once we do that here. We can also add a name for us to make it for, for it to make it easier for us. So we're just gonna say express dash 5000. Let's start running this. And now we can actually check the logs of our container express 5000

[00:09:40] and we can see that the server is listening on port 5000. Perfect. So our container is actually able to access the value of the environment variable that is coming from our Docker file. If we were to crawl once again port 5000 here, then no problem. We get the answer back from express once and just to summarize, we are accessing the value of the port here

[00:10:05] by accessing environment variables in the node process. This is the syntax for us in node Js to access environment variables and then we simply add a dot and pass the name of the variable. This name here is matching

[00:10:20] the name of the environment variable that we are passing in our container here. This instruction is setting a default value for the port environment variable. But there are many other ways that we can pass environment variables to Docker containers. So let's take a short break and come back in the next lecture where we continue exploring environment variables.