[00:00:00] Hello and welcome back. Let's now continue with the setup of our application and we already have something up and running for our notebooks backend. Let's then focus on our notes. Back end the setup of our notes backend will be very similar to the notebooks backend. So we're just going to copy, paste a couple of files here and make some small adjustments.

[00:00:22] I'm gonna take the Docker file dot dev, the compose the M and the dockerignore. I'm gonna copy this and I'm gonna paste inside of our node backend folder. We will also copy the DB config folder and we will paste again inside of our nodes backend folder.

[00:00:40] Let me expand the side a little bit so that we can see it a little bit better. Basically, the structure of these two folders is exactly the same. We have db config with our Mongo init script inside of it. We also have a source folder that's here. And then we have four files that are used by Docker compose, namely the dockerignore

[00:01:02] in the compose YAML and the Docker file dot dev for us to be able to build and run our project locally. Let's then make a couple of adjustments to our files here. The Docker file stays the same because the project structure is the same. The compose file will be the one that will change primarily here.

[00:01:22] So we will say that this will be the nodes back end, we will have a node service and the build context is the same. It's the current folder and the Docker file is the Docker file dot dev. The port here that we would like to map. This is slightly important. If we were to run these two projects side by side,

[00:01:41] we're gonna have an impact or gonna have a conflict because the port 3000 of localhost will be allocated to whatever project starts first. So we can either leave the port 3000 for the notebooks back and as we already did and map the port 3001 here of our localhost or we could do it the other way around.

[00:02:02] I'm just gonna leave it as it is because the ports here we are just using for some intermediary checks. We're gonna get rid of these ports here. In our final setup, the db URL will change slightly. Now we're gonna use the nodes DB user environment variable. We also use the nodes

[00:02:20] DB password here and we're going to connect to the nodes DB database. So nodes DB and we will use the nodes DB name like so the develop stays the same since the folder structure here. Is the same for our networks. We then use the nodes net here. And this will depend on the nodes DB instead of notebooks.

[00:02:43] So let me just rename also the notebooks db to nodes db. The N file stays the same, the volumes here will change. So now we will have nodes dash data instead of notebooks, dash data. And the bind volume, the bind amount here will stay the same

[00:03:00] because the relative path that the B config for mongo in it is the same. And the target here is also the same. The networks here will change to nodes net. And then we will have here just nodes, data and nodes dash net like so perfect. Now, from within our dot M file,

[00:03:20] we're gonna just leave the root username and the root password to be the same. We're just gonna change the notebooks db user to nodes DB, user, nodes DB, password and nodes DB name. And then we'll just say here, nodes underscore user, nodes underscore password and then we'll say nodes db like. So the last thing we need to do is inside of our DB config folder. Let's open our Mongo

[00:03:44] init dot Js, let's just do a search and replace here. Let's copy notebooks, let's search our ID and in VS code, I just need to click here and then I can replace notebooks with notes. I would like to identify or to at least map this here or mark this as

[00:04:03] case sensitive because there are a few things that I don't want to replace here. So let's replace one by one. We're just seeing here that on the screen, this is being replaced and that is looking good. Let's now go for notebooks and I will just change this with notes with a capital N

[00:04:20] and then we have also notebooks here and we'll just change this with capital letters notes 123. Perfect. And really, that's all we need to do. And we have already our project set up here. We did not update the code inside of our server dot Js file. So let's do this same here. We're just going to copy the code from here.

[00:04:42] We're gonna paste inside of our server dot Js. Let's copy everything here. Let's remove this code and paste the new one. Let's then have a look at the things we need to change. And primarily here, we just need to change from notebooks to notes. The port here will stay the same

[00:05:00] because once again the port that we are specifying in our compose file. This is the port that the container will listen to or basically that our node that our express server will be listening for requests. However, to avoid conflicts, the important one is the that we map in our localhost. Once again,

[00:05:20] this is needed only if we want to access this service directly from our host network. Because if we are communicating between containers then we don't really need to expose these ports here. So later on, as already mentioned, we can get rid of this or we will be able to get rid of this. Once we have our reverse proxy in place,

[00:05:41] let's come to the file here. Let's just have a look whether there is something else we need to change. There is a notebook here. So let's just change the node server listening on port and then this port here, let's save this. And now let's jump to the terminal and let's see whether we can just quickly run a Docker compose up and test whether this setup here is working

[00:06:03] in the terminal. Let's change into our nodes back end and let's simply run Docker compose up dash dash build like. So you can also use the dash dash watch test whether the watch functionality is working. After a couple of seconds, we're just gonna install all the dependencies.

[00:06:20] And as you see here, we already start getting the messages that both our database is up as well as our notes back end. Let's then quickly jump to Postman and let's try to send a get request to the port 3001 of our localhost. The moment we do that, we get back hello from notes, which is exactly what we expected

[00:06:42] back in our terminal, we can split this into two terminals. And if we change into our notebooks back and here we can simply run a Docker compose up. Once again, we can also use both the build and the watch flags. And you will see here that we actually also get connected to Mongodb

[00:07:00] starting server notebook server listening on port 80. Coming to our postman interface here. We can also send a get request to port 3000. So now basically we have the notebook server running on port 3000 and the nodes back end the note server running on port 3001.

[00:07:20] Congratulations. We have achieved a great deal already by dockerizing both our notebooks backend and our notes back end. As you can see here, the configuration is very similar between these two because these are two express projects, they are very similar between each other. But we were able to achieve a state where we can quickly spin up the notebooks backend service together with the necessary database.

[00:07:44] And here you can think of also that we could include a caching, for example, if we wish to do so. So it's very easy to extend this configuration to spin up other containers for us to continue the development of our application. The same thing goes to the nodes backend and all we had to do is create its own compose file with the correct configuration.

[00:08:05] Perfect. That is a great moment for us to take a short break and come back in the next lecture.