[00:00:00] Hello and welcome back. We have completed and tested the code for our application here to register users and to get the users, everything is saved in memory. That means that if we stop the server, we're gonna lose all the changes here. But let's continue with this version because this will also enable us to explore a couple of the concepts of Docker.

[00:00:21] When it comes to stopping or pausing containers. In this lecture, we're gonna write the Docker file for this application. So let's create a new file here within our folder where we have created our index dot Js file. We're just gonna call this Docker file like so and if you prefer to place the index dot Js in a new folder called SRC or source.

[00:00:44] And that's not a file, it's actually a folder, then you can also do that. You can create a folder. Put the index dot Js inside of the folder, you can just click, move the main here is gonna be just SRC forward slash index and the script here will be node SRC forward slash index.

[00:01:03] Now back to our Docker file. Let's then define the base image here and the one we will use is gonna be the node image itself. The node image already has all the dependencies that we need to run node applications. I will simply just go with the 22 we can say 22

[00:01:20] or we can also say 22 alpine if you wish to do. So we're just gonna go with the 22 for now. So from node 22 the first instruction we will execute is to set the working directory for the rest of the Docker file. So I'm just gonna say work the forward slash app. As you remember the instructions from a Docker file are executed from top to bottom.

[00:01:44] That means that any instruction that comes below this work there here is going to be executed within this directory. So the first thing, let's just write down a few comments here of what we need to do. We need to copy the package, the Jason and package

[00:02:02] package dash lock dot json because this is important for us to have a list of all the dependencies that our application has. We then wish to install the dependencies. We would then like to, after we install the dependencies, we would like to copy

[00:02:20] the source code of our app. And then to start our application start application, we can also potentially between copy, copying the code of our app and starting the application. We can also expose the application port if we wish to do so So remember here in our index dot Js,

[00:02:42] the port is hard coded to 3000. So we're gonna just go with this hard coded value for now and then we can expose this application part. Let's then start with copying the package dot Jason and package lock dot Json. So the instruction here is very straightforward, it's gonna be copy

[00:03:01] and it will say package and we can actually pass a star here to include both the package dot Jason and the package lock dot Json and simply copy it to the current directory. Once these files are available inside of our current directory, we can run a command called NPM C

[00:03:21] NPM C stands for clean install. And it's a more, it's a lightweight version of our NPM install. It runs faster and it also install the dependencies in a more predictable way. So we're just gonna remove the comments here because they are self explanatory. Once we have executed the NPM C, we can now copy the source of our code.

[00:03:42] So let's just say copy and that's gonna be SRC forward slash index dot Js. And we're just going to copy this into the index dot Js file. So we are not copying it inside of the source directory. This is just gonna be forward slash app forward slash

[00:04:00] index dot Js. We can then say expose 3000 and let's try to run here the CMD. We're simply going to say node colon index dot Js, let's see if that's gonna work. If it doesn't work, then we can also add a forward slash forward slash index dot Js.

[00:04:20] Let's just see how this command here goes through whenever we build and run this image. Make sure that you are inside the folder where you have defined your application code. So let's just double check here. All the files are here. That's perfect. So let's now run a Docker build and then we're going to just pass a tag here.

[00:04:40] I'll simply say express app. Let's then pass a column here V 0.0 0.1. And the context is just gonna be once we do that, we'll get a lot of things downloading. If you do not have node 22 locally, it's going to download that from, from Dockerhub. If you do have it locally, then it's gonna be considerably faster.

[00:05:02] But after a couple of seconds, we should be good to go. Perfect. As you can see here, it has built the image, all the comments, all the instructions were successfully executed. If we clear the console and run Docker images, then we should get our express app. As you can see, this is a fairly large image, it's more than one gigabyte.

[00:05:23] So the base image node 22 is fairly large and this is leading to a big container. But nonetheless, let's ignore this. Let's assume that this is not an issue for us. We will discuss how we can optimize all these things, both security build time as well as the image size a bit later on.

[00:05:42] But for now, I'm just interested in running a container in a detached model. Actually, let's just run here directly with the port 6000. The name here name is going to be our Express app. And the image we will use is Express underscore app

[00:06:02] colon V 0.0 0.1. Perfect. As you can see the server is listening on port 3000. I have split my terminal here. So let's just run a command to the port 3000 and we will get hello world back. If we send this command or this request to forward slash users,

[00:06:22] we just get the users an empty array back. So our application, our server is running and we managed to dockerize our first express application. Congratulations on doing that. I just wanna go back to the IDE and once again, go through the individual steps here because these are fairly common steps, right?

[00:06:43] So for every application you do need a runtime environment, that's gonna be node, that's gonna be Python. It's gonna be Java, it's gonna be go it doesn't matter. You need a runtime environment for every application out there. Then setting a working directory is a good practice. And then we always have our dependency files. That's gonna be the package lock dot

[00:07:03] JSON or the palm dot dot XML or the requirements dot TXT, for example, so we need to copy these dependencies into our container and then we run whatever command is necessary to install the dependencies. And you can think of the parallels here in the programming language you're most familiar with.

[00:07:21] So after we install the dependencies, we then need to copy our source code. And because this is javascript, you do not need to compile the code. If this was Java or Go, for example, we would also here, for example, we would have a compile step which without put some binary or some Java file that we would then run

[00:07:41] for javascript, we just need to have the javascript file and then we can directly run our application in the case of javascript here with node and the file name. This is a fairly standard process. It's always like this. We start with runtime environment, copy dependencies, install dependencies,

[00:08:00] copy source code, compile the code if necessary and then start our application. This is how a very simple do file looks like for an express application for a javascript application. There are many things we can improve here, but this is already working. So that's a great moment for us to take a short break, tap us on the back and say congratulations.

[00:08:22] We have managed to dockerize a nodejs express application. So let's then take a short break and come back in the next lecture.