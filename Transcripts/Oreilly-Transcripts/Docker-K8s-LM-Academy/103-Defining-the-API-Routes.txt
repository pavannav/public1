[00:00:00] Hello and welcome back now that we have both our database container and our backend container already configured. Let's continue with the development of our application code. I would like to confirm that we have both running. So both the database here. Let's run the start DB command and let's also run the start backend command.

[00:00:21] We should then also confirm that both of the containers are running. And if we were to cURL here in the back end, if we were to just say C and let's hit the health endpoint, we should get back the up response. That's great. We can also have a look at our logs from the back end container and we can confirm that Nodemon is running.

[00:00:42] That means that also whenever we change files, the backend service will be automatically restarted. Let's then come back to the id. Let's confirm that we have all the necessary dependencies, body parser express and mongoose. And let's come now to the server dot JS file, our key value app will have a couple of routes

[00:01:02] here for us to manage our key value pairs. So let's create first of all our three routes here, the first one will be a post route to the end point forward slash store. And it's just gonna be our key value store. We will have the request and the response here

[00:01:20] and then let's do a little bit of processing or actually before we do the processing, let's just define all the routes. So we would like to post to the store to create a new entry or basically to store a certain value under a certain key. We then would like to be able to get a certain key here. So that would be the store forward slash and then call on.

[00:01:40] And the key here will be then extracted as a parameter as a path parameter. And we can then use that afterwards. And we also would like to allow updating. So that would be a put request and deleting the keys and there would be a delete request to prevent heavy, a very big server Js file. We can start splitting this up.

[00:02:01] So let's create here a new folder. We're gonna call this routes and the two groups of routes that we want to create our first, the health group as well as the store group. So let's just create your two files. I wanna say store dot Js and the other one here will be health dot Js

[00:02:20] in our server dot Js file. We are using the Express app that we have created here but express also offers the functionality of defining what are called routers. So let's get our express here. We're gonna import the express here as well. And let's create a key value router.

[00:02:40] This is nothing more. It's very simple. We're just gonna call the router constructor here. So this constructor will basically allow us now to define the multiple routes that we would like to handle in this router. All we're gonna do is we're just gonna copy the store or let's cut and paste the store routes inside of this file here.

[00:03:02] And let's change from app to key value router. We can also remove this store here so that we can define this common prefix later on in our server dot Js last but not least we would like to export this. So module dot exports is going to be equal to

[00:03:21] key value router. Just one small adjustment. I actually have created this inside of the health dot Js file. Let's just copy this and move everything to the store dot Js file. The logic here is exactly the same. So let's just move it around. And then for the health dot JS, we will just get the app dot get here.

[00:03:43] Let's paste this here. The principle is the same. I'm gonna have the express and then here we're gonna create a health router that's gonna be health router. And I will simply say app or health router dot get forward slash health. And then we're gonna return the app text. We can then say module dot exports

[00:04:03] the health router like so great. We can now import these two values here. So we're gonna say constant key value router is equal to require. And we're gonna use a relative path here dot forward slash routes,

[00:04:21] forward slash store. And then we will also have the health router here, health router. And we're going to require the health endpoint here or the health file. Once we do that, we can now actually just use these routers with once again the app dot use function

[00:04:40] and we can define a prefix to this route. So we're gonna say forward slash health. And then here we're gonna use the health router. That means that every request that starts with this forward slash health path will be handled by this health router. Basically, it will just enter into this route handling here and it's gonna look, OK.

[00:05:02] This is the root path within this router. I'm just gonna handle the request like so similarly here we can create an app dot use for the store. I'm gonna say store here and here we're gonna use the key value store or the key value router. We can save this here, come back to the terminal and confirm that we have quite a few restarts here.

[00:05:24] You see that are, that are also errors and it doesn't really matter. So the process here is not gonna exist. Nodemon will wait for file changes and it will try to restart the process. Once there are changes, let's now just add some dummy text to our routes here to make sure that they are being properly handled.

[00:05:43] So here we simply say return response dot send and then we will say here creating key value pair and then let's just do the same here for most of them or for all of them. Actually, we're gonna say response dot send, getting key value pair,

[00:06:01] then we get here the the put request that is gonna be updating key value pair. And then we will also get here deleting key value pair like so let's save this and we can test our API by coming to postman. Let's just issue a couple of requests here. So that's gonna be HTTP localhost port 3000.

[00:06:24] And if we were to issue a get request to the root path, there is actually no route here for the root path. If you have a look, we do not have any routes that the root path here would enter into. So we will have responses for the health and the store routes.

[00:06:41] Once we do that here, we can send a request, we get an error back and you'll see a 404 not found the status here is 404. If we were to then hit the forward slash health endpoint and send this, we get a response back the store one here. It does not really have a get request or it is not able to, to process the get request

[00:07:04] at the forward slash store, we would need to pass a key here. So let's say example and here it is able to handle the request. So we say here getting key value pair, if we were to send this to just the store endpoint, we will get a 44. Because the only method that is enabled at the

[00:07:21] forward slash store here is the post method. If we were to send the post method, then that's great. We will get the creating key value pair. If we were to delete here, we would also have to pass the key here. So the example key, let's send this here deleting key value pair and the put request here for the forward slash

[00:07:40] store forward slash example would then return updating key value pair. Perfect. So our little API is up and running and able to process requests. Let's take a short break and come back in the next lecture to handle, persisting these key value pairs.