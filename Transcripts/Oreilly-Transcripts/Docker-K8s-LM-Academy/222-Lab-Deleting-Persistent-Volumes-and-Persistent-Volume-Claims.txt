[00:00:00] Hello and welcome back. Let's explore a little bit what happens when we delete the persistent volume claims and persistent volumes that we have created. So let's just start by deleting here. So let's have a look A T all that we have. So keep cut, get pod PV PV C and we will get all the things that we have here.

[00:00:20] We have two pods, we have one persistent volume and one persistent volume claim. Let's clear the screen and let's start by deleting the pods cube color, delete pod, local volume example and local volume volume example two example two like so and we will also pass a force

[00:00:42] flag here and that's gonna delete both pods for us. Oops, I believe that this should be a local volume pod and not example. Sorry for that. Let's have a look local volume pod, local volume example, local volume pod looks better. Great. So the pods are deleted. Let's now delete our persistent volume claim with qar

[00:01:01] delete P DC and that's gonna be local volume claim like so and remember the policy that we set here in our id in the definition for our persistent volume is the retain policy. This is also the default for manually created persistent volumes.

[00:01:20] So if we come back to the terminal and we get the persistent volume here, then we can see that the persistent volume is still existing, it is retained and now the status changed to released. Now, this doesn't mean that the the persistent volume can be reused in other claims.

[00:01:41] And so now here, if we were to, let's have a look if we can apply once again the local volume example. And as you can see here, it says that the local volume claim has been created. But when we have a look at the persistent volume, then we still get the status released. And if we have a look, once again, let's add here, the persistent volume claim,

[00:02:00] we will see that this persistent volume claim aims is in a status of pending. So it cannot be reclaimed, it cannot be reused whenever the policy here is retained. And then the administrator would have to come and do some manual work here to maybe clean up this volume and then potentially recreate it.

[00:02:21] So the idea is that whenever you use the retain policy, the status of the persistent volume will be released if the persistent volume claim is deleted, but it will not be automatically reused by newer volume claims by newer persistent volume claims. Even if the criteria, even if the conditions here,

[00:02:41] they match what the volume or they match the volume characteristics naturally. If we have a look at minicube SSH here, we can also get the contents of our file. So mount disks, then local one and then we will have hello dot txt, the file is still there,

[00:03:01] right? So deleting the volume claim and recreating the volume claim, it doesn't delete the underlying files if the policy is retained, so that the persistent volume will stay there and will not be deleted. Let's then exit this. Let's now have a look at what happens when we delete everything. So we're just gonna delete with the force here and we will pass the

[00:03:22] local local volume example like so that's gonna delete everything including the persistent volume. So now we have no persistent volumes found here. But let's have a look at whether the files were actually persisted in our Minicube or in our node here in

[00:03:42] Cnis. So let's run minicube SSH. And then we can now have a look at whether the mount disks forward slash local one exists, it does exist. And if we list the contents here, the file is still available, right? So even deleting the persistent volume here doesn't mean that

[00:04:00] the files will be deleted from your local directory. That means you could potentially recreate everything and get access to these files by newer pods. If you are using a dynamically provisioned persistent volumes, then the default policy here that is specified under the reclaim policy is to delete and that is then dependent on how the configure

[00:04:24] of the persistent volume works. Because if the underlying storage mechanism, let's say that you're using an EBS backed or an EFS backed here for the dynamic provisioning. And this is in AWS, right. So elastic block storage, that's a typical SSD drive

[00:04:41] and elastic file storage. That is a network file system. If the configuration is then to delete the underlying storage here, that was dynamically provisioned, then yes, the contents of this persistent volume will also be deleted. But this can all be configured. And it would also go a little bit beyond the scope of this discussion around local volumes.

[00:05:02] So for us here, it's just good to know that even after deleting the volumes, the files will be present here, we can remove everything from the directory if we wish to clean it up to reuse it as a new clean volume, but otherwise the files will still be available. Great. Let's then exit from our, from our minicube node.

[00:05:22] And I believe that's a great moment for us to take a short break. Let's just confirm that we have nothing here. PV PV C and pod. I believe they are all gone. That's perfect. There are no resources found. So that's it for our discussion around using local volumes. They are very useful for local development in case you want

[00:05:41] to test persistent volumes and working with more persistent data. So just keep them in mind. Let's take a short break and I'll see you in the next lecture.