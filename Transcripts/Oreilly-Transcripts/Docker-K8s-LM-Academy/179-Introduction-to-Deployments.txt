[00:00:00] Over the last couple of lectures, we explored how we can run pods individually and how we can leverage replica sets to maintain a stable set of running pods based on a template configuration. We also identified shortcomings of both approaches, running pods individually as well as using replica sets. And we presented the case for deployments so that we can

[00:00:23] manage updates in our replica sets and in our pods in a more consistent and more automated and a more user friendly way, let's then discuss deployments in a little bit more details and understand the features they offer as well as how their configuration looks like. On the right side. I have an example of a deployment definition file here.

[00:00:43] We're gonna touch upon several sections of this in a little bit, but let's start understanding why we need deployments in the first place. So replica sets, they offer a great way to ensure that we have a pre five number of pods running at any given time, but they do not provide all the

[00:01:00] needed functionality for managing applications at scale. That means what happens when we need to update our containers or our pods or let's say that we roll out a broken update and we wanna revert that update or that we even want to check the history of which updates were done to these pods. Replica sets by themselves do not offer any of these features.

[00:01:23] And that's why deployments exist. Deployments are a higher level of abstraction on top of replica sets and they leverage replica sets behind the scenes, but they add more functionality to them. So what I already said that deployments do offer a higher level of abstraction and they add several features.

[00:01:42] The first one and perhaps the most important one is regarding rolling updates, we can gradually replace old pods with new ones without any downtime. This way, we can ensure that the application remains available throughout the update cycle. If something goes wrong during the update, deployments support roll back,

[00:02:03] so we can roll back to a previous version in case something is not working. This is crucial for maintaining the stability of our application and quickly recovering from errors. Thirdly, because we are maintaining the deployments in configuration files, we get a list of the declarative updates of the replicas over time.

[00:02:22] So we also get this audit trail to understand exactly what was changed, what was updated and how this impacted our deployment. As mentioned. We have the history and revision control. Since we keep track of the history of all the changes made, we can view the information around previous versions as well as

[00:02:40] revert to any of them in case we have to, this is something that is supported at the Cnis level. So here we are not mentioning version control in in git, for example, this is really here when I say revision control, this is something that we will explore, we get from the Kubernete site. And last but not least since we have the possibility

[00:03:01] of rolling updates and also of configuring our rollouts, we can limit the risk by leveraging some control mechanisms to ensure that the updates are applied gradually and safely. If we look here on the right side, you will see that much of this is actually very similar to the replica set file that we have created.

[00:03:20] The API version is the same. The kind naturally is different and it points to deployment. The metadata here is similar as well. We're just changing the name from replica set to deployment and the spec section. As you can see, it also has replicas selector and template. And these are once again very similar to how the replica set file looks like.

[00:03:44] The new section that we have here is called a strategy section. And this strategy is defining that the type of the updates here is going to be of a rolling update. We can then configure the rolling update functionality by specifying how many pods might

[00:04:01] be unavailable at any point in time as well as other configuration options. Let's now spend a few minutes to understand how this rolling update strategy would look like in practice when we create the next deployment, what's gonna happen is it's going to look at the number of replicas and add a template here

[00:04:21] and it's gonna create a replica set behind the scenes. All the pods are gonna be brought to a running state. And as you already know, the replica set itself is going to monitor these PSS, it's gonna once again kick in here and going to create a new pod based on the template section of our configuration.

[00:04:42] Let's now say that we have changed the template. The deployment is gonna then trigger an automatic rollout of this template. So that new pods are created based on the new configuration. What will happen is that there will be a second replica set that is automatically created by the deployment.

[00:05:02] This new replica set will take into account the new template. And naturally this replica set is not gonna have any pods related to it because all the pods are still being managed by the old replica set. What will now happen is a gradual rollout of new pods under the replica set or under the new replica set.

[00:05:22] So one pod will be created with the new template inside of this new replica set. And the moment this pod is up and running, then the old pod will be removed. Then we will create a new or Kubernes and deployment will create a new pod, a second pod under the new replica set.

[00:05:40] And it's going to remove one of the old pods from the old replica set. And this process will go on until the old replica set is empty. Once the old replica set is empty, and Cnis can confirm that the pods are up and running in the new replica set with the new configuration. Then the deployment is going to delete the old replica set

[00:06:01] and only that new replica set will be up and running. So as you can see here, what deployments are actually doing is they are acting as a higher level of abstraction to make transitioning between different replica sets with different configurations, very smooth and intuit seamless experience.

[00:06:21] This also enables CNIS to add a lot of interesting features on top of replica sets. And these include the revision history that we can access in certis as well as rolling back to previous versions in case something goes wrong with the new deployment. That's great. Hopefully, this helps to understand a little bit better.

[00:06:40] What's happening behind the scenes once we are using deployments and how these three concepts relate to each other, how the three objects of a deployment of a replica set and of a pod all come together. Now to give a very smooth, a great experience for managing applications at scale. Let's then take a short break and come back in the next lecture.