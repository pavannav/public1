[00:00:00] Hello and welcome back. Let's then start implementing or deploying our color API to our GKE cluster. We're gonna reinforce several of the concepts we have covered throughout the course and we will also see how these concepts they relate to different GKE and Google cloud constructs. So to begin with, we don't need this deployment and the service anymore.

[00:00:22] Let's just delete these two files here so that we have an empty directory here. I would also like to include customizing our project in a few different places. So let's start by creating a new directory here and this directory will be called shared dash config and this will basically contain configuration that is necessary for our color API.

[00:00:45] We will also create another top level folder here and we're going to call this name spaces and within the name spaces, we're gonna have a DEV dot YAML and a prod dot YAML. So let's just write here API version and that's gonna be V one, the name or the kind here will be name space and then metadata,

[00:01:04] we're gonna have metadata, name DEV like so, right. So let's just have this here. Let's also have a prod name space. And then we can also change this metadata name here to prod inside of our shared configuration. I would like to then create a folder for DEV and another folder for PROD,

[00:01:24] right? So now this shared configuration will for the moment contain only the config maps are actually only the secrets that hold information about our Mongodb root credentials and the Mongo DB color db credentials. Let's create a new file here. That's gonna be customization

[00:01:42] dot YAML. Let's also create another file that's gonna be en NVT root creds dot DEV. And we will then have also another one here that's gonna be not root creds, but that's going to be color DB creds dot dev, like so,

[00:02:00] right. So now this here is just for us to store this information, these credentials here in a dot M file and we're going to leverage customized to generate these secrets for us. We could also use the native Google cloud secret solution here. But since it does require quite a bit of setup on the Google cloud side of things,

[00:02:21] I'm gonna go with a simpler approach. If you are more comfortable, if you're comfortable with Google cloud and are comfortable with how it works, then by all means, I would encourage you to explore this option as well. Since in terms of CORS construct, they would be very similar to what we're gonna do here and you should also be able to follow them with everything we have discussed so far.

[00:02:42] But once again, since that requires a bit more extensive knowledge on Google Cloud, I'm gonna leave that up to you depending on how comfortable you feel with it. So let's start with our local version here of the secrets. The API version that we will use here is customize dot config

[00:03:00] dot K eight dot IO forward slash V one beta one. The kind is a customization like so and then the name space here will be the DEV name space, the generator options we would like to disable here. So let's disable the name suffix. Hash, let's set this to true. And that's because we would like to reference

[00:03:21] the secrets that are created in this shared cic customization in other parts of our project, which are not managed by this customization dot Yamo file. And therefore we would need some more stable names here. So let's now define both secret generators. Let's start with secret generator.

[00:03:40] The name here will be MongoDB dash root, dash grads and the environment files that we will use here is simply the en NVT root dash grads, oops root like so dash grads dot DEV, we have another secret that I would like to generate. That's the Mongodb dash color

[00:04:01] DB grads and this will take its value from our environment variable dot env dot color DB credits dot dev. Like so let's now populate both our files. So the root credentials simply have a username key and that's going to be root underscore user

[00:04:21] and password key and that's gonna be root underscore password. And then we will also have our color db credentials here. Or actually, I'm thinking here, since we're going to deploy this on the cloud, right. And it's gonna be a little bit more exposed than actually just working on our machines. Let's actually get some more robust passwords. So I just generated something here.

[00:04:44] And once again, by the moment you see this password, the database will be deleted. So there is nothing to worry about here. But I just thought, ok, maybe we can actually generate something a little bit more robust. Let's then now also have a look at our color DB credentials.

[00:05:00] So that's gonna be our color DB user and here is a more robust password for our user as well. Let's then also implement this same structure in our prod folder here. So let's just copy everything here and paste to prod. And now the difference is that this color DB creds will be dot prod and this root DB creds will also be dot prod.

[00:05:23] The customization file will now target the prod name space. And I would also like to generate both the MGO B root credentials from the PROD file and the caller db credentials from the proud file. I also generated some different passwords here just

[00:05:40] for the sake of keeping things somewhat realistic. So let's save this now and let's come to our terminal. And the first thing we would like to do is to create our name spaces. So let's apply the name spaces folder. We can confirm that both DEV and prod name spaces have been created. And I would like to apply here our customizations. The first one is the sharedconfig

[00:06:03] forward slash DEV. And the second one is the sharedconfig forward slash prod. We can now confirm here that we have the secrets created in the name space Dev as well as in the space prod. Right? So now here we have both these secrets, both for the color

[00:06:21] DB credentials and the root credentials created in both our namespaces. That's great. Having the secrets and the credentials in place is a great start for us. Let's then take a short break and I'll see you in the next lecture.