[00:00:00] Before we move forward, I just want to take the opportunity to go through the pod security standards documentation page just to show you what actually means to have a privileged baseline or restricted standard. So let me zoom in a little bit here. I don't think I can zoom in much because it's not very zoom friendly here,

[00:00:21] but maybe like this is good enough. So as you can see on the base line here, then we have a table that specifies or that lists here all the types of fields that are restricted. So you see here, these are the restricted fields when we are running windows containers,

[00:00:40] so windows pods here and then we also have other fields here, right? So you see that, for example, we are not allowed under the baseline security standard. So or the baseline policy, we are not allowed to set the security standard privileged to true neither for containers nor for init nor ephemeral containers,

[00:01:01] right? So if we scroll down a little bit more here, you see that if we want to add capabilities, we are restricted to these capabilities here. So this will also give you a more detailed overview of how we can configure containers so that they comply with the baseline policy.

[00:01:20] And the same thing here, let's scroll down a little bit more, the same thing for the restricted. So the restricted here also specifies exactly what you can do. And this also touches the volumes field, for example. So you can see here that the local volume type is not allowed or that the host path is not allowed.

[00:01:40] So it also restricts which kind of volumes we can create. If we are using the restricted based the restricted policy, you can also see that privilege escalation is not allowed. So there are quite a few other constraints here. So I just wanted to show you this page because this might also be useful to understand the different configuration

[00:02:00] options that you might have for containers and also to give you some guidance on how we can improve the security posture of your applications. That's it for this short discussion. Let's then take a short break and I'll see you in the next lecture.