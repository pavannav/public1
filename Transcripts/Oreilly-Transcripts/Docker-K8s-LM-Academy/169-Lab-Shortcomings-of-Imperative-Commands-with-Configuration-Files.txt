[00:00:00] I would like to now discuss some limitations of this imperative style. Even when we are using configuration files, it's important for us to understand these limitations because most of the times they drive us naturally to use the declarative style, which means that indirectly, we are switching to a more complex style of working with

[00:00:23] Cnis objects and we might not even be aware of that. So I want to make this distinction explicit and also discuss some of the limitations and some of the error messages that you might get when trying to work with the create and replace commands and using configuration files.

[00:00:40] Let's just print here on the screen what we have in our NGX called configuration. So we have our engine X file here working with the version with the image 1 27.0. We also have our pod here up and running engine X pod. And if we were to describe the spot, let's just copy this here.

[00:01:00] We're gonna run cube cri pod, the engine X pod, we will get here that the image we are working with is once again the 1.27 0.0. 1 important distinction to make is that the file that we have here with engine X pod. This is not what Kubernes is actually looking at when managing the object,

[00:01:22] when managing the pod running in the cluster. This is a file, this is a definition that we have written as developers and we have used this here in the fields that are set here. But behind the scenes, Kubernes is actually storing this configuration in its own storage and adding a lot more information to it.

[00:01:43] So I want to compare what we have written with what the Kubernes or with what Kubernes is actually using behind the scenes. And for us to get that, we can run the cube and then if we were to run here to next pod like so

[00:02:00] we will get a very simple message here, but we can just output this as a yo. So I'm gonna press up here to get this from the history. Let's now output this as a YAML file. And the moment we do that, you can see that they are very, very different, right? So if we scroll up a little bit more here, we start getting some related or some similar

[00:02:22] things like the API version and the kind, we also get our name and labels here. But we can or we start observing that there are some big differences already in the metadata section. Kubernes has added the creation timestamp and universal idea or UI D here. A resource version.

[00:02:40] It has also explicitly set the name space to the default name space. It has also added some additional configuration to our image or to our container definition with the image pool policy. As well as here, if we scroll down, we have volume mounts, we have also Kernis has also added a lot more fields to the spec section here of the configuration

[00:03:03] that we have not specified in the file we have written. So it's important for us to be able to or to be aware of this difference. What we have written is not what is actually being used by Kernis. This is used to translate and make a but Kubernes

[00:03:20] behind the scenes is using its own stored version or its own stored configuration of the resources. Let's now see how this impacts one small change that we might want to do here to our pod definition. The change that I want to introduce is I simply want to change the image of our pod. Let's come to the ide

[00:03:40] and let's open the engine X pod file and let's now move away from the vanilla 1.27 0.0. And let's use the alpine version of this image. Let's save this here. This is going to use a different version of the image and let's switch to the terminal. Now, our tendency here would be to use the cube cut,

[00:04:01] replace command and then pass the engine X pod file like So, however, if we were to run this command, you will see that we actually get some very weird message. And let me expand this a little bit for us to see it better. And you will see that we get an error message. It's forbidden.

[00:04:20] The pod updates may not change fields other than the spec container, image and blah, blah, blah. So these are the fields that we are allowed to touch. However, by simply using the replace here imperative command, what we are telling Kubernes is simply saying, hey, the volumes here are not specified in the file. So please set them to new to now or new here.

[00:04:42] And if we scroll down a little bit more, we will see here the same thing for volume amounts as well. And if you scroll down there is also the service account name is not specified in the file and the node name is not specified in the file. So what is actually happening here is Kubernes is looking at the file and it is not finding the configuration for the volumes.

[00:05:04] It's not finding the configuration for volume mounts and it's not finding service account nor node name and it's not going to find it because these fields were actually set by the Kubernetes system itself. So they are not reflected in our configuration file. That means that entirely replacing the resource is gonna fail here,

[00:05:24] even though we are trying to actually use or or introduce a valid change, right? So here it is explicitly telling us pod updates may not change fields other than the image field is one of the value changes. So even though we are trying to introduce a valid change here,

[00:05:44] the code API is complaining because we have not specified these fields in our configuration and they are actually present in the live configuration that CNIS is using to manage the objects. You can see here if we scroll down a little bit more, I apologize a bit for the printing here,

[00:06:01] but I am favoring the fact that you are able to read this here even on smaller screens. But as you can see here, the volume mounts is specified here and this was actually added by the CLI system itself and not by us in the configuration file. This is one example of a change that is actually going beyond what we intended to change.

[00:06:23] We just wanted to change the image of the pod. However, Cerni is not letting us do that because certain parts of the configuration are not specified in our files. And we are actually not allowed to touch these fields without deleting and recreating the pod. This is not a problem.

[00:06:40] However, when we are using declarative Management, because in the Collative management, Kubernes itself will calculate the changes that need to be applied to this live object and it will be able to identify that only the image here field only the image field of the first container actually needs to be changed.

[00:07:01] We will see that in practice a little bit later on when we dive a little bit deep into the CNIS into the declarative style. For now, let me just close here this new terminal. Let's clear the console. And the only way for us to actually get these changes applied is to delete here our pod by using the engine

[00:07:21] X pod file and then recreating this by running cube cle create. And then let's apply this once again if we were to now describe here our pod engine X and scroll up a little bit here. We will confirm that this is using the correct image and we were able to apply our changes as you might

[00:07:41] imagine always deleting and recreating objects in combinators is not really the best approach. And that's where declarative management really starts to shine before we wrap up this lecture. Let's just make sure we delete all the resources or the objects we have here. So let's first of all delete our pod cube cut delete. And then we're going to pass here the engine X pod.

[00:08:03] We can also pass multiple files here. So let's also pass the engine X service here. And this will delete both the service and the pod. As you can see here, we can delete multiple objects with a single command. And this is also very useful whenever we are using configuration files,

[00:08:20] I believe we still have an alpine pod here up and running. We can actually leave the alpine pod up and running because we might want to execute some cor requests and we don't need to install coral. Once again, we can just exact into this container here into the container that is running inside of the pod and the coral will already be installed and ready to be used.

[00:08:41] Let's then take a short break and come back in the next lecture.