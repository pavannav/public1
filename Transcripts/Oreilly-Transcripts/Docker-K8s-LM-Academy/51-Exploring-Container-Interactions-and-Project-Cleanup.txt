[00:00:00] Hello and welcome back. I have our Express app running here in our container or in our terminal on the top. If we try to stop this actually funnily enough, this is not gonna go through. So this is something a little bit tricky with this setup. Let's stop this container with a new terminal. I will simply say here, Docker stop and we're gonna pass the container.

[00:00:22] And as you can see here, this is actually hanging and it will hang for some seconds until Docker stop simply says, OK, your shutdown period, graceful shutdown period is over. I'm gonna kill the container and that's what it does. So afterwards, after a couple of seconds here, it will simply terminate the container and we

[00:00:41] will see that the container exited here. I would now like to start the container once again. So when I say Docker start with the Express app here, and I would like to leave this second terminal open here so that we can run a couple of operations on this container that is now running. So if we were to run Docker ps here,

[00:01:01] we will see that our container is up and running and let's go back to postman for us to create a couple of users in our express API in postman. If we just send a get request to the users, we're gonna get an empty body back. So let's now change the body here. I'm just gonna say John let's post send this.

[00:01:20] The user is registered. Now when we use get we get users the user John back. Remember this is something that is so this container, this express application is storing information in memory. So let's stop our express app container here and let's then start this container once again after a couple of seconds,

[00:01:44] this is gonna be stopped and once it does, then I'm just gonna run the Docker start once again to bring this container up. So just to confirm the Docker ps here, it shows that we still have the container up and running. It's up six seconds.

[00:02:00] Let's now go back to Postman and check what happens whenever we send a get request to 3000 forward slash users. And as you can see the moment we do that, we get an array of an empty array back for our users. So the contents are really stored in memory. And whenever we stop the container,

[00:02:21] then these contents are erased. We haven't really explored the pause and unpause commands of Docker. So let's do that right now. Let's just pause here this once again to our users end point. Let's then change to get confirm that we are getting the user back and let's go back to the terminal here. And now let's say Docker

[00:02:41] pause Express underscore he and the moment we do that, you see that this operation is actually immediate because Docker is not terminating the container, just pause in the container. Let's explore the Docker ps result here. And as you can see this shows now paused, but it's still saying that the container is up about a minute, but it is paused.

[00:03:04] If we were to try to get our users here, we'll simply send the request. And as you can see, this is hanging, the container is not up. The container is not able to answer to respond to this request here. So let's cancel this. Let's go back to the terminal.

[00:03:20] Now click on the one above where we have where we are starting and stopping our Docker container and let's now unpause. So it's not the start comments actually un pause here and simply say express underscore app. And the moment we do that and we run Docker PS, once again, we will see that the pause here is gone.

[00:03:40] So now we should be able to hit our container and get back a response. Let's go back to postman. Let's now send a get request to our users end point once again and surprise, surprise, we actually get the user back. So pausing doesn't really remember. We saw that quite a while ago when we discussed the life cycle of a container.

[00:04:02] Pausing does not clean the in memory, the memory contents of our container, it literally pauses, freezes the container in time with its memory contents. And once we unpause that is gonna bring that and it's gonna keep that in the container so that we can actually retrieve this information.

[00:04:20] Awesome. So this also gives us a great opportunity to explore the pause and unpause commands of Docker. Let's start wrapping up here. Let's stop our Express app or actually, since the Express app is going to take some time, let's just run a Docker Kill Express underscore app here the moment we do that and we were to now run here. Let's run the Docker PS

[00:04:43] at the bottom here. We are not going to get any results. If we were to run Docker PS A, we get our Express app container here. Let's now close this at the bottom because I feel like we are not gonna operate on containers anymore. So we can have a little bit more room here. Let's now have a look at our images. So Docker images

[00:05:03] and if we wish to delete this image here, if we were to say Docker RM I and then express underscore app colon V 0.0 0.1. This is not gonna work because there is a container which is using this image for us to remove an image.

[00:05:20] We actually need to remove the container with Docker RM Express underscore app, this removes the container and now we are free to remove the image. That's amazing. Congratulations on completing this lecture. Let's then take a short break and come back in the next one.