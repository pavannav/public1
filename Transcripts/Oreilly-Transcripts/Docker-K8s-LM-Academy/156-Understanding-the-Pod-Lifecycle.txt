[00:00:00] Let's take a closer look at the pod's life cycle. It's very important for us to understand all the phases that a pod goes through during creation until the execution, completion of that pod. So let's start discussing here what happens when either as a developer or let's say a replica set or some other system,

[00:00:21] a deployment sends a request to our cluster to actually create a pod. We will see how this is done with the cube cut C utility and we will also see later on how replica sets automatically recreate pods. So just think that whenever something is happening inside of the cluster,

[00:00:41] someone or a certain system is interacting with the cluster via the Kubernes API. So here we're just sending a request to create a pod. We are telling the cluster, hey, please create a pod for me, run a certain container with a certain configuration. So the moment we send this request, the pod enters into a pending state, right?

[00:01:04] So here it simply means that one or more containers are not ready to run yet and they are either. So the pod is either to be scheduled in a node, basically. Remember that CNIS it does run on nodes. It has this node abstraction

[00:01:21] and pods, they need to be placed in the respective node. So if a pod is not or cannot be scheduled in a node, maybe because of a lack of resources, then the pod will remain in a pending state until there are enough resources to schedule that pod. Another phase of the pending state or under step of

[00:01:40] the pending state is when we download the container images. So whenever you get the status of pods, you will often see this container creating message here the status quo. And it simply means that the code is downloading, it is pulling the images and it's starting the containers once all the containers have been created and at least one

[00:02:01] is still running or in the process of starting or restarting the pod will be considered in a running phase. So even if you have, let's say five containers and four of them stopped, there is still one container running the pod, the whole pod will be considered as running once all the containers have either successfully completed what they are supposed to do

[00:02:22] or successfully failed. That really depends this will determine the next phase of the pod life cycle. So the succeeded phase here simply means that all the containers in the pod have terminated successfully and they are not supposed to be restarted. And the failed here means that all containers in the pod

[00:02:40] have terminated and at least one container has terminated in failure. Now, these phases here, they don't really apply to containers that are supposed to be up and running. So always up and running. For example, a web server is supposed to be always up and ready to accept and process requests. So the pod that has that container will most likely be in a

[00:03:02] running state throughout its life cycle if it needs to be replaced, instead of actually updating the containers inside of the pod. What coins will normally do is to actually delete the existing pod and recreate a new pod with the new container image.

[00:03:20] We will then see that the pod has been terminated but really the succeeded and failed here, they are more applicable whenever the containers, whenever the processes running inside of the pod are supposed to come to an end. So I'm just mentioning this because most of the time you're not gonna see these things here when you get the pods and have a look at their statuses,

[00:03:41] you will normally see the running here. If the communication has stopped working. If we are not able to talk to the node or to the cube L to get the status of the pod, then we might see this unknown status here. So this gives a short like a brief overview of all the steps that a pod might go through.

[00:04:00] What is also important for us to understand is how pods handle container errors. So let's now say that a container is either in a creating state or running state. And then suddenly this container crashes, cober supports restart policies. And we can define either always on failure or never,

[00:04:20] if always is defined, then the container will always be restarted when it crashes. If the on or rather when it exists. Right? So here, if the container exists, it will be restarted. The on failure here really refers to crashes. That means nonzero exit codes here, then the container will be restarted.

[00:04:40] And the never simply says, no, don't restart my container, just leave it in failed state or in a succeeded state. So if the restart policy does not apply, then the container remains exited with the error code. I'm not taking here the oas or the succeeded status here. I'm just referring to when containers crash, right?

[00:05:01] So if there is no restart policy, then the container just remains exceeded with the error code. If on the other hand, there is a restart policy and it applies, then what Kubernes will do is it will kind of track how many times the container has restarted and the more times a container restarts,

[00:05:21] the longer it's going to wait to try to restart this container once again, this is what's called an exponential back off. And the logic here is really to prevent quick successions of container recreation and crashing, recreation and crashing, recreation and crashing. So you might see crash loop back off on the screen.

[00:05:41] And that simply means that the system coins is waiting a little bit until or trying to restart the container. Once again, the name here, exponential backoff also reveals how this mechanism works. So for every additional crash here that happens close to when the container was restarted,

[00:06:01] the delay here between the crash and the container restart will get larger. So at the beginning, it's gonna be very quick and then we're gonna start noticing a couple of seconds and then we're gonna start noticing one or two minutes and this will increase up to the boundary of five minutes. So we might see this here quite a few times when trying

[00:06:21] to restart a container and trying to get it up and running. So now let's suppose that the container is up and running again, it's finally healthy. We have managed to normalize to fix the situation. So after a few minutes, kernel is gonna check. Ok. Does the container have any recent race starts? And if this is not the case,

[00:06:41] then the system will also reset the exponential backoff count. So let's say that our container has been crashing here quite a few times, maybe 1015 times. And the crash loop back off is maybe more than a minute here. But now the container is finally healthy. We managed to normalize the situation. And then after I don't know, 1015 minutes,

[00:07:01] the system is going to look has the container crashed recently. And if the answer is no, then it's gonna be set count here. And then if the container happens to crash again, it will be quickly restarted because this entire loop here will start once again from zero. A few helpful comments that we will also explore

[00:07:20] during the course that can help us debug. This kind of situation here is to describe the pod to have a look at the current state, which events have come from the pod. Also to have a look at the logs of the pod to understand whether there are any issues when creating the container, maybe some fatal errors here that are causing the container to exit with an error state.

[00:07:41] And we can also have a look at the events related to a pod for the logs and the events. We can also append the dash dash watch flag and it can be helpful to stream the updates real time in the terminal. So we don't need to keep rerunning the commands to get updates. Perfect. So hopefully, this also gives a better overview of how

[00:08:01] the entire initialization or restart of containers flow. Here happens in Kubernetes. We will also explore and come across this several times throughout the course. So let's then take a short break and I'll see you in the next lecture.