[00:00:00] Hello and welcome to this section where we will cover a few advanced topics related to Docker. We're going to start by diving a little bit deeper into how we can set resource limits for containers. We will explore setting CPU limits for pods, memory limits for pods and we will also discuss what happens when these limits are exceeded.

[00:00:21] This is very important to protect other processes running inside of the Docker host so that they are not impacted in case a container has a bug or some malicious software and starts consuming too much CPU or memory. We will then discuss restart policies and how they work in Docker

[00:00:40] and we will cover or explore Docker networking in more details. We will go through the default Docker networks that are already shipped when we install Docker in our system. And we will also learn how we can leverage user defined networks for improved functionality. I'm sure you're looking forward to getting started. So let's move right away to our next lecture.