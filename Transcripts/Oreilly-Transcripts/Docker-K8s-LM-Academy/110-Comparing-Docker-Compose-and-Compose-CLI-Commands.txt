[00:00:00] Hello and welcome back in the upcoming lectures. We will make heavy use of Docker compose. You will notice that my comments will use the standalone version of Docker compose. You can identify this by the dash here between Docker and compose. And this simply means that I have installed Docker compose separately from Docker in the past.

[00:00:20] This might also be the case for you if you used brew to install Docker Compose or if you also installed the standalone version in Linux machines. If however, you try to run this command and you get an error saying that the Docker dash compose is not defined. Then by all means you can use the plugin version, which is the Docker space compose instead of Docker dash compose.

[00:00:43] And this will work just as expected. The compose plugin is installed by default in all Docker desktop distributions and the initial setup videos for Linux and the Bone to also install the Docker compose plugin. So if the Docker compose version command doesn't work for you,

[00:01:03] it returns an error saying that this is not defined then simply use the Docker space compose command in the upcoming lectures. The behavior is exactly the same as you can see here. The versions that I have installed are exactly the same. It is really just a small difference here in the syntax

[00:01:21] that the plugin does not have the dash here. While the standalone Docker compose has a dash in its name. With that being said, if you run into any issues related to this setup here in the upcoming lectures, then just let me know in the Q and A section and we will find a solution together. Let's then take a short break and come back in the next lecture.