[00:00:00] Hello and welcome back in this action. We will take a deep dive into working with images in Docker. We are going to start by exploring the Docker file in more details as well as Docker's layered architecture. We will explore how different instructions in a Docker file lead to different layers in a Docker image and how we can leverage that for our benefit.

[00:00:22] We then move on to exploring build contexts and the dockerignore file. This is also a very common component in whatever Docker project you come across. So it's good to be familiar with them. And then we cover a couple of topics when working with Docker files and with containers primarily understanding how

[00:00:40] to pass environment variables to containers, understanding the difference between CMD and and exploring distroless images and understanding their benefits. Distroless images is something that brings many security benefits whenever we know how to work with them properly. But it also presents several challenges.

[00:01:00] So we're going to explore them in more details. Also in this section, we will then move on to multi stage builds. Very important multi stage builds are everywhere whenever you are working with Docker. So it definitely makes sense. We spend quite a bit of time mastering or learning how to work with them and how to use them effectively.

[00:01:20] And last, but not least we will explore strategies to optimize both our image size as well as the build time. I'm very excited to go through this lecture with you. So let's take a short break and get started in the next lecture.