[00:00:00] Hello and welcome back. We are almost done setting up our users in Kernis. Now, all we need to do here. So we have, once again, we have our, let me just print this in a little bit more structured way. We have the files, a dot certificate and Alice dot private key here and Bob dot certificate and Bob dot private key.

[00:00:20] So all we need to do now is to actually set the credentials for our users by specifying our client key and client certificate. And then we can create individual contexts for each of these users. We could also use different cube config files for that, but that would add a little bit of overhead to the setup here.

[00:00:42] So I'm just gonna stick to a single cube config file. So we can once again have a look at where this cube config file is by just running. Let's have a look here. I believe that this is under our home directory dot cube forward slash config like so perfect. So this is the default location here.

[00:01:01] We can also override the cube config file with cube config environment variable. And then we can say here's some path, right? So some path here and then this would use this environment variable here as the cube config location. So we would export this like so and then we would run cube cut and we would start getting the or we would start using another cube config file.

[00:01:23] But for us here, we're just gonna as mentioned, we're gonna keep using the one that we already have here. So now I would like to set a certain context here. Let's set the context. Let's have a look if we can set the context for Alice before actually creating the the Alice user.

[00:01:43] But let's see here, Minicube and then user will be Alice like so yes, we can create the context even though we have not set Alice credentials. So let's now view our configuration here and you can see that a new context has been added. So we previously had only one context with the name Minicube.

[00:02:03] Now we have a new context with the name of Alice which references the user Alice under the cluster minicube. Once again, cluster minicube is mentioned here under clusters. That is the minicube one. So this here under the name Minicube and the user Alice, we haven't set up the authentication credentials yet.

[00:02:23] The only user we have here is the Minicube user which is referenced under the Minicube context. So now let's set our credentials with cube config set credentials. Alice, we wanna set the credentials for Alice, the client key here will be, and we're gonna use something called the real

[00:02:42] path function. So the client key will be under real path. And then that's gonna be Alice dot key. And then we will have here, the client certificate will also be under the real path dot

[00:03:00] CRT. Now, you can embed the certificates by running embed C equals true like so and this will basically not mention the files themselves, but it will actually add the base64 encoded version of these files here.

[00:03:20] So basically it would just be writing these certificates and private keys directly into the cube config file. I do find that it's better whenever possible to just reference the files for path, right? So here oops a small error that should be set credentials not credential. And now that's looking much better.

[00:03:40] So now let's once again, have a look at our configuration and you can see that a new user has been added to our cube config. So this is the Alice user and the Alice user is then under or it's not under. But it's actually the, the certificate is found in this file and the key is found in this file. If we were to embed this here with embed

[00:04:03] C equals true, then we can have a look once again cube config or cube config view. And you will see here that we have actually. So the data is omitted here. So if we were to actually print our config then we will see that this is the base64 encoded version.

[00:04:22] All right. But as already mentioned, I would prefer to not embed this. So let's just have a look here that's looking better. And now we are able to actually use this Alice context here to try to perform API request. So if we were to say

[00:04:40] cuo config use context Alice here, then we'll see that we have switched to the context Alice. And if we were to now have a look cuo config current context, that's going to be Alice. And now here we can try to get pods, right?

[00:05:00] And you can see that the moment we try to do that, we start getting an error from the server. This is forbidden user. Alice cannot list pods in the API group empty here. So that's the root API group in the name space default. And this is perfectly normal. It's perfectly expected. We have created the user, we have set the

[00:05:20] indication credentials, but we have never created any role or cluster role and nor the respective role bindings or cluster role bindings to actually give permissions to Alice. So it makes sense that Alice cannot do anything in the cluster. We're going to set up the permissions in the upcoming lectures. But for now, let's just wrap up here for Bob, right?

[00:05:41] So we would like to now let's have a look here for Bob. Where is this setcontext? Bob cluster minicube and user will be Bob here. And now I also would like to set the credentials. So keep config set credentials. And now we're gonna change this to Bob and the file,

[00:06:02] the client key here will also change to Bob and the set credentials. We're gonna set credentials for the Bob user. So now we can confirm here that Bob was also added to our list of users here and Bob is using their certificate and

[00:06:20] their key here to authenticate their requests. The current context is still set for Alice, even though we have multiple users here listed in her, keep config so whatever command we execute now with CCA we try to get or we reference the Alice user and therefore use ALYS client certificate and client key. Perfect.

[00:06:43] So that's it for our setup for the users. We now have two new users that we can use to explore here. Congratulations on completing this setup. I understand this is not straightforward if you are not used to this process here of generating and signing certificates and understanding

[00:07:01] how we can use them with cube cut and don't hesitate to use the Q and A section if you have any doubts about how this actually works or just have a look online and see what you can find as well in the documentation. But hopefully, the step by step that we did here is clear and detailed enough. And by the end, you have managed to reach out the state where we have multiple users

[00:07:23] that we can use to explore our role based access control capabilities. Let's then take a short break and I'll see you in the next lecture.