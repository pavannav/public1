[00:00:00] Hello and welcome back. Let's continue with the implementation here of our routes for handling notebooks. Let's now move on to retrieving a single notebook. This route will be very similar to the one above. So I'm just gonna copy this and paste it here and instead of directly retrieving all of the notebooks here,

[00:00:21] we're just going to extract the ID from our request parameters or request dot parames here and then we can say find by ID like so we can pass the ID here. This will retrieve a single notebook. So now we could say if not notebook.

[00:00:41] So if this is not defined and defined by ID will return a false value in case the document is not found with this ID here. So we can rest assured that if notebook has either null or undefined, then it was not found. We can then say if not notebook, we're going to return

[00:01:00] a status of 404. And then we'll simply send the JSON body here or the Jason object with an error of notebook not found. If the notebook was found, then we will return once again status of 200 the data here will just be notebook. Let's save this.

[00:01:21] Let's come back to Postman and let's test whether this is working in postman. We can take the ID of the notebook we have just created. So let's copy this idea here. And now let's send a request to the get Endpoint API notebooks and then let's paste the ID here. Let's send this. And the moment we do that, we can confirm that we get something back.

[00:01:44] We can also try to pass an invalid or a non existing ID here. And I'm curious whether Mongoose will actually process this or whether it will throw an error if we were to send this here. Perfect. Exactly. This is what I was expecting. Mongoose tries to convert this into an object ID

[00:02:03] and if it doesn't manage to do so, then it actually throws an error here. And this is coming from our catch block inside of our route handler from the user perspective, from the client's perspective. This is really a very bad message. We don't like that. We don't really want to expose this kind of information

[00:02:23] for the client's perspective. We should just inform, hey, this is not really a valid or let's say this ID was not found in the database, right? So if it cannot be cast to object ID, it simply means there is no object in the database with this ID. So instead of sending a 500 internal server error and a very ugly response here.

[00:02:43] Let's add a quick check in our route before we actually try to find the notebook by ID here. Let's import once again. So constant mongoose is equal to require mongoose. And now we're going to use a feature or one of the methods of mongoose here to validate the ID that we are receiving.

[00:03:03] After we receive the ID, we can simply say if not mongoose dot types dot object ID is valid and then we can pass the id here. So if the received id is not valid, then we're just gonna return the exact same error message, we're gonna return

[00:03:23] response status four or four. And just say, hey, this notebook was not found. If this is an invalid object ID, there cannot be any notebook with this id in the database to begin with. Because the id is an object ID generated by mongoose, let's then save this and come back to Postman to verify that

[00:03:43] we managed to provide a better user experience here for our API consumers in postman. Let's then send this here. And now we start seeing that we actually get an error here. Notebook not found. We can also say AB C for example here. And we get once again, notebook not found.

[00:04:01] If we were to now return to the valid notebook here, we can send this and we will get the notebook back. And if we were to pass, let's say here, a valid object ID So I'm just gonna change from A to B here. This is a hexadecimal string. And that's why changing from A to B is a valid change here.

[00:04:22] But this is a non existent id. If we were to just send this here, then we will also get a notebook not found. Awesome. Let's then come back to the ID here and let's complete both the put and the delete methods in the put method. We would like to update a notebook that is already existing with this ID here.

[00:04:43] So let's do once again try and it will simply say here, const name and description will be equal to our request body. So remember we are trying to update a notebook that means that we should allow our users to provide the new information here in the

[00:05:03] request body. We also would like to have a catch statement here for our error and then we will simply send back response status dot error like so great. We also like to validate our ID. So let's do the same code here. And I do apologize for the application.

[00:05:21] I know that there are much better ways to go around it. So you could also try to tackle this on your own, how we can extract this into a validation function. But for me here, I'm just going to leave this in the body of our route handler. So I'm just gonna say K notebook is equal to a weight notebook

[00:05:40] dot find by id and update here. And then in this function, we would then like to first pass the id that we have already validated. We then would like to pass the new information here. So that's gonna be both the name and the description. And then we would like to return here the new object we're gonna mark new to true.

[00:06:02] So that notebook contains the data of the updated object or it contains the updated data instead of the old data. Let's then once again check here if no notebook, we're just gonna return a 44 as well. So let's just mark this here.

[00:06:20] Return 44. Otherwise we will just return response dot JSON. And then the data here will be our notebook object for the delete method. The logic will be very similar. So I'm just gonna copy this whole code here and let's add inside of the body of our delete function.

[00:06:41] Let's just save this. So it formats it better. But now we don't have a name and description. We do still have the validation of the id. And then instead of having find by id and update, we will have find by id and delete like so and then we don't have any additional parameters or arguments

[00:07:01] except the id of the notebook. We wish to delete. We will also have the if not notebook here, then we'll just return this status and then we're just going to return the response here. Let's then save this code and let's jump into Postman to test our new endpoints in Postman. Let's once again, try to send here, remember we changed from A to B.

[00:07:23] So now we don't need to memorize this. Let's just get all the notebooks. Let's copy the ID here. Let's paste it here. Let's confirm that we can get the notebook here and now let's update, let's send a put request and we will simply say here description, updated. So updated description

[00:07:42] like so let's send this request. And then the moment we do that we get once again a 200 response here with the updated description. That's awesome. So the put method is working if we send an invalid ID. So let's just send an invalid ID here. We just get a notebook not found as expected. That's great.

[00:08:03] And now we can also test that with the delete method. Let's send the delete method with an invalid ID notebook not found. And then if we were to send with a valid ID, then we will get the data back here. That was deleted. Maybe we can actually improve on this because the one of

[00:08:21] the conventions is to just send a 204 instead of 200. So let's just do that. I just want to confirm that once we try to get this notebook here by ID, we actually start getting a notebook not found. And when we get all the notebooks our notebook is gone. So the delete method is working, it is working as expected.

[00:08:41] Just instead of sending the data here, we will send a status of 204 if the operation was successful. Perfect. Congratulations on completing this small API for handling notebooks. Let's take a short break and come back in the next lecture.