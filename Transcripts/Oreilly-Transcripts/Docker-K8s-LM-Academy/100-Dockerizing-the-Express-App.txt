[00:00:00] Hello and welcome back at the end of the last lecture, we realized that we cannot just run NPM. Start from our package dot JSON to start our project locally because this prevents us from connecting from finding the Mongodb container when trying to reach out to this database. In this lecture. Let's create a very simple Docker file here.

[00:00:22] So let's just add a new Docker file and we can call this a development Docker file. So Dockerfile dot nev and then we can simply start coding our development Docker file. Let's then start with node 22 alpine. We could also fix any of the alpine versions here if you wish to do so.

[00:00:40] But 22 alpine is already enough for us. Let's add the working directory to the forward slash app. Let's then copy our package, do a and our package lock, do a files and let's paste them here inside of the current folder. We would then like to run NPM. So that would be run NPM. CI

[00:01:01] once we do that, we would like to copy the remaining files and the command we would like to run at startup time will be simply npm. Colon start like, so this should already be familiar to you. That's a fairly straightforward, fairly simple Docker file. We have already written more complex Docker files in the past.

[00:01:20] So that should already be known territory. Let's save this and now let's come back to our terminal and let's build this image here back in the terminal. I'm inside of the backend folder. So within the backend folder, let's simply issue the Docker build command. We would like to tag this as just key value back end. Like so

[00:01:41] and the Docker file that we will use here, we will pass with the F option F. It will simply say Docker file dot dev like so we are missing the context and that's just the current directory once we do that and after a few seconds, we should then have the Docker file or

[00:02:00] the image actually here mentioned in our images. Perfect. So as you can see here here is our key value back end and now we should be able to actually run a container. So if we were to say Docker run, let's run on detached mode, I'm going to clear the screen here just so that we can see the whole command. We would like to add a name here and we'll simply

[00:02:21] say key value or let's just say back end like so we would also like to add this container to the key value net network. And we would then like to use our key value back and image. Also not forget to map the port 3000 to the port 3000 of the container.

[00:02:42] And once we do that, we can start up this container and we can have a look at which containers are running. We have our back end container up and running for two seconds and we can then inspect the logs of this container to see whether we were able to actually connect to the database. Perfect.

[00:03:00] As you can see, this is already different than the previous error that we got. And this is actually a successful message. So we were able to connect to the database. And once that connection was established, our app started listening on port 3000. If we were to. Now, let's clear the console, just issue a call request here to localhost colon 3000.

[00:03:23] We should actually, that's not the root path here. We have added default slash health, right? So here we are getting a standard error message. This is the default not found page that we get from express. That means the express application is running. And if we were just to crawl the health endpoint here, we will get an up response back.

[00:03:44] That's amazing. We were able to set up our Docker file, build our image, start up a container and connect to the Mongodb database here by using the Mongodb container name instead of having to specify the localhost here or any fully fledged URL,

[00:04:02] we were just able to connect to it by referencing the container name. There are still several improvements that we can make. So let's take a short break and come back in the next lecture.