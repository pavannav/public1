[00:00:00] Let's now dive a little bit deeper into services in Cnis. Let's explore what they are different types that are available and when to use each of them services are a great way for us to abstract pod details. To facilitate connectivity. They allow us to expose through a stable of pip or an internal DNS name

[00:00:20] a network application that is running as one or more pods in our cluster. Let's first understand how communication would take place if services did not exist, we would have a pod here. This can be a Frontend pod or it could be a simple microservice pod and this pod wants to communicate with some other pods that are managed by a deployment

[00:00:42] without services. We need to specify in this pod here that wishes to initiate the communication. We would need to specify the internal or the individual IP addresses of our target pods. As you already know, pods are expected to be ephemeral. That means we should not make any assumptions about a pod being long lived

[00:01:04] and a pod might be terminated and restarted at any point in time during its life cycle. That means it's internal IP address is not stable. And if it so happens that a pod is terminated, then the Frontend here is not able to communicate with this pod anymore.

[00:01:22] And it will start throwing us some errors services then create a layer of abstraction on top of the pods. And the Frontend can then just communicate with the service itself and service itself will take care of routing the requests to healthy pods.

[00:01:40] So it would then abstract this internal IP addresses. The frontend does not need to reference individual internal IP addresses anymore. It can simply talk to the service and the service will then redirect the request, taking into account the pods that are healthy. If a pod is terminated and then recreated inside of the deployment,

[00:02:02] then that old pod here will simply not be targeted by the service. On the right side, we have a very simple service definition of type nodeport. We will explore the different types in a few minutes. I just want to highlight here that there are a few important sections. The first one is the name section of the metadata section here of our definition

[00:02:24] and this will create an internal DNS name that we can also use to reach out to the service. The selector is another important section and it will specify a set of labels that the service will look at in order to match existing pods and redirect traffic to them.

[00:02:42] We also have the ports section which specifies what is the port that the service will redirect to and which port will be in this case exposed in the node. Let's then quickly go through the different types of services available in Cnis, the first one and the default type, if you don't specify a type as we did here.

[00:03:03] So if you don't specify this type option, the default type will be cluster IP. This exposes the service on an internal stable cluster IP. That means we're not going to look at internal pod IP addresses anymore. The IP that will be assigned to the service is stable

[00:03:21] and we can use it to query the underlying pods. It is only accessible within the cluster and this will be best used for internal communication between Microservices within the cluster. Also, if your application doesn't need node balancing, you might combine, you might see this combination with ingress to expose the applications via HTP or HTTPS.

[00:03:44] But once again, this is then going beyond the scope of the cluster IP service. So the cluster IP is accessible only within the cluster and it is intended for internal communication between services that need to talk to each other from within the cluster.

[00:04:00] If we want to enable external connectivity, then we do need to introduce something else and that would be ingresses. The next service type is the nodeport that we have seen in the previous slide. It does expose the service on each node IP at a static port in the previous slide, we have defined 3 30,007.

[00:04:20] And if not specified, then company is going to pick a port from this range here to allocate to this service. It is accessible or basically the service and its underlying pods will be accessible from outside the cluster using the node IP colon. Then the nodeport, this is useful for exposing a service externally without

[00:04:42] a load balancer sitting in front of it. And it's often used for development or testing. So you would rarely see the A nodeport service used for production applications would normally see the next type which is a load balancer type. The load balancer type, it exposes the service externally using a cloud provider's load balancer.

[00:05:03] So this is something somewhat of a dependency here, we do need to be using a cloud provider and a supported load balancer. And it does provide a single external IP that we can use to reach the service. This is best for production environments where we normally face the need of load balancing.

[00:05:21] And then the load balancer service type would come in handy. The last one that I want to discuss is the external name service type. And what this does basically it does the reverse, right. So here instead of mapping traffic or redirecting traffic from outside into a certain or towards a certain set of pods,

[00:05:41] what this does is it maps the service to an external DNS name and we can redirect traffic to a service or to some DNS name outside of our cluster. This is used whenever we want to provide a stable name here so that the services within the cluster can access external services via DNS.

[00:06:01] We could always hard code the L that we are actually defining in the external name, service as the target here. But the external service might provide a stable interface or a stable name that we can reference and it will take care of redirecting this to the external DNS name. All right, now that we know the four types of service here,

[00:06:22] we're going to focus on three of them, the cluster IP the nodeport and the external name for. Now, since we have not introduced working with cloud providers yet, then the load balancer service here is not going to add much. There will be not much to see or to discuss here. So I'm keeping it on purpose for now. We're going to come back to this a little bit

[00:06:41] later after we cover deploying applications in a cloud managed Cnis cluster. Awesome. Let's then take a short break and I'll see you in the next lecture.