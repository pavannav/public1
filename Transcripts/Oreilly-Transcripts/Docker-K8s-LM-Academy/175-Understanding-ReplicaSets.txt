[00:00:00] So far, we have only run individual pods in Cnis. But what happens when we want to run several copies or several replicas of a certain configuration and also delegate to CORS the job or the responsibility of ensuring that if any pod is deleted, then a new one is automatically created by following the rules.

[00:00:22] We define configuration files. That's where replica sets come into play. So let's explore them in a bit more details on the right here, we have a yo file detailing how a configuration yo would look like for a replica set. We're gonna get into that in a few minutes.

[00:00:40] Let's just go through the features and benefits that replica sets offer. So so far our pods were run manually and if there was any problem, the pod was deleted or it entered into an unhealthy state, we would need to manually intervene to solve the problem replica sets, then address this issue of replacing, exited on unhealthy pods and making sure that a stable number of

[00:01:03] identical pods are up and running at any point in time. This provides the foundation for high availability and fault tolerance in our applications. Replica sets, they work by identifying pods via selectors. And then they continuously check whether the number of running pods

[00:01:20] matches the desired number of pods specified in the configuration. We can explore this on the right here. So as you can see under the spec section and the API version kind and metadata should already be familiar to you. What might be a little bit new here is the spec section. So let's dive a little bit deeper into this section here. First, we specify the number of replicas we want to run

[00:01:43] the replica set will then ensure that at any given point in time, there are three replicas of the pods running in our system. But how does a replica set actually know how to identify the pods that it should manage or that it should continuously monitor? That's where the selector section comes into place.

[00:02:02] As you can see here, we define a match labels and then we define a set of key value pairs here. The labels that when present in the pods will actually add the pods to the list of monitored pods by this replica set. We then define a template here for a new pod. So as you can see here, the metadata and the spec sections are also under this template,

[00:02:24] but they are not referring to the replica set resource. They're actually referring to the underlying pods that will be created by this replica set. Whenever the number of running pods goes below the specified number of three. It is important for us to specify here that the created

[00:02:43] pods include the necessary label specified in the match labels. Otherwise, the replica set will not really keep monitoring the created pod. So it's important that there is here a correspondence. There is a match between these labels here and the selectors here.

[00:03:00] The spec section really just matches the specifications of a pod just spinning up a container with an engine X image here. And we have already done that in the past, the replica sets will then recreate these pods automatically based on this template section here in the specification. That's great. So now let's dive a little bit deeper into how the replica set actually works in

[00:03:24] CERNI. So it starts with a replica and a template here. It's gonna use this replica information to evaluate whether there are three pods matching the labels here. And if not, it's gonna use the template to create the new pods.

[00:03:41] That's why it's important that the metadata and the labels here, they match the conditions under the selector section of the specification. Once these three pods are up and running the replica set will then continuously monitor them to make sure that these pods exist and are in a healthy state under the replica set.

[00:04:01] If by any chance a pod is deleted here from Kernis, then this continue monitor is gonna actually reach a state of inconsistency. There are only two pods available while the desired number of replicas is actually three. So what it's gonna do is it's going to look at the template once again to recreate to add a new pod

[00:04:21] in Kubernetes and then start managing tracking this pod under the replica set that was specified here because this will be matching the selector conditions of our specification. This process will really continue throughout the life of the replica set. So there is continuous monitoring. If something goes wrong,

[00:04:40] the continuous monitoring looks at the template recreates the pod based on a template. If something goes wrong, this process is repeated and the replicas here is really defining the source of truth for how many pods should be concurrently executed. I do believe it makes sense. I already tell you that replica sets are hardly ever used by themselves. In

[00:05:01] Cnis, replica sets are normally created automatically as part of deployments and we will explore deployments a little bit later on in this section. However, I do believe it does make sense that we have a solid understanding of replica sets and how they work in Cnis because they are really the ones that will make sure that the pods are up and running following the replicas and the template that we provide.

[00:05:25] That being said, let's then take a short break and come back in the next lecture.