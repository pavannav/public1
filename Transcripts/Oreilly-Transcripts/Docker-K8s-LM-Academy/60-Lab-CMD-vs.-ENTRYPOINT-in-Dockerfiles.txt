[00:00:00] Let's now discuss the differences between the CMD and entrypoint instructions in a Docker file for us to better demonstrate the differences. Let's create two Docker files here. We're going to create the first one with Dockerfile dot CMD for the command instruction, we will create another Docker file entry point for us to illustrate the entrypoint instruction.

[00:00:23] We will also create a third Docker file here where we're going to combine both of them and see how they work together. Let's start with the CMD or command instruction. We're going to use a very simple alpine image here. So I'm just going to say from alpine and let's use here the 3.20 tag

[00:00:40] and all we're going to do here is define our CMD or command instruction and we're going to echo something on the screen. So echo hello from CMD in Docker file dot C MD, right? So this, you can imagine that here in between the from instruction and the command instruction, you would have all the other steps of your Docker file.

[00:01:01] And in the end, you would like to execute a command. Let's save this and let's jump to the terminal to build and run this image in the terminal. Make sure that you are inside of the same directory that you use to create your files. Let's confirm that we have the three files here. That's perfect. We can now run Docker build.

[00:01:20] Let's tag this image as CMD dash example. Let's pass the Docker file dot cmd as the Docker file to be used and the context and here we need to pass a F for the Docker file name. And then the context will simply be the current directory. Once we execute that and it completes the build process, we can then run a container.

[00:01:42] Let's remove this container. After it finishes, the name of the image is CMD example. And if we were to just run this like, so we're going to get the message hello from CMD in dockerfile dot CMD. So the first point here comes into play as you can see,

[00:02:00] the command instruction is executed. Whenever we run a container based on a certain image, we can now overwrite the command instruction by passing here our own custom instruction at the end of this command. So if we were to just simply run the example alike, so and here, let's say we're gonna pass another echo. We're gonna say hello

[00:02:23] from C MD in the terminal. In the moment we do that, we will see that we will actually override the initial or the default command here that is present in the Docker file. When we override a CMD instruction, we can really pass whatever we want here.

[00:02:41] So if we were to say Docker run, and then let's just retrieve here from the history of our terminal. And then we were to say shell and execute then this string and we're gonna say APK at row and then we're gonna row for example, the Google website. So www.google.com like so

[00:03:00] and we were just to execute this, as you can see, this instruction is perfectly fine. It is executed in our alpine container. So the CMD instruction in a Docker file, it really provides the default instruction to be executed, but it doesn't mean it always needs to be executed. If we pass a custom instruction here that is present or if

[00:03:21] we pass a custom command after the name of the image here, then this will be taken as the new CMD instruction and it will be executed instead of the one present in the Docker file. Let's then come back to the ide and let's explore the entry point. Let's copy what we have here in our Docker file. Let's paste inside of the Docker

[00:03:42] file dot entry point. But now let's change this instruction to entry point like so let's now change hello from CMD in Docker file to hello from entrypoint in Dockerfile dot entrypoint. And we're going to build this image once again, let's just save this, come back to the terminal. Let's clear the screen here and let's simply run a Docker build command.

[00:04:03] I'll go one step up here. Let's use the entry point file and let's tag this as entry point example, let's then build our image that should be relatively quick. And now if we were to run this once again, I'm just going to remove what we have here.

[00:04:21] And we're going to run a container based on the entry point example image. Let's clear the console, let's run this. And as you can see we get as expected hello from entry point in entry point. So the entrypoint instruction is also executed whenever it is present in the file,

[00:04:41] there is a difference though, instead of being overwritten like the CMD instruction, any additional commands or any additional arguments, we pass here at the end of our Docker run instruction will be appended to the entrypoint instruction. So if we were to run something like this Docker

[00:05:00] run entrypoint example, and then we would simply say here echo or actually let's just pass a string here hello from the terminal like so then you will see that this actually gets appended here. So it is still executing the entrypoint instruction from the Docker file. And then it is appending another string here at the end of the echo command.

[00:05:22] Since the echo command accepts multiple strings, then we can without any issues past that. But it might be the case that when we start passing new strings here or when we would for example, expect this to execute as a command. We are actually seeing that this is appended as a string.

[00:05:40] And that's because this is actually part of the echo command from the entrypoint instruction in the Docker file. We can for sure, override the entry point. But that requires a little bit different approach. And that is with the entry point flag like so, so if we were to pass entry point echo, like so and small mistake on my side before running the command,

[00:06:01] please keep in mind that Docker is sensitive to the order of arguments here. So the flag should be passed before the image name. Let's just erase this here. Let's pass the entry point here, entry point echo or we could also say echo between quotes like so and then let's try to run this. And as you can see

[00:06:22] whenever we override the entry point, it will then take this instruction here on this command. Whatever this is, this can be a file name. This can be a script. This can be some executable inside of the container. It's simply going to take this and it will execute instead of the one that is defined in the Docker file. So because we are passing then a string here,

[00:06:42] this string is as expected being appended to the entry point. But since the new entry point is just a plain echo command, all it's doing is simply printing this string in the terminal. Great. Let's then go back to the ide and let's see how we can combine both entrypoint and cmd

[00:07:00] instructions to achieve a more advanced functionality in the Docker file. Let's then open our Docker file without any extensions that we have created here. Let's start with once again from alpine tag 3.20. And now let's define the entry point as being simply the echo command. If we were to now simply save and build images based on this Docker file here,

[00:07:24] we would have nothing to print by default. So one very common use case of combining entrypoint and CMD instructions is to provide both a default command to be executed as well as default options to be passed to this command.

[00:07:41] So if we were to say here, default message like so then we would be in a good place here to run a container from this or based on an image from this Docker file. And this container would already have some default functionality. It would simply echo the default message. Let's save this. Let's come back to the terminal and let's see what

[00:08:02] we can do with this back in the terminal. Let's clear the screen. Let's then run a Docker build and let's pass here. Instead of entry point example, we're going to pass CMD entry point example, the Docker file we don't need to pass here because it is the default name, we can just execute this build and now we can run a command here. Let's simply run a new container

[00:08:24] with the CMD entry point. Example, in the moment we do that, we get the default message printed on the screen this container. However, it is more flexible because now if we want to print any custom message here,

[00:08:40] we don't need to override our entire executable message instruction. We can simply pass the custom message here and this will override only the CMD instruction. Remember the entry point is still the echo command. But the CMD instruction is providing a default list of

[00:09:00] parameters that is being passed to the entry point. The moment we pass here once again, new parameters to the Docker run command after the image name here, then all of these parameters here. This list is going to override the CMD instruction. That means we will print custom message on the screen. This is a common pattern when you want to execute a

[00:09:21] certain command at the startup or when running a container. But you want to allow the user to customize the flags that are passed to this command. So the command, the CMD instruction, once again, it gets completely overwritten by whatever parameters we pass here. The entry point, it remains stable,

[00:09:40] but it will have all these parameters appended to it and then it will be executed. So the bottom line is the actual command executed is basically a combination of entry point plus CMD. If you are overwriting the entry point here, then it's going to take the overwritten value here. Otherwise it will take whatever default value is set in the Docker file.

[00:10:02] And the same thing is for the CMD instruction. And in the end Docker combines both of them and run this when starting the container. OK, good. So hopefully, this helps clarify a little bit the role of CMD and entrypoint instructions. And it also gives some food for thought for how you can combine

[00:10:21] these two to provide a better experience when creating and running containers. Let's take a short break and come back in the next lecture.