[00:00:00] Hello and welcome to this project where we will build a key value rest API. Let's have a closer look at what we will develop. Our focus is once again Docker and also working with multiple containers. We will introduce a database which will also cover topics related to working with volumes.

[00:00:20] So let's see what we will achieve with this project. We're going to call a key value API to store information under specific keys. We're going to run multiple containers for both the application which will be an express based application and the database, which will be a Mongodb based database. We're going to leverage volumes to persist the data from the database container.

[00:00:41] We will also connect application and database containers by leveraging user defined networks last but not least, we will also leverage shell scripts to manage the created containers, networks and volumes. Let's then have a at the API structure for the service. For the back end, we will develop

[00:01:01] the back end should be able to receive a post request to the forward slash store endpoint. And the expected behavior is as follows. It expects the request body to contain both a key and a value with arbitrary values here, both the key and the value. If this is not the case, the endpoint should return a 400.

[00:01:20] If the key already exists in the database, it should also return a 400. And if the key does not exist, it should store both the key and the value and return the code 201 which is equivalent to created. The second endpoint should allow the user to query for the value stored under a certain key.

[00:01:40] This colon key here is a syntax represent that the key might be variable and it's basically a parameter. If the informed key here does not exist, this endpoint should return 404 for not found. And if the key exists, should return 200 with both the key and the value in the body of the response.

[00:02:01] The next endpoint a put request to the store variable here or parameterized key should expect the request body to contain value should return 400 if this is not the case. And if the key does not exist, it should return 404. And if the key exists, it should update the value of the key and return 200.

[00:02:24] The delete endpoint follows the same pattern store and then a variable key here. And if the key does not exist, it should simply return 404. But if it does exist, then it should delete the key and return 204 which is equivalent to no content

[00:02:40] last but not least we should have a general health endpoint here that returns 200 the text up whenever queried. Once again, if you wish to give it a try and implement this project by yourself, you're more than welcome and encouraged to do so. So let's take a short break and come back in the next lecture.