[00:00:00] So far, we have only customized the name space of our resources. Let's then spend some more time to understand some of the transformations we can implement with customize. So one of them that is also very commonly used here whenever we want to create overlays is to set name prefixes and name suffixes.

[00:00:22] So we can set the name prefix here. Let's just call this DEV dash for example and the name suffix, we can just say here alpha, right? So let's just say that we are running a DEV application and we would like to have alpha appended to the name here. We could also say alpha beta for example, or V one

[00:00:42] we could do or add whatever suffix we want to add here to our DEV overlay. So once we do that and we save this, let's come to the terminal and we don't really need to actually create the resources to inspect what will be the effect. So let's just run cube cut, customize and then engine

[00:01:00] X app overlays and DEV. And the moment we do that, then you will see here that our metadata dot name from both our deployment and our service will be modified by customize, it will add the name prefix and the name suffix to both of their names. So as you can see, this is something that is very useful whenever you

[00:01:22] want to change the name of your object, depending on the overlay you are working in, particularly if you're also deploying them in the same name space. So let's suppose that we removed the name space here and we also removed the name space from prod. Let's save this. Let's now apply here. Both our overlays with cube cut,

[00:01:43] apply. And I'm just gonna get here the PROD overlay and then the DEV overlay. And now if we get the pods here, if we get the pods, then we will see immediately that the name of the pod will also include the new name of the deployment because this is added as part of the pod name.

[00:02:03] If we were to now get both our deployment and service, we will see that the deployments here that we have from the DEV overlay, they are also marked as DEV, right? So this can be very useful when you are working with overlays and creating them managing these objects in the same name space.

[00:02:22] You could also use a team name or other ways here to identify by name. Now, the names here are not the only things we can use to identify resources. Another possibility that customize offers us is to set a common set of labels to all our resources.

[00:02:40] So we could say, let's say that this is from Team Finance and the project here is just gonna be our Ecommerce app, right? And then here we can also say that the tier will be back end and that the environment will be deaf, right? So the moment we do that to our DEV overlay and we come back to our terminal and we run cube cut will apply,

[00:03:00] let's apply our DEV overlay once again. And this is giving us an error here because the field, as you can see here, the label selector requirement is immutable. So we would need to delete and recreate this. So let's delete our DEV overlay and let's now recreate our DEV overlay and we are good to go.

[00:03:21] Now, a very interesting thing that happens when we set the common labels here. Let's run cube cut and let's run customize and show the results on our DEV overlay. As you can see here, the labels, they are added to our template metadata labels,

[00:03:40] but not only that they are also added to our selector match labels and to our labels of our deployment as well as here to the labels and the selector of our service. So this common set of labels is not only applied at the metadata top level here of our deployment and service

[00:04:01] customize is also smart enough to update both the match labels, condition of our deployment and the labels from our pod templates. So as you can see, this is a very powerful feature as well. If we want to customize the labels of our application, we can also add common annotations, right? So common annotations,

[00:04:23] let's say that we would either like to add some configuration annotations or let's say that we will change, move here the theme to annotations and we'll also add a maintainer annotation. And then we could say here, let's say finance at our company.org,

[00:04:41] right? So now we know here where the how to reach the maintainers of this application, we could also add a repository here to add our git repo, right? So there are multiple annotations that we can use here. Let's now save this and now we will need to delete and recreate this once again. So let's delete our overlay and let's recreate our overlay

[00:05:04] and now let's run, run once again our customize here. And we will see that the annotations they are added at the top level of our deployment and they are also added under the metadata section of our pod template. That means any pod that is created and managed

[00:05:21] by this deployment will also include these annotations. So we can also add here pod annotations for configuration purposes. And then these will also will be applied to all the pods that are created by whatever deployment, stateful sets, jobs and so on and so forth that are part of our project of either of our base or of our overlay.

[00:05:42] Let's then come back to the ide. And another cool thing that we can do here with customize is that we can actually change the image, right? So let's say here that we have 1 27.0. But now let's say that we would like to work with 1 27.1 in our DEV overlay.

[00:06:01] So we can set a field here called images and then we can pass a list here. And it's important that the image here, it matches the name of the image that we want to change, right? So here this is going to be under our deployment. It is going to be X and let's now come to customization and now we need to pass

[00:06:20] a new image here so we can pass a new name, sorry, not new image, but either new name or a new tag as well. So let's say that the new tag here that we wanna pass is 1.27 0.1. Let's then save this here. Let's now run, let's clear the screen and let's run customize once again.

[00:06:40] And the moment you do that, you'll see that the image engine X here has been updated to version 1 27 1. There are many other transformations that we can perform here. For example, we can set the replica count. So another common thing is that probably we want to have a lower replica to our deployment here in prod.

[00:07:03] So we should come here and have a look at our deployment name that's gonna be engine X and then the name here, we're gonna form name is X and it will say count here. Let's say we want to in our X deployment or our GX overlay. And then in prod, we would like to have four replicas, right?

[00:07:22] So just this example here, let's save this. Let's now come back here to the terminal. Let's run our customized command once again. And we will see that the replicas field is added here under our deployment. If we were to now run the customized command with prod,

[00:07:40] we will also see that the field replicas is correctly set to four. The best place for you to find all the fields that you can use here to customize your overlays as well as any possible application notices is really coming here to the cube Qle docs, Cerni dot IO and then accessing. So let's come here to the main page

[00:08:02] and then accessing the reference here on the top and then under reference, you see customize and then here in customize, you will see customization, right? So this here will really give you all the fields that you can use here is an example of images and you can see here how this is going to work or the exact behavior here.

[00:08:23] And we'll also give some great examples. You also have common labels and you also have common annotations. And you also have here a few fields which are deprecated. For example, this field patches, a strategic merge was used fairly often up to version four and it is still in

[00:08:42] use here because version five is the latest major release at the time of this recording. But this node is very important. It simply says that this field will never be removed from V one beta one, but it will not be included in the V one version here. So it also provides you how to actually fix this

[00:09:01] here by running or migrating this into the patches field, which is then also present here. You also see here that patches is considered by the documentation a synonym to overlay. I do like to keep both kind of separate from one another because the concept of base and overlays has become fairly widespread and these names,

[00:09:22] they have acquired a certain specific meaning. But nonetheless, whenever you see patches and overlays, you just think about overwriting fields on resources, whether you are doing that by writing this here at the top level of your customization or by passing a patches option like so which

[00:09:40] we will also explore a little bit later on, gives just two different ways for you to actually customize, to tailor the resources here to this specific overlay or to this specific configuration. So I just wanted to show you the documentation as well because this set of fields here, it they will evolve over time. Right. Some of them will be deprecated and new ones will be added.

[00:10:02] And it's always great for us to know where to check the latest version. Whenever we are, we are working with customize. That's it for this lecture. Let's then take a short break and I'll see you in the next one.