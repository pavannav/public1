[00:00:00] Hello and welcome back. Let's now spend a few minutes exploring how we can work with secrets. In certis. I created a new directory here. There is nothing inside. So let's start by creating a secret. Like any other Cnis object. It is possible for us to create a secret via a Manifest here.

[00:00:21] So we can create, let's say my secret and we could then start defining the secret here. However, secrets are probably one of the very few objects that it makes more sense for us to actually create via the cube co CLI instead of using files. That's because we don't want to commit these files.

[00:00:40] It would be very bad practice to commit secret files because the contents here would be base64 encoded. And that is not really encryption. Anyone with access to a base64 string can decode that and get the original value of the secret. So this will be one case for us to actually use the cube cURL

[00:01:00] to create our secrets without hardcoding them in any file. So let's create here. Our first secret. We're gonna say Cube car create secret. And the generic type is the one that we're gonna use here because it is an arbitrary set of key value pairs. And if you are not sure about how, what it does or how it works,

[00:01:20] then you can add a help flag here to get a little bit more information. So you see that there are three available commands. We can create a secret for use with the Docker registry. We can create a secret from a local file directory or a literal value that is the generic one and we can also create a TLS secret. So let's explore a little bit more the create secret generic command here.

[00:01:45] And we'll also add the help flag and then this will give you all the options that you could put here. You could pass to the command. It will also give you several examples here. For example, how to create a secret from a file or how to create a secret here using literals.

[00:02:03] So we're gonna use literals here. We're gonna use this shape and let's just create a simple secret here. So let's clear the console. I'm gonna say cube C create secret generic. And then we will just call this db credentials or db creds.

[00:02:20] And then we will add the from dash literal here. And this will then take a key value pair and then we'll say username is equal to db underscore user. And then we can pass more here. We can pass other literal. So from literal, we'll then set the password and then we'll pass this,

[00:02:40] we'll set this to db password or DB pass just like so and so this will then create the secret here. And now if we run cube cut, get secret, we will then get our secret here. And you will see that this is of type opaque. If we use the cube K describe secret command here, we will not see the actual values of the secret.

[00:03:03] So we will just get that there are seven bytes and seven bytes here. So one way for us to actually visualize the values of the secret is to run cube cut, get secret and then pass the name of the secret here, the BRETS and then the output of

[00:03:20] YAML, this is gonna show the secret values here. And then you can see that this is actually even though it looks like a little bit weird, we can actually just retrieve the original value by running echo like so and then piping this into a base64 decode and then this gives us the value back, right?

[00:03:40] So as you can see, this is not really encrypted in any way, this is just a base64 string. And anyone with access to this will have access to the actual value of the secret. That's why it is not good practice to commit such files or commit such configuration here. Because then if for whatever reason, this ends up in the wrong hands,

[00:04:01] then that person or that system would have access to these values. Great. So let's clear the screen and let's now come back to our ID and let's see how we can retrieve the values from this secret for this. We will simply create a demo pod here. That's gonna be a simple application and this will just be a busy box container.

[00:04:21] So I'm just gonna put kind pod here and I will leverage the the auto completion so busy box and then here let's just clean this up. And now the image will be Busybox with a version of 1.36 0.1. We don't need to advertise any ports here. Resources we can leave

[00:04:44] and accessing the value of secrets is actually fairly straightforward. So very similar to how we did with Configmaps, we can say environment variables here and then this takes a list. So let's take the first name will be db user, that's gonna be the value of our environment variable.

[00:05:02] And this will take its value from a secret key ref right? So secret key ref here and then we'll specify the key inside of the secret. Let's come back to the terminal and let's once again, just describe our secret DB credits and this will give us at least the keys, right?

[00:05:21] So if it's not needed for us to actually get the value of the secret, the describe secret will be good enough. So the user will be the key username. Let's copy this, let's paste it here. And the name of the secret will just be DB creds like, so we can then also get the value for our db password.

[00:05:43] And this will come from the same secret, but the key will be password. All right, let's save this here. And now let's just override our command or entry point here. So when we pass the command, this will actually override the entry point inside of the container. So this is going to be, we're gonna just run a shell or we're gonna run a command here

[00:06:05] by passing shell and then dash C and we will pass the command we want to execute. So the command will just be to print here. So we're gonna echo the db user and echo the db password as well. So let's use double codes here. Db user echo db

[00:06:23] underscore password like so and then we're just gonna sleep here for maybe, I don't know, let's say half an hour. So that's gonna be 1800 seconds. Let's save this here. This looks good to me. Let's now come to the terminal here once again. And now let's apply this file

[00:06:43] with the demo pod, right? So the Busybox pod has been created, we can now have a look at our pods and we can also get the logs of our Busybox pod and confirm that it's able to retrieve the values for us. So as you can see the values for the secrets, they are retrieved and they are decoded.

[00:07:03] So even though they are stored as base64 in our secrets object in Cnis, when they are mounted, they are passed to a certain container, they will be passed in a decoded manner. That means it is the responsibility of the application. It is the responsibility of whatever application container we are running

[00:07:21] here to not print these secrets in any kind of logs. Of course, you can also add some additional layers. And if this application here ends up printing in the logs, you you could detect this in external parties in third party software. But just to make it clear the secrets, they become available as an encrypted data inside of this application.

[00:07:42] Great. So similar to config maps, we can also pass the entire secret object and mount all the key value pairs as environment variables. If we wish to use a slightly shorter version, we can also use the M from here and then we will pass a secret ref like so

[00:08:02] and the name of the secret will be our db creds. And so this will pass and mount or it will basically map all the data or the key value pairs from our secrets into environment variables. Now, we need to change this here because now this is not called db user with capital letters anymore.

[00:08:20] It's called username and the other one is password, right? So if I'm not mistaken, these are the ones we can confirm that once again, let's describe our secret and then we can confirm perfect. These are the keys. So first of all, let's delete our busy box pod, busy box like so and let's now recreate the pod.

[00:08:40] If it takes a while, then once again, just add the force flag here so that it stops immediately. And let's now apply once again our file. And if we inspect the logs of our busybox pod, we can confirm that we are still echoing the information, right. So similarly to configmaps, we can pass the entire secrets or we can pass

[00:09:02] only specific parts of the secret in to the container. Great. We're gonna leave as it is now here. I'm not gonna delete the secret. Let's just delete the pod once again here so that the pod is gone. Let's then take a short break and let's come back in the next lecture to inspect how we can pass secrets via volumes and volume mounts.