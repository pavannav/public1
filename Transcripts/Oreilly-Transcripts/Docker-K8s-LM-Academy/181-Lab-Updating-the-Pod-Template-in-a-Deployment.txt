[00:00:00] Let's now clear the console and let's jump back to the ide to change, make an update in our pod template in the ide. Let's just scroll down here to the image and let's move from 1 27.0 to 1 27.0 Alpine. That's the only change we're going to do and we're going to test this now in our id before applying,

[00:00:21] I just want to show remind you of this D command here that can be very useful to identify the changes that might be applied are actually the changes that will be applied whenever we run the cube, apply command. If we scroll down a little bit more here, we will see that there are two changes. The first one is a generation change.

[00:00:42] So the first generation will become now the second generation. That's because the deployment will be updated. And then we will see that the image will change from 1.27 0.0 to 1.27 0.0 dash Alpine. Since we are fine with both these changes, we can now proceed to applying them.

[00:01:00] But before we do that, let's just split this terminal once again into two here or actually into three. And let's now get the pods here. Let's watch the pods that we have created. And let's also do the same for our replica set. So cube cut, get replica sets and we will also watch the replica sets

[00:01:21] now that we agree with the changes that will be applied with cube cut apply. Let's just run cube cut, apply and let's pass the engine X deployment file here. Once I execute this command, you will see that there will be quite a few changes here. That's great. So now you can see there are quite a few logs, quite a few changes of numbers here.

[00:01:40] We're going to go through this in a little bit, but let's just go up a little bit more here and also see that all our pods are actually changing, they start terminating and new ones start being created. So Kubernes starts by creating a new pod here from the new deployment or from the new version of the deployment.

[00:02:00] As you can see here that this hash actually is different from the previous one. So this is the first new pod that is created as we scroll down here. We can then see that pods from the old replica set start being terminated. And eventually the new pods here also start running, they start entering into the running state.

[00:02:21] We can confirm here that we have a fairly similar picture when we look at the replica set. So we had the new replica set here, new replica set is basically being created. And as you can see here, it says that hey, I want to have two desired pods. And once these two desired pods become available here,

[00:02:42] then the number of desired pods is increased to three. But the number of current pods is still running at two, we then create a new pod here and then this number it starts to increase gradually, right? So you can see that both the desired and the current numbers here, they are increasing gradually in our new replica set.

[00:03:03] And they're also decreasing gradually in our old replica set. While the logs might be a bit confusing here if we close these two windows or these two terminals, and we describe our deployment with cube cut, describe deploy and an engine X deployment

[00:03:21] like so we will see here that we actually have all the events mentioned here on the top, we have the older events. So this is the first one at creation time and it said it scaled the replica set automatically to the number of desired replicas. We then have several events that are coming here from our rolling update.

[00:03:42] So we started with a new replica set. This is the new replica set. We have scaled it up to two and then we have scaled down the old replica set to four from the previous five. As you can see here. We then have a new event scaled up the new replica set to three from two.

[00:04:02] So basically now it's just taking single steps at a time if we were to now describe the pod. So let's run here. Cube cle describe or actually first let's get our pod. So cube cut, get pod, let's then copy here. The name of one of the pods. Let's run cube K

[00:04:20] describe pod. We pass the pod, Let's then get the image field here. And as you will see this image is automatically updated to the new one. That's great. Congratulations on completing and creating your first deployment in Kernis. There are still a few topics I would like to discuss. But first, let's take a short break and I'll see you in the next lecture.