[00:00:00] In the previous lecture, we ran our first container and we also issued a couple of Docker commands. The Docker run Docker, stop Docker kill. So let's spend a few minutes to understand the container life cycle and how Docker manages containers whenever we start, stop, pause or remove containers.

[00:00:21] Here is an overview diagram and I know this might seem confusing in the first place. So let's take it step by step and let's go through all of the components of this diagram one by one. So whenever we run an image, this is what we did Docker run image and we provide the name of the image here. In our case, this was Nginx.

[00:00:42] And actually to be more precise, we would provide both the name and the tag. But if we omit the tag, then it just defaults to the latest tag. The important thing here is that we issue a Docker run command and this is a combination of both Docker create and Docker start.

[00:01:01] So behind the scenes, Docker will create a container with the provided image and then it will start the container. We can also use these commands here independently. We can just create the container without actually starting it or we can just start a container that is already created by Docker. Once we issue this command, if it goes through successfully,

[00:01:23] the container will be in a running state. We can issue Docker logs, command or Docker inspect command or we can exec into the container. This we will also cover later on on the course. Don't worry if you're not familiar with these commands. I'm just showing you that whenever the container is running,

[00:01:40] we can issue a lot of commands to interact with this container from the running step. There are a few possibilities, right? So we can pause a certain container and this doesn't truly stop the container. It's just gonna pause it and it's gonna keep the memory contents of the container. We can then unpause it here by running Docker and pause and

[00:02:02] then pass in the container ID whenever you see CD here or C here. This simply means the container ID. It's either that long hash that we saw in the previous lecture or the name of the container. If you pass the name option when issuing the Docker run command,

[00:02:21] if we pause the container and then unpause it, then the container is just back to that running state. In addition to pausing, we can stop the container gracefully. So this is gonna just issue a stop command and it's gonna clear the memory contents. We can also forcefully stop the container with Docker Q

[00:02:40] that's gonna issue a SQ signal and this will stop the container as well. The danger here is that we might lose some content that is in memory and the process, the container doesn't have time to persist this content to disk. For example, while the Docker stop command might give some time to the container to wrap up its processes and exit gracefully.

[00:03:04] We then also have two other possibilities. The main process within the container, the process with an idea of one, it might exist naturally with either a zero code or with a non zero code. The zero code signifies that the container exited without an error.

[00:03:22] So basically if it is just a temporary container, it does a certain job, certain task runs a script for example and then it exits with zero for Docker. This simply means ok, good. The container did its job and exited successfully. That's it. Now, if the container exited with a nonzero code, then for Docker,

[00:03:41] that means that an error occurred. This is primarily important when we look at restart policies because we can inform in Docker, hey, please restart the container if it exits with an error, right? So it would look at the exit error here or the exit code and it would identify, oh,

[00:04:01] this is an error. It is different than zero. I'm gonna restart this container automatically but don't worry about restart policies. They come later on in the course. Now here we're going to assume that there is no restart policy in place. So if the container is stopped, killed or exits with either zero or nonzero codes,

[00:04:20] then we're gonna come to the container stopped, phase in the container stopped phase. You will not be able to see it. If you run just Docker PS. If you add a a at the end of Docker PS or the flag, a then the container will show up because that container still exists.

[00:04:41] It is still there. It's just not running. We can also inspect the logs of the containers and we can also inspect the containers themselves. And if by any chance we want to restart the Docker container, then we can just issue a Docker start with the same container ID that was used here to stop or to kill the container.

[00:05:02] If we issue a Docker, start with that container ID, then once again, the container will be restarted and it will come to the running phase. If we do not want to restart the container, if we just want to remove it completely to do some cleanup and free up some resources, then we can just issue Docker RM CI D with the container ID here

[00:05:24] and what this will do, it will completely remove the container and its contents. We're not gonna be able to see the logs or inspect the container anymore. It's basically just gonna be removed and the resources will be freed up. I understand, there are lots of commands here.

[00:05:40] And by no means I expect you to be familiar with all of them. Don't worry about it at all. We're gonna use these commands over and over again. Of course, in different scenarios under different circumstances. And we will explore how they behave. My goal with this lecture is to give you a bird's eye overview, the big picture

[00:06:01] of the different phases of the container life cycle, the running phase, the stopped phase and the removed phase. We also have the initial phase here when we actually download the image if it is not present locally and start running the container. This is the main message of this lecture. So keep this diagram in mind as we move throughout the course.

[00:06:22] And it's gonna make things much, much easier for you to manage and work with your Docker containers. Wonderful. Let's then take a short break and come back in the next lecture.