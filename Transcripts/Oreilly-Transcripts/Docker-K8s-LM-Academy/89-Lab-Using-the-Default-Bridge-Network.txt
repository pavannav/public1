[00:00:00] Hello and welcome back. Let's discuss some important topics related to networking in Docker that will help us understand how we can better connect our containers while still keeping an isolated environment. By default, Docker creates three networks for us and we can inspect them by running Docker network. LS

[00:00:20] should match also to the network types that we have discussed during the previous lectures, namely the bridge network, which is a private network that is running inside of the Docker host. But that is it is isolated from the Docker host. We then have the host network which can be used to remove isolation between a running container and the Docker host.

[00:00:41] And we have the non network which can be used to prevent a container from connecting to any network. By default containers will be connected to the default bridge network that is provided by Docker. So if we just run a container here in the detached mode and let's pass a name, let's call this web server and let's just run the engine

[00:01:02] X 1 27.0 image like so if we were to now inspect the bridge network, so running Docker network inspect bridge we will see that we will have a container connected or added to this network. We can also see the IP address of the container inside

[00:01:21] of the network as well as the name of the container. One important topic to highlight in a very common use case is if we want to connect to the container by name, we cannot leverage the default bridge network. This feature offered by Docker is called auto service discovery or DNS resolution

[00:01:40] and this is not supported. This is not available in the default bridge network for that. We would need to create our own user defined networks and they are very easy to create, we will explore them a little bit later. Let's then clear the console here and just confirm that our container is up and running by running Docker ps Here.

[00:02:00] We can also confirm that the container is connected to the bridge network by running Docker inspect and then our container. And we can confirm here under the network settings and then scrolling down a little bit under the network's object that this container is connected to the bridge network. We also have the IP address of the container

[00:02:22] and if we scroll down a little bit, we will see this DNS names set to know this setting here indicates that the DNS name resolution is not enabled in this network. So if we were to try to access the container by its name, which is web server, this is not gonna work in this network.

[00:02:41] That is great. Now, let's see if we can connect if we can reach this container by IP. So let's once again, run the inspect here. Let's copy this address. IP. Let's just copy this and now let's run another container. So let's run an Ubuntu container here. We don't want to start it in detached mode. Let's just run Ubuntu

[00:03:01] and let's run with the tag here. 24.04. The command we would like to execute is just the shell command. If you don't have the image locally, it will take a little bit to download. But soon we should have a shell running. That's perfect. We can also confirm that we are within the correct container here by just mentioning this OS release.

[00:03:21] You see that we didn't inform any network whenever we ran the engine X or the Ubuntu container. So by default containers get connected to the Docker bridge network. Let's then clear the console and let's install cURL. So let's run apt update and apt upgrade.

[00:03:40] Let's wait for some moments for this package manager here to update with the most recent packages. And we can type Yes, here if we are asked to do so. Once everything has been updated, we can now run apt install cURL and this will install cURL for us. Let's type. Yes, here it will install all the dependencies

[00:04:01] and it should be fairly quick installation. We can now clear the console once again and let's now try to crawl the IP address of our engine X container that will be 172.17 0.0 0.2. And the moment we do that we are actually able to establish connectivity. So as you can see here, we get the message. Welcome to Nginx

[00:04:23] back from our container. We are then able to reach out or communicate between containers by using the IP addresses of those containers. However, if we were to try to reach out by name, so let's do that. Once again, chro web server,

[00:04:40] we will that it could not resolve this host web server. That is because as already mentioned, the DNS resolution and the service autodiscovery is not available. It is not enabled in the default bridge network. Great. Let's exit our Ubuntu container. Let's also remove this Ubuntu container. So let's have a look at the name of the

[00:05:02] Ubuntu. This is here, this is the container ID. So I'm just gonna remove this container here and let's also stop and remove our engine X containers. So I'm just gonna remove with a force flag here so that the container is also terminated. The default bridge network is a good starting point if you just

[00:05:21] want to spin up a few containers and play around with Docker. However, as you saw, it does offer some limitations around communicating between containers and particularly if we cannot really use the container's name that leaves us very vulnerable to IP addresses changing. If a container is recreated and its IP address is different than before,

[00:05:42] then we will simply lose the connectivity here. And this might be very annoying to maintain. Let's see how we can address that with user defined networks. But before that, let's take a short break and I'll see you in the next lecture.