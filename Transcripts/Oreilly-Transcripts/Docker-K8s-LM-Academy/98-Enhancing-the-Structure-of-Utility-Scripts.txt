[00:00:00] Hello and welcome back. Let's do a little bit of Refactoring to our start DB script because we would like to avoid running into errors if we run this script multiple times. First of all, if we were to just run the start DB script here, and we do not have our created network and created volume,

[00:00:21] then we will get an error here informing that the network was not found. So the first thing I would like to do here is to really create both the network and the volume from within our start DB script. We could also split this into another script. So let's come here to the ide, let's create a new file. We're going to say this, we're going to call the setup dots

[00:00:44] and the setup dot sh will be responsible responsible for creating volumes and networks. So this file here would just be responsible for creating volumes and networks. We would like to have the information here of our variables

[00:01:04] to be shared between the setup and the start db. So what we can do here is we can create, for example A dot and dot network and we can also create another one dot m dot volume.

[00:01:20] And we can provide the information here by taking it from our start db. So let's just copy the connectivity here or at least the network name. Let's just copy the network name into our dot m dot network and the volume name as well here inside of our dot m dot volume,

[00:01:41] right. So now we can save this here inside of the setup dot S, we can say source dot and dot network source dot m dot volume. And here I would like to also export this to make sure that they become available in the shell. So export this and export this as well

[00:02:02] in our setup. Let's just copy these comments and also paste them here inside of our start DB script. So we're gonna say source dot M here and let's also add a source here. Now we know that some of the information is coming from within this dot m

[00:02:20] dot network and some of the information is coming from the dot m dot volume. In our setup dot sh here, we now would like to check whether there is a a volume with the provided name. So we're going to say here if and I will simply say open square brackets and then we will run a command here.

[00:02:40] This command will be Docker volume, LS Q and then we can filter here by the name of the volume and the name will simply be equal to the volume underscore name. This is coming from our dot m dot volume here. So the volume name and if this is present, right? So if this returns a non

[00:03:03] empty string here, then we would like to simply echo a message. And we will also have an LS here. And the end of the, if so if there is a volume, we will simply say echo a volume will the name, volume

[00:03:20] underscore name already exists, skipping volume creation. All right. So now very straightforward, if the volume already exists, we will simply skip the creation. Let's close here. The the double quotes. If there is no volume, then we would like to say Docker volume create.

[00:03:44] And I will simply say here the volume underscore name like so perfect. Let's come to the terminal, let's clear the console. Let's change the mode of our setup file. So let's add the execute here and we'll say set up dots

[00:04:01] and let's now try to execute the setup dot As you can see here, it says that a volume with the name, key value data already exists. So we could also say volume or actually Docker volume RM key value data like so and then before to execute this again,

[00:04:21] then it will simply print the volume and it will create the volume for us. Now, if we execute this multiple times, then it will just skip the volume creation volume creation can be done multiple times with Docker and there will be no impact. So if we were to run Docker volume, create key value data here,

[00:04:41] then it will just output the same result. You see, I'm running multiple times. But if we were to run Docker volume LS, then we just see that there is one volume here. So it's not super bad if we run this command multiple times without checks. Nonetheless, I do find that it is a little bit more user friendly and developer friendly to have this kind of setup.

[00:05:02] We can add the same logic to the network. So let's just copy this here. And then we're gonna say the network. So network here, LS and then we'll simply say that we have the network name here, the network name. And then we'll simply say a network

[00:05:20] with the name, network name already exists. So we will say skipping network creation, otherwise we will have Docker network create and then the network name. Perfect. So this will take care of both the volume creation and the network creation. We can also execute this script here from within our start

[00:05:44] DB. So we can also say setup dots here. Let's save this now. And let's try to run this from the terminal. Let's clear the console here. And let's run our startup DB. As you can see, it says here, a volume with the name,

[00:06:01] key value data already exists is keeping volume creation. That's great. This is our network and this is our container. And if we were to try to run this again, we will see that we have a volume and the network with the name already exists. But we are still getting an error here and that is from the container name. The MongoDB container is actually up and running.

[00:06:22] So if we were to run here, the MongoDB is up and running, let's kill this container here. And now we can run our script Once again, we will get the, that the volume and the network already exist and we get the container in the end. Let's then just add a quick check here for the Mongodb

[00:06:40] container also in our start DB script to make sure that we are not running into this problem. So in our start database script, let's add a new line here. And we'll simply say if and the same logic here, same logic. We're just going to run the Docker PS command. I'm gonna say talker PS dash Q. In the future, the name here will be equal to the contain or underscore name.

[00:07:05] This is at the beginning of the file here. So if this exists, then we're gonna add it here. Like so then, and then we will end the condition here. So all we're gonna do is we're gonna say echo a container

[00:07:20] with the name and it will say container underscore name already exists. We will then echo here as well and we will simply say to remove the container run or even better here. Let's actually add another echo line. And we will say here the container will be removed

[00:07:42] when stopped A dot here. And then we simply say to stop the container run Docker Q and then we pass the container underscore name like so perfect. So let's save this here. Just a small comment. You could remove the source dot m dot volume and dot m

[00:08:00] dot network because they are also sourced from within our script here. However, I would rather keep them here in case we do wish to execute the setup script all the time. So let's just keep this here. This is not going to lead to any problems. Let's save this. Let's come back to the terminal and let's see now what happens when we run the script once again.

[00:08:22] Oops, I forgot one small detail. Let's come back here to the ide. After we print this information, we do need to exit from the process, right? So let's exit with a code of one here. Let's save this, come back to the terminal. And now when we run this, we get the messages on the screen. So we can see that the volume already exists, the network already exists.

[00:08:43] The container already exists, the container will be removed when stopped. So let's just copy this command. Let's paste it here. And the moment we do that and run this start database once again, we now get this up and running and we get much more user and developer friendly error messages.

[00:09:01] The last step I would like to do with you is to just write a little cleanup here. Let's just rename this and this cleanup will just delete everything that we are creating. OK? We could split this up into database and set up, clean up if you wish to do so, then by all means, go ahead. No worries. There is nothing stopping us from doing this.

[00:09:21] I just really want to have one script that we can run that will stop and remove, so stop and remove containers. Let's then create here a new dot dot DB and this will just contain the information for our database. You can ignore here this extension.

[00:09:40] This is just from the icons of visual studio code. Let's just have this here and copy our container name like so, so we're gonna copy this here and let's rename this to db container name. We're gonna export this. And at the beginning of our startup script, we also source dot

[00:10:00] and dot db like so and now here we just need to update the reference. So that should be db container name here, db container name like so that is looking better and also here, right? So I believe that we do not have any other container underscore name.

[00:10:20] I believe that everything was correctly refactored. If you wish to move all the source statements to the top of the file that's also fine. It might get a little bit confusing if you are doing sources in the middle of the file, feel free to do this Refactoring if you wish to do so, we can now also add here all the source statements to our cleanup file.

[00:10:42] So let's add this here, source the network. We're going to also source the volume. We're gonna also source the db here. And all we're gonna do is we're just gonna have exactly the same checks here, right? So let's just copy this. And we will say inside of our cleanup, if the volume exists, now, the logic is a little bit different.

[00:11:03] If the volume exists, then we would like to remove. Otherwise we would like to skip this. So say here a volume with the name blah blah blah does not exist skipping volume deletion. Otherwise, we would simply say here, Docker

[00:11:20] volume RM and then the volume name, we can do the same here for the network. So I'm just gonna flip the order. We'll say a network with the name blah blah blah does not exist skipping network deletion. Otherwise we will just remove the network here. Docker network RM network name.

[00:11:41] We can also add here some echo statements. We say echo removing network and then network underscore name. And we'll do the same here for our volume, removing volume and then volume underscore name like so

[00:12:02] let's then open our start db here. We're gonna copy the same if check, that is gonna make sure that our container is up and running and the container is the first thing we actually need to remove. So we need to remove the container before we remove the volume and the network to ensure that we can remove this volume and network successfully that

[00:12:23] they are not being used by any container. So if there is a container and we can actually add the dash A Q here to list all the containers in case we are not removing the container and when it is stopped. So if there is something that we would like here, otherwise I'd like to do something else. So if the container exists,

[00:12:41] then we will say here removing echo, removing the big container and then we simply press db underscore container underscore name like so we can remove this, we can remove the exit one and then we simply say Docker Q

[00:13:00] and we will kill the container here. So Docker kill this container and we can then also here add a Docker RM for the container name. Now this Docker RM here is not necessary because we are running the container with the dash dash RM flag. However, if you are not doing so, then you would want to add this part.

[00:13:23] So add, if not running with dash dash RM. All right. So that's the first one. And if there are no containers with this name, we're just gonna print here a container with the name, the B container name

[00:13:42] does not exist skipping container deletion. Perfect. So I believe that's all we need for our cleanup statement. As you can see, there are just a bunch of if else checks very straightforward logic. And I am very well aware that this is a lot of manual work and a lot of manual code here

[00:14:02] there is for sure a better way for us to do that And Docker Compose helps us with this. I'm just writing some utility scripts so that we don't need to run these comments manually. Let's save this. Let's come back to the terminal. Let's clear the screen here. Let's make sure that we have the correct execution mode.

[00:14:22] So if we run in LS L here, we will see that we have the correct execution permissions for the clean up. So we can just go ahead and run the cleanup dot sh Perfect. So this will remove the DB container, MongoDB, the container was running when we ran this command here.

[00:14:41] So this is why the container was removed. We have also removed the volume and the network. If we run this again, we just get the information that they do not exist. So we're just gonna skip the deletion. We can now run our start db here and we will see that we will create both the volume,

[00:15:01] the network and the container. If we were to now run a Docker PS command, we have our container up and running. And if we were to kill this container manually here and then run the clean up script, we should also get a message that the container does not exist. We are skipping container deletion, but we are still removing the volume and the network.

[00:15:22] Perfect. I believe we are at a very good place. Now, we have a script to start our database as well as a script to clean up our infrastructure. Let's then take a short break and come back in the next lecture.