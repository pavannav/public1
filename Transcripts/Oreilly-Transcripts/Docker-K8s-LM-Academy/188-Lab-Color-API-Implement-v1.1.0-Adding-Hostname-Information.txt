[00:00:00] Hello there. In this lecture, we're gonna make a small adjustment or improvement here. We want to implement a little bit of additional functionality to our call API If you are not interested in following the coding aspect of our examples, then by all means you can skip this lecture. That's entirely optional. We're just going to reference the Docker image in the next lecture.

[00:00:22] And in this one, I just want to show you the changes that we are going to implement. So if you are interested in the application development side of things, then stick around for this lecture. Otherwise you can safely skip it. What I wish to do in this lecture is to add some information here to our call API that's going to give us back the hostname of whatever machine

[00:00:41] or whatever container or whatever pod is running this application. We will also add an additional endpoint here so that we can get some informat message just in plain json and that we will also serve some demonstration purposes. So let's start here by adding our new route that's just going to be

[00:01:00] API like. So we're going to have request and response as always and then we will have response dot Json and then we can just send here whatever response we wish to send in JSON. Let's first of all, extract our color from outside of our response here. Let's change this into VTI so that we can interpolate the value of the color.

[00:01:22] Let's extract this and then let's just add a variable here. I'm going to say constant color is equal to blue, right? So the color here is just sending or set to a hard coded blue value here. And now, in addition to sending the color back in our get request to the root path,

[00:01:40] we also want to send the hostname. So let's add the color here to the JSON first. And now let's add our hostname to add our hostname. We're gonna have to import the OS package that comes already with nodes. So you don't need to install this using NPM. And all we got to do here is add a new line

[00:02:00] and delete the white spaces here so that they are not included in the response. We can add a header to level here. We're going to simply say hostname, colon. And then all we gotta do is OS dot hostname like. So now this is gonna be something that we will also send back in the

[00:02:20] JSON response. So we're just gonna say here, hostname is equal to OS dot hostname and I will also send it back here. And here I also want to implement a small improvement here or a small possibility for formatting the response that we get from our API because it might also be interesting for us to get this response as plain text.

[00:02:43] Both without the HTML here, just gonna leave this for the base route. And then in the API here, it's just gonna be either JSON or plain text. So we're gonna use the query parameters here inside or they can be passed to our forward slash API.

[00:03:01] And what we're gonna do here is just extract it. So let's say cost format is equal to R dot query like, so this would enable the user to pass something like this. So it's gonna, let's say, send a request to localhost slash API forward slash or actually not forward slash but question mark.

[00:03:20] And then we would say for example, format json or text, right? So this is just, we're gonna provide this possibility here for the user for the call to specify which format they want to receive. So if the format is equal to Json like, so then we're just gonna return here,

[00:03:41] return a response dot Json response dot Jason. If the format is anything else, then we're just gonna return here. So just gonna return response dot send and then we're going to format this string. So that's gonna be with back Ts as well so that we can format it or interpolate values. And then we say here color

[00:04:03] and the color is going to be the color variable that we have. And then we also have here, hostname is going to be the hostname that we have at the top, right. So now here we have a very straightforward answer or text formatted answer here

[00:04:21] so that the users can get in a single line, all the information that they need. This will help us once again with some demonstrations. So let's come to the terminal. Let's build this image here and let's create a container to test that everything is working as expected in the terminal. Make sure you are inside of the color API directory.

[00:04:40] This is the same one as it's open in the IDE. And now we can simply run Docker build. Let's tag this with LM Academy or whatever your username is, call our API. And since we were using the 1.0 0.0 version before, let's simply bump here. The minor version,

[00:05:00] there are no breaking changes in our API. So we're just gonna bump the minor version here to 1.1 0.0. And let's add the context to the current directory. We now have our image built here. So let's simply run a container Docker, run in detached mode, mapping the port 80 or you could even map the

[00:05:20] 8080 of our localhost to the container port 80. I'm just gonna pass the image name here. We then get the container hash back and I forward to cURL localhost colon 8080. We're gonna get back our HTML format called here and as you can see, I forgot to close the H two tag.

[00:05:41] My apologies for this. Let's quickly come back to the IDE and let's close it here. H two. Like, so that's great. Let's now once again, turn back to the terminal, we can terminate our container. So let's have a look at the running containers here. We're gonna remove, we're gonna remove this container. So Docker

[00:06:00] RM, let's remove with force this container. Let's build our image once again and it's OK for us to override the tag here, we could actually bump the patch version if you wish to do so because this was a bug. But since this image was not published anywhere and it's not used anywhere and we can just override this with a new tag

[00:06:21] and now we can once again run Docker, run, let's clear the console Docker run with LM Academy like so and now we should be able to once again row localhost 8080 we get our H two header back. So you can see that this hostname here is

[00:06:40] matched in the beginning of our container hash. And if we were to also crawl the forward slash API, we're going to get the text answer back. And if we were to pass here format equals to JSON, we're gonna get an error. Yeah, because we need to interpolate this or we need to escape this with double quotes like so, right, so for to just cURL this with the double codes here, it's interesting,

[00:07:03] we are still getting an error and that was a small misstep from my side. Once again, let's come back to the ide. And now here we need to either say request query dot format like so or we can use this structure in here and then immediately extract the format property from the query object.

[00:07:24] That should be all we need to do to fix this error. So let's save this, come back to the terminal in the terminal. Let's once again, stop our container. So we have the container hash here, we can remove this container first here. Let's build once again once again, overriding the tag, no problem.

[00:07:42] And now let's run this image once again, that's going to create a new container. And if we were to now row with a format of JSON, that's perfect. Then now we are actually getting the JSON response back once again, if we query with anything other than JSON. So text here, we're going to get the text

[00:08:01] oration back if we query with whatever we will get the text representation. And as you can see, once again, the hostname matches the beginning of our container. Hash That's great. The image is built. So we can now push this here to Dockerhub. Let's push LM Academy or whatever username you have here, call or A

[00:08:20] P 1.1 0.0. And that's going to push this image to Docker Hub once we've done that and we can confirm in Docker hub that the image is available. Then we are ready to go to the next lecture. Let's take a short break and I'll see you in the next video.