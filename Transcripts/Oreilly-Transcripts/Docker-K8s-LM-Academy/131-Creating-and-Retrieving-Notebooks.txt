[00:00:00] Hello and welcome back. Let's continue developing the routes of our notebooks back end. Make sure that you have your compose project up and running at the root of the directory so that the nodes rest API folder and that all the services are up and able to process the requests. Let's then define a couple of routes here.

[00:00:21] We would like to let me add a couple of comments so that we know the routes we would like to define. So first, I would like to create new notebooks. This is gonna be a post request to the endpoint forward slash like so we then would like to retrieve all notebooks, retrieve

[00:00:40] all notebooks. And that's just going to be a get request to once again the root of this router, we can then create or update a single notebook, single notebook that's going to be a put request to the notebook ID. So the request would look something like this localhost colon 8080

[00:01:01] forward slash API forward slash notebooks forward slash. And then here some ID, we then would like to also get a single notebook. So let's say here retrieve a single notebook that's going to be a get request to once again the endpoint with the ID here. And then we also would like to delete a single notebook, delete a single notebook.

[00:01:24] It's just going to be the delete request or using the delete method also to a specific notebook endpoint with this ID provided in the URL. Let's then quickly create these routes and we will start populating them. So we're going to have a post request to the forward slash here.

[00:01:42] That's gonna have also the request and the response. And then we're going to populate the body of this function a little bit later on, we already get the or we already have the get here. Let's remove the body because we're gonna change this. Let's then get also a specific notebook by ID.

[00:02:03] We would also then like to update a notebook by ID and to delete a notebook also by ID. Since we will be dealing with database operations, then all these handlers here, they will need to perform asynchronous operations.

[00:02:20] Let's mark them all with async so that we can use the async await syntax here. Let's then start with the body for the post handler. Let's wrap this in a try catch statement so that in case there are any errors here, we can just return a nice message to our client,

[00:02:40] just gonna say response dot status of 500 in case there is any error. And let me just send the JSON here informing error or dot message, right? So just something like this perfect within the try block here, we would then like to extract both the name and the description from the body of the request.

[00:03:02] So that's going to be request dot body. And then we would like to validate this because the name is a mandatory field. So if there is no name, we would immediately return here response dot status of 400 dot JSON And I will simply inform error

[00:03:20] and we will say here, name is required. We can also say here a name field is required. Let's save this. And if we pass this, if here, so if we do not enter this condition, that means our function will not return. Let's then just create our notebook. So notebook will be equal to new notebook

[00:03:42] and then we will pass both the name and the description here. We also need to import the notebook at the top. So let's say constant notebook is equal to require from the same directory dot forward slash models

[00:04:00] like so let's scroll down a little bit. And then after we have created the model here or the instance of the notebook, we then would like to await notebook dot save. So this is the actual operation that will persist the notebook in the database. And once this happens, we're gonna say response dot status,

[00:04:21] we're gonna set a status of 201 and then we will send the body here we're gonna say data and that's just gonna be our notebook. If you wish to pick a couple of individual fields here from the notebook, then feel free to do so. But for us, let's just return here the information that Mongoose populates for us.

[00:04:42] Awesome. That's it. For creating a new notebook. We can jump to postman and try to send a post request here. Let's then set the body. So let's click on body raw and then let's select JSON. In my case, it's already selected here. Let's then just send a request with an empty body.

[00:05:01] And we can confirm that we are already getting the error here and the status of bad request. And that's amazing if we were to now send the body here and define the name and we're just going to say here my notebook and then we can also provide a description here will simply be let's say my first

[00:05:20] notebook like so let's then send this request and we can confirm that we get back here the response including the notebook and its ID. That's great. So our post method is working, let's now implement quickly the get method to retrieve all the notebooks from our database within the ide.

[00:05:42] Let's scroll down a little bit more to the get method here and this get method will be slightly smaller than the post method. So let's just once again, wrap this in a try catch statement. And if you are once again, already familiar with express. And you wish to extract this to a common Middleware to handle this error.

[00:06:01] Then by all means, feel free to do so, I'm going to keep it simple here. Let's just copy and paste and send back the error message here. In case something goes wrong, we can then within our try block, simply say notebooks is equal to a weight notebook dot find and then we will not pass any kind of filters here.

[00:06:24] That means that Mongoose will retrieve all the documents here. Let's then simply return response status of 200. You don't need to set this status explicitly if you are just sending back a json here. So you could also say

[00:06:40] Res dot Json like. So that is exactly the same as what we are doing. Let's then use data here and we'll simply return the notebooks array. Perfect. Let's save this and let's double check in Postman that we can retrieve all the notebooks back here in Postman. Let's change the request method to get

[00:07:01] and let's send this and then we can confirm that we get our notebook back. Perfect. So both notebook creation and retrieving all the notebooks is working as expected. Let's take a short break and come back in the next lecture to continue with the implementation.