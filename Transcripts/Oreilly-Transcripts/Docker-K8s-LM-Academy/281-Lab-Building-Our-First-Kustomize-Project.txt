[00:00:00] Let's now create our first customized project. We're gonna create a couple of Yamma files here. So Cnis manifests and then we will see how we can use a customization file to include specific resources into our customized project. Let's start by creating a development name space here.

[00:00:20] The API version will be version V one, the kind will be name space. The metadata here will just include name DEV like. So, so that is for the name space. Let's now create both a deployment and a service here and that's just gonna be an NGX deployment.

[00:00:41] So we don't need anything super fancy here. This will serve us as an example. The kind here will be deployment. We're gonna leverage the autocomplete here. So let's use Ngen X and now here, let's also set the image right. So the image here just gonna use engine X and then the version 1

[00:01:01] 27.0 and the port here will just expose port 80. We would then also like to create a service that will match these labels here. So let's create a new service here. That's just gonna be the engine X SVC kind will be service and then we're gonna use the NAMEX

[00:01:21] SVC and the selector will be APPX. Let's just confirm that this is the correct one. Yes, that's the one that the pods will be started with. So let's now here, clean this up and let's use APPX the port where the service will listen to or at least it will redirect from port 80 to our containers.

[00:01:41] Port 80. Let's say that. Now we would like to create a customized project here that will include both the deployment and the service under the same project. This help us also reuse this as we work later on with customizing different environments for both the deployment and the service.

[00:02:00] So the way for us to create a customized project is very straightforward, we just need to create a file called customization dot YAML and this file here, it does need to have a very specific API version at the time of this recording. That is customize dot config dot K

[00:02:21] dot IOV one beta one, the kind here that we will use is customization. So customization and then we need to. So basically here we can start declaring multiple things for this point in time. For this discussion, we will simply declare which resources we would like to

[00:02:41] include in this customization or in this customized project. So we will declare the resources key here and this is a list of files contained in the same directory. So we can also use paths here for the resources. We're gonna see that a little bit later on. But for now, we can just declare both the engine

[00:03:02] X dash deployment dot YAML and the engine X dash SVC dot YAML like. So, so this will include both these two files here in our customization. Let's then come back to the terminal here and let's now run the cube cut, customize command and let's simply pass the directory.

[00:03:23] We would like to run this customized command. So this can be the current directory, but it could also be some nested directory, right? So we can also run this from upper or from parent folders. And then we can simply pass the nested folders. This is very useful when we are working with bases and overlays.

[00:03:40] And we want to just run this command with a single overlay, but we're gonna come to that a little bit later on. So the moment we do that, you see that this actually outputs just our service and our deployment here, we can then run the cube cut, apply and then we can apply this entire customization with dash K

[00:04:02] and then pass the current directory here. And the moment you run this command, you can see that this is slightly different than the output of dash F. Because if we run the dash F flag here, if we run the command with dash F, it's gonna look at all our CORS manifests within this directory here and it's gonna create all of them.

[00:04:23] That's gonna be the DEV NS, that's gonna be the engine X deployment and the engine X service with the K flag for customize. This is different because it's gonna look for a customization file and it's gonna look inside of this file and it's gonna deploy only the resources that are mentioned here.

[00:04:42] We can also delete only the objects that were created via the dash care the customization file here by running cube, cut, delete dash K and this will delete both the service and the deployment. So let's now start seeing a little bit of what we can do with this. I'm just gonna give one example where we will come back to

[00:05:01] other many features of customized as we move along this section. But let's now suppose that we would like to deploy both the engine X deployment and the service in the DEV name space. So far, we would have to actually come to both files and add the name space here under the metadata of each file.

[00:05:20] That means we need to duplicate this in both these files. Now, we are talking about only two objects, only two manifests here, but this can easily grow to tens or even more depending on how many objects you are managing for a certain application. So copying this here is really not scalable and even more.

[00:05:40] So if we wanted to have the deployment and the service deployed in both the DEV and the product name spaces, you would have to actually have a copy of the file. So basically, we would have one with the name space DEV, we would duplicate the deployment for broad and we would change just the name space field. So this is really not convenient, right?

[00:06:01] So it's not a scalable approach for us to manage our application objects and manifests. So one thing that customize allows us to do is to actually inform the name space in the customization file itself. So if we were to now come here and say name space, DEV, let's save this here. Let's come to the terminal. Let's clear the screen.

[00:06:21] And before we run the the custom, the cube Carol apply command, let's run customize with A dot And as you can see here, this outputs the service and the deployment with our name space already populated. This is a small change, but hopefully, this helps you start imagining,

[00:06:40] trigger the imagination of how many cool things this can actually help us do. So before we apply our customized or before we apply our customization or our customized project, we do need to create our name space. Now, name spaces. So let me just create it here, Dev, Ns dot Now name spaces are something that they are,

[00:07:02] they are not necessarily application specific, right? So for me here, we could theoretically include the name space also as part of the customization here. But I don't find this to be a good practice because you might also want to deploy multiple customizations inside of the same name space.

[00:07:21] And then by adding it here in the resources, I find that it really creates a little bit of coupling between the name space and this specific customization. So name spaces are something that I do prefer to create separately primarily because they are objects that can be intended to hold multiple applications or multiple different projects.

[00:07:41] So let's create the name space separately here. We have created our name space and now we can apply our customization here. So not dash F but dash K in the moment to do that, we can confirm that we get both our deployments and our services in the name space DEV. And they are perfectly deployed and up and running.

[00:08:01] That's amazing. Congratulations on completing this first lecture on customize. There are still many aspects and many features I want to discuss with you. But for now, let's just delete our customized project and let's also delete our name space here. So let's delete the DEV, dash and S like so, and we are good to go. Let's take a short break and I'll see you in the next lecture.