[00:00:00] Great. Let's explore the different components of the control plane. These are once again Kernis components. So it's basically the Kernis software that is running and making sure that the cluster is working as expected. Let's then start with the API server. So the API server, as already mentioned, it exposes the Kubernes

[00:00:22] API. This is the main entry point for all the tasks that we are going to run with Cnis. Whenever we wish to create pods, update pods, get all the running pods or any kind of other operation within Kernis, we will communicate with the API server. This is also often abstracted because we normally interact via the Cube

[00:00:46] Koi. And this cuo it really takes very human friendly or intuitive commands and it translates these commands to API calls, HTTP calls that are then sent to the API server, except if you are writing something to directly

[00:01:01] make HTTP calls to the API server from CNIS. The cube is normally the entry point for communication and then it will take care of translating this to the HTTP call. Now, let's talk about the scheduler. The scheduler has a very important job and it is

[00:01:20] basically responsible for placing pods on the most suitable notes. There are a variety of factors that are taken into consideration when scheduling pods on worker nodes. And one example of this is resource availability. Let's say that we want a pod that needs two gigabytes of. But our first worker node here has only 1.5 gigabytes of ram available.

[00:01:44] While the second worker node here, maybe it has four gigabytes of ram available. So the scheduler is going to look at the resources available and it is going to place our pod in the second worker node. As I mentioned, there are many other factors here and we can also inform or constraint,

[00:02:00] define certain rules that the scheduler should follow when choosing the nodes for our pods. Therefore, since this is a fairly complex task here, there is an entire process which is the cube scheduler running to take care of this task, we then have what's called the controller manager. And the controller manager is responsible for

[00:02:22] running controller processes that handle routine tasks. Now one example here is the node controller. So think of a controller as basically just a loop that is doing one single thing at a time. So the node controller is taking care of making sure that the nodes are healthy,

[00:02:40] registered with CNIS and they are running correctly. The replica set controller, for example, is going to take care of the replica sets that are inside of our nodes and it will make sure that they have the correct number of replicas. Same thing, the job controller will take care of jobs that we create within the worker nodes and it will make sure that they are being executed correctly.

[00:03:02] So whenever you see something that is happening in Kubernes, and you are wondering how does this actually happen behind the scenes? How does a replica set know when a pod is deleted and it needs to create a new one? Well, the answer is actually there is probably a controller behind the

[00:03:20] scenes which is looking at the data that is stored in the store and it is continuously checking whether the state of the cluster is matching its desired state. And if there is any difference, that specific controller process, which is part of this controller manager here will then take action to bring the cluster back

[00:03:43] to its desired state before we move forward. Let me just quickly clarify what a replica set is. I've used it in a few examples. A replica set is basically a Cnis object where we can specify how many pods we want to have running. And the replica set will take care of ensuring that

[00:04:02] we always have this desired number of pods running. We're going to enter into replica sets in much more detail later on in the course. But since we are already giving some examples here, it's good to have a high level overview. So let's take once again, the example of a replica set. And I just want to highlight that as you can see, there is already some interdependency here,

[00:04:21] the controller manager or the replica set controller, let's say that it identifies that it needs to create a new pod. I want, let's say four pods, but there are actually only three pods running on my cluster. So the replica set controller says, hey, I need a new pod, please create a new pod. And then the scheduler will also come into play.

[00:04:41] So there is a dependency here between these elements and the scheduler will come into play because it is responsible for scheduling the pod in one of the worker nodes given all the constraints that are in place in our cluster. Great, let's now come to the store. And this is a distributed key value store that stores all the cluster data.

[00:05:03] If you think about cluster state or data, think store, it includes both the configuration of the cluster as well as its state. The store is the single source of truth for the cluster state. And the API server is used to write data and to read data from the

[00:05:23] store. Whenever we need to either update the state of the cluster or read, what's the current state I have also included the cloud controller manager here because this is a very common component in current cluster configurations. And what the cloud controller manager does is it enables Kernis

[00:05:41] to interact with the underlying cloud infrastructure, it will handle tasks such as managing the cloud based load balancers, the persistent storage that will happen many, many times outside of the Cnis cluster using cloud native solutions such as AWS S3 or EBS or EFS, as well as node management.

[00:06:02] I want to make a very important distinction here, Kubernes is software. Kubernes is not hardware. So there is an underlying infrastructure needed. Basically, we need computers where we run the Kubernetes cluster. So we think here, normally we will find that nodes, they map to individual VMS.

[00:06:22] So we would run nodes in individual or in different VMS. So that if one virtual machine goes down, then only one node goes down the entire cluster, right? But if you don't want to do, so then you can also run an entire Cnis cluster in a single machine. The best example for this is when we run

[00:06:42] Cnis locally, except if you have a fleet of virtual machines that you are somehow using to run Cnis cluster, which I think is hardly ever the case you are running Kubernetes cluster in your personal computer, in your laptop. And that's a single machine. Nonetheless, whenever you create

[00:07:00] a cluster using minicube, for example, you can specify that you want more than one node. So nodes here, they are not necessarily VMS, they are not 1 to 1 maps to VM or they are not synonyms to VM. However, it's best practice that we run nodes in different VMS and that we most

[00:07:20] keep the masternode individual or the master nodes separate individual or apart from the worker nodes. As you might imagine all these VMS, they are most of the time run in cloud infrastructure. So it's also great to have this cloud controller manager taking care of managing the underlying VMS,

[00:07:41] the underlying nodes here so that they are working as expected. Awesome. So hopefully this lecture helped us understand a little bit better the role, the individual role of each of the components as well as how they come together to execute the functionality or to deliver the functionality from

[00:08:00] CNIS and make this tool. So amazing to work with. Let's take a short break. I believe this is a very well earned break. Let's take a short break and we'll come back in the next lecture to explore worker nodes.