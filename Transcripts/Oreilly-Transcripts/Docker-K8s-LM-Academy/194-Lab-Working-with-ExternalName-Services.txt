[00:00:00] Hello and welcome back. Let's explore now, the external name service, as I mentioned, the load balancer one, we're gonna postpone discussing it a little bit more to when we actually discuss how we deploy a co cluster in a cloud provider so that we can see how load balancer service and the cloud load balancers relate to each other.

[00:00:21] So let's go to the ID and let's create our last file for our external name service in the ide. Let's create a new file here. We're gonna simply call this Google Google ext name dot yo. And this is gonna be once again a object of type service.

[00:00:41] So API version is V one kind is service. We then have the metadata section, metadata here and we're going to specify the name and the name will simply be here my external SVC. And so now here, all we need to do in the specification is to

[00:01:02] specify that this is a of type external name. And then we need to provide the external name configuration. This is a DNS name. So you should not include http nor https here. So this should not be included

[00:01:21] and then this will be the DNS name that the service will resolve to. Now, we might run into issues with HTTP and HTTPS requests here. So I don't expect this to work perfectly out of the box, but at least we can get this demo up running and we will see that we are actually able to go through to the Google endpoint

[00:01:44] back in the terminal. Let's just apply the file with Google ena dot YAML. The service will be created. And if we now get the services within our cluster, we should see that both the cluster IP and the nodeport services we have created

[00:02:01] and then we have my external SVC. So we can now create a new pod here. Let's create our traffic generator pod, not for the traffic itself, right? So let's just create this here, traffic generator dot Yo, I'm just creating this to run a bash or

[00:02:20] a shell here so that we can run call requests. Let's start up the pod and now we can exact into the pod. Let's get an interactive terminal here. The name of the pod is traffic generator. And then here we should pass a dash dash and then a

[00:02:41] shell or sh here to start a shell inside of the container. You might also let me just exit here. You might also remove these two dashes, but then we should actually get a deprecated warning, right? So just to stay on the safe side here, let's add the two dashes clear the console. Let's add the two dashes here and now we can crawl my external SVC

[00:03:06] and the moment we do that, we get something back already. So you can confirm this is an HTML document. There's probably some Google stuff around here. Yeah, that's good google.com. That's looking good here. And we get a 44 not found, right?

[00:03:20] So this is probably because of this HTTP https incorrect configuration. We would have to do a little bit more here to get this up and running as expected without returning any errors. But the bottom line here is that we were able to actually send a request to some internally defined service.

[00:03:40] And in turn this service here will return a cname record which will then be used Byrow to actually forward or to actually query the Endpoint google.com. We don't use this service as often as we use cluster IP load balancers, but it's still good to know that it exists. So let's just keep it in the back of our minds.

[00:04:01] This is one possibility in case we would like to have a stable name here that we can use to send our requests to. So let's just exit here from our pod and let's do a little bit of cleanup. So now we can confirm that we still have our pods up and running here. So let's now delete everything here by running cube, cut,

[00:04:22] delete and then passing the current directory just before we do that. Let's just confirm that we have everything here. So we have the color API cluster IP the deployment to nodeport, the Google external name and the traffic generator definition files. Whenever we run cube, cut delete and then pass the current directory,

[00:04:42] everything will be deleted. We can also pass a force here to immediately terminate the traffic generator. Since this one here, it doesn't gracefully terminate within the grace period that is given by CORS. So let's just force delete everything and we can confirm that everything is gone if we were to now get the pods.

[00:05:02] There is nothing in the default name space deployments. There is nothing in the default name space services. There is not nothing in the default name space except for the CDIS one that already comes to us. But everything that we have created is successfully deleted. That's amazing. Let's take a short break and I'll see you in the next lecture.