[00:00:00] Hello and welcome back in this lecture. I want to explore the concept of build contexts. We have been building Docker images based on Docker files for a while now. So it does make sense that we spend some time to understand what a build context is and how this relates to the Docker demon and to the Docker files that we write

[00:00:20] for us to better visualize this. Let's first of all, just create a very simple index dot Js file. All this file will do is just console log a message A hello from node. And we're gonna write a Docker file for us to actually execute this index dot Js.

[00:00:40] Let's just create a new Docker file here. This Docker file, we will start with a from node colon 22. And for the sake of doing things a little bit different. Let's just use the alpine version of node 22. Since we have a completely clean javascript project here without any dependencies, we do not need to have a package dot

[00:01:02] Jason or a package lock dot Jason. We can just directly copy all the files from our directory into our container. Let's change the work there here to forward slash app beforehand. Let's copy all the files and then let's just have a command here which will be node

[00:01:20] call on index dot Js. Once we save this, we can now come back to the terminal and let's check that we have all the files here in the directory. Perfect. So now we can run Docker build. Let's tag this here from hello from node. And when we pass a dot I have already mentioned this,

[00:01:41] but I didn't really spend too much time explaining what it means. And this dot here is the context. Now what does a context, a build context mean? Basically a build context refers to all the files and subdirectories and all the content that is required by our Docker file.

[00:02:00] In order to build the image, this context here will be uploaded to the Docker host to the Docker demon because the build process is not run in the Docker cli. We are just triggering the build process, but the build is executed once again by the Docker demon by the Docker server in our machines.

[00:02:20] If you are using Docker locally here, it just happens that the Docker cli and the Docker server, they are both running at the same machine. But this might also be the case that the Docker cli is running in your machine for example, but it is connected to a remote host. So this context here will be uploaded to the Docker host,

[00:02:41] which is then responsible for building the image. And since the Docker host might be separated from the machine which is issuing the build command, then the Docker host doesn't really necessarily has the same set of files. So what this will do is it will upload all the informed context here to the Docker host.

[00:03:01] Great. Let's run this command here and see what's gonna happen. You will see here if we inspect the first few lines, you can see that there is a transferring context here. There is also a loading a dockerignore file as well as some metadata. But for us, these two lines here are interesting.

[00:03:20] So we are loading something called a Docker. Ignore that we did not explore yet. And then we are transferring the context. This context here is very small because we have just an index dot Js file. We scroll down here a little bit and we just run the Docker images. Then we will see that we have our hello from node. And if we were to compare this here with our previous Express image,

[00:03:43] you can see that this is almost 10 times smaller and we have just changed the base image. If we were to just out of curiosity here, Docker history, hello from node, we will see let's once again, reduced size a little bit here. You will see that it has considerably fewer layers.

[00:04:03] So let's just now run here, Docker History Express app once again without clearing the console. And then here you can see the big difference. This one once again has those big big layers. While this image here, the alpine image has considerably less.

[00:04:20] Anyways, I'm diverging from the content here. Let's go back to our current Docker images here to inspect that we now have our hello from node. Let me just quickly adjust here the zoom and that this image here was also built based on this Docker file. Now, this process was fairly quick because our directory once again,

[00:04:43] it has very light files, right? So this directory here has very small files, there is nothing heavy to be uploaded. However, whenever we are running a Docker build command and we are passing the whole context here. It just so happens that if we have some heavy directories,

[00:05:01] maybe some dependencies like an old modules directory with several dependencies or some other, maybe a Python virtual environment or something, this can have several 100 megabytes. And if we just pass the context like so then this will all be uploaded to the Docker host. So we can simulate this by creating some fake file in Mac.

[00:05:22] This is the MK makefile command here. So just gonna say Makefile dash n and then we can create something with, I don't know 10 gigabytes or if you have just one gigabyte or five gigabytes, let's go with five gigabytes. And we're just gonna say here some dash large dash file. Once we create that,

[00:05:40] it will take a little bit of time to create one file with five gigabytes. But once that is done, let's have a look here. Once again, we have our very large file, right? So this is some large file here. And now let's clear the console. And now the moment we run and this is a fairly heavy command because once again, let's inspect our Docker file,

[00:06:02] we are copying all the files into our container so that five gigabytes file will actually be copied into the container. So for us here, we will simply say Docker build hello from node. Let's clear the console run this and let's wait. Now you can see that we are transferring the context and the context is very large.

[00:06:21] It is five gigabytes. So it is taking a lot of time for the Docker build command to actually upload the file from our local machine from the local directory to the Dockerhost. Once again, we are running this locally, right? You can see here that the copy command also takes five seconds. The exporting layers is also taking a lot of time.

[00:06:43] And if we were to actually run and visualize our images here, you will see that our image has five gigabytes. Hopefully this gives a little bit of clarity around what the context is. It's basically all the files in the current directory that are then used in our image during the build command. And considering our Docker file.

[00:07:03] Let's quickly edit our Docker file. We can either use Vim or we can come to the ide. And instead of copying all the files, let's change this. And let's simply copy the index dot Js into index dot Js like so, so we're just copying a single file here.

[00:07:23] Let's save this and come back to the terminal to build our image again. Once we come back to the terminal and we run Docker build with the same tag and the same context here. Let's run this. And now as you can see, Docker is smart enough to identify here,

[00:07:40] you can see this transferring context to bytes here. Docker is smart enough to identify that that large file is not necessary in the Docker file is not needed and it's not gonna transfer it to the Docker host. If you were to then also have a look at Docker images, we will see that the new image is actually very small

[00:08:00] and even though the large file is still in our directory here, it was not uploaded to the Docker host. There is still another topic I would like to discuss with you, which is the concept of a dockerignore file. Since we will also work with this large file, I'm not gonna delete this for now. Just gonna leave it in my machine. I will also leave the images here.

[00:08:23] But if you wish to go ahead and do some clean up because of space concerns. Then by all means, just go ahead and delete these files here, otherwise you can leave them. Let's take a short break and come back in the next lecture.