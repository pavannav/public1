[00:00:00] Hello and welcome back. Remember from our project about the engine X. When we customized the contents of our Nginx server, we did run several commands manually. So a great way for us to practice what we have just learned about Docker files is to create a very simple one to automate all the changes that we did and write them in a single file. Let's create a new file here.

[00:00:21] We're going to call this Docker file. And then once we do that and I'm using VS code here. So for whatever ID that you are using or text or if you do have the chance to install an extension that will help you manage the files better, then by all means just go ahead and click on install. You see we'll come here.

[00:00:40] This will be installed. And once we do that immediately for me here, we are already getting an error. So the from instruction in a Docker file, this is mandatory. So we do need to provide a base image here. I would recommend that you go ahead and install this extension or an equivalent one in whatever IDE you are working on. And the one from Microsoft here is a fairly widely adopted one.

[00:01:04] So I would say let's just go with the standard. If you do find other useful extensions, then also feel free to install them. I'm not going to dive too deep into extensions on VS code. Feel free to customize the ID as you please. So let's come back here to our Docker file. One more remark.

[00:01:20] I'm within a new folder here where I created my Docker file. So this folder is empty and I'm using it exclusively for this Docker file and this video discussions. So go ahead and create a new empty folder and create our Docker file within that folder. And let's start by defining the base image. So the from here will take our base image.

[00:01:41] We're going to simply say X and we will stick to the version that we have been using so far 1.27 0.0. As you can see here, we do have some nice autocomplete or at least not autocomplete, but at least some documentation here specifying the syntax of this command. The next step for us is to install the VM

[00:02:01] dependency that we wanted to use from within the container. So let's just run apt get and we're gonna say update and then we will run another a PT dash getit and we will say install them here. I'm mirroring the dependencies we have installed when we implemented the engine

[00:02:20] X project. If you wish to install any other shell dependencies here. Then by all means, feel free to do so, particularly because we will be working with the IDE fairly often. So it might be the case we don't use VM anymore, or at least we're gonna use it less often. But anyways, I just want us to be able to have a clear comparison, a clear parallel between the commands we ran manually

[00:02:42] and the commands when they are codified in a Docker file. That's why I'm gonna stick to the same dependencies. And if you wish to change that or customize it in any way, then by all means, please go ahead. This here would already enable us to use Vim from within our container. Let's save this and let's come back to the terminal.

[00:03:00] Make sure that you are within the same folder where you have created your Docker file. In my case, that's the Docker file X and let's then run the command Docker build. Let's pass a tag here. So I'm gonna say next and Genex local or we could actually say, let's say just web server like so

[00:03:23] and we're not going to specify any version here. So let's just say web server and we're going to pass a dot Once we do that here, then we will see that Docker will build this based on the engine X 1.27 0.0. And we will see that a few of the commands here are executed. Now here, I forgot that we need to

[00:03:42] accept this command because the apt get install, remember it was hanging and asking us to confirm whether we wish to continue with this operation because this depends or this waits for user input. Then it doesn't really work for automated builds, right? So automated builds by default, they do not allow for user interaction.

[00:04:01] So for us, we need to assume a yes here with apt get and the way to do so is to add a dash Y in the command. So back in the ide, let's add here. Y and the moment we do that, let's save this, come back to the terminal and rerun the Docker build command back in the terminal. Let's just clear the screen run Docker

[00:04:21] build once again and see if now everything will go through. Perfect. So as you can see here, our build was successfully executed and we have also tagged this with web server if we were to check our images here. So Docker images, let's have a look. We have our web server here. That's great.

[00:04:40] So let's clear the screen and now we can actually Docker run, let's run it on a detached mode. We do not need to map any ports here. Let's just run our web server like so and this will start the container. We did not provide any name. So here do not get confused. This web server is the tag that we're using for our image,

[00:05:00] it's not the name of the container anymore. So now if we were to run Docker ps here, we will see that the name is different. Even though the image is web server, the name is some random name. So let's just copy this container id, clear the screen Docker exec it to get an interactive shell. And we're just gonna say shell

[00:05:21] and now if we were to run Vim here, we actually get Vim installed already in our container. That's pretty cool, isn't it? So we were able to install the dependencies by defining them in the Docker file. I'm just exiting here from Vim and I'm exiting from the container as well. Coming back to the ide, let's just revise what we have done. So we have specified the base image here.

[00:05:44] That is the engine X that we started working with. We then ran two comments or basically we defined here two instructions. So remember the Docker file is just a series of instructions that are executed from top to bottom. So the first instruction will be run in our shell

[00:06:01] within our image after we pull the base image here. So we're going to execute this instruction and then we're gonna execute the second instruction once this is completed, then the image is built for us and we can use that image later on to make sure that we avoid any confusions. I do understand that the web server image here might be quite confusing.

[00:06:23] So let's just change this a little bit. We're gonna say Docker stop or actually Docker RM here or let's first stop our container. So Docker stop, we have the the container ID here. So we gonna stop the container first. And now we're going to remove the container once we remove the container.

[00:06:42] So we will not be able to see that anymore. Here with Docker PS A, we are then free to delete this image here. So let's copy the image id or also the tag here. We can say Docker RM to remove the image and then paste the tag here. Once we do that, we will see Docker images,

[00:07:00] we will see that there are no images here anymore. Let's clear the screen and let's run our Docker build command once again. But now we're just gonna say web server image. And so now here we will know that this is the image of our web server. Once we do that, this should be fairly quick. And here you will see that we get some very interesting cached messages.

[00:07:21] We're going to touch upon Docker's layered architecture again. A little bit later on, we have already explored that little bit when we discuss the foundations of Docker file. But here you will start to see that when the commands in a Docker files stay the same. When they don't change Docker can leverage its local cache to speed up the build. Perfect.

[00:07:41] But now that we have built our image and that we can visualize it here with Docker images. We can see that web server image is a little bit more descriptive. And hopefully, that's not going to confuse us too much with the web server containers that we were running previously. Let's take a short break and come back in the next lecture.