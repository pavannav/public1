[00:00:00] Hello and welcome back. Let's start getting our hands dirty and practicing how we can run containers in Docker. We will start by running a simple container. Understanding what happens when we run a container with the Docker cli we're going to then move on to understanding the container life cycle. It's very important that we are aware of the different states.

[00:00:23] A container can be in as well as which CLI commands or which Docker commands we can use to transition between these states. We will then move to the core of this section which is to explore the Docker. And we will start by demonstrating essential c comments for managing containers and images

[00:00:41] as well as covering or explaining the container behavior and how Docker manages both short lived as well as long lived or persistent containers. We will also cover comments for logging for running commands inside of containers and for executing shelves. And last but not least, we will also introduce the Docker build process by creating a

[00:01:01] simple Docker file and then building a custom Docker image. After we cover the core of Docker cli, we will also go through how we can actually get help from within the CLI and how we can make best use of the CI itself to clarify, how to use a command. Aren't you excited? I for sure am. So, let's get started right away.