[00:00:00] Hello and welcome back. Let's take a few minutes here just for a little refresher around the motivation for volumes for data persistence in Docker. Let's run a very simple engine X container so that we have a long running process here. So we're gonna run and we're gonna call this name web dash server.

[00:00:20] The image will simply be X and the version here, we can use 1.27 0.0 and we can actually start this in the detached mode. So we are not gonna, that should be after the name here. So we're not gonna do anything with this container. We're gonna, we're not gonna query the container. So we don't need to map any ports here.

[00:00:42] The moment we do that, then this is gonna download the image if it's not found locally and it's gonna start the container for us. We can confirm that the container is running with Docker PS and we can now exact into the container. Docker exec it, the name of the container is web

[00:01:01] server and the command will run is just shell and that should be just a single dash here. That's looking much better. Perfect. So now we have our container running here. If we were to list all the files, then we will see that there are several folders. Let's just change here into the temp folder. So forward slash temp

[00:01:21] and let's once again, we have nothing inside. So let's just write a new file here. Hello world inside of our temp folder. So just gonna say hello dot TXT, we can now confirm that the file exists and that is great. So remember this is inside of default slash temp and then hello dot TXT,

[00:01:41] let's now exit this here. Let's clear the console and let's run Docker, stop our web server, right? So remember Docker will persist. So the files will persist between restarts of the containers. So if we have a Docker stop and then a Docker start here. Once again, we can run the Docker exec

[00:02:02] and we can just get forward slash T forward slash hello dot TXT and we will get the contents of our file back. That is great. So if the container for whatever reason it stops and then it starts again, we have our files there. However, this is as far as persistence goes.

[00:02:21] If we are not using volumes, if you were to stop the web server here to stop the web server, and if we were to remove this container, web server here, then this will for sure delete our files. If we were to now run another container with a name of web server, even if it's based on the same image here.

[00:02:42] And we were to run once again the Docker exact command and look for our file. So get forward slash forward slash hello DXT. This does not exist anymore. Hopefully by now, that actually makes a lot of sense to you because these are just really two different containers, right? They have nothing to do with each other.

[00:03:01] So it doesn't really make sense that the files that belong to the first container here are actually present in a completely new container. So to summarize the data inside of a container is by default restricted to that container's life cycle. While this is OK for stateless applications and workloads that we are running.

[00:03:22] What happens if we actually need access to that file beyond the life cycle of the container. If this is a database or if this is something that has some state that needs to also be available to other containers, then we're in a pretty bad place if we are just running containers like so,

[00:03:40] so come once again into the picture Docker volumes and this really provides us a solution for data persistence in Docker. So let's take a short break and come back in the next lecture to dive a little bit deeper into Docker volumes.