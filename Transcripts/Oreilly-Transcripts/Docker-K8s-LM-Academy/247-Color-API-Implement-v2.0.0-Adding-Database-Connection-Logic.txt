[00:00:00] Hello and welcome back. The only thing that's missing here in our application is for us to connect to a database and we're gonna do that from our index dot Js file. The first thing we need to do is to require Mongoose as a package here since we will use Mongoose to connect to the database. And then all we need to do here is to just come before the app

[00:00:20] listen and then we will connect to Mongoose. So we're gonna say Mongoose dot connect here and this will take a string to connect to our database. So that's gonna be the database URI as you can see here, we're gonna take this from our environment VBO. So simply say process

[00:00:40] env dot DB underscore URL and then we will add here the then and catch blocks, right? So if the connection is successful, then we will say console log connected to mongo DB and then only then we will actually start listening for requests.

[00:01:01] We don't want to listen for requests if our connection is not successful. So let's add the app dot listen inside of this block. The last part here is just to add a catch block in case something goes wrong. So I'm gonna say here error and then we will say console dot log

[00:01:20] or console dot error here is better, could not connect to Mogo db. And then we will also print the error here console dot error er R like. So, so this is looking much better. We now have a fairly straightforward set up here, much cleaner index dot Js.

[00:01:40] We have our routers here isolated and modularized and then we have our delayed startup so that we don't break anything here and then we have our connection to Mongoose. Perfect. So before I forget here, we did start working with Mongoose and it might very easily be overlooked here that we actually need to update some of our functions from our or

[00:02:03] actually from the time here where we start using our functions from the db four slash color Js. They are all asynchronous, right? So here we actually need to update our get color to be an await here. So this will be a sync, this router

[00:02:20] and then we need to await our get color here, the same thing for our root route. So let's change this into a sync and let's await our get color. Perfect. So I believe we already did a lot in this video. There is still something missing here that I would like to implement with you and it is to actually be able to update the colors in our database.

[00:02:41] So we have an API router get here for just forward slash API, but we don't have the routes to either get a single color or get all the colors nor to update a color in our database. Let's take a short break and let's come back in the next video to wrap up this functionality.