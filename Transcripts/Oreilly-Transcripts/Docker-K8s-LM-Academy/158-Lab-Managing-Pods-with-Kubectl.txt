[00:00:00] Let's now explore how we can get some more information about this pod. We have created the pod and we are able to retrieve the pod as well as some summary here or some very high level information by running the cube K get pods command. This is going to retrieve all the pods that have been already created in the Kubernetes

[00:00:21] cluster, but it doesn't really give us a lot of information about the pod. There is a much better command for us to use and it is the describe comment. So we're gonna say cube car describe. And then here whenever we use the describe command, we do need to provide as the first argument here, the resource that we are looking at.

[00:00:40] So this would be let's just put here between the sign. So that would be the resource. And then here we would provide the name of whatever we want to describe. This can be a pod, can be a service, can be a deployment, can be a lot of things here and we actually provide the name here. So we're gonna run cube cut describe pod Genex like. So

[00:01:02] this is gonna print a lot of information on the screen, just gonna scroll up a little bit more here so that we can go through this in details. And as you can see here, we have the name space where the spot is created, which service account it is using the node where it was created the start time of the pod.

[00:01:20] We also have the IP address and this is something that we will use in a little bit to communicate with the pod. We have the status of the pod here which is a running status. We have more information about the container image. This can be very useful when debugging to know which actual image you are deploying in each container.

[00:01:41] If we scroll down a little bit more here, we will also see that we have a series of conditions which can be useful to identify whether there was any problem when scheduling the pod or when initializing the pod. And at the very bottom here, we have a list of events. This list of events can also provide pressures and very relevant information

[00:02:02] when trying to debug whether there were any issues when creating the pod. That's great. But how do we actually talk to this pod? So for now, we have an IP address here which you can use to communicate with this pod. This is a private IP it is restricted. So only pods and workloads running inside of Kubernes

[00:02:21] can communicate with the pod by using its IP address, if we were to just try to copy this here and let's clear the console, try to run a cURL command to the pod. This is not going to complete. You will see that the cURL here is hanging and then eventually it will fail. And that's because this IP address is reachable.

[00:02:41] It can be resolved only when we are sending requests or only when we are communi communicating from within the cluster. All right, this is hanging for quite a while. For me, it's almost been a minute. So I'm just gonna stop this here and let's now create another pod. So let's run cube cut, run, let's pass the dash ID flex here.

[00:03:02] Very similar to how we can do it with Docker to get an interactive shell running inside of this. Here are actually inside of the container that will be running inside of the pod. Let's call this pod alpine and let's use the image here. Alpine, image alpine and the tag here. Let's just say 3.20 like so

[00:03:22] and then we can pass here a command to be executed at the startup of this container. And this is going to give us the interactive shell by simply starting a shell script or by simply starting a shell here. As you can see if the prompt here changes. And you also see this message here, then you are inside of this container.

[00:03:42] Let's then add here cURL. So let's just add the cURL utility by running a PK updaterow like so that will take just a few seconds. We can confirm that Chro is installed by running cURL here like so and also crawl dash dash help. We can also confirm that we get some help information.

[00:04:00] Let's just scroll up a little bit here. I still have the IP address. So let's just call copy the IP address here. Let's scroll down and now let's try to crawl our Nginx pod. And as you can see here, this is working as expected, we immediately get a response from this container here with the HTML code for a defaultin

[00:04:22] index dot HTML. Page one interesting aspect is that by default, we cannot use names here to communicate with the container. So let's clear the console here. And in Docker, we can set up the by using user defined networks,

[00:04:40] we can talk between containers so we can use the container name as a communication mechanism as a host to reach out to and talk to those containers here. This is not possible. So for to just try to run chro engine X, it's just gonna give us a, I cannot resolve this engine X host here. Now, this is just the default behavior, right? So we have just created a pod here.

[00:05:03] We didn't do anything else. And of course, Cnis offers many ways for us to actually set up this communication, this interpod interservice communication here. So we're gonna come back to this in a little bit. I just wanna show you that by default the resolution here based on the name is not gonna work. Awesome.

[00:05:21] Let's then exit this shell here. Let's once again have a look at our container. So cube cut, get pods or actually let's have a look at our pods. You can see that our container or alpine here was restarted and that's because of this exit command here. So I just restarted, but it is still running.

[00:05:41] It's basically running that shell command that we have passed here and it's waiting for input. We can also get the logs of all the containers within a pod by running cube cut logs and then passing the name of the pod here. That's gonna be engine. As you can see, this is our current request that we sent and these are the initialization logs of our engine X container.

[00:06:04] Whenever we get cube cut or logs with a pod that has more than one container, then we do need to specify the container. We can do that by passing a dash dash container flag here. And if we just pass the engine X, that is the only container that is running here. So it will print the logs of the container engine X.

[00:06:22] If we have just one container, then the cube catalogs will just match the logs of that container. This can also be very useful when inspecting and analyzing the behavior of the containers running within the pod. Awesome. Let's now delete some of the pods. So let's clear this once again, cube cut cat pods, we can confirm that both pods are here.

[00:06:42] So let's start by deleting the engine X spot with cube cut, delete engine X or actually let's leave the engine X running. Don't delete the engine X here. The cube cut delete is very similar to the cube cut described in terms of the structure of the command, meaning that we do need to provide here a resource type that we wish to delete

[00:07:04] and a name here afterwards. So I recommend would look like cube K delete pod and then alpine like so before we run this, I just want to show you that we can always add the dash dash help flag here and there is comprehensive documentation as you can see here,

[00:07:21] there are many examples that we can use here to understand how to work with the cube color in different scenarios for us. We're just gonna clear the console here and run cube K delete pod alpine like so it might take a while to terminate the pod here. So we might have to wait a couple of seconds, but this eventually should finish.

[00:07:43] You can also pass a forced flag if you don't wish to give some grace period period to terminate the pod. Otherwise it might take quite a few seconds. The pod was deleted here for me. And if we were to now run cube K get pods. We would get just the engine X pod up and running. Great.

[00:08:01] Let's leave our engine X pod up and running. Because in the next lecture, I want to briefly touch upon how we can actually set up a simple service to be able to communicate with the pod here by its name. But before that, let's take a short break and I'll see you in the next lecture.