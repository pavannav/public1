[00:00:00] Hello and welcome back in this lecture. We will get a very basic Mongodb server running through Docker for us to get started. The first thing we need is an image that we can run that we can create containers based upon. So I am once again here in the Docker hub and here in Dockerhub, we can search for Mongodb.

[00:00:22] Let's press enter and let's inspect the results. So we have Mongo, we have Mongo Express and the one we are interested in is this Mongodb community server. This is the server that we will run and that will, it will store our data base. Let's scroll down a little bit more here.

[00:00:41] We will see that there is a getting started page and if you wish to go through this page, I would also recommend that is a great documentation here covering multiple use cases and please feel free to go through this and explore on your own. We will step by step build upon different steps here so that we can get our server not only up and running

[00:01:02] but also in a reliable and sustainable way. Let's then scroll up a little bit. If we click here on tags. We will see that the one that seems to be the newest is actually this 5.0 0.24 but this is not really the case. So it just happens that this was pushed four hours ago.

[00:01:20] So very recent push. However, this refers to an earlier major release of the community server. Let's look for the tag 7.0. That at the time of this recording is the latest version that is available for us. We will see that there are quite a few options here, most of them with patch numbers.

[00:01:41] However, if we were to actually just add a dash to here, then we should also find the 7.0 tag which would include any patch updates here. Since this is pointing to the latest patch release in terms of security, that is good that we introduce new patches as they are published.

[00:02:04] And I believe that with 7.0 this will already give us enough stability so that you can also follow these lectures later on as you're watching this down the road, maybe weeks or months into the future. But since we have a stable release here, then we can rest assured that we will all be on the same page.

[00:02:22] Let's then use the tag 7.02 22 04. This project will require quite a bit of coding. So I would recommend that you put all the files in a new directory. Let's call this the key value app directory. At least that's how I'm calling. You can call this. However you want, just make sure that this is a new directory with no files inside.

[00:02:42] For us to run this container. We could simply issue a Docker run detached here and we can pass the name, let's say Mongodb. And then we would pass our image mongo DB forward slash and I'm just jumped here to the Docker hub so that we can just copy paste this from here, right?

[00:03:00] So Mongodb forward slash Mongodb community server 7.0 UBON to 22 04. I'm just gonna copy this and go back to the terminal and then just drop this here. The moment we do that, we will see that it's unable to find the image. It will download the image for us. This might take a while since it has several 100 megabytes.

[00:03:20] But hopefully the container will be up and running soon. As you can see, I already have a container named Mongodb here probably from a from a previous iteration of this lecture. So let me just remove this Mongodb container. In the moment we do that, we can run this again. And then right now, yes, we get this container up and running

[00:03:40] if we were to run Docker PS here, we will get that. This is our image and I apologize for the formatting here, but it's just in two lines, right? So we can see the name here is MongoDB. And now we can confirm that this is up and running by first of all, looking at the logs of our MongoDB and confirming that we have no error logs here. These are quite a few logs

[00:04:01] they are presented in JSON format. And for most part here, we are not really interested in understanding these logs just to show that the container and the Mongodb server is up and running. We can also confirm once again by running a Docker ps here and confirm that the state of this email,

[00:04:20] the status of this container is up for 56 seconds. If there was any initialization error, the container would exit with a non zero code and we would not get the status here of up 56 seconds. We can also exact into the container by running Docker, exact it once again the name Mongodb

[00:04:41] and then we can run a Mongo shell. This is going to open a RLE so a shell here that we can execute commands and interact with our server. Once we run this command, you will see that we get some messages here informing us about Mongodb as well as the address we are connecting to. And here at the bottom, we can, for example, run the command, show the

[00:05:02] B and this would show all the databases that are created for us. We can also switch here to the admin database where we run the command, use admin. So the admin here is the database we want to use and we simply run the command, use admin to switch to the admin database.

[00:05:20] Once we do that, we get the confirmation here. And now if we were to show our collections, then this would show all the collections that are stored by default under the admin database. This is our first step and there is nothing really new here. So you're already used to running containers based on a certain image

[00:05:40] then exacting into the containers to run some commands. So hopefully, this is all fairly standard by now. But I do believe it makes sense. We start step by step here and cover all the necessary steps in the process of getting our Mongodb server up and running before we exit this shell here. I want to highlight a few drawbacks of this solution

[00:06:01] to begin with. We have not specified any authentication whatsoever. Anyone can run this exact command. There is no password or user that needs to be provided and anyone who is able to get a hold of our container would actually be able to start running commands such as showing which databases are available

[00:06:21] using the admin database, showing the collections in the admin database. And this is by far something that we do not want to allow. We could argue that yeah, this is a development server and it's running locally on our machines here and there is nothing really sensitive. We shouldn't really worry about it. But I do wish to use this opportunity to explore a little bit

[00:06:42] more about how we can set up some basic security around our Mongodb server and how we can leverage some of Docker's features to put that in place. So before we take a short break, let's exit here this command-line and we can confirm that our container is still running.

[00:07:00] So let's stop and remove this container. We're gonna say doc RM dash F MongoDB. This will both kill the container and remove it so that it's not available anymore. Once we do that, we should see that Docker PS dash A does not return any containers and that's the state we want to be in. Perfect. Let's then take a short break and come back in the next lecture.