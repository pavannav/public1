[00:00:00] Hello and welcome back. One comment that we have not discussed so far with replica sets is the scale comment. So now we have a cube cut deployment here. So we have a community deployment, not a cube cut deployment. So let's just get our deployment. We have the engine X deployment here. We can. Now we see here that we have five replicas that are ready that and are available.

[00:00:23] Now, what happens if we want to actually increase or reduce this number? One way we could do that temporarily is to use the scale command and we're gonna scale here our deployment and to next deployment and then the number of replicas will pass as an argument here as an option to our command.

[00:00:42] Let's say that we now want to scale this down or scale this to three replicas once we do that and we run cube cle get deployed here again, we can confirm that our deployment now has three desired pods. Now, there was a reason why I mentioned temporarily

[00:01:00] and it is basically because this command here. Once again, it does not, it is not reflected in the configuration file that we have created So if we were to now run cube cut D and that's past the file here, we will see that the next time we apply the changes in this file, the replicas will actually come back to five

[00:01:20] replicas here and the generation will be increased. So for to just run cube cut apply, we will see that. Now once we get our deployment here, so get deployed, it is back to five replicas. We have scaled in here. So on the top, we have reduced the number of replicas.

[00:01:40] But let's say that we have increased this to 10 replicas. And then suddenly we start relying on the fact that we have 10 instead of five replicas. And suddenly the configuration file gets applied again and we are back to those initial five replicas. This is another example of an imperative command

[00:02:00] that is modifying the state of our cluster. And I highly discourage you rely on this scale command for long term operations. There is one special case where your pods might need to be restarted. If you need to just restart the pods, you can quickly scale the deployment to zero replicas.

[00:02:20] You can run this here and then you can quickly scale it back to the desired number of replicas here and your pods will be restarted. This might happen in rare cases when the pods go into an unhealthy state, even though there is no configuration error and you just need to quickly restart quickly recreate them. And this is pretty much the only use case that I would tell you, hey,

[00:02:42] the cube cut scale command, the imperative one is actually the better choice otherwise. Or in pretty much all the other cases, I would say, hey, go back to the configuration file, change the number of replicas and then apply it following the process of using cube Carol. Apply that being said,

[00:03:00] I definitely think it is worth it for you to know about this command and how to use it just to wrap up with this scaling method here. As you can see when we look at the rollout history of our deployments. There are no changes here. That's great. This is one of the missing comments I wanted to discuss. Let's take a short break and come back in the next lecture.