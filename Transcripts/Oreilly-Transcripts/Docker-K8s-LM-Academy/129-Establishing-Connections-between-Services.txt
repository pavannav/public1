[00:00:00] Hello and welcome back. Let's now implement a new network where we will connect our reverse proxy with both existing services, notebooks and nodes. We can create a new network at the top level here by just informing networks. And let's just define this notes app connect the engine

[00:00:20] X container here to the nodes app network. So networks colon and it will just say nodes app like. So the question is how do we actually connect the existing services here? So if we were to open a compose YAML in this como Y there is really no nodes app here defined in our networks,

[00:00:43] how can we actually pass these values back to this compose so that we connect this node service to both the nodes net and the nodes app net. Let's change the nodes app here a little bit to be nodes app net like so so that we know this is the network

[00:01:00] just for the sake of consistency. And I understand it's a little bit confusing with the nodes back end and the nodes app here the nodes app network. But please bear with me. The naming could be a little bit better but I hope it's clear the way for us to actually pass an additional piece of configuration from the beginning or from this root composed demo

[00:01:22] is to actually create something called an override file. So we say como dot Override dot YAML and the name is important because compose looks for this file here to also override some of the information from the individual included, compose files in this compose dot override dot YAML,

[00:01:43] we can actually define several pieces of information here. So services we can then reference both our notebooks service as well as our nodes service. And then here we can define several pieces of information that we either wish to merge with the original compose files

[00:02:03] or that we wish to override completely. If the properties are arrays, then these arrays by default will get merged with the original arrays. So here we just need to define the new network. We also wish to connect to let's copy this, let's paste it here

[00:02:20] and the same thing for our node service. So we also wish to connect the nodes appnet and that's pretty much everything we need to do. So now we will have both our notebooks service connected to the nodes appnet and reachable by name of service from this network as well as the node service connected to the

[00:02:41] same network and reachable by name from the network. This does not make the nodes database in the notebooks database available from this new network pay attention that this network is only adding or we are only connecting the notebook service and the notes service. If we look at the compose

[00:03:00] demo, the nodes database is included to the nodes net, that means that the nodes service can reach out to the nodes database. However, the reverse proxy cannot directly connect to the nodes database by using its name. This is intended and it provides a layer of isolation between our reverse proxy and the database itself.

[00:03:22] That's great. So we have created this como over right. And we can then add new values here to be added to the networks of our containers. We can also define more environment variables if we wish to do so or we can also map other ports if we wish to completely replace the networks array here, not just merge them,

[00:03:44] but just completely replace the one that's coming from notebooks and then use only this one. Then we should use the exclamation mark override flag. This is normally used for when we are remapping ports and we don't wish to keep the original parts because they would,

[00:04:00] for example, conflict. Let's say that we had here in this 1 3000 and in this one here in the notebooks also 3000. And we were to include both in this composed Yamo project here in the root project and then try to start this up. They would conflict and we could then, for example, completely override the port mapping by using this tag here.

[00:04:23] We're not gonna do this here. So let's just remove it. But it is a utility that is there. In case you need to use it, all the available tags are documented in the merge page here. So under the compose file of our documentation, we can then find them documented in the merge option for. Now, let's keep it simple.

[00:04:43] And let's see if this configuration is enough for our reverse proxy to start up successfully in our terminal. Let's run Docker compose up and let's rebuild all our images just for the sake of completeness. And let's also start watch mode. We will get a bunch of logs and let's then split our terminal and inspect what was created

[00:05:03] in the same folder where we have created our compose project. We can then run Docker compose PS and we can now confirm that our reverse proxy container is successfully up and running if we were to try to send a call request to localhost port 8080.

[00:05:21] And remember here, we have defined the forward slash API forward slash notebooks path to be one of the paths redirected by our proxy. We will get a message back here, but this is actually coming from our express application. This is simply because we have not defined a forward slash

[00:05:40] API forward slash notebooks route in our application. Our application is just forward slash notebooks. So let's go back to the ide and change that in both the notebooks and the notes application. Let's change the route here to forward slash API forward slash notebooks like so and let's then do the same here for our notes back end source.

[00:06:03] And that's gonna be forward slash API forward slash note, notes, just notes not notebooks. Let's then save this here. Come back to the terminal. We can confirm that both the services were restarted by Nodemon here. The changes were identified and the services were restarted.

[00:06:21] And now when we try to cURL this, we actually get our response back. Isn't this amazing? We were able to bring two individual compose projects together in a bigger third compose project, which is then leveraging the services that were already created for us. This will also work with the notes end point and there are no surprises here.

[00:06:44] This localhost 88 is just a proxy that is really redirecting the request to upstream services dependent on the path. This is a very common pattern when we are in a Microservices world where we have different services available. And then we have a single entry point here that is gonna know

[00:07:02] how to redirect the requests based on multiple URL paths or configuration options. That's amazing. So we were able to bring everything together and make our reverse proxy work as expected. Let's close this terminal here. Let's stop our running project.

[00:07:20] We can also take this compose project down if you wish to clean up all the resources or you can leave it existing in case you will continue with the course right away. I'm just gonna take it down and delete all the volumes here. So you can see that there are many different components that are now managed with a single project and with very intuitive commands.

[00:07:42] Awesome. This is a great moment for us to take a short break and come back in the next lecture.