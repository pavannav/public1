[00:00:00] Hello and welcome to this project. I will containerize a React application with Docker. The focus of this project is on again, working with Docker but primarily leveraging multi stage builds so that we can both build and serve our React application. Let's see what we will achieve with this project. We're going to spin up a simple React app using the Create React

[00:00:23] App utility. We will then write a multistage Docker file to build the React application. We're going to extend this Docker file to copy the static files generated by the build process and serve them through an engine X server. We will use this discussion to get more familiar

[00:00:41] with different use cases for using multistage builds. In addition to what we already discussed throughout the course, let's then take a short break before we get started and I'll see you in the next lecture.