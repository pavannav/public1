[00:00:00] Great. Let's briefly discuss annotations and how we can leverage them in our CORS resources. Annotations are a great way for us to add useful information and configuration to resources. So on the right here, we have one example of how we can add annotations to configure an

[00:00:20] ingress that is backed by an engine X ingress controller. Now we haven't discussed ingresses, we haven't discussed NGEN X ingress controllers. So I don't expect you to know exactly what these annotations are actually doing. But the bottom line here is that they are being used to configure a certain ingressing the behavior of our resource.

[00:00:43] So, annotations are key value pairs attached to Kubernetes objects, just like labels and to think key value pairs here. But unlike labels, annotations are not supposed to store identifying metadata and they are often used by tools or by the Kubernetes system itself to configure certain services or to perform other activities.

[00:01:04] There are some common use cases around it. So first of all tools specific metadata and configuration, that's what we have here on the right. So external tools such as monitoring systems or agents for logging systems may leverage annotations to attach custom data to resources.

[00:01:21] Let's say for example, that we, whenever we are logging something, we wish to add some additional data here to the resources we might also edit in the annotations section. Annotations can also be used to configure in grass controllers. That's exactly what we are doing here the right. And you can see here that we are configuring ssl redirection

[00:01:42] and we are forcing that http traffic is redirected to https traffic. So for example, we can define path three rights custom load balancing and other things. Now this depends on how the ingress controllers of the application here is actually being developed and how we can,

[00:02:01] how it expects us to use annotations. So this is very application or system specific. Another use case is that we might want to store build and version information. So notations can be used to store metadata such as the time stamps of the builds, the version numbers if we wish to store the commit hash from git,

[00:02:22] so that we can quickly retrieve which exact commit led to this resource in a certain current state. And we can also use this for runtime configuration for operators. So some certain coins, operators or controllers, they can use annotations to customize their runtime behavior for specific resources.

[00:02:43] The structure of annotations is as follows. So we can have a prefix which must be a DNS valid DNS subdomain here. So you can see this really looks like a DNS subdomain and this is expected this is necessary for automated system components,

[00:03:00] either from the community side or as from from other third party providers which are adding annotations to the objects that are created by us. So if automated systems are adding annotations behind the scenes, then they are expected to provide such a prefix. If the prefix is omitted, then the key here is actually presumed to be from a private user namely from us,

[00:03:25] right? So we can add ourselves annotations however, we want. But if automated systems are adding annotations, then they are expected to have this prefix in addition to the prefix, then annotations should also have a name that needs to be less than 63 characters and contain only these characters here.

[00:03:44] All right. So that's it. For annotations, you probably come across annotations much more often as you start working with other automated systems. And just for you to keep in mind that if you see some documentation around hays set annotations to this and that value to actually configure how the certain object here is working,

[00:04:03] then this is also common practice in Cnis. Great. Let's then take a short break and come back in the next lecture.