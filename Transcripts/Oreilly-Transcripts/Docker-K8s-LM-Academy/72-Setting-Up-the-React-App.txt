[00:00:00] Hello and welcome back. Let's start setting up our React application for this project. Once again, we're not gonna do anything fancy with React. The goal here is to see how we can use Docker to both build as well as serve a React application. So let's start with a very simple NPX Create React app.

[00:00:21] And this is gonna spin up a React app for us with a lot of dependencies that are necessary for the application to work properly. We can also use a template here to leverage typescript if you wish to do so, I'm just gonna leave it here just to show you that it's possible, but you can also leave it out. We then need to pass the target directory where we want to create our React app.

[00:00:44] I'm just gonna call this containerize React App. If your current folder is empty, you can also leave this part here. You can also leave the target directory empty. It's not necessary. But for me, since I wish to create within this directory, I'm just gonna pass the option here.

[00:01:01] Let's press enter and it's gonna take a while until it installs all the dependencies. And at least for me, this took about three minutes. So it's actually a fairly long process. There are quite a few dependencies to install. If we scroll up a little bit here, you will see that we have added 1483 dependencies and add

[00:01:21] afterwards. Also 42 packages. And then down here we have few other updates on the packages. So in total, we have roughly 1500 packages, quite a lot of dependencies, but it's all taken care of behind the scenes. For us. We can then change into the container. I React app, we can clear the console and we can try to run NPM start.

[00:01:45] Once we do that, we should get a new window popping up on our default browser. In my case, that's Chrome. And this will just have the very basic code that is shipped by default with Create React App. We can very quickly make a change here by coming to the ide

[00:02:02] opening the folder that we have created containerized React app open in the source directory and then the app TSX file. And we can, for example, scroll down a little bit. We can see that this is one of the parts of the content that is shown. So we can just change here

[00:02:20] and say I have been edited just very simple change. Let's now save this. And we will see that this has some hot reloading features. So if we come back to the browser here. We will see that we get the changes immediately reflected on the browser. One thing I would like to discuss with you before we wrap

[00:02:40] up this lecture is that when we run this NPM start command, what is actually happening behind the scenes is that the application or at least the project that was created by the Create React app utility is spinning up a development server. Let's jump to the ide and let's understand how that's happening

[00:03:00] in the ide. Remember we ran the command NPM. Start the file that defines what happens when we run NPM. Start is the package dot JSON file. So if we scroll down a little bit here, we will see that we have the start command. This start command is in turn calling something called React scripts, start.

[00:03:20] And then when we look at the dependencies here, we will see that there is a React script installed for us. So the dependencies are always installed inside of the node modules and they do follow the convention that if they are specified like so in our package dot Chase on file, then we will have a folder called React script.

[00:03:40] So if we scroll down a little bit until we find the folder here, we can see we find it here, React scripts. Let's then click here. Expand. And inside of this folder, we find another folder called scripts and then we find our start script. So as you can see here. This is actually matching quite a few things that we have here.

[00:04:00] So the start script is located here. The build script is located here. The test script is located here and the eject one is located here. So let's open the start one and the start one. So it has a couple of 100 lines, a little bit, a little bit less than 200. But somewhere here in between, we actually find something called a new Webpack

[00:04:24] DEV server. This Webpack DEV server is actually spinning up an HTTP server behind the scenes that will take care of handling the requests and correctly downloading or correctly serving the necessary javascript files for our index dot HTML.

[00:04:42] This is virtually invisible when you run the NPM start command. But it's important for us to understand what is happening behind the scenes. So what's gonna happen here is every time we make a code change, what the NPM start command or what the application will do is it will rebuild our code,

[00:05:00] it will rebuild a development version of our code. And then this new version will be shipped again by our Webpack DEV server. This live rebuilding and reserving of the application is called hot reloading and this is available whenever we are running the NPM start command. Now, what we actually want to achieve here is not to provide

[00:05:21] something that is a development build nor that has live reloading. What we want to achieve in this project is that we can build our React project and then serve the HTML and the javascript and the CSS file. So all the bundle of files that is output by our process. We want to serve that from within a context.

[00:05:40] So Webpack DEV server is not the best choice here if we want to have a production version of this application being served by a production level server. So we're gonna tackle this in the upcoming lecture. So we'll first understand how we build our application and how we can serve it locally. And then we will see how we can containerize it and serve it with

[00:06:03] Nginx. Let's take a short break and come back in the next lecture.