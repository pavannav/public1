[00:00:00] Hello and welcome back. Let's start writing our Docker file here. And the Docker file that we will write is you might already expect a multi stage Docker file. As you have seen in the previous lectures. We have actually two steps that we need to implement here. In the first step, we need to build our production files, right? So we need to build our production bundle. Let's call it like this.

[00:00:22] And in the second step, we actually need to serve this production bundle. So we need to serve the bundle with an HTTP server. Before we start writing and testing anything here. Let's just create our dot dockerignore file. And there are two folders I would like to ignore here.

[00:00:40] The first folder is the node modules. As it is tradition, we do not need to copy the node modules because the dependencies will be installed as part of the Docker file. We can also ignore the build folder because this build folder is exactly what we want to build with the Docker file. So copying the local version here doesn't really make sense.

[00:01:02] Plus relying on the build folder being copied from this local directory here. Is very, very brittle. It can very easily lead to outdated versions and we don't want to do that. So we always want to build as part of the Docker file. Let's then start calling here and the from or the first image that we need,

[00:01:20] we need to have something which has npm installed. So the node 22 dash alpine will do the job for us, right? So this is just a smaller image. And here we are not going to use alpine at the end of our Docker file or in the second stage here in the second stage, you will actually use engine X. But since the alpine is a more lightweight image,

[00:01:41] it downloads faster than it's actually recommended here for our case because the builds will just be faster. Let's then set our work directory and the work directory is going to be just forward slash app. We could also see something else here, but just following convention. And just before I go a little bit further here,

[00:02:00] as you can see the Docker file and the Docker ignores are actually created inside of the source folder. So let me just move them outside. I'm just moving that outside here and a Docker file outside. If you have already created them outside of the source folder, then nothing to do here, just make sure that they are at the root of your folder.

[00:02:20] So now we would like to copy the package Json and the package lock dot JSON files into the current directory. We would then like to run the NPM command so that we build all the dependencies that are necessary to both ship as well as to build our application.

[00:02:40] If you have a look at the package dot Jason file, the package do Jason file that is generated by create React app. It does not include a DEV dependent. If you were to look here for the key dev dependencies, we're not gonna find it here. It just provides dependencies and there are lots of dependencies here that are actually development time dependencies.

[00:03:03] But once again, we will trust that this is handled and the dependencies are handled behind the scenes by our build command. We're not gonna touch this here even though I do not really like this format. It is the format that is shipped by default and I don't really want to touch it here so that we don't diverge

[00:03:20] too much from Docker. Let's just leave it as it is. And let's see how we would build our application. Awesome. So back in the Docker file, we will just install all the dependencies here. And once the dependencies are installed, we can actually copy the source folder into the source folder here and we can also copy the public folder into the public folder.

[00:03:41] We could also go ahead here and just copy everything into the folder because we are safely ignoring both the node modules and the build folders. So this will just copy these two folders here as well as also the dot Get ignore the read me and the TS config and actually we need to copy the TS config as well, right?

[00:04:00] So we would have here the source folder, we would have the TS config as well. So maybe we just go ahead and copy everything because we are ignoring the the folders here that we do not really want to copy. Once again, it's always a balance between having more specific commands here and having a little bit easier to maintain Docker file.

[00:04:20] And so for us here, since we know that there are not a lot of things that we also need to ignore or that we should worry about because we are already copying the package and package lock. And the only two things here that we would copy that we don't need is the dot gitignore and the read me markdown file that is actually fine for me. So it's no problem here.

[00:04:41] We can just copy everything. And now in the next step, we will say here, run NPM, run build. So this is gonna build our project and it's gonna output the files inside of the build folder. We can actually test, take this Docker file here for a spin so that we can at least create this.

[00:05:00] We're gonna just build our application, we're gonna, we are not gonna serve it, but let's then start a shell in this or actually start a container, create a container from this image and directly run the shell command. And let's explore the directory structure back in the terminal. Make sure that you are inside of the directory of your application.

[00:05:21] Let's now run the command here. Docker build. Let's tag this as react app colon Alpine. So that we know that this is the image version that contains the alpine base image. It's not the multistage one. And then here we will simply pass the current directory as the context.

[00:05:40] The NPM command might take a while to execute. Remember, we have more than 1000 500 dependencies but it was actually fairly quick. So 10 seconds is not bad. Perfect. So we have now our container or actually our image, which is called React app and we can run a container here.

[00:06:00] Let's add a dash dash RM it here and the image is React app called on alpine. And the command we wish to run is the shell command. The moment we do that, we immediately fall inside of our container with the forward slash app folder. And if we were to just list the directories here,

[00:06:20] we should get our build directory. So you can see that both the build and the node modules they are created by the build command or by the Docker build process here. And we have exactly the same structure from what we get in our local machine. Let's now see if we have the tree command here. So tree built

[00:06:41] and we actually have it. So that's good. So it's just gonna print here the recursive structure or the structure of the build folder. As you can see, this is once again, very similar to what we have in our machine. Here is the static folder, the Js folder containing the generated Js files as well as the CSS folder.

[00:07:00] So we are successfully building this inside of the first stage of our Docker file. Perfect. We have confirmed that we are successfully executing the bill. We can exit from this container here and this will also remove the container. So if we run a Docker PS A, we should not see the container here. If you have other containers running,

[00:07:21] then this will actually not just running but other containers either running or stopped. You might see entries here, but you should not see a new entry. You should not see a container from the react app alpine image.