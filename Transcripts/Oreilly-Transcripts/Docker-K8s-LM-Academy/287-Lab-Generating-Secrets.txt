[00:00:00] Hello and welcome back. Let's now discuss secret generators and the behavior here is really similar, very similar to what we have for Configmap. The difference is that this will actually output secrets instead of config maps. So let's now move our local configuration from our config maps to actually our secrets.

[00:00:21] And that's because maybe we want to also add here some DB underscore username and that's gonna be our local DB and db underscore password and that's gonna be our local password, right? So um dot ENP files, they are normally not committed to repositories. So we can also use them to set our

[00:00:41] local credentials here for our local application development. So let's just suppose that we have something like this. And then here we will generate our secret which will be local config from this environment variable. We can also add a type here of opaque to indicate to Cnis that this is a generic secret.

[00:01:00] So let's save this here. Let's now come back to the terminal and let's rerun here our overlay. Let's scroll up a little bit until we find our secret. And perfect. This is the secret here. So as you can see we have generated both a database, password and database username here as well as the host.

[00:01:21] This information here is base64 encoded as you already know. And then you can see here in the name of the secret that this suffix is also disabled, right? So if we go up here, we have disabled name suffix hash for the entire generators that includes both the config map generators and the secret generators.

[00:01:41] That's all good here. But then it comes the question, how do we actually use these config maps inside of our deployments before we explore how we can use patches to update deployments from a base here. Let's just create a very simple deployment. I'm just copying and pasting the NGX deployment here.

[00:02:01] And I'm just going to put here for the Namex inside DEV, like so very simple. The app here are gonna copy, paste the name as well and put this in the app label. We're going to also match this here, the container we are going to leave it like so and then here we're gonna have a volume amount, right? So here for

[00:02:22] volume amounts of our container, we're going to specify a couple of volume mounts. We will provide this in a little bit. I just want to specify the volumes here and the volume that we will have. Let's say that we want to mount here our DB in it config right? So let's just remove this some other key here

[00:02:40] and let's mount this here. So let's copy this, let's paste here in our volumes. The entry will be db config like so and then the config map here will be represented with the name DB init config the volume mount. We're just going to pass here the name of our volume that is db config

[00:03:01] and then the mount path, which will be here, let's just say forward slash db forward slash config, right. So this is just for demonstration purposes. Now, the question here is if the name of our config map is stable. So if we have actually disabled here, the config map generator,

[00:03:21] the disabled name suffix hash will customize be smart enough to identify that it should actually append and prepend here. The name of our config map with the name prefix and suffix. Well, we can figure that out by first of all, including the NGX deployment in our list of resources. So

[00:03:41] Nginx deployment dot YAML, let's then save this here. Let's now come back to the terminal. Let's then run our overlays DEV here and Bingo as you can see here. Perfect. We can already see that from this engine X inside DEV here, we can confirm that the name of the config map was actually updated here.

[00:04:03] It was updated by customized to include both the name prefix and the name suffix that are defined in our customization. So it's important for us to include this deployment under our resources. So that customize, knows that. OK. If there is any config map that has this name here,

[00:04:23] I do need to follow the naming convention of this project. I need to add the prefix, the suffix. And if there is any name suffix hash here, I should also add that. Let's remove this option. Let's save this. And let's now confirm that this is also updated here

[00:04:40] to include our new config map. And as you can see, that's exactly what happens, right? So we have this FBF 87 here. And if we scroll up until we find our config map, let's just try to find it here. Should be perfect. It's here and we can confirm that this also has the DB init dot Js file. So this is the correct configmap.

[00:05:02] This is also valid with the secret. So we can also reference a secret by its name here in our customization dot Yamo file. And then customize will take care of replacing these secrets in our deployments or whatever files or whatever objects reference the secret by the name,

[00:05:20] right? It's important that the name here of the config map or that the name of the secret at the volume definition. Here it matches what is specified in our customization file. So this is what needs to match and then customize will take care as long as the Coors objects that reference this config maps and secrets,

[00:05:40] they are listed under our resources. So let's remove this from now here for now, let's just delete this as well. I wanted to show you this behavior because this can also be very helpful when managing your applications. You now don't need to create secrets manually or create Manifest files for secrets.

[00:06:00] You can generate them based on local dot env or on local files that are not committed to your repository and then you can reference them and customize will take care of correctly linking, degenerated secrets with the objects that need to use them. That's it for this brief discussion around configmap generators and

[00:06:20] secret generators as well as how we can use them. So let's now take a short break and I'll see you in the next lecture.