[00:00:00] We have touched upon Docker files very briefly in the previous lecture. So it does definitely make sense that we spend a lot of time discussing Docker files. And we will, throughout the course, we will explore different instructions as we move through the course. For now, I just want to give you an overview of its structure. So the Docker file allows us to programmatically define

[00:00:22] the steps for the creation of Docker images. If you remember from previous lectures, we have specified the from instruction which needs to be at the top of every Docker file. And it specifies which base image is to be used after the from instruction or after the from line, we get a bunch of instructions.

[00:00:43] So we have instructions, instructions, instructions and they are executed in order, right? So they will be executed from top to bottom like this. Even though I'm drawing from bottom to top, they're executed actually from top to bottom. This is just how the arrow is shown on the screen, right. So each instruction may have a variable number of arguments,

[00:01:03] they might accept one argument or two arguments or even a variable number of arguments. Maybe they can work with five or they can work with 10 arguments. It depends on the instruction, right. So each instruction has its own syntax and it's not gonna be always the same number of arguments per instruction.

[00:01:22] We're gonna come back to what's at the bottom here in a little bit. I just wanna go through the main benefits of using Docker file. They bring many advantages, right? So the first thing is that they can, we can write custom images that perfectly match our requirements that by far one of the biggest advantages,

[00:01:40] but they also give us reproducibility. That means that since we define all the steps in a Docker file, we can ensure that anyone that has that Docker file can recreate the exact same image as you can imagine. This is huge, right? Because it really solves the whole problem of, hey, it runs on my machine,

[00:02:01] it works on my machine. Why doesn't it work in all in, in yours? And that's because the dependencies, the overall environment of the machines are different. But with Docker files, we get the exact steps that are needed, everything that is included that is needed to run the software that we are developing. So anyone with that Docker file can recreate the exact same image.

[00:02:24] We also get automation, right? If there are 30 instructions maybe here, then they will be executed one after the other in and out at fashion. That means we do not need to manually execute the third instructions. This is also a huge plus because it drastically reduces the risk of human error,

[00:02:42] not only from executing, forgetting, maybe let's say that you forget the instruction or that you execute with the incorrect parameters or that you execute them in a different order as there are many things that we can do and that can go wrong whenever we are doing things manually. The automation that we get with Docker files is also a big plus,

[00:03:02] we get transparency and documentation as well. Docker files should act as good documentation for how your image is built. If the Docker file is very confusing and it has like steps or instructions in a mixed order and it's not clear what it's actually doing. Then I would say that OK,

[00:03:20] you should actually revisit the Docker file and make it clear because whoever opens the Docker file should understand. What is the base image? What are the dependences being installed? What are the configuration commands? Maybe we are sending a different user or different working directory. This should be clear for whoever is reading the Docker file and last

[00:03:40] but not least we also get the chance to implement some nice optimizations. Docker files, give us full control over the build steps. So we can tackle into optimizations to improve our security. Maybe instead of using that node based image, we're going to use the alpine version image of node which will improve security. It will also improve the image size.

[00:04:02] We can also speed things up by ordering the instructions in a certain way to leverage Docker's caching mechanism and speed up the build time. Once again, this is fairly theoretical for now. Don't worry about it. We do have lectures where we will really tackle into the optimization.

[00:04:21] Practically, we're gonna see different examples and we're gonna see how we can improve the Docker files to improve both security build time as well as image size and last but not least. No, I did not forget the little diagram here at the bottom time for us to understand what this is doing. And this is actually very important

[00:04:41] in Docker. Every instruction creates a new image. This this is something which has to be very clear in your mind. So as you can see here what I try to illustrate this is the image ID. Let's suppose this is the image ID right now this image here is the base image that is coming from our from instruction,

[00:05:03] this base image plus the instruction one this instruction that we have here. It's going to create another image, an intermediary image which is the C 52 E blah blah blah image. This is an image by itself, it can be used by itself.

[00:05:20] Now this image here it comes at the bottom and then combine with instruction to let's say this instruction here. It's going to create yet another image, the F 99 a blah blah blah image. So each and every instruction in our Docker file, each and every instruction creates an intermediary image.

[00:05:42] It creates an image as a result of this. That means and this is perhaps very important for us to understand that means that if the instructions are stable, if the results of the instructions do not change, the images also do not change, they don't need to be recreated.

[00:06:01] Let's say that only instruction three changes between two runs of the Docker file. Let's say that we're copying some files here in instruction three and then one of the files changed. The base image stays the same. The first instruction stays the same. The second instruction stays the same.

[00:06:20] That means that the image id of the from instruction will stay the same, the image id of instruction one will stay the same. And so will the image id of instruction too. And Docker doesn't need to recreate these images. Perhaps this is the first time you're coming into contact with this layered architecture of Docker.

[00:06:41] So don't worry about it once again. Overview here, we do work with it also throughout the course, we're gonna understand, we're gonna visualize the intermediary images. We're gonna try to leverage them to start containers and we'll also understand how to use this structure to optimize our Docker files. But I think that's enough for this discussion.

[00:07:02] I think we touched upon many different topics. Very important topics this is a very important lecture. Even though we didn't touch code, it's very important for you to understand all the benefits of Docker files as well as their structure and the foundations, the basics of the Docker layered architecture.

[00:07:20] Let's then take a short break and come back in the next lecture.