[00:00:00] Great. Whenever we talk about persistent storage and needing some state in our applications, then stateful sets always come into the discussion in Cnis because they do provide some guarantees, they do provide some features around stability and pod identity that can be useful whenever we need pods to have state

[00:00:23] or whenever we need pods to have a stable identity and access to a stable storage. Let's discuss stateful sets. Then in a little bit more details, they help us manage stateful applications more efficiently and stateful sets maintain stable identities for each pod

[00:00:40] ensuring that even if the pod is restarted, it will retain its unique identity and the connection to persistent story. So more specifically what stateful sets provide is a stable unique network identity for each pod created as part of the stateful set. Also a consistent and stable persistent storage here associated with eight pods.

[00:01:02] Each pod also across rart that means if your pod is restarted, it will give it will have access to the same persistent storage that it had before. It also guarantees order pod creation, scaling and deletion. In case this is necessary whenever we are working.

[00:01:21] For example, with databases or replicated services. We can specify a persistent volume claim template in the pod definition file. And as long as this stays the same. So as long as the volume claim claim template stays the same, then the pod will use the same claim and have and have access to the same data

[00:01:42] once again, also across restarts. So each replica in a stateful set will have its own persistent volume claim. So data is not shared across different replicas. This is important to highlight if you need data to be shared across different replicas, then this is not the way to go one of the expectations. So let's say

[00:02:02] stateful sets are designed so that each replica, each pod has its own persistent volume claim and it does not share data with other replicas or pods from this stateful set. Another thing to keep in mind is that the persistent volumes, they are not deleted automatically when a replica, when a pod is deleted.

[00:02:23] So you would have to go and manually delete them here. The pod names and their network identities are also stable and they will follow the following pattern. So that will be the stateful set name here, then a dash and some ordinal index that is increasing. So we would have let's say

[00:02:42] pod one or let's say my stateful set zero, my stateful set one and so on and so forth. So we can also use what's called a headless service to expose the pods via more stable domains. We also discuss this in the practical exercises, how we can leverage headless services, what they are and how we can use them to expose pods in

[00:03:02] a stateful set via stable domains or DNS host names. So just a small quick diagram here to to summarize what we did. So we have a stateful set here, then this stateful set will start the creation of pods in an ordered manner here. So that's this here.

[00:03:20] And the names of the pods will follow the name of the stateful set, some ordinal index here which will be increasing. Now, all of this is coupled bound together, that means this pod here will have then its own persistent volume claim which will be attached to its own persistent volume. If this is statically provisioned, then it should be available and it should,

[00:03:44] there should be a match here, right? So the the persistent volume claim, the conditions here, the request that we provide, it would ideally find a match in the persistent volume. Otherwise this can never be fulfilled. If this is dynamically provisioned, then it's easier because based on the persistent volume claim,

[00:04:01] the respective persistent volume will be created. So if this pod gets restarted, then it will get attached to the same persistent volume claim and the same persistent volume. Once the first pod is up, then the state will have to create the second one and the third one and

[00:04:20] each one of them will have their own persistent volume claim and persistent volume. Now, the deletion order is going to be different or if we scale in, for example, it will be different because it will start the deletion by the latest one here or by, let's say this will be the youngest part, the one that was created latest, right?

[00:04:41] So this would be the one, the first one to be deleted. Once this one is removed, then the second one would be deleted and then the third one would be deleted. So this ordering here might be useful in some applications. But I do find that the main benefits here are the stability around network identity and name identity

[00:05:01] as well as this consistent and stable persistent storage, which is associated with each pod. Let's then take a short break before we start practicing. And also in the next lecture.