[00:00:00] Before we start deploying our color API application to our GK cluster, I just want to spend a few minutes to show you here the difference when we have a look at cube cut API resources. So before we do that here, let's just refresh our memories with cube catal config use context minicube.

[00:00:20] And let's just refresh here. What we already know that is available in our minicube cluster. So as you can see here, there are quite a few API resources. I'm using a slightly smaller font. I hope that's still good to read, but hopefully that should be fine. Um And as you can see here, there are quite a few resources including several named groups here

[00:00:42] that end with dot K eight dot IO, right. So we have also quite a few things here and all these things here, they represent what our cluster can do, which kind of objects, which kind of resources our cluster can create. Now, when we talk about a managed solution, as you might have already noticed,

[00:01:00] there are many integrations with cloud native products. And for that, let's just have a look here at our demo cluster. So let's just switch the context here once again to our demo cluster. So config current context like so we can confirm that we are inside of our cluster and all run the same command here C

[00:01:22] API resources. And the moment we do that we actually start seeing a considerably different and more extensive list. So we still see these resources from the core API group here. We also see a couple ending with K eight dot IO as well as the apps forward slash V

[00:01:40] one, for example, for deployment replica sets, stateful sets. And if we scroll down a little bit more, we'll start seeing a few things regarding Gke dot IO. So this year it actually starts extending what our cluster can do, which kind of resources our cluster can create or we can create in our cluster.

[00:02:01] So just wanted to show you this because this might also be helpful when understanding what is available for you as well as when following with some of the lectures where we will use some of these API groups and versions here. You can also Google this. You can also look for this information here

[00:02:21] about what actually these different resources refer to. And this can also help you understand which kind of functionality is available and how it integrates from certis to the Google cloud native capabilities. That's the only thing I wanted to show you. Let's just clear our screen. Let's now take a short break and let's come back in the next lecture.