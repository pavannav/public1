[00:00:00] Hello and welcome back before we get started. Let's just do a little bit of cleanup here. I think I ended the last lecture a little bit abruptly. Let's just have a look at all the resources that we have in the cluster and see if everything is good for us. So I just clear the console here. I get all the pods that are running. That's looking good to me.

[00:00:20] We want to leave these pods running. I get all the services now. So get SVC here and we have a cluster IP service which is also looking good to me. We can confirm that our deployments are up and running, oops, not deploy but deploy like this. And we can also confirm that our replica sets are up and running.

[00:00:40] So we're all good to go here. Let's now focus on the nodeport service type for that. Let's come back to the IDE, let's create a new file here. And actually, instead of creating a file, let's just copy, paste the cluster IP one and let's change the name from clusterip to nodeport like. So,

[00:01:01] so service definition changes slightly here, the name we're going to change to Nodeport and the labels will stay the same. The type here is going to change to nodeport. The selector will stay the same because we are still interested on the same pods. And there is one very small but very important change here that we need to do.

[00:01:23] We need to inform which port of our node we wish to expose this service on. So now the nodeport service is intended to exposing a specific port or a port range within our nodes. So that applications or requests from the

[00:01:41] outside world outside of our node can reach out and communicate to the underlying pods which are being managed by the service. The big difference here is that the cluster IP is intended for internal communication. While the node port one is intended for exposing a port.

[00:02:00] So the change we need to do here is we need to inform the port and that's just going to be a node port. Let's use the port here 30,007. So that's all we need to change here. Let's save this. And now let's come back to the terminal in the terminal. I believe I can actually close the one at the bottom here. Let's just once again apply our new file. So color API nodeport like so

[00:02:26] and that's going to create a new service. So cube color get service, we're going to get here the color API nodeport. And as you can see, it also has a cluster IP. So the nodeport service type, it's actually also providing the functionality from the cluster IP service type.

[00:02:44] We can use its name to reach out to the underlying pods. We can use its cluster IP to reach out to the underlying pods. And additionally, we can use the node port 30,007 to reach out to the underlying pods. And that means that

[00:03:01] the Nodeport service is less secure or at least let's think about it. From the perspective of cluster security. We don't want to expose all our pods to external traffic all the time, right. So this is not, it's rarely going to be the case in a CORS cluster. So the cluster IP service would be the default one for us because by default,

[00:03:22] we would like to keep our traffic internal and not expose the pods to external traffic. Only those pods that actually need to be accessible from the outside would then be candidates for this kind of nodeport service. But once again, this is not the final solution that we would use in production.

[00:03:42] Nonetheless, we could use this for development purposes in our machines. All right. So if you want running minicube in mac or windows, then we are not going to be able to directly hit the node IP and the port here and actually talk to the service.

[00:04:01] So if we were to do cube, get nodes and using an output of wide here, we will get our node IP address. So you see here, this is the node Minicube and then we have here our internal IP. If we were to try to run a call request with this internal IP port

[00:04:21] 007 that's not gonna work. It's gonna be handy. And then that's because of how the overall system Docker here in Cnis are set up using Minicube. So if you are in this kind of setup, we need to run something or need to run a mini cube command. So Minicube service

[00:04:41] and then we can pass the nodeport service here and we can pass then the URL flag so that it returns only the URL here for us to query this. Now you will see that this port is different than this part. So it's a little bit confusing here. But bottom line is this is mapping a port in our localhost to the node IP port 30,007,

[00:05:08] right. So you can also see here that the terminal needs to be open so that we can run it. So let's just copy this here and let's hit this endpoint on the browser. And as you can see if we open the browser and we hit this endpoint here,

[00:05:22] then we get the correct response. We can also hit the forward slash API endpoint. We're going to get a response from a different node. And if we keep refreshing this, I don't know, it's not very convenient because of the caching on the browser. So this is not changing as much. But you remember, and this is exactly the same as the cluster IP.

[00:05:42] If we were to have a look at the requests that were sent by the cluster IP server or by the traffic generator to the cluster IP, it was hit in different pods and that's exactly the same here. So if we were to now pass here, I believe it's format JSON, I, I'm gonna get the response in

[00:06:00] JSON here from a different pod. That's great. So we are actually able to communicate to our pods from outside of the cluster, right? The browser here is outside of the cluster. It's not inside of the cluster. When we used the traffic generator, we created a pod inside of the cluster and we were able to talk to other pods inside of the cluster.

[00:06:22] What we are now able to do is to actually talk to a pod from outside of the cluster. And that's amazing. Congratulations on deploying this service. Let's jump back to the terminal here. Let's stop the running process. If we were to now try to hit this endpoint here once again with a

[00:06:40] request, then we can confirm that this is not going to work because this is not really so many service. The tunnel here is not up and running and there is no binding here. So basically this part is not bound to any node IP address and port behind the scenes, I mentioned that this is necessary when we are running on mac and Windows.

[00:07:03] So in the next lecture, I just want to show you the behavior here of this overall nodeport service in a Linux machine. Let's take a short break and come back in the next lecture.