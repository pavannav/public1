[00:00:00] So I think we went through the main comments of this Docker compose cli and I really don't think that it makes sense for me to start going into the nitty gritty here because we have a lot of good documentation. Remember the dash dash help is always your friend. It will always tell you all the comments that you can run. And if we are to use here,

[00:00:21] if you are to use the help flag with whatever command you want. So let's say stats here. For example, I want to know a little bit more about the stats command. It sounds interesting display live stream of container resource usage. So how can I use that? Well, let's just type the help here and let's have a look. Well, how does the help work? Well, it receives a couple of options here.

[00:00:41] We can show all the containers otherwise it will show just running containers and we can then give it a try. As you can see here, we should run Docker compose stats, we can pass these options and we can optionally pass a service. So let's just go here, Docker compose up once again

[00:01:00] and we can start ondetached mode, everything gets created. So now how about we go, Docker compose stats and bam we start getting some CPU some memory usage here, some statistics about our container. If we were to stop this and to run. Now our stats just with the back end then as expected,

[00:01:20] this is going to start showing us statistics only for the back end. And here for me, it just has a little bug when I scroll up here, it starts printing everything on the screen. But if I just scroll down here, it just keeps the last line. And just in case you start seeing this printing, try to scroll down all the way in your terminal. Perfect. Let's then stop this here.

[00:01:40] And this was just one example of how we can simply pick a command at random here and find, find out more information about how we can actually run that command and customize that command via our options. Another important aspect that I think makes sense. Repeating is the fact that whenever we run the Docker compose cli and then whatever command here,

[00:02:01] we can refer directly to individual services without having to worry about the actual container name. This can be very useful because the services and their names are defined in our compose file. So it's very easy to identify here to know where you want to get this information from.

[00:02:20] If we were just using the plain Docker CLI, this would be a little bit more challenging because we would need to figure out how the containers from a specific service actually named. Perfect. That's it for this overview of the Docker compose cli. Let's now take all our services down including the volumes here. Let's just make sure that everything is removed.

[00:02:41] Awesome with all the containers, volumes and networks removed. That's a great moment for us to take a short break and come back in the next lecture.