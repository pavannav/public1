[00:00:00] Hello there. In this lecture, we're gonna dive a little bit deeper into Docker cli commands and how we can manage containers and images with the CLI. First of all, I'm starting with a clean slate here. So we have no images and we also have no containers running as you can see here, we already have two commands. The first one is Docker images to show all the images that we have stored locally.

[00:00:23] And this is not referring to the images in Docker hub, right? So this is just the images that we have downloaded and stored locally either via Dockerpool or Docker run. And then we can see all the containers both running and stopped containers. If we issue a Docker PS

[00:00:41] A let's then use Dockerpool to download the Ubuntu image. If we do not specify any tag after the image name here. Ubuntu, then it's just gonna default to latest. Once we do that here, we should also see using default tag latest and it's gonna pull this image from Docker

[00:01:00] hub. As you can see if this is successful, we should see a downloaded newer image for Ubuntu latest. We can also run an Ubuntu container. However, let's have a look at what's gonna happen if we were to run Docker, run Ubuntu. And just like, so then you see that this is just gonna print something here.

[00:01:20] It's actually not gonna print anything, but it's just gonna wait a few seconds here, even, maybe less than a second. And then we're just gonna get a new terminal here or a new line where we can write commands. That means that this Ubuntu here, this Ubuntu container was started and it actually exited. And we can see that here with Docker PS A,

[00:01:40] we actually have this container which was run here with the image Ubuntu, the command that was executed, the default one was bash. And because there was nothing in this command that would require the container to remain alive, then this just exited with a zero code. Remember when we exit with a zero code for Docker, that means

[00:02:02] the container has exited successfully. This is one example of a short running container. So basically, it's just gonna run the batch command. There is nothing else to run here. And once it completes it just exists, if we were to run directly here with Docker runin X, then this is gonna be a little bit different. Remember

[00:02:20] that X is a web server. So it needs to stay up and waiting for connections. Let's wait for it to download the image. As you can see here, we have all the logs already from NGX. So that's gonna start here actually. And then we can also see that it has downloaded, it has pulled the image. So Docker run will automatically pull the image for us in

[00:02:42] case it's unable to find the image locally. If it does have the image locally, then this is going to be much faster, right? So let's just stop this container here and let's clear the console. Let's now run once again, Docker run engine X and you will see that the container starts immediately. That's because the image is already cached here locally.

[00:03:03] And since there were no updates to the latest tag, then Docker just directly reflects or directly leverages the image that we have stored here in our cache. Let's once again exit the container here and let's explore which kind of containers were created for us. So first let's have a look at our images. We have both the engine

[00:03:22] X and the Ubuntu ones. And this is basically just the images that we have worked with so far in the lecture. If you have tried other images, for example box, then they will also be listed here. If we run then a Docker PS we will see that there are no containers running actively.

[00:03:41] However, we have quite a few containers that have exited. As you can see here, we have the two engine X containers that were created when we ran the Docker run engine X command and the one Ubuntu container that was created when we ran Docker run Ubuntu. What's so important here is to observe that

[00:04:02] we actually have different engine X containers even when the command is the same. And that's because Docker is gonna create a new container based on this image. Every time we issue the Docker run command, this is a combination from create and start.

[00:04:20] If we do wish to leverage a container that has already been created, then here we did not pass any name. So let's just copy the hash here and we should actually issue the command. Docker start and then pass the container ID. And the moment we do that, we are now able to see the container that is running with Docker PS.

[00:04:41] So here we have a created we a container. So let's just have a look here. Docker PS A we had this container here, which is already created for us. It already exists. It is the same thing as the second engine X container. It's already there. It already exists, but it is in a stopped state.

[00:05:01] And now we want to move this to a start or to running state. And the command for that is Docker start. So once we run Docker start and then the container ID, that's gonna take an existing container and then it's going to start it up. As you can see here, we have the status of our engine X container. It is up for 13 seconds. At the time that we ran this command.

[00:05:23] If we try to run Docker, start with a container that doesn't exist. So let's just take here, let's close this. Let's clear the console, let's say Docker start and let's just have a look at our hashes here. So Docker PS dash eight, an offer to run Docker start with a container hash that doesn't exist,

[00:05:41] then Docker will just tell us that no container exists, right? So the Docker start here assumes that the container with either the name or the container ID here they are already created. Let's then stop our engine X container and because we did not pass any name here, then we need to have the container ID.

[00:06:01] So I'm just gonna say Docker stop and then this is gonna stop the engine X container. If we run now, Docker PS dash A, then you see that the container exited three seconds ago as you might have guessed the more containers we run, the more containers get created and the more resources get consumed, right?

[00:06:20] So space here is gonna get consumed. And if we have lots of containers running, then that's gonna also impact CP U and RM. So it's also a good practice for us to remove containers that are not needed anymore. The command that the command for that is Docker RM and then the container ID, right? So that's gonna be either the container ID or the container name.

[00:06:41] If you have passed a name flag. So let's just work with the container ID here. Let's clear the console. Let's run Docker PS A once again and now we can run Docker RM and we can pass the container ID. The moment we do that, we get the container ID printed on the screen and this container is not visible anymore when we run Docker PS dash A.

[00:07:03] So this was removed, it was deleted and the resources were freed up. Let's then clear it once again. And I just want to show you that you can pass multiple container I DS to Docker stop or to Docker RM. Sorry. So let's have here, Docker RM. We're gonna pass the first container.

[00:07:20] We're gonna pass the second container and the moment we do that, then we should have no containers listed when we run Docker PS dash A great. We can also work with filters whenever we are running Docker PS. So let's run here and Ry next container we're gonna as a name and this is just gonna be web underscore server.

[00:07:40] We're gonna run this on the detached mode. We're gonna, we're not gonna map any parts or actually we can map a part here 8080 in case you want to open this in localhost. And the image that we will use is NGX. That's great. The container was created. Dockerps is showing the container here and we can see the name it has web server

[00:08:01] right? So now let's start a few other ones. Let's run here another GX and that's gonna be maybe just without the name. So let's just run engine X like so in a detached mode, engine X, let's run it once again. Let's run it once again. That's great. So now we have four GX running here and if we were to run a Docker PS, we would actually get all of them.

[00:08:22] So if we were looking for a specific name, we would either have to run, let's clear here, Docker PS A and then probably a grab for web server if you are looking for it like this, or we can also pass a future to Docker PS. So we can say Docker PS and we don't need the dash A here because these are all running containers.

[00:08:43] So if we were to run Docker PS filter and the name here is going to be equal to web underscore server, then we're just going to get back the web server container that is running. One of the advantages of this is that you can build more complex filters here and this might

[00:09:01] get a little bit more complicated when you are using web or at least harder to understand. So using the future is a good way for us to build more complex filtering criteria here. Whenever we're looking for a specific container, this lecture is getting a little bit longer than I expected because we are discussing many aspects of using the Docker CLI

[00:09:20] and there are still a few comments I want to discuss with you, but maybe let's just do a little bit of clean up here and let's take a short break. So let's have a look once again at all the containers that are running Docker PS oops do without a here Docker PS and this is going to show all the four GX containers. We can also confirm that is these are the only containers that exist with Docker PS A.

[00:09:44] Now, we would like to stop all these engine X containers. Now it's not really the, the easiest way here or the most user friendly for us to run Docker PS and then Docker stop and then come here and copy this and paste it here and copy this and so on,

[00:10:01] on and so forth. So maybe there is a better way. And in shell here, you can just run a sub command or run a command in a subshell here and we're gonna just gonna get the IDS of the container. So we're gonna do something like this. Docker PS Q. Now, before executing this, let me just show you the results here. Docker PS

[00:10:23] Q is just gonna return the container IDS the dash Q stands for quiet, I believe. So it just returns less information. Just the IDS, we can then use this here as a result. So let's just run the Docker stop with Docker PS dash Q

[00:10:40] and as you can see here, we're gonna stop all the four containers. So if we were now to run Docker PS, there are no containers running and if we check Docker PS dash eight, then we get that all the containers exist exited roughly 10 seconds ago. As you might imagine, this can also be combined with Docker RM for us to clean up our containers.

[00:11:03] So I'm just gonna do Docker PS but now instead of just dash Q, we also need to add the dash A because we want to show all the containers including the stopped ones. The moment we do that, we can see the four containers are listed here. And then once again, Docker PS dash eight returns, no containers.

[00:11:23] Amazing. As I said, there are still comments for us to discuss and they are very interesting ones. But first let's take a short break and then we will come back in the next lecture.