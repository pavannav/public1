[00:00:00] Hello and welcome to our project to persist the data from our call API. In this project, we are going to deploy a mod B stateful set and update our color API to store the color information in the database. In order to achieve that, we will work through the following concepts.

[00:00:20] We're going to deploy a stateful set, a headless service and a persistent volume claim for dynamically creating our persistent volumes in minicube. We will then create the necessary config maps and secrets for the database initialization and connection from the color A P. We will update our color A P code to receive the database connection via environment variables.

[00:00:41] And these environment variables are to be retrieved from the respective services. We create, we will also update our code to connect to Mongodb and to use Mongoose for handling database interactions and we will add the relevant paths and the respective actions to our rest API. So here is a little diagram of what we are

[00:01:00] trying to implement within the cluster within the default namespace. We will deploy a Mongod B stateful set with a headless service that allows communication to the underlying parts of our stateful set. This state for set will be able to read config maps and secrets that we create. And it should also create a persistent volume claim by using the

[00:01:22] default or the standard storage class present in a Minicube cluster. This storage class, this default storage class will then automatically dynamically provision a host path volume here. Under our minicube node, we will then also deploy a new color API version of our color API

[00:01:40] which communicates with the database via this headless service and retrieves the necessary credentials and configuration from the config maps and secrets. We create, we will also deploy a nodeport service here that allows us external users or developers to reach out to the caller API and test our changes and interact with our rest API.

[00:02:02] So here is a general overview of the paths I would like to implement. Whenever you send a get request to forward slash API, you should be able to provide a color key as a query URL parameter. And then this forward slash API should return our already existing message containing the specified color under the color key

[00:02:22] and the hostname of the pod processing the request. If you send, get a get request to forward slash API, forward slash color, it should return all the stored colors. Whenever you send a get request to a forward slash API forward slash call forward slash colon key or a variable key path here,

[00:02:43] then if a caller with the provided key does not exist. The API should return photo four. Otherwise it should return the call information. If you send a post request to forward slash API call key, if a color with the provided key does not exist, it should save the color in the database.

[00:03:02] Otherwise, if the color with the provided key exists, it should update the color in the database. You can also choose to split this into post and put methods if you wish to do so. And this could be an interesting exercise if you want to get more familiar with javascript and working with Mongodb and Mongoose.

[00:03:21] Finally, the delete method whenever we send a delete request to the path API color and then a dynamic key here, it should delete a color by its key. So as you can see, the rest API itself is nothing fancy, nothing involving super complex business logic, but it will definitely give us everything we need to

[00:03:42] test connecting and storing data in a persistent database. In this case, Mongodb, as well as allowing users to interact with our API via HTTP calls. If you wish to give it a try by yourself, then by all means just pause the video and try to implement the project. And let's continue with our discussion and implementation in the next lecture.