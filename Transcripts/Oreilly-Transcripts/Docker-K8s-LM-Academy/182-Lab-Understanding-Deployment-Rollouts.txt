[00:00:00] Great. We have our deployment in place. Let's have a look here. Cube K get deploy. We should have our deploy here up and running. If you haven't deleted the deployment, make sure to recreate it before following along with this lecture because we will tackle several points related to rollouts and how we can actually see the different versions of a deployment.

[00:00:22] The cube card command for this is the rollout command. Now, I'm going to start with a dash dash help here for us to see the available sub comments in the rollout QK. The first one is the history where we can see a history of the rollout. We can then specify the deployment here that we want to get the history for.

[00:00:43] We can pause a certain rollout. So we can let's say mark a deployment as paused. This is this will prevent the deployment from rolling out automatically. We can then restart the deployment or restart the rollout. We can then resume any paused deployment. So if we pause something,

[00:01:00] we should first resume before we can automatically roll out the changes. We can show the status of the rollout and we can undo a previous rollout. Once again, if you wish to get more information about each of these sub commands, you can by all means add the help flag to the sub command. I have not deleted our deployment here. So we have two versions, right?

[00:01:23] Remember we had the first with the next 1 27.0 and another one with the 1 27.0 alpine. If we were to now run cube cut rollout history, and we can say deployment forward slash X dash deployment,

[00:01:40] this will show that there are two revisions for this deployment. As you can see here, the change cause is not specified because we have not provided this information. Let's then begin by undoing the second rollout here that we did and back to the first one, let's open here a new terminal

[00:02:00] just so that we can keep track of the pods here in the first one. So let's use Cube cut, get pods and let's watch the pods here and let's now run the cube cut rollout and do and we're going to pass your deployment and we can also pass the deployment. We should also pass the deployment name here,

[00:02:20] deployment. Now, you might notice that sometimes you will find a forward slash here. Sometimes as you can see here you will find. So the forward slashes here, sometimes you will find no forward slash both of them work just fine, ok? Just choose whatever you feel most comfortable with And the moment we do this here, as you can see the pods,

[00:02:40] they start to actually be deleted and we are getting back the replica set and its respective pods with the previous hash. As you can see, there is no magic here. The deployment controller in Kernis, they are just looking at the old hash, the old version of the template and they are creating a new pod

[00:03:01] or they're creating actually five new pods here based on the older version, we have a little bit of a mixed output here. So let's just clear the console and let's run the get pod watch once again. We can now confirm that we have five pods running with the old replica set. If we now have a look at Qle

[00:03:21] rollout and let's then use the history here. We will see that we actually have a revision two and three here. We can get more information about this rollout if we were to just run the cube cut rollout history and let's then pass here. Revision

[00:03:40] and the revision is going to be, let's say revision number two here and the output we would like to get in emo now, as you can see here, if we scroll up a little bit more, we actually get the details of the template that is used that is considered in this revision. So we have the revision two that we have passed.

[00:04:02] If we scroll up a little bit more, we can also confirm that this is a replica set. And if we scroll down, we get the information for our pods. Let's do the same here for revision number three. So I'm just gonna run the cube color rollout history, specify the revision and output this as

[00:04:21] A Y oig. If we scroll up a little bit more, we will see this now matches the actual, the initial version that we had in our definition file. If we continue to scroll up here, we will also see that there are two fields, both the revision and the revision history fields. And the revision is the current one and this

[00:04:41] history here is the previous one for this revision. So if we were to run now the revision two here, we will see that these fields are going to be slightly different. There is just the revision. So this is just number two here, but there is no revision history. Hopefully this starts painting a little bit of a better picture regarding the different revisions and how cube

[00:05:03] cle rollout history actually works because as you can see, this might have gotten a little bit confusing when we ran the cube cle rollout history. The first time we got two revisions one and two and our revision one is basically has it, it has disappeared. Where is revision one?

[00:05:20] Well, in reality, revision one became revision three when we ran the cube rollout and do so when we run this command here, cube cut rollout and do and then we pass the deployment then the revision, that was the first one here in our list. It actually became the recent one. So the revision number three. All right.

[00:05:42] Hopefully, that's not too confusing. Now, we can confirm here that if we were to let's describe our pod. So I'm just going to get here the name of the pod. Let's copy this if we were to now run a describe pod here and let's get the image. So let's just paste it here. Get the image. We can confirm that this is 1.27 0.0.

[00:06:03] So let's see how we can add this change. Cause here to our revision list back in the ide, there is an annotation that we can add here to the metadata section that will help Kernis populate the change. Cause this annotation can also be extremely useful whenever you are looking at a list of revisions.

[00:06:21] As you might imagine, there are many changes that happen to deploy. And it's very helpful to have a list of what was the cause for a certain change? We can now add an annotation here. So let's add annotations. And the name of the annotation is Kubernes Kubernes dot IO

[00:06:40] change cause and I'm just going to add this between double quotes like so and it will simply say here, update engine X two tag 1.27 0.0 dash alpine, right? So now we have updated engine X to this Stack here. We have set the container image here.

[00:07:02] Remember we reverted the last rollout that we did. So we have the old 1.27 0.0 running. So let's now save this file, come back to the to the terminal and let's on the top here. Run once again, let's run the diff before. So Cubot D, we can confirm that the image will be updated.

[00:07:21] And we can also confirm that this Cnis IO change cause annotation will be added. That's great for us. Let's then apply cube, apply the configuration file and this deployment has been configured. We can now see that the new template is used here that the new hash appears here. And if we were to now run cube cut

[00:07:41] rollout history, once again, we now get the change cause update X to tag 1.27 0.0 dash alpine. Isn't this great? This provides a lot of information around what is actually happening with our deployments. And I would recommend to keep this information available. Now, it might be the case that you are running, let's say a deployment,

[00:08:02] triggering a rollout from a CI CT pipeline. And you don't really want to edit the file to save this information in the file. But rather you would like to annotate the deployment here. So let's go back to the ide and let's remove these annotations here. So I'm just going to remove these annotations here.

[00:08:20] And let's change the image from 1.27 0.0 to 1 27.1. Let's save this. Let's come back to the ide. Let's now trigger another apply here to roll out. Once again another image, let's confirm that the containers will be created. You will see here that they are taking a little bit more time,

[00:08:40] but they are successfully running. And that's because the image was not available locally in my machine. So now we can confirm that the containers are running or the pods are running. Let's stop this. Let's run the get pod once again, that's perfect. This is a valid image tag. But now if we were to run cube cle rollout history, once again,

[00:09:01] we will get that the fourth revision is annotated, but the fifth one, which is our new one, it's not really annotated. As I mentioned, we can do that imperatively from the Cubot C by running the cube cut annotate command. So let's run cubicto annotate dash

[00:09:20] dash help. This is gonna provide or this will provide some information around the command itself. As you can see there are quite a few options here. So let's clear this, let's actually close this on the top here so that we have a little bit more space. Let's then clear the console and let's just annotate our deployment. So we're gonna run cube cut

[00:09:40] annotate and then deployment forward slash X dash deployment and then we need to provide our annotation and annotation is the same as before. So if we copy what we had in our file previously and just paste it here, we should just use an equal sign instead of the colon there.

[00:10:00] So let's just use it like. So the name is going to be Cnis dot IO change cause and then we're going to say here update engine X to tag 1 27.1 alpine. This will then say that the deployment has been annotated and this is not reflected in the file. So if we come back here to the ID and we have a look at the file contents,

[00:10:21] we don't really see any new annotations here. However, we can confirm that the deployment is annotated by running Cube Carle describe deployment and then the engine X deployment. So X dash deployment like so we can confirm here on the top if we scroll up a little bit more,

[00:10:40] we can confirm that our change cause is specified here. Let's then clear the console. And if we were to now run cube cut rollout history, once again, we now get our fifth revision properly annotated. This might be useful once again, if you are running this in a CICD pipeline and then after the rollout is completed,

[00:11:00] you wish to add some information about what was the cause for the change. And this could be maybe the commit message or some summary that is being automatically provided, you can then use the cube cut annotate command to actually add this information. All right, I believe that we are already talking for quite a bit of time.

[00:11:20] We have discussed a lot of important aspects around rollouts. And once again, deployments are a very extensive topic in CORS and the best way to learn is to practice and to play around. So I would suggest you to give it a try. What happens when you up the configuration, when you write something that's not valid. What happens when you delete a certain part?

[00:11:41] What happens if you delete the entire replica set? You can really try different commands and see what happens. We are in a sandbox here and you should be trying all these things in a sandbox environment, meaning that the blast radius here is really limited. So just go crazy here and try to run different commands to see the impact that they have in the cluster.

[00:12:00] That being said, I still want to discuss a few more topics around deployments. But before that, let's take a short break and I'll see you in the next lecture.