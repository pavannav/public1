[00:00:00] We now know that we can pass in line patches here to our customization emo file. But as our base and as our projects grow, this can very quickly become very hard to manage. And that's because we would have a huge list of patches here. They are all in line in our customization, not Yamo file.

[00:00:20] And this is just really not a scalable approach. So another way that we can use these batches here is to actually create individual Yaro files. And let's just say here, update dash resources dot YAML. So let's now copy this content, the entire content here, just gonna copy this and paste it here.

[00:00:40] And yes, we will start getting some errors from the schema validation here. And that is because our id, so that's a visual studio code issue here. It is expecting us to provide the entire resource definition, but we know that this is a patch resource. So we can either rename this here to make it very clear to

[00:01:01] our maintainers that we are expecting to have some partial resource declaration here. That's a update resources dot patch dot YAML. And the moment we save this here and come back to the customization, not yamo file. We can now instead of specify an inline patch, we can use a file, we can pass the path to the file

[00:01:21] and the path here will be update resources dot patch dot YAML like. So let's now save this. Let's come back to the terminal, clear the screen, rerun our overlay. And as expected, we still have the expected results. So we have the same result here as before.

[00:01:40] So customize is able to identify to load also from multiple paths. And this can actually be very scalable. Let's now say that we want to hear use latest dash GX for example. And then let's say that in our engine X deployment for our container with the name

[00:02:00] X, the image here should be Xgen X colon latest like so and now if we just save this and then add another patch here with the new path being use latest dash GX or we could even say use latest image or use latest tag. Let's say that's I think is a more precise name.

[00:02:22] Let's just rename this here, use latest patch dot YAML, right? So I forgot here the patch. Let's then add it here. Let's save this. Let's come back to our terminal, clear the screen and rerun customize and you will see that. Now the image here was also updated, we can keep writing patches here, right?

[00:02:44] So let's say that now we would like to mount our DB init config so mountd bin it dot Pat dot YAML. Like so and let's say that for whatever reason here, we want to actually mount this config map that we just created here.

[00:03:01] Our DB init config inside of an existing deployment here, our engine X deployment. So let's just copy this update resource. Let's paste it here. And then all we need to do here is inside of our containers. Let's say that we're gonna use the engine X container and we're gonna say volume

[00:03:21] mounts and this volume mounts will just identify a volume by name. Let's say here, db config like so and then we pass a mount path which is just gonna be our dummy db config. We can then also specifies, specify a volume key here where we're gonna have some volumes like so

[00:03:40] where we're gonna have our name and the name here will be db config and then the config map will have its name being exactly what we have defined here. DB init config. So let's copy and paste this here. Let's now also add this patch here to our customization.

[00:04:02] So let's just copy and paste this here and let's just copy this file name. So that's mountd B in it dot patch dot YAML like. So just copied it and paste it here. Let's come back to the terminal once again, let's clear the screen and let's now run our project and you will see that customize not only defined our volumes and volume amounts entry here,

[00:04:25] but it also correctly identified and customized the name of our config map. So as you can see, this is a very powerful option using different entries here can allow us to decompose the changes we want to implement in different objects into atomic changes,

[00:04:43] which I find to be the more maintainable way here so that we don't have a huge deployment resource declared here with different changes. And then we might end up being confused what exactly is being changed when compared to the base deployment. And just as a side note here,

[00:05:00] there are still modifications that we cannot perform with this kind of partial resource configuration here. And also there might be some conflicts, right? So let's say for example here that we have a use latest tag here. And let's say that now we have a use stable tag, for example, stable tag dot patch dot copy like so

[00:05:21] and I would like to getx stable like so if we were to now save this, let's add this other patch here. So I'm gonna say use stable tag, then let's now save this, come back to the terminal. Let's apply here. And if we scroll up a little bit, you will see that the one that prevails is actually the stable one.

[00:05:43] If we were to come back here and change the order of our patches. So let's change the order here. Let's save this. Let's come back to the terminal and let's apply once again. Now, the one that survives is the latest one. So this becomes susceptible to ordering here. And this is really not something that we want to start worrying about.

[00:06:03] Right. So if we have this kind of changes which are touching the same fields, then we would ideally group them inside of the same patch resource here so that we can clearly define what is the final intended state. Let's also delete our use stable tag patch here.

[00:06:22] And I believe that's enough for this discussion. I would still like to discuss Jason patches with you. But before that, let's take a short break and I'll see you in the next lecture.