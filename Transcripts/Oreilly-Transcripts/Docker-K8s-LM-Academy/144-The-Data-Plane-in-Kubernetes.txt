[00:00:00] Awesome. It's great for us to learn about the core architecture and what is actually happening under the hood. Let's now focus on the worker nodes and that's where our applications are running, where our containers are actually running. So I put the control plane here on the side and expand it a bit more on the components of the worker node.

[00:00:20] Let's go through them one by one. The Qle is an agent that is running on each worker node and it is communicating continuously with the control plane via the API server. The job of the QLA is to ensure that the containers are running in the pods as it is defined in the pod specification.

[00:00:42] We will see as we start working with pods that we define which containers we want to run within a pod. And then it's the job of the Qle to ensure that these containers are running as defined by the specifications, we then have the container runtime which is responsible for running the containers on

[00:01:02] kernel. So the most common one here or the one that you might be most familiar with is Docker, right? So we can have Docker as the container runtime. But there are also other options such as container D or cryo. So container runtime here is basically the software that will be running

[00:01:20] our containers that providing the runtime for the containers to run. We then have cube proxy which is responsible for managing the network rules on each of our worker nodes. And it maintains the network connectivity and the load balancing for services and ensures that these requests, whatever requests come via these services here,

[00:01:42] they are routed to the appropriate pods. We are going to discuss services in much more detail later on in the course. So just keep the proxy in mind whenever we are talking about them, we then have many, many other Kubernes object. We will discuss many of them throughout the course and they are

[00:02:00] providing a lot of functionality and a lot of cool stuff within Cnis. Examples here include replica sets, deployments, jobs, services, stateful sets and so on and so forth. As I said, there are many Cnis objects. They are all optional, which means you can have a cluster running only single pods, right? You don't need to have services, you don't need to have deployments.

[00:02:22] They are not mandatory, they are optional, but you will hardly ever come across a cluster which doesn't have at least one or two or even, I don't know five of these components. As already said, we're going to discuss them in detail as we progress through the course, we will see when and how to use them.

[00:02:40] So don't worry if this is not clear to you, I'm just showing you where these components here where these objects would actually live, where they would exist. Now, one important thing is for us to understand that they are higher level CORS objects. So here for example, services at the end of the day, they are going to be using Q proxy.

[00:03:00] At the end of the day, it's going to be Q proxy who will be responsible for routing the requests and for ensuring that the rules from services are followed. And when we talk about deployments at the end of the day, deployments are actually running pods and the pods are within the container runtime. So these higher level objects here, they will normally have controllers on the control plane side and the

[00:03:24] controller manager component which will be responsible for ensuring or for more entering the state of this object here and ensuring that the actual state is matching its desired state. We will also see some examples of this as we discuss these components individually.

[00:03:40] So don't worry if you, if it's not 100% clear what this means and I don't expect it to be 100% clear. This is an introductory introductory lecture. But just keep in mind when we talk about deployments, replica sets in jobs and so on and so forth. But just keep in mind that these CORS here, they also provide a lot of important functionality for us. Awesome.

[00:04:01] So hopefully, this helps also differentiate between the control plane and the data plane. What is happening in each of these? And I believe we have also earned a short break and let's come back in the next lecture.