[00:00:00] Hello and welcome back up to the last lecture. We have managed to configure our backend service as well as our database service. And we could confirm that they are up and running. What we have not set up yet with Docker compose is the hot reloading feature that no demand of offers for us.

[00:00:20] For that, we need to either create a bind mount here in our back end services or we can also use a little piece of configuration called the watch configuration here and the watch configuration is under the develop key. So there is a develop key here, develop configuration, then there is the watch object

[00:00:40] and this is basically a list of objects. So we can set an action here to sync folders and then we can define here what is the path that we wish to sync in our case? This is dot slash backend SRC. So we're just gonna look at the source folder.

[00:01:01] We can then set a target here and the target inside of our container will be forward slash app forward slash source. If you wish to have a look at the entire back end here and then the entire app. For example, as a target, you can also ignore folders here by setting the ignore option, you can then set,

[00:01:20] for example, to ignore the node modules folder. This is also possible. However, for us here, just looking at the source path inside of the back end and then mounting or syncing this to the source path inside of our container. That's already good enough in our terminal. Let's then run the Docker compose up command and let's inspect the logs.

[00:01:43] The moment we do that, we see here that we get both from the database and the back end. However, if we were to come back to our code, open the server dot Js file and let's say here, we're just going to add a new exclamation mark to the connected to the database and we were to save this.

[00:02:00] We will actually see that there is no new log with the updated message coming through. This is not a bug. We are just missing here, a small piece of configuration that we need to pass to the doc compose command. So let's stop our running containers and let's rerun this Docker compose command with a new flag, which is called the watch flag. So I'm just going to clear the screen

[00:02:24] and then run Docker compose up watch. Like so the moment we do that, we can also see here at the bottom that we have a different message we now see here, disable watch. Let's come back here. Let's save this and let's see if the code recompiles. Perfect.

[00:02:42] So as you can see here, we actually are getting now the connected to the B message with an exclamation mark, we have now managed to enable hot reloading. That means if we wanted to do something new with the code here, let's say, for example, inside of the server dot Js, let's just add a root route here,

[00:03:01] just gonna say app dot use and that's gonna be just forward slash. And then we can say here, let's just send a message, request response and let's just send here return response dot JSON. And then we will say we're gonna just type welcome to our key

[00:03:20] value store. Let's just save this here. Let's now jump into Postman and let's see if these changes were already reflected in our running containers. Actually, before we jump here, let's just change. This is not used. I got a little bit confused because of the use here. This should be the get method. So whenever we get the root route, we're just gonna get this message back.

[00:03:44] Let's save this. Let's come back to postman. Now, let's remove here what we had before. And let's just try to send a request here and perfect, we can see that our message is already reflected in our running container. Awesome. Congratulations on enabling hot reloading in your Docker compose project.

[00:04:03] This really saves a lot of time in a lot of headaches during development because this enables us to get very quick feedback whenever we change something in our code. Just one small note, I have tried a few variations and when I use volumes with a bind mount, that would be something like this.

[00:04:21] So we would say here dot slash backend forward slash SRC and then that would be mounted inside of the app SRC. Whenever I use this with Nodemon, they don't really work very nicely together. I ran into issues that Nodemon was not able to or was not identifying the changes in the files just by saving them.

[00:04:44] So to prevent any incompatibilities, I would suggest that we go with the more native solution of defining this develop option and then setting a watcher here for the files and then syncing whatever we change in our local path with the target here inside of the container.

[00:05:01] Perfect. We have covered many topics around Docker compose. As you can see there is nothing really new here. It's just a different way of setting up containers and bringing them together. And this uses a declarative approach that means we just need to define here at the bottom which volumes we want, which networks we want,

[00:05:20] we just need to define which services, which containers we want as well as which image and the configuration they should use. We can then run the Docker compose up command to start these things or to create them and Docker compose will take care of running the correct commands with the correct configuration. And in the correct order,

[00:05:40] this is a much nicer approach than us having to imperatively run the dock recommends using the Docker cli and having to worry ourselves about putting everything in the same order or making sure that they already exist or not and how to actually handle this potential edge cases. Even though we have explored Docker compose in details.

[00:06:00] Here, we did not really spend a lot of time working with the Docker compose cli. So let's take a short break and come back in the next lecture to do exactly that.