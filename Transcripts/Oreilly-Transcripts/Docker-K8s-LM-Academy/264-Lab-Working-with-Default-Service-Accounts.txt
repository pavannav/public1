[00:00:00] Hello and welcome back in this lecture. I want to explore the concept of service accounts. So service accounts, they allow us to pass a certain identity to our pods so that Kernis manages the authentication of these pods. And then in the end, the pods can interact with the

[00:00:21] Kernis API according to the permissions that are bound to this service account, right? So here we have two pods with engine X images. I would like to create a third pod here. Let's just use the cURL or the alpine pod, the alpine cURL one that we have here. So LM Academy

[00:00:41] Alpine cURL, the version is 1.0 0.0. Then I'm just gonna call this cURL and then we're gonna say here alpine cURL and then let's add this here as well to the metadata. The name space where we're gonna create this pod is gonna be the DEV name space.

[00:01:01] So far we haven't specified service accounts in the pod definition files that we currently have. That means that there is a service account which will be applied to this pod here by default and that's gonna be the default service account. So let's come to the terminal and let's have a look cube car

[00:01:21] get service accounts and I think that I'm gonna get an error because I'm still in the Ellis context. Perfect. Yeah, that is working well. So let's just change our context to minicube and let's run the get service account once again. And as you can see we get a default service account. So let's just have a look at this service account, describe

[00:01:41] service account, default. And this service account here is scoped to the default name space and there is actually not a lot of information here. Another thing that we can have a look here is that every name space gets its own default service account. So if we were to have a look at the DEV name space,

[00:02:03] and this is the shorthand here for service account, we also get a service account and I have a look at the product name space. We also get a service account. So let's have a look at what happens here when we get for all the name spaces. And as you can see here, we get the default service account under the default name space,

[00:02:22] the DEV or the default service account under the DEV name space, the other name spaces within our system, they also have default service accounts. And here at the bottom, we should then see the default service account for the proud name space, the cube system here because this is already set up by minicube and

[00:02:42] converts for us. It creates a lot of service accounts and these service accounts they will have then the necessary permissions for each of the pods to perform their necessary tasks, right? So if we were to think, for example, we have here a horizontal pod auto scalar or a deployment controller here,

[00:03:01] then this service account, it actually needs permissions to manage deployments, right? So this would have its own set of permissions in the role and then be connected to this in the in the role binding. Let's have a look at whether we can actually find the role binding for this. So let's have a look at cube cut

[00:03:20] getrow binding and then let's pass here the name space cube dash system. And let's try to grab here deployment, right? So let's have a look at whether there is anything here. No, there is nothing. So let's have a look at all the bindings that we have for the cube name system. And I believe that because these services or controllers here,

[00:03:40] they have to operate across the cluster, then they would actually have cluster role bindings rather than roll bindings. So let's clear this, let's see if we can get the cluster role binding. So cluster role bindings. And then here let's we can add the name cube, name space cube system. And then let's see if we can grab here for deployment,

[00:04:03] right. So maybe perfect. Yeah. So this gives us here something back. And this is binding the deployment controller service account to this cluster role here, system controller, deployment controller. So let's have a look at whether we can describe this

[00:04:21] role here or let's get the cluster role deployment controller. Let's output as YAML here, we have to add the whole cluster role name and the whole cluster role name is system colon controller, colon deployment controller. So let's try once again get cluster roll. Let's paste it here. Let's output AC

[00:04:42] O and perfect. As you can see now, we actually get the permissions for this cluster roll. So if we now have a look at our rules here, you see that this deployment controller, it has permissions over the deployment resources, it can get list update and watch.

[00:05:00] It can also update the status of deployments. And if we keep scrolling down here, it can also manage replica sets here. So it can create delete list patch update and watch. So as you can see, it has quite a few of the necessary permissions here to actually manage the deployments and their related resources.

[00:05:21] So this is a very good example of a process that's a deployment controller which is running in our cube system name space and it needs to operate on our cluster needs to perform operations against our CNIS API. And for that, it has its own service account and this service account is then bound via a cluster role binding

[00:05:44] to this cluster role that has this set of permissions. Great. So I think I diverged a little bit from what I wanted to do, but it was a very productive discussion. Our goal here is to actually understand how we can create our own service accounts and how we can leverage them to interact with the

[00:06:02] Cnis API from within our pods. But our discussion went in a little bit different direction. So I think that maybe it makes sense for us to take a short break, reset our brains and then let's come back in the next lecture.