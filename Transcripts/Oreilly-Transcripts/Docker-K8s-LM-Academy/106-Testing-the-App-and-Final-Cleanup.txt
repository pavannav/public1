[00:00:00] Hello and welcome back. Congratulations on completing the implementation of our key value rest. API I would like to take the opportunity to now explore a little bit the data persistence here and see what happens when we stop and restart the database container. And what happens when we remove the volume, for example, and then recreate that volume.

[00:00:20] So we're gonna start following here. I'm gonna stop following the logs. Let's then have once again a look at the processes that are running here for us. We have both the backend container and the MongoDB container and the backend container is dependent on the MongoDB. So if we were to just stop here, let's say Docker stop back end

[00:00:42] and then we will say Docker stop MongoDB as well, we should then confirm that we do not have any container running. However, if we were to have a look at the volume, so let's just say Docker volume, LS, we still have this key value data volume. So if we were to completely remove,

[00:01:01] let's completely remove our MongoDB container and actually it gets automatically removed. So we have here the start db command here, we see that we have the dash dash RM flag. That means whenever we stop the container, it gets automatically removed, we can confirm that we have no container here in Docker PS that is in an exited state.

[00:01:23] That means that our container is completely gone. If we did not have any volumes, the data would also be gone. However, since we are working with volumes, we can actually confirm that if we were to start this database once again. And if we were to also start the back end service here,

[00:01:43] we would also be able to retrieve the keys that were stored initially. We can even issue a post request to confirm that this key already exists. And if we were to get this hello key here, we will also get the correct result back.

[00:02:01] This means that we are correctly storing this information permanently or persistently in our volumes. And as long as the volumes are there, the data will be available. And whenever we create a new container here. So whenever we come, let's get once again, whenever we create a new container

[00:02:20] that leverages the same volume name. And this is important, the volume name here should be the same. If we change the volume name name, this will be considered a different persistent storage. So the volume name should be the same as long as we leverage that here, then we will get access to the data that was persisted during the previous interactions with this volume.

[00:02:42] Perfect. This was an amazing project. We tested a lot of different things here. We saw how complex or how long the Docker Run comments can actually become. And hopefully, this also acts as a motivation for us to try to look for a more robust and more scalable solution. And that's exactly the upcoming topic of Docker Compose.

[00:03:03] I'm sure you're excited to learn more about Docker Compose and we're gonna do that in just a few moments. But before we wrap this up, let's stop our containers. Let's stop the back end. Let's stop our Mongo DB container. Let's also remove both the volume and the network.

[00:03:22] Remember that we have a clean up script for that. So we're just gonna execute this cleanup script. We say dot cleanup dot sh. And then we will say that a container with the name MongoDB does not exist. That's good. We are then removing the key value data volume and removing the key value net network.

[00:03:42] Perfect with our system clean. So we do not have any containers. We do not have any volumes here. We also don't have any networks that were created. I mean, we do have some default networks that comes with Docker, but our key value net is gone. If you also wish to remove the images that we

[00:04:02] have created the key value back end here and the Mongodb community server, I'm just gonna remove them here because I like to start each section with a clean slate. So I'm just gonna say Docker images or Docker image, LS dash Q and then this will make sure that we remove all our images. Perfect.

[00:04:22] Let's then take a very well deserved break and come back in the next lecture.