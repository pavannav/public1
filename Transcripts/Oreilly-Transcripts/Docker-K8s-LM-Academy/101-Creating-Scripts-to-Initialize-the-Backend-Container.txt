[00:00:00] Hello and welcome back. Let's start working on several of the improvements we can make to our backend project structure. The first thing I would like to do is to just stop and remove the container that is running our back end server. So just gonna say Docker remove and we're gonna force here, we're gonna remove the back end container

[00:00:21] and then we can confirm with Docker PS that we have just our MongoDB container up and running. If we were to run Docker PS dash eight, then we should also not see our backend container here in the ide. Let's create a new script at the root level here. We'll simply say start backend

[00:00:40] shell and this is gonna be very similar to our db command. So let's just copy everything from here. We're gonna paste on the start back end. We're gonna remove quite a few things here. So we don't need the mongo image and the Mongo tag. We are not going to be working with this. We do not need the root credentials, the key value credentials we still need.

[00:01:02] So we can potentially actually extract this to the dot m dot DB file. Let's just cut this here. So let's cut this and let's move them to the dot m dot db file. Let's export all of them here. So we're gonna export them

[00:01:20] and then we can already confirm that we are importing them. We are sourcing them both at the start back end as well as the start DB files. So we can remove them also from here and then we can move this source. Let's just move here or actually, it doesn't really matter where we place it in the DB file.

[00:01:40] Let's just keep it where it was. Let's come back to our start back end. We do need to source the dot m dot network. So let's just keep it here. Remember the dot m dot Network has information about the network name and we're going to use that to start our container for the localhost port and the container port.

[00:02:00] We're just gonna use 3000 here. So let's say 6000. And let's have a look at what we have at the volume. So this is the key value data. This is specific to the database to the mongo DB configuration. So we don't need to have any persistent storage here for our back end setup.

[00:02:20] We also source the setup dot shell here. So if we were to open, this is basically to just set up the volume and the network, we are not going to do that at the back end. So let's just keep this up here. We could do that or actually, we are doing that when we set up the database and in one place that's already good enough.

[00:02:40] The setup, she is also fairly robust. That means if we were to run it here and the volume and the network they already exist, we will just skip their creation. Perfect. Let's add another file here or another variable. We simply say backend container name and we're gonna say just backend. So now let's check here

[00:03:01] if there is a container which is already running here. And we could actually say for all of them, all of the containers. If there is one with the back end name, then we would say a container with the name backend already exists. The container will be removed when stopped to stop the container, run Docker kill.

[00:03:21] And then we can simply pass the backend container name here. We can now customize our command by running Docker run. We're gonna remove the container. When we stop, we're gonna run on the detached mode. We will run with the backend container name. We do not need to pass here the MongoDB in it, the B root user name and password.

[00:03:41] But we do need to inform actually our credentials and our database for the key value database. So let's keep them here and let's come back to our server dot Js file and let's make these variables. So the first one that we would like to make a variable here is to just turn this into BTI

[00:04:03] so that we can interpolate and the key value db will be the first one. So we say process dot m dot key underscore value underscore db. This name here needs to match the name here, right? It doesn't need to match the name of our local variable in the shell script.

[00:04:20] It just needs to match the name of the environment variable that we set here. I will leave these names here because they are fairly intuitive. So let's just leave them as they are. We can then use our key value user in our server dot Js. So instead of username here, we will say process dot m dot key value user and for the password, we will say we will use the key

[00:04:43] value password. So let's just say here, process dot m dot keyvalue password. Let's then come back here and also pass as another environment variable, the port and the port here will be the container port. So I just would like to pass this here because this port will be used inside of the container,

[00:05:02] inside of the code server dot Js to listen for requests. So we're gonna say here process dot Mport like. So let's also use this value here on the interpolation. We could potentially extract this to another verbal, let's do that at the top here.

[00:05:20] So constant app and we can also say constant port is equal to process dot dot port like so, so now we're gonna use that here as well as here on the string. Perfect. There is also a final piece of information and that is the hostname of our Mongodb database.

[00:05:40] This Mongodb will be valued here only if we are running inside of this very specific network. But if we are deploying this container somewhere else, and we are also, for example, connecting to a Mongodb database, then this Mongodb server here will really or this mongo DB host address will really

[00:06:00] not be resolved anywhere and we will not be able to connect to the database. So let's make this also an environment variable. We're gonna simply say here, let's add the interpolation syntax, I'm gonna copy and remove this value. So cut the value here and simply say process dot m dot Mongo DB host like. So,

[00:06:21] so now we can come back to our start back end and we can also pass another environment variable called this MongoDB underscore host and just say MongoDB like. So we should also remove the two volumes that we are creating here because these volumes are specific to our database instance.

[00:06:40] The first one is to set the persistent storage. And the second one is to populate our initialization script in the database. Let's remove them here and let's confirm that the network is also having the correct value. This is coming from the dot m dot network. So if we click here, we can confirm that's correct. Perfect.

[00:07:01] And now the last thing we need to change is to change the image that we are using. We can also add a command here at the top for us to build this image. Since we are creating this script at the top level directory here, the command will be slightly different. We will simply say Docker

[00:07:20] build. The tag here will be key value backend and the file that we will use will be inside of our back end forward slash direct. So inside of our back end directory and the file is just Docker file dot dev. Let's just start also adding here a couple of lines. So that's the first one

[00:07:40] and the context here will actually be also the backend folder. So we don't need the context to be the current directory. The backend directory is enough for our Docker build command. As you can see here, this will always rebuild the code whenever we run the start backend command for our purposes here,

[00:08:02] that's totally fine. So let's just always execute the build command. This will also exit fairly quickly or it will complete fairly quickly whenever there are no changes to the code because we will leverage all the caching from a Docker. Let's also add here another variable to our image name.

[00:08:21] So let's come here to the top and then we'll simply say back and let's put at the top here back and image name and the image name will simply be key value back end like so we can now leverage this here. So let's just remove this and replace by backend image name.

[00:08:42] And we can also add another variable here for the mongo DB host. And that's just gonna be mongo DB like so, so we're gonna copy this and we're gonna use this here. Perfect last but not least we can also leverage the back end image name here at the bottom to start our container,

[00:09:00] right? So here we were using the MongoDB image and the tag we are now interested in using the back end image name. Let's save these changes here. Let's come back to the terminal and let's check whether the script is working as expected in our key value app folder. Let's first of all, change the mode of the script if you haven't done so yet,

[00:09:22] let's add the execute mode here and the name of the script is start backends. The moment we do that we are now able to execute the start backend command. So start backends and we will build this fairly quickly. We will see that at the end here, we get the container sharp and we can actually see the container.

[00:09:44] Now by running Docker PS, we can confirm that the container is up and running by running Docker logs. We have successfully connected to the database. And if we were to cURL once again the health endpoint the container is up and running. If we were to now run the start backend.

[00:10:03] Once again, then we will see that the container with the name backend already exists, we can then run the Docker kill backend here. And once we do that, the start backend will also work as expected. Perfect. So back in the ide, we were able to just reviewing what we did, right?

[00:10:20] We were able to extract a lot of the information that we have here and we are now passing this information as environment variables in our start pack and script. This provides much more flexibility because now we can run this exact same code here and the exact same Docker image with other setup with other Mongo DB host, with other key value data.

[00:10:44] If we wish to do so with another authentication method, we can change this here and we can customize just by changing our environment variables and our code remains the same. This is a more robust and a more stable approach. That's wonderful.

[00:11:00] So let's take a very well deserved break and come back in the next lecture.