[00:00:00] Let's now start exploring how the the collaborative management of objects works in Cnis by or with using the apply command. At the end of the last lecture, we have changed the image name from 1.27 0.0 to 1 27.0 dash alpine. I'm just going to remove the alpine here so that

[00:00:21] we start with the same image that we had before. And let's now switch to the terminal. There are two important commands for the collaborative style. The first one is the apply command and this apply command can work with either files, individual files or directories. So if we pass an individual file here, let's say the Nginx

[00:00:43] pod dot YAML, then this command will identify that there is no Nginx pod running in the cluster and it will create one behind the scenes. So if we were to now run cube K get pods, we can confirm that our Nginx pod is up and running to delete resources. Kubernes

[00:01:02] does recommend that we use the imperative command, the delete one here and we can just pass our engine X pod file like so this is going to remove the engine X pod. We can now also pass a directory here to the cuo apply command. So for run cube cut

[00:01:21] and then apply, pass A F and we can pass the current directory with just A dot Here. We can also to make it a little bit more clear. Let's go back one directory and let's run cube cut, apply. And then we're going to pass the object management directory. Remember there are two object files here, there are two configuration files.

[00:01:43] The first one is the pod and the second one is the service. The moment we execute this, you can see that we actually create both of the objects. You can imagine that this is extremely convenient because whenever we have here multiple objects being managed inside of directories,

[00:02:02] then we can just apply all the changes at once with this command. At the same time, this is a more dangerous or more sensitive operation because we might have changes in individual files that we don't want to apply. And if we apply the whole directory, then everything will be applied. The second command I want to discuss with you is the diff command

[00:02:22] and this D command is going to show the differences that CORS to apply when we or before or actually when it runs when we run the apply command. So if we were to run cuo div and then pass the object management here, we will see that there is nothing,

[00:02:40] there are no differences because we have not modified any of the files. Let's now switch to the ide and let's add our alpine suffix here to the image. Let's now go back to the terminal back in the terminal. Let's run this cube once again here. And as you can see, Kubernes is actually comparing here the configuration that we have.

[00:03:03] So this is as you can see the live configuration, we have mentioned that several times And this is the configuration that is actually used by Kernis. It is actually looking at this configuration here to manage our pod object. And as you can see the difference here is simply saying that hey,

[00:03:21] I'm gonna change the image of this pod or actually the image of the first container running on this pod. If you remember from previous lectures where we have discussed one of the shortcomings of using the replace command in cube cut, we saw that there were many other changes that Kubernes

[00:03:40] would want to apply here. It is able to identify that the only field that it needs to change is the image. And we also saw that in the error message. In previous lectures, the image was a field that we could modify. It was among the allowed fields for us to make the changes. So this is a valid change in our pod definition.

[00:04:03] And if we were to go ahead with cube car apply and then once again, pass our object management folder, then we will see that the engine X pod has been configured here. It has been updated and the service remains unchanged because there were no changes to the service file. Let's now clear the console.

[00:04:23] Let's run a describe here to get our pod configuration. And if we scroll up, we will see that the pod image has been correctly updated. Now, I want to also dedicate a few minutes to understand here or to go through a few interesting fields.

[00:04:41] As you can see here, the pod, it has been created a few minutes ago. So I'm looking here at my clock, the clock here is actually roughly 27 now. So we ran this command a few minutes after we have created the the pod. So the pod was not deleted and recreated

[00:05:01] what has actually changed is the container here. So as you can see, we have this X container entry here and we can have a look at this file or this configuration here, these two pieces of information. And we can see that the last state of our container is actually terminated.

[00:05:21] That means cuber needs was smart enough to terminate the container and then start a new container here at the time that we ran the command. And you can see here that this is roughly the start time of the pod. So it matches the start time of the pod here. And the termination time of the old container

[00:05:40] also matches the creation time of the new container, the pod itself was not deleted and recreated. What was deleted and recreated was the container. If we come down a little bit more here, we will also get in the events section. We can see here that we have an event with an age of roughly four minutes

[00:06:01] where we have pulled the engine X 1 27.0 image. We can also see that the containers have been created twice over the time spent here of roughly four minutes since we have created the pod. And if we scroll down a little bit more here, we will also have a pooled event for the new image when looking at these events

[00:06:24] as well as on the information here about the last state of our engine X container. We start getting a better picture of how Benis is actually updating the containers here whenever we change the image on that container. Another important aspect of working with the collaborative approach

[00:06:42] is that when we get here the configuration. So when we say cube car get and then we get the configuration of our created pod when we run this and scroll up a little bit more here, we will actually get a field here which is called the last applied configuration.

[00:07:00] This was not present. So as you can see, this is an annotation and this was not present when we ran when we got this configuration. After running the create command with the configuration file. As you can see this here matches our configuration file that we have defined except that it's using

[00:07:20] JSON instead of YAML. But this is an exact copy of what we have defined in our file. This is also used by Kubernetes to calculate the differences in the configuration and apply the changes. That's great. As I mentioned in previous lectures, the collaborative approach is a little bit more complicated,

[00:07:40] a bit more complex than the imperative versions that we have either with cube cut commands or with configuration files. And that's because there might be some complex merging and applying log logic here behind the scenes. That being said, when we take into consideration some of the limitations of the imperative approaches,

[00:08:00] then this declarative one here becomes an important one for us to leverage for us to be able to update our CNIS objects. Let's scroll down clear the console here. Let's change into the object management folder once again, just so that we know where we are. And now let's delete here so we can delete once again,

[00:08:20] we can pass multiple files here or we can also pass the whole directory. This is going to delete both our pod and service and we can confirm here that we have just the alpine pod up and running. I believe that's a great moment for us to take a short break. So I'll see you in the next lecture in a few minutes.