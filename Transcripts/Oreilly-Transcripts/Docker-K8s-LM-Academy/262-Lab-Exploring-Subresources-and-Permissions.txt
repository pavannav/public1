[00:00:00] So far, we have given pod admins the possibility of execute any action in pod resources. However, we have not explored what or how subresources come into play here when we want to get specific types of information from our pods. So let's come back to the terminal here

[00:00:20] and let's have a look at our engine xpos. So if we were to run engine X here, get pod X and the name space would be dev we will get this engine X spot here. However, the moment we wish to actually get the logs of our engine X pod, we're gonna get a forbidden error. It's like, OK, wait,

[00:00:40] I thought I had permissions to actually get stuff related to the pod. But as it turns out as we already discussed in the API, the logs is considered a sub resource of pods. That means we need to explicitly set permissions for users to be able to access the logs.

[00:01:00] And this actually makes sense, right? Maybe we want to allow developers to start pods, maybe deploy some applications, but maybe there is some logs there or there are some specific container pods that we don't want to allow access to logs and this would be done by just omitting here, the pods logs from the resources section.

[00:01:20] So let's have a look at the API call that is actually done behind the scenes here. And let's add the dash dash V equals eight to our request. And if we scroll up a little bit more here, you will see that we get this get request here. Now, what this get request is doing is it is basically telling us which API which http rest API endpoint

[00:01:43] from our Cober API server will be hit behind the scenes by cube. So as you can see here, we get the API V one name spaces DEV pods and gene X. And then we get a forward slash log here. This forward slash log is what is considered a sub

[00:02:03] resource. And we need to set permissions explicitly to this log sub resource if we were to try to exact into this container as well. So cube cut exact, let's say here that's gonna be engine X and then the name space will be DEV and we would like to get a running shell.

[00:02:21] We will see that we have no permissions here and you can see that we get exactly this answer that user Alice cannot create this resource pods forward slash exec. So we need to provide this in this, this permission here. Also in our cluster role.

[00:02:40] Let's then come to our cluster row and let's set this here, we're gonna say pods forward slash log and then pods forward slash exec. And we will also add here pods forward slash attach. And this would enable us to actually get this interactive shell into the container. Let's save this.

[00:03:00] And now remember Ellis does not have permissions to apply the role, right? So if we were to try here to apply the pod admin dot YAML, we got an error. Alice cannot do that. So we need to change to our Minicube master user here or the Minicube super user that we have and then we can apply the changes here and the role will be configured

[00:03:23] when we now change to Alice back once again. And we try to get the logs of our engine X container, then no problem, right? Then we are able to access the logs. And if we were to also try to exact here into the container, then no problem. We also get our shell here and we can just run our commands, right? So

[00:03:43] as you can see here, there are also some cases where just giving permissions to specific Cnis object is not enough. At the end of the day, we might also need to give permission to sub resources. This is not the case with every resource, but it is for example,

[00:04:01] with pods or if we also wanted to allow deployments here deployments and then users to be able to scale deployments, then we would have to say deployments forward slash scale, right? So this kind of sub resources or operations under nested paths here might

[00:04:20] have to be also authorized or explicitly listed in our role or cluster role definition. Perfect. That brings this discussion around sub resources to an end. We can just delete here. Let's know that we have permissions. Let's delete our running pods. And that's a great moment for us to take a short break and I'll see you in the next lecture.