[00:00:00] Hello and welcome back. Let's discuss Docker volumes in a little bit more detail. So volumes allow us to persist data beyond a container life cycle. Basically, we can persist data both beyond the container as well as persisting data to share it with other containers that exist simultaneously.

[00:00:22] So Docker volumes act as managed directories or files and they are separate from the container file system which ensures that our data remains safe and accessible outside of the container as well. So there are a few benefits of using volumes. The first one is actually the most obvious one and it is data persistent.

[00:00:42] So we can persist the data even if the container stops or it is removed, we can also share data. We can share data between multiple containers by sharing or by using the same volume by mounting the same volume. We can also use volumes for backup and recovery. Since the data is actually separate from the container's file system.

[00:01:04] If the container goes down, the file is still or the data is still available, and we also get some added benefit since we can now decouple our data from our containers, we can achieve more flexibility in managing and deploying our containers by separating the data from the application

[00:01:22] run time. So there are a few types of volumes that are available in Docker. And one that I'm gonna tell you, we are not going to cover is the anonymous volumes because they are just not often used, they are automatically created by Docker and they don't really have a name that enables reusability. So they are very hard to manage, right?

[00:01:41] So they they are really used for temporary data that doesn't need to persist. And this is not really achieving anything special for us. So the two important ones are the first one here bind mounts which allow us to directly link files or directories from our host system to our container.

[00:02:01] And a use case for this is for example, for real time development updates, we will explore how we can use bind mounts to actually have a development container for a react application that is actually looking at our local machine and is automatically refreshing the application with hot reloading.

[00:02:20] We then get to what you could actually call rio volumes in Docker. So that name the volumes that we can create and manage via the Docker cli and then we can reuse them across containers. So this here is really the use case for persisting data. Perfect. I know that you're super excited to jump into the practical exercises. I am too.

[00:02:43] So let's take a short break and come back in the next lecture.