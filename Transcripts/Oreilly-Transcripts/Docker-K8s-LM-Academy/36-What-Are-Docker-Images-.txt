[00:00:00] Hello and welcome back. We have been running Docker images for quite a while now. So I want to dedicate a little bit of time to explore in more details what the Docker images are and what they actually contain. You can think about Docker images as the DNA of our container. So basically, they are a blueprint that's gonna be used by Docker

[00:00:22] to create new containers. Every container that is created based on an image will have the exact same shape at the beginning of that container lifetime. If you exact into the container and change something there, then yes, it's gonna become different. But at the startup time,

[00:00:40] every container is gonna be the same if they are using the same image. So images are self contained, read only 10 plate that encapsulates everything that we need to run our application. And if it doesn't encapsulate everything we need to run our application, then we should actually revisit the design of the image because the intention of

[00:01:00] an image is to encapsulate everything that we need to run our application. The first thing the base layer here, this base image is often a minimal Linux distribution, you will see very often alpine popping up or a more fully fledged one like Ubuntu, we then have our runtime environment. For example,

[00:01:20] the Python runtime environment or node Js or Java required by our application, we then have our dependencies, right. So here, let's say we have node Js applications. Node is roughly the same depending on the version. Maybe you need a different version, but node needs to be there for all the node JS applications

[00:01:41] while the libraries and dependencies, they are dependent on whatever you are developing. So if you're developing an express application, then you would have express the dependency. But if you are developing, I don't know something else. And JS application, then it would probably have a different set of dependencies, same thing with other languages, right?

[00:02:00] So if you're developing Python, then these libraries and dependencies here would be the installed versions of what is in your requirements dot the XD file for example or and if you're using Java with Maven, then that would be what's in your palm dot XML file. So these are just basically the dependencies of your application. Then on top of this, we have our application code.

[00:02:21] So we need to actually have the source code in case of interpreted languages like Python and no Js or we need to have the compiled binaries. Let's say you're working with go with Java and you need to provide the compiled application code. So then you would also have that inside of your container. And then if there is some base configuration that

[00:02:42] you would like to already ship with the container, then you would also add this here settings for your application and its environment configuration might be a little bit trickier to package inside of the image because this varies very often depending on which environment you're running the the containers on.

[00:03:00] So I'm just gonna leave this as a kind of side note, right? But this is also a valid thing to package within the container if there is a use case for it good. So images are really a snapshot of your application and its complete runtime environment. You can actually create an image by taking a snapshot of a container at any point in time.

[00:03:21] So let's say you start some container and then you exact into the container, you install some dependencies and then you model some files and you go and you take a snapshot and you actually create an image based on that current state of that container. This is possible, but I would not recommend you to do that because you would always need to replicate steps.

[00:03:41] Whenever you would like to change the state of the container for that, we have a much better way that to use Docker files. And then once again, we're going to explore that a bit later on. So the default rule here, images are created whenever you need to create your custom image, they are created via Docker files and the Docker build command.

[00:04:04] But anyway, I'm getting ahead of myself here. Let's come back, come back, Claudio, please. Let's come back to the lecture. So images can be sourced from multiple locations. The Docker hub is a very common location. It's probably the biggest Docker or the biggest image container repository that is out there.

[00:04:21] It offers a vast collection of images for various purposes. But you might also work and you probably will also work with private registries. Whenever you need to fetch images that are proprietary to the software your company is developing, right? So let's say that you have a Docker image, you want to ship that. Of course, I'm gonna, you're not going to publish it to a public repository in

[00:04:42] Docker hub because that would give everyone access to the image. No, you just want your organization, right? So if you have proprietary software, then private registries will provide that for you. And as I mentioned, already building our own images here, we can basically leverage Docker files and this

[00:05:01] is really the real power of Docker here, we can create images that perfectly match our application's requirements and then publish them in private registries and then store them in the different environments we're working on. Perfect. So that's it for a little bit of a more deeper dive into what are the different components of images.

[00:05:21] And we will really see this repeating itself over and over again as we build custom images, we're gonna start with the base layer. We're gonna make sure that we have the necessary runtime environment. We're gonna install our libraries, we're gonna compile or copy our application code and we're going to do some default configuration with ports and exposing some stuff in the container

[00:05:43] and that's it. And then we have the startup command at the end. Perfect. So hopefully this clarifies a little bit what is entailed in actually creating and what is contained in an image. Let's then take a short break and come back in the next lecture.