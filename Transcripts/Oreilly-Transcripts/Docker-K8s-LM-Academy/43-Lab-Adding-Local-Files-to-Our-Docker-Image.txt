[00:00:00] Hello and welcome back. Remember that when we created and customized our entry X container, we also changed the contents of the index dot HTML file that was stored within a certain path inside of the container. We would also like to do that here in this Docker file. But instead of editing the file directly,

[00:00:21] what I would like to do is to create a new index dot HTML file here in our Docker file engine X folder. And then use the Docker file to copy this file from my local machine into the image that is being built for us to do that. Let's just retrieve the index dot HTML file that is sent by GX.

[00:00:42] We can do that either by Googling if you wanna just go to Google and look for how the index dot HTML looks like. This is also a possibility, but we can also run a container here in detached mode. Let's map the port 80 to 80 here. And this is gonna be the web server image that we have just created.

[00:01:02] Once we do that, we get our container here and we can just execute AC command to once again localhost and this is gonna give us back the HTML file. I'm just gonna copy everything here that is sent to us. Just gonna copy this and go back to the ide. If you wish, you can also dump the results of the core command into an index dot HTML file.

[00:01:23] I'm just gonna do the a little bit more manual work here. So let's create an index dot HTML and let's paste this file. Perfect. So this is looking good and you can also look for visual studio code extensions. If you wish to have some HTML syntax checking or additional functionality here.

[00:01:42] If you do not install anything, then you also be on the very safe side. No worries. Let's just remove here the things that are in our HTML file. I'm gonna leave one H one tag for our title and one paragraph here just to describe what we are doing. So let's say here,

[00:02:00] welcome to my custom Docker file based engine X, right? So this is the very first message that we're going to print on the screen. And the paragraph here, we're just going to say this file was copied from our local machine

[00:02:20] in two a Docker image, right? So now here, whenever you're copying a file from a local machine, we are now let's say copying the file from this directory here. However, whenever you're running this on a CI CD for example, then that's just gonna probably check out some repository and copy some files from that repository into the Docker image.

[00:02:43] Another important aspect for us to remember is the path where the index dot HTML file is stored. So we can add comments here with hashtags and I'm just pasting the path here. So once again it's forward slash user slash sharex HTML and then index dot HTML.

[00:03:00] So all we have to do here with the Docker file. And as you can see here, once we type C, we already get some autocomplete, it is the copy command. So the copy command specifies a source and a destination, the destination as you might get is just our user sharex HTML, index dot HTML file.

[00:03:21] And the source here will simply be index dot html. This is all we need to do to copy a file from within the current directory into a specific folder or destination here within our image. So this file will actually be stored then inside of this folder in our new image.

[00:03:42] And this is going to be actually reflected in the container whenever we start it up. Now I keep saying from our current directory. But what does that mean? Well, in technical terms, this would actually be the context of our Docker Bill command. We're going to dive a little bit deeper into context later on in the course for now,

[00:04:00] let's just assume that this is the current directory or actually we're not assuming that this is the current directory. It is the current directory. Because whenever we issue the Docker build command, we pass a dot That means the dot stands for the current directory where we are running the Docker Build command. Great. So now we have everything we need in our Docker file. Let's clear the console here and let's run once again the Docker build command

[00:04:24] and we will leave the tag as it is for now, web server image, you could also add a new tag here in case you wish to have the old one also available for us. We're going to discuss what happens with tags. And whenever we use the same tag, what happens with images that were previously built,

[00:04:40] we're gonna have a lecture dedicated for that for now, let's just run it like this with the dot here. And as you will see, we will get the two C comments that we already had and then the copy command that means now that we have a new image. So let's clear the console and we will see Docker images. And as you can see here, our previous one, this was our previous image.

[00:05:03] This actually does not have the tag anymore and it does not have the repository. So whenever we are using the same tag, this will get moved to the latest image that was built with the Docker file, we can now use this image here. So let's say Docker run, I'm just gonna copy it here

[00:05:21] so that we already have everything. Let's start our container and that is a little issue here because we already we have the port 80 BZ. So let's just run here the Docker PS and let's stop this container here. So I'm just gonna copy this. We're gonna say Docker stop this container and now we should be able to run this new container.

[00:05:43] So stopping the container is enough to free up the port and then we can create a new container. Let's now have a look at the container that is running here. This is our container ID. So if we try to run crawl here within our localhost or just to hit our localhost, we are actually getting a 403 forbidden.

[00:06:02] And the problem here is that the owner of the HTML file that we have created is different than the next user which is running within this container. So we also have to change the ownership of the file that we have just copied to match the NGX user.

[00:06:20] Let's go back to the Docker file here and we can do that by running another command, let's say C ownership engine X colon engine X. So the user and the group here and I'm just going to pass the file path here. Perfect. So we're gonna save this. Let's go back to the terminal. Let's stop our container. So let's stop this container here. Docker,

[00:06:43] stop the container and then let's run the Docker build command once again, I'm just going to keep the same tag here. Let's build our image once again. And you will see that in the end here we get our change ownership. So now let's clear this and let's run Docker run.

[00:07:01] And now here we're going to use the same image that we had so detached in the port. We're going to map to 80 here and the image is going to be web underscore server underscore image. Once we do that, we'll get our container and we can now try to run Curt P colon localhost like so and we're actually getting our content back.

[00:07:22] Awesome. So we were able to fully automate all the operations that we ran manually except for this change ownership here. This we did not have to do explicitly because we were executing the commands from within the running container. So the user was already the correct one. However,

[00:07:40] whenever we are developing here locally and then we are pushing the changes or copying them from our local machine, then the owner is actually different. That means that in some cases we're gonna start getting some weird errors and the message, the 43 message, the forbidden one that we get was not very clear that the problem was actually ownership, right?

[00:08:00] So this is some engine specific configuration and for us here changing the owner was enough. Great. So let's just remove this comment here since this is not needed anymore. And congratulations on completing this Docker file. Hopefully, this helped you understand a little bit more how to start building Docker files and how to really just automate the commands that would be run manually.

[00:08:22] Otherwise, let's then take a short break and come back in the next lecture.