[00:00:00] In this lecture, I would like to quickly discuss with you one way that we can generate a configuration file from a, from an imperative cube comment. So let's say that here, we are not really sure how the configuration file should look like. And we would like to get some information here from Cnis that would guide us to actually write this configuration file

[00:00:22] in the terminal. There is a very useful flag here that we can use whenever run the cubic run or expose commands so that we can transform this imperative commands into configuration files. Let's say that we now want to run a certain pod here. Let's use our color API example.

[00:00:40] And the name of the image here is going to be LM academy color or actually forward slash color API. And the version we will use is 1.0 0.0. Now, instead of running this command, we can pass a flag called the dry run flag and then pass set,

[00:01:00] dry run to client. And then we can output this as a yo file. What this will do is it will basically run entirely in the Q cut utility. That's what the client here means it means this request never goes to the server. When we output as a YAML file here, then this will give us the configuration file that would be then

[00:01:23] used here in effect or effectively behind the scenes by this command. Let's run this here. And the moment we do that, we see that we get something very similar to what we have created in the previous lectures. We have the API version as well as the kind here set correctly. We also have a couple of labels and the name set for our pod here, the color API pod

[00:01:46] and we also get the containers array, the containers list populated for us. We get some additional fields here that we have not discussed. But the bottom line is that we could use this strategy here to generate these files and then keep the important fields for us,

[00:02:02] save it as a file and then simply use the cube Carol create and then pass the file name here. We can also confirm that this works with the expose command. So if we were to run cube Cole expose and now here we would need to pass a valid pod. So the engine X dash pod, let me just confirm that this is up and running with cube

[00:02:22] cle get pods. Perfect. Yes, our engine X pod is here. So let's run cube Cole exo pod. Genex pod. The type here will be node port, the port will be 80. And now we can simply say dry run equals client.

[00:02:41] And we will output this as a YAML file. The moment we do that, we get once again our API version and the kind correctly set here, we also get the labels and as you can see it correctly identified the label here that we have set in our pod. And it has populated both here as well as here in the selector.

[00:03:01] And the selector one is the important one, right? So this selector one here is the one that we or that the service will look at. It will look for pods with this selection criteria here and it will redirect the requests. So this can be very useful whenever you are not entirely sure how to create these files or whether we you need to have these files created quickly,

[00:03:21] you should definitely check documentation to make sure that everything is correctly set here. But this is just a quick way for us to actually migrate to move away from using this imperative commands to actually leveraging configuration files. That's great. Let's then take a short break and come back in the next lecture.