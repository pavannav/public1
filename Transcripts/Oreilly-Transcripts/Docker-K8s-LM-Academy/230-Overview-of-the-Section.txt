[00:00:00] Hello and welcome to this section about configuration management in Kubernetes. Let's have a look at the topics we will discuss. We will start with presenting the native options from Kernis for configuration and sensitive data management and these are the config maps and the secrets. We will then move on to exploring and working with config maps.

[00:00:20] We will create and manage config maps in Cnis and we'll also learn how to pass data from config maps via environment variables or via mounting them as volumes in containers. Once we get a good grasp of config maps, we will move on to working with secrets. We're going to do pretty much the same. We're gonna see how we can create and manage secrets in Cnis.

[00:00:40] What is the difference between a secret and a config map from Cnis perspective? We will also explore how to pass data from secrets via environment variables and how to mount secrets as volumes in containers. I'm sure you can't wait to get started. So let's jump into our next lecture right away.