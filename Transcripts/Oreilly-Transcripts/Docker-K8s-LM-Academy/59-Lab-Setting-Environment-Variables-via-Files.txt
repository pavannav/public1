[00:00:00] Hello and welcome back in the previous lecture, we saw that we can pass our environment variables here in the Docker run command by just using the E option and then passing the key which is the name of the variable. And then we pass the value of that variable. Now you can see that this command is getting actually fairly long here.

[00:00:22] And as we add more environment variables, this will get even longer. There is another way for us to handle environment variables or multiple environment variables. And that is by using dot files, another multi for files is that we can quickly differentiate between environments.

[00:00:41] For example, let's say that we want to run a container here by using some development configuration. The port is set to 8080 the app name would be like development application. And then we want to run another container with a production configuration. And then we would need to remember exactly which part the production is running on.

[00:01:03] And what is the app name for production to avoid having to always remember these values or write them somewhere else and then go look there and pass here in the command. Docker run, we can leverage once again dot M files. Let's then switch to the terminal here.

[00:01:20] And before we create our first m file, what I would like to do is to immediately add a line to the dockerignore file. We hardly ever want to bundle M files inside of the Docker image. And there are several reasons for that. But the most important one is that files, they can actually contain sensitive information like a P

[00:01:42] keys. So bundling it inside of the image would actually cause some security breaches or could potentially cause security breaches because anyone could create a new container based on that image and just explore the file system to extract this information. There are also other reasons why we wouldn't want to

[00:02:01] bundle a specific dot M file with the image. For example, if we have different configurations as we already mentioned to development and production, then it's not really clear which one we should bundle, right. So it's better not to bundle anything here and then just pass it at runtime. So I'm just gonna exclude everything. So a recursive exclude, exclude here

[00:02:21] and we will also ignore any dot M file and anything that comes afterwards. So this would potentially exclude dot m dot prod. This would also exclude dot m dot dev. This would also exclude, for example, something like this env forward slash dot m

[00:02:40] dot prod for example, so or we could say something like N prod forward slash dot env. So this will also be excluded, right? This is just gonna match everything here, recursively. That is great. So let's now create our first dot env or dot m. So we're just gonna call this dot m dash prod or dot m dot prod here.

[00:03:03] It doesn't really matter. Both of them will be excluded. Let's copy this port and app name. We're gonna copy this here. We're gonna paste it here and we're gonna change the port of our production application to 9000. We haven't used the port 9000 yet.

[00:03:20] I'm just gonna say here my prod app now we do not need to rebuild this image. Remember we are actually excluded this ENV file here from the context that gets sent to build the image. So it's not going to have any effect whether we build the image again or not. We're just going to reference this dot ENV

[00:03:41] dot PROD in the Docker run command. We are now here back at the terminal. Let's cancel. Let's not run this Docker run command here. Let's now run another Docker run and then we can just simply press M dash file and the M file here is gonna be dot ENV dot PROD. We're gonna start a detached mode and the port here is 9000.

[00:04:02] We're gonna map to the container port 9000. The name will be just express dash prod and the image will be express at the moment. We do that we can confirm here by running the logs, Docker logs express prod. We can confirm that our server is listening on port 9000 by running once again the C

[00:04:24] command HTTP colon forward slash localhost call on 9000. We can just ops 9000 here. We can just confirm that we are getting a hello from my prod app. Another comment is this double codes. They are now included here.

[00:04:40] We had to include them here because of spaces whenever we were setting the value explicitly here. But if we switch to the ide, we can now actually remove this double codes from here. We can ignore this syntax highlight index because the ide is actually matching this to a shell script. But that is not really the case.

[00:05:01] Let's now copy this actually instead of creating a new file, we're just going to copy paste here and let's change this to M dot DEV. And now the DEV here is going to be port, let's say 3000 and I will say here my Dev app perfect. Once we save this, we can now come back here. Let's clear the console.

[00:05:20] Let's just make sure we have no other containers running on port 3000. It is free. So now let's run our command once again. And the end file here is going to be dot m dot DEV. And the port that we need to map is actually port 3000 to the 3000 of the container. Oops, I forgot to change the name here.

[00:05:40] So let's change express product to express DEV. And once we do that, we get the container running, we can once again confirm with the container hash that the port is correct and we can confirm with the co that the application is taking my DEV app from the correct environment file. That's great. So passing a dot ENV or dot M file is a faster way for us or it's a more structured way

[00:06:05] for us to both list many environment variables as they grow as the number of environment variables grow. Using A dot M file is a more structured way for us to define all these variables in one place. And it is also a

[00:06:20] way for us to keep the Docker run command a little bit more readable since it doesn't become a huge command, whether you will use this exact solution in production or not, that is highly dependent on how you are deploying these containers. And I'm not gonna discuss that. Exactly right now. I just want to show you the possibility using dot M files is fairly common for development.

[00:06:42] So this is definitely one way for us to leverage them and pass them to our containers whenever issuing the Docker run command. Perfect. That's it for exploring passing environment variables to our containers. Let's then take a short break and come back in the next lecture.