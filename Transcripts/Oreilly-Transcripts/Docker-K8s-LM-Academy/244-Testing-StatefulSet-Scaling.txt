[00:00:00] All right. So we got our MongoDB stateful set up and running. We can confirm that our pods are up and running here. That's looking good. Now, I would like to explore with you what happens when we scale this to more pods, we can simply run the cube cut scale command here. We're gonna scale a stateful set

[00:00:20] and that's gonna be the MongoDB dash, stateful set and the name and the number of replicas, let's set this to three. So the moment we scale this, we can once again have a look here Cuga pod and we'll see that it has added two more pods here. Now, the thing is that if we were to now exact into our existing pod here,

[00:00:41] so let's exact into our MongoDB SS zero here with the Mongo shell, then we should still be able to run all the commands here. So let's run the use color db, right? Use color db. And if we were to say here db dot Test Collection, I believe that's how we called it

[00:01:01] dot Find, then we should get an unauthorized error. That's perfect. We have not authenticated. This requires authentication. So we can just authenticate with our color db, call db user and the color db, color db password like. So I believe that's the correct one. Perfect.

[00:01:21] And now when we run the command here to get the objects of our collection, we can confirm that they exist, that's totally fine and it's working as expected. Now, let's exact into the first or actually the second pod that was created. And the moment we do that here, then we can confirm once again. So if we were to say, show DBS,

[00:01:42] we will get an unauthorized error. That means that we are initializing the root username and password as expected. And let's now use our color db database here. Let's now try confirm once again that we need to be authorized. So let's just copy and paste the command here. That's also looking good.

[00:02:01] Let's then authenticate into the database. Let's paste it here and let's now try to retrieve our test collection. Now, what do you think is going to happen? Will this a new pod share the storage? It shared the data from our first pod or will this command here return an empty array?

[00:02:21] Let's find out. So let's copy this and let's paste it here. And the moment we do that, we actually get nothing back, right. So this is actually the expected behavior of our stateful sets. So let's exit the mongo shell here and let's have a look at our persistent volume claims.

[00:02:40] And in this persistent volume claims, we have a different persistent volume claim per pod, right? So this we already know and this is bound to a different persistent volume behind the scenes. So if we look at minicube SSH here, and then we change into the temp directory and then host path and

[00:03:00] then the provisional default here we have as you already know three folders. And that means that the data from the first folder is completely not shared, it's not shared at all. And the data from the other folders, they are completely independent from one another. In other words, the three pods that we have running here,

[00:03:21] they are basically three different completely independent instances of MongoDB databases, whatever operation we run in the first pod will not be reflected by default in the second nor in the third pod, there are ways for us to set replication within Mongo DB. And we would then enable this kind of replicating the data across different pods,

[00:03:45] but this is not enabled by default. So I just wanted to use this as an example for us to explore the how the persistent volume claims and the underlying persistent volumes, how they are independent from one another. Now, of course, here, if we were to actually delete our pod MongoDB SS zero,

[00:04:04] like so this will be recreated, right? So it will be immediately recreated here, you see that it's three seconds old and if we were to exact into the spot, so let's go with EXEC once again here. And now let's exact into the first spot.

[00:04:20] Let's use caller DB, let's authenticate here. So call DB, user, caller DB password and then let's get everything from our test collection. So test collection dot find, then we will get the object back, right? So this is once again

[00:04:41] due to this stable storage identity that we get from stateful sets. And that's the perfect use case for a database system, even if the pod enters into a crashed state or it is restarted for whatever reason or deleted and recreated, or we need to roll out a new change, a new version of Mongodb here.

[00:05:00] Let's say we want to roll out an update, a patch, a security update at these pods here, they will be recreated and they will still have the same underlying persistent volume and persistent volume claim backing up the data. Let's then exit our Mongo shell here. And before we wrap up this lecture, let's just do a little bit of cleanup.

[00:05:21] I would like to scale back our stateful set to having a single replica here. And now this is not just it. So we still need to have a look at something that is not deleted by default, whenever pods in a stateful set are deleted, and these are the persistent volume claims. So even though we have scaled this back to one replica,

[00:05:43] and we can confirm that with get pod here, we have a single pod running. If we were to scale this out once again to three replicas, the underlying volumes of those replicas would still be here, right? This is a very good thing because it means we can persist data also between scaling of a certain state for set.

[00:06:02] So we can reduce the number of pods here and then we can scale out whenever necessary. And the underlying persistent data is still saved. In our case here, we know that we want to delete this persistent volume. Let's just run cube cut, delete PV. And let's pass both the second and the third one.

[00:06:20] Sorry, that was a little Typo should not be PV, but PV C, right? So we are talking about persistent volume claims here and that's what we want. That's perfect. So we can also confirm that our persistent volumes, the corresponding persistent volumes they were deleted here. We have only the persistent volume that is assigned to that persistent volume claim here.

[00:06:42] We can also confirm that the the directories were deleted in our minicube host. So let's come here to minicube host path provision or default. And if we list, we can confirm, we have only the the folder here from our first pod. That's great. So this is the clean up that I wanted to do just before wrapping up this lecture.

[00:07:03] We can confirm once again, we have just a single pod up and running and I hope this discussion was interesting for you, for sure. It was for me, I'm enjoying very much discussing these topics with you and I hope you are enjoying the course so far. Let's take a short break and I'll see you in the next lecture.