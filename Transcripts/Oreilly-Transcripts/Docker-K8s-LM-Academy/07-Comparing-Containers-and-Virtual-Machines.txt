[00:00:00] Hello and welcome back. Let's briefly discuss containers and virtual machines and understand their similarities as well as their differences. Let's start with virtual machines or virtualization. When we use virtualization, we start with a hardware layer as always, right. So we do need to have some CPU and some memory. We need to have some hardware, some computer, some data center

[00:00:22] and we also need to have some host operating system that's gonna be the base operating system. And on top of this, we have something called a hypervisor. The hypervisor is really the key piece of software or hardware here that enables us to run these virtual machines side by side. Once the hypervisor is in place,

[00:00:41] we can then start creating and running our individual virtual machines. One important component of the virtual machine is that each virtual machine, each VM has its own guest operating system. If you launch these two instances in AWS, for example, then you know that whenever you start with an AMI that AM has one base operating system,

[00:01:02] most of the times that's going to be some Linux based operating system, but it does come with its own operating system. So you can see here that when we have virtualization, we do have two operating systems, we have the host operating system as well as the guest operating system. And the hypervisor is really acting as a

[00:01:21] layer of abstraction here and translating the instructions that are coming from the VMS into instructions that the host operating system understands. The hypervisor is also doing more behind the scenes. So it's not just translating instructions, it's also ensuring that resources are properly allocated and that there is a correct or proper isolation between the VMS.

[00:01:42] But just here for us to understand the structure of the system, it does its key characteristics or its main points. Here are really that there is some hardware host operating system and then the different VM S they are managed through a hypervisor. Let's now jump to containers. And as you can see here, the beginning is very similar to virtualization.

[00:02:03] So we do need some hardware, right? The containers need to be run somewhere and there has to be some CPU there has to be some memory and we also need some host operating system. The difference here is in the upper part. So instead of having a hypervisor which will translate guest operating system instructions to the host operating system,

[00:02:22] we will have a container engine which will be responsible for managing our containers. Now, instead of packaging an operating system, an entire operating system inside of our containers, we can actually get rid of it and package just the code and its dependencies because there is no additional operating system running inside of the containers.

[00:02:43] This contributes to having faster and leaner containers. Now we do lose a little bit of isolation here because the containers are actually all running inside of this container engine and this is directly on top of the host operating system. So virtualization does provide a stronger isolation than using containers.

[00:03:05] However, containers do enable us or they do allow us to configure a lot of isolation rules. So this is not something that we have to completely let go of. Awesome. So let's compare a few features of virtual machines and containers side by side.

[00:03:21] First and foremost, in terms of isolation, the virtual machines, as already mentioned, they do provide a stronger isolation. Since each VM has its own operating system, they are completely isolated from each other. While in containers, we do have process level isolation. That means that the containers, they are sharing the host operating system kernel,

[00:03:42] but they are running in isolation of each other. And this isolation is managed by the container engine. When we talk about the size or overhead of having each of the solutions, the size of virtual machines is larger and they have a larger footprint because of the operating system.

[00:04:00] And the virtual hardware that they need to manage containers are considerably more lightweight since they have minimal overhead. And they share the kernel with the host operating system. In terms of portability, the VMS, they are actually a bit less portable than containers because they might be tied to specific hypervisors or some configuration

[00:04:21] in the operating system for containers. They are really fully portable containers should be platform agnostic. If they are not platform agnostic, then they are not well designed. So they should really be platform agnostic and look exactly the same from the outside. So I should be able to run a container in my machine as easily as you should be able to run one in yours.

[00:04:43] So let's have a look at when to use each of them virtual machines would be recommended if you need a stronger isolation between different environments. Or if you're dealing with some legacy applications that maybe you can containerize that easily. If you also want to replicate a complete system environment for testing or for development,

[00:05:02] then virtual machines might actually be the choice here. You should focus on containers if you are building cloud native applications. And if you are leveraging Microservices, if you need to scale your applications quickly and efficiently, and if you also wish to leverage portability

[00:05:20] across different environments as a top priority. So hopefully this gives an overview and it makes it clear that these two technologies, they are not mutually exclusive. It might be the case that for some parts of your application or of your system, virtual machines will be the choice to go while for others, you will leverage containers that being said they both have their strengths

[00:05:42] and their weaknesses and it's important to be aware of them, to be knowledgeable of them, to be able to make the right decision. Awesome. Let's then take a short break and come back in the next lecture.