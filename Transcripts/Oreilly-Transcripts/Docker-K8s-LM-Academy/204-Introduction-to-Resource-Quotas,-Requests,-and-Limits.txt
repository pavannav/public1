[00:00:00] Let's now talk about resource quotas, requests and limits that are important whenever we want to make sure that our pods and whatever objects we're creating with Cnis have enough resources available for them to run smoothly. Resource quotas requests and limits. They allow us to define how much CPU memory and storage the objects might use.

[00:00:23] Now, these are basically two sides of the same coins. They are used to ensure that our resources are distributed fairly among applications. And they can also prevent a single application or name space from consuming excessive resources from the nodes and potentially leading to resource starvation

[00:00:43] and then pods being killed or slowed down. So there are two sides of the same coin. Basically resource quotas. There are policies applied at the name space level and they define hard limits on the number of resources such as view, memory and object counts here.

[00:01:00] For example, pods that can be consumed by all objects within that name space. On the flip side of that, whenever we want to run a pod, for example, inside a name space that has a resource quota set, we then need to define requests and limits for the resources consumed by that pod. They are defined at the container level within the pods.

[00:01:23] So if you have multiple containers within a same pod, we would need to define requests and limits on a container basis. And they specify how much of each resource a container needs requests. They focus on the minimum amount of CPU or memory. A container should always have available after scheduling

[00:01:42] and the limits, they specify the maximum amount of CPU or memory that a container is allowed to use a resource quota definition looks something like this. So let's say we have two name spaces. One is the DEV name space and the other one is the prod name space. And then we will create a resource quota object per name space.

[00:02:04] We can define a name for the resource quota and we can bind this resource quota to a specific name space. We can then specify here in the spec session. How much are the hard limits for the requests dot CPU and requests for memory

[00:02:20] and limits dot CPU and limits dot memory. These are just examples, there are also other configuration options and you can set these limits for other settings as well. On the right side here, we have one example of how we could set the product resource quota. For example, to make sure that the product name space has six times more resources available than the DEV name space.

[00:02:44] This would ensure that whatever pods or objects are running inside of our product name space, they would have all these resources available to them. And if pods within the DEV name space, start to consume too much, this would not impact the pod name space.

[00:03:01] Let's then have a look at how the scheduling would happen here. So let's say that we have a pod which we wish to deploy on the DEV namespace. We look at the request CPU memory limits as well and we can see that since there are no pods scheduled. Well, we do have 500 millis here of CPU available

[00:03:21] here. We are specifying with the extension M here. So notice the extension M and that's basically the smallest unit. It stands for millicores here and we could also use fractional or decimal values if we were not specifying the M suffix here. But I do find it good to specify M especially if you are using less than one VCPU.

[00:03:45] If you're also using one or more as long as it doesn't get really confusing. If you start using 100 VCPU, then it might be hard to visualize using M here. However, for our use case here, I just want to show you that this is possible. So 500 millis are available here. We have 1000 available.

[00:04:01] That's the first part one GB byte of memory request is also available. We have two here and then we also have available 1000 million for the limits and two GB bytes for memory. So it's all good. This pod gets scheduled and starts running and then we move to the next one

[00:04:21] also in the name space de but now let's say we just want 200 million, 512 mebi bytes, 400 million of limits and one GB byte. This is all good. So if we add up these two lines here, we would get 700 million. This is still under the 1000 of requests. We will get 1.5 GB bytes here. That's also below the two GB bytes.

[00:04:44] And the same thing for the limits for both CPU and memory. Now, let's say that a third pod comes and this pod actually would like a 400 millicore of request VCPU. However, whenever we add all these three values here,

[00:05:00] we would go over the request CPU quota that we have specified in our resource quota as a consequence. This pod does not get scheduled in this name space. It will be shown in a state of pending. Same thing if we go over limits. So let's say here we have three GB bytes already reserved for these two pods.

[00:05:22] And our limit for memory is four GB bytes. If a new point comes here that wishes to use two GB bytes or wishes to set two GB bytes for limits here, then this is not gonna be allowed and the pod once again will not be successfully scheduled. It's important highlight that if the resource quotas are set for name spaces,

[00:05:43] then pods must specify requests and limits otherwise Cobern might not work correctly. All right, there is a little bit of math involved here, but it's nothing really fancy. It's just making sure that there are enough resources in our name spaces as defined by the resource quota

[00:06:02] so that the pods can be scheduled and run successfully. Also, of course, the estimates here they must or they should let's not say must, but they should be done based on some real data. Maybe you do a little bit of stress testing and some expectations around the load, how much load your pod is expected to handle

[00:06:21] how many requests and maybe how much work it's supposed to do. And then based on that and based on some synthetic traffic, you could then monitor how much CPU and memory is necessary and then you add a little bit of buffer just in case and you could come up with this value some approximation. So this doesn't need to be an exact, very exact science here.

[00:06:41] But the recommendation is also not that you set these things blindly because this might actually impact other pods, right. So if we start setting values here, that end up being too high, if we have many pods running, then we might be reserving resources that we don't actually need

[00:07:00] and then we will end up hitting resource quota bounds here. And new pods will not be able to be scheduled. Awesome. Let's wrap up this lecture. Take a short break and come back in the next one for some practical hands on.