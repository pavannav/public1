[00:00:00] Let's now explore pod security standards and pod security standards. They allow us to leverage different security levels to ensure security best practices in pods. So basically pod security standards, they offer a set of guidelines and they define different security contexts for pods. They can be used to ensure that a pod adhere to specific standards,

[00:00:23] reducing the risk of potential vulnerabilities and of misconfigurations. So this is not some thing that we can create easily on our own. And this is something that is already part of CORS I believe since version 1.25 or something like this, at least as a stable version. So

[00:00:40] for sure if you're following this course in any newer Cobern its version, then you should be good to go. There are three pod security standards that are available for us and basically the different profiles here, they provide different levels of restrictions. So the privileged one has no restrictions whatsoever. This is

[00:01:00] the least restrictive policy just allows full access to everything. If you need applications to manage the cluster or to perform some advanced administrative tasks, then you would probably use the privileged one, but this should definitely not be used for your applications. The baseline one provides a very good compromise here between security

[00:01:23] and flexibility and it enforces a minimal set of security restrictions and it is suitable for general purpose workloads. So this policy, it allows common configurations that most of the configurations you will come across. But it also prevents, for example, privileged escalation like mounting sensitive host paths

[00:01:42] or by modifying the host network settings, right. So the baseline here would be what you would normally want to enforce at least for general purpose workloads. That being said, we also have a risk restricted profile. And personally, I would say, well, why not start with the restricted profile and then see if the

[00:02:02] pod cannot comply because of some very well justified reasons here, then probably move to the baseline one. So the restricted one, it enforces the strictest set of security standards. It is designed for applications that are running in a highly sensitive or regulated environment. And then we would need to have these controls here enforced.

[00:02:24] Now, there is a difference here between the profile and the action that is taken in Cnis. So let's understand that as well. So this pod security standards, they are enforced via what's called a pod security admission controller. As I mentioned, this is already activated by default in the later

[00:02:45] Cnis releases. So anything from 126 forwards, I think that would already have this activated. If it's not, you can always activate this feature manually. So the the controller is enabled by default, we already know that. And this admission controller,

[00:03:01] it uses labels for configuration and it is set on a name space basis. So the label looks like the spot dash, security.co dot IO forward slash a certain mode and then a certain level. So the mode here that we can set is enforced. So there are three available modes.

[00:03:20] The first one is enforced and the enforced one will block the creation of pods that violate the security standards. We then have one which will show something on the cap cater facing warning, but it will still allow the pod to be created and then we have audit which will log a security violation in audit logs but still allows the pod to be created.

[00:03:43] The available levels here are the ones we already discussed. These are the profile sort of privileged baseline and restricted. That means you can enforce a baseline for example, but you can also warn a restricted or let me rephrase that you

[00:04:01] can enforce a baseline profile here by blocking pods that violate this. But you can also warn whenever a pod violates a restricted level or a restricted profile. This is also a good compromise to know that we can deploy our pods without major issues.

[00:04:20] But we will also get warnings if we are violating this restricted profile. Now, uh pod security standards, they are not enough by themselves, right? So we should combine them with other security mechanisms such as role based access control, to properly enforce the access rules to name spaces that accept privileged workloads. So let's say that we have our acute system name space

[00:04:41] where there's a lot of stuff that manages the cluster running. We then combine this with our back or broad based access control to make sure that only those that have to have access to the cube system namespace actually have the access. And last but not least it is possible for us to configure this built in admission controller to set cluster wide defaults.

[00:05:02] And then we can override these defaults at the name space level. So we can be, let's say very restrictive. If we really want to set a very restricted default for the entire cluster, we can go with restricted and we can override at the at the name space level and say like OK, in this name space, I'm gonna allow baseline,

[00:05:20] but I'm gonna warn restricted, for example. So these are all possibilities that you can explore how to configure. Perfect. Let's then take a short break and I'll see you in the next lecture.