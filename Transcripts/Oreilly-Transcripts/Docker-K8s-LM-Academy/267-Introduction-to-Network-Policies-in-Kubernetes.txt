[00:00:00] Let's now explore network policies and network policies allow us to regulate ingress and egress traffic for pods. So network policies, they define how pods can communicate with each other and with other network endpoints. This also includes external traffic to and from the pods,

[00:00:21] we can specify rules for both ingress and egress traffic. So far in the course, we haven't specified any network policies. So by default, all traffic between pods is allowed in certis. So far, we were able to communicate not only with pods between or within the same name space

[00:00:40] but also with pods across name spaces. So if you need to isolate this traffic or if you need to set some boundaries, network policies provide one solution. They are not the only solution, but they are one solution that we can use. And it is a co native solution. Even though we do need to have a container network interface plugin that supports network policies,

[00:01:03] they are one solution to isolate the pods. And then as a consequence, reduce the attack surface in case a pod becomes compromised. So in order for network policies to have any effect, the container network interface plugin, that we use in the cluster must support this feature.

[00:01:20] There are several solutions and some of them include calico solium and weave net. Now a network policy includes three main configuration pieces. The first one is which pods the policy applies to. So this is something that is mandatory, we need to inform which pods the policy applies to.

[00:01:40] We can configure it so that it applies to all the pods. But we can also be very specific here, how we set this option depending on the policy. If it is an ingress policy, then we can set the ingress rules to govern this policy. And once again, if it is an egress policy, we can set the egress rule. Now,

[00:02:00] I'm marking this as optional here because it might be the case we have ingress only policies and then we would not have egress rules or it might have only our egress only policies and would not have ingress rules. We can also have policies that define both of these in the same policy. So here are two examples of policies.

[00:02:21] Let's have a look at how these three elements here, these three main configuration pieces they match to these policies. So the first one is the pod selector, right? So under the specification of our network policy, we will specify which pods this policy applies to. We can then define the

[00:02:40] ingress rules here, you can see that the policy types is a list and here we have included only ingress. So then we define an ingress rule here which specifies from which pods traffic is allowed as well as which parts these pods that are matching this condition here can communicate to. So in our case here,

[00:03:00] we are effectively selecting all the mongo DB pods within this name space here. And then we are saying, hey, traffic is allowed into these pods only if it is coming from the pods that belong to our back end application and only if they are communicating with port 27,000 17,

[00:03:22] we can then set the egress rules as well. And this is here the flip side of the coin. So as you can see here, we have selected, we allowed traffic coming from the back end. And here we are selecting or we are defining an egress policy in our backend pods. So we have selected all the pods that have the app back end label here.

[00:03:42] We have defined the egress policy and this policy does. It says you can communicate only with the database pods. And here I believe there's a small Typo. This should actually be Mongodb here based on this label. So let me just quickly fix that. That's looking better and you can communicate only via the port to 27,017.

[00:04:05] So if we were to send some traffic that does not match this condition here, this traffic would not be allowed to go out from our pod as you can see here, we are using pod selectors to define the, in this case here, the ingress the sources of allowed traffic and here

[00:04:23] in the egress the targets of allowed traffic. But there are multiple ways that we can actually configure the rules. So pod selector is one that we have been using with both match labels and match expressions, name space selector. We can also use to set rules at the name space level

[00:04:40] and we can use match labels and match expressions. And here we cannot name or we can mention the name space by name, but there are certain system generated labels that have the exact same information. Basically the name space name and then we can just use them here. We can also use cidr blocks and configure IP blocks for both source and target traffic.

[00:05:03] And we can also specify as we already saw the ports and protocols that we want to allow traffic to flow. One important element of defining our network policies is that different list elements under the from or two conditions, they behave as an or condition.

[00:05:20] While if we have multiple selection options under the same list element, they behave as an end condition. So here is one example, this is a single entry under the from configuration option. And this single entry here specifies two conditions. The namespace selector with a label that has a name of staging

[00:05:44] and the pod selector which has a label app with the value of Frontend. So this will allow ingress only from frontend pods inside of the staging name space. This is different from this other condition here.

[00:06:00] This condition has two elements under the from configuration option. The first element is effectively allowing traffic from the entire staging name space. And the second one is allowing traffic from whatever pod has this app Frontend here. So these conditions they are different and it is important

[00:06:21] for us to understand that the first one here, it it behaves as an end condition. And the second one here because these are two different elements of our list, it behaves as an or condition perfect. So that's it for this introduction to network policies. Let's now take a short break and come back in the next lecture to start working with it in Minicube.