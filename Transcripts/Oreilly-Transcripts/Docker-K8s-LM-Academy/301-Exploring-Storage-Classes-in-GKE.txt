[00:00:00] Now that we have created our first objects in our GKE cluster. Let's continue with exploring a few of the features that Google Kubernetes engine offers for us as managed features. So remember from the discussion that we had around persistent volumes and persistent volume claims and storage classes

[00:00:20] that when we are using minicube. So let's just switch here. Config use context minicube like. So so that when we are using Minicube, we get a single storage class here. So get storage class and this storage class is a very simple standard storage class which uses

[00:00:41] the host path volumes to dynamically provision persistent volumes in our cluster. Now, when we change this to a managed cluster, either from Google cloud or from Aws or from Azure, what we will actually get here is quite a few or are quite a few interesting options for storage classes.

[00:01:01] So let's just change here back into our Google cloud cluster, the demo cluster here by just running the gcloud container clusters, get credentials, demo cluster and then passing the region here. If you already have this set up in your cube config, then you can also might also be able to switch

[00:01:20] the context here and then you don't need to run the cloud. Nonetheless, I do find that running this command is always helpful here to make sure that you have the latest data available. So now we are, let's have a look here, Cubot, get or config current context. So now we are inside of our

[00:01:41] GKE autopilot cluster. Let's clear the screen and let's now get our storage classes. And as you can see, there is a lot more information here. This is not very easy to read. So let's just filter or let's just retrieve a few columns specifically here. So the way for us to do that is to actually use. So let's run here storage class

[00:02:04] and is to actually use the option for outputting the result here as a custom columns and just make sure that this matches this exact format here. And then we can define which exact columns we want to retrieve and we need to inform where the value of these columns are present.

[00:02:23] So the name column is under the metadata dot name field. As already as you're already aware of it, any cated object has this metadata name field here. So you can just map this value from our E ML definition file or

[00:02:40] from whatever object data there is available in the cluster to the column name. We also want to retrieve the provisioner and the provisioner here is actually under a top level field provisioner, right? So the moment we do that here and I have a small Typo that should not be equal, that should be column. So let's see. Yeah, that's looking much better. Now, we can spend a little bit of time

[00:03:03] just quickly discuss the different storage classes and how they actually relate to underlying Google cloud offerings. As you can see, there are many different options here, standard and premium options. There is also an enterprise option and there are both read, write

[00:03:21] one and read, write many right. So you can see here that when the option changes from read, write one to read, write many the provisional changes, right? So here it is using a persistent disk from the compute engine offering product of Google cloud.

[00:03:40] While the readwrite menu here, it uses a different provisioner because the persistent disk one here it does not support the read write many mode. So if you use a premium readwrite menu or a standard readwrite menu here, then it will behind the scenes use the file store provisioner.

[00:04:00] This is a network file system from Google cloud and this does support the read write menu requirement. You can also see here that there is a difference between premium readwrite one and standard readwrite one, right? So Google Cloud differentiates between the two here and even though the provisioner is the same,

[00:04:22] let's now jump into the details of these two storage classes. So we're gonna say storage class and then we're gonna provide here. So instead of that, let's describe the storage class and we're gonna provide the premium read write one. And then we will see here that the parameters the type is set to persist in this SSD.

[00:04:43] If we were to now describe the standard read write one, we will see that the type of the disk that will be used is different. So it's not the high performance SSD, it is a lower performance tier, but it also gives more cost savings here. Another thing that you can see here is that both

[00:05:02] the standard readwrite ones and the premium readwrite ones, they have the reclaim, reclaim policy set to delete. That means if a persistent volume claim that is leveraging this storage class here is deleted, then both the persistent volume and the underlying

[00:05:21] disk that was backing up this persistent volume, they will be deleted. Additionally, the volume binding mode here is weight for first consumer and this simply means that the persistent volume and the underlying disk they will not be created when the persistent volume claim is created, they will be created first when a pod is

[00:05:42] scheduled and will use that persistent volume claim, right? So this just makes sure that we can create the persistent volume claim without actually creating the disk already in the background. And then this will also save us some costs because we will not have idle disks.

[00:06:00] So basically, the disks will be created when the pods will actually want to use this persistent volume claim before we continue with the implementation of our project. Let's just explore here a little bit by typing GK storage classes. And you will then come here to this page storage for the GK A cluster overview. I'm gonna just minimize here on the side.

[00:06:22] And you can see here that it mentions actually the different options that we were using, right. So here we find that balanced persistent disk which is suitable for standard enterprise provides a balanced performance and cost. And we also have the performance persistent disk, which is the one that is used in the in the premium tier in the premium storage class.

[00:06:43] And this is this should be used for performance sensitive workloads, right? So this is backed by more expensive S sds and it is faster basically. So that's the difference here between the two persistent disks. We also have other things here,

[00:07:00] maybe the more cost effective one here if you wanna use hard disk drives and you can also use, however, you wish to configure your own storage classes, you can also reference the persistence disks here behind the scenes. Even if there is no default storage class that is offered by Google cloud, you can always create your own and mention the necessary persistent

[00:07:23] disk class here based on the needs of your workload. So feel free to go through this page. They provide a lot of details around the different storage options that you have here. And if expand this, you can also see how you can use the different Cnis concepts here to leverage the different storage classes and the

[00:07:43] different types of storage that are offered by Google Cloud. So hopefully, this gives you some inspiration over how you can actually go through the documentation of Google Cloud and understand a little bit better how the different code objects, these primitives here that we saw like persistent

[00:08:01] volume claims and the persistent volumes that they create how to actually map to offerings of Google cloud itself. This is really also very good illustration of what a managed service really means. Because behind the scenes here, just by creating a cluster in GKE, we already got all this integration here with other Google cloud services.

[00:08:25] And so if we click here on Google cloud, let's come to the console here, then we should see that we have here, let's view all products and then we should also see compute engine here and then under storage, we have the disks option here.

[00:08:40] And this is really where we're going to start seeing the disks that are created by our persistent volume claims. So this kind of integration is one of the greatest one of the biggest benefits we get from using a managed solution from a specific cloud provider. Before we move on with our project implementation. Let's just take a short break and I'll see you in the next lecture?