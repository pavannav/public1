[00:00:00] Let's start exploring how we can create and manage pods using the cube cut utility for that. Let's make sure that Kubernetes is up and running. So let's just run cube cut version like so and we should get some response from the server. We can also confirm that our current context is set to minicube by running cube

[00:00:20] qle conf current context. And this is gonna give us the cluster that the cube cat utility is talking to remember. The cube cat utility is just a cli utility that is translating the commands that we issue here into API instructions that are then sent to a certain Cobert cluster.

[00:00:40] The context is defining which cluster we are actually talking to. So it's important to make sure that we are talking to the minicube cluster so that whatever we spin up or whatever we delete is just done within this cluster here. It might be the case that you are already interacting with Cnis in your machine and you might be deploying some workloads in remote

[00:01:02] Cnis clusters. So to make sure that we are interacting with the correct cluster, just make sure that you have your cube cut context matching the minicube context or actually pointing to the minicube cluster. If this prints a different value, not a minicube, then you can update the context by running cube

[00:01:22] catal config set context minicube like so and this is gonna make sure that the cube cat utility will now be talking to the minicube cluster. Great. Let's start exploring how we can create pods here with the cube cat utility. And the command that we use most of the time is the run command.

[00:01:43] As always, I do like to explore the run or whatever command we use with the C with the help flag here so that we can get a bit more explanation of what the command is doing. This is also a great practice. If you're not sure whether you are, for example,

[00:02:00] using the correct flags here or if you are unsure about what each flag is doing, you can see that everything is described here. And if we scroll to the top, we also get several examples. You can see that there is a very simple example here, starting an engine X pod and then it also starts providing a few more complex examples here. So you can take your time to go through this

[00:02:21] and explore the different options that we can use with the run command. Let's start with the simplest one. That's the one at the top here. I'm just gonna copy this and we will spin up a pod that has a single engine X container. So let me just copy this command here. Let's scroll down to the bottom. I'm going to clear the screen actually.

[00:02:41] And let's paste the command here for the image. I'm going to set the tag for the engine X to be 1 27.0 to make sure that we all are working with the same engine X image. Since we will be doing basically no operations here with engine X. It's not really going to make a difference here if we use a later version.

[00:03:01] But once again, just for us to be on the same page, I always like to provide the image, the version or actually the tag that I'm using. Awesome. Once we run this command here, we can now confirm that the pod was created by a cube cut, get pods command and you will see here that our engine X pod is in a ready state.

[00:03:20] It is running, it had no restarts, which means we have no errors here and it's roughly 30 seconds old. That's great. So congratulations on spinning up your first pod. As you can see here. The structure of the command is fairly straightforward. If it is a simple command here, we will use the cube cut and then the run, we're gonna pass the name of the pod. This is the pod that we want to use

[00:03:44] or the name that we want to use for the pod. This is printed here in the name column. And then we pass with the image here we pass the image name X as well as the image tag. We would now like to start exploring inspecting and understanding how to manage this pod more closely.

[00:04:02] But before we do that, let's take a short break and I'll see in the next lecture.