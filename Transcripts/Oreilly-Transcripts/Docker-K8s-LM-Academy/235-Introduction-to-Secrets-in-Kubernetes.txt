[00:00:00] Hello and welcome back. Let's spend a few minutes to discuss secrets in Cnis and secrets. They help us decouple and inject sensitive data into pods. They can be used to store this and to store and inject this sensitive data into containers and they help us not have to include this sensitive information in application code.

[00:00:22] Now, it's important to highlight that the data is stored in base64 encoded format and it is unencrypted by default. We can set encryption at rest for secrets and this is recommended best practice. But anyone who gets access to this base64 encoded string

[00:00:41] will be able to decode this with a very simple shell command and get a hold of the real value of the secret. So don't rely on having base64 encoded and saying, hey, this is encrypted. It is not true. Anyone with access to the string can get the human readable version of the secret. It is best practice to set up role based access control rules with least privileged

[00:01:05] permissions so that only the authorized parties can retrieve or update the values of secrets. Now accessing secrets is similar to accessing config maps. So basically they they work very similarly to each other from the pod or container perspective.

[00:01:20] And they can be passed via environment variables as well as via files in volume mounts. In addition to generic secrets which basically contain arbitrary sets of key value pairs. Kernis also has other specific secret types. Examples are service account tokens or TLS secrets or

[00:01:40] Docker registry secrets and there are a few others. So if you come across a secret that has a type opaque, that means this is a generic secret, which is expected to have just an arbitrary set of key value pairs. And last but not least whenever you are working with a specific cloud provider and their managed

[00:02:01] offering, then they normally provide native integration with secrets managers. So in the end, this leads to an improved end to a more secure secret storage. This is how a secret definition would look like. And as you can see here, I'm just using a base64 string. If you were to copy this and paste in

[00:02:21] your terminal and then run the base64 decode command, then you would get the actual human readable value of this. You can see type opaque here and then we can use this secret just as we have also used configmaps and the keys from within configmap. So the username key is mentioned here

[00:02:40] and the name of the secret has to match the name of the secret here. And also they need to be in the same name space So whenever we want our pod to access the secret, it needs to be in the same name space as that secret. All right. So that's it. For this short discussion around secrets. We're gonna take a short break and then we will come back with the practical lectures to start exploring how to work with secrets in code.