[00:00:00] Hello and welcome back in this lecture. I would like to add our script that we have created to initialize our key value database and set the role here for our keyvalue user to be able to both read from and write to our key value database. Let's as before, create here, a new folder that's going to be called TB

[00:00:20] config. And the name of the script here will be our mongo dash init dot Js. The script will be exactly the same as before. So if you have it handy there, you can just copy it. I'm just gonna quickly type that process dot m dot key value DB. Then we have our username as well. So let's just copy paste here. These two

[00:00:43] that's gonna be user, that's gonna be password we then have here user and we have password here, password. And the first thing we would like to do is to just get our database. So get sibling DB, that's gonna be the key value db.

[00:01:02] And once we get our sibling database, we can then say DB dot Create user and that's going to receive an object. The user will be our key value user, the password will be and the password here is PWD. The password will be key value password.

[00:01:21] And then the rows here will be an array of rows which will contain an object where we will specify the row, that's going to be a read write row. And the DB here will be our key value db like, so that's all we need to write for the script. And now we actually need within our compose

[00:01:40] YAML to create a bind mount a volume here that will map the script from this folder to the respective and correct folder inside of our container. Let's then create a volumes key here. And then we can either use the shorthand where we're going to specify the path. So the B

[00:02:00] config mongo init dot Js and then we're going to use the column here. And this will specify the container path, which is Docker entry point initd BD mongo init dot Js. And then we can set the read

[00:02:22] only permissions here. This is one way of specifying the volume. This is how we have also done with the Docker cli whenever we issue the Docker run command. However, there is a more explicit way and I do tend to favor that because it just favors readability. We can specify here the type of the volume and that's gonna be the bind mount type.

[00:02:45] We can then specify our source and the source will simply be the dot forward slash db config forward slash mongo dash init dot Js. And then we can specify the target here as well. And the target will be our container path here. So the path inside of our container

[00:03:05] like so I do tend to find this syntax here, easier to read. And it also allows us to quickly differentiate the type of the volume. We don't need to look too close to the syntax. We can just directly identify that this is a bind mount.

[00:03:20] We can also set here the read only flag to true if we wish to mark this as read only. Perfect. So let's see whether our script here will be executed successfully for us to have a look at whether this will actually output any logs. I'm not sure whether it will or not, but we can give it a try. So let's say

[00:03:40] initializing and I will say here, key value DB user. And so just something like this here just for us to see whether we're gonna get this keyword in our logs or not in our terminal. If you were to run Docker compose PS here, we will get nothing.

[00:04:00] But if we were to add a dash A, then we will see that our container is actually still existing for us to be on the safe side. Let's just remove this by running Docker compose. And then we can just type here down. The down command will basically destroy all the different pieces of infrastructure.

[00:04:20] That are created by this Docker compose project that would include also networks but not volumes. We will discuss the C commands here a little bit later down the road. But just if we were to run Docker compose down, we will see that the compose DB one was removed and the network composed default were also removed.

[00:04:40] If we were to run Docker PS or Docker compose PS, then we do not get any containers back. Let's then run once again a Docker compose up. And once we do that, we will then create all the containers and the default network as well. Let's see if we find here somewhere. Let's do a search here and we're gonna type initializing. Perfect.

[00:05:03] We can see here that we actually get our log line. So this information has been printed to the console and we can confirm that our script was actually picked up by this container here and it was executed. Let's then give it a try to see if we can connect here with the Docker

[00:05:21] run command and let's go up a little bit more on the history here. Let's see if we find the command that we actually pass the credentials and the database. So here, the only thing that we need to change, we do now need to use the compose db dash one here como

[00:05:40] DB one. This is the name of the container from Docker's perspective. And we also need to use the Compose default compose default network. Once we do that, we will see that we also get some logs here. And the logs that are coming is actually saying here, checking authorization failed. And this is just saying not authorized on admin to execute whatever command is specified here,

[00:06:04] right? So we are not authorized on the admin database. That is good, but we are authorized in our own database. If we were to just say show collections here, then we will actually get the collections since there is nothing here, then it's just printing an empty string.

[00:06:20] Amazing. So we were able to also wire up an initialization script. Let's exit this here. I'm just gonna close this new terminal and also stop our containers here. There are still quite a few topics for us to explore including networks and volumes in Docker compose. But before that, let's take a very well deserved short break and come back in the next lecture.