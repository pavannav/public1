[00:00:00] Let's have a closer look at how empty door and local volumes work. So empty door is pod level storage here. It is considered ephemeral by the definition between ephemeral and persistent. So this is defined at the pod level and the volume is created when the pod is assigned to a node,

[00:00:21] it is created and accessible only from within the pod. That means we cannot have an empty door here that is accessible or that is created in one pod and then trying to access from another pod. Now, I'm talking pods and not containers, right? So containers within the same pod, they can all access this volume here.

[00:00:41] As long as we mount these volumes within the containers, the volume is initially empty and all the containers in the pod can read and write the same files in this empty door volume, they might also mount the volume in different paths, right? So the actual end path here doesn't really matter. So I mean end path inside of the container,

[00:01:02] the we we can use different paths for that when a pod is removed from a node, then the data in the empty door is deleted permanently. It will be gone, right. So you should never use this for persistent storage, local volumes. They are persistent by definition in comparison to FM

[00:01:21] here, that means the data is persisted beyond the pod life cycle and local volumes are defined at the node level. So the cube scheduler then it knows how to assign pods to nodes based on the affinity constraints defined in the persistent volume configuration.

[00:01:41] So when we create local volumes, we need to specify some node affinity configuration, we will see how to do that. And then Cnis will take care of assigning pods that use these volumes to the nodes that match or to the nodes that have the volume available. So setting a persistent volume,

[00:02:00] node affinity is mandatory when using local volumes and like other persistent volumes, it does require creating persistent volume claims so that the pods can use the storage. One small remark which might be useful to keep in mind is that local volumes only support static provision. So we need to create them,

[00:02:20] we need to create persistent volumes before we create the claims for them to work properly. All right. So just to summarize here, very important to keep that in mind because keeping the data life cycle in mind is very important. The empty door volumes, they follow the pod's life cycle. That means once the pod is gone, the data is gone,

[00:02:41] the local volumes, they follow the nodes life cycle. That means if for whatever reason your node is gone and it is replaced with a new node, then the data will also be gone. So none of this module, none of these modes here, none of these volumes is actually recommended for production workloads for storing persistent data.

[00:03:01] If the data can be lost, like if it is OK to lose data, then you might consider such a solution. But normally in production setups, you would not use empty door nor local volumes except if you have the the use case for them, right? Which is here like really you want to store

[00:03:20] some temporary FMR OData and share between containers. Then by all means go ahead with empty door or you want to have some also data stored in the node or maybe you want your containers to read some data from the node. Then local volumes here can also be used. All I want to highlight is that just keep in mind the data will be gone

[00:03:40] if the pod is deleted here or if the node is deleted or replaced here. Awesome. That's it for an introduction to empty door and local volumes. Let's take a short break and come back in the next lecture.