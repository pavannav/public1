[00:00:00] Now that we know how to work with stateful sets and statically provisioned volumes. Remember we did create persistent volumes here manually by using the local volume type. And we had yes to enter into minicube, create these different directories here and set everything up so that this would work as expected, we can now experiment transitioning from the statically provisioned

[00:00:23] persistent volumes to the dynamically provisioned persistent volumes. And as you might have guessed, this is a very simple migration in our statefulset file. All that it requires is to either comment out this or completely remove this line here can remove that like so or we can just change the storage class name to standard.

[00:00:43] I do prefer to keep the storage class name here explicitly mentioned for the very simple reason that if we do not include that and then we change which storage class is default behind the scenes, then there might be unexpected behavior here. So if we just fix the storage class name,

[00:01:02] then we can rest assured that the storage class that will always be used is the one with the name standard. So just to confirm here, this standard name is the name of the storage class. It is not the fact that it's set as default. And the fact that it's set as default is represented by this default here next to its name,

[00:01:23] right? So now if we use this storage class name, then the persistent volume claims they will lead to the creation of the underlying persistent volumes. So all we have to do here and just make sure that you are inside of the same folder, stateful sets. Let's apply then the stateful set file.

[00:01:43] The moment we do that, Cnis, we want to create two replicas ready to remember here that we have two replicas. They are now up and running. That's all good. And now let's get our precision volume claims. And we see that we got two persistent volume claims, the names of our persistent volume claims they are composite.

[00:02:01] So this is the metadata dot name key that is coming from here. And then we also have the pod name here. And this stable index at the end of the pod name is really what allows Cnis to also identify that. Hey, if the pod called demo Stos set one is recreated, then we will link this persistent volume claim here with the pod,

[00:02:24] right? So just to clarify if we were to describe our pod, so let's describe the pod demo dash SS zero like so then we will see that under our volumes here, the local volume is the name of the volume.

[00:02:40] The type is persistent volume claim. The claim name is this one and this is then behind the scenes, this persistent volume claim is bound to a persistent volume that was dynamically created for us. Let's clear the screen here and now we can also sage into minicube. So let's as age into mini cube here. And then we can confirm also that the folders

[00:03:02] under our host path default were created successfully. We have them two folders, one for each of the persistent volumes. And we can then start persisting data. Another important aspect here is that when we delete a stateful set, so let's just delete our state full set once again,

[00:03:21] then the persistent volumes and the persistent volume claims they are not deleted, right? So CUCA get persistent volume claim, they remain alive and they are still bound to the underline to the same underlying persistent volumes. And this makes sense because stateful sets, they are by definition state for applications.

[00:03:40] And if we were to, I mean, or if CORS were to immediately delete our persistent volume claims and the underlying persistent volumes, then we would basically just lose this data, right? So we can definitely delete them manually, so we can delete them with the cube cut, delete persistent volume claim. And then we can pass the name here or we can also

[00:04:02] pass the all flag like so to delete both persistent volume claims, we can do the same with our volumes So keep cut, delete persistent volumes all and here the persistent volumes they were automatically removed because that is the reclaim policy that is set in our storage class standard,

[00:04:21] right? So if we have a look here, let's describe, oh here, perfect. The reclaim policy is already here. So that's the delete reclaim policy. That's great. So we have migrated our state for set to working with dynamically provisioned persistent volumes. Another nice migration here for us to practice a little bit more and explore a little bit more the functionality of CORS

[00:04:43] and of Minicube. Let's come back here to the IDE. I just want to make sure that the directories inside of Minicube are deleted and sometimes they are not. So the clean up sometimes doesn't happen. And let's have a look whether they were removed. Perfect, they were removed. So we are good to go.

[00:05:00] Let's then take a short break and I'll see you in the next lecture.