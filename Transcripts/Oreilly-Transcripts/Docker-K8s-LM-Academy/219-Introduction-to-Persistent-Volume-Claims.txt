[00:00:00] Great. Before we dive deeper into working with local volumes, let's just discuss persistent volume claims because they come into the picture. Now, whenever we want to work with local volumes, and it will also be useful for working with a variety of different volumes good. So persistent volume claims is what allows us to

[00:00:22] claim durable storage and actually use them in pods. We cannot use persistent volumes directly in pods, we need to go via claims. So the claims here, the persistent volume claims are actually what reserves the persistent volume. So here PV stands for persistent volume

[00:00:42] or that creates and reserves A PV. And this really boils down to whether we are using static provisioning. The PV. The persistent volume must exist beforehand. But when we are using dynamic provisioning, then it might not exist beforehand. It doesn't have to, it can also be created as a result of a claim being created.

[00:01:03] So claims are bound to a single persistent volume. Very important here. That's a 1 to 1 relationship is not many to, one is not one to many. It is 1 to 1. If a persistent volume is bound to a claim, then it cannot be bound to any other claim.

[00:01:21] And if we have a persistent volume that has 10 gigabytes and the claim needs only one gigabyte, then there will be nine gigabytes of excess storage in this persistent volume that cannot be used by other claims. This is very important to keep in mind and that's where this is particularly important for static provisioning, right.

[00:01:42] So dynamic provisioning would look at the request from the claim and it would create volumes according to this request. So it's less of an issue. But when we are using static, when we are creating the persistent volumes beforehand, then this is something we should be mindful of.

[00:02:00] So we can specify we can set specific criteria in a claim so that only the persistent volumes that match this criteria are considered for binds. And also let's once again remind ourselves that we should be mindful of the possibility for extra unused capacity, particularly with static provisioning.

[00:02:21] So there are a couple of reclamation policies. Once a claim is deleted, basically Kubernes needs to answer the question, what happens with the persistent volume, right. So we used to have this recycle here that's currently deprecated and dynamic provisioning is encouraged as you might imagine

[00:02:40] recycle here is really just scraping, removing the data and making the persistent volume available. Once again, we have retain here to begin with. So retain will retain the volume, it will not delete the volume. So if the persistent volume is dynamically provisioned and we set retained as the reclamation policy here,

[00:03:03] then if the claim gets deleted, the volume will still be maintained. And we can either have a look at the data, clean the data ourselves. I don't know, but we can just retain the volume. We also have the possibility of deleting the volume and this is the default, right? So when we dynamically provision things

[00:03:21] and the claim gets deleted, it is only natural that hey, we are going to go and delete the volume as well so that we can also save some costs. We don't have unused volumes. There are also a couple of access modes that might be interesting. So read, write once the volume can be mounted as read, write by a single node.

[00:03:41] So node is the keyword here and not pods, right. So a volume can still be used by any numbers of pods, any number of pods within that node. But it can be mounted as read, write by a single node. Read only many here. The volume can be mounted as read only by many nodes.

[00:04:00] We have read, write many. The volume can be mounted as read, write by many nodes. And we have also a newer one which is readwrite one's pod and the volume can be mounted as read, write by a single pod. So basically, if we want to restrict a volume to be used by a single pod,

[00:04:20] then in our persistent volume claim. Here, we would specify that the access mode is read, write one pod. If we specify only read write once, then there is no guarantee that only a single pod will use that volume. Great. So just to summarize your static versus dynamic provisioning,

[00:04:40] so static is really best effort match based on access mode and size request. So when we create our volume claim, we can say what's the access mode we want and how much size we would like. And then static provisioning is going to look at the existing persistent volumes and it's gonna try a best effort match

[00:05:00] might not be fulfilled depending on what's available. So if we ask for something that is read, write once with five gigabytes, but there is only persistent volumes that are read, write once with two gigabytes, then this new claim will never be fulfilled. So we create the persistent volumes first, we then create a persistent volume claim which will

[00:05:22] hopefully find hopefully find a matching persistent volume. And then the pods can use this persistent volume claim to actually mount the volume within containers. Now over two dynamic provisioning in dynamic provisioning, the storage is provisioned dynamically when persistent volume claims are created

[00:05:43] and if the back end supports what's requested, then this will always be fulfilled, right? So as long as there is space and you can think here cloud storage is most of the time advertised as infinite storage space. So space is not going to be an issue. As long as the adopted solution also supports the specified access mode,

[00:06:03] then the volumes, the persistent volumes will be created automatically and we don't need to do that manually. So here we first create the persistent volume claim and then this is going to lead to the creation of the persistent volume behind the scenes. And if we create a new claim, then this will also create a new persistent volume.

[00:06:23] And then the pods can use this persistent volume claims. And just to highlight we can have pods here using persistent volume claims backed by both static provisioning and dynamic provisioning. That's fine, right. So pods can use multiple persistent volume claims here.

[00:06:40] And if they are backed by either static or dynamic provisioning this from the pod's perspective, doesn't really matter. All right. Amazing. So hopefully the concepts are clear to you. Let's then take a short break and come back in the next lecture.