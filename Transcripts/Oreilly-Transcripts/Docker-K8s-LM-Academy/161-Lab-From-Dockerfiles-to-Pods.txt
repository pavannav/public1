[00:00:00] In this lecture, we will see how we go from a Docker file and building our images in our local machine to pushing them and then running them in Cnis with pods. If you skipped the previous lecture, make sure that you have the code available locally. This can be retrieved either from the github repository or from the resources of this lecture.

[00:00:22] And I just would like to quickly walk through the code. Very simple code. So we have a single source directory. This is an npm application we are using express here to serve our requests. This application is a very simple express application that has a single endpoint for now, just the root endpoint here.

[00:00:41] And whenever we hit this endpoint, it returns some very basic HTML code with a color set here for our header. We have also written a Docker file to build images for this application and we will leverage this Docker file in this lecture. If you're interested in more details, then feel free to go through the package dot

[00:01:00] JSON file as well as the dockerignore. But really there isn't much here to explore at this point in time. This is a fairly simple application, but it will serve the purposes of demonstrating how we go full cycle here, how we go from developing the application to building the image, to pushing it to running it with Cnis. Great. Let's switch to the terminal and let's now build our image.

[00:01:22] So the first thing we're going to do is we'll simply run Docker build. This should already be familiar to you and we will tag this image with our Docker username in my case LM Academy forward slash and then we will provide here the name of the repository or the name of the image here,

[00:01:40] that's gonna be called API. And then we will provide a tag here. In my case, I'm just gonna call this 1.0 0.0. This tag is important. We're going to reference this later on in our Kubernetes pod creation. So make sure that you just keep this in mind if you're using a different number, that's also fine, but just make sure that you are consistent with it.

[00:02:00] The context to build this image is gonna be the current directory. So just make sure that you are inside of the color API directory before running this command. Let's then run this command and wait for the build to complete. And we can now confirm here that the image is available by running Docker images LM Academy. And I'm not sure whether that's gonna be enough here.

[00:02:21] So let's just run the color API So that should be the full name. Perfect. We can, we can confirm that the repository is correct here. LM Academy color API. And that the tag is also correct. We would now like to push this image to Dockerhub. However, for us to achieve that, we need to be logged into Dockerhub here from our machine.

[00:02:42] Let's clear the console here and let's run the command. Docker login. You can either use your Docker id or mail address here and you can use your password. But I would recommend using a personal access token. So I'm just gonna pass my username here and now we're gonna switch to the browser and let's jump

[00:03:01] into Dockerhub to see how we can create this personal access token in Dockerhub. Make sure that you are logged in here so we can confirm that the username is correct and then you can click on account settings. Once you click here, you will see that there are quite a few options. I'm just gonna zoom in a little bit more here.

[00:03:20] Let's scroll down a little bit more. We have personal access tokens here under securities, personal access tokens. Let's then click here. And if you already have a personal access token, it will list the token here and it will give you the option to create another one. We are going to create a token that has all the permissions to read,

[00:03:41] write and delete permissions. And then we're just gonna name this token. However, we wish I'm gonna call this LM Academy local so that I know here that this is being used by my local machine. Let's then click on generate. And after we click on generate, the token is gonna be shown here on the screen. I'm just gonna copy it and paste in the terminal

[00:04:00] back in the terminal. Make sure that you have copied your token and now you can paste it here and press enter. Nothing is gonna be printed on the screen because this is a sensitive value. But if you see the message login succeeded, then you can rest assured that everything went fine. That's great. Now we can push our image with Docker push and then we

[00:04:21] need to provide here the full name and tag of the image in my case, that's gonna be LM Academy caller API colon 1.0 0.0. And it's just gonna take a little bit to upload everything to Dockerhub. That's great. Once the upload has completed, we can also confirm in the browser under our repositories here.

[00:04:41] Let's click on repositories. We can confirm that the image was successfully uploaded. We can find it here and then let's click on color API, let's click on tags here and we can confirm that the tag 1.0 0.0 was successfully uploaded. That's great. Now that our image is available on Docker hub,

[00:05:01] we can simply use its name and tag here to create pods based on it. It's really just like the name, the image and the tag that we have been using. So let's switch to the terminal. Let's clear the console here and let's issue a cube car run. Let's now as a name for our pod. Let's just call this call API.

[00:05:21] And now let's pass the image here. And that's going to be once again, we need to provide our username forward slash the image name and then the tag that we wish to use. Let's then run this here and let's confirm that the pod was successfully created. So as you can see it's already running here, we can also have a look at the logs of our pod. So logs call dash API

[00:05:45] and we can confirm that the application is running and listening on port 80 if we were to now describe our pod. So describe pod color A P, we can get the IP address here. So that is the IP address that we are interested in.

[00:06:01] Let's now run here another alpine container as we have already done. So cube cut, run and I just went a few steps up in my terminal history here. So the command we're going to run is just the alpine command with the image 3.20. We can now use APK dash dash update at

[00:06:20] C if we scroll up a little bit more, let's just copy here the IP address. So let's copy this. And now we can confirm that we can reach the spot by sending AC request to its IP address. Isn't that amazing? So we went full cycle, we coded the application, created a Docker file, built the Docker file image pushed the image Docker hub

[00:06:42] and then we used the image here to create our pod and to run a pod in CORS. That's amazing. Congratulations on completing this exercise before we wrap up the lecture. Once again, I always like to wrap up or to clean up the resources. Let's clear the console here.

[00:07:00] Let's have a look at which pods we have running. So we do have the alpine and the color API pods. So let's start here by deleting. So delete pod and then we can also pass your force through and we're going to delete the alpine pod. Let's then execute this command and it's going to give us a warning that immediate deletion is not going to wait for any confirmation.

[00:07:21] So you can see here that also the pod alpine was deleted much more quickly than if we did not force its deletion. Here, we know we are on the safe side of force deleting a pod. But if you are running this, for example, in a production workload, then you're definitely going to give some some grace shutdown period for the pod to terminate and containers to terminate and exit gracefully.

[00:07:42] But here, since we are just running in a very controlled environment, then there is no problem here. Let's also delete our pod of the color API. It could also be with force here and then we can now confirm that we have no pods running in our default name space. That's amazing. Let's then take a short break and come back in the next lecture.