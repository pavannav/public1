[00:00:00] Hello and welcome in this demonstration, we will explore how to configure and test password based authentication for SSH. Let's get started. First, we will locate the main SSH configuration file which is crucial for controlling how

[00:00:21] SSH operates. Let's open it using the command. So do V I slash etc slash SSH, SSHD underscore conflict. This file contains settings like whether root login is allowed,

[00:00:40] which authentication methods are enabled at the SSH port. As we scroll through notice the commented line permit root login prohibit password by default root login is disabled for password based access but allowed with

[00:01:01] SSH key via prohibit password. If we uncomment this line, it will still behave in a similar way. You can also set this to no to disable root login. Always keep this as no or prohibit password unless necessary. Next look at this line, password authentication

[00:01:24] by default, password authentication is disabled for security reasons. If you need to enable it, just uncomment the line password authentication. Yes, you can also observe a similar line to set public key authentication

[00:01:42] by default, public key authentication is enabled via although it's commented. However, if you uncomment and change the settings like setting public key authentication to know, then you override those defaults and the new settings will take effect.

[00:02:02] Now let's disable public key authentication by uncommenting and changing it to. No, let's scroll up. We will also review the port 22 setting. If required, you can uncomment the line and change the port here to enhance security.

[00:02:23] But for now, we will keep it as it is sometimes there is an include directive in this file that points to additional configuration files. Let's locate it. If this is present, copy the path, open the referenced file

[00:02:42] and ensure the same changes are applied there. Let's do so do V and this file name, we will make sure password authentication is set to. Yes. After making these changes, let's save and exit the file by pressing escape,

[00:03:01] typing, colon WQ and hitting, enter with the configuration ready. We now need to create or update a password for our user.

[00:03:24] Let's set a password for the Ubuntu user with SUDU passwd, Ubuntu. We will enter and confirm the new password once done. The password is updated now let's test our setup first, exit the current session using exit,

[00:03:45] then reconnect to the server using SSH Ubuntu at the rate IP address, hyphen I option is no longer needed as the identity file or the key file is not required for logging in

[00:04:02] it. Prompts us for the password. Let's enter the password which is set as you can see, we have successfully logged into the server using password based authentication and that's it. We have configured and tested password based authentication for

[00:04:21] SSH. Remember while enabling passwords is straightforward, it is not recommended for production environments due to security risks, public key authentication is always the safer choice that concludes this demonstration. See you in the next to explore key based authentication.