[00:00:00] Hello everyone and welcome to another lecture in this lecture, we are diving into advanced Linux commands which are set. And if you're still getting familiar with the basics of Linux commands, feel free to skip this lecture for now. You can always come back to it when you're

[00:00:21] ready to dive deeper into these powerful tools. The set command stands for stream editor and is used for filtering and transforming text. It's a powerful tool in Linux system allowing you to perform text manipulations directly in a stream of data.

[00:00:45] Whether you're working with files or input, pass through a pipe set helps you efficiently perform tasks like text replacement, deletion, insertion and more all without needing to open a text editor.

[00:01:04] The A command is a powerful programming language used for pattern scanning and text processing in Linux environments. It allows operations like searching, replacing formatting and reporting data

[00:01:22] based on the programming language by Yu Weinberger and Kernahan processes text using patterns which is when to act and actions, which is what to do with that data

[00:01:40] arc doesn't require compilation and supports variables, numerical string functions and logical operators. Common use cases include data extraction, text manipulation and output formatting. When you check the details for

[00:02:00] a using man or you will likely open the page for Gaw instead. This is because A and gawk are essentially the same. The difference is that gog is gnu implementation of the programming language offering additional features

[00:02:20] and extensions beyond the standard or let's begin our demonstration with the set command. The basic syntax for using set is set options, script file name. Notice that the script which is essentially the command to execute

[00:02:40] must be enclosed in a single quote. Let's say we have the string, hello world and we want to replace it with hello Linux, we can do this by running eco hello world pipe said in the codes S

[00:03:01] slash slash Linux slash here is how it works. The S stands for substitution, the forward slash is the delimiter. It separates the components of the substitution command. First, you have the pattern to search for which is world in this case,

[00:03:23] then you have the replacement text which is Linux. Now let's move on to a more practical example. We have employees dot TXT file here and we want to replace all the occurrence of Charlie with David.

[00:03:44] We can use this command like this. This will replace all occurrences of Charlie with David in the output. Let's take another example

[00:04:01] here. The word hello appears on both lines of a file and is repeated multiple times on the same line. If we use this command, you will notice something interesting. Only the first H on each line gets replaced with pi

[00:04:20] the 2nd and 3rd occurrences on the same line remains unchanged. This happens because the command without the G flag replaces only the first occurrence of ha on every line to replace all occurrences of hello. On each line, we need to add the G flag like this

[00:04:44] here. The G stands for global replacement, meaning that every occurrence of hello on each line will be replaced with. Let's go back to employees example, we replaced Charlie with David, right.

[00:05:03] However, if we open the employees dot txt file, we will see that the changes haven't been saved. This occurs because the set command only modifies the output display of the terminal and do not alter the file by itself

[00:05:24] to save the modified text. You have two options. One is to redirect the output to a new file. For example, this will save the changes into a new file called updated underscore employees

[00:05:43] or we can overwrite the original file for this. We can use the hyphen I option like this. This edits the file directly replacing all occurrences of Charlie with David.

[00:06:01] In this example, the name Alice appears twice. If we want to replace only the second occurrence of Alice with Andrew, we can use the set command and specify the line number here is how

[00:06:20] the two indicates that the substitution should occur only on the second line of the file. This ensures that only the AES on the specific line is replaced with an but that's not all, you can also specify a range of lines for replacement.

[00:06:42] For example, this will replace Alice with Andrew on both the 1st and 2nd lines. If you want to replace, starting from a specific line, all the way to the end of the file, you can use the dollar symbol like this.

[00:07:02] Instead of displaying the entire result, we can choose to print only the lines where replacements occur by combining the hyphen and option with the P flag. Here is how it works.

[00:07:20] The hyphen and option suppresses the default behavior of displaying all lines and the P flag ensures that only the replaced lines are printed. Once this command replaces Alice with Andrew starting from

[00:07:40] the second line to the end of the file and prints only the lines where the replacement happens. The rest of the file remains hidden in the output as there is only one occurrence of a from line two, we can see only one line is being replaced.

[00:08:02] We can also delete specific lines from a file using the D option inset. For example, this will remove any line containing Eve from the file. Alternatively as

[00:08:20] eve is the last line of the file. You can use dollar D to delete just the last line. You can also specify a range of lines to delete for instance to delete from fifth line to the end of the file, we can use something like this.

[00:08:42] The hyphen option instead is used to specify multiple editing commands to be applied to a file for the given file. You can use hyphen E to perform multiple substitutions or actions simultaneously.

[00:09:01] Suppose you want to replace Alice with Alicia and February with web in the file, we can use something like this. Let's now move to the command.

[00:09:20] The basic syntax of the command is to use codes around a pattern and action like a pattern action file name where pattern specifies what to search for and action specifies what to do with that matching line.

[00:09:42] I have a sample log file here. If we run cat sample dot txt, it will display the entire contents of the log file. Now using a print sample of text

[00:10:00] will give us output that look very similar to that of the cat command because it prints all the log entries in a the print statement is the action that outputs either the entire line or specific columns based on the patterns specified.

[00:10:21] This log contains details such as the date time, log level and the component or module that generated the message. Let's say we are only interested in displaying just the date, which is the first column

[00:10:40] we can extract this by using the command like this here. Dollar one represents the first column which contains the date. Now, if you also want to include the time and log level, which are the 2nd and 3rd column.

[00:11:01] You can use something like this. In this case, dollar one is the date. Dollar two is the time and dollar three is the log level. These are also called as arc variables, just like they said the command you can use or to search for patterns in a file.

[00:11:25] For instance, if you want to retrieve lines that contain the word error, you can use this command. This command searches for lines matching the pattern error and prints them.

[00:11:41] Now, let's say we want to refine our search further. We want to search for pattern error and only printed timestamp, which is the first two columns of each matching line. Here is how we could achieve this using our current knowledge of other commands

[00:12:02] to filter out lines containing the word error and extract the date. We could use a combination of set and cut. This will first filter lines containing error using set

[00:12:20] and then extract the 1st and 2nd columns that is timestamp using cut, the two fields are separated by a space. So cut hyphen D space specifies that spaces are the delimiter. However, we can achieve the same result with a single command like this

[00:12:45] arc can both filter lines based on patterns like set and extract specific fields from the lines like cut in one go with or you don't need to pipe multiple commands together

[00:13:01] which simplifies our workflow and improves efficiency. Now, what if you also want to include the line numbers along with the content, you can achieve that with something like this. Here is what each part of the command does.

[00:13:23] NR stands for number of records, which essentially is the current line number zero represents the entire content of the current line. So the command outputs each line prefixed by its line number.

[00:13:44] Let's say you want to display only the log message, which is the last column to do this. You can use dollar NF which refers to the last field or column in the line. Similar to NR NF is a predefined variable whose value

[00:14:05] is the number of fields in the current record. For example, this command would retrieve the last column. However, the output may not be what you expect.

[00:14:20] This is because by default orc treats space as the separator. And if the message contains spaces, it will split the message into multiple fields. In this case, the last word is considered as the last field, not the entire message

[00:14:41] to fix this and get only the message after the colon, you need to change the field separator using the hyphen F option. The hyphen F option allows you to specify the character that should use to split the line.

[00:15:00] If it just uses space as the separator, the output will be the same as before with spaces acting as separators. But if you use hyphen F with colon where the colon is the separator you would get the message correctly.

[00:15:20] You can also use orc to filter and display lines based on their length. For example, if you want to retrieve only those lines that are longer than 70 characters, you can use this command here is how it works. Length. Dollar zero, calculates the length of the entire line.

[00:15:45] Since dollar zero represents the full line, the condition length dollar zero, greater than 70 filters lines where character count exceeds 70.

[00:16:00] This is a great example of a capability to use string functions for text processing. Remember earlier, we mentioned that a is a programming language, right? It doesn't require compiling and allows you to utilize variables,

[00:16:20] numeric and string functions, logical operators. And much more the length function we are using here is one of the many string functions available in a previously, we demonstrated how to search for the pattern error in the entire log using this command.

[00:16:42] This worked well since the error pattern was only present in the log level column. However, in real world scenarios, the pattern could appear in other parts of the log as well. So what if we specifically check if the pattern error is

[00:17:01] present only in the third column which is log level column? We can achieve this by using an if condition in the command. Here is how in this command, dollar three refers to the third column

[00:17:24] if dollar three equal to equal to error checks. If the value in the third column is exactly error print dollar zero, outputs the entire line. When the condition is true,

[00:17:43] we are almost at the end of this section. Now let's take another example to understand the A command better. We already know that the slash etc slash passwd file contains user account details such as usernames,

[00:18:04] user ids and the default shell assigned to each user. Now what if we want to display only the usernames looking at the file? We can see that the fields are separated by colons

[00:18:21] to extract the user names. We can use a similar command to the one which we used earlier here is how to make this even more useful. We can add a starting and ending statement for better clarity.

[00:18:42] For example, we can modify the command to display a message before and after the output, making it more user friendly. This line prints a header message before any usernames are displayed.

[00:19:00] Print dollar one, prints the first field which is usernames from the slash etc slash passwd file. This line prints a message after all usernames have been displayed, the begin and end blocks in the command are often referred to as preprocessing and postprocessing respectively.

[00:19:26] We can decorate the output records for better clarity using multiple print statements. For example, to make the list of usernames more readable, you could use something like this.

[00:19:43] But what if we want to add additional details like the user id? The user id is the third fleetd etc file. We can display both the username and the user id in a neatly formatted output like this

[00:20:04] here, percentage minus 20 is format specifier ensures that the username is left aligned within a field of 20 characters. This makes sure the output looks uniform even

[00:20:21] if some user names are longer than others. Similarly, percentage minus 10 S ensures that the user id is left aligned within a field of 10 characters

[00:20:41] that concludes this lecture on said or commands. It's important to remember that the functionality of both tools goes far beyond what we have covered here. They are incredibly powerful tools and it would be nearly impossible

[00:21:00] to cover every aspect of them in a single course. The same applies to any command we have learned throughout this course. While we have focused on the essentials to give you a strong foundation, there are always more advanced features to explore.

[00:21:20] We hope you now feel more confident in using said and do thank you.