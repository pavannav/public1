[00:00:00] Hello everyone in this lecture. Let's go through some of the filter commands and see them in action. The grep command searches through files to find lines that match a specified pattern or regular expression grab extends Grab by supporting more advanced regular expressions

[00:00:24] making it easier to search for complex patterns. WC counts the number of lines, words and characters in a file or input helping you quickly gauge content size.

[00:00:40] The pipe symbol passes the output of one command directly as the input to another facilitating command chaining com compares two sorted files and shows their unique and common lines useful for analyzing differences or similarities.

[00:01:03] Find allows you to search for files and directories based on criteria like name, type and size, providing detailed search capabilities within directories. Let's dive into the demonstration with the

[00:01:21] grep or global regular expression print command. It's a powerful and widely used tool in Linux. The primary function of grab is to search for patterns in text files and display the lines that contain those pattern.

[00:01:41] The simplest syntax for the grip command involves specifying the pattern to search for followed by the file name in which to perform the search I'm currently inside the employee's folder. Let me walk you through what's in here.

[00:02:02] First, there are two files details dot TXT, which has all the key employee details like ID name, position, department, etcetera. The file address dot TXT is where you will find the ID name and address of the employees.

[00:02:23] It's important to note that specifying the TXT extension is not always necessary by default if no specific file extension is provided in Linux files are often treated as plain text.

[00:02:42] Next, there is a subfolder called contacts inside it. There is a file named contacts TXT. This file contains the ID name and mobile numbers of the employees.

[00:03:02] Suppose we want to search for the name Alice in the details TXT file. If we run grep with Alice in double codes and all lower case patron, we might not get a match as a is capitalized in Alice in the file.

[00:03:23] This happens because grip performs a case sensitive search by default to overcome this. We can use the hyphen I option to make the search case insensitive. This allows script to match both Alice with or lower case or

[00:03:45] Alice with capitalized A or any case variation in the text. Sometimes we may want to exclude lines containing a certain pattern to do this. We can use the hyphen V option which inverts the search.

[00:04:06] This will display all lines in employees DXT file that do not contain the word Alice for case insensitive exclusion you can combine the hyphen V option with the hyphen I option.

[00:04:22] This ensures that the search is case insensitive. Let me walk you through how we can search for employees in the IT department. We can use the command graph hyphen I I

[00:04:40] details dot txt to search the file details dot txt for the term it, the hyphen nine option ensures the case insensitive. However, there is a catch if we look closely, we might notice that Jane Smith who actually works in

[00:05:03] hr is also being shown up in the results. This happens because Smith contains ID which matches our search pattern to fix this. We can use the hyphen W option along with grip.

[00:05:24] The hyphen W option ensures that the search only matches for whole words. So it will only match the department and not part of other words like Smith,

[00:05:41] if the file is large and we want to see the line numbers where the pattern occurs. We can use the hyphen N option. This will display each matching line along with its line number. Let's say we want to not only to display the line containing

[00:06:04] a search pattern but also show a few lines after it. You can do this by using the hyphen A option in grip which stands for after context. After specifying hyphen Y,

[00:06:22] you also need to mention how many lines you want to display after the match. For example, if we search for the term Alice and want to see the next two lines. After her record, we can use this command.

[00:06:40] If you want to display lines before the match, you can use the hyphen B option where B stands for before context. Similarly to show both the lines before and after the match, you can use the hyphen C option which displays both

[00:07:00] before and after context with the number of lines you specify using this context option can be helpful especially when you're working with scripts and need to understand the surrounding context of a specific line.

[00:07:20] Let's say you want to search for something like Alice in not just a single file but also across multiple files in the current directory. You can do this easily by using Grab Alice dot slash star

[00:07:40] here. The dot represents the current directory and the star means all files in that directory. So this will search for analyze in each file of the current territory. Here we notice that the results are showing

[00:08:02] a from both the details dot TXT and address txt files. However, there is a directory contact and grep is throwing an error because it's not a file to avoid this error. And search only in text files, you can specify the file extension using

[00:08:25] dot TXT. This will search only in dot PXD files and exclude directories like contacts. So error won't appear

[00:08:40] now to search through all files including those inside subdirectories. Use the hyphen R option with grab. This enables recursive searching allowing grab to look through files within directories and their subdirectories.

[00:09:00] This will search for all lists across all files, including those inside subdirectories. If we want to find out how many employees are developers, we can use the hyphen C option with Grip. The hyphen C option doesn't display the entire matching lines,

[00:09:24] but instead returns account of how many lines match the search pattern. Alternatively, you can also combine the Grip command with word count for counting. Let's first take a look at how WC

[00:09:43] word count works. If you run WC on a file line details dot TXT, it provides three key pieces of information. The total number of lines in the file, we have 10 employee records. So it is showing 10.

[00:10:02] Next is the total word count, which in our case is 75. And lastly the total number of bytes in the phi which is 553. If you only want the line count, you can use the hyphen L option with WC

[00:10:22] WC hyphen L details dot DXT. Now let's combine both the grep and WC commands. This can be done using the pipe pipe is used to connect two or more commands where the output

[00:10:42] of one command is passed as the input to the next command. Here is how it works. We use the groupb command to search for developer in details dot TXT with the hyphen I option for the case insensitive search.

[00:11:01] Then we can use the pipe symbol to pass the result of the grip command to WC hyphen L which counts the number of lines returned by the grip search. What happens here is the grep command searches for the pattern developer in details dot

[00:11:23] DXT, the output of the grab which is the matching lines is then passed to WC hyphen L which counts how many lines match the pattern. This is how you can chain commands together using the pipe,

[00:11:40] making the process much more efficient. Grab is often used in combination with other commands like C A LS or find. For instance when you list files in slash user slash bin, you may get a long output.

[00:12:01] If you're only interested in the files that match the pattern app, you can use LS slash user slash bin pipe graph hyphen I this filters the output to show only files that contain apt.

[00:12:22] If you want to count how many files match the pattern, you can combine it with WC hyphen L. In this case, the result might show that 14 files match the pattern. This can be an apt example of using pipe to

[00:12:42] pass output of one command as input to the next. Let's take a real time scenario. Let's say we encounter an issue in the development environment where we need to identify the errors from log files.

[00:13:01] Then we can search within the log directory using the grab command. This command searches recursively for the word error in all log files under the

[00:13:21] log and all its subdirectories, the hyphen R flag enables recursive searching. While hyphen hyphen include equal to asterisk dot log, ensures only files with a log extension are searched.

[00:13:42] Now, if we want to identify errors from all the files within the log directory, but exclude backup files from the search result, we can use the hyphen hyphen exclude option with the grip command like this.

[00:14:01] This command searches recursively for the word error in all the files under slash slash log and its subdirectories while ignoring any file with the pack extension. Now let's explore

[00:14:21] Grip, E grab identical to grab hyphen. E supports extended regular expressions enabling complex patterns without escaping which is ideal for advanced matching.

[00:14:40] We have the file named context dot dxt inside a folder called contacts. And this file contains phone numbers, the phone numbers follow a specific pattern. They start with plus one followed by a hyphen, then three digits,

[00:15:01] another hyphen three more digits and finally another hyphen followed by four digits. Now, if you're familiar with regular expressions, you know that we need to escape the plus symbol because it's a special character in Regexs. So we write a regular expression like this.

[00:15:23] So what's happening here? The issue lies in the difference between Grip and Grip by default. Grep uses basic regular expressions which treats certain characters with curly braces differently

[00:15:42] and it doesn't interpret the reputation and tags like three and four which are inside the curly braces the way we want. On the other hand, E grab or grab hyphen E uses extended regular expressions which handles the curly prices,

[00:16:00] reputation properly. And that's why it works. While you can accomplish the same task. Using grab, it requires escaping several special characters or using the hyphen P option with Grab to enable pearl compatible regular expressions.

[00:16:21] However, with grep, you can directly use meta characters like plus curly braces and pipe without needing to escape them. This makes your patter cleaner easier to read and simpler to maintain. In our example, the pattern was relatively simple but the real advantage of

[00:16:45] egrep shines in more complex scenarios. Let's say we have two files, file one and file two and we need to compare them. This can be done using the con command.

[00:17:02] The output of the com command has three columns when comparing two sorted files, column one shows lines unique to the first file, which is file one in our case. And column two shows lines unique to the second file, which is file two

[00:17:23] and column three shows lines common to both files. If we want to display only the third column, that is the common lines, we can use the com hyphen 1251 dot

[00:17:40] txd 52 dot txt. The hyphen 12 option in the command surpasses the 1st and 2nd columns leaving only the third column which contains the lines common to both the files,

[00:18:00] we will now move on to the last command in this lecture, which is the Find command in Linux. The find command is used to search for files and directories within a specified location. It offers powerful search capabilities based on

[00:18:21] various attributes such as name size, type, modification date and permission. While grab can be used to search for files, it works differently from Find. The Find command is designed to search for files based on specific attributes

[00:18:44] like name type or size. Let's say you want to find a file named nsw.com inside the slash etc directory. You can use the Find command like this.

[00:19:03] This command will search for files named nsw.com under the slash etc directory and display their paths.

[00:19:35] If you want to find all com files within the

[00:19:41] etc directory, you can use the command like this. Additionally, if you're interested in finding only directories, you can use the hyphen type D option which restricts the search to directories.

[00:20:03] This will display all the directories within slash etc. You can use hyphen type to search only for regular files. Other options like B for block device, C for character device, P for named P

[00:20:20] and L for symbolic link are also available to explore all these options. Use the man find command. The output from the find command can often be quite large. We have already learned that we can manage such output

[00:20:41] more effectively by using commands like tail head more or less to limit or paginate the results. Additionally, we can use pipes to combine commands and streamline the process. For instance, to display only the 1st 10 directories found within the slash

[00:21:03] etc. You can use the head command in combination with find through a pipe. Here is how that concludes this lecture on filter commands. We will see you in the next one.