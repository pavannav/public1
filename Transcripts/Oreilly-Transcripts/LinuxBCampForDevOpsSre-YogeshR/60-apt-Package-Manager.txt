[00:00:00] Hello and welcome to this lecture. In this lecture, we will dive into a package manager as discussed earlier on Debian based systems. Software is distributed as DOTD packages and tools like APP and DPKG are used to manage them. Let's understand the dotd

[00:00:21] file name format. A depth package file name is structured as name underscore version plus release underscore architecture not depth. Let's take an example, name describes the contents of the package.

[00:00:41] For example, when version is the software's version number release is the release number set by the package architecture is the CPU architecture. The package is compiled for say, for example, am T underscore 64 stands for 64 bit systems.

[00:01:03] Other examples include A R 64 for arm processors. While DPKG focuses on individual dotd files, app acts as a more user friendly frontend, streamlining tasks like searching repositories,

[00:01:22] resolving dependencies and handling software updates effortlessly. The advanced package tool or app simplifies package management with a set of versatile commands. These commands help you to search for available software in the repositories,

[00:01:43] installer upgrade packages along with their dependencies, keep your system secure and up to date with regular updates and upgrades, remove packages or free up disk space by clearing the local cache.

[00:02:00] Let's walk through these commands. We will start with the basics running SUDU app update. The package manager refreshes the local package cache which is to copy of metadata and package information from software repositories on your system.

[00:02:21] And it ensures it is synced with the latest repository data keeping most up to date information about available software. Next using SUDU at upgrade, you can upgrade all installed packages to their latest versions, ensuring your system remains secure and up to date.

[00:02:58] For example, when transitioning to a newer Linux

[00:03:01] version, full upgrade is recommended to handle package dependencies, smoothly, full upgrade performs the function of upgrade but will remove currently installed packages. If this is needed to upgrade the complete system, it may remove existing packages if they conflict

[00:03:23] with new version or if dependencies require it. So be cautious in using full upgrade.

[00:03:46] So do a search package name, searches for specific packages in the repository listing package names and brief descriptions to help locate the design software

[00:04:15] to list the installed packages in Debian based distros type DPKG

[00:04:20] hyphen L or a list.

[00:04:54] Let's switch to the terminal and see how APPD commands work in real time.

[00:05:00] First, let's run the command sudo apt update. This updates the local package database with the latest information from the repositories.

[00:05:35] On the other hand, get signifies that new metadata or updated package lists are being fetched from

[00:05:42] the specified repository with the size of the data downloaded displayed in brackets for instance, metadata like in release files and specific components like packages or translation hyphen in are downloaded if updates are available.

[00:06:03] This process ensures the system has the latest information about available and updated packages. Observe how it lists the packages that needs upgrading along with their details.

[00:06:26] Confirm the upgrade by typing. Y when prompted if there are updates requiring changes in dependencies or removal of obsolete packages, we can use pseudo

[00:06:41] a full hyphen upgrade. This command manages complex upgrades, ensuring everything is handled smoothly. Now, let's look for a specific package in the repository. Let's search for Apache two.

[00:07:01] Apache two is a popular open source web server software used to serve websites on the internet. Now to search for Apache two and its related packages in the repository, run the command apt search Apache two.

[00:07:20] Notice how it lists all packages related to Apache two including brief descriptions. Now let's go ahead and install it on our system to do this. We will use the command sudo apt install Apache to

[00:07:40] hyphen P. Observe the output showing dependencies being downloaded and the package installed, the hyphen Y flag automatically answers yes to any prompts that might come up during installation. This ensures the process runs smoothly without any interruption

[00:08:03] but be cautious while removing the package as it might remove some important dependencies, you can check the version of Apache two installed by the command. Apache two hyphen V. The output signifies the Apache

[00:08:22] two package is installed in system with the latest stable version. You can also reconfirm by executing the command apt list installed Apache two. This command lists the name of Apache

[00:08:40] two package installed. This is the same package name which was there at the top of the search list. Observe that the Apache two configuration files are located in the pathc Apache two if you no longer need Apache two, but want to retain its configuration files usese

[00:09:04] to apt remove Apache two. This removes the software while keeping the settings intact. Only a few packages are removed. Press Y to continue in the output. You can observe removing Apache two

[00:09:20] and also observe a suggestion to execute PSEO act or to remove. So execute this command. This command identifies the packages that were installed as dependencies for Apache two but are no longer required after its removal.

[00:09:40] Press Y to continue, but the configuration files are not removed, we can list them by executing the command ls

[00:10:01] etc Apache two. You can check for the list of installed package for Apache two. The output displays residual hyphen config. And on running Apache to hyphen V, it displays no such file or directory

[00:10:22] to completely remove a package including its configuration files usese to apt purge Apache two. We have already removed the package Apache two. The message removes the residual config files.

[00:10:40] You can see the message displaying in output purging configuration files for Apache two. Now try to list the Apache conflict fight again. Notice the output, confirming the package and its related configuration files are deleted,

[00:11:00] Apache file is removed and the rest other config files listed earlier are also gone. Basically P is more thorough and should be used when you want to completely erase all traces of a program including its settings. Why remove is preferred when you want to

[00:11:21] reinstall the program later with its previous configuration. Finally, let's free up disk space by clearing the local package cache, run the command. So do a clean. This removes cached package files located in slash

[00:11:42] slash cash app archives, ensuring your system has more available space. That concludes this lecture on app package manager. In the next lecture, let's explore yum package manager in detail.