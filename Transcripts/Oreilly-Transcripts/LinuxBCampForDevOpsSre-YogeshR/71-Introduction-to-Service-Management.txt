[00:00:00] Hello and welcome to this lecture on introduction to service management. Have you ever wondered how a web server database or even a logging system continues running smoothly without any manual intervention. The answer license services,

[00:00:20] special background processes that are essential for a systems operation. Services in Linux are vital for performing various tasks. They could range from handling network connections to managing schedule tasks.

[00:00:40] It even handles crucial tasks like web hosting or providing database access. But how do these services start stop and interact with the operating system? This is where service management comes in. Traditionally, Linux use the CSV

[00:01:01] in its system for managing services. Sysv. Init worked well for years as it launched essential services during system boot. However, as systems grew more complex cis we in it faced challenges like managing dependencies between services and ensuring

[00:01:23] they started in the correct order. For example, imagine needing a database service like mysql to be fully operational. Before starting a web servers, we init lacked an efficient way to handle such dependencies which

[00:01:41] could lead to delays or even failures during startup. Enter systemd, the modern replacement for CSVT systemd revolutionized service management by introducing features like parallel startup dependency resolution and robust logging.

[00:02:03] It doesn't just manage services. It takes charge of the entire boot process, ensuring system resources are allocated efficiently and tracks all processes. Now here is where demons fit into the picture.

[00:02:23] Daemons are a subset of services managed by tools like systemd. They are specialized processes designed to run continuously in the background. Think of them as the backbone of many Linux functionalities.

[00:02:41] System D interacts with these demons to control their life cycle from starting and stopping them to ensuring they run smoothly. For example, when the system boots systemd initializes the SSHD demon which handles secure remote logins.

[00:03:02] Similarly systemd can restart a daemon if it fails, ensuring services remain available without manual intervention. Let's break down how demons operate. Daemons are typically started during system boot by systemd.

[00:03:25] For instance, the HTTPD demon is launched to ensure your web server is ready to serve requests immediately. Daemons rely on configuration files to define their behavior. For example, the

[00:03:40] SSHD daemon reads settings from slash etc slash SSH slash SSHD underscore config allowing administrators to customize parameters like access permissions and port numbers. Once running demons monitor events or perform tasks in the background.

[00:04:04] Take Crond as an example, it schedules and executes jobs at predefined times without needing any user interaction. Users don't directly interact with demons but rely on applications and commands that connect to them.

[00:04:23] For instance, when you use the SSH command, you are indirectly communicating with the SSHD demon. Apart from SSHD, there are many other examples of demons. Now, let's have a look at them httpd, the Apache web server daemon which serves web pages over http or https.

[00:04:47] Crond responsible for scheduling and running tasks at specific times. Mysqld, the daemon that manages mysql database services

[00:05:00] and SYSTEMD journal team which collects and stores logs from various system services for easier troubleshooting that concludes this lecture. See you in the next to understand the role of system D in detail.