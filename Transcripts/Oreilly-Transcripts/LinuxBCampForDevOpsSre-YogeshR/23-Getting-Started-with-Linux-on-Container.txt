[00:00:00] Hello and welcome to this demonstration in this demonstration, we will dive into play with Docker, an interactive browser based Docker playground that serves as a good learning environment for Linux. If you're looking to start with the basics quickly and at no cost play with Docker is an excellent choice

[00:00:23] play with Docker is hosted at labs dot play with docker.com to log in and use the playground. You will need a Docker account if you don't have one, create it at hub docker.com/signup

[00:00:42] on the play with Docker page, click login, select Docker from the list of login methods and then sign into your Docker hub account to complete the login process on successful login. You should see the start button,

[00:01:01] click it to start a session, a single session lasts four hours to begin using Docker. Click add new instance. This creates a sandbox environment with Docker pre installed run Docker hyphen

[00:01:22] hyphen version to check details of the environment. Now that we are set up on play with Docker. Let's dive into creating a container. We will use the open to image to give us a full open to environment right here in Docker

[00:01:43] to get started type the following command. Docker container run hyphen it open to colon. Latest, let's break down what this command does. First we have Docker container. This is the command for managing containers.

[00:02:02] Next run is a subcommand that tells Docker to create and start a new container. Then we have hyphen I these flags open an interactive terminal session meaning you will be able to type

[00:02:20] commands directly into the container as if you were on a regular Linux systems. Finally, we specified Ubuntu Kan. Latest, this tells Docker to use the open to image specifically the latest version available.

[00:02:40] If you don't already have this image in your environment, Docker will automatically download it from Docker hub for you. When we run this command, Docker will start the container, download the Ubuntu image from Docker hub registry as it's not available locally.

[00:03:00] We are now inside the container. Now we have a working open to environment where you can try out Linux commands and explore as you would on any open two system to check the version of Ubuntu run

[00:03:20] cat slash etc slash OS release to exit the container simply type exit and the Docker will stop the container for you. You can verify that using Docker container, LS hyphen A

[00:03:40] to delete the container. You can use Docker container RM and provide the container name to delete an instance. Click delete to close the session, click close session on the top. And this deletes any active instances,

[00:04:01] the session automatically ends when four hours are up. Keep in mind that these instances are temporary or ephemeral and shouldn't be used to store critical data as any files created will be lost once the instance or session ends.

[00:04:23] And that wraps our demonstration of play with Docker as a learning environment for Linux. Thank you.