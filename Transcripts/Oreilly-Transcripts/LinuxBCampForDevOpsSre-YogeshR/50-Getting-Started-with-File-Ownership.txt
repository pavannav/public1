[00:00:00] Hello and welcome to this lecture in this lecture. We will discuss file ownership. Earlier in section four, we explore the LS command with the hyphen L option which displays a detailed listing of files. We also discuss the significance of the first character

[00:00:22] in the output which indicates the file type. We also discuss this in brief in the previous section as well. In this lecture, we will take a closer look at the entire output of the LS hyphen L command.

[00:00:40] When you execute LS hyphen L, you will notice that the output consists of seven fields. The first field indicates whether the item is a file directory or special file as well as the associated permissions.

[00:01:00] This second field shows the number of links to the file or directory. We will discuss the details of these links in an upcoming lecture. The third field specifies the user name of the file owner. The fourth field indicates the group associated with this file.

[00:01:23] The fifth field represents the size of the file in bytes. The sixth field shows the timestamp of the last modification. The seventh field displays the name of the file or territory

[00:01:41] fields. 567 are quite straightforward. In fact, we have already covered field five earlier when we learned how to use the LS hyphen L command with the hyphen option, the hyphen edge option helps display file sizes in a human readable format.

[00:02:03] For example KB MB GB making it easier to interpret than raw byte values. In the upcoming lecture, we will delve into the concept of links. However, in this lecture, our focus will be on file permissions.

[00:02:23] We will explore what file permissions are, how they work and how they are related to the file's owner and group. In the first field of files details, the last nine bits represent its permissions.

[00:02:43] These permissions apply to three distinct groups, user who is the owner of the file group or set of users who have specific rights to the file and others are everyone else who doesn't fall into first two categories.

[00:03:04] Here is an interesting detail. The owner of a file doesn't always have to be the person who created it. For example, a file creator might lose ownership after creating it depending on how the file is managed.

[00:03:23] These nine beds are organized into three sets of three. The first set specifies the action allowed for the owner. The second set defines the permissions for the group. The third set outlines the rights for others

[00:03:43] within each set. The three bits represent specific actions. The first character indicates read permission represented by R. The second character indicates write permission represented by W

[00:04:01] the third character indicates execute permission represented by X. These are the three types of permissions available for each group, owner, group and others. A hyphen indicates the absence of a permission.

[00:04:20] For example, if a hyphen appears instead of X, it means that there is no execute permission. Every file type has a default set of permissions but you can modify these defaults or adjust the permissions at any time to suit your needs.

[00:04:42] We have discussed that it's possible to change the ownership group and permissions of files and territories. This can be done using file permission management commands which will control who can access and modify files.

[00:05:01] Let's have a look at these commands. Chmode stands for change mode. It allows us to modify the permissions like read, write and execute for a file or territory for the user group and others.

[00:05:20] C stands for change owner. This command lets us change the owner or user and or the group associated with the file or territory. CH group stands for change group. It's used to change the group, ownership of a file or directory.

[00:05:44] We have already covered ch modern CH group. In the previous section, we will not revisit CH group but we will discuss ch modern detail and cover C in this section.

[00:06:02] Now, let's discuss an important concept how to use the CH mod command. There are two main ways to do this symbolic method and or numeric method in symbolic method.

[00:06:20] We modify permissions using letters to represent permission groups, U for user, G for group O for others. And A for all, we have different permission types that define the mode

[00:06:40] R for read, W for write and X for execute. The operators are plus to add permissions minus to remove permissions and equal to explicitly set permissions replacing any previous ones.

[00:07:01] Let's say the think NEXTX TXT file currently has RW hyphen permissions for the owner or user to add, execute permission for the user. We can use chmode UX. Think next

[00:07:20] do DX. Now if the group has read axis and you want to remove it, you can use chmode G minus R think nextx, you can also set permissions for all the categories which is user group and others all at once.

[00:07:41] For example, this gives the owner, read and write access and only read access to the group and others. A method is a shorthand way for setting permissions using a three digit number.

[00:08:02] Each digit represents permissions for the user group and others. And each permission is assigned a value four for read two for write and one for execute. The values 42 and one are based on binary values where

[00:08:23] each permission corresponds to a bit in a binary number. For example, the read permission or R hyphen hyphen has a binary value of 100 which when converted to

[00:08:40] till it gives four right or hyphen W hyphen is 010, converted to Octa gives two and execute. Or hyphen hyphen X is 001. Similarly converting to octal will be one.

[00:09:02] The reason it's called the octal method is that the three digits are in base eight or octal. And each digit represents a binary value. Since each octal digit corresponds to three binary digit,

[00:09:20] it allows a compact representation of all permission combination from 0 to 7 in just three digits to calculate the numeric value. For each category, you can simply add up the values of the permission you want.

[00:09:41] For example, seven means full access, which we will get from read, which is four plus write, which is two plus execute, which is one similarly six means read and write, read, which is four plus write, which is two

[00:10:01] five means read and execute. Four for read plus one for execute. Now let's say you want to give the user full access, the group, read and write and others read and execute. You would use

[00:10:20] chmode 765. Think Nextx. This way you can easily set file permissions using numerical values permissions for files and directories in gleno work differently impacting

[00:10:41] how users interact with them. If read permission is given to a file, it allows viewing the contents of the file. But if read permission is given to a directory, it allows listing the files and subdirectories.

[00:11:00] It contains if write permission is given to a file, it allows modifying the contents of the file. However, if right permission is given to a directory, it allows creating deleting or renaming files inside that directory.

[00:11:20] If execute permission is given to a file, it allows executing the file as a program or command. But if execute permission is given to a directory, it allows traversing the directory and accessing files and subdirectories inside it.

[00:11:43] There are also some special cases which we need to remember a user who has read and execute permissions on a directory can list and access the contents of a file within but only in a read only manner

[00:12:03] without right permission. They can't alter the files. If a user has only read access on a directory, they can list the file names but can't view other details like file permissions or time stamps.

[00:12:21] If a user has only execute access on a directory, they can enter the directory using CD but cannot list the file names, they would need to know the exact name of a file. They wish to access users who own a directory or have right permissions on it

[00:12:44] can remove files inside regardless of the file's own permissions or ownership to restrict this behavior and ensure that only the file's owner can delete it. The sticky bit permission can be used.

[00:13:03] This prevents other users from deleting files they do not own even if they have right access to the directory in summary directory and file permissions serve different purposes in controlling

[00:13:21] how files and directories are accessed modified and deleted, using combinations of read, write and execute permissions allows for nuanced control over user actions. That's it for this lecture. See you in the demonstration.