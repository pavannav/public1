[00:00:00] Hello and welcome in Linux processes can operate in two modes, foreground and background in this lecture. Let us explore these two modes. Foreground processes are the ones you interact with directly they run in the terminal and while active they occupy the terminal completely,

[00:00:25] you can't use it for anything else until the process is finished or stopped background processes. On the other hand, run independently in the background allowing you to continue using the terminal for other tasks.

[00:00:42] These processes do not block your terminal and you can still monitor or control them if needed to manage these processes. You can use commands like jobs to list background tasks FG to bring a background process to the foreground

[00:01:03] and BG to resume a post process in the background. The Linux kernel has several background processes too often referred to as kernel threads or kernel processes that run in the background to perform essential system tasks.

[00:01:23] Let's understand this better through the demonstration. Until now, everything we have been executing was a foreground process, foreground processes completely occupy the terminal, which is why we have been using two separate

[00:01:42] terminals for demonstrations like V I and sleep. Now let's explore this further by running the sleep command again, sleep 100. You will notice the terminal is fully occupied and we can't enter any other command.

[00:02:02] While this process is running to free up the terminal, we can suspend the process by pressing control. Z this pauses the process temporarily next to resume it. In the background. We type V.

[00:02:20] This moves the sleep process to the background allowing the terminal to accept new commands. While sleep continues running, we can confirm this by typing jobs. This shows the job id and its status which should now indicate that sleep is running in the background.

[00:02:43] We used a simple BG command because there is only one job running. However, if we have multiple background jobs, you can specify the job id in the square brackets to manage them. For example, if you have several jobs running and you want

[00:03:01] to resume a specific one in the background, you would use BG percentage one. In our case, since the job is already running in the background, you will see the job one already in background to bring a background process back to foreground. We use the FG command.

[00:03:25] This will move the process back into the terminal occupying it until the process finishes or is interrupted by default FG brings the most recent background job to the foreground.

[00:03:41] However, if you have multiple jobs running, you can specify the job id using the job identifier in the square brackets for example, to bring a specific job to the foreground, you can use FG percentage one. This will bring job one back to the foreground,

[00:04:01] allowing you to interact with it directly in the terminal. Once the process completes or is interrupted, the terminal will return to the prompt. This simple workflow highlights how we can manage processes effectively switching between foreground and background as needed

[00:04:24] to run a process in the background. Right from the start, you can append a percent symbol at the end of the command. This tells the shell to run the command in the background, allowing you to continue using the terminal for other tasks.

[00:04:40] While the command execute. For example, if you use the sleep command like this, this will initiate the sleep 60 command which will pause for 60 seconds, but it will run in the background.

[00:05:01] This means that after executing the command, you can still use the terminal to run other commands without waiting for the lead to finish backdrops of this lecture. We hope you found it helpful and insightful. Thank you.