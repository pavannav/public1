[00:00:00] Hello and welcome to this practical demonstration before getting started. Let's get a glance at one of the ways of how to securely copy files in Linux as we have seen earlier. SSH isn't just for logging into a remote server

[00:00:21] and in package management section, we explode that it also provides a secure method for transferring files. This is done using CP or secure copy CP allows you to securely copy files between your local machine and a remote server.

[00:00:42] This is the syntax for loading a file from local machine to remote server CP options, path of file or file name, remote user at the rate IP address of the remote server, colon tilda,

[00:01:00] path of the destination territory. Some of the common options are hyphen R for recursive copy of entire directories, hyphen I for identity file. In this demonstration, we will see how to use SCP for file transfers.

[00:01:20] CP ensures that files are copied over an encrypted connection, protecting sensitive data from eavesdropping during transit before starting with the SSH key based authentication. Let me set the context of what are the steps we would be following.

[00:01:42] We will modify SSH settings in the SSHD underscore config file on the virtual machine. This is to enable key based authentication. Next, we will create a new Ubuntu virtual machine. Let's call this as Ubuntu

[00:02:00] two with the same configuration as that of the first virtual machine. Let's call that as Ubuntu one generate SSH key pair. That is the public and private keys locally transfer the private key to the remote server to one using CT method

[00:02:20] set the minimum permissions on the private key file on the remote server using chmode transfer the public key to both the remote servers to one and append the contents of the public key file to the SSS authorized underscore keys file of

[00:02:41] 22 server. Now to test SSH connections SSH into to one using the private key, then from 1 to 1 SSH into two without specifying the key and password on the prompt to ensure key based authentication works.

[00:03:03] After understanding it conceptually, let's begin by changing the settings in the config file of the virtual machine. So do V slash etc slash SSS slash SSHD underscore config

[00:03:20] change P authentication to yes and password authentication. No, no. Now let's copy this path for this configuration file. Let's see and takes it now at the prompt, paste the config file path and pseudo V before it

[00:04:01] next create another Ubuntu

[00:04:03] VM name it as Ubuntu two, keeping all the configurations same as earlier, same keypair and then launch instance VM is successfully created. First, we will generate an SSH key pair on our local machine using

[00:04:24] SSH hyphen Keygen. When prompted we press enter to accept the default path and leave the passphrase empty for easier usage. This creates a public key and a private key in the

[00:04:41] SSH directory. Now let's navigate to the dot SSH territory where the keys are stored next. Let's list the contents of this directory to verify the generated files. You can see the files listed including

[00:05:00] ID underscore ed 2 55 19, which is nothing but the private key, ID underscore ed 2 55 19 dot pub which is nothing but the public keys. And you will also see other files like non underscore hosts. Let's go back to the downloads folder.

[00:05:21] We will now use the CP command to securely copy the private key to the remote server. Open to one by using this command. This command uses the hyphen I option to specify the identity file and transfers

[00:05:40] the private key to the remote server. After copying, it is crucial to set the minimum permissions for the key file on the remote server. So let's do the same tilda

[00:06:00] slash dot SSH slash ID underscore 2 55 90. This secures that only the owner has the read access to the key file, improving security. Next, we will use the CP command to transfer the public key file from

[00:06:23] our local machine to the first server which is open to one. Here is the command the hyphen I key name specifies the private key used for authentication. This is the file which is being transferred and

[00:06:40] this is the destination path on the remote server. After running the command, you will see a message confirming 100% file transfer indicating the key has been successfully copied to 1 to 1 next. We will repeat the process for the second server which is

[00:07:00] open to two change the IP address to the second server. When prompted to confirm the connection type PS to proceed after this, the pub key will be securedly copied to 1 to 2. For both of these commands, you will see the output like this.

[00:07:20] This confirms that the dot Pubkey has been successfully transferred to the respective service. Now, this message will be encountered when you connect to a new remote server via SSH. For the first time, you will see a prompt asking if you want to

[00:07:40] add that server's details to your known underscore hosts file. Essentially, it's asking you whether you trust the remote server and want to save its information on your system. This file stores the public keys of all the remote hosts you have connected to.

[00:08:01] It's crucial because it helps ensure that you're connecting to a legitimate server by saving the server's identity locally. It protects you from man in the middle attacks type A and press enter to add the host to the known underscore hosts file.

[00:08:21] Now that public key and private key is uploaded to put the open to virtual machines. We will use the SSH command to remotely execute AC command that will append the content of the public key file copied to the SSH

[00:08:40] authorized underscore keys file on the Ubuntu server. The authorized underscore keys file specifies which SSH keys can be used for logging into the node. Do not replace this file, especially on two hosts as it will remove the default key and you may not be able to log in

[00:09:06] once done. Let's move forward and check how this works. Now, let's SSH into our first Linux which is open to one system we have logged in.

[00:09:20] Now let's try to log into second virtual machine which is Ubuntu two. But this time we won't use the hyphen I option or the identity key file. As you can see, it asks us to conform, adding up to two to the known underscore hosts file,

[00:09:40] go ahead and type PS prompt. This step ensures that in the future, we won't get this prompt again. Since the host details are already verified, it also means you will be able to connect seamlessly without needing to enter a password. Since the public key authentication is now set up,

[00:10:03] we do not need the second server we created in this demonstration so we can go ahead and delete it. We will continue using our main server for the rest of our course. That concludes this demonstration. See you in the next to understand DHCP in brief.