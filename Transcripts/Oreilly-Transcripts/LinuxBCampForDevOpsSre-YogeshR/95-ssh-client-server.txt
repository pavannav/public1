[00:00:00] Hello and welcome to this lecture on SSH client server. Let's first talk about SSH and the need for it. SSH or secure shell is a protocol used for securely connecting to remote systems over a network. It provides a secure encrypted channel between a local machine which is the client

[00:00:26] and a remote machine which is the server. SSH became essential because traditional methods of remote access like telnet and FTP transmitted data including passwords in plain text, making it vulnerable to attacks.

[00:00:46] SSH. On the other hand uses encryption to ensure that the communication between the client and server is secure. In many instances, we need to manage servers remotely

[00:01:01] SSH enables system administrators and developers to access and manage servers securedly from anywhere even over unsecured networks, whether you're managing a cloud server, a personal machine or a production system.

[00:01:21] SSH offers a safe way to access and execute commands remotely. Now, let's explore the SSH client server model more closely as we have seen. SSH relies on a client server model. The

[00:01:40] SSH client is a software program running on your local machine that is the machine you use to access the remote server, the client initiates the connection and communicates with the server using the SSH protocol. On the other hand, the SSH server is a software running on the

[00:02:03] remote machine which is the server you're connected to the server. Listens for incoming SSH requests on a specific port. When a client connects the server authenticates the client's identity, establishes a secure connection

[00:02:22] and provides access to the system in a typical workflow. The SSH client connects to the remote machine via port 22. The remote server's SSH demon which is SSHD listens on this port and handles incoming connections

[00:02:42] once authenticated via password or key based authentication. The server grants access to the client port 22 is the default. But for added security system administrators often change it to a different

[00:03:01] port to reduce the chances of automated attacks on the default port for now, we will assume we are using the default port for simplicity. Let's revisit how we have been using SSH throughout this course to connect to a remote machine. We use the command

[00:03:22] SSH hyphen nine, the keyname, Ubuntu at the rate IP address. Let's break this command down to understand each part of it. SSH is the command used to initiate a secure SSH connection to a remote server.

[00:03:42] It's the client site tool you use to communicate with the server. The hyphen I option specifies the identity file or the private key used for authentication. We use this key to authenticate ourselves without a password ensuring a secure login.

[00:04:03] This is the username and IP address of the remote server. Ubuntu is the username on the remote system. And this is the IP address of the remote machine we are connecting to. So essentially, we are telling SSH,

[00:04:20] use this key to log in as the user Ubuntu to the server. At this IP. With this command, we can securely access and manage the remote server. Next, let's briefly discuss key based authentication, which is one of the most secure ways to authenticate with SSH.

[00:04:42] In this method, we use a pair of cryptographic keys, a private key and a public key. The private key is kept on the client machine that is your local system. While the public key is stored on the server in the authorized underscore keys file,

[00:05:03] when you try to connect the server checks. If the public key on the server matches the private key you're using. If it matches the connection is authenticated without needing a password, we will explore setting up SSH

[00:05:20] key pairs and configuring passwordless authentication. In an upcoming demo section. SSHD or SSH demon is the openssh server process responsible for handling incoming connections via the SSH protocol.

[00:05:40] It manages tasks such as user authentication, encryption, terminal sessions, file transfers and tunneling, serving as the backbone for secure communication. In the service management section. We discussed how services run in the background. We also learned about

[00:06:02] SSHD, which is the background service on the server that handles SSH connections. When you try to connect to a server, SSHD listens for the incoming requests on port 22 and verifies the credentials provided by the

[00:06:21] SSH client. By default openssh is installed in Ubuntu and the SSH daemon status is active and running as the SSH service, it runs continuously on the server waiting for incoming client requests.

[00:06:40] The SSHD service is controlled using the commands so do system TLS stop SSH to stop the SSH service to restart the SSS service.

[00:07:02] After configuration changes, we will use the restart command to reload the SSH service. After configuration changes without stopping, we would use pseudo system CTL reload SSH. Additionally, the SSHD

[00:07:21] surveys has some important configuration files that control its behavior. SSH D underscore config which is the server site configuration file and authorized underscore keys where the public keys are stored. Now, let's take a closer look at these critical configuration files involved in SSH

[00:07:47] slash etc slash SSH. SSHD underscore config is the primary configuration file for the SSH demon. It contains settings that control the behavior of SSS on the server

[00:08:04] such as whether root login is allowed, which authentication methods are permitted and whether password authentication is enabled. For example, we have port 22 here and we have permit root login prohibit password

[00:08:24] which will disable root login for security, password authentication, which will enable password authentication or we can set no to disable authorized keys. File holds the public keys of client that are authorized to access the server.

[00:08:46] This file is checked whenever a client attempts to connect via SSH. If the client's public key matches one stored in this file, access is granted without a password.

[00:09:02] Both of these files are essential for securely configuring SSH and controlling how it behaves on your system. That concludes this lecture. See you in the subsequent demonstration to explore how to configure settings to enable password based authentication.