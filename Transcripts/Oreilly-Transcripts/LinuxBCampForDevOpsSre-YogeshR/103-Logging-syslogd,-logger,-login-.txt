[00:00:00] Hello and welcome to this lecture on logging mechanisms. In open 2 24 04. Logging plays a vital role in monitoring system activity, debugging issues, tracking security events and ensuring system stability.

[00:00:20] We will cover syslogd or RSYSLOG. The system log demon responsible for handling logs, logger, a command-line tool to manually add entries to system log. Login logs are for tracking user authentication events.

[00:00:40] Journalctl is for viewing system logs managed by systemd log rotation helps in managing and compressing old logs with log rotate to begin. Let's understand syslogd or R Cislo which is used for system logging

[00:01:03] in 2 24 04. System logging is handled by RSYSLOG. An advanced and efficient logging demon that enhances the capabilities of the traditional Cislo service

[00:01:20] log offers several key improvements, higher performance. More filtering options support for various protocols like TCP and TLS ability to write logs to databases, files and remote servers. Let's get started with demonstration by directly exploring this terminal

[00:01:45] logs are stored in the slash wire slash log directory

[00:02:10] to view the system logs, execute the command loglog or for real time monitoring. We can use the

[00:02:21] tail command as discussed in the earlier section. Let's break down the output and understand each field in the log file. This is the date which is in year, month and date format.

[00:02:40] This is the time in RMD microseconds format. This is the time zone which is hostname identifies the system where the log was generated. This is useful in multiserver environments. Next is so or processing

[00:03:00] which is the system processes responsible for the event. In this case, cron indicates a scheduled task execution. In the square brackets. We have process ID or PID. It is a unique identifier for the running process useful for tracking and debugging specific executions.

[00:03:23] This is the user, the user account executing the command here ro means the task is executed with administrative privileges. The last field is the action or command which shows the exact command that was executed.

[00:03:41] We will understand more about our log in more detail later in this section. Next, let's learn about logger. Command, logger command allows users and scripts to write messages to system logs present in

[00:04:02] CIS log. For example, we can write logger with the message in double codes. Some of the common use cases are log a message to cis log log messages with a specific tag input from command-line and log to the CIS

[00:04:21] log assigning priorities to custom log messages. Let's check this in a quick demonstration, execute the logger command logger, custom log message from think next to verify, check the logs tail hyphen

[00:04:40] 10 slash slash slash CIS log. The output displays last 10 messages from slash slash log slash sis log and includes the entry of the log, custom log message from. Think next. You can also log messages with a specific tag

[00:05:02] logger. Hyphen P Myapp application started successfully here. Hyphen T stands for tag, check the logs again, the output shows the tag with the message. Now let's enter with the ECO command from command-line input

[00:05:25] system snapshot taken successfully pipe locked logger. Hyphen P user dot warning temperature exceeds threshold here. Hyphen P option stands for priority.

[00:05:42] Now, let's see these two entries in the log file. The output display system snapshot taken successfully and temperature exceeds threshold in the log file. We will see the actual benefit of using hyphen pay option in a file

[00:06:01] for system admins. Logger is useful for debugging scripts and tracking events when executing commands in Linux errors and output messages are typically displayed in the terminal. However, we can redirect them to system logs for better tracking and debugging.

[00:06:24] The following command captures both standard error and standard output of a command and locks them under a specific priority and tag here in command, we will provide whose locks to be stored

[00:06:41] two greater than son one redirects error messages to standard output pipe logger sends the output to the system locks. Hyphen P user error sets the priority as error under the user facility.

[00:07:00] And at the end hyphen T command tags. The log entry with CMD for easy identification in Linux, rs' log is used to manage logs and it categorizes logs based on two key parameters.

[00:07:20] One is facility which defines the source or category of the log message say for example, kernel or user processes system demons authentication logs, printer system logs or custom logging for applications. For example, local zero to local

[00:07:41] seven, then we have priority or severity level. This indicates the importance of severity of the message. Say for example, info warning or error. In this example, the command attempts to list a nonexisting directory captures the error message,

[00:08:06] standard error and standard output one both in the same log file and then pipes the log under the local zero facility with a warning priority by adding the tag CMT.

[00:08:23] You can find the entry with the tax cmd in the CIS lock. You can use this command to see the output display in this century. Now what if I want to store the custom logging for application in a separate log file?

[00:08:45] Let's check for the active conflict file included, list the files by using the command LS slash etc slash RS log star. This displays the default conf file.

[00:09:02] Let's open the file to check the configuration cat slash etc slash log dot conf it shows that the conflict file included is stored in slash etc slash sis dot

[00:09:21] TS check the files by navigating to sis use this. It contains the Q file for UFW and others. But relevant con for custom log file like local zero is not present. So let us create a configuration entry in RSYSLOG

[00:09:44] under slash A TC slash R sis log dot T using this command and add the line for storing all local zero in slash slash slash local zero dot log.

[00:10:01] Whether it's local dot error or local zero warning, all the facility related to local zero will go to this log file, save and exit. Then restart ARSIS lock system CTL restart ARSIS lock.

[00:10:22] Now test logging again, logger hyphen P for priority local zero.info with hyphen T tag alert underscore demo and execute that earlier command of warning message to

[00:10:40] now check the custom log file local zero dot log using this command. The output shows the two logs of local zero log files can grow large over time. Open two uses log, rotate to manage and archive logs automatically

[00:11:02] configurations are stored in a log rotate dot conf when we get this file, we see that rotate log files are weekly. We can keep four weeks worth of backlogs. With rotate four,

[00:11:21] we can create new or empty log files after rotating old ones. If you want to change the settings, check for the usage in man log, rotate, scroll down to the end to find sample configuration script.

[00:11:51] This line specifies the log file to be managed by log. It rotate seven keeps seven rotated log

[00:12:01] files before deleting old ones daily rotates the logs every day. SU root ATM runs log rotation as root user and ATM group ensure correct permission before using it missing. OK. Means if the log file is missing, don't show an error. Just continue.

[00:12:24] Not if empty does not rotate the log. If it's empty, compress, compresses old log files to save file. Say for example to GZ format delay, compress delays compression until the next rotation.

[00:12:42] This is useful when another process might still write to the log postrotate. Starts a script block that runs after log rotation. This line restarts R log surveys to ensure new logs are correctly written.

[00:13:04] This hides of pos this ensures that even if the command fails log rotate does not fail and script and the postrotate block save and exit.

[00:13:23] So do log rotate, hyphen DC log rotate local zero here, hyphen D stands for debug mode which checks for errors. And thus we have created new state custom configuration setting for log rotate

[00:13:47] that ends the demon logger command monitoring. User authentication logs is essential for maintaining system security and

[00:14:00] ensuring that only authorized individuals access your Linux environment.

[00:15:01] Next to review the recent login, we can use the last command.

[00:15:07] It reads from the

[00:15:11] W temp file and displays a list of all users who have logged in and out since the file was created.

[00:15:20] This includes details such as username, terminal, used IP addresses or hostname of the remote host and login or logout times for failed login attempts. The last P command reads from the B temp file

[00:15:41] to monitor authentication logs. In real time, you can use the tail command with the hyphen F option.

[00:16:06] Let's break down the output timestamp indicates the date and time when the event occurred. Hostname is the name of the host or server where the event was logged in service or program name identifies the service or

[00:16:22] application that generated the log entry process id or PID is the unique identifier of the process that logged. The event message content provides detailed information about the event. Here. Module refers to the pluggable authentication module or PAM used

[00:16:46] service context specifies the service and the type of PAM interaction. So for example, session description indicates that a session for the user route has ended. Now let's walk through this file in the demonstration.

[00:17:02] As discussed to track user logins and authentication events, we can check the auto log file by using this command. You can observe that the output for timestamp, hostname service or program name, process id and message content.

[00:17:22] But if you want to filter out only session opened message, then execute the command pipe grip session opened. The output displays all the entries with the message session opened.

[00:17:41] Similarly to see all failed login attempts, we can execute this command grab, failed password slash slash log slash or dot log. No error in the output means no failed login attempts. Next, let's see the last command,

[00:18:01] the last command displays the output in this order, username, which shows who logged in terminal displays the session time. Say for example, local TTY or remote PTS source IP where the login originated from login date and time when the session started,

[00:18:23] logout time and duration when the session ended. And how long it lasted to sum up the login logs help in auditing and detecting unauthorized access attempts. Journalctl

[00:18:40] is used to view logs managed by systemd in open 2 24 04 LTs as covered earlier modern open two systems. Also use systemd hyphen journald which stores logs in binary format to view all the logs use journalctl.

[00:19:02] This shows all logs from system boot until present to view logs in real time. Use journalctl hyphen F like tail hyphen F it streams logs life to view boot logs. We can use journalctl hyphen P.

[00:19:22] This shows logs from the current boot. The journal in system offers a comprehensive logging system that captures data from various sources. Beyond traditional clog implementation, it includes logs from the early

[00:19:40] putrow the kernel, the initial ram disk which is initd and application standard error and output streams. This centralized logging approach provides a more complete view of system activities. You can also use journalctl

[00:20:00] hyphen X. This command in Linux is used to view detailed system logs and troubleshoot errors. The option hyphen X provides extra details and explanations for log messages. In case if it's present hyphen E jumps at the end of the log showing the most recent entries. First,

[00:20:24] this command helps us diagnose system errors and failure provides detailed explanations of logs useful for troubleshooting service failures and security alerts. Let's do a quick demonstration. Let's execute the command journal CTL hyphen XE.

[00:20:46] The output shows that collect surveys, service in that state, etc it is useful for troubleshooting survey failures and security alerts to filter logs for a specific survey. Say for example,

[00:21:03] SSH, use the command along with period journal C hyphen U SSH hyphen hyphen S one to follow real time logs. We have already covered JOURNALCTL

[00:21:21] hyphen F output shows real time logs from systemd, the init system managing services, etc, logging is a critical part of system administration helping in t bugging, security audits and performance monitoring.

[00:21:42] We have explored log logger authentication logs, journal CTL and log rotate that wraps up this lecture. See you in the next lecture to discuss alerting and Linux.