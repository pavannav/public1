[00:00:00] Hello and welcome to this lecture on Linux terminologies. By now, you have learned the basics of operating systems and glanced at Linux history and features. Before we get into the core concepts of Linux, let's familiarize ourselves with some key terminologies that

[00:00:21] you will often hear throughout this course. And your journey with Linux, let's start with the kernel often referred to as the brain of the Linux system. The kernel is the core that controls everything it directly communicates with the hardware, manages resources and creates a bridge between your

[00:00:42] hardware and applications to ensure smooth functionality. Now, Linux itself is essentially just a kernel but to make Linux usable as an operating system, additional software components are added on top of this kernel

[00:01:01] creating what we call a Linux based operating system. This collection of the kernel and other software like system libraries and several other utilities forms a Linux distribution or distro distributions or distros offer different experiences and packages.

[00:01:24] For example, when you hear names like Ubuntu Fedora or Red hat, these are distributions built from the Linux kernel, each with its own set of tools and software. Each one takes the Linux kernel and builds on it in unique ways to fit various needs.

[00:01:46] Making Linux flexible and customizable, we will cover the concept of distributions in more detail in the upcoming lecture in Linux. A process is simply an instance of a running program.

[00:02:02] When you execute a command or launch an application, Linux creates a process to handle the program's tasks. Each process has a unique process ID that helps the operating system keep track of it, manage resources and stop it if necessary.

[00:02:21] Next, let's talk about services. These are programs running in the background that support other applications and system functions. Examples include httpd for handling web services. FTPD for file transfers

[00:02:40] and systemd is a service that manages and supervises the entire system from booting up to shutting down. We also have the file system which stores organizes and manages files in Linux. Different file systems exist like ext three ext

[00:03:02] four fat XFS and Butter FS each with its own benefits depending on the use case file systems directly affect system performance, data integrity and storage efficiency. The Xwindow system offers the tools for creating

[00:03:23] graphical user interfaces while desktop environments like gnome KDE FC and flux box build on top of this, creating user friendly visuals and controls for interacting with Linux.

[00:03:40] Then there is the environment shell or command-line interface in Linux which allows you to type commands to control Linux directly. It is the shell that acts as an intermediary between the user and the operating system, interpreting commands and executing them.

[00:04:01] When you type a command into the shell, it translated into instructions for the Linux kernel to carry out common shells. In Linux include bash and C shell that concludes this lecture. These terms form the foundation of Linux and will help you understand the system better.

[00:04:22] In the upcoming lectures, we will delve into these concepts in detail starting with types of shells.