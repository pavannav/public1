[00:00:00] Hello and welcome to this lecture in the Linux environment. Users interact with the system through two main interfaces, the graphical user interface or Gui and the command-line interface or CLI. Each has unique trends catering to different user needs and scenarios.

[00:00:23] The GOI offers a visual intuitive way to navigate files and manage settings which is ideal for beginners and those who prefer a point and click experience. In contrast, the C

[00:00:41] provides a powerful text based approach that emphasizes speed, precision and control, making it the preferred choice for advanced users and system administrators. In this course, we will focus primarily on the C to dive deeper into Linux core functionalities.

[00:01:06] As we learned earlier, the shell is a program that interprets commands provided by the user commands represents the names of programs or scripts installed on the system.

[00:01:20] And each command may include options and arguments when entered at the shell prompt. A command-line typically has three main elements. Command is the name of the program or script being executed. Options are optional flags often starting with the dash like

[00:01:44] hyphen P or hyphen hyphen print that modify the command's behavior arguments specify what the command operates on such as files or directories.

[00:02:01] Let's go through an example. First, we have the command LS which lists the files and directories in the current path. Next, when we add the hyphen L option like LS hyphen L, it tells the command to display the files and directories in a detailed format.

[00:02:21] Finally, when we include both an option and an argument such as L hyphen L slash DEV, the hyphen N option to list the files in detail. While slash DEV specifies the directory, we want to list

[00:02:43] now that we understood the basic elements of a shell command. Let's explore some fundamental Linux commands that everyone should be familiar with. First, we will review what these commands do and later we will see them in action.

[00:03:03] The echo command displays the specified string or text to the standard output the unname command in Linux display system information such as the operating system name, hostname of the system kernel version and the hardware architecture.

[00:03:23] It provides details about the system's environment. The whoami command prints the username associated with the current effective user id. The who command displays a list of all

[00:03:40] active users currently logged into the system. It provides details such as their usernames, the terminals they are using and their time stamps indicating when a user logged into the system. The up command tells you how long the system has been running since its last reboot

[00:04:05] and provides the system load averages for the last 15 and 15 minutes, the date command prints or sets the system's date and time now that we have understood some basic commands. Let's see them in action

[00:04:26] for this and the upcoming demonstrations, I will be using the aws cloud and the Ubuntu E two instance we created in lecture 3.4. However, feel free to use any setup of your choice.

[00:04:42] I have already logged into the terminal and we are ready to get started. First, let's break down what each part of the string means in the shell. Pro the first part indicates the current user.

[00:05:00] In my case, Ubuntu is the current user. The string after the other eight represents the system's hostname. Initially, this was the private IP address of our E two instance, but we later changed it to think nextx Linux using the hostname CTL command.

[00:05:22] The next part of the prompt shows the current directory. The tilda represents the home directory of the user. Finally, the dollar symbol indicates that the user is a regular user. If the prompt shows hash, it means the user is logged in as root.

[00:05:45] This shell prompt gives us essential information about the current user system and working territory. And it helps us to understand our environment within the terminal.

[00:06:01] As we discussed earlier. A shell is a command interpreter that allows you to type commands to interact with the operating system kernel. Every user is assigned a default shell which determines the shell prompt. You see when you log in into a Linux system

[00:06:23] to identify the default shell. We can use the ECO command. Specifically running eco dollar shell displays the path to the user's default shell as it is set in slash etc slash passwd

[00:06:40] file. In our case, it will show slash bin slash bash, which is the shell that starts when a new terminal session begins. Alternatively, you can use ECO dollar zero which prints the name of the script or shell currently in use.

[00:07:02] We can also use the echo command to display text. For instance, running eco Linux will simply print the message. Hello Linux in the terminal. The unname command is used to display system information in Linux.

[00:07:21] When executed without any options, it shows the kernel name by default. However, you can use various options to display more specific details to display the name of the kernel. We can use your name hyphen

[00:07:40] output the same as above to display the hostname of the machine. Execute you name hyphen N as we have set the hostname to think next Linux, the same is being displayed here to display the release of the kernel run you name hyphen R

[00:08:05] to display the machine architecture use you name hyphen M as our machine is 64 bit. The architecture shown is X 86 underscore 64

[00:08:20] to display all available system information, execute the command you name hyphen A. We see that kernel name, hostname, release version machine type etc are displayed. Each option provides different details about the system

[00:08:42] and you can combine them as needed to get a comprehensive overview of your system's configuration. Moving to the next command to display the name of the current user, we can use the command. Who am I as we have already seen the current user. In this case is

[00:09:03] Ubuntu. When you run the who are my command, it simply returns the username of the currently logged in user. The who command displays only the name of the current user. Whereas the who command shows a list of all users currently logged into the system.

[00:09:25] It provides details about each user including their login names, the terminals, they are using the time they logged in and their remote IP addresses. In case it's a remote machine.

[00:09:41] The H command also comes with multiple options. For example, using the hyphen R option prints the current run level. In this case, we see that the run level is five as we have learned in section two of the BTA process, run level five indicates a multiuser environment with a graphical interface,

[00:10:06] meaning the system is set up to allow multiple users to log in simultaneously and provide a gui for interaction. If we want to check how long the system has been running,

[00:10:22] we can use the uptime command. This command also provides additional information. Let's break it down. The first part shows the current time up indicates the system's uptime, which is how long it has been operational since the last restart.

[00:10:45] In this case for 11 minutes. The next part shows the number of users currently logged into the system, which is one. Finally, the last part displays the system's load averages

[00:11:00] over the last 15 and 15 minutes respectively. Next, let's talk about the date command, both the uptime and date commands provide time related information. But the date command specifically displays or sets the system's current date and time.

[00:11:22] It's useful for checking the exact date and time on the system and can be formatted in various ways or used to adjust the system clock. In contrast, uptime focuses on the system runtime information. As the next lecture is on working with

[00:11:42] terminal utilities and is a continuation of this, we shall keep the terminal as it is that wraps up this demonstration on getting started with terminal and execution of some basic commands. See you in the next one.