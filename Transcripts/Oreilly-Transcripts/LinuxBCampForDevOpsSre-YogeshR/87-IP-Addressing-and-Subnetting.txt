[00:00:00] Hello and welcome to this lecture. Ever wondered how your computer communicates over the internet. It all starts with an IP address. In this lecture, we will break down IP addresses subnetting and how networks communicate efficiently IP or internet protocol addressing is a numerical label assigned

[00:00:24] to each device on a network to enable communication. There are two versions. One is IPV four which is internet protocol version four, which is a 32 bit address divided into four octets. It consists of four octets separated by dots which

[00:00:43] is the decimal value ranging from 0 to 255. Say for example, 192.168 0.1 0.1. Next we have IPV six or the internet protocol version six, which is a 128 bit hexadecimal address

[00:01:01] designed to replace IPV four due to address exhaustion. Each IPV four address is composed of two parts network id, which identifies the network host ID, which identifies a device within that network. Let's understand this in detail.

[00:01:23] An IP address is a unique identifier for a device on a network. Think of it as a home address for your computer. It's a 32 bit number, usually written in four decimal parts like 1 92.168 0.1 23.1 32.

[00:01:41] But computers see it in binary like this. For example, in 1 92.168 0.1 23.1 32 if we use a standard subnet mask, which is 2 55.255 0.2 55.0.

[00:02:01] The network portion is 1 92.168 0.1 23.0. And the host portion is 0.0 0.0 0.1 32 this tells the router that the device is on the 1 92.168 0.1 23.0 network.

[00:02:24] Now, how does the system know which part of an IP belongs to the network and which to the host for that? Let's understand what is Subnet mask. A subnet mask is another 32 bit number usually written as a dot B dot C dot D say for example,

[00:02:44] 2 55.255 0.2 55.0 which is in binary three octets which are all ones and the fourth octet all are zeros, the ones represents the network part and the zeros represents the host part.

[00:03:05] So the 1st 24 bits are the network id and the last eight bits are for the host ID IP. Addresses are divided into classes class A include IP addresses where the first octet ranges from 1 to 126.

[00:03:27] However, there is an Exception. 10 is reserved for private networks and 255 is used for broadcast. The default subnet mask is 2 55.0 0.0 0.0.

[00:03:42] And these addresses are mainly used for large networks like ISP S and data centers. 1 27.0 0.0 0.1 is reserved for localhost class B covers IPs with the first octet ranging from 128 to 191.

[00:04:04] An Exception here is 1 72 16 XX to 1 27 31 XX which is reserved for private networks. The default subnet mask is 2 55.2 55.0 0.0.

[00:04:21] Making it ideal for medium size networks such as universities or large organizations. Class C consists of IP addresses where the first octave falls between 192 and 223. The private range in this class is 1 92 1 68

[00:04:42] XX, commonly used in home and office networks. The default subnet mask is 2 55.2 55.2 55.0. And these are perfect for small networks. Class D spans from 224 to 239. In the first,

[00:05:04] these addresses don't have a subnet mask because they are used for multicasting which are mainly for streaming or broadcasting data to multiple devices simultaneously. Class C includes 240 to 255 D first OCT.

[00:05:23] These are reserved for research and experimental purposes. So they aren't used in general networking, private IP addresses are used within organizations and are not routable on the internet. Each class has a default subnet

[00:05:41] mask but sometimes you need more flexibility. This is where subnetting comes in. Subnetting allows us to divide a large network into smaller more manageable subnetworks. This helps in optimizing address allocation and reducing condition,

[00:06:02] classless inter domain routing or Cidr are also called a Cider notation simplifies submitting using a slash notation. For example, if you have an IP address of 192.168 0.1 0.1

[00:06:20] and a subnet mask of 2 55.255 0.2 55.0. The Cidr notation would be 1 92.168 0.1 0.1 slash 24 here slash 24 means 24 bits are for the network leaving eight bits for host addresses.

[00:06:44] Let's take an example 192.168 0.1 23.0 slash 24 which is a class C network and understand how subnetting is done. The default subnet mask for class C network is 2 55.2 55.2 55.0

[00:07:04] which in binary is this, this means the 1st 24 bits are for the network and the remaining eight beds are for hosts. Now, if we borrow one bit from the host portion, the new subnet

[00:07:22] mask becomes like this. Let's determine the number of subnets. The number of subnets can be calculated by using two rays to the power of borrowed bits. Here we are borrowing one bit. So it will be two raised to the power of one which is nothing but two.

[00:07:41] Now, the network is divided into two subnets which is 1 92.168 0.1 23.0 to 127 and 0.1 28 to 255 horse per subnet is calculated by using two rays to the power of remaining host beds minus two.

[00:08:03] Here, the remaining host bits is seven. So two raise to the power of seven minus two which gives us 126. So each Subnet has 126 usable host, which means 128 total addresses

[00:08:21] minus two for network and broadcast. Now, if we borrow two bits, the new subnet mass becomes like this. How did we do that number of subnets as we know is two rays to the power of borrowed bits. So two rays to the power of two will become four.

[00:08:40] Now we have four subnets 1 92.168 0.1 23.0 to 63 then 64 to 127 then 128 to 191. And then lastly 192. To 255 to calculate hosts per subnet,

[00:09:00] we know that it's two raised to the power of remaining host bits minus two. So remaining host bits is six in this case, so two raised to the power of six minus two, which will be 62. So each subnet now has 62 usable hosts, which means 64 total addresses minus two for network and broadcast.

[00:09:25] This process is called subnetting where we divide a large network into smaller manageable subnets based on the requirements. There is always more to explore a network which concludes this lecture on IP addressing.

[00:09:41] See you next.