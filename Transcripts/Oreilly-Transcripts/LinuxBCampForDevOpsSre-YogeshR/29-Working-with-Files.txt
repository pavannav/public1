[00:00:00] Hello and welcome to this lecture in this lecture, we will dive into working with files first, let's review the commands related to the file management and then we will move on to the demonstration. The touch command creates an empty file or updates the timestamp of an existing file.

[00:00:22] It's often used for quickly making placeholder files. The file command in Linux is used to determine the type of a file by examining its contents rather than relying on its file extension. The CP command is used to copy files or directories from one place to another.

[00:00:45] It keeps the original file intact while creating a duplicated destination MV command moves or renames files and directories. It can also be used to change the name of

[00:01:00] a file or to relocate it to a different directory. The Arab command removes files or directories. Be careful as this command permanently deletes files without any confirmation. Now, let's see these commands in action. We will start by creating a think nextx folder again.

[00:01:24] Next, we will create a file inside the next directory. Since we are in slash temp territory, we will first navigate into techniques using the CD command to create a new file. We will use the touch command.

[00:01:43] The touch command creates an empty file. If it doesn't already exist, let's create a file named first TXT by running touch first txt. If we perform a long listing of the files, using LS hyphen L,

[00:02:02] we will see that this file type represents a hyphen which indicates that it's a regular file. We have already discussed the file command in the last lecture. it is used to determine the type of a file. For example, if we run file first dxt,

[00:02:22] it will indicate that the file is empty. Since it doesn't contain any data, we will now add some content to the file while we can use text editors like VI or nano for this. But we will explore those in the upcoming lectures

[00:02:42] for now, we will use the echo command as we learned in the previous lecture. The eco command displays a line of text. However, it can also be used to write text to a file. If the file doesn't exist, it will be created

[00:03:01] to add content to a file. We can use the single greater than simple. It's important to note that this will override the existing content in the file with the new text. If you want to append text instead of overwriting the file,

[00:03:21] use the double greater than simple. We will add a large amount of content to this file. I have gathered some content from the internet and we will add it to the file. In the following way.

[00:03:40] If we use the file command again, we will notice that it now identifies the file as Unicode text with very long lines totaling 902 lines to copy a file or directory. We can use the copy command followed by

[00:04:02] the source and target file or directory names. For example, to copy first text to a new file named second TXD. Within the same techniques folder, you can use copy first dot dxt

[00:04:20] second dot txt. This creates a duplicate of first dot txt and saves it as second dot txt. Both files will now exist in the tech nexts directory. Let's say we want to copy our file to another directory.

[00:04:41] First, let's create a new directory named, test, inciting slash tab directory. Currently, we are inside the think next directory to move one step up to its parent directory. We can use CD command. This is a quick way to navigate back one level in the directory structure.

[00:05:04] Also note that we are using a relative path here. Don't forget to leave a space after city. On a related note, if we want to return to our home directory like slash home slash Ubuntu. In my case,

[00:05:20] we can use the CD tilda or simply CD. This command is a shortcut to jump directly to your home directory. Now let's head back to the temp territory instead of typing out the full path or repeating commands,

[00:05:40] we can return to the previous working territory using CD hyphen. This command acts as a memory taking you back to the last territory you were in now that we are in the slash temp directory. Let's create a new folder named test using the MKDIR

[00:06:04] command. Next, we will copy the first dot txt file from the think nextx folder into this new test folder to do. So we can use the command CP

[00:06:23] text space test. This command specifies the source file, first text and the target location test. Alternatively, you could navigate into the think next folder and use a related path for the test folder.

[00:06:42] There are multiple ways to achieve this and this is one of them to verify that the file was successfully copied. We can use the LS command a less test. You can also copy multiple files into a directory in a single command.

[00:07:02] For example, copy technique first TXT text test this command copies both 1st and 2nd text from the techni folder into the test directory.

[00:07:21] It's important to note that if the test directory already contains a file named first DXT from a previous copy operation, the content of this file will be overwritten by the new first txt. If the new file has different content, the old content will be lost

[00:07:43] to prevent accidental overrides. You can use the hyphen I option which stands for interactive mode. With this option, the system will prompt you to confirm whether you want to overwrite each existing file.

[00:08:01] For example, since first dot txt and second txt already exist in the test folder, you will receive two prompts asking if you want to overwrite them. If you type y, the file will be replaced. If you type in the copy action will not occur and the existing file remains unchanged.

[00:08:23] Thus using hyphen, I adds an extra layer of safety, especially when working with important files. Interestingly, the hyphen V or verbose option in the copy command provides detailed information about the actions being performed.

[00:08:44] This can be useful to confirm what files are being copied and where they are being placed. This output clearly shows the source file in its destination after the copy operation.

[00:09:02] The final operation we will discuss in the copy command is the hyphen R option which stands for recursive. This is used when you want to copy an entire directory along with its subdirectories and files for instance to copy the entire think

[00:09:22] next directory to a new directory called test two. You can use the following command copy hyphen R. The next test two. What happens here is the hyphen R option tells the system to copy everything inside.

[00:09:40] Think next, including all files and subdirectories. If the test two directory doesn't exist, it will be created automatically the entire think nextx directory with all its contents will be copied to test two. This is useful when you need to copy a whole directory structure,

[00:10:00] ensuring all files are included. Let's now dive into the move command. This command is used for two primary purposes, moving files and directories or renaming them to understand how it works. Let's rename second

[00:10:23] TXT to third TXD insect, think nextx folder using the command. Move, think second dot slash dot Unlike the copy command which retains both the source and destination files,

[00:10:41] the move command removes the source and keeps only the destination. Now let's move this renamed file third DXT into the test folder. Move think N slash third dot dxt test this command relocates the file into the test territory.

[00:11:05] Now in test folder, you will find three files. Additionally, the move command can also be used to move entire directories similar to the copy command. It also supports options like hyphen

[00:11:21] hyphen P. Feel free to try out these options on your own. And if you need to explore more features, the man command is your go to resource simply type man MV. Next,

[00:11:40] let's explore the RM command which is used to delete files and directories in Linux to begin. Let's delete the file third dxt from the test folder RM TX. This will remove the file permanently.

[00:12:01] Remember, unlike some graphical interfaces, the RM command does not move files to a trash or recycle bin. If you want to remove an entire dietary along with its contents, use the hyphen R option for recursive deletion.

[00:12:20] For example, let's delete the test folder. RM hyphen R test. You might be wondering how RM hyphen R differs from RMDIR command. The key difference is that remove directory is used to remove empty directories. While

[00:12:40] remove hyphen R is used to remove directories even if they contain files or subdirectories. In our case, since the directory wasn't empty and we wanted to remove the entire directory. Remove hyphen R was the appropriate choice. If you want to suppress warnings,

[00:13:00] you can use the hyphen F option to force the deletion. In the test two folder. We have only two files. First dot txt and second dot txt. If you use the command at S two slash txt

[00:13:21] second, you will get a warning that third TXD doesn't exist. In such cases, you can use the hyphen F option which will ignore nonexistent files and arguments

[00:13:40] and will never prompt for confirmation. Let's try that. However, be cautious when using this option as it can lead to unintended consequences. If not used carefully. You can also use the hyphen

[00:14:00] hyphen V options similar to the copy and move commands. Feel free to explore them. That concludes this lecture. We have covered how to use the common file management commands, like touch file, copy, move and remove. We hope this was helpful and we will see you in the next lecture.