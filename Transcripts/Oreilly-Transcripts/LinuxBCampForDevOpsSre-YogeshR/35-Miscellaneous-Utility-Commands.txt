[00:00:01] Hello everyone. In this lecture, we will be exploring some essential utility commands that can make your work on the command-line more efficient. Let's dive into an overview of the commands we will cover. First, we have date which helps us to display or set the system date and time.

[00:00:25] We will also look at sleep which lets us pause execution for a set amount of time, something that comes in handy in scripts. Then there is time which is really useful for

[00:00:40] measuring how long a command takes to run, which is great for performance monitoring. If you ever need to limit how long a command runs, timeout will come to the rescue by terminating the command if it runs for too long. Now, let's see them in action.

[00:01:04] In this section, we have already discussed the basic usage of the date command in the first lecture. Now let's explore some additional arguments and formatting options available with the date command.

[00:01:21] The date command is used to display the system's current date and time. By default, it shows the local time. If you want to display the current UTC time, you can use the hyphen flag to customize the output format.

[00:01:42] You can use the plus option followed by a format string. For example, if you want to display the date in the format year hyphen hyphen day, a colon, minute colon. Second, you can use the following command.

[00:02:03] Additionally, you can set the system date and time by using the hyphen option. For example, to set the system date and time to December 31st, 2024 at 23 K 59 Ken 59

[00:02:21] you can run this command next, let's move on to the sleep command. The Linux sleep command pauses the terminal for a specified amount of time

[00:02:41] by default, the time is in seconds. For example, sleep phi will wait for five seconds. You can also specify time in minutes, hours or days by appending M or D respectively.

[00:03:00] The time doesn't need to be an integer. For example, you can set it to 2.5 seconds. You might wonder where this can be useful. The sleep command is ideal for introducing delays, controlling execution speed, simulating wait times and managing automation scripts.

[00:03:24] It's essential for tasks like testing rate limiting requests and handling time based actions. In Linux, let's say we want to measure the time it takes to run a command like LS,

[00:03:43] we can use the time command to check the execution time. For example, we can check how long does the LS command takes in the slash user slash bin directory. The output will show three values real user access

[00:04:05] real is the total time elapsed from start to finish. In this case, it's 15 milliseconds. User shows the time the CPU spent on your code and her its two milliseconds, meaning

[00:04:21] very little CPU time was used in user mode. C indicates the time the CPU spent in kernel mode which is seven milliseconds. So in this example, most of the time was spent in the kernel mode with almost no time spent on user

[00:04:41] level code. If you want to limit the execution time of a command, you can use the timeout command. If the time limit is exceeded, the command will be terminated. For example, the last command ls took 17 milliseconds.

[00:05:04] Let's try to restrict it to just three milliseconds. You will notice it lists almost all files. Now let's reduce the time limit to just two milliseconds. And now we will see that the command is terminated early as it

[00:05:22] was cut off when it has reached the files starting with the letter. Yes, this command will terminate if it exceeds the specified time limit, resulting in the execution being cut short and only a few files being displayed

[00:05:43] in the real world. A timeout can be extremely useful by preventing a system or application from hanging indefinitely, ensuring that it either proceeds or fails gracefully when a task takes longer than expected.

[00:06:02] That concludes our lecture on some of the utility commands. See you in the next lecture.