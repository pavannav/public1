[00:00:00] Hello and welcome to this lecture in this lecture. We will dive into working with territories. Before jumping into the demonstration, we will start with an overview of the key commands we will be using. Let's get started. The PWD command displays the full path

[00:00:21] of the current working directory providing the absolute path from the root directory to your current location in the file system. It's a quick way to check where you are within the directory structure. The CD command changes the directory.

[00:00:41] You can use it to move around the file system by specifying the name of the directory. You want to enter the Ellis command lists the contents of a directory. It shows the files and subdirectories inside the current directory or one that you specified.

[00:01:03] The MKDIR command creates a new directory. You give it a name and it creates the directory in the current working directory. The RMDIR command removes empty directories. It will only work if the directory is empty.

[00:01:21] So make sure to clean it out before using this command before we dive into these commands. Let's quickly understand absolute and relative paths. These are the two ways to locate files and directories and understanding them will make navigating the system much more easier.

[00:01:45] An absolute path always starts from the root territory and provides the complete path to a file or folder it begins with, for example, to go to the

[00:02:00] slash user slash bin directory, you would use CD slash user slash bin. No matter where you are in the system, this path will work because it gives the exact location starting from the root. Here we are using the CD command which is used to change directories.

[00:02:24] A relative path starts from your current territory. It doesn't start with slash, but instead uses shortcuts like for the current territory, double dot for the parent territory

[00:02:40] and tilda for your home territory. For example, if your current directory is slash home slash user slash documents and you want to go to the parent directory slash home slash user, you would use CD space double dot

[00:03:01] here double dot moves you up by one level. Now let's say you're in a deeper directory like slash home slash user slash document slash work. If you want to go back to two levels up to slash home slash user,

[00:03:21] you would use CD double dot slash double dot Here is how it works. The first double dot takes you to the slash home slash user slash documents and the second double dot takes you to the slash home slash user.

[00:03:44] Relative paths are great for quick navigation between nearby directories. For example, going from home user documents to slash home slash user slash projects is faster with CD

[00:04:04] projects. However, for deeper or fixed locations, absolute paths are much more reliable. For example, if you are in a long directory structure and need to go to slash user slash bin, using the absolute path is easier

[00:04:24] knowing when to use each method makes navigating Linux much more smoother. Let's see how this works in the upcoming demonstrations. Now let's apply these commands in practice

[00:04:42] to begin, we will check our current territory using the PWD command. The output will show the absolute path of the current directory. In my case, it is Ubuntu which is the home directory for the

[00:05:00] Ubuntu user. We will navigate to the slash temp directory for this demonstration. As we learned in the Linux directory structure lecture, the temp directory is used to store temporary files since the files we are using are only for practice. This is an ideal location

[00:05:23] to move to this territory. We will use the CD slash temp. We can use the PWD command again to confirm that we are inside the slash term folder. Let's look at how to list the contents of a directory using the LS command.

[00:05:44] When you run it, you will see a list of files and directories for a more detailed view including file types and permissions. The LS hyphen L command is used the hyphen L option enables the long listing format,

[00:06:02] providing comprehensive details. Also we hope you can recall that she is the option for the LS command, which is one of the basic elements of a shell command. In this format. The first character of each entry indicates its type.

[00:06:21] If it's a directory, you will see ad at the beginning. If it's a regular file, you will see a hyphen. In our case, everything here is a directory. You might also see other characters such as L for symbolic links, B for block devices, C for character devices,

[00:06:43] P for named pipes, also called as P four, S for sockets. These last five types are special files in Linux, the fifth column of the LS hyphen L output indicates the file size.

[00:07:02] However, you might notice that directories almost always show a size of 4096. Why is that this happens? Because file and directory sizes are measured differently for a file, the size reflects its actual content.

[00:07:22] But for a directory, it represents the space used to store metadata. It's essentially a list of links pointing to the files inside and not their actual sizes. Think of a directory as an index rather than a container

[00:07:41] on most ext three or ext four file systems. The smallest allocation unit is one block which is typically 4096 bytes or four K, which is why directories often display this size when dealing with large files.

[00:08:01] The size can be difficult to interpret to address this. We can use the hyphen edge option along with hyphen L which provides a detailed list of files in a human readable format using units like KB MB, GB, etcetera.

[00:08:20] In this format, the size of the file is displayed in a more understandable way. We will explore the other columns of the LS hyphen L output in more detail in the upcoming sections. Often directories include hidden files to view all the files including hidden files.

[00:08:43] We use the hyphen A option which stands for all in the upcoming sections. We will see the meaning of single dot and double dot Now we will create a directory called, think

[00:09:00] to do this, we will use the command mkdir. Think after the directory is created, if we run Ls hyphen L, we will see that the first character of this description is D which indicates that it's a directory. If you want to view the details of only the think

[00:09:22] nexty itself and not its contents, we can use the command, LS hyphen LD, think next this command will display information specifically about the directory itself and not the files or subdirectories within it.

[00:09:40] The hyphen D flag stands for directory and it tells LS to list information about the directory rather than its contents. Alternatively, you can use the file command to determine the type of the file file techniques. This command analyzes the contents of the file and provides a

[00:10:03] description of its type rather than relying on file extensions. We will discuss more on this in the next lecture. Now, let's say we want to create a directory, directory two inside directory one,

[00:10:20] but directory one doesn't exist yet. If we run mkdir directory one, directory two, it will throw an error. One way to solve. This is by creating directory one first and then running the command again. Alternatively, we can use the hyphen P option with MKDIR.

[00:10:43] This will automatically create the parent directory in my case directory. One, if it doesn't exist already allowing girls to create directory two. In one step earlier, we explore the basic elements of a command where we learned about options and how

[00:11:03] they modify the behavior of a command. We also saw how multiple options can be combined for more functionality. Now let's dive into arguments which are used to specify what the command operates on such as files or directories.

[00:11:23] By now, we have a good understanding of the Linux directory structure where the root directory serves as the top level structure of the entire file system hierarchy. By using arguments, we can interact with different parts of the file system

[00:11:40] without needing to navigate into them using the CD command. For example, if we want to list the contents of the directory, we can simply use the command LS, where is the argument specifying the directory to list.

[00:12:00] This will display the contents of the root directory, including other directories like bin DEV and so on. Additionally, we can use commands with both options and arguments. For instance, if we want to get a detailed listing of the

[00:12:22] temp directory that includes hidden files and present sizes in a human readable format, we can combine options and arguments like this. Ls hyphen lah slash temp. Here, Ls is the command

[00:12:41] hyphen. LH are the options and slash temp is the argument specifying the temp directory. This is a clear example of how we can use both options and arguments with the command to refine our desired output.

[00:13:02] Let's wrap this lecture by exploring the RMDIR command in Linux. The RMDIR is specifically used to remove MD directories. Let's delete the think mix directory. Since it is an empty directory, the RMDIR

[00:13:22] command will successfully remove it R MD A Y that concludes this lecture on commands for working with territory. We will see you in the next one.