[00:00:00] Hello and welcome to this lecture. Memory management is a fundamental aspect of the Linux operating system, ensuring efficient utilization of the system resources and maintaining optimal performance. The Linux kernel employs a sophisticated memory management subsystem

[00:00:23] that handles various tasks including virtual memory implementation, demand, paging memory allocation for both kernel structures and user space programs and file mapping into process address spaces.

[00:00:40] Now let's discuss some of the key components of managing memory in Linux virtual memory is a technique that allows a computer to use its hard disk to extend its ram or random access memory. It's used when a computer doesn't have enough ram to run programs.

[00:01:04] When a computer runs out of ram, it moves data from ram to the hard disk, the computer swaps data between ram and the hard disk as needed. Virtual memory allows the computer to run programs

[00:01:20] that require more memory than it has. Linux provides each process with its own virtual address space creating the illusion of a large contiguous memory area. This abstraction allows processes to operate as if they have access to

[00:01:41] the full address space regardless of the actual physical memory available. The kernel manages the mapping between virtual address and physical memory. Enabling efficient memory utilization and process isolation

[00:02:00] memory is divided into fixed size blocks called pages. When a process accesses a page not currently in physical memory, a page fault occurs prompting the kernel to load the required page from disk into ram. This mechanism supports the execution of large

[00:02:22] applications and efficient use of physical memory. The kernel maintains page tables to track the mapping between virtual pages and physical frames. In Linux swap space refers to a designated area on a storage device that the

[00:02:43] operating system uses as an extension of the system's physical ram. When the physical memory is fully utilized, the kernel moves inactive or less frequently used data from ram to the swap space,

[00:03:00] thereby freeing up ram for more active processes. While this prevents out of memory errors, excessive swapping can degrade system performance due to slower disk io compared to ram to enhance performance. Linux uses a page cache to store frequently accessed this data in memory.

[00:03:27] This caching mechanism reduces disk io operations. When a process reads a file, the kernel first checks the page cache. If the data is not present, it reads from disk and update the cache.

[00:03:45] The kernel organizes physical memory into zones to optimize allocation zone underscore D is reserved for devices that can only access low memory addresses

[00:04:02] zone underscore normal is used for general purpose allocations zone underscore high mem is available on systems with more than four GB of ram which is used for high memory allocations. These zones help the kernel manage memory efficiently

[00:04:24] ensuring that devices and processes access appropriate memory regions for efficient memory management of kernel objects. Linux employs the slab allocator.

[00:04:40] It maintains caches of commonly used objects allowing for quick allocation and de allocation, thereby reducing fragmentation and improving allocation efficiency. Linux allows processes to allocate more memory

[00:05:01] than is physically available. A behavior known as overcome it. The kernel uses heuristics to manage, overcome it, balancing the need for memory allocation with the risk of running out of memory. The overcommit handling policy can be configured

[00:05:23] to control the behavior of memory allocation requests on multiprocessor systems. Linux supports Numa architectures where memory access times vary depending on the processor's proximity to the memory.

[00:05:42] The kernel optimizes memory allocation to minimize Latency and improve performance by allocating memory on the same node as the requesting processor, reducing the need for cross node memory access

[00:06:00] understanding these components provides insight into how Linux manages memory ensuring system stability and performance across various workloads. Now it's time for a hands on demonstration understanding current memory utilization

[00:06:22] helps in identifying potential issues. The free command provides a snapshot of the system's memory usage. Three hyphen H the hedge flag displays the output in a human readable

[00:06:40] format showing total used and available memory for real time monitoring of system processes and memory usage execute top this command has been discussed in detail and process management. Let us customize the output.

[00:07:01] You can customize the output of top command to display only memory related fields. Press the F key to enter the field management mode. This mode allows you to select which field are displayed, navigate and select memory fields.

[00:07:21] Use the arrow keys to navigate through the list of available fields. Press the D key to deselect all fields which are not relevant, leave only the memory related fields. So for example, let's choose percentage, ma'am, virtual memory,

[00:07:40] resident memory and shared memory. Once you have selected the desired fields, press Q to return to the main top display. As we have discussed earlier, we can sort the processes by memory usage by pressing the caps lock

[00:08:02] key on the keyboard. This will arrange the processes in descending order based on their memory consumption, press Q to exit the top interface. Now let's move on to another command VM stat or virtual memory statistics

[00:08:24] previously covered in monitoring section. VMSTAT not only provides CPU metrics but also offers insights into memory usage, disk activity and swap performance. Say for example VM

[00:08:42] 210 two will refreshes every two seconds 10 which is to collect 10 recordings before stopping. Let's discuss about two of the significant files slash slash mem info

[00:09:00] and slash slash VM of proc territory in Linux which feeds the configurations and values to the command, select top VM etc slash proc is virtual file system that provides real time information about the system processes,

[00:09:21] hardware memory and kernel parameters. Brock mem info is a virtual file in Linux that provides detailed information about system memory usage. It contains various statistics related to total memory available memory cached memory and swap space

[00:09:45] to check memory details use cat slash bro slash mem info. Some of the commonly found fields include mem total which is the total physical memory or ram in KP

[00:10:02] mem freee which is unused memory available mem available which is estimated available memory for new applications buffers which are memory used by kernel buffers cached is the memory used for caching files.

[00:10:21] Swap. Total is the total swap space swap free is the unused swap space active or inactive. Is the memory pages actively used or recently used dirty is the memory pages waiting to be written to disk and many others to explore

[00:10:42] or you can even filter specific values by using the command like this. Next is BRO vmstat. It's a virtual file in Linux that provides detailed statistics about the virtual memory subsystem.

[00:11:04] It contains various counters related to memory usage, paging and swapping, check the contents of slash brock slash vmstat using cat slash slash

[00:11:20] vmstat pipe less. Some of the commonly found fields include P GPG in or P GPG out which is the number of pages read from or written to the disk PWP PSP out which is the number of pages swapped in or out

[00:11:41] PG fault or PG fault. Is the number of page faults or major page faults. Nr underscore underscore pages, which is the number of free memory pages. Nre di is the number of dirty or the modified but not written pages

[00:12:00] in our underscore right back, which is the number of pages actively being written to disk. Common use case for the file is it helps in analyzing memory pressure and swap activity useful for diagnosing high page faults or excessive swapping

[00:12:21] can be used to optimize kernel parameters for memory management. As discussed in previous lecture, swap space acts as a overflow for your RM allowing the system to handle additional tasks by moving inactive pages from RM to disk.

[00:12:42] This is particularly useful for systems with limited physical memory. First, let me check existing swap space, verify if any swap space is already configured. So to swap on hyphen hyphen show

[00:13:01] no output is written indicating that no swap space is currently active. Let's try grab swap slash brock slash me for displays zero in the output. It appears that our system currently has no swap space configured as indicated by the outputs of groupb

[00:13:22] command and cat command. Both showing no swap entries swamp space is essential for systems with limited physical memory or ram to prevent out of memory errors by providing additional virtual memory. Creating a swap file is a straightforward method

[00:13:42] to add swap space without modifying this partitions. We need to decide on the size of the swap file based on your system's requirements. A common recommendation is to set the swap size equal to the amount of ram for systems with up to two

[00:14:02] GB of ram and for systems with more than two GB of ram, a swap size of two GB is often sufficient. Use the F allocate command to create a swap file of the design size. For example, to create a two GB swap file. Let's use the command.

[00:14:24] So do if allocate hyphen L two G swap file, F allocate is a command used to pre allocate disk space efficiently. Hyphen L two G specifies the size of the file which is two gigabytes. In this case

[00:14:44] slash swap file is the name of the location of the swap file being created. If, if allocate is not available or doesn't support your file system, you can use DD. Like this

[00:15:01] is the command used to copy and convert data at a low level. This indicates input file or if is slash dev slash zero. A special file that provides an infinite stream of zero bytes. This indicates output file or O

[00:15:22] is slash swap file which will store the swap space. The block size or PS is set to one megabyte per write operation. This writes 2048 blocks of one megabyte each creating a two GB swap

[00:15:40] file. So this command creates a two GB file named swap file. As F allocate is working for me, I will not be executing this

[00:16:06] to set up the swap area on the file. Let's use the command. So to mkswap swap file,

[00:16:27] verify that the swap is active by running the command. PSE to swapon hyphen hyphen show this command displays the swap file, swap file as active

[00:16:41] to ensure the swap file is used after a system reboot added to the FSTAB file, edit the file, open the file in a text editor. Add the new line at the end of the file slash swap

[00:17:00] file, none swaps W 00. This line tells the system to use swap file as swap space, save and exit. Now let's learn about another phi slash bro

[00:17:20] slash S slash vam. This directory within the slash file system contains various files that allow users to read and modify settings related to kernels, virtual memory management, including parameters like swappiness which controls how

[00:17:41] aggressively the system swaps memory to disk. You can check the default swappiness value in Linux using the command cat slash slash slash VM slash swappiness. The value is usually 60 by default on most Linux distributions.

[00:18:02] Swappiness ranges from 0 to 100 0 is the minimum swapping only swap when absolute necessary 100 means aggressive swapping move inactive pages to swap frequently. You can change the swappiness value using the

[00:18:22] CTL command temporarily change swappiness until reboot using the command pseudo CTL hyphen W VM swappiness equal to 30. This set swappiness to 30

[00:18:40] but it will reset after a reboot to make the change persistent across reboots, edit the Cital configuration file, pseudo nano etc add or modify this line VM swapping is equal to 30

[00:19:01] save and exit. Apply the changes without rebooting. Run the command pseudo C hyphen peak. The hyphen P option stands for load configuration from a file. By default, it loads settings from the

[00:19:20] ctl.com file. You can check the setting again by using proc VM swappiness. The output displays the changed value of 30. If you no longer need the extra swap space, this can be removed with swapoff command.

[00:19:41] So do swapoff swap file. You can verify the status by swap on hyphen S do not forget to remove the entries from the slash etc slash FSTAB file. That concludes this lecture on memory management in Linux. See you in the next.