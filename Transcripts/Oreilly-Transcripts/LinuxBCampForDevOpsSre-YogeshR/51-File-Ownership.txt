[00:00:00] Hello and welcome to this demonstration. In this demonstration, we will explore Linux file permissions by setting up a file called think next with different access levels for three users, Alex Bob and Charlie. Alex is the file owner and team lead,

[00:00:21] Bob is a team member in the developers group and Charlie is an outsider with no access. We will demonstrate how ownership and permission settings impact each other's ability to interact with the fight. We will cover these steps,

[00:00:42] set up the users, Alex Bob and Charlie and create a group called developers create the think nextx file in site share directory set, Alex as the owner and assign the developer group as the group owner.

[00:01:00] Ensure Alex has read, write access. The developer group has read only access and others have no access, verify access for Alex Bob and Charlie under different permission settings.

[00:01:21] Let's dive into step one first, we will create the users, Alex Bob and Charlie and then set up a group called developers.

[00:01:50] Charlie will not belong to any special group. Let's verify the group membership.

[00:02:00] Let's switch to the user Alex to set up a shared directory. In the previous section, we simply used su hyphen followed by the username, but now we are using Sudu in front of it.

[00:02:20] The difference is that su hyphen Alex switches to the user Alex and requires Alex's password. While Sudu su hyphen allows the current user to switch to the root user by providing their own password granting elevated privileges.

[00:02:45] We will create a directory named shared under slash home. Since creating directories and slash home requires elevated permissions. We will usese

[00:03:00] Sudu mkdir hyphen P slash home slash shared by default. This directory will have both its user and group ownership set to root because we used su let's verify the directories' ownership and permissions.

[00:03:22] LS hyphen LD slash home slash shared at this point. The directory is owned by root and root. We want Alex as the owner and the developers group to have access to do this. We will use the C

[00:03:42] command so to C on Alex Oen developers slash home slash shared. This sets Alex as the user owner and the developers group as the group owner of the slash home slash share directory.

[00:04:04] Next, we will modify the directory permissions to ensure both Alex and developers's group have read, write and execute permissions. So we will use SUDU chmode 770

[00:04:22] slash home slash seven for full permission, read, write, execute for the owner. Alex. The second seven is for full permission, read, write, execute for the group developers and zero means no permission for others.

[00:04:43] Let's verify the changes. The output should now show Alex as the owner, developers as the group and the permission string. D RWX, RWX with three hyphens.

[00:05:03] Now let's navigate into the shared directory and create a file named think Nextx. By default. Since Alex created the techniques file, both the user owner and the group owner are set to Alex.

[00:05:24] We want to change the group ownership to developers to ensure the group members can collaborate on this file to do this. We will use the C command. So do C on

[00:05:40] Alex colon. Developers think next here, this sets the user owner to Alex and this sets to group owner to developer. Let's check the files ownership and permissions. LS have an L. Now, both Alex and the members of the developers group can

[00:06:03] work with the techniques file based on the file's permission. Next, we will configure permissions so that Alex has readwrite access. The developers group has read only access

[00:06:21] and others have no access. As we can see, Alex already has redry access. So let's set it for group and others. The permission now allows Alex to read and write.

[00:06:43] The developer's group can only read and others have no access. Let's test as Alex, Alex should be able to read as well as write to the file

[00:07:01] paint mix. Let's add some content to the file eco let's say test from Alex and then if we can think mix, we should be able to see the string. Next. Let's log in as Bob and test read and write permissions.

[00:07:22] Sudu su hyphen Bob CD slash home slash shared. Let's codify now it's successful. Let's attempt to write. They should fail. Bob can read but cannot write because of the read only group commission.

[00:07:46] Let's change permissions to make the file writeable for the group. Last time we used symbolic mode this time, let's try numeric mode. Let's exit

[00:08:00] C mode 660. Think next hyphen L. Now, group members can read and write. Let's verify as Bob again. This time, we see that Bob can not only read the contents of the file

[00:08:25] but write to the file as well because of the updated group permissions. Finally, let's switch to charging and verify his access. Let's exit sudu

[00:08:40] su hyphen Charlie CD slash home slash shared. We see he's not able to accomplish it cat slash home slash shared slash technics. Let's try to

[00:09:00] write something to the file. We see that Charlie is unable to view or modify the contents of the file. Let's exit. By now, you should have a solid understanding of how to manage file permissions and control user access.

[00:09:23] In our previous lecture, we discussed the differences between file and directory permissions. Now let's take a closer look at how directory permissions work as they differ significantly from file permissions.

[00:09:42] We are logged in as Alex. Now, first, we will create a directory named test directory with the permissions 770. This means Alex, the owner and group will have full access, read, write and execute

[00:10:02] others won't have any access to the territory. We will keep Alex as the user but change group to developers.

[00:10:25] Now let's check the directory axis, Alex as the owner has full permission. Let's check this by creating a file in the directory. Let's create file one.

[00:10:43] Alex is able to see the newly created file. Hence, Alex, the owner can create list and access files inside the directory as he has full access to it. Charlie is not in the group and doesn't have permission. Let's check that

[00:11:06] we see the permission has been denied. Bob is a member of the developers group so he should have full access to the directory too, right? Let's check that

[00:11:26] since Bob already has full access to the directory, they can create delete and modify files in the directory. Bob can create the file Bob file because he has right access.

[00:11:42] Let's stress that Bob can delete file one even though it's owned by Alex because he has full access to the directory. We will now log in as Alex and create the file

[00:12:04] one inside test territory again with the same permissions as before. Now, we will introduce the sticky bit to prevent Bob from deleting files that he doesn't own even though he has right access to the directory,

[00:12:24] the ch mode plus the command adds the sticky bit to the directory you can also use T to achieve the same result where O stands for others indicating that the sticky bit is applied to the other permissions.

[00:12:46] When listed with LS hyphen lt, the sticky bit appears as the T at the end of the directory's permission, you might see either an upper case or lower case T.

[00:13:00] Now how they are different, the sticky bit is shown as lower case T when the execute permission is set for others, it's shown as uppercase T when the execute permission is not set for others. In our case, since execute permission is not set for others, it shows as an upper

[00:13:23] case, let's assign, execute permission to others and check the director's permissions. Notice that the sticky bit appears as the lower case T we will revert this change.

[00:13:40] Now, let's see if Bob can delete this file that he doesn't own. We see that Bob cannot delete file one this time because of the sticky bit. The sticky bit ensures that only the owner of a file can delete or modify it.

[00:14:01] But Bob can create view and delete his own file, Bob file because the sticky bit allows users to delete or modify only their own files in a shared directory. Let's try deleting it.

[00:14:22] You see it's successful. Thus, the sticky bit ensures that even if multiple users have right access to a directory, only the file's owner can delete or modify their own files. Importantly, this only applies to directories and does not affect files.

[00:14:46] Other users can't delete these files, they don't own even if they have right access to the directory. But what happens if we change the owner to Bob?

[00:15:12] However, both the owner and the root user can change the group ownership for regular users.

[00:15:20] They can only change the group if they are a member of the target group. Now, if Bob tries to delete file one, again, the operation will be successful because Bob is now the owner of the directory.

[00:15:40] In the previous section, we learned how to change group ownership using the C group command. But did you know you can also use the C owned command for this? For example, if you want to change the group ownership of slash

[00:16:00] home slash shared slash test territory from developers to contractors, you can run this command here is how it works. The colon tells C to only update the group ownership, the user ownership remains unchanged.

[00:16:22] Let's not execute this as there is no group by the name contractors. But we hope you understood the concept that concludes this demonstration. Please do not delete these users or their groups as we will continue using them in the next lecture.

[00:16:40] See you there.