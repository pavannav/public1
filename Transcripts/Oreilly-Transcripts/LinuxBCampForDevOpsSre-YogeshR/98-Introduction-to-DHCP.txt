[00:00:00] Hello and welcome to this lecture. DHCP or dynamic host configuration protocol is a network protocol used to automatically assign IP addresses to devices on a network. When a device joins a network, it needs an IP address to communicate with other devices.

[00:00:22] DHCP simplifies this process by allowing devices to receive their IP address dynamically from a DHCP server. Instead of having to manually configure the address. The role of DHCP and network connectivity is crucial.

[00:00:42] It eliminates the need for administrators to assign IP addresses to each device manually which can be error prone and time consuming. DHCP also ensures that each device gets a unique IP address

[00:01:00] and that IP addresses are efficiently managed within a network. When you connect to a network, your device will typically use DHCP to request an IP address from the DHCP server. Once assigned your device can use this address to

[00:01:20] communicate with other devices and access network resources. You can check the current network configuration of your system using the command IP. This command shows information about all network interfaces including the IP addresses assigned to them.

[00:01:43] Here is how to use it. The output displays the network interfaces including details like IP address or int mac address and other configuration information. If your system has received an IP address through

[00:02:01] DHCP, it will be listed under the appropriate interface example, a NX zero or Wlan zero, etcetera. Some of the important components of DHCP are the DHCP server. It's the system that assigns IP addresses and network configurations to connect

[00:02:24] DHCP client is the device which is a computer phone, etc that requests an IP address from the DHCP server IP address pool is a range of available IP addresses that the

[00:02:41] DHCP server can allocate to clients lease time is the duration for which an IP address is assigned to a client before it needs renewal system. D hyphen network D is a system service used to manage network configuration on Linux systems.

[00:03:04] It handles network interfaces and can automatically assign IP addresses via DHCP. Making it a key component in modern Linux networking. And Netplan is the default tool used for network configuration on

[00:03:22] Ubuntu 24.04. And later, it allows you to define network settings using YAML configuration files. And it works alongside system D hyphen network D to manage network interfaces. Now that we know what systemd networking and Netplan are.

[00:03:44] Let's observe how they manage DHCP. Network settings including DHCP are defined in configuration files located in slash etc slash netplan.

[00:04:00] Let's start with a quick demo and have a look at the main configuration file on Ubuntu 24.04 CD slash TC slash Netplan to change to the directory and then LS to list the file.

[00:04:27] Let me explain the key parts of this file version two. This is the version of the Netplan configuration format. This section defines your Ethernet interfaces and

[00:04:40] you can configure multiple ones here. This is used to match a specific network interface in this case by its MAC address. This enables DHCP for IPV four. Meaning your system will automatically receive an IP address from the DHCP server.

[00:05:01] This disables DHCP for IPV six, meaning it won't request an IPV six address. This sets the network interface's name to ENX zero. This configuration here tells IPV four connectivities enabled

[00:05:21] and has prevented the system from attempting to configure an IPV six address. Now let's focus on lease time while most systems automatically handle DHCP leases in the background. The DH client tool allows you to manually request release or renew IP addresses from a DHCP

[00:05:45] server. DH client provides granular control over the DHCP process which is especially useful in troubleshooting demonstrations or configuring network interfaces manually.

[00:06:00] It's particularly beneficial when you need to manually release and renew DHCP leases. You can also specify different options when requesting a lease which makes DH client ideal for demonstrations in network configurations or testing how

[00:06:21] DHCP work in a controlled environment. So do apps install I

[00:06:41] hyphen DHCP, hyphen client. Let's provide hyphen V once installed, you can use DH client to manually request a DHCP lease release it or renew it. This is useful when you want to see how the system interacts with the

[00:07:01] DHCP server or if you need to troubleshoot IP assignment issues. But in our case, it's awsec two server. So IP address is already assigned. You can verify this by executing the command. So do the client. This command sends a request to the DHCP server for a new IP address,

[00:07:23] but you will not get the expected result here as it throws IP is already assigned and this cannot be changed. According to AWS policy, the lease information provided by DHCP servers is stored in a log file.

[00:07:41] You can check the lease information for your system in the DHCLIENT lease file. The file is typically located at DHCP. DH leases review this file. Let's use the cat command,

[00:08:01] the contents of the DH client leases file show the DHCP lease information for the interface ex zero. The key details are fixed IP address, subnet mask router or the default gateway DNS server, lease time, broadcast address, hostname, domain name, etc.

[00:08:25] The lease details also include renewal, repoint and expiration time stamps showing the current leases life cycle. This file contains details about the IP addresses.

[00:08:40] Your system has been assigned for the interfaces, lease durations and other DHCP related information. It help tracks the lease history and can be valuable for troubleshooting network issues. You can also check the lease time of IP by using the command

[00:09:00] IP A show an NX zero A X zero should be replaced by your interface. The output of IP show ex zero shows the DHCP lease details for your ex zero interface. This shows the valid lifetime of the IP address,

[00:09:21] which is the time and seconds remaining before the lease expires. And the system needs to request a new IP. This is the preferred lifetime which indicates the time and seconds until the system can no longer use the IP address without refreshing the lease.

[00:09:40] So both values indicate that the DHCP lease for this IP will remain valid for about three seconds before it needs to be renewed. That concludes this lecture. Thank you.