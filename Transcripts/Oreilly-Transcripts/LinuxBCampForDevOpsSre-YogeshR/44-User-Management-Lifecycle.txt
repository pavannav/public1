[00:00:00] Hello and welcome to this lecture in this lecture. We will explore user management in detail before managing users in Linux. It's essential to understand the key configuration files that store critical user related information.

[00:00:20] These files define how users are created managed and authenticated. Let's explore the key files involved in Linux user management. In Linux, user management revolves around four crucial system files.

[00:00:42] First, we have slash etc slash passwd which stores essential information about user accounts such as usernames, uids, home directories and default shells. However, to enhance security, actual passwords are not stored here

[00:01:05] instead, hashed passwords and password policies are securely kept in slash etc slash shadow a file accessible only by the root user. This separation prevents unauthorized access to sensitive data

[00:01:25] slash etc slash scales. As a template directory stands for skeleton, it holds default configuration files like PRC and

[00:01:40] profile. Whenever a new user account is created, these files are copied to the user's home directory. Ensuring they start with the preconfigured environment.

[00:02:13] Always use SUDU V SUDU to edit the file as it checks for syntax errors before saving changes.

[00:02:26] Now that we have learned about the essential system configuration files involved in managing users. Let's dive into a practical demonstration. We will explore these files directly in our Ubuntu

[00:02:43] terminal to see how Linux handles user account information, password security and default user settings. In real time, we will begin by examining the slash

[00:03:00] etc slash pass W file which stores essential user account details, running the command cat slash, etc slash passwd displays a list of all user accounts. Each line represents a user with fields separated by colons.

[00:03:23] Let's break down the entry for our default user Ubuntu. The first field shows the username Ubuntu which serves as the login name. The second field contains an X indicating the

[00:03:41] actual password is securely stored in another file. Next we see the unique user id or UID which helps identify the user in the system. Next, we have the G id or group id linking the user to a primary group.

[00:04:02] After that, the home directory specifies where personal files are stored. And finally, the default shell determines the command-line interface assigned to the user. The UID range is structured as follows.

[00:04:21] UID zero is reserved for the super user or root account granting full system access. Uids from 1 to 200 are statically assigned to system accounts for specific system processes.

[00:04:40] Uids between 21 and 999 are dynamically assigned to system processes that do not own file on the system but need an unprivileged UID. Finally, uids starting from 1000 are allocated to regular unprivileged users.

[00:05:19] Since this file contains sensitive information,

[00:05:23] it's only accessible by the root user. Each line includes key details related to passwords and account security. Let us understand this line in detail.

[00:05:41] Ubuntu is the account name after the username is the password hash. Here, exclamation mark means the password is locked, preventing password based authentication. This is typically for cloud based instances where

[00:06:01] SSH key authentication is used, you may also encounter other options in the password hash field such as double exclamation mark, which indicates the password has never been set as the risk signifies that no password can be used to access the account

[00:06:23] a hashed password like this is used for regular password based authentication. This is the number of days since Jan 1st 1970 when the password was last changed,

[00:06:43] zero is the minimum number of days required between password changes. This is the maximum number of days before the password must be changed. This indicates that practically unlimited password lifespan

[00:07:02] seven is the number of days before expiration to want the user. We see that inactive days are not set, meaning no specific account inactivity policy. No expiration date is set as well.

[00:07:24] Next, let's explore the slash etc slash scale directory using the command ls hyphen A slash etc slash scale. This directory holds default configuration files that are automatically

[00:07:42] copied to a new user's home directory upon account creation files like dot bashrc dot profile and dot bash logout ensure that new users start with a preconfigured environment

[00:08:20] just below the comment user privilege specification.

[00:08:24] You can find user root and their privileges. Here, root is the username for the system's super user account. The first all in the line says this rule applies to all hosts or machines.

[00:08:42] 2nd and 3rd, all in the braces indicates that the root user can run commands as any user and any group. The fourth all means that root can execute any command without restriction.

[00:09:01] In short this line grants the root user full unrestricted access to run any command as any user on any machine. It is the default behavior for the root account.

[00:09:48] Now,

[00:09:49] moving on to managing users, we will explore how to create modify and delete user accounts. In Linux.

[00:10:00] This includes assigning appropriate permissions, setting passwords and configuring user environments to ensure efficient system administration. Linux provides powerful commands for managing users.

[00:10:21] The commands discussed here will be for Debian based systems like Ubuntu, a user creates a new user account and adds the details to the system files like slash etc slash pass

[00:10:40] by default. It creates the home territory based on slash etc slash skill passwd involves changing user passwords. It offers other functionalities such as updating password aging policies and unlocking user accounts.

[00:11:04] User mode allows modifying user details such as changing the home territory, adding the users to groups or updating account settings. Dell User deletes a user removing them from slash

[00:11:22] etc slash passwd. You can choose whether to delay the user's home directory as well. Let's go through user management in Linux, starting with creating, modifying and deleting users,

[00:11:40] we will create a new user dev user to demonstrate these actions. First, let's create a new user named devuser.

[00:12:03] As you can see the user got added and is also part of new group that is the primary group, web user home directory is also being created. It prompts for the password for the user.

[00:12:20] Let's do that retype the new password. The password is updated successfully. Enter the full name for you, sir. If needed, then enter the other info like room number, work info, etcetera. We will be skipping this for this demonstration

[00:12:42] press y to confirm and the user is added. Let's check the slash etc slash passwd file to see the details of the newly created user cat slash, etc slash pasta beauty.

[00:13:02] We can see that the DEV user has been added to slash etc slash passwd with the standard details including its home territory slash home slash devuser slash bin slash bash. As the default,

[00:13:21] you can see two regular users, the default user Ubuntu and devuser with UID and GID assigned to them. You can also observe the full name and blank commas for the additional info which we skipped while creating the user devuser.

[00:13:41] Now let's add devuser to pseudo file to give devuser administrative privileges, pseudo just below the user privilege specification copy the routes, entering and paste it just below it.

[00:14:03] Change the text route with DEV user and then save and exit the file. Now that DEV user is created. Let's change the password for this user

[00:14:21] to do that. The SUDO command is used alongside passwd, followed by the username, SUDU passwd de user. You will be prompted to enter and confirm the new password

[00:14:40] as a system administrator. It's crucial to keep your root password secure. If you need to create or change the root password, we can switch to super user root using the command. Sudo hyphen as demonstrated earlier

[00:15:01] or you can also use su hyphen. This command attempts to switch to the root user but it requires the root password which is unset or locked by default. This is a Linux security measure to prevent direct access to the root account.

[00:15:25] If you simply press enter, it results in an authentication failure,

[00:15:40] you will be prompted to enter and confirm the new password for the root user type. Su hyphen again and it asks for the password set by you by typing it. You will now be able to log in as a root user

[00:16:00] type exit and get back to Ubuntua. Now go to Ubuntu's home territory by typing CD tilda. Let's now explore how to force a user to change their password upon their next login effectively managing password expiry.

[00:16:27] We will start by checking the current password expiry status of the user first type the following command to view the password expiry details for the user to

[00:16:40] user. So to ch hyphen L do the output list details such as when the password was last changed, the minimum and maximum password age. And we can see that password expires is set as never

[00:17:04] to enforce a password change immediately upon the next login. We can use the PASSWD command with the hyphen E option. Let's run SUDU passwd hyphen. Ew

[00:17:22] This command expires the password for DEV user forcing the user to update it during their next login. The output confirms that the password has been changed. Now, let's verify the status again by using

[00:17:41] SUDU chh hyphen and devuser. This time, the output shows that the password must be changed. This ensures the user will be prompted to update their credentials at their next login attempt.

[00:18:00] Next, let's switch to the user account by typing SU DEV user immediately. The system prompts for a password. After entering the current password, it enforces a password update. The user is prompted to provide the current password again,

[00:18:23] followed by entering a new password and confirming it by retyping it. Once the password is updated successfully, the user is logged in this process ensures that the password is changed securely

[00:18:41] and aligns with the organization's password management policies, type exit and get back to Ubuntu user. If you need to temporarily lock the password of an account,

[00:19:00] you can lock it, use the command passwd hyphen L command followed by the username hyphen L option disables a password to log the password of DEV. User, execute the command to

[00:19:20] pass W hyphen L devuser the output messages password changed. Try sue DEV user and enter the password. It will display authentication failure message. This prevents the user from logging in until the account is unlocked.

[00:19:46] If you later decide to reenable the locked password, use the password hyphen new command to unlock the user account. So to pass the hyphen, you debut.

[00:20:03] Now, the user can log in again, type su devuser, enter the password and it switches to DEV user but still in the home directory of the open to user. But when we execute CT tilda, it switches to DEV user's own home directory

[00:20:27] type exit and get back to, to you sir. Next to enforce a password expiration policy for a user, you can specify a maximum number of days before their password expires.

[00:20:46] For instance, if you want the user's password to expire after 30 days, you can use this command. Sudu ch hyphen M 30 W

[00:21:02] or you can also use SUDU passwd hyphen X 30 W. This ensures that DEV user will have to change their password every 30 days. That is maximum number of days between password change is 30 days.

[00:21:24] You can check the password expiration settings for any user by running the command. Sudu CH hyphen L Deer. This displays the aging policy set for the particular user.

[00:21:40] Like you can see the last password change, password expire set and just see the maximum number of days set to 30. Now, let's modify DEV user. We will change the home directory of DEV user to

[00:22:03] slash home slash DEV home using user mode command. First, let us switch to the DEV user using Sue hyphen devuser. This command will switch directly to home directory of

[00:22:20] the user and then type pwd to see the default home directory created automatically with user creation. Let's create some files using touch DEV file one da file two, then type a hyphen L and you can see the files in the home directory.

[00:22:44] Next type exit and come out of the home territory of the DEV user and logged in as the user. Now, suppose you want to change the home directory of the DEV user and move all the contents of the previous home directory.

[00:23:05] Then type so do user mode for user modification, hyphen D for directory and then mention the new home territory which is slash home slash DEV home

[00:23:21] and give it a space hyphen hyphen move hyphen home and then mention the username, which is DEV user in our case. And then press enter. Now again, switch user go to user's home directory by typing CD.

[00:23:42] Verify by typing PWD and LS hyphen L to list the files, you can see the files are present in the new home territory. Let's verify the change by inspecting in slash

[00:24:01] etc slash passwd, file, cat slash, etc slash passwd. And you can see that the new home directory is updated for devuser. Devuser now has slash home slash DEV home as the home directory. The user

[00:24:20] UID and GID D remains unchanged, but we have successfully updated the home directory type exit and go back to, to user. Next, let's change the UID of DEV user and update the default shell to slash bin slash

[00:24:43] ZSH. So to usermode hyphen U 2000 DEV user here, hyphen U stands for U, we are setting it to 2000 and then providing the user name.

[00:25:03] Let's press enter next. So do usermode hyphens slash bin slash ZSH user here, hyphens stands for shell.

[00:25:21] We're changing it to slash bin slash ZSH and then we are entering the username. Let's press enter warning message is due to executable file for slash bin slash ZSH, not installed

[00:25:41] to avoid the warning and to use the shell, execute the command. So do act update. So do app install hyphen Y ZSH.

[00:26:01] Let's check the slash etc slash passwd file again to confirm the changes, etcd devuser now has a new UID 2000 and the default shell has been updated to slash

[00:26:20] bin slash ZSH. The primary group ID or GID remains unchanged and the home directory is updated to slash home slash DEV. Finally, let's delete devuser from the system using Dell user.

[00:26:41] So to tell you sir, Dave and score user. The command executed will remove the user. Let's verify by executing. Sue hyphen devuser. It displays the message DEV user does not exist

[00:27:01] but the home directory of user will be intact. Let's check that by changing the directory CD slash home LS hyphen L shows the home directory. The home is not removed. If you need to delete the home directory of the user,

[00:27:22] including mail files and personal configuration, then execute the command. So do tell the user hyphen hyphen remove hyphen home DEV user, we will not be executing this command as it will throw an error because the user does not exist

[00:27:41] to free up the memory hyphen hyphen remove hyphen home option can be used with TUSER now type CD tilda to get back to home directory of to user. Let's check the slash TC slash password again

[00:28:00] to ensure that the user has been removed as expected. Devuser is no longer in the slash etc slash pass file, confirming that the user has been deleted. That concludes this lecture on user management commands.

[00:28:20] See you in the next lecture.