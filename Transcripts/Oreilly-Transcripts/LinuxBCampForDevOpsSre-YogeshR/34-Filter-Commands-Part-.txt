[00:00:00] Hello and welcome to the next lecture. Let's begin with the next set of filter commands. The sort command helps you organize lines of text from input or a file in ascending or descending order based on specific criteria.

[00:00:20] Unique removes consecutive duplicate lines from sorted input, leaving only unique ones for clarity. NL adds line numbers to each line of a file or input, making it easier to reference specific lines,

[00:00:42] cut extracts specific part of lines from files or inputs. Often used to work with columns or fields with DR you can translate or replace characters in the input, which is useful for text substitutions.

[00:01:01] Column, takes input and arranges it into neatly aligned columns, making data more readable pr formats, text files for printing, adding features like page numbers and headers for easy Pagination

[00:01:22] T reads from standard input and writes it to both standard output and files, which is useful for logging or saving real time data. Let's start with a simple demonstration to understand how the sort command works.

[00:01:42] I have prepared a couple of files in the think next territoy. These files will help us understand how the sort command behaves with different options. And use cases. Here is a simple file containing the names of employees.

[00:02:02] As you can see, the names are not arranged in alphabetical order. We can easily sort them alphabetically using the sort command. Simply provide the file name as an argument to the sort command and it will organize the names in the desired order.

[00:02:23] As you can see the names have been successfully sorted as expected. This was a simple example. Right now, let's consider employee names with random cases. When sorting employee names with random letter cases using the sort command,

[00:02:44] uppercase letters are prioritized over lowercase ones. This behavior occurs because uppercase letters have smaller as key values than their lowercase counterparts

[00:03:00] by default, the sort commands follow as ordering and sorts based on these values. This is why Alice with uppercase appears before Alice in lower case and so on. However, we can ignore case sensitivity and sort

[00:03:21] alphabetically by using the hyphen F option. Let's try that. Now, as you can see the command has ignored the case and sorted the names as expected. Next,

[00:03:40] we have a list of ones that aren't arranged in order. If we use the sort or sort hyphen F command, it will sort them alphabetically. However, we want to sort them in the actual order of the months. This can be achieved using the hyphen

[00:04:01] M option. Let's see that in action, we can reverse the order while sorting by months using the hyphen R option by combining hyphen M and hyphen R, we can sort the MSON reverse order

[00:04:22] when using the sort command on a list of numbers. Without any options. The numbers are treated as strings and sorted alphabetically rather than numerically. This is why numbers like one lakh 20,000 appear before 95,000

[00:04:44] as the sorting is based on their string representation or lexicographic order to sort numbers correctly. We can use the hyphen and option which ensures the data is interpreted numerically resulting in the correct order.

[00:05:06] Sometimes we encounter comma separated values where each line contains an employee name, joining month and salary by default, the sort command sorts based on the first field,

[00:05:24] which is the employee's name in our case. But what if we need to sort by joining month or salary instead, this is where the hyphen T option becomes useful. The hyphen T option allows us to specify a delimiter. In this case, a comma

[00:05:45] and the hyphen K option which specifies the field or column to sort by, for example, to sort by joining month, we use the, the limiter is a comma

[00:06:00] and hyphen K two specifies the second field which is joining month in our case, but this will sort the months alphabetically. So let's also include the hyphen M option to indicate that the field represents month.

[00:06:24] Let's move on to the next command. Unique. The unique command is used to report or omit repeated lines in a file. Consider the employee example where some data has been repeated multiple times.

[00:06:43] If you want to remove the repeated lines, you can use the unique command. If you use the hyphen C option, it will show the number of times each line appears. The hyphen uppercase D in option prints all duplicate lines.

[00:07:05] The hyphen lowercase D option prints only one instance of each duplicate line. That is, it shows duplicates but only once for each group of reputations. Now

[00:07:20] let's say we want to add line numbers to the employee file. In this case, we can use the NL command as you can see the lines are repeated. What if we want to find the unique lines and then add line numbers to them.

[00:07:43] This is where piping comes in. We already know that the pipe command is used to combine two or more commands. And in this case, the output of one command acts as the input to another command. In our case,

[00:08:00] it is allowing us to combine unique command with NL to first remove duplicates and then number the unique lines like this. Suppose you only want to extract only the employee names and exclude all other details.

[00:08:24] Can we achieve this? Yes. By using the cut command. For example, this command allows us to extract the employee names here. Hyphen D specifies the delimiter. In this case, it's a comma

[00:08:41] and hyphen F specifies the field number. Since the employee name is the first field, we can use hyphen F one to display only the names. If the employee names are repeated, you can first use the unique command to filter out duplicates

[00:09:02] and then apply cut on top of it. If you want to replace commas with tabs for better readability, the T command can be used to translate characters. First, we will use the cat command to display the employee details

[00:09:22] and then pipe the output to T the first argument to Dr specifies the character to be replaced. And the second argument specifies what to replace it with. In this case, we will replace commas with tabs.

[00:09:41] Although tabs are used, the columns may not be aligned properly due to varying lengths of the data to improve the output. We can use the column command, the column utility formats the input into multiple columns.

[00:10:00] And by using the hyphen T option, it automatically determines the number of columns and formats the output into a need table. We can format employees dot TXT for printing with two columns. Using the PR command.

[00:10:23] Pr stands for printing or prepare for printing. By using the hyphen T option. We suppress the header and footer that pr normally adds. We can then specify the number of columns.

[00:10:40] We need to specify with the hyphen two option which will format the output into two columns. Here is healthy out. Now that we have formatted the output as needed, we can save the result into a file

[00:11:02] by using the T command, the output will be displayed on the terminal. However, if you specify a file name after the command, the output will be saved to that file as well. Let's wrap up this lecture for now.

[00:11:21] In the next lecture, we will explore some of the utility commands. See you there.