[00:00:00] Hello, everyone. Welcome to this demonstration on creating a local repository in al nine. But the question arises, why do we need to create a local report? Creating a local repository in RHL nine is essential for managing software installation and

[00:00:22] updates in environments where internet access is limited, restricted or unavailable. It allows administrators to store all required packages in one location for easy distribution. Maintain a uniform version of packages across all systems in the network.

[00:00:44] Avoid downloading packages repeatedly from external repositories by using a single local source provide package installation and updates in isolated networks without internet connectivity

[00:01:00] by hosting a local repository. You can control the software used in your environment, ensure compliance and reduce dependencies on external sources. After this clarity of concept. Let's see this in action first, we need the

[00:01:21] RHL nine iso file visit the official Red Hat website, ensure that you download the full version of RHL nine and not the minimal version. As the minimal version might not include all the packages. This link will prompt you to register first

[00:01:41] if you're not registered earlier. Click on register for the Red Hat account, fill the details in the form and then click on create my account. It will redirect and also verify you by sharing a confirmation email in the registered email ID,

[00:02:01] once verified log in with email or user id and this will lead you to the download page of red hat. I already have the account and hence logged in directly here. Scroll down to full installation image section. It is displayed as recommended and click on the button. Download now in the right

[00:02:24] download of ISO file begins immediately as it is heavy file of 7.9 gigabyte. It will take a while once download is completed. In my case, it is downloaded in my local machine. So we need to upload it to the RHL nine VM

[00:02:44] open powershell and go to the folder where the ISO file exists and execute this command here. CP is used for secure copy and we will be covering this in subsequent section. Observe that it's very similar to the SSH

[00:03:02] command which we executed in the previous lecture. It's just that we are providing the name of the ISO and the slash home slash two hyphen user which is our PM home directory is the path where this is so will be copied to.

[00:03:20] This will take a while, leave it undisturbed until it completes 100%. Once the ISO is uploaded, first check, the ISO file exists by executing LS command, then create a directory to mount the ISO file by executing the command

[00:03:41] Sudu mkdir hyphen P mount reports. Now mount the ISO file in the directory using this command here mount reports is the directory where the ISO will be mounted.

[00:04:00] The hyphen O loop option allows mounting an ISO file as if it were a physical disk. As an ISO file is an image of a disk and is typically created as read only. So while mounting the system recognizes this and displays the warning

[00:04:21] to let you know that the source is right protected. You can ignore this to verify the list of contents of the mounted directory, use LS mob report disk. Next, we need to create a repository configuration file in the

[00:04:41] slash etc slash Y repos directory. Use this command to open the editor sudu V I slash etc slash yum repo RHL nine dot This configuration file is created to enable

[00:05:03] more DNF to access local repositories for package management. In RHL nine using mounted media. We will be creating two sections. The Baseos repository section is created to provide essential core

[00:05:21] packages required for the system's minimal functionality. While the upstream repository section is to fetch additional software tools and frameworks to support various applications and workloads. Let's begin by adding this

[00:05:41] Baseos defines the Baseos repository section. This describes the repository for easy identification. This ensures the metadata does not expire, enables verification of package signatures for security

[00:06:01] enabled. Equal to one, activates this repository. This points to the base OS directory on the mounted ISO this provides the GPG key location to verify package integrity. The same applies to the upstream section

[00:06:20] but it points to mount reports, appstream to refresh and clean up any existing package metadata run. So to DNF clean on the repolist command will show all available repositories

[00:06:42] including the local ones which is configured, verify that base OS and appstream are listed now that the local repository is configured. Let's install a package, for example, httpd

[00:07:00] first disable the online repositories to ensure we are using local ones. The hyphen hyphen disablerepo option disables the online repositories. The hyphen hyphen enablerepo option ensures we are using our local baseos

[00:07:21] and appstream repositories and that's it. Your local repository is now up and running. You can install packages without relying on online repositories. That's it for this section. Thank you.