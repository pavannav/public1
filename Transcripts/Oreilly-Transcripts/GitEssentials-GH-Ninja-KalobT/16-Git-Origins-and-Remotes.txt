[00:00:00] Let's talk a little bit more about an origin. An origin is really where your remote code is going to live. Now, if I did get in it and I did not connect this to github, then currently there is no remote origin. Now, we can actually see all the origins here by doing git remote dash V.

[00:00:20] And we can see we've got an origin in here for fetching and pushing data fetching is when we try to download updates and and maybe apply those updates to our computer. And pushing is where we're going to push our files from our computer to let's say github. Now, in this origin, it says that we're using Getit at github.com. So we know because it's getat that it's using SSH.

[00:00:41] If you're not using SSH, it's going to say HTTPS github.com/caleb tale slash getit dash essentials dot get something like that. So this is our origin and really all that means is we have some distributed place to host our code.

[00:01:02] Now, it doesn't not necessarily need to be github. It can be gitlab, it could be bitbucket, it could be some other service. It does not necessarily need to be github. Now, it just happens to be that we're using github because github is the brand name. Now, the good thing behind having an origin or be

[00:01:20] behind having remotely hosted code or in this case, distributed code is there's an official copy of your code somewhere else other than your computer. Now, the good thing behind that is servers can access this. If your computer were to die today and you bought a new laptop, your laptop could access this and you would not lose any of your work.

[00:01:40] If you wanted to share this project with a friend who might contribute to this repository as well, you could share this link and they could clone it using SSH or HTTPS and they would have access to this code. So instead of them downloading directly from you, they download from a distributed system such as github, gitlab or bitbucket.

[00:02:00] Now, in our case, our origin is always going to be github because that's what we're we're working with here. If we had said we don't want to use github, we wanna use GITLAB or Bitbucket, then this URL here is going to change. It's not going to say github.com, it'll say get lab.com. But regardless when we type a command, get push origin master, get

[00:02:21] push origin master. This origin is always referring to here. Now, technically, we can have other origins and they don't need to be called origin either. It just happens to be that by default is called origin. If we wanted to, we could add another remote, we could add another repository to push to. So we could, if we had added another repository to add to which we're not going

[00:02:42] to because I think that gets a little too confusing at the, at the beginning. But if we had another one that said not origin, maybe it's called production. We have a different repository for our production code. We could do get push production and then the branch name, which in our case is going to be master, but it might be a different branch name, feature, branch name, something like that.

[00:03:01] And now we've swapped out origin for whatever else might be available to us. Now again, we're not going to add a new origin. I just wanted to show you what origin is because we're going to be typing git push origin master or git push origin branch name quite a bit down the line.

[00:03:20] And so we just wanna make sure that we are all on the same page when it comes to all four of these, when it comes to what git is, what pushing is, what the origin is and what your branch name is.