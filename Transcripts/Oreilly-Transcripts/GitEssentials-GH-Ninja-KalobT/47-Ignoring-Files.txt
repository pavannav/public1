[00:00:00] OK. One thing you're going to want to do is not commit certain file types. So for example, we saw this in the last video. If I do git status, I have this weird Ds store file now on a Mac, that's just a hidden file. We don't usually see that, but github picks up on it. You're going to see things like this, maybe not this exact file,

[00:00:20] but you're going to see things like this on Windows and Linux as well. And we can actually get ignore these files and it is a file called gitignore and all it does is say, hey, ignore certain files, don't let them be committed. So if we open up our text editor and we just create a new file in here called dot git,

[00:00:41] ignore, we can ignore certain file types and I'm just going to put a new file type on your DS underscore store and save that. Now, if I open up my editor or my terminal again, I can do git status and DS store does not show up. But my gitignore does, that's OK.

[00:01:00] We want to ignore these files permanently so we can commit this file that's OK. Now, let's say you had other files in here. Maybe you had a bunch of uh maybe you had a bunch of images that you were using locally, but they should not be pushed to your github repository. So you could say anything. That's a JPEG dot JPEG. And from now on it will ignore JPEG files.

[00:01:23] Probably a bad idea unless your JPEG files are really, really big. But a good example would be if you write Python, we have these pi cache files, py CS, we also have P cache folders so we can ignore all of those as well. And it's really just one file per line or one file type or one folder per line.

[00:01:44] That's all it is. So we could do git status. We have this gitignore file in here and we can do git a dash dash all or just get a dot gitignore, get status. We'll see it's green, get commit, ignore files,

[00:02:02] git push origin. No, let's do this. Let's clear this off, get push origin master. And all this is going to do is again is going to ask you for your username and password. And for me that was pushed up to github and we can always verify that by going to our repository clicking refresh. And we'll see there's a gitignore file in here and it's going to ignore D store

[00:02:23] dot pyc and P cache folders.