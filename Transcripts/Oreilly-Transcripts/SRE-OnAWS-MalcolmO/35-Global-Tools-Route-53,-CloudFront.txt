[00:00:00] Welcome course taker to global tools, route 53 and cloudfront. So in this video, we'll look at the global applications that allow you to stitch together all the things we've done so far, namely route 53 for the balancing between regions and cloudfront, for content distribution and balancing the user content.

[00:00:20] So if you're ready, let's get started. So hopefully you'll know what DNS is and how it works. But here's a quick reminder. So we have our user on the left who wants to go to google.com. So he makes his browser will make a DNS request saying where is ww.google.com to root name server? The root name server will redirect to the.com name server and

[00:00:42] the.com name server will redirect finally down the route to google.com. But google.com name server will know how to get to www. So it provides a specific route back to the user who then opens up a TCP connection to 1.1 0.1 in this case. So at the bottom end where the Google name server is,

[00:01:01] you've got a lot of opportunity to influence which name server, which IP address is returned based on information you know about the user ie where they're based based on their IP address. So, geolocation, the Latency or the health of the server as known by the Google server or any other metrics that you might want to include.

[00:01:22] So route 53 is Amazon's DNS service. You can have public hosted zones, you can have private hosted zones for inside your VPC as well. We're not going to touch on that today. We're just focusing on public hosted zones. So here you register your domain name, Amazon's a registrar as well. So you can now buy your domain name through Amazon

[00:01:41] and basically does the mapping between the name and IP address and back again. And it has the capability to check the health of some of your Amazon resources, which is very useful as we will see in a second. So within a DNS survey, you have lots of different records. Here's the main ones. So we have an, a record which points to and from a name to an IP address. You have a PTR one not shown here.

[00:02:03] That does the inverse, we have a CNAME, which points from a name to another name, a text record which just holds data. So often this is very useful for things like certificate, authentication, and retrieving certificates. And then you have the name server addresses which say these are the physical or these are the IP addresses that actually host my records

[00:02:22] and the one I skipped over the alias record is specific to Amazon. What that does is map, it kind of works a bit like a cname. It maps a name to an Amazon resource like a load balancer that doesn't have a static IP address. So you'll never get a low balancer IP address until you actually query

[00:02:40] the actual name of the low balancer. So Amazon created this alias record that just allows you to point to a dynamic resource. So it's not really part of the DNS standard. You won't see it in other DNS servers. Although you often see something that's a bit similar, but an alias record, you'll use a lot when you're using route 53. So there's a couple of failover routing policies that are very useful.

[00:03:02] So if you imagine our scenario where I had our ECs cluster running in Ireland and in the UK, so I can use a failover routing policy that says, well, let's say, for example, E US two is our primary and E US one is our secondary. So I can create a failover routing policy and set that up

[00:03:20] to have a CNAME with an active and passive failover route. So if we lose London, then everything falls over to Dublin. The other thing is I could use a geolocation routing policy to say, well, where is the user coming from? So if they're coming from the UK, they go to London, if they're coming from Ireland or the rest of Europe, they go to Ireland.

[00:03:42] So there's two examples of using failover routing policies within DNS. There's a couple of other alternatives, geo proxy routing, which is a bit like geolocation. But you use route 53 traffic flows and then Latency based routing policies where you're actually looking at the Latency of the traffic and trying to determine

[00:04:01] what's the best region to send the traffic through. So all of these allow us really to utilize our multiple region scenario in a better way. So either from a failure perspective, so if one region goes down the other one can pick up the load or in the case of Latency or location. So I might want to block content from a particular set of users using geolocation rules.

[00:04:23] Or I might want to just give users the best use Latency experience they can get if you're interested in seeing the other ones, then go to that URL where you've got all the details there. So finally, Cloudfront's, well, we've talked a lot about, you know, our back end and our API so a quick touch on the Frontend. So

[00:04:40] Cloudfront is a CDN content distribution network that basically allows you to distribute static content like HTML, CSS or javascript files. That's what we're going to use for our Frontend. So the process would be agency or developer uploads their content to an S3 bucket, then creates a web distribution

[00:05:00] in phase in step two on creating that web distribution, what you'll get is a cloudfront URL. So here it's yyy dot cloudfront .NET that basically gives you a location specific URL. So if I'm in Europe or if I'm in Asia or somewhere else, then

[00:05:20] I will resolve to different endpoints based on that URL. So if I've got an index dot HTML, my URL is gonna be PXXXYYY cloudfront .NET slash index dot HTML. I'll go to different endpoints if I'm Asia as opposed to if I'm in Europe and cloudfront, just keeps that synchronization of content it performs all of that work.

[00:05:42] So all we need to do then is create an alias record for our car booking system Frontend to point to that cloudfront distribution and that will give us a reliable Latency sensitive way of distributing our frontend HTML, CSS and javascript files.

[00:06:01] Ok. So I hope that's been useful. Please join me in the next video where we'll look at the impact of what we've done on our users and developers. Thanks very much.