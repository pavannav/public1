[00:00:00] Welcome course takers to video number four, deploying and testing our pi simple application. In the previous videos, we prepared all the infrastructure required to deploy our application. We created the container, we created the load balancer and the ECs cluster. So in this video, we're gonna prepare the database. We're then gonna deploy our task which is our container.

[00:00:22] And finally, we're going to test and validate that our application is running and behaving as expected. So if you're ready, let's get started. So we're in the final leg of this. So what are we going to do in this video? Well, we're going to add our schema through the CL. So we can't, we're not going to be too clever about adding our objects.

[00:00:41] Remember we created our rds database, but it's just a blank database at the moment. There's no tables, there's no triggers. Once we have that done, we'll run our terraform to create task and our service which will deploy our application. We'll review that and then we'll do our testing. So we'll use our new pie test module and we'll test our application

[00:01:00] and we'll also look at the old postman test to make sure they're still working. And then finally, we'll start using cloudwatch to provide our SL data. So let's go. So if we go back into our AWS console and we go to EC two, we can look at our load balancer we created in the previous step. So here's our load balancer

[00:01:20] and here's the DNS name that we're going to use. So we'll take a quick copy of that. If we look at our target group, we look at our targets. We can see that there are no healthy targets at the moment because we haven't deployed our application. If you remember we're using an IP target group. So one of the tasks that we have is once we create our service, we'll actually register those IP addresses assigned to those containers with

[00:01:43] this low balancer and then everything should pop into life. So if we go to our Python module, there's a couple of things that have changed. Basically, we're gonna add an environment variable called ECs run and it works in the same way as the code build works is that when it detects it instead of trying to load a profile,

[00:02:01] like when we're running it locally, it will actually just run it from the role attached to it. So when you're running within Amazon, you actually need to make sure that you're not trying to use a profile, like you'd be running it from local. So I've added this code, I've already pushed this code and created a new container. So if you go back into R,

[00:02:23] I've now got a 0.0 0.2 which contains the new code. And I've done that through our standard process of updating the code and running the build job. If I look at my cluster, here's my cluster. I don't have any services. I don't have any tasks. You can see it's all a fargate cluster.

[00:02:42] So there's nothing deployed as yet until the actual task definition of my cluster. If I go back into two and look at running instances, what we've done is just create a small bastion server. So this is our jump post in order for me to run the SQL scripts. So let's do that now.

[00:03:02] So here I'm on the host. So all I'm going to do is run this command PSQL, gonna log on to the database. Here's my rds database name. And if I have a quick look at the SQL file, it's the same SQL file that was used in the original application. So the first thing it does is drop anything that exists, it creates the tables

[00:03:22] and then creates the function. OK? So we've configured our database, we've created the tables. It's a blank database. Now, if I go to my Python module and just open up a terminal, and then if I run,

[00:03:42] what I see is I'm getting an invalid response code for my initial query. And if we remember what we're doing in our pie test. The first thing we do is go and get the data. We have a fixture for getting cars. Now it's hitting the load balancer, but obviously there's no URL or anything behind it, which is why it's getting a 500.

[00:04:03] So we know our test is failing, which is good. So let's deploy our service. We've done our rds database. So if I go now into my main task, we've got a whole set of terraform data queries. We need to pull back things like the ECR repository, the target name. And then this is our main definition and we'll go

[00:04:22] through the definition in more detail once we've created it. But a couple of things, task A RN is very important. This is what we need to call, this is what we need to provide the task itself. So there's a, a full A RN for a role and that role allows us to generate cloud watch logs and do R DS stuff as well.

[00:04:40] We give it a name, we say it's a VPC that it's a Fargate. We give it a amount of CPU and memory. We give it an executor role for what it needs to do. And then we basically configure our task itself. So configure our logging, we configure our port mapping and then we launch our service, right? So once we've created our task definition, we simply need to launch the service.

[00:05:03] OK. Now, one of the things here, desired count, local app count. If we go back up to the top here, we have our app count of two. So we're going to create two instances which will be more reliable than what we had before. Ok. And then we've got a load of information in here around our environment variables, what we're going to pass into,

[00:05:24] toggle the word wrap. So it's easy to see. So we're gonna give it a host, which is our aws DB instance name. We're gonna give it our DB user, our DB password. And here's where we're going to set our EC S run. So we know that it's running within EC S so that we need to use the role and this is the task role so that we can

[00:05:42] generate our Cloudwatch metrics and also query our rds database. OK? So let me just run that. So I'm already in my task directory. So if I just do a better form apply and they'll go off. So it's quite quick. Now, it does take a little bit of time to spin up,

[00:06:00] but let's look at the task definition. So we go back into our container service and here we now have an active task definition. So I'm currently running a version 18 to see all the other ones that I've done. This is all part of testing. We're gonna just focus on 18 at the moment. So in here, we've got our definition name, we've got the task role that we assigned and we

[00:06:20] can have a quick look at the details behind that. OK. So cloudwatch and R DS has been assigned to that role, which means that the code that we had that generated the metrics and queried R DS are being fulfilled. It's a filegate container. We've got an execution role, execution role is really just what the task needs.

[00:06:41] In order to initialize. We've set up 512 and 256 units of CPU. Now, if we go into our cluster, what we should have is our service and we've got two containers and two that are running. You can look at the actual service itself

[00:07:00] and here we have two instances running. I could see the IP addresses. So a couple of things, if we now go back into our target group, what I should have, it is two targets registered. Yeah, and they're healthy remembering that if I go back into my load,

[00:07:21] back my target group and look at the health check. So I'm querying my API for health. OK. So we've got a solution up. We've got our containers running. One last thing I wanted to show you is going to cloud watch. Am I going to logs

[00:07:41] here? I've got my logs, I've got two log streams and in there we can see the responses and the output from our program from our application. So I'm using environment variables. I'm connecting to the database. I've got the db. So I've connected and we're configuring Cloudwatch and the second instance is the same.

[00:08:03] So we've got two instances running connected to the same database and we haven't had to do anything. So already we've increased the reliability of our platform. Now, if I go back to our tests, simply rerun those tests. I'm now getting one is passing.

[00:08:20] So we've tested our application. If I open up Osman, I've got this URL. So we're now going to the load balancer. I can go to health. I can see how many d brains see how many cars are available if I actually want to create a car.

[00:08:42] So in the body, I'm gonna create car one, we get a 200 success. And if I get cars, I've got car one and if I actually get the details for car one here, I've got the car and then I can do a put K one,

[00:09:02] forget it again. There we go. So our application is working. We've tested it using the old soap tests or the old postman tests. We've tested it using the old postman test. We've tested it using the new test. We can see that there are two instances running.

[00:09:20] So the final thing is we need to look at the monitoring. So we've already looked at logs. If I go to metrics, I've got my simple metrics. So I actually wanna look in here. So here's my simple metrics. So I've got my sl I db failed for get put, get car, put car.

[00:09:40] And we can see, I actually want to graph these. In fact, I'm only interested in getcar. Let's just change this into number and we'll change it to summary and we'll do it for 30 days. So we've got SL I db success

[00:10:03] and we can see it's called GETCAR. So what we probably want to do is we want to add some additional logic in here. So I want to total up the total. I want to create the total number of requests. So that's really M one plus N two and we'll call this total.

[00:10:21] So they are URL S. So we're just adding those two together. So now I've got a total of three and let's do it over a four week period. OK. So we have a bit of more variants in our data. So 46 requests, this is part of my testing, obviously and we've got three failures. So 49 so that all adds up. Well, now I want to do a mass expression, another one.

[00:10:43] So we'll do 100 minus. So P one divided by so total number of requests divided by failed requests and one, let's promoting that app. So currently we could call this reliability. So we're currently running at 43.7% reliability.

[00:11:05] We're currently running at 83.7% reliability, but obviously, there's been some failures and some testings and you'd expect the CRL to be more populous. And if I hit get car, what I should see is these values going up and it can take a little bit of time to get through.

[00:11:21] And there we go. So based on our performance, based on the actions we're doing, we've created an SL I that is now measuring a specific URL. So get cars. Now, whether or not that's the right URL to define. I don't know, but that was something you would work out with your product owner. And of course, now that we can call this

[00:11:41] and we can put that into our dashboard, we can change that into snapped graph or a line graph. If we want, I quite like numbers, it's easy to see. But of course, because we've got other metrics, we've also got ELB.

[00:12:01] So if we look at our ELB and we look at our per ELB metrics, I can also look at the total number of 500 messages from our ELB, I can look at the request count

[00:12:20] and I can do a similar. So here we've got our M three M four, so M four, so 100 minus M four divided by M three, my list app reliability.

[00:12:41] How am I like that? So here we have an overall application reliability of 86.7%. We have reliability of our individual API get for cars and we can build a model just using simple graphs like this

[00:13:02] to actually represent our SL I and Slo. So I hope that's been interesting. Join me for the last video in the section where we review what we've built. Thanks very much.