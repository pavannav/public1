[00:00:00] Now that we've gone over a few of the different CI CD tools that are out there. Let's talk a bit more about what a CI CD pipeline actually is. So you can think of a CI CD pipeline as an automated process that streamlines how developers and testers integrate, test and deploy new code. It typically consists of sequential or parallel stages such as source control,

[00:00:24] build test and deploy these stages validate code quality and functionality before a release. The pipeline starts with source control where developers commit their changes to a version control system like git once changes are pushed, the CI

[00:00:42] D platform detects that changes have been made and kicks off the automated execution of the pipeline. As you may already know source control solutions like git and github, ensure all code modifications are tracked. This enables collaboration across teams and offers

[00:01:00] traceability for every feature or fix. Next, we have the build stage in the build stage code is compiled, dependencies are resolved and build artifacts like jar files or Docker images are generated. This step basically ensures the application is structurally sound before it moves on.

[00:01:20] So for example that there are no syntax errors in the code or missing packages or other dependencies. If a build fails here, the pipeline normally stops as there's a problem with the code that needs to be fixed before we can execute any testing on it. After a successful build, the pipeline runs the automated tests.

[00:01:42] Now, these can be unit tests, integration or API tests and sometimes even performance tests, we're actually going to be incrementally adding all of these different test types to the pipelines we develop in this course. So you're gonna get a really good feel for the importance of each of them.

[00:02:00] But the testing stage provides immediate feedback on the code that was recently checked into source control. If any test fails, the pipeline will typically be configured to stop and fail so that the issue with the failing test can be investigated. And so code with a defect isn't released. Once the code passes all tests,

[00:02:20] it moves into the deploy stage which can release the application to staging or production environments. Again, we're actually going to be doing all of this in the course. For both Jenkins and github actions, we'll basically deploy our code to a staging environment, then run some post deployment tests to check that everything is ok

[00:02:42] and then automatically have the same code deployed to production just as a reminder for what we talked about earlier in continuous delivery deployment may be triggered manually by a team member in continuous deployment releases automatically go live whenever the pipeline is green,

[00:03:02] we'll be going down the more advanced continuous deployment route in this course. But it would be easy to change to a continuous delivery approach if we wanted to have a final manual step before releasing code to production. In summary, a CICD pipeline integrates the source build test

[00:03:21] and deploy stages into a unified automated workflow. This approach accelerates software delivery ensures high quality and reduces manual errors by validating code at every step. The great thing is that teams are free to tailor each stage of the pipeline. For example, a team might choose to run some tests in parallel or add

[00:03:43] performance checks or require manual approvals at certain points in the pipeline. We'll be seeing plenty of examples of how to adjust and configure our pipelines in the course as we build them out in both Jenkins and github actions.