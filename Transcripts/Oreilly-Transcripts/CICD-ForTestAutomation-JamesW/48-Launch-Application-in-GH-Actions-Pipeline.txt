[00:00:00] So let's first start doing the work to get our application running on our github actions runner. So I'm gonna open up the CI dot YAML. I'm gonna scroll down to the bottom down here and then we're gonna start working on our API tests. So I'm basically gonna make a new job and for now, we'll just call this job API tests. So again, we need to launch the application first.

[00:00:22] So that's what we're going to do in this lecture and that's what we're going to focus on. Now. The way that we did this for Hugh Jenkins is that we had to run Docker in Docker so that we could use Docker compose. We are going to use Docker Compose again on github actions. But because we're using the Ubuntu latest runner, that runner does actually come with Docker already installed.

[00:00:41] So we shouldn't actually need to install anything separate. We should literally just be able to use Docker Compose directly on that runner. So let me show you how to do that now. So in order to run these API test, we first want to wait for both the backend and the frontend test jobs to run first. So both of these jobs up here, we have our

[00:01:01] um up here, we have our backend test job here. We want that to finish. And our frontend test job is that on this one here? We want this one to finish as well first. So we're gonna add in some code for that just to make sure that those finish. We're gonna say these needs, it needs the unit tests back end job to be finished.

[00:01:24] And it also needs the unit tests, frontend job. So yeah, we want to make sure that the unit test jobs finished before we start running API tests or launching the application or anything like that. We're also going to want to run on B to latest

[00:01:41] as we've been doing. Um And we, what else do we need to do? So let's add in the steps that we need. So the first step will be just checking out the repository. So this is similar to what we're doing exist already for the unit tests. So the first thing, we will have a step of checkout repository.

[00:02:04] Let me just fix the formatting this. Give me, let me say that is going to use the actions. Uh It's check out version four. OK? So we're going to check out the repository and then once we've checked out the repository,

[00:02:20] what we're first going to do is just launch our application. So as I said, hopefully, I think that this Ubuntu runner this should already come with Docker. So we should just be able to directly run Docker compose up here. So we should be able to literally do Docker compose up dash dash build dash D.

[00:02:40] So again, this is like what we're doing locally when we're running the application locally, we should be able to just do this directly on the runner. Now, when the application comes up, we also want to make sure that we wait for the port to become available. So we'll do an MP X command and we'll call the health endpoint just so that we're waiting for the health endpoint that become available before we move on and start executing the tests.

[00:03:01] So to do that, we can do MP X weight ports and the port that we're gonna be looking for will be localhost T 80 when the application is running and then we have our API health endpoint and we'll set a time out of that of 30,000 milliseconds or 30 seconds.

[00:03:20] Ok. Let's save that. Uh I'm gonna open up my terminal and I'm gonna make a commit and I'm just committing this directly to the main branch. So when we commit to the main branch, uh it should trigger a build for us because that's what we've already got set up in our pipeline.

[00:03:40] So let's push that back up to main looks good. OK? So we've pushed the branch up to main and now if we go over to the github UI go over to the actions tab. We should see a build has kicked off and we do. So we've got this launch application that's kicked off. So you can see here we're running the back end and the Frontend test first.

[00:04:03] So we wait for those two jobs to finish before we start the API tests job. So we'll just give that a second to run through and then we'll see how API tests gets on. So the create code coverage step that skipped as we would expect because we're not opening with a poor request.

[00:04:20] So again, that job, it checks, it checks whether there's a poor request. This isn't a poor request. It's a commit direct domain. So that job just skips and let's take a look at API tests. So it's working through now trying to launch the application, we can watch the logs down here. So it's already looking good. It looks like the Docker compose has worked. So it does look like we were right, that Docker

[00:04:40] compose is already included on Ubuntu image. So that's really good. This is a bit easier than what we had to do with Jenkins where we had to pull in a separate Docker image and we had to do, had to like run this route. For example, with here, it looks like we can just run Docker compose directly. So that does make it a bit easier on github actions. Certainly, in this case,

[00:05:00] and again, that's what I really wanted to show you in. This course is like different tools are gonna have different pros and cons and ways of working. You know, there's other stuff that we can do on Jenkins. It's maybe more difficult to do on github and vice versa, but it's good to, you know, get experience trying out all of these different tools. OK? So we're going through here. It looks like our containers are trying to spin up.

[00:05:21] Let's just see how that finishes good. And looking at the logs here, it looks like we've got a successful message saying that it's waited for localhost 8080 it's detected that it's connected. So that's great. That looks like our application is running on github actions as part of the job. So what we'll do in the next section is we'll actually start

[00:05:41] executing some API tests against that application that we just started. So quick pause and I'll see you in a moment.