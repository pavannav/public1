[00:00:00] OK. So let's go ahead and publish our end to end test report. So I'm just gonna minimize the terminal here just to give us a bit more space. So after the steps run here, let's add in a post call, let's say always because we always want this step to run. And again, as we, we've seen earlier, we're gonna do JUnit first. So the folder where these test results live is tests E two E

[00:00:23] forward slash test results, uh forward slash results dot XML. And the other thing that we'll do is we'll publish the HTML report. So we just specify the target for that report

[00:00:42] similar to before we first specify the report directory. And that's tests E two E forward slash playwright hyphen reports. So again, that's list over here and we're gonna wanna get that index file. So the report files just want to get that index

[00:01:03] dot HTML file. Make sure I add the commas after these as well so that we don't get any syntax errors. And finally, we'll give our report a name and then let's just call it playwrights E two E test report. OK. That should be all that we need. So let's save the Jenkins file

[00:01:22] again. I'll open up my terminal. Let me check the git status git add that Jenkins file and then we're going to say the commit message, publish end to end results. Can I push that back up to mine?

[00:01:40] I'll jump back over to Jenkins. I'm gonna kick off another build. So we'll just let that build run through now and then once that build goes through, hopefully, we should get a end to end test results report appear in Jenkins. So I'll give that a few seconds now just to run through. Ok. So the build's finished successfully, the end to end tests have run. So if I click into run number 25 if I look here in the test results,

[00:02:03] we now have this integration dot spec and that's where our free end to end tests are being run. So we've got those results in here. And again, if I just go back to the bug tracker projects, we've now got this playwright end to end test report. So again, we've got the test, right? Um The playwright test report being published here

[00:02:20] as well alongside the other different reports.