[00:00:00] OK. So we're now running both the backend and the frontend test for Jenkins and we're producing a nice report that has details of the tests that passed and failed. Another thing that we often like to do as testers is add in code coverage reports. So I want to show you now how we can generate code coverage reports for both the back end and the Frontend and then publish those on Jenkins.

[00:00:21] So let's start with the back end and see how we can do that. So first of all, let's run it just locally. So again, we're in the bug tracker back end. So I'm gonna see the inter bug tracker backend folder. So to run the code coverage report locally with go, we can enter this command, go test cover profile coverage dot A cover mode atomic and then that will just run

[00:00:42] the test locally and that's going to produce a nice code coverage report for us. So again, once we run that, we can then see that it to the console, it prints out this code coverage report for us as well as that is, we look in the backend folder, we can see we also get this coverage dot out file. So it's this coverage dot out file that we're going to publish

[00:01:01] on Jenkins and then turn that into our code coverage report. So to do that, we can enter this command, go to cover and say, take for HTML file the coverage dot out report and then turn that into a file called coverage dot HTML. So if I just run that locally that produces this coverage dot HTML file,

[00:01:20] and this is basically going to be the HTML file, which is going to contain our code coverage report and this is what we're going to publish on to Jenkins, ok. So let's work on putting that into our Jenkins file now. So I'm just gonna close down the terminal. So we've got a little bit more room. So line 14 here, we're still gonna run our go test first and that's gonna produce our J

[00:01:40] JUnit report. So we're not gonna touch that. We're gonna leave that alone under that. We're gonna have the comments and we're gonna say generate the coverage report and that's gonna be done in separate actions and because we're gonna be running a few shell commands, now, let's do all of those within one single block to go. Um So I'm just gonna add sh with the three dots here,

[00:02:01] gonna move what we've got here, cut that and paste it in here. Take out the sh because we don't need that. Now, don't need the just fix the formatting, you don't need the dots there either. OK? So under generate generate coverage report,

[00:02:21] the first one has this go test, same color profile use coverage dot an the common mode is SOIC again, we just want to run all of the GO unit tests within our folder. That's the first step.

[00:02:40] And then the first, the second step is to generate the actual report. So we do go tool cover dash HTML coverage dot out. So it takes in the coverage dot out file that we just produced above, let me say minus o coverage dot HTML. So that's the output file is in the coverage dot HTML file.

[00:03:04] So what we've written here, this is just gonna generate those reports, but it's not actually going to publish them to Jenkins just yet. So in order to actually publish them, we need to add some more code into the post step here. And before we do that, what we're gonna do is we're gonna move the reports that are getting generated from this code. We're gonna move those into their own folder.

[00:03:21] I just so that we can easily publish them. So you'll see what I mean in a second when we write this in the first thing we'll do is we'll do a maker to make a new directory, add the minus P flag. So this will only create this directory if it doesn't exist. Otherwise it won't fail. It will just use the existing directory and it will say move the coverage dot HTML file.

[00:03:41] That's the one that we just created, the one that we created just down here, move that into the reports directory. So now we can go down here to our poster and you can say publish HTML. We specified the target that we want to publish.

[00:04:00] So we add in some square markets, you first need to specify the report directory. That directory is gonna be a bug tracker, dash B ends forward slash reports. Excuse me. So that's the folder that we created just up here.

[00:04:20] That's the one that we're specifying here. Next, we need to specify the report files. What file? That's just the cut bridge dot HTML file that we just created. And finally, we need to give our report a name. So let's just go one back end coverage.

[00:04:41] Ok. Let's save that. So again, just to talk through this quickly line by line. We first we've left our go tests. Um We're just doing JUnit test so that's been left untouched. But then after that, we do another go test. We run the test again. But this time we generate a coverage, we generate the code coverage from that. We then take that and then we convert that into the coverage dot HTML file.

[00:05:03] We're then moving that coverage dot HTML file into this new reports folder that we've just created. And then down here, we're then publishing that report to Jenkins. So let's run the pipeline through that. Let's run the pipeline through now and then we can see if that report gets generated successfully. So I've saved the Jenkins file

[00:05:22] can open up my terminal again and make sure that it's Cindy back to the bug tracker folder. Get status just to check first, get the state, just get the state. Oh, there we go. We get dots, get permits

[00:05:40] and send the same, publish that then coverage reports and then push the hat to the main branch. I head back over to Jenkins. I'm just going to execute the build. Just give that a few seconds to run through. Ok. The builder's passed if I go into this build number 14.

[00:06:03] Ok. So the builder's passed. If I just refresh the page, we can see now we have this back end coverage report. And so if we click on that, we can see we've now got this nice code coverage report for us. So we can jump between the different files. We can clearly see in the HTML file where our code coverage already is.

[00:06:20] So this is a really nice addition to our CI D pipeline that's going to be really useful for our development.