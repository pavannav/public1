[00:00:00] OK. So let's get our end to end test to run through Jenkins. So I'm just gonna close down the terminal here. I'm gonna close this playwright file. Um Now I'm gonna go to the bottom of my Jenkins file and just under the stage here, we're gonna add another new stage. So we call this stage end to end tests.

[00:00:21] E two E tests. And this is gonna be really similar to what we did for the API test just above. So we're gonna run it for a Docker container again and we're gonna use the same uh Microsoft playwright image as well. So can we do an agent? We wanna run through Docker and I'm just gonna be lazy and just copy the same image.

[00:00:41] And in fact, I'm going to take all of these settings because they're all going to be the same with the reused node, etcetera. So we're using the same image, we're using reused nodes and we're, we're supplying the same arguments as well. So with the network host and we're running as the root user. OK? Let's go down and add in our steps. We need to make sure that we're running in the tests,

[00:01:02] tests end to end directory. So again, that's the directory that we have over here. Tests end to end. That's why we're pointing to that over here. Then first we'll do M PM CI so that would just pull in any of the dependencies that we've set up. And then after that, we can simply do MP X playwright test.

[00:01:23] Ok. Let's save that Jenkins file. Gonna open up my terminal again. CD back to the bug tracker uh folder, the root folder gonna add in the Jenkins bar to make the commit. So saying, running end to end tests

[00:01:43] and then push that back up to main head over to Jenkins again, we can trigger a new build, ok? That builds, triggered. So we'll let that run through now and then we'll see if the end to end tests run at the end. Ok? So the build's finished, that ran nice and quickly, we can see that the end to end tests have passed and again,

[00:02:01] that ran quickly because we've already cached that playwright Docker image if we just take a quick look at the logs. So I'll scroll down to the bottom for the end to end tests. So down at the bottom here, we can see that the three tests ran and again, they, we can see that they passed. Now again, we have to look at the logs at the moment to see that those test pass.

[00:02:20] We're not actually publishing the test report yet. But we are generating a test report locally. So if I just look at the local code, you can see here in this test center and we are again producing this test results in these playwright report. So in the next lecture, let's look at how we can actually publish this over to Jenkins as well. So that, that can be viewable from Jenkins. So quick pause here and we'll do that in the next lecture.