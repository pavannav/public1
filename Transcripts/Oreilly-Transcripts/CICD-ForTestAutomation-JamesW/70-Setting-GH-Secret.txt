[00:00:00] OK, great. So let's set up our github actions workflow to do our deployment to staging for us. So to that, we're gonna add in a new job to our CI dot YAML. So I'm gonna scroll right down to the bottom here. After we've run our end to end tests, we're gonna add in a new stage or a new job I should say,

[00:00:22] and our new job will be deployed to the staging environment. So in terms of running this, we need it to first make sure that we have the previous step run. IE the API N to M perf tests. So this stage isn't going to run until those previous tests have passed.

[00:00:42] So we don't want it to run in parallel. We want to make sure that the previous lot of tests have run. We also want to set this to only deploy to staging when we're merging into the main branch. So at the moment, you know, we're running a poor request every time we push up our code changes, we don't want every pool request to be deployed to the staging environment.

[00:01:03] We only want it to be deployed. We have the main branch. So to do that, we can say the github ref that should be refs forward slash head forward slash main. So again, this basically just checks that we're only on whether we're on the main branch before, before it runs this job, this job to deploy to staging

[00:01:23] now because we're testing this first on a branch, we can actually comment this line out. So because we want to make sure that the code changes that we're getting deployed to staging that it works that we're adding in. We're gonna deploy, we're gonna comment out that check first. So the poor request is gonna deploy to staging this time.

[00:01:41] But then once that works, we'll add this com, we'll remove that comment, we'll add this line back in and then from there on it will only, this should only run when we do an actual deploy to staging. I hope that makes sense. So we want this as well to only run on Ubuntu latest. So we say runs on Ubuntu

[00:02:00] latest and now we should be able to go ahead and add in our steps. So as usual, the first step that we add in is actions, checkout and that's four. And then the next thing that we want to do is we want to set up fly CTL

[00:02:21] now because we're using github actions, github actions. Luckily has a special fly CTL existing action that we can already use. So remember with Jenkins, we had to go through a little bit of work just to down, just to do a cURL command to download fly CTL. We could do that here or an easy way to do. It would be to be to use an existing support

[00:02:40] github action. So let's use that so we can use the official action that's maintained glider fly team. So that's a fly CTL super fly forward slash fly CTL dash actions. And it's called setup fly CTL. And we want to run their master version

[00:03:00] and that should all be all that we need in order to set up fly CTR. So it's a lot more straightforward on github actions than it was when we were running it on Jenkins. So once we've got FLYCTL set up, the next thing that we want to do is deploy our backend. So we can add in a new step here to do the deploy backend.

[00:03:20] And again to this, we do need to add in an environment variable. The environment variable that we need to add is the fly API token and that's the github action secret that we added in previously. So in order to, we get that secret, we can add in the special github notation here. We can say dot

[00:03:40] We can say it's dollar double curly brackets, secrets dot Fly underscore API underscore token because that was what we named our secret. And then that should bring in that fly API token environment variable and that should be have the secret already added in for us.

[00:04:00] The next thing that we need to do is add in the working directory. So again, this is the back end that we're deploying. So we say bug tracker dash backend, and then we need to add in the command that we want to run. So the command that we want to run to do the deployments for staging is fly CTL deploy dash she and the file that we want is fly dot staging dot to.

[00:04:24] So if we remember from before, that's this fly dot this fly dot staging dot toml file here, this is the one that does has the configuration for us to deploy to staging and we want that to run on the fly servers. So we do remote only rather than running on our own github

[00:04:41] action servers. That's just when we're doing the actual build, that should be all that we need to deploy the back end. Let's also deploy the Frontend as well, which is going to be very similar. So you're gonna copy that over and then paste that just below. Let me just fix the formatting. Here we go.

[00:05:01] So that's gonna use the same token as before. It's almost exactly the same. So we're just gonna change back end to Frontend and then the deploy command can stay the same as well. So this should do all of the work now to deploy to staging just for us to check that this actually works. Let's change the version number.

[00:05:22] So for the backend, we go into the internal conflict folder, I'll change this to 1.0 0.4. Save that. And then for the Frontend, we go into source uh config app and then we'll change that to 1.0 0.4 as well.

[00:05:40] Close those down and now we should be able to commit our changes. So we're still on the deploy staging branch. Let's just check what we're gonna add. So we're gonna add CI dot the O and the two new version numbers. So get at docs. So getit minus m deploy staging

[00:06:01] and then let's push that up to our deploy staging branch. OK? That's gone up to the branch. If we jump back over to github, we do already have an existing pool request where we're doing the deploy staging. So if we check out that poor request, if we have a look at that poor request, if we go down to the bottom, we should see that the checks are starting to run now.

[00:06:23] So again, we can't merge this in strictly until we, until this github action has finished and all of these checks has passed. So let's go over to the actions tab we can see that our build is running now and we can see a nice visualization of this in here now as well. So it's running the back end and the Frontend test 1st

[00:06:42] 1st, it's going to create our code coverage, comments here. Uh It's then gonna run through our API end to end and performance tests. Once that's finished, it's gonna try and do the deployment into staging for us. So let's just give this a minute or so to run.

[00:07:00] OK. So our API end to end performance tests have run and now it's trying to deploy staging. Let's go into that job and see if we can watch that going through. So it's done the setup of fly CTL. It's now running through the part where it's going to actually deploy the backend for us. So we can see how much quicker it was setting up the Fly CTL using an existing github

[00:07:20] actions. So again, that's a really good advantage of github actions over Jenkins and that we can reuse things like existing workflows like this. They just make our life a bit easier. So it's now doing the deploy to the backend. I can just see that we've got deploy backend in twice here. So I'm wondering if I just forgot to change the name in github

[00:07:40] action. Yes, I did. So in the github action, I should have named this to deploy Frontend. So I'm just gonna change that here quickly and save that next time we commit that um that should update. So that should be OK. So if I head back over to github actions, I can see the frontend's been deployed. It's now deploying the back end down here looks like that's going ok as well.

[00:08:03] Ok. And it looks like that's finished and it looks like that's deployed successfully. So let's go back to the summary page. So it looks like yes, it's from the back end, the Frontend tests, the API to end tests and it's done the deployment to staging if I just jump over to my staging environment,

[00:08:21] which I think is a bug tracker dash staging dash. My name open that one up and we can see here in the top right that it's updated to version 1.0 0.4. We can also check that the back end version is correct. We do a cURL command, we can do a cURL command to our staging environment

[00:08:43] and that version number should also be 1.0 0.4. So that's great. We're deploying now through github actions. So brilliant. Let's take a quick pause here. Then when we come back, we'll look at adding in our postdeployment test through github actions as well. Those will run against our sty

[00:09:00] environment. So quick pause and I'll see you in a second.