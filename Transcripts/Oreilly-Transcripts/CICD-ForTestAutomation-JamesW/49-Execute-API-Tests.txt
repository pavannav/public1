[00:00:00] OK. Now, in order to run our API tests, that should actually be relatively straightforward. So let's go back over to the CI dot YAML. So under here at the bottom where we've got the steps in our API tests. So we've got our API test job here. We've got a couple of steps in here. So far, all that we need to do is we just need to add in another step underneath.

[00:00:21] So after we've launched the application, let's add a step to run the API tests. And for this one, we need to make sure that we remember to specify the working directory. So our working directory for running these API tests is the tests API directory.

[00:00:40] So again, that's this directory over here tests API, we need to make sure that that corresponds over here with our working directory. And then we can simply do a run command. We'll do a pipe so that we can do uh we can write lines on multiple lines. We'll do M PM CI just to install our dependencies.

[00:01:01] And then we should just be able to do MP X playwrights test and that should run our API test for us. Let's save that. And then I'm gonna commit that to the branch. We get A dot git commits and let's say running API tests

[00:01:23] and then push that again back up to the main branch. And then if we head over to the github actions, UI we'll click on the actions tab. We can see that the build has started running. So it's running through the back end and the Frontend tests.

[00:01:40] So once those are finished, the API test should start, OK, the API tests have started running, let's go in and watch that job. So the application started and we can see here that the API tests have all been run successfully and it looks like all of the tests have passed. We got all five tests in here as well. OK? So that's really good.

[00:02:01] So we're running the application first and then we're kicking off the API test straight away and those are running successfully. Now at the moment, we're not publishing the report yet. So we're still having to look into the logs to see whether the API test ran and if there was any errors. Um So what we'll do in the next lecture is we'll look at how we can publish our API test results

[00:02:20] report that's being generated each time so that we can see that in github actions and so that we can see the report in github actions clearly. So quick pause here and then we'll do that in the next lecture.