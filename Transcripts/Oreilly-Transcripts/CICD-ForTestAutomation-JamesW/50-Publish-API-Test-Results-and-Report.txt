[00:00:00] OK. So let's look at how we can start uploading and publishing our test results reports. So you remember when we run these API tests, we get a couple of different reports, we get this index dot HTML file and in the test results as well, we also get this results dot XML. So we're gonna want to upload both of these. If you remember though, we're not going to publish any HTML report just because

[00:00:23] that's a bit more involved in github actions. You don't generally publish HTML, but we are still going to upload the report so that it can be downloaded if users do need to look at it. So I'm going to show you how to do that. One thing I thought that we should do first is that after we've done running the application here,

[00:00:40] once the API tests are finished, we should actually stop the application as well. So let's just add in an extra step to do that. So at the moment after the run API tests, we can add another a step and that will be to shut down the application and we can do this just like we do it locally. So we can just literally say you should run a Docker

[00:01:05] composed down. That's it. So that should just stop the application now. And uh once, once the API tests are finished and now that the application has been stopped, now, we can work on actually uploading our results reports. So let's first upload the API test results.

[00:01:22] So we'll call this one upload uh API test results. So again, as we did before, we want to add an if condition and make sure that we always run this, always run this even if a previous step has failed. And then we use a certain action for this. We use the special upload artifact action

[00:01:44] and we use V four of that action and then we give that a few with parameters. So we give this a name that's called API test results. We need to specify the path. So these test results, they live in the tests API folder

[00:02:04] and then uh in the test results folder. So again, that's gonna be this one over here, this test results folder, it's just gonna basically upload everything that's in, in that folder. Um This, this results dot XML.

[00:02:21] The other thing we can specify here is a retention period. So we're gonna say the retention days to 30. This is just basically the number of days that the test results will stay on github on so that they can be downloaded. So again, you can set this to whatever you want typically, I think a 30 days is a good like default for us to set here.

[00:02:42] So that's these test results here being uploaded. Uh Here, there's test results in this folder, those are getting uploaded. We also want to make sure that we send out the playwright report as well. So let's do that. We can actually just copy this step because it's gonna be very similar.

[00:03:02] I'm gonna copy that, paste that below. Let me fix the formatting and just fix this formatting as well. OK. And so we're gonna say this one is gonna be upload API tests. Let's change that to the playwright report

[00:03:23] again. That's gonna be, if always it's gonna use the same action. So this upload artifact action. So let's change this one's name to API test playwrights reports and then the path we will change that over as well. That's going to be looking on the left, the playwright

[00:03:43] report. So let me just check that. I've got that right. So this path here tests API playwright report. That's this one here tests API playwright report. So specifically this folder that looks good, we can leave the retention period of 30 days there as well.

[00:04:01] OK. So that's gonna actually upload the report. So let's commit this first and see how that looks. So I'm gonna save the file. I'm gonna open up my terminal and then I'm going to add that CI file. Uh let's say upload API test results

[00:04:23] and then I'm gonna push that to the main branch. So that should trigger a build for us. Let's head over to the actions tab. Ok. That's running through. Let's let's let this pipeline run through and then we'll check what the API test results, how that job gets on.

[00:04:41] Ok? So the job's finished running, but it does look like it's failed. So let's take a look at the logs and looking here in the shutdown application. Oh, it looks like I've got a done a silly typo with Docker, so I've misspelled Docker. So let's just fix that right away. Sit down here at the bottom. Uh Yes, let me change that. Apologies for that. That's fine.

[00:05:03] So just going over to the github though, it looks like the other steps below where we're uploading the API test results, those, those steps still ran and they ran because in the UI in the um in the YAML here, we'd set these if blocks to make sure that this always runs. So that's good. So that's those steps have still run, which is good.

[00:05:20] That's actually what we wanted. So that's fine. Let's go back to the summary. So because we've uploaded those test reports, if we scroll down to the bottom, now, we now have these two test reports here. So we have the playwright report and we have the actual test results. Now, they're not easily viewable, but they can be downloaded. So if a user wanted to see that report,

[00:05:40] they can just literally click on API test playwright report and you can actually download the report. Now, obviously, that's useful if you want to download the report. But we're not actually publishing any test results here. So we haven't got like a backend or frontend test report like we've got here because we're not actually publishing anything

[00:06:00] but we can publish, we can certainly publish those XML um results so that we can view the API test like this. So let me show you how we could publish that through um the github actions just by making a change to the CI dot YAML. So let me just close the terminal so that we've got a little bit more space. So at the bottom, after we do the second upload, let's add in another step.

[00:06:27] And for this one, we'll just call it, publish API test results. And this is similar to like what we've done before. So we're gonna use a special action from Dawie test reporter

[00:06:41] and that's test reporter V one again, we want this to always run. So we'll say if it's always and then we'll give it our parameters. So we'll give this a name, let's call it API tests reports, the path that this lives in. So that path is gonna be in the tests API folder.

[00:07:01] Uh It's gonna be called the test results folder and then we're just gonna win and make sure let's just say we'll get anything dot XML. So we could specify results dot XML, but we could also just put a wildcard staff. So that will just take any, any file that's XML. It's gonna take that and publish that. We also need to take the name of the reporter and

[00:07:21] the reporter that we're using again is just J units. Ok? Let's save that. And then I will push that up as well. So we do get our doc commits mind, let's say, publish the P I test results and then push that up to the main branch.

[00:07:44] OK? That's gonna trigger off another build. Now, hopefully, this time it will shut down the container. OK? We won't run into any problems there and hopefully it should also publish the results report for us so that we can get a report that's similar to the front and back end.

[00:08:01] So let's let this run through and then we'll see if the report comes up. OK? So the API test job is run this time. It looks like it passed this time. So that's good that Typo was fixed, looks like all of the steps have passed. And if we look over here, now, we can see that we've now got this API test report appearing as well above our back end and our Frontend.

[00:08:21] So we can now click on that report and now we get a nice report just showing all of our API test results. So that's great. That's been that's been published in the same kind of format as we have for the back end and the Frontend tests as well. So this is a nice easy way for us to be able to visualize the tests that are being executed. So that's how to execute API tests.

[00:08:41] What we'll do now is we'll take a quick pause and then in the next module, we're going to look at how we can run end to end tests, um, which will be very similar to the API test because we're using playwright. Um, but yeah, we'll take a quick pause and then we'll do that in the next lecture.