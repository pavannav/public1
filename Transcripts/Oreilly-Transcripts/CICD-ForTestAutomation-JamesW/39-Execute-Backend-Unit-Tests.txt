[00:00:00] OK, great. So, as I said, now, we're going to move away from Jenkins and we're gonna go over to github actions and we're gonna start building out our CI CD solution and our workflows on there instead just going back into the D. The first thing that I'm gonna do is I'm just going to stop the Jenkins server from running. So I've opened up a terminal at the bottom CD into the Jenkins folder and I'm going to do Docker compose down

[00:00:23] and that's just gonna stop the Jenkins server. So we'll start it again later in the course when we want to reuse it. But for now, let's just kill Jenkins just so that's not running on our local machine anymore. I'm also going to close down the script dot JS because we don't need this. I'm gonna close down the Jenkins file. So all of our github

[00:00:40] actions workflow that we're gonna create. As I said, we're going to create it within our source code repository here. So we're gonna create it in this github folder. And you can see at the moment we've already got this first workflow. This is the one that we created previously in the github ui. So what we're gonna do, we're actually gonna take this out completely so we can just delete this file.

[00:01:00] I'm gonna create a new workflow file instead just completely from scratch. So let's create a new file and we're gonna call it CI dot YAML. So what we're gonna do is we're gonna go through and we're going to add in the code for this file just line by line just to give us a bit of a reintroduction into how the github actions workflow file looks.

[00:01:21] So the first thing that we need to do is that we need to give our workflow a name. So let's just give it a name of CI and then we also need to set up when we want this to the workflow to run. So we're gonna add in a couple of different options. So we say on and I'm going to say oops on

[00:01:40] just add two spaces on, push up to github under that we can then specify which branches and for our branches, we're going to specify just the main branch. So basically we want this workflow to run every time we push to the main branch. Now, as well as having the option to run on push,

[00:02:01] we also want to be able to trigger this manually in the UI. So to do that, we can add a workflow dispatch if I can spell that right workflow dispatch. There we go. So again, the workflow dispatch. That just means that basically when we run it, we can then run this directly

[00:02:20] and we push to the main branch as we've specified here. Ok? That looks good. Let's now add in the details of our jobs. So we're gonna start out kind of similar to what we did with the Jenkins file. We're gonna start out by just running the unit tests first and what we'll do is we'll just, so let's create a job and let's call this job, unit tests backend.

[00:02:42] And we're gonna say that job, we need to specify where you want it to run. So we're gonna say we want this job to run on an Ubuntu latest runner. Again. We talked a bit about the different kind of runners previously. Once we've got our runs on, we can then add in the steps. So these are the steps that the job needs to go through.

[00:03:04] Ok? So the first job is just gonna be checking out the repository. So to do that, we use an existing github actions and that action and that action is just called checkout. Again, we did a similar thing in Jenkins. I think it's included automatically in Jenkins.

[00:03:20] Um Let me just check in the Jenkins file that does if we specify that. No, we didn't even need to specify in Jenkins. It does it automatically. But in github, we are actually just specifying that we just want to check out the source code. So now that we've checked out the code, we want to run the back end unit test. So to do that, we can identify another step.

[00:03:41] We're just giving this step and name of execute back end unit tests. So this is just the name that's going to appear in the, in the, in the UI when we run it. So we'll see that in a moment. Now again, we need to specify the working directory. So the working directory again, this is just like what we did before with Jenkins. But we're just basically specifying the directory

[00:04:02] that we're, that we want the unit tests to run in. So again, this working directory here that corresponds to the bug tracker back end here. So those are the same. So once we've got the right working directory, we just say run and then we can then put in the pipe

[00:04:21] and then we can specify the shell commands that we want to run. So again, we want to run the unit tests. So to do that, it's go test minus V dot forward slash triple dot And that should run all of the go unit tests. Now, before we commit this and we actually run it, I can tell you right away that this isn't going to work and it's going

[00:04:40] to have the same problem that we ran into on Jenkins is that when we, when we use this Ubuntu latest runner, it's not going to have go installed. So we're gonna have to install, go beforehand. Now, what we were doing before with Jenkins is that we were either installing go on on Jenkins or we were using a Docker image that would install,

[00:05:01] go. Now, what we can do on github is that we can actually use special actions that have already been predefined and these will do the work of installing go quickly on our github instance for us. So that's what we're gonna use now just to get an idea of how those work. So let me show you how we can use one of those actions.

[00:05:21] So we'll give uh we'll have another step here. So the step, the step will be called set up go. And again, we say users because we want to specify the actions that we're going to use. And the existing action that we're using is the set up go action. And again, that's version four of that,

[00:05:41] we can then add in some parameters for that action that it needs. We can specify which particular Go version we want to use. So I'm gonna say go version 1.21. And below that, we can also specify a cache dependency path. Let me just type that in. So cash dependency path,

[00:06:02] what this cash dependency path basically does is once um the branch, once it's cloned go and it's got the cache set up, it basically makes all of the runs after that much quicker. So the first time when we don't have the cash dependency path, uh The github action, it needs to cache all of the go the GO files that come with it.

[00:06:21] But then once those are cache, that then speeds up the run. So it's normal that we'll put in a cash dependency path. Now, for us, our GO file that's in this go dot sum, this is the cash dependency. So we're gonna add in just a link to that. And again, that's just gonna make our builds that little bit more efficient. We say that lives in the bug tracker back, end forward slash go dots

[00:06:44] file. So again, that's this file over here. OK? That looks good for our initial CI dot YAML. So what we're going to do is we're going to commit this. So I'm gonna make sure I'm in the root bug tracker project,

[00:07:02] get status just to check what I'm adding to get that dot That looks good. So we've removed the first workflow, we're adding the CI dot YAML. And then let's say git commit, let's say running back end unit tests on

[00:07:20] guitar actions and let's push that up to the main branch. OK? And up that goes now again, because we've set up this push on main here. We should see this workflow run directly in github actions once we push it up. OK? So I'm here in my github

[00:07:41] project. So if I go over to the actions tab, we can see that our action here is running. So if we click into it, we can see now that we've got this unit test back end. So this is what we defined in the CI dot YAML. If we click into that, we can see that it's running through. So it's gone through the steps here. It's gone through the steps of checking out the source code.

[00:08:01] It's then done the action to set up, go. So it's set up go version 1.21 for us. It's then gone through and executed our back end unit tests. We can have a look through the logs. It looks like all of the tests are passing. So that's really good. And now it's doing the post setup go. So it's doing like the clean up and it's going to be doing the caching. So we can see down the bottom here.

[00:08:20] It's saying that the cache it's been saved. So again, this might take a couple of minutes to run through, but it should mean that next time when we make builds, this cash should be saved and it should actually go through a lot quicker. So I'm just gonna go back to the top level and just give that a minute to finish. Click on here. I'll just wait for the post setup, go to finish. Ok. That's finished. And now it looks like the job's finished successfully.

[00:08:42] So that's great. And we're now running the unit tests. So if I just go back to the CI here, we can see at the top level. So we can see that when we run that, it took about two, maybe three minutes to run. And that's again because it had to do a bit of caching. What I'm gonna do is I'm actually going to set this workflow off again so I can click run workflow on the branch main.

[00:09:01] And again, we can do this because we added the uh workflow dispatch call in here. So I've just triggered that off to run again. If we just refresh, you can see here that the run has started. So we'll just give this a few seconds to run through. So it's doing the steps again to do the setting up of go. It's then gonna execute the back end unit tests.

[00:09:21] So the tests are running through, it's done the post setup and you can see how much quicker that ran through this time. So if we just look at the top level again, so this time it ran through in 20 seconds. So you see, because it's been cashed now, the runs are gonna be a lot, a lot quicker. Ok? So that's how to set up our back end unit tests. Let's take a quick pause here.

[00:09:40] Then when we come back, we'll look at how we can get the frontend test set up. It's a quick pause and I'll see you in a minute.