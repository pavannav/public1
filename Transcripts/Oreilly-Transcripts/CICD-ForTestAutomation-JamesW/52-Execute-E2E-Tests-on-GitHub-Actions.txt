[00:00:00] OK. So let's start running our end to end tests through github actions. So the first thing I'm gonna do, as I said is I'm gonna change the name of this job. So I'm gonna change it to a P A API dash end to end tests. Just to show that we're running both the API tests and the end to end tests. And then we're gonna add it in a new step that's gonna run the end to end test just after this one here.

[00:00:21] When we run the API tests, once that one's finished, we're gonna add in another one and we're gonna call that one run and end to end tests. So for that uh step, we want to specify the working directory and we're gonna call that,

[00:00:41] we're gonna point that one to the test dash, end to end directory. So again, as I said before, we're gonna, it's gonna run this in headless mode and it's going to do that because in the playwright config it's gonna detect this parameter here is already set. Now, what we could do is we could actually set an environment variable.

[00:01:02] I just want to show you how to set an environment variable just so we're explicitly setting this. So though this environment variable should already be set, I just wanna show you how we can actually set specific environment variables. So it's looking for this E MVC I environment variable. So I'm gonna say environment variable

[00:01:20] or environment variable is CI and I'm explicitly setting that to one. So this is kind of optional. You wouldn't necessarily need to set that here for in order to get that to work because it should already be set to one because it's running in github actions. But I just want to show you how to explicitly set environment variables. We'll be setting more environment variables later a bit later on in the course.

[00:01:41] But I just wanted to show you that now. OK. So with that set, we're now ready to run our playwright tests. Now, just before we do that, one thing I did want to mention is if you remember in Jenkins, um we were running our playwright tests, we were running them using a special Docker container that

[00:02:00] had basically the playwright bits and pieces already installed. So if I just go over to the Jenkins just to find that here. So we're using this Docker image here, the playwright Docker image. So that's one thing that we could do in github actions as well. We could run in a Docker image that would be fine. What I'm going to do here is I'm actually

[00:02:21] going to just install playwright here at this point just to show you like a different way of basically how we could, how we can run this. It should still run very quickly because it's only running in headless mode. It shouldn't take very long in terms of downloading playwright and installing it. I just want to show you basically a different way that we could run it. There's obviously always like lots of different options

[00:02:41] for how you set up your pipelines, your CI CD. But I just want to show you some of the different options. So the first thing that we do then is we run M PM CI and again, that grabs all the dependencies. We're then going to say MP X playwright install. So again, that's just gonna make sure that playwright gets installed

[00:03:00] and then after it's been installed, we can do MP X playwright test, ok. So that should run the end to end test for us. I don't think we need anything else after the end to end test run, it should shut down the application. So let's see if this runs. Ok. So I'm going to open up a terminal

[00:03:23] gonna add in my changes, gonna commit this and we're gonna say running end to end tests. Oops before I do the commit, I need to make sure that I'm in the right folder. Apologies for that. So I've just cded into the roots and then we do get that up. Now we do the commis

[00:03:42] again and now we can push up to the main branch and then once we push up to main, that should trigger a build for us, we can see this one here running end to end tests. So you can see here that our job name has now changed to API dash, end to end tests. But if we look in that job, so we're still waiting for it to run.

[00:04:03] Uh I think I can't click into it until it's actually running. So let me just give that a second just for that to start running, then we can click into it. There we go. So it's just waiting for the github action runner to pick up the job. There we go. So let me just close this so that we can see clearly what's going on so close the launch application.

[00:04:23] So even though everything is still running within this API end to end test, we can still see the different stages clearly. So we can still see that we're launching the application, we then have the step where we run the API tests and then we have the step below that where we run the end to end test as well. So even though it's all within one job, we can still see the steps.

[00:04:43] So it is still like separated quite nicely. And as I said, this is all running on this one particular machine. So at the moment here where we do the setup job, this actually, you know, procures the runner for us and then it's doing all of the work to launch the application to run the API tests. And then the end to end test should run straight after that.

[00:05:02] Um So again, we could run this on a separate, a completely separate job that would be fine. It's just that if we did that, we would have to launch the application each time and we can see that it takes around a minute or so for the application to launch. So that's why I'm just running it all on this one.

[00:05:20] on this one job basically to try and speed up the pipeline again. For your use case, you might be fine with it taking a bit longer, you might want to have it on separate jobs. That's totally fine. I just want to show you different ways that we can do it. Ok. So it looks like the API tests are running. That's good. Those have gone through quickly. So we're now running the end to end tests and we can see that it's installing

[00:05:40] um playwright and it's installed in the browsers that we need for us as well. So even though it's still gonna run in headless, it still needs to download some uh some element of the browser even in order to run it in a headless mode. So it looks like the test is running through. It looks like the tests have all passed. So that's really good. It's now stopping the application and it's publishing our test results reports.

[00:06:03] So that's great. That's the end to end tests that are all running successfully. The only thing that we're missing is that we're not yet publishing any end to end test report. So again, we're gonna want to upload the playwright report that gets generated and we're gonna want to try and publish an end to end test results report.

[00:06:23] So let's take a quick pause here. And in the next video, we'll look at how we can publish our test results report in github actions.