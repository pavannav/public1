[00:00:00] OK. So that's how we look at how we can add in a back end unit test report. So if you remember from our work that we did with Jenkins previously to generate the JUnit backend report because it's a go project, that's actually a little bit more involved. So if you remember we had to run this command here, this go test and we had to use this Go JUnit

[00:00:22] report as well. This was actually a different dependency that we had to go ahead and install. What I was doing before is that I was, we were adding this into this particular Docker image, but we're not using that Docker image anymore. So what we're gonna have to do is we're actually gonna have to install the, the Go JUnit report dependency as well. So let me show you how we can do that in github

[00:00:42] actions. So I'm actually just gonna take this line here and copy that just to save me typing that out, gonna go back over to my CI dot YAML. And then let's go up here to where we're executing the back end unit tests. So I'm going to replace the current line here where we're running the test and I'll replace that one with the line that we just copied.

[00:01:03] So again, we're piping out the output and then we're using Go JUnit report and then we're publishing that to this test results dot XML. So we're generating this XML file, then we're going to use that with the same reporter to create a simple sort of report to what we're doing for the Frontend. So in order to use this Go JUnit report, we are going to need to install that.

[00:01:21] So let's add in another step just above which is going to do that installation. So let's add in a new step. And the step we're gonna call it install go uh J units, units reports. And all we're going to do is just say run and what we want to run is go install,

[00:01:44] we say github.com and then it's forward slash JS te double M er forward slash go dash J units, units, dash reports. And then I think it's feed two and the latest version,

[00:02:02] ok? So that's gonna install the Go Je unit report for us so that we can use it down here. That looks good. And then, so now after we've executed the unit test, we should be able to publish that report. So let's add in another step similar to what we did for the backend, let's say publish backend test results

[00:02:24] and we're going to use the same action as before. So it's gonna say it uses the test reporter at V one from dawn. And again, we want to make sure that this always runs for the same reasons that we spoke about previously.

[00:02:41] So then we say we're gonna give our report a name. So we add some parameters, we give it a name, let's call it backend unit tests reports. Uh We need to specify a path. So again, the path is going to be the bug tracker back end and then our file I think is called test results.

[00:03:01] Yes, test results dot XML test iphone results dot XML. And finally, we specify that the reporter. And again, we're using the J JUnit reporter. OK? I think that looks good. Let's save that. Push it up

[00:03:21] circuit status, add skit commit minus M and now it's published back end results report. That would do let's push that up to the main branch. Once we push it up to main, that will trigger the build. Let's hop back over to github.

[00:03:41] I go to the actions tab, we can see that that's kicked off. So we're running the back end unit tests now. So if I click into that, I can see all of the steps have run. So the frontend tests are still running, but you can see already this backend unit test report has been generated. And we've now got a really nicely formatted backend unit test report.

[00:04:01] So this is similar to the Frontend, but it shows in a nice graphical reference sensation how all of the back end unit tests were run. So if we wait just a few seconds, if I go back to the summary, then the Frontend unit test should finish. And then we have the frontend test report as well. So now we've got the back end and the Frontend unit test report.

[00:04:21] These are available in github actions for us.