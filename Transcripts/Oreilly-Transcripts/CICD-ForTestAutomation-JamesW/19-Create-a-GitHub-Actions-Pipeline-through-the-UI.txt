[00:00:00] OK. So now we've seen how to run a simple pipeline through Jenkins. Let's now also see how we can build a very similar workflow or pipeline with github actions instead. So again, we're just going to do this through the browser. So again, I'm in my github, the branch of the bug tracker over here. I'm just going to click on the actions tab

[00:00:20] here. We can start building out our first github actions or our first github actions workflow. So let's just search here. Let's search for simple workflow one for us just to get started with quickly. So when there's simple workflow here, let's just choose to configure and now let's just go through and just talk through this line by line. So first here we just give our workflow a name.

[00:00:41] So they called it CI let's change this to first workflow. So that's just the name of the workflow. So down here, these are the build triggers. So this basically indicates when the workflow is gonna run. So at the moment, if we make a pull request to our main branch or if we make a push directly to our main branch, that's gonna trigger this workflow to run.

[00:01:02] There's also this option here for workflow dispatch. What this basically means is that we can execute the workflow manually using the github actions tab. And we're going to see that in just a minute. So if we go down a bit, we can see on line 17, we have the jobs. And again, the workflow is basically made up of lots of different jobs and we're gonna be writing a lot of these throughout the course.

[00:01:21] So here they've given us one example job and they've just called it build. Again, you can call our jobs anything. So for this one here, they've just called it build. And again, throughout the course, we'll be naming our jobs differently as we build out our different jobs and different workflows. Now, we can see here we have the runner um specified. So this one runs on Ubuntu latest. So throughout the course, most of the time, we'll be using the

[00:01:42] Ubuntu latest runner. That's just basically the machine where the workflow is gonna run for us, the type of machines. So that's basically just the machine that the workflow is gonna run on for us where the CI CD is going to run. So for this course, we should be fine to just leave this on Ubuntu latest if we head over to the github

[00:02:00] documentation, which is uh this address where you can just put in the first part and then just browse down to this you can see here some of the different runners that we could choose. So for example, we're choosing Ubuntu latest or you could choose a specific version of Ubuntu. You could also choose like windows, for example, macOS, etcetera.

[00:02:20] But again, for this course, we'll just be using the 12 latest. So we don't need to worry about it too much. Let me close that tab. So back here down in the file, if we now go down to the steps. So as it says here, the steps are basically the sequences of tasks that are going to run. So here, the first step that's set up is to check out. So this basically just checks out the source code repository.

[00:02:40] So it just checks out the code that we have in our repository. And here it's just doing a simple one line script. So it's doing an echo hello world. So this is very similar to what we just did in Jenkins, except this is now running through github actions instead. And then down here we have a multiline script. So again, this is very similar to what we have B except this is just basically a run command over several different lines.

[00:03:03] So because it has the little pipe symbol here, it lets us do commands on different lines. So this is letting us run shell commands. So again, we could do another shell command, we could run date here. For example, as we did with echo and then that would print out the date as part of this workflow run. OK? So let's save this file. So to do that, we're gonna click on commit changes,

[00:03:22] we're just gonna commit directly to the main branch. And actually, before we do that, let's give our workflow a proper name. So let's just call this one, let's say first dash word flow. And again, now we'll click on commit changes. So now we're saying we're going to create our first workflow dot YAML. We can just commit this directly to the main branch, click on commit changes.

[00:03:43] So if we now open up the actions tab, we can see here that our first workflow, it's got this orange symbol which means that it's executing, we click on that, we can see the build message here. If we click on that, we can then look through the logs in here like so we can see here where it checked out all of the source code,

[00:04:01] it then ran our one line script, then it ran our multiline script and we can see here at the bottom. It did that extra command that we added in to do the echo just to echo out the date for us. So again, it's very similar to what we just did with Jenkins, but this is doing it through github actions. So if we go back up to the top, I'll go back to the workflow here.

[00:04:22] Actually, if we just click on actions up here. So if we click on our first workflow, it's the one we just created. And if we want to run it again because we have that. And if we want to run it again, because we have that workflow dispatch, we can just click run workflow, click run workflow. I've just refresh the page and now we can see that it's been executed again here, like I say, so if we click on it,

[00:04:43] but basically the run is going to spin up, it's going to run the job again for us. We don't get the green check and then we can click into it. And again, we can go through the logs and see how the run went and see the logs from the run. So that's how to create github actions through the browser. And again, mostly through this course, we'll be correct,

[00:05:01] we'll be creating them in code and we'll be doing that in our ide. But for this lecture, I just wanted to show you how we can create them quickly in the browser.