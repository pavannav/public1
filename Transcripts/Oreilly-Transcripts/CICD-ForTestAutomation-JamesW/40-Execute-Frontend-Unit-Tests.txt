[00:00:00] OK, great. Now that we've got our back end test working. Let's look at getting the Frontend test working as well. So to do that, we're gonna add in a separate job. So right above here, we've got our unit test back end job just below that. We're going to add in another job. We'll call it unit tests Frontend. Now, for this job at the moment, we could have them run in parallel and that is what we'll do later on.

[00:00:21] But for now I'm going to make it run sequentially. So we're gonna wait for this first job up here, the back end job. We're gonna wait for this one to finish first. I just wanna show you how we can set that up and then we'll remove that later on and we'll have them run in parallel. So to do that, we can just say needs and we're basically going to say it needs the unit tests back end to finish first.

[00:00:43] We're saying that we want, we don't want this job to run until this one has finished. Basically. OK. We're also gonna specify what we want it to run on and again, we're just gonna run it on Bantu latest. OK. So you bond to latest and then we define the steps as we did before.

[00:01:04] So the first step that we'll have is the actions and that will be the checkout again. That's a pretty standard step for us to do first. Once we've added in the action, we'll again add in another one where we're gonna execute uh the frontend,

[00:01:22] frontend unit tests as before we need to set the working directory for these tests. So our working directory here is gonna be bug tracker forward slash frontend. So again, that's this directory over here. And then we need to say how we run the frontend tests.

[00:01:42] So just as a reminder to run the frontend tests, if I CD into the bug tracker frontend folder to run the frontend tests, we refer to M PM CI to install all of the dependencies that we need. And then once the dependencies have all been installed, we then do I think it's M PM test and that will run, run all of the different tests.

[00:02:03] So it's an M PM CI let's do our M PM test and then that should just run the unit tests quickly for us. So you can see here that the tests have all been run successfully CD back to the route, just close down the terminal again for a second. So let's put in the run command,

[00:02:21] we'll put in the pipe because we want commands to run on multiple lines. The first one we'll run is M PM CI and the next one is M PM test. Now, of course, we're gonna have the same problem as before and that we need to install node. So again, there's another action that we can use here. That's where we can set up node in our github

[00:02:40] actions. Kind of like what we did with um with what we did with Goland before, but we can do it now with node instead. So we'll do that before we run the execute Frontend unit tests. So underneath, once we've checked out, let's do another one for where we're setting up node. So set up node dot JS

[00:03:02] and that's going to have an action. Sorry, that's gonna say uses an action, I should say uses actions and the action we can only use is the special setup, node action. So again, this is an existing github action that's maintained by someone else. And then that does all of the work to set up node JS for us

[00:03:24] again, similarly to what we did above, we can give it a few parameters. Oops. And so we can say we want the node version, we want that node version to be version 20. And we can also set up the cache again as we did before.

[00:03:40] So we just specify that we're using M PM as our cache. And kind of like what we did above. We have the cache dependency path we can actually do a similar thing here for, for our node setup. So our cash dependency path, let me just make sure I spell it right, cash dependency path that's gonna be in the bug tracker forward slash frontend folder

[00:04:04] and that file will be called, it's this package lock dot Jason. So we've got bugtracker frontend forward slash package dash lock dot JC. So again, this is doing a similar thing but before it's gonna take a bit of time to run through the first time,

[00:04:21] then when we go through again, when we run a second run, um everything should be cached for the Frontend tests and it should make our pipeline run much quicker. Ok? I think that looks good. So let's go ahead and commit that. So I'm gonna open up my terminal. Let me do get add dot Let's go to commits. So we say get commits, let's say running frontend

[00:04:44] unit. That looks good. Let's push that up to the main branch again. When we push up to main, that should trigger a build automatically. So let's jump over to github and watch that we go to the actions tab. We can see here that the build is being triggered.

[00:05:01] So we can see here now in the UI it's showing that it's going to be running the back end unit test first. And we can see here that the frontend test. Those are waiting until the backend tests have passed before they start executing. So the backend tests have now finished and now the frontend tests are running. So if we want, we can click into the backend tests, we can see all of the details here.

[00:05:20] These are the logs of the backend test. And if we go into the Frontend again, we can see how the frontend tests are getting on and how those are running. So again, this is gonna take a few minutes just to run through the tests. We can watch these in real time and then it's gonna take a little bit of time just to cache the bits and pieces at the end. OK? That's done.

[00:05:40] So that was pretty quick. And that's those have run through now. So now we've got our backend tests and our Frontend tests running. Now at the bottom, there is this error here about the cache not being found. This should only happen on the first run through because the cache had to be created the first time that we executed this workflow. So what we could do is if we go back if we just execute it again.

[00:06:00] So I click on CI I'm gonna click on run workflow again and run the workflow that's going to trigger the workflow to run again. So let me just refresh the page. We can see that we've got this new build that's appeared up here. So that should start running through in a second. OK? So you can see the back end test. Those have gone through already. The Frontend tests are running through now.

[00:06:21] So we'll just give those a few seconds to run, just go back to the main CI page while we're waiting. So again, what's happening each time is that github is spinning up a special runner for us. It's been running our tests and it's giving us all of the logs et cetera of the tests that run. Let's click in here, see how that's doing. So it looks like the tests have gone through. It's now doing the post setup, set, post setup bits that looks good.

[00:06:43] If we go through to the summary, we can see that that error is now gone as well. So now because it's all cached, it's running through as quickly as it can and we're not getting any cash related errors anymore.