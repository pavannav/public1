[00:00:00] OK. So we've got our back end code coverage report. Now, let's now see how we can do the same for the Frontend. So I'm gonna go back over into the ID and we take a look in the frontend folder if we look in our package dot Jason. So at the moment, you can see here when we run the test command, we've actually already set this up to have code coverage already included.

[00:00:22] So we can actually do this a lot easier with node packages compared to go. So there isn't actually going to be quite as much work for us to have to do let's CD into the frontend folder again. And then let's just do MP mpress just to run the tests. So again, we can see that the test front and we actually get this coverage folder here.

[00:00:41] And within that, that's already got the index dot HTML file with our code coverage file already generated for us. So all that we need to do is we just need to publish this on Jenkins in a similar way that we did in the previous clip. So let's close that file. I'm going to close down the terminal just to give us some more room and come down here to the Frontend part of our Jenkins file.

[00:01:01] So here under npm test, I'm gonna again make another directory. So we make another directory and we'll call that one reports. Ok? We're gonna say move everything from coverage, move that into the reports folder. So again, what's that's just saying is just move everything that's in coverage and that's gonna be the coverage which we have

[00:01:22] it up here. Everything in this folder was saying basically to get moved into the reports folder. So once we've done that in our post, step here down at the bottom again as we did before, we can publish HTML to specify the target

[00:01:40] and then target the fault directory. Call it our report directory of tracker frontend. It's gonna be forks folder that we just created above and then looking in coverage and then the particular report files that we need.

[00:02:00] That's gonna just be this one over here, this index dot HTML file. And again, that's the one that lives in this coverage folder, the coverage folder that we're specifying here and that we moved into reports when we did this step here in my blood, the report file which is called index dot HTML.

[00:02:20] And we just need to give our full name. Let's give it a name of frontend coverage, ok? That looks good CD pack. Make sure that I'm in the roots again, get status ski at the Jenkins file again,

[00:02:42] get add dots, click commit. Uh Let send, publish contents cover the sport. Push that to the main branch. Head back over to Jenkins. Now gonna build that. Uh build the branch

[00:03:02] looks like we might have an error. Let's have a look at our port. Ah Yes, I can see. I forgot to add the commas, those apologies for that, adding a co commas there for this array. This time again, that commits

[00:03:20] push back to main again. Let's go back over to Jenkins, try and run that once more. That looks like that's running this time. So let's see. Let's just give that a few seconds just to run through the build is finished. Let me just refresh the page. You can now see here we have this Frontend coverage report.

[00:03:40] So if I click on that, we now have this nice nicely formatted frontend code coverage report for us. So this is really cool. We now have a frontend code coverage report and a backend code coverage report being published directly into our Jenkins pipeline and this is really useful for them for other team members to go and see exactly what our code coverage is like in our project.