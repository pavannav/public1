[00:00:00] OK. So now that we've got our API test working in Jenkins, let's also look at how we can run our end to end tests. So can I jump back over to the ID? And you can see below tests. API, we've got this tests, end to end folder. And this is quite similar to the API test because we're running these with playwright again. But what we're doing with these is that they actually run through a browser.

[00:00:22] So these are the proper end to end tests. Typically the test that you run when you want to test the full journey through the browser. So in um in this test, end to end folder, we've got a folder in here and that's got um this integration spec dot ts file and that's got our tests in here for basically for allowing us to run our playwright tests.

[00:00:41] So before we run these on Jenkins, let's just try and run them locally. So to do that, I'm going to open up a terminal and then I'm going to CV into the tests E two E folder. And from here I can do M PM install just to grab all of the dependencies that we need. And now that I've got depends, let me try running MP X playwright test.

[00:01:02] And so when I ran MP X playwright test, I actually ran them through the actual browser. So let me just run that one more time. So here, when we can, when we run that, we've um my screen moves over to this one, you can see the actual browsers here and it's actually running through each of our test cases. So we can actually watch the test running because it's actually going through

[00:01:21] a real browser and it's implementing the playwright test that we've specified. OK. So that's how to run the playwright or the end to end tests locally. And when we actually run this through the CI we're not really going to want to run it through the actual browser, we're gonna want to run in what's called a headless mode. So again, this isn't really a playwright course.

[00:01:40] So I don't want to go into too much detail about playwright. But in the configuration options here, I've actually added in an option here to make sure that we run in headless mode when we're running on the CI environment. So we go, when, when we run through Jenkins, it's basically playwright's gonna detect that it's running on the CI through Jenkins. So instead of running through the browser because of this setting,

[00:02:01] it's actually going to run in headless mode. And we're gonna see that in the next clip when we run this through Jenkins. So let's take a pause here and then the next step, we'll get our Jenkins file set up so that we can run the end to end test through Jenkins.