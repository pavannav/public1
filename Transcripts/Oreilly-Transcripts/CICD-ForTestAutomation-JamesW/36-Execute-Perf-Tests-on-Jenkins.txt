[00:00:00] OK. So let's see how we can run our K six test through Jenkins. So we're gonna run, we're gonna add it in a new stage to our Jenkins file to run those tests. So I'm gonna go down here after my last end to end test stage, gonna add in a new stage and we'll call this one performance tests.

[00:00:20] So again, we've got a few options here. We could go and install K six every time or we could use something like a Docker container. Now K six does have its own uh specially dedicated Docker container. So I think we're just gonna do that again. Um Just for, just to keep it consistent with everything else we've been doing in Jenkins. So again, let's specify an agent. It's gonna be another Docker agent.

[00:00:42] So the image that we're gonna have, that's going to be the Grafana slash K six image. Let me just pull the latest one. As always, we're gonna reuse the node to set, reuse node to true and we need to pass in the same sort of args that we've been passing in previously as well.

[00:01:02] So we're gonna pass in the network equals host, oops network equals host, gonna set the root user. So say minus U to root. And also because of this K six Stocker container, the way that it runs, we actually need to overwrite the entry port into the container in order for this to work.

[00:01:20] So to do that, we're just gonna specify an entry point flag and we're just gonna specify, we're just gonna set the entry point to nothing or an empty string. So just make sure you add that it's in otherwise the K six container, it's not going to work through Jenkins. We need to overwrite the entry point here for this to work. OK? That's our agent setup. Now, we can simply add in our steps

[00:01:40] and we just need to specify the directory that we're running in. So again, that's gonna be our tests per directory. So again, as always as we've been doing before, that's this directory over here that corresponds to the test per directory. And in that, we're just going to run a single shell command. And as we did locally, that's gonna be K six R

[00:02:00] script dot Js. OK? That looks good. Let's save that far. Gonna open up my terminal again, my CD into the bug tracker folder at my Jenkins file. I'm going to add in a commit message, let's say running

[00:02:20] tests on Jenkins and let's do git push origin main. We'll head back over to Jenkins, we'll kick off another build and as always, we'll let that build run through and then we'll see if our performance test gets run at the end. And after a bit of time our build succeeded, our performance tests have run.

[00:02:42] Let's go and have a quick look in the logs. I'll check the console output. Scroll down to the bottom. We can see here. We've got logs for where our performance test has been run. So we're gonna, we're running the performance test. We're not actually publishing the report yet. So let's do that in the next lecture. So we'll take a quick pause in the next lecture.

[00:03:01] We'll actually publish the performance HTML report as well.