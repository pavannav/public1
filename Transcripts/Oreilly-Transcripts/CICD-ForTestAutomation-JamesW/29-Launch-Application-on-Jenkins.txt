[00:00:00] OK. So in order to run our application on Jenkins, what we're gonna do is that we're gonna run it in a similar way to how we're doing it locally. So we're gonna run it through Docker and using Docker Compose. So in order to do that, we're gonna have to use another Docker container. And we're basically going to run Docker inside Docker. This is quite a common practice with CI CD. So it would be good for us to get some experience of doing it.

[00:00:21] So let's see how we can do it now. So we're gonna add a new step. So if we're here in our Jenkins file, I'm just here. I'm just adding in, I'm gonna add in a new stage just after the execute unit test stage. I'm gonna add a stage and Lancer stage. Let's call this one launch application

[00:00:46] and then this again is gonna be an agent. It's gonna be Docker. So the Docker image that we're going to use, we're just going to use the official Docker image. So again, this is the Docker in Docker image. So we're gonna call, we'll just call a specific version. Let's call 27.5 0.1.

[00:01:04] Again, we need to add the reuse node flag. It's the flag that we added before when we were using dock up just to make sure that the Docker image is reusable throughout our whole Jenkins pipeline. And then in order to get Docker composed to work, we do need to add in a few args here.

[00:01:22] So this just basically sets up the Docker networking for us. So let's just add those in first and then we'll talk through them. So the args that we need to specify, we need to specify durable. Let me say the run Docker dot So, and then we bind that to V

[00:01:42] one sock. So bar run Docker dot sock, what we're specifying here. So var run Docker dot So here what we're binding here. This basically just means that the Docker that we're running, it allows it to listen across the system and that it allowed us to bind on the correct port so that we can launch our application and

[00:02:01] that we can send our API test against it. The other thing that we need to do here is that we do need to run this Docker container as root. So to do that, we can just type in so to run as root, we can just to minus you zero. So that basically just tells, tells our scripts or our Jenkins file here that we want to run Docker here as the root user.

[00:02:21] And again, this is necessary because the Docker socket that we're running, it requires root privileges in order to set up the networking for us. Now again, you might not want to do this with a production system, but because we're just trying this out locally, we're trying to get it set up, we're just trying to keep it as simple as possible. So for a local setup, this is OK for a more production setup, you'd have to look into more advanced networking

[00:02:41] configuration and probably not run this route. But for our use case here, this is OK. So now that we've got the Docker image set up, let's add in our steps. It's just under the agents uh area. Let's do steps. We're just going to run one single step. It's gonna be a shell command. And just as we've been doing on our local machine,

[00:03:00] we're gonna do Docker compose up and then we'll say dash dash build minus D again, that's just going to build the Docker containers if they aren't already built and then run in detached mode. OK. Let's save that Jenkins file. Let's open up our terminal, let CD back into the root of our project into the bug tracker.

[00:03:22] And then let's update the uh Jenkins boxes get as dots tip permits minus M I say running location, we need Jenkins.

[00:03:41] Let me just push that top. I'm gonna jump back over to Jenkins and I'm going to run the pipeline by clicking build now. Ok. So we'll just let the pipeline execute our unit tests and then we'll see what happens. Excuse me in the next step after that, hopefully it will launch the application, ok? And the pipeline is finished successfully and launch application that screen.

[00:04:01] So let's just take a look at the logs. I'm going to go down to the bottom and look at the launch application logs and we can see that here. We've got the logs that show that the back end and the Frontend were both started. So our application was started. Now, we haven't actually run any tests against that application yet. We literally just started the application and the pipeline finished.

[00:04:20] So what we'll do in the next clip is we'll add in another stage where we're going to actually run the API tests against the application that we just started. So we'll do that in the next clip.