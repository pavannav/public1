[00:00:00] So what we're gonna do now is we're gonna clone the application that comes with this course. So the bug tracker application, this is basically an application that has a back end and a Frontend and it's, you know, a full system. So we're gonna clone this now down to our local machine, gonna get it running running locally and we're gonna have a look at. So please browse over to this URL here. This

[00:00:22] github.com James Willet forward slash tracker if you browse over there. And what we're gonna do is we're just going to fork this repository. So I've logged into my second github account here and I'm gonna click on fork up here. So this will then give you the options to create a new fork on your local machine.

[00:00:40] You can leave this ticked here. So where it says copy the startup branch only, that's the only branch that we're going to need to do, leave that ticked and then simply click on create fork. So after a few seconds that fork should be created and then we can clone that to our local machine. So to do that, I'm going to click on code, I'm going to use SSH to clone and I'm going to copy that URL to my clipboard.

[00:01:01] So I'm here on the terminal, I've just created this folder here called bug tracker. So then there, I'm just going to type git clone and then just paste in the command that I previously copied the answer. And that's gonna clone down the repository that we just bought. We can now open that up in visual studio code. So the quickest way to do that is to type code dot

[00:01:22] And then that's going to open up the repository in visual studio code for us. And you should get a screen that looks something like this. So we can see over here on the left that the bug tracker application comes with a few different folders. So we have the back end code, the Frontend code and then some other folders here that have the different tests. So we're gonna go through each of these in turn,

[00:01:40] but I'll go through them one at one at a time and talk about each one in order. So let's start by having a look at the back end folders. So the back end is basically written in go now, don't worry you don't need to understand go lang or know how it works at all. For this course, all of the code is included for you already. I'm just going to show you how we can run how we can run it on our local machine. So I'm just gonna open up a new terminal

[00:02:01] and then I'm gonna CD into the bug tracker back end. So top here, I'm gonna do go mod download, just download any missing dependencies. So again, I can do this because I've already installed Golan. If you haven't installed, go lang or you don't want to install, go lang, I'm gonna show you in a minute how we can run this through Docker instead. But if you've got go lang installed, you can run it locally without Docker,

[00:02:21] which is what I'm going to show you here. Run up the we can do go runcmd bugtracker go. So when we do that, we get a message down the bottom here that the server has been started and that's running on localhost port 80. So our back end is now running and what we can do is we can now start making some API calls to that back end.

[00:02:40] So if I open up the Readme dot MD, this Readme file, this contains all of the details of all of the different um HTTP calls or API calls that we can make to the back end. So we're going to try out a few of those different calls now. So I want to leave this terminal open with the back end running. So I'm going to create a second terminal so that just click the plus one here

[00:03:01] and now we can make our cURL calls or API calls'. Do that I'm going to do cURL to first call the health endpoint. So I'm doing cURL localhost at a health. We can see when we make that call. We get back this message with the status. OK? And the version number that we're running off our back end.

[00:03:20] So another thing that we could do is we could actually create a bug. So to do that, I'm gonna make a post call. So this post call is gonna submit uh this Jason body based, which is basically going to create a new bug in our application for us. So once I send that call, then we get this response back, which is just the details of the bug that was just created in our database.

[00:03:42] Let's now make a call where we can actually get all of the box to do that. We can make a call to bug endpoint here. So I'll just do this call command. We can see when we make that call command that we then get back this message here. And this just contains all of the box in our database.

[00:04:00] So maybe I'll create a couple more bugs, make two more post calls. And if I do another get call, just get the bugs. You can see now that we're getting three different bugs coming back from our database. So again, feel free to try out the other API calls. If you want, there's calls here to do updating a bug, we can get a single bug,

[00:04:20] we can delete bugs, et cetera. So feel free to try out all of those other calls. Again, they're all documented in the Read me here, how you can make those API calls if you want to? So now that we've got our back end working, let's also see how we can get the Frontend working. So I'm going to open up the frontend folder here. So our Frontend, this is written with node using nextjs. Again, you don't need to know anything about node or nextjs in order to run the Frontend,

[00:04:43] all of the code is included here for you. I'm just going to show you how we can run it locally. And again, I'm able to run it locally because I've installed node on my machine. If you don't want to install it locally again, in a moment, I'm going to show you how to do it through Docker, but let's first see how to do it directly on our machine. So in my first terminal here, we can see that we've got the back end running.

[00:05:02] So that's good. We're just gonna leave that running that in the other terminal, get CD into backer. And then from there that CD is AB tracker Frontend. Now, from here, I'm gonna do M PM install and this is just gonna install all of the frontend dependencies that we need. So after a little bit of time,

[00:05:20] you should get a message back here just saying all the packages have been installed and then to run this locally, we can just do the M PM run DEV. And then again, you should get a message back here that says that the application is running on. We can see here localhost 3000. So I've opened up my browser here and I've gone over to localhost 3000. You can see this is our bug tracker application running locally.

[00:05:41] You can see here the three different bugs that we created earlier, their status priority, etcetera. So we can edit any of these bugs. So if we click on edit here, we might want to change the status or the priority and save the changes, we can even go into an individual bug. We get the details then of that individual bug,

[00:06:01] then add some comics here, my name, text comments, add comments from here. Again, we could edit the bug again if we wanted or we could even delete the delete the bug. We get taken back to the home screen here with the list of bugs. So again, feel free to explore this application a bit.

[00:06:20] It's quite a simple application, but it does have again, a lot of functionality in it as well. And it's gonna be a really good one for us to write some tests against and some, you know CD pipelines against as well. So again, I'm back in the terminal. What I want to do now is I'm going to show you how we can run this application through Docker. So again, if you don't install go L or node, you can actually run it through Docker.

[00:06:40] Let me just show you how to do that quickly. I'm going to stop the Frontend here to do that. I do control and see on my keyboard. I'm going to do the same in the back end as well. So both my backend and my Frontend been stopped, Make sure or we need to see the up one level. We need to make sure that we're here just in this tracker folder. So this is just basically the root folder of our project.

[00:07:00] Now, please make sure that you have Docker running. So do install Docker desktop, etcetera if you haven't already and make sure that Docker is running before you try and run the next command to run the application locally, we can do Docker compose up D we do dash dash, we're building it for the first time as well. So this might take a minute or two to run because it needs to build all of the Docker containers.

[00:07:22] But then after a mid or so we should get a message that the application, both the frontend and the backend have been started. So I can see here at the bottom that both the back end and the Frontend have both been started. And if I go back to my browser again and go to localhost 3000 here, like so we can see that our application is running again, we can now start to add

[00:07:43] new product to our system like so, so our application is now running through Docker. So again, this isn't going to be a Docker. Course, I just wanted to show you how we can actually run the application through Docker as well. If you want to stop the application from running, just go back to the terminal and we can just do Docker and hose down and that will stop both the back end and the

[00:08:01] Frontend from running and it will shut them down.