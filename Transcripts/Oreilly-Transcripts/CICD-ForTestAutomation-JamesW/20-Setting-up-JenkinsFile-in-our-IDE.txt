[00:00:00] So we've now seen how we can create pipelines in both github actions and in Jenkins. Now, what we're gonna do for the next few modules of this course is that we're just gonna focus on Jenkins. So we're gonna get our pipeline set up. We're gonna get all of our test running all of our CI CD through Jenkins first and then we'll switch over and we'll do the same in github actions. But for now, we're just gonna focus on Jenkins.

[00:00:21] So again, I've got my Jenkins running locally, gonna go over to my IDE again. And what we're gonna do is we're basically gonna add in that Jenkins file that we created previously. We're going to add that into our code and then hook that up with our Jenkins so that we can start writing our Jenkins file directly. And let's do that. Let's open up this Jenkins folder here. Let's right.

[00:00:41] Click, choose new file to keep our file, old Jenkins file. And then in that Jenkins file, I'm just gonna paste in the same pipeline that we created previously. So I'm gonna go over to the, my second pipeline, which is configure just gonna clear this pipeline script that we had from before and then in the Jenkins file,

[00:01:00] I'll just paste that in like so and then just save that file. So now I'm gonna push this file up to gear. Let's do that. I'm gonna open up the term. Let me make sure I'm in the bug tracker folder and the first thing I need to do because we actually made a change last time when we added the github actions file, we need to pull that one down first. Let's first do git

[00:01:20] origin, right? So that should pull the changes down for us. Now, we can include git status to check what we need to commit. We're going to just commit this Jenkins file. So say hi dad Gadt to add all of the files. Let's do K commit minus M, let's say adding initial Jenkins file and then let's push back up to main.

[00:01:40] So do get push minus U origin mate. OK? Let's push back up to our main branch for us. So let's see now how we can get that William Jenkins. So I'm gonna go over to Jenkins here, gonna go over to my dashboard. So let's create a new item. Let's give it a name of bo tr dock. This one's also gonna be for pipeline press. OK?

[00:02:02] And now we want to make sure that we get the pipeline script that we just pushed up to the main branch in our repository. So if I open our Buck dragger up in github. If we go into the Jenkins folder, you can see here, we just pushed up this Jenkins file here. So this is the one that we now want to pull into Jenkins. So let's see how we can do that. So in the configuration for the Jenkins pipeline,

[00:02:22] we're going to scroll down here and where it says definition instead of pipeline script, we're going to do pipeline script from SCM. This means pipeline script from source code management. So as the SGM, we're going to choose git and then we need to paste in the repository URL. And if I go back to the roots bug tracker repo here and again,

[00:02:40] you should be able to do this one for whatever your views. Uh If you click on code here, then HTTPS and then just copy this URL to the clipboard, go back over to Jenkins and paste that in, then you should be able to paste in repository URL. So again, this should be similar to, to what you have except your name here will be different. This will be whatever you whatever organization you FK to.

[00:03:01] But then it should still be the same repository, the same bug tracker do credentials, you should be able to leave as none because should just be a private repository. If we go down here for the branch is to build, it's currently trying to build off the master branch. So we want to change that to make sure that you change that to the main branch because that's what our branch is called. And if we go down a little bit more the script path,

[00:03:21] we also need to change slightly as well. So our script actually lives within this Jenkins folder and then it's called Jenkins file, but then that, that actually lives within this, within this folder here. So we need to change that because it's not actually in the root, it's actually in this Jenkins folder. So to do that, we just go over to Jenkins and just say it

[00:03:40] lives in the Jenkins folder and it's called Jenkins file. OK. Let's click on apply, click save and then click build now. So that should trigger the build for us. If we click on this circle here, we should be able to watch the build. You can look through the logs like so if we look through, you can see that it's pull that Jenkins file for our github and then it's executed the script for us.

[00:04:01] So can we have the same sort of thing where it's executed? The date is execute. Hello Jenkins, etcetera, all of the commands that we basically put into this Jenkins file. So this Jenkins file in our repository that's now being executed in Jenkins when we choose to execute the job, it's actually pulling that direct github because we push that up there previously.