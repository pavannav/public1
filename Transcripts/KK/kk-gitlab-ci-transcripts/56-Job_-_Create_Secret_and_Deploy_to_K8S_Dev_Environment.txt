Let's go ahead and do a round of integration testing.
So, within our pipeline, I would like to add a new job to do the integration testing.
So, I'll just copy-paste a job and then explain what I'm doing.
So, I added a job named Kubernetes Dev Integration Testing,
and this is going to run with the same dev deploy job.
It's also going to use the same Alpine job image as a container.
And within the scripts, I'm making use of a couple of cURL commands
in order to run the /live and the /ready endpoints.
And I'm also making use of a jq program to get the status
and also to check the status of live and ready.
So, if this is successful, it means that the application was successfully deployed
to Kubernetes and it was able to give you these responses.
But here, the first thing is you need to use curl.
So, you need to make sure that the curl and the jq programs
are available on this running machine.
So, for doing that, you can go ahead and install them within the before script.
So, let me quickly paste a before script.
So, let me just clean this up a bit.
So, in the before script, I'm just making use of the Alpine package keeper
to install both the curl and the jq programs.
So, once installed, they can be used within the script part.
And the next thing is you need the Ingress URL.
What is this URL?
So, once the application is deployed and the Ingress URL is created,
you need to have that Ingress host URL in order to do the integration testing.
So, how do we get this host URL?
So, if you go back to my local terminal and if I do
kubectl get ingress within the development namespace,
you can see that it gives you the host.
So, this host is required in order to do the integration testing.
So, how do we get the host?
So, again, you can make use of the JSONPath output
and you can only fetch the host value.
So, how do we do this within the CI job?
So, within the CI job, you need to do this within the deploy job itself.
So, let me quickly explain how to do this.
So, let me go back to the deploy job.
So, in the deploy job, once the deployment is successful,
so I'll add one more step.
In this step, I'm creating an environment variable.
So, the name of the environment variable or the key is ingress_url
and it's going to get the value from the Kubernetes cluster
by running a command kubectl get ingress from the development namespace
and it's going to get only the host by filtering the JSON path.
Once I get the host path, I'm just going to save this within a .env value
or a .env variable.
So, the .environment variable is app_ingress_url.env.
So, why am I saving this to a .environment variable?
So, I know it has a couple of advantages or benefits.
So, if you go to the documentation,
so if you have a .env variable defined within your script,
so it can be collected as a .env report
and these are nothing but, you know, it's a set of environment variables as artifacts
and these collected variables are registered as runtime created variables of the job
and this can also be used to, you know, set the dynamic environment URLs
after a job finishes.
So, we have not yet talked about environments and environment URLs.
So, we'll be talking about them in the upcoming sessions.
So, in order to, you know, use this host as a URL within the environments,
you know, we will be making use of this .env value.
So, once you have created this environment variable
and stored it within the .env,
you can also, you know, persist it using the artifacts.
So, I'm going to define an artifact and I'm just going to use a reports artifact.
And within the reports artifact, of course, we have a lot of different reports.
I'm going to use the .env
and I just need to provide the name of the .env which I want to create as an item.
Okay.
So, that is how you need to, you know, fetch the Ingress URL
from within the Kubernetes dev deploy job
and I'm just saving it as an artifact.
So, once the host is saved as an artifact,
it can be accessed in the other jobs, right?
So, it can be accessed in the K8s dev integration testing job.
All you need to do is you need to add a dependency here.
So, here I can simply add needs
and give the name of the job from which it has to, you know, get the artifacts
and once the artifacts are fetched, you know,
it will be available on the same environment variable,
which is the Ingress URL.
Okay.
So, I'm just going to echo the URL
and the same URL will be used within the curl commands
in order to, you know, make a call to the live and the ready endpoints,
get the status and, you know, just grep for the values
to see if they are available or not.
Okay.
So, this is how you configure the tests.
So, let me just quickly commit the changes to configured tests.
Okay.
So, if I go to visualize, you can see I have two jobs,
two stages and two jobs within the dev deploy
and the dev integration job is dependent on the deploy job.
So, that's about it.
So, let me quickly commit the changes and see this in action.
So, let's commit.
A new pipeline should be triggered.
Yep.
That's in running state.
So, it's going to run a total of five jobs.
Okay.
So, you can see here the dev deploy job has failed.
So, let's see why this has failed.
Okay.
So, here I think it is failing within the Ingress URL,
which we just configured in this job.
And the error is, it's a syntax error,
unterminated quoted string.
Okay.
So, I guess that I have opened the string,
but I have not closed it over here.
So, I need to close the string since I missed the quotation.
Syntax error.
So, let me quickly do the changes.
So, if I come here.
So, yes.
So, there is a double quote missing over here.
So, let me quickly add it.
Commit the changes.
A new pipeline should be triggered.
So, let me go to the pipeline.
Okay.
So, again, the same jobs will be running again.
Okay.
So, we can see that all the jobs have been successfully completed.
So, let's go to the dev deploy job.
And we are only interested in the last thing,
where we are trying to create a new environment variable,
where it's going to fetch the host URL of the Ingress URL,
and it's going to be stored as a .env value.
And we are also uploading this artifact.
So, the artifact has also been successfully uploaded.
You can see 201 created.
Okay.
So, the advantage of this .env is the same environment key can be used in another job,
which is referencing this artifact,
and it will be automatically created as an environment variable over there.
And the other advantage, we are going to look at it in the upcoming sessions,
where it can be used as the Ingress URL.
So, even that will be deemed as a benefit of the .env values.
So, let's go to the integration testing job.
And here, again, it's going to, if you see these steps,
it's going to make use of the Alpine image.
It's going to install the curl and the jq programs.
And here, you can see in the echo command,
that it was able to get the Ingress URL from the dev deploy job.
And since it got the Ingress URL,
the curl commands have successfully completed.
So, that's how you can get the host URLs,
save them as .env values,
and refer to them in the upcoming jobs to do the integration testing.
That's all for now.
Thank you.
