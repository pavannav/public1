Let's talk about how to add project batches.
So batches are a unified way to present condensed information about your projects.
So a batch consists of a small image and a URL that the image points to.
So in GitLab, batches are displayed below the project description,
and they can be displayed both at the project and at the group level.
So how do you create these batches?
So you have three types of batches, pipeline status batch, test coverage report batch,
and the latest release batch, but it also supports some custom batches.
So if you're interested, you can just go and check them out.
So there are various different statuses for pipeline batches.
And if it is a code coverage report batch, it gives you the percentage of
code covered based on the code coverage job.
You can also customize the batches by having different styles,
text, and images as well based on your requirement.
So let's see how to, you know, define or create these batches for our projects.
So for doing that, so if you see within my Solar System project repository,
I don't have any batches as of now.
So let's go to settings.
If you go to CI/CD settings, and if you go to general pipeline,
if you scroll down, you have something called as pipeline status and code coverage.
So the pipeline status and the code coverage display the status based on the last run job.
So if you go to build pipelines, the last run pipeline,
whatever the status of the pipeline that is going to be used.
And if this pipeline has a job called Code Coverage,
or if it is collecting the Code Coverage report or the percentage,
so that percentage will be picked up and displayed as a badge.
So if you're not running any code coverage step or a job,
this will not be reflected in the batch.
So it will be reflected only if you run the job and the coverage is calculated within the job.
So coming back, so you can use those badges as a markdown, HTML or ASCII doc.
And the same thing can be done for different branches.
And the code coverage, logo or the badge is going to look in this way.
And again, it has different colors based on the coverage percentage.
So if I go back, if you talk about the test coverage report batches,
they are, I think, five different colors based on the percentage of the code coverage.
And you can also modify them based on your requirements.
So once you have this endpoint, how do you set the batch?
So you can set the batch by again, going to settings.
In settings, I have to go to general.
So in general, if I scroll down, I have batches option, you can add batches.
So expand, let's click on add batch, you can give a name.
So let me just name this as pipeline status.
You need to provide a link.
So the first one is you need to provide a link to your project.
So let me just copy this or if I go back.
Or if I go back, so this is a link to my project.
And then you need to give the batch image URL.
So basically, if you see the example, this is a .svg endpoint.
So again, back in CI/CD settings, if you see there is this endpoint,
I can simply copy this batches/feature branch/coverage.svg.
So I'm not sure, I would like to use the pipeline.
So let me just copy the pipeline path, paste it.
So you can also see the batch preview at the bottom.
And you can say add batch.
So this is a project batch.
And let me add another batch.
Let me say code coverage report.
The first one is going to be the project link.
And the second one is going to be the code coverage link, the .svg file.
Let me come back here.
And the batch preview should change.
It should show coverage and the percentage whatever the job has displayed.
So we added two new project level batches.
You can also add batches at the group level.
So now, if I go back to my project,
I should see both the batches being displayed under the project information.
So any person who is coming to my project,
can have a quick understanding that the previously ran pipeline has successfully
passed and the code coverage was 86%.
So that's how the batches will be useful.
And this is how you configure them at the project level.
That's all for now.
Thank you.
