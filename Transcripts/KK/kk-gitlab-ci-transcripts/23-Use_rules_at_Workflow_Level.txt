Let's talk about pipeline schedules. So far, we have triggered the pipeline by committing messages
or by committing changes to a main branch or when we created a Merge Request. But now let's explore
the pipeline schedules, right? So it's under the build option. So let's go to pipeline schedules.
So this is nothing but it's going to schedule a pipeline to start automatically at regular
intervals like daily, weekly, monthly, or based on a current job. So the pipeline is going to run
for a specific branch or tag, and it can also have some custom CI/CD variables as well. So let's go
ahead and create one. So let me just provide a small description about this pipeline. So this
is going to be a manual pipeline trigger. So we are going to manually trigger this.
And you can also give the interval pattern. It could be, you know, every day around 9:27 PM.
It could be weekly or monthly. And if you see whenever I'm selecting these patterns,
it's, you know, also using a cron expression or cron syntax, or you can also give your own
cron syntax as well. So if you're familiar with cron, you can give your own values over here.
So let me just provide my value here. So I'm just going to, you know, execute or schedule
this pipeline to run once every year at midnight of 1st January. Okay. So if you're not familiar
with how the syntax works, you know, you can just go ahead and check out what is cron syntax and,
you know, how to schedule it. You can also select a time zone. So I really don't care
about the time zone for the time being, because I'm just doing this for a small demo part. So I'll
just, you know, select, let's say, Mumbai. Okay. And you can select for which branch you would
like to run this for. And if you remember, this project has two branches, the main branch and the
default branch. So I'll just select the main branch. And you can also give variables or files,
which can be accessible by the pipeline. So I would like to, you know, hard code the deploy variable.
So if you remember, our pipeline uses a deploy variable. So I'll just give this a manual value.
So I'll just say, I'll just say manual.
It's deployment, not development. So I'll just say manual deployment. That's it. So I'll also
say that it's activated so that it runs once, you know, every midnight on the 1st of January.
And let me just, you know, create the pipeline schedule. So once it is created, you can have
multiple pipeline schedules, and it's going to, you know, run based on the cron syntax,
which is provided. And as you can see here, it does not run any pipeline so far. And the next
run is around 11 months because, you know, today it's January, right? So that's how it's going to
run. But I don't want to wait for 11 more months. So I can manually, you know, run the pipeline.
So let's trigger this manually. So a new pipeline has been successfully scheduled to run,
I can go to the pipeline stages page. And here, if you see a new pipeline is running,
and before going to the job, I also want to show you the tag. So if you see here,
it's going to show a scheduled tag, it's saying that this pipeline was created by a schedule.
So if it is executed, or started using a merge request, you'll see the merge request tag, right?
So these tags are also useful to understand how, you know, the pipeline was, you know, created,
or you know, which triggered the pipeline. Okay, so let's go and check out the job. So we only have
one job which successfully passed. So let's go and see the variable. So if you see here,
the variable is manual deployment. So this is the variable which we have, you know, declared,
you know, within the pipeline scheduled option. So it's going to make use of the variables which
you add in the schedule, you know, during the runtime of the job. So that's how you can schedule
pipelines, run them manually, and also edit them if required. That's all for now. Thank you.
