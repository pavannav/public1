Let's go ahead and install libraries using the before script.
So, let me again add some new lines of code and explain the requirement.
So, here I defined a job and a workflow. So, you can give a workflow a unique name. So,
this is going to be the pipeline's name. So, using this pipeline, I would like to generate
some ASCII artwork. And I have a job named ASCII_job, which has a script,
and which is going to, you know, generate some ASCII artwork using the K8sgpt program.
So, Kause is a library which basically generates ASCII artwork using an animal with some message.
So, I'm using the command kause -f using a dragon image and with some, you know, message.
And whatever ASCII artwork which gets generated, I'm just, you know, copying it to a file named as
dragon.txt. In the next command, I'm just trying to check if, you know, the file exists or not
using the grep command. And then I'm just using a cat command to, you know, read the contents of
the dragon.txt file, right? So, this is what the job does. So, let me quickly, you know,
commit this to the main branch and check out the pipeline. So, let's go to the pipeline.
So, a new pipeline gets triggered. So, let's go or you can also check,
you know, the pipeline status over here. So, let's go to the API job.
It's going to make use of the default Linux small runner. And within this runner, it's going to make
use of the Ruby Docker machine. So, let me just minimize the steps and explain them. So, as usual,
it's making use of the Ruby container. It's going to, you know, just check out and clone the
repository. It's going to execute the step. And while executing the step, if you see, it is failing.
The pipeline currently failed. And the reason is, it's saying that it could not find the cowsay
command. So, within the Ruby container, there is no library which could, you know, run this cowsay
program. So, that is a reason for the failure of this job. And if you see the status of the
job, it is failed. And if the job failed, automatically, the pipeline will also be
marked as failure. If you see, the pipeline is also in fail state. So, how do we install libraries
and, you know, use them within the pipelines? So, if you go back, so within the script itself,
you can, you know, add one more line and, you know, install the required libraries.
But there is another efficient way of, you know, installing the libraries. So, what we can do is,
we can make use of something called as a before script. So, you can define or let me just copy,
paste a new set of steps. So, I just defined a before script here. So, before script, you know,
defines the script which should be running before the job script executes. So, using the before
script, I'm just adding a single command, which is going to, you know, install the cowsay library
using the gem command. Since, you know, this is going to be run on a Linux runner using a Ruby
container, I'm using a Ruby specific library, which is gem. And similar to before script,
you can also have an after script, which runs, you know, some other commands, which
will be run after the main script gets executed. Okay. So, this can be used for,
you know, cleaning or removing some content. Right. So, that's the job. Let's go ahead and
commit it. So, it's going to create another pipeline. So, if you see the pipeline got
created here itself. So, let me just go to the job directory. So, it's again going to,
you know, provision another Linux small virtual machine and it's going to repeat the same steps
again. So, let's give it a couple of seconds and see the output of this job.
This time, if you see the job succeeded successfully. So, let's quickly, you know,
check out the logs. So, it's using the Ruby container. It's, you know, getting the source
from the Git repository, and now it's executing the script. So, first, if you see here, it is
executing the gem install Kausai command from the before script. So, once the Kausai CLI is
installed, then it's executing the commands which are available in the job script. So, here it was
able to execute the Kausai command. Earlier, it was giving us an error, but now there is no error.
It created the file. It could also get up the word dragon from the dragon.txt file.
And you can also, you know, read the contents of the file. As you can see here, it's an image of
a dragon, you know, with the message which was provided in the command. And since all the
commands have been executed, it's also going to, you know, run the after script, which is going to
be executed after the job script completes. And I'm just using an echo command, which was
successfully executed. Since all the commands have been executed successfully, the job will be
marked as passed. Right. So, this is how, you know, you can make use of the before and the
after scripts in order to, you know, install or prepare the environment for the job. And once the
job script completes, you can use the after script to clean the system as well. That's all for now.
Thank you.
