In this session, we are going to work on a couple of things.
First, we are going to customize the GitLab agent configuration.
And then we will also create a staging environment and associate the agent
with the staging environment as well. So let's go ahead and check it out.
So in the last session, we did create the GitLab agent and we connected it
with the Kubernetes cluster and the connection status is connected.
But if you see here, it is using the default configuration.
So in the default configuration, the GitLab agent cannot deploy the Kubernetes manifest
files to the Kubernetes cluster. And at the same time, users cannot access the
Kubernetes dashboard within the environments because you need to enable that configuration.
So how do you enable that? So if you go to documentation,
if you talk about installing the agent for Kubernetes, we completed a couple of steps.
We completed the prerequisites and the installation steps. So now the agent is installed.
The next step is creating an agent configuration file.
So where do you create this agent config file and how do you create it?
So this file has to be created in order to manage different stuff. So you can manage
data or steps related to GitOps workflow. It can be used for managing GitLab CI/CD workflows.
And if you also want to allow some specific projects or group members to access Kubernetes,
all these things can be done using this config file. And the config file should be created in
the default branch and it should be at this specific path. So in the default branch,
it should be at .gitlab/agents. You need to give the name of the agent which we created
/the config file. So let's go ahead and create this and then we'll try to configure it.
So if you remember, it's saying that it should be in the default branch.
So how do you know which is your default branch? So if I go back and if I go to my code,
there is a branches option. Let's go to the branches option. I have two branches and you can
see that my main branch is the default branch. So let us go to the main branch.
So here you need to create a config file. So I'll just create one here. So let's just say add,
let me add a new file because we're only working with a single file. So let me copy the path,
the entire path, but we need to replace the agentK name. So let me get the agentK name from here.
Okay. So it's at the root. I'm just creating .gitlab/agents/KK GitLab agent
/config.yaml. So what needs to be given here? So let's go and check out the documentation.
So in the documentation, again, if you want, you can configure it. So let's click on configure.
So you can do a couple of different configurations. One is for the GitOps workflow,
if you want to work with FluxCD and automate the deployments. The other one is for the GitLab CI/CD
workflows, right? So you can authorize the agent to access your projects in order to use the agent
for the kubectl commands. So let us click on authorize the agent to access your project.
It's going to take you to a different documentation where it talks about why the authorization is
required. So let's assume, you know, you have multiple GitLab projects and you need to authorize
the agent to access the projects where you keep your Kubernetes manifest files. So what does this
mean? So this means that a single agent can be used for, you know, deploying multiple Kubernetes
manifest files across different projects. You don't need to have multiple agents. A single
agent can do that, but before it can have access to that project, you need to authorize the agent.
So for that authorization, you can follow the steps given here. So again, it should be done
in the config.yaml file since we already created the file. So we have to use, you know, the CI
_access keyword. So this CI_access keyword is going to, you know, take
projects as ID. You need to give the project ID. So this is nothing but the ID to your project
group name slash the project name. So after you give this, it's going to authorize the project.
I mean, it's going to connect with the project and whatever manifest files which are available
at the project can be, you know, deployed using the kubectl, you know, CLI. So once this is
configured, all the jobs have access to the kubeconfig file, which can be used by the kubectl
commands for interacting with your Kubernetes cluster. So let's copy this, go to the file,
and here, what is the path? So all my Kubernetes manifest files are in the same project. So I only
want to, you know, authorize this project. So it needs to be a group slash the project. So here,
I'll just copy the group and the project which I have in the URL. It is, you know, demos-hyphen
group slash the project name is solar-hyphen system. Okay, so this is how you need to give
the CI access. So I mean, this alone is sufficient for your GitLab CI pipelines or the jobs to,
you know, make use of the kubecontext and deploy to the Kubernetes cluster.
But we also want to provide the Kubernetes dashboard access. So we have something called
the Kubernetes dashboard. So let me show you quickly, Kubernetes. Right, so we have a dashboard
for Kubernetes where, you know, it's going to show you the status of your cluster, like, you know,
how many pods are running, how many deployments, how many services are running. So how do I access
this Kubernetes dashboard? So again, this can be accessed by providing the user access keyword. So
if I click on this, it's going to take you to a different page where you can configure the
Kubernetes access. So again, within the config.yaml file. So this is config.yaml file,
right. So yeah, the agent config.yaml file, you have to provide the user access. And it could
be for the project level, or you can also give the access at the group level as well. And talking
about the access, you can simply use access with an empty object for, you know, giving plain access.
So let me just copy this configuration. So if you see this should be within the config YAML itself.
So let me go back. And here, again, the project name should be the same thing, the group ID
slash the project ID. Okay, so this is how I will be providing both user access and the CI
access for the GitLab agent. So this is a customized, you know, configuration. Let's say
configured agent config file. Okay, so let's commit the changes to the main branch.
Okay, so now the file is added. So if I go to the repository back,
right, so if you see a new directory has been created, which has the config.yaml file.
So now let's go to the Kubernetes cluster. So let me open the GitLab agent.
Okay, I'm sorry, this is already connected, right. So let us go back and see the configuration. So
earlier it was using the default configuration. But now, since we define the config.yaml at
the default branch, you can see that, you know, it is referring to this configuration file. Okay,
so once it is referring to this config file, all the jobs within the CI pipeline will be able to
have access to the agent's kube-context file. And it can be used by the kubectl CLI to
deploy the applications or the manifest files. So if I go back to my cluster,
and if I try to get the context, so here you still see you only have one context, which is the,
you know, VK context, but the jobs within the CI pipeline will be having access to the GitLab agent
context as well. Okay, so we will see that in the next session. But before that,
let us also quickly explore the user access. So for the user access, you need to see the
Kubernetes dashboard. So where can you see that you need to go to environments, and you can edit
an existing environment or let me create a new staging environment. So I'm creating a new staging
environment. And here, earlier, we were not able to see the GitLab agent. The reason is
it was not authorized. But now since I've given a customized config file with user access,
so this environment can be associated with the GitLab agent. So once you add the agent,
so the agent will be able to provide access to your Kubernetes clusters API endpoint. So you
can select, you know, which endpoint, you know, you want to, you know, check out, I think I'll
just give access to all the namespaces. So let me just check it out. Okay, so I guess,
selecting a namespace is optional, as you can see here. But if you want to associate this with the
staging environment, you can do that. But I just want to keep it optional. And let me save this.
So when you save this, the environment is created, it does not have any deployments,
we will deploy it in the next session. But let me go back to the environments page
and show you the dashboard. If I expand development, I don't see any dashboard. But
if I expand staging, so there are no deployments, but it has access to the Kubernetes overview. So
what is this overview? So this is basically a dashboard, which is, you know, going to show you
like to which agent this is connected to, is it connected or not. And at the same time, it shows
you all the services which are, you know, a part of your Kubernetes cluster. If you select any
specific namespace in the previous page, it would only show you the details of that specific
namespace. But since I did not select any namespace, it's, you know, simply going to show
you, you know, the services from all the different namespaces. Okay, so I think these are the
different services available in my cluster. And it's also going to show you like how many pods
are running on the cluster. So a total of 16 pods are running. So apart from this, if I want to
see any other information, so let's go to the Kubernetes dashboard. So let me just go back to
the dashboard for Kubernetes. So it should also show you the deployments and the replica sets,
but I'm not sure why it's not showing. So let me see how to get that access.
The environment already exists. Okay. Okay, I think you need to select a namespace to see the
deployment and the replica set details as well. So when you click on the dashboard,
it's going to show you. Okay, so let me try to configure that. So let me go back
to my environments. So I would like to go to the development environment. I would like to edit it
and, you know, select the development namespace. So let's see if it's going to make any change. So
let me click on edit in the development environment, select the agent, and I'll select
the development namespace. So in this namespace, of course, we don't have any Flux, you know,
CD component installed. So we are not covering Flux in this training. If you have, you can select
it, but let me just click on save. So let me go back to the environments. So now both the
environments are configured with the agent. So both the environments should have the Kubernetes
overview option. And here you can see that even in this agent or the overview, it's just showing me
the services and it's showing me like how many pods are running in the development namespace.
Okay. So there should be a configuration, which should also show you like, you know,
the summary apart from the services. So let me see. It's only showing me the services. It's not
showing me the summary part for the time being, but I guess it's not available for the free
tier. It might be available in the ultimate or the premium tier. But I hope you got an idea,
like how to configure the agent so that it can be accessed, you know, at the environment level
and also how it can be accessed at the CI level. Okay. So before we close this session,
let's do one more thing. So since we created the staging environment, let's also go ahead
and create a few variables for the staging environment. So if I go to settings CI/CD
variables, so we'll create a couple of variables for the staging environment. So, so far, you know,
we have just created variables associated with all the environments, but I want to change that.
So now I will be deploying the application to the staging environment in the next session.
So I again need another set of namespace and replicas. So how do we do that? So for doing that,
I can click on add variables. I'll say variables, type is variables, but for the environments,
I'll select the staging environment. And whatever variable which I create now will only be accessible
for the staging environment deployments. So I'll just say namespace and the namespace
would be staging. Let me add the variable. So a new variable is added. This is of environment
staging. And let me add another variable within the staging environment. And this is going to be
replicas. Okay. And for the staging environment, I would like to have four replicas of the pods.
So I'll just add four. Okay. So this is how you can associate environments with specific,
I mean, variables with specific environments. And while you run the jobs, if a job is assigned
to a specific environment, it's going to make use of these specific, you know, variables.
So that's all for now. Thank you.
