In this session, we are going to understand the NodeJS application running it in the
local machine before we start building it in the GitLab Actions workflow.
The source code of the application is hosted on GitLab.
So let me copy this URL. It also contains some details about how to run this application if you
are, you know, running this for the first time. So it has all the commands also defined over here.
So let me open Visual Studio Code on my local machine. And let me quickly clone it.
Okay, I need to go back a directory. And let me clone this.
All right. So this is the directory. Let me zoom this a bit or zoom out. Okay.
So it has various different files over here. So since this is Node.js based application,
we have the package.json file, which contains all the details with respect to the application,
the application name, version, author details, the scripts for starting the application,
testing the application, as well as coverage. And for coverage, we can also, we have also
mentioned that we have coverage of 90 lines. And we have also made use of fewer dependencies,
like the Express dependency for creating REST APIs. For testing purposes, we are making use of
Mocha JUnit Reporter. We are using Mongoose for connecting to MongoDB. And finally, we are using
NYC Istanbul for coverage reports. And apart from this, we have the application itself, app.js,
which contains the backend business logic, connecting to the MongoDB using the Mongoose
connector. So here you can see that it expects three variables or parameters, which are the
MongoDB URI, username, and password. So we will be passing this using the variables and secrets
within the GitHub Actions. And it also has the endpoints. Okay, so this is the backend code.
And the test cases for this code are part of app.test.js. So you shouldn't be, you know,
knowing about this, but I'm just giving you an overview about what are the files we have and,
what do they contain? So here we have all the test cases. So we are using Chai and Chai HTTP
for writing the test cases. And this application also has a frontend. So for frontend, I have a
simple index.html page. So all the HTML code goes into here. And then, so I also have a JavaScript
file, you know, pulls data and, you know, displays the data in the HTML page. So this controller has
the JavaScript logic over here. And apart from that, I think we will be building Docker images.
So this is the Dockerfile. So we'll be talking about Dockerfile in a later session. And
similarly, we'll be deploying this to Kubernetes. So we also have a folder which contains the
Kubernetes, you know, manifest files. So, you know, these are the major files which we have
over here. So let us quickly start by installing all the dependencies. So let's go to the folder.
I can do a simple npm install. So it is going to install all the dependencies, which are part of
package.json. And it's going to create a new folder called as node_modules. Okay. So once
you run the dependencies, the next step is you can do the testing part. So for testing, since we
have already defined a script, so the only command which we need to give is npm test. But this is
going to fail, right? So it's going to fail, it's going to give you an error message. And the message
is that, you know, we are not passing the MongoDB variables, right? So since we are not passing them,
you know, it could not connect to the MongoDB, right? So here, in app.js, you need to pass these
values. Okay. So when you are working with workflows, we will be passing them as part of
variables and secrets. But for now, just for the demo purpose, you know, I will be simply hard
coding, you know, these variables, okay, and let me save this. So after you hard code them, if I run
the same npm test command again, so it's going to successfully connect to the database, start the
server and run all the test cases. So it won't display anything over here, you can use a simple
echo $? and it should give you zero as the exit code, which means that all the test cases were
successfully passed. And this command also, you know, gives you test_results.xml file. So this
contains all the test case results, right? So we will be, you know, uploading this and using this
in the workflow. Okay. So this is the testing part. And then after testing, you can also do
npm run coverage. So let me say npm run coverage. So it gives you the coverage report.
So again, it connects to the database, it starts a server, you know, it runs all the test cases,
and it's going to, you know, say that all the test cases are passing. And the coverage is currently
88.88%. But in the package.json file, we mentioned the threshold as 90%. This coverage is failing
for the time being, you can see this is an error message, right? So you can also see that
this is an error message, right? So you can also check the echo $? output, it's going to give you
one as the exit code, it means that the previous command failed. Okay. So currently, the coverage
is failing. And it is intended because we are going to cover, you know, a few scenarios in
GitHub Actions based on this output. Okay. And once both of these are done, you can start the
server basically, right. So I can use a simple npm start. And before that, the coverage also
generates reports, it creates a folder called as coverage. Within the coverage folder, it creates
two different types of reports. One is the Cobertura coverage.xml. The other one is the
JSON summary. And the third one is the LCOV reports. Okay, so these are the different reports
which are generated by the coverage test. Okay, finally, we can do npm start. So what it is going
to do is, it is going to start the server on the local machine and listen on port 3000.
So I can go to a browser, I can do localhost 3000. And I should see the UI. So this is the front end,
which is, you know, designed or which is developed in the application. So it is a simple
solar system-based application, where it gives you the details about each and every planet.
Okay. And it also gives you the host name, like where the application is running. So currently,
the application is running on my local machine, the name of my local machine is Silent Shadow.
So that is the reason you see the port name as Silent Shadow. But once you deploy this to
Kubernetes, it is going to give you the name of the port, which is hosting this application,
which is running the application. You can also search different planets by just giving the
number of the planet. So if I give three, it is going to give you the details of Earth.
And if you give six, it is just going to give you the details of Saturn. So these details,
these images are stored in an external cloud-based MongoDB database. So it is, you know,
making a call to the database, fetching the data and displaying it in the UI. Okay,
so this is the application, which we will be creating a workflow in the GitHub Actions.
So that is all for now. Thank you.
