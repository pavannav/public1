Let's have a look at predefined CI/CD variables.
Predefined CI/CD variables have completely separate documentation which you can go through
for different examples. So these are the variables which are automatically provided by GitLab in
every CI/CD pipeline. They contain information about the job, pipeline, and other useful values
relevant to your builds and deployments. So this allows you to write flexible and dynamic scripts
without hardcoding any sensitive information directly in your GitLab-CI YAML file.
And talking about the availability of these variables. So some variables are available
as soon as GitLab creates the pipeline, allowing you to use them in the pipeline configuration
or in any job scripts. And some other variables become available only when a runner executes the
job, meaning that they can only be used within the job script. Apart from this, there are also
different variables which are only accessible during a merge request or during an external
pull request. So we'll be exploring them in one of our examples as we move forward.
So you have like I think hundreds of variables available. So you can quickly go through this
and understand how they work. Apart from that, if you want to quickly see how the variables can be
accessed within your job, you can also use something called an export command, which is
going to list all the variables available to the script using the export command.
So let's try to create a new project and export all the available predefined variables.
And also let's see the most widely used variables within the jobs. So let me go back
to my workflow. And in our previous example, we used one predefined variable, which you know,
gives you the Pipeline ID. So now let me create a new project. I would like to work in a new project.
So I'll just create a new project. Let me create a blank project. I'll name this as
predefined variables. That would be the project slug as well. I would like this to be within the
demos group. And I want the visibility to be public. Initialize with a README file.
I guess I'm good with the other stuff. So let's create the project.
So within the project, of course, you will see only a simple README file.
So let's go ahead and create or set up the CI/CD. So let's configure a pipeline. Of course,
I don't want to use the default pipeline. So I'll just remove everything, paste my own code,
and explain what I have just copy-pasted. So I have added a workflow name for this pipeline.
And the name is exploring predefined variables pipeline. And I have three jobs. So the first
job is export variable job. And it only does the export. So this is the command which we just
saw in one of the documentation. So this export command just lists all the variables available
to the script. So let's see what variables will be a code in our pipeline. And then,
if you want to access predefined variables using the key ID, you can also use dollar
and the name of the predefined variable. So here, I just created another job named
generic predefined variables. And within the script part, I'm just echoing, you know,
some predefined variables like the user login, the user email, the author who committed the pipeline,
the commit branch, project name, project URL, the job stage. So by default, if you don't give
any stage name, it's going to be in the default test stage, the pipeline name. So this will only
be available if you define the workflow name at the top. And I'm also just echoing the pipeline ID
and the source. So that's about the second job. And in the third job, I'm trying to access a few
merge request, you know, predefined variables. Okay, so let's commit this. So if you see the
third job, I'm, you know, trying to access everything with respect to merge request labels,
merge request, target branch, assignees, source branch and title. So we have these three jobs.
So let me, you know, quickly push this to the main branch and see, you know, what would
be echoed back. So I'll just say, added predefined variable jobs and commit the changes.
So new pipeline should be triggered. Let's go to the pipelines.
Yep, so this is a fresh project. So you will only see that we have one pipeline in the running stage.
And it has three jobs, all the three jobs will be running in parallel, because I've not defined
any stages or any needs over here. So I'll just pause the video till the job gets completed.
Okay, so as you can see here, all the three jobs are successfully completed.
Let's go to the first job. So the first job, if you remember, we were just trying to use
the export command. And if you see it exported a lot of variables, to be honest, so I think it's
more than 100 variables were exported. So let's go up. Right. So in the first job, it's using the
export command. And if you see, it exported, I think around 130 variables, if you count the lines
or the line numbers. So it's exporting out some CI, API Graph URL, some build directory,
the commit author. So I was the person who committed it. So it's giving me the name
and the email ID of the author, the before share, the branch on which I committed,
the commit message, whatever commit message I've added. So even that has been logged,
the ref name. Right. So this is about the repository. And if I scroll down, it also
logged a few things about the dependency proxies and the users. I think this is related to the
proxy server, which GitLab is using. And then it has some logs with respect to the job,
which I'm running. So it has details about the job ID, the job name, the job stage,
when it was started, the status, timeout, and few details. And then it also had details about
the pipeline. Right. So the pipeline ID, the namespace, namespace ID, project ID.
It logged a few things about the CI registry. And it also logged about the runner, right,
the runner, which is running this job currently, some server details, and a few other details as
well. Okay. So it also logged about the GitLab user details as well. So that's what is logged
in the first job. So let's go to the second job, which is we used some specific predefined variables.
So let's see what is logged here. So I was trying to log the GitLab user login,
the person who is logged in currently, the user email, the commit author, commit branch,
the project name, the project URL, the job stage, and a few pipeline details. And if you see,
all the details have been, you know, just printed out, right. So I'm just printing them
for demo purposes, but you can use them, you know, within your pipeline as required.
So that's about the second job. So let's go to the final job, which is the merge request.
predefined variables. And here, the job is successful. But if you see, I don't see the
values, I can see the keys, but I don't see the values. The reason why you're not able to see the
values is that these variables are only available when you're working with a merge request. So
we'll talk about the merge request in the next session. But generally speaking, a merge request
is equivalent to a pull request within GitHub or any other, you know, Git-based products, right.
So since I was just committing my changes directly to the main branch, there was no merging or no
pull request in the scenario. So these labels are not printed because they are not accessible
for the general committing. So these are accessible during the merge request. So if
you go back to the documentation and the predefined CI/CD variables, so you have something
called as predefined variables for merge request pipelines. So if you see the documentation,
it says that these variables are available when the merge request is open or for the pipelines,
which are merge request pipelines, right. So I'll just stop the video here. In the next session,
we'll briefly talk about what a merge request and then we'll make the, you know, appropriate
changes and see how to access these specific variables. That's all for now. Thank you.
