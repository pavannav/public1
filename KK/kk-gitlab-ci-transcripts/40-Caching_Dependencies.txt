Let's go ahead and see how to invalidate the cache which we created in the last session.
So when we created the cache, the cache is currently dependent on the package-lock.json
SHA. So this is a SHA which was used in the earlier cache. So now let's try to update
our packages and see how it's going to affect, you know, the cache within the runners. So let me go
back to my local where I have the dependencies or where I have my repository cloned over here.
So in this, I'm just going to quickly go ahead and install a new dependency. So if you see here,
I don't have a dependency called NodeMon. So let me go to cd solar system. So here I'm
going to use a command npm install NodeMon. So I mean, this is used for, you know, automatically
redeploying the application on the local machine whenever you make changes to your code. So I'm
just going to install this for the demo purpose. It's really not used in the application business
logic. So let me quickly install this. It's going to take some time and install it on my
local machine. Okay. So it did install it on my local machine, took around nine seconds for it to
install it. And if you see, I have two changes. So one change is in the package.json. You can see
that there is a green line. It's saying that a new dependency has been added. And it also updated
the package-lock.json. If you see the symbol M, right? So something has been modified here.
So let's not go into it. But yes, things have been modified in these two files. So let's commit
them and see how the pipeline is going to be executed. So if you see here, two changes will
be committed back to the repository, which is, I think, okay, should be doing this in the feature
branch. So I think this is currently in the main branch. So let me do these changes in the feature
branch. Okay. So let me quickly go back. I'll do a quick git checkout. So this is in the main branch.
I want to make the changes in the feature branch. So I'll just say, git checkout. Okay. So this is
in the main branch. So I'll just do a git checkout to the feature branch. So let me get the name of
the feature branch. So this is, okay. So currently I'm in the feature branch and in the feature
branch, I do have the node_modules. So I think I'll just make this change in the feature branch
itself. Okay. So let me just say, added new dependencies. Okay. And I'll just commit and push
to the feature branch. Okay. So let me quickly check this out. If I go back to my
pipeline, so I'll just open a new pipeline. So, okay. It's not yet triggered. So let's give it a minute.
Okay. Let me see if the changes are over here.
This is the main branch. Let me go to the feature branch. Okay. So I see the changes
to the feature branch. So these are added, but I don't see any pipeline getting triggered for it.
Yeah. The pipeline has triggered now. It took some time, but it triggered now. So let's
go to this pipeline. So let's go to the unit testing job. Okay. So as you can see,
the job has successfully passed. So now let's see from the caching perspective.
So as you can see here, currently it's saying that the cache does not exist. And if you see the key,
it is node_modules-hyphen, there is a new sha value. The reason of the new sha value is because
we made some changes to the package-lock.json. So it's going to generate a new sha value.
And the existing cache was using a different key. So since these are two different keys,
it won't be able to use the new SHA value. So it's going to generate a new SHA value.
It won't be able to use the existing cache. And this is how you can invalidate the cache.
So hence the cache was not extracted. In the next step, in the npm install step,
it has to again download the packages. So any changes you have made within the dependencies,
it's going to either download the new dependencies and it takes another seven seconds.
And now once the dependencies are downloaded, once the job gets executed successfully,
it's again going to create a new cache with this new key. And it's going to upload the
new dependencies as well. As you can see here, it has found around 6,000 artifacts currently.
So earlier, if you remember, it was around 5000 artifacts. So if I go to the earlier pipeline,
it had around 5700 artifacts. But now since we added a new dependency, the number increased.
So it's going to cache the new dependencies which it finds in the node_modules directory.
Right. So this is how you can make use of caching. So you can cache it, you can invalidate it by
making changes to the files. And you can also manually delete the cache as well. As I've
shown you in the previous session, you can go to the pipelines, you can just click on clear,
you know, runner caches, and it's going to, you know, successfully clear the cache
for all the runners for this specific repository or this specific application.
So that's about caching. Thank you.
