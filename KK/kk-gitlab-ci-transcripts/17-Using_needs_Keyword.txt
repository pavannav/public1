Let's have a quick look at how we can use variables at different levels within the pipeline.
So within our existing pipeline, if you see at the jobs of the Docker build, testing and the push,
you see that we are making use of the same values in different jobs. And if you, you know,
use multiple jobs for the same settings, it would be better if you define them using variables and
reuse them in multiple jobs, right? So how can you define variables within the pipeline?
So if you go back to the syntax reference, and if you go to variables at the bottom,
so variables can be defined at the global and at the job level as well. And if you define variables
at both levels, so the job level variable takes higher precedence. And if you want to
understand how variables are defined, so in this example, this variable is defined at the global
level. So you need to use the keyword variables, and you can define your variable key and the value.
And in order to refer to this variable within the job, so this global variable will be available
to all the jobs within the pipeline. So you can refer to the variable using dollar and the key of
the variable. So that is how you can define and use variables. So now let's modify our Docker
build jobs to make use of variables at the job level. So I'll just copy-paste a few commands
instead of typing and spending a lot of time on it. So just to save some time, I just copy-pasted,
you know, the same jobs, but with variables. So if you see here, I created a job-level variable,
I'm using four variables: username, registry, image, and version. And in order to use these
variables within the script, I'm just using the dollar, you know, syntax, the dollar syntax,
and the name of the key. So here I created a username variable. And I'm also referring to that
variable within another variable within the registry variable. So even that level of dependencies can
be used at the variables also. Okay, so this same set of variables is also used in the testing job
as well. And the same set of variables is also used at the Docker push job. But one additional
variable at the push job is the password, which is required for, you know, logging into the Docker
hub. Right. So again, using variables, we are repeating the same variables across multiple jobs.
So is there a better way to reuse variables across multiple jobs? Yes, we can define
commonly used variables at the global level as well. So what we can do here is, so let me just
copy up to the version. So I can go to the top. So let me just minimize the other job.
So at the global level, I'll just define the variables and, you know, use these variables.
And how do I refer to them? Right. So these are automatically referred to in all the jobs. So
wherever you have the same values, so you can, you know, simply remove them. So I can remove
the variables from the Docker build job. I can remove these variables from the Docker testing job.
And I can also remove them from the Docker push job. But the push job still requires a password
only for this specific job. So I'll just, you know, keep the password available over here.
So just to show you that demo, so what I'll do is I'll just add another echo command
at the, you know, deploy job and I'll try to refer to the username and the password values.
So let me just use the username. So I can refer to it using dollar username and let's say password.
So I can refer to it using password. Okay. So this is how you can define the variables.
So let me just quickly check all of these fields. Okay. So I guess everything is fine
so far. So let's go ahead and commit this and, you know, check it out. But before committing,
I also want to make one small change. So instead of using a hard-coded value for the version,
I would like to use a different type of variable. So we have something called as
predefined CI/CD variables, which can be used to dynamically use some GitLab values.
So here, I just want to use a predefined variable, such as CI underscore
pipeline ID. So it's going to use the CI pipeline ID automatically as the version. Okay. So I'll
just add a commit message. And if you see the pipeline syntax is correct. So let's quickly
commit the changes. Let's go to a pipeline. So we'll talk more about the predefined variables
at a later session. So I'm just using this, you know, in this demo. So let's go to the pipeline.
Oh, this is going to run all the jobs. So I'm just going to, you know, pause the video
till all the jobs get completed successfully. As you can see here, all the jobs have been
successfully completed. So let's go and check out how the variables were used in the Docker jobs.
So since we are making use of the same variables across all the, you know, three Docker jobs. So
let me just go to the last job. So Docker push job. So here, again, we'll go through the logs
in a minute. So if you see here, it is making use of the variable. So if you see the command,
so we're using echo docker login. So we're using $username and $password. So this $username
was defined at the global level in the pipeline editor or in the GitLab CI file. And if you
see the value is replaced here while executing it, and even the password is also, you know,
replaced. But one bad thing about the password is it is exposed in plain text. And also the password
is again, you know, committed in plain text to the repository, which is not at all recommended
or best practice, right? So we'll, we'll try to mask the password and see how it can be masked
in the next session. But for the time being, you know, it is in plain text. Apart from that,
if you see the Docker push command, we are using the $version. And for the version, if you see,
it is making use of a numeric value. So what is this numeric value? So if you go back to the
editor, and if you see for the version, we made use of a predefined CI variable. So we'll talk
about predefined variables in the later session. So what is this, you know, predefined CI pipeline
ID. So this is the ID of the pipeline, which was, you know, generated and executed.
So this is the same ID of the pipeline which executed this job, right? So this is how you can
refer images based on the pipeline ID as well. Okay, so that's about the password. So let me go
to the last job. If you remember, we also have a deploy job, where I was trying to, you know,
access the username and the password. So if you see, I'm using an echo username to get the username
variable and the password value from the password variable. So here, it was able to access the
username because this was defined at the global level. But the password field is empty or null,
because it was only defined at the Docker push job level. So other jobs cannot access the password
variable because it's not defined at the deploy job. So if you go back to the editor, so at the
global level, we only have the username, we don't have the password. So the password is only
accessible at the Docker push. So no other job can access the password variable. And again, the
password is hardcoded here. So in the next session, we'll see how you can, you know, securely store
and mask the passwords in GitLab CI/CD. That's all for now. Thank you.
