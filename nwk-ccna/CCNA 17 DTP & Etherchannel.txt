ript


Okay. So guys, uh we are going to learn this topic today. DTP. Uh DTP.
Mic voice is clear. Everything is okay. Camera is on. Recording is on. Yeah, we
will learn DTP also. We are going to learn uh uh what we call ether channel.
Okay. So, ether channel is also the important topic for today. Yeah. So what is DTP? So DTP is a dynamic trunking
protocol. Okay, this protocol is called dynamic trunking protocol.
So what happens like in Cisco if you make one side trunk automatically other
side it will automatically become trunk port and how the other side will automatically become trunk. So let's
open the packet tracer and by the way I hope you remember what
is trunk port. So trunk is a port which participate in multiple vans where you
can forward ven 2 information ven 3 v 5. Okay. So if you have 10 vans you can
pass all vain information from from trunk port. The trunk port is like your
u like a bridge okay where you can forward all the traffic. So access port
is like part of one ven. So if you remember yesterday practical. So all the
computers which connect with the switches they are called access switches. Okay. So if I show you
yesterday practical. So this is the practical we have done yesterday. Okay.
And I asked everyone to do that. Okay. But what do you see here? So yesterday
like we created two departments IT and admin. And after that like once we added
all them in the VLANs then after that we configured SVI switch virtual interface
on multi-layer switch. Okay. So please complete this lab this week. Okay. So what is access port which is part of one
ven only and what is trunk port which is part of multiple vans.
Okay. So which is part of multiple vain access is which is part of one ven
okay so like all these computers okay all these computers are part of one ven
only so that's why this is called access port and switches to switches
between switches we will configure the trunk port which is part of multiple vans okay so now when it comes to like
if you configure one side okay so let's Take an example.
I will add the two switches.
Okay. You can take layer two, layer three. That's DTP. DTP works on both.
Layer two also, layer three also. Okay. So now you can see this is like we
connected the cable between two devices. And if I go to switch the first switch,
let's give the name here switch zero.
Packet tracer should not crash because whenever this circle comes now so
at mostly that time packet tracer file gets crashed.
You can see application not responding. Okay, I have to close the packet tracer.
I have to open the again.
So these small bugs you get in packet tracer and
all because these are
okay. So let me open the old version.
Packet tracer is dancing. M okay so DTP is basically if you
configure one side automatically other side will become trunk okay I'll show you but this topic you will easily
understand if you understand this table okay so what does it mean that if one
side is trunk port other side is also trunk it will form trunk port it's
simple right okay so let me log in fast Please.
Why packet tracer is creating issue? Yeah. So you can see we have four types
of port. Dynamic, auto, dynamic, desirable, trunk and access. Okay. Trunk
and access you already know but still like if you configure one side trunk and
other side is also trunk. You can see this table from top to bottom and then left to right. So you can see if one
side is trunk the other side is also trunk it is going to form what
okay so let me add switches now so I added two switches
okay and let's give the name switch S1 S2
and let's add a cable
So on fa01 I type a command show interface f01
switch port. Okay. So what do you see here? This
command is showing you that by default I am in which mode dynamic auto. Okay. And
I'm behaving like what access port. Okay. So by default if you see Cisco
switch by default their admin mode is dynamic auto remember dynamic auto and
if I go to another switch which is switch 2. Okay so just change the name host name S2 and check the port number
inter F01 show interface FA01 switch port. So you what we can see here admin
mode is also dynamic auto and this said also dynamic auto. So they both are
behaving like which port? Access port. Because if both side are dynamic auto
they will behave like access. Access means they are part of any van. Okay. So
you can see they are already by default they are member of van one. This is not a trunk port. You can check show
interface trunk. So there is no trunk port because this port is not trunk port. This is access. So right now this
port is in van one. Okay. So now uh if you add two computers, okay, like uh one
computer this side, one this side, okay, so if you connect a cable,
okay, and if you add the IP address like uh uh
192 168.1 and this side is 1.2. So will they ping?
What do you think? Yes, they will ping. I'll show you. So
here we have given 1.1 on another computer we have given 1.2
okay and if I go to the first computer go and ping the second computer
okay so ping okay I'm able to ping because right now what is happening
okay what is happening the packet is going from this
Yeah. So packet is going from this computer to switch. So by default switches the like this is not a trunk
port or this is by default in which wheel and v wheel v one and you are forwarding which vand traffic this all
the ports are in v one then only this is the reason you are able to communicate
to be s computers v one or switch connection be v one. Okay, computer to
switch and switch to switch and switch to computer. These all ports are in v one and this port is behaving like
access. So there is no problem. Okay. So, so what is dynamic auto? So dynamic
auto means if I configure this side access this will become also access. If
I configure trunk this side, this will also become trunk. Okay. So let me go
here and say config t interface f01.
I say switch port mode trunk. Okay. So you can see I have given the switch port
mode trunk. So what I did here if you check properties. So I made admin mode
trunk also operational mode also trunk. So if I configure this side trunk port.
Okay. If I configure admin mode trunk operational mode trunk. So this side
what we have dynamic auto. So dynamic auto will not change because we change
from this side to change. Okay. We changed on S1 and this switch two is a
dynamic auto. Let's see what will happen this side now. So now you can see the port is dynamic
auto still but operational mode changed to trunk port.
Can you see that? Yes. If by default they both are in dynamic auto. So when they were both in
dynamic auto they were behaving like access port. Right? You can see access.
Okay. But when I configure this side trunk port. So trunk once I configure
the trunk port this side the dynamic auto is not changed but the operational
mode got change to trunk port because operational means what is operational
mode that right now the port is behaving like trunk port because one side you configure trunk. So once you configure
one side trunk the other side is dynamic autoto and dynamic autoto will automatically change the port to the
trunk port. So do not see the admin mode focus on the operational mode that how
the port is working right now. So operational mode means right now they are working like trunk port. Earlier
both were dynamic auto. At that time they were working like excess.
Okay. So now I'll go to another mode. Okay. So how do you
remember all these ports? Just by doing some practice. Okay. So now the third
mode I'll show you access. If I go to if I go to the switch one again and
configure f01 switchport mode access. Okay. So right now you can see the
command is switchport mode access. Okay. So there is some blocking or
something is happening. So just to do shutdown and no shutdown if it will show you some uh this inconsistent or all
switch port mode access switch port access
v one by default it's in v one now
still it is giving me some errors. Okay, let me remove cable and connect
again.
So sometimes if error is not stopping like uh you can uh like disconnect cable
or maybe you can try to add in another port.
Okay. So let me add in f03. Okay. But this time again this is a new port and
new port is always in which which uh admin mode. So you can see this
is a new side. Again it's dynamic auto this side and the other side is also dynamic auto because it's a new port
right? I just changed the port because that was giving some issue bug or
something. Okay. Packet tracer has a little few bugs. Okay. So do not think
uh there is a command issue or something. Sometimes packet tracer behave uh even if you have done
everything correctly still it will not work then try to restart the device. Try
to close packet tracer and try again. Okay. So these are the basic things. Okay. So now in the switch this side I
will configure f03 switch port mode access.
Okay. So once I configure access what will happen this side? So my admin mode is also access. Operational mode is also
access. So why operational mode is access.
Okay. Why admin mode I change to access. But why operational mode is access?
Because you when you configure access the other side is dynamic auto. Automatically it will also form what?
Access. So dynamic mode auto means whatever is opposite side I will convert
the port into that situation. Okay. If other side is trunk I will become trunk.
If other side is excess I will become what? Access.
Okay. So that's the story and if you configure one side one side trunk one side access it will
not work. So to understand this is the table. So if just focus on this table
which is uh and it's easy guys I remember this topic
I learned in 2011 2009 uh 10 or 11 approx. So in my college days okay so
dynamic so you can see one side is trunk
yeah they go so trunk if one side is trunk the other side is trunk then it
will become what trunk port this is how you check this table okay so now let's take another case if one side is excess
okay one side excess one side excess the other side is dynamic dynamic auto then
it will become what? Excess. Okay. So one is a dynamic auto other is
also dynamic auto. So what we will become excess that we already know right? So dynamic auto dynamic auto is
becoming excess. Dynamic auto plus trunk. If one side is dynamic auto one
side is trunk it is becoming trunk. Okay. So one side is excess and other
side is trunk. So you can see limited connectivity. So you have to remember you have to understand this very simple
very simple just uh you need to uh revise this again and again but the
common thing let me tell you dynamic auto dynamic auto b access dynamic
dynamic will come access trunk and auto will become trunk access and auto means
becomes access because dynamic auto is fully dependent on other side if you become trunk I will become trunk If you
become access I will become excess. If you are dynamic auto I am also dynamic auto. I will work as a access port. I
will work as a access port. And if I convert this two there is
another port dynamic desirable. Okay. So what is the difference?
I go to f03
switch port mode. dynamic desirable.
Okay. So, switchboard mode dynamic desirable.
Okay. I think I have to first change this to auto.
Okay. Conflict between no negotiate and dynamic status. Okay.
Okay, we I'll discuss what is no negotiate. But uh you can see I just configured dynamic desirable this side
and if you configure desirable so what it will show you one side dynamic
desirable other side is auto. So if you configure one set dynamic desirable
which is also like so what's the difference then between auto and desirable. So dynamic auto will never
say that I want to become trunk. Okay. So dynamic auto depend on other side. So
if you other side is trunk this will become trunk. If other side is excess this will become excess. But dynamic
desirable say that I want to become a trunk port. So if you can say that I
want to become a trunk port and if other side is auto if other side is auto dynamic auto other
side because this dynamic desirable is saying I want to become trunk. So
dynamic auto will also convert this port into what trunk
because dynamic desirable say I want to become trunk. So the dynamic auto will
change his position to trunk because as I mentioned dynamic auto is fully dependent on opposite side. So what we
are learning here what is the DTP protocol used for. So that is a Cisco protocol which is just helping you. So
that if you configure one side trunk the other side will automatically become trunk. If you configure excess this will
become excess. If you configure dynamic desirable this will also become trunk. Okay. So nothing much difference in DTP.
So DTP is normally used in Cisco switches to negotiate if the interface should become access or trunk port.
Okay. So so it's a small protocol by Cisco just so that you do not configure
trunk everywhere. Okay. So if you configure one side automatically it will become other side. So mostly all Cisco
devices by default they come with the dynamic auto mode.
Mostly all Cisco devices Cisco layer 2 or layer 3 switches they set their admin
mode to dynamic auto in some situation maybe it is desirable also. So what is
the difference between auto and desirable? So nothing much auto do not
say that I want to become trunk. Okay but desirable always say I want to become trunk.
Okay. So now but when you configure trunk
trunk both side that is the best thing. So what is the best thing? How to turn
off DTP if you want to disable DTP. Okay. So you go to FA03 port
and say switchport mode trunk and give this command switch port no negotiate.
Okay. So this command will disable DTP. Okay. So what is the command? Go to the
port, configure the trunk port and just give the no negotiate command. So this
will actually become the admin and operational mode.
You can see my admin mode is also trunk. Operational mode is also trunk.
Okay. But if you configure the other side also you know that this side automatically it
will become trunk h you can see fa03 switch port so you can see I'm behaving
like trunk but my admin mode is not trunk okay so that's okay but for
security reason you can see there is a thing here what is called uh negotiation
so you can See negotiation of trunking is on and if negotiation of trunking is
on like maybe an ethical hacker maybe what we call hacker penetration tester
or something so they anybody can attack to the switch okay and say I'm the
switch because consider this is the hacker laptop so he say that I am the switch and he start sending you DTP
packets and you will become the trunk port okay so that's a security loophole
Okay. So that when the negotiation of trunking is on there are lot of attacks in the market. So you can see here DTP
Kali Linux. Okay. So I'm not teaching you cyber security just idea. There is a
tool called Yaria something in Kali Linux. So you can see you can do lot of
attacks STP, CDP, DTP, DHCP, HSRP. Okay.
So that's why we call uh if you want to get into cyber security networking is also one of the major part because just
imagine if you start learning cyber security you don't know these protocols what we are learning in CCNA CCNP so
then it might you may know the software but you will not understand the
technology that on which technology are doing attacks okay
so you can see as I mentioned And
yeah, you can see a laptop with Kali Linux SSO device. Yeah. And after that
you just install DTP. So you can see DTP packets are going on. You can check show
interface trunk. This is a trunk port and the same command. So you run this tool called ERCIA and uh
click on the DTP. You can see DTP and enable trunking. Can you see that on the Kali Linux you say DTP enable trunking
and what is happening? You start sending DTP packets. Okay. And you can see okay
the trunking. Okay. This is the trunk port.
So it will create issue. Okay. So this will you will get a lot of packets. So
how to get secure? Okay. So you just get secure by protect your access ports. So
protect your access port by giving this command what I mention
because if you want to make trunk port but you do not want to take uh the risk
so then you go to the port give the command trunk and give the no negotiate command. So this command will disable
the negotiation. Means if anybody say that I I want to send you DTP packet you
will not accept. This command will disable DTP.
Okay. So this is the command FA03 switch port switch port no negotiate.
Okay. So now this side.
So once you configure trunk this side as I mentioned this side is dynamic auto and this is still dynamic auto and if
you are in dynamic mode now whether it is dynamic auto dynamic desirable you cannot disable negotiation of trunking.
This is the problem of cyber security. This will uh create problem in company's
negotiation of trunking is on. Okay. So some people asked me sir why we give
access port switch port mode access switchport mode access will also disable this trunking command. So if you have
computer right if you connect a computer like this FA02 and all so if you check
FA02 port F0 port if I show you FA02 which is
connected to computer. So you can see this is behaving like access but this is
dynamic auto and you can see negotiation of trunking is on. So again anybody can
install Kali Linux, install Yenia you can. So this computer or attacker can do
a lot of problem in DTP. But how to protect your company network okay from
hackers if they are in your network uh on the cable and all. So just go and go
to that particular port assign them to the particular VLAN. Okay. And then also
give switchport mode access. So when you give the switchboard mode access command actually the real meaning of this
command is not this is not adding any computer into any vain command
computer this command is just to disable the negotiation of trunking. Can you see
that negotiation of trunking is off. So now if even if it is ethical hacker even
if whatever he is if he try to send DTP packet the switch will not understand
because we disable the negotiation of trunking off. Okay I disable the
negotiation of trunking off because switch port mode access command will also disable this thing. But if you want
to disable this on the trunk port, so you have to configure switch port mode trunk and switch port no negotiate.
Okay. So both the sides f03. So I'll uh
do the same thing this side also. Switch port mode trunk and switch port non negotiate. So what will happen? we will
form a trunk port on this side and we will also disable the DTP. So what will
what is the benefit here? If anybody try to capture our packets and all so nobody
will be able to get the DTP information because between these two switches I
disable the DTP but because I have given the no negotiate command on these links.
Okay. So DTP is a theory topic basically. Okay. Hardly people will ask you in interviews but this is a Cisco
protocol. So you just have to learn the theory. Okay. DTP is used for
negotiation. If one side is trunk other side will also become trunk. If one side is excess other side will also become
excess. If one side is dynamic desirable so desirable also say I want to become
trunk. So you will form trunk. Okay. So dynamic port is like you can see if one side is desirable and one side is excess
this will also become access. Why? Because dynamic
side because one side is excess and this is dynamic port. So dynamic port automatically change his mode to the
access. If this is trunk this will become become trunk port. Right? So
nothing you do not have to take stress. This is a theory topic and whenever we have theory topics only so we get
confused okay we get little trouble okay what is this man this is not a nice
topic but trust me it's very simple easy topic just a 5 minutes theory okay so if
anybody will ask you question they will just ask you okay tell me what is DTP so you can say DTP is a protocol for
uh auto negotiation or something like if you make one side trunk the other side will automatically become trunk. I don't
know why Cisco actually created this. it is not that uh useful in the companies
uh because if you configure one side trunk obviously we are not lazy we can configure other side also I don't know
some protocols are meant to be just in the syllabus so we have to learn that
but not all technologies not all protocols okay in the real world hardly
you will see network engineers are talking about DTP does companies
DTP but it's it's a what we call DTP just a
theory topic of C that's it okay but you should not miss any topic in season
because maybe god knows that who is going to take your interview so at least
you should have overview knowledge okay so we can learn again DTP is a
dynamic trunking protocol so by default DTP is enabled. The by default DTP is
enabled because it's a Cisco protocol. So it is enabled and the interface of
switches will be in which state by default we all are in dynamic auto and some of the Cisco switches are also
dynamic desirable. Okay. So this means that whenever you receive a DTP packet
that request to form a trunk your interface will become trunk. So basically what is happening? So if I
show you simulation mode and uh if I go and click on the DTP.
Okay. So you can see I just capture the DTP. Okay. And if I go and say FA03. So
right now DTP is not working because I disable by giving the no negotiate command. But if I change this port to
maybe FA04. Okay.
Okay. So let me come back to Okay. So right now you can see DTP is captured and if I just give a command
here like maybe config T interface F04 switchport mode trunk. So what do you
see here some packets and these are DTP packets. So DTP what is DTP doing. So
whenever you press the trunk command the DTP protocol will forward this detail to
other switch and this switch will know okay one side is trunk I have to convert
this port into which port? trunk port. This is how the other side automatically
become trunk port access port according to the rule. Okay because these messages
are DTP messages. Okay. So you can see the DTP message. So where the status is
going this side it's saying dynamic auto that I'm the dynamic auto and if one side you are getting trunk so trunk and
dynamic auto becomes trunk. Okay. So that's the basic theory of DTP guys.
Okay. So please look at this.
Okay. Please have a look. I'll come in 2 minutes.
Okay guys, are you able to understand?
So dynamic is flexible for all and desirable is raised for everyone.
No desirable is also flexible. So because desirable
Okay. So let's take a question. So if one side is dynamic auto,
other side is trunk what it will become?
Yes, trunk. Very good. If one side is trunk, other side is also trunk. What it
will
Okay. Okay. So, one side is dynamic desirable other side is auto.
Yes. So, dynamic dynamic auto both side then
what? Access. Dynamic. So dynamic auto dynamic auto
become excess dynamic desirable and dynamic auto become trunk
dynamic desirable trunk. Okay DD DD okay
so DD if both side DD it will become
okay and yes my speaking language
Okay. So, so remember only there are two problems. So when when you configure one
side trunk and other side is access it is limited connectivity means it will not work properly. This will not give
you any conclusion. other side also excess and trunk or trunk or excess okay it will not form anything but other than
that everything is okay so I'm just adding this uh for your reference
yes
okay so you can read here okay so trunk port uh uh and access and the most
important thing here is no negotiation Okay. So what is a no negotiation in and
how to turn off that is the command you can see no negotiate
trunk.
Okay. So trunk negotiation is a command that disable DTP. Okay. So if anybody
ask you how to disable DTP in Cisco switches. So this is the command just to say switch port trunk no negotiate and
this will disable the DTP and it will protect you from the tax. The most important command in this is uh that
what I run here the show yeah command
use what is the most important command show with interface name interface number and the switch port okay so I'm
going to highlight this command
okay so this command is uh okay to
to check the DTP information you can use below
command. Okay. So this is all about DTP and uh
so let's see if we have any questions for DTP. So ask Cisco DTP
MCQ
Okay. So what is the purpose? Uh so this is you can use chat GP and all also. So
like these are going to be maybe basic questions. So what is the purpose of dynamic trunking protocol? So it is to
it this is uh used for the auto negotiation because if one side so if we
have uh if we configure one side trunk the other side
automatically become trunk port.
Okay. So basically this protocol is made for the lazy people. So what are the two DTP modes that a
switch interface can be in? Okay. So basically Cisco devices they are mostly
in the dynamic auto and also they are sometimes in dynamic desirable.
I think this is the correct answer. So how can you you can check the DTP information for a specific interface?
The command was show interface and the interface number and then switch
port. So this is the command to check the DTP information. So which command
can be used to disable DTP negotiation on a Cisco switch that is switch port
uh trunk no negotiate something.
Uh the command was uh okay the command was command. Yeah.
Okay. So these are the commands. Go to the port. Go to
switch port mode trunk. And once we configure trunk, you can give no negotiate also. There is another command
or configure or configure
switch port mode access.
Okay. So when trunk negotiate command is used what happens to the interface operating mode? So operating mode will
not change right. So trunk negotiate command then okay
when you give trunk negotiate command okay like switch port trunk negotiate
this will become the admin mode trunk operation mode trunk
because you give negotiate trunk command when you are in the trunk port. So this is the answer.
I think these are the this DTP is enough uh uh today uh
because nothing much in DTP if even if we keep going detail in detail on DTP
you will not get much about DTP because it's a small protocol we just understand
okay that's for the auto negotiation and that's whether it is a CCNA level or CCNP DTP is till now up to this level
only okay whether Even if you do CCI so DTP
okay so DTP is normally used in Cisco switches to negotiate if the interface should become an access port or
transport this table you should remember okay uh so just have a look and that's
So I just added the DTP here.
so ether channel I have to teach after STP only because uh when I discuss about
okay so the next topic we have is guys ether Ether channel. So what is ether
channel? That is called a link bundle. Okay. If I want to bundle the links,
okay, if I want to get more bandwidth, okay, because if you have a switch but
you want to increase the capacity of these switches, because if you add a cable, one cable, okay, there is no
problem. Okay, but if you add multiple cables between switches, what will happen? There is a protocol called
spanning tree protocol. Okay, that we will learn definitely after this topic. Okay, so STP is basically whenever you
have a loop you can see. So when I connect two cable one of the port go in
the block port. So this port you cannot forward any traffic. If you add third
link the two ports will block. If you add fourth link the three ports will block.
Means only one port at a time will work. Okay. But in this situation, if you want
to forward a lot of traffic between two switches, you will be able to forward at the maximum speed of one port that is
right now 100 Mbps. Link we have four links. All links are
100 100. So instead of 400 Mbps instead of 400 Mbps I'm getting the
speed of 100 Mbps because my spanning tree protocol my STP protocol is
blocking the port. So STP will learn but this is a technology which will block
the loops. So whenever there is a loop right now what is happening? So if you check
uh that right now between two switches we have multiple links and that is
causing the loop and that's why we are blocking these ports. So STP is a loop avoidance protocol. STP STP is a loop
avoid. So whenever you have two links one of the port gets blocked. Okay, that's the nature of uh Cisco devices
because they are already by default they are running what STP protocol. So what
we are going to do in the ether channel. So like this is the physical view four links. If we bundle them together
bundle if you bundle them together they will work like one port. And if STP will
consider also this is one port so it will not block. So I'm char port all
four ports I'm going to bundle them together in one port. And if we make them one port, STP protocol will not
block. Okay? Like if I remove these three cables. So do you think one port is getting blocked? No. There is no
problem in one link. But if you have multiple links, then only the STP is blocking my port. And STP is not a bad
thing. It's a good thing for my network. It is protecting me from the loops. But
actually but the drawback is if we have multiple links if we have multiple links then we
are not able to get the full utilization of bandwidth instead of 400 Mbps I'm
getting the speed of 100 MB so that's the main problem here so how
to avoid that so ether channel topic or technology which is also called link
aggregation okay so in juniper or other companies we call this link aggregation
in Windows servers and all that's called NIC teaming okay like nick cards NIC
cards uh are getting teamed are getting grouped so that's why it's called NIC
teaming also so it has multiple names so what are the major advantages of
ether channel like we will be able to get more bandwidth okay so instead of getting uh instead of go and buy the new
hardware so we can just upgrade our normal switches by bundle the links
together if you get 400 MB okay so will you upgrade to very high-end switches no required in your
company maybe right totally if you are able to manage your current infrastructure why you will upgrade
right so with some knowledge with some technology you can upgrade with ether channel you can upgrade your links with
the ether channel technology So now there are ether channel two protocols. Okay. One is called
LACP and one is called PAGP.
Okay. So LACP is open standard guys and PAGP is Cisco protocol.
Uh
okay. Okay. So LACP so LACP is a open standard
like between if you want to bundle Cisco switch and Juniper switch how you are
going to bundle. If you want to bundle the Cisco and maybe HP switch how you
are going to bundle. So in that condition you have to use LACP. Okay. So you have to use LACP protocol and if the
you if your devices are Cisco then we can use PAGP. Okay. So let's do the PA
AGP practical first very easy. So remember very simple it's like related
to DTP but it's not DTP. So you can see if one side is desirable and if one side
is also desirable it will form ether channel side desirable command. If you configure
both side desirable it will become ether channel. Yes. If one side you configure
auto one side desirable this will also become ether channel. But if you
configure one side desirable auto it will also become but auto like in dynamic auto dynamic auto you do
not become trunk right to become excess. The same way ether channel if you configure dynamic auto dynamic auto like
auto auto this will also not become ether channel. So to become a ether channel so we must have desirable at
least one side practical let's understand with practical way okay so remember when I
configure ether channel so these are my ports and if you do not see your port numbers
uh like uh we can go and check my
show ip interface brief and you can see these These are four ports are up right 1 to four. So you can just have a guess.
Okay. 1 to 4. So configure a terminal like switch two. So interface range FA01
to 4. Okay. 1 to 4 port 1 to 4. You can see a pop up. So I say channel group.
Channel group. So you can give any number like 1 2 6 or sometimes in
switches you get 40 50 like if you have uh you want to make multiple ether
channels this is the reason we put group numbers okay so if I say channel group
number one and you give mode and then you give okay this side let's configure
you can see pagp so auto means if other side is pagp then
only I will become pagp And the desirable means enable PAGP unconditionally. Means desirable means
that please activate the PAGP protocol unconditionally.
Okay. So let's say unconditional one side because one side you have to give desirable mandatory. So a side desirable
then okay. So we have given the channel group one mode desirable. Okay. So other side
also you can give the desirable or other side also you can give auto because one
side you have already given the desirable. So on switch three
you check the ports similar way show ip interface brief all first four ports. So
config 3 interface f04 range f01 to 4
channel should be same guys. Okay. If other side is one
channel group one mode let's say this side I give channel group one auto.
Okay. So what I did here I go to the ports simply I say channel group one mode auto. That's it. Okay. So remember
you give these commands under the ports.
Okay. So under the ports.
Okay. So interface f0 1 to 4 channel group one more desirable channel group
and it is not mandatory that both side you should have the same port number.
Okay. I can add like 5 6 7 8 port number also this side. So after some time can
you see that all ports are green. How cool right? And you can see some
message that port channel because we created group number one. So it will create my port number also with one show
ip interface brief. So you can see on the top there is a port channel one.
So show IP interface brief. Okay, show ip interface brief already we have
given. So but yes you can see port channel but what is the main command of
ether channel that is show ether channel summary. Okay. And you can see that in the ether
channel summary you can see I have a group called port channel one and this su represent okay
this is a s ba s big s means layer two
and u means it is in use. So right now it's a layer 2 ether channel because we
are using layer 2 uh switches. Uh so the protocol is running right now PAGP port
uh the full form of PAGP is uh what we call that uh
port aggregation protocol yeah port aggregation protocol port aggregation means combination okay port combination
protocol port aggregation protocol okay so it's a open it's not a open standard
it's a Cisco technology okay and we have LACP protocol all if we if we want to
configure uh the open standard okay but right now I both my switches are Cisco
so I'm okay with PAGP protocol P A GP okay and if I go and check the bandwidth
show interface port channel one
okay so you can see some bandwidth is better now okay instead of 100 MB I'm
getting almost uh uh 400 500 400 500 MB
approx. Okay. So it is not showing you the correctly one but still it's okay
because it should show me because we have four ports. Now if I check show interface f01 switch port
or maybe just show interface f01. Okay. Okay. So you can see 100 MB. One
port is 100 MB and if I check the port channel it is giving me 500 but this is again
it's a bug maybe but it should show me 400 MB because how many ports are
connected in ether channel right now if I go show ether channel summary. So we
have 1 2 3 and four char port we have four ports only. So it should not give
me 500. It should give you 400. Okay, so it's a small errors. Okay, you
can ignore. But all these ports are written PPP. You can see in the brackets the P means
these ports are in the port channel. So in any case if any port gets down
suppose FA02 gets shut down. So will the
so what will happen? Do you think the ether channel will break? No. So until
unless you have one single port enabled till that time ether channel will work but one port will show you this is D. D
means down and P means it is in use. So we are waiting for that port to come up.
So when it comes up automatically automatically it will become in use.
Okay. But if you have all four ports are down then only the ether channel port
will do go down. But if three ports, two ports are down, ether channel will still
work. Ether channel will not go down until unless all the ports are down.
Okay. So that's the port channel LACP. So remember one side we have to give
desirable. Other side also we have to give desirable or auto. But auto do not
work. Auto auto not work. You can read this by P A GP protocol post
uh post aggregation protocol. Yeah, port sorry port aggregation protocol. So one
side is desirable one side desirable yes it will become ether channel. If you say
desirable auto yes it will become ether channel. But if one side is auto other side is auto then it will not become
ether channel. Okay. So if I go to this side and make the port up again
20,000 and give the no shutdown command. So when the port comes back so in the ether
channel you can see all four ports are operational again.
Sir if one interface is down any difference? No. No. No. Yes. One interface is down. Bandwidth will
decrease obviously. Can we do in giga ports? Yes, we can do in giga ports.
Yes. Big error. 100 MB extra is showing in packet tracer. Yeah, it's a error in
packet tracer. WTP is a different protocol which
may come in further classes. So dynamic desirable opposite switch port also
change or no no. So one thing is important guys in any case if you add
this port into any ven okay suppose this these are v two but this side you have
given v one then they will not become ether channel. So if you want to make ether channel they should be in the same
ven or if one side is trunk the other side also become trunk. Okay. But 99% of
the time between switches you will always configure what? Trunk port. And
if you want to make them trunk, what you will do? If you want to make this now
trunk port, how you will make this port trunk? That's the question. The question so because the combination of port is
called port channel one. So I go to direct interface port channel one and directly give switch port mode
trunk. Very simple. Instead of going to all four ports, I just go to my port
channel one and I give trunk port command and this side also interface port
channel one switch port mode trunk. That's it. And
this will become what this will become trunk or not. Let's see. Show interfaces trunk. So sometimes
in the real industry this issue I also got when I was a fresher guy. So when I
was checking a switch in one of the company and I see okay so uh one of the
team manager or something he said please check the port is down but when I check
trunk port it is showing me port channel one and I'm saying sir it is not down
and he just told me okay go and check the ether channel command and then I got to know okay yeah I remember that topic
what I learned in CNN and all in ether channel then I got to know okay this second port is down I have to change uh
the port or maybe I have to change the cable and all okay that is a separate thing but when you go to the trunk
command the trunk also show you only the port channel one
okay because you make the trunk port you make a trunk your ether channel so
bundle port trunk you have made the bundle port trunk. So you cannot see
which port is down here. You have to run the ether channel summary command to check the port is down here or not.
Okay. So what is LACP? Again LACP is like
similar way like we have done desirable autonomide active active will work. Yes, active
passive will also work. But if you configure passive passive it will not work. Okay. Like I told you auto auto
will not work. So if I change this to
if I go to those uh ports f01 to 4 and I
give channel one channel command channel
group one mode auto. So if I configure this to auto
whole my port channel you can see it is not again like it's not becoming it is
not showing me green and also if you check your ether channel summary so you
can see down inate inate inate means they are not working properly so the I
means standalone standalone means they are not uh forming ether channel okay we
have given the command this side auto. We have added them in the group but
still it is not forming ether channel because both side is auto. But once I change this again this port to channel
group one mode may be desirable. Okay. So automatically we will become
what ether channel. So remember the modes are important. So modes
DTP or Ether channel you have to remember the uh what we call modes
whatever we have learned previously in the DTP also in this PA AGP also in the
LACP because PAGP LACP are similar okay
they are not related to DTP so DTP is a different protocol as I mentioned earlier it's for the synchronization
it's for the trunk negotiation and all but P AP LACP is for the bundling of
ports. Okay. So basically we are doing
uh like bundle of the port so that we get more bandwidth more speed.
Okay. So in the real production network where we do bundling and all. So see this diagram. I always try to show you
the real scenarios. So you can see. So when you have a lot of knowledge I'll show you some practical. So you can see
between routers also or multi-layer switches we can form ether channels.
Okay we can we call them layer three ether channel. You can see layer three ether channel layer 2 ether channel
layer 2 ether channel. So in CCNP topics we learn layer three channel. So layer 3
is nothing. We just give IP address to the port that's become layer 3 channel.
Okay. So when we do not give any IP when they work like trunk so they are called layer 2 ether channel but when you give
IP address to the ether channel that's called layer three ether channel okay but you can see all the way we combine
ports and why we have multiple links because in production network you will
not you cannot rely on one cable yes
so that's why you use multiple links multiple links everywhere Okay. And that's a good thing. That's called
redundancy. So when you have multiple links for multiple devices, that's called redundancy.
Redundant connections. Okay.
Redundancy. So this means uh we have multiple links. It's a technical word redundancy. So in this scenario, do we
have redundancy? Yes. Okay. Because we have multiple links. So that's a
redundant network. And whenever you have redundancy when you have multiple links
then there is a chance that ports will automatically block and if you are
putting multiple links then why you are blocking the port. So you must use those
ports and you can only use all the ports if you combine them together because
spanning tree protocol will block the ports. So that's the main concept.
Okay. So let's uh create another switch here because I don't have HP in the packet tracer. Okay. But what we can do
here we will use the Cisco switch because in Cisco we can still configure LACP. So this switch consider this is a
new switch and we okay or other company like maybe let's call this HP switch.
Okay. And we are going to add cables. Okay. F05.
Okay. Let's add in two links only. So we are five and six right this side ports.
Okay you have to remember the ports so that you do not make mistake. So I'm just writing so that I don't skip. This
is five and six and this is one and two.
Okay. So we will configure LACP link aggregation control protocol. So LACP
stand for link aggregation control protocol. Similar like P A GP just the
modes are not called desirable auto we call them active and passive. So active active will work yes passive passive
will not work. Active passive will work. Yes. Active active, yes. Passive,
passive, no. Desirable, desirable, yes. Auto, auto, no. Auto desirable, yes.
Bus. Yeah. Yes. No. Yes. No. Correct. Okay. So, interface range FA0526.
And if I configure I have to say uh channel the same command channel group
one a one I cannot assign because one I already given here. Okay. So this side I
give channel group maybe two and mode is maybe uh let's say
active and active and desirable. So like desirable
say PGP unconditional right but active also say LACP unconditional. So it means
active is more important also in LCP. So when you give the active command this
side so other side you can configure maybe you configure uh
this uh passive or active both.
Okay. So this side we have given the channel group mode active.
Okay. So channel group two mode uh active. Okay. And other side I give the same
command but this side maybe I can give passive.
Okay. But remember passive passive will not work. one side it should be active.
So but and this side is port number one and two. So this side we configured on
five and six. Okay. So the opposite of that one and two
interface range F01 and two
channel group two mode passive and one side already active and after some
Diwali lights it will become ether channel. Yeah. So you can see we have the ether channel again and now ether
channel command will show me both. You can see the port channel one what we
created is PAGP. It's a bundle of four ports. But the other side we configure
LACP which is bundle of only two ports five and six. Okay. And you can run that
show LACP command. I think uh not show
ether channel summary. Yeah. Summary detail
port channel 2. Uh you can see port channel one. It is it's
a combination of four ports using PAGP and we do not have any security right
now on this ports. And this is the port channel 2 where we have get two ports
and this protocol is LACP. Okay. So that's the basic stuff
and this is how we bundle the ports guys. So remember so between these two
switches first we configured what that is called what? P AP.
Okay. So, P A GP.
Okay. So, just check these two and configure the same commands. Very simple. Those switch or either one side
you give desirable, other side give desirable or auto whatever. And then you can compare this with the uh which
command show ether channel summary. uh and this command will give
you some overview. Okay, this is the command and after that
you configure LACP between these two switches. Okay, because it's a open standard
protocol something.
Okay, so this is the PAGP. This is the LACP and command is similar in the LACP also.
So yeah command I just configure these two commands one side active one side passive you can configure active active
also and this table is already here. So you have to remember this table also in
the DTP you have to remember that table and that is okay.
So again these uh like this will give us advantage of more bandwidth.
So why we use um ether channel
and give
Okay. So let's see uh these four or five questions. What could be the answers?
Okay. So the first question is okay. So what is the purpose of using
ether channel guys? To combine multiple physical links into logical one. Yes.
To increase the speed of single single links. No. We want to increase the full
uh like we want to increase the speed of all links not a single single. It will
separate traffic on different vans. No. To provide redundancy against link failure. No. Okay. Okay. So it's not for
the redundancy uh or link failure. It's a main concept of ether channel is to
bundle combine the ports into one logical port.
So what protocol is used for ether channel on Cisco devices only and that
we already know that is P A GP right because the PA AGP is a Cisco
proprietary port aggregation protocol port aggregation
protocol.
Okay. So what advantages does ether channel provide in terms of bandwidth?
What are the benefits of in terms of bandwidth? It combines the bandwidth of
all links into logical link. It increases the bandwidth of all single single links. It separate the bandwidth
for different vans. No, it dynamic adjust bandwidth. No. So the right answer it combines the bandwidth of all
physical links into logical one because you combine all the bandwidth.
So what configuration consistency does ether channel provide?
So which configuration will be consistently
consistency does provide ether channel provide?
So configuration task can be performed on each individual port can be performed on logical interface configuration task.
So I think sorry it's asking me where you will configure ether channel. If you
make bundle will you configure directly in the port channel or you can configure
single single also. So yes we can perform on both individual
and logical but uh as of now it's better to say only ether channel. Okay. So uh like here
if I want to make trunk port okay so like last time I just configured port
channel one and make that port switch port more trunk okay but if I want to
make f5 and six so I say interface range f05
and six switch port mode trunk okay and if I show you show interface
trunk okay so after some Okay,
this side it's issue something.
Five and six sprung
against
Yeah, it's uh you can see I just shut down and O shut down again it is fixed. But if I try to check my trunk port. So
you can see even if you configure five and six port numbers,
if you go and scroll up, you can see five or six ports, I made them trunk and
automatically it is not reflecting in my trunk. It is still reflecting as port
channel 2. So the answer to this question I think the right answer you can check on Google and all but it can
be performed on both individual and logical interface. So the right answer I
say C rest you can check.
Okay. Which parameter must be same on all participating links in the ether channel. Okay. So
interface type should be same. Yes.
Speed and duplex speed if it is one side 100 MB. So can we connect other side 1
GB? No. So speed should match. Ven should match. Interface type if it is
fiber cable ether channel means technology should match. So the right answer that what all should match. The
right answer is D. All of the above. So they should be in the same van. They should with the same speed and duplex
setting. Okay. So like if it is 100 MB it should be 100 MB. If it is half
duplex it should be half duplex only. Okay. But nowadays we do not talk about duplex or half duplex and all but uh an
interface type. So which parameter must be same on so
these kind of questions. Okay. Main lab if you understand labs if
you understand the cabling I have already created the scenario you can see P A GP so gig port G01 to2 channel group
two mode one auto and one side is desirable so tell me in this scenario
will they become ether channel one side is desirable one side is auto
will they become ether channel yes no
Yes, mostly people are saying yes. So the right answer is yes
h no bro it's not a trunk port now I'm
talking about ether channel. So one of the students saying yes yes it
will become trunk ar it's not trunk desirable auto it will become ether channel
okay don't get confused about DTP and this pagp
is for the trunk and all but this topic is for the ether channels
okay so now now tell me in this.
So you can see we have already in the notes I have already mentioned how LACP
can create. So LACP one side is active other side is passive. Do you think we
will able to configure LACP? If one side is active, one side is passive. Do you think ether channel BA?
Yes, it will become ether channel. Very good. Yes. Okay. So how to make a L3 uh ether
channel what we call okay so that is just go to the port channel give no
switch port command this only becomes in layer three switches so do one thing try
to do this practical it's advanced level but still if you want it's a multi-layer
switches you connect a 44 55 cables okay go and configure LACP with active and
passive command and after once the port channel is ready you go to the port
channel and give the no switch port command because after this command only
you will be able to give the IP so give the IP give IP routing on another switch
go port channel one no switch port give another IP like 1.2 to IP routing and
then you will be able to ping each other on the bundle. If one cable goes down,
still ping will work. If two cables down, still ping will work. Okay.
Okay. So, let me add uh three links here are enough. Okay. So,
how to make ether channel P Ap
is also okay. So, enable config T interface range F01
23. So in PGP we say switch port uh not channel group
channel group one mode desirable. So one side desirable.
Okay. So one side we have given desirable. So other side we can give auto.
Okay. So we have given the desirable. Desirable desirable.
Okay. Okay. So this side I can give desirable also auto also because one side it should be desirable
desirable. So both the side I made ether channel. So ether channel one we made
the ether channel that's called layer 2 ether channel. and how to make layer three. So you know the port is called
now port channel one right. So if I go and
say interface port channel one and then I say channel
uh not channel I say no switch port because if you give IP address command
it will not accept on multi-layer switches if any port you want to make layer three port so you have to say no
switch port first then only you will be able to assign IP like 1.1 255 2550 zero
or shut down like this.
Okay. Okay. So you can see interface port channel one no switch port IP very
simple the similar way the other switch also similar similar
so this is your switch two interface port channel one uh no switch port IP
address 192 1681.2 to
not we have given the IP and if I try to ping uh 192 1681.1
okay repeat uh
okay so let's see if I'm able to ping yes I'm able to ping guys right so if I
remove the cables one by still my ping will not work. That's the beauty of ether channel that if you have
link down. Okay. So let me delete one cable.
Okay. So let's say if I'm able to ping. Yes, I'm still able to ping, right? And
let's delete one more. See, are you getting my point? So this
is heavily used in the industry. Okay. when you go and join ISP companies,
okay, like maybe AEL or maybe JO and all, okay, so they have uh what we call
link aggregation, okay, like Nokia, Alcatel, Semens, these companies are
also used, okay? And if you check link aggregation
in routers, if I check Nokia or Alcatel, you can see there is a lag. This is also
called lag both. This is called lag also. So these big routers also you can
combine the ports. Okay. But the concept is similar similar. Okay. You can see
configure port. After that you go Ethernet no negotiate
speed 100 duplex full. Okay. So this is just a basic setting but this is the
command I think. Configure router interface AB no port
which I see here. You can go and read about no here.
Okay. So some routers are GUI also. So you can see here it is written how
you can configure GUI. You click here you add the links.
Okay. So you can check Cisco and HP uh LACP
configuration. Okay. And the best is what we call chat GPS and all. Okay. Like uh uh show me uh
syntax how to configure LACP between
Cisco and HP switch.
Cisco command like Cisco you already know switch port mode trunk switch channel group one mode active and in HP
switch you go here trunk 111 TRK LACP VLAN one untag TRK something okay so
this is how you can use chat GPS and all uh and you can take little help from
them also so just for your learning tools are also important
Okay. So, show me syntax how to configure LC between Cisco and Juniper Juniper switch. Show me configuration
between Cisco and Juniper switch.
Show me LACP configuration between one Cisco and one
Juniper switch. Okay.
You can see Cisco switch that interface 101 connected to
description just it is connected to juniper switch port mode trunk channel group one or active okay port channel
switch port mode trunk and this juniper is a little different so like show interfaces Ethernet options so we have
to give 802.3 A so this AD basically means LACP this is the standard of LACP
And this is called aggregation port only a zero and all.
So this zero means uh unit zero. This is the by default version
that please add all one port two ports. Uh you can see add in the van member all
something. You can see this is how so I was in routing team. I know a little bit about Juniper switches but yes this is
how we configure Juniper or any other different so you can get a syntax and
whatever technologies you guys are learning at least try to add some
uh commands in your notebook and all if you get time okay like you can say Cisco
and Juniper sample
okay so this helps you to add more
things on your resume.
Okay, great. Aim. So, can we configure LACP in AWS or something? uh configure
uh it uh what we call link aggregation
in AWS and uh
show me the commands maybe it will not show
maybe Yeah. Yeah. The command.
So this is like to configure link aggregation between AWS, you can use the
AWS command line. Here are the commands. AWS EC2. EC2 is called virtual machines.
So it's like link aggregation between uh like if I say AWS EC to LACP.
CCNA juniper juniper AWS. So what I'm trying
to show you that one of the technology you guys are preparing for the network
basics. So if you understand LACP or the STP, TNET, DHCP, all these protocols
will keep helping you in further technologies also like maybe you can see
here which bundle it's related to bundling only. You can see lag one. This is the
customer data center location two. This is the data center and we are combining these links together and that's called
lag. Lag is link aggregation. I told you ether channel is also called lag
link aggregation that's called lag okay or let me write down lag okay
all connection must be dedicated 1 GB to 2 GB and all
so you can see once all connection in a lag operate in active active mode when
you create a lag from existing connection we deassociate some MAC IP
Next, create a lag. How to create? You go to the your machine,
create a lag, name, location, port speed. And this is
how you create create a L using the console.
Easy.
or let's say services which will use Twitter, Netflix
and Amazon. So you will mostly here this is one one
pattern in so it's a concept only nobody's showing you here tag will not
but uh if you check AWS official videos reside right inside your VPC the network
management and the middle attacks establish a dedicated and private
so that's a private link but yes this is how I'm trying to relate your all technologies I'm trying to show you that
all technologies these are related to each other and whatever you guys are learning so don't stop learning because
you guys are just uh maybe I always show people road maps okay so maybe you guys
are on you guys are somewhere here only right
CCA CNB but you can see after that I have to follow a big path big path then
only I can learn about the cloud cloud security, AWS, Azure, GCP, then I can
become a cloud security, DevOps, whatever I want to become. Even if I want to become a cyber security, I have
to keep learning and learning. This is going to be my road map for at least next four years, 5 years or maybe within
2 years I want to achieve this. So follow the career road maps. Don't stop
learning the courses. Okay? So many people they enroll. I trust me I have
seen lot of students they enroll in CCNA and they just discover CCNA for 2 years
3 years after four four five years they learn CCNP then after again 3 4 5 years
they come and learn firewall don't be like that average guys okay so uh at
least give time to yourself okay I want to at least complete CNS CNP firewall
AWS all means I have to complete this road map in next maximum in 2 years if
you give this target to yourself. So minimum you will be earning 20 lakhs 25
lakhs in with some experience. I'm not saying saying that as a fresher you will
get but very fast you are going to get very high salaries. Okay. But if you have to follow a road map because you do
not know where I want to go but if you keep uh hustling here between this path
you will not move further. Okay. So what I tell my student see consider CCNA is
like your schools. Okay. Maybe this is your like in India we call them 10th
primary secondary class schools. So it's like your 10th class and CCNP is like
your plus two. Okay. what we call 12th. Okay. So now after that, so now tell me
when you score 50% in CCN 10th class, so
do you think it's a low number? I know. But do uh do you think you will repeat
the 10th class again? No, you will jump to 12th class. You may get maybe 60% in
plus two, right? So maybe after that you join B Tech but in Btech you got 70%.
Okay? But at the end of the day your 50 60% does not matter. At the end of the
day your salary matters. Yes or no? Your salary matters. Okay. What you are earning, what you are doing right now in
your career. Nobody will ask your 10th plus 2 CCNP marks. So at the end of the
day you have to follow a road map. And so never stuck yourself in the dead
zone. This is what people are doing. they're stuck in only one or two uh
basic courses and they do not learn and obviously if you limit yourself only to
CCNA CCNP if you are not going with the trend trend is all about cloud cloud
security devops lot of things are going on in the market so if you follow the
trend if you follow the technologies then only you will be getting high salaries that's the only way to succeed
guys getting my point energy

