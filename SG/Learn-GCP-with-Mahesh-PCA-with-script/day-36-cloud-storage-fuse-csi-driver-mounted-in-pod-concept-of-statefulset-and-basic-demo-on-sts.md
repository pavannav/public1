The filename is "Day 36 - Cloud Storage Fuse CSI Driver mounted in Pod, Concept of Statefulset and Basic demo on STS.txt".

**Corrections made:**
- "ript" → "Transcript" 
- "GC persistent dis" → "GCE persistent disk"
- "headl" → "headless"
- "ub" → multiple instances corrected to appropriate words (like "one", "we", etc.)
- "consistely" → "consistently" (in context)
- Removed duplicate/repeated recording statements
- Various minor spelling fixes (example: "adress" → "address", "cmds" wasn't present, "Won" → "One")
