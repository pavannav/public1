# 2.1 Components of SQL

All right.So now I'm going to start with this.I'm going to show you an SQL code.And then quickly I'm going to explain for you the basic elements that you can find in each SQL code.So now we have this SQL code.And we call the whole thing an SQL statements query.And that's because it starts with a Select.And now the first thing that we see here is something green.This is the comments comment.It is just a note from you.And SQL will not execute this part of the code.And now what else we can see in our code is clauses.So our query is split into three main sections.And each section we call it clause.So here we have the Select clause, the From clause and the where they are like different sections thathas specific tasks like for example the selects.It's all about to select the columns and the from clause focus on from which table we want to get thedata, and the Where clause has the task of filtering the data.Now the next one we have the keywords.They are SQL standard reserved words that define a command or an operator.And as you can see, all of them are blue.So once you see something blue in SQL, that means it is a keyword and it has a specific operation.And you can see in this code all of them are uppercase, right?So all of them are capitalized.And this is the best practices how to write them.If you are writing an SQL code, make sure each time you have something blue make it completely capitalizeduppercase.This makes it easier to read and to and understand.So make it, as a rule through the whole course.Now moving on to the next one.This one.The lower it is special keyword and we call it function.So they are like built in functions in SQL that accept an input, then process it and has at the endan output.So this function here takes the values from the country and makes it lowercase.So those functions can take something and transform it.Now what else we can see inside our query.We see those stuff name, country, customers country, those black words.We call them identifiers.They are just names of columns or tables.So for example the country here is a column name.Customers.Here is a table name.So they are just names for something in your database.So moving on to the next one.We have here something called operator.We usually use them inside the Where clause in order to compare two values.So it's simply going to go and compare the country with the value Italy.And we call those tough operators.Now the last one you can see here the red value.We call this a static value.It is something fixed that we have written inside our query.So my friends that's all.Those are the basic components of each SQL query.And you will find those elements not only in a Select statements but as well in the other stuff likethe insert, create, delete.So they are the basic elements of any SQL code.Okay.So that's all about the basic components of the SQL code.Next we're going to deep dive into the Select statement in SQL.So let's go.