# 9.16 ISDATE

Okay.So what is is dates.So the is date is very simple.It's going to check whether a value is a date.So it's going to return one.If the string value is a valid date or zero if it is not a valid date.Okay.So let's check quickly the syntax of the dates.It's very simple.The keyword is date is the function name, and it accepts only one value.So for example you can pass a string like this and you can ask SQL is it a date.So is date and the value.And of course for this example you will get true or one.So as you can see we are passing here a string value.And we are validating whether it is good enough to be a date or as well you can go and specify a numberlike here 2025.So is this value a date.And of course SQL can accept it and say yeah, this is a year.So you will get as well a one so you can pass as well a number or integer.So you are just checking the values whether they are suitable enough to be a date.So that's all about the syntax of the dates okay.So now let's have few examples.For example let's go and select and we're going to say is date.And we will check a value.So let's say this value is a string 123.Let's go and call it date check one.Let's go and execute it.Now in the output it's going to say no it is not a date.And that's why we are getting the value zero.Which is correct because 123 is not a date.Let's pick another value.The same thing is date.And now the value is going to be the following.So 2025 August 20th.So let's call it date check two and let's go and execute it.Now in the output we will get one.That means the value that we have provided is a date.And that's why we have a one in the output.Because SQL is saying this is a date.Now let's have another example.We're going to take the whole thing.So this is a check three and remove this from here.But I would like to go and change the format.So let's say that we start with a day then month and then the year.Let's go and check.Now in the output you can see it is zero because SQL does not understand the format.So we are not following the standard format of the database and SQL, and that's why it's going to say,no, this is not a date.This is like a string value.So that means only if the value is following the status format SQL can understand.This is a date.Now let's go and check another thing.For example let's say is date.And let's have only the year.So 2025.And let's give it the name date check for let's go and execute it.Now in the output we will get one.So that means SQL considering this value as a date.So that means SQL is smart enough to understand okay we have provided a year information and the SQLcan accept it and say okay, maybe this is the 1st of January of 2025.Now let's go and do the same thing.But for the month, let's see what the SQL can accept it.So check five and we have the month of August.Let's go and check.Now SQL can say no I don't understand this value.This is zero.So that means this value is provided is not a date.So by checking those results as you can see SQL understands only the standard formats.And it allows you as well to check whether a year is a date.So this is how the estate works in SQL.And now you might ask, well, when I'm going to do this, when I'm going to check whether the valueis a date or not, let me give you this following scenario.Now imagine that we have the following date.So we have four values as a string.And now if you check the data you can see that we are following the standard format.But only one value has an issue.So we have here data quality problem.So now what we want to do we want to go and cast this string value to date.We don't want this to stay as a string value.We would like to have it in the final result as a date.So what we usually do is that we go and have like subquery on top of those values.So like this.So now what we're going to do we're going to go and say we would like to go and cast the order dateas date.We don't want it as a string.And we're going to call it order date from these values.So let me just make it like this and let's go and execute it.Now it's going to give you an error and say, well, I cannot convert everything to date because youhave maybe corrupt data.And this is of course because of this row.So SQL is not able to convert this string to a date.But of course now the example is very simple.We know that.But if you have a huge table it's going to be really hard to identify those issues.But now still I would like to go and convert those values here.I don't want to get an error.And now if there is like some values like here that is corrupt and so on, this value could be null.So how we can force SQL to convert the data type from string to date and not give us this error.And for this we can go and use the help of the function is date.Let me show you how I usually do it.So let's go and say let's check whether the order date is a date.So let's have it like this.And now before we go and execute, I'm going to make this as a comment.Because if I execute it like this, we will get an error.And let's go and get the order date in our selects.So let's go and execute it.Now as you can see in the output we have our string value.So they are not yet a date.And we have the result of our check.So as you can see the first row we are getting is zero.So SQL is saying this value is not a date, but for all other values we are getting one.So they are passing the check and they are dates.So now what we're going to do, we're going to go and build the logic where we're going to say go andcast the value from string.To date, only if the flag or the check is equal to one.So this means we can go and use the help of the case when statement.Let me show you how we can do that.So let's do it step by step.We're going to say case win.Now we need the check.So is date the order date.So if the output of this check is equal to one then you are allowed to do the casting.So let's go and get the cast as a result of this condition.And if it's not equal to one then it could stay as a null.So let's have it as a null if it didn't pass the test.So end and I'm going to call it new order date.So now let's go and execute it.Now as you can see we are not getting error from SQL.So now if you check the output for the invalid dates we are getting null.So we are not getting any SQL error.And now only if this string values are a valid date, it's allowed to be casted so that you can go andcast a string value to a date, even though that you have bad data quality.And this is a very important step in order to prepare the data before doing analysis.And it helps us as well to find data quality issues.So for example, we can go over here and say, you know what, let's go and search for all issues.So we're going to go and take the dates.So let's go and get the check.And I'm going to say let me see all string values that are invalid that are failing the test.So let me execute it.And with that we are getting this record.And now imagine we have a lot of data.So it's now it's really easy to identify those issues by just using the dates.So this is as well amazing way in order to identify data quality issues.Now of course you might say, you know what I don't want to see here null.Maybe let's get a dummy value.Well it's very easy.We can go over here and say else so and we can go and get for example, very large value something likethis that is easy to identify.So now with that, instead of getting nulls inside your data you can get such a dummy value.So now you understand the use case of the estate and why this function is amazing.Doing data cleanup.