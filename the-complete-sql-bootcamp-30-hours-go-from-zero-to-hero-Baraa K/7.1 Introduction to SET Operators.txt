# 7.1 Introduction to SET Operators

All right, my friends.So now, as we learned before, in order to combine two tables, we have two methods.If you want to combine the columns, we use the joins.And we have learned all those different types on how to combine data using join.So we have covered this section.But now if we want to combine the rows of two tables we can use the set operators.And here we have four different types.We have union union all except and intersects.So now we're going to go and deep dive into this word on how to combine the rows of tables using theset operators.And now of course in this course we're going to cover everything.So let's go.