# 2.8 DISTINCT

Okay, so what is exactly distinct?If you use it in SQL, it's going to go and remove duplicates in your data.Duplicates are like repeated values in your data, and it can make sure that each value appears onlyonce in the results.So it sounds very simple.And as well the syntax is easy.So as usual we start always with a select by.Directly after the select we use the keyword distinct, so there is nothing between them.And then the normal stuff.We specify the columns and then the from in order to get the data from table.Let's say that I would like to get a list of unique values of the country.So the first thing that SQL gonna do, of course, is to get the data from the database using the from.And now the second step is the select.So SQL can execute it.And going to select only one column the country all other columns can be excluded or removed from theresults.And now SQL can go to the third step.It's going to go and apply the Distincts on the country values.So it acts like a filter where it's going to make sure each value happens only once.So it's going to start with the first value Germany.Now it's going to look to the results.Do we have Germany.Well we don't have anything yet so that's why it's going to include it in the results.Then the next value is going to be USA.The same thing.We don't have USA in the results so it can go and include it.And this happens as well.For the UK, we don't have UK in the final results, that's why it's going to go as well.Included.Now comes Germany again.Now it's going to say wait, we have it already so it will not go and add it again in the outputs becauseit must appear only once.So we will not have Germany twice and as well for the last value, the USA, we have it already in theresults.That's why it will not appear again.And with that we have removed the duplicates or the repetition inside our data.So each value is unique.Now let's go back to SQL okay.That task is very simple.It says return unique list of all countries.So let's go and do that.It's going to be funny.So select.And now let's get the column country from our table.Customers like this.Now you can see we have a list of all countries.But the task says we need a unique list.So that means I cannot have repetitions inside it.And with that you can use the very nice distinct.So if you do it like this let's go and execute.You will see there will be no duplicates in your results.And all the values in the result can be unique.So thus we have solved the task.It's it's very simple.Now, there is one thing about the distincts that I see a lot of people using it, a lot in cases thatit's not really necessary.So for example, let's go and get the ID.Now if you go and execute it, you can see here we have a list of all IDs and there are no duplicates.But now if I go and remove that distinct and execute it, we will get the same results.Because the IDs are usually unique.So it really makes no sense to go and say distincts because as you can see, the database has to goand make sure each value happens only once.So there is like extra work for the SQL and it is usually an expensive operation.So if your data is already unique, don't go and apply Distincts only if you see repetitions and duplicates,and you don't want to see that only in this scenario go and apply that distincts.Don't go blindly for each query applying distinct just in case there is duplicates.This is usually bad practices.Okay, so that's all for Distincts.Okay my friends.So with that you have learned how to remove the duplicates using the Distincts.In the next step, we're going to talk about another keyword that you can use together with the selects.You can use top in order to limit your data.So now let's go and understand what this means.