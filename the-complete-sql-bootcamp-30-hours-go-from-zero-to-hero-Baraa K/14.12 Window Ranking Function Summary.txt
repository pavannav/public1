# 14.12 Window Ranking Function Summary

All right, friends.So now let's have a quick recap for the window ranking functions.So what are they going to do.They're going to go and assign a rank for each row within a window.And we have two types of ranking right.The first one is the integer based ranking.It's going to go and assign a number an integer for each row.And here we have four functions.Row number rank, dense rank and n tile.And the second type of ranking we have the percentage based ranking.So SQL is going to go and calculate a rank and then assign it for each row.And here we have two types of formulas or functions.So we have the oldest the cumulative distribution.And the second one we have the percent rank.And now the the next point if you are talking about the rules of the syntax.So the expression should be empty.We should not pass any argument to the functions.We must use order by in order to sort our data.So it is required.And the From clause are not allowed to use.So you cannot go and customize a frame within their window function.And as we learned, there are many use cases for the ranking functions.For example we have the top n analysis the button and analyzes in order to identify our top performersor the worst performers in our business.Another use case.Using the row number, we can identify and remove duplicates in our data so we can use it in order tofind data quality issues and as well to improve the quality.And another use case.If our table don't have a clean primary key, we can go and generate a unique IDs using the row numberin order to do as well.Paginating.One more use case.It was that data segmentations.You can use the Intel in order to segment your customers, your products, employees, and so on.And another use case.We can do data distribution analysis.As we learned.We can use the list in order to understand the data distributions of our data points compared to theoverall.And the last use case is more for data engineering.We can use the internal function in order to equalize the loading process of our etls.So as you can see, there are many use cases for the ranking functions.Okay, so that's all about how to rank your data using the window functions.And now we're going to cover the last group.We will learn about the value window functions how to access another records.So let's go.