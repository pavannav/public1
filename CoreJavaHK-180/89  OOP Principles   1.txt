ript


right good morning everyone
are you able to hear
me so in today class we'll try to understand encapsulation so in last class I given
you Clarity on object oriented programming is divided into two parts one is programming number of two
features okay up to now we have discussed object oriented
programming up to now we have discussed about object oriented programming now we
are trying to understand object oriented programming
features okay so all these concepts are comes
under object oriented program pramming we learn how to write a program to store
Real World objects perform operations on those Real World objects then now we are
understanding these Concepts so these are nothing but object oriented programming features okay let's start uh first we
completed what is object oriented programming principles difference between object based programming
language and object oriented programming language tell me what is object based programming
language a language that supports object
but a language that supports objects but it doesn't
have class doesn't have it doesn't allow us to create a class doesn't allow us to
create object doesn't have encapsulation inheritance polymorphism okay repeating
again a programming language that has implicitly object object is there but
you cannot create class youu cannot create object you cannot Implement encapsulation inherence polymorphism all
those things then we call that uh programming language as what object
based programming language is it
clear for example JavaScript JavaScript is a object based programming language
okay it has object based programming language it has object frame is a form is an object
window is an object document is an object Dom Dom is an object okay but we
cannot create object by our own we cannot create any class so JavaScript is the best example for object based
programming language Java is object oriented programming language it supports how to
create object it supports class it supports encapsulation
inheritance polymorphism everything it will support is my point understand
right so I hope you got Clarity on these points then how many object oriented programming principles are there three
what are the three encapsulation inheritance polymorphism what the need of encapsulation encapsulation doesn't
allow you to create any object if you create object it will suggest if you
create object it suggest how to how how to uh provide security to your object
data inheritance doesn't create object it will support it will suggest you how
to develop you know subtype relation how to develop relation with another object
okay parent child relation how to develop it will suggest and next if you develop polymorphism polymorphism
doesn't allow you to uh you know write program it will it will suggest you how
to provide multiple implement present ation multiple behaviors to the object one object with multiple behaviors how
to provide that facilities it will provide okay object oriented programming principles will not allow you to create
objects it will allow you it will provide suggestions how to provide security to your object data how to
provide you know dynamic binding code reusability dynamic binding to your
object okay right so using these Concepts okay class instance variables
methods Constructor block inner class using all these Concepts you are developing real world object in the
programming world but by using these Concepts you are not going to create real world object you are just adding
some more features to this object that two features only we are adding number one what you call security number two
code reusability number three Dynamic binding Dynamic binding is nothing but runtime polymorphism in order to add
these three features I need what concepts encapsulate inheritance polymorphism okay now in today class let
us try to understand encapsulation right I'll directly write program and after program I'll explain you the the
points did they cover already operators operators control flow
statements
cover it is no
Right Packages completed now packages we have
learn okay packages and working with
[Music]
job [Music]
there
okay encapsulation now observe what is encapsulation what the
what is the benefit of encapsulation the benefit of encapsulation is providing
security to object data remember it what the meaning of security to object data we will discuss I'm taking one example
bank account for easy connecting to the subject in this bank account class I'm taking one variable double
balance save save with the name bank
account from here onwards you know whatever I'm typing automatically will
understand no need of
explanation if you're not understanding automatically you are expecting some explanation then that is your problem
not my problem okay you not practiced
well class Bank main method uh tell me inside main
method this is a bank I'm creating bank account account one equal
to new bank
account okay now I want to deposit money in the bank
bank account so I am directly
accessing balance variable and depositing some 5,000 rupees I want read
balance how much value there I want to verify account one do balance what I did in these two lines I
have directly access balance variable which is available in one object this is another
programmer okay this is a u your program this is another
programmer another programmer trying trying to access your object data
allowing another programmer accessing your object data directly is a wrong
design because who is he sir he is also me only sir okay if you are allowed to
access directly some third party person unknown person also will access your data directly because already there for
example you constructed a home without any door okay gate is there I mean door is
there but there is no gate to this door you can go inside but there is no gate you are feeling that only I will go
inside this home this is my home only I will go then why are you writing why are you placing the door for protecting
click on Rise button right so you are going your family members are going but
there is a chance unknown person some thief also will come so to protect
you okay you are placing the door and four walls you you constructed right to
your home nothing but to your belongings to your belongings four walls you
constructed and provide security and in order to allow only you go and you come
unknown person's not allowing you have provided what door uh these four walls
and this door concept is nothing but encapsulation are you catching my point
so you have created a class okay you have created a class called bank account now this class bank
account must provide door nothing but why what you can say you have to close
the access of this balance variable from direct access uh right you have to stop
providing access to it and you have to provide door means what you must to provide you must to provide the access
to only authorized people the providing access to authorized people concept conceptt is nothing but Setter method
how can we stop other people to stop accessing declare this variable as what
private okay and and uh I want to allow to access this variable by other people
with proper checking tell me once you close the door here you have a small camera from there we are looking outside
who came somebody came and knocked the door then you are not directly opening first you are checking who is he is he
valid person then open the door if he's a not valid person don't open the door that means how how you are checking by
using your eyes that I in in Java is nothing but if condition if if condition
the given value verify is it a valid the given person valid okay so the coming person the accessing person and the
given value is valid then go and execute logic otherwise throw
exception everybody got clearity here so what encapsulation is telling don't
provide direct access to your object data if you provide direct access anyone
can anyone can access and uh you know anyone can access and problem will come
okay wrong values may be stored tell me now minus 5,000 I'm storing okay is is
compiler stop storing this minus 5,000 no tell me in the balance variable I must store only positive values must not
store negative values correct I must store positive negative values not
allowed only positive values can compile a stop no compiler cannot stop storing positive negative values because minus
5,000 is in the range of balance variable data type double so it's accepted let me save this program
first it accepted compile successfully look at the new bank spelling
mistake compile execute look at Theus 5,000 is coming uh now how can we stop
to store this wrong values another problem try to observe I'm creating another class called ATM so tell me once
you create bank account we can also access we can also access from another class another objects also ATM also I'm
taking ATM Bel any ATM okay I have taken bank account of J and now I'm trying to
do deposit or withdraw bu can you please find this two lines of the code again
you repeated is it not code redundancy is it not code redundancy
if you provide direct access to the variable available in your object you'll face several problems first problem
anyone can access no security second problem wrong values can be stored third
problem code redundancy because of code redundancy in the future if you want to
add any logic change okay for example I want to write if condition to stop negative number I should write here and
also I should write here this a code and see now small modification small modification in the
program affects complete project to change okay so three problems are there
anyone can access wrong values can be stored code redundancy small changes will affect a complete project
modification to overcome all these problems okay object oriented programming designers has given a
concept called encapsulation tell me is encapsulation creating object no encapsulation is just a giving
suggestion are using class you create object using variables you store the value but how to store value declare all
properties as a private Ru okay and so that if you declare all
properties as a private then other programmers cannot access compiler will throw error balance as a private access
balance has a private access balance has a private access then how to provide access to it you provide access via VIA
publicly available set get method set balance okay set balance Open Bracket
closed bracket set balance Open Bracket closed bracket uh public wide get
balance get balance Open Bracket closed bracket uh set balance you take the
balance value into your variable here 5,000 directly assigning to your original variable directly assigning to
original variable don't allow take this 5,000 into your hand okay take 5,000
into your hand that means door is there okay uh the door has a gate now here you
have the camera first you look if you don't place this gate then what anybody can come now here you need to provide
the door and one camera so outside who is standing on knock the door you can see then if you are if he is valid
person allow him if not valid don't allow so take this 5,000 into your hand first how can I take the 5,000 into my
hand take the parameter double balance okay and here what logic you write this
do balance equal to uh now balance I have written Bel if I write this there
is no difference between regular code with a with set method without set method there is no difference right are
you following that there is no difference now here directly you cannot access and then what to do you must
access via get method what you write account one set balance account one do
set balance of what are you writing minus 5,000 okay 5,000 orus 5,000 minus
I'll see later 5,000 and then get balance method you have to read by using
get balance method account one dot get balance method you have to write the code here properly return return type is
double and return return type is double and here you should write return
balance save if you declare variable as a private and provide its access via
publicly available set get method then now in all other classes what we do you
must access by using by using the methods you cannot access by
using you cannot access methods you cannot access variables directly by using their name you must access only
via methods tell me any compile time error no compile time error any runtime error no runtime error 5,000 stored and
coming 5,000 do you understand the flow I hope no need to explain set methods ,000 will give to set balance method set
balance method balance variable receive 5,000 that parameter value stored in the this do balance what the this object
account one object and then get balance get balance method control is go reading the balance method from where current
object current object what is coming 5,000 now can I pass minus 5,000 yes can
I pass minus 5,000 yes minus 5,000 receiving by double variable copy into
balance variable stored here tell me is the setter method stopping the negative number to store no by default Setter
method cannot stop negative number to store you have to write the code compile executed what value coming minus 5,000
tell me earlier if you want to write if condition to stop the minus 5,000 I should write everywhere here I should
write here I should write but now how many places I need to write only one place
here only one place we need to write here I'm
writing if condition if what I need to write here if
balance balance less than or equals to zero then
throw new illegal argument
exception language fundamental chapter
language fundamental chapter if wrong values are given through exception uh
throw new illegal argument exception don't pass negative number control one compile
successfully I have added if condition only one place this condition automatically applied to bank and also
ATM we will get exception control one control2 look at
that Java Lang dot illegal argument exception don't pass negative number tell me we
able to stop storing wrong values now so tell me what type of code you
must develop in the companies in a company when you declare a class to represent object okay you must declare
all Fields as a private all Fields as a private and provide its access only via
publicly available set method so that what is the advant Advantage we have four advantages Advantage number one
other programmer cannot directly access your object Fields so that you will get
security second problem second Advantage you can verify the values are right or
wrong if wrong you not you will not store if write you will store third you write the code one one place and the
code is reusable to all places so that you will get centralized code change and code reusability and centralized code
change uh the code modification not affected to other places no need to modify code and no need to recompile
directly you can execute this style of writing program is
called encapsulation declaring all Fields as a
private providing their access via Setter getter methods is called
encapsulation this style of writing program is called encapsulation tell me did you learn encapsulation already in
the earlier programs already we know encapsulation but we don't know that we are writing
encapsulation today you have realized that is encapsulation uh now tell me by
developing encapsulation what are you doing this variable axis you are
providing via you are providing via methods that
means you are wrapping variable access
why you are wrapping variable access bya method you are binding variable and
Method as a one unit by using class and providing access to the variable only
via publicly available set getter method okay this style of writing code is
nothing but encapsulation so what the definition of encapsulation if somebody ask a definition there are two
definitions are there Sun micro system definition and textbooks definition Sun
micro system definition is a technical definition declaring all variables as a private and providing their access via
setet methods is called encapsulation hide direct access hide
direct the process of hiding direct access uh the process of hiding object
data from direct access from other objects and declaring all its properties
as a private providing their access only publicly available Setra getter methods is called encapsulation this is the
definition of sun micro system but if you give this definition company people will feel that you don't
know what is encapsulation because you didn't use the word called wrapping up off binding up off Sun micro system
doesn't worry about wrapping or binding okay they're they're directly using the technical programming but other people
are worrying about what wrapping and binding rapping is the definition of balur Swami textbook binding up off is
the definition of complete reference textbook okay people are following these two textbooks more but not Sun micro
system documents and not harikrishna textbook so initially I used to follow follow Sun micro system documentation
and used to prepare the definitions students are coming back and telling sir your definition are not accepting in the interview oh my God how not let me
change and then now I are writing the definition combining both Sun micro system sorry combining both textbook
definitions and Sun micro system definition I am following the principle give respect take respect first you give
answer what intervie knows and you give your answer what you know so that interv feel that oh you are Plus+ you know the
knowledge more than me let may not lose you everybody got Clarity here right so
I am preparing the definition such a way first textbook definition following by our own definition right let me let me
prepare listen carefully the process of creating a class by wrapping variables
and methods as a one unit and providing access to the variables to modify data
read data V are publicly available Setra methods is called encapsulation
by by developing encapsulation I'm connecting my definition by developing
encapsulation we are hiding object data from direct access
and providing the access to this object data to read and modify via publicly
available set getter methods with proper validations if required proper
validations if required in set method I need validations I provideed in get method I no need of any validation so I
didn't write so by developing encapsulation we can achieve security to object data and we'll achieve the
advantages we can stop wrong unauthorized people to access our object data we can stop wrong Value Story we
can modify the code without affecting other parts of the project so these are all the points about encapsulation I
hope you all understood where it went and Stor where I save this question these
programs oh my God
can I hope all people got
clarity
look at this few images quickly t uh now I have bank account object with some properties account number account holder
name balance and username password now customer is trying to access bank account object for storing values and
reading values directly it is a wrong design okay right providing access directly to the customer that means you
are providing direct access to all the people in the world so what is the solution to this problem okay uh what
are the problems lose data security okay we lose data
security anyone can access object data wrong values can be stored code Rundy for every change we must modify complete
project what the solution we must hide the data how can we hide the data declare all Fields as private next if
you declare all Fields as a private other people cannot access but one also cannot access you know you Constructor
four walls without door then as a owner you also cannot go inside then that place is useless so you should provide
door with production all right so that production is nothing but encapsulation provide access just to declare variables
as a private is called Data hiding provide the access to those variables with some proper protection is called
encapsulation as part of encapsulation to this object we must provide protection by using set getter method so
we are wrapping variables and methods as a one unit by using class and providing access to those
variables to operate those variables via set and get methods for reading on storing values and then customer will
contact set method for store value customer contact get method for read value okay then now what is the how can
you develop encapsulation declare all Fields as a private and Define set G method for each property then what
customer will do customer create object invoke set meod to store value invoke get meod to read value t
l take screenshot this is encapsulation diagram in your textbook it's there but
not this much elately take take a screenshot next what is encapsulation
why encapsulation encapsulation is one of the whoop principle encapsulation is
the one of the whoop principle encapsulation is one of the whoop principles principles plural one of
multiple are there among them one the process of wrapping up of variables and methods as a one unit by using a class
and operate those and operating those variables only via methods either either
for reading or modifying values is called encapsulation by developing encapsulation we are hiding data from
direct access from other objects and providing access via publically available getter set methods with proper
validation first definition is textbook second definition is Sun micro system but I connected both by using the word
by developing encapsulation so that inter field that you you know some more points next hence through encapsulation
we will get security to object data and reusability we will get security to object data reusability centralized
modification to object centralized modification to object code are are catching my
point is it clear my point rapping and hiding
ra people are there ready to ask questions but not thinking what the meaning of wrapping up sir you are
wrapped night time you are wrapped under your body you wrapping
under one blanket now wrapping folding placing
under your body wrapped under blanket night time that is
called rapping stop asking nonsense questions please try to
think uh next problems if you don't develop
encapsulation advantages if you develop encapsulation did you understand all people are having mindset only
encapsulation provide security not only security it also provide reusability did you understand the meaning of
reusability if I do any changes inside this is seter
method that modification affecting to all the places in the project
right that is called code reusability you write in code one place it is accessible to all the places if you do
any changes in the Setra method that modification is affected to all places
that is called code reusability and centralized modification if interview ask the question what the advantage of encapsulation you have to tell all the
three points code security we can we can stop other people to access we can stop other people to
access and uh we can provide code reusability and centralized modification
next tell me advantages if you implement encapsulation the same three advantages same three points I'm telling
here don't tell only security okay now look at this is the code okay declare
variable as a private balance variable cannot be accessed from other classes it will be accessed only via set balance
get balance now in set balance I have written if condition validation code for stop storing wrong values okay and main
method create bank account object call set method call get method
that's all encapsulation over any
doubts this is the encapsulation JV architecture when I run Java C clerk.
Java bank account and clerk Java clerk first clerk class is loaded main method
main method came into Java STX area account one uh account object created with no values then I call set account
number 1 2 3 4 set account holder name HK set balance 10,000 and next now I'm
calling get methods get account number get account holder get balance so stack frames all are working with the same
object okay
next uh now the execution flow flow of execution when I run Java clerk
prompting enter amount I entered minus 500 ,000 uh I
enter - 5,000 now - 5,000 is storing an amount variable and passing that amount
variable to set balance method set balance method will it store no set balance method if condition now throwing
exception and displaying error message what is that balance should be positive number by seeing this error message now
you are entering the amount correctly 5,000 then 5,000 is going here then 5,000 is correct value stored in object
then get balance method you call get balance method reading the amount from the balance variable and return and Stor
5,000 this is the execution flow next look at here small test cases quickly
quickly I want on small test cases tell me do you have any encapsulation here no
why because the variable has direct access first first F you need to answer first uh this variable doesn't have
encapsulation I mean this class doesn't have encapsulation because we provide direct access uh tell me if you want to
provide encapsulation what to do you have to you have to you have to
provide declare a variable as a private no encapsulation now I declare variable as a private if you declare variable as
a private do you have encapsulation if you declare variable as
a private do you have encapsulation no we have only data hiding only data hiding we have we don't have
encapsulation then tell me now so by declaring variable as a private you cannot access you stopped accessing but
I want to access by customer properly then I provided set balance get balance method tell me do you have encapsulation
now if you provide set getter method do you have encapsulation now encapsulation is there but
validation there or not there by seeing validation you should
not say encapsulation encapsulation means provide access via set balance method get balance method further you
are having validation or not having validation or depending on your project requirement
are are catching my point uh tell me if you just Define set balance method get balance method without any validation
wrong values can you stop storing no okay wrong values can be stored can you stop accessing unauthorized people no
you will access so what you need to do either you know or don't know
validations habituate implementing encapsulation every time habituate
implementing encapsulation every time and in a future if you need come to know
any validation to add add in set method that is automatically applied it to clerk class other classes
now your your manager I mean your project requirement didn't tell you any
uh validations anyone can access your data given permission if you don't develop encapsulation what is the
problem now you are allowing all 100 classes to access your data directly suddenly now the after five days six
days now the client is telling Babu should not store the negative values somewhere negative values are storing
please stop storing then what to do you have to go to all 100 places and modify code that is wrong then you have to
develop encapsulation now if you develop encapsulation now you have to modify the code in all 100 places that is a wrong
design so either you need or not read of developing encapsulation practice every
time developing encapsulation that's what suggestion given by object oriented programming designers always you must
develop encapsulation either validations required or not required what the meaning of encapsulation all right
declare variable as a private provide their access via set galance method get set balance get balance method without
any if condition you provide encapsulation is there in a future if you want to stop negative numbers or you
want to write some other validation write in set method automatically that effect will be given to other
people code reusability you will get hello so encapsulation without
validation is nothing but accessing directly only but future changes will not affect other people that is only the
difference okay if you modify code in bank account class clerk should compile
again when difference between account. balance and account. set balance if you
use account. balance unauthorized people can use maybe user sending wrong values if you modify code in bank account class
uh now Clerk Al should modify and compile if you go via account. set balance method another people currently
sending wrong accessing uh wrong values are currently sending but if you modify
coding set balance method now clerk class no need to modify for reusability and centralized code change purpose
encapsulation always you are to develop now tell me do you have encapsulation
here no encapsulation then no encapsulation then I provide set X and
get X method do you have encapsulation do you have
encapsulation me there set to get me are given
encapsulation there right no encapsulation you have provided two doors you were how your house is there
your house is having three doors let's say then from from which door the thief
is coming into your home you don't know okay you provided two doors direct
access and V set meod okay so you have to close one door then
allow you only via set galance method get balance method so still we don't have no encapsulation because people can
access variable directly your set X get X is wish so what to do now declare as a private now we have only one door you
are not allowed to access why there you can you have only one door how did you
what did you do by developing encapsulation you are hiding direct
access from the properties so this is the actual main point you should tell to interview by developing encapsulation
what are you doing you are stopping direct access to the object properties
and providing access via set methods then what are the advantages security is
one advantage wrong value storing one advantage by developing encapsulation you are not providing security you are
stop accessing direct access you are stopping that is the point you must highlight by developing encapsulation we
are hiding object data from direct access and providing only via cator method then what is the advantage
security don't tell encapsulation provide security encapsulation doesn't provide security you have to write your
own if conditions that's what the point I'm trying to highlight encapsulation can only stop direct access and provide
access via set getter methods all the people accepted the statement that's why definitions I have prepared very clearly
what the definition I given in this definition I didn't give the encapsulation provide security look at there hence through
encapsulation we will get we will get by writing by writing some if conditions
okay uh right the pro by developing encapsulation what are you doing we are hiding data from direct access from
other objects and providing access via publicly available get set methods with proper validations because of this
validation we got security we got reusability we got centralized
modification okay next Point try to answer how can we develop encapsulation
you know what the advantages of encapsulation if you develop encapsulation how can I devop in simple
terms encapsulation is declare Fields as a private and provide their access via set and get methods in a is called
encapsulation so final conclusion whenever you are planning to create object you must provide the ACT access
to the object only via set method and get method that's
it the everyone
understand okay these are the final questions on encapsulation
read encapsulation entry questions what is encapsulation real time example uh
what the real time example of encapsulation ATM
machine can you access the parts of ATM machine can you access the money in the
ATM directly no that is the best example of
encapsulation can you access your CPU internal parts directly no that is encapsulation all
parts of CP U are encapsulated all parts of fan are encapsulated all parts of
switchboard are encapsulated tell me switchboard background lot of wire connection is there can you see that no
all parts of keyboard circuts and everything is encapsulated so your body all parts are
encapsulated by using your skin and [Music]
bones all parts of mobile are encapsulated can
you tell me any object which doesn't have encapsulation please find one object in
the real world which doesn't have encapsulation any
object there is no object in this world without
encapsulation
spoon tum fellow spoon handle
is spoon handle is set
spoon you are handling not the part you are handling handle there now that handle is set get method if handle is
not there how can you use it police
station police station door setg meod inside police station uh police people
are there you are trying to go to police station for what purpose to access
police people police people are encapsulated by
that police station
okay here object is not police station object is
police book bangar you are taking book for what purpose book is not book book
is what pages pages are encapsulated by book book is me pages is the
object okay now keyboard you are taking tell me
keyboard you are taking uh tell me keyboard the outer side available things
are you are touching but inside circuit you are not touching when you press a a
you are you are touching but a inside function internal that particular device
you are not touching that is en Absol tell me your body I can touch but your
body internal parts I cannot
touch understand that all objects you can take you can
touch you can access all objects you can access but internal parts you cannot
access okay
now CH wonderful Cher is a object doesn't have
any internal parts legs are
there you are not accessing chair legs you are accessing chair legs why of the
the top accessing chair is nothing but
accessing
legs food excellent uh food what you are
accessing food you are accessing but internal energy you are not
accessing okay give me any example any other
example food has energy energy are not directly
accessing
okay Road you are not accessing Road inside concrete you accessing
okay cement you accessing okay you take
anything you are not directly accessing object you are accessing object but not
its internal parts every object has internal parts you are accessing powder
but not Beauty after adding po powder to your face beauty is added okay every
object has internal parts you are going to that object to access in internal
parts but directly not accessing you are accessing outer layer every object outer
layer you are accessing to access internal parts mobile Parts you are
accessing by using front end screen mobile front end screen is the layer
shielding background the parts of the thing okay parts of parts of the mobile
so encapsulation means hide object object properties all people are feeling that
object hiding object not hiding if object is hiding how can I
access encapsulation doesn't hide object encapsulation hide object properties
look at that here example1 equal new example that is object object is not
hidden from you object property is hiding from
you tell me can you access your stomach directly cut your stomach and food place
the food in your stomach no you accessing stomach via
mouth stomach is object object property body is object
stomach is object property and mouth is C
method everyone understand that you have you have access to object but not access
to object properties remember that
point shall I move to the next Point what is encapsulation real time example all you understand
ATM is an object it has internal part cash cash you cannot take directly by
swiping card only you can take swiping card is a seter method bya set method
you are taking money how to develop encapsulation You Know sample program and execution flow
advantage of encapsulation problems if you don't Implement
encapsulation uh difference between data hiding or data
abstraction data hiding means stop accessing data abstraction is data
hiding only abstraction is the synonym for hiding data hiding data abstraction
both are same data abstraction abstraction means hiding what the difference between data
hiding and encapsulation data hiding means only declare variable as a private don't provide access is called Data
hiding encapsulation means what hide the property declare the variable as a private and provide it access via set
method is called
encapsulation okay
now
seventh question is very important what the difference between encapsulation and abstraction this is a very powerful
important important question if you develop encapsulation abstraction will come if
you develop abstraction encapsulation will come tell me what is the logic of
set method you are hiding the balance
variable a access and providing its access via set method
right what is the logic of set method is main method programmer whoever calling
method does he know no Setter method logic getter
method logic is hiding from user programmer user programmer only knows
the method declaration but he don't know what is the logic of that
method everyone got Clarity here method implementation logic hiding
is obstruction okay method
implementation the method logic hiding from user programmer is
abstraction the variable direct hiding the variable direct access is
encapsulation try to understand encapsulation abstraction both are
working on hiding only abstraction is telling us hide the implementation
details what logic you have written in the set method if condition you placed
some other logic you placed that doesn't matter for the user tell what method to
call for example you go to ATM the moment you stand in front of the
ATM you it is showing one message insert card when I insert a card how it is
reading my card details how it is verifying my PIN number how it is
connecting this card to my bank account that that background internal logic none of my business
now come on come on please connect it to the example please connect it to example
when I move move the left Arrow right arrow when I move left Arrow right arrow
how these slides are going back how the are coming front none of my business when I move to Arrow slide going to
previous when I move to right arrow slide moving to right next slide coming
previous slides are coming or not only my headache how this keyboard internal functionality is implemented none of my
business for implementing that internal functionality what are the different
different electronic devices they have used none of my business hello I need
set get me declaration nothing but I should know how to insert card or how to
swipe the card or how to press the key nothing but what method I should call only I should know this method logic is
what none of my business abstraction this logic background what are the variables it is accessing what are the
values it is accessing none of my business so values hiding variables
hiding is what encapsulation method implementation logic hiding is what abstraction so if you develop
encapsulation automatically abstraction is coming if you develop abstraction automatically encaps encapsulation is
coming so data I mean abstraction encapsulation are lovers wherever you go
both will come everybody got cleara so what is encapsulation encapsulation is one of
the whoop principles it is suggesting how to hide data from direct access
abstraction is suggesting how to hide meod logic from how to hide meod logic
that's it right for example you take ATM in ATM identify in ATM identify various
encapsulation various abstraction I cannot directly access the
cache that is called encapsulation the object properties I cannot directly
access that is encapsulation when I swipe the card how the card details are reading what is the logic is running
background to read card details my PIN number verification my bank account connecting reading the values I don't
know how it is deducting money I don't know that is called abstraction my body if I take now my my
body stomach I cannot directly access that is encapsulation uh now I can access that stomach via mouth via mouth
and why mouth and throat why mouth and throat I can access then when I give my
my food to mouth and make it as a water and give to throat how it is traveling inside my body and going into stomach
and digesting generating blood and giving that blood to my body other parts and making me energy how it is working I
don't know that is called what encapsulation or
abstraction abstraction body parts hiding from me we cannot access directly
that is called encapsulation how this body part what internally working I don't know that is called abstraction so
tomorrow not tomorrow today after class completed at 11:00 or 1:00 the moment you are eating food then immediately you
should start telling oh my stomach encapsulated now food going
abstracted abstraction encapsulation working together to give me energy wonderful okay
now tell me now 1:00 you are going and sitting in front of the plate your
mother prepared the food done giving and giving placing in your plate then ask your mother mommy how did you prepare
this Curry mommy what even mother will
tell are tum fellow none of your business
eat and tell me none of your business eat uh then tell me what your mother
applied encapsulation abstraction abstraction how she prepared not telling
you abstraction what are the different ingredients used for preparing that Curry not telling you that is
ingredients curry curry properties encapsulated Curry properties are
encapsulated the process of preparing Curry is abstracted always logic is
abstraction and the and the properties variables and values are encapsulated I hope you
understand tell me what are the different parts are in bike engine
what are the different parts in bike engine say I don't
know people are thinking seriously what are the different parts are in bike engine what do you say I don't know tum
fellow clutch plate Center I don't know and then what
encapsulation I don't know different parts of the bike engine I don't know
encapsulation when I press a start button how how the engine
started I don't know that is what encapsulation abstraction abstraction
when I press the button some logic running all the parts are working that is called
abstraction okay uh switch on switch on your fan in your room you house light
there now light fans are there now switch on
light switch on light light is started
how I don't know uh switch on fan fan started how I
don't know uh tell me when I switch on how the
power is traveling via wi and going and light and how it is consuming I don't
know that is abstraction tell me what are the different parts are used inside
to send the power from the switchboard to light wire switch background connection I don't know that is called
what a encapsulation so both encapsulation
abstraction is I don't know means hiding from me Parts hiding
encapsulation process hiding logic hiding
abstraction so now I hope you connected to both concept encapsulation and
abstraction tell me Mobile screen your mob Mobile screen is encapsulating dash
abstracting dash Mobile screen is
encapsulating different parts of the mobile camera Ram R battery process processor
everything is abstracted I mean encapsulated Mobile screen when I click on the dial button when I click on the
dial button now it is calling my friend and until my friend not connected it is
keep on ring ring ring ring come s is coming how it is connected to my friend
when I click on that button background what is the logic I don't know that is abstraction so I hope everyone connected
to the subject okay solid solid understanding go if inter ask a question
tell something about encapsulation and abstraction will you
smile come on come on will you smile hey I know okay by by seeing your smile
face inter should decide you know this also
fellow uh now tell me meaning of there are there is a there is a
terminology called tight encapsulation or strong encapsulation which word is correct
straight stronger
which encapsulation which word is correct tight or stronger what encapsulation is doing it is a shielding
shielding means bolts you you are bolts after bolts now you are trying to break
it you cannot break when you cannot break cannot break encapsulation is a
shielding and as a user we are trying to break it you cannot break means is it
tighter or stronger what you cannot want to
break tight encapsulation or strong encapsulation tight is wrong word many
people are using the word called tight encapsulation that is wrong strong encapsulation is right
word we have types of encapsulations encapsulation completed types of encapsulation wherever you go types come
in Java variable types of variables method types of methods Constructor types of Constructor block types of
blogs class types of classes encapsulation types of encapsulation we are two types of encapsulations weak
encapsulation strong encapsulation inside a class at least
one field you didn't declare as a private you have provided direct access it is a weak encapsulation do you know
strong boat weak boat small small hole is there in the
boat okay then there is a strong boat or weak boat weak boat slowly slowly water
is coming inside then complete boat will be sink in the
water okay now uh tell me what is the meaning of week encapsulation declaring all fields
in a class as a private if you declare all fields in a class as a private is
called we can if you declare at least one variable in the
class as not not private then we call it as what weak
encapsulation if we declare all fields in the class as a private then we call it as strong
encapsulation in the zoom class chatting is weak encapsulated or
strong encapsulated my screen is weekly
encapsulated or strongly encapsulated can you access my
screen can you access my screen use your mouse and
access draw something on my screen move the slide to next slide you can see but
can not access seeing is different accessing is
different it is strongly encapsulated completely strongly
encapsulated and now there is a animation animation option is there right uh now before I join the class you
can write some diagrams on the screen you can animate but after I join in the
class I stopped animation I make it as a private chart window before I join you
can chart anybody that is strong encapsulation I mean weak encapsulation after I joined I make it
as a private then a weak encapsulation or strong encapsulation strong all
properties declar as a private then is a strong encapsulation in strong encapsulation we
have another scenario I have final variable but not declared as a private is it a weekend capsulation not strong
en capsulation private
variable private
variable final variable not declared as a private still it is a strong encapsulation because encapsulation is
meant for other people to not modify my data directly or if final variable is
not allowed to modify that variable value is a common to all people to access now final variable maintain
always common value so what the point now final variable even though you don't
declare it is a consider as encapsulation only now look at this
question read this question this is the direct scjp
question this is the direct scjp question please find out correct
answer
identify which option is correct according to that question
excellent weend capsulation okay the problem what the
people have identified we can capsulation why the method which is
allowed you to access only within the same methods of current class
is allowed to access from other classes a variable or method which
should not be accessed by other people provided access then it is a weak
encapsulation now this is the final conclusion on encapsulation All Points of encapsulation explain something about
encapsulation if interview given a question explain something about encapsulation how to narrate
encapsulation is one of the W principles encapsulation will provide
suggestions to develop a class by providing security to object
data we can say the process of binding or wrapping variables and methods as one unit by using a class and operating
variables only via methods and methods is called encapsulation by developing encapsulation we can hide object data
from direct access thereby we can provide access to object only via publicly available set gter methods with
the proper validations if required because we are providing access object via methods with the validation we can
say through encapsulation we can provide security to object data in Java we can develop encapsulation by declaring
variables as a private by providing their access via set and get methods if you develop encapsulation we'll get
below advantages we can stop unauthorized people to access object data we can stop storing wrong values as
per project we can modify code without affecting other classes code reusable we have two types of
encapsulation weak encapsulation strong encapsulation if all non- Fields sorry
non-final variables and local usage methods if all non-final variables and
local usage methods are declared as a private and provide their access via public methods then we call this class
as a strongly encapsulated class otherwise we will call this class as weekly encapsulated
class so these are the nine points you have to talk to uh interviewer when
encapsulation story came uh assignments on encapsulation
okay you try to take bike example bike has five gears you must write a program
to change only the gears from one to five 0 to five if you change trying to
change six or seven or eight gear you must take it is not allowed okay assignments I'm giving try
to develop by your own develop a project to create real world object bike in the
programming World allow the allow the bike Rider to chain the gear only from 0
to six because you are creating the bike in the program using keyboard I can
assign any number now set gear change gear I can assign any number but your
program should not allow him to change what of the gear he likes you must stop changing the gear okay
now so that you have to develop try to develop that's all about encapsulation
complet so any doubts
are people comfortable with encapsulation so got a feeling
completeness did you get completeness yes encapsulation everything understood God
clarity ninth Point what is ninth
[Music]
Point what is your doubt in nth
Point ainka what is your doubt in nth
Point
yes the same point I told you now if all non-final variables non-final
means it's not declared as a final Rob I didn't say final variable nonf final
variables if all non-final variables and local usage methods are declared as a
private
okay tight is the word come to coupling tight coupling loose coupling
tight coupling loose coupling this is scjp question not my own question this is UN micro system
terminology people has wrongly understood saying tight coupling tight
encapsulation is wrong strong and weak is Right tight and loose low and high
okay people has Tau wrongly that's their problem I cannot do anything encapsulation means what
breaking winding and we are outside people we are trying to
break we cannot break because tighter because stronger
strong now I am clicking on stop share in a zoom I click on stop share then my
screen stopped sharing to you uh stop share button logic is
encapsulated is encapsulated or abstracted
abstracted I'm clicking on leave button when I click on leave button I am
disconnecting from the class how I am disconnecting from the class I don't know that is
abstractor tomorrow I will click on join class I will connect to the class how I
connected I don't know that is abstracted
okay that's all for the day once I say that's all for the day you
are leaving the class and what you will do in your life next coming 24 hours I don't no that is what encapsulated your
body you are hiding from me encapsulated you are not accessible to me and what you do I don't know that is abstracted
that's all for the day see you tomorrow tomorrow we'll discuss inheritance thank
you

