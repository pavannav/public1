ript


in editing phase developer will type the code save with Java
extension compiler I mean then next developer will compile the
program by using Java C command do class file generated and saved in the same
folder and next execution will done by using Java command developer will
execute the same do class file the bite code by using Java command then Java command uh internally perform how many
operations three operation jvm phases three phases loading phase verifying
phase and interpretation phase in a loading phase now uh the do class file
bite code the bite code will read from the do class file from the hard dis and stored inside jvm and then next inside
jvm we have a special area called method area in that method area the bite code
will go and store after that verifying pH in a verifying phase uh now it it
read the bite code from the method area verifies is the bite code valid or not if valid continue for the next phase
interpretation phase in interpretation phase the bite code will be translated to machine language will get output if
everything is correct so totally a Java program development compilation execution comes under five phases
editing compiling loading verifying interpreting loading verifying
interpreting will comes under jvm phases these are the points we completed up to last class now today we will try to
understand jvm architecture to understand jvm architecture first you
people should have Clarity on what are the different types of members we can create inside a class and
tell me jvm architecture is meant for what jvm is meant for what executing a
class right jvm is meant for executing a class what are the members available inside a
class first of all you should know what are the members available inside a class
you must know now tell me what are the members available inside a class inside a class we have 10 different members are
there yes or
noct recol it
inside a class we have 10 different members all members available in the class are divided into two types if you
take a class two types are there what the two types static members and non-static members at least did you get
a clue huh at least at least did you get
clue what are the different static members we have what the different non-static
members we have come on come on what the different static members and what the different
non-static members we have in static members we have static
variable we have five members what are the five members static variable static block static variable
static block static method main method static iner class static variable static
block static method main method static iner class uh next non-static members
how many non-static members are there five what the five opposite static variable non-static variable static
block non-static block static method non-static method main method non-static
main method huh nonstatic main method
non Str main method uh no what is that Constructor in place of main method what
is coming Constructor Constructor uh the next the next nonstatic inner class so
these are total 10 members static members non-static members totally 10 members we will create so now jvm is
responsible for executing these 10 members is it clear so jvm architecture
we are learning to know how our class is exist executed by jbm to know how our class is executing
by jvm first of all we should know what are the different members we can create inside a class inside a class we can
create 10 members those 10 members are divided into two types static members
and non-static members static members are of five types and non-static members are of five types static members are
static variable static block static method main method static and class non-static members are of five types
non-static variable non-static block non-static meth other Constructor and nonstatic in our class okay then now
tell me um when should we create static members when we should create non-static members you tell me your idea give your
idea then then I can help you jvm architecture okay so tell me when we should create static member static
member meant for providing how many copies of memory one copy memory to
store value and execute logic common to all objects yes or no static members are
meant for uh what when should we use a static members when should we use non-static members when we want to store
value and execute logic common to all objects by providing one copy memory we
must use static members or when we want to execute when
we want to provide memory and when we want to execute logic without without
object or common to all objects then we should use static members when should we use a non-static members if you want to
store value and execute logic specific to one object then we should create
non-static members already we experienced it in Bank application uh now if you if you take Bank application
in Bank application account a bank name Branch name ifsc values are common to
all objects so we must declare them as static for initializing these three
variables I need staty blog to read and display values of these three variables we need stat
methods okay uh then next tell me uh if you take what do you call non-static
Members when we should go for nonstatic members if you need to uh allocate the
memory and execute logic specific to one object now tell me account number
account holder name balance these three values are specific to every account separately should be there so we have
created them as a non-static variable in order to initialize that non-static variables we have non-static block and
Constructor in order to read and display those values we have non-static
method okay now now among if I create a class with all these 10 members okay if
I create a class with all these 10 members among these 10 members what are the members are automatically what are
the members are automatically executed by jvm
if I write a class with all these 10 different members and if I give that class to jvm will jvm execute all 10
members come on Boss come on come on Fast
reply are all these 10 members are automatically executed by jvm no and
tell me what are the members are executed by
jvm okay main method is executed automatically only only main method only main
method come on come on only main method executed automatically any other method also any other member also
executed all static members all static members are automatically executed by
jvm is a static method automatically executed by jvm h is static method automatically
executed by jvm no n static method is you have to
call right static method is not executed automatically by jvm you have to call static method
now so tell me what are the members are automatically executed static variable must have one
copy memory so it should provide memory one time so it is memory allocated by
jvm next static block static variable once memory allocated it must be initialized initializing job is what
static block so static variable static block and to start the class execution
main method so what are the members executed static variable static block and main method these three members are
automatically executed by jvm remaining seven members are not
executed by jvm automatically static method is executed only when you call
non-static members are executed only when we create object
and after object creation if you call non-static method then non-static method is executed are you catching my point
among all these members jvm executes only static variable static block main method
automatically jvm execute static method only when we invoke it okay jvm execute
non-static members only when we create object did you understand up to
now
[Music] so today we started with understanding jvm architecture to understand jvm
architecture first you should have a Clarity on what are the different members we can create inside a class why
we should know different members available inside a class because jvm uh what do you
call jvm is responsible to execute our class executing our class is nothing but
executing the different members available in the class so it is necessary to know what are the different
members available in our class inside a class we can create a 10 different members among these 10 different members
what members are automatically executed and what are not automatically executed you should know
static variable static block main method are executed when class loaded into jvm REM remaining members are executed only
when we call okay based on these members okay tell me what is the class
recommended structure with both static members and non-static members if you take class bank account what is the
order we will arrange all the 10 Members First we'll create static variables then non-static variables static block
non-static block Constructors then following by Static methods and nonstatic I methods and a common method
like display method at last we'll create static in class and non-static iner
classes everyone understand so this is how we will create but among the main
method is not there if a main method is not there can you execute that class come on if a main method is not
there can you execute that class no so I'm also introducing main method also then when you run Java bank account what
happened jvm loading the bank account cter class then static variables memory
allocated static blocks executed and initialized then main method executed in the main meod if I create object then
non-static members are executed only when we create object and when we
call this is a conclusion Point can I hope all people got
clarity
shall I enter it into jvm architecture now we got sufficient
information to launch JV architecture blog diagram and now JV architecture blog diagram now
visualize I'm going to talk you try to visualize I told you when jvm when you
run Java bank account I told you inside OS jvm is created as
a process click on Rise button jvm is created as a process by occupying some
memory from the ram okay assume that jvm is prepared
like this box okay assume that jvm is created like
this box this box is nothing but memory now l in this memory this entire class
bite code bite code is nothing but the members of the class must be loaded
after loading First Step load verifying okay interpreting uh then tell me after
loading interpretation verification completed static variable should get memory static block logic should execute
and main method logic should execute then in the main method if I create object object is created non-static
variables memory allocated and the next uh non stry block
is executed and the next Constructor is executed right if you call static method
static method executed if you call non-static method non-static method is executed so this is the activities has
to be done in the jvm now think think think now I'm going to ask some
questions carefully think and tell me right once you run Java command Java command will prepare J VM as a process
by occupying some memory from the ram in this RAM memory our class by code has to
load and stored in this memory static variable memory should allocate in this
memory object must be created and non-static variables memory should be allocated in this memory static block
non-static block Constructor static method non-static method Logics must be
executed and as part of the logic parameter memory and local variables memory must be
allocated yes or no following then my question is this jvm memory is a single
memory is it divided into blocks think think and tell me is this
jvm memory is it a single memory block or internal subblocks are there b code
should store then static variable memory should allocate and again
further objects must be allocated and again logic should execute uh it is not
a single memory background it is a blocks if you take our home our home is nothing but four walls now this four
walls internal walls also we are constructing and dividing this entire space into few rooms yes or no we want
to perform different different operations so for performing different operation different rooms we are constructing for not disturbing with
others are you catching my point right so bedroom one room kitchen room one room washroom one room okay Hall one
room so like that we are separating those rooms and then we are start living there same way jvm is also divided into
internal memory blocks how many memory blocks are there we need to identify now are you ready once jvm is created jvm is
divided into internal memory blocks what are the memory blocks are coming we need to identify tell me first first we class
bite code has to store visualize a big memory box visualize as a jvm now inside
memory box visualize now I want to store the class bite code in the jvm once the
class bite code store immediately there only static variables memory should allocate oh class loading and static
variables memory allocation okay then next I want to create object of the class and allocate memory for non-static
variables okay then next all static block nonstatic block con structor
static method non-static method Logics must execute parameters and local variables memory should execute third
activity okay so totally how many activities we are doing three activities class loading on static variables memory
allocation then next objects creation and then Logics execution totally three
activities we are doing Oho then how many memory blocks are there three memory blocks okay three activities
three memory blocks okay still not completed tell me I am calling first jvm calling main method yeah inside main
method I call M1 method inside M1 method I call M2 method inside M2 method I call
M3 method then control jvm is moving control from main method to M1 method M1
method to M2 method M2 method to M3 method control is moving then now tell
me after M3 method execution completed control is come back to where M2 method
M2 method next line will m control come to M2 method M3 method calling statement
next lines are executed in M2 method after M2 method completed control come back to where M1 method in M1 method M2
M2 method calling next statements are executed after M1 method completed control come to main method in main
method after M1 method calling statements are
executed finally main me completed program terminated now my question here
how jvm can remember from where main method is ex
from where M3 method executed how control come come from M3 method to M2
method how gvm can remember M2 method in a M2 method program should
resume from line number five onwards after M2 completed how jvm
remember that it should go back to M1 method how it remember I should should execute from middle of the logic from
line number nine onwards how it remember so to
remember these flow of execution okay to remember to track the
flow of execution to track the execution flow of the program uh we have separate
memory area to store each instruction address are you catching my point now
while methods are executing while methods are executing each method
instruction is stored in each method instruction is stored in
separate memory block so totally how many memory blocks we got four memory
blocks first memory block contain bite code following by Static variables memory next memory block objects next
memory block methods logic next memory block instruction address is stored to
track the execu of methods next do you remember somewhere
you have learn a keyword called nat there is a keyword called N2 n a v N2
totally we have 13 modifiers among the 13 modifiers we have one modifier called N2 by using Nat method we can develop
Nat logic in Java that means I'm creating class called Edition Inside
Edition class I'm creating add method wide add of open closed bracket my own
logic I am writing but we can do one thing we can declare this method as a n
to wide add of into a comma into a comma into B semicolon ends with semicolon
close then this method is called what method nat2 method what the meaning of
nat method this add method contains logic not in Java this add method
contains logic in na language means other than Java language jvm native
language what the jvm native language C language so background we are creating
one C program okay background we are creating one C program add. C here I'm
creating Logic for add me same prototype why add of in a in a comma in B Open
Bracket here I'm writing print F of A+ B
print f of percentage D percentage D percentage comma bracket
A+ B Okay C language we can write logic in
C program and compile it we can link this Java method with C
method okay we can link Java method with C method so how can you link sir by using a special API called jni Java Nat
interface by using Java na interface we can link Java methods Java method
declaration with C language logic click on raise button did you understand native methods what is a native method a
Java method that has implementation logic in CR C++ is called what NATO method NATO
method has only declaration means only prototype ends with semicolon and its implementation body available with the c
language okay and we connect the Java na method declaration with C language
method definition by using what JN very good now the question in what
situation we should go for this native API we don't go for regular mathematical
programming when from java program if you want to access any W functionality
for example if you remember I wanted to retrieve the date from the I want in my
program I want to know what is the today date I want to get today date date is not part of jvm JV date is part of time
and date is part of calendar and the calendar is part of what OS so if you want to interact with
the O for uh accessing the date and time if you want interact with the jvm
internal code for accessing some memory Access Memory access if you want then where you have W interactions where you
need pointers pointers implementation where you need pointers implementation
where you need o interaction there we need to develop that method as what Native method accepted if you want to
interact with cc++ programs which are developed in CN C++ program then you
have to develop your method as Nat method in your regular programming we don't develop Nat method don't worry
okay in our regular projects development we never develop native methods Sun
micro system has already developed required number of native methods for their purpose to interact with the O and
to interact with the jvm just we are calling the native methods in our program but you should have an awareness
about Native methods okay nice so this is the basic idea about Native methods
now I have a question think if you call this add method in your test class in your calculator class
you call this add method then tell me where this add method should execute in a third memory
block third memory block is meant for executing logic but that logic what
logic execute there there we need to execute Java Logic for
example um for example your family members can go into any room your
bedroom kitchen room washroom anywhere can go your relatives are coming are you
allow them to any room to go and sit no we are allowing the Lees to sit only in
the hall right on sofa or in chairs okay there they are sitting and sometimes we
are talking and they're leaving that's all tell me we should show some difference between Java logic methods to
C logic methods that's why in order to execute native methods we have one more
block is there separately so tell me totally jvm is divided into how many
internal memory blocks five memory blocks first memory block is meant for
storing the complete class bite code the complete class bite
code and allocating memory for static variables second memory block is meant
for allocating memory for objects and non-static variables
third memory block is meant for executing stat block instance block
Constructor block static method non-static method logic and allocating
memory to parameter and local variables third memory block is meant
for storing the instruction address to track the execution flow of the
program fifth memory block is meant for loading and executing native methods
logic so totally how many blocks are there five memory blocks are there there Ru did you understand first memory block
name is Method area second memory block name
is Heap area third memory block name is Java STX
area fourth memory block name is
programmer programmer counter register area program counter register area
fifth memory block name is Nat Stacks area Nat method Stacks area or
Nat stack area anyway two ways you can call native me stack area or simply you
can call native stack area can you tell me once again once you run Java command
jvm is prepared by occupying some memory from the ram okay and then further and then
further it is divided into five internal memory
areas these five internal memory areas are technically called runtime data
areas what we call runtime data areas okay these five runtime data areas
names are method area Heap area Java Stacks area program counter register
area native methods stack area
a small Bal Babu movie is there let me [Music]
play introduction to Java virtual machine
architecture by the director harik Krishna when I run Java
sample jvm is
created by occupying some memory from the RAM and it divided into five runtime
data areas method area Heap area Java Stacks area program counter register
area and Native methods stack area okay
now hello this is
jvm so jvm ready look at that all our
containers okay all our containers they are ready for containing something what they contain
now meod area contain bite code meod area inside meod Area what is there
complete class bite code class declaration static variable declaration non-static variable
declaration okay my dear confusion Masters non-static variable
declaration static block declaration logic instance block declaration logic
Constructor declaration logic static me uh static method
declaration logic non-static method declaration logic main meod declaration logic complete all 10 members by code is
everything stored here okay then static variable memory allocated here
non-static variable memory allocated here methods and blocks and Constructor
logic is loaded and executed here parameters and local variables memory
ated here and when methods are executing that instruction address is stored here
and tracking the execution flow and if any native method is there native method
logic is loaded and executed here this is the explanation I given can I hope
all you understand now the question according to our uh previous
diagram if you go back to our pre diagam who is responsible to load your class
into jvm currently jvm is formed okay currently jvm is a formed jvm is
prepared who is responsible to load the class into jvm class loader is responsible right uh
then tell me after jvm is prepared jvm is requesting class
loader jbm is requesting class loader class loader class loader I need this
class sample to execute can please find and load it in my
jvm everyone
understand
h now I wanted to give you small explanation so this is the jvm is ready
okay so now this jvm requesting requesting whom is
requesting class loader right requesting class
loader
okay so now the story starts a big story there need work cooperation for five
minutes class loader okay first step what happened jvm after jvm runtime data areas are
prepared Now jvm searching for the class sample inside method
area in method area the class is not
found okay so we run Java sample because of java sample what happened jvm is
prepared jvm is prepared and is divided into five
runtime data areas and looking for the class inside method area inside method
area it is searching for the class sample not available then now the jvm
requesting what jvm is requesting class loader subsystem to find for the class
then class loader subsystem getting ready okay jvm I will find for you don't worry tell me what are the different
places the class is available where where class is available it is searching for the class in the
hard disk where where it is searching where the class is available can anybody can guess and tell me class can be
available at different places in the hard disk where number one it can be a
predefined class available in c/ jdk SL
j/ libu slash j/ li/ rt.jar or some
other jar first rt.jar or some other jar files also it
can be available in jdk c/ jdk SL JRE SL
there is a folder called EXT jdk slj EXT slash and here there are
so many jar files are there maybe available class here or maybe available
in our class path okay in our application directory class path class path equal to maybe in current directory
Dory maybe some other directory or maybe some other directory or maybe some other
directory anywhere it may be available all are catching my point so a
class can be available at different places now this this rt.jar is
technically called this rt.jar file class class path is technically
called bootstrap class path
what it is called bootstrap class path why it is named as a bootstrap booting
ABA booting starting is called generally booting JV may start with the classes available in rt.jar only now that's why
it is called bootstrap class path and next this C colon jdk this folder path
is called extension class
path what it is called extension class path what the meaning of extension class
path jdk software required jdk software required and our project required common
classes are called extension classes those common extension classes are given
where in this particular EXT folder okay for example in my project in my system
10 projects are running here in all 10 projects I need some jar files commonly
instead of copying the 10 jar files in all 10 projects common 10 projects individually we can copy those 10 jar
files in the EXT folder so first jvm searching for the classes in a bootstrap
class path if not available then it will search in the extension class path from the extension class path the classes are
loaded into jvm and executed extension the name itself is telling extension the next this class
path variable folders are called these are our application right our project required these are
called application class path everyone understand so totally there are three
places are there the classes are available number one class path configured folders jdk EXT folder jdk
rt. Jr file so any of the three places the class will be available now tell me
Java course is divided into three sections core Java Advanced Java
Frameworks then tell me to teach these three subjects we have one faculty no we
have separate three faculties now the same way in order to search and load the classes we have three separate classes
we have class loader background using three separate class loaders what the three class loaders come on come on tell
me core Java core Java faculty bootstrap class path uh uh bootstrap class
loader bootstrap class loader and next next come on come on
extension class loader uhuh the next third one
application class loader so in order to search the classes
and to load the classes into jvm from bootstrap class path we have a class
called what bootstrap class loader in order to search the class and load the class from the extension class path we
have extension class loader for searching the class and loading the class into uh what you call jvm from
application class path we have application class loader below class
loader and bootstrap class loader all these are together we called class
loader subsystem all these three class loaders together is called what class
loader subsystem okay class loader subsystem these three class loaders there is a
relation between these three class loaders observe application class loader is the subclass of extension class
loader extension class loader is the subass of bootst class loader okay they
are not individual there is a relation that's why subsystem system means what collection of things working together to
perform an operation is called what system okay system is nothing but computer no computer is also called
system because different parts are connected together and working together to complete one operation that's why
that is called what system
he uh these three class loaders are called about sub class loader subsystem fine then how these three class loaders
will work very important point class loader will search for the class by following one algorithm that is called
what parent parent delegation parent delegation
hierarchy hierarchy
algorithm okay now so what the algorithm class loaders there are three class loaders are there so class loader
working with one special algorithm that algorithm name is what parent delegation
hierarchy algorithm by following this
algorithm classes
searching algorithm okay classes searching
algorithm jvm follows I mean class loader subsystem follows and delegation
hierarchy algorithm for finding the classes what the meaning of it come on first stop what happen tell me jvm Java
command prepare the jvm by occupying some memory from the RAM and divide it into five runtime data areas and then
jvm searching for the class sample class bite code in method area not available
then requesting the class loader subsystem to load the class then class loader subsystem contains three class
loaders then what happen here observe first class loader
subsystem class loader subsystem is giving the request to application class
loader to find class application class loader is responsible for finding the class in
application class path but will not okay it will not search in application class
path okay it will delegate the request to its parent it delegate the request to
its parent that's what parent delegation hierarchy algorithm it will not search
for the class in application class path it delegate the request to its parent extension class loader then extension
class loader responsible for searching for the class in extension class path okay in extension class path it is not
searching it is a delegating request delegating and sending RAB giving information RAB passing to it's higher
higher higher hierarchy above hierarchy top level people sending information to
top level people is called what delegating okay right so English not ja
don't think delegation in English is English word Saum
shumar means giving information to it's higher level offic higher officials now
who is the higher level bootstrap extension class loader delegate is request to whom now bootstrap class
loader are you catching my point now bootstrap class loader have any higher person no okay then it has to search now
bootstap class loader searching for the class in the bootstrap class path if we
available it will read and load the class inside jvm method area okay but
it's not found sample class anyway is not found then bootstap class loader will throw an exception to extension
class loader saying that class not found okay extension class load loader
will cast that exception and searching for the class in extension class path in
extension class path if the class is available in extension class path if class is available okay it will uh load
the class from extension class path to Method area if it is not available then
extension class loader will throw an exception to application class loader class not found exception then
application class loader will cast that exception and search the class in application class path okay now in
application class path if the class isn't available it will load the class into method area if not available then
application class loader finally will throw an exception what any of you remember class not found exception or or
no class def found error class not found exception or no class def found error
any one of these exceptions will come okay if class is found then what
happened the application class loader will load the class into method
area okay now this the complete story is going
background this much heavy story is there background this is the complete jvm architecture
still not completed 70% only completed 70% only completed still we
have story then try to answer now try to answer no what happened here first
step First Step jvm has given request
to tell me first step jvm has given request to class class
loader class loader given that request to application class loader application
class loader delegate the request to extension class loader extension class loader delegate the request to bootstrap
class loader bootstrap class loader searching for the class in
bootstrap class path bootstrap class if class is not found in the bootstrap class path it
throwing exception to application extension class loader
extension class loader searching for the class in extension class path extension class path if the class is not available
throwing exception to application class loader application class loader searching for the class in application
class path application class path also if the class is not available through class not found exception if available
then loading the class into jvm and storing inside
jvm okay
now totally 12 steps a class searching will contain totally 12
steps okay after class loading into jbm the memory distribution will be done and
then execution will be start tell me who is responsible to execute the class loaded into jvm uh for loading the class
executing into jvm okay by execution
engine okay execution engine is it clear my
point did you understand then execution engine contains several components what
the components first
interpreter next jit
compiler next
next hotspot
profiler hotspot
profiler next garbage
collector next security
manager all these five components together called execution
engine uh interpret is meant for helping the I mean interpretor is M main person to execute
jit compiler hotspot profiler these two are meant for helping The Interpreter to
execute first garbage collector is responsible for
destroying unreferenced objects available in Heap area unused memory to
destroy unused objects unreferenced objects from Heap area we have what garbage
collector the next security manager me for checking the bite code is valid or
not okay sorry security manager meant for checking the code you are writing is
valid or not you have permission to call or not it is veryify so it is having the link with what runtime data
areas runtime data areas okay and SEC what do you call
this um class loader subsystem also responsible for loading the class and uh
also responsible for unloading class the classes loading unloading will be taken
care by
what class loader subsystem then next in order to execute Nat methods we have
Java nat2 interface J ner ja Java na interface is there jni is
also responsible for interact
with Java runtime data areas it is responsible for
linking native
methods linking Java methods to native methods and returning native method
result to Java methods and either it is Java method or
it is native method who will execute execution engine only will execute so
jni is also have two-way communication link with two-way communication link
with what
JN okay so tell me totally how many
components are there inside jvm four components number one class loader
subsystem is one number two Java runtime data areas number two number three
execution Engine Number Four JN all these four components together we call
what uh run what do you call jvm architecture now observe here jvm
architecture blog
diagram okay this is the complete architecture jvm architecture block diagram class loader subsystem contain
three class loaders inside the box method area Heap area Java stack area program count register area native me
stack area all these five runtime data five memory areas are called runtime data areas and then to execute the
classes loaded inside this runtime data areas we have execution engine execution
engine contains five parts inside and then native method stack here na native
method interface J Java na method interface it is responsible for loading and executing na
libraries so this is the official diagram given by Sun micro system this is the elaborated diagram given by
harikrishna System okay and more elaborated
diagram here only concentration is on on runtime data areas in meod area class by
code stored static variables memory is allocated Heap area objects are created non-static variables memory allocated
Java STX area we have threads there is a concept called thread thread is nothing but one flow of execution one flow
second flow third flow fourth flow n number of flows of execution can be there logic is executed parameter local
variable memory is execute allocated and program counter register as many threats
we have those many program counter registers are created for main thread flow to track main method program
counter register for thread zero tracking thread zero program counter register for thread one tracking thread
one program counter register okay now and Native methods there are some your
favorite methods are there native methods nothing but you know executing with the O help Sleep Method join method
weight method clone method get class Method All These are native methods they are loaded and ex executed here and
these methods logic executed by jna with the help of execution engine so this is the concentration is on this diagram
concentration is on method area totally how many components are there four components now next following by this is
the complete story about pent delegation hierarchy algorithm and uh jvm background look at the step numbers step
number one first first Java command found J performed jvm loading example
class uh trying to execute example class not found requesting class loader subsystem by following parent delegation
hierarchy algorithm the class is loaded into jvm okay shall I ask some questions now ready to
answer ready to answer now tell me the class bite code
is the class bite code is storing in method area tell me is Method area is a
small memory is Method area is a small memory
inside this memory inside this memory inside this memory uh so many classes
are loading now come on inside this memory so many classes are loading a
Class B Class C Class D Class E class so many classes are loading for example if
you consider this is our classroom in this classroom so many students are coming and occupying right so many
students are coming and sitting here then tell me for each student we are providing a
now for each student we are providing separate chair right we are not providing one chair right uh now tell me
as much space required for this student to sit in the class that much space one
chair we are providing another chair another chair providing like that what is the chair in Java for storing Java do
okay for storing the class bite code for storing the class bite code Bel bite
code is nothing but set of instructions so much of information is there if you want to store so many values in the
program I need object now if you want to store student data student has so many
values to store all the values we need one object so now one object is created
here then which class object is this if you want to store student I have student
class object if you want to store employee we have employee class object similarly if you want to store a class
click on Rise button and tell me if you want to store class information
below which class object is created inside jvm inside jdk software we have a
predefined class with the name class we have a predefined class with the name
class for storing student student employee employee bank account bank account same way class class there is a
predefined class called class that class class object is created okay in the class class object bite code is stored
observe this diagram after 10th step now the class bite code is found do class
file is found then the class bite code is storing Inside jvm by creating java.
lang. class instance all you able to see now all are able to see now background
what is created java. lang. class instance is created java.lang do class
instance in that java.lang do class instance EX example class bite code is storing class is stored and it's a
static variable stored and then further memory allocated 11th step java. lang.
class instance is created and the bite code stored this is 11th step then what is the 12th step 12th step is allocating
memory for static variable 13th Step executing static Block 14 14th step
initializing static variables with the values given in the static block first the default values then the given values
then 15th step 15th step main method right main method is loaded and object
is created then background object creation is going on and then now um
further what Constructor executed variables are initialized all already following so now now the object creation
then execution is going on okay here static variable non-static variables methods execution we will see separate
diagram all these points okay at this minute don't worry too much about them we are going to work with the separate program there you get more clarity at
this minute you need to understand only one point uh now classes how it is searching and loading how it is storing
that much only okay java. cl. class instance is created to store for example
in the main method I have created sample class object let's say look at that sample class object I created all you
people can you please participate tell me if you want to create sample class object sample class must load into jvm
now right then jvm going on looking into method area sample class available no
again requesting class loader application class loader extension class loader bootstrap class loader bootstrap
class path not available extension class path not available application class path found now the from the application
class path sample class is loaded by creating one more java.lang docl class instance tell me as many classes we are
loading into jvm those many java.lang do class instances are created 100 classes
if you load 100 java.lang docl class instances are created and then complete
class bite code is stored here static variable memory allocated and then the sample class object is created here
static block Constructor of sample classes are loaded executed here so this is the basic idea about complete jvm
architecture with its components can I hope you understand jvm
architecture [Music]
now there are two questions in a parent delegation hierarchy algorithm I have two questions question number
one look at here question number one class loader giving request to
application class loader why application class loader directly not searching an application class
path why it is delegating request why it is going up going up and
then from there coming down why why this pent delegation hierarchy
algorithm come on Sir why this parent delegation hierarchy
algorithm simple point with this pent delegation
hierarchy algorithm first classes are searching in where
predefined classes right and first priority giving to our local classes are
predefined classes predefined classes do you
remember uh in identifier chapter in a data types
chapter I have explained you one point I can create our own classes with the
predefined class names I can create our own class with the name String I can
create our own class with the name system any of you
remember if I create class name system with our own
name if we create class with our own name
Bel what happened our class is loaded a predefined class
loaded there is no error we can create class loader application class loader if
you give the request directly to application class loader directly searching for
application directly searching for the class in application class
path then the class is coming from application class path predefined classes are never loaded now
do you understand my
question again repeating the question try to understand why parent delegation
hierarchy algorithm is created parent delegation hierarchy algorithm is
developed to give first priority to predefined classes to load into
jvm in Java we are allowed to create predefined classes I mean we are allowed
to create user defined classes with a predefined class name if you create a user defined class with a predefined
class names if parent delegation hierarchy algorithm is not there then the classes are loaded from our local
classes predefined classes are never loaded so to give first priority to predefined classes to load if predefined
classes are not available then to load our local classes the hierarchy algorithm is
given okay now you may get a question sir I can differentiate user defined class and predefined class with a
package name now sir if I write directly string that is
user defined class if write java. lang. string then it is a predefined
class you can also create the package also with the user defined user defined package with a predefined package name
now you can also create package name java. Lang you can create so even package name also you can
create with predefined package so your package and class both you created as
java. lang. java. lang. string then how jvm can differentiate it is a predefined
class or user defined class that's why it is given with pent delegation hierarchy algorithm all people
understand the question number one question number two if parent delegation
hierarchy algorithm priority is giving first parent delegation hierarchy algorithm Target is giving first
priority to predefined classes then directly the request can go class loader
request directly can go to bootst class loader now directly give request to bootst
class loader why is coming here to here to
here like a snake why it is going why it is not giving direct
request to bootst class loader very
simple if I catch you from you I can find your father grandfather grandfather
grandfather from there come to you if I go to your parent from there I
can go his parent details not children details
hello yes or no if I come to a class when I entered into one class from that class I can
find parent parent parent parent class cannot find the sub classes child classes cannot find if the request is
directly given to if request is directly given to bootstrap class loader if the
class is not found again request come to class loader again class loader call
extension class load Lo uh now again come to class loader class loader again
call application class loader look at there how many manual calls are
happening so that's why they're not giving direct request to bootst class loader right so class loader requesting
application class loader it will take automatic implicit calling
mechanism from child if you go to parent from parent again can come to child
for automating this calls mechanism they are giving request to application class loader can I hope you understand
[Music]
both next from java 9 version
onwards we do don't have extension class
pa okay from java 9 version onwards we don't have extension Class
part and we don't have extension class loader okay we don't have direct
extension class loader extension Class part all people know from java 9 version onward J folder is not there EXT folder
is not there there is a lot of differences are happen from java n version in Java 9 versiones I will
explain you that this whatever explanation I given this is Java 8
version up to Java 8 version this is the flow of the class loader subsystem in
Java 9 version this extension class loader extension class path mechanism got changed that change I will explain
you in Java 9 version features is it clear I'm not going to talk that
point okay remember Java 9 version have changes in
this jbm architecture class loader
subsystem can I hope all the people got Clarity on this
story so at the top level if you understand class loader what the job of class loader class loader is responsible
for finding and loading the given class by code
finding and loading Java application classes and Java API classes class loader is
responsible for finding and loading Java application classes means our project
classes also predefined class Java API classes into jvm and allowing execution
engine executing that logic with the help of underlining host comp computer operating system execution engine
interact with the host computer operating system with the help of jni and finally executing our program so the
main three components involving in our program execution class loader execution
engine and jni runtime data areas not involving in execution they're responsible for only providing the
memory okay now right and then so now this is the basic idea about jvm
architecture and jvm components so please look into material all these points are clearly documented in
material every Point clearly documented and uh in tomorrow class we'll try to
understand more details about this story okay now some more points I need to explain I'll explain in the tomorrow
class today I'm stopping session tomorrow we'll start complete programming practicals okay today I completed jvm
architecture Theory tomorrow we'll try to understand practicals anyways if I start now cannot complete it is will be
continued tomorrow also so please try to practice material in
material all points are clearly documented please try to read perfectly and come tomorrow tomorrow we'll start
practicals about this
topic Bank project second video is just main method only Now call main method
create main method and call those methods can you create main method of class also
create class bank and call those methods right right that's all for the day see
you tomorrow practice all the theory points I have explained and come tomorrow thank
you any
questions

